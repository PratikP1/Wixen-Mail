# Guards, and the break each one is supposed to notice.
#
# A guard test is written, taken red once against the defect it defends, and
# then never measured again. The code around it goes on changing. Nothing
# re-asks whether it would still go red, so a guard can stop defending anything
# and keep its name, and the suite stays green the whole time.
#
# That has happened here. One commit added a "neither copy moved" arm to the
# contacts merge; two tests that used to reach the arm the guard was about
# started reaching the new one instead. Both arms do nothing, so every count,
# every sentence and every flag those tests asserted came out the same either
# way. Two guards became one, with two names still promising two, and it was
# found by hand three commits later.
#
# Each entry below is that measurement written down so a machine can take it
# again. `scripts/guards.sh` applies `before` -> `after`, runs the whole
# library, and requires the tests that failed to be exactly the ones named in
# `red`.
#
# Exactly, in both directions. It used to run only the named tests, which could
# see a `red` list longer than the truth and never a shorter one. So a record
# could quietly fall behind: "an edit written here and then lost is still
# counted and said" named eight tests for a break that reddens seventeen,
# because a later commit gave the function two new callers and nobody measured
# it again. Nine tests were guarding something nobody had written down and
# every run said the record was right. A record shorter than the truth is the
# thing this file exists to stop, so the run now notices.
#
# Adding one: take the break by hand first and write down what really went red,
# all of it. Do not write down the tests you expected.
#
# `before` has to appear exactly once in the file. When it stops appearing, the
# run fails and says so, which is the signal that somebody moved the code a
# guard is about and the guard needs measuring again by hand. That is not a
# reason to edit this file until the break matches; it is a reason to check the
# guard still discriminates and then write down what it does now.
#
# That rule is also checked at commit time now, by
# `test_every_guard_record_still_names_one_place_in_the_tree` in
# tests/house_style.rs, because a full run costs a build and a suite run per
# record, a rate times a count that docs/development/measurements.md holds
# as a product row and this file does not, and nothing forces one. It says a
# record still points at one place. It cannot say the list of tests beside
# that record is still true; only a run says that. This comment put a full
# run at one or two hours from the day it was written, 2026-08-12, at about
# 192 records, until 2026-09-14.
#
# ── `tests_last_seen`, and what it is worth ─────────────────────────────────
#
# The other way a record falls behind: a later change adds a test that reaches
# the rule it is about, so the record now names too few and nothing fails. That
# direction never announces itself, and a run of 2026-09-01 found 21 records in
# it, one of them written days before.
#
# So each record carries the tree it was last checked against: for the files its
# red list names and for the file it breaks, how many test functions each held.
# `test_every_guard_record_says_how_many_tests_the_files_it_names_held` compares
# those numbers with the tree on every commit, in milliseconds, and when they
# stop matching it names the records and prints the command that measures
# exactly those:
#
#     scripts/guards.sh --remeasure "a name" "another name"
#
# That command measures each record and, for every one whose red list turns out
# still to be exactly right, writes its counts down again. A record that comes
# out short does not get its counts written: correct its red list by hand first,
# and the run after that records it.
#
# What the numbers do not say. They are not a claim that a record is right; they
# say what the files it names held when somebody last looked. A record that was
# already short stays short and this notices nothing. A test added to a file no
# record names can redden a record and no count of any file predicts it. And a
# count is a size rather than a set, so a test deleted and another added leaves
# the number where it was. Only the whole run answers any of those, and the whole
# run is hours.
#
# The numbers on every record below were written on 2026-09-02 by
# `--recount-everything`, which measured nothing. They are the tree as it stood
# that day, not a statement that 548 records are correct.
#
# ── What the sweep of 2026-09-15 covered, and what it did not ───────────────
#
# 797 records were swept that day.
# 238 records have arrived since and have not been through it.
#
# 798 rather than the 802 this line read until 2026-09-18: four swept
# records on `names.rs`'s `should_select_first_row` were retired by 11-03
# with the function, and the comment where they stood says why.
#
# And 797 rather than 798 since 2026-09-22: "a page's body is taken out of
# its divs before it is read" was retired by 12-02.1 after the sweep of
# 2026-09-20 and a measurement here both found its break reddening nothing,
# and the comment where it stood says why.
#
# The one sweep of the milestone, every record once, on GitHub's Windows
# runners rather than on a machine: 41 shards of about 20 records each,
# dispatched by hand from `.github/workflows/guards.yml` at `main` as it stood
# at `df3437a1`, which held 803 records. Run 34965790937, first shard started
# 11:56:37Z and last finished 16:04:09Z, 4 h 7 min of wall clock; the median
# record cost 260 s there, rebuild 144 s and run 111 s, against 92 s on the
# machine the rate row on docs/development/measurements.md was taken on. The
# log is `.planning/phases/08-every-number-the-project-quotes/sweep.log`, 803
# blocks, no record contended and none unmeasurable. It reads 802 above
# rather than 803 because one of the 803, "the other setting is named only
# while the change is somebody's work", was retired the same day: its break
# reddened nothing, on the runners and again here, and a comment where it
# stood says why. The 3 arrived on 2026-09-15 with the theme_reach fix,
# each measured by hand when it was written.
#
# 772 of the 803 agreed with what they said. 31 did not, every one of them
# measured again on this machine before anything was edited, and 29 were
# short here too; the other 2, "an hour with no zone means an hour here" and
# "the walk into Windows own chain structures really happens", are right on
# this machine and blind on a runner, whose clock is UTC and which has no
# certificate chain to walk, and they are left as they are with that said on
# each. Of the 29: 24 named too few, because a test written after the record
# reaches the same rule, 21 of them in a file the record had never named,
# which is the limit the count check cannot see, and 3 in a file the record
# names but written before the 2026-09-02 recount stamped every record
# without measuring; 2 named a test that no longer reaches the break, one
# since 2026-09-05 when ce3e89ba rewrote its scenario and one that fails only
# in company and never guarded what it was listed under; 1 had its break on
# the title's line while its tests were about the body, and was given the
# body's line; 1 named a rule no marking now holds and was renamed to the one
# fact its moved break really guards, the rule going to the ledger; and 1
# reddened nothing and was retired. Each correction is dated on its record,
# every corrected record was measured again through `--remeasure` after the
# edit, and 08-07's summary lists them by cause.
#
# ── What the sweep of 2026-08-12 covered, and what it did not ───────────────
#
# 192 records were swept on 2026-08-12, and by 2026-09-15, when the sweep
# above ran over all of them, 614 had arrived since that had not been.
#
# Six went on 2026-08-26 with the code they were about: five in
# application/notes.rs, whose module doc already said nothing in the running
# program reached it, and one on the duplicate-address check in
# data::account::AccountManager. Removed rather than re-measured, because
# there is nothing left to measure by hand when the code a record names has
# gone; the test each one required to redden went with it.
#
# Those two numbers add up to the records in this file, and
# `test_the_sweep_written_at_the_top_of_the_guard_records_covers_every_record_in_it`
# in tests/house_style.rs says so on every commit. Add a record and the second
# number goes up in the same edit. That is the whole reason it is written as
# two numbers: this section used to say "192 records, all 192 checked" under a
# heading that gave no sign of being a photograph, and by the time anybody read
# it again the file held 210. Eighteen records had never been near the sweep
# the heading described, and nothing said so.
#
# Everything below this line is about the file as it stood that day. It is not
# a description of the file now.
#
# All 192 were checked for pointing at exactly one place: none was ambiguous
# and none had moved. Every test named in a `red` list resolved to a test that
# was in the tree.
#
# That one property is the only thing here that cannot go stale, because it is
# checked on every commit for every record by
# `test_every_guard_record_still_names_one_place_in_the_tree`, as the paragraph
# above the heading says. Everything else in this section needs a sweep to be
# true again.
#
# 24 of the 192 were defended only by tests that read the tree as text. Every
# one of those was read and judged against one question: would it go red if the
# behaviour went while the source text stayed? Three could not. All three had
# one shape, a refusal that is said and then not acted on:
#
#   - the two arms that refuse a delete on an account with no recognised trash,
#     which were changed to say the refusal and carry on deleting
#   - the refusal about a day of a series that cannot be kept, which was changed
#     to say no and take the day off anyway
#   - the calendar window's own refusal, which was changed to say no and queue
#     the change in the same breath
#
# All three were guarded by the end of it, each by a record whose break is that
# narrower edit, and each was measured against the old guard first and seen to
# stay green. The other 21 read wiring facts with no runtime path in a test, and
# each of their tests says in its own words what stays green while the behaviour
# goes.
#
# 18 of the 192 were measured with the runner as part of that sweep. The rest
# were not: a full run is one build and one suite each and takes hours, and it
# needs a quiet machine.
#
# ── What changed on 2026-08-12, after that sweep ────────────────────────────
#
# The first of the three above is no longer a source read. Deciding whether to
# send a delete and sending it were moved into one function, so the three
# records about the two refusals are now defended by tests that hold a server
# and ask what it was told. All three were measured again with the runner and
# each reddens two such tests. That leaves two of the three, the two about the
# calendar, still standing on a reading of a file, and the records for both now
# say in their own words what stays green while the behaviour goes.
#
# The count of records defended only by a source read is therefore 21 rather
# than 24 as against the file of that day, and one more record has since been
# added for the order the folder tree is read in, which is measured against a
# database rather than read.
#
# ── Every sort in the program, asked whether anything guards it ─────────────
#
# Taken on 2026-08-13 by making each sort leave its collection in the order it
# arrived in, one at a time, and running the library. A stable sort told every
# pair is equal does exactly that, which is also the defect being hunted: a
# test whose fixture is already in the order it expects back cannot tell that
# apart from a sort that ran.
#
# 25 sorts in code that is not a test. 13 have something that goes red when
# they stop sorting. 12 have nothing.
#
# The twelve nothing guards, so the next person does not have to find them
# again: the agenda's own ordering in the calendar; the list of messages the
# server no longer has; the note list; the days a repeat falls on; the task
# list; the three that put the modules of the main window in their settled
# order; two of the seven ways of sorting the mail list; the identifiers a
# folder search hands back; and the spelling suggestions.
#
# Two of those twelve were closed the same day, which is why the count of
# sorts and the count without a guard do not move together: the seven ways of
# sorting the mail list now have two records of their own, further down. The
# folder tree was closed the day before. The other ten are written down here
# and not fixed, and each is a place where a fixture in the order it expects
# back would prove nothing.
#
# One thing this run got wrong first and is worth not repeating. It read
# cargo's "error: test failed" as a build that never compiled, so every sort
# that really was guarded came back as unmeasurable, which is the opposite
# answer. Ask whether the run printed a test result at all.

# ── The two questions the contacts sync must not confuse ────────────────────
#
# What to keep is asked of the whole stored contact; what to send is asked of
# one address book. Asking the narrow question where the wide one belongs
# writes an address book's copy over an edit another address book has never
# been given.

[[guard]]
# Re-measured on 2026-09-05, because plan 03-09 renamed a test this record
# names and a rename can change what a break reddens. It had.
#
# `test_a_held_contact_is_not_resolved_by_the_next_sync`, which was
# `test_an_edit_an_address_book_replaced_is_said_once_and_not_on_every_later_sync`,
# stayed green with this break applied and is taken out of the list. Its subject
# moved with its name: it used to be about a loss being said once and is now
# about a second sync leaving a held contact alone, and a held contact is
# skipped before this question is asked at all.
#
# The other four still redden, so the guard still discriminates. Four rather
# than five, measured rather than assumed.
name = "the Google merge asks what the whole contact is owed"
file = "src/application/contacts_sync.rs"
before = """                        the_copy_here_holds_work_nobody_has_sent(local),
                        arrived_at.as_deref(),"""
after = """                        this_address_book_is_still_owed_the_change(local, &AddressBook::Google),
                        arrived_at.as_deref(),"""
red = [
    "application::contacts_sync::tests::test_a_change_only_outlook_still_needs_is_not_written_over_quietly_by_google",
    "application::contacts_sync::tests::test_a_change_only_outlook_still_needs_survives_a_google_read_that_moved_nothing",
    "application::contacts_sync::tests::test_a_contact_typed_here_that_google_already_holds_is_not_lost_without_a_word",
    "application::contacts_sync::tests::test_the_other_address_book_setting_is_named_only_while_the_change_is_somebodys_work",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "the Microsoft merge asks what the whole contact is owed"
file = "src/application/contacts_sync.rs"
before = """                        the_copy_here_holds_work_nobody_has_sent(local),
                        arrived_at,"""
after = """                        this_address_book_is_still_owed_the_change(local, &AddressBook::Microsoft),
                        arrived_at,"""
red = [
    "application::contacts_sync::tests::test_a_change_only_google_still_needs_is_not_written_over_quietly_by_outlook",
    "application::contacts_sync::tests::test_a_change_only_google_still_needs_survives_an_outlook_read_that_moved_nothing",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A change built on a copy the address book has moved past ────────────────
#
# The one refusal with a remedy: ask what the address book holds now, put the
# change on that marker, and send it again. Each provider words the refusal its
# own way, so each half has to be read, and neither half has a second reader.

[[guard]]
name = "Google turning a change down for an old marker is read as one"
file = "src/application/contacts_sync.rs"
before = """    matches!(status, 400 | 409) && (said.contains("failed_precondition") || said.contains("etag"))"""
after = """    false"""
red = [
    "application::contacts_sync::tests::test_a_change_google_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_change_that_cannot_be_sent_again_is_kept_rather_than_replaced_by_the_read",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_the_two_answers_an_address_book_gives_to_an_old_marker_are_both_read",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "Outlook turning a change down for an old marker is read as one"
file = "src/application/contacts_sync.rs"
before = """    if *status == THE_MARKER_WAS_NOT_THE_CURRENT_ONE {"""
after = """    if false {"""
red = [
    "application::contacts_sync::tests::test_a_change_outlook_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_the_two_answers_an_address_book_gives_to_an_old_marker_are_both_read",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A change that never reached the address book at all ─────────────────────
#
# A push that fails at the network carries no answer from the address book at
# all, so it is not a refusal that could repeat for ever the way a content
# refusal can. Read the same as a change a setting held back: kept for the
# next sync rather than lost to a concurrent move the read in this one finds.

[[guard]]
name = "a push that failed at the network is read as one"
file = "src/application/contacts_sync.rs"
before = """fn the_push_failed_at_the_network(error: &Error) -> bool {
    matches!(error, Error::Network(_))
}"""
after = """fn the_push_failed_at_the_network(error: &Error) -> bool {
    let _ = error;
    false
}"""
red = [
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_google_is_kept_not_lost_to_a_concurrent_move",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_outlook_is_kept_not_lost_to_a_concurrent_move",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# The rule is written out once per provider, and this project has shipped a
# bug before where only one of the two copies was covered and nothing here
# reddened. One guard per call site, the same shape as the pair that already
# covers the marker-conflict resend, so a fix landing in only one push
# function cannot pass silently.
name = "a network failure is kept for Google's push, not only Outlook's"
file = "src/application/contacts_sync.rs"
before = """        // now holds.
        if sent.as_ref().is_err_and(the_push_failed_at_the_network)
            && the_copy_here_was_written_here(&contact)
        {
            still_built_on_an_old_copy.note(&contact.id);
        }"""
after = """        // now holds.
        if false {
            still_built_on_an_old_copy.note(&contact.id);
        }"""
red = [
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_google_is_kept_not_lost_to_a_concurrent_move",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# The Outlook half of the pair above. Fixing only the Google push would leave
# this call site exactly as broken as before, with nothing here to notice.
name = "a network failure is kept for Outlook's push, not only Google's"
file = "src/application/contacts_sync.rs"
before = """        // it at all.
        if sent.as_ref().is_err_and(the_push_failed_at_the_network)
            && the_copy_here_was_written_here(&contact)
        {
            still_built_on_an_old_copy.note(&contact.id);
        }"""
after = """        // it at all.
        if false {
            still_built_on_an_old_copy.note(&contact.id);
        }"""
red = [
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_outlook_is_kept_not_lost_to_a_concurrent_move",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# Measured again after a network failure was folded into the same
# accumulator: a push that never reaches the address book keeps the edit the
# same way a push refused for a stale marker does, so a break here now reddens
# both new tests as well as the one that was here first.
name = "a change that could not be sent again is not replaced by the read"
file = "src/application/contacts_sync.rs"
before = """            || still_built_on_an_old_copy.holds(&the_copy_here.id))"""
after = """            || false)"""
red = [
    "application::contacts_sync::tests::test_a_change_that_cannot_be_sent_again_is_kept_rather_than_replaced_by_the_read",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_google_is_kept_not_lost_to_a_concurrent_move",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_outlook_is_kept_not_lost_to_a_concurrent_move",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a change sent over a newer copy is said and not only counted"
file = "src/application/contacts_sync.rs"
before = """    if !result.sent_over_a_newer_copy.is_empty() {"""
after = """    if false {"""
red = [
    "application::contacts_sync::tests::test_a_change_sent_over_a_newer_copy_is_said_and_not_only_counted",
    "application::contacts_sync::tests::test_a_change_google_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── One address book cannot speak for the other ─────────────────────────────
#
# An address book saying it has deleted somebody speaks for itself and for
# nothing else. Read as speaking for the whole row, it took the other address
# book's name for her, and the change that book was still owed, away with her.

[[guard]]
name = "a deletion by one address book leaves a person the other still has"
file = "src/application/contacts_sync.rs"
before = """    if another_address_book_still_has_them(local, address_book) {"""
after = """    if false {"""
red = [
    "application::contacts_sync::tests::test_a_change_waiting_only_for_the_address_book_that_deleted_them_stops_waiting",
    "application::contacts_sync::tests::test_a_google_deletion_leaves_the_change_outlook_is_still_owed_where_it_is",
    "application::contacts_sync::tests::test_a_person_one_address_book_deleted_and_the_other_still_holds_is_one_person",
    "application::contacts_sync::tests::test_an_outlook_deletion_leaves_the_change_google_is_still_owed_where_it_is",
    "application::contacts_sync::tests::test_work_waiting_for_nobody_in_particular_survives_one_address_book_letting_go",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── The deletion warning, read three wrong ways ─────────────────────────────
#
# Where nobody else holds the person the row does go, and it takes every
# address book's waiting change with it, so the sentence is decided by work
# owed anywhere in the row. Each of the three narrower readings has to be
# caught.
#
# Two of these named a test each that used to reach the deletion with a contact
# both address books knew. Those contacts are no longer deleted at all, so
# those two tests no longer discriminate anything here and were taken out of
# the lists rather than left to look like cover. What is left was measured
# going red.

[[guard]]
name = "a deletion asks Outlook as well as Google what is owed"
file = "src/application/contacts_sync.rs"
before = """    if the_copy_here_holds_work_nobody_has_sent(local) {"""
after = """    if this_address_book_is_still_owed_the_change(local, &AddressBook::Google) {"""
red = [
    "application::contacts_sync::tests::test_a_contact_outlook_removed_takes_a_waiting_change_with_it_and_says_so",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a deletion asks Google as well as Outlook what is owed"
file = "src/application/contacts_sync.rs"
before = """    if the_copy_here_holds_work_nobody_has_sent(local) {"""
after = """    if this_address_book_is_still_owed_the_change(local, &AddressBook::Microsoft) {"""
red = [
    "application::contacts_sync::tests::test_a_contact_google_deleted_takes_a_waiting_change_with_it_and_says_so",
    "application::contacts_sync::tests::test_a_deletion_says_what_went_with_it_whatever_the_contact_says_of_itself",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a deletion asks the address books, not the contact's own flag"
file = "src/application/contacts_sync.rs"
before = """    if the_copy_here_holds_work_nobody_has_sent(local) {"""
after = """    if local.pending {"""
red = [
    "application::contacts_sync::tests::test_a_deletion_says_what_went_with_it_whatever_the_contact_says_of_itself",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Clauses of a summary that stop being said ───────────────────────────────
#
# Every one of these leaves its count right and stops the person hearing it. A
# test that asserts the count and not the words does not notice.

[[guard]]
name = "a task kept on this computer is said"
file = "src/application/tasks_sync.rs"
before = """        if self.local_only > 0 {"""
after = """        if false {"""
red = [
    "application::tasks_sync::tests::test_a_task_in_a_list_this_computer_made_is_counted_rather_than_reported_as_a_failure",
    "application::tasks_sync::tests::test_what_stays_here_is_said_without_being_called_a_problem",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "what went up to the provider is said"
file = "src/application/tasks_sync.rs"
before = """        if self.sent > 0 {"""
after = """        if false {"""
red = [
    "application::tasks_sync::tests::test_a_deletion_the_provider_accepted_is_counted_once_and_stops_being_owed",
    "application::tasks_sync::tests::test_a_local_change_the_provider_took_is_counted_and_stops_waiting",
    "application::tasks_sync::tests::test_every_clause_at_once_is_still_read_as_sentences",
    "application::tasks_sync::tests::test_the_summary_counts_what_went_up",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "the tasks nobody touched are said"
file = "src/application/tasks_sync.rs"
before = """        if self.unchanged > 0 {"""
after = """        if false {"""
red = [
    "application::tasks_sync::tests::test_a_task_nobody_touched_is_counted_once_and_left_in_the_database_as_it_was",
    "application::tasks_sync::tests::test_the_summary_says_how_many_were_left_alone",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "nothing removed is not read out as a removal"
file = "src/application/tasks_sync.rs"
before = """        if self.deleted > 0 {"""
after = """        if true {"""
red = [
    "application::tasks_sync::tests::test_a_tombstone_for_a_task_this_computer_never_had_is_not_counted_as_a_removal",
    "application::tasks_sync::tests::test_every_clause_at_once_is_still_read_as_sentences",
    "application::tasks_sync::tests::test_one_of_each_is_not_read_as_a_plural",
    "application::tasks_sync::tests::test_the_summary_says_what_happened_in_words",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "a task refusal is said as a problem"
file = "src/application/tasks_sync.rs"
before = """        if !self.errors.is_empty() {
            // The count, not the text."""
after = """        if false {
            // The count, not the text."""
red = [
    "application::tasks_sync::tests::test_a_deletion_the_provider_refused_is_reported_and_tried_again_next_time",
    "application::tasks_sync::tests::test_a_failure_is_counted_rather_than_swallowed",
    "application::tasks_sync::tests::test_every_clause_at_once_is_still_read_as_sentences",
    "application::tasks_sync::tests::test_several_failures_are_counted_not_listed",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "the calendar summary says how many events went"
file = "src/application/calendar.rs"
before = """        "Calendar sync: {} created, {} updated, {} deleted",
        result.created, result.updated, result.deleted"""
after = """        "Calendar sync: {} created, {} updated",
        result.created, result.updated"""
red = [
    "application::calendar::tests::test_an_event_microsoft_says_is_gone_is_removed_and_counted_once",
    "application::calendar::tests::test_every_clause_at_once_is_still_read_as_sentences",
    "application::calendar::tests::test_one_change_waiting_and_one_thing_wrong_are_not_read_out_in_the_plural",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── A clause never decides how it joins to its neighbours ───────────────────

[[guard]]
name = "a clause is joined without a space of its own"
file = "src/application/summing_up.rs"
before = """    part.trim().trim_end_matches('.').trim_end()"""
after = """    part.trim_end().trim_end_matches('.').trim_end()"""
red = [
    "application::summing_up::tests::test_a_part_written_with_a_space_in_front_of_it_is_not_spoken_with_two",
]
tests_last_seen = [
    { file = "src/application/summing_up.rs", tests = 10 },
]

# ── A count and the thing it counts ─────────────────────────────────────────
#
# Five modules used to answer this question in their own words and two of them
# got it wrong, so "1 errors" and "1 changes are waiting here ... to send them"
# were both read out. There is one routine now, and this is the whole family in
# one break.

[[guard]]
name = "a count and the thing it counts agree in number"
file = "src/service/caldav.rs"
before = """    if count == 1 {
        format!("1 {thing}")"""
after = """    if count == 0 {
        format!("1 {thing}")"""
red = [
    "application::calendar::tests::test_every_clause_at_once_is_still_read_as_sentences",
    "application::calendar::tests::test_one_change_waiting_and_one_thing_wrong_are_not_read_out_in_the_plural",
    "application::contact_groups::tests::test_the_sidebar_selection_is_named_when_the_list_changes_to_match_it",
    "application::contacts_sync::tests::test_a_deletion_that_landed_and_one_that_had_nowhere_to_land_are_said_apart",
    "application::contacts_sync::tests::test_every_clause_one_sync_can_say_is_still_read_as_sentences",
    "application::contacts_sync::tests::test_one_edit_to_one_contact_both_books_hold_is_said_once_and_not_twice",
    "application::contacts_sync::tests::test_one_thing_going_wrong_is_not_read_out_as_one_errors",
    "application::contacts_sync::tests::test_the_summary_says_a_deletion_reached_the_address_book",
    "application::mail_sync::tests::test_a_folder_holding_one_message_does_not_say_one_messages",
    "application::mail_sync::tests::test_a_sync_says_when_a_rule_could_not_file_mail",
    # Added 2026-09-09, found by --remeasure rather than predicted. 05-07's new
    # count is said through `how_many`, so "1 removal waiting for the new copy
    # to be sent" becomes "1 removals" under this break.
    "application::tasks_sync::tests::test_a_deletion_held_by_a_copy_is_said_apart_from_one_held_by_the_setting",
    "application::tasks_sync::tests::test_a_list_that_went_is_said_rather_than_left_to_be_noticed",
    # Added 2026-09-09 by 05-08, found by --remeasure rather than predicted, and
    # the same shape one plan later: this one reads the summary while a move is
    # half done, so it meets both counts and both go through `how_many`.
    "application::tasks_sync::tests::test_a_move_held_by_allow_changes_is_counted_apart_from_one_held_by_the_copy",
    "application::tasks_sync::tests::test_every_clause_at_once_is_still_read_as_sentences",
    "application::tasks_sync::tests::test_one_of_each_is_not_read_as_a_plural",
    "presentation::wx_app::what_the_status_line_says::test_a_mailbox_holding_one_message_does_not_say_one_messages",
    "presentation::wx_app::what_the_status_line_says::test_a_module_holding_one_thing_does_not_say_one_things",
    "presentation::wx_app::what_the_status_line_says::test_an_account_with_one_folder_does_not_say_one_folders",
    "service::ical_subscription::tests::test_event_without_a_uid_is_not_stored_and_the_feed_says_why_it_is_empty",
    # Added 2026-09-15 by 08-07's sweep, on the runners at df3437a1: two more
    # sentences built through `how_many`, written 2026-09-10 and 2026-09-11 in
    # files this record had never named, so the count check could not see them.
    "application::notes_sync::tests::test_the_summary_names_the_setting_only_when_something_was_really_held",
    "presentation::note_folder_tree::tests::test_the_branch_says_what_is_under_it_even_when_it_is_one_or_none",
    # Added 2026-09-17 by 10-01, found by --remeasure rather than predicted:
    # the sentence at the end of a download of everything counts folders and
    # messages through `how_many`, in a file this record had never named.
    "application::bringing_everything_down::tests::test_the_whole_account_is_said_in_one_sentence_with_both_counts",
    # Added 2026-09-17 by 10-04, found by --remeasure rather than predicted:
    # the result line said once at the end of a check, "Work, 1 new message",
    # counts through `how_many` and reads "1 new messages" under this break.
    "application::mail_sync::tests::test_what_arrived_names_each_folder_that_received_something_with_its_count",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 1 and a
    # measurement here that found the same four. The sentence a batch says
    # about several messages counts through `how_many`, in a file this record
    # had never named.
    "application::choosing_messages::tests::test_a_batch_with_a_refusal_says_how_many_went_and_how_many_did_not",
    "application::choosing_messages::tests::test_each_outcome_has_its_words",
    "application::choosing_messages::tests::test_the_sentence_counts_the_messages_with_the_singular_right",
    "application::choosing_messages::tests::test_the_sentence_names_the_conversations_when_one_was_in_the_set",
]
tests_last_seen = [
    { file = "src/application/bringing_everything_down.rs", tests = 23 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/choosing_messages.rs", tests = 18 },
    { file = "src/application/contact_groups.rs", tests = 30 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/application/mail_sync.rs", tests = 149 },
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/presentation/note_folder_tree.rs", tests = 5 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "src/service/caldav.rs", tests = 170 },
    { file = "src/service/ical_subscription.rs", tests = 19 },
]

# ── One contact, however many address books hold a copy ─────────────────────
#
# Every number in a contacts summary is a number of people. Each address book
# reads its own copy of the same person, so a count kept per copy said one edit
# had happened twice and somebody heard it said twice.

[[guard]]
name = "folding two address books counts one person once"
file = "src/application/contacts_sync.rs"
before = """    fn and(&self, others: &Contacts) -> Contacts {
        Contacts(self.0.union(&others.0).cloned().collect())
    }"""
after = """    fn and(&self, others: &Contacts) -> Contacts {
        use std::sync::atomic::{AtomicUsize, Ordering};
        static NEXT: AtomicUsize = AtomicUsize::new(0);
        Contacts(
            self.0
                .iter()
                .cloned()
                .chain(others.0.iter().map(|id| {
                    format!("{id}#{}", NEXT.fetch_add(1, Ordering::Relaxed))
                }))
                .collect(),
        )
    }"""
red = [
    "application::contacts_sync::tests::test_a_change_a_setting_held_back_survives_the_read_in_the_same_sync",
    "application::contacts_sync::tests::test_a_contact_both_address_books_knew_is_deleted_at_both_and_counted_once",
    "application::contacts_sync::tests::test_a_contact_one_address_book_moved_is_not_also_called_unchanged",
    "application::contacts_sync::tests::test_a_first_sync_of_people_both_address_books_hold_counts_each_person_once",
    "application::contacts_sync::tests::test_a_person_one_address_book_deleted_and_the_other_still_holds_is_one_person",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_folding_two_address_books_together_counts_one_contact_once",
    "application::contacts_sync::tests::test_folding_two_address_books_together_keeps_every_count",
    # `test_one_edit_to_one_contact_both_books_hold_is_said_once_and_not_twice`
    # was named here until 2026-09-15 and taken off by 08-07's sweep: since
    # ce3e89ba of 2026-09-05 its scenario holds the edit for somebody to
    # choose and offers the second address book nothing, so the fold this
    # break doubles is not on its path and it stays green with the guard
    # broken. Eight remain, measured on the runners and again here.
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "unchanged means nothing happened to that contact"
file = "src/application/contacts_sync.rs"
before = """    let untouched = result
        .unchanged
        .apart_from(&result.contacts_something_happened_to());"""
after = """    let untouched = result.unchanged.clone();"""
red = [
    "application::contacts_sync::tests::test_a_contact_one_address_book_moved_is_not_also_called_unchanged",
    "application::contacts_sync::tests::test_a_person_one_address_book_deleted_and_the_other_still_holds_is_one_person",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Created, updated and deleted are one person each ────────────────────────
#
# A person both address books hold arrives from the first and is folded from
# the second, so an ordinary first sync of shared contacts said "3 created, 3
# updated" about three people. One address book can move its copy while the
# other deletes the contact, which said "1 updated, 1 deleted" about one person
# who is gone.

[[guard]]
name = "a person who arrived this sync is not also counted as changed"
file = "src/application/contacts_sync.rs"
before = """    let changed = result
        .updated_local
        .apart_from(&result.deleted_local)
        .apart_from(&created);"""
after = """    let changed = result
        .updated_local
        .apart_from(&result.deleted_local);"""
red = [
    "application::contacts_sync::tests::test_a_first_sync_of_people_both_address_books_hold_counts_each_person_once",
    "application::contacts_sync::tests::test_a_person_who_arrived_this_sync_is_not_also_counted_as_changed",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a person this sync removed is not also counted as changed"
file = "src/application/contacts_sync.rs"
before = """    let changed = result
        .updated_local
        .apart_from(&result.deleted_local)
        .apart_from(&created);"""
after = """    let changed = result.updated_local.apart_from(&created);"""
red = [
    "application::contacts_sync::tests::test_a_person_this_sync_removed_is_not_also_counted_as_changed",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a change the other setting holds back is noted on the way past"
file = "src/application/contacts_sync.rs"
before = """                if the_copy_here_was_written_here(&contact) {
                    result.waiting_on_how_far_a_change_goes.note(&contact.id);
                }
                return None;"""
after = """                return None;"""
red = [
    "application::contacts_sync::tests::test_a_change_held_from_the_other_address_book_by_the_setting_is_said",
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# A record retired on 2026-09-15 by 08-07's sweep: "the other setting is named
# only while the change is somebody's work". Its break took the
# `the_copy_here_was_written_here` gate off the `note` call above, and it
# named one test. Since ce3e89ba of 2026-09-05 that test's scenario holds the
# contact for somebody to choose rather than replacing the edit, so the gate
# is not on its path, and the sweep on the runners and a re-measurement here
# both found the break reddening nothing at all. A record naming no test is
# one the runner refuses, and a rule no test would notice is a ledger entry
# rather than a record: the gate is in the tree and untested, and the test
# that covers it belongs in contacts_sync.rs, which 77 records name, so it is
# written when somebody is prepared to re-measure those. WINDOWS.md has it.

[[guard]]
# Measured again on 2026-08-12. A later commit added a second sentence beside
# this one, opening on the same two lines, so the break stopped naming one
# place and the run could no longer apply it. The tests that go red are the
# same seven; only the text of the break changed, and it now names the routine.
#
# And again on 2026-09-09, when it came out short by one. `05-06` extracted the
# tail of the single-change sentence into `allowed::turn_the_setting_on`, so a
# move can name the setting in the same words a sync does, and built this arm on
# it rather than leaving a second copy of the words beside it. The new test
# asserting the two have not drifted apart therefore reddens under this break as
# well, which is the whole point of it and was not something the record could
# have predicted. Eight now, measured, not seven.
name = "one change waiting is not read out in the plural"
file = "src/application/allowed.rs"
before = """pub fn changes_waiting_here(count: usize) -> String {
    match count {
        1 => format!("""
after = """pub fn changes_waiting_here(count: usize) -> String {
    match count {
        0 => format!("""
red = [
    "application::allowed::tests::test_a_waiting_change_names_the_one_place_that_can_send_it",
    "application::allowed::tests::test_one_change_held_at_the_moment_it_was_made_names_the_setting_the_sync_names",
    "application::calendar::tests::test_one_change_waiting_and_one_thing_wrong_are_not_read_out_in_the_plural",
    "application::contacts_sync::tests::test_a_change_a_setting_held_back_survives_the_read_in_the_same_sync",
    "application::contacts_sync::tests::test_a_change_google_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_change_the_summary_said_was_waiting",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-09 in a file this
    # record had never named.
    "application::tasks_sync::tests::test_a_deletion_held_by_a_copy_is_said_apart_from_one_held_by_the_setting",
]
tests_last_seen = [
    { file = "src/application/allowed.rs", tests = 27 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
# The sentence whose arrival made the record above ambiguous, and which had no
# record of its own, so the same plural fault could come back in it with
# everything green. Anchored on the routine for the same reason.
name = "one removal waiting is not read out in the plural"
file = "src/application/allowed.rs"
before = """pub fn removals_waiting_here(count: usize) -> String {
    match count {
        1 => format!("""
after = """pub fn removals_waiting_here(count: usize) -> String {
    match count {
        0 => format!("""
red = [
    "application::allowed::tests::test_a_removal_held_back_says_the_mail_is_still_on_the_server",
]
tests_last_seen = [
    { file = "src/application/allowed.rs", tests = 27 },
]

[[guard]]
name = "folding two results keeps the other setting's count"
file = "src/application/contacts_sync.rs"
before = """        self.waiting_on_how_far_a_change_goes = self
            .waiting_on_how_far_a_change_goes
            .and(&other.waiting_on_how_far_a_change_goes);
"""
after = ""
red = [
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
    "application::contacts_sync::tests::test_folding_two_address_books_together_keeps_every_count",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Both copies of a contact both sides changed ─────────────────────────────
#
# SCALE-06. A contact changed here and changed in the address book between two
# syncs has two copies and one of them is about to go. Until 2026-09-05 the
# address book's copy won, the edit went, and a sentence said so afterwards.
# Being told is not being asked, and an edit that disappeared with a sentence
# about it in a summary nobody heard is an edit that disappeared.
#
# Who wins is still decided in one place, `whose_copy_wins`, and these records
# are about what happens in one of its four arms rather than about how the arm
# is chosen. A second decision about who wins would disagree with the first the
# moment either changed.
#
# The record this pair replaces was "a lost edit is said once, not on every
# later sync", whose break was the gate on counting a loss. There is no loss to
# count now, so the code it named is gone rather than moved.
#
# Measured by hand on 2026-09-05, the whole library with --no-fail-fast on a
# quiet machine, each break applied on its own.

[[guard]]
# The break is the arm falling through to the write, which is exactly what it
# did before this branch. Seven tests, and the last of them is the one nobody
# working on this feature would have filtered for: it is about a setting being
# named while the change is somebody's own work, and it reaches this arm to set
# that up.
name = "a contact changed in both places is held rather than written over"
file = "src/application/contacts_sync.rs"
before = """                if answer == WhoseCopyWins::TakeTheAddressBooksOverAChangeMadeHere
                    && the_copy_here_was_written_here(local)
                {
                    hold_both_copies_of(
                        cache,
                        local,
                        &remote_contact,
                        &AddressBook::Google,
                        arrived_at.as_deref(),"""
after = """                if false {
                    hold_both_copies_of(
                        cache,
                        local,
                        &remote_contact,
                        &AddressBook::Google,
                        arrived_at.as_deref(),"""
red = [
    "application::contacts_sync::tests::test_a_change_only_outlook_still_needs_is_not_written_over_quietly_by_google",
    "application::contacts_sync::tests::test_a_contact_changed_at_google_and_here_is_held_rather_than_written_over",
    "application::contacts_sync::tests::test_a_contact_typed_here_that_google_already_holds_is_not_lost_without_a_word",
    "application::contacts_sync::tests::test_a_held_contact_is_not_resolved_by_the_next_sync",
    "application::contacts_sync::tests::test_a_kept_change_an_address_book_turns_down_for_its_own_reasons_is_held_and_asked_about",
    "application::contacts_sync::tests::test_one_edit_to_one_contact_both_books_hold_is_said_once_and_not_twice",
    "application::contacts_sync::tests::test_the_other_address_book_setting_is_named_only_while_the_change_is_somebodys_work",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# Only somebody's own work is worth asking about. Once an address book's copy
# has replaced an edit, what is waiting is that address book's own words on
# their way to the other one, and asking somebody to choose between two copies
# neither of which is theirs is a question with no right answer.
#
# One test, measured rather than expected, and it is the push retry's own test
# rather than anything named for holding.
name = "only work typed here is held for a choice"
file = "src/application/contacts_sync.rs"
before = """                if answer == WhoseCopyWins::TakeTheAddressBooksOverAChangeMadeHere
                    && the_copy_here_was_written_here(local)
                {
                    hold_both_copies_of(
                        cache,
                        local,
                        &remote_contact,
                        &AddressBook::Google,
                        arrived_at.as_deref(),
                        &mut result,
                    )?;
                    continue;
                }
                // Google adds itself to the address books that know this"""
after = """                if answer == WhoseCopyWins::TakeTheAddressBooksOverAChangeMadeHere {
                    hold_both_copies_of(
                        cache,
                        local,
                        &remote_contact,
                        &AddressBook::Google,
                        arrived_at.as_deref(),
                        &mut result,
                    )?;
                    continue;
                }
                // Google adds itself to the address books that know this"""
red = [
    "application::contacts_sync::tests::test_only_work_typed_here_is_sent_again_over_googles_newer_copy",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# The half that keeps an unresolved conflict from becoming a silent overwrite
# at the provider. The change is still owed while somebody decides, so without
# this the very next push sends this computer's copy over the one they have not
# chosen to give up.
#
# The fixture for the third test had to be fixed before this measurement meant
# anything, and that is the finding rather than an aside. Written first with an
# account open for reading only, the write gate refused the push before
# anything was offered; written next with an address book that took the change,
# the flag cleared and the second sync owed nothing. Both versions asserted
# that nothing was offered and both passed with this filter removed. It turns
# the change down now, so the address book is still owed it and this filter is
# the only thing that can stop the offer.
name = "a contact held for a choice is offered to nobody"
file = "src/application/contacts_sync.rs"
before = """            if waiting_on_a_choice.iter().any(|id| id == &contact.id) {
                return None;
            }"""
after = """            if false && waiting_on_a_choice.iter().any(|id| id == &contact.id) {
                return None;
            }"""
red = [
    "application::contacts_sync::tests::test_a_held_contact_is_not_resolved_by_the_next_sync",
    "application::contacts_sync::tests::test_one_edit_to_one_contact_both_books_hold_is_said_once_and_not_twice",
    "application::contacts_sync::tests::test_the_other_address_book_setting_is_named_only_while_the_change_is_somebodys_work",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# The other half. A sync that reads over a held contact answers the question
# the person has not, and holding both copies then buys nothing but a slower
# overwrite. Broken at the Google read only, because the break has to name one
# place in the tree and the Outlook one is the same two lines.
name = "a contact held for a choice is left alone by a later read"
file = "src/application/contacts_sync.rs"
before = """                // keeping both copies buys nothing but a slower overwrite.
                if cache.is_held_for_a_choice(&local.id)? {"""
after = """                // keeping both copies buys nothing but a slower overwrite.
                if false && cache.is_held_for_a_choice(&local.id)? {"""
red = [
    "application::contacts_sync::tests::test_a_held_contact_is_not_resolved_by_the_next_sync",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
# Measured again after a network failure joined the reasons this predicate
# gates: it is still one function guarding one line, but two more tests now
# depend on it answering true for a contact typed here.
name = "an edit written here and then lost is still counted and said"
file = "src/application/contacts_sync.rs"
before = """fn the_copy_here_was_written_here(contact: &ContactEntry) -> bool {
    contact.last_synced_at.is_none()
}"""
after = """fn the_copy_here_was_written_here(contact: &ContactEntry) -> bool {
    let _ = contact;
    false
}"""
red = [
    "application::contacts_sync::tests::test_a_change_a_setting_held_back_survives_the_read_in_the_same_sync",
    "application::contacts_sync::tests::test_a_change_google_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_contact_changed_at_google_and_here_is_held_rather_than_written_over",
    "application::contacts_sync::tests::test_a_change_held_from_the_other_address_book_by_the_setting_is_said",
    "application::contacts_sync::tests::test_a_change_only_google_still_needs_is_not_written_over_quietly_by_outlook",
    "application::contacts_sync::tests::test_a_change_only_outlook_still_needs_is_not_written_over_quietly_by_google",
    "application::contacts_sync::tests::test_a_change_outlook_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_contact_changed_at_outlook_and_here_is_held_rather_than_written_over",
    "application::contacts_sync::tests::test_a_change_that_cannot_be_sent_again_is_kept_rather_than_replaced_by_the_read",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_google_is_kept_not_lost_to_a_concurrent_move",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_outlook_is_kept_not_lost_to_a_concurrent_move",
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
    "application::contacts_sync::tests::test_a_contact_typed_here_that_google_already_holds_is_not_lost_without_a_word",
    "application::contacts_sync::tests::test_a_kept_change_an_address_book_turns_down_for_its_own_reasons_is_held_and_asked_about",
    "application::contacts_sync::tests::test_a_held_contact_is_not_resolved_by_the_next_sync",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_one_edit_to_one_contact_both_books_hold_is_said_once_and_not_twice",
    "application::contacts_sync::tests::test_the_other_address_book_setting_is_named_only_while_the_change_is_somebodys_work",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_change_the_summary_said_was_waiting",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-10 in a file this
    # record had never named.
    "application::carddav_sync::tests::test_a_contact_changed_in_both_places_is_held_for_somebody_to_choose",
]
tests_last_seen = [
    { file = "src/application/carddav_sync.rs", tests = 14 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Sending a change again over a newer copy ────────────────────────────────
#
# An address book that has moved past a change refuses it. The push then reads
# the copy it holds now and sends the change again on top of that, which is
# right for work somebody typed here and wrong for a copy that came from the
# other address book: sending that again puts one address book's older words
# over the other's newer ones on nobody's authority. The gate is the same
# question the merge asks, it is written out twice, once per provider, and
# neither copy of it was written down here until now.

[[guard]]
name = "only work typed here is sent again over Google's newer copy"
file = "src/application/contacts_sync.rs"
before = """        if sent.as_ref().is_err_and(the_address_book_had_moved_past_it)
            && the_copy_here_was_written_here(&contact)
        {
            match google.the_copy_it_holds_now(token, &name_there).await {"""
after = """        if sent.as_ref().is_err_and(the_address_book_had_moved_past_it) {
            match google.the_copy_it_holds_now(token, &name_there).await {"""
red = [
    "application::contacts_sync::tests::test_only_work_typed_here_is_sent_again_over_googles_newer_copy",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "only work typed here is sent again over Outlook's newer copy"
file = "src/application/contacts_sync.rs"
before = """        if sent.as_ref().is_err_and(the_address_book_had_moved_past_it)
            && the_copy_here_was_written_here(&contact)
        {
            match ms_client.the_copy_it_holds_now(token, &name_there).await {"""
after = """        if sent.as_ref().is_err_and(the_address_book_had_moved_past_it) {
            match ms_client.the_copy_it_holds_now(token, &name_there).await {"""
red = [
    "application::contacts_sync::tests::test_only_work_typed_here_is_sent_again_over_the_address_books_newer_copy",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A change no setting let out ─────────────────────────────────────────────
#
# The push runs first and the read runs after it, in the same sync. A change a
# setting refused was offered to nobody before the read arrived to replace it,
# so the summary named a setting to turn on to send something that was already
# gone. Turning it on then sent nothing, twice over: once for each setting.

[[guard]]
# Measured again after a network failure was folded into the same
# accumulator this condition reads: the whole gate covers three reasons a
# push can fail to leave now, not two, and a break here reddens a test for
# each.
name = "a change no setting let out is kept, not replaced"
file = "src/application/contacts_sync.rs"
before = """    if answer == WhoseCopyWins::TakeTheAddressBooksOverAChangeMadeHere
        && the_copy_here_was_written_here(the_copy_here)
        && (a_setting_stopped_this_sync_sending(&the_copy_here.id, result)
            || still_built_on_an_old_copy.holds(&the_copy_here.id))
    {
        return WhoseCopyWins::KeepWhatIsHere;
    }
    answer"""
after = """    let _ = (the_copy_here, result, still_built_on_an_old_copy);
    answer"""
red = [
    "application::contacts_sync::tests::test_a_change_a_setting_held_back_survives_the_read_in_the_same_sync",
    "application::contacts_sync::tests::test_a_change_google_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_change_outlook_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_change_that_cannot_be_sent_again_is_kept_rather_than_replaced_by_the_read",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_google_is_kept_not_lost_to_a_concurrent_move",
    "application::contacts_sync::tests::test_a_change_the_network_dropped_at_outlook_is_kept_not_lost_to_a_concurrent_move",
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
    "application::contacts_sync::tests::test_a_kept_change_an_address_book_turns_down_for_its_own_reasons_is_held_and_asked_about",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_change_the_summary_said_was_waiting",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "only work somebody typed here is kept against an address book"
file = "src/application/contacts_sync.rs"
before = """        && the_copy_here_was_written_here(the_copy_here)
        && (a_setting_stopped_this_sync_sending(&the_copy_here.id, result)"""
after = """        && (a_setting_stopped_this_sync_sending(&the_copy_here.id, result)"""
red = [
    "application::contacts_sync::tests::test_an_address_books_own_copy_does_not_freeze_the_contact_against_the_other_one",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a change this sync stopped waiting is not a change waiting here"
file = "src/application/contacts_sync.rs"
before = """    result.waiting_on_the_setting.forget(&merged.id);
    let version = version_given_by(&merged, address_book);"""
after = """    let version = version_given_by(&merged, address_book);"""
red = [
    "application::contacts_sync::tests::test_an_address_books_own_copy_does_not_freeze_the_contact_against_the_other_one",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "Allow Changes is one of the two settings that keeps a change"
file = "src/application/contacts_sync.rs"
before = """    result.waiting_on_the_setting.holds(contact_id)
        || result.waiting_on_how_far_a_change_goes.holds(contact_id)"""
after = """    result.waiting_on_how_far_a_change_goes.holds(contact_id)"""
red = [
    "application::contacts_sync::tests::test_a_change_a_setting_held_back_survives_the_read_in_the_same_sync",
    "application::contacts_sync::tests::test_a_change_google_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_change_outlook_had_moved_past_is_sent_again_rather_than_lost",
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
    "application::contacts_sync::tests::test_a_kept_change_an_address_book_turns_down_for_its_own_reasons_is_held_and_asked_about",
    "application::contacts_sync::tests::test_an_edit_both_address_books_had_moved_past_reaches_both_of_them",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_change_the_summary_said_was_waiting",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "how far a change goes is the other of the two settings that keeps it"
file = "src/application/contacts_sync.rs"
before = """    result.waiting_on_the_setting.holds(contact_id)
        || result.waiting_on_how_far_a_change_goes.holds(contact_id)
}"""
after = """    result.waiting_on_the_setting.holds(contact_id)
}"""
red = [
    "application::contacts_sync::tests::test_a_change_two_settings_hold_back_names_both_of_them",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A contact deleted here ──────────────────────────────────────────────────
#
# The deletion used to stop at this computer. The row went, the product said
# "deleted", nothing was left to say it had happened, and the next read wrote
# her back down. Three runs reported it before it was fixed, and it was live
# with the shipped settings the whole time, so each half of the fix is written
# down here.

[[guard]]
name = "a deletion made here leaves a note for the address books that knew her"
file = "src/data/message_cache/contacts.rs"
before = """                 FROM contact_identities WHERE contact_id = ?1",
                params![contact_id, chrono::Utc::now().to_rfc3339()],"""
after = """                 FROM contact_identities WHERE contact_id = ?1 AND 1 = 0",
                params![contact_id, chrono::Utc::now().to_rfc3339()],"""
red = [
    "application::contacts_sync::tests::test_a_contact_both_address_books_knew_is_deleted_at_both_and_counted_once",
    "application::contacts_sync::tests::test_a_contact_deleted_here_does_not_come_back_from_the_read_that_follows",
    "application::contacts_sync::tests::test_a_contact_deleted_here_is_deleted_at_google_too",
    "application::contacts_sync::tests::test_a_contact_deleted_here_is_deleted_at_outlook_too",
    "application::contacts_sync::tests::test_a_contact_deleted_here_that_also_held_an_unsent_edit_is_deleted_not_sent",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_a_contact_outlook_still_holds_does_not_come_back_after_google_took_the_deletion",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_a_deletion_allow_changes_held_back_is_still_waiting_afterwards",
    "application::contacts_sync::tests::test_a_deletion_the_address_book_refused_is_kept_and_said",
    "application::contacts_sync::tests::test_an_address_book_that_has_never_heard_of_her_is_not_asked_again",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_deletion_the_summary_said_was_waiting",
    "data::message_cache::contacts::tests::test_deleting_a_contact_leaves_a_note_for_every_address_book_that_knew_her",
    "data::message_cache::contacts::tests::test_telling_one_address_book_leaves_the_other_still_owed_the_deletion",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-10 in a file this
    # record had never named.
    "application::carddav_sync::tests::test_a_contact_deleted_here_is_not_written_back_down",
]
tests_last_seen = [
    { file = "src/application/carddav_sync.rs", tests = 14 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "the Google read does not write back somebody deleted here"
file = "src/application/contacts_sync.rs"
before = """        if everybody_deleted_here_that_google_knew.holds(&person.resource_name) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "application::contacts_sync::tests::test_a_contact_deleted_here_does_not_come_back_from_the_read_that_follows",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_deletion_the_summary_said_was_waiting",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# And the same question asked the narrow way, which this has had wrong twice.
# Reading what is still owed answers "has the address book been told", the same
# answer right up to the moment it stops being: a deletion that goes stops
# being owed, so a guard reading what is owed disarms itself for the one person
# whose deletion has just succeeded. Reading the push's own list fixed that and
# left the next sync open, because that list ends with the sync that made it
# and the address book goes on naming her.

[[guard]]
name = "the read asks who was deleted, not only what is still owed"
file = "src/application/contacts_sync.rs"
before = """        Ok(notes) => notes
            .into_iter()
            .filter(|note| &note.address_book == address_book)
            .map(|note| note.provider_contact_id)
            .collect(),"""
after = """        Ok(notes) => notes
            .into_iter()
            .filter(|note| note.so_far.still_owed() && &note.address_book == address_book)
            .map(|note| note.provider_contact_id)
            .collect(),"""
red = [
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-10 in a file this
    # record had never named.
    "application::carddav_sync::tests::test_a_contact_deleted_here_is_not_written_back_down",
]
tests_last_seen = [
    { file = "src/application/carddav_sync.rs", tests = 14 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "the Outlook read does not write back somebody deleted here"
file = "src/application/contacts_sync.rs"
before = """        if everybody_deleted_here_that_outlook_knew.holds(&ms_contact.id) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "application::contacts_sync::tests::test_a_contact_outlook_still_holds_does_not_come_back_after_google_took_the_deletion",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A deletion outlives the sync that sent it ──────────────────────────────
#
# The note used to go the moment the provider took the deletion, so the read
# that followed in the same sync had nothing to consult and wrote the thing
# straight back down. The same shape stood in all three syncs. Each of these
# breaks puts that exact defect back.

[[guard]]
name = "an address book that took a deletion is remembered, not dropped"
file = "src/data/message_cache/contacts.rs"
before = """                "UPDATE deleted_contacts SET taken_at = ?3
                 WHERE contact_id = ?1 AND address_book = ?2","""
after = """                "DELETE FROM deleted_contacts
                 WHERE contact_id = ?1 AND address_book = ?2 AND ?3 IS NOT NULL","""
red = [
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
    "data::message_cache::contacts::tests::test_telling_one_address_book_leaves_the_other_still_owed_the_deletion",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-10 in a file this
    # record had never named.
    "application::carddav_sync::tests::test_a_contact_deleted_here_is_not_written_back_down",
]
tests_last_seen = [
    { file = "src/application/carddav_sync.rs", tests = 14 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "the Google calendar read does not write back an event deleted here"
file = "src/application/calendar.rs"
before = """        // An event somebody deleted on this computer. Google is still naming
        // it, and writing it back down puts it on the screen again with
        // nothing left to say it was ever deleted.
        if deleted_here.holds(&event.id) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "application::calendar::tests::test_a_day_of_a_series_this_computer_deleted_is_not_written_back",
    "application::calendar::tests::test_a_deletion_in_a_calendar_made_here_holds_across_three_syncs",
    "application::calendar::tests::test_a_deletion_in_a_read_only_calendar_of_this_account_holds_across_three_syncs",
    "application::calendar::tests::test_a_deletion_of_an_event_in_no_calendar_holds_across_three_syncs",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_a_later_sync",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_the_read_that_follows",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "the Outlook calendar read does not write back an event deleted here"
file = "src/application/calendar.rs"
before = """        // An event somebody deleted on this computer, for the reason written
        // beside the same question in the Google read.
        if deleted_here.holds(&event.id) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "application::calendar::tests::test_an_event_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_outlook",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a calendar deletion the provider took is remembered, not forgotten"
file = "src/application/calendar.rs"
before = """        let sent = delete_google_event(google, token, &at_google, provider_event_id).await;
        if sent.is_ok() {
            let _ = cache.the_provider_took_the_deletion_of_an_event(
                &note.id,
                &crate::application::deletions::written(chrono::Utc::now()),
            );
        }"""
after = """        let sent = delete_google_event(google, token, &at_google, provider_event_id).await;
        if sent.is_ok() {
            let _ = cache.forget_deleted_calendar_event(&note.id);
        }"""
# Remeasured when the clearing itself started refusing notes with a provider
# identity: that refusal defuses this break's same-sync half, because the note
# it wrongly clears survives the clearing and still masks the read. The push
# never marks it taken, which is what the two tests left here see.
red = [
    "application::calendar::tests::test_an_event_deleted_here_is_deleted_at_the_provider_and_stops_being_owed",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_a_later_sync",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "the calendar server read does not write back an event deleted here"
file = "src/application/caldav_sync.rs"
# The comment line is part of the break because the feed refresh now asks the
# same question in the same words a page further down, and a break has to land
# on exactly one of them.
before = """        // nothing left to say it was ever deleted.
        if deleted_here.holds(&remote.uid) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "application::caldav_sync::tests::test_a_day_of_a_caldav_series_this_computer_deleted_is_not_written_back",
    "application::caldav_sync::tests::test_a_deletion_with_no_address_at_the_server_is_kept_and_masks_the_read",
    "application::caldav_sync::tests::test_an_event_this_computer_deleted_is_not_written_back_by_a_later_caldav_sync",
    "application::caldav_sync::tests::test_an_event_this_computer_deleted_is_not_written_back_by_the_read_that_follows",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "the feed refresh does not write back an event deleted here"
file = "src/application/caldav_sync.rs"
# The fourth read beside the three above, and the one the mask was forgotten
# from when it was added to its siblings. A feed carries the whole calendar on
# every refresh, so without this mask a deletion made here is undone at the
# very next refresh however carefully the note was kept.
before = """        // question was first added.
        if deleted_here.holds(&remote.uid) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "application::caldav_sync::tests::test_an_event_deleted_here_is_not_written_back_by_a_feed_refresh",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "the tasks read does not write back a task deleted here"
file = "src/application/tasks_sync.rs"
before = """    if deleted_here.holds(&stored.id) {
        return;
    }"""
after = """    if false {
        return;
    }"""
red = [
    "application::tasks_sync::tests::test_a_deletion_survives_the_list_it_was_in_going_away",
    "application::tasks_sync::tests::test_a_deletion_survives_the_list_it_was_in_going_away_at_outlook",
    "application::tasks_sync::tests::test_a_task_this_computer_deleted_is_not_written_back_by_a_later_sync",
    "application::tasks_sync::tests::test_a_task_this_computer_deleted_is_not_written_back_by_outlook",
    "application::tasks_sync::tests::test_a_task_this_computer_deleted_is_not_written_back_by_the_read_that_follows",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

# ── What the clearing itself refuses ───────────────────────────────────────
#
# Every mask above is built from the notes' provider identities, so a note
# with one is load-bearing for as long as any provider could hand the event
# back. Three callers each decided for themselves whether a note was safe to
# clear, one decided wrong for three kinds of calendar at once, and every
# wrong answer resurrected a deleted event. The answer lives in the delete
# statement now: a note with a provider identity is refused, a note with none
# masks nothing and drains. Both directions are guarded, because widening the
# refusal is a table that grows for ever and narrowing it is a deleted event
# back on the screen.

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named.
# The CalDAV series day is the same rule reaching one more kind of deletion.
name = "forgetting a calendar deletion a provider could still hand back is refused"
file = "src/data/message_cache/calendar.rs"
before = """                "DELETE FROM deleted_calendar_events
                 WHERE id = ?1 AND provider_event_id IS NULL","""
after = """                "DELETE FROM deleted_calendar_events WHERE id = ?1","""
red = [
    "application::caldav_sync::tests::test_a_day_of_a_caldav_series_this_computer_deleted_is_not_written_back",
    "application::caldav_sync::tests::test_a_deletion_with_no_address_at_the_server_is_kept_and_masks_the_read",
    "application::calendar::tests::test_a_deletion_in_a_calendar_made_here_holds_across_three_syncs",
    "application::calendar::tests::test_a_deletion_in_a_read_only_calendar_of_this_account_holds_across_three_syncs",
    "application::calendar::tests::test_a_deletion_in_a_subscribed_calendar_is_kept_so_the_feed_cannot_hand_it_back",
    "application::calendar::tests::test_a_deletion_of_an_event_in_no_calendar_holds_across_three_syncs",
    "data::message_cache::calendar::tests::test_forgetting_a_deletion_a_provider_could_still_hand_back_is_refused",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/data/message_cache/calendar.rs", tests = 39 },
]

[[guard]]
name = "a calendar deletion no provider ever held still drains"
file = "src/data/message_cache/calendar.rs"
before = """                "DELETE FROM deleted_calendar_events
                 WHERE id = ?1 AND provider_event_id IS NULL","""
after = """                "DELETE FROM deleted_calendar_events
                 WHERE id = ?1 AND 1 = 0","""
red = [
    "application::caldav_sync::tests::test_an_event_the_server_never_had_leaves_no_request",
    "application::calendar::tests::test_an_event_that_never_reached_the_provider_leaves_no_request",
    "data::message_cache::calendar::tests::test_forgetting_a_deletion_no_provider_ever_held_lets_the_row_go",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/data/message_cache/calendar.rs", tests = 39 },
]

# The same pair for tasks, which went without one for a round. A task's note
# has no column naming the provider, because for a task the provider's name for
# it is the row id itself, so the refusal asks the id rather than the table.
# The caller that got it wrong dropped every note belonging to a list that had
# gone away, including notes about tasks the provider had merely moved into a
# list that stayed, so those tasks came back on the next sync.

[[guard]]
name = "forgetting a task deletion a provider could still hand back is refused"
file = "src/data/message_cache/tasks.rs"
before = """        if crate::application::tasks_sync::a_provider_holds(task_id) {
            return Ok(false);
        }
        let removed = self"""
after = """        let removed = self"""
red = [
    "application::tasks_sync::tests::test_a_deletion_of_a_provider_held_task_with_no_list_is_kept_rather_than_forgotten",
    "application::tasks_sync::tests::test_a_deletion_survives_the_list_it_was_in_going_away",
    "application::tasks_sync::tests::test_a_deletion_survives_the_list_it_was_in_going_away_at_outlook",
    "application::tasks_sync::tests::test_a_deletion_waiting_for_a_list_that_has_gone_is_kept_because_the_provider_may_still_hand_the_task_back",
    "data::message_cache::tasks::tests::test_forgetting_a_deletion_a_provider_could_still_hand_back_is_refused",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/data/message_cache/tasks.rs", tests = 22 },
]

[[guard]]
name = "a task deletion no provider ever held still drains"
file = "src/data/message_cache/tasks.rs"
before = """        if crate::application::tasks_sync::a_provider_holds(task_id) {"""
after = """        if true {"""
red = [
    "application::tasks_sync::tests::test_a_deletion_of_a_task_made_here_still_drains_when_its_list_goes",
    "application::tasks_sync::tests::test_a_task_deleted_before_it_ever_reached_the_provider_leaves_nothing_to_tell_it",
    "data::message_cache::tasks::tests::test_forgetting_a_deletion_no_provider_ever_held_lets_the_row_go",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/data/message_cache/tasks.rs", tests = 22 },
]

# The other half of keeping that note: it now outlives the list it names, and a
# note kept is a note the push keeps finding. Asking a provider to delete
# something in a list it no longer has is refused every time, which is a
# problem on the status line for ever about something nobody can act on.

[[guard]]
name = "a deletion whose list has gone stops being sent to the provider"
file = "src/application/tasks_sync.rs"
before = """        if !lists_here.iter().any(|list| list.id == list_id) {"""
after = """        if false {"""
red = [
    "application::tasks_sync::tests::test_a_deletion_for_a_list_that_has_gone_is_not_sent_again_and_is_not_a_problem",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

# ── What lets a deletion go ────────────────────────────────────────────────
#
# The other end of the rule. A note the provider has taken is let go of by the
# clock, so the three tables drain rather than growing for ever; a note nobody
# has taken is work rather than a memory and the clock must never touch it, or
# the thing stays deleted here and present at the provider for ever, after the
# product said "deleted". And a sweep nothing calls is a rule that says
# "remembered for ever".

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: three more tests went red than this named,
# all three of them a day of a series rather than a whole event. The three
# tables this sweep drains hold a day's deletion the same way they hold an
# event's, which is the rule and not a surprise.
name = "only a deletion a provider took is let go of by the clock"
file = "src/data/message_cache/mod.rs"
before = """                    &format!("DELETE FROM {table} WHERE taken_at IS NOT NULL AND taken_at < ?1"),"""
after = """                    &format!("DELETE FROM {table} WHERE taken_at IS NULL OR taken_at < ?1"),"""
red = [
    "application::caldav_sync::tests::test_a_day_of_a_caldav_series_this_computer_deleted_is_not_written_back",
    "application::caldav_sync::tests::test_a_deletion_with_no_address_at_the_server_is_kept_and_masks_the_read",
    "application::caldav_sync::tests::test_an_event_deleted_here_is_deleted_at_the_calendar_server_and_stops_being_owed",
    "application::caldav_sync::tests::test_an_event_this_computer_deleted_is_not_written_back_by_a_later_caldav_sync",
    "application::caldav_sync::tests::test_an_event_this_computer_deleted_is_not_written_back_by_the_read_that_follows",
    "application::caldav_sync::tests::test_deleting_a_caldav_occurrence_exception_never_sends_a_delete_for_the_whole_series",
    "application::calendar::tests::test_a_day_of_a_series_this_computer_deleted_is_not_written_back",
    "application::calendar::tests::test_a_day_of_an_outlook_series_this_computer_deleted_is_not_written_back",
    "application::calendar::tests::test_a_deletion_in_a_calendar_made_here_holds_across_three_syncs",
    "application::calendar::tests::test_a_deletion_in_a_read_only_calendar_of_this_account_holds_across_three_syncs",
    "application::calendar::tests::test_a_deletion_in_a_subscribed_calendar_is_kept_so_the_feed_cannot_hand_it_back",
    "application::calendar::tests::test_a_deletion_in_the_other_providers_calendar_is_left_for_that_provider_to_carry_out",
    "application::calendar::tests::test_a_deletion_nobody_has_taken_is_never_let_go_of_by_the_clock",
    "application::calendar::tests::test_a_deletion_of_an_event_in_no_calendar_holds_across_three_syncs",
    "application::calendar::tests::test_an_event_deleted_here_is_deleted_at_outlook_and_stops_being_owed",
    "application::calendar::tests::test_an_event_deleted_here_is_deleted_at_the_provider_and_stops_being_owed",
    "application::calendar::tests::test_an_event_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_a_later_sync",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_outlook",
    "application::calendar::tests::test_an_event_this_computer_deleted_is_not_written_back_by_the_read_that_follows",
    "application::contacts_sync::tests::test_a_contact_both_address_books_knew_is_deleted_at_both_and_counted_once",
    "application::contacts_sync::tests::test_a_contact_deleted_here_does_not_come_back_from_the_read_that_follows",
    "application::contacts_sync::tests::test_a_contact_deleted_here_is_deleted_at_google_too",
    "application::contacts_sync::tests::test_a_contact_deleted_here_is_deleted_at_outlook_too",
    "application::contacts_sync::tests::test_a_contact_deleted_here_that_also_held_an_unsent_edit_is_deleted_not_sent",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_google_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_a_contact_outlook_still_holds_does_not_come_back_after_google_took_the_deletion",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_is_not_written_back_by_the_same_read",
    "application::contacts_sync::tests::test_a_contact_outlook_took_the_deletion_for_stays_gone_on_a_later_sync",
    "application::contacts_sync::tests::test_a_deletion_allow_changes_held_back_is_still_waiting_afterwards",
    "application::contacts_sync::tests::test_a_deletion_the_address_book_refused_is_kept_and_said",
    "application::contacts_sync::tests::test_an_address_book_that_has_never_heard_of_her_is_not_asked_again",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_deletion_the_summary_said_was_waiting",
    "application::tasks_sync::tests::test_a_deletion_survives_the_list_it_was_in_going_away",
    "application::tasks_sync::tests::test_a_deletion_survives_the_list_it_was_in_going_away_at_outlook",
    "application::tasks_sync::tests::test_a_deletion_waiting_for_a_list_that_has_gone_is_kept_because_the_provider_may_still_hand_the_task_back",
    "application::tasks_sync::tests::test_a_task_this_computer_deleted_is_not_written_back_by_a_later_sync",
    "application::tasks_sync::tests::test_a_task_this_computer_deleted_is_not_written_back_by_outlook",
    "application::tasks_sync::tests::test_a_task_this_computer_deleted_is_not_written_back_by_the_read_that_follows",
    # Added 2026-09-15 by 08-07's sweep: the CardDAV and notes syncs, written
    # 2026-09-10 and 2026-09-11 in files this record had never named, both
    # keep a deletion the clock must not let go of.
    "application::carddav_sync::tests::test_a_contact_deleted_here_is_not_written_back_down",
    "application::notes_sync::tests::test_a_deletion_the_backend_has_taken_is_still_remembered",
    "application::notes_sync::tests::test_a_note_deleted_here_is_not_written_back_down_while_the_backend_still_names_it",
    "application::notes_sync::tests::test_a_note_deleted_here_is_offered_to_the_backend_for_deletion",
    "application::notes_sync::tests::test_a_removal_is_only_offered_to_the_container_the_note_was_in",
    "application::notes_sync::tests::test_a_removal_waits_for_the_copy_a_move_is_making",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/carddav_sync.rs", tests = 14 },
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
name = "a sync lets go of what it has remembered long enough"
file = "src/application/calendar.rs"
before = """    let mut result = CalendarSyncResult::default();
    forget_the_deletions_remembered_long_enough(cache, &mut result);
    push_to_google(cache, google, token, account_id, &mut result).await;"""
after = """    let mut result = CalendarSyncResult::default();
    push_to_google(cache, google, token, account_id, &mut result).await;"""
red = [
    "application::calendar::tests::test_a_sync_lets_go_of_a_deletion_it_has_remembered_long_enough",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "an address book that says she is gone is not asked to delete her"
file = "src/application/contacts_sync.rs"
before = """    cache.drop_synced_contact(&local.id)?;"""
after = """    cache.delete_contact(&local.id)?;"""
red = [
    "application::contacts_sync::tests::test_a_contact_google_deleted_is_not_sent_back_to_google_as_a_deletion",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a deletion a setting held back is kept, not forgotten"
file = "src/application/contacts_sync.rs"
before = """            result.waiting_on_the_setting.note(&note.contact_id);
            return;"""
after = """            result.waiting_on_the_setting.note(&note.contact_id);
            let _ = cache.the_address_book_took_the_deletion(
                &note.contact_id,
                &note.address_book,
                &crate::application::deletions::written(chrono::Utc::now()),
            );
            return;"""
red = [
    "application::contacts_sync::tests::test_a_contact_deleted_here_does_not_come_back_from_the_read_that_follows",
    "application::contacts_sync::tests::test_a_contact_outlook_still_holds_does_not_come_back_after_google_took_the_deletion",
    "application::contacts_sync::tests::test_a_deletion_allow_changes_held_back_is_still_waiting_afterwards",
    "application::contacts_sync::tests::test_turning_allow_changes_on_sends_the_deletion_the_summary_said_was_waiting",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a deletion the address book refused is kept, not forgotten"
file = "src/application/contacts_sync.rs"
before = """                "Contact {} could not be deleted in {address_book_called}: {refused}",
                note.contact_id
            ));
            return;"""
after = """                "Contact {} could not be deleted in {address_book_called}: {refused}",
                note.contact_id
            ));
            let _ = cache.the_address_book_took_the_deletion(
                &note.contact_id,
                &note.address_book,
                &crate::application::deletions::written(chrono::Utc::now()),
            );
            return;"""
red = [
    "application::contacts_sync::tests::test_a_deletion_the_address_book_refused_is_kept_and_said",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A 404 is not a deletion that reached anybody ────────────────────────────
#
# The note goes either way, because a contact already gone from a phone must
# not fail on every sync for ever. What is not the same is what was sent: the
# address book answered that it has no such contact, so nothing travelled, and
# counting it with the deletions that did made the one sentence somebody has
# for whether their deletion travelled say it had.

[[guard]]
name = "a deletion the address book never took is not counted as one it took"
file = "src/application/contacts_sync.rs"
before = """        TheDeletionIsDone::SheWasNotThereToDelete => result
            .already_gone_from_the_address_book
            .note(&note.contact_id),"""
after = """        TheDeletionIsDone::SheWasNotThereToDelete => result.deleted_remote.note(&note.contact_id),"""
red = [
    "application::contacts_sync::tests::test_an_address_book_that_has_never_heard_of_her_is_not_asked_again",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a contact the address book never held is said, not passed over"
file = "src/application/contacts_sync.rs"
before = """    if !result.already_gone_from_the_address_book.is_empty() {"""
after = """    if false {"""
red = [
    "application::contacts_sync::tests::test_a_contact_the_address_book_never_held_is_not_said_to_have_been_sent_there",
    "application::contacts_sync::tests::test_a_deletion_that_landed_and_one_that_had_nowhere_to_land_are_said_apart",
    "application::contacts_sync::tests::test_two_contacts_the_address_book_never_held_are_said_in_the_same_words",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "folding two results keeps the contacts an address book never held"
file = "src/application/contacts_sync.rs"
before = """        self.already_gone_from_the_address_book = self
            .already_gone_from_the_address_book
            .and(&other.already_gone_from_the_address_book);
"""
after = ""
red = [
    "application::contacts_sync::tests::test_folding_two_address_books_together_keeps_every_count",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a deletion that reached the address book is said"
file = "src/application/contacts_sync.rs"
before = """    if !result.deleted_remote.is_empty() {"""
after = """    if false {"""
red = [
    "application::contacts_sync::tests::test_a_deletion_that_landed_and_one_that_had_nowhere_to_land_are_said_apart",
    "application::contacts_sync::tests::test_the_summary_says_a_deletion_reached_the_address_book",
    "application::contacts_sync::tests::test_two_deletions_that_reached_the_address_book_are_not_read_out_in_the_singular",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "folding two results keeps the deletions that went out"
file = "src/application/contacts_sync.rs"
before = """        self.deleted_remote = self.deleted_remote.and(&other.deleted_remote);
"""
after = ""
red = [
    "application::contacts_sync::tests::test_a_contact_both_address_books_knew_is_deleted_at_both_and_counted_once",
    "application::contacts_sync::tests::test_folding_two_address_books_together_keeps_every_count",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Deleting a calendar, a task list, a note folder or a group ──────────────
#
# None of the four reaches a provider, and the question asked first says so.
# It has to say it about the one being deleted rather than about its kind: a
# calendar or a task list made on this computer inside a Google account is sent
# nowhere, so promising it back at the next sync leaves somebody waiting for a
# sync that will never mention it.

[[guard]]
name = "the deletion question asks about the container, not its kind"
file = "src/application/new_item.rs"
before = """        (
            ContainerKind::Calendar | ContainerKind::TaskList | ContainerKind::NoteFolder,
            WhereItCameFrom::AProvider
        )"""
after = """        (
            ContainerKind::Calendar | ContainerKind::TaskList | ContainerKind::NoteFolder,
            WhereItCameFrom::AProvider | WhereItCameFrom::ThisComputer
        )"""
red = [
    "application::new_item::deletion_tests::test_a_calendar_made_on_this_computer_is_not_promised_back_from_a_provider",
    "application::new_item::deletion_tests::test_a_group_is_not_promised_back_from_a_provider_and_a_backed_note_folder_is",
    "application::new_item::deletion_tests::test_a_task_list_made_on_this_computer_is_not_promised_back_either",
]
tests_last_seen = [
    { file = "src/application/new_item.rs", tests = 42 },
]

[[guard]]
name = "the deletion question is handed the container it is about"
file = "src/presentation/managers.rs"
before = """        where_it_came_from(&cache, kind, &id),
    );"""
after = """        crate::application::new_item::WhereItCameFrom::AProvider,
    );"""
red = [
    "presentation::managers::deletion_wiring::test_the_question_is_asked_about_the_container_being_deleted",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "where a calendar came from is read off its row"
file = "src/presentation/managers.rs"
before = """        ContainerKind::Calendar => cache
            .get_calendar(id)"""
after = """        ContainerKind::Calendar => cache
            .get_calendar("never this one")"""
red = [
    "presentation::managers::where_a_container_came_from::test_a_calendar_google_sent_is_read_as_the_providers",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "where a task list came from is read off its identifier"
file = "src/presentation/managers.rs"
before = """        ContainerKind::TaskList => crate::application::tasks_sync::a_provider_holds(id),"""
after = """        ContainerKind::TaskList => true,"""
red = [
    "presentation::managers::where_a_container_came_from::test_a_task_list_is_read_from_its_identifier",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── A change that nothing anywhere can send ─────────────────────────────────
#
# A row can be left waiting to be sent with nothing able to send it, and until
# these existed nothing said so: the push saw the calendar was not its own,
# dropped the row without counting it, and did the same on every sync from
# then on. The move that put it there had already announced itself as done.

[[guard]]
name = "a change in a calendar that can only be read is said"
file = "src/application/calendar.rs"
before = """    if container.is_read_only {
        return Some(Nowhere::OnlyReadable);
    }"""
after = """    if false {
        return Some(Nowhere::OnlyReadable);
    }"""
red = [
    "application::calendar::tests::test_a_change_in_a_calendar_the_account_can_only_read_is_said_rather_than_dropped",
    "application::calendar::tests::test_one_sentence_for_the_calendar_rather_than_one_for_every_change",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a change in a calendar no account holds is said"
file = "src/application/calendar.rs"
before = """    (!PROVIDERS_A_CHANGE_CAN_REACH.contains(&came_from)).then_some(Nowhere::MadeHere)"""
after = """    None"""
red = [
    "application::calendar::tests::test_a_change_in_a_calendar_made_on_this_computer_is_said",
    # Added 2026-09-15 by 08-07's sweep: written 2026-08-19 in the file this
    # record names, eleven days after the record, and stamped over on
    # 2026-09-02 by --recount-everything, which measured nothing.
    "application::calendar::tests::test_two_calendars_that_share_a_reason_are_still_said_separately",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a calendar with a pass of its own is not warned about twice"
file = "src/application/calendar.rs"
before = """    if CALENDARS_WITH_A_PASS_OF_THEIR_OWN.contains(&came_from) {
        return None;
    }"""
after = """    if false {
        return None;
    }"""
red = [
    "application::calendar::tests::test_a_calendar_with_a_pass_of_its_own_is_left_to_say_it_itself",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "what a feed cannot save is counted by asking the calendar"
file = "src/application/caldav_sync.rs"
before = """    let waiting = changes_waiting(cache, calendar, account_id, &mut result).len();"""
after = """    let waiting = 0;"""
red = [
    "application::caldav_sync::tests::test_a_change_to_an_event_in_a_subscribed_feed_survives_the_next_refresh_and_is_said",
    "application::caldav_sync::tests::test_a_change_waiting_on_an_event_a_feed_has_stopped_carrying_is_kept_and_said",
    "application::caldav_sync::tests::test_an_event_the_feed_never_carried_is_counted_as_one_that_cannot_be_saved",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

# ── A move into a container that could never hold it ───────────────────────
#
# The refusal that already existed asks about the item and never about the
# place it is going, so a calendar this program can only read was offered,
# chosen, written and announced as done.

[[guard]]
name = "a calendar that can only be read is left out of the chooser"
file = "src/presentation/managers.rs"
before = """        .filter(|(id, _, _)| can_only_be_read(cache, holder, id).is_none())"""
after = """        .filter(|(_id, _, _)| true)"""
red = [
    "presentation::managers::tests::test_a_calendar_this_program_can_only_read_is_not_offered_as_somewhere_to_move_to",
    "presentation::managers::tests::test_an_event_with_nowhere_but_a_read_only_calendar_to_go_is_told_so",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a move into a calendar that can only be read is refused where it is written"
file = "src/presentation/managers.rs"
before = """        && let Some(name) = can_only_be_read(cache, holder, into)"""
after = """        && let Some(name) = can_only_be_read(cache, holder, "never this one")"""
red = [
    "presentation::managers::tests::test_moving_an_event_into_a_calendar_this_program_can_only_read_is_refused",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
# Re-anchored 2026-09-08. The break was `capitalise(a_thing(kind))` on its own,
# which named one place until `is_not_kept_in_a_container` arrived in the same
# file and used the same call for the same reason. Two places is a break that
# cannot be applied at all, so the anchor now carries the line above it, which
# only `cannot_be_moved` has. Re-measured by hand after the anchor moved rather
# than edited until it applied.
name = "a refusal names the kind with the article that belongs with it"
file = "src/application/pim_command.rs"
before = """        holder.label().to_lowercase(),
        capitalise(a_thing(kind))"""
after = """        holder.label().to_lowercase(),
        format!("A {}", thing(kind))"""
red = [
    "application::pim_command::tests::test_the_refusal_names_the_kind_with_the_article_that_belongs_with_it",
]
tests_last_seen = [
    { file = "src/application/pim_command.rs", tests = 38 },
]

# ── An edit that was not an edit ────────────────────────────────────────────

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named. It
# asks the same question of an occurrence exception.
name = "a save that changed nothing leaves the row alone"
file = "src/presentation/managers.rs"
before = """    if !holds_a_change(data, opened) {
        return stored;
    }"""
after = """    if false {
        return stored;
    }"""
red = [
    "presentation::managers::tests::test_a_day_of_a_series_opened_and_not_typed_in_is_not_counted_as_a_change",
    "presentation::managers::tests::test_an_occurrence_exception_that_was_not_really_changed_is_handed_back_untouched",
    "presentation::managers::tests::test_saving_an_event_nobody_changed_leaves_it_settled",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── Which day of a series was open ──────────────────────────────────────────
#
# Every day of a repeating event is one stored row, and the editor is filled
# from the day on the screen. Saved against the stored row instead, the
# difference between the two was read as a date somebody had typed: opening the
# fortieth Tuesday and correcting a spelling moved the series onto that Tuesday
# and the first thirty-nine were gone, with nothing said.

[[guard]]
name = "a repeating event is saved against the day that was opened"
file = "src/presentation/managers.rs"
before = """    let in_the_series_frame = as_if_the_series_start_had_been_shown(opened, &stored, data);
    let edited = event_entry(stored.id.clone(), &stored.account_id, &in_the_series_frame);"""
after = """    let edited = event_entry(stored.id.clone(), &stored.account_id, data);"""
red = [
    "presentation::managers::tests::test_correcting_a_spelling_on_a_repeating_event_keeps_the_offset",
    "presentation::managers::tests::test_moving_a_whole_series_to_another_day_moves_every_day_by_the_same_much",
    "presentation::managers::tests::test_moving_the_time_of_a_whole_series_leaves_the_day_it_starts_on_alone",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: two more tests went red than this named,
# both of them the calendar-server half of a rule the record already carried
# for Google and for Outlook.
name = "one day taken off a series joins the days it had already taken off"
file = "src/application/calendar.rs"
before = """        exception_dates: Some(all),"""
after = """        exception_dates: series.exception_dates.clone(),"""
red = [
    "application::caldav_sync::tests::test_a_caldav_series_and_its_moved_day_in_one_resource_leaves_two_linked_rows",
    "application::caldav_sync::tests::test_a_day_cancelled_at_a_calendar_server_is_taken_off_the_series_here",
    "application::caldav_sync::tests::test_a_day_moved_at_a_calendar_server_and_then_cancelled_leaves_nothing_behind",
    "application::calendar::tests::test_a_day_called_off_is_written_the_way_the_start_it_came_from_is_written",
    "application::calendar::tests::test_a_day_cancelled_at_google_is_taken_off_the_series_here",
    "application::calendar::tests::test_a_day_cancelled_at_google_stays_off_when_the_next_read_names_only_the_series",
    "application::calendar::tests::test_a_day_cancelled_at_outlook_is_taken_off_the_series_here",
    "application::calendar::tests::test_a_day_cancelled_at_outlook_stays_off_if_a_later_read_answers_with_the_series_itself",
    "application::calendar::tests::test_a_day_moved_at_google_and_then_cancelled_leaves_nothing_behind",
    "application::calendar::tests::test_a_day_moved_at_google_is_one_meeting_at_its_new_time",
    "application::calendar::tests::test_a_day_of_a_series_is_folded_in_even_when_google_names_it_first",
    "application::calendar::tests::test_a_series_with_a_change_waiting_here_keeps_waiting_when_google_calls_a_day_off",
    "application::calendar::tests::test_an_outlook_series_with_a_change_waiting_here_keeps_waiting_when_outlook_calls_a_day_off",
    "application::calendar::tests::test_calling_a_day_off_a_row_written_the_old_way_leaves_one_reading_of_it",
    "application::calendar::tests::test_calling_off_a_day_already_called_off_does_not_name_it_twice",
    "application::calendar::tests::test_calling_off_one_day_keeps_the_days_the_series_had_already_called_off",
    "application::calendar::tests::test_calling_off_one_day_takes_that_day_off_the_series_and_leaves_the_others",
    "application::calendar::tests::test_the_changed_days_of_a_google_series_are_counted_once_and_not_again",
    "application::calendar::tests::test_the_changed_days_of_an_outlook_series_are_counted_once_and_not_again",
    "application::calendar::tests::test_who_took_the_day_out_decides_whether_the_series_is_still_waiting_to_be_sent",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_a_series_kept_only_here_is_not_refused_over_its_zone",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_taken_off_a_series_is_no_longer_shown_and_the_others_still_are",
    "presentation::managers::changing_one_day_of_a_series::test_changing_one_day_stores_the_changed_day_and_the_series_with_that_day_called_off",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a day called off is written the way the start it came from is written"
file = "src/service/caldav.rs"
before = """    if is_all_day {
        denormalize_ical_date(the_day_opened)
    } else {
        denormalize_ical_datetime(the_day_opened)
    }"""
after = """    denormalize_ical_datetime(the_day_opened)"""
red = [
    "application::calendar::tests::test_a_day_called_off_is_written_the_way_the_start_it_came_from_is_written",
    "service::caldav::tests::test_a_called_off_day_is_written_by_the_same_routine_as_the_start",
    "service::caldav::tests::test_a_day_called_off_lands_on_the_line_its_own_form_belongs_on",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "the question about which days focuses the answer it ticks"
file = "src/presentation/wx_which_days.rs"
before = """    if let Some((_, ticked)) = buttons.iter().find(|(_, button)| button.get_value()) {
        ticked.set_focus();
    }"""
after = """    if let Some((_, first)) = buttons.first() {
        first.set_focus();
    }"""
red = [
    "presentation::wx_calendar::tests::test_the_window_puts_focus_on_the_answer_it_ticks",
]
tests_last_seen = [
    { file = "src/presentation/wx_calendar.rs", tests = 15 },
    { file = "src/presentation/wx_which_days.rs", tests = 0 },
]

[[guard]]
name = "Enter does not answer the question about which days"
file = "src/presentation/wx_which_days.rs"
before = """    cancel.set_default();"""
after = """    carry_on.set_default();"""
red = [
    "presentation::wx_calendar::tests::test_the_window_does_not_let_enter_carry_the_question_out",
]
tests_last_seen = [
    { file = "src/presentation/wx_calendar.rs", tests = 15 },
    { file = "src/presentation/wx_which_days.rs", tests = 0 },
]

# ── The zone a moment was in, across an edit ────────────────────────────────
#
# The event editor shows ten characters of date and five of clock and has
# nowhere to put a zone. Rebuilding an edited event from those two boxes wrote
# "2026-07-27 09:00" over "2026-07-27T09:00:00+05:30", and the Google writer,
# with nothing left saying which zone that clock face meant, read it as a time
# on this computer: correcting a spelling moved a nine o'clock meeting in
# Kolkata to nine o'clock here, nine and a half hours out, in somebody's real
# calendar.

[[guard]]
name = "an edited event's times come from the record, not from the two boxes"
file = "src/presentation/managers.rs"
before = """    let start = moment_after_the_edit(
        &shown.start,
        &edited.start_datetime,
        &stored.start_datetime,
        named_zone,
    );
    let end = moment_after_the_edit(
        &shown.end,
        &edited.end_datetime,
        &stored.end_datetime,
        named_zone,
    );"""
after = """    let start = edited.start_datetime.clone();
    let end = edited.end_datetime.clone();"""
red = [
    "presentation::managers::tests::test_a_zone_name_that_names_nothing_does_not_get_to_decide",
    "presentation::managers::tests::test_correcting_a_spelling_keeps_a_clock_face_and_the_zone_it_is_named_in",
    "presentation::managers::tests::test_correcting_a_spelling_keeps_a_moment_written_in_universal_time",
    "presentation::managers::tests::test_correcting_a_spelling_keeps_the_moment_to_the_second",
    "presentation::managers::tests::test_correcting_a_spelling_keeps_the_offset_the_provider_sent",
    "presentation::managers::tests::test_correcting_a_spelling_on_a_calendar_server_event_keeps_it_in_universal_time",
    "presentation::managers::tests::test_correcting_a_spelling_on_a_repeating_event_keeps_the_offset",
    "presentation::managers::tests::test_correcting_a_spelling_on_a_whole_day_event_keeps_the_dates_it_arrived_with",
    "presentation::managers::tests::test_moving_a_whole_series_to_another_day_moves_every_day_by_the_same_much",
    "presentation::managers::tests::test_moving_an_event_named_in_a_zone_keeps_the_name_and_moves_the_clock",
    "presentation::managers::tests::test_moving_an_event_that_names_a_zone_across_summer_time_keeps_the_clock_face",
    "presentation::managers::tests::test_moving_an_event_to_a_different_time_moves_it_in_the_zone_it_is_in",
    "presentation::managers::tests::test_moving_an_event_with_an_offset_and_no_zone_name_keeps_the_offset",
    "presentation::managers::tests::test_moving_an_event_written_in_universal_time_keeps_it_there",
    "presentation::managers::tests::test_moving_an_events_time_leaves_a_moment_that_is_read_out_as_a_date",
    "presentation::managers::tests::test_moving_the_time_of_a_whole_series_leaves_the_day_it_starts_on_alone",
    "presentation::managers::tests::test_putting_a_time_on_a_whole_day_event_does_not_borrow_midnights_zone",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a box nobody typed in keeps the moment the provider sent"
file = "src/presentation/managers.rs"
before = """    if as_the_editor_reads_it(shown) == as_the_editor_reads_it(rebuilt) {
        return stored.to_string();
    }"""
after = """    if false {
        return stored.to_string();
    }"""
red = [
    "presentation::managers::tests::test_correcting_a_spelling_keeps_the_moment_to_the_second",
    "presentation::managers::tests::test_correcting_a_spelling_on_a_whole_day_event_keeps_the_dates_it_arrived_with",
    "presentation::managers::tests::test_correcting_a_spelling_on_an_event_made_here_leaves_its_time_alone",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a clock face somebody moved keeps the zone the event was in"
file = "src/presentation/managers.rs"
before = """    let marker = match crate::common::moment::the_zone_named(named_zone) {
        Some(_) => "",
        None => zone_marker(shown),
    };"""
after = """    let marker = "";"""
# The repeating-event spelling test used to be on this list and is not any
# more. It opens on one day of a series and types nothing but the summary, so
# now that the day it was opened on reaches the save, both date boxes read back
# as they were filled and the stored moment is handed back whole. It never
# reaches the zone marker at all.
red = [
    "presentation::managers::tests::test_a_zone_name_that_names_nothing_does_not_get_to_decide",
    "presentation::managers::tests::test_moving_a_whole_series_to_another_day_moves_every_day_by_the_same_much",
    "presentation::managers::tests::test_moving_an_event_to_a_different_time_moves_it_in_the_zone_it_is_in",
    "presentation::managers::tests::test_moving_an_event_with_an_offset_and_no_zone_name_keeps_the_offset",
    "presentation::managers::tests::test_moving_an_event_written_in_universal_time_keeps_it_there",
    "presentation::managers::tests::test_moving_the_time_of_a_whole_series_leaves_the_day_it_starts_on_alone",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a zone named on the event beats an offset frozen on another date"
file = "src/presentation/managers.rs"
before = """    let marker = match crate::common::moment::the_zone_named(named_zone) {
        Some(_) => "",
        None => zone_marker(shown),
    };"""
after = """    let marker = zone_marker(shown);"""
red = [
    "presentation::managers::tests::test_moving_an_event_that_names_a_zone_across_summer_time_keeps_the_clock_face",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "an empty zone name on an edited event names nothing"
file = "src/presentation/managers.rs"
before = """    let marker = match crate::common::moment::the_zone_named(named_zone) {
        Some(_) => "",
        None => zone_marker(shown),
    };"""
after = """    let marker = match named_zone {
        Some(_) => "",
        None => zone_marker(shown),
    };"""
red = [
    "presentation::managers::tests::test_a_zone_name_that_names_nothing_does_not_get_to_decide",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# The same question asked of the two writers rather than of the editor. A clock
# face with no zone stored beside it is one this program's own editor wrote, so
# it means an hour on this computer. Google's converter read it that way and
# Graph's called it universal time, and the test named for the two agreeing
# checked the shape of each and never compared them.
#
# ── A runner cannot judge this record, and that is written here rather than
#    only at the top of the file, where the last two sweeps did not read it ──
#
# What a runner cannot see: its clock is UTC, so `chrono::Local` and
# `chrono::Utc` are the same conversion there and the break changes nothing.
# On a machine set to any other zone the two differ and the break reddens.
#
# Green on a runner twice, and reported as a finding both times: run
# 34965790937 of 2026-09-15 and run 35520204784 of 2026-09-20, a build and a
# run each.
#
# Red here on 2026-09-22, measured by 12-02.1 with
# `scripts/guards.sh --wait-until-quiet`: "all 2 tests named went red, and
# nothing else did", rebuild 44 s and run 49 s. The record is right and the
# runner is what cannot read it. The sweep of 2026-09-15 said this would be
# written on each of the two such records and it was written at the top of the
# file instead, which is why the next sweep found them again.

[[guard]]
name = "an hour with no zone means an hour here, to Outlook as well as to Google"
file = "src/application/calendar.rs"
before = """                let here = chrono::Local.from_local_datetime(&clock).earliest()?;
                return Some(MsDateTimeTimeZone {
                    date_time: here
                        .with_timezone(&chrono::Utc)
                        .format(GRAPH_WALL_CLOCK)
                        .to_string(),
                    time_zone: COORDINATED_UNIVERSAL_TIME.to_string(),
                });"""
after = """                return Some(MsDateTimeTimeZone {
                    date_time: clock.format(GRAPH_WALL_CLOCK).to_string(),
                    time_zone: COORDINATED_UNIVERSAL_TIME.to_string(),
                });"""
red = [
    "application::calendar::tests::test_a_zone_name_that_names_nothing_sends_both_providers_the_same_hour",
    "application::calendar::tests::test_the_two_providers_are_given_the_same_hour_in_the_shape_each_one_reads",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# -- The shapes a stored moment can take ------------------------------------
#
# One column in one table, written five ways by four sources, and read by the
# announcement, by the two provider writers, by the calendar-server sync and by
# the reminder check. Three of those kept a list of the shapes and each knew a
# different subset. The two the reader was missing are the two Microsoft Graph
# writes, so every Outlook event was handed to the announcement as the stored
# string, and nothing was red: an unreadable moment comes back exactly as it was
# stored, which is the right fallback and looks the same as a date nobody has
# checked.
#
# The break takes the two shapes written with a T back out. Thirty-four tests in
# twelve modules go red, which is the shape of the thing: one list, read
# everywhere.
#
# That sentence said fourteen tests in six modules until 2026-09-02, and the
# count is corrected here from the run of 2026-09-01 rather than from a hand
# break of its own. Twenty more tests had arrived since the record was written,
# and where they arrived is the finding rather than how many: undo send, free
# and busy, the task writer and the spoken hour all read a stored moment
# through this one list, and none of the four is a subject anybody writing this
# record would have filtered for. Nothing here is a break that reaches too far.
# The list is the rule, so a reader that stops knowing a shape stops knowing it
# for every caller at once, and the only honest record of that is the whole set.

[[guard]]
name = "a moment written with a T is one the reader knows"
file = "src/common/moment.rs"
before = """const CLOCK_FACES: [&str; 4] = [
    "%Y-%m-%dT%H:%M:%S%.f",
    "%Y-%m-%dT%H:%M",
    "%Y-%m-%d %H:%M:%S%.f",
    "%Y-%m-%d %H:%M",
];"""
after = """const CLOCK_FACES: [&str; 2] = [
    "%Y-%m-%d %H:%M:%S%.f",
    "%Y-%m-%d %H:%M",
];"""
red = [
    "application::caldav_sync::tests::test_what_an_answer_covers_decides_which_absences_mean_anything",
    "application::calendar::tests::test_a_new_series_near_midnight_tells_outlook_one_day_for_the_rule_and_for_the_meeting",
    "application::calendar::tests::test_a_repeat_outlook_could_not_be_told_is_said_rather_than_left_to_be_found",
    "application::calendar::tests::test_a_repeat_outlook_sends_is_stored_as_a_rule_and_not_as_a_page_of_its_own_shape",
    "application::calendar::tests::test_a_repeating_event_made_here_reaches_outlook_carrying_its_repeat_rule",
    "application::calendar::tests::test_a_series_created_at_outlook_is_one_meeting_here_after_the_pull_that_follows",
    "application::calendar::tests::test_an_event_filed_under_several_categories_in_outlook_keeps_all_of_them",
    "application::calendar::tests::test_every_field_the_event_form_can_set_is_answered_for_at_both_providers",
    "application::calendar::tests::test_every_shape_a_stored_time_comes_in_reaches_microsoft_readable",
    "application::calendar::tests::test_the_date_of_the_month_outlook_is_told_is_the_date_the_meeting_lands_on",
    "application::calendar::tests::test_the_day_outlook_is_told_a_series_starts_is_the_day_it_is_told_the_meeting_starts",
    "application::due::tests::test_a_reminder_goes_off_whichever_shape_its_time_was_stored_in",
    "application::repeating::tests::test_every_repeat_this_program_offers_survives_the_trip_to_outlook_and_back",
    "application::sending_later::what_undo_send_is_about::test_a_message_whose_time_cannot_be_read_can_still_be_taken_back",
    "application::sending_later::what_undo_send_is_about::test_an_empty_queue_and_a_queue_nothing_can_be_caught_in_are_different_answers",
    "application::sending_later::what_undo_send_is_about::test_the_message_that_can_be_taken_back_gives_no_refusal",
    "application::sending_later::what_undo_send_is_about::test_undo_send_takes_back_the_message_most_recently_sent",
    "application::when_people_are_free::tests::test_a_timed_event_blocks_the_instants_its_own_zone_puts_it_at",
    "application::when_people_are_free::tests::test_an_event_naming_a_zone_nobody_knows_is_read_where_the_person_is",
    "common::moment::tests::test_a_fraction_of_a_second_is_read_whichever_separator_it_uses",
    "common::moment::tests::test_a_value_padded_with_spaces_is_still_the_moment_it_names",
    "common::moment::tests::test_an_offset_is_not_mistaken_for_a_clock_face",
    "common::moment::tests::test_every_zoneless_shape_the_cache_holds_is_read_as_a_clock_face",
    "common::moment::tests::test_the_day_a_moment_falls_on_is_read_from_every_shape",
    "presentation::date_display::tests::test_the_hour_spoken_comes_from_the_parsed_moment_not_the_characters",
    "presentation::managers::tests::test_correcting_a_spelling_keeps_a_clock_face_and_the_zone_it_is_named_in",
    "presentation::managers::tests::test_every_moment_the_editor_writes_is_a_shape_the_shared_reader_knows",
    "presentation::managers::tests::test_moving_an_event_that_names_a_zone_across_summer_time_keeps_the_clock_face",
    "presentation::managers::tests::test_moving_an_events_time_leaves_a_moment_that_is_read_out_as_a_date",
    "presentation::read_aloud::tests::test_every_shape_a_stored_moment_takes_is_read_as_a_date",
    "service::caldav::tests::test_a_moment_moved_by_a_duration_keeps_the_shape_normalize_ical_datetime_gives_it",
    "service::caldav::tests::test_a_real_radicale_master_and_override_in_a_legacy_iana_zone_alias_read_correctly",
    "service::caldav::tests::test_a_whole_day_event_whose_column_holds_an_hour_still_goes_out_as_a_day",
    "service::tasks_api::tests::test_a_completion_time_microsoft_gave_us_goes_back_as_the_same_hour",
    # Added 2026-09-15 by 08-07's sweep: the due window's sentences, written
    # 2026-09-14 in a file this record had never named, read a moment with a T.
    "presentation::wx_reminder_alert::tests::test_one_row_is_said_as_its_own_sentence_exactly_as_before",
    "presentation::wx_reminder_alert::tests::test_several_rows_are_said_as_a_count_then_each_then_how_many_more",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/due.rs", tests = 46 },
    { file = "src/application/repeating.rs", tests = 27 },
    { file = "src/application/sending_later.rs", tests = 47 },
    { file = "src/application/when_people_are_free.rs", tests = 47 },
    { file = "src/common/moment.rs", tests = 10 },
    { file = "src/presentation/date_display.rs", tests = 47 },
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/presentation/read_aloud.rs", tests = 51 },
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
    { file = "src/service/caldav.rs", tests = 170 },
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# -- A whole day sent to a calendar server is a day ------------------------
#
# A whole-day event that came from Google or from Graph keeps the day in its
# date columns and midnight in its datetime column, and the datetime column is
# the one this writer is given. Taking the dashes out of that produced
# DTSTART;VALUE=DATE:20260727T00:00:00Z, which is not a date, and a server that
# checks what it is sent refuses the whole change. Nothing carries such an event
# here today: the only route is moving it into a calendar server's calendar and
# presentation::managers::moving_can_be_told refuses to move any event a
# provider already holds. The guard is here so that removing that gate does not
# quietly open this.

[[guard]]
name = "a whole day goes to a calendar server as a day"
file = "src/service/caldav.rs"
before = """        (
            denormalize_ical_date(&event.dtstart),
            ";VALUE=DATE".to_string(),
        )"""
after = """        (event.dtstart.replace('-', ""), ";VALUE=DATE".to_string())"""
red = [
    "service::caldav::tests::test_a_whole_day_event_whose_column_holds_an_hour_still_goes_out_as_a_day",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

# -- What a zone name of no letters means -----------------------------------
#
# Four writers read the zone-name column to decide where a clock face stands:
# the Google converter, the Graph converter, the event editor and the calendar
# server's document writer. Each answered "is this name a name" for itself and
# they gave three different answers. Google asked is_empty with no trim, so a
# name of one space was a zone; Graph trimmed, so the same event went to the two
# providers five and a half hours apart. The calendar server's writer asked
# nothing and wrote `DTSTART;TZID=:20260305T090000`, which is not a calendar
# document.
#
# The break makes a blank name a name again. Eight tests in four modules go red,
# which is the shape of the thing: one answer, read by every writer.

[[guard]]
name = "a zone name of no letters names no zone"
file = "src/common/moment.rs"
before = """    stored.map(str::trim).filter(|named| !named.is_empty())"""
after = """    stored"""
red = [
    "application::calendar::tests::test_a_whole_day_event_is_not_given_a_zone_name_that_names_nothing",
    "application::calendar::tests::test_a_zone_name_that_names_nothing_sends_both_providers_the_same_hour",
    "application::calendar::tests::test_an_event_stored_with_no_zone_worth_the_name_is_sent_as_a_moment",
    "common::moment::tests::test_a_zone_name_of_nothing_but_space_names_no_zone",
    "common::moment::tests::test_a_zone_name_with_space_round_it_is_still_the_zone_it_names",
    "presentation::managers::tests::test_a_zone_name_that_names_nothing_does_not_get_to_decide",
    "service::caldav::tests::test_a_zone_name_that_names_nothing_is_left_off_the_document",
    "service::caldav::tests::test_a_zone_name_written_with_spaces_round_it_still_names_its_zone",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/common/moment.rs", tests = 10 },
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/service/caldav.rs", tests = 170 },
]

# -- Which addresses say two records are one person -------------------------
#
# A contact card carries no identifier this application keeps, and neither does
# an address book's answer about somebody it has never handed this computer an
# identifier for. An address is the whole of what matches the two, so the rule
# for reading an address is load bearing in a way it does not look. Asked of
# the main line alone, and letter for letter, one person became two rows, one
# of which was still joined to Google and Outlook and one of which was then
# offered to them as somebody new.

[[guard]]
name = "an address is matched however it is spelled"
file = "src/data/message_cache/mod.rs"
before = """        if self.email.trim().eq_ignore_ascii_case(looking_for) {
            return true;
        }
        self.every_address_in_the_list()
            .iter()
            .any(|held| held.address.trim().eq_ignore_ascii_case(looking_for))
    }"""
after = """        if self.email.trim() == looking_for {
            return true;
        }
        self.every_address_in_the_list()
            .iter()
            .any(|held| held.address.trim() == looking_for)
    }"""
red = [
    "application::contacts_sync::tests::test_a_person_the_address_book_spells_in_capitals_is_the_one_here",
    "data::message_cache::contacts::tests::test_a_card_repeating_the_addresses_already_held_changes_nothing_about_them",
    "data::message_cache::contacts::tests::test_a_card_whose_address_is_written_in_capitals_is_the_same_person",
    "data::message_cache::contacts::tests::test_two_cards_one_address_each_are_one_person_holding_both_addresses",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
name = "every address a contact holds is hers"
file = "src/data/message_cache/mod.rs"
before = """        self.every_address_in_the_list()
            .iter()
            .any(|held| held.address.trim().eq_ignore_ascii_case(looking_for))
    }"""
after = """        false
    }"""
# It reaches further than it did. Joining a card's addresses onto the ones
# already held asks this same question of every address on the card, so a
# reading that sees only the main line now also relabels and duplicates
# addresses on an import that should have changed nothing.
red = [
    "application::contacts_sync::tests::test_a_person_the_address_book_writes_to_at_her_second_address_is_the_one_here",
    "data::message_cache::contacts::tests::test_a_card_carrying_somebodys_second_address_is_that_person",
    "data::message_cache::contacts::tests::test_a_card_repeating_the_addresses_already_held_changes_nothing_about_them",
    "data::message_cache::contacts::tests::test_importing_a_card_does_not_forget_a_change_still_waiting_to_be_sent",
    "data::message_cache::contacts::tests::test_importing_a_card_that_changed_nothing_queues_nothing",
    "data::message_cache::contacts::tests::test_two_cards_one_address_each_are_one_person_holding_both_addresses",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
name = "the address the other copy keeps second is asked about too"
file = "src/data/message_cache/mod.rs"
before = """        other
            .every_address_in_the_list()
            .iter()
            .any(|entry| self.is_written_to_at(&entry.address))
    }"""
after = """        false
    }"""
red = [
    "application::contacts_sync::tests::test_a_person_here_at_the_address_the_address_book_keeps_second_is_the_same_person",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

# The coupling rather than the rule. The importer and the sync both have to ask
# the one question, and the sync is where asking a narrower one duplicates
# somebody in a real address book rather than only here.

[[guard]]
name = "the sync asks the same question about an address as the import"
file = "src/application/contacts_sync.rs"
before = """        .find(|c| c.shares_an_address_with(arriving) && c.id_in(address_book).is_none())"""
after = """        .find(|c| c.email == arriving.email && c.id_in(address_book).is_none())"""
red = [
    "application::contacts_sync::tests::test_a_person_here_at_the_address_the_address_book_keeps_second_is_the_same_person",
    "application::contacts_sync::tests::test_a_person_the_address_book_spells_in_capitals_is_the_one_here",
    "application::contacts_sync::tests::test_a_person_the_address_book_writes_to_at_her_second_address_is_the_one_here",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# -- An imported card is a change, and only when it is one ------------------
#
# Two halves that pull against each other. What a card says has to reach the
# address books that hold the person, or the next read writes over it and
# nothing says so. What a card does not change must not be sent, or reading a
# backup of this address book pushes every contact in it.

[[guard]]
name = "an imported card is queued for the address books that hold her"
file = "src/data/message_cache/contacts.rs"
before = """    fn waiting_for_the_address_books(mut contact: ContactEntry) -> ContactEntry {
        contact.pending = true;
        contact.last_synced_at = None;
        for identity in &mut contact.known_to {
            identity.change_is_waiting = true;
        }
        contact"""
after = """    fn waiting_for_the_address_books(contact: ContactEntry) -> ContactEntry {
        contact"""
red = [
    "application::contacts_sync::tests::test_what_an_imported_card_says_reaches_the_address_book_that_holds_her",
    "data::message_cache::contacts::tests::test_a_card_for_somebody_new_is_work_waiting_rather_than_a_copy_of_an_address_book",
    "data::message_cache::contacts::tests::test_a_card_naming_an_address_she_does_not_hold_yet_adds_it_to_the_ones_she_has",
    "data::message_cache::contacts::tests::test_a_card_that_does_change_a_contact_stored_by_an_older_version_is_queued",
    "data::message_cache::contacts::tests::test_importing_a_card_queues_its_contents_for_every_address_book_that_holds_her",
    "data::message_cache::contacts::tests::test_two_cards_for_somebody_nobody_here_holds_are_one_person",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "an import that changed nothing is not queued"
file = "src/data/message_cache/contacts.rs"
before = """        Self::what_the_record_says(folded) != Self::what_the_record_says(held)"""
after = """        let _ = (folded, held);
        true"""
red = [
    "application::contacts_sync::tests::test_an_import_that_changed_nobody_sends_nothing_to_the_address_book",
    "data::message_cache::contacts::tests::test_a_card_repeating_the_addresses_already_held_changes_nothing_about_them",
    "data::message_cache::contacts::tests::test_importing_a_card_does_not_forget_a_change_still_waiting_to_be_sent",
    "data::message_cache::contacts::tests::test_importing_a_card_that_changed_nothing_queues_nothing",
    "data::message_cache::contacts::tests::test_re_importing_a_backup_does_not_queue_a_contact_stored_by_an_older_version",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# Measured again when a card's addresses started joining onto the ones already
# held, and it now defends something narrower than it did. It used to be the
# reason a backup of an older row read as no change: the card's list replaced
# the stored nothing, so the two records only matched through this stand-in.
# The list joins now, and a card saying nothing new hands the stored value back
# untouched, so those two records match without it. What is left is the join
# itself, which reads this to learn which addresses a row already holds. Both
# named tests that used to redden here stay green, measured, and the record was
# cut to what really fails.

[[guard]]
name = "an address with no list beside it is the address it is"
file = "src/data/message_cache/contacts.rs"
before = """            emails_json: contact.emails_json.clone().or_else(|| {
                serde_json::to_string(&[super::EmailEntry {
                    label: Self::NO_LABEL.to_string(),
                    address: contact.email.clone(),
                    name: String::new(),
                }])
                .ok()
            }),"""
after = """            emails_json: contact.emails_json.clone(),"""
red = [
    "data::message_cache::contacts::tests::test_a_card_adding_an_address_to_a_row_with_no_list_keeps_the_one_on_its_main_line",
    "data::message_cache::contacts::tests::test_a_second_card_joins_the_person_the_first_card_matched",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# -- What a card may do to the addresses somebody already holds --------------
#
# The list of addresses is the one list an import joins onto rather than
# replaces, because an address is the whole of what says two records are one
# person. Several address books export one card per address, so a card that
# replaced the list left her holding whichever address the first card named,
# the second card matched nobody, and she became two contacts at a real address
# book with one address between them. Both halves of the matching rule above
# worked on their own and this is where they failed to compose.

[[guard]]
name = "a card adds an address and takes none away"
file = "src/data/message_cache/contacts.rs"
before = """        let every_address = Self::every_address_she_has_after_the_card(&from_card, held);"""
after = """        let every_address = from_card.emails_json.clone().or_else(|| held.emails_json.clone());"""
red = [
    "data::message_cache::contacts::tests::test_a_card_matched_by_address_keeps_the_addresses_it_is_silent_about",
    "data::message_cache::contacts::tests::test_a_card_naming_an_address_she_does_not_hold_yet_adds_it_to_the_ones_she_has",
    "data::message_cache::contacts::tests::test_two_cards_for_somebody_nobody_here_holds_are_one_person",
    "data::message_cache::contacts::tests::test_a_card_naming_one_address_twice_adds_it_once",
    "data::message_cache::contacts::tests::test_a_card_repeating_the_addresses_already_held_changes_nothing_about_them",
    "data::message_cache::contacts::tests::test_an_address_the_card_adds_keeps_the_label_the_card_gave_it",
    "data::message_cache::contacts::tests::test_two_cards_one_address_each_are_one_person_holding_both_addresses",
    "data::message_cache::contacts::tests::test_a_second_card_joins_the_person_the_first_card_matched",
    "data::message_cache::contacts::tests::test_importing_a_card_does_not_forget_a_change_still_waiting_to_be_sent",
    "data::message_cache::contacts::tests::test_importing_a_card_that_changed_nothing_queues_nothing",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# The other direction. Joining has its own way of going wrong: an address she
# already holds written down a second time, in whatever case the card's writer
# chose and with the label it did not carry, and then sent to Google and to
# Outlook as a change nobody made.

[[guard]]
name = "an address she already holds is not written down again"
file = "src/data/message_cache/contacts.rs"
before = """        let mut new_to_her: Vec<super::EmailEntry> = Vec::new();
        for arriving in from_card.every_address_in_the_list() {
            // Asked of what this card has already added as well as of what she
            // is stored as being written to at. A card written by a program
            // that merged two records names one address on two lines, and not
            // always spelled the same way, and asking only the stored list
            // writes it down twice.
            let hers = held.is_written_to_at(&arriving.address)
                || new_to_her.iter().any(|added| {
                    added
                        .address
                        .trim()
                        .eq_ignore_ascii_case(arriving.address.trim())
                });
            if !hers {
                new_to_her.push(arriving);
            }
        }"""
after = """        let new_to_her: Vec<super::EmailEntry> = from_card.every_address_in_the_list();"""
red = [
    "application::contacts_sync::tests::test_an_import_that_changed_nobody_sends_nothing_to_the_address_book",
    "data::message_cache::contacts::tests::test_a_card_adding_an_address_to_a_row_with_no_list_keeps_the_one_on_its_main_line",
    "data::message_cache::contacts::tests::test_a_card_matched_by_address_keeps_the_addresses_it_is_silent_about",
    "data::message_cache::contacts::tests::test_a_card_naming_an_address_she_does_not_hold_yet_adds_it_to_the_ones_she_has",
    "data::message_cache::contacts::tests::test_a_card_naming_one_address_twice_adds_it_once",
    "data::message_cache::contacts::tests::test_a_card_repeating_the_addresses_already_held_changes_nothing_about_them",
    "data::message_cache::contacts::tests::test_an_address_the_card_adds_keeps_the_label_the_card_gave_it",
    "data::message_cache::contacts::tests::test_importing_a_card_does_not_forget_a_change_still_waiting_to_be_sent",
    "data::message_cache::contacts::tests::test_importing_a_card_that_changed_nothing_queues_nothing",
    "data::message_cache::contacts::tests::test_re_importing_a_backup_does_not_queue_a_contact_stored_by_an_older_version",
    "data::message_cache::contacts::tests::test_two_cards_one_address_each_are_one_person_holding_both_addresses",
    "data::message_cache::contacts::tests::test_a_second_card_joins_the_person_the_first_card_matched",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# The narrow half of the same rule, and the one found by trying to break the
# wide half with a card nobody here had written yet. A card from a program that
# merged two records names one address on two lines, sometimes spelled two
# ways. Asked only about the stored list, both lines are new to her.

[[guard]]
name = "an address the card names twice is added once"
file = "src/data/message_cache/contacts.rs"
before = """            let hers = held.is_written_to_at(&arriving.address)
                || new_to_her.iter().any(|added| {
                    added
                        .address
                        .trim()
                        .eq_ignore_ascii_case(arriving.address.trim())
                });"""
after = """            let hers = held.is_written_to_at(&arriving.address);"""
red = [
    "data::message_cache::contacts::tests::test_a_card_naming_one_address_twice_adds_it_once",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# The card is the only thing that knows anything about an address nobody here
# has recorded, so the label it carries is the only label there is.

[[guard]]
name = "an address the card adds keeps the label the card gave it"
file = "src/data/message_cache/contacts.rs"
before = """                new_to_her.push(arriving);"""
after = """                new_to_her.push(super::EmailEntry {
                    label: Self::NO_LABEL.to_string(),
                    address: arriving.address,
                    name: arriving.name,
                });"""
red = [
    "data::message_cache::contacts::tests::test_an_address_the_card_adds_keeps_the_label_the_card_gave_it",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# A row written before the lists existed has its address on its main line and
# no list at all. Joined onto that empty list, the address she is really
# written to at is the one that drops out.

[[guard]]
name = "a row with no address list still brings its own address to the join"
file = "src/data/message_cache/contacts.rs"
before = """        let joined: Vec<super::EmailEntry> = Self::what_the_record_says(held)
            .every_address_in_the_list()"""
after = """        let joined: Vec<super::EmailEntry> = held
            .every_address_in_the_list()"""
red = [
    "data::message_cache::contacts::tests::test_a_card_adding_an_address_to_a_row_with_no_list_keeps_the_one_on_its_main_line",
    "data::message_cache::contacts::tests::test_a_second_card_joins_the_person_the_first_card_matched",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# -- What the first-run screen reads out is what it will do ------------------
#
# The screen ticks one of three answers and puts focus somewhere. While those
# were two decisions they disagreed: focus went to "read my mail, change
# nothing" and the tick to the answer that sends changes to tasks, contacts and
# the calendar up to a provider. Somebody using a screen reader hears the answer
# focus is on, presses Enter on the first thing they hear, and has writing to
# their real address book switched on without being told. Which answer it starts
# on is a separate question and this is not about that.

[[guard]]
name = "the first-run screen focuses the answer it ticks"
file = "src/presentation/wx_first_run.rs"
before = """    if let Some((_, ticked)) = buttons.iter().find(|(_, button)| button.get_value()) {
        ticked.set_focus();
    }"""
after = """    if let Some((_, first)) = buttons.first() {
        first.set_focus();
    }"""
red = [
    "presentation::first_run::tests::test_the_screen_puts_focus_on_the_answer_it_ticks",
]
tests_last_seen = [
    { file = "src/presentation/first_run.rs", tests = 16 },
    { file = "src/presentation/wx_first_run.rs", tests = 0 },
]

# The part of an import that leaves this computer is said at the time rather
# than left to the sync summary afterwards. Afterwards is too late to decide
# against it.

[[guard]]
name = "an import says how many contacts are on their way out"
file = "src/application/importing_contacts.rs"
before = """    if read.waiting_to_be_sent > 0 {"""
after = """    if false {"""
red = [
    "application::importing_contacts::tests::test_an_import_says_how_many_contacts_are_on_their_way_to_the_address_book",
    "application::importing_contacts::tests::test_one_contact_on_its_way_to_the_address_book_is_said_in_the_singular",
]
tests_last_seen = [
    { file = "src/application/importing_contacts.rs", tests = 9 },
]

[[guard]]
name = "the contacts on their way out are counted as they are written"
file = "src/data/message_cache/contacts.rs"
before = """                    if still_owed {
                        waiting.insert(row);
                    }"""
after = """                    if false {
                        waiting.insert(row);
                    }"""
red = [
    "data::message_cache::contacts::tests::test_a_card_that_does_change_a_contact_stored_by_an_older_version_is_queued",
    "data::message_cache::contacts::tests::test_two_cards_for_somebody_nobody_here_holds_are_one_person",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# -- Two cards in one file for a person nobody here holds -------------------
#
# An address is what says two records are one person, and there is one pair of
# cards it cannot answer for: the two an address book writes when it exports one
# card per address, for somebody this account does not hold yet. Neither card
# names an address the other has and neither matches anything stored, so both
# were written down and both were queued. A first import of an exported address
# book duplicated every person that address book had split across cards.
#
# The break makes the card-against-card question always answer no, which is
# where this started.

[[guard]]
name = "two cards that are one card but for the address are one person"
file = "src/data/message_cache/contacts.rs"
before = """        !arriving.name.trim().is_empty() && earlier == arriving"""
after = """        false"""
red = [
    "data::message_cache::contacts::tests::test_two_cards_for_somebody_nobody_here_holds_are_one_person",
    "data::message_cache::contacts::tests::test_a_second_card_joins_the_person_the_first_card_matched",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# The same rule where somebody looks for a person rather than where two records
# are folded together. The list shows one address per row, so searching only
# that one meant the address you have for somebody found nobody whenever it was
# one of her others.

[[guard]]
name = "the contacts list finds a person by any address she holds"
file = "src/presentation/wx_managers.rs"
before = """        || contact.emails.iter().any(|e| holds(&e.address))
        || contact.phones.iter().any(|p| holds(&p.number))"""
after = """        || holds(contact.primary_email())
        || holds(contact.primary_phone())"""
red = [
    "presentation::wx_managers::tests::test_a_contact_is_found_by_a_number_that_is_not_her_first",
    "presentation::wx_managers::tests::test_a_contact_is_found_by_an_address_that_is_not_her_first",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
]

# The same rule about an address where it decides whether a mailbox is one this
# already holds. Everything stored is filed under an account's identifier, so a
# second account for one mailbox is a second mailbox as far as the rest of the
# program is concerned.

# Re-measured by hand on 2026-09-03, after the 220-record sweep refused it: the
# text the old break replaced appeared 0 times, because a `debug_assert_eq!`
# about flag-bit overlap was added between the call and the combination and the
# expression now goes through a local. The behaviour is unchanged and better
# guarded than it was. Break applied by hand against the whole library: exactly
# the one test named went red and nothing else did.
#
# Worth noting what the sweep bought here. This is the one record of 220 it
# found, and it is the direction that announces itself rather than the silent
# one: the break stops applying and the run says so. The count check cannot see
# it, because a refactor that moves an expression changes no test count.

[[guard]]
name = "a deletion that cannot be undone does not let Enter answer Yes"
file = "src/presentation/asking.rs"
before = """    two_answers | ENTER_DOES_NOT_ANSWER_YES"""
after = """    two_answers"""
red = [
    "presentation::asking::tests::test_a_question_that_cannot_be_undone_does_not_let_enter_answer_yes",
]
tests_last_seen = [
    { file = "src/presentation/asking.rs", tests = 5 },
]

# What each first-run answer costs is the button's description, which is what a
# screen reader reading through Microsoft Active Accessibility says when the
# button takes focus. That is the moment the decision is made. It went missing
# once already: three buttons that read correctly and three explanations of
# what each one costs that nobody ever heard.

[[guard]]
name = "what each first-run answer costs rides on the button"
file = "src/presentation/wx_first_run.rs"
before = """        set_accessible_name_and_description(&button, choice.label(), choice.explanation());"""
after = """        set_accessible_name(&button, choice.label());"""
red = [
    "presentation::first_run::tests::test_what_each_answer_costs_rides_on_the_button_and_on_the_screen",
]
tests_last_seen = [
    { file = "src/presentation/first_run.rs", tests = 16 },
    { file = "src/presentation/wx_first_run.rs", tests = 0 },
]

# The compose tab's signature answer. It was on the screen, hard-set to ticked
# and read back by nothing, for as long as the settings screen has existed, so
# a screen reader announced a setting that was not there. Reading it and then
# handing the composer the stored signature anyway would be the same defect
# with more code in it.

[[guard]]
name = "a composer opens with no signature when somebody asked for none"
file = "src/application/sign_off.rs"
before = """    if automatically { stored } else { "" }"""
after = """    stored"""
red = [
    "application::sign_off::tests::test_a_composer_opens_with_nothing_when_somebody_asked_for_no_signature",
    "application::sign_off::tests::test_a_message_opened_with_no_signature_is_not_signed_at_all",
]
tests_last_seen = [
    { file = "src/application/sign_off.rs", tests = 18 },
]

# ── A day stored without a time is a date, never an hour count ──────────────
#
# The shipped date style is relative, and the one function every list cell,
# reading and reminder alert gets its date wording from measured a whole day
# from its midnight. A task due today was announced "Due: 12 hours ago" at
# noon: the reading calling it overdue. The birthday reading always refused
# this; the rule is now kept for every stored day. The break lets a whole day
# fall through to the relative measurement again, which is where this started.
#
# The call in the break text became `date_part_asking(which, ...)` on
# 2026-09-13, when the month names started coming from the machine and every
# reading here grew a locale to be asked about. Found by
# `test_every_guard_record_still_names_one_place_in_the_tree` rather than by
# anybody noticing: a rename leaves a record that reads as fine and is
# unmeasurable, because `guards.py` refuses a break it cannot find and says so
# as though the tool were broken. Re-measured rather than edited until it
# applied.

[[guard]]
name = "a whole day is spoken as a date under the relative style"
file = "src/presentation/date_display.rs"
before = """    if let Moment::WholeDay(day) = moment {
        return date_part_asking(which, day, settings);
    }"""
after = """    if let (Moment::WholeDay(day), DateStyle::Absolute) = (moment, settings.style) {
        return date_part_asking(which, day, settings);
    }"""
red = [
    "application::due::tests::test_an_alert_for_a_day_reminder_says_the_day_not_an_hour_count",
    "presentation::date_display::tests::test_a_whole_day_in_the_future_stays_a_date_under_the_relative_style",
    "presentation::date_display::tests::test_a_whole_day_under_the_relative_style_is_a_date_and_never_an_hour_count",
    "presentation::read_aloud::tests::test_a_note_updated_on_a_bare_date_is_a_date_under_the_shipped_default",
    "presentation::read_aloud::tests::test_a_reminder_due_on_a_day_is_a_date_not_an_hour_count_under_the_shipped_default",
    "presentation::read_aloud::tests::test_a_task_due_today_is_a_date_not_an_hour_count_under_the_shipped_default",
    "presentation::read_aloud::tests::test_an_event_on_a_bare_date_is_a_date_not_an_hour_count_under_the_shipped_default",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
    { file = "src/presentation/date_display.rs", tests = 47 },
    { file = "src/presentation/read_aloud.rs", tests = 51 },
]

# The hour the calendar's out-of-hours note judges comes from the same parsed,
# locally-converted moment as the time spoken beside it. It used to be picked
# out of the raw characters, which read ninety-nine out of garbage and called
# it "after the working day", and read the stored hour of an offset-carrying
# value while the cell beside it spoke the local one. The break is that sniff
# put back.

[[guard]]
name = "the out-of-hours note judges the hour the cell speaks"
file = "src/presentation/date_display.rs"
before = """pub fn the_hour_spoken(stored: &str) -> Option<u32> {
    use crate::common::moment::Moment;

    match crate::common::moment::read(stored)? {
        Moment::WholeDay(_) => None,
        names_an_hour => local_instant(names_an_hour).map(|when| when.hour()),
    }
}"""
after = """pub fn the_hour_spoken(stored: &str) -> Option<u32> {
    let (_, time) = stored.trim().split_once([' ', 'T'])?;
    time.split(':').next()?.parse().ok()
}"""
red = [
    "presentation::date_display::tests::test_the_hour_spoken_comes_from_the_parsed_moment_not_the_characters",
    "presentation::pim_rows::tests::test_garbage_in_a_start_time_earns_no_working_day_note",
]
tests_last_seen = [
    { file = "src/presentation/date_display.rs", tests = 47 },
    { file = "src/presentation/pim_rows.rs", tests = 25 },
]

# A reminder set for a day goes off at that day's start, which is deliberate,
# and the late flag was measured in minutes, so from one past midnight the
# alert called the whole day overdue. Late is now asked in the granularity the
# reminder was stored in. The break puts the minute rule back on whole days.
#
# The rule moved out of what_is_due into is_late on 2026-09-14, when a due
# thing stopped being only a reminder and the moment a row is late from
# stopped being the moment it was raised at: an event is raised at its start
# less a lead and is late from its start. Same break, new lines, measured
# again that day: the task test the same rule now carries went red beside
# the reminder's, and nothing else did.

[[guard]]
name = "a reminder for a day is not called overdue during that day"
file = "src/application/due.rs"
before = """        Moment::WholeDay(day) => now.date_naive() > day,
        names_an_hour => match local_instant(names_an_hour) {"""
after = """        Moment::WholeDay(day) => match local_instant(Moment::WholeDay(day)) {
            Some(at) => now.signed_duration_since(at) > Duration::minutes(1),
            None => false,
        },
        names_an_hour => match local_instant(names_an_hour) {"""
red = [
    "application::due::tests::test_a_reminder_for_a_day_is_not_called_overdue_during_that_day",
    "application::due::tests::test_a_task_is_late_once_the_day_it_names_has_passed_and_not_before",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
]

# ── A task change no setting let out ────────────────────────────────────────
#
# With Allow Changes off, every pending task write was pushed into `errors`,
# so the status line said "1 problem" on every sync, forever, about a change
# that was simply waiting on a setting, and the sentence that names the
# setting was never said. The calendar and contacts syncs had already been
# through this; the tasks sync is the third module to grow the same defect,
# which is why each piece of the fix is measured here.

[[guard]]
name = "a task deletion held by Allow Changes is a wait, not a problem"
file = "src/application/tasks_sync.rs"
before = """            Err(e) if crate::service::outward::was_refused_by_the_gate(&e) => {
                result.waiting_on_the_setting += 1;
            }
            Err(e) if refused_for_permission(&e) => result.needs_sign_in = true,
            Err(e) => result.errors.push(format!("Deleting a task: {e}")),"""
after = """            Err(e) if refused_for_permission(&e) => result.needs_sign_in = true,
            Err(e) => result.errors.push(format!("Deleting a task: {e}")),"""
red = [
    "application::tasks_sync::tests::test_a_deletion_held_by_allow_changes_waits_rather_than_failing",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "a task change held by Allow Changes is a wait, not a problem"
file = "src/application/tasks_sync.rs"
before = """            Err(e) if crate::service::outward::was_refused_by_the_gate(&e) => {
                result.waiting_on_the_setting += 1;
            }
            Err(e) if refused_for_permission(&e) => result.needs_sign_in = true,
            // The task's id, not its title."""
after = """            Err(e) if refused_for_permission(&e) => result.needs_sign_in = true,
            // The task's id, not its title."""
red = [
    # Added 2026-09-09 by 05-08, found by --remeasure rather than predicted. A
    # move whose create the gate refuses is a change held by the setting like any
    # other, so it counts here and this break turns it into a reported problem.
    "application::tasks_sync::tests::test_a_move_held_by_allow_changes_is_counted_apart_from_one_held_by_the_copy",
    "application::tasks_sync::tests::test_a_task_change_held_by_allow_changes_waits_rather_than_failing",
    "application::tasks_sync::tests::test_turning_allow_changes_on_sends_the_change_the_summary_said_was_waiting",
    "application::tasks_sync::tests::test_two_syncs_with_the_setting_off_both_say_waiting_and_never_error",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "the waiting count survives the two providers being folded together"
file = "src/application/tasks_sync.rs"
before = """        self.local_only += other.local_only;
        self.waiting_on_the_setting += other.waiting_on_the_setting;"""
after = """        self.local_only += other.local_only;"""
red = [
    "application::tasks_sync::tests::test_folding_two_providers_together_keeps_every_count",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "the waiting count is said as a sentence naming the setting"
file = "src/application/tasks_sync.rs"
before = """        if self.waiting_on_the_setting > 0 {"""
after = """        if false {"""
red = [
    "application::tasks_sync::tests::test_a_change_waiting_on_the_setting_is_said_as_a_sentence_not_a_problem",
    # Added 2026-09-09, found by --remeasure rather than predicted. 05-07's test
    # asserts that a deletion held by an outstanding copy is said apart from one
    # held by this setting, so it names this sentence in order to say the two
    # are not the same thing, and losing the sentence reddens it.
    "application::tasks_sync::tests::test_a_deletion_held_by_a_copy_is_said_apart_from_one_held_by_the_setting",
    # Added 2026-09-09 by 05-08, and it is the same shape as the line above one
    # plan later: this one reads the summary while a move is half done and
    # asserts that the sentence naming the setting is there and that the removal
    # count was not folded into it.
    "application::tasks_sync::tests::test_a_move_held_by_allow_changes_is_counted_apart_from_one_held_by_the_copy",
    "application::tasks_sync::tests::test_two_syncs_with_the_setting_off_both_say_waiting_and_never_error",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

# ── The days a series calls off, crossing time zones whole ──────────────────
#
# A cancelled day is one value in a comma-joined column, and each value says
# for itself what it is: an instant in UTC, a clock face in some zone, or a
# bare day. The writer once labelled the whole list by its last value, so one
# order claimed UTC and a zone at once and the other lost the zone entirely;
# the reader once stripped each line's own zone, so a nine in New York was
# stored as a nine in London.
#
# The zone survives even when nothing here can convert it. Outlook and Exchange
# name zones the timezone database does not know, and a value in one of those
# used to be stored as bare digits and sent back dressed in the meeting's zone,
# four to five hours from where the server put it. The value carries its zone
# now, one routine takes it apart and one puts it back, and all four directions
# are measured here.

[[guard]]
name = "cancelled days go out grouped by what each value says"
file = "src/service/caldav.rs"
before = """        match day.form {
            CancelledDayForm::SaysUtc => says_for_itself.push(day.clock_face),
            CancelledDayForm::WholeDay => whole_days.push(day.clock_face),
            CancelledDayForm::ClockFace => {
                let under = day.its_own_zone.or(zone);
                match clock_faces.iter_mut().find(|(named, _)| *named == under) {
                    Some((_, faces)) => faces.push(day.clock_face),
                    None => clock_faces.push((under, vec![day.clock_face])),
                }
            }
        }"""
after = """        let under = day.its_own_zone.or(zone);
        match clock_faces.iter_mut().find(|(named, _)| *named == under) {
            Some((_, faces)) => faces.push(day.clock_face),
            None => clock_faces.push((under, vec![day.clock_face])),
        }"""
red = [
    "service::caldav::tests::test_a_birthday_sent_to_a_calendar_server_still_happens_every_year",
    "service::caldav::tests::test_a_cancelled_day_in_utc_gets_no_zone_even_when_the_meeting_it_belongs_to_has_one",
    "service::caldav::tests::test_a_day_called_off_lands_on_the_line_its_own_form_belongs_on",
    "service::caldav::tests::test_a_mixed_list_of_cancelled_days_splits_by_what_each_value_says",
    "service::caldav::tests::test_a_time_stored_with_a_small_z_still_goes_back_out_saying_it_is_utc",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

# The column holds two shapes, and a comma means something different in each.
# Deciding which shape it is before splitting is the whole of the rule, and
# splitting first is how the zone written once at the front of a row stopped
# reaching the days named after the first comma.

[[guard]]
name = "a whole property line is told from a list of values before either is split"
file = "src/service/caldav.rs"
before = """    let column = column.trim();
    if names_the_property(column, "EXDATE")
        && let Some((in_front, values)) = column.split_once(':')
    {"""
after = """    let column = column.trim();
    if false
        && let Some((in_front, values)) = column.split_once(':')
    {"""
red = [
    "service::caldav::tests::test_a_row_written_the_old_way_keeps_its_zone_on_every_day_it_names",
    "service::caldav::tests::test_the_column_is_walked_by_one_routine_whatever_shape_it_holds",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a day already called off is recognised by its clock face and its zone"
file = "src/application/calendar.rs"
before = """        named_already |= day.clock_face == new_day.clock_face
            && day.its_own_zone.or(its_zone) == new_day.its_own_zone.or(its_zone);
        all.push(a_cancelled_day_stored(day.its_own_zone, day.clock_face));"""
after = """        named_already |= a_cancelled_day_stored(day.its_own_zone, day.clock_face) == called_off;
        all.push(a_cancelled_day_stored(day.its_own_zone, day.clock_face));"""
red = [
    "application::calendar::tests::test_a_day_a_row_written_the_old_way_already_calls_off_is_not_named_twice",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a row written out again keeps the zone each day it names arrived under"
file = "src/application/calendar.rs"
before = """        all.push(a_cancelled_day_stored(day.its_own_zone, day.clock_face));
    }
    if !named_already {"""
after = """        all.push(day.clock_face.to_string());
    }
    if !named_already {"""
red = [
    "application::calendar::tests::test_calling_a_day_off_a_row_written_the_old_way_leaves_one_reading_of_it",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a cancelled day arriving in another zone is moved into the meeting's own"
file = "src/service/caldav.rs"
before = """    if Some(named) == events_zone {
        return one.to_string();
    }"""
after = """    if Some(named) != events_zone {
        return one.to_string();
    }"""
red = [
    "application::calendar::tests::test_a_called_off_day_with_a_zone_on_it_is_still_a_called_off_day",
    "application::calendar::tests::test_a_called_off_line_named_in_lower_case_is_still_read",
    "application::calendar::tests::test_a_google_cancellation_in_another_zone_is_stored_in_the_events_own_zone",
    "application::calendar::tests::test_every_called_off_day_google_sends_is_kept_not_only_the_first",
    "service::caldav::tests::test_a_cancelled_day_in_a_windows_zone_goes_back_out_in_that_zone",
    "service::caldav::tests::test_a_cancelled_day_in_a_windows_zone_is_stored_still_naming_that_zone",
    "service::caldav::tests::test_a_cancelled_day_in_a_zone_on_a_utc_meeting_is_stored_as_the_instant_it_names",
    "service::caldav::tests::test_a_cancelled_day_in_a_zone_we_can_read_names_the_same_instant_after_a_round_trip",
    "service::caldav::tests::test_a_cancelled_day_keeps_its_own_instant_when_its_zone_differs_from_the_meetings",
    "service::caldav::tests::test_a_cancelled_day_the_clocks_skipped_keeps_the_zone_it_was_written_in",
    "service::caldav::tests::test_a_cancelled_time_the_clocks_repeat_is_taken_at_its_first_passing",
    "service::caldav::tests::test_a_changed_day_of_a_series_is_kept_when_the_server_sends_it_with_the_series",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a cancelled day this program cannot move keeps the zone it arrived in"
file = "src/service/caldav.rs"
before = """    let in_the_zone_it_arrived_in = || a_cancelled_day_stored(Some(named), one);"""
after = """    let in_the_zone_it_arrived_in = || one.to_string();"""
red = [
    "application::calendar::tests::test_a_called_off_day_with_a_zone_on_it_is_still_a_called_off_day",
    "application::calendar::tests::test_a_called_off_line_named_in_lower_case_is_still_read",
    "application::calendar::tests::test_every_called_off_day_google_sends_is_kept_not_only_the_first",
    "service::caldav::tests::test_a_cancelled_day_in_a_windows_zone_goes_back_out_in_that_zone",
    "service::caldav::tests::test_a_cancelled_day_in_a_windows_zone_is_stored_still_naming_that_zone",
    "service::caldav::tests::test_a_cancelled_day_the_clocks_skipped_keeps_the_zone_it_was_written_in",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a cancelled day carrying its own zone goes out under that zone"
file = "src/service/caldav.rs"
before = """                let under = day.its_own_zone.or(zone);"""
after = """                let under = zone;"""
red = [
    "application::caldav_sync::tests::test_a_new_event_whose_cancelled_day_names_a_zone_we_cannot_describe_stays_waiting",
    "application::calendar::tests::test_calling_a_day_off_a_row_written_the_old_way_leaves_one_reading_of_it",
    "service::caldav::tests::test_a_cancelled_day_in_a_windows_zone_goes_back_out_in_that_zone",
    "service::caldav::tests::test_a_cancelled_day_the_clocks_skipped_keeps_the_zone_it_was_written_in",
    "service::caldav::tests::test_a_row_written_the_old_way_keeps_its_zone_on_every_day_it_names",
    "service::caldav::tests::test_cancelled_days_in_two_zones_go_out_on_a_line_each",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/caldav.rs", tests = 170 },
]

# ── The timezone rules a document must carry ────────────────────────────────
#
# The calendar standard requires a VTIMEZONE component for every TZID a
# document uses. No document this program wrote defined any, which a strict
# server refuses whole and a lenient one quietly guesses at. The rules are
# derived from the same timezone database every conversion here uses, the
# zones defined are the ones the produced document really names, and a zone
# the database cannot describe is refused at the create path and passed
# through with a warning at the change path, where the server has already
# accepted its own document.

[[guard]]
name = "a document built from nothing defines every zone it names"
file = "src/service/caldav.rs"
before = """    for zone in time_zones_named_on(&owned) {
        if let Some(rules) =
            crate::service::vtimezone::timezone_rules_for(&zone, the_year_the_event_is_in(event))
        {
            lines.extend(rules);
        }
    }
"""
after = ""
red = [
    "application::caldav_sync::tests::test_a_day_cut_out_of_a_series_goes_up_before_the_day_is_taken_off_the_series",
    "application::caldav_sync::tests::test_a_new_event_whose_cancelled_day_names_a_zone_we_cannot_describe_stays_waiting",
    "application::caldav_sync::tests::test_a_replacement_that_went_up_is_kept_when_taking_the_day_off_the_series_fails",
    "application::caldav_sync::tests::test_a_series_that_comes_first_in_the_queue_still_waits_for_the_day_cut_out_of_it",
    "application::caldav_sync::tests::test_the_series_stays_waiting_when_the_server_refuses_the_replacement",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_a_series_keeps_a_zone_a_server_will_take",
    "presentation::managers::changing_one_day_of_a_series::test_the_day_a_refusal_is_asked_about_names_only_the_events_own_time_zone",
    "presentation::managers::changing_one_day_of_a_series::test_what_a_rows_calendar_allows_is_answered_once_for_both_doors",
    "service::caldav::tests::test_a_document_built_from_nothing_defines_the_zone_it_names",
    "service::caldav::tests::test_a_written_document_read_back_through_our_own_reader_keeps_every_instant",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a change into a document that defines no zone adds the rules"
file = "src/service/caldav.rs"
before = """    for zone in time_zones_named_on(meant) {
        if timezone_ids_defined(&written).contains(&zone) {
            continue;
        }"""
after = """    for zone in time_zones_named_on(meant) {
        if true {
            continue;
        }"""
red = [
    "service::caldav::writing_tests::test_a_change_into_a_document_that_defines_no_zone_adds_the_rules",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a zone already defined in the document is not defined again"
file = "src/service/caldav.rs"
before = """        if timezone_ids_defined(&written).contains(&zone) {
            continue;
        }"""
after = """        if false {
            continue;
        }"""
red = [
    "service::caldav::writing_tests::test_a_change_into_a_document_already_defining_the_zone_adds_no_second_copy",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "the zones defined are the document's own, never the zone column"
file = "src/service/caldav.rs"
before = """    for zone in time_zones_named_on(&owned) {"""
after = """    for zone in crate::common::moment::the_zone_named(event.time_zone.as_deref()).map(str::to_string) {"""
red = [
    "service::caldav::tests::test_a_document_that_names_no_zone_defines_none",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a zone the document leaves undefined is reported before anything is sent"
file = "src/service/caldav.rs"
before = """        if !defined.contains(&zone) {
            return Some(zone);
        }"""
after = """        let _ = zone;"""
red = [
    "application::caldav_sync::tests::test_a_change_naming_a_zone_the_writer_cannot_describe_stays_waiting",
    "application::caldav_sync::tests::test_a_day_whose_replacement_cannot_be_created_is_not_taken_off_the_series_at_the_server",
    "application::caldav_sync::tests::test_a_new_event_whose_cancelled_day_names_a_zone_we_cannot_describe_stays_waiting",
    "application::calendar::tests::test_no_refusal_said_before_anything_is_written_describes_a_write_that_was_tried",
    "application::calendar::tests::test_taking_one_day_off_is_not_refused_over_a_zone_the_replacement_would_have_needed",
    "application::calendar::tests::test_the_editor_and_the_sync_say_the_same_thing_about_a_zone_they_cannot_write",
    "application::calendar::tests::test_the_one_day_answer_does_not_promise_a_calendar_server_a_zone_it_cannot_be_told",
    "application::calendar::tests::test_the_refusal_check_can_tell_a_good_sentence_from_a_bad_one",
    "application::calendar::tests::test_the_window_and_the_write_refuse_an_edit_in_the_same_words",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_an_outlook_zone_series_is_refused_and_neither_half_is_written",
    "presentation::managers::changing_one_day_of_a_series::test_what_a_rows_calendar_allows_is_answered_once_for_both_doors",
    "service::caldav::writing_tests::test_a_zone_the_document_leaves_undefined_is_named_before_anything_is_sent",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a name the timezone database does not know writes no rules"
file = "src/service/vtimezone.rs"
before = """    let zone: Tz = tzid.parse().ok()?;"""
after = """    let zone: Tz = tzid.parse().unwrap_or(Tz::UTC);"""
red = [
    "application::caldav_sync::tests::test_a_change_naming_a_zone_the_writer_cannot_describe_stays_waiting",
    "service::vtimezone::tests::test_a_name_that_names_no_zone_writes_nothing",
    # Added 2026-09-15 by 08-07's sweep: ten tests written 2026-08-11 to
    # 2026-08-13, two to four days after the record, about refusing a change
    # in a zone the writer cannot describe; one in the file the record names,
    # stamped over on 2026-09-02 by --recount-everything, and nine in two
    # files it never named.
    "application::caldav_sync::tests::test_a_day_whose_replacement_cannot_be_created_is_not_taken_off_the_series_at_the_server",
    "application::caldav_sync::tests::test_a_new_event_whose_cancelled_day_names_a_zone_we_cannot_describe_stays_waiting",
    "application::calendar::tests::test_no_refusal_said_before_anything_is_written_describes_a_write_that_was_tried",
    "application::calendar::tests::test_taking_one_day_off_is_not_refused_over_a_zone_the_replacement_would_have_needed",
    "application::calendar::tests::test_the_editor_and_the_sync_say_the_same_thing_about_a_zone_they_cannot_write",
    "application::calendar::tests::test_the_one_day_answer_does_not_promise_a_calendar_server_a_zone_it_cannot_be_told",
    "application::calendar::tests::test_the_refusal_check_can_tell_a_good_sentence_from_a_bad_one",
    "application::calendar::tests::test_the_window_and_the_write_refuse_an_edit_in_the_same_words",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_an_outlook_zone_series_is_refused_and_neither_half_is_written",
    "presentation::managers::changing_one_day_of_a_series::test_what_a_rows_calendar_allows_is_answered_once_for_both_doors",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "a zone that follows no yearly rule lists its days rather than guessing"
file = "src/service/vtimezone.rs"
before = """            format!("RDATE:{}", onsets.join(",")),"""
after = """            "RRULE:FREQ=YEARLY;BYMONTH=1;BYDAY=1SU".to_string(),"""
red = [
    "service::vtimezone::tests::test_a_zone_whose_changes_follow_no_yearly_rule_lists_its_days_rather_than_guessing",
    # Added 2026-09-15 by 08-07's sweep: written 2026-08-19 in the file this
    # record names, ten days after the record, and stamped over on 2026-09-02
    # by --recount-everything, which measured nothing.
    "service::vtimezone::tests::test_listed_blocks_keeps_each_pair_of_offsets_in_its_own_block",
]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "the offsets written are the zone's own"
file = "src/service/vtimezone.rs"
before = """    let sign = if seconds < 0 { '-' } else { '+' };"""
after = """    let sign = if seconds < 0 { '+' } else { '-' };"""
red = [
    "service::caldav::tests::test_a_written_document_read_back_through_our_own_reader_keeps_every_instant",
    "service::vtimezone::tests::test_a_zone_that_never_changes_writes_one_block_and_no_rule",
    "service::vtimezone::tests::test_the_rules_for_a_zone_that_changes_carry_both_offsets_and_both_change_rules",
    "service::vtimezone::tests::test_the_rules_for_a_zone_that_counts_weeks_forward",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "the counted weekday of a change rule is the one the zone changes on"
file = "src/service/vtimezone.rs"
before = """    let nth_of = |onset: &NaiveDateTime| (onset.day() - 1) / 7 + 1;"""
after = """    let nth_of = |onset: &NaiveDateTime| (onset.day() - 1) / 7;"""
red = [
    "service::vtimezone::tests::test_the_rules_for_a_zone_that_counts_weeks_forward",
]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

# ── A privacy claim no marker word puts at installation time ─────────────────
#
# The privacy page opened by saying nothing in this program sends your
# messages, contacts, calendar or links to anybody. With the shipped answer the
# contacts, the calendar and the tasks all go to the provider somebody signed
# in to, so the sentence was false, and it carried no word putting it at
# installation time, so the check that reads every other such claim walked past
# it for the whole of this project's life.
#
# Both of these run against the house style suite rather than the library,
# because that is where a rule about what the tree says lives. See `Guard.suite`
# in scripts/guards.py.

[[guard]]
name = "a sweeping refusal is read without any word dating it"
file = "tests/house_style.rs"
suite = "house_style"
before = """const A_SWEEP_OPENS_WITH: &[&str] = &["nothing", "nobody", "none", "never", "no", "nowhere"];"""
after = """const A_SWEEP_OPENS_WITH: &[&str] = &[];"""
red = [
    "test_the_new_installation_check_can_tell_the_two_apart",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A path added to the data folder and left off the pages listing it ────────
#
# `security.key` was on neither of the two pages that list what Wixen Mail
# leaves on somebody's disk, and `oauth.toml` was on one of the two, for the
# whole of this project's life. Somebody backing a machine up, or wiping one,
# reads those pages to find out what is there.
#
# The break is the half-fix rather than the absent one: it makes the
# enumeration skip an accessor rather than deleting the check. Measured by hand
# on 2026-09-12 with `--no-fail-fast` across every target, and exactly one test
# goes red. `test_every_path_this_module_hands_out_is_named_on_both_pages`
# cannot see this break at all, because skipping a path means checking fewer
# rather than checking wrongly, which is why the enumeration has a second test
# of its own.

[[guard]]
name = "every accessor handing out a path is one the pages have to name"
file = "src/common/paths.rs"
before = '''            ("security_key", paths.security_key()),'''
after = '''            ("logs_dir", paths.logs_dir()),'''
red = [
    "common::paths::tests::test_the_enumeration_reaches_every_accessor_this_module_has",
]
tests_last_seen = [
    { file = "src/common/paths.rs", tests = 22 },
]

# ── A shipped page promising the Windows warning will go away ────────────────
#
# `docs/installing.md` said "Once the setup file is signed, this box stops
# appearing" until commit 32af82b of 2026-09-04. It is false, and the installer
# ships the docs folder, so it sat on the disk of everybody who had installed a
# build. It was corrected by hand, which is how that page came to be wrong about
# signing twice.
#
# The break narrows the predicate rather than deleting the guard, because that
# is the shape CLAUDE.md says a document guard really fails in: it goes on
# reading, finds nothing, and reports a clean tree.
#
# Measured by hand on 2026-09-12 with `--no-fail-fast` across every target. Only
# the companion goes red. `test_no_document_promises_the_windows_warning_will_go_away`
# stays green under the break, because no document violates it today, so the
# corpus walk on its own cannot tell a working predicate from a narrowed one.
# That is the whole argument for the companion and it is measured rather than
# assumed.
#
# Written with ''' rather than """ because a TOML basic string reads a trailing
# backslash as a line continuation and collapses an escaped one, which has
# silently emptied a break here before.

[[guard]]
name = "a page cannot promise the Windows warning will go away"
file = "tests/house_style.rs"
suite = "house_style"
before = '''    "stops appearing",'''
after = '''    "a phrase no page uses",'''
red = [
    "test_the_signing_check_can_tell_a_promise_from_a_correction",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a word that opens a sweep is a word the reading finds a refusal in"
file = "tests/house_style.rs"
before = """const A_SWEEP_OPENS_WITH: &[&str] = &["nothing", "nobody", "none", "never", "no", "nowhere"];"""
after = """const A_SWEEP_OPENS_WITH: &[&str] = &["nothing", "nobody", "none", "never", "no", "nought"];"""
suite = "house_style"
red = [
    "test_a_sweeping_negative_is_a_word_the_reading_already_knows",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── The two questions Windows answers about a withdrawal ────────────────────
#
# Both tests these records cover were rewritten on 2026-09-13 because they were
# claims about the machine wearing the clothes of claims about the code: they
# passed here and failed on a CI runner whose certificate store is not this
# one's. A rewritten test that has never been red proves nothing, so each of
# them gets the break that must redden it written down and measured.

[[guard]]
# Written naming one test and measured on 2026-09-13 as reddening eight, which
# is the warning CLAUDE.md gives about writing a red list from what a break
# obviously ought to break. Two of the eight are end-to-end tests under
# `application`, in modules nobody thinking about a certificate store would have
# filtered for. All eight are here because that is what really went red.
name = "a withdrawal answer is read out of the chain rather than invented"
file = "src/service/signed_mail.rs"
before = """        if chain.error_status & CERT_TRUST_IS_REVOKED != 0 {"""
after = """        if true {"""
red = [
    "application::checking_signatures::end_to_end::test_a_signed_message_reopened_from_the_cache_says_what_it_said_the_first_time",
    "application::importing_messages::end_to_end::test_a_signed_message_brought_in_from_a_file_can_still_have_its_signature_checked",
    "service::signed_mail::tests::test_a_strangers_own_certificate_is_never_asked_about_over_the_network",
    "service::signed_mail::tests::test_asking_this_computer_about_withdrawal_answers_without_asking_anybody",
    "service::signed_mail::tests::test_the_withdrawal_question_does_not_manufacture_bad_news",
    "service::signed_mail::windows_store::tests::test_a_certificate_really_checked_and_found_sound_says_so",
    "service::signed_mail::windows_store::tests::test_every_way_of_not_finding_out_says_which_one_it_was",
    "service::signed_mail::windows_store::tests::test_nothing_asked_about_withdrawal_is_not_the_same_as_nothing_wrong",
]
tests_last_seen = [
    { file = "src/application/checking_signatures.rs", tests = 8 },
    { file = "src/application/importing_messages.rs", tests = 41 },
    { file = "src/service/signed_mail.rs", tests = 113 },
]

# ── A runner cannot judge this record, and that is written here rather than
#    only at the top of the file, where the last two sweeps did not read it ──
#
# What a runner cannot see: GitHub's Windows images have no certificate chain
# to walk, so the arm the break replaces is never reached there and taking its
# call away changes nothing. On a machine with a chain the test reaches it and
# the break reddens.
#
# Green on a runner twice, and reported as a finding both times: run
# 34965790937 of 2026-09-15 and run 35520204784 of 2026-09-20, a build and a
# run each.
#
# Red here on 2026-09-22, measured by 12-02.1 with
# `scripts/guards.sh --wait-until-quiet`: "the one test named went red, and
# nothing else did", rebuild 44 s and run 50 s. The record is right and the
# runner is what cannot read it. The sweep of 2026-09-15 said this would be
# written on each of the two such records and it was written at the top of the
# file instead, which is why the next sweep found them again.

[[guard]]
name = "the walk into Windows own chain structures really happens"
file = "src/service/signed_mail.rs"
before = """                    _ => withdrawal_result_of_the_first(chain),"""
after = """                    _ => None,"""
red = [
    "service::signed_mail::tests::test_the_withdrawal_question_really_reaches_windows_own_answer",
]
tests_last_seen = [
    { file = "src/service/signed_mail.rs", tests = 113 },
]

# ── The compiler named in two places ────────────────────────────────────────
#
# CI was red from 2026-09-10 to 2026-09-13 because rust-toolchain.toml did not
# exist and every workflow asked for whatever stable happened to be. The check
# that now compares them is a corpus walk over a directory, which is the kind
# that passes by finding nothing, so both halves of the reading get a record.
#
# The first record gained a third test on 2026-09-15: the reading of the
# mutation workflow's dispatch reuses the same helper to hold its toolchain
# call site to the pin, and its companion's wrong-compiler case reddens under
# this break too. Found by the count check's remedy, which reported one test
# red that the record did not name.

[[guard]]
name = "the compiler a workflow names is really read out of the workflow"
file = "tests/house_style.rs"
suite = "house_style"
before = '''                .split_once("dtolnay/rust-toolchain@")'''
after = '''                .split_once("a string no workflow holds")'''
red = [
    "test_ci_and_this_machine_are_told_to_use_the_same_compiler",
    "test_the_compiler_agreement_check_can_see_a_disagreement",
    "test_the_dispatch_reading_can_see_a_flag_the_script_does_not_accept",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "the components the pin file installs are really read out of the pin file"
file = "tests/house_style.rs"
suite = "house_style"
before = '''        .find_map(|line| line.strip_prefix("components = ["))'''
after = '''        .find_map(|line| line.strip_prefix("a key the pin file does not have"))'''
red = [
    "test_ci_and_this_machine_are_told_to_use_the_same_compiler",
    "test_the_compiler_agreement_check_can_see_a_disagreement",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A record in this file that had stopped naming one place ─────────────────
#
# A commit added a second sentence in the settings module opening on the same
# two lines as the one a record here is about. The break matched twice from
# then on, `scripts/guards.py` refuses to guess between two places, and that
# record measured nothing for a whole commit while a report said the opposite.
#
# The runner is strict about this and nothing was strict about it at commit
# time, and a full run is hours that nothing forces; how many is the product
# row on docs/development/measurements.md, not a figure here. So the same
# question is now asked in milliseconds by a test that reads this file. The two
# breaks below are the two halves of that reading: text that appears twice, and
# text that appears nowhere.

[[guard]]
name = "a break that could be applied in two places is refused rather than guessed at"
file = "tests/house_style.rs"
suite = "house_style"
before = """    if found > 1 {"""
after = """    if found > 2 {"""
red = [
    "test_the_guard_record_check_can_tell_the_two_apart",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a break whose text has gone is not passed off as one that still points somewhere"
file = "tests/house_style.rs"
suite = "house_style"
before = """    if found == 0 && !file_is_mid_measurement {"""
after = """    if found == 0 && file_is_mid_measurement {"""
# Two, measured rather than expected. Turning the exemption round makes the
# reading complain about exactly the records a break is meant to excuse, and
# this break is one of those records, so the sweep over the real file reports
# itself and goes red alongside the made-up-input test.
red = [
    "test_every_guard_record_still_names_one_place_in_the_tree",
    "test_the_guard_record_check_can_tell_the_two_apart",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A change the mail server refused, reported as one that happened ─────────
#
# Two of the commands that write to a mailbox were sent through a helper that
# hands the result back as a stream, and that stream ends at the server's
# answer without ever reading what the answer was. A refusal came back looking
# exactly like a change that worked. Every flag this program sets went through
# one of them, and the other is the command that removes a message, so a person
# was told "Deleted" over mail still sitting in the folder.
#
# Neither result was used for anything. Both are sent as plain command lines
# now, and so is every read, so the break written down here is the old defect
# rebuilt by hand: send the line, read to the answer, and never look at what the
# answer said.

[[guard]]
name = "a flag the mail server refused is not reported as set"
file = "src/service/protocols/imap.rs"
before = """        with_timeout(
            COMMAND_TIMEOUT,
            self.session
                .run_command_and_check_ok(format!("UID STORE {uid} {operation} ({flag})")),
            "changing a message flag",
        )
        .await?
        .map_err(protocol_error("Could not change the message"))
    }"""
after = """        let tag = self
            .session
            .run_command(format!("UID STORE {uid} {operation} ({flag})"))
            .await
            .map_err(protocol_error("Could not change the message"))?;
        while let Ok(Some(response)) = self.session.read_response().await {
            if response.request_id() == Some(&tag) {
                break;
            }
        }
        Ok(())
    }"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_removal_the_server_refuses_still_leaves_the_newer_draft_on_it",
    "service::protocols::imap::against_a_server_that_answers::test_a_delete_whose_flag_the_server_refuses_changes_nothing",
    "service::protocols::imap::against_a_server_that_answers::test_a_move_that_fails_at_the_flag_has_already_made_the_copy",
    # Added 2026-09-15 by 08-07's sweep: the waiting flag changes and the
    # crossed moves, written 2026-09-05 and 2026-09-08 in files this record
    # had never named, both read a refused flag as a refusal.
    "application::flag_changes_waiting::tests::test_a_server_that_answers_no_is_read_as_a_refusal_and_still_puts_the_change_back",
    "application::flag_changes_waiting::tests::test_a_server_that_drops_the_connection_is_read_as_never_having_been_asked",
    "application::mail_across_accounts::tests::test_a_source_that_will_not_mark_the_message_leaves_it_in_both_places",
    "application::mail_across_accounts::tests::test_no_way_a_crossed_move_can_end_leaves_the_bytes_behind",
]
tests_last_seen = [
    { file = "src/application/flag_changes_waiting.rs", tests = 16 },
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a message the mail server would not remove is not reported as removed"
file = "src/service/protocols/imap.rs"
before = """        with_timeout(
            COMMAND_TIMEOUT,
            self.session
                .run_command_and_check_ok(format!("UID EXPUNGE {uid}")),
            "deleting the message",
        )
        .await?
        .map_err(protocol_error("Could not delete the message"))
    }"""
after = """        let tag = self
            .session
            .run_command(format!("UID EXPUNGE {uid}"))
            .await
            .map_err(protocol_error("Could not delete the message"))?;
        while let Ok(Some(response)) = self.session.read_response().await {
            if response.request_id() == Some(&tag) {
                break;
            }
        }
        Ok(())
    }"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_delete_the_server_would_not_finish_comes_back_saying_where_both_copies_are",
    "service::protocols::imap::against_a_server_that_answers::test_a_move_that_fails_at_the_expunge_leaves_the_message_in_both_places",
    "service::protocols::imap::against_a_server_that_answers::test_deleting_in_place_that_the_server_will_not_remove_says_it_is_only_marked",
    "service::protocols::imap::against_a_server_that_answers::test_deleting_into_a_trash_the_server_will_not_remove_the_original_from_says_both_places",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── How many previous drafts were really removed ────────────────────────────
#
# The caller writes the new copy as soon as this comes back without a failure.
# On a server that can only flag the old copy and leave it, a count of what was
# found rather than what went ends the day with two drafts and neither of them
# obviously the newer.

[[guard]]
name = "a draft only flagged for removal is not counted as removed"
file = "src/service/protocols/imap.rs"
before = """            if !self.abilities.uid_expunge {
                continue;
            }
            self.expunge_one(*uid).await?;
            removed += 1;"""
after = """            if self.abilities.uid_expunge {
                self.expunge_one(*uid).await?;
            }
            removed += 1;"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_server_that_cannot_remove_one_message_leaves_the_older_copy_and_says_so",
    "application::mail_controller::against_a_server_that_answers::test_a_server_without_uidplus_only_flags_the_old_copy_and_counts_none_removed",
    "service::protocols::imap::against_a_server_that_answers::test_a_draft_that_could_not_be_removed_is_not_counted_as_removed",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── A blind copy is blind because of a default nobody wrote down ────────────
#
# The address is taken out of the header into the envelope and the header is
# then removed, so the recipient list carries no sign of it and neither does
# the copy filed in Sent. Turning that off is one call, it makes the whole
# thing easier to write, and it would put every blind address on the wire for
# every recipient to read.

[[guard]]
name = "a blind copy reaches the server and never the message"
file = "src/service/protocols/smtp.rs"
# Measured by hand again on 2026-08-16 with scripts/guards.py, after a round
# that let a recipient carry a name changed `bcc`'s element type from a bare
# `String` to an `EmailAddress`, which moved the address one field deeper
# (`bcc` to `bcc.address`) and fell one edit behind its own file within the
# same round. What the break does and what it reddens are unchanged: the
# same `keep_bcc()` call, still reddening only the one test named.
before = """    for bcc in &email.bcc {
        builder = builder.bcc(parse_mailbox(&bcc.address, bcc.name.as_deref())?);
    }"""
after = """    for bcc in &email.bcc {
        builder = builder.bcc(parse_mailbox(&bcc.address, bcc.name.as_deref())?);
    }
    builder = builder.keep_bcc();"""
red = [
    "service::protocols::smtp::against_a_server_that_answers::test_a_blind_copy_reaches_the_server_and_never_the_message",
]
tests_last_seen = [
    { file = "src/service/protocols/smtp.rs", tests = 40 },
]

# ── Which mailbox a change is applied to ────────────────────────────────────
#
# A UID means a different message in every mailbox, so a change sent without
# opening the folder it names lands on whatever was open, under the right
# subject line. The older check for this threw the answer away and so could not
# tell a refusal from a success; these read the transcript of a server that
# answers.

[[guard]]
name = "a delete opens the folder it names before it removes anything"
file = "src/application/mail_controller.rs"
before = """        once_more_if_the_connection_went!(self, session, {
            if session.selected_folder() != Some(folder) {
                session.select_folder(folder).await?;
            }
            session.delete_message(uid, trash).await"""
after = """        once_more_if_the_connection_went!(self, session, {
            session.delete_message(uid, trash).await"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_delete_the_server_would_not_finish_comes_back_saying_where_both_copies_are",
    "application::mail_controller::against_a_server_that_answers::test_a_delete_with_a_trash_folder_really_moves_the_message_there",
    "application::mail_controller::against_a_server_that_answers::test_changing_a_message_opens_the_folder_it_names_before_asking",
    "application::mail_controller::against_a_server_that_answers::test_deleting_a_message_past_an_open_gate_opens_its_folder_then_moves_it",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
]

# ── And which mailbox a saved copy leaves alone ─────────────────────────────
#
# Saving a copy of a sent message deliberately opens nothing. Its caller is
# usually in the middle of reading a folder, and opening one here changes which
# mailbox is open underneath them.
#
# Re-measured 2026-08-12: two tests now, not one. The second drives the adapter
# that really files the copy rather than the command underneath it, and it asks
# the same question one layer up, because that is the layer the send loop uses.

[[guard]]
name = "saving a copy does not change which folder is open"
file = "src/application/mail_controller.rs"
before = """        once_more_if_the_connection_went!(self, session, {
            session.append_message(into, flags, arrived, raw).await
        })"""
after = """        once_more_if_the_connection_went!(self, session, {
            session.select_folder(into).await?;
            session.append_message(into, flags, arrived, raw).await
        })"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_saving_a_copy_of_a_sent_message_appends_it_where_it_was_told",
    "application::mail_controller::against_a_server_that_answers::test_the_copy_of_a_sent_message_is_appended_where_it_was_told_flagged_as_read",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
]

# ── A mutation run that tested nothing, reported as one that found nothing ───
#
# Whether a run had produced anything was decided by asking whether its results
# file existed. That file is created empty before the first mutant is built, so
# a run whose build failed had one, it was empty, and the script printed that
# every mutant was caught and exited clean. The record written beside it said
# the build had failed the whole time.
#
# Underneath that, a failed build is recorded under one heading whichever way
# it failed: the compiler rejected the mutant, or the compiler never started.
# Only the first was tested. The whole-tree run of 2026-08-05 recorded 595 that
# way and 473 of them had never reached a compiler.
#
# On top of that, a run in which not one mutant ever reached the suite counted
# as a clean result: every mutant rejected by the compiler, or no mutants at
# all, printed a headline nobody could tell from a run that caught everything.
#
# All four run against the house style suite rather than the library, because
# these are rules about what the tree says. See `Guard.suite` in
# scripts/guards.py. The wording rules the reading itself follows are worked
# examples inside it, and the house style suite now runs them, so they hold on
# an ordinary change rather than only on the days somebody runs a mutation run.

[[guard]]
name = "a mutation result is read from the record, not from the lists written for people"
file = "scripts/mutants.sh"
suite = "house_style"
before = """exec python scripts/mutants_report.py --exit-status "$STATUS" "$OUT/mutants.out\""""
after = """[ -s "$OUT/mutants.out/caught.txt" ]"""
red = [
    "test_no_mutation_result_is_read_from_the_lists_written_for_people",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "the mutation script and the pull request check ask what a change touched the same way"
file = ".github/workflows/mutants.yml"
suite = "house_style"
before = """      run: scripts/mutants.sh --since origin/${{ github.base_ref }}"""
after = """      run: cargo mutants --in-diff changed.diff -j 2"""
red = [
    "test_what_a_change_touched_is_asked_the_same_way_in_both_places",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a mutation run that failed is not passed off as one that finished"
file = "scripts/mutants.sh"
suite = "house_style"
before = """    cargo mutants -j "$JOBS" --output "$OUT" || STATUS=$?"""
after = """    cargo mutants -j "$JOBS" --output "$OUT" || true"""
red = [
    "test_no_mutation_run_has_its_failure_swallowed",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a mutation run that never reached the tests is not a result"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """    if run.answered_by_the_suite == 0:"""
after = """    if False:"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A run the suite never finished against, read as one that found nothing ───
#
# The same shape as the four above, one layer along, and it shipped. Whether a
# run had learned anything was read off how many mutants were handed to the
# test suite. A mutant the suite hung on was handed to it, so a run in which
# every single mutant timed out passed that check, reached the summary, found
# nothing missed, and printed that every mutant which built was caught. Nothing
# had been caught, and run by hand it then exited zero.
#
# A timeout is a suite that was started and never got to the end. Nothing came
# back about that mutant, so the run knows neither that a test would notice nor
# that none would. It is not a catch and it is not a miss, and the three places
# that used to disagree about it now read one count: the refusal below, the
# summary, and the exit code.
#
# The last of these is the other half of the same job. A run can pass all three
# checks and still have been turned away from most of what it asked, because a
# mutant the compiler rejects is counted and says nothing. One run declared 84
# mutants, the suite answered for 30, and every sentence written about it
# afterwards quoted the 28 it caught. The proportion is printed in words now
# rather than left for somebody to work out of five bucket counts.

[[guard]]
name = "a mutation run the suite never finished against is not a result"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """        return len(self.caught) + len(self.missed)
"""
after = """        return len(self.timed_out) + len(self.caught) + len(self.missed)
"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a mutant the suite never finished against is not summed up as one it caught"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """    if not run.missed and not run.timed_out:
        print("Every mutant that built was caught.")"""
after = """    if not run.missed:
        print("Every mutant that built was caught.")"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a mutation run the suite never finished against does not pass"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """    return 1 if run.missed or run.timed_out else 0"""
after = """    return 1 if run.missed else 0"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a mutation report says how much of the run asked a question"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """    print(how_much_of_it_asked_a_question(run))"""
after = """    pass"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A guard measurement that does not return, and the shard it used to cost ──
#
# Run 35520204784 of .github/workflows/guards.yml lost shard 40 on 2026-09-20.
# The shard logged 2,637 s of measured work and then spent about 5 h 16 min
# inside one record, "the settings dialog is frozen while its pages are built",
# which printed a header and never a timing line, until the job's 360-minute
# cap ended it with nine records unmeasured. The longest shard that finished
# took 7,442 s, so the shard count was never the cause: nothing in
# scripts/guards.py bounded a record or a suite's pre-read, and one that does
# not return costs a whole shard whatever the shard count is.
#
# What covers that file is a worked example. There is no shell suite for the
# guard runner, and test_the_guard_runner_still_obeys_its_own_examples is what
# runs the examples on a commit. So the break takes the budget off the one
# cargo call that every record's build and every record's run goes through, and
# the example asserting that an already spent budget stops that call is what
# reddens. Every other example still passes under the break, including the ones
# over the timeout machinery itself, which pass their own budgets in: the only
# thing this break removes is the budget reaching cargo.
#
# Measured by hand on 2026-09-22 before it was written down.

[[guard]]
name = "a record's own time limit reaches the cargo call it is supposed to bound"
file = "scripts/guards.py"
suite = "house_style"
before = """    finished = run_to_completion(
        ["cargo", *arguments],
        budget,"""
after = """    finished = run_to_completion(
        ["cargo", *arguments],
        None,"""
red = [
    "test_the_guard_runner_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A reason a mail server already punctuated, punctuated again ──────────────
#
# Both sentences saying where somebody's only copy of a message ended up
# finish with the server's own words, and a server that ends them with a full
# stop got a second one. Read aloud that is a stumble in the middle of the one
# sentence that says whether the message survived.
#
# Neither break below can be found by a mutation run. The early return is a
# call on a list of three characters and the trim beside it takes no arguments,
# so cargo-mutants produces no mutant for either line, and both went untested
# through every run of both files. This is what "a mutation report is a
# starting point, not the list" means in practice.
#
# Two records because the routine is written out once in each of the two files,
# byte for byte the same. A fix to one of them leaves the other stumbling and
# only a test in each would notice.

[[guard]]
name = "a reason a server already punctuated is not punctuated again in the draft path"
file = "src/application/draft_copy.rs"
before = """    if reason.ends_with(['.', '!', '?']) {
        return reason.to_string();
    }"""
after = """    if reason.is_empty() {
        return reason.to_string();
    }"""
red = [
    "application::draft_copy::tests::test_a_reason_that_already_ends_in_a_full_stop_does_not_get_a_second_one",
]
tests_last_seen = [
    { file = "src/application/draft_copy.rs", tests = 11 },
]

[[guard]]
name = "a reason a server already punctuated is not punctuated again in the sent path"
file = "src/application/sent_copy.rs"
before = """    if reason.ends_with(['.', '!', '?']) {
        return reason.to_string();
    }"""
after = """    if reason.is_empty() {
        return reason.to_string();
    }"""
red = [
    "application::sent_copy::tests::test_a_reason_that_already_ends_in_a_full_stop_does_not_get_a_second_one",
]
tests_last_seen = [
    { file = "src/application/sent_copy.rs", tests = 22 },
]

# ── The connection a queue of outgoing mail opens, never let go of ───────────
#
# One session is opened for a whole queue and closed at the end of it. Emptying
# the closing out was the one mutant nothing caught out of the 66 across the
# four files that decide whether a copy of a message survives, so a version of
# this program that held a connection open at the server after every send would
# have passed the whole suite.
#
# The break below is that mutant written by hand.

[[guard]]
name = "the connection a queue of outgoing mail opens is let go of"
file = "src/application/sent_copy.rs"
before = """    pub(crate) async fn close(self) {
        if let Self::Open(session) = self {
            let _ = session.disconnect_imap().await;
        }
    }"""
after = """    pub(crate) async fn close(self) {}"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_closing_the_queue_lets_go_of_the_connection_it_opened",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/application/sent_copy.rs", tests = 22 },
]

# ── A changelog entry for a fix nobody could have noticed ────────────────────
#
# The clearest shape of a changelog entry claiming a change that did not
# happen: a value saying what became of somebody's mail is worked out, and the
# only place in the running program that asks for it drops it. The entry
# describing the improvement is then true of the code and false of the day.
#
# It really happened. Replacing a filed draft was changed to answer with how
# many copies really went rather than how many were found, an entry announced
# it, and the one caller threw the number away and appended regardless. The
# compiler cannot see this, because discarding a value is legal.
#
# The break below is that shape put back into the draft path, which is where
# it was. It runs against the house style suite, since the rule is about what
# the tree says.
#
# Re-measured 2026-08-12, and this is the one thing about it worth knowing. The
# named test reads the tree as text, so on its own it could not tell a body that
# still reads the answer from one that stopped. It is no longer on its own: the
# same break now reddens two tests that drive this body against a server and
# read what came back, "a server that cannot remove one message leaves the older
# copy and says so" and "a removal the server refuses still leaves the newer
# draft on it". They are not named here because this record runs the house style
# suite, which never builds them. If this record is ever widened to the library,
# they belong in the list.

[[guard]]
name = "an answer about somebody's mail is read rather than thrown away"
file = "src/application/draft_copy.rs"
suite = "house_style"
before = """        self.session
            .remove_these(folder, uids)
            .await
            .map_err(|e| e.to_string())"""
after = """        let _ = self.session.remove_these(folder, uids).await;
        Ok(uids.len())"""
red = [
    "test_no_answer_about_somebody_s_mail_is_worked_out_and_thrown_away",
]
tests_last_seen = [
    { file = "src/application/draft_copy.rs", tests = 11 },
    { file = "tests/house_style.rs", tests = 74 },
]

# ── An unrecognised trash used to mean a permanent deletion ─────────────────
#
# Three readers answer "is this folder the trash", and the last of them treated
# "I do not recognise any of these" as "there is nowhere to move it to". The
# ordinary Delete then flagged the message and removed it from the server with
# no copy kept anywhere, and said "Deleted", which was true. A server naming
# its trash in another language was enough.
#
# The four breaks below are the two halves of the fix and the two narrower
# edits that used to go unseen. The halves are the decision that names the
# refusal and the step that refuses instead of sending the delete. The narrower
# edits leave the refusal being said and take away the stop after it, which
# reads as tidying and is the message loss itself: the arm's value becomes the
# same "nowhere to move it to" the delete-off-the-server answer gives, so the
# sentence is spoken and the only copy is removed anyway.
#
# These three records are no longer defended by a reading of a file. Deciding
# whether to send a delete and sending it now sit in one function, so the tests
# named below hold a server, run the delete against it and ask what it was
# told. On the two refusals the answer has to be nothing at all.
#
# Measured by hand on 2026-08-12, each break applied on its own and the whole
# library run. The second and third records are the say-it-and-carry-on edit,
# and the transcript under the first of them is the loss itself: SELECT INBOX,
# UID STORE 7 +FLAGS (\Deleted), UID EXPUNGE 7. Every one of the three reddens
# two tests, one over a server that records what it was told and one over a
# server that really speaks IMAP.
#
# What these records still cannot see: whether the window ever reaches this
# code. That is read as text in tests/wired.rs, and that reading says so in its
# own words.

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named,
# and it came from a feature this record has never opened. Emptying a folder
# was built after the hand measurement above and asks this same question of the
# same decision, so the record covered one more caller than it said it did.
name = "not knowing where the trash is does not read as having nowhere to put it"
file = "src/application/destinations.rs"
before = """    else {
        return DeletedGoesTo::NoTrashFolderFound;
    };"""
after = """    else {
        return DeletedGoesTo::OffTheServer;
    };"""
red = [
    "application::deleting_at_the_server::tests::test_a_delete_on_an_account_with_no_recognised_trash_asks_no_server_anything",
    "application::destinations::tests::test_an_account_with_no_folder_that_is_the_trash_does_not_delete_the_message",
    "application::emptying::tests::test_an_account_whose_trash_is_not_recognised_is_refused_rather_than_emptied",
    "application::mail_controller::against_a_server_that_answers::test_a_delete_with_no_recognised_trash_reaches_no_server",
]
tests_last_seen = [
    { file = "src/application/deleting_at_the_server.rs", tests = 5 },
    { file = "src/application/destinations.rs", tests = 37 },
    { file = "src/application/emptying.rs", tests = 23 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

[[guard]]
name = "the delete step says why it did nothing rather than deleting anyway"
file = "src/application/deleting_at_the_server.rs"
before = """        DeletedGoesTo::NoTrashFolderFound => {
            return Deleted::NothingWasSent(NO_TRASH_FOLDER_FOUND);
        }"""
after = """        DeletedGoesTo::NoTrashFolderFound => None,"""
red = [
    "application::deleting_at_the_server::tests::test_a_delete_on_an_account_with_no_recognised_trash_asks_no_server_anything",
    "application::mail_controller::against_a_server_that_answers::test_a_delete_with_no_recognised_trash_reaches_no_server",
]
tests_last_seen = [
    { file = "src/application/deleting_at_the_server.rs", tests = 5 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

[[guard]]
name = "the arm that found no trash stops rather than carrying on"
file = "src/application/deleting_at_the_server.rs"
before = """            return Deleted::NothingWasSent(NO_TRASH_FOLDER_FOUND);"""
after = """            let _ = Deleted::NothingWasSent(NO_TRASH_FOLDER_FOUND);
            None"""
red = [
    "application::deleting_at_the_server::tests::test_a_delete_on_an_account_with_no_recognised_trash_asks_no_server_anything",
    "application::mail_controller::against_a_server_that_answers::test_a_delete_with_no_recognised_trash_reaches_no_server",
]
tests_last_seen = [
    { file = "src/application/deleting_at_the_server.rs", tests = 5 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

[[guard]]
name = "the arm that knows no folders yet stops rather than carrying on"
file = "src/application/deleting_at_the_server.rs"
before = """            return Deleted::NothingWasSent(NO_FOLDERS_KNOWN_YET);"""
after = """            let _ = Deleted::NothingWasSent(NO_FOLDERS_KNOWN_YET);
            None"""
red = [
    "application::deleting_at_the_server::tests::test_a_delete_on_an_account_whose_folders_are_not_known_yet_asks_no_server_anything",
    "application::mail_controller::against_a_server_that_answers::test_a_delete_on_an_account_whose_folders_are_not_known_yet_reaches_no_server",
]
tests_last_seen = [
    { file = "src/application/deleting_at_the_server.rs", tests = 5 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

# ── A half-finished move announced as one that never happened ───────────────
#
# The fallback move is copy, then flag, then remove. Once the copy has landed,
# nothing that goes wrong afterwards has lost anything, but it used to come
# back as a plain failure. The list kept its row and said the message was where
# it had always been; the server had it in two folders, one of them flagged.
#
# So a failure now means nothing on the server changed, and that only holds
# while the copy is confirmed before anything touches the original. The third
# record is the one that keeps that order honest.

[[guard]]
name = "a removal the server refused does not read as a move that never happened"
file = "src/service/protocols/imap.rs"
before = """        if let Err(refused) = self.expunge_one(uid).await {
            return Ok(Moved::CopiedAndFlagged(StillHere::TheServerRefusedIt(
                refused.to_string(),
            )));
        }
        Ok(Moved::Moved)"""
after = """        self.expunge_one(uid).await?;
        Ok(Moved::Moved)"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_delete_the_server_would_not_finish_comes_back_saying_where_both_copies_are",
    "service::protocols::imap::against_a_server_that_answers::test_a_move_that_fails_at_the_expunge_leaves_the_message_in_both_places",
    "service::protocols::imap::against_a_server_that_answers::test_deleting_into_a_trash_the_server_will_not_remove_the_original_from_says_both_places",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a message the server still holds unflagged keeps its row in the list"
file = "src/application/server_delete.rs"
before = """        Deletion::CopiedToTrashAndNotFlagged(_) => ThenWhat::LeaveTheRow,"""
after = """        Deletion::CopiedToTrashAndNotFlagged(_) => ThenWhat::MarkItDeletedHere,"""
red = [
    "application::server_delete::tests::test_a_message_the_server_still_holds_unflagged_keeps_its_row",
]
tests_last_seen = [
    { file = "src/application/server_delete.rs", tests = 17 },
]

[[guard]]
name = "the copy is confirmed before the original is touched"
file = "src/service/protocols/imap.rs"
before = """        self.copy_message(uid, into).await?;
        if let Err(refused) = self.set_flag(uid, flag::DELETED, true).await {"""
after = """        self.set_flag(uid, flag::DELETED, true).await?;
        if let Err(refused) = self.copy_message(uid, into).await {"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_delete_the_server_would_not_finish_comes_back_saying_where_both_copies_are",
    "service::protocols::imap::against_a_server_that_answers::test_a_copy_that_fails_leaves_the_original_untouched",
    "service::protocols::imap::against_a_server_that_answers::test_a_move_that_fails_at_the_expunge_leaves_the_message_in_both_places",
    "service::protocols::imap::against_a_server_that_answers::test_a_move_that_fails_at_the_flag_has_already_made_the_copy",
    "service::protocols::imap::against_a_server_that_answers::test_a_server_without_move_copies_then_flags_then_expunges_in_that_order",
    "service::protocols::imap::against_a_server_that_answers::test_deleting_into_a_trash_the_server_will_not_remove_the_original_from_says_both_places",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── A failed draft save that left no copy on the server ────────────────────
#
# The old copy was removed and the new one filed second, on purpose, so that a
# failure could not leave two drafts with nothing saying which was newer. What
# it left instead was none: a save the server turned down took the only copy
# somebody's other devices had, and both failures went to a line in a log
# inside a background task with no way of speaking.
#
# Swapping the two lines is not the fix either. Both copies carry the same
# identifier, so a sweep run afterwards takes the copy just filed. The copies
# that are there are asked for first, and the removal names exactly those.

[[guard]]
name = "the newer draft is on the server before the older one is taken away"
file = "src/application/draft_copy.rs"
before = """    let older = asked.clone().unwrap_or_default();

    if let Err(refused) = server.file_this_one(folder, raw).await {"""
after = """    let older = asked.clone().unwrap_or_default();
    let _ = server.remove_these(folder, &older).await;

    if let Err(refused) = server.file_this_one(folder, raw).await {"""
red = [
    "application::draft_copy::tests::test_a_draft_the_server_will_not_take_leaves_the_older_copy_alone",
    "application::draft_copy::tests::test_the_new_copy_is_filed_before_the_old_one_is_removed",
    "application::mail_controller::against_a_server_that_answers::test_a_draft_the_server_refuses_to_take_leaves_the_older_copy_on_the_server",
    "application::mail_controller::against_a_server_that_answers::test_a_draft_the_server_takes_is_filed_before_the_older_copy_is_removed",
    "application::mail_controller::against_a_server_that_answers::test_the_copies_the_server_is_told_to_remove_are_the_ones_it_named",
]
tests_last_seen = [
    { file = "src/application/draft_copy.rs", tests = 11 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

[[guard]]
name = "a draft the server would not take is said rather than logged"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """        if filed.needs_saying() {
            handle.block_on(async {
                let _ = tx
                    .send(UIUpdate::StatusUpdated(filed.what_happened()))
                    .await;
            });
        }"""
after = """        tracing::warn!("{}", filed.what_happened());"""
red = [
    "test_a_draft_the_server_would_not_take_reaches_the_person",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# Rewritten 2026-09-19 by 11-07 onto the batch's shape, where the branch
# answers the outcome and whether the message went; the break is the one it
# was, and it reddened the same one test.
[[guard]]
name = "the move handler asks one place what to say and what to do with the row"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """                .map(|moved| {
                    let next =
                        crate::application::server_delete::after_a_move(&moved, &into.id, &subject);
                    let left = next.then == ThenWhat::MarkItDeletedHere;
                    (next, left)
                }),"""
after = """                .map(|moved| {
                    let _ = &moved;
                    let next = crate::application::server_delete::WhatToDoNext {
                        then: crate::application::server_delete::ThenWhat::MarkItDeletedHere,
                        said: format!("Moved to {}: {subject}", into.id),
                    };
                    let left = next.then == ThenWhat::MarkItDeletedHere;
                    (next, left)
                }),"""
red = [
    "test_the_delete_and_move_handlers_ask_one_place_what_to_say_and_what_to_do",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# ── A read the mail server refused, reported as a folder with nothing in it ──
#
# The same swallowing as the pair above, on the side that deletes. Every helper
# in this library that hands a result back as a stream ends that stream at the
# server's last line without reading it, so a server refusing to list a folder
# produced no messages, and no messages is exactly what an empty folder
# produces. The sync believed it and deleted every message it had stored for
# that folder, and the copy of each message kept for reading offline with it.
#
# Every read is now sent as a plain command line and read to its own answer in
# one place. Break that one place and every read goes back to swallowing at
# once, which is the point of it being one place.

[[guard]]
name = "a read the mail server refused is not a folder with nothing in it"
file = "src/service/protocols/imap.rs"
before = """                        return match status {
                            Status::Ok => Ok(collected),
                            _ => Err(server_refused(doing, status, information.as_deref())),
                        };"""
after = """                        let _ = (status, information);
                        return Ok(collected);"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_flag_read_the_server_refuses_does_not_read_as_nothing_having_changed",
    "service::protocols::imap::against_a_server_that_answers::test_a_folder_list_the_server_refuses_is_not_an_account_with_no_folders",
    "service::protocols::imap::against_a_server_that_answers::test_a_header_fetch_the_server_refuses_is_not_a_folder_with_no_mail",
    "service::protocols::imap::against_a_server_that_answers::test_a_message_the_server_refuses_to_send_is_told_apart_from_one_that_is_not_there",
    "service::protocols::imap::against_a_server_that_answers::test_a_search_that_finds_some_and_then_refuses_is_not_a_partial_list",
    "service::protocols::imap::against_a_server_that_answers::test_a_search_the_server_refuses_is_not_a_folder_with_no_messages",
    "service::protocols::imap::against_a_server_that_answers::test_a_server_that_will_not_say_what_it_supports_is_not_a_server_that_supports_nothing",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── A connection that went away, reported as an answer that arrived ──────────
#
# The other way a read used to come back empty without being empty. A server
# that closes the socket after its data and before its final line ended the
# stream just as cleanly as a finished command, so a short answer read as a
# whole one and, on the search, as a mailbox holding nothing.

[[guard]]
name = "a connection that closed mid answer is not an answer that finished"
file = "src/service/protocols/imap.rs"
before = """                    let Some(response) = arrived else {
                        return Err(Error::Network(format!(
                            "The mail server closed the connection while {doing}"
                        )));
                    };"""
after = """                    let Some(response) = arrived else {
                        return Ok(collected);
                    };"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_server_that_hangs_up_in_the_middle_of_a_search_is_not_an_empty_folder",
    # Added 2026-09-15 by 08-07's sweep: the session that signs in again after
    # a dropped connection, written 2026-09-04 in a file this record had never
    # named, needs the drop read as a drop.
    "application::mail_session::tests::test_a_connection_the_server_dropped_is_signed_in_again_and_the_work_finishes",
    "application::mail_session::tests::test_a_reconnect_that_fails_too_is_refused_in_plain_words",
    "application::mail_session::tests::test_a_server_that_drops_every_connection_is_tried_once_more_and_no_more",
]
tests_last_seen = [
    { file = "src/application/mail_session.rs", tests = 14 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── A chunk of a whole-folder request that lands and is not shown ────────────
#
# SCALE-03, and #24. Until 2026-09-17 there were two five hundreds and they
# were separate decisions: `mail_sync::INITIAL_FETCH_LIMIT` bounded what came
# down from the server and `wx_app::FOLDER_LIST_PAGE_SIZE` bounded what was
# read out of the cache into the list, and this record held the arm the
# request sends once per chunk to moving the second one. 10-02 took the second
# bound off, because it hid every message past the newest 500 and pushed the
# oldest shown off the end as mail arrived, which is what the tester reported.
# So the record was rewritten on that day onto the new break rather than
# re-measured: its old `before` was the block that grew the limit, which no
# longer exists, and its old `red` named a test that was renamed. What it
# guards now is the other half of the same failure: a chunk landing in the
# cache and the open folder not being read again, so what the chunk brought
# stays off the list.
#
# The break takes the re-read out of the arm, leaving the chunk in the cache
# and the list as it was. One test goes red out of the eight in this target;
# the companion that plants the old arm is over a snippet and stays green.
#
# This record does the second job as well: it is what couples the target to
# `wx_app.rs`, so a census living under `tests/` runs on the commits that could
# change the answer rather than only on the commits that change the census. The
# target is under `tests/` rather than beside the code for a cost reason. The
# handler needs a window, a frame and a running event loop to reach, so it is a
# source read either way, and 57 guard records fingerprint the number of tests
# in `wx_app.rs` on 2026-09-17 (thirty-four when this was first written on
# 2026-09-04), which at one build and one full library run each is hours for
# one test.
#
# Measured by hand on 2026-09-04 as first written; rewritten and measured
# through `--remeasure` on 2026-09-17.
#
# Renamed on 2026-09-17 with the target, when 10-05 retired the whole-folder
# request for #20 and #23: the download of everything sends the same update
# once per chunk of headers, so the arm and the break are unchanged and the
# target that reads them is `everything_comes_down_without_being_asked`.
# Measured again through `--remeasure` that day.

[[guard]]
name = "a chunk of the download that lands is shown, and no limit grows"
file = "src/presentation/wx_app.rs"
suite = "everything_comes_down_without_being_asked"
before = """            // is `mail_sync::INITIAL_FETCH_LIMIT`, on what one chunk fetches.
            reread_folder_if_open(state, message_cache, *folder_id, tx);"""
after = """            // is `mail_sync::INITIAL_FETCH_LIMIT`, on what one chunk fetches.
            let _ = folder_id;"""
red = [
    "test_a_chunk_arriving_rereads_the_folder_and_grows_no_limit",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/everything_comes_down_without_being_asked.rs", tests = 18 },
]

# ── Every check ends by starting the download (#20, #23) ────────────────────
#
# The download of everything is reached from one place: the end of the
# check for mail, which F9, the watch waking and, after 10-06, the schedule
# all end in. A check that stopped asking for it would leave every folder at
# its first five hundred, which is the tester's report, with the runner
# built, tested and reached by nothing. The break keeps the variant's name
# in the check and sends nothing, which is the shape a reading that matched
# a mention would pass; the reading matches the call. One test goes red out
# of the sixteen in the target; the companion is over a snippet.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# everything_comes_down_without_being_asked`.
[[guard]]
name = "every check ends by starting the download"
file = "src/presentation/wx_app.rs"
suite = "everything_comes_down_without_being_asked"
before = """        say(UIUpdate::DownloadRequested);
    });
}"""
after = """        let _ = UIUpdate::DownloadRequested;
    });
}"""
red = [
    "test_every_check_ends_by_starting_the_download",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/everything_comes_down_without_being_asked.rs", tests = 18 },
]

# ── The download asks whether to stop between chunks ─────────────────────────
#
# Pause Downloading is one flag, read by the runner before each chunk of
# headers and handed to each chunk of text. The break leaves the read in
# place and never lets it answer yes before a chunk of headers, so a pause
# waits for the text pass, which on a first download is hours away. One
# test goes red out of the sixteen in the target; the companion is over a
# snippet.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# everything_comes_down_without_being_asked`.
[[guard]]
name = "the download asks whether to stop between chunks"
file = "src/presentation/wx_app.rs"
suite = "everything_comes_down_without_being_asked"
before = """                if stop() {
                    ended = WhyTheRunEnded::ItWasPaused;
                    break 'accounts;
                }"""
after = """                if false && stop() {
                    ended = WhyTheRunEnded::ItWasPaused;
                    break 'accounts;
                }"""
red = [
    "test_the_download_asks_whether_to_stop_between_chunks",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/everything_comes_down_without_being_asked.rs", tests = 18 },
]

# ── A failed chunk waits before the download is tried again ──────────────────
#
# A server that refused a chunk is left alone for a growing time, thirty
# seconds doubling to thirty minutes from `trying_again`, and the runner
# records when the next attempt may start for the main timer to serve. The
# break records the moment without asking the rule, so the next attempt is
# due at once and a provider that has had enough is asked again every ten
# seconds, which is T-10-16. One test goes red out of the sixteen in the
# target; the companion is over a snippet.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# everything_comes_down_without_being_asked`.
[[guard]]
name = "a failed chunk waits before the download is tried again"
file = "src/presentation/wx_app.rs"
suite = "everything_comes_down_without_being_asked"
before = """                let wait = s.downloading.wait.next_wait();"""
after = """                let wait = std::time::Duration::ZERO;"""
red = [
    "test_a_failed_chunk_waits_before_the_download_is_tried_again",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/everything_comes_down_without_being_asked.rs", tests = 18 },
]

# ── Pausing the download ticks the item as well as flipping the flag ─────────
#
# A check item says "checked" or "unchecked" from its own state, so a pause
# that flipped the flag and left the tick would have the Tools menu telling
# a screen reader the opposite of the truth half the time. The break drops
# the tick and keeps the flag, which is the shape that gets past a guard
# written to look for the flag. One test goes red out of the eighteen in the
# target; the companion is over a snippet.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# everything_comes_down_without_being_asked`.
[[guard]]
name = "pausing the download ticks the item as well as flipping the flag"
file = "src/presentation/wx_app.rs"
suite = "everything_comes_down_without_being_asked"
before = """    sync_menu_check(frame, ID_PAUSE_DOWNLOADING, paused);
    if paused {"""
after = """    let _ = frame;
    if paused {"""
red = [
    "test_pause_downloading_is_on_the_tools_menu_ticked_and_carries_the_warning",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/everything_comes_down_without_being_asked.rs", tests = 18 },
]

# ── The download's warning says no real account has met it ──────────────────
#
# One sentence replaced two on 2026-09-17, when the download of everything
# took the place of Fetch Missing Message Text and Download This Whole
# Folder, and it carries the one risk no test here can settle. The break
# keeps the sentence and drops the clause saying no real account has met the
# download, which leaves a warning that reads as ordinary caution. One test
# goes red out of the twenty-seven in the file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the download's warning says no real account has met it"
file = "src/application/allowed.rs"
before = """     its own after every check for mail, and it is experimental: it has never been run against \\
     a real account. Asking your provider for a whole mailbox chunk after chunk is something \\"""
after = """     its own after every check for mail, and it is experimental. \\
     Asking your provider for a whole mailbox chunk after chunk is something \\"""
red = [
    "application::allowed::tests::test_downloading_everything_says_it_is_experimental_and_says_what_could_go_wrong",
]
tests_last_seen = [
    { file = "src/application/allowed.rs", tests = 27 },
]

# ── A deletion that stops being noticed because nobody looks any more ────────
#
# The resume above buys its saving by asking about the uids above the highest
# one held, and a listing like that says nothing about the messages below it.
# So a folder that only ever resumes never sees a deletion: a message somebody
# removed on their phone stays listed here for good, and pressing Enter on it
# gets an error instead of mail.
#
# The bound is what stops that. Every six hours a folder is asked for the whole
# mailbox and compared, and a folder nothing has ever compared is compared on
# its next sync, so the bound cannot quietly mean "never".
#
# The break widens the interval to a hundred and fourteen years. Three tests go
# red out of a library of 6,163: the rule, the sync that acts on it, and the
# range assertion beside the constant.
#
# **The first measurement of this reddened one test, and that was the finding.**
# Both behaviour tests set their fixture to the interval plus a minute, so an
# offset taken from the constant moved with it: widen the interval and the
# fixture is a hundred and fourteen years stale and still due. Only the range
# assertion spoke, which is a check about the number rather than about the rule,
# so a widened bound would have shipped with the tests green. Both fixtures say
# twenty-four hours now, written out, and both go red.
#
# The other direction was taken by hand too, on 2026-09-04. Narrowing the
# interval to nought, so every sync compares and the saving is given back,
# reddens four: the two tests that say a folder compared a moment ago is not
# compared again, the flagship resume test, and the same range assertion.

[[guard]]
name = "a folder is compared against the whole mailbox often enough to notice a deletion"
file = "src/application/finding_what_was_deleted.rs"
before = """pub const A_FOLDER_IS_FULLY_COMPARED_EVERY: i64 = 6;"""
after = """pub const A_FOLDER_IS_FULLY_COMPARED_EVERY: i64 = 1_000_000;"""
red = [
    "application::finding_what_was_deleted::tests::test_a_folder_nobody_has_compared_for_a_day_is_due",
    "application::finding_what_was_deleted::tests::test_the_interval_is_hours_rather_than_days_or_minutes",
    "application::mail_sync::tests::test_a_folder_due_a_comparison_is_listed_in_full_and_forgets_what_went",
]
tests_last_seen = [
    { file = "src/application/finding_what_was_deleted.rs", tests = 9 },
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── A seam the sync reaches past ─────────────────────────────────────────────
#
# How a folder learns what the server no longer has is a named set of ways with
# two members, one built and one declared. The property that is worth anything
# is that adding the second one is an implementor and a probe arm and touches
# nothing else, and that property is only real while `sync_folder` asks the
# chosen way rather than calling the comparison by name. A sync that names the
# comparison has a seam in name only, and the next person to add QRESYNC finds
# it out then rather than now.
#
# The break puts the direct call back. One test goes red out of a library of
# 6,162: the census that walks the shipping half of `src/application` looking
# for places that name the comparison and asks that the one place is the seam's
# own implementor.
#
# Measured by hand on 2026-09-04. It lives against `mail_sync.rs` rather than
# against the seam module because the change worth noticing is the sync reaching
# past the seam, and that is an edit to the sync.

[[guard]]
name = "the sync asks the chosen way rather than naming the comparison"
file = "src/application/mail_sync.rs"
before = """    let forgotten = finding_what_was_deleted::what_the_server_no_longer_has(
        finding_what_was_deleted::the_way_this_server_offers(
            controller.what_this_server_can_do().await,
        ),
        &on_server,
        &stored,
    )?;"""
after = """    let forgotten = uids_to_forget(&on_server, &stored);"""
red = [
    "application::mail_sync::tests::test_the_sync_asks_the_chosen_way_rather_than_naming_the_comparison",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── A folder that was synced before, asked what it holds all over again ──────
#
# SCALE-01. `sync_folder` listed every uid in a folder on every sync, so opening
# a mailbox of forty thousand messages asked the server for forty thousand
# numbers to find the three that had arrived. The state that makes the narrow
# question possible had been stored and read back for a phase and a half, and
# nothing acted on it.
#
# The break makes the decision never resume, by narrowing the arm that answers
# the narrow question to a uid no message can have. Written that way rather than
# as "always answer EveryUid" because a variant nothing constructs is dead code
# and `-D warnings` refuses the build, so a break that deletes the resume cannot
# be measured at all. That constraint is also why the flagship test below passed
# at its own red commit: this record is the measurement standing in for it.
#
# Measured by hand on 2026-09-04, whole library, 6,147 tests. Two went red and
# nothing else did.
#
# Measured again the same day, one commit later, and it had already fallen
# behind: the bound below added a test that resumes a folder, so the break
# reddens three now rather than two. That is the "a record is a measurement with
# a date" failure inside one plan, and it was caught by the re-measure the count
# check asked for rather than by anybody thinking to look.
#
# The opposite direction was taken by hand too, because a bound that can be
# widened without a test failing is the defect this is about. Dropping the two
# CONDSTORE conditions, so every folder with a matching numbering resumes,
# reddens three: the two tests written for those conditions, and
# `test_a_message_the_server_no_longer_has_leaves_the_list`, which is the
# deletion detection saying it has stopped working. That third one is the whole
# reason task 2's bounded comparison exists.

[[guard]]
name = "a folder that was synced before is not asked what it holds all over again"
file = "src/application/mail_sync.rs"
before = """    match held.highest_uid {
        Some(highest) => WhatToAskFor::TheUidsAbove(highest),
        None => WhatToAskFor::EveryUid,
    }"""
after = """    match held.highest_uid {
        Some(highest) if highest == 0 => WhatToAskFor::TheUidsAbove(highest),
        _ => WhatToAskFor::EveryUid,
    }"""
red = [
    "application::mail_sync::tests::test_a_folder_compared_recently_is_not_listed_and_forgets_nothing",
    "application::mail_sync::tests::test_a_folder_that_was_synced_before_is_not_listed_again",
    "application::mail_sync::tests::test_a_folder_with_everything_stored_resumes_from_the_highest_uid_held",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── A mailbox counted full and then listed empty ─────────────────────────────
#
# Two commands ask about one mailbox in every sync and only one of them was
# believed. Counting it reads the server's answer properly; listing it went
# over the helper above. So a refused listing arrived as an empty mailbox
# beside a count saying it held forty thousand messages, and nothing anywhere
# noticed the two disagreeing before the rows were deleted.
#
# The read side is fixed. This is the belt that does not depend on it.
#
# Re-pointed on 2026-09-04 by plan 03-07, which narrowed the check rather than
# moving it: it takes the listing now instead of its length, because only a
# listing claiming the whole mailbox can contradict a count of it. A resumed
# sync asks about the uids above the highest one held and is answered with none
# on every folder nothing has arrived in, so the old shape would have refused
# the ordinary sync of every quiet mailbox.
#
# Worth knowing about the two checks that watch this file, because this change
# slipped past one of them. Changing the call left the record's before-text
# absent and its after-text present, which is exactly what a `scripts/guards.sh`
# run in progress looks like, so `test_every_guard_record_still_names_one_place`
# took the mid-measurement exemption and said nothing. The count check next door
# is what named this record, and only because the same commit happened to change
# the number of tests in the file.
#
# It is anchored on the function rather than on the call site now, and that is
# the second half of the same lesson. The old anchor quoted the call and the
# `uids_to_forget` line under it, so every plan that touches either of those
# lines knocks this record over, and the next one to do so is the plan sitting
# beside this one: task 2 replaces that call with the seam. The function's own
# body is the thing this guard is really about and nothing else has a reason to
# move it.
#
# Measured by hand on 2026-09-04, twice. Deleting the call site reddens one
# test. Making the function always answer no, which is the anchor written down
# here, reddens two: that test and the unit test for the rule itself, out of a
# library of 6,147.

[[guard]]
name = "a mailbox the server counted and then would not list is not one that emptied"
file = "src/application/mail_sync.rs"
before = """fn listing_contradicts_the_count(listed: &ServerListing, counted: u32) -> bool {
    matches!(listed, ServerListing::TheWholeMailbox(uids) if uids.is_empty()) && counted > 0
}"""
after = """fn listing_contradicts_the_count(listed: &ServerListing, counted: u32) -> bool {
    let _ = (listed, counted);
    false
}"""
red = [
    "application::mail_sync::tests::test_a_server_that_counts_a_full_mailbox_and_then_lists_none_of_it_removes_nothing",
    "application::mail_sync::tests::test_two_answers_about_one_mailbox_that_disagree_are_not_believed",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── A message the server copied is not a message the server moved ───────────
#
# A mail server without MOVE takes a copy and marks the original instead, and
# some will not even mark it. That leaves one message in two folders. Both
# answers used to fall through to the same two lines as a real move: the row
# was filed away on this computer, hiding the copy that is really still there,
# and the message was added to "sorted by your rules". Being told twelve were
# sorted and then finding twelve duplicates is worse than being told nothing.
#
# The break below is the code exactly as it was. It also brings back the worse
# half: with the row filed away, its uid leaves what the cache holds, so the
# next check for mail fetches the original again as though it were new and the
# rules file a third copy of it.

[[guard]]
name = "a message the server copied rather than moved is not counted as filed"
file = "src/application/mail_sync.rs"
before = """            Ok(crate::service::protocols::imap::Moved::CopiedAndFlagged(_)) => {
                could_not.push(copied_and_the_original_marked(&from.name, &into.name));
                continue;
            }
            Ok(crate::service::protocols::imap::Moved::CopiedAndNotFlagged(said)) => {
                could_not.push(copied_and_the_original_left(&from.name, &into.name, &said));
                continue;
            }"""
after = """            Ok(partly) => could_not.push(partly.spoken(&into.name)),"""
red = [
    "application::mail_sync::tests::test_a_message_the_server_put_in_two_folders_is_not_counted_as_sorted",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── Two reads that were never asked whether they read anything ──────────────
#
# Both found by mutation testing on the day the read side was rewritten. Every
# test around them asserted a refusal or an absence, so the arm that does the
# actual reading could be deleted outright with the whole suite still green.
#
# Taking the subscriptions out means no folder on any account is ever
# subscribed, and the window that asks which folders to sync offers an empty
# default. Taking the message out means every message a server hands over is
# reported as a message that is not there.

[[guard]]
name = "a subscription the server lists reaches the folder it is about"
file = "src/service/protocols/imap.rs"
before = """                |response| match response {
                    Response::MailboxData(MailboxDatum::List { name, .. }) => {
                        Some(name.to_string())
                    }
                    _ => None,
                },"""
after = """                |response| match response {
                    _ => None,
                },"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_subscriptions_a_server_lists_reach_the_folders_they_are_about",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named,
# a second reading of the same fetch against a server that answers.
name = "a message the server hands over is not reported as one that is not there"
file = "src/service/protocols/imap.rs"
before = """                format!("UID FETCH {uid} BODY.PEEK[]"),
                "fetching the message",
                |response| match response {
                    Response::Fetch(_, attributes) => {
                        attributes.iter().find_map(body_bytes).map(<[u8]>::to_vec)
                    }
                    _ => None,
                },"""
after = """                format!("UID FETCH {uid} BODY.PEEK[]"),
                "fetching the message",
                |response| match response {
                    _ => None,
                },"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_session_allowed_to_read_reaches_the_command",
    "service::protocols::imap::against_a_server_that_answers::test_the_message_a_server_sends_back_is_the_one_that_is_handed_over",
    # Added 2026-09-15 by 08-07's sweep: a move across accounts, written
    # 2026-09-07 and 2026-09-08 in a file this record had never named, fetches
    # the message from the source and every one of its cases reads what the
    # server handed over. Nineteen, which is what a record naming two said
    # nothing about for a week.
    "application::mail_across_accounts::tests::test_a_copy_across_accounts_says_nothing_to_the_source_that_changes_it",
    "application::mail_across_accounts::tests::test_a_destination_that_refuses_the_append_leaves_the_source_untouched",
    "application::mail_across_accounts::tests::test_a_folder_that_could_not_be_read_beforehand_settles_nothing_afterwards",
    "application::mail_across_accounts::tests::test_a_message_that_was_already_there_is_not_read_as_the_one_just_sent",
    "application::mail_across_accounts::tests::test_a_message_too_large_to_keep_still_moves",
    "application::mail_across_accounts::tests::test_a_move_that_crosses_never_sends_a_copy_or_a_move_to_the_source",
    "application::mail_across_accounts::tests::test_a_refusal_is_an_answer_so_a_message_already_there_is_not_read_as_an_arrival",
    "application::mail_across_accounts::tests::test_a_source_that_cannot_remove_one_message_says_it_is_here_and_marked",
    "application::mail_across_accounts::tests::test_a_source_that_will_not_mark_the_message_leaves_it_in_both_places",
    "application::mail_across_accounts::tests::test_an_account_this_program_may_not_write_to_refuses_at_its_own_session",
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_about_a_message_with_no_identifier_removes_nothing",
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_that_is_found_afterwards_is_removed_at_the_source",
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_that_is_not_found_removes_nothing",
    "application::mail_across_accounts::tests::test_no_way_a_crossed_move_can_end_leaves_the_bytes_behind",
    "application::mail_across_accounts::tests::test_nothing_is_said_to_the_source_that_changes_it_until_the_append_has_gone_out",
    "application::mail_across_accounts::tests::test_the_destination_is_told_to_append_into_the_folder_that_was_chosen",
    "application::mail_across_accounts::tests::test_the_message_is_already_on_the_disk_when_the_append_goes_out",
    "application::mail_across_accounts::tests::test_the_message_reaches_one_server_and_leaves_the_other",
    "application::mail_across_accounts::tests::test_the_message_the_destination_is_given_is_the_one_the_source_handed_over",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 9 and a
    # measurement here that found the same ten. 11-07.2 made a crossing a
    # waiting move with its bytes, so `moves_waiting` now fetches the message
    # from the source too and every one of its crossing cases reads what the
    # server handed over. A file this record had never named, which is the
    # direction the count check cannot see.
    "application::moves_waiting::tests::test_a_copy_across_accounts_lands_with_nothing_removed_at_the_source",
    "application::moves_waiting::tests::test_a_crossing_is_replayed_at_a_check_of_the_destination_account",
    "application::moves_waiting::tests::test_a_crossing_that_lands_answers_done_and_the_row_settles_in_the_other_account",
    "application::moves_waiting::tests::test_a_destination_that_already_held_the_identifier_reads_only_a_new_number_as_the_arrival",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_cannot_be_asked_answers_not_reached_and_the_bytes_stay",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_does_not_hold_it_answers_refused",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_then_holds_it_answers_done_and_removes_at_the_source",
    "application::moves_waiting::tests::test_a_destination_that_refuses_the_append_answers_refused_and_the_undo_puts_the_row_back",
    "application::moves_waiting::tests::test_a_message_too_large_to_hold_is_fetched_and_the_store_says_so",
    "application::moves_waiting::tests::test_two_crossings_are_replayed_in_the_order_they_were_asked",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]
# ── Mail collected over POP, removed from a server nobody asked ─────────────
#
# The gate that holds every other write back was never asked over POP. DELE was
# sent with no check of any kind, and neither the account's own setting nor
# --read-only could stop it, so every screen and every document saying writes
# are behind Allow Changes was wrong for a POP account. It only ran for an
# account whose own housekeeping is set to clear the server after so many days,
# which is why it went unnoticed: the default leaves everything alone.

[[guard]]
name = "a POP removal refused by the setting says nothing to the server"
file = "src/service/protocols/pop3.rs"
before = """        self.may_i("remove a message from the mail server")?;
        self.command("DELE", &[&id.to_string()]).await?;"""
after = """        self.command("DELE", &[&id.to_string()]).await?;"""
# Re-measured on 2026-08-31: five, not four. Plan 02-01 added a test asserting
# that receiving a POP message is *not* gated, and an absence assertion is worth
# nothing without a positive control, so that test reads this gate first to
# prove its reading can see a gate at all. Removing this gate takes the control
# out from under it. That is the intended behaviour of the control and it makes
# this record one test longer.
red = [
    "application::mail_controller::against_a_server_that_answers::test_a_pop_removal_is_refused_here_when_the_session_may_not_change_anything",
    "service::outward::completeness::test_every_mailbox_write_is_on_the_mail_wire_list",
    "service::outward::completeness::test_nothing_sends_a_changing_command_without_asking_the_gate",
    "service::protocols::pop3::against_a_server_that_answers::test_nothing_is_said_to_the_server_with_the_gate_closed",
    "service::protocols::pop3::tests::test_receiving_a_pop_message_is_not_gated_on_the_read_setting",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/outward.rs", tests = 38 },
    { file = "src/service/protocols/pop3.rs", tests = 10 },
]

# ── A clear-out the setting held back, reported as a failed mail check ──────
#
# Everything downloaded correctly and the only thing that did not happen was
# the clearing out. Raised through the question mark it ends the whole check,
# so the polite ending that commits a POP session never runs and the person is
# told their mail did not arrive when all of it did. The same shape the
# calendar and the tasks syncs were already fixed for.

[[guard]]
name = "a POP removal held by Allow Changes is a wait, not a failed check"
file = "src/application/pop_sync.rs"
before = """    let mut removed = 0;
    let mut waiting = 0;
    for (id, _) in &stale {
        match server.mark_for_deletion(*id).await {
            Ok(()) => removed += 1,
            // Held back by Allow Changes rather than refused by the server.
            // Nothing left this machine, the mail is still there, and the
            // check itself worked, so this is a wait and not a failure. The
            // same rule the calendar and the tasks syncs follow.
            Err(e) if crate::service::outward::was_refused_by_the_gate(&e) => waiting += 1,
            Err(e) => return Err(e),
        }
    }"""
after = """    let removed = stale.len();
    let waiting = 0;
    for (id, _) in &stale {
        server.mark_for_deletion(*id).await?;
    }"""
red = [
    "application::pop_sync::tests::test_a_removal_held_by_allow_changes_is_counted_as_waiting_rather_than_failing",
]
tests_last_seen = [
    { file = "src/application/pop_sync.rs", tests = 49 },
]

# ── A subscription change nothing had ever sent ─────────────────────────────
#
# Which folders somebody has chosen to sync is written to the server so the
# choice holds in every client the account is opened in. Neither direction had
# ever been sent to anything: the loopback script had no arm for either command
# and the gate-closed list had no row for it, so the one write with no
# measurement at all was the one that decides what gets synced.

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: three more tests went red than this named.
# All three count the gate rather than exercise it, which is the strongest kind
# of extra to find here: taking one `may_i` away moves a census, so the record
# was resting on three checks nobody had written down.
name = "a subscription change refused by the setting says nothing to the server"
file = "src/service/protocols/imap.rs"
before = """        self.may_i("change which folders you are subscribed to")?;
        let outcome = if subscribed {"""
after = """        let outcome = if subscribed {"""
red = [
    "service::outward::completeness::test_every_mailbox_write_is_on_the_mail_wire_list",
    "service::outward::completeness::test_nothing_sends_a_changing_command_without_asking_the_gate",
    "service::outward::completeness::test_the_read_gate_does_not_move_the_count_of_gated_writes",
    "service::protocols::imap::against_a_server_that_answers::test_every_mailbox_write_says_nothing_to_the_server_with_the_gate_closed",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── Every message sent left with no identifier of its own ───────────────────
#
# The mail library fills in a Date when one is missing and writes no
# Message-ID unless asked, and nothing asked. So no receiving client could
# thread anything sent from here, the copy filed in Sent had an empty
# identifier column, and replying to your own sent mail started a new
# conversation.

[[guard]]
name = "a sent message carries an identifier of its own"
file = "src/service/protocols/smtp.rs"
before = """        .subject(&email.subject)
        // Taken from the message rather than made here. The mail library will
        // make one if asked with nothing, and what it makes carries the name
        // of this computer, which is not something to send to a stranger.
        .message_id(Some(email.message_id.clone()));"""
after = """        .subject(&email.subject);"""
red = [
    "application::sent_copy::tests::test_a_reply_to_a_message_this_program_sent_joins_its_conversation",
    "service::protocols::smtp::against_a_server_that_answers::test_the_identifier_on_the_wire_is_the_one_filed_in_sent",
    "service::protocols::smtp::tests::test_a_sent_message_carries_an_identifier_naming_the_domain_it_is_from",
]
tests_last_seen = [
    { file = "src/application/sent_copy.rs", tests = 22 },
    { file = "src/service/protocols/smtp.rs", tests = 40 },
]

# ── One queued message, one identifier, however many attempts ───────────────
#
# A send that failed after the message reached the server keeps its outbox row
# and is tried again. Built afresh each time, so a random identifier per
# attempt is the plausible mistake, and the recipient would get what looks
# like two messages.

[[guard]]
name = "one queued message keeps one identifier"
file = "src/application/mail_controller.rs"
before = "        message_id: crate::application::message_id::derived(&req.queue_id, &req.from_address),"
after = "        message_id: crate::application::message_id::fresh(&req.from_address),"
red = [
    "application::mail_controller::send_request_tests::test_one_queued_message_keeps_one_identifier_across_attempts",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
]

# ── What is said about a repeating event, and when ──────────────────────────
#
# This product exists for people who cannot see the list, so a sentence that
# disagrees with what happened is not a cosmetic fault, it is the fault. Four
# of them lived on this path at once: a day taken off announced while the day
# stayed on the list, two exits after a confirmed delete that said nothing at
# all, a window that announced a deletion at the moment it wrote the request
# down rather than when it was carried out, and a save reported as a deletion.

[[guard]]
name = "a confirmed delete that finds nothing to delete still says so"
file = "src/application/pim_command.rs"
before = """    format!("{named} is no longer there. Nothing has been changed.")"""
after = """    format!("{named} deleted")"""
red = [
    "application::pim_command::tests::test_a_row_that_went_away_between_the_question_and_the_answer_is_said_not_left_silent",
]
tests_last_seen = [
    { file = "src/application/pim_command.rs", tests = 38 },
]

[[guard]]
name = "one day taken off a series is not called a deletion"
file = "src/application/calendar.rs"
before = """        title => format!("{title}: that one day is taken off. The other days are unchanged."),"""
after = """        title => format!("{title} deleted"),"""
red = [
    "application::calendar::tests::test_the_words_for_a_day_taken_off_say_the_others_are_unchanged",
    "application::calendar::tests::test_what_the_calendar_window_did_is_said_in_words_a_person_can_tell_apart",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_taken_off_by_the_calendar_window_says_the_same_thing_as_the_panel_does",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "nothing the calendar window has written down is said as though it happened"
file = "src/application/calendar.rs"
before = """        WrittenDown::WholeSeriesDeleted => "The event will be deleted when you close this window.","""
after = """        WrittenDown::WholeSeriesDeleted => "Event deleted.","""
red = [
    "application::calendar::tests::test_nothing_the_calendar_window_writes_down_is_said_as_though_it_had_happened",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "both exits after a confirmed delete say the row has gone"
file = "src/presentation/managers.rs"
before = """            let Some(opened) = opened else {
                return send_refusal(tx, rt, &no_longer_there(kind, &name));
            };"""
after = """            let Some(opened) = opened else {
                return;
            };"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_every_answer_a_delete_gives_reaches_the_one_place_that_speaks_and_reloads",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── The two doors every answer leaves through ──────────────────────────────
#
# Every outcome and every refusal the managers layer produces goes out as one
# of two updates. Both write to the status bar, which is a line at the bottom
# of a window and not somewhere anybody navigating by ear goes, and both say
# what they write as well. If either stopped saying it, that whole layer would
# go quiet at once and nothing here would have noticed: neither of these was
# recorded until the round that found the Calendar window had gone quiet the
# same way.
#
# Each of these now reddens a second test as well, and this is the run that
# found it. The round that made three silent screens speak added a check over
# every arm of that handler at once: an arm may write to the status bar and say
# nothing only if somebody has registered it as quiet with a reason. These two
# arms are not registered, so taking either announcement away is now caught
# twice, by the check about those two doors and by the check about all the
# arms. The records said one test each until the run below said otherwise.
#
# What these records cannot see. Both tests are source reads, because reaching
# these arms needs a window, a frame and a running event loop. They can tell
# that the arm announces what it shows and at what priority. They cannot tell
# whether the announcement reaches a screen reader, or whether the sentence
# handed in is true. Only a screen reader run answers the first.

# The line moved from Low to Normal on 2026-09-17 (#38), when the steady run
# of a sync left this channel for `Progress` and what remains on it, the
# answers to a key, was raised so a sync cannot bury them; the same break,
# the announcement taken out, on the new line, measured again.
[[guard]]
name = "what goes to the status bar is said as well as shown"
file = "src/presentation/wx_app.rs"
before = """            let _ = a11y.announce_topic(status, Priority::Normal, "status");"""
after = """            let _ = status;"""
red = [
    "presentation::wx_app::what_the_status_line_says::test_every_arm_that_shows_something_says_it_or_is_named_as_quiet",
    "presentation::wx_app::what_the_status_line_says::test_the_two_answers_every_command_leaves_through_are_said_out_loud",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named,
# and it is in the managers module rather than the window, which is a caller
# this record's own two tests give no sign of.
name = "a refusal is said as well as shown"
file = "src/presentation/wx_app.rs"
before = """            let _ = a11y.announce_topic(why, Priority::High, "refusal");"""
after = """            let _ = why;"""
red = [
    "presentation::managers::tests::test_a_refused_move_is_said_where_a_refusal_is_said",
    "presentation::wx_app::what_the_status_line_says::test_every_arm_that_shows_something_says_it_or_is_named_as_quiet",
    "presentation::wx_app::what_the_status_line_says::test_the_two_answers_every_command_leaves_through_are_said_out_loud",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "a series that is still there is not reported as deleted"
file = "src/presentation/managers.rs"
before = """    if the_event_is_still_there {"""
after = """    if false {"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_a_day_taken_off_by_the_calendar_window_says_the_same_thing_as_the_panel_does",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── One action, one answer, whichever door it came through ──────────────────
#
# Taking one day off a repeating event is reachable two ways: the Delete key on
# the calendar panel, and Delete inside the Calendar window. The panel said the
# sentence somebody needs. The window handed back the identifier the row is
# stored under, which went to the status bar and was announced nowhere, so the
# same action was answered two ways and one of the answers reached nobody.

[[guard]]
name = "the two doors say one sentence about a day taken off"
file = "src/presentation/managers.rs"
before = """        crate::application::calendar::one_day_taken_off(name)"""
after = """        format!("Calendar event saved: {name}")"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_a_day_taken_off_by_the_calendar_window_says_the_same_thing_as_the_panel_does",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "one owner for the words about a day taken off"
file = "src/application/calendar.rs"
before = """        WrittenDown::OneDayTakenOff => one_day_taken_off(name),"""
after = """        WrittenDown::OneDayTakenOff => format!("{name}: that day is off."),"""
red = [
    "application::calendar::tests::test_what_the_calendar_window_did_is_said_in_words_a_person_can_tell_apart",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── The Calendar window's closing sentence ──────────────────────────────────
#
# It reported that calendar events had been saved whatever happened, so opening
# the window and pressing Close announced a save nobody had asked for. Every
# other manager returns without a word when the dialog is cancelled.
#
# What the first of these records cannot see. Half of its test is a source
# read, because the decision sits at the end of a routine that needs a window
# on a screen to reach. The read can tell that the window asks before it
# speaks; it cannot tell whether the sentence it then says is true. The other
# half is a real call and does check the sentence.

[[guard]]
name = "a calendar window that did nothing says nothing"
file = "src/presentation/managers.rs"
before = """    if failures.is_empty() {
        if let Some(said) = what_to_report(&done) {
            send_status(tx, rt, &said);
        }
    } else {"""
after = """    if failures.is_empty() {
        send_status(tx, rt, "calendar events saved");
    } else {"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_a_calendar_window_that_did_nothing_says_nothing_was_done",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── A sentence about something that did not happen ──────────────────────────
#
# The Sync button in the Calendar window announced that a sync had been asked
# for and started nothing at all. That is worse than silence: the next sentence
# from the same place is believed too.
#
# What this record cannot see. It is a source read, so it can tell that the arm
# calls the routine that starts a sync. It cannot tell whether that sync then
# reaches a server, and nothing here has ever run against a real account.

[[guard]]
name = "the sync button in the calendar window really starts a sync"
file = "src/presentation/managers.rs"
before = """                crate::presentation::wx_app::spawn_calendar_sync(state, tx, rt);
                done.push("A calendar sync has started.".to_string());"""
after = """                send_status(tx, rt, "Calendar sync requested");"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_the_sync_button_in_the_calendar_window_really_starts_a_sync",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── The order the calendar window does two things in ────────────────────────
#
# The three records below are all defended by one test, and that test reads the
# window as text. This note is what none of them said until now.
#
# What these records cannot see. Reaching the arms they break takes a window on
# the screen, a list with a row picked out and somebody clicking through two
# dialogs, so the test behind them never runs a line of the code they change.
# The arm could stop being reachable, the menu line could lose its binding, the
# whole branch could go dead, and all three records stay green while every one
# of them is applied.
#
# They also cannot say whether the refusal is a true one, whether the sentence
# fits what was refused, or whether an announcement raised from inside a modal
# dialog is heard. Only a screen reader run answers the last, and that has not
# been done for these arms.
#
# What they do hold is the order: the question asked before anything is queued,
# the refusal and the thing it refuses on opposite branches, and the sentence in
# the tense of something that has not happened yet. The middle one of the three
# is the narrow edit that leaves the order intact and the behaviour gone, which
# is the shape this project has now got wrong three times.

[[guard]]
name = "the calendar window asks whether an answer can be carried out before it queues it"
file = "src/presentation/wx_calendar.rs"
before = """        if let Err(refused) = can_be_honoured(WhatIsBeingDone::Deleting, means, &allows) {
            said_and_shown(status, a11y, &refused, Priority::High);
        } else {
            state.actions.push(CalendarAction::DeleteEvent(item, means));"""
after = """        {
            state.actions.push(CalendarAction::DeleteEvent(item, means));"""
red = [
    "presentation::wx_calendar::tests::test_the_calendar_window_asks_before_it_says_anything",
]
tests_last_seen = [
    { file = "src/presentation/wx_calendar.rs", tests = 15 },
]

# The narrower edit, the third of this shape found in this project. The record
# above takes the question away altogether. This one leaves the question asked
# and the refusal read out, and takes away the branch, so the day is refused
# out loud and queued for the calendar server in the same breath. Everything
# still happens in the order the guard used to check.

[[guard]]
name = "a calendar answer that was refused is not queued anyway"
file = "src/presentation/wx_calendar.rs"
before = """            said_and_shown(status, a11y, &refused, Priority::High);
        } else {
            state.actions.push(CalendarAction::DeleteEvent(item, means));"""
after = """            said_and_shown(status, a11y, &refused, Priority::High);
        }
        {
            state.actions.push(CalendarAction::DeleteEvent(item, means));"""
red = [
    "presentation::wx_calendar::tests::test_the_calendar_window_asks_before_it_says_anything",
]
tests_last_seen = [
    { file = "src/presentation/wx_calendar.rs", tests = 15 },
]

[[guard]]
name = "the calendar window says what is waiting rather than what is done"
file = "src/presentation/wx_calendar.rs"
before = """            said_and_shown(
                status,
                a11y,
                what_is_waiting(match means {
                    EditMeans::OneDay => WrittenDown::OneDayTakenOff,
                    EditMeans::WholeSeries => WrittenDown::WholeSeriesDeleted,
                }),
                Priority::Normal,
            );"""
after = """            said_and_shown(
                status,
                a11y,
                match means {
                    EditMeans::OneDay => "That one day is taken off.",
                    EditMeans::WholeSeries => "Event deleted.",
                },
                Priority::Normal,
            );"""
red = [
    "presentation::wx_calendar::tests::test_the_calendar_window_asks_before_it_says_anything",
]
tests_last_seen = [
    { file = "src/presentation/wx_calendar.rs", tests = 15 },
]

# ── Every answer a window gives on a line of text has to be heard ───────────
#
# These windows put their sentences on a line of text under the title. Nothing
# raises a notification for that line, so a sentence only shown there is an
# answer nobody navigating by ear ever gets. That is what a commit in round 17
# left behind: the refusal for "just this one day" on a Google, an Outlook or a
# read-only calendar stopped being queued and started being written to that
# line, and a refusal that used to be read out at once became silence.
#
# The one call that shows and says was the calendar's own until the round that
# found three more windows doing the same thing. It moved into a module of its
# own rather than being copied three times, because two pieces of code
# answering one question is how most of the faults recorded in this file
# started. So this break is now measured where that call lives, and it silences
# every window that leans on it at once.
#
# What this record cannot see. It is a source read, so it can tell that one
# routine shows and says every sentence and that nothing else writes to the
# line. It cannot tell whether the announcement reaches a screen reader from
# inside a modal dialog, whether the priority is the right one, or whether the
# sentence itself is true. Only a screen reader run answers the first.

[[guard]]
name = "every answer a window gives on a line of text is said out loud"
file = "src/presentation/status_line.rs"
before = """    line.set_label(said);
    let _ = a11y.announce(said, priority);"""
after = """    line.set_label(said);
    let _ = (a11y, priority);"""
red = [
    "presentation::status_line::tests::test_the_one_place_that_shows_a_sentence_says_it_too",
    "presentation::wx_account_manager::tests::test_every_answer_the_account_manager_gives_is_said_out_loud",
    "presentation::wx_calendar::tests::test_every_answer_the_calendar_window_gives_is_said_out_loud",
    "presentation::wx_managers::tests::test_every_answer_the_manager_windows_give_is_said_out_loud",
]
tests_last_seen = [
    { file = "src/presentation/status_line.rs", tests = 3 },
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
    { file = "src/presentation/wx_calendar.rs", tests = 15 },
    { file = "src/presentation/wx_managers.rs", tests = 44 },
]

# ── The failures on the screen accounts are created on ──────────────────────
#
# Twenty-three answers on the account manager landed on a line of text above
# the buttons and nowhere else, and among them were failures somebody has to
# act on: signing in failed, and the browser would not open on the page that
# hands out app passwords. Somebody adding their first account by ear pressed
# a button, heard nothing, and had no way to know whether it had worked.
#
# Signing in failing is the one measured here because it is the worst of them:
# it leaves an account that looks added and cannot fetch mail. It is said
# above the ordinary run of outcomes, because it is the answer to the button
# just pressed.
#
# What this record cannot see. It is a source read, because reaching this arm
# needs a window, a frame and a running event loop. It can tell that the arm
# says what it shows and at what priority. It cannot tell whether the
# announcement reaches a screen reader from inside a modal dialog, or whether
# the sentence is a true one. Only a screen reader run answers the first.
#
# Rewritten on 2026-09-18 by 11-02: the arm no longer says the sentence through
# said_and_shown at High and then signals the sign-in event at Urgent, but
# goes out as one notification through shown_and_signalled with the event
# carrying the sentence (the record below is that break). The old before no
# longer named a place in the tree. The break is the same silence as before,
# the sentence shown and nothing said or signalled, and it now reddens three:
# the two it always did, and the reading of reauthorize_selected that counts
# the one notification per unauthorised arm.

[[guard]]
name = "signing in failing on the account screen is said, not only shown"
file = "src/presentation/wx_account_manager.rs"
before = """                    shown_and_signalled(
                        status,
                        a11y,
                        FeedbackEvent::AccountNeedsAttention,
                        &format!("Signing in failed: {msg}"),
                    );"""
after = """                    status.set_label(&format!("Signing in failed: {msg}"));"""
red = [
    "presentation::wx_account_manager::tests::test_every_answer_the_account_manager_gives_is_said_out_loud",
    "presentation::wx_account_manager::tests::test_the_two_failures_on_this_screen_are_said_above_the_ordinary_run",
    "presentation::wx_account_manager::tests::test_an_account_still_unauthorised_after_trying_again_reaches_the_earcon_channel",
]
tests_last_seen = [
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
]

# ── A sign-in failure is one notification, not a sentence and a cue ─────────
#
# Each of the three ways Sign In Again can leave an account unauthorised used
# to say its sentence through said_and_shown at High and then signal
# AccountNeedsAttention at Urgent, a millisecond apart, on two topics. NVDA
# run 35336142908 on main at 744d05ef heard the second and never the first:
# the transcript ends "Sign-in needs attention, Scan target", and the case
# listening for "Signing in failed" had failed the same way at every run since
# 2026-09-15, behind a badge that reported success over the failed job. One
# outcome sent as two notifications is guardrail 5's flood in miniature, and
# one notification cannot cut off another. Since 2026-09-18 each arm is one
# call, shown_and_signalled, which puts the sentence on the line and raises
# the event with the sentence as its detail, so the words, the earcon, the
# braille and the visual come from one event on the channels the person's
# Feedback row for it allows.
#
# The break puts the two notifications back into the Failed arm. The reading
# of reauthorize_selected counts the one call per unauthorised arm and refuses
# a separate signal in the function; the reading of the two failures said
# above the ordinary run holds the arm to the one call and the event.
#
# What this record cannot see. It is a source read, because reaching this arm
# needs a window, a frame and a running event loop. It cannot tell whether
# the one line, "Sign-in needs attention, Signing in failed: ...", is heard
# whole by a screen reader; that is the runner's case at the next push, or
# the tester's ear (ledger 531's neighbour in 11-02's summary).

[[guard]]
name = "a sign-in failure on the account screen is one notification, not a sentence and a cue a moment apart"
file = "src/presentation/wx_account_manager.rs"
before = """                    shown_and_signalled(
                        status,
                        a11y,
                        FeedbackEvent::AccountNeedsAttention,
                        &format!("Signing in failed: {msg}"),
                    );"""
after = """                    said_and_shown(
                        status,
                        a11y,
                        &format!("Signing in failed: {msg}"),
                        Priority::High,
                    );
                    let _ = a11y.signal(FeedbackEvent::AccountNeedsAttention, &name);"""
red = [
    "presentation::wx_account_manager::tests::test_an_account_still_unauthorised_after_trying_again_reaches_the_earcon_channel",
    "presentation::wx_account_manager::tests::test_the_two_failures_on_this_screen_are_said_above_the_ordinary_run",
]
tests_last_seen = [
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
]

# ── The one call for an outcome that is also an event raises the event ──────
#
# shown_and_signalled exists so that an outcome which is both an answer and an
# event goes out once: the sentence on the line, and the event raised with the
# sentence as its detail. Its whole point is the signal. With the signal gone
# the sentence is shown and nothing is said, sounded or brailled, which is the
# silence status_line.rs was written to end, now for the one outcome that asks
# a person to act. The other half, the sentence announced as well as
# signalled, is the two notifications the call replaces, and the same reading
# refuses that too.
#
# What this record cannot see. A source read of the helper's body; it cannot
# tell whether the one notification is heard.

[[guard]]
name = "an outcome that is also an event is shown and signalled from one call"
file = "src/presentation/status_line.rs"
before = """    line.set_label(said);
    let _ = a11y.signal(event, said);"""
after = """    line.set_label(said);
    let _ = (a11y, event);"""
red = [
    "presentation::status_line::tests::test_an_outcome_that_is_also_an_event_is_one_notification_carrying_the_sentence",
]
tests_last_seen = [
    { file = "src/presentation/status_line.rs", tests = 3 },
]

# ── What the tag, signature, filter and contact windows answer ──────────────
#
# Added, updated, deleted with a name, and being told to select something
# first: ten answers across four windows, every one shown on a line of text
# above the buttons and said nowhere. The description of the chrome those
# windows share said that line announced changes, and it never had, which is
# how it went unnoticed for as long as it did.
#
# Deleting is measured because it is the one that names something: hearing
# which item went is the only confirmation that the right one did. The line it
# goes on has two names in this one file, one in the shared loop and one in the
# contact loop, and the check counts both, because a check that counted one
# would pass with half the file silent.
#
# What this record cannot see. It is a source read, because reaching these arms
# needs a window, a frame and a running event loop. It can tell that the arm
# says what it shows and at what priority. It cannot tell whether the
# announcement reaches a screen reader from inside a modal dialog, or whether
# the name in the sentence is the item that was really deleted. Only a screen
# reader run answers the first.


# Re-measured by hand on 2026-08-13: the delete arm this guard is about now
# asks manager_words::deleted for its words, named on a kind rather than
# worded here, so before/after are updated to match. Confirmed against the
# whole library that the break still reddens exactly the one test named.

[[guard]]
name = "deleting from a manager window says which item went"
file = "src/presentation/wx_managers.rs"
before = """        said_and_shown(
            status_text,
            a11y,
            &manager_words::deleted(kind, &name, left),
            Priority::Normal,
        );"""
after = """        status_text.set_label(&manager_words::deleted(kind, &name, left));"""
# Re-measured by hand on 2026-09-01 against the whole 5,942-test library, after
# `deleted` gained the count that goes on the end of a condition list's
# sentence and the old break stopped naming a place in the tree. Still exactly
# one, unchanged.
red = [
    "presentation::wx_managers::tests::test_every_answer_the_manager_windows_give_is_said_out_loud",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
]

# ── An update that shows something either says it or is named as quiet ───────
#
# Four arms of the one routine that handles every update wrote to the status
# bar and said nothing. Three of them were quiet for a reason nobody had
# written down and the fourth, the task lists arriving, was an oversight: it
# sits between calendar containers and reminders, both of which show and say.
# Notes turned out to be worse still, filling its sidebar without showing or
# saying anything, and now does both.
#
# The other three stay quiet on purpose and are registered with a reason each.
# A silence is now allowed only if somebody writes a sentence saying where the
# same event is spoken instead, and the four records below are what notices
# when one of those explanations stops being true.
#
# What these records cannot see. They are source reads, because reaching an arm
# needs a window, a frame and a running event loop. They can tell that the arm
# announces what it shows. They cannot tell whether the announcement reaches a
# screen reader, whether two announcements a second apart are distinguishable
# by ear, or whether the sentence is a true one. Only a screen reader run
# answers those, and opening Tasks now says two sentences where it said one.

[[guard]]
name = "how many task lists arrived is said, not only shown"
file = "src/presentation/wx_app.rs"
before = """            let _ = a11y.announce_topic(&msg, Priority::Low, "task-lists");"""
after = """            let _ = &msg;"""
red = [
    "presentation::wx_app::what_the_status_line_says::test_every_arm_that_shows_something_says_it_or_is_named_as_quiet",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "the offline mode change is spoken by the sentence sent ahead of it"
file = "src/presentation/wx_app.rs"
before = """    send_status(tx, rt, label);"""
after = """    let _ = label;"""
red = [
    "presentation::wx_app::what_the_status_line_says::test_the_quiet_arms_are_each_said_somewhere_else",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "the outbox queue count is covered by the flush result sent before it"
file = "src/presentation/wx_app.rs"
before = """        let _ = tx.send(UIUpdate::OutboxFlushComplete(sent, failed)).await;"""
after = """        let _ = (sent, failed);"""
red = [
    "presentation::wx_app::what_the_status_line_says::test_the_quiet_arms_are_each_said_somewhere_else",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "switching module is said where the switch happens"
file = "src/presentation/wx_app.rs"
before = """                    // Announce to screen reader
                    let _ = a11y.announce(
                        &format!("Switching to {}", label),
                        crate::presentation::accessibility::announcements::Priority::Normal,
                    );"""
after = """                    let _ = label;"""
red = [
    "presentation::wx_app::what_the_status_line_says::test_the_quiet_arms_are_each_said_somewhere_else",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── One day cut out of a series is two writes, and only one takes a day away ─
#
# Changing one day of a repeating event creates a separate appointment for that
# day and takes the day off the series. The second write removes something, so
# it waits for the first. Sent the other way round, a create the server refuses
# leaves the day gone from the calendar server and stored on this computer
# alone, which is where it stayed until a reinstall.

[[guard]]
name = "the day cut out of a series is stored naming the series it came from"
file = "src/presentation/managers.rs"
before = """        categories: series.categories.clone(),
        cut_from_event_id: Some(series.id.clone()),"""
after = """        categories: series.categories.clone(),"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_the_day_kept_on_its_own_names_the_series_it_was_cut_from",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a day is not taken off a series until the appointment kept for it has really arrived"
file = "src/application/caldav_sync.rs"
before = """        if let Some(said) = why_this_one_has_to_wait(cache, &event.id) {
            result.errors.push(said);
            continue;
        }
        let sent = send_one_change(cache, caldav, calendar_url, &event, username, password).await;"""
after = """        let sent = send_one_change(cache, caldav, calendar_url, &event, username, password).await;"""
red = [
    "application::caldav_sync::tests::test_a_day_whose_replacement_cannot_be_created_is_not_taken_off_the_series_at_the_server",
    "application::caldav_sync::tests::test_the_series_stays_waiting_when_the_server_refuses_the_replacement",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "a day cut out of a series is offered to the server before the series it came from"
file = "src/application/caldav_sync.rs"
before = """            let (cut_out, everything_else): (Vec<_>, Vec<_>) = waiting
                .into_iter()
                .filter(|event| event.calendar_id.as_deref() == Some(calendar.id.as_str()))
                .filter(|event| !waiting_on_a_choice.iter().any(|id| id == &event.id))
                .partition(|event| event.cut_from_event_id.is_some());
            cut_out.into_iter().chain(everything_else).collect()"""
after = """            waiting
                .into_iter()
                .filter(|event| event.calendar_id.as_deref() == Some(calendar.id.as_str()))
                .filter(|event| !waiting_on_a_choice.iter().any(|id| id == &event.id))
                .collect()"""
red = [
    "application::caldav_sync::tests::test_a_day_cut_out_of_a_series_goes_up_before_the_day_is_taken_off_the_series",
    "application::caldav_sync::tests::test_a_replacement_that_went_up_is_kept_when_taking_the_day_off_the_series_fails",
    "application::caldav_sync::tests::test_a_series_that_comes_first_in_the_queue_still_waits_for_the_day_cut_out_of_it",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "reading the calendar back keeps which series an appointment was cut out of"
file = "src/application/caldav_sync.rs"
before = """    merged.cut_from_event_id = held.cut_from_event_id.clone();
    merged.categories = held.categories.clone();"""
after = """    merged.categories = held.categories.clone();"""
red = [
    "application::caldav_sync::tests::test_reading_the_calendar_back_leaves_the_day_still_naming_the_series_it_came_from",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "one day of a series is refused before either half is written when the zone cannot be sent"
file = "src/presentation/managers.rs"
before = """    crate::application::calendar::the_zone_that_cannot_be_written(that_day)
}"""
after = """    let _ = that_day;
    None
}"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_an_outlook_zone_series_is_refused_and_neither_half_is_written",
    "presentation::managers::changing_one_day_of_a_series::test_what_a_rows_calendar_allows_is_answered_once_for_both_doors",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a one-day change on a calendar kept here is not refused over its zone"
file = "src/presentation/managers.rs"
before = """    if goes != crate::application::calendar::WhereAChangeGoes::ACalendarServer {
        return None;
    }
    crate::application::calendar::the_zone_that_cannot_be_written(that_day)"""
after = """    crate::application::calendar::the_zone_that_cannot_be_written(that_day)"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_a_series_kept_only_here_is_not_refused_over_its_zone",
    "presentation::managers::changing_one_day_of_a_series::test_what_a_rows_calendar_allows_is_answered_once_for_both_doors",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── One question, one answer, on both doors ─────────────────────────────────
#
# Whether one day of a series can be taken out on its own was answered in three
# places from three rules: the sentence read under the answer before anybody
# chooses, the refusal the window gives, and the refusal the write gives. So the
# sentence promised a calendar server would get both halves and the write
# refused the edit a moment later, and somebody heard the promise first.
#
# The doors differ and that is the point. A change keeps an appointment for the
# day, so it needs a time zone the server can be told. A delete keeps nothing,
# so refusing it over that zone would take away a delete that works.

[[guard]]
name = "the question and the refusal about one day are one answer"
file = "src/application/calendar.rs"
before = """                (WhatIsBeingDone::Changing, Some(clause)) => Err(one_day_cannot_be_kept(clause)),"""
after = """                (WhatIsBeingDone::Changing, Some(clause)) => {
                    let _ = clause;
                    Ok(())
                }"""
red = [
    "application::calendar::tests::test_taking_one_day_off_is_not_refused_over_a_zone_the_replacement_would_have_needed",
    "application::calendar::tests::test_the_question_and_the_refusal_are_one_answer",
    "application::calendar::tests::test_the_window_and_the_write_refuse_an_edit_in_the_same_words",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── A day cut fresh out of a series is not the edit still refused ───────────
#
# can_be_honoured answers two different questions about a day cut out of a
# series on a calendar server, and this arm is the answer to only one of them.
# A day the server has already turned into an exception of its own is refused
# a few lines above, unconditionally, because it shares that exception's
# address with the whole series. A day cut out here for the first time has no
# such address yet, so this arm lets it through, and both halves of it really
# do reach the server. The two rows look alike from the outside; narrowing
# this arm to refuse a calendar server the way the other refusal does would
# look like closing a gap and would really be taking away the one case that
# already works.

[[guard]]
name = "cutting a day fresh out of a series is not refused the way an existing exception is"
file = "src/application/calendar.rs"
before = """        (EditMeans::OneDay, WhereAChangeGoes::ACalendarServer | WhereAChangeGoes::KeptHere) => {
            match (done, allows.keeping_the_day_apart.as_deref()) {"""
after = """        (EditMeans::OneDay, WhereAChangeGoes::KeptHere) => {
            match (done, allows.keeping_the_day_apart.as_deref()) {"""
red = [
    "application::calendar::tests::test_a_calendar_this_program_can_only_read_says_the_other_reason_as_well",
    "application::calendar::tests::test_changing_one_day_is_carried_out_where_both_halves_can_reach_the_calendar",
    "application::calendar::tests::test_taking_one_day_off_is_not_refused_over_a_zone_the_replacement_would_have_needed",
    "application::calendar::tests::test_the_question_and_the_refusal_are_one_answer",
    "application::calendar::tests::test_the_window_and_the_write_refuse_an_edit_in_the_same_words",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── A refusal decided before anything is written says so ────────────────────
#
# The refusal for a day that cannot be kept on its own opened "That one day was
# not kept", which describes a write that went to a calendar server and came
# back refused. Every path that reaches it asks before a single write goes out,
# so there was no such attempt on any of them, and somebody who went looking
# for what it left behind found a calendar nothing had touched. It also ended
# without a way out of the refusal.

[[guard]]
name = "a refusal said before anything is written does not describe a write that failed"
file = "src/application/calendar.rs"
before = """        "That one day cannot be kept as a separate appointment: {clause} \\
         Nothing has been changed and the day is still part of the series. \\
         Time zones written this way come from Outlook and Exchange, and \\
         this program cannot yet describe one to a calendar server. Change \\
         the event's time zone to one this program knows, then open the day \\
         again.\""""
after = """        "That one day was not kept as a separate appointment: {clause} \\
         Nothing has been changed and the day is still part of the series. \\
         Time zones written this way come from Outlook and Exchange, and \\
         this program cannot yet describe one to a calendar server.\""""
red = [
    "application::calendar::tests::test_no_refusal_said_before_anything_is_written_describes_a_write_that_was_tried",
    "application::calendar::tests::test_the_refusal_check_can_tell_a_good_sentence_from_a_bad_one",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── The day a refusal is asked about names only the event's own zone ────────
#
# That refusal now tells somebody plainly to change the event's time zone, with
# none of the hedging the sync's sentence carries. It can only do that while
# the day being asked about names one zone and it is the event's own. A series
# calls a day off in a zone of its own spelling, so a day that carried those
# would send somebody to change a zone that was fine while the one that really
# stopped the write sat on a line they never see.
#
# The test that already asserts the day carries no called-off days uses a
# series that has none, so None to None passes either way and it stays green
# under this break. Measured: it does.

[[guard]]
name = "the day cut out of a series carries none of the days the series calls off"
file = "src/presentation/managers.rs"
before = """        recurrence_rule: None,
        ..entry"""
after = """        recurrence_rule: None,
        exception_dates: series.exception_dates.clone(),
        ..entry"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_the_day_a_refusal_is_asked_about_names_only_the_events_own_time_zone",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# -- One owner for what a delete did ----------------------------------------
#
# A delete at the server ends five ways and only one of them is the message
# really gone. The place that knows all five words the sentence. This window
# had a second, blunter one on a match arm of the type it is handed, saying
# "Deleted" flat, and it was out of reach only because the arm that could have
# taken it always wrapped its answer.
#
# The type that words its own outcome no longer holds a delete, so a second
# spelling cannot be written back onto it. What is left to guard is that
# nothing this window does word for itself is a delete sentence, and that the
# delete path still says what the owner gave it.

# Rewritten 2026-09-19 by 11-07 onto the batch's shape, where the copy
# branch answers the outcome and that it went; the break is the one it was,
# and it reddened the same one test.
[[guard]]
name = "one place words what a move or a copy did"
file = "src/presentation/wx_app.rs"
before = """                .map(|()| {
                    (
                        crate::application::server_delete::after_a_copy(
                            crate::application::server_delete::Copied::WithinTheAccount,
                            &into.id,
                            &subject,
                        ),
                        true,
                    )
                }),"""
after = """                .map(|()| {
                    (
                        crate::application::server_delete::WhatToDoNext {
                            then: crate::application::server_delete::ThenWhat::LeaveTheRow,
                            said: format!("Copied to {}: {subject}", into.id),
                        },
                        true,
                    )
                }),"""
red = [
    "presentation::wx_app::one_owner_for_what_a_delete_did::test_only_the_owner_words_what_a_move_or_a_copy_did",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "nothing this window words for itself is a delete sentence"
file = "src/presentation/wx_app.rs"
before = """            FlagChange::Read(true) => format!("Marked read: {subject}"),"""
after = """            FlagChange::Read(true) => format!("Deleted: {subject}"),"""
red = [
    "presentation::wx_app::one_owner_for_what_a_delete_did::test_nothing_that_words_its_own_outcome_here_says_what_a_delete_said",
    "presentation::wx_app::one_owner_for_what_a_delete_did::test_only_the_delete_owner_words_what_a_delete_did",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# Rewritten 2026-09-18 by 11-06.1 onto the new shape of the agreed case,
# which hands the answer to show_or_say_what_happened_next; the break is the
# same, the window wording a delete's outcome for itself, on the shown
# channel now, and the same one test goes red on the library.
[[guard]]
name = "the delete path says what the one owner of that sentence gave it"
file = "src/presentation/wx_app.rs"
before = """                        show_or_say_what_happened_next(&say, message_row_id, next);
                    }
                }
                return;"""
after = """                        let _ = next.said;
                        say(UIUpdate::MessageDeletedFromCache(message_row_id));
                        say(UIUpdate::Shown(format!("Deleted: {subject}")));
                    }
                }
                return;"""
red = [
    "presentation::wx_app::one_owner_for_what_a_delete_did::test_only_the_delete_owner_words_what_a_delete_did",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# -- One owner for what a failed command says --------------------------------
#
# Eight places in the windows wrote their own version of the sentence for a row
# that has gone, while the personal information commands already owned it and
# already added the half those copies left off, that nothing has been changed.
# Which sentence somebody heard depended on which panel they were in.
#
# Every one of those commands also failed into one sentence, "That did not
# work", followed by whatever the storage layer said. Read aloud that names
# nothing: a failed delete and a failed move are the same words, the row is not
# named, and there is nothing in it to act on.

[[guard]]
name = "one place words a row that has gone"
file = "src/presentation/managers.rs"
before = """        return send_status(tx, rt, &no_longer_there(kind, ""));"""
after = """        return send_status(tx, rt, "That row is no longer there");"""
suite = "house_style"
red = [
    "test_only_one_place_words_a_row_that_has_gone",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a command that failed says what it was and what to try"
file = "src/application/pim_command.rs"
before = """    format!(
        "{tried} did not work: {}. Nothing has been changed. Try it again, and if it \\
         keeps happening, close Wixen Mail and open it again.",
        reason.trim().trim_end_matches('.')
    )"""
after = """    let _ = tried;
    format!("That did not work: {reason}")"""
red = [
    "application::pim_command::tests::test_a_command_that_failed_says_what_it_was_and_what_to_try",
]
tests_last_seen = [
    { file = "src/application/pim_command.rs", tests = 38 },
]

[[guard]]
name = "a delete is not described as an edit that keeps a second appointment"
file = "src/application/calendar.rs"
before = """        WhatIsBeingDone::Deleting => {
            "Takes only the day you opened off the series, and leaves the rest of them \\
             alone. Nothing is kept for that day."
        }"""
after = """        WhatIsBeingDone::Deleting => {
            "Changes only the day you opened, and leaves the rest of them alone. That day \\
             is taken off the series and kept as a separate appointment, so it is two \\
             entries from then on rather than one moved day."
        }"""
red = [
    "application::calendar::tests::test_a_delete_is_not_described_as_an_edit_that_keeps_a_second_appointment",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "the calendar window asks about the day before it writes either half"
file = "src/presentation/managers.rs"
# Moved on 2026-09-14 from the arm inside `manage_calendar` into
# `change_an_opened_event`, which the calendar window and the due window
# both call; re-measured there.
before = """            let goes = the_calendar_it_is_in(cache, opened);
            if let Some(refused) = why_that_day_cannot_be_kept(goes, &that_day) {
                return Err(NotChanged::Refused(refused));
            }
            one_day_of_a_series_changed(cache, &series, opened, that_day)"""
after = """            one_day_of_a_series_changed(cache, &series, opened, that_day)"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_the_one_day_answer_asks_whether_the_day_can_be_kept_before_it_writes",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# The narrower edit, which the record above could not see. Asking is not the
# behaviour: stopping is. This leaves the question asked and the refusal said,
# and takes away the stop after it, so the day is refused out loud and then
# taken off the series anyway and the calendar server keeps the removal. The
# guard read the order of two calls and stayed green through it.

[[guard]]
name = "the refusal about a day that cannot be kept stops the write"
file = "src/presentation/managers.rs"
# Moved with the record above on 2026-09-14. The refusal is now a return
# out of `change_an_opened_event` rather than a `continue`; the break keeps
# the sentence and drops the return, so the write runs straight afterwards.
before = """            if let Some(refused) = why_that_day_cannot_be_kept(goes, &that_day) {
                return Err(NotChanged::Refused(refused));
            }"""
after = """            if let Some(refused) = why_that_day_cannot_be_kept(goes, &that_day) {
                tracing::warn!("{refused}");
            }"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_the_one_day_answer_asks_whether_the_day_can_be_kept_before_it_writes",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── How a task identifier becomes part of an address ────────────────────────
#
# Two rules, one function, because every address built for tasks goes through
# it: the six writes and both reads. They did not before. The writes escaped
# nothing and took the prefix off either way; the reads escaped nothing and
# took nothing off. What that cost is not hypothetical: measured against the
# identifiers the two services really hand out, a deletion aimed at one task
# arrived asking to remove another, and a read of a list went out with the
# three parameters that make it a whole read dropped, which the sync reads as
# a list somebody emptied.

[[guard]]
name = "a task identifier is escaped before it becomes part of an address"
file = "src/service/tasks_api.rs"
before = """fn in_an_address(id: &str, prefix: &str) -> String {
    in_a_path(&strip_prefix(id, prefix))
}"""
after = """fn in_an_address(id: &str, prefix: &str) -> String {
    strip_prefix(id, prefix)
}"""
red = [
    "service::tasks_api::tests::test_a_google_task_identifier_goes_into_the_address_the_way_a_calendar_one_does",
    "service::tasks_api::tests::test_a_google_task_read_names_its_list_as_carefully_as_a_write_does",
    "service::tasks_api::tests::test_a_microsoft_task_identifier_goes_into_the_address_the_way_a_contact_one_does",
    "service::tasks_api::tests::test_a_microsoft_task_read_names_its_list_as_carefully_as_a_write_does",
]
tests_last_seen = [
    { file = "src/service/tasks_api.rs", tests = 62 },
]

[[guard]]
name = "a task identifier loses this application's prefix before it becomes part of an address"
file = "src/service/tasks_api.rs"
before = """fn in_an_address(id: &str, prefix: &str) -> String {
    in_a_path(&strip_prefix(id, prefix))
}"""
after = """fn in_an_address(id: &str, prefix: &str) -> String {
    in_a_path(id)
}"""
red = [
    "application::tasks_sync::tests::test_the_six_task_writes_a_sync_uses_each_reach_the_provider_rather_than_the_forwarder",
    "service::tasks_api::tests::test_a_change_to_a_google_task_names_the_task_in_the_address_and_carries_what_changed",
    "service::tasks_api::tests::test_a_change_to_a_microsoft_task_names_the_task_in_the_address_and_says_which_day",
    "service::tasks_api::tests::test_a_google_task_deletion_names_the_list_and_the_task_and_sends_no_body",
    "service::tasks_api::tests::test_a_google_task_identifier_goes_into_the_address_the_way_a_calendar_one_does",
    "service::tasks_api::tests::test_a_google_task_read_asks_for_the_list_under_the_name_google_gave_it",
    "service::tasks_api::tests::test_a_microsoft_task_deletion_names_the_list_and_the_task",
    "service::tasks_api::tests::test_a_microsoft_task_identifier_goes_into_the_address_the_way_a_contact_one_does",
    "service::tasks_api::tests::test_a_microsoft_task_read_asks_for_the_list_under_the_name_graph_gave_it",
    "service::tasks_api::tests::test_a_new_google_task_is_posted_to_the_list_it_belongs_in_and_claims_no_identifier",
    "service::tasks_api::tests::test_a_new_microsoft_task_is_posted_to_the_list_it_belongs_in_and_claims_no_identifier",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# ── The one-line bodies that join a sync to a real client ───────────────────
#
# Each of these forwards one call and does nothing else, and a body that short
# is a body nothing notices going. Replaced by an answer of its own, every
# change is reported as accepted at the provider with nothing having left this
# computer, and the push then clears the flag or the note that was the only
# record the change was ever owed. Three of them had no test at all in either
# direction until this round.

[[guard]]
name = "the task forwarders really reach the client rather than answering themselves"
file = "src/application/tasks_sync.rs"
before = """        TasksClient::google_create_task(self, token, list_id, task).await
    }"""
after = """        Ok(GoogleTask::default())
    }"""
red = [
    "application::tasks_sync::tests::test_the_six_task_writes_a_sync_uses_each_reach_the_provider_rather_than_the_forwarder",
    "application::tasks_sync::tests::test_the_trait_the_sync_talks_through_still_asks_the_read_only_client",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "the Google address book forwarder really sends a deletion"
file = "src/application/contacts_sync.rs"
before = """        GoogleApiClient::delete_contact(self, token, provider_contact_id).await
    }"""
after = """        Ok(())
    }"""
red = [
    "application::contacts_sync::tests::test_the_google_address_book_a_sync_uses_really_sends_a_deletion",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "the Outlook address book forwarder really sends a deletion"
file = "src/application/contacts_sync.rs"
before = """        MsGraphClient::delete_contact(self, token, provider_contact_id).await
    }"""
after = """        Ok(())
    }"""
red = [
    "application::contacts_sync::tests::test_the_outlook_address_book_a_sync_uses_really_sends_a_deletion",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Mail waiting to go sat at the bottom of the tree ────────────────────────
#
# Where a folder sits in the tree had two answers. One was a database
# expression that sorted the tree read out of this computer, and it had no
# place at all for the Outbox, so mail nobody had managed to send fell to the
# end, below the trash on a POP account and among somebody's own folders on an
# IMAP one. The other said it belongs above Sent, with the drafts, because it
# is the one folder there is something to do about.
#
# The two answers are now one function and the sort is taken from it in both
# places. This break moves the Outbox back to where the database used to put
# it.
#
# What this cannot see: the test that reads the tree back and checks it against
# the same function stays green under this break, because both sides of it move
# together. That test is about the two answers being one answer and not about
# which order the one answer gives. The two named below are the ones that spell
# the order out.

[[guard]]
name = "mail waiting to go sits where the tree order says"
file = "src/common/types.rs"
before = """            // Above Sent, because mail waiting to go is something to act on
            // and mail already gone is something to look up.
            FolderType::Outbox => 2,"""
after = """            // Above Sent, because mail waiting to go is something to act on
            // and mail already gone is something to look up.
            FolderType::Outbox => 7,"""
red = [
    "common::types::tests::test_mail_waiting_to_go_sorts_above_mail_that_has_gone",
    "data::message_cache::folders::tests::test_mail_waiting_to_go_sits_between_the_drafts_and_what_has_gone",
]
tests_last_seen = [
    { file = "src/common/types.rs", tests = 30 },
    { file = "src/data/message_cache/folders.rs", tests = 61 },
]

# ── The adapter that saves a draft at the server had never run ──────────────
#
# Three steps run on the automatic save, once a minute for as long as somebody
# is writing, and the last of them removes messages from a server. Nothing had
# ever constructed the object that does them. What looked like proof was a
# second implementation of the same three steps living in the test module,
# which the wire tests drove instead, and it spelled the draft flags out again
# by hand. The two agreed by luck. Changing the flags the running program sends
# would have left every one of those tests green.
#
# The duplicate is gone and the tests drive the object that really runs. These
# two breaks are what that bought, measured by hand on 2026-08-12: before it,
# the first of them reddened nothing at all.

[[guard]]
name = "a draft filed at the server is filed as a draft"
file = "src/application/draft_copy.rs"
before = """            crate::service::protocols::imap::flag::DRAFT,"""
after = """            crate::service::protocols::imap::flag::ANSWERED,"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_the_draft_filed_at_the_server_is_flagged_as_a_draft_that_has_been_read",
]
tests_last_seen = [
    { file = "src/application/draft_copy.rs", tests = 11 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

[[guard]]
name = "the removal names the copies the search found"
file = "src/application/draft_copy.rs"
before = """    match server.remove_these(folder, &older).await {"""
after = """    match server.remove_these(folder, &[]).await {"""
red = [
    "application::draft_copy::tests::test_the_new_copy_is_filed_before_the_old_one_is_removed",
    "application::draft_copy::tests::test_the_removal_only_names_the_copies_that_were_there_before_the_new_one",
    "application::mail_controller::against_a_server_that_answers::test_a_draft_the_server_takes_is_filed_before_the_older_copy_is_removed",
    "application::mail_controller::against_a_server_that_answers::test_a_server_that_cannot_remove_one_message_leaves_the_older_copy_and_says_so",
    "application::mail_controller::against_a_server_that_answers::test_the_copies_the_server_is_told_to_remove_are_the_ones_it_named",
    "application::mail_controller::against_a_server_that_answers::test_the_draft_filed_at_the_server_is_flagged_as_a_draft_that_has_been_read",
]
tests_last_seen = [
    { file = "src/application/draft_copy.rs", tests = 11 },
    { file = "src/application/mail_controller.rs", tests = 71 },
]

# ── The adapter that files a copy of everything sent had never run ──────────
#
# Every message that goes out has a copy filed at the account's server, and
# nothing had ever constructed the object that files it. It could not be: it
# signed in for itself, so reaching it needed a real account and a real server.
# The only thing holding it to the running program was a check that reads the
# source, and a check that reads source cannot tell a body that still sends the
# right thing from one that stopped.
#
# The sign-in moved out to one place used by both this and the draft path, so
# what is left is the append, and a test can hand it a session it opened and
# allowed itself. The break below is what that bought, measured by hand on
# 2026-08-12: before it, changing the flag on the copy reddened nothing.
#
# Flagged read matters because the copy lands in the same Sent folder the sync
# fills. A message somebody wrote themselves is not unread mail waiting to be
# dealt with, and an unread count that climbs every time they send something is
# a count nobody can use.

[[guard]]
name = "the copy of a sent message is flagged read at the server"
file = "src/application/sent_copy.rs"
before = """        let already_read = format!("({})", crate::service::protocols::imap::flag::SEEN);"""
after = """        let already_read = format!("({})", crate::service::protocols::imap::flag::FLAGGED);"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_the_copy_of_a_sent_message_is_appended_where_it_was_told_flagged_as_read",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/application/sent_copy.rs", tests = 22 },
]

# ── Four answers to "which part of this file ships", three of them wrong ────
#
# A check that reads source text has to decide what a release build compiles.
# Four pieces of code decided it, and three cut at the first `#[cfg(test)]`
# anywhere in the file. An indented one on a test-only helper is ordinary Rust,
# so the contacts guard read 227 of 11123 lines and the outbound census read
# 186 of caldav's 6411. There is one answer now, in `common::what_ships`.
#
# The break below puts the naive cut back into the census. Measured on
# 2026-08-12: it reddens exactly one test, and that is worth being clear about.
# The census sweep over the tree stays green, because no file in the tree today
# hides a way out of this program behind an indented `#[cfg(test)]`. The only
# thing that notices is the test that hands the reading source text directly.
#
# So this record measures a reading, not a state of the tree. The day some file
# does hide a client that way, the sweep would go red too and this record would
# be short. That is the failure this file exists to catch, and it is caught by
# the run rather than by the record, so leave the record at one and let a run
# say when it has changed.
#
# What neither test can see: both read source text, so a way out through a
# crate nobody classified, or through a macro that writes the call, is invisible
# to them. The manifest sweep in the same module covers the first. Nothing
# covers the second.

[[guard]]
name = "the outbound census reads the whole shipped half of a file"
file = "src/service/outward.rs"
before = """        let ships = what_ships(source);"""
after = """        let ships = source.split("#[cfg(test)]").next().unwrap_or_default().to_string();"""
red = [
    "service::outward::completeness::test_the_census_sees_a_client_built_by_its_short_name",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
]

# The prose rule runs against the house style suite rather than the library,
# because a rule about what the pages say lives where the pages are read.
#
# Measured on 2026-08-12: the break reddens exactly one test, and it is the
# plant-proof rather than the sweep. That is the whole reason the plant-proof
# exists. With the reading made blind the sweep passes, reports a clean tree,
# and says nothing, which is how two checks that only read source text shipped
# in round seventeen while the thing they were about was already gone.
#
# What it cannot see: a machine name that is not a dependency at all. A
# database engine and a widget toolkit walk past this reading, and both were
# corrected by hand rather than by the check.

[[guard]]
name = "a machinery name in a page a person reads is still recognised"
file = "tests/house_style.rs"
suite = "house_style"
before = """    if ALSO_AN_ORDINARY_WORD.contains(&name) {"""
after = """    if true {
        return None;
    }
    if ALSO_AN_ORDINARY_WORD.contains(&name) {"""
red = [
    "test_the_machinery_check_can_tell_the_two_apart",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A meeting that repeats, on its way to Google ────────────────────────────
#
# A create that says nothing about the repeat files a weekly meeting as one
# appointment, and the read that follows brings that one appointment back and
# takes the repeat off the copy here as well. Both ends lose it and nothing is
# said, which is why these are measured rather than remembered.

[[guard]]
name = "a new meeting tells Google how it repeats"
file = "src/application/calendar.rs"
before = """            TheBodyIsFor::MakingIt => how_the_series_repeats(event),"""
after = """            TheBodyIsFor::MakingIt => Vec::new(),"""
red = [
    "application::calendar::tests::test_a_repeating_event_made_here_reaches_google_carrying_its_repeat_rule",
    "application::calendar::tests::test_a_repeating_event_still_repeats_here_after_the_pull_that_follows_the_create",
    "application::calendar::tests::test_a_rule_google_already_named_goes_back_without_being_named_twice",
    "application::calendar::tests::test_a_series_created_here_carries_the_days_it_has_already_called_off",
    "application::calendar::tests::test_every_field_the_event_form_can_set_is_answered_for_at_both_providers",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "the days a new series has already called off go up with its rule"
file = "src/application/calendar.rs"
before = """    if let Some(called_off) = worth_sending(event.exception_dates.as_deref()) {"""
after = """    if let Some(called_off) = worth_sending(None) {"""
red = [
    "application::calendar::tests::test_a_series_created_here_carries_the_days_it_has_already_called_off",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a Google calendar is asked for the series rather than for its days"
file = "src/service/google_api.rs"
before = """        "{}?singleEvents=false&maxResults=2500","""
after = """        "{}?singleEvents=true&maxResults=2500","""
red = [
    "application::calendar::tests::test_a_repeating_event_still_repeats_here_after_the_pull_that_follows_the_create",
    "service::google_api::tests::test_the_calendar_is_asked_for_the_series_rather_than_for_its_days",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/google_api.rs", tests = 37 },
]

[[guard]]
name = "a rule that already carries its property name does not get a second one"
file = "src/service/caldav.rs"
before = """    format!("RRULE:{}", without_the_property_name(rule))"""
after = """    format!("RRULE:{rule}")"""
red = [
    "application::calendar::tests::test_a_rule_google_already_named_goes_back_without_being_named_twice",
    "service::caldav::tests::test_a_rule_that_arrived_with_its_property_name_is_not_sent_with_two",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/caldav.rs", tests = 170 },
]

# ── A meeting that repeats, on its way to Outlook and back ──────────────────
#
# Outlook has no rules, only a shape and a stretch of time, so there are two
# spellings of one thing and they have to be held against each other. Every
# other pair like this in the program has drifted at least once.

[[guard]]
name = "a new meeting tells Outlook how it repeats"
file = "src/application/calendar.rs"
before = """        TheBodyIsFor::MakingIt => how_outlook_is_told_it_repeats(event, &start),"""
after = """        TheBodyIsFor::MakingIt => None,"""
red = [
    "application::calendar::tests::test_a_new_series_near_midnight_tells_outlook_one_day_for_the_rule_and_for_the_meeting",
    "application::calendar::tests::test_a_repeat_outlook_could_not_be_told_is_said_rather_than_left_to_be_found",
    "application::calendar::tests::test_a_repeating_event_made_here_reaches_outlook_carrying_its_repeat_rule",
    "application::calendar::tests::test_a_series_created_at_outlook_is_one_meeting_here_after_the_pull_that_follows",
    "application::calendar::tests::test_every_field_the_event_form_can_set_is_answered_for_at_both_providers",
    "application::calendar::tests::test_the_date_of_the_month_outlook_is_told_is_the_date_the_meeting_lands_on",
    "application::calendar::tests::test_the_day_outlook_is_told_a_series_starts_is_the_day_it_is_told_the_meeting_starts",
    "application::repeating::tests::test_every_repeat_this_program_offers_survives_the_trip_to_outlook_and_back",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/repeating.rs", tests = 27 },
]

# The sentence about a repeat that could not be sent is on this list because it
# is now asked of the body that really goes out rather than of a second call
# beside it. The trip out to Outlook and back is on it because that check now
# takes the day the series begins out of the body as well, instead of writing
# one down and handing it to both directions, where it agreed with itself
# whatever the two sides did.

[[guard]]
name = "the day a new meeting repeats from is the day it is sent on"
file = "src/application/calendar.rs"
before = """    let starts_on = crate::application::repeating::the_day_a_graph_start_names(start)?;"""
after = """    let _ = start;
    let stored = MsDateTimeTimeZone {
        date_time: event.start_datetime.clone(),
        time_zone: String::new(),
    };
    let starts_on = crate::application::repeating::the_day_a_graph_start_names(&stored)?;"""
red = [
    "application::calendar::tests::test_a_new_series_near_midnight_tells_outlook_one_day_for_the_rule_and_for_the_meeting",
    "application::calendar::tests::test_the_date_of_the_month_outlook_is_told_is_the_date_the_meeting_lands_on",
    "application::calendar::tests::test_the_day_outlook_is_told_a_series_starts_is_the_day_it_is_told_the_meeting_starts",
    "application::repeating::tests::test_every_repeat_this_program_offers_survives_the_trip_to_outlook_and_back",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/repeating.rs", tests = 27 },
]

# The break puts back the exact defect this fixes: the day taken off the stored
# column instead of off the start going into the same body. The two are the same
# day for most meetings and different days for one near midnight where the clock
# is hours from Greenwich, so a record that only tried a meeting in the middle
# of the day would never go red.

[[guard]]
name = "a repeat Outlook cannot say is refused rather than rounded to a near one"
file = "src/application/repeating.rs"
before = """                index: Some(outlook_calls_the_week(nth)?.to_string()),"""
after = """                index: Some(outlook_calls_the_week(nth).unwrap_or("first").to_string()),"""
red = [
    "application::calendar::tests::test_a_repeat_outlook_could_not_be_told_is_said_rather_than_left_to_be_found",
    "application::repeating::tests::test_a_repeat_outlook_cannot_say_is_refused_rather_than_rounded",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/repeating.rs", tests = 27 },
]

# The second name was missing before this and had been for as long as both
# existed. That test sends a series repeating on the second Tuesday from the end
# of the month and looks at the bytes that leave for a repeat that is not there;
# rounded to the first Tuesday instead of refused, one is, and the test has
# always said so. Measured, not guessed.

[[guard]]
name = "a repeat Outlook sends is stored as a rule and not as its own shape"
file = "src/application/calendar.rs"
before = """        recurrence_rule: event.recurrence.as_ref().and_then(|repeats| {"""
after = """        recurrence_rule: None.and_then(|repeats: &crate::service::microsoft_graph::MsPatternedRecurrence| {"""
red = [
    "application::calendar::tests::test_a_repeat_outlook_sends_is_stored_as_a_rule_and_not_as_a_page_of_its_own_shape",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# Re-measured by hand on 2026-08-14: the code this guard was about moved.
# Closing the Outlook occurrence gap replaced the single loop over every
# remote event, which used to skip a day of a series already held with the
# check this guard broke, with a partition into whole events and the changed
# days of a series, the second handled by a new function of its own,
# one_day_of_a_microsoft_series. The same question the old check asked, is a
# day of a series already held here already drawn from the rule, is now
# asked inside that function, and this record now names that line instead.
# One break, two new tests, and one existing test: the existing round-trip
# test only ever drove an unmodified day, and the two new focused tests, one
# for an unmodified day and one for a moved day, both reach this same line
# from opposite sides of it.

[[guard]]
name = "the days of a series held here do not arrive from Outlook a second time"
file = "src/application/calendar.rs"
before = """    if series.is_some() && event.occurrence_type.as_deref() != Some("exception") {
        return Ok(());
    }"""
after = """    if series.is_some() && event.occurrence_type.as_deref() == Some("exception") {
        return Ok(());
    }"""
red = [
    "application::calendar::tests::test_a_day_moved_at_outlook_is_shown_at_its_new_time_though_the_old_slot_may_still_show_too",
    "application::calendar::tests::test_a_new_changed_day_of_an_outlook_series_counts_as_created_and_a_held_one_as_updated",
    "application::calendar::tests::test_a_series_created_at_outlook_is_one_meeting_here_after_the_pull_that_follows",
    "application::calendar::tests::test_an_outlook_read_of_one_changed_day_takes_outlooks_category_and_keeps_the_status_set_here",
    "application::calendar::tests::test_an_unmodified_day_of_an_outlook_series_already_held_is_still_drawn_once",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a status typed here survives a read from Outlook"
file = "src/application/calendar.rs"
before = """    if status == TheStatus::OnlyHere {
        merged.status = held.status.clone();
    }"""
after = """    if status == TheStatus::AlsoAtTheProvider {
        merged.status = held.status.clone();
    }"""
red = [
    "application::calendar::tests::test_a_google_read_keeps_the_category_typed_here_and_takes_googles_status",
    "application::calendar::tests::test_a_google_read_of_one_changed_day_keeps_the_category_typed_here_and_takes_googles_status",
    "application::calendar::tests::test_a_status_typed_here_is_not_overwritten_by_a_read_from_outlook",
    "application::calendar::tests::test_an_outlook_read_of_one_changed_day_takes_outlooks_category_and_keeps_the_status_set_here",
    "application::calendar::tests::test_an_outlook_read_takes_outlooks_category_and_keeps_the_status_set_here",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a repeat Outlook could not be told is said out loud"
file = "src/application/calendar.rs"
before = """            if made.is_ok() && this_repeat_cannot_reach_outlook(&event) {"""
after = """            if false && this_repeat_cannot_reach_outlook(&event) {"""
red = [
    "application::calendar::tests::test_a_repeat_outlook_could_not_be_told_is_said_rather_than_left_to_be_found",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a repeat Outlook sends that means other days is refused rather than read"
file = "src/application/repeating.rs"
before = """            if every > 1 && !counted_from.eq_ignore_ascii_case(A_WEEK_STARTS_ON) {"""
after = """            if false {"""
red = [
    "application::repeating::tests::test_a_shape_outlook_sends_that_means_other_days_is_refused_rather_than_read",
]
tests_last_seen = [
    { file = "src/application/repeating.rs", tests = 27 },
]

# ── The census that says every way out of this program has been classified ──
#
# The reading looks for the names of crates that exist here only to reach a
# server, and, for the two crates whose names are everywhere for other reasons,
# for the names of the sockets themselves. Take the second half away and a
# module that writes `use tokio::{io::AsyncWriteExt, net::TcpStream};` names
# nothing the reading knows, which is how a module with a real connect and a
# real write walked past this census twice.
#
# Measured on 2026-08-12: the break reddens exactly one test, and that is worth
# being clear about. The sweep over the tree stays green, because no file in the
# tree today reaches a server that way. The only thing that notices is the test
# that hands the reading source text directly. So this record measures a
# reading, not a state of the tree, and the day some file does bring a socket in
# that way the sweep would go red too and this record would be short.

[[guard]]
name = "the outbound census sees a socket brought in beside something else"
file = "src/service/outward.rs"
before = """            .chain(A_SOCKET_UNDER_ITS_OWN_NAME.iter())
            .find(|name| names_it_as_a_whole_word(&code, name))"""
after = """            .find(|name| names_it_as_a_whole_word(&code, name))"""
red = [
    "service::outward::completeness::test_the_census_sees_a_socket_brought_in_beside_something_else",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
]

# The sweep written at the top of this file says how many records went through
# it and how many have arrived since, and a check adds them up against the
# records really here. That check is only worth having while it can still find
# those two sentences: reword one and the reading finds nothing, and a reading
# that finds nothing has no opinion about anything.
#
# The break rewords one of the sentence endings the reading looks for. It runs
# against the house style suite rather than the library, because a rule about
# what a page says lives where the pages are read.
#
# Measured on 2026-08-12: it reddens the check itself, which can no longer read
# the header, and the proving test beside it, which hands it made-up headers and
# expects both numbers back.

[[guard]]
name = "the check on the guard sweep header can still read the header"
file = "tests/house_style.rs"
before = """            " records were swept that day.","""
after = """            " records were swept on some day.","""
red = [
    "test_the_sweep_header_check_can_tell_the_two_apart",
    "test_the_sweep_written_at_the_top_of_the_guard_records_covers_every_record_in_it",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]
suite = "house_style"

# ── The order the folder tree is read in ────────────────────────────────────
#
# The rows come back from the database in the order they were written, and the
# tree order is decided afterwards in one place. Three of the four tests about
# that order were arranged in the order they expected back, so the whole sort
# could be deleted and only one of them noticed. Each fixture is now stored in
# an order that differs from the answer it asserts, so handing the rows back
# untouched is a different list from the one every test names.
#
# Measured by hand on 2026-08-12, by deleting the sort and running the library:
# before the fixtures were rearranged one test went red, and after it four did.
# The one that always saw it is the one about the inbox coming first, which was
# already stored out of order.
#
# The record about the padded folder kind is deliberately not written here as a
# second entry: the same test also reddens when the trim goes, which was checked
# by hand the same day, and that break belongs to the reader of the column
# rather than to this sort.

[[guard]]
name = "the folder tree is put in order after it is read rather than left as it was written"
file = "src/data/message_cache/folders.rs"
before = """        let mut folders = folders;
        folders.sort_by_key(|folder| {
            crate::common::types::tree_position(
                crate::common::types::FolderType::from_stored(&folder.folder_type),
                &folder.name,
            )
        });"""
after = """        let folders = folders;"""
red = [
    "data::message_cache::folders::tests::test_a_folder_type_stored_with_spaces_round_it_sorts_as_what_it_is",
    "data::message_cache::folders::tests::test_mail_waiting_to_go_sits_between_the_drafts_and_what_has_gone",
    "data::message_cache::folders::tests::test_the_tree_lists_the_inbox_first_and_ordinary_folders_last",
    "data::message_cache::folders::tests::test_the_tree_reads_folders_in_the_order_the_folder_type_gives",
]
tests_last_seen = [
    { file = "src/data/message_cache/folders.rs", tests = 61 },
]

# ── The order somebody asked the message list to be in ──────────────────────
#
# Seven ways of sorting the mail list, reachable from a menu and from clicking
# a column heading, and until now not one of them was asserted anywhere. For
# anybody working down a list by ear the order is the whole of how the list is
# read, and a heading that announces a new order over a list that did not move
# is worse than one that does nothing at all.
#
# The break makes every one of the seven leave the list as it arrived, which is
# what a stable sort does when told every pair is equal. It is written as a
# catch-all so that no arm survives it.
#
# Measured by hand on 2026-08-13: before the two tests below existed the whole
# library stayed green under this break. With them it reddens exactly these two
# and nothing else. The fixture is stored in an order that is not date, sender,
# subject or read order, so leaving the list alone is the wrong answer for
# every one of the seven.

[[guard]]
name = "each way of sorting the message list really puts it in that order"
file = "src/presentation/mail_sort.rs"
before = """        MailSortOption::DateNewestFirst => messages.sort_by(|a, b| b.date.cmp(&a.date)),"""
after = """        MailSortOption::DateNewestFirst => messages.sort_by(|_a, _b| std::cmp::Ordering::Equal),"""
red = [
    "presentation::wx_app::tests::test_each_way_of_sorting_the_list_puts_it_in_that_order",
]
tests_last_seen = [
    { file = "src/presentation/mail_sort.rs", tests = 0 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "sorting by unread brings the unread ones to the top"
file = "src/presentation/mail_sort.rs"
before = """        MailSortOption::UnreadFirst => messages.sort_by_key(|a| a.read),"""
after = """        MailSortOption::UnreadFirst => messages.sort_by(|_a, _b| std::cmp::Ordering::Equal),"""
red = [
    "presentation::wx_app::tests::test_sorting_by_unread_brings_the_unread_ones_to_the_top",
]
tests_last_seen = [
    { file = "src/presentation/mail_sort.rs", tests = 0 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "the contacts search box narrows within the sidebar's group filter rather than replacing it"
file = "src/presentation/wx_app.rs"
before = """            belongs(&contact.id, contact.favorite, &shown, &member_ids)
                && contact_matches_search(contact, &query)"""
after = """            belongs(&contact.id, contact.favorite, &shown, &member_ids) && true"""
red = [
    "presentation::wx_app::tests::test_a_search_query_narrows_within_the_selected_group_rather_than_resetting_to_all_contacts",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── A priority no task service understands stops before the wire ────────────
#
# Microsoft To Do takes three words for a priority and refuses the whole change
# for anything else. The reader folded anything unrecognised into "normal" and
# the writer sent the stored value exactly as it was, which is two answers to
# one question, and the copy-a-message-into-a-task path really produced a
# fourth value. A task like that was refused on every sync, never got an
# identifier from the provider so never became a change that could heal, and
# the person heard only that a sync had a problem.
#
# The break puts the fold back on the writing side: an unrecognised priority
# leaves as "normal" instead of stopping here. That is the plausible regression,
# because it compiles, keeps every task syncing, and quietly rewrites somebody's
# priority to something they did not choose.
#
# Measured by hand on 2026-08-13 with scripts/guards.py: this break reddens
# exactly the three below and nothing else in the library.

[[guard]]
name = "a task priority nothing understands is refused rather than quietly changed"
file = "src/service/tasks_api.rs"
before = """    let importance = TaskPriority::stored(&task.priority)
        .ok_or_else(|| TaskPriority::nothing_understands(&task.priority))?;"""
after = """    let importance = TaskPriority::stored(&task.priority).unwrap_or(TaskPriority::Normal);"""
red = [
    "application::tasks_sync::tests::test_a_task_whose_priority_nothing_understands_is_named_rather_than_left_to_the_provider",
    "service::tasks_api::tests::test_a_task_priority_microsoft_does_not_know_reaches_no_listener_that_could_have_seen_it",
    "service::tasks_api::tests::test_the_only_words_that_can_leave_here_as_an_importance_are_the_three_microsoft_takes",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# ── A choice the form never asked for is not an answer of nothing ───────────
#
# Copying a message into a task or an event fills in a title and a body and asks
# nothing else. The questions nobody was asked used to be stored as answered
# with nothing, and nothing is the one value both calendar providers and the
# task provider refuse.
#
# The break gives back the empty answer instead of the choice the form would
# have shown, which is exactly what the column held before.
#
# Measured by hand on 2026-08-13 with scripts/guards.py: this break reddens
# exactly the four below and nothing else in the library.

[[guard]]
name = "a choice the form never asked for falls back to the one it would have offered"
file = "src/application/item_fields.rs"
before = """        offered.first().unwrap_or(&"").to_lowercase()"""
after = """        answered"""
red = [
    "application::item_fields::tests::test_a_choice_the_form_never_asked_for_falls_back_to_the_one_it_would_have_offered",
    "presentation::managers::tests::test_a_task_copied_from_a_message_is_stored_with_a_priority_the_task_service_knows",
    "presentation::managers::tests::test_an_event_copied_from_a_message_is_stored_with_a_status_and_a_show_as_the_calendars_know",
    "presentation::managers::tests::test_an_event_stored_without_being_asked_still_reaches_both_calendars_with_words_they_take",
]
tests_last_seen = [
    { file = "src/application/item_fields.rs", tests = 33 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── Nothing this program sends says a Google task is not deleted ────────────
#
# Google will take a change to this field, and every change this program sent
# carried it as false. Tick off a task here that somebody deleted on their
# phone: the change goes up before the read comes down, so the claim lands while
# the tombstone is still unread, Google puts the task back, and the read that
# follows finds it alive and keeps it. The deletion is undone and nothing is
# said.
#
# Two breaks, because the field has two halves and the wrong fix for one kills
# the other. The first sends it again. The second is the mistake that looks like
# the fix: `skip` rather than `skip_serializing`, which stops the tombstone
# arriving as well and turns the task sync into a list that only ever grows.
#
# Measured by hand on 2026-08-13 with scripts/guards.py. The second break
# reddens exactly one test in the whole library, and it is a test that parses
# the field out of real JSON. Every other test that exercises a Google
# tombstone builds the struct in Rust with the field already set, so none of
# them can see a field that never arrives.

[[guard]]
name = "a change to a Google task carries no claim about whether it was deleted"
file = "src/service/tasks_api.rs"
before = """    #[serde(default, skip_serializing)]
    pub deleted: bool,"""
after = """    #[serde(default)]
    pub deleted: bool,"""
red = [
    "service::tasks_api::tests::test_a_change_to_a_google_task_says_nothing_about_whether_it_was_deleted",
    "service::tasks_api::tests::test_a_new_google_task_says_nothing_about_whether_it_was_deleted",
]
tests_last_seen = [
    { file = "src/service/tasks_api.rs", tests = 62 },
]

[[guard]]
name = "a Google task tombstone still arrives after the claim stopped being sent"
file = "src/service/tasks_api.rs"
before = """    #[serde(default, skip_serializing)]
    pub deleted: bool,"""
after = """    #[serde(default, skip)]
    pub deleted: bool,"""
red = [
    "service::tasks_api::tests::test_a_deleted_google_task_is_marked_as_one",
]
tests_last_seen = [
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# ── The name Google works out for itself is read and never sent back ────────
#
# Google computes this name from the parts and throws away whatever is sent in
# it, so sending one was a second answer to a question Google already answers.
# It costs nothing while Google keeps ignoring it. `names` is a field a change
# replaces wholesale, so the day Google starts honouring it is the day this
# copy quietly beats the parts sent beside it.
#
# Four breaks, because the fix has two halves and each half has a wrong version
# that looks right. The attribute stops it going out; the converter stops
# filling it in. `skip` in place of `skip_serializing` would stop it arriving as
# well, and it is the only whole name Google sends, so every contact would read
# as nameless. The reader's fall back to the one-line name is what stops a
# contact this program sent coming back without one.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "a contact sent to Google carries no name Google works out for itself"
file = "src/service/google_api.rs"
before = """    #[serde(default, skip_serializing)]
    pub display_name: String,"""
after = """    #[serde(default)]
    pub display_name: String,"""
red = [
    "service::google_api::tests::test_a_change_to_a_contact_reaches_google_without_the_name_google_works_out_for_itself",
    "service::google_api::tests::test_a_new_contact_reaches_google_carrying_what_somebody_typed_and_no_server_fields",
    "service::google_api::tests::test_serialize_person_for_create",
]
tests_last_seen = [
    { file = "src/service/google_api.rs", tests = 37 },
]

[[guard]]
name = "the name Google works out still arrives after it stopped being sent"
file = "src/service/google_api.rs"
before = """    #[serde(default, skip_serializing)]
    pub display_name: String,"""
after = """    #[serde(default, skip)]
    pub display_name: String,"""
red = [
    "service::google_api::tests::test_deserialize_connections_response",
]
tests_last_seen = [
    { file = "src/service/google_api.rs", tests = 37 },
]

[[guard]]
name = "nothing fills in the name Google computes on the way out"
file = "src/application/contacts_sync.rs"
before = """        vec![GoogleName {
            display_name: String::new(),"""
after = """        vec![GoogleName {
            display_name: contact.name.clone(),"""
red = [
    "application::contacts_sync::tests::test_contact_to_google_person",
    "application::contacts_sync::tests::test_the_name_google_computes_is_read_and_never_sent_back",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a contact whose whole name is on one line is not read as nameless"
file = "src/application/contacts_sync.rs"
before = """                n.unstructured_name.clone()"""
after = """                n.display_name.clone()"""
red = [
    "application::contacts_sync::tests::test_roundtrip_google",
    "application::contacts_sync::tests::test_the_name_google_computes_is_read_and_never_sent_back",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A postal address Google holds is kept here, so a change does not clear it ─
#
# The Google reader set both address columns to nothing, so an address held at
# Google was read into nothing at all. `addresses` is one of the fields a change
# to a contact names, Google clears every named field the body leaves out, and
# the body is built from the list the reader never filled. So editing a Google
# contact here for any reason, a nickname, a phone number, anything, took that
# person's postal address off their Google contact and said nothing about it.
#
# The break puts the reader back where it was. It is one line, and that is the
# point: the writer was already right and had been for a long time, and the
# whole loss sat in the half nobody read.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "a postal address Google holds is kept here rather than dropped"
file = "src/application/contacts_sync.rs"
before = """        phones_json,
        addresses_json,
        custom_fields_json: None,
        // Google's copy of the contact, as Google has it."""
after = """        phones_json,
        addresses_json: None,
        custom_fields_json: None,
        // Google's copy of the contact, as Google has it."""
red = [
    "application::contacts_sync::tests::test_a_google_sync_takes_a_postal_address_held_at_google_into_a_contact_already_known_here",
    "application::contacts_sync::tests::test_a_postal_address_google_holds_is_kept_here_and_sent_back",
    "application::contacts_sync::tests::test_a_postal_address_google_holds_only_as_one_line_is_kept_and_sent_back",
    "application::contacts_sync::tests::test_the_address_google_calls_the_main_one_is_the_one_this_program_calls_the_main_one",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Every number Outlook holds is kept here, so a change does not drop one ──
#
# The Microsoft reader kept one number in the single-number column and set the
# list to nothing, while the Google reader beside it kept the list. The writer
# builds Graph's three phone fields from that list, and Graph replaces a phone
# list with whatever it is given, so a contact with two home numbers at Outlook
# came back with one the first time anything was changed about them here.
#
# The break is the reader again, one line, exactly as it was.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "every number Outlook holds is kept here rather than all but one dropped"
file = "src/application/contacts_sync.rs"
before = """        emails_json,
        phones_json,
        addresses_json,
        custom_fields_json: None,
        pending: false,
        known_to: vec![ProviderIdentity {
            address_book: AddressBook::Microsoft,"""
after = """        emails_json,
        phones_json: None,
        addresses_json,
        custom_fields_json: None,
        pending: false,
        known_to: vec![ProviderIdentity {
            address_book: AddressBook::Microsoft,"""
red = [
    "application::contacts_sync::tests::test_a_microsoft_sync_takes_a_phone_list_held_at_outlook_into_a_contact_already_known_here",
    "application::contacts_sync::tests::test_every_number_outlook_holds_is_kept_here_and_sent_back",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A postal address held at Google reaches a contact already known here ────
#
# The two guards above keep a postal address in the reader that brings it
# down from Google and the writer that sends it back up. Neither covers the
# function between them: the one that folds a provider's copy over a contact
# this program already had never named `addresses_json`, so `..local.clone()`
# kept whatever was already stored no matter what Google now held. An
# address changed, or taken away, at Google never reached a contact synced
# more than once.
#
# The break takes the line back out, on the Google side.
#
# Measured by hand on 2026-08-14 with scripts/guards.py.

[[guard]]
name = "a postal address held at Google reaches a contact already known here"
file = "src/application/contacts_sync.rs"
before = """        emails_json: emails_json.and_then(|list| serde_json::to_string(&list).ok()),
        phones_json: remote.phones_json.clone(),
        addresses_json: remote.addresses_json.clone(),
        // The older one-line column, kept in step with the list above.
        // Leaving it out meant `..local` supplied the stored one, and two
        // readers fall back to it when the list is empty, so an address
        // deleted at the provider was still shown, still read aloud, and
        // still sent back out on the next change. Both doc comments already
        // claimed the postal address was covered here.
        address: remote.address.clone(),
        ..local.clone()"""
after = """        emails_json: emails_json.and_then(|list| serde_json::to_string(&list).ok()),
        phones_json: remote.phones_json.clone(),
        ..local.clone()"""
red = [
    "application::contacts_sync::tests::test_a_google_merge_names_every_field_it_takes_and_every_field_it_keeps",
    "application::contacts_sync::tests::test_a_google_sync_takes_a_postal_address_held_at_google_into_a_contact_already_known_here",
    "application::contacts_sync::tests::test_an_address_no_longer_held_at_google_is_no_longer_held_here",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A postal address held at Outlook reaches a contact already known here ───
#
# The same gap as the guard above, in the Microsoft half of the same
# function: `addresses_json` was never named, so a contact synced more than
# once kept whatever address was already stored no matter what Outlook now
# held.
#
# Measured by hand on 2026-08-14 with scripts/guards.py.

[[guard]]
name = "a postal address held at Outlook reaches a contact already known here"
file = "src/application/contacts_sync.rs"
before = """        emails_json: remote.emails_json.clone(),
        phones_json: remote.phones_json.clone(),
        addresses_json: remote.addresses_json.clone(),
        // The older one-line column, kept in step with the list above.
        // Leaving it out meant `..local` supplied the stored one, and two
        // readers fall back to it when the list is empty, so an address
        // deleted at the provider was still shown, still read aloud, and
        // still sent back out on the next change. Both doc comments already
        // claimed the postal address was covered here.
        address: remote.address.clone(),
        ..local.clone()"""
after = """        emails_json: remote.emails_json.clone(),
        phones_json: remote.phones_json.clone(),
        ..local.clone()"""
red = [
    "application::contacts_sync::tests::test_a_microsoft_merge_names_every_field_it_takes_and_every_field_it_keeps",
    "application::contacts_sync::tests::test_a_microsoft_sync_takes_a_postal_address_held_at_outlook_into_a_contact_already_known_here",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A phone list held at Outlook reaches a contact already known here ───────
#
# The same function's Microsoft half never named `phones_json` either, so a
# phone number changed or removed at Outlook was not picked up by a sync of a
# contact already known here, even though the reader beside it already reads
# every number Graph gives.
#
# Measured by hand on 2026-08-14 with scripts/guards.py.

[[guard]]
name = "a phone list held at Outlook reaches a contact already known here"
file = "src/application/contacts_sync.rs"
before = """        emails_json: remote.emails_json.clone(),
        phones_json: remote.phones_json.clone(),
        addresses_json: remote.addresses_json.clone(),
        // The older one-line column, kept in step with the list above.
        // Leaving it out meant `..local` supplied the stored one, and two
        // readers fall back to it when the list is empty, so an address
        // deleted at the provider was still shown, still read aloud, and
        // still sent back out on the next change. Both doc comments already
        // claimed the postal address was covered here.
        address: remote.address.clone(),
        ..local.clone()"""
after = """        emails_json: remote.emails_json.clone(),
        addresses_json: remote.addresses_json.clone(),
        // The older one-line column, kept in step with the list above.
        // Leaving it out meant `..local` supplied the stored one, and two
        // readers fall back to it when the list is empty, so an address
        // deleted at the provider was still shown, still read aloud, and
        // still sent back out on the next change. Both doc comments already
        // claimed the postal address was covered here.
        address: remote.address.clone(),
        ..local.clone()"""
red = [
    "application::contacts_sync::tests::test_a_microsoft_merge_names_every_field_it_takes_and_every_field_it_keeps",
    "application::contacts_sync::tests::test_a_microsoft_sync_takes_a_phone_list_held_at_outlook_into_a_contact_already_known_here",
    "application::contacts_sync::tests::test_a_phone_number_no_longer_held_at_outlook_is_no_longer_held_here",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── An event filed under two categories at Outlook goes back under two ──────
#
# The column holds an event's categories with commas between them, and every
# other reader of it splits on the comma. The Outlook writer sent the whole
# column as a single category, so an event filed under two came back filed under
# one called "Health,Personal", and Graph reads the list it is given as the
# whole truth, so that one replaced both.
#
# The break puts the whole column back in one entry.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "an event filed under two categories at Outlook goes back under two"
file = "src/application/calendar.rs"
before = """    crate::application::categories::on(stored)"""
after = """    vec![stored.trim().to_string()].into_iter().filter(|c| !c.is_empty()).collect()"""
red = [
    "application::calendar::tests::test_an_event_filed_under_several_categories_in_outlook_keeps_all_of_them",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── One day of a Google series that is no longer like the rest ──────────────
#
# Google is asked for the series itself rather than for its days. Asked that
# way it goes on naming separately every day of a series somebody has called
# off or moved, and nothing here read those, so a day cancelled in Google
# Calendar went on being drawn and a day moved to another time was drawn twice.
# The seven records below hold the reading of them.
#
# Widened by hand on 2026-08-14, not remeasured: closing the matching gap on
# the Outlook side gave `one_day_of_a_microsoft_series` the same shape as
# `one_day_of_a_google_series`, on purpose, so several of these seven records'
# `before` text started matching a second place in the file, in the new
# function, and the check that a record still names exactly one place failed
# on all of them at once. Each was widened with just enough of the
# surrounding, differently-named code, a variable name or a called function
# that only one of the two functions uses, to point at one place again. None
# of the widening changed what a record's break actually does, so none needed
# a fresh run; `test_every_guard_record_still_names_one_place_in_the_tree`
# checked the anchor and `scripts/guards.py` re-confirmed the ones a new test
# also newly reaches.

[[guard]]
name = "a Google series is read before the days of it that changed"
file = "src/application/calendar.rs"
before = """    let (whole_series, the_days_of_them_that_changed): (Vec<&GoogleEvent>, Vec<&GoogleEvent>) =
        remote_events
            .iter()
            .partition(|event| event.the_series_it_is_one_day_of().is_none());"""
after = """    let (whole_series, the_days_of_them_that_changed): (Vec<&GoogleEvent>, Vec<&GoogleEvent>) =
        remote_events
            .iter()
            .partition(|event| event.the_series_it_is_one_day_of().is_some());"""
red = [
    "application::calendar::tests::test_a_day_cancelled_at_google_is_taken_off_the_series_here",
    "application::calendar::tests::test_a_day_cancelled_at_google_stays_off_when_the_next_read_names_only_the_series",
    "application::calendar::tests::test_a_day_moved_at_google_and_then_cancelled_leaves_nothing_behind",
    "application::calendar::tests::test_a_day_moved_at_google_is_one_meeting_at_its_new_time",
    "application::calendar::tests::test_a_day_of_a_series_is_folded_in_even_when_google_names_it_first",
    "application::calendar::tests::test_a_day_of_a_series_this_computer_deleted_is_not_written_back",
    "application::calendar::tests::test_a_google_day_with_no_original_start_is_stored_as_a_plain_meeting",
    "application::calendar::tests::test_a_google_read_keeps_the_category_typed_here_and_takes_googles_status",
    "application::calendar::tests::test_a_repeating_event_still_repeats_here_after_the_pull_that_follows_the_create",
    "application::calendar::tests::test_a_series_with_a_change_waiting_here_keeps_waiting_when_google_calls_a_day_off",
    "application::calendar::tests::test_an_event_google_calls_cancelled_is_taken_off_and_the_others_are_kept",
    "application::calendar::tests::test_the_changed_days_of_a_google_series_are_counted_once_and_not_again",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "the days a Google series already calls off survive a read naming only the series"
file = "src/application/calendar.rs"
before = """                    TheCategory::OnlyHere,
                    TheStatus::AlsoAtTheProvider,
                );
                let merged = everything_both_copies_call_off(merged, &ex);
                cache.save_calendar_event(&merged)?;"""
after = """                    TheCategory::OnlyHere,
                    TheStatus::AlsoAtTheProvider,
                );
                cache.save_calendar_event(&merged)?;"""
red = [
    "application::calendar::tests::test_a_day_cancelled_at_google_stays_off_when_the_next_read_names_only_the_series",
    "application::calendar::tests::test_the_changed_days_of_a_google_series_are_counted_once_and_not_again",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a day of a Google series this computer deleted is not written back"
file = "src/application/calendar.rs"
before = """    if deleted_here.holds(&event.id) || deleted_here.holds(at_google) {"""
after = """    if deleted_here.holds(&event.id) {"""
red = [
    "application::calendar::tests::test_a_day_of_a_series_this_computer_deleted_is_not_written_back",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a Google day moved and then cancelled loses its appointment as well as its place"
file = "src/application/calendar.rs"
before = """        if existing.is_some() {
            cache.delete_calendar_event_by_provider_id(account_id, &event.id)?;
            the_day_went = true;
        }
        if let (Some(series), Some(the_day_it_was)) =
            (series, the_day_a_google_instance_replaces(event))"""
after = """        if false && existing.is_some() {
            cache.delete_calendar_event_by_provider_id(account_id, &event.id)?;
            the_day_went = true;
        }
        if let (Some(series), Some(the_day_it_was)) =
            (series, the_day_a_google_instance_replaces(event))"""
red = [
    "application::calendar::tests::test_a_day_moved_at_google_and_then_cancelled_leaves_nothing_behind",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a Google read that calls a day off leaves a change waiting here still waiting"
file = "src/application/calendar.rs"
before = """            (series, the_day_a_google_instance_replaces(event))
        {
            let called_off = crate::service::caldav::the_called_off_value_for(
                &the_day_it_was,
                series.is_all_day,
            );
            let (after, went) = with_one_more_day_called_off(&series, &called_off);
            cache.save_calendar_event(&CalendarEventEntry {
                pending: series.pending,
                ..after
            })?;"""
after = """            (series, the_day_a_google_instance_replaces(event))
        {
            let called_off = crate::service::caldav::the_called_off_value_for(
                &the_day_it_was,
                series.is_all_day,
            );
            let (after, went) = with_one_more_day_called_off(&series, &called_off);
            cache.save_calendar_event(&CalendarEventEntry {
                pending: false,
                ..after
            })?;"""
red = [
    "application::calendar::tests::test_a_series_with_a_change_waiting_here_keeps_waiting_when_google_calls_a_day_off",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a day of a Google series whose series is not held here is kept as the meeting it is"
file = "src/application/calendar.rs"
before = """    let Some(series) = series else {
        return cache.save_calendar_event(&that_day);
    };
    // Set here because carrying the local-only fields over does not carry it,
    // so a second read would otherwise store the day with nothing left saying
    // which series it came out of.
    that_day.cut_from_event_id = Some(series.id.clone());

    let Some(the_day_it_was) = the_day_a_google_instance_replaces(event) else {"""
after = """    let Some(series) = series else {
        return Ok(());
    };
    // Set here because carrying the local-only fields over does not carry it,
    // so a second read would otherwise store the day with nothing left saying
    // which series it came out of.
    that_day.cut_from_event_id = Some(series.id.clone());

    let Some(the_day_it_was) = the_day_a_google_instance_replaces(event) else {"""
red = [
    "application::calendar::tests::test_a_day_of_a_series_this_calendar_does_not_hold_is_kept_as_the_meeting_it_is",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "what Google says about one day of a series is never sent back"
file = "src/service/google_api.rs"
before = """    #[serde(default, skip_serializing)]
    pub recurring_event_id: Option<String>,"""
after = """    #[serde(default)]
    pub recurring_event_id: Option<String>,"""
red = [
    "application::calendar::tests::test_a_change_waiting_here_is_sent_to_google_before_the_calendar_is_read",
    "application::calendar::tests::test_a_repeating_event_changed_here_is_not_sent_in_a_way_that_flattens_it",
    "service::google_api::tests::test_an_event_with_nothing_set_is_sent_to_google_as_nothing",
    "service::google_api::tests::test_changing_one_thing_about_a_google_event_names_only_that_thing",
    "service::google_api::tests::test_nothing_google_says_about_a_series_instance_is_sent_back",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/service/google_api.rs", tests = 37 },
]

[[guard]]
name = "who took the day out decides whether the series is still waiting to be sent"
file = "src/application/calendar.rs"
before = """        pending: series.pending || who == WhoTookTheDayOut::SomebodyHere,"""
after = """        pending: series.pending,"""
red = [
    "application::calendar::tests::test_who_took_the_day_out_decides_whether_the_series_is_still_waiting_to_be_sent",
    "presentation::managers::changing_one_day_of_a_series::test_changing_one_day_stores_the_changed_day_and_the_series_with_that_day_called_off",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a day changed on this computer leaves the series waiting to be sent"
file = "src/presentation/managers.rs"
before = """        crate::application::calendar::WhoTookTheDayOut::SomebodyHere,"""
after = """        crate::application::calendar::WhoTookTheDayOut::TheProviderItself,"""
red = [
    "presentation::managers::changing_one_day_of_a_series::test_changing_one_day_stores_the_changed_day_and_the_series_with_that_day_called_off",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a day changed at Google leaves the series owing Google nothing"
file = "src/application/calendar.rs"
before = """    let Some(the_day_it_was) = the_day_a_google_instance_replaces(event) else {
        // Google should not send one. Said out loud rather than swallowed,
        // because there is no day to take off the series and the meeting would
        // otherwise be drawn twice with nothing anywhere saying why.
        tracing::warn!(
            "A day of a repeating event in a Google calendar does not say which \\
             day of it it stands in for, so that day cannot be taken off the \\
             series and the meeting may be shown twice. The event is {} and the \\
             series is {at_google}.",
            event.id
        );
        result.days_that_may_be_shown_twice += 1;
        return cache.save_calendar_event(&that_day);
    };
    one_day_kept_out_of_the_series(
        cache,
        &series,
        &that_day,
        &the_day_it_was,
        WhoTookTheDayOut::TheProviderItself,
    )"""
after = """    let Some(the_day_it_was) = the_day_a_google_instance_replaces(event) else {
        // Google should not send one. Said out loud rather than swallowed,
        // because there is no day to take off the series and the meeting would
        // otherwise be drawn twice with nothing anywhere saying why.
        tracing::warn!(
            "A day of a repeating event in a Google calendar does not say which \\
             day of it it stands in for, so that day cannot be taken off the \\
             series and the meeting may be shown twice. The event is {} and the \\
             series is {at_google}.",
            event.id
        );
        result.days_that_may_be_shown_twice += 1;
        return cache.save_calendar_event(&that_day);
    };
    one_day_kept_out_of_the_series(
        cache,
        &series,
        &that_day,
        &the_day_it_was,
        WhoTookTheDayOut::SomebodyHere,
    )"""
red = [
    "application::calendar::tests::test_a_day_moved_at_google_is_one_meeting_at_its_new_time",
    "application::calendar::tests::test_a_day_of_a_series_is_folded_in_even_when_google_names_it_first",
    "application::calendar::tests::test_the_changed_days_of_a_google_series_are_counted_once_and_not_again",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "a day of a Google series already called off is not counted as a deletion again"
file = "src/application/calendar.rs"
before = """            the_day_went |= went == ADayWent::OffTheSeries;
        }
        // Counted once, and only when something really went. Google names"""
after = """            the_day_went |= went != ADayWent::OffTheSeries;
        }
        // Counted once, and only when something really went. Google names"""
red = [
    "application::calendar::tests::test_the_changed_days_of_a_google_series_are_counted_once_and_not_again",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── A structured address pushed to Google never carries an empty formattedValue ─
#
# `formatted_value` had no `skip_serializing_if`, so every address sent to
# Google carried `"formattedValue":""` even when nothing in the struct held
# anything but an empty string. `addresses` is one of the fields a change
# replaces wholesale, so an explicit empty value in it is an instruction, not
# silence. The existing struct-level test asserted `formatted_value.is_empty()`
# on the Rust value and could never see this: the field was empty before and
# after, only the wire body changed.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "a structured address sent to Google carries no empty formattedValue key"
file = "src/service/google_api.rs"
before = """    #[serde(default, skip_serializing_if = "String::is_empty")]
    pub formatted_value: String,"""
after = """    #[serde(default)]
    pub formatted_value: String,"""
red = [
    "application::contacts_sync::tests::test_a_change_to_a_contact_does_not_ask_google_to_clear_the_line_it_composes",
    "application::contacts_sync::tests::test_a_postal_address_google_holds_only_as_one_line_is_kept_and_sent_back",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/service/google_api.rs", tests = 37 },
]

# ── An address with no recorded label sends no type key to Google ───────────
#
# `address_type` had no `skip_serializing_if` while its siblings on
# `GoogleEmail` and `GoogleUrl` both do. No writer in this program produces an
# address with an empty label today, so this was a hazard rather than a live
# loss, but a label read back from stored JSON that omits the key (the struct
# carries `#[serde(default)]`) would have gone out as `"type":""` instead of
# being left off the wire.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "an address with no recorded label carries no type key to Google"
file = "src/service/google_api.rs"
before = """    #[serde(default, rename = "type", skip_serializing_if = "String::is_empty")]
    pub address_type: String,"""
after = """    #[serde(default, rename = "type")]
    pub address_type: String,"""
red = [
    "application::contacts_sync::tests::test_an_address_with_no_label_asks_google_for_no_label_rather_than_an_empty_one",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
    { file = "src/service/google_api.rs", tests = 37 },
]

# ── An address Google holds only as a composed line is kept, not read as nothing ─
#
# Google fills in `formattedValue` on every address it returns, whether or not
# it was given separate parts. An address with none of the structured parts
# set was read into six empty strings here and sent back as an address of
# nothing, which cleared whatever Google held, because `addresses` is a field
# a change replaces wholesale. The existing round-trip test could not see
# this: its source has every structured part filled in.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "an address Google holds only as a composed line is kept rather than read as nothing"
file = "src/application/contacts_sync.rs"
before = """            if entry.on_one_line().trim().is_empty() {
                let line = address.formatted_value.trim();
                if !line.is_empty() {
                    entry.street = line.to_string();
                }
            }
            entry
        })
        .collect();"""
after = """            entry
        })
        .collect();"""
red = [
    "application::contacts_sync::tests::test_a_postal_address_google_holds_only_as_one_line_is_kept_and_sent_back",
    "application::contacts_sync::tests::test_the_address_google_calls_the_main_one_is_the_one_this_program_calls_the_main_one",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A postal address stored before the lists existed still reaches an address book ─
#
# `chosen_emails` and `chosen_phones` fall back to the single column when the
# JSON list is empty, and drop a blank row. The two address writers called
# `stored_list` on the JSON column directly: no fallback, no blank-row filter.
# A contact whose address lived only in the single column went out with an
# empty `addresses` list, which is a field a change replaces wholesale, so a
# provider holding an address for that contact from any other route would
# have had it cleared. Two records, because each writer is its own call site
# and a break inside a shared helper only proves itself at the site it is
# measured from.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "a postal address stored before the lists existed still reaches Google, and a blank row does not"
file = "src/application/contacts_sync.rs"
before = """    let addresses = chosen_addresses(contact)
        .into_iter()
        .map(|entry| GoogleAddress {"""
after = """    let addresses = stored_list::<AddressEntry>(contact.addresses_json.as_ref())
        .into_iter()
        .map(|entry| GoogleAddress {"""
red = [
    "application::contacts_sync::tests::test_a_postal_address_stored_before_the_lists_existed_still_goes_to_google",
    "application::contacts_sync::tests::test_an_empty_address_row_is_not_sent_to_google",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a postal address stored before the lists existed still reaches Microsoft, and a blank row does not"
file = "src/application/contacts_sync.rs"
before = """    let stored_addresses = chosen_addresses(contact);
    let home_address = stored_addresses"""
after = """    let stored_addresses = stored_list::<AddressEntry>(contact.addresses_json.as_ref());
    let home_address = stored_addresses"""
red = [
    "application::contacts_sync::tests::test_a_postal_address_stored_before_the_lists_existed_still_goes_to_microsoft",
    "application::contacts_sync::tests::test_an_empty_address_row_is_not_sent_to_microsoft",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── The name Outlook keeps beside one address is read in and sent back ──────
#
# Graph lets a name be recorded beside each address a contact has there,
# separately from the contact's own name; a maiden name kept on an old
# address is the ordinary case. The reader ignored that field, and the writer
# stamped the contact's own name onto every address on every push, so the
# first change made to a contact here overwrote a name Outlook had recorded
# for one of her addresses. Two records: the reader and the writer are
# separate call sites, and a break inside one half only proves itself there.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "the name Outlook keeps beside an address is read in rather than left blank"
file = "src/application/contacts_sync.rs"
before = """                name: e.name.clone(),"""
after = """                name: String::new(),"""
red = [
    "application::contacts_sync::tests::test_a_second_address_no_longer_held_at_microsoft_is_no_longer_held_here",
    "application::contacts_sync::tests::test_the_name_outlook_keeps_beside_an_address_is_kept_here_and_sent_back",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "the name recorded for an address is sent back rather than the contact's own name"
file = "src/application/contacts_sync.rs"
before = """            name: if entry.name.trim().is_empty() {
                contact.name.clone()
            } else {
                entry.name
            },
            address: entry.address,"""
after = """            name: contact.name.clone(),
            address: entry.address,"""
red = [
    "application::contacts_sync::tests::test_the_name_outlook_keeps_beside_an_address_is_kept_here_and_sent_back",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── A name Outlook gave survives a Google sync and an edit made here ────────
#
# Google says nothing about a name kept beside one address among several;
# only Outlook does. Replacing the stored email list wholesale, on a Google
# sync or on an edit made in the contact editor, would erase a name Outlook
# gave for an address either still lists, because neither source can tell the
# merge or the editor what that name was. Two records: the merge and the
# editor round trip are separate call sites of
# `EmailEntry::with_the_names_already_recorded`.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "a Google sync keeps the name Outlook gave for an address Google also lists"
file = "src/application/contacts_sync.rs"
before = """    let emails_json = remote.emails_json.as_ref().map(|_| {
        let fresh = stored_list::<EmailEntry>(remote.emails_json.as_ref());
        let recorded = stored_list::<EmailEntry>(local.emails_json.as_ref());
        EmailEntry::with_the_names_already_recorded(fresh, &recorded)
    });"""
after = """    let emails_json = remote.emails_json.as_ref().map(|_| {
        let fresh = stored_list::<EmailEntry>(remote.emails_json.as_ref());
        fresh
    });"""
red = [
    "application::contacts_sync::tests::test_a_google_sync_keeps_the_name_outlook_keeps_beside_an_address",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "an edit made here keeps the name Outlook gave for an address it does not show"
file = "src/presentation/contact_convert.rs"
before = """        let recorded: Vec<EmailEntry> = replacing
            .map(|existing| parse_list(existing.emails_json.as_ref()))
            .unwrap_or_default();
        EmailEntry::with_the_names_already_recorded(fresh, &recorded)
    };"""
after = """        fresh
    };"""
red = [
    "presentation::contact_convert::tests::test_an_edit_keeps_the_name_outlook_keeps_beside_an_address",
]
tests_last_seen = [
    { file = "src/presentation/contact_convert.rs", tests = 29 },
]

# ── The address and the email Google calls primary come first here too ──────
#
# This program decides which of a contact's several addresses or emails is
# the main one by list order everywhere: the contact panel, the editor, and
# the Microsoft writer all take the first. Google decides it with a flag on
# the value itself instead, and until now this program took Google's list in
# whatever order Google sent it, so which address or email showed as the
# contact's main one was decided by Google's list order rather than by what
# Google actually calls primary. Two records: the address and the email are
# separate calls to `with_the_main_one_first`.
#
# Measured by hand on 2026-08-13 with scripts/guards.py.

[[guard]]
name = "the address Google calls primary is put first here, not read in whatever order Google listed it"
file = "src/application/contacts_sync.rs"
before = """    let addresses_in_order = with_the_main_one_first(&person.addresses, |a| {
        a.metadata.as_ref().is_some_and(|m| m.primary)
    });"""
after = """    let addresses_in_order: Vec<&GoogleAddress> = person.addresses.iter().collect();"""
red = [
    "application::contacts_sync::tests::test_the_address_google_calls_the_main_one_is_the_one_this_program_calls_the_main_one",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "the email Google calls primary is shown first here, not read in whatever order Google listed it"
file = "src/application/contacts_sync.rs"
before = """    let emails_in_order = with_the_main_one_first(&person.email_addresses, |e| {
        e.metadata.as_ref().is_some_and(|m| m.primary)
    });"""
after = """    let emails_in_order: Vec<&GoogleEmail> = person.email_addresses.iter().collect();"""
red = [
    "application::contacts_sync::tests::test_the_email_google_calls_the_main_one_is_the_one_this_program_shows_first",
]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

# ── Tasks: a provider's markup and a provider's progress word ───────────────
#
# Two of the fixes in this round are the ones most likely to be quietly
# undone by a change that looks like a simplification. Measured by hand on
# 2026-08-13 with scripts/guards.py: each break reddens exactly the tests
# named below and nothing else in the library.

[[guard]]
name = "a task or event body written as html is read as the structure it carries, not sanitized and flattened"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11. `from_markup` is now one of two entry points onto
# `read_markup`, so the break moved to the function that does the work and
# covers both readings at once. Re-measured 2026-09-16 when the sanitising
# call gained the `<title>` element and `words_of_markup` became a third
# reading through it: the `before` is the body as it stands, and the words
# test reddens with the rest.
#
# And again on 2026-09-22 by 12-02.1, after run 35520204784 could not apply
# this break at all: the text appeared 0 times. `d470a105`, 11-11.0's
# newsletter work for #90, gave `read_markup` the `Keeping` match over
# `drop_what_the_sender_hid` and the `keep_the_layout_claim` call, so the
# recorded body was three quarters of a function that had grown. The anchor is
# the whole function again, which is what it was before and what it has to be:
# the break replaces the whole of it.
before = '''fn read_markup(html: &str, keeping: Keeping) -> String {
    // What the sender hid is dropped before anything reads it (#90), so a
    // hidden preheader and its padding reach neither a row's snippet nor a
    // description read aloud. Only for speech: a note read back to be
    // edited keeps everything typed, hidden or not, for the reason on
    // [`from_markup_to_edit`].
    let shown = match keeping {
        Keeping::OnlyWhatIsSpoken => {
            std::borrow::Cow::Owned(super::hidden_text::drop_what_the_sender_hid(html).0)
        }
        Keeping::EverythingTyped => std::borrow::Cow::Borrowed(html),
    };
    // `ammonia`'s defaults drop a `<script>` or `<style>` element with its
    // content and strip every other disallowed tag around its content. A
    // `<title>` is the one element whose content is not part of what a reader
    // would say either: a whole message carries one in its head, and kept, it
    // arrived at the front of every newsletter's snippet (#32). A table's
    // claim to be layout is kept, so the walk below can read one as its
    // blocks rather than as a table with no columns to name.
    let mut cleaner = ammonia::Builder::default();
    cleaner.add_clean_content_tags(["title"]);
    super::hidden_text::keep_the_layout_claim(&mut cleaner);
    let cleaned = cleaner.clean(&shown).to_string();
    let fragment = scraper::Html::parse_fragment(&cleaned);
    let mut out = String::new();
    markup::blocks(*fragment.root_element().deref(), &mut out, keeping);
    collapse_blank_lines(&out)
}'''
after = '''fn read_markup(html: &str, keeping: Keeping) -> String {
    let _ = keeping;
    ammonia::clean(html)
}'''
# Measured 2026-09-11 for ledger 270. The record named five tests and the break
# reddens forty. Thirty-one of the thirty-five it missed live in
# `src/service/onenote_page.rs`, which arrived on 2026-09-11 and which this
# record had never named, so the count check could not see them arrive: it
# compares test counts for the files a record names, and a test added to a file
# no record names is invisible to it. Four more are ledger 270's own. A record
# naming five where the truth is forty is the shape this file exists to stop,
# and nothing found it except running the remedy the count check printed.
red = [
    "application::long_text::tests::test_the_words_of_markup_are_the_words_alone_without_markers_or_stylesheets",
    # The snippet and the search index read an HTML-only message body through
    # this function since 2026-09-16 (#32), so a reader that flattens reddens
    # them too; the runner named these five, the first draft of this list did
    # not.
    "data::message_cache::bodies::tests::test_a_snippet_of_a_heading_and_a_list_holds_the_words_and_no_markers",
    "data::message_cache::bodies::tests::test_a_snippet_of_an_html_only_body_is_its_words_and_not_its_stylesheet",
    "data::message_cache::bodies::tests::test_an_html_only_body_still_gives_a_snippet",
    "data::message_cache::bodies::tests::test_stored_snippets_are_put_right_once_index_included_plain_bodies_too",
    "data::message_cache::searching::finding_things::test_an_html_only_body_is_indexed_by_its_words_and_not_by_its_stylesheet",
    "application::calendar::tests::test_an_outlook_event_body_written_as_html_is_read_as_the_structure_it_carries",
    "application::long_text::tests::test_a_bar_inside_a_cell_does_not_break_the_table_it_is_in",
    "application::long_text::tests::test_a_line_break_kept_for_editing_is_still_a_line_break",
    "application::long_text::tests::test_a_line_break_read_for_speaking_is_still_a_space",
    "application::long_text::tests::test_a_link_kept_for_editing_keeps_its_address",
    "application::long_text::tests::test_a_link_read_for_speaking_still_loses_its_address",
    "application::long_text::tests::test_a_nested_list_in_a_providers_markup_comes_back_nested",
    "application::long_text::tests::test_a_picture_kept_for_editing_is_still_a_picture",
    "application::long_text::tests::test_a_picture_nobody_described_is_still_kept_for_editing",
    "application::long_text::tests::test_a_picture_read_for_speaking_is_still_its_description_alone",
    "application::long_text::tests::test_a_table_in_a_providers_markup_comes_back_as_a_table",
    "application::long_text::tests::test_a_table_whose_header_row_is_ordinary_cells_still_gets_headings",
    "application::long_text::tests::test_an_address_holding_a_bracket_is_written_so_it_can_be_read_back",
    "application::long_text::tests::test_link_text_and_image_alt_text_survive_markup_being_read",
    "application::long_text::tests::test_markup_from_a_provider_is_read_as_the_structure_it_carries",
    "service::onenote_page::tests::test_a_heading_at_every_level_comes_back",
    "service::onenote_page::tests::test_a_heading_straight_after_a_list_comes_back",
    "service::onenote_page::tests::test_a_horizontal_rule_does_not_come_back_at_all",
    "service::onenote_page::tests::test_a_line_break_comes_back_as_a_line_break",
    "service::onenote_page::tests::test_a_link_comes_back_with_its_address",
    "service::onenote_page::tests::test_a_list_nested_three_deep_comes_back_at_all_three_depths",
    "service::onenote_page::tests::test_a_nested_list_comes_back_nested",
    "service::onenote_page::tests::test_a_note_goes_out_as_a_page_and_the_page_comes_back_as_a_note",
    "service::onenote_page::tests::test_a_note_that_is_one_space_comes_back_empty",
    "service::onenote_page::tests::test_a_notes_title_is_not_repeated_as_a_heading_in_the_body",
    "service::onenote_page::tests::test_a_page_written_in_capitals_reads_the_same_as_one_in_small_letters",
    "service::onenote_page::tests::test_a_paragraph_comes_back",
    "service::onenote_page::tests::test_a_picture_comes_back_as_a_picture",
    "service::onenote_page::tests::test_a_picture_with_no_description_comes_back_as_a_picture_with_no_description",
    "service::onenote_page::tests::test_a_quote_comes_back_as_an_ordinary_paragraph",
    "service::onenote_page::tests::test_a_script_on_a_page_does_not_reach_the_note",
    "service::onenote_page::tests::test_a_style_element_on_a_page_does_not_reach_the_note",
    "service::onenote_page::tests::test_a_table_comes_back_as_a_table",
    "service::onenote_page::tests::test_an_empty_note_makes_a_page_and_comes_back_empty",
    "service::onenote_page::tests::test_an_event_handler_attribute_on_a_page_does_not_reach_the_note",
    "service::onenote_page::tests::test_an_ordered_list_comes_back",
    "service::onenote_page::tests::test_an_unordered_list_comes_back",
    "service::onenote_page::tests::test_bold_comes_back_as_plain_words",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
    "service::onenote_page::tests::test_inline_code_comes_back_as_plain_words",
    "service::onenote_page::tests::test_italic_comes_back_as_plain_words",
    "service::onenote_page::tests::test_microsofts_own_list_comes_back_as_a_list",
    "service::onenote_page::tests::test_microsofts_own_page_comes_back_with_its_heading_as_a_heading",
    "service::onenote_page::tests::test_microsofts_own_page_comes_back_with_its_title_and_its_paragraphs_apart",
    "service::onenote_page::tests::test_struck_out_text_comes_back_as_plain_words",
    "service::onenote_page::tests::test_the_inline_span_styles_do_not_appear_in_what_comes_back",
    "service::tasks_api::tests::test_a_microsoft_task_arrives_from_json_with_its_progress_and_its_markup_read",
    "service::tasks_api::tests::test_a_microsoft_task_body_written_as_html_is_read_as_words_not_tags",
    # Added 2026-09-15 by 08-07's sweep: a OneNote page's body, written
    # 2026-09-11 in a file this record had never named, is read through the
    # same reading.
    "service::onenote_notes::tests::test_a_note_is_its_title_from_the_resource_and_its_body_from_the_document",
    "service::onenote_notes::tests::test_a_note_made_here_reaches_onenote_as_a_page_in_the_section_it_was_given",
    "service::onenote_notes::tests::test_what_onenote_could_not_keep_is_read_back_off_the_page_rather_than_worked_out",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "src/data/message_cache/searching.rs", tests = 26 },
    { file = "src/service/onenote_notes.rs", tests = 13 },
    { file = "src/service/onenote_page.rs", tests = 48 },
    { file = "src/service/tasks_api.rs", tests = 62 },
]

[[guard]]
name = "the progress word Outlook holds a task under is kept, not folded to completed or not started"
file = "src/service/tasks_api.rs"
before = """        status: if task.is_completed {
            TaskProgress::Completed.as_word().to_string()
        } else {
            task.remote_status
                .as_deref()
                .and_then(TaskProgress::stored)
                .filter(|progress| *progress != TaskProgress::Completed)
                .unwrap_or(TaskProgress::NotStarted)
                .as_word()
                .to_string()
        },"""
after = """        status: if task.is_completed {
            "completed".to_string()
        } else {
            "notStarted".to_string()
        },"""
red = [
    "service::tasks_api::tests::test_the_progress_microsoft_holds_is_kept_when_a_change_goes_back",
]
tests_last_seen = [
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# ── A Microsoft task's completion time trusts only the zone this was asked for ─
#
# `ms_task_to_entry`'s `completed_at` used to take whatever `dateTime` Graph
# sent back with no regard to the `timeZone` sent beside it, correct only
# because `TasksClient::ms_tasks` never asks Graph for a zone other than
# universal time. A header added there for some unrelated reason would have
# silently misread every completion time as if it were universal when it was
# not, with nothing but a comment and a test of the header's absence to
# notice it. The read side now asks the same question the write side already
# answers strictly: a completion time is trusted only when it is labelled
# `calendar::COORDINATED_UNIVERSAL_TIME`, and dropped rather than misread
# otherwise.
#
# Measured by hand on 2026-08-15 with scripts/guards.py: this break reddens
# exactly the one test named below and nothing else in the library.

[[guard]]
name = "a Microsoft task's completion time is trusted only when Graph labelled it universal time"
file = "src/service/tasks_api.rs"
before = """        completed_at: task.completed_date_time.as_ref().and_then(|done| {
            (done.time_zone == crate::application::calendar::COORDINATED_UNIVERSAL_TIME)
                .then(|| done.date_time.clone())
        }),"""
after = """        completed_at: task
            .completed_date_time
            .as_ref()
            .map(|done| done.date_time.clone()),"""
red = [
    "service::tasks_api::tests::test_a_microsoft_completion_time_read_in_a_different_zone_is_dropped_not_misread",
]
tests_last_seen = [
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# ── Mail joins the write census, and a read that cannot carry a changing verb ─
#
# Four rounds running, a planted module reached a server through a pattern the
# text census had not been taught, and the census itself covered four HTTP
# clients and no mail protocol at all: a new IMAP, SMTP or POP3 write could
# arrive with nothing reading its request and nothing failing. POP3 shipped
# ungated for the life of this project behind exactly that gap. This adds a
# wire census for the three mail transports, a guard at the point a write
# actually reaches the session or the transport rather than at a name the
# census recognises, and a type that makes a WebDAV read unable to carry a
# changing verb at all.
#
# Measured by hand on 2026-08-13. The break removes the gate call a mailbox
# write asks before it reaches the session: it reddens the mail wire census,
# because the write no longer shows up as a write for `writes_in` to count;
# the guard at the write itself, because the same call is what it looks for;
# and the functional test that already existed for this method, because the
# account really would send a command to the server with changes turned off.
# Three different checks, three different reasons, over the one line.

[[guard]]
name = "a mailbox write asks the gate before it reaches the session"
file = "src/service/protocols/imap.rs"
before = """    pub async fn copy_message(&mut self, uid: u32, into: &str) -> Result<()> {
        self.may_i("copy a message")?;
        self.require_selected()?;"""
after = """    pub async fn copy_message(&mut self, uid: u32, into: &str) -> Result<()> {
        self.require_selected()?;"""
# Re-measured on 2026-08-31, when plan 02-01 tightened MAIL_TRANSPORTS' imap
# entry from a floor of 11 to the count of 11 and added a second test that reads
# that census. Four now, not three. The record did not go stale because this
# guarded code changed; it went stale because a test was added elsewhere that
# depends on the same census, which is the direction nothing prompts anybody to
# check.
red = [
    "service::outward::completeness::test_every_mailbox_write_is_on_the_mail_wire_list",
    "service::outward::completeness::test_nothing_sends_a_changing_command_without_asking_the_gate",
    "service::outward::completeness::test_the_read_gate_does_not_move_the_count_of_gated_writes",
    "service::protocols::imap::against_a_server_that_answers::test_every_mailbox_write_says_nothing_to_the_server_with_the_gate_closed",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# Measured by hand on 2026-08-13. This is the other direction from the guard
# above: the write still asks the gate, so it is still counted and nothing
# about reaching the server changes, but the row that is supposed to prove a
# test read its command off a socket now names a command nothing sends. Only
# the test that checks the account is true notices; the test that checks the
# write is accounted for at all does not, because the method and the file are
# still right, and that is worth being honest about rather than assumed.
#
# A row deleted outright, the shape this guard first tried, does not compile:
# the list carries an explicit length and Rust will not shrink it to match a
# shorter literal. The corrupted row is the break this file can actually
# apply and re-measure.

[[guard]]
name = "every mailbox write named measured is really on the mail wire list"
file = "src/service/outward.rs"
before = """        \"UID COPY 7 \\\"Archive\\\"\","""
after = """        \"UID COPY 7 \\\"Nowhere\\\"\","""
red = [
    "service::outward::completeness::test_every_mailbox_write_called_measured_had_its_command_read_off_a_socket",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
]

# Measured by hand on 2026-08-13. `reading_with` used to take any
# `reqwest::Method` with nothing gating it, so a caller could hand it a
# changing verb and it would go out as though it were the read it was built
# for. `AskWith` closes that at the type level, so the only way left to break
# it is to point one of its two variants at the wrong word, which is what this
# does.
#
# The break also reaches a real caller: `CalDavClient::discover_calendars`
# asks with `AskWith::Propfind`, and the calendar test server this reddens
# only answers a `PROPFIND`, so the discovery test that reads what it offers
# goes red as well as the type-level test. That is the guard proving it is not
# only checking itself.

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named,
# the census that walks every way of asking and requires each to be a verb that
# changes nothing.
name = "a WebDAV read cannot carry a changing verb"
file = "src/service/outward.rs"
before = """            Self::Propfind => "PROPFIND","""
after = """            Self::Propfind => "DELETE","""
red = [
    "service::caldav::discovery_tests::test_what_is_asked_of_a_calendar_server_is_the_four_things_the_screen_needs",
    "service::outward::a_question_is_not_a_change::test_every_way_of_asking_is_a_verb_that_changes_nothing",
    "service::outward::tests::test_a_read_that_is_not_a_get_can_only_ask_for_something",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
    { file = "src/service/outward.rs", tests = 38 },
]

# ── The category and status choice at a calendar merge's call site ──────────
#
# `carry_over_local_only` decides whose copy of a category and a status
# survives a sync, and the two providers want opposite answers for the two
# fields. The only guard watching that decision called the helper directly
# ("a status typed here survives a read from Outlook", above), so the two
# production call sites that actually pass the choice, inside
# `sync_google_calendar` and inside `sync_microsoft_calendar`, were never
# reached by a test that could see either one get it wrong. Flipping the pair
# of arguments at either call site changed no test's answer.
#
# Measured by hand on 2026-08-13, against the whole library, one build and run
# per break.

[[guard]]
name = "a Google merge keeps the category typed here and takes Google's status"
file = "src/application/calendar.rs"
before = """                carry_over_local_only(
                    &mut merged,
                    &ex,
                    TheCategory::OnlyHere,
                    TheStatus::AlsoAtTheProvider,
                );
                let merged = everything_both_copies_call_off(merged, &ex);"""
after = """                carry_over_local_only(
                    &mut merged,
                    &ex,
                    TheCategory::AlsoAtTheProvider,
                    TheStatus::OnlyHere,
                );
                let merged = everything_both_copies_call_off(merged, &ex);"""
red = [
    "application::calendar::tests::test_a_google_read_keeps_the_category_typed_here_and_takes_googles_status",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# Measured by hand on 2026-08-13. The Outlook mirror of the guard above.
# Reddens a second, older test as well as the new one: an existing test
# already drove this call site through the real sync and watched the category
# half of the choice, so it goes red alongside the new test that watches both
# halves together. Written down rather than pruned, because that is what
# "measured, not guessed" looks like when an existing fixture already covered
# part of a claim.
#
# Widened by hand on 2026-08-14, not remeasured: closing the Outlook
# occurrence gap added `everything_both_copies_call_off` to this same call
# site, the same protection Google's whole-series merge already had against a
# later read that names no series erasing a day learned some other way. The
# text this record broke sat between `carry_over_local_only` and
# `cache.save_calendar_event`, so the new line landed inside it and the old
# `before` was no longer in the file at all. Widened to include the new line
# rather than only the two lines around it, which is also what tells this
# record apart from the two on the Google side of the same shape now that
# both call sites end the same way.

[[guard]]
name = "an Outlook merge takes Outlook's category and keeps the status set here"
file = "src/application/calendar.rs"
before = """                carry_over_local_only(
                    &mut merged,
                    &ex,
                    TheCategory::AlsoAtTheProvider,
                    TheStatus::OnlyHere,
                );
                let merged = everything_both_copies_call_off(merged, &ex);
                cache.save_calendar_event(&merged)?;"""
after = """                carry_over_local_only(
                    &mut merged,
                    &ex,
                    TheCategory::OnlyHere,
                    TheStatus::AlsoAtTheProvider,
                );
                let merged = everything_both_copies_call_off(merged, &ex);
                cache.save_calendar_event(&merged)?;"""
red = [
    "application::calendar::tests::test_a_category_changed_in_outlook_reaches_this_computer",
    "application::calendar::tests::test_an_outlook_read_takes_outlooks_category_and_keeps_the_status_set_here",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# Measured by hand on 2026-08-14. The Outlook mirror of "the days a Google
# series already calls off survive a read naming only the series", above:
# `everything_both_copies_call_off` was added to this same call site to close
# the matching gap, a later read naming only a whole event silently erasing a
# day an earlier read had already learned was called off. This program's own
# calendar view is not known to ever answer with a series itself as a single
# item outside its own occurrences, unlike Google, so unlike that guard's own
# test this one is a defence rather than a proven scenario: it proves the
# merge would hold if such an item ever arrives, not that one has been seen
# to.

[[guard]]
name = "the days an Outlook series already calls off survive a read naming only the event"
file = "src/application/calendar.rs"
before = """                TheStatus::OnlyHere,
                );
                let merged = everything_both_copies_call_off(merged, &ex);
                cache.save_calendar_event(&merged)?;"""
after = """                TheStatus::OnlyHere,
                );
                cache.save_calendar_event(&merged)?;"""
red = [
    "application::calendar::tests::test_a_day_cancelled_at_outlook_stays_off_if_a_later_read_answers_with_the_series_itself",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# Measured by hand on 2026-08-14. `carry_over_local_only` is called a third
# time, inside `one_day_of_a_google_series`, for a single day of a series that
# changed rather than for a whole series or a whole meeting. The two guards
# above only ever drove the whole-series call sites; this one was reachable
# and unwatched, the same gap the pair above closed for the other two, found
# while reading the same function for the sync gap on the Outlook side of a
# series' changed days.

[[guard]]
name = "a Google merge of one changed day keeps the category typed here and takes Google's status"
file = "src/application/calendar.rs"
before = """            carry_over_local_only(
                &mut that_day,
                held,
                TheCategory::OnlyHere,
                TheStatus::AlsoAtTheProvider,
            );
            result.updated += 1;"""
after = """            carry_over_local_only(
                &mut that_day,
                held,
                TheCategory::AlsoAtTheProvider,
                TheStatus::OnlyHere,
            );
            result.updated += 1;"""
red = [
    "application::calendar::tests::test_a_google_read_of_one_changed_day_keeps_the_category_typed_here_and_takes_googles_status",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# Measured by hand on 2026-08-14. The Outlook mirror of the guard above, at
# the matching call site inside the new one_day_of_a_microsoft_series: a
# fourth carry_over_local_only call site, and the fourth instance of the
# choice this whole section exists to watch. Outlook's own polarity is the
# opposite of Google's at every one of its own call sites, so this is also
# the single easiest mistake a change here could make: copying Google's
# per-day polarity instead of Outlook's own whole-event one.

[[guard]]
name = "an Outlook merge of one changed day takes Outlook's category and keeps the status set here"
file = "src/application/calendar.rs"
before = """            carry_over_local_only(
                &mut that_day,
                held,
                TheCategory::AlsoAtTheProvider,
                TheStatus::OnlyHere,
            );
            result.updated += 1;"""
after = """            carry_over_local_only(
                &mut that_day,
                held,
                TheCategory::OnlyHere,
                TheStatus::AlsoAtTheProvider,
            );
            result.updated += 1;"""
red = [
    "application::calendar::tests::test_an_outlook_read_of_one_changed_day_takes_outlooks_category_and_keeps_the_status_set_here",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── One sentence for "this build has no sign-in credentials" ────────────────
#
# The condition used to be worded four ways in three files: three levels of
# jargon and one that read a file name aloud. `no_sign_in_credentials` is now
# the one place that words it, and the two guards below cover its two kinds of
# reader: the mail path that raises the condition with no dialog open, and the
# account manager screen that reports it in three places.
#
# Measured by hand on 2026-08-13, against the whole library, one build and run
# per break.

[[guard]]
name = "a missing OAuth credential is reported through the shared sentence, not a wording of its own"
file = "src/application/mail_auth.rs"
before = """        return Err(Error::Authentication(no_sign_in_credentials(&provider)));"""
after = """        return Err(Error::Authentication(format!(
            "No {provider} client credentials are configured, so this account cannot sign in."
        )));"""
red = [
    "application::mail_auth::tests::test_an_account_needing_a_new_token_is_told_which_control_to_press",
    "application::mail_auth::tests::test_the_missing_credentials_sentence_is_worded_once_and_names_the_way_out",
]
tests_last_seen = [
    { file = "src/application/mail_auth.rs", tests = 10 },
]

# Measured by hand on 2026-08-13. The account manager screen answers the same
# condition in three places; this breaks one of them, the edit path, back to
# its old jargon with no remedy.

[[guard]]
name = "the account manager's edit path reports missing credentials through the shared sentence"
file = "src/presentation/wx_account_manager.rs"
before = """                                OAuthFlowResult::NoCreds(provider) => {
                                    said_and_shown(
                                        status,
                                        a11y,
                                        &format!(
                                            "Account updated. {}",
                                            no_sign_in_credentials(&provider)
                                        ),
                                        Priority::High,
                                    );
                                }"""
after = """                                OAuthFlowResult::NoCreds(provider) => {
                                    let _ = &provider;
                                    said_and_shown(
                                        status,
                                        a11y,
                                        "Account updated. OAuth credentials are not configured",
                                        Priority::High,
                                    );
                                }"""
red = [
    "presentation::wx_account_manager::tests::test_the_missing_credentials_condition_is_worded_once_not_here",
]
tests_last_seen = [
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
]

# ── One owner for what a manager window says about a changed row ────────────
#
# Three manager windows and the account manager worded a delete as
# "Deleted: {name}", character for character what the mail path says when a
# message is gone from a server. By ear, on a product where by ear is the
# point, those read as the same event, and neither one said whether a
# contact, a filter, a tag, a signature or an account had gone.
# manager_words::deleted is now the one owner; this reverts it to the old
# collision, which is the change the round's own intersection test was
# written to catch.
#
# Measured by hand on 2026-08-13, against the whole library.

[[guard]]
name = "a manager window's delete sentence names its kind and does not collide with mail"
file = "src/presentation/manager_words.rs"
before = """pub(crate) fn deleted(kind: &str, name: &str, left: usize) -> String {
    format!("Deleted the {kind}: {name}{}", and_how_many_now(kind, left))
}"""
after = """pub(crate) fn deleted(kind: &str, name: &str, left: usize) -> String {
    format!("Deleted: {name}{}", and_how_many_now(kind, left))
}"""
# Re-measured by hand on 2026-09-01 against the whole 5,942-test library, after
# `deleted` gained the count a condition list says with every change. The red
# set grew from two to four, and the two new ones are tests this same change
# added: the sentence a condition list says is built from the kind as well, so
# dropping the kind takes the tally with it. That is the staleness `CLAUDE.md`
# says never announces itself, caught here only because the break stopped
# naming a place in the tree and forced a re-measurement.
red = [
    "presentation::manager_words::tests::test_a_condition_change_is_one_sentence_and_not_two",
    "presentation::manager_words::tests::test_every_manager_sentence_names_the_kind_of_thing_it_happened_to",
    "presentation::manager_words::tests::test_no_manager_window_says_what_the_mail_path_says_when_a_message_is_gone",
    "presentation::manager_words::tests::test_only_a_condition_list_counts_out_loud",
]
tests_last_seen = [
    { file = "src/presentation/manager_words.rs", tests = 9 },
]

# ── The app password hint attached to the password box's description ───────
#
# The visible hint under the email box was shown and never spoken,
# deliberately, because it reruns on every keystroke and speaking it would
# flood somebody typing. That left the advice reaching nobody working by ear.
# password_box_description decides whether this address's password box
# carries that advice as an MSAA description, read once when the box takes
# focus rather than announced.
#
# Measured by hand on 2026-08-13, against the whole library.

[[guard]]
name = "the password box's description carries the app password advice for the right addresses"
file = "src/presentation/wx_account_manager.rs"
before = """fn password_box_description(email: &str) -> Option<&'static str> {
    offers_app_passwords(email).then_some(APP_PASSWORD_HINT)
}"""
after = """fn password_box_description(email: &str) -> Option<&'static str> {
    let _ = email;
    None
}"""
red = [
    "presentation::wx_account_manager::tests::test_the_password_box_is_described_with_the_app_password_advice",
]
tests_last_seen = [
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
]

# ── A changed day of a CalDAV series has to be marked seen before it can be
#    forgotten again ─────────────────────────────────────────────────────────
#
# A day changed out of a CalDAV series is stored under an identity minted for
# it, "{uid}:{recurrence-id}", because a CalDAV override shares its series'
# own UID on the wire and has no identifier of its own. The pass that removes
# whatever a sync's answer did not name spares only a row whose identity is in
# seen_uids, so a changed day folded into storage without that identity being
# marked seen looks, to the very next sync, exactly like one the server had
# dropped, and is deleted the moment after it was created.
#
# Measured by hand on 2026-08-14, against the whole library.
#
# Rewritten on 2026-09-15 by 08-07's sweep, twice in one day, and the second
# time is the honest one. The sweep found the break above, the insert at the
# top of `one_caldav_day_kept_out_of_its_series` taken out, reddening nothing
# on the runners and again here. The break was moved to the other marking,
# where the loop over the server's answer marks whatever identity a row was
# stored under, on the reading that the rule had moved there; measured, that
# break reddens exactly one test, and it is not the moved-day test above. So
# neither marking is what keeps a moved day now, the rule this record was
# named for has no test that would notice it broken, and it is a ledger
# entry rather than a record; the first insert is a dead-code candidate on
# the same ledger. What stays is what the measurement found: the marking of
# a stored identity is guarded by the one test named below, and the record
# is renamed to what it guards. The old name and the old red are the two
# lines above this paragraph in git.

[[guard]]
name = "a row stored under the identity the server named is not taken for one it dropped"
file = "src/application/caldav_sync.rs"
before = """            seen_uids.insert(under.to_string());"""
after = """            let _ = under;"""
red = [
    "application::caldav_sync::tests::test_an_event_stored_under_half_an_identifier_keeps_what_was_typed_on_it",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

# ── A SASL response line has to be told apart from an ordinary one ──────────
#
# AUTHENTICATE carries its credential differently from LOGIN and PASS: the
# server prompts with a bare "+" and the client answers on a line of its own,
# with no command word in front of it for without_the_credential to
# recognise. Nothing drove that exchange through this harness until the
# XOAUTH2 loopback test below did, and until then the gap was silent: the
# transcript is read by every failing assertion and printed with it, so an
# unredacted credential would have gone out with the first unrelated failure
# on a connection that happened to authenticate with a token.
#
# Measured by hand on 2026-08-14, against the whole library.

[[guard]]
name = "a SASL response is withheld from the transcript, and an empty one is not"
file = "src/common/answering.rs"
before = """        if awaiting_sasl_response && !said.is_empty() {
            record(recording, "<a SASL response, withheld>").await;
        } else {"""
after = """        if false && !said.is_empty() {
            record(recording, "<a SASL response, withheld>").await;
        } else {"""
red = [
    "common::answering::line_protocol_tests::test_a_sasl_response_does_not_reach_the_transcript_but_an_empty_one_still_shows",
]
tests_last_seen = [
    { file = "src/common/answering.rs", tests = 14 },
]

# ── A refused XOAUTH2 credential is not sent a second time ──────────────────
#
# XOAuth2's own doc comment: a server that refuses the credential sends a
# second challenge carrying a JSON error, and the client has to answer that
# one with an empty line before the failure is reported. Nothing had ever
# driven MailAuth::OAuth2 through the loopback harness before the test below,
# so this had never run against anything that could refuse it.
#
# Measured by hand on 2026-08-14, against the whole library.

[[guard]]
name = "an XOAUTH2 retry answers empty rather than resending the credential"
file = "src/service/protocols/imap.rs"
before = """    fn process(&mut self, _challenge: &[u8]) -> Self::Response {
        if self.sent {
            return String::new();
        }
        self.sent = true;
        std::mem::take(&mut self.credential)
    }"""
after = """    fn process(&mut self, _challenge: &[u8]) -> Self::Response {
        self.sent = true;
        self.credential.clone()
    }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_refused_oauth_sign_in_completes_the_continuation_rather_than_hanging",
    "service::protocols::imap::tests::test_xoauth2_answers_the_credential_once_and_then_answers_empty",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── Capability is read once at sign-in and never again ───────────────────────
#
# abilities.rs's own doc comment: capabilities are "asked once, at sign-in,
# and carried on the session," replacing a CAPABILITY command before every
# operation that cared, which cost a round trip per delete and could answer
# differently between two lines of the same function. Nothing had ever
# counted how many times this file actually asks.
#
# Measured by hand on 2026-08-14, against the whole library.

[[guard]]
name = "capability is asked for once at sign-in, not again on every connect"
file = "src/service/protocols/imap.rs"
before = """        };
        session.introduce_ourselves().await;
        Ok(session)"""
after = """        };
        let _ = session.read_abilities().await;
        session.introduce_ourselves().await;
        Ok(session)"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_capability_is_asked_for_exactly_once_after_signing_in",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── A second SELECT of the same folder still reads the server's answer ──────
#
# UIDVALIDITY only means anything if a caller can see it change: a second
# SELECT of a mailbox another client has touched in between renumbers every
# UID, and the cached ones stop meaning anything. Nothing had ever read the
# value back off a real SELECT reply, so a caching "optimisation" keyed on the
# folder already being the one selected could have reached this file without
# anything noticing.
#
# Measured by hand on 2026-08-14, against the whole library.

[[guard]]
name = "a second SELECT of an already-open folder still asks the server"
file = "src/service/protocols/imap.rs"
before = """    pub async fn select_folder(&mut self, path: &str) -> Result<MailboxStatus> {
        // CONDSTORE asks the server to name the folder's highest modification"""
after = """    pub async fn select_folder(&mut self, path: &str) -> Result<MailboxStatus> {
        if self.selected.as_deref() == Some(path) {
            return Ok(MailboxStatus {
                uid_validity: Some(1),
                highest_modseq: None,
            });
        }
        // CONDSTORE asks the server to name the folder's highest modification"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_second_select_of_the_same_folder_reports_a_changed_uidvalidity",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── A CalDAV exception row is recognised without needing its series ─────────
#
# Round 22 made a day a calendar server moved out of a series, and still
# filed at that series' own address, refused for an edit or a delete. It
# worked by comparing the row against the series named in
# `cut_from_event_id`, which is only useful once that series is stored here.
# The ordinary first sync of a brand-new account, a series outside the window
# a sync asked for, or a resource whose answer never carries the master
# alongside its override all leave `cut_from_event_id` unset, and with it
# unset the round-22 comparison never even runs: it bails out to "nothing is
# shared" before asking anything else, and a delete on a row like that sent
# an unconditional DELETE to the whole series, exactly as before round 22.
#
# `provider_recurrence_id` closes that: a row a calendar server sent as one
# VEVENT among several for one series carries it the moment it is read,
# whether or not the series is stored here yet, and its presence alone is
# enough to answer the question. Breaking the leading check that reads it
# reproduces the exact gap round 22 left open, one call at a time: the pure
# fact function, the production glue code in `presentation::managers` that
# never gets a chance to compare against a series it cannot find, and the
# edit call site the gate protects, reproduced end to end against a synced
# first-sync CalDAV answer that never names the series.
#
# The delete call site was the fourth test named here on 2026-08-15, and it
# is written up rather than quietly dropped, because losing it is the part
# worth knowing. That test is built in two arms: the gate refuses, so check
# the refusal reads right; the gate lets it through, so drive the delete and
# prove an unconditional DELETE goes out at the address this row shares with
# its series. The second arm was the whole of its value, because that day
# the deletion loop in `push_to_the_calendar_server` held one unconditional
# `delete_event` and nothing else. `ba7ed88`, the next day, gave that loop a
# second door: a deletion note carrying its own `provider_recurrence_id`
# goes through `delete_one_occurrence`, which marks the one VEVENT cancelled
# rather than removing the resource. So the damage the second arm was
# written to catch cannot happen by that route any more, both arms pass, and
# no answer this gate can give will fail that test.
#
# Measured three ways rather than assumed. The break on its own leaves it
# green. The break with that routing put back reddens it, with the DELETE at
# the shared address this record describes. The routing put back on its own,
# with this gate whole, reddens the sibling test instead:
# `application::caldav_sync::tests::test_deleting_a_caldav_occurrence_exception_never_sends_a_delete_for_the_whole_series`
# takes the open arm, because its series is known here, and asserts the
# cancelling PUT.
#
# So what keeps a delete off the whole series now is that routing rather
# than this gate, and that routing has a guard of its own.
#
# The first draft of this paragraph said that nothing was left watching this
# gate's answer for deleting, which was reasoned from the three tests above
# and not measured. It is wrong. Taking the refusal off deleting altogether,
# `&& matches!(done, WhatIsBeingDone::Changing)` on the refusal in
# `can_be_honoured`, reddens two tests in `application::calendar`:
# `test_a_row_that_shares_its_address_with_its_series_is_refused_whatever_is_chosen`
# and
# `test_a_row_that_shares_its_address_is_allowed_only_to_change_the_whole_series_once_its_series_is_known`.
# They build `WhatTheCalendarAllows` by hand, which is why the break above
# does not reach them and why they hold the decision rather than the flag.
#
# So no link in the chain is unguarded: the flag has the three tests below,
# the decision has those two, and the routing has its own. What is gone is
# the one test that joined them, the only one that drove a real delete from
# this flag out to a request. It cannot be written again in its old shape,
# because the request it was watching for is no longer sent whatever this
# gate answers.
#
# Re-measured by hand on 2026-09-02, against the whole library: exactly
# these three tests went red and nothing else did.

[[guard]]
name = "a CalDAV exception row shares its series' address without needing the series"
file = "src/application/calendar.rs"
before = """    if row.provider_recurrence_id.is_some() {
        return true;
    }
    let Some(cut_from) = row.cut_from_event_id.as_deref() else {"""
after = """    if false {
        return true;
    }
    let Some(cut_from) = row.cut_from_event_id.as_deref() else {"""
red = [
    "application::calendar::tests::test_a_day_carrying_a_provider_recurrence_id_shares_the_series_address_even_when_the_series_is_not_given",
    "application::caldav_sync::tests::test_editing_a_caldav_occurrence_exception_whose_series_was_never_resolved_is_refused_before_it_can_loop_forever",
    "presentation::managers::changing_one_day_of_a_series::test_what_this_rows_calendar_allows_flags_a_row_whose_series_is_not_yet_resolved",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── An occurrence exception's own change lands in its own VEVENT ───────────
#
# `ical_with_the_event_changed` is pinned to the first VEVENT a document
# holds, on purpose: its own doc comment says why, and the guard nearby on
# `holds_the_event` protects it. `ical_with_the_occurrence_changed` is the
# sibling that exists because that pin is wrong for a day the server has
# already split out, which is hardly ever first. This guards the one line
# that makes it right: locating by UID and RECURRENCE-ID rather than falling
# back to the document's first VEVENT the way its sibling always does. That
# fallback is the exact naive shape a first attempt at this took, caught
# before it shipped: the series gets the change instead of the day, or the
# document is refused outright, and either way a sibling exception sitting
# beside the one being changed is exactly the case most likely to go unnoticed
# in a document with only one VEVENT in it.
#
# The first measurement named three tests and missed two: the pure-function
# tests in `service::caldav` are not the only ones that build a real
# multi-VEVENT document and change one occurrence in it.
# `application::caldav_sync`'s own occurrence-exception tests do too, one
# level up, and this break reaches them the same way.
#
# Measured by hand on 2026-08-16, against the whole library: exactly these
# five tests went red and nothing else did.

[[guard]]
name = "an occurrence exception is located by UID and RECURRENCE-ID, not taken as the first VEVENT"
file = "src/service/caldav.rs"
before = """    let its = the_occurrence_named(&events, &lines, &event.uid, recurrence_id)
        .ok_or(WhyTheChangeWasNotMade::TheOccurrenceIsNotInTheDocument)?;"""
after = """    let its = events
        .first()
        .ok_or(WhyTheChangeWasNotMade::TheOccurrenceIsNotInTheDocument)?;"""
red = [
    "service::caldav::writing_tests::test_editing_the_occurrence_leaves_the_series_and_a_sibling_exception_alone",
    "service::caldav::writing_tests::test_editing_an_occurrence_not_present_in_the_document_is_refused",
    "service::caldav::writing_tests::test_editing_the_wrong_events_occurrence_is_refused",
    "service::caldav::writing_tests::test_cancelling_the_occurrence_leaves_the_series_and_a_sibling_exception_alone",
    "application::caldav_sync::tests::test_editing_a_caldav_occurrence_exception_is_refused_before_it_can_loop_forever",
    "application::caldav_sync::tests::test_editing_a_caldav_occurrence_exception_sends_if_match_with_the_fetched_tag",
    "application::caldav_sync::tests::test_deleting_a_caldav_occurrence_exception_sends_a_cancellation_rather_than_a_delete",
    "application::caldav_sync::tests::test_deleting_a_caldav_occurrence_exception_never_sends_a_delete_for_the_whole_series",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/service/caldav.rs", tests = 170 },
]

# ── An occurrence is read off the document the same careful way ─────────────
#
# `the_occurrence_as_the_document_holds_it` is the read-side sibling the
# delete path uses: it has to locate the same one VEVENT among several that
# `ical_with_the_occurrence_changed` above locates to write into, through the
# same [`the_occurrence_named`], so a caller that reads a day off a document
# before cancelling it and the writer that splices the cancellation back in
# cannot come to disagree about which VEVENT either one means. This is a
# second call to that function, not the same call the guard above protects,
# so a naive fallback to the document's first VEVENT here needs its own
# guard: the one above only breaks the call inside
# `ical_with_the_occurrence_changed` and would leave this one, a completely
# separate line of source, untouched.
#
# Measured by hand on 2026-08-17, against the whole library.

[[guard]]
name = "an occurrence is read off the document by UID and RECURRENCE-ID, not taken as the first VEVENT"
file = "src/service/caldav.rs"
before = """    let its = the_occurrence_named(&events, &lines, uid, recurrence_id)
        .ok_or(WhyTheChangeWasNotMade::TheOccurrenceIsNotInTheDocument)?;"""
after = """    let its = events
        .first()
        .ok_or(WhyTheChangeWasNotMade::TheOccurrenceIsNotInTheDocument)?;"""
red = [
    "service::caldav::writing_tests::test_the_occurrence_is_read_off_the_document_with_its_own_current_properties_intact",
    "service::caldav::writing_tests::test_the_occurrence_is_not_found_when_the_document_names_no_such_recurrence_id",
    "service::caldav::writing_tests::test_cancelling_the_occurrence_leaves_the_series_and_a_sibling_exception_alone",
    "application::caldav_sync::tests::test_deleting_a_caldav_occurrence_exception_sends_a_cancellation_rather_than_a_delete",
    "application::caldav_sync::tests::test_deleting_a_caldav_occurrence_exception_never_sends_a_delete_for_the_whole_series",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/service/caldav.rs", tests = 170 },
]

# ── An occurrence exception is opened exactly as narrowly as intended ───────
#
# The refusal above stayed absolute for a day a calendar server already split
# into its own VEVENT until an earlier round: whichever of edit or delete was
# chosen, on a row like that, it was always refused. That round opened one
# door: changing the whole event, once this program has the series stored
# locally to change one VEVENT of the shared resource against rather than the
# whole document. A later round opened a second door the same narrow way:
# deleting the whole event, once a write primitive existed
# (`application::caldav_sync::delete_one_occurrence`) that marks the one
# VEVENT cancelled rather than removing the resource. A break that makes the
# escape clause always true reopens both doors for every combination at once,
# including a row whose series was never resolved, which has no document
# locally to change or cancel one VEVENT of.
#
# Three tests catch this now, and the two that walk `can_be_honoured`'s own
# matrix directly are the reliable ones: a network-level delete test that
# used to catch a wrongly widened door no longer can on its own, once a
# write primitive exists for deleting that is itself safe regardless of
# what this gate decides, because it routes on the row's own
# `provider_recurrence_id` rather than on anything the gate computed. That
# is not a reason to trust the gate less; it is a second, independent
# layer refusing the same thing this gate refuses. The third test is
# reliable for a different reason: forcing the door open lets an edit
# whose series was never resolved through too, and that one still fails
# downstream, because nothing before this line ever fetched a document to
# change.
#
# Measured by hand on 2026-08-17, against the whole library.

[[guard]]
name = "editing or deleting a CalDAV exception is allowed exactly as narrowly as intended"
file = "src/application/calendar.rs"
before = """    let a_known_occurrence_exception_reached_as_a_whole = allows.goes
        == WhereAChangeGoes::ACalendarServer
        && allows.the_series_it_left_is_known_here
        && matches!(done, WhatIsBeingDone::Changing | WhatIsBeingDone::Deleting)
        && means == EditMeans::WholeSeries;"""
after = """    let a_known_occurrence_exception_reached_as_a_whole = true;"""
red = [
    "application::calendar::tests::test_a_row_that_shares_its_address_with_its_series_is_refused_whatever_is_chosen",
    "application::calendar::tests::test_a_row_that_shares_its_address_is_allowed_only_to_change_the_whole_series_once_its_series_is_known",
    "application::caldav_sync::tests::test_editing_a_caldav_occurrence_exception_whose_series_was_never_resolved_is_refused_before_it_can_loop_forever",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
]

# ── The POP delete checkboxes keep their spoken consequence ────────────────
#
# "Leave mail on the server after downloading it" and "Let me delete mail on
# this computer" used to read to a screen reader as a name and a checked
# state and nothing else. Neither said what happens next, and the two are
# easy to mix up on a POP account: one throws mail off the server for good
# with no Trash behind it, the other never leaves this computer at all.
# cb_with_description attaches SERVER_REMOVAL_IS_PERMANENT and
# DELETING_HERE_NEVER_REACHES_THE_SERVER as their accessible descriptions,
# one call each, the same shape describe_password_box already uses above it
# in this file for the app password hint.
#
# Reverting either checkbox back to the plain `cb` closure this dialog
# builds every other checkbox with is exactly how that regression would
# happen: quietly, in a future edit with no reason to suspect these two
# controls are different from their neighbours. `cb` and
# `cb_with_description` look almost identical on purpose, because the whole
# point is that they attach an accessible object the same one-call way; nothing
# else marks the difference.
#
# Measured by hand on 2026-08-16, against the whole library: the one test
# named went red and nothing else did. The break's text changed on
# 2026-09-16, when `cb` and `cb_with_description` stopped handing back the
# empty static they used to place before each box (09-05, #42), and both
# records were measured again the same day through `--remeasure`.

[[guard]]
name = "POP's leave-on-server checkbox keeps its spoken consequence"
file = "src/presentation/wx_account_manager.rs"
before = """    let pop_leave = cb_with_description(
        "&Leave mail on the server after downloading it",
        true,
        SERVER_REMOVAL_IS_PERMANENT,
    );"""
after = """    let pop_leave = cb("&Leave mail on the server after downloading it", true);"""
red = [
    "presentation::wx_account_manager::tests::test_the_pop_delete_controls_carry_their_consequence_to_a_screen_reader",
]
tests_last_seen = [
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
]

[[guard]]
name = "POP's local-delete checkbox keeps its spoken consequence"
file = "src/presentation/wx_account_manager.rs"
before = """    let allow_deleting = cb_with_description(
        "Let me delete mail on this &computer",
        true,
        DELETING_HERE_NEVER_REACHES_THE_SERVER,
    );"""
after = """    let allow_deleting = cb("Let me delete mail on this &computer", true);"""
red = [
    "presentation::wx_account_manager::tests::test_the_pop_delete_controls_carry_their_consequence_to_a_screen_reader",
]
tests_last_seen = [
    { file = "src/presentation/wx_account_manager.rs", tests = 14 },
]

# ── A server that never answers is waited for off the window's thread, but ──
# ── still has to be given up on ──────────────────────────────────────────────
#
# Moving the asking off the window's own thread stops the freeze; it does not
# by itself stop an unbounded wait. Before this guard existed, taking the
# bound back out of the asking, while leaving it running away from the
# window's thread and the waiting window itself untouched, left the whole
# library green: the window would stay responsive but the "Looking for
# calendars" dialog would sit open forever against a server that never
# answers, with no way out but Stop and no sentence ever telling somebody
# that is what happened.
#
# The break takes the bound back out and waits on the asking directly, exactly
# as it read before this call was moved off the window's thread.
#
# Measured by hand on 2026-08-16 with scripts/guards.py: the one test named
# went red and nothing else did.

[[guard]]
name = "a calendar server that never answers is still given up on"
file = "src/presentation/managers.rs"
before = """    rt.spawn(async move {
        let asking = calendar_source::what_a_server_has(&address, &user_name, &password);
        let answer =
            match tokio::time::timeout(calendar_source::HOW_LONG_A_SERVER_IS_GIVEN, asking).await {
                Ok(answer) => answer,
                Err(_) => Err(calendar_source::NO_ANSWER_IN_TIME.to_string()),
            };"""
after = """    rt.spawn(async move {
        let answer = calendar_source::what_a_server_has(&address, &user_name, &password).await;"""
red = [
    "application::calendar_source::tests::test_the_window_is_never_held_while_a_calendar_server_is_asked",
]
tests_last_seen = [
    { file = "src/application/calendar_source.rs", tests = 52 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── A recipient copied from a message is split the quote-aware way ─────────
#
# `addresses()` had no coverage at all for a "Name <address>" entry before
# this guard existed: every existing fixture was already a bare address, so
# a naive split on comma and semicolon passed the whole suite while failing
# every reply to a real, named sender. The break puts that naive split back,
# adapted to whatever `addresses()` returns at the time it is measured.
#
# Measured by hand again on 2026-08-16 with scripts/guards.py, after a second
# round added four more tests for a name containing a comma or a bracket: all
# 9 tests named went red and nothing else did. The first measurement, the
# same day, named 5; the record fell one edit behind its own file before it
# was ever run against anything else, which is the exact failure mode this
# file exists to catch.
#
# Measured by hand a third time on 2026-08-16, the same day, after a round
# that let a recipient's name travel all the way to the outgoing header
# renamed `bare_address` to `recipient_address` and widened what it returns
# from a bare `String` to an `EmailAddress` carrying the name too. The record
# fell one edit behind its own file again within the same round, exactly the
# case this project's own guardrails warn a guard can go stale without a
# session boundary in between. The break itself is the same naive split as
# before, written against the type `addresses()` returns now; the count of
# tests it reddens grew from 9 to 10, because four of this round's renamed
# tests and one wx_app real-chain test now also check the name survives, not
# only the address.

[[guard]]
name = "a recipient copied from a message is split the quote-aware way, name and address both"
file = "src/application/mail_controller.rs"
before = """fn addresses(field: &str) -> Vec<EmailAddress> {
    crate::application::reply::split_addresses(field)
        .iter()
        .map(|entry| recipient_address(entry))
        .collect()
}"""
after = """fn addresses(field: &str) -> Vec<EmailAddress> {
    field
        .split([',', ';'])
        .map(|addr| EmailAddress::new(addr.trim().to_string(), None))
        .filter(|addr| !addr.address.is_empty())
        .collect()
}"""
red = [
    "application::mail_controller::send_request_tests::test_a_name_and_address_recipient_reaches_the_request_as_address_and_name_both",
    "application::mail_controller::send_request_tests::test_a_name_and_address_cc_recipient_is_split_too",
    "application::mail_controller::send_request_tests::test_a_display_name_with_a_comma_does_not_split_into_two_bad_recipients",
    "application::mail_controller::send_request_tests::test_a_mixed_list_of_bare_and_named_recipients_all_reach_the_request_each_with_whatever_name_it_had",
    "application::mail_controller::send_request_tests::test_a_comma_in_a_stored_display_name_still_reaches_the_request_with_the_name_kept",
    "application::mail_controller::send_request_tests::test_an_angle_bracket_in_a_stored_display_name_still_reaches_the_request_with_the_name_kept",
    "presentation::wx_app::reply_recipients_reach_the_wire::test_replying_to_a_sender_whose_name_has_a_comma_reaches_the_wire_as_a_bare_address",
    "presentation::wx_app::reply_recipients_reach_the_wire::test_replying_to_a_sender_whose_name_has_an_angle_bracket_reaches_the_wire_as_a_bare_address",
    "presentation::wx_app::reply_recipients_reach_the_wire::test_replying_to_a_named_sender_reaches_the_wire_as_a_bare_address",
    "presentation::wx_app::reply_recipients_reach_the_wire::test_replying_to_a_named_sender_carries_their_name_onto_the_to_header_on_the_wire",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── A name written back out is quoted when it needs to be ──────────────────
#
# `EmailAddress`'s `Display` wrote a name next to its address with no quoting
# at all. A name containing a comma read back as a second, non-existent
# recipient; a name containing a literal angle bracket read back as the start
# of a second address wrapper, corrupting the real one behind it. The break
# puts the old, unquoted line back.
#
# Measured by hand on 2026-08-16 with scripts/guards.py: all 10 tests named
# went red and nothing else did. (Corrected from an earlier count of 8: the
# break also reddens the escaping tests added a commit later, which the
# original count was taken before.)
#
# Measured by hand again on 2026-09-02, and that is the first measurement
# this record has allowed since the day it was written. Two of its ten names
# had not existed for seventeen days, so every run that reached it stopped at
# "the test harness never ran" before reporting anything about the break.
# The two tests were renamed, not removed. 8445b96, the same day and under
# four hours later, carried a recipient's display name onto the outgoing To
# and Cc headers and renamed the pair from `..._reaches_the_request_bare` to
# `..._reaches_the_request_with_the_name_kept`, widening each from asserting
# the address alone to asserting the address and the name both. That commit
# corrected those same two names in the record directly above this one and
# left them stale here. It re-measured the two records whose code it edited;
# this was the third, made stale by renaming tests it names rather than by
# touching the line it breaks, and nothing asked about that direction.
#
# Nothing stopped guarding the rule. All ten tests named on 2026-08-16 still
# go red, the renamed two included, and those two now assert more than they
# used to. What the record had wrong is the other direction: the break
# reddens 20 tests, not 10. Ten more read a name back through this same
# writer and none of them was written down. Replying to a sender whose name
# holds a bracket, exporting contacts, the people-lookup line, writing a
# message out to a file, and the message list's correspondent column all
# corrupt when the quoting comes off. All 20 went red, nothing else did, and
# the file was restored byte for byte from a copy taken before the break.

[[guard]]
name = "a name written back out is quoted when it needs to be"
file = "src/common/types.rs"
before = """impl fmt::Display for EmailAddress {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match &self.name {
            Some(name) if needs_quoting(name) => write!(f, "{} <{}>", quoted(name), self.address),
            Some(name) => write!(f, "{} <{}>", name, self.address),
            None => write!(f, "{}", self.address),
        }
    }
}"""
after = """impl fmt::Display for EmailAddress {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match &self.name {
            Some(name) => write!(f, "{} <{}>", name, self.address),
            None => write!(f, "{}", self.address),
        }
    }
}"""
red = [
    "common::types::tests::test_a_display_name_with_a_comma_is_quoted_so_it_reads_back_as_one_person",
    "common::types::tests::test_a_display_name_with_an_angle_bracket_is_quoted_so_it_does_not_look_like_a_second_address",
    "service::mime::tests::test_a_quoted_name_with_a_comma_is_read_back_as_one_address",
    "service::mime::tests::test_a_quoted_name_with_an_angle_bracket_is_read_back_as_one_address",
    "application::mail_controller::send_request_tests::test_a_comma_in_a_stored_display_name_still_reaches_the_request_with_the_name_kept",
    "application::mail_controller::send_request_tests::test_an_angle_bracket_in_a_stored_display_name_still_reaches_the_request_with_the_name_kept",
    "presentation::wx_app::reply_recipients_reach_the_wire::test_replying_to_a_sender_whose_name_has_a_comma_reaches_the_wire_as_a_bare_address",
    "presentation::wx_app::reply_recipients_reach_the_wire::test_replying_to_a_sender_whose_name_has_an_angle_bracket_reaches_the_wire_as_a_bare_address",
    "common::types::tests::test_a_quote_mark_inside_a_display_name_is_escaped_so_the_quoting_stays_well_formed",
    "common::types::tests::test_a_backslash_inside_a_display_name_is_escaped_so_it_is_not_read_as_an_escape_itself",
    "application::export_tree::tests::test_a_list_of_awkward_names_keeps_its_length_and_its_order",
    "application::export_tree::tests::test_any_name_a_stored_column_can_hold_comes_back_as_itself",
    "application::export_tree::tests::test_every_name_in_a_stored_column_is_the_same_name_when_it_comes_back",
    "application::looking_people_up::tests::test_the_line_a_choice_makes_reads_back_as_the_people_who_are_in_it",
    "application::message_files::tests::test_every_header_a_message_carries_survives_being_written_out",
    "application::reply::tests::test_a_reply_to_one_person_whose_name_has_a_bracket_says_the_whole_name",
    "application::reply::tests::test_key_of_recovers_the_real_address_when_the_name_contains_a_literal_angle_bracket",
    "application::reply::tests::test_name_or_address_recovers_the_whole_name_when_it_contains_a_literal_angle_bracket",
    "application::reply::tests::test_reply_to_all_does_not_double_a_sender_whose_name_has_a_bracket_in_it",
    "presentation::message_rows::tests::test_correspondent_recovers_the_whole_name_when_it_contains_a_literal_angle_bracket",
]
tests_last_seen = [
    { file = "src/application/export_tree.rs", tests = 38 },
    { file = "src/application/looking_people_up.rs", tests = 37 },
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/application/message_files.rs", tests = 51 },
    { file = "src/application/reply.rs", tests = 28 },
    { file = "src/common/types.rs", tests = 30 },
    { file = "src/presentation/message_rows.rs", tests = 41 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "src/service/mime.rs", tests = 52 },
]

# A week-start day other than Monday used to be refused on every rule that
# named one, whatever the rule's own shape. RFC 5545 3.3.10 only gives WKST a
# reading on a WEEKLY rule naming two or more weekdays with INTERVAL greater
# than one; everywhere else it cannot move the answer, and Google writes
# WKST=SU on nearly every rule it exports regardless. The break below puts
# that unconditional refusal back.
#
# Measured by hand on 2026-08-17: applied the break, ran the library, and
# exactly these three tests failed and nothing else did. Restored, reran,
# clean.

[[guard]]
name = "a week-start day other than Monday is refused only where it could move the answer"
file = "src/application/occurrences.rs"
before = """        let wkst_could_move_this = how == How::Weekly && every > 1 && weekdays.len() > 1;"""
after = """        let wkst_could_move_this = true;"""
red = [
    "application::occurrences::tests::test_a_real_google_calendar_rule_with_wkst_su_and_three_weekdays_expands_correctly",
    "application::occurrences::tests::test_a_real_google_calendar_series_with_wkst_su_and_eight_exclusions_expands_correctly",
    "application::occurrences::tests::test_wkst_other_than_monday_is_accepted_wherever_it_cannot_change_the_answer",
]
tests_last_seen = [
    { file = "src/application/occurrences.rs", tests = 63 },
]

# A monthly or yearly rule fast-forwards past a first date far before the
# window it is asked about rather than stepping one block at a time from the
# beginning, and months_apart is the only arithmetic that says how far to
# jump. Nothing in the big walked-vs-stepped comparison happened to pick
# dates far enough apart, in the right shape, to tell a correct jump from a
# wrong one, so a whole-tree mutation run found four different ways to break
# this function with nothing catching any of them.
#
# Measured by hand on 2026-08-17, one representative break of the four
# (replacing the multiplication with a division, cargo-mutants' own finding):
# exactly this one test failed and nothing else did.

[[guard]]
name = "months_apart counts a year crossed as twelve months, not one"
file = "src/application/occurrences.rs"
before = """    (i64::from(to.year()) - i64::from(from.year())) * 12 + i64::from(to.month())"""
after = """    (i64::from(to.year()) - i64::from(from.year())) / 12 + i64::from(to.month())"""
red = [
    "application::occurrences::tests::test_months_apart_counts_signed_month_boundaries_crossed",
]
tests_last_seen = [
    { file = "src/application/occurrences.rs", tests = 63 },
]

# blocks exists only so a test can watch how much work the stepping loop in
# days_stepping_from did; nothing in production reads it. The one test that
# watched it only ever checked an upper bound (did it reach the MOST_STEPS
# backstop), which a counter stuck at zero also satisfies, so a whole-tree
# mutation run found that replacing the increment with a no-op survived.
#
# Measured by hand on 2026-08-17: exactly this one test failed and nothing
# else did.

[[guard]]
name = "the block count days_stepping_from reports is actually counted, not stuck at zero"
file = "src/application/occurrences.rs"
before = """            blocks += 1;"""
after = """            blocks *= 1;"""
red = [
    "application::occurrences::tests::test_a_hand_counted_weekly_walk_matches_the_blocks_it_reports",
]
tests_last_seen = [
    { file = "src/application/occurrences.rs", tests = 63 },
]

# ── long_text.rs's markup walk, tag by tag ──────────────────────────────────
#
# A mutation sweep of `application::long_text` found 17 places in `markup`'s
# tree walk (`blocks`, `list`, `quote`, `inline`) where deleting a tag's own
# arm, or inverting the guard beside it, left the whole suite green. Every one
# was pre-existing correct behaviour; the gap was coverage, not a bug.
# `from_markup`'s own end-to-end tests exercise several tags in one document
# and check the words survived, which a deleted arm's neighbouring fallback
# can satisfy by accident. The 17 guards below each go straight at one tag or
# one guard through `blocks_output`/`inline_output`, which call `blocks` and
# `inline` directly on a fragment `ammonia::clean` never touched, and assert
# the exact markdown string, not a substring.
#
# Measured by hand on 2026-08-19: each of the 17 applied alone, against
# `application::long_text::markup::tests`, reddened exactly the test(s) named
# and nothing else in that module. Restored and reran green after each.

[[guard]]
name = "blocks reads a bare text node with no tag around it"
file = "src/application/long_text.rs"
before = """                Node::Text(text) => {
                    let trimmed = text.trim();
                    if !trimmed.is_empty() {
                        out.push_str(trimmed);
                        out.push_str("\\n\\n");
                    }
                }
                _ => {}"""
after = """                _ => {}"""
# Measured 2026-09-11 for ledger 270. The record named one test and the break
# reddens four. The other three live in `src/service/onenote_page.rs`, which
# arrived on 2026-09-11 and which this record had never listed, so the count
# check could not see them: it compares test counts for the files a record
# names, and a test added to a file no record names is invisible to it. That
# file is listed below now, so the next test added to it says so.
red = [
    "application::long_text::markup::tests::test_bare_text_with_no_tag_around_it_becomes_a_paragraph",
    "service::onenote_page::tests::test_a_code_block_comes_back_as_one_line_of_words",
    "service::onenote_page::tests::test_a_hidden_div_carrying_the_source_does_not_bring_it_back",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

# ── One record split in two on 2026-09-22 by 12-02.1 ───────────────────────
#
# It was "blocks joins a p or div's inline content into one line", one break
# over the shared `"p" | "div"` arm, and run 35520204784 could not apply it:
# the text appeared 0 times. `d470a105`, 11-11.0's newsletter work for #90,
# split the arm, because a newsletter's whole body is a `div` holding its
# headings, its tables and its paragraphs and reading that as one line ran
# every block's words into the next block's. A `p` still goes to
# `push_paragraph` and a `div` now goes to `blocks_or_a_paragraph`.
#
# So the record named one fact about two decisions, and one break can only
# exercise one of them. Split rather than narrowed, because the rule holds for
# both arms and a record covering only the `p` would leave the `div` guarded
# by nothing, which is the shape this file exists to stop. The second census
# number moves by one with it.

[[guard]]
name = "blocks joins a paragraph's inline content into one line"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11 for ledger 270, which put a nested list and a table
# through this walk and moved the arms either side of this one. The break used
# to be the deletion of this arm, written as the two lines it sits above; that
# spelling broke the moment a neighbour changed. It now reads the paragraph as
# a container rather than as one line of inline text, which is the same mistake
# and depends on nothing but the arm itself.
before = '''                    "p" => push_paragraph("", child, out, keeping),'''
after = '''                    "p" => blocks(child, out, keeping),'''
# Re-measured 2026-09-11 after the second reading landed.
# `test_a_picture_with_no_description_comes_back_as_a_picture_with_no_description`
# was named here and stays green with the break applied, so it is taken out: a
# picture reaches the block walk as its own element either way, and reading a
# `p` as a container no longer changes what that test sees. A named test that
# stays green is a test whose name still promises something it no longer
# checks.
#
# Measured again on 2026-09-22 with the arm split, and the list below is that
# run's answer rather than the old record's.
red = [
    "application::long_text::markup::tests::test_a_paragraph_joins_its_inline_content_into_one_line",
    "application::long_text::tests::test_a_line_break_kept_for_editing_is_still_a_line_break",
    "application::long_text::tests::test_a_line_break_read_for_speaking_is_still_a_space",
    "application::long_text::tests::test_a_link_kept_for_editing_keeps_its_address",
    "application::long_text::tests::test_a_link_read_for_speaking_still_loses_its_address",
    "application::long_text::tests::test_an_address_holding_a_bracket_is_written_so_it_can_be_read_back",
    # Unnamed until 2026-09-22, and in a file this record had never named, so
    # the count check could not have seen it: a snippet of a body that is only
    # markup is taken through this arm, and a paragraph read as a container
    # leaves it with nothing to give.
    "data::message_cache::bodies::tests::test_an_html_only_body_still_gives_a_snippet",
    "service::onenote_page::tests::test_a_line_break_comes_back_as_a_line_break",
    "service::onenote_page::tests::test_a_link_comes_back_with_its_address",
    "service::onenote_page::tests::test_bold_comes_back_as_plain_words",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
    "service::onenote_page::tests::test_italic_comes_back_as_plain_words",
    "service::onenote_page::tests::test_struck_out_text_comes_back_as_plain_words",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "blocks joins a div's inline content into one line when the div holds no block"
file = "src/application/long_text.rs"
# The other half of the arm `d470a105` split, recorded on 2026-09-22 by
# 12-02.1. A `div` holding a block child is walked as blocks, which is what
# that commit is for; a `div` holding only a line of words is one paragraph,
# and reading it as blocks cuts the line at every inline element inside it.
# The break asks the walk to do the second to both kinds, which is the mistake
# the shared arm used to make.
before = '''                    "div" => blocks_or_a_paragraph(child, out, keeping),'''
after = '''                    "div" => blocks(child, out, keeping),'''
# Measured on 2026-09-22 rather than predicted; this list is that run's answer.
red = [
    "application::long_text::markup::tests::test_a_div_joins_its_inline_content_into_one_line_like_a_paragraph",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "blocks turns a ul's items into a bulleted list"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11 for ledger 270. `list` gained an indent argument and
# the arm gained a body, so the old break, which deleted this arm by naming the
# one below it, matched nothing. Now it asks the walk to count a bullet list,
# which is one edit inside the arm and survives a neighbour moving.
before = '''                        list(child, out, None, "", keeping);'''
after = '''                        list(child, out, Some(1), "", keeping);'''
red = [
    "application::calendar::tests::test_an_outlook_event_body_written_as_html_is_read_as_the_structure_it_carries",
    "application::long_text::markup::tests::test_a_bullet_list_does_not_run_into_the_paragraph_after_it",
    "application::long_text::tests::test_a_nested_list_in_a_providers_markup_comes_back_nested",
    "application::long_text::tests::test_markup_from_a_provider_is_read_as_the_structure_it_carries",
    "service::onenote_page::tests::test_a_heading_straight_after_a_list_comes_back",
    "service::onenote_page::tests::test_a_list_nested_three_deep_comes_back_at_all_three_depths",
    "service::onenote_page::tests::test_a_nested_list_comes_back_nested",
    "service::onenote_page::tests::test_a_note_goes_out_as_a_page_and_the_page_comes_back_as_a_note",
    "service::onenote_page::tests::test_a_page_written_in_capitals_reads_the_same_as_one_in_small_letters",
    "service::onenote_page::tests::test_an_unordered_list_comes_back",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "blocks counts an ol's items instead of bulleting them"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11 for ledger 270, the mirror of the record above and
# moved for the same reason.
before = '''                        list(child, out, Some(1), "", keeping);'''
after = '''                        list(child, out, None, "", keeping);'''
red = [
    "application::long_text::markup::tests::test_a_numbered_list_counts_its_items_instead_of_bulleting_them",
    "service::onenote_page::tests::test_an_ordered_list_comes_back",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
    "service::onenote_page::tests::test_microsofts_own_list_comes_back_as_a_list",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "blocks still bullets a li with no list around it"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11 for ledger 270. A `table` arm now sits between this
# one and the `blockquote` the old break named, so that spelling matched
# nothing. The break is now the silent drop the arm's own comment argues
# against, applied inside the arm.
before = '''                        push_item("- ", child, out, keeping);'''
after = '''                        let _ = &child;'''
red = ["application::long_text::markup::tests::test_a_list_item_with_no_list_around_it_still_becomes_a_bullet"]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "blocks quotes a blockquote's paragraphs"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11. The arm gained a `keeping` argument, and the break
# no longer names the arm below it: naming a neighbour is what made five
# records in this file stop matching at once when a `table` arm was added
# between them. The quote is now read as a container instead, which is the
# mistake this guard is about and depends on nothing but its own line.
before = '''                    "blockquote" => quote(child, out, keeping),'''
after = '''                    "blockquote" => blocks(child, out, keeping),'''
red = [
    "application::long_text::markup::tests::test_a_blockquote_with_no_paragraph_inside_is_quoted_as_one_line",
    "application::long_text::markup::tests::test_a_blockquote_quotes_each_paragraph_on_its_own_line",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "blocks reads a bare line break between blocks as a newline"
file = "src/application/long_text.rs"
before = """                    "br" => out.push('\\n'),
                    // An image reached without a paragraph around it."""
after = """                    // An image reached without a paragraph around it."""
red = ["application::long_text::markup::tests::test_a_bare_line_break_between_blocks_is_a_newline"]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "blocks silently drops a script or style reaching the block walk directly"
file = "src/application/long_text.rs"
before = """                    "script" | "style" => {}
                    // Anything else, a `body`, `span` or `article` this"""
after = """                    // Anything else, a `body`, `span` or `article` this"""
red = [
    "application::long_text::markup::tests::test_a_script_reaching_the_block_walk_directly_is_silently_dropped",
    "application::long_text::markup::tests::test_a_style_reaching_the_block_walk_directly_is_silently_dropped",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "blocks does not paint whitespace-only text as a paragraph"
file = "src/application/long_text.rs"
before = """                    if !trimmed.is_empty() {
                        out.push_str(trimmed);
                        out.push_str("\\n\\n");
                    }
                }
                _ => {}"""
after = """                    if trimmed.is_empty() {
                        out.push_str(trimmed);
                        out.push_str("\\n\\n");
                    }
                }
                _ => {}"""
# Measured 2026-09-11 for ledger 270, and short for the same reason as the
# record above: three of the five it reddens live in a file it never named.
red = [
    "application::long_text::markup::tests::test_bare_text_with_no_tag_around_it_becomes_a_paragraph",
    "application::long_text::markup::tests::test_whitespace_only_text_contributes_nothing",
    "service::onenote_page::tests::test_a_code_block_comes_back_as_one_line_of_words",
    "service::onenote_page::tests::test_a_hidden_div_carrying_the_source_does_not_bring_it_back",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "list's numbered counter increments, not decrements, after each item"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11 for ledger 270. `list` now builds the marker before it
# advances the counter, so that a nested list can be indented by the marker's
# width, and the two lines the old break named are no longer adjacent.
before = '''                        *n += 1;'''
after = '''                        *n -= 1;'''
red = [
    "application::long_text::markup::tests::test_a_numbered_list_counts_its_items_instead_of_bulleting_them",
    "service::onenote_page::tests::test_an_ordered_list_comes_back",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
    "service::onenote_page::tests::test_microsofts_own_list_comes_back_as_a_list",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "list's numbered counter increases by exactly one, not scaled, after each item"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11 for ledger 270, moved for the same reason as the
# record above.
before = '''                        *n += 1;'''
after = '''                        *n *= 1;'''
red = [
    "application::long_text::markup::tests::test_a_numbered_list_counts_its_items_instead_of_bulleting_them",
    "service::onenote_page::tests::test_an_ordered_list_comes_back",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
    "service::onenote_page::tests::test_microsofts_own_list_comes_back_as_a_list",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "quote is a real walk of the blockquote's paragraphs, not a stub"
file = "src/application/long_text.rs"
before = '''    fn quote(node: NodeRef<'_, Node>, out: &mut String, keeping: Keeping) {
        let mut saw_a_paragraph = false;
        for child in node.children() {
            if let Node::Element(element) = child.value()
                && element.name() == "p"
            {
                saw_a_paragraph = true;
                push_paragraph("> ", child, out, keeping);
            }
        }
        if !saw_a_paragraph {
            push_paragraph("> ", node, out, keeping);
        }
    }'''
after = '''    fn quote(node: NodeRef<'_, Node>, out: &mut String, keeping: Keeping) {
        let _ = (node, out, keeping);
    }'''
red = [
    "application::long_text::markup::tests::test_a_blockquote_with_no_paragraph_inside_is_quoted_as_one_line",
    "application::long_text::markup::tests::test_a_blockquote_quotes_each_paragraph_on_its_own_line",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "quote only counts a p element as one of the blockquote's paragraphs"
file = "src/application/long_text.rs"
before = """            if let Node::Element(element) = child.value()
                && element.name() == "p"
            {
                saw_a_paragraph = true;"""
after = """            if let Node::Element(element) = child.value()
                && element.name() != "p"
            {
                saw_a_paragraph = true;"""
red = ["application::long_text::markup::tests::test_a_blockquote_quotes_each_paragraph_on_its_own_line"]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "quote only falls back to quoting the whole node when it saw no paragraph"
file = "src/application/long_text.rs"
before = '''        if !saw_a_paragraph {
            push_paragraph("> ", node, out, keeping);
        }'''
after = '''        if saw_a_paragraph {
            push_paragraph("> ", node, out, keeping);
        }'''
red = [
    "application::long_text::markup::tests::test_a_blockquote_with_no_paragraph_inside_is_quoted_as_one_line",
    "application::long_text::markup::tests::test_a_blockquote_quotes_each_paragraph_on_its_own_line",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

[[guard]]
name = "inline reads a bare line break as a space"
file = "src/application/long_text.rs"
# Re-measured by hand 2026-09-11 for ledger 270, and this record had already
# stopped working before that change touched it. Its old break quoted an `img`
# arm reading `if let Some(alt) = element.attr("alt")`, which this file has not
# held since the arm learned to say `image with no description`. `guards.sh`
# refuses a break it cannot find rather than reporting the guard, so from that
# rewrite until now this record said nothing and read as covered. The break is
# now the one line the record is about.
before = '''                    "br" => out.push(match keeping {
                        Keeping::OnlyWhatIsSpoken => ' ',
                        Keeping::EverythingTyped => '\n',
                    }),'''
after = '''                    "br" => {}'''
red = [
    "application::long_text::markup::tests::test_a_line_break_inside_inline_text_becomes_a_space",
    "application::long_text::tests::test_a_line_break_kept_for_editing_is_still_a_line_break",
    "application::long_text::tests::test_a_line_break_read_for_speaking_is_still_a_space",
    "service::onenote_page::tests::test_a_line_break_comes_back_as_a_line_break",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "inline contributes an image's alt text"
file = "src/application/long_text.rs"
# Re-measured by hand 2026-09-11 for ledger 270, and stale for the same reason
# and since the same rewrite as the record above: its break quoted an `img` arm
# this file no longer holds, so it matched nothing and guarded nothing.
before = '''                    "img" => match keeping {
                        Keeping::OnlyWhatIsSpoken => {
                            match element.attr("alt").map(str::trim).filter(|a| !a.is_empty()) {
                                Some(alt) => out.push_str(alt),
                                None => {
                                    out.push_str(&format!("image with {}", super::NO_DESCRIPTION))
                                }
                            }
                        }
                        Keeping::EverythingTyped => push_image(element, out, keeping),
                    },'''
after = '''                    "img" => {}'''
red = [
    "application::long_text::markup::tests::test_an_image_inside_inline_text_contributes_its_alt_text",
    "service::onenote_page::tests::test_a_picture_comes_back_as_a_picture",
    "service::onenote_page::tests::test_a_picture_with_no_description_comes_back_as_a_picture_with_no_description",
    "service::onenote_page::tests::test_every_row_of_the_fidelity_table_is_true",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "src/service/onenote_page.rs", tests = 48 },
]

[[guard]]
name = "inline silently drops a script or style reaching it directly"
file = "src/application/long_text.rs"
# Re-measured 2026-09-11. Written as the deletion of one arm rather than as two
# lines naming its neighbour, for the same reason as the records above.
#
# And re-anchored on 2026-09-22 by 12-02.1, after run 35520204784 could not
# apply it: the text appeared 0 times. `d470a105`, 11-11.0's newsletter work
# for #90, put the `tag if is_a_block(tag)` arm between this one and the
# catch-all, so the two lines are no longer adjacent. Written as two adjacent
# lines again anyway, because the arm's own text is four words that appear
# twice in this file, once here and once in the block walk. The anchor is
# therefore the arm plus the comment that follows it, which names one place.
before = '''                    "script" | "style" => {}
                    // A block met inside inline content, a paragraph inside
                    // a table cell, is read with a space either side, or its
                    // last word runs into the next block's first (ledger
                    // 555: "seven daysActions speak louder than wordsGary").
                    tag if is_a_block(tag) => {'''
after = '''                    // A block met inside inline content, a paragraph inside
                    // a table cell, is read with a space either side, or its
                    // last word runs into the next block's first (ledger
                    // 555: "seven daysActions speak louder than wordsGary").
                    tag if is_a_block(tag) => {'''
red = ["application::long_text::markup::tests::test_a_script_reaching_inline_text_directly_is_silently_dropped"]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
]

# ── contacts.rs's card-over-what-is-held merge, field by field ─────────────
#
# A mutation sweep of `data::message_cache::contacts` found 21 misses across
# five functions. Twenty were untested but already correct; the other,
# `label_a_type_value_names`'s `>` against `>=`, is genuinely equivalent (no
# standards TYPE word contains a caret, so the two operators can never
# disagree at the one length where they could) and is documented with a
# `debug_assert!` instead, so it gets no guard here.
#
# Sixteen of the twenty are `a_card_over_what_is_held`: every field folded in
# with `from_card.X.or_else(|| held.X.clone())` had no test proving the
# card's value ever replaced a stale held one, for that field specifically.
# `company`, `job_title`, `notes` and `emails_json` already had coverage and
# are not repeated here.
#
# Measured by hand on 2026-08-19: each guard applied alone, against
# `data::message_cache::contacts::tests`, reddened exactly the test named and
# nothing else in that module (100 tests). Restored and reran green after
# each; the whole module was green again at the end.

[[guard]]
name = "a card over what is held replaces a stale name, not just an empty one"
file = "src/data/message_cache/contacts.rs"
before = """            name: match from_card.name.is_empty() {
                true => held.name.clone(),
                false => from_card.name,
            },
            given_name: from_card.given_name.or_else(|| held.given_name.clone()),"""
after = """            given_name: from_card.given_name.or_else(|| held.given_name.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's given_name over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            given_name: from_card.given_name.or_else(|| held.given_name.clone()),
            family_name: from_card.family_name.or_else(|| held.family_name.clone()),"""
after = """            family_name: from_card.family_name.or_else(|| held.family_name.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's family_name over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            family_name: from_card.family_name.or_else(|| held.family_name.clone()),
            phone: from_card.phone.or_else(|| held.phone.clone()),"""
after = """            phone: from_card.phone.or_else(|| held.phone.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's phone over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            phone: from_card.phone.or_else(|| held.phone.clone()),
            company: from_card.company.or_else(|| held.company.clone()),"""
after = """            company: from_card.company.or_else(|| held.company.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's website over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            website: from_card.website.or_else(|| held.website.clone()),
            address: from_card.address.or_else(|| held.address.clone()),"""
after = """            address: from_card.address.or_else(|| held.address.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's address over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            address: from_card.address.or_else(|| held.address.clone()),
            birthday: from_card.birthday.or_else(|| held.birthday.clone()),"""
after = """            birthday: from_card.birthday.or_else(|| held.birthday.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's birthday over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            birthday: from_card.birthday.or_else(|| held.birthday.clone()),
            avatar_url: from_card.avatar_url.or_else(|| held.avatar_url.clone()),"""
after = """            avatar_url: from_card.avatar_url.or_else(|| held.avatar_url.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's avatar_url over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            avatar_url: from_card.avatar_url.or_else(|| held.avatar_url.clone()),
            avatar_data_base64: from_card"""
after = """            avatar_data_base64: from_card"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's inline photo over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            avatar_data_base64: from_card
                .avatar_data_base64
                .or_else(|| held.avatar_data_base64.clone()),
            vcard_raw: from_card.vcard_raw.or_else(|| held.vcard_raw.clone()),"""
after = """            vcard_raw: from_card.vcard_raw.or_else(|| held.vcard_raw.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_cards_inline_photo_also_replaces_a_stored_one"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's vcard_raw over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            vcard_raw: from_card.vcard_raw.or_else(|| held.vcard_raw.clone()),
            notes: from_card.notes.or_else(|| held.notes.clone()),
            nickname: from_card.nickname.or_else(|| held.nickname.clone()),"""
after = """            notes: from_card.notes.or_else(|| held.notes.clone()),
            nickname: from_card.nickname.or_else(|| held.nickname.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's nickname over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            nickname: from_card.nickname.or_else(|| held.nickname.clone()),
            department: from_card.department.or_else(|| held.department.clone()),"""
after = """            department: from_card.department.or_else(|| held.department.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's department over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            department: from_card.department.or_else(|| held.department.clone()),
            relationship: from_card.relationship.or_else(|| held.relationship.clone()),"""
after = """            relationship: from_card.relationship.or_else(|| held.relationship.clone()),"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's relationship over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            relationship: from_card.relationship.or_else(|| held.relationship.clone()),
            emails_json: every_address,"""
after = """            emails_json: every_address,"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's phones_json over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            phones_json: from_card.phones_json.or_else(|| held.phones_json.clone()),
            addresses_json: from_card"""
after = """            addresses_json: from_card"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's addresses_json over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            addresses_json: from_card
                .addresses_json
                .or_else(|| held.addresses_json.clone()),
            custom_fields_json: from_card"""
after = """            custom_fields_json: from_card"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card over what is held carries the card's custom_fields_json over a stale one"
file = "src/data/message_cache/contacts.rs"
before = """            custom_fields_json: from_card
                .custom_fields_json
                .or_else(|| held.custom_fields_json.clone()),
            ..held.clone()"""
after = """            ..held.clone()"""
red = ["data::message_cache::contacts::tests::test_a_card_that_speaks_on_every_field_it_can_carry_is_what_gets_stored"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a department survives export even with no company recorded"
file = "src/data/message_cache/contacts.rs"
before = """        if contact.company.is_some() || contact.department.is_some() {"""
after = """        if contact.company.is_some() && contact.department.is_some() {"""
red = ["data::message_cache::contacts::tests::test_a_department_survives_export_even_with_no_company_recorded"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a leading indented line with nothing above it stands on its own rather than vanishing"
file = "src/data/message_cache/contacts.rs"
before = """                Some(carried_on) if !lines.is_empty() => {"""
after = """                Some(carried_on) if true => {"""
red = ["data::message_cache::contacts::tests::test_a_line_indented_with_nothing_above_it_stands_on_its_own"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card the database refuses is counted upward, not downward"
file = "src/data/message_cache/contacts.rs"
before = """                    read.not_written_down += 1;"""
after = """                    read.not_written_down -= 1;"""
red = ["data::message_cache::contacts::tests::test_a_card_the_database_refuses_is_counted_and_does_not_stop_the_rest_of_the_file"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

[[guard]]
name = "a card the database refuses is counted by one, not left at zero"
file = "src/data/message_cache/contacts.rs"
before = """                    read.not_written_down += 1;"""
after = """                    read.not_written_down *= 1;"""
red = ["data::message_cache::contacts::tests::test_a_card_the_database_refuses_is_counted_and_does_not_stop_the_rest_of_the_file"]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
]

# ── vtimezone.rs's yearly-rule detection and repeating.rs's Outlook range ──
#
# A scoped sweep of `src/service/vtimezone.rs` found 15 missed mutants and 1
# TIMEOUT; `src/application/repeating.rs` found 2. Twelve of the fifteen
# vtimezone.rs misses are guarded below. The rest: `the_change_between`'s `>`
# against `>=` is genuinely equivalent (its only caller only ever supplies a
# gap that is a whole number of days, so the two operators can never diverge
# in what they return) and is documented with a `debug_assert!`; the two
# December-branch mutants on `year + 1` are equivalent for a different reason
# (December always holds 31 days, so the function's return value cannot tell
# `year + 1` apart from `year - 1` or `year` itself); and the TIMEOUT in
# `changes_within` (`<` against `<=`, a genuine infinite loop once `at`
# reaches `end`) was closed with a `debug_assert!` on the loop's forward
# progress, not a test. None of those four get a guard here.
#
# The three `one_yearly_rule` onset guards share one `before`: the whole
# four-line condition, with three `||`. Applying cargo-mutants' actual
# AST-level swap for the middle and rightmost operators needs explicit
# parentheses, since `&&` binds tighter than `||` in Rust and a naive
# in-place substitution would test a different expression than the one the
# sweep actually reported.
#
# Measured by hand on 2026-08-19: each guard applied alone, against the
# whole library, reddened exactly the test(s) named and nothing else.
# Restored and reran green after each.

[[guard]]
name = "one_yearly_rule rejects a year whose onset breaks the pattern on the year itself"
file = "src/service/vtimezone.rs"
before = """        if onset.year() != *year
            || onset.month() != first.month()
            || onset.time() != first.time()
            || onset.weekday() != first.weekday()"""
after = """        if onset.year() != *year
            && onset.month() != first.month()
            || onset.time() != first.time()
            || onset.weekday() != first.weekday()"""
red = ["service::vtimezone::tests::test_one_yearly_rule_rejects_a_year_whose_onset_breaks_the_pattern"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "one_yearly_rule rejects a year whose onset breaks the pattern on the month"
file = "src/service/vtimezone.rs"
before = """        if onset.year() != *year
            || onset.month() != first.month()
            || onset.time() != first.time()
            || onset.weekday() != first.weekday()"""
after = """        if (onset.year() != *year || onset.month() != first.month())
            && onset.time() != first.time()
            || onset.weekday() != first.weekday()"""
red = ["service::vtimezone::tests::test_one_yearly_rule_rejects_a_year_whose_onset_breaks_the_pattern"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "one_yearly_rule rejects a year whose onset breaks the pattern on the time or weekday"
file = "src/service/vtimezone.rs"
before = """        if onset.year() != *year
            || onset.month() != first.month()
            || onset.time() != first.time()
            || onset.weekday() != first.weekday()"""
after = """        if (onset.year() != *year || onset.month() != first.month() || onset.time() != first.time())
            && onset.weekday() != first.weekday()"""
red = ["service::vtimezone::tests::test_one_yearly_rule_rejects_a_year_whose_onset_breaks_the_pattern"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "one_yearly_rule rejects a year whose offsets do not match the rest"
file = "src/service/vtimezone.rs"
before = """        change.from_seconds != group[0].from_seconds || change.to_seconds != group[0].to_seconds"""
after = """        change.from_seconds != group[0].from_seconds && change.to_seconds != group[0].to_seconds"""
red = ["service::vtimezone::tests::test_one_yearly_rule_rejects_a_year_whose_offsets_do_not_match_the_rest"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "one_yearly_rule's is_last is a day-count comparison, not one that never trips early"
file = "src/service/vtimezone.rs"
before = """        |onset: &NaiveDateTime| onset.day() + 7 > days_in_month(onset.year(), onset.month());"""
after = """        |onset: &NaiveDateTime| onset.day() + 7 >= days_in_month(onset.year(), onset.month());"""
red = ["service::vtimezone::tests::test_one_yearly_rule_does_not_call_a_year_last_when_the_same_weekday_recurs_in_the_month"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "one_yearly_rule's is_last adds seven days, rather than scaling the day of month"
file = "src/service/vtimezone.rs"
before = """        |onset: &NaiveDateTime| onset.day() + 7 > days_in_month(onset.year(), onset.month());"""
after = """        |onset: &NaiveDateTime| onset.day() * 7 > days_in_month(onset.year(), onset.month());"""
red = ["service::vtimezone::tests::test_one_yearly_rule_does_not_call_a_year_last_when_the_same_weekday_recurs_in_the_month"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "listed_blocks keeps each pair of offsets in its own block"
file = "src/service/vtimezone.rs"
before = """            .filter(|change| change.from_seconds == from && change.to_seconds == to)"""
after = """            .filter(|change| change.from_seconds == from || change.to_seconds == to)"""
red = ["service::vtimezone::tests::test_listed_blocks_keeps_each_pair_of_offsets_in_its_own_block"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "days_in_month is a real computation, not a stub returning nought"
file = "src/service/vtimezone.rs"
before = """fn days_in_month(year: i32, month: u32) -> u32 {
    let next_month = if month == 12 {
        // `year + 1` names which January this is, but December holds
        // thirty-one days in every year the Gregorian calendar has, so the
        // day this function actually returns cannot tell that year apart
        // from `year - 1` or `year` itself. Written as `+ 1` because that is
        // what next January actually is, not because a test could tell it
        // from another year's January here.
        NaiveDate::from_ymd_opt(year + 1, 1, 1)
    } else {
        NaiveDate::from_ymd_opt(year, month + 1, 1)
    };
    next_month
        .and_then(|first| first.pred_opt())
        .map_or(31, |last| last.day())
}"""
after = """fn days_in_month(year: i32, month: u32) -> u32 {
    0
}"""
red = [
    "service::vtimezone::tests::test_how_many_days_a_month_holds",
    "service::vtimezone::tests::test_one_yearly_rule_does_not_call_a_year_last_when_the_same_weekday_recurs_in_the_month",
]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "days_in_month is a real computation, not a stub returning one"
file = "src/service/vtimezone.rs"
before = """fn days_in_month(year: i32, month: u32) -> u32 {
    let next_month = if month == 12 {
        // `year + 1` names which January this is, but December holds
        // thirty-one days in every year the Gregorian calendar has, so the
        // day this function actually returns cannot tell that year apart
        // from `year - 1` or `year` itself. Written as `+ 1` because that is
        // what next January actually is, not because a test could tell it
        // from another year's January here.
        NaiveDate::from_ymd_opt(year + 1, 1, 1)
    } else {
        NaiveDate::from_ymd_opt(year, month + 1, 1)
    };
    next_month
        .and_then(|first| first.pred_opt())
        .map_or(31, |last| last.day())
}"""
after = """fn days_in_month(year: i32, month: u32) -> u32 {
    1
}"""
red = [
    "service::vtimezone::tests::test_how_many_days_a_month_holds",
    "service::vtimezone::tests::test_one_yearly_rule_does_not_call_a_year_last_when_the_same_weekday_recurs_in_the_month",
]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "days_in_month's December check is month == 12, not != 12"
file = "src/service/vtimezone.rs"
before = """    let next_month = if month == 12 {"""
after = """    let next_month = if month != 12 {"""
red = ["service::vtimezone::tests::test_how_many_days_a_month_holds"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "days_in_month's non-December branch looks at next month, not the month before"
file = "src/service/vtimezone.rs"
before = """        NaiveDate::from_ymd_opt(year, month + 1, 1)"""
after = """        NaiveDate::from_ymd_opt(year, month - 1, 1)"""
red = ["service::vtimezone::tests::test_how_many_days_a_month_holds"]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "days_in_month's non-December branch adds a month, rather than multiplying it"
file = "src/service/vtimezone.rs"
before = """        NaiveDate::from_ymd_opt(year, month + 1, 1)"""
after = """        NaiveDate::from_ymd_opt(year, month * 1, 1)"""
red = [
    "service::vtimezone::tests::test_how_many_days_a_month_holds",
    "service::vtimezone::tests::test_one_yearly_rule_does_not_call_a_year_last_when_the_same_weekday_recurs_in_the_month",
]
tests_last_seen = [
    { file = "src/service/vtimezone.rs", tests = 10 },
]

[[guard]]
name = "as_outlook_says_it carries the start date on an end-date range, not just the never-ending one"
file = "src/application/repeating.rs"
before = """        Until::OnDate(last_day) => MsRecurrenceRange {
            range_type: "endDate".to_string(),
            start_date: starts_on.to_string(),
            end_date: Some(last_day.clone()),
            ..Default::default()
        },"""
after = """        Until::OnDate(last_day) => MsRecurrenceRange {
            range_type: "endDate".to_string(),
            end_date: Some(last_day.clone()),
            ..Default::default()
        },"""
red = ["application::repeating::tests::test_every_ending_tells_outlook_the_same_start_date_for_the_range"]
tests_last_seen = [
    { file = "src/application/repeating.rs", tests = 27 },
]

[[guard]]
name = "as_outlook_says_it carries the start date on a numbered range, not just the never-ending one"
file = "src/application/repeating.rs"
before = """        Until::AfterTimes(times) => MsRecurrenceRange {
            range_type: "numbered".to_string(),
            start_date: starts_on.to_string(),
            number_of_occurrences: Some(*times),
            ..Default::default()
        },"""
after = """        Until::AfterTimes(times) => MsRecurrenceRange {
            range_type: "numbered".to_string(),
            number_of_occurrences: Some(*times),
            ..Default::default()
        },"""
red = ["application::repeating::tests::test_every_ending_tells_outlook_the_same_start_date_for_the_range"]
tests_last_seen = [
    { file = "src/application/repeating.rs", tests = 27 },
]

# ── caldav.rs and caldav_sync.rs, from the same scoped sweep ────────────────
#
# caldav.rs: 16 misses in scope, including 4 of the round's 5 TIMEOUTs. The
# four TIMEOUTs (events_in's two, fits_in's two) are hand-confirmed genuine
# infinite loops that existing tests already reach; nothing needed fixing and
# nothing here can guard a mutation that never lets the runner finish. Three
# more misses are genuinely equivalent, each documented with a
# `debug_assert!`/`debug_assert_ne!` rather than a test: events_in's
# `closed + 1` against `closed * 1` (an event's own close line can never also
# be read as opening one, proven structurally); parameter_named_on's
# `semicolon > colon` against `>=` (a byte position can hold only one
# character, so the two can never name the same position); and
# parameter_named_on's slice starting at the semicolon itself rather than
# past it (an empty leading fragment never contains `=`, so it is always
# skipped the same way regardless). The other 9 are guarded below.
#
# caldav_sync.rs: 4 misses. One, `one_caldav_day_kept_out_of_its_series`
# dropping the explicit `pending` field from a struct literal built with
# `..series.clone()`, is genuinely equivalent for the same reason as
# calendar.rs's own version of this pattern, and is already a
# `debug_assert_eq!` in the source. The other 3 are guarded below.
#
# Measured by hand on 2026-08-19: each guard applied alone, against the whole
# library, reddened exactly the test named and nothing else. Restored and
# reran green after each.

[[guard]]
name = "zone_left_undefined counts entering a timezone block, not scaling the count"
file = "src/service/caldav.rs"
before = """    for line in &lines {
        if names_the_timezone(component_opened(line)) {
            inside += 1;
            continue;
        }"""
after = """    for line in &lines {
        if names_the_timezone(component_opened(line)) {
            inside *= 1;
            continue;
        }"""
red = ["service::caldav::writing_tests::test_a_stray_zone_parameter_inside_a_timezone_blocks_own_definition_is_not_asked_about"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "zone_left_undefined only skips a parameter while still inside a timezone block"
file = "src/service/caldav.rs"
before = """        if inside > 0 {
            continue;
        }"""
after = """        if inside < 0 {
            continue;
        }"""
red = ["service::caldav::writing_tests::test_a_stray_zone_parameter_inside_a_timezone_blocks_own_definition_is_not_asked_about"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a marker is refused unless what follows it is a clean single word"
file = "src/service/caldav.rs"
before = """    let is_a_component_name = !named.is_empty() && !named.contains([':', ';', ',', ' ', '\\t']);"""
after = """    let is_a_component_name = !named.is_empty() || !named.contains([':', ';', ',', ' ', '\\t']);"""
red = ["service::caldav::tests::test_a_marker_is_refused_unless_what_follows_it_is_a_clean_single_word"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "ical_duration's one_unit refuses a run of digits that is empty or carries a non-digit"
file = "src/service/caldav.rs"
before = """        (!digits.is_empty() && digits.bytes().all(|b| b.is_ascii_digit()))"""
after = """        (!digits.is_empty() || digits.bytes().all(|b| b.is_ascii_digit()))"""
red = ["service::caldav::tests::test_ical_duration_reads_the_grammar_and_only_the_grammar"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "ical_duration's seconds unit adds onto the running total, not subtracts"
file = "src/service/caldav.rs"
before = """            if let Some((secs, after)) = one_unit(rest, 'S') {
                seconds += secs;
                rest = after;
                named_something = true;
            }"""
after = """            if let Some((secs, after)) = one_unit(rest, 'S') {
                seconds -= secs;
                rest = after;
                named_something = true;
            }"""
red = ["service::caldav::tests::test_ical_duration_reads_the_grammar_and_only_the_grammar"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "ical_duration's seconds unit adds onto the running total, not multiplies it"
file = "src/service/caldav.rs"
before = """            if let Some((secs, after)) = one_unit(rest, 'S') {
                seconds += secs;
                rest = after;
                named_something = true;
            }"""
after = """            if let Some((secs, after)) = one_unit(rest, 'S') {
                seconds *= secs;
                rest = after;
                named_something = true;
            }"""
red = ["service::caldav::tests::test_ical_duration_reads_the_grammar_and_only_the_grammar"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a property name ending in more than one octet does not move the delimiter mid character"
file = "src/service/caldav.rs"
before = """    parameter_among(&line[semicolon + 1..colon], parameter).map(str::to_string)"""
after = """    parameter_among(&line[semicolon - 1..colon], parameter).map(str::to_string)"""
red = ["service::caldav::tests::test_a_property_name_ending_in_more_than_one_octet_does_not_move_the_delimiter_mid_character"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a cancelled day is a whole day only at exactly eight digits"
file = "src/service/caldav.rs"
before = """    if bytes.len() == 8 && bytes.iter().all(u8::is_ascii_digit) {"""
after = """    if bytes.len() == 8 || bytes.iter().all(u8::is_ascii_digit) {"""
red = ["service::caldav::tests::test_a_cancelled_day_is_a_whole_day_only_at_exactly_eight_digits"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "the occurrence change has to come back out of its own VEVENT, not be assumed"
file = "src/service/caldav.rs"
before = """fn the_occurrence_came_back_out(
    document: &str,
    uid: &str,
    recurrence_id: &str,
    meant: &[String],
) -> bool {
    let lines = unfolded(document);
    let events = events_in(&lines);
    let Some(its) = the_occurrence_named(&events, &lines, uid, recurrence_id) else {
        return false;
    };
    if its.closed_on.is_none() {
        return false;
    }
    let came_back_out: Vec<&str> = lines_at(&lines, &its.its_own)
        .into_iter()
        .filter(|line| a_change_replaces(line))
        .collect();
    came_back_out == meant.iter().map(String::as_str).collect::<Vec<&str>>()
}"""
after = """fn the_occurrence_came_back_out(
    document: &str,
    uid: &str,
    recurrence_id: &str,
    meant: &[String],
) -> bool {
    let _ = (document, uid, recurrence_id, meant);
    true
}"""
red = ["service::caldav::writing_tests::test_the_occurrence_change_has_to_come_back_out_of_its_own_vevent"]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "deletions_waiting counts only this calendar's own notes still owed"
file = "src/application/caldav_sync.rs"
before = """                note.so_far.still_owed()
                    && note.calendar_id.as_deref() == Some(calendar.id.as_str())"""
after = """                note.so_far.still_owed()
                    || note.calendar_id.as_deref() == Some(calendar.id.as_str())"""
red = ["application::caldav_sync::tests::test_deletions_waiting_counts_only_this_calendars_own_notes_still_owed"]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "a cancelled caldav day only counts as deleted when something about it really changed, not always"
file = "src/application/caldav_sync.rs"
before = """            the_day_went |= went == crate::application::calendar::ADayWent::OffTheSeries;"""
after = """            the_day_went &= went == crate::application::calendar::ADayWent::OffTheSeries;"""
red = ["application::caldav_sync::tests::test_a_cancelled_day_only_counts_as_deleted_when_something_about_it_really_changed"]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

[[guard]]
name = "a cancelled caldav day is counted deleted when it really left the series, not when it did not"
file = "src/application/caldav_sync.rs"
before = """            the_day_went |= went == crate::application::calendar::ADayWent::OffTheSeries;"""
after = """            the_day_went |= went != crate::application::calendar::ADayWent::OffTheSeries;"""
red = ["application::caldav_sync::tests::test_a_cancelled_day_only_counts_as_deleted_when_something_about_it_really_changed"]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

# ── imap.rs and oauth.rs, from the same scoped sweep ────────────────────────
#
# imap.rs: 15 misses in scope, every one untested but already correct.
# `flags_from_attributes` alone accounts for 7 of them, cargo-mutants' full
# set of canned replacements for a function returning
# `Option<(u32, Vec<String>)>`: `None`, and `Some` paired with each of two
# canned uids and three canned flag lists. `remove_these`'s guard condition
# was pulled into `removal_fell_back_to_flag_and_leave` specifically so it
# could be pinned without opening a socket: the warning it used to gate has
# no effect a test can observe, so nothing ever did.
#
# oauth.rs: of its 4 misses, 2 are guarded below and 2
# (`wait_for_redirect_code`, the one place in this codebase that binds every
# network interface rather than loopback) are structurally hard to test and
# stay open; see the round's own report for why forcing a test there was
# judged worse than the gap.
#
# Measured by hand on 2026-08-19: each guard applied alone, against the whole
# library, reddened exactly the test(s) named and nothing else. Restored and
# reran green after each.

[[guard]]
name = "fetch_flags reads the FLAGS a server sends back on its CONDSTORE path"
file = "src/service/protocols/imap.rs"
before = """                    format!("UID FETCH 1:* (UID FLAGS) (CHANGEDSINCE {modseq})"),
                    "reading what changed",
                    |response| match response {
                        Response::Fetch(_, attributes) => flags_from_attributes(attributes),
                        _ => None,
                    },"""
after = """                    format!("UID FETCH 1:* (UID FLAGS) (CHANGEDSINCE {modseq})"),
                    "reading what changed",
                    |response| match response {
                        _ => None,
                    },"""
red = ["service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "fetch_flags reads the FLAGS a server sends back on its batched path"
file = "src/service/protocols/imap.rs"
before = """                    format!("UID FETCH {set} (UID FLAGS)"),
                    "reading the message flags",
                    |response| match response {
                        Response::Fetch(_, attributes) => flags_from_attributes(attributes),
                        _ => None,
                    },"""
after = """                    format!("UID FETCH {set} (UID FLAGS)"),
                    "reading the message flags",
                    |response| match response {
                        _ => None,
                    },"""
red = ["service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "removal only reports falling back to flag-and-leave when something was actually asked for"
file = "src/service/protocols/imap.rs"
before = """fn removal_fell_back_to_flag_and_leave(uids: &[u32], abilities: &Abilities) -> bool {
    !uids.is_empty() && !abilities.uid_expunge
}"""
after = """fn removal_fell_back_to_flag_and_leave(uids: &[u32], abilities: &Abilities) -> bool {
    uids.is_empty() && !abilities.uid_expunge
}"""
red = ["service::protocols::imap::tests::test_removal_only_reports_falling_back_to_flag_and_leave_when_it_actually_did"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "removal only reports falling back to flag-and-leave when both conditions hold, not either"
file = "src/service/protocols/imap.rs"
before = """fn removal_fell_back_to_flag_and_leave(uids: &[u32], abilities: &Abilities) -> bool {
    !uids.is_empty() && !abilities.uid_expunge
}"""
after = """fn removal_fell_back_to_flag_and_leave(uids: &[u32], abilities: &Abilities) -> bool {
    !uids.is_empty() || !abilities.uid_expunge
}"""
red = ["service::protocols::imap::tests::test_removal_only_reports_falling_back_to_flag_and_leave_when_it_actually_did"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "removal only reports falling back to flag-and-leave when the server really lacks UIDPLUS"
file = "src/service/protocols/imap.rs"
before = """fn removal_fell_back_to_flag_and_leave(uids: &[u32], abilities: &Abilities) -> bool {
    !uids.is_empty() && !abilities.uid_expunge
}"""
after = """fn removal_fell_back_to_flag_and_leave(uids: &[u32], abilities: &Abilities) -> bool {
    !uids.is_empty() && abilities.uid_expunge
}"""
red = ["service::protocols::imap::tests::test_removal_only_reports_falling_back_to_flag_and_leave_when_it_actually_did"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a FETCH reply's Gmail message id reaches the message the server sent"
file = "src/service/protocols/imap.rs"
before = """            AttributeValue::GmailMsgId(id) => Some(*id),
            _ => None,
        }),"""
after = """            _ => None,
        }),"""
red = ["service::protocols::imap::tests::test_gmails_own_fields_reach_the_message_when_the_server_sent_them"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a FETCH reply's Gmail labels reach the message the server sent"
file = "src/service/protocols/imap.rs"
before = """                AttributeValue::GmailLabels(labels) => Some(
                    labels
                        .iter()
                        .map(std::string::ToString::to_string)
                        .collect(),
                ),
                _ => None,"""
after = """                _ => None,"""
red = ["service::protocols::imap::tests::test_gmails_own_fields_reach_the_message_when_the_server_sent_them"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── The seven ways of faking a flags reply ─────────────────────────────────
#
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own. All seven records below break one function
# seven ways, and every one of them named the same single unit test. Each now
# reddens a second test as well, over a server that really answers, and that
# test reaches both of the paths a FETCH of flags can take. So the seven have
# stopped resting on one reader: the extra one arrived after these records were
# written and nothing asked them again.

[[guard]]
name = "flags_from_attributes is a real computation, not a stub answering nothing"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    None
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "flags_from_attributes pairs the real uid with the real flags, not a canned uid 0 with no flags"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((0, vec![]))
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "flags_from_attributes pairs the real uid with the real flags, not a canned uid 0 with one blank flag"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((0, vec!["".into()]))
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "flags_from_attributes pairs the real uid with the real flags, not a canned uid 0 with a made-up flag"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((0, vec!["xyzzy".into()]))
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "flags_from_attributes pairs the real uid with the real flags, not a canned uid 1 with no flags"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((1, vec![]))
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "flags_from_attributes pairs the real uid with the real flags, not a canned uid 1 with one blank flag"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((1, vec!["".into()]))
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "flags_from_attributes pairs the real uid with the real flags, not a canned uid 1 with a made-up flag"
file = "src/service/protocols/imap.rs"
before = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((uid_of(attributes)?, flag_names(attributes)))
}"""
after = """fn flags_from_attributes(attributes: &[AttributeValue<'_>]) -> Option<(u32, Vec<String>)> {
    Some((1, vec!["xyzzy".into()]))
}"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_flags_a_server_reports_reach_the_caller_on_both_fetch_flags_paths",
    "service::protocols::imap::tests::test_flags_from_attributes_pairs_the_uid_with_the_flags_the_server_sent",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a bad status says the server did not understand, rather than a generic refusal"
file = "src/service/protocols/imap.rs"
before = """        Status::Bad => Error::Protocol(format!(
            "The mail server did not understand the request while {doing}, and refused it.{words}"
        )),
        _ => Error::Protocol(format!("The mail server refused while {doing}.{words}")),"""
after = """        _ => Error::Protocol(format!("The mail server refused while {doing}.{words}")),"""
red = ["service::protocols::imap::tests::test_a_bad_status_says_the_server_did_not_understand_rather_than_a_generic_refusal"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "as_one_short_line leaves exactly the bound alone and cuts one more"
file = "src/service/oauth.rs"
before = """    if tidy.chars().count() > MOST_CHARACTERS_FROM_A_REPLY {"""
after = """    if tidy.chars().count() >= MOST_CHARACTERS_FROM_A_REPLY {"""
red = ["service::oauth::tests::test_as_one_short_line_leaves_exactly_the_bound_alone_and_cuts_one_more"]
tests_last_seen = [
    { file = "src/service/oauth.rs", tests = 51 },
]

[[guard]]
name = "post_token_request reads a refusal status as a refusal, not as success"
file = "src/service/oauth.rs"
before = """    if !status.is_success() {"""
after = """    if status.is_success() {"""
red = [
    "service::oauth::tests::test_a_refusal_status_is_read_as_a_refusal_even_when_its_body_would_parse_as_a_token",
    "service::oauth::tests::test_a_success_status_is_read_as_the_token_it_carries",
]
tests_last_seen = [
    { file = "src/service/oauth.rs", tests = 51 },
]

# ── calendar.rs, contacts_sync.rs, tasks_sync.rs, message_cache/tasks.rs, ──
# ── message_cache/mod.rs and tasks_api.rs, from the same scoped sweep ───────
#
# 17 raw mutant lines across these 7 files (calendar.rs, contacts_sync.rs,
# tasks_sync.rs, reply.rs, message_cache/tasks.rs, message_cache/mod.rs,
# tasks_api.rs). Two are genuinely equivalent and documented with a
# `debug_assert!` instead of a test: calendar.rs's
# `one_day_of_a_microsoft_series` dropping an explicit `pending` field from a
# struct literal built with `..series.clone()` (the same shape as
# caldav_sync.rs's version of this), and reply.rs's `name_or_address` guard,
# which became unreachable when that function moved onto the shared
# `mime::parse_addresses` (which already turns an empty or whitespace name
# into `None` before `name_or_address` ever sees it). Neither gets a guard
# here; reply.rs contributes none at all.
#
# Measured by hand on 2026-08-19: each guard applied alone, against the whole
# library, reddened exactly the test(s) named and nothing else. Restored and
# reran green after each.

[[guard]]
name = "changes_nothing_can_send counts two calendars sharing a reason separately, not together"
file = "src/application/calendar.rs"
before = """            .find(|(already, was, _)| *already == name && *was == why)"""
after = """            .find(|(already, was, _)| *already == name || *was == why)"""
red = ["application::calendar::tests::test_two_calendars_that_share_a_reason_are_still_said_separately"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "EditMeans::spoken is the label with its keyboard letter taken off, not a stub returning nothing"
file = "src/application/calendar.rs"
before = """    pub fn spoken(self) -> String {
        self.label().replace('&', "")
    }"""
after = """    pub fn spoken(self) -> String {
        String::new()
    }"""
red = ["application::calendar::tests::test_what_each_answer_is_called_when_it_is_read_out"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "EditMeans::spoken is the label with its keyboard letter taken off, not a canned word"
file = "src/application/calendar.rs"
before = """    pub fn spoken(self) -> String {
        self.label().replace('&', "")
    }"""
after = """    pub fn spoken(self) -> String {
        "xyzzy".into()
    }"""
red = ["application::calendar::tests::test_what_each_answer_is_called_when_it_is_read_out"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "WhereAChangeGoes::named says which calendar it is, not a canned word for all five"
file = "src/application/calendar.rs"
before = """    const fn named(self) -> &'static str {
        match self {
            Self::ACalendarServer => "your calendar server",
            Self::Google => "your Google calendar",
            Self::Outlook => "your Outlook calendar",
            Self::OnlyReadable => "a calendar this program can only read",
            Self::KeptHere => "this computer",
        }
    }"""
after = """    const fn named(self) -> &'static str {
        "xyzzy"
    }"""
red = ["application::calendar::tests::test_what_each_calendar_kind_is_called_in_the_middle_of_a_sentence"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "further_off_for's extra sentence is a real warning, not a stub returning nothing"
file = "src/application/calendar.rs"
before = """const fn further_off_for(goes: WhereAChangeGoes) -> &'static str {
    match goes {
        WhereAChangeGoes::OnlyReadable => {
            " A calendar this program can only read takes no change at all, so \\
             what you type is kept on this computer and the next refresh writes \\
             over it."
        }
        _ => "",
    }
}"""
after = """const fn further_off_for(goes: WhereAChangeGoes) -> &'static str {
    ""
}"""
red = ["application::calendar::tests::test_the_extra_sentence_is_said_only_for_a_calendar_this_program_can_only_read"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "further_off_for's extra sentence is a real warning, not a canned word for every calendar"
file = "src/application/calendar.rs"
before = """const fn further_off_for(goes: WhereAChangeGoes) -> &'static str {
    match goes {
        WhereAChangeGoes::OnlyReadable => {
            " A calendar this program can only read takes no change at all, so \\
             what you type is kept on this computer and the next refresh writes \\
             over it."
        }
        _ => "",
    }
}"""
after = """const fn further_off_for(goes: WhereAChangeGoes) -> &'static str {
    "xyzzy"
}"""
red = ["application::calendar::tests::test_the_extra_sentence_is_said_only_for_a_calendar_this_program_can_only_read"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "further_off_for's extra sentence is said only for a calendar this program can only read, not for none"
file = "src/application/calendar.rs"
before = """        WhereAChangeGoes::OnlyReadable => {
            " A calendar this program can only read takes no change at all, so \\
             what you type is kept on this computer and the next refresh writes \\
             over it."
        }
        _ => "","""
after = """        WhereAChangeGoes::OnlyReadable => "",
        _ => "","""
red = ["application::calendar::tests::test_the_extra_sentence_is_said_only_for_a_calendar_this_program_can_only_read"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "what_one_day_will_do never promises a sync for a calendar kept only on this computer"
file = "src/application/calendar.rs"
before = """        (_, _) if goes == WhereAChangeGoes::KeptHere => {"""
after = """        (_, _) if false => {"""
red = ["application::calendar::tests::test_a_one_day_answer_for_a_calendar_kept_here_never_promises_a_sync"]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
]

[[guard]]
name = "the Google address book a contacts sync uses really reads one contact back"
file = "src/application/contacts_sync.rs"
before = """        GoogleApiClient::get_contact(self, token, provider_contact_id).await"""
after = """        Ok(Default::default())"""
red = ["application::contacts_sync::tests::test_the_google_address_book_a_sync_uses_really_reads_one_contact_back"]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "the Outlook address book a contacts sync uses really reads one contact back"
file = "src/application/contacts_sync.rs"
before = """        MsGraphClient::get_contact(self, token, provider_contact_id).await"""
after = """        Ok(Default::default())"""
red = ["application::contacts_sync::tests::test_the_outlook_address_book_a_sync_uses_really_reads_one_contact_back"]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a contacts sync really lets go of a deletion it has remembered long enough, not a stub that does nothing"
file = "src/application/contacts_sync.rs"
before = '''pub(crate) fn forget_the_deletions_remembered_long_enough(
    cache: &MessageCache,
    result: &mut SyncResult,
) {
    if let Err(unwritten) = crate::application::deletions::let_go_of_what_was_remembered_long_enough(
        cache,
        chrono::Utc::now(),
    ) {
        result
            .errors
            .push(format!("Old deletions could not be let go of: {unwritten}"));
    }
}'''
after = '''pub(crate) fn forget_the_deletions_remembered_long_enough(
    cache: &MessageCache,
    result: &mut SyncResult,
) {
    let _ = (cache, result);
}'''
red = ["application::contacts_sync::tests::test_a_contacts_sync_lets_go_of_a_deletion_it_has_remembered_long_enough"]
tests_last_seen = [
    { file = "src/application/contacts_sync.rs", tests = 281 },
]

[[guard]]
name = "a tasks sync really lets go of a deletion it has remembered long enough, not a stub that does nothing"
file = "src/application/tasks_sync.rs"
before = """fn forget_the_deletions_remembered_long_enough(cache: &MessageCache, result: &mut TaskSyncResult) {
    if let Err(e) = crate::application::deletions::let_go_of_what_was_remembered_long_enough(
        cache,
        chrono::Utc::now(),
    ) {
        result
            .errors
            .push(format!("Old deletions could not be let go of: {e}"));
    }
}"""
after = """fn forget_the_deletions_remembered_long_enough(cache: &MessageCache, result: &mut TaskSyncResult) {
    let _ = (cache, result);
}"""
red = ["application::tasks_sync::tests::test_a_tasks_sync_lets_go_of_a_deletion_it_has_remembered_long_enough"]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

[[guard]]
name = "forgetting a deletion that removed no row reports that nothing was there, not that something was"
file = "src/data/message_cache/tasks.rs"
before = """        Ok(removed > 0)"""
after = """        Ok(removed >= 0)"""
red = ["data::message_cache::tasks::tests::test_forgetting_a_deletion_that_has_no_note_reports_that_nothing_was_there"]
tests_last_seen = [
    { file = "src/data/message_cache/tasks.rs", tests = 22 },
]

[[guard]]
name = "a recorded name carried over has to match the address and have one, not either alone"
file = "src/data/message_cache/mod.rs"
before = """                        !held.name.trim().is_empty()
                            && held.address.trim().eq_ignore_ascii_case(looking_for)"""
after = """                        !held.name.trim().is_empty()
                            || held.address.trim().eq_ignore_ascii_case(looking_for)"""
red = ["data::message_cache::tests::test_the_recorded_name_carried_over_has_to_match_the_address_and_have_one"]
tests_last_seen = [
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
name = "TaskProgress::stored reads notStarted back to NotStarted, the word Graph actually sends"
file = "src/service/tasks_api.rs"
before = """            "notStarted" => Some(Self::NotStarted),
            "inProgress" => Some(Self::InProgress),"""
after = """            "inProgress" => Some(Self::InProgress),"""
red = ["service::tasks_api::tests::test_every_progress_word_graph_sends_reads_back_to_the_state_it_named"]
tests_last_seen = [
    { file = "src/service/tasks_api.rs", tests = 62 },
]

# ── Accessibility subsystem, from the 2026-08-22 mutation-sweep round ───────
#
# 31 misses across accessibility.rs, automation.rs, feedback.rs, names.rs and
# screen_reader.rs. Fifteen guarded below.
#
# Two are genuinely equivalent: names.rs's FOCUSABLE|SELECTABLE and
# FOCUSED|SELECTED, an OR read as an XOR. Every MSAA STATE_SYSTEM_* constant
# wxdragon defines is a single, distinct bit, so OR and XOR compute the same
# value on every input, not just the ones a test happened to try. Documented
# in place with debug_assert_eq!, the same pattern already used for the two
# disjoint dialog flags in asking.rs. No guard for these.
#
# Fourteen more need a live Windows accessibility control, a real screen
# reader attached to the machine, or the current foreground window to observe
# anything: Accessible::new's unconditional FFI call (names.rs's refresh,
# set_accessible_name, set_accessible_name_and_description, and the whole-
# function shape of set_accessible_checked_rows), the native:: UI Automation
# and MSAA calls in screen_reader.rs (announce_via_live_region,
# raise_notification, notify_name_change, and the whole-function shape of
# clients_are_listening), and feedback.rs's emit, a direct kernel32::Beep FFI
# call with no decision left to extract. Each is documented in place rather
# than guarded here; this project's test fakes construct a null window
# handle, not a live control, and wx_app.rs's own comment already explains
# why one process can run wxdragon::main at most once, which the parallel
# test binary rules out.
#
# Measured by hand: each guard applied alone, against the whole library,
# reddened exactly the test(s) named and nothing else. Restored and reran
# green after each. AutomationStore::update_state's guard reddens two tests,
# not one: Accessibility::update_node_state calls straight through to it, so
# breaking the lower layer breaks the integration test above it too.

[[guard]]
name = "every node registered at startup carries enabled, not just two of the three"
file = "src/presentation/accessibility.rs"
before = """            description: Some("Primary application window".to_string()),
            state: automation::AutomationState {
                enabled: true,"""
after = """            description: Some("Primary application window".to_string()),
            state: automation::AutomationState {"""
red = ["presentation::accessibility::tests::test_every_node_registered_at_startup_is_enabled"]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

[[guard]]
name = "the folder tree is registered enabled, not left to the state default"
file = "src/presentation/accessibility.rs"
before = """            description: Some("Folder navigation".to_string()),
            state: automation::AutomationState {
                enabled: true,"""
after = """            description: Some("Folder navigation".to_string()),
            state: automation::AutomationState {"""
red = ["presentation::accessibility::tests::test_every_node_registered_at_startup_is_enabled"]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

[[guard]]
name = "every module panel is registered enabled, not left to the state default"
file = "src/presentation/accessibility.rs"
before = """                state: automation::AutomationState {
                    enabled: true,"""
after = """                state: automation::AutomationState {"""
red = ["presentation::accessibility::tests::test_every_node_registered_at_startup_is_enabled"]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

[[guard]]
name = "update_node_state really reaches the tree and tells the bridge, not a stub returning Ok"
file = "src/presentation/accessibility.rs"
before = """        self.automation.update_state(node_id, state)?;
        self.screen_reader
            .notify_event(automation::AutomationEvent::NodeUpdated(
                node_id.to_string(),
            ))?;
        Ok(())"""
after = """        let _ = (node_id, state);
        Ok(())"""
red = ["presentation::accessibility::tests::test_updating_a_nodes_state_reaches_the_tree_and_tells_the_bridge"]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

[[guard]]
name = "feedback_settings reports what was just stored, not the type's default"
file = "src/presentation/accessibility.rs"
before = """        self.feedback.lock().map(|s| s.clone()).unwrap_or_default()"""
after = """        Default::default()"""
red = ["presentation::accessibility::tests::test_feedback_settings_reports_what_was_just_stored_rather_than_the_default"]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

[[guard]]
name = "live_region_update really notifies the tree and announces the text, not a stub returning Ok"
file = "src/presentation/accessibility.rs"
before = """        self.screen_reader
            .notify_event(automation::AutomationEvent::LiveRegion(
                region_id.to_string(),
                text.to_string(),
            ))?;
        self.screen_reader.announce(text)"""
after = """        let _ = (region_id, text);
        Ok(())"""
red = ["presentation::accessibility::tests::test_live_region_update_notifies_the_tree_and_announces_the_text"]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

[[guard]]
name = "AutomationStore::update_state really replaces the stored state, not a stub returning Ok"
file = "src/presentation/accessibility/automation.rs"
before = """    pub fn update_state(&self, node_id: &str, state: AutomationState) -> Result<()> {
        let mut nodes = self.nodes.write().map_err(|_| {
            crate::common::Error::Other("Automation tree lock poisoned".to_string())
        })?;
        if let Some(node) = nodes.get_mut(node_id) {
            node.state = state;
        }
        Ok(())
    }"""
after = """    pub fn update_state(&self, node_id: &str, state: AutomationState) -> Result<()> {
        let _ = (node_id, state);
        Ok(())
    }"""
red = [
    "presentation::accessibility::automation::tests::test_updating_a_nodes_state_replaces_what_it_had",
    "presentation::accessibility::tests::test_updating_a_nodes_state_reaches_the_tree_and_tells_the_bridge",
]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
    { file = "src/presentation/accessibility/automation.rs", tests = 2 },
]

[[guard]]
name = "a Channel displays as its stored key, not an empty default"
file = "src/presentation/accessibility/feedback.rs"
before = """        f.write_str(self.key())"""
after = """        Ok(Default::default())"""
red = ["presentation::accessibility::feedback::tests::test_a_channel_displays_as_its_stored_key"]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
]

# The plausible wrong implementation, and the one a reader reaches for: an
# empty set looks like the absence of a choice and is a choice, so emptying
# the entry silences the event while the screen says it is back to normal.
# The three tests below were measured by applying this break by hand on
# 2026-09-12 and running the whole library, 7,124 tests, not predicted from
# reading the code.
#
# The signature line is part of the break because `set_event_channels` holds
# the same `retain` line, and a break has to be exactly one edit.
[[guard]]
name = "putting an event back to the default removes the override rather than emptying it"
file = "src/presentation/accessibility/feedback.rs"
before = """    pub fn use_the_default_for(&mut self, event: Event) {
        self.per_event.retain(|(e, _)| *e != event);
    }"""
after = """    pub fn use_the_default_for(&mut self, event: Event) {
        self.set_event_channels(event, BTreeSet::new());
    }"""
red = [
    "presentation::accessibility::feedback::tests::test_an_override_survives_a_round_trip_and_so_does_taking_it_away",
    "presentation::accessibility::feedback::tests::test_putting_an_event_back_to_the_default_removes_the_override",
    "presentation::accessibility::feedback::tests::test_putting_an_event_with_no_override_back_to_the_default_changes_nothing",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
]

# The defect the whole of criterion 1 is about, written as the wrong
# implementation a reader would reach for. One control on the screen stands for
# words, and words means speech and braille, because the notification carrying
# them takes no medium parameter and the screen reader decides. A switch naming
# only speech puts braille back where it was: a channel the storage holds and no
# control on any screen can reach, which is the setting-nobody-can-find shape
# this phase exists to remove.
#
# Measured by applying the break by hand on 2026-09-13 and running the whole
# library, 7,126 tests, not predicted from reading the code. One test went red
# and nothing else did, and the prediction written down first was the same one.
# A second candidate break was considered and not measured, for the reason in
# the summary: every break of this arm reaches the same single test, because
# that test is the only reader of `channels()` until the panel lands.
[[guard]]
name = "the one control standing for words names braille as well as speech"
file = "src/presentation/accessibility/feedback.rs"
before = """            Switch::SpokenOrBrailled => &[Channel::Speech, Channel::Braille],"""
after = """            Switch::SpokenOrBrailled => &[Channel::Speech],"""
red = [
    "presentation::accessibility::feedback::tests::test_every_channel_is_named_by_exactly_one_switch",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
]

# Moving the event picker has two halves and the first is the one that gets
# forgotten: what is on screen belongs to the event being left, and it has to be
# written down before the event being arrived at is painted over it. Without it,
# ticking a box and then moving the picker throws the tick away in silence,
# which is the worst shape of settings bug because the screen afterwards looks
# correct.
#
# Measured by applying the break by hand on 2026-09-13 and running the target:
# the one test in the suite went red with five findings, three ticks lost, the
# model holding None where it should hold an empty answer, and the line about
# whose answer it is saying the wrong thing.
#
# The signature line is part of the break because `remember_what_is_on_screen`
# is called twice in the file, here and at the save path, and a break has to be
# exactly one occurrence.
[[guard]]
name = "moving the event picker writes down what is on screen before painting over it"
file = "src/presentation/wx_settings.rs"
suite = "every_event_has_a_control"
before = """    pub fn show(&self, at: usize) {
        self.remember_what_is_on_screen();"""
after = """    pub fn show(&self, at: usize) {"""
red = ["test_every_event_is_reachable_and_keeps_what_it_was_given"]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/every_event_has_a_control.rs", tests = 1 },
]

# "The setting survives a restart" is a claim about the join between two halves
# that were each proved on their own: the model round trips an answer, and the
# screen holds one. What carries the second across to the first is the save path,
# and nothing held it, so the criterion's third clause rested on reading.
#
# The break is the save path exactly as it stood before the panel existed:
# rebuilding from the stored value and writing only the global channels. It was a
# correct implementation of a tab that could not create a per-event answer, and
# it silently discards every one of them the moment the tab can. That is the
# break worth recording because it is not hypothetical, it is what was there.
#
# Measured by hand on 2026-09-13: the one test in the suite went red saying a
# restart reads None for an event the panel was left answering.
#
# A second break was measured on the global half, writing only the first channel
# a switch answers for, and it reddens on its own with a different sentence. The
# two halves of that check discriminate independently, which is worth knowing
# because one control standing for two channels is the whole reason there are
# three answers and four channels.
#
# The break moved with the code on 2026-09-17, when 09-09 made the Feedback
# page one that is built when its tab is first shown and read back only then;
# the same rebuild-from-stored break, inside that `if let`, measured again.
# It moved a second time the same day, when 10-04 took the block out of
# `read_settings` into `read_the_feedback_page`, where the stored value is
# `cfg`'s before the write; the same break, on the new lines, measured again.
[[guard]]
name = "pressing OK writes the answers the panel holds rather than rebuilding from the stored value"
file = "src/presentation/wx_settings.rs"
suite = "every_event_has_a_control"
before = """    w.per_event.remember_what_is_on_screen();
    {
        let mut feedback = w.per_event.working.borrow_mut();"""
after = """    {
        let mut feedback = FeedbackSettings::from_stored(&cfg.feedback_channels);"""
red = ["test_every_event_is_reachable_and_keeps_what_it_was_given"]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/every_event_has_a_control.rs", tests = 1 },
]

# The last clause of the roadmap's first criterion is a sentence rather than a
# control, and a sentence is the easiest thing in a dialog to lose: a static text
# nobody hands back is invisible to every check in the tree, so deleting it would
# break the criterion and redden nothing.
#
# The break is the realistic near-miss rather than an empty label, because an
# obviously-absent case is one every version of the check catches, including the
# broken ones. This is the sentence a competent author would write while failing
# the rule: accurate, helpful, and silent about whose decision it is.
#
# Measured by hand on 2026-09-13: the one test in the suite went red twice over
# the same sentence, once for not naming the screen reader and once for not
# saying the choice is not made here.
[[guard]]
name = "the screen says whose decision speech or braille is"
file = "src/presentation/wx_settings.rs"
suite = "every_event_has_a_control"
before = """            "Whether something is spoken, shown on a braille display, or both is \\
             chosen in your screen reader, not here. Wixen Mail sends one \\
             notification and your screen reader decides what to do with it.","""
after = """            "Choose whether events are announced, sounded, or shown. Announcing \\
             covers both speech and braille.","""
red = ["test_every_event_is_reachable_and_keeps_what_it_was_given"]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/every_event_has_a_control.rs", tests = 1 },
]

# The second rule of the same panel, and the plan for this task asked for one
# record rather than two. Both breaks were measured because the first plausible
# break is often the weak one, both turned out equally strong, and they guard
# different rules, so both are written down.
#
# Putting an event back to the default and switching every channel off for it
# are opposite answers. The model keeps them apart on purpose, in two methods
# named for the difference, because an empty answer round trips and means
# silence while no entry at all means the default. A button that empties instead
# of removing silences the event it claims to have restored.
#
# Measured by hand on 2026-09-13: the one test in the suite went red with five
# findings, three ticks that did not come back, the event still holding an
# answer of its own, and the line about whose answer it is saying the wrong
# thing.
[[guard]]
name = "the button putting an event back to the default removes its answer rather than emptying it"
file = "src/presentation/wx_settings.rs"
suite = "every_event_has_a_control"
before = """        self.working.borrow_mut().use_the_default_for(self.shown());"""
after = """        self.working
            .borrow_mut()
            .set_event_channels(self.shown(), BTreeSet::new());"""
red = ["test_every_event_is_reachable_and_keeps_what_it_was_given"]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/every_event_has_a_control.rs", tests = 1 },
]

# Four records retired on 2026-09-18 by 11-03, all on `names.rs`'s
# `should_select_first_row`: "the first row becomes current only without a
# selection, not with one too", and the three "the first row becomes current
# only when a row exists" records (count exactly zero, negative,
# non-negative). Each named the one test of that function. The function, the
# `CheckedRows` object it served and that test went with the folder chooser's
# `CheckListBox`, which is a native tree with `TVS_CHECKBOXES` now; a
# reading over MSAA found the object answering read-only where it meant
# checked (the paragraph in `names.rs` where it stood says why). A record
# whose `before` names no place in the tree is one the runner refuses, and a
# rule that no longer exists has nothing to guard. All four were in the sweep
# of 2026-09-15, which is why the count above reads 798.

[[guard]]
name = "a nonzero UI Automation client count means something is listening, not only a nonpositive read"
file = "src/presentation/accessibility/screen_reader.rs"
before = """        raw != 0"""
after = """        raw == 0"""
red = ["presentation::accessibility::screen_reader::native::tests::test_a_nonzero_client_count_means_something_is_listening"]
tests_last_seen = [
    { file = "src/presentation/accessibility/screen_reader.rs", tests = 22 },
]

[[guard]]
name = "a failed live-region registration is logged as a warning, not routine info"
file = "src/presentation/accessibility/screen_reader.rs"
before = """if handle == 0 {"""
after = """if handle != 0 {"""
red = [
    "presentation::accessibility::screen_reader::tests::test_a_failed_registration_is_logged_as_a_warning",
    "presentation::accessibility::screen_reader::tests::test_a_working_registration_is_logged_as_routine_info_not_a_warning",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/screen_reader.rs", tests = 22 },
]

[[guard]]
name = "registering with something waiting logs how many were waiting, not silence either way"
file = "src/presentation/accessibility/screen_reader.rs"
before = """if !waiting.is_empty() {"""
after = """if waiting.is_empty() {"""
red = [
    "presentation::accessibility::screen_reader::tests::test_registering_with_nothing_waiting_does_not_log_a_waiting_count",
    "presentation::accessibility::screen_reader::tests::test_registering_with_something_waiting_logs_how_many_were_waiting",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/screen_reader.rs", tests = 22 },
]

# ── Security and safety subsystem, from the 2026-08-22 mutation-sweep round ─
#
# 46 misses and one timeout across credentials.rs, oauth_credentials.rs,
# safety.rs, security.rs and the four safebrowsing files. Nineteen guarded
# below.
#
# credentials.rs contributes none at all: its real `keyring::Entry`-backed
# store only runs outside `#[cfg(test)]`, and exercising it for real writes
# into whoever's running the tests' actual Windows Credential Manager, which
# is exactly how this file's own comment says an earlier test left an account
# called "acc-1" behind on a real machine.
#
# oauth_credentials.rs's `credentials_for`, `resolve_gmail`, `resolve_outlook`
# and `load_from_toml`, and safebrowsing's `api_key`, `refresh_lists` and one
# of `check_links`'s two `339:5` mutants, all need a real environment
# variable, a real gitignored `oauth.toml`, or a real answer from Google to
# ever return anything other than what a clean machine already returns, which
# both module doc comments already say plainly. `security.rs`'s
# `find_existing_key` is the same shape one layer down: its Windows branch
# reads the real Credential Manager and its file branch resolves the real
# per-machine data folder. None of the seven are guarded here.
#
# Three are genuinely equivalent and documented in place rather than guarded.
# `safety.rs`'s `Verdict::ordinary` is `Self::default()` written out, and a
# mutation swapping it for `Default::default()` calls the identical function;
# unlike an operator swap there is no external fact left to assert once the
# whole body is replaced, so this one carries a doc comment and no
# debug_assert. `safebrowsing/rice.rs`'s `(quotient << parameter) | remainder`
# cannot be told apart from `^`: the ceiling check just above guarantees the
# two operands never share a set bit, the same disjoint-bits shape asking.rs
# already reads with either operator, and this one does carry a
# debug_assert_eq! proving the operands stay disjoint. `safebrowsing/urls.rs`'s
# `p.len() > 1` cannot be told apart from `>= 1` either: the surrounding
# `.all()` guard already requires every part non-empty, so the two operators
# can only disagree at length one, and "0" reads the same in octal as decimal.
#
# One timeout, also in urls.rs's `unescape_once`, is real and already fixed,
# not guarded. `i += 3` on a genuine escape match, mutated to `i *= 3`, freezes
# the cursor at zero and grows the output forever; confirmed as a real hang
# under a 10-second kill, not a slow pass. The fix is two debug_assert!s
# proving the cursor moves after every branch, the same shape as
# `common/error.rs`'s pair from this same round: real code always advances by
# a positive amount, so neither assert ever fires outside the mutation that
# would have hung, and there is no separately-named test the record could
# point at.
#
# Measured by hand: each guard applied alone, against its own file's test
# module, reddened exactly the test(s) named and nothing else. Restored and
# reran green after each. Two entries redden two tests apiece where a second,
# independent local guard inside the same function also depends on the
# mutated line.

[[guard]]
name = "the oauth.toml path names the file it reads, not nothing"
file = "src/service/oauth_credentials.rs"
before = """    crate::common::paths::AppPaths::resolve()
        .ok()
        .map(|paths| paths.oauth_toml())"""
after = """    None"""
red = ["service::oauth_credentials::tests::test_the_oauth_toml_path_names_the_file_it_reads"]
tests_last_seen = [
    { file = "src/service/oauth_credentials.rs", tests = 9 },
]

[[guard]]
name = "the oauth.toml path names the real file, not a made-up default"
file = "src/service/oauth_credentials.rs"
before = """    crate::common::paths::AppPaths::resolve()
        .ok()
        .map(|paths| paths.oauth_toml())"""
after = """    Some(Default::default())"""
red = ["service::oauth_credentials::tests::test_the_oauth_toml_path_names_the_file_it_reads"]
tests_last_seen = [
    { file = "src/service/oauth_credentials.rs", tests = 9 },
]

[[guard]]
name = "an unrecognised OAuth provider never has credentials, not always"
file = "src/service/oauth_credentials.rs"
before = """    credentials_for(provider).is_some()"""
after = """    let _ = provider;
    true"""
red = ["service::oauth_credentials::tests::test_an_unrecognised_provider_never_has_credentials"]
tests_last_seen = [
    { file = "src/service/oauth_credentials.rs", tests = 9 },
]

[[guard]]
name = "the security key path names the file the service reads and writes, not a made-up default"
file = "src/service/security.rs"
before = """        // The key sits beside the data it unlocks. It used to live in the
        // roaming profile while the encrypted database stayed local, so the key
        // crossed the network at every logon and the mail it protects did not.
        let paths = crate::common::paths::AppPaths::resolve()?;
        fs::create_dir_all(paths.root()).map_err(|e| {
            Error::Config(format!("Could not create {}: {e}", paths.root().display()))
        })?;
        Ok(paths.security_key())"""
after = """        Ok(Default::default())"""
red = ["service::security::tests::test_the_key_path_names_the_file_the_service_reads_and_writes"]
tests_last_seen = [
    { file = "src/service/security.rs", tests = 15 },
]

[[guard]]
name = "a stored key decodes back to its real bytes, not nothing"
file = "src/service/security.rs"
before = """        STANDARD.decode(encoded.trim()).ok()?.try_into().ok()"""
after = """        let _ = encoded;
        None"""
red = ["service::security::tests::test_a_key_decodes_back_to_the_exact_bytes_it_was_encoded_from"]
tests_last_seen = [
    { file = "src/service/security.rs", tests = 15 },
]

[[guard]]
name = "a stored key decodes back to its real bytes, not an all-zero one"
file = "src/service/security.rs"
before = """        STANDARD.decode(encoded.trim()).ok()?.try_into().ok()"""
after = """        let _ = encoded;
        Some([0; 32])"""
red = [
    "service::security::tests::test_a_key_decodes_back_to_the_exact_bytes_it_was_encoded_from",
    "service::security::tests::test_text_that_is_not_base64_is_not_a_key",
    "service::security::tests::test_a_decoded_value_of_the_wrong_length_is_not_a_key",
]
tests_last_seen = [
    { file = "src/service/security.rs", tests = 15 },
]

[[guard]]
name = "a stored key decodes back to its real bytes, not a made-up one"
file = "src/service/security.rs"
before = """        STANDARD.decode(encoded.trim()).ok()?.try_into().ok()"""
after = """        let _ = encoded;
        Some([1; 32])"""
red = [
    "service::security::tests::test_a_key_decodes_back_to_the_exact_bytes_it_was_encoded_from",
    "service::security::tests::test_text_that_is_not_base64_is_not_a_key",
    "service::security::tests::test_a_decoded_value_of_the_wrong_length_is_not_a_key",
]
tests_last_seen = [
    { file = "src/service/security.rs", tests = 15 },
]

[[guard]]
name = "the Safe Browsing environment key is trimmed and blank is not a key"
file = "src/service/safebrowsing/mod.rs"
before = """    (!trimmed.is_empty()).then_some(trimmed)"""
after = """    (trimmed.is_empty()).then_some(trimmed)"""
red = ["service::safebrowsing::tests::test_the_environment_key_is_trimmed_and_blank_is_not_a_key"]
tests_last_seen = [
    { file = "src/service/safebrowsing/mod.rs", tests = 24 },
]

[[guard]]
name = "the Safe Browsing toml key is trimmed and blank is not a key"
file = "src/service/safebrowsing/mod.rs"
before = """        .filter(|key| !key.is_empty())"""
after = """        .filter(|key| key.is_empty())"""
red = ["service::safebrowsing::tests::test_the_toml_key_is_trimmed_and_blank_is_not_a_key"]
tests_last_seen = [
    { file = "src/service/safebrowsing/mod.rs", tests = 24 },
]

[[guard]]
name = "an update response is matched to its list by name, not by whichever name it is not"
file = "src/service/safebrowsing/mod.rs"
before = """        .find(|kind| kind.as_api_name() == update.threat_type)"""
after = """        .find(|kind| kind.as_api_name() != update.threat_type)"""
red = ["service::safebrowsing::tests::test_an_update_response_is_matched_to_its_list_by_name"]
tests_last_seen = [
    { file = "src/service/safebrowsing/mod.rs", tests = 24 },
]

[[guard]]
name = "two links that collide on the same prefix are asked about once, not twice"
file = "src/service/safebrowsing/mod.rs"
before = """            if !prefixes.contains(prefix) {"""
after = """            if prefixes.contains(prefix) {"""
red = ["service::safebrowsing::tests::test_two_links_that_collide_on_the_same_prefix_are_asked_about_once"]
tests_last_seen = [
    { file = "src/service/safebrowsing/mod.rs", tests = 24 },
]

[[guard]]
name = "check_links really says nothing before a fetch and after no collision, not a made-up verdict"
file = "src/service/safebrowsing/mod.rs"
before = """    let held: Vec<database::PrefixSet> = LISTS
        .iter()
        .map(|kind| database::PrefixSet::load(&list_path(cache_dir, *kind)))
        .collect();
    if held.iter().all(database::PrefixSet::is_empty) {
        // Nothing has been fetched yet, so every link would "not match" for
        // the wrong reason. Better to say nothing than to imply a check ran.
        return None;
    }
    let borrowed: Vec<&database::PrefixSet> = held.iter().collect();

    let (prefixes, full_hashes) = collisions_for(urls, &borrowed);
    if prefixes.is_empty() {
        // The ordinary case for ordinary mail: nothing collided, so nothing is
        // sent and Google learns nothing, not even that a message was read.
        return None;
    }

    let response = match client::find_full_hashes(api_key, &prefixes, &LISTS).await {
        Ok(response) => response,
        Err(e) => {
            // Not silently absorbed. A check that could not run is a gap, and
            // reporting the message as unremarkable would hide it.
            tracing::warn!("Safe Browsing could not be asked: {}", e);
            return None;
        }
    };
    verdict_for(&client::confirmed(&response, &full_hashes))"""
after = """    let _ = (api_key, cache_dir, urls);
    Some(Default::default())"""
red = [
    "service::safebrowsing::tests::test_check_links_says_nothing_before_any_list_has_been_fetched",
    "service::safebrowsing::tests::test_check_links_says_nothing_when_no_link_collides",
]
tests_last_seen = [
    { file = "src/service/safebrowsing/mod.rs", tests = 24 },
]

[[guard]]
name = "a list update request asks for the state this copy already holds, not another list's"
file = "src/service/safebrowsing/client.rs"
before = """    .find(|(other, _)| *other == kind)"""
after = """    .find(|(other, _)| *other != kind)"""
red = ["service::safebrowsing::client::tests::test_each_list_asks_for_the_state_it_already_holds_and_no_other"]
tests_last_seen = [
    { file = "src/service/safebrowsing/client.rs", tests = 18 },
]

[[guard]]
name = "a confirmed unwanted-software hash is named, not silently dropped"
file = "src/service/safebrowsing/client.rs"
before = """            "UNWANTED_SOFTWARE" => super::ThreatKind::UnwantedSoftware,
            "POTENTIALLY_HARMFUL_APPLICATION" => super::ThreatKind::PotentiallyHarmfulApplication,"""
after = """            "POTENTIALLY_HARMFUL_APPLICATION" => super::ThreatKind::PotentiallyHarmfulApplication,"""
red = ["service::safebrowsing::client::tests::test_every_threat_type_google_sends_names_the_right_list"]
tests_last_seen = [
    { file = "src/service/safebrowsing/client.rs", tests = 18 },
]

[[guard]]
name = "a confirmed potentially-harmful-application hash is named, not silently dropped"
file = "src/service/safebrowsing/client.rs"
before = """            "UNWANTED_SOFTWARE" => super::ThreatKind::UnwantedSoftware,
            "POTENTIALLY_HARMFUL_APPLICATION" => super::ThreatKind::PotentiallyHarmfulApplication,"""
after = """            "UNWANTED_SOFTWARE" => super::ThreatKind::UnwantedSoftware,"""
red = ["service::safebrowsing::client::tests::test_every_threat_type_google_sends_names_the_right_list"]
tests_last_seen = [
    { file = "src/service/safebrowsing/client.rs", tests = 18 },
]

[[guard]]
name = "only a scheme starting with a letter counts as one, not any alphanumeric run"
file = "src/service/safebrowsing/urls.rs"
before = """                && !scheme.contains('.')
                && scheme"""
after = """                && !scheme.contains('.')
                || scheme"""
red = ["service::safebrowsing::urls::tests::test_only_something_starting_with_a_letter_counts_as_a_scheme"]
tests_last_seen = [
    { file = "src/service/safebrowsing/urls.rs", tests = 22 },
]

[[guard]]
name = "a hash that arrives percent-encoded is escaped back, not let through"
file = "src/service/safebrowsing/urls.rs"
before = """byte == b'#' || extra.contains(&byte)"""
after = """byte == b'#' && extra.contains(&byte)"""
red = ["service::safebrowsing::urls::tests::test_a_hash_that_arrives_percent_encoded_is_escaped_back"]
tests_last_seen = [
    { file = "src/service/safebrowsing/urls.rs", tests = 22 },
]

[[guard]]
name = "a non-IP-shaped host is not folded by the octal check, not treated as one anyway"
file = "src/service/safebrowsing/urls.rs"
before = """    if parts.len() == 4
        && parts"""
after = """    if parts.len() == 4
        || parts"""
red = [
    "service::safebrowsing::urls::tests::test_a_non_ip_shaped_host_is_not_folded_by_the_octal_check",
    "service::safebrowsing::urls::tests::test_a_part_that_only_looks_numeric_does_not_get_folded_into_an_address",
]
tests_last_seen = [
    { file = "src/service/safebrowsing/urls.rs", tests = 22 },
]

[[guard]]
name = "a host part that only looks numeric is not folded into an address, not treated as one anyway"
file = "src/service/safebrowsing/urls.rs"
before = """.all(|p| !p.is_empty() && p.chars().all(|c| c.is_ascii_digit()))"""
after = """.all(|p| !p.is_empty() || p.chars().all(|c| c.is_ascii_digit()))"""
red = ["service::safebrowsing::urls::tests::test_a_part_that_only_looks_numeric_does_not_get_folded_into_an_address"]
tests_last_seen = [
    { file = "src/service/safebrowsing/urls.rs", tests = 22 },
]

# ── notes.rs, from the 2026-08-22 mutation-sweep round ──────────────────────
#
# Five misses in application/notes.rs, all untested but already correct, all
# guarded below. reading_habits.rs and threading.rs, this same builder
# group's other two files, contribute none: their guard-worthy work landed in
# commit 3fcd460 bounding DisjointSet::find, whose own diff is the record of
# what that commit covers.
#
# Measured by hand: each guard applied alone, against the whole library,
# reddened exactly the test(s) named and nothing else. Restored and reran
# green after each. Two entries name two tests apiece: a folder store that
# never keeps what it is given also fails the load test, which checks the
# same folder by content, not only the folder test built to catch it; adding
# a folder that never gets pushed does not, since nothing there reads it back
# through the cache.

[[guard]]
name = "a real heading is found and placed correctly in the text, not an empty page"
file = "src/service/pdf.rs"
before = """    use pdfpurr::content::structure_detection::BlockRole;

    let Ok(blocks) = document.analyze_page_structure(page) else {
        return Vec::new();
    };
    let characters: Vec<char> = page_text.chars().collect();
    let mut cursor = 0usize;
    let mut found = Vec::new();

    for block in blocks {
        let BlockRole::Heading(level) = block.role else {
            continue;
        };
        let label = block
            .runs
            .iter()
            .map(|run| run.text.as_str())
            .collect::<String>();
        let label = label.split_whitespace().collect::<Vec<_>>().join(" ");
        if label.is_empty() {
            continue;
        }
        let Some(at) = find_from(&characters, &label, cursor) else {
            // Extraction and detection disagreed about this block's text,
            // which happens with ligatures and odd encodings. Dropping the
            // landmark loses a jump target; inventing an offset would put the
            // caret somewhere arbitrary, which is worse.
            continue;
        };
        cursor = at + label.chars().count();
        found.push(PdfHeading {
            // Page markers are level 2, so a document's own headings start at
            // 3 and never compete with them. Capped at six the same way the
            // HTML side caps, so the two agree about what a deep heading is.
            offset: page_start + at,
            level: (usize::from(level) + 2).min(6),
            label,
        });
    }
    found"""
after = """    let _ = (document, page, page_text, page_start);
    Vec::new()"""
red = [
    "service::pdf::tests::test_a_real_heading_is_found_and_placed_correctly_in_the_text",
    "service::pdf::tests::test_the_same_heading_twice_on_a_page_is_matched_to_each_occurrence_in_turn",
]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

[[guard]]
name = "the cursor after a heading moves forward by the label's length, not backward"
file = "src/service/pdf.rs"
before = """        cursor = at + label.chars().count();"""
after = """        cursor = at - label.chars().count();"""
red = ["service::pdf::tests::test_the_same_heading_twice_on_a_page_is_matched_to_each_occurrence_in_turn"]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

[[guard]]
name = "the cursor after a heading moves forward by the label's length, not by its product"
file = "src/service/pdf.rs"
before = """        cursor = at + label.chars().count();"""
after = """        cursor = at * label.chars().count();"""
red = ["service::pdf::tests::test_the_same_heading_twice_on_a_page_is_matched_to_each_occurrence_in_turn"]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

[[guard]]
name = "a heading's offset is the page's start plus where it sits, not minus"
file = "src/service/pdf.rs"
before = """            offset: page_start + at,"""
after = """            offset: page_start - at,"""
red = [
    "service::pdf::tests::test_a_real_heading_is_found_and_placed_correctly_in_the_text",
    "service::pdf::tests::test_the_same_heading_twice_on_a_page_is_matched_to_each_occurrence_in_turn",
]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

[[guard]]
name = "a heading's offset is the page's start plus where it sits, not their product"
file = "src/service/pdf.rs"
before = """            offset: page_start + at,"""
after = """            offset: page_start * at,"""
red = [
    "service::pdf::tests::test_a_real_heading_is_found_and_placed_correctly_in_the_text",
    "service::pdf::tests::test_the_same_heading_twice_on_a_page_is_matched_to_each_occurrence_in_turn",
]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

[[guard]]
name = "a heading's level is two below a page marker's, not two below subtracted"
file = "src/service/pdf.rs"
before = """            level: (usize::from(level) + 2).min(6),"""
after = """            level: (usize::from(level) - 2).min(6),"""
red = [
    "service::pdf::tests::test_a_real_heading_is_found_and_placed_correctly_in_the_text",
    "service::pdf::tests::test_the_same_heading_twice_on_a_page_is_matched_to_each_occurrence_in_turn",
]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

[[guard]]
name = "a heading's level is two below a page marker's, not their product"
file = "src/service/pdf.rs"
before = """            level: (usize::from(level) + 2).min(6),"""
after = """            level: (usize::from(level) * 2).min(6),"""
red = ["service::pdf::tests::test_a_real_heading_is_found_and_placed_correctly_in_the_text"]
tests_last_seen = [
    { file = "src/service/pdf.rs", tests = 18 },
]

# ── spellcheck: which language this machine and this word list offer ───────

[[guard]]
name = "language_of_this_machine really asks the machine, not a stub answering None"
file = "src/service/spellcheck/mod.rs"
before = """    let wanted = system_language()?.to_ascii_lowercase();
    best_available_match(&wanted, &available_languages())"""
after = """    None"""
red = ["service::spellcheck::tests::test_the_machines_language_is_exactly_the_best_match_for_its_real_locale"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "an exact language match has to be available, not merely spelled the same"
file = "src/service/spellcheck/mod.rs"
before = """        .find(|c| c.available && c.tag.to_ascii_lowercase() == wanted)"""
after = """        .find(|c| c.available || c.tag.to_ascii_lowercase() == wanted)"""
red = [
    "service::spellcheck::tests::test_the_exact_tag_is_preferred_over_its_family",
    "service::spellcheck::tests::test_an_unavailable_exact_match_does_not_win_over_an_available_family_member",
    "service::spellcheck::tests::test_nothing_answers_when_no_family_member_is_available",
    # Added 2026-09-15 by 08-07's sweep to all four records below this line
    # and this one: the same fourth test the record after them was found to
    # redden on 2026-09-13, written 2026-09-03 in `data::config`, a file none
    # of the five names. That finding was written on one record and the four
    # beside it, with the same break site, were not measured again.
    "data::config::permission_tests::test_a_language_nothing_here_can_check_falls_back_to_english_on_purpose",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the exact language match has to spell the tag the same, not differently"
file = "src/service/spellcheck/mod.rs"
before = """        .find(|c| c.available && c.tag.to_ascii_lowercase() == wanted)"""
after = """        .find(|c| c.available && c.tag.to_ascii_lowercase() != wanted)"""
red = [
    "service::spellcheck::tests::test_the_exact_tag_is_preferred_over_its_family",
    # Added 2026-09-15 by 08-07's sweep; see the record above.
    "data::config::permission_tests::test_a_language_nothing_here_can_check_falls_back_to_english_on_purpose",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "a family language match has to be available, not merely share the family"
file = "src/service/spellcheck/mod.rs"
before = """            c.available && c.tag.to_ascii_lowercase().split('-').next() == Some(family.as_str())"""
after = """            c.available || c.tag.to_ascii_lowercase().split('-').next() == Some(family.as_str())"""
red = [
    "service::spellcheck::tests::test_nothing_answers_when_no_family_member_is_available",
    "service::spellcheck::tests::test_an_unavailable_exact_match_does_not_win_over_an_available_family_member",
    # Added 2026-09-15 by 08-07's sweep; see the first record of the four.
    "data::config::permission_tests::test_a_language_nothing_here_can_check_falls_back_to_english_on_purpose",
    # Added 2026-09-16 by 09-02's re-measure: the family arm moved into
    # `first_available_in_the_family_of` and `language_to_use`'s last arm
    # reaches it too, so two of the resolver's tests redden under this break.
    "service::spellcheck::tests::test_a_language_windows_does_not_support_at_all_matches_nothing",
    "service::spellcheck::tests::test_a_stored_language_nothing_offers_resolves_to_nothing",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 23 and a
    # measurement here that found the same two. The row the settings screen
    # shows for a stored language is worked out through the same resolver, in
    # a file this record had never named.
    "presentation::which_language_row::tests::test_a_bare_tag_the_checker_cannot_place_stands_as_its_own_row_when_listed",
    "presentation::which_language_row::tests::test_a_tag_nothing_offers_is_added_as_stored",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/presentation/which_language_row.rs", tests = 6 },
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "a family language match has to share the family, not differ from it"
file = "src/service/spellcheck/mod.rs"
before = """            c.available && c.tag.to_ascii_lowercase().split('-').next() == Some(family.as_str())"""
after = """            c.available && c.tag.to_ascii_lowercase().split('-').next() != Some(family.as_str())"""
red = [
    "service::spellcheck::tests::test_an_unavailable_exact_match_does_not_win_over_an_available_family_member",
    # Added 2026-09-15 by 08-07's sweep; see the first record of the four.
    "data::config::permission_tests::test_a_language_nothing_here_can_check_falls_back_to_english_on_purpose",
    # Added 2026-09-16 by 09-02's re-measure: `language_to_use`'s last arm
    # reaches this line too, so a break that matches the wrong family answers
    # the resolver's family cases with the wrong language or with a language
    # where it wanted none.
    "service::spellcheck::tests::test_a_bare_stored_language_takes_the_first_offered_when_the_machine_speaks_another",
    "service::spellcheck::tests::test_a_language_windows_does_not_support_at_all_matches_nothing",
    "service::spellcheck::tests::test_a_stored_language_nothing_offers_resolves_to_nothing",
    "service::spellcheck::tests::test_the_machines_own_region_wins_only_when_it_is_available",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 24 and a
    # measurement here that found the same two. The row the settings screen
    # shows for a stored language is worked out through the same resolver, in
    # a file this record had never named.
    "presentation::which_language_row::tests::test_a_bare_tag_the_checker_cannot_place_stands_as_its_own_row_when_listed",
    "presentation::which_language_row::tests::test_a_tag_nothing_offers_is_added_as_stored",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/presentation/which_language_row.rs", tests = 6 },
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
# Found short on 2026-09-13 by a run filtered for something else entirely, while
# measuring two new records about certificate withdrawal. This break reddens a
# fourth test, in `data::config`, and no count of `src/service/spellcheck/mod.rs`
# could ever have predicted it: the file that gained the test is not a file this
# record names. That is the third of the three limits the header of this file
# lists, happening in the open.
#
# The name is added by hand rather than by the runner, because a record that
# comes out short does not get its counts written. The next measurement records
# them.
name = "a real Windows language list is used instead of the built-in fallback, not the other way round"
file = "src/service/spellcheck/mod.rs"
before = """    if !windows_choices.is_empty() {"""
after = """    if windows_choices.is_empty() {"""
red = [
    "service::spellcheck::tests::test_an_empty_windows_list_falls_back_to_the_built_in_six",
    "service::spellcheck::tests::test_a_real_windows_list_is_used_instead_of_the_fallback",
    "service::spellcheck::tests::test_only_english_is_offered_as_available_in_the_built_in_fallback",
    "data::config::permission_tests::test_a_language_this_machine_can_check_is_the_one_it_checks_in",
    # Added 2026-09-16 by 09-02's re-measure: the resolver's test that hands
    # `choices_from` a real list of three reads the fallback six instead under
    # this break, and none of the six is en-US.
    "service::spellcheck::tests::test_a_bare_language_is_checked_in_the_machines_own_region_rather_than_windows_first",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "only English is offered as available in the built-in fallback, not every language but English"
file = "src/service/spellcheck/mod.rs"
before = """            available: language.code == "en","""
after = """            available: language.code != "en","""
red = ["service::spellcheck::tests::test_only_english_is_offered_as_available_in_the_built_in_fallback"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# Until 2026-09-16 this record was "the first regional variant Windows offers
# shares the language, not differs from it", breaking `find_regional_variant`.
# That function took the first of the family Windows listed for a bare stored
# tag, and on an en-US machine the first English Windows lists is the
# Caribbean (#21). It is retired; the family fallback is now the last arm of
# `language_to_use`, reached only when the machine's own region is not in
# the stored tag's family, and this is that arm taken out. The test asking
# for `None` for a language nothing offers stays green under the break on
# purpose: `None` is what it wanted either way.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads.
[[guard]]
name = "a bare stored language on a machine set to another falls to the first of its family, not to nothing"
file = "src/service/spellcheck/mod.rs"
before = """    first_available_in_the_family_of(&stored, offered)
}"""
after = """    None
}"""
red = [
    "service::spellcheck::tests::test_a_bare_stored_language_takes_the_first_offered_when_the_machine_speaks_another",
    "service::spellcheck::tests::test_the_machines_own_region_wins_only_when_it_is_available",
]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the locale Windows reports keeps every code unit GetLocaleInfoW wrote, not one extra"
file = "src/service/spellcheck/mod.rs"
before = """    Some(String::from_utf16_lossy(&buffer[..(written - 1) as usize]))"""
after = """    Some(String::from_utf16_lossy(&buffer[..(written + 1) as usize]))"""
red = ["service::spellcheck::tests::test_system_language_reports_something_real_on_this_machine"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the locale Windows reports drops the terminator, not keeps the whole buffer"
file = "src/service/spellcheck/mod.rs"
before = """    Some(String::from_utf16_lossy(&buffer[..(written - 1) as usize]))"""
after = """    Some(String::from_utf16_lossy(&buffer[..(written * 1) as usize]))"""
red = ["service::spellcheck::tests::test_system_language_reports_something_real_on_this_machine"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# ── spellcheck: how a source and a spelling summary describe themselves ────

[[guard]]
name = "each spelling source describes itself honestly, not with one canned sentence"
file = "src/service/spellcheck/mod.rs"
before = """        match self {
            Self::Windows => "Windows, so it knows the words you have added in Windows Settings",
            Self::Hunspell => "a dictionary installed on this computer",
            Self::Builtin => {
                "a small built-in word list, which will not know names or \\
                 technical terms"
            }
        }"""
after = """        "xyzzy""""
red = ["service::spellcheck::tests::test_every_source_describes_itself_honestly"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "exactly three misspelled words are all named, not one named and two counted as others"
file = "src/service/spellcheck/mod.rs"
before = """        count if count <= NAMED => format!("""
after = """        count if count < NAMED => format!("""
red = ["service::spellcheck::tests::test_exactly_three_words_are_all_named_without_an_and_zero_others"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# ── spellcheck: the Speller trait, reached through a trait object ──────────

[[guard]]
name = "the trait object's suggest really calls the inherent method, not a stub returning nothing"
file = "src/service/spellcheck/mod.rs"
before = """    fn suggest(&self, word: &str, max: usize) -> Vec<String> {
        SpellChecker::suggest(self, word, max)
    }"""
after = """    fn suggest(&self, word: &str, max: usize) -> Vec<String> {
        let _ = (word, max);
        Vec::new()
    }"""
red = ["service::spellcheck::tests::test_the_speller_trait_object_suggests_the_same_as_the_inherent_method"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the built-in checker really refuses to learn a word through the trait, not a stub claiming success"
file = "src/service/spellcheck/mod.rs"
before = """        Err(Error::Other(
            "Words can only be learned when Windows is doing the spell \\
             checking. This computer is using the built-in word list."
                .into(),
        ))"""
after = """        Ok(())"""
red = ["service::spellcheck::tests::test_the_builtin_checker_refuses_to_learn_a_word_through_the_trait"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the trait object's language really reports the checker's own, not an empty string"
file = "src/service/spellcheck/mod.rs"
before = """    fn language(&self) -> &str {
        SpellChecker::language(self)
    }"""
after = """    fn language(&self) -> &str {
        ""
    }"""
red = ["service::spellcheck::tests::test_the_speller_trait_object_reports_its_real_language"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# ── spellcheck: SpellChecker's own methods ──────────────────────────────────

[[guard]]
name = "has_hunspell says yes for a checker really built from hunspell data, not always no"
file = "src/service/spellcheck/mod.rs"
before = """        matches!(self.backend, Backend::Spellbook(_))"""
after = """        let _ = &self.backend;
        false"""
red = ["service::spellcheck::tests::test_from_hunspell_data_checks_against_the_data_it_was_given"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# Two red since 2026-09-17: `source_for_language` names the source without
# building a checker, and its test holds that answer to the one a built
# checker reports, so a built-in checker claiming Hunspell data is a
# disagreement that test sees as well. Found by the remedy run of that day
# and measured again by hand after the second name was written down.
[[guard]]
name = "has_hunspell says no for the plain built-in checker, not always yes"
file = "src/service/spellcheck/mod.rs"
before = """        matches!(self.backend, Backend::Spellbook(_))"""
after = """        let _ = &self.backend;
        true"""
red = [
    "service::spellcheck::tests::test_a_plain_checker_without_a_real_dictionary_says_so",
    "service::spellcheck::tests::test_the_source_named_without_a_checker_is_the_one_a_checker_built_would_report",
]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the alphabet picked for hunspell data matches the language asked for, not any other"
file = "src/service/spellcheck/mod.rs"
before = """        let alphabet = supported_languages()
            .into_iter()
            .find(|l| l.code == lang_code)"""
after = """        let alphabet = supported_languages()
            .into_iter()
            .find(|l| l.code != lang_code)"""
red = ["service::spellcheck::tests::test_from_hunspell_data_picks_the_alphabet_for_the_language_asked_for"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "loading a dictionary file adds every nonblank line, not only the blank ones"
file = "src/service/spellcheck/mod.rs"
before = """                let w = line.trim().to_lowercase();
                if !w.is_empty() {
                    dict.insert(w);
                    count += 1;
                }"""
after = """                let w = line.trim().to_lowercase();
                if w.is_empty() {
                    dict.insert(w);
                    count += 1;
                }"""
red = ["service::spellcheck::tests::test_load_dictionary_file_adds_every_nonblank_line"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "a built-in suggestion is filtered down to real dictionary words, not left as every edit"
file = "src/service/spellcheck/mod.rs"
before = """                    edits.into_iter().filter(|e| set.contains(e)).collect();"""
after = """                    edits.into_iter().collect();"""
red = [
    "service::spellcheck::tests::test_suggest_offers_a_real_correction_for_a_common_misspelling",
    "service::spellcheck::tests::test_the_speller_trait_object_suggests_the_same_as_the_inherent_method",
]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "word_count adds this session's custom words, not subtracts them"
file = "src/service/spellcheck/mod.rs"
before = """        base + self.custom_words.len()"""
after = """        base - self.custom_words.len()"""
red = ["service::spellcheck::tests::test_word_count_includes_words_added_this_session"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "the default dictionary search paths include the Windows location, not skip it"
file = "src/service/spellcheck/mod.rs"
before = """    } else if cfg!(target_os = "windows")
        && let Some(program_data) = std::env::var_os("ProgramData")
    {
        paths.push(PathBuf::from(program_data).join("hunspell"));
    }"""
after = """    }"""
red = ["service::spellcheck::tests::test_default_dict_search_paths_includes_the_windows_location"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "a checker reports the search paths it actually searched, not a permanently empty slice"
file = "src/service/spellcheck/mod.rs"
before = """    pub fn dict_search_paths(&self) -> &[PathBuf] {
        &self.dict_search_paths
    }"""
after = """    pub fn dict_search_paths(&self) -> &[PathBuf] {
        Vec::leak(Vec::new())
    }"""
red = ["service::spellcheck::tests::test_dict_search_paths_reports_what_this_checker_actually_searched"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# The break moved with the code on 2026-09-17, when 09-09 took the search for
# the two files out into `hunspell_files_for`, which `source_for_language`
# asks as well; the loop over what it finds is what the stub replaces now.
# Measured again.
[[guard]]
name = "try_load_spellbook really finds a dictionary sitting on its path, not a stub answering None"
file = "src/service/spellcheck/mod.rs"
before = """    for (aff_path, dic_path) in hunspell_files_for(hunspell_name, search_paths) {
        if let Ok(aff) = std::fs::read_to_string(&aff_path)
            && let Ok(dic) = std::fs::read_to_string(&dic_path)
        {
            let aff_static: &'static str = Box::leak(aff.into_boxed_str());
            let dic_static: &'static str = Box::leak(dic.into_boxed_str());
            if let Ok(dict) = spellbook::Dictionary::new(aff_static, dic_static) {
                tracing::info!(
                    "Loaded Hunspell dictionary '{}' from {}",
                    hunspell_name,
                    aff_path.display()
                );
                return Some(Backend::Spellbook(Box::new(dict)));
            }
        }
    }
    None"""
after = """    let _ = (hunspell_name, search_paths);
    None"""
red = ["service::spellcheck::tests::test_try_load_spellbook_finds_a_dictionary_on_one_of_its_paths"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "a single letter is too short to be worth checking, not only an empty string"
file = "src/service/spellcheck/mod.rs"
before = """    if s.is_empty() || s.len() <= 1 {"""
after = """    if s.is_empty() {"""
red = ["service::spellcheck::tests::test_a_single_letter_is_too_short_to_be_worth_checking"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "both an @ and a dot have to be there to read as an email, not either alone"
file = "src/service/spellcheck/mod.rs"
before = """    if lower.contains('@') && lower.contains('.') {"""
after = """    if lower.contains('@') || lower.contains('.') {"""
red = ["service::spellcheck::tests::test_a_dot_alone_is_not_treated_as_an_email"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# ── spellcheck: document locale and i18n ────────────────────────────────────

[[guard]]
name = "set_document_locale really writes the code for reading back, not a no-op"
file = "src/service/spellcheck/mod.rs"
before = """    if let Ok(mut locale) = DOCUMENT_LOCALE.write() {
        *locale = Some(code.to_string());
    }"""
after = """    let _ = code;"""
red = ["service::spellcheck::tests::test_set_document_locale_writes_the_code_for_reading_back"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "loading a translations file counts every entry in the table, not zero"
file = "src/service/spellcheck/mod.rs"
before = """        let count = map.len();"""
after = """        let count = 0;"""
red = ["service::spellcheck::tests::test_load_translations_file_reads_every_entry_in_the_table"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "set_locale really changes which language is read back, not a no-op"
file = "src/service/spellcheck/mod.rs"
before = """        self.active_locale = Locale::from_code(code);"""
after = """        let _ = code;"""
red = ["service::spellcheck::tests::test_set_locale_changes_which_language_is_read_back"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

[[guard]]
name = "tf really fills in its placeholders, not a stub returning the template unfilled"
file = "src/service/spellcheck/mod.rs"
before = """        let mut result = template;
        for (i, arg) in args.iter().enumerate() {
            result = result.replace(&format!("{{{}}}", i), arg);
        }
        result"""
after = """        let _ = args;
        template"""
red = ["service::spellcheck::tests::test_tf_fills_in_its_placeholders"]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# ── spellcheck: windows_speller.rs, the parts that are not COM ─────────────

[[guard]]
name = "a wide string's PCWSTR points at its own buffer, not a null pointer"
file = "src/service/spellcheck/windows_speller.rs"
before = """    fn as_pcwstr(&self) -> PCWSTR {
        PCWSTR(self.0.as_ptr())
    }"""
after = """    fn as_pcwstr(&self) -> PCWSTR {
        PCWSTR(std::ptr::null())
    }"""
red = ["service::spellcheck::windows_speller::tests::test_as_pcwstr_points_at_the_wide_strings_own_buffer"]
tests_last_seen = [
    { file = "src/service/spellcheck/windows_speller.rs", tests = 9 },
]

[[guard]]
name = "display_name really asks Windows for the locale's own name, not a canned word"
file = "src/service/spellcheck/windows_speller.rs"
before = """    let wide = Wide::new(tag);
    let mut buffer = [0u16; 128];
    // Safe: the buffer's length is passed with it, and the return value says
    // how much was written before any of it is read.
    let written = unsafe {
        windows::Win32::Globalization::GetLocaleInfoEx(
            wide.as_pcwstr(),
            windows::Win32::Globalization::LOCALE_SNATIVEDISPLAYNAME,
            Some(&mut buffer),
        )
    };
    if written <= 1 {
        return tag.to_string();
    }
    // The count includes the terminator.
    String::from_utf16_lossy(&buffer[..written as usize - 1])"""
after = """    let _ = tag;
    "xyzzy".to_string()"""
red = ["service::spellcheck::windows_speller::tests::test_display_name_reads_a_locale_windows_always_knows"]
tests_last_seen = [
    { file = "src/service/spellcheck/windows_speller.rs", tests = 9 },
]

[[guard]]
name = "display_name really asks Windows for the locale's own name, not the tag it was given back"
file = "src/service/spellcheck/windows_speller.rs"
before = """    let wide = Wide::new(tag);
    let mut buffer = [0u16; 128];
    // Safe: the buffer's length is passed with it, and the return value says
    // how much was written before any of it is read.
    let written = unsafe {
        windows::Win32::Globalization::GetLocaleInfoEx(
            wide.as_pcwstr(),
            windows::Win32::Globalization::LOCALE_SNATIVEDISPLAYNAME,
            Some(&mut buffer),
        )
    };
    if written <= 1 {
        return tag.to_string();
    }
    // The count includes the terminator.
    String::from_utf16_lossy(&buffer[..written as usize - 1])"""
after = """    tag.to_string()"""
red = ["service::spellcheck::windows_speller::tests::test_display_name_reads_a_locale_windows_always_knows"]
tests_last_seen = [
    { file = "src/service/spellcheck/windows_speller.rs", tests = 9 },
]

# ── The bug hunt of 2026-08-27 ──────────────────────────────────────────────
#
# Six guards written against faults found by sweeping the boundaries that
# recent work disturbed. Each break below was taken by hand and the whole of
# the named suite run, twice for the two that share a file, because the first
# measurement of one of them was wrong: it named four tests where the truth is
# nine. Writing down what was expected rather than what happened is the one
# thing the top of this file asks nobody to do, and it is easy to do anyway.
#
# The registry tests had to be repaired first. They deleted keys shared with
# each other, so a full run failed at random: the same break gave one spurious
# failure and then five. That makes every record in this file untrustworthy
# rather than only the ones about that code, because a run decides a guard
# still works by requiring the red set to match exactly.

[[guard]]
name = "a picture leaves the body before the message is sent"
file = "src/application/pictures.rs"
before = """    if !is_a_picture_we_carried(address) {
        return None;
    }"""
after = """    if is_a_picture_we_carried(address) {
        return None;
    }"""
red = [
    "application::pictures::tests::test_a_decorative_picture_goes_out_as_a_part_of_its_own",
    "application::pictures::tests::test_a_description_written_with_a_quote_in_it_reads_as_it_was_typed",
    "application::pictures::tests::test_a_message_whose_pictures_are_all_decorative_says_nothing_about_pictures",
    "application::pictures::tests::test_a_picture_leaves_the_body_before_it_is_sent",
    "application::pictures::tests::test_every_picture_in_a_message_gets_a_name_of_its_own",
    "application::pictures::tests::test_one_described_and_one_decorative_picture_leave_one_line_in_the_plain_half",
    "application::pictures::tests::test_the_body_points_at_the_part_that_was_taken_out_of_it",
    "application::pictures::tests::test_the_picture_is_carried_out_whole_and_still_described",
    "application::pictures::tests::test_the_plain_half_still_says_what_the_pictures_were",
    "service::protocols::smtp::tests::test_a_picture_goes_out_as_a_part_rather_than_written_into_the_body",
    "service::protocols::smtp::tests::test_the_plain_half_of_a_sent_message_says_what_the_picture_was",
    "service::protocols::smtp::tests::test_the_sent_body_and_the_sent_picture_name_each_other",
]
tests_last_seen = [
    { file = "src/application/pictures.rs", tests = 44 },
    { file = "src/service/protocols/smtp.rs", tests = 40 },
]

[[guard]]
name = "the plain half of a message says what its pictures were"
file = "src/application/pictures.rs"
before = """    format!("\\n\\nPictures in this message:\\n{}", described.join("\\n"))"""
after = """    let _ = described;
    String::new()"""
red = [
    "application::pictures::tests::test_one_described_and_one_decorative_picture_leave_one_line_in_the_plain_half",
    "application::pictures::tests::test_the_plain_half_still_says_what_the_pictures_were",
    "service::protocols::smtp::tests::test_the_plain_half_of_a_sent_message_says_what_the_picture_was",
]
tests_last_seen = [
    { file = "src/application/pictures.rs", tests = 44 },
    { file = "src/service/protocols/smtp.rs", tests = 40 },
]

[[guard]]
name = "forwarding takes the original's pictures with it"
file = "src/presentation/wx_compose.rs"
before = """        "---------- Forwarded message ----------",
        ThePictures::ComeAlong,"""
after = """        "---------- Forwarded message ----------",
        ThePictures::StayBehind,"""
red = [
    "presentation::wx_compose::tests::test_forwarding_a_message_passes_its_pictures_on",
]
tests_last_seen = [
    { file = "src/presentation/wx_compose.rs", tests = 43 },
]

[[guard]]
name = "raising the window takes it out of the taskbar"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """                            frame.iconize(false);
                            frame.show(true);
                            frame.raise();"""
after = """                            frame.show(true);
                            frame.raise();"""
red = [
    "test_nothing_raises_the_window_without_also_taking_it_out_of_the_taskbar",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
name = "Cut and Copy ask the box what is selected"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """                EditCommand::Cut => {
                    put_on_the_clipboard(&box_.get_string_selection(), a11y);"""
after = """                EditCommand::Cut => {
                    put_on_the_clipboard(&box_.get_value(), a11y);"""
red = [
    "test_cut_and_copy_ask_the_box_rather_than_indexing_its_value",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
name = "a sentence carrying the source indentation is found"
file = "src/presentation/wx_settings.rs"
suite = "house_style"
before = """        "A read receipt tells the sender your address is live and roughly when \\
         you were at your desk. Nothing is sent unless you choose it here.","""
after = """        "A read receipt tells the sender your address is live and roughly when      \\
         you were at your desk. Nothing is sent unless you choose it here.","""
red = [
    "test_no_sentence_is_written_with_the_source_indentation_inside_it",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/house_style.rs", tests = 74 },
]

[[guard]]
name = "a second copy hands over rather than running alongside the first"
file = "src/main.rs"
suite = "wired"
before = """        how_to_start(another_was_already_running, run.handed_over.as_deref())"""
after = """        how_to_start(false, run.handed_over.as_deref())"""
red = [
    "test_a_second_copy_hands_over_rather_than_running_alongside_the_first",
]
tests_last_seen = [
    { file = "src/main.rs", tests = 0 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
name = "every item list is drawn in the font that was chosen"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """                msg_list.set_font(&list_font);"""
after = """                let _ = &list_font;"""
red = [
    "test_every_item_list_is_drawn_in_the_font_that_was_chosen",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
name = "words beside a signature are checked against those words"
file = "src/service/signed_mail.rs"
before = """    let content = parts
        .content
        .clone()
        .or_else(|| signature.wrapped_content.clone());"""
after = """    let content = signature
        .wrapped_content
        .clone()
        .or_else(|| parts.content.clone());"""
red = [
    "service::signed_mail::the_words_that_were_signed::test_words_beside_a_signature_are_checked_against_those_words",
]
tests_last_seen = [
    { file = "src/service/signed_mail.rs", tests = 113 },
]

[[guard]]
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: one more test went red than this named,
# and it is the export rather than a sync. Exporting reads the kept original,
# so the budget this record is about decides what an export can hand over, and
# no test named here said so.
name = "the form a signed message arrived in is really kept"
file = "src/data/message_cache/signed_original.rs"
before = """            .is_ok_and(|size| size <= LARGEST_SIGNED_MESSAGE_KEPT_BYTES)"""
after = """            .is_ok_and(|size| size > LARGEST_SIGNED_MESSAGE_KEPT_BYTES)"""
red = [
    "application::checking_signatures::end_to_end::test_a_signed_message_reopened_from_the_cache_says_what_it_said_the_first_time",
    "application::importing_messages::end_to_end::test_a_signed_message_brought_in_from_a_file_can_still_have_its_signature_checked",
    "application::pop_sync::tests::test_a_signed_message_collected_over_pop_can_still_have_its_signature_checked",
    "data::message_cache::signed_original::tests::test_a_signed_message_there_was_no_room_for_still_says_it_was_signed",
    "data::message_cache::signed_original::tests::test_a_signed_message_too_large_to_keep_still_says_it_was_signed",
    "data::message_cache::signed_original::tests::test_dropping_one_to_stay_under_the_budget_leaves_it_saying_it_was_signed",
    "data::message_cache::signed_original::tests::test_the_form_a_signed_message_arrived_in_comes_back_byte_for_byte",
    "presentation::wx_app::what_an_export_holds::test_a_signed_message_reaches_the_export_as_it_arrived",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-06 in a file this
    # record had never named.
    "data::message_cache::how_it_arrived::tests::test_the_one_call_still_keeps_the_bytes_a_signed_message_arrived_in",
    # Added 2026-09-16 by 09-08's remedy, which ran this record because
    # importing_messages.rs gained tests: written by 09-07 the same day, in
    # a new file no record named, so the count check could not have flagged
    # it. The composition reads the kept original to say what a signature
    # was worth, so the budget this record is about decides its verdict too.
    "application::reading_a_message::tests::test_a_signed_message_in_the_cache_has_its_verdict_and_an_enveloped_one_its_sentence",
]
tests_last_seen = [
    { file = "src/application/checking_signatures.rs", tests = 8 },
    { file = "src/application/importing_messages.rs", tests = 41 },
    { file = "src/application/pop_sync.rs", tests = 49 },
    { file = "src/application/reading_a_message.rs", tests = 7 },
    { file = "src/data/message_cache/how_it_arrived.rs", tests = 9 },
    { file = "src/data/message_cache/signed_original.rs", tests = 8 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── Signed mail brought in from a file said nothing about its signature ─────
#
# Reading a file is the one path that has in its hands the exact bytes a
# signature was made over, and it dropped them. The message went into the folder
# reading as ordinary mail, which is a different claim from "signed, and not
# checkable here" and a false one. Silence reads as ordinary mail, so nothing
# about the message ever suggested there was anything to look at.
#
# The break leaves everything else about the import working, which is what the
# defect looked like: the message arrives, the folder count is right, and the
# only thing missing is the sentence nobody knows to expect.
#
# **Re-measured by hand on 2026-09-06, because the code moved.** 04-09 gave this
# call a second fact to record and renamed it, so the recorded break stopped
# matching the file and `test_every_guard_record_still_names_one_place_in_the_tree`
# said so. The break was taken again rather than the record edited until it
# applied, and it now reddens more than it did: the same end-to-end test, and the
# completeness guard over the arrival paths, which reads the source and no longer
# finds this one.

[[guard]]
name = "signed mail brought in from a file keeps the bytes its signature is over"
file = "src/application/importing_messages.rs"
before = """    if let Some(raw) = &read.the_form_it_arrived_in
        && let Err(e) = cache.note_the_form_it_arrived_in(stored, raw)
    {
        tracing::warn!("Could not record the form an imported message arrived in: {e}");
    }"""
after = """    let _ = &read.the_form_it_arrived_in;"""
red = [
    "application::importing_messages::end_to_end::test_a_signed_message_brought_in_from_a_file_can_still_have_its_signature_checked",
    "data::message_cache::how_it_arrived::tests::test_this_reading_finds_the_arrival_paths_that_exist_today",
]
tests_last_seen = [
    { file = "src/application/importing_messages.rs", tests = 41 },
    { file = "src/data/message_cache/how_it_arrived.rs", tests = 9 },
]

# Two guards from the same stretch of work are deliberately not recorded here,
# and the reasons are different.
#
# `test_no_source_file_carries_an_invisible_control_character` would need its
# break to write a real control byte into a source file. The mechanism puts the
# file back afterwards, but a run stopped in the middle would leave behind
# exactly the thing that guard exists to find, and that byte cost an hour of
# reading a regex that was correct and matched nothing. Taken by hand instead,
# when it is taken.
#
# `test_a_selection_and_the_value_are_counted_the_same_way` measures wxdragon,
# not this program: it asks a real control how it counts and would stay green
# through any change made here. There is nothing in the tree to break that
# would redden it. What guards the code that has to agree with that answer is
# "Cut and Copy ask the box what is selected", above.

[[guard]]
name = "a sign-in the credential store would not keep is refused rather than reported as authorized"
file = "src/service/oauth.rs"
before = """        self.store_tokens(&tokens)?;"""
after = """        let _ = self.store_tokens(&tokens);"""
red = [
    "service::oauth::when_the_credential_store_refuses::test_nothing_keeps_going_after_a_token_it_could_not_keep",
]
tests_last_seen = [
    { file = "src/service/oauth.rs", tests = 51 },
]

# The first measurement of this one reddened nothing, which is the whole reason
# the check above it exists. The test beside it proves `store_tokens` reports a
# refusal; nothing asked whether either caller reads the answer, and that is
# exactly where the defect was. Taking the break by hand is what found it.

[[guard]]
name = "an account whose password or tokens will not go is not deleted"
file = "src/data/message_cache/accounts.rs"
before = """        if !still_stored.is_empty() {
            return Err(Error::Security(format!("""
after = """        if false {
            return Err(Error::Security(format!("""
red = [
    "data::message_cache::accounts::tests::test_an_account_whose_secrets_will_not_go_is_not_deleted",
    "data::message_cache::accounts::tests::test_one_account_that_will_not_go_does_not_throw_away_the_other_edits",
]
tests_last_seen = [
    { file = "src/data/message_cache/accounts.rs", tests = 22 },
]

[[guard]]
name = "a calendar whose sign-in could not be kept is not added"
file = "src/application/calendar_source.rs"
before = """    sign_in::store(&row.id, user_name, password)
        .map_err(|_| "The sign-in could not be saved on this computer. Try again.".to_string())?;"""
after = """    let _ = sign_in::store(&row.id, user_name, password);"""
red = [
    "application::calendar_source::tests::test_a_calendar_whose_sign_in_will_not_be_kept_is_not_added_at_all",
]
tests_last_seen = [
    { file = "src/application/calendar_source.rs", tests = 52 },
]

[[guard]]
name = "a read or flagged state the database refused is put back on the row and said"
file = "src/presentation/wx_app.rs"
before = """        if let Some(row) = s.messages.iter_mut().find(|m| m.message_id == cache_id) {
            (row.read, row.starred) = was;
        }
    }
    let _ = tx.try_send(UIUpdate::ErrorOccurred(format!(
        "That change could not be saved on this computer, so it has been put back: {e}"
    )));
    false"""
after = """        let _ = (s.messages.len(), was);
    }
    true"""
red = [
    "presentation::wx_app::what_the_status_line_says::test_a_read_or_flagged_state_that_will_not_save_is_put_back_and_said",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "a refused move is said on the refusal channel rather than the status line"
file = "src/presentation/managers.rs"
# `Moved` became `Filed` in 05-03, when copy joined move on this arm, and the
# record was unmeasurable until the text below named the code as it now stands.
# Re-measured by hand on 2026-09-08 after the rename rather than edited until it
# applied: the break still reddens exactly the one test named.
before = """                Some(Filed::Refused(why)) => {
                    let _ = tx.try_send(UIUpdate::CommandRefused(why));
                    return;
                }"""
after = """                Some(Filed::Refused(why)) => Ok(why),"""
red = [
    "presentation::managers::tests::test_a_refused_move_is_said_where_a_refusal_is_said",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

[[guard]]
name = "a password left in the database by an older version is cleared on every load, not once"
file = "src/data/message_cache/accounts.rs"
before = """                if !in_the_row.is_empty()
                    && let Err(e) = self.forget_stored_password(account_id)
                {"""
after = """                if false
                    && let Err(e) = self.forget_stored_password(account_id)
                {"""
red = [
    "data::message_cache::accounts::tests::test_an_old_copy_left_in_the_database_is_cleared_the_next_time_too",
]
tests_last_seen = [
    { file = "src/data/message_cache/accounts.rs", tests = 22 },
]

# Rewritten 2026-09-19 by 11-07: the line after the refusal reads the
# selection off the control now; the break is the one it was, and it
# reddened the same one test.
[[guard]]
name = "the reason a command did nothing does not go out as a status line"
file = "src/presentation/wx_app.rs"
before = """        return send_refusal(tx, rt, "No message store is available");
    };
    let chosen = match chosen_messages("""
after = """        return send_status(tx, rt, "No message store is available");
    };
    let chosen = match chosen_messages("""
red = [
    "presentation::wx_app::what_the_status_line_says::test_a_refusal_is_not_written_to_the_status_line",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "nothing but the seam and the uninstall sweep opens a credential entry of its own"
file = "src/service/credentials.rs"
before = """fn write_secret(account_id: &str, password: &str) -> Result<()> {
    secret_store::write(KEYRING_SERVICE, account_id, password)"""
after = """fn write_secret(account_id: &str, password: &str) -> Result<()> {
    let _ = keyring::Entry::new(KEYRING_SERVICE, account_id);
    secret_store::write(KEYRING_SERVICE, account_id, password)"""
red = [
    "service::secret_store::tests::test_only_the_seam_and_the_uninstall_open_a_credential_entry_of_their_own",
]
tests_last_seen = [
    { file = "src/service/credentials.rs", tests = 10 },
    { file = "src/service/secret_store.rs", tests = 6 },
]

[[guard]]
name = "a mailbox name that is not ASCII reaches the server encoded"
file = "src/service/protocols/imap.rs"
before = """        let encoded = mailbox_name::encode(path);"""
after = """        let encoded = path.to_string();"""
red = [
    "application::mail_controller::against_a_server_that_answers::test_making_a_folder_reaches_the_server_encoded_and_opens_nothing",
    "service::protocols::imap::against_a_server_that_answers::test_creating_a_folder_sends_the_name_in_the_encoding_the_server_reads",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "creating a folder on a server asks the gate before it builds a command"
file = "src/service/protocols/imap.rs"
before = """    pub async fn create_mailbox(&mut self, path: &str) -> Result<()> {
        self.may_i("create a folder on the server")?;"""
after = """    pub async fn create_mailbox(&mut self, path: &str) -> Result<()> {"""
# Measured by hand twice, and the second measurement is why the first is not
# what is written here. With the imap floor in MAIL_TRANSPORTS still at 8 this
# break reddened three tests. Raising the floor to 9, which is what an added
# gated write requires, made the census notice this one too: nine gated writes
# minus this one is eight, and eight no longer clears the floor. Written down
# as it really is rather than as it first measured.
#
# Re-measured a third time on 2026-08-31, for a reason neither of the first two
# would have predicted: plan 02-01 added a second test that reads the same
# census, so the same break now reddens five. Nothing about this guarded code
# changed. That is the staleness direction a run of the named tests alone cannot
# see, and it is why `scripts/guards.sh` compares the two sets for equality.
red = [
    "service::outward::completeness::test_every_mailbox_write_is_on_the_mail_wire_list",
    "service::outward::completeness::test_the_read_gate_does_not_move_the_count_of_gated_writes",
    "service::protocols::imap::against_a_server_that_answers::test_creating_a_folder_with_the_gate_closed_says_nothing_to_the_server",
    "service::protocols::imap::against_a_server_that_answers::test_every_mailbox_write_says_nothing_to_the_server_with_the_gate_closed",
    "service::protocols::imap::against_a_server_that_answers::test_the_two_ways_of_being_refused_a_folder_can_be_told_apart",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "the column that says which conversation a message is in has a writer"
file = "src/data/message_cache/messages.rs"
# thread_id shipped as a column nothing wrote. Its only mention in the whole
# data layer was the line creating it, so every row held NULL while a sort
# expression, an in-memory field of the same name and forty other references
# made it read like working infrastructure. This break puts it back to that:
# the column, the sort and the tests all still exist, and nothing fills it.
#
# Measured by hand on 2026-08-30 against the whole library, 5310 tests, then
# again the same day at 5674 after plan 01-11, and a third time at 5761 after
# plan 01-12. Five tests became seventeen and then twenty-one, and the reasons
# matter more than the numbers. 01-11 built a conversation listing that groups
# on this column, so twelve tests about counting and naming conversations came
# to depend on the writer. 01-12 built what a collapsed conversation row acts
# on, which reads the same column, so four more did: three about how far a
# delete reaches and one about a deleted conversation coming back whole.
#
# Three measurements, two of them prompted by the run saying the record had
# fallen behind rather than by anybody remembering to check. That is the whole
# point of it asking in both directions, and this record is now the clearest
# example in the file of a guard that would have quietly shrunk otherwise.
#
# The one worth having is still `test_sorting_by_thread_groups_a_conversation`:
# the Thread column's sort has always been "m.thread_id", so with the writer
# gone the sort silently orders every row by NULL, which is what nobody noticed
# for the life of the column.
#
# Measured a fourth time on 2026-08-31, after plan 01-13 moved the call out of
# the parameter list into a name above it, because the merge that plan added
# needs the same answer and computing it twice would be two places able to come
# to differ. The run said the recorded text was no longer in the file, which is
# the check working: the break now replaces what the parameter list passes, so
# the column goes back to holding NULL exactly as it did before, and the writer
# still exists above it for the merge to use.
#
# Twenty-one tests became thirty-one. Ten, not the eight this note first
# predicted before the run was taken: the number was written down as a guess,
# the run disagreed, and the guess is recorded here rather than quietly
# replaced, because the file's own rule is to write down what really went red
# and not what somebody expected.
#
# Nine of the ten are 01-13's, and the tenth is the one worth reading. It is in
# `wx_app`, not here: a conversation listing groups on this column, so with no
# row naming a conversation the list comes back empty and the test that mail
# arriving into an open folder reaches the rows on screen fails. A guard about
# a database column now reaches the interface, which is what a column having
# acquired real readers looks like.
#
# Measured a fifth time on 2026-09-02, prompted the same way: plan 02.1-06 added
# tests to this file, the count check flagged this record among ten, and the
# re-measurement found it three short. Thirty-one became thirty-four. None of
# the three is about conversations, which is the point. Two are new Safety and
# column-ordering tests that build their fixtures through `in_conversation`, so
# a writer that stores NULL leaves them with no rows to order. The third is a
# new identifier test whose whole assertion is that a join finds a conversation.
# Nobody working on either subject would have filtered for this record, and
# nine of the other ten came out unchanged.
#
# Measured a sixth time on 2026-09-04, prompted the same way again: plan 03-04
# added four tests to this file and the count check flagged this record among
# seventeen. Thirty-four became thirty-eight, and this was the only one of the
# seventeen that had fallen behind. The four are the archived-mail tests, and
# they belong here for the plainest possible reason: every one of them asks what
# a conversation holds, so a column nothing fills leaves them with no
# conversation to ask about. Their own record is next to this one and is about
# which folders are counted, not about whether the column is written, so neither
# record makes the other redundant.
#
# And a seventh time an hour later, in the same session, prompted the same way
# again: plan 03-04's second task added eight more tests to this file. Three of
# them reach this rule and thirty-eight became forty-one. Two are about a copy
# of a message on a server that offers no identifier of its own, and one about a
# message with no identifier at all; every one of them asks what a conversation
# holds, so a column nothing fills leaves all three with nothing to hold.
#
# Six of the seven measurements were prompted by the run rather than by anybody
# remembering, which is what the count check is for. The two an hour apart are
# also the plainest argument this file has for putting all of a plan's tests in
# one commit: the second measurement cost the same as the first and found the
# same kind of thing.
#
# And an eighth time on 2026-09-04, after plan 03-05 added ten tests to this
# file. Forty-one became forty-two, and the arithmetic hides what happened: the
# first run of it reported one new test red and one NAMED test green, and only
# the second direction was the finding.
#
# The new one asks that a row still holding the older bracketed spelling names
# its conversation, which a column nothing fills cannot answer.
#
# The named one that had gone green is `test_the_same_message_arriving_twice
# _moves_nothing_the_second_time`, and it went green because 03-05 rewrote it.
# The merge now records what a message named against the message, so it needs a
# row that exists, and the test was changed to store the connector through the
# real path and ask again. Its new first assertion counted distinct
# conversations, which is one when every row holds the same conversation AND one
# when every row holds nothing, so under this break it passed. A test weakened
# by an edit nobody would have called risky, caught by this record and by
# nothing else. It names the conversation it expects now, and is red under the
# break again, which is why the list grew by one rather than swapping a member.
#
# That is the argument for asking in both directions, in one measurement. A run
# that only asked "did the named tests go red" would have reported the new test
# as an unnamed failure and said nothing about the weakened one. A count check
# would have flagged the record and neither.
#
# And a ninth time on 2026-09-20, after plan 11-11.1.3 added four tests to
# this file and the count check flagged this record among twenty-five.
# Forty-two became fifty, and only four of the eight are that plan's: the
# every-inbox listing groups on this column, so All Inboxes shown as
# conversations has no rows without the writer. The other four are 11-08.1's
# of 2026-09-19, in `server_thread_ids` on both layers, a module no record
# named, so no count check could fire for them: the hole the file's own
# header describes, a test added to a file no record names, found by this
# record being measured for a different reason a day later.
before = """                    &conversation,"""
after = """                    None::<String>,"""
red = [
    "application::server_thread_ids::tests::test_a_refusal_records_nothing_and_the_next_check_asks_for_what_is_still_missing",
    "application::server_thread_ids::tests::test_a_server_that_names_no_conversation_is_not_asked_and_the_pass_is_recorded",
    "data::message_cache::messages::tests::test_a_chain_that_arrives_later_moves_the_message_into_its_conversation",
    "data::message_cache::messages::tests::test_a_conversation_archived_with_no_label_is_still_in_the_list",
    "data::message_cache::messages::tests::test_a_conversation_gets_the_same_id_stored_as_a_batch_or_one_at_a_time",
    "data::message_cache::messages::tests::test_a_conversation_in_another_account_is_never_merged_into_this_one",
    "data::message_cache::messages::tests::test_a_conversation_is_found_by_a_message_in_the_middle_of_it_not_only_its_root",
    "data::message_cache::messages::tests::test_a_conversation_of_one_message_counts_one",
    "data::message_cache::messages::tests::test_a_conversation_split_across_two_folders_counts_the_same_from_either",
    "data::message_cache::messages::tests::test_a_conversation_that_does_not_touch_this_folder_is_not_listed",
    "data::message_cache::messages::tests::test_a_copy_on_a_server_that_gives_no_identifier_of_its_own_is_counted_once",
    "data::message_cache::messages::tests::test_a_delete_reaches_the_message_archived_with_no_label",
    "data::message_cache::messages::tests::test_a_deleted_conversation_comes_back_whole_and_not_in_part",
    "data::message_cache::messages::tests::test_a_deleted_message_is_not_counted",
    "data::message_cache::messages::tests::test_a_filed_draft_and_a_synced_reply_naming_it_are_one_conversation",
    "data::message_cache::messages::tests::test_a_folder_holding_a_copy_of_every_message_does_not_double_the_count",
    "data::message_cache::messages::tests::test_a_folders_conversation_rows_say_the_folder_and_the_account_they_were_read_in",
    "data::message_cache::messages::tests::test_a_hostile_reference_chain_is_stored_and_read_back_unchanged",
    "data::message_cache::messages::tests::test_a_late_message_naming_two_conversations_merges_them_into_one",
    "data::message_cache::messages::tests::test_a_message_a_sync_stores_carries_its_conversation_id",
    "data::message_cache::messages::tests::test_a_message_already_deleted_is_not_reached_again",
    "data::message_cache::messages::tests::test_a_message_archived_with_no_label_counts_toward_its_conversation",
    "data::message_cache::messages::tests::test_a_reply_arriving_into_an_open_folder_joins_the_conversation_it_names",
    "data::message_cache::messages::tests::test_a_row_still_holding_the_bracketed_spelling_is_the_same_message_as_its_copy",
    "data::message_cache::messages::tests::test_a_second_sort_level_is_taken_over_the_conversation_too",
    "data::message_cache::messages::tests::test_a_stored_row_holding_the_bracketed_spelling_still_names_its_conversation",
    "data::message_cache::messages::tests::test_all_inboxes_counts_a_message_filed_elsewhere_only_when_the_reach_is_the_whole_account",
    "data::message_cache::messages::tests::test_all_inboxes_lists_a_conversation_held_in_two_accounts_as_two_rows",
    "data::message_cache::messages::tests::test_an_account_whose_inbox_is_empty_contributes_no_row_to_all_inboxes",
    "data::message_cache::messages::tests::test_an_identifier_nothing_holds_names_no_conversation",
    "data::message_cache::messages::tests::test_an_older_message_arriving_renames_the_conversation",
    "data::message_cache::messages::tests::test_both_views_of_the_safety_column_rank_the_verdicts_the_same_way",
    "data::message_cache::messages::tests::test_every_column_still_orders_two_messages_the_way_that_column_reads",
    "data::message_cache::messages::tests::test_rerooting_says_how_many_messages_it_moved",
    "data::message_cache::messages::tests::test_sorting_by_a_column_agrees_with_what_that_column_says",
    "data::message_cache::messages::tests::test_sorting_by_thread_groups_a_conversation",
    "data::message_cache::messages::tests::test_the_merge_reaches_the_losing_conversation_wherever_it_is_filed",
    "data::message_cache::messages::tests::test_the_messages_an_action_reaches_are_the_ones_its_row_counted",
    "data::message_cache::messages::tests::test_the_row_is_still_there_when_the_folder_being_read_is_the_all_mail_one",
    "data::message_cache::messages::tests::test_the_same_message_arriving_twice_moves_nothing_the_second_time",
    "data::message_cache::messages::tests::test_the_same_message_downloaded_and_filed_carries_one_conversation_id",
    "data::message_cache::messages::tests::test_the_two_reaches_take_different_numbers_of_messages",
    "data::message_cache::messages::tests::test_the_unread_count_holds_a_message_archived_with_no_label",
    "data::message_cache::messages::tests::test_the_unread_count_is_the_unread_messages_by_the_same_reach",
    "data::message_cache::messages::tests::test_two_accounts_do_not_share_a_conversation",
    "data::message_cache::messages::tests::test_two_messages_with_no_identity_of_their_own_are_two_messages",
    "data::message_cache::messages::tests::test_with_the_reach_on_one_folder_a_conversation_counts_only_what_is_here",
    "data::message_cache::server_thread_ids::tests::test_naming_a_reply_brings_its_header_named_conversation_under_the_word",
    "data::message_cache::server_thread_ids::tests::test_naming_a_row_writes_the_word_and_files_the_row_under_it",
    "presentation::wx_app::tests::test_mail_arriving_into_an_open_folder_showing_conversations_rereads_them",
]
tests_last_seen = [
    { file = "src/application/server_thread_ids.rs", tests = 7 },
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/data/message_cache/server_thread_ids.rs", tests = 5 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
name = "a message stored by a sync and the same message filed after sending get one conversation id"
file = "src/data/message_cache/messages.rs"
# This break adds code rather than removing it, which is unusual here and is
# the honest way round for this one.
#
# The plan this came from said to break the sent path by removing the
# conversation_root call from file_message_here. There is no such call to
# remove: file_message_here is upsert_message plus one UPDATE, so the single
# call inside upsert_message already serves both doors. The defect this guard
# is about is therefore not a deletion but an addition, and it is the exact
# addition the plan itself asked for: a second copy of the rule living in the
# filing path, disagreeing with the first. threading::as_stored's own doc
# comment says why that is the thing to fear, that two copies mean the same
# message threading one way when it arrives and another when it is sent.
#
# So the break writes a second answer over the first, using the message's own
# identifier and ignoring its chain. Measured by hand on 2026-08-30 against the
# whole library: one test, which is the one that stores the same message both
# ways and compares.
before = """    pub fn file_message_here(&self, incoming: &IncomingMessage) -> Result<i64> {
        let id = self.upsert_message(incoming)?;"""
after = """    pub fn file_message_here(&self, incoming: &IncomingMessage) -> Result<i64> {
        let id = self.upsert_message(incoming)?;
        self.conn
            .execute(
                "UPDATE messages SET thread_id = ?1 WHERE id = ?2",
                params![incoming.message_id.clone(), id],
            )
            .map_err(|e| Error::Other(format!("Failed: {}", e)))?;"""
red = [
    "data::message_cache::messages::tests::test_the_same_message_downloaded_and_filed_carries_one_conversation_id",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
name = "the conversation backfill fills only the messages that have none, so it can never write over one"
file = "src/data/message_cache/messages.rs"
# The backfill runs on open, over the only copy of somebody's mail. Its WHERE
# clause is the whole of what makes it able to fill and unable to overwrite,
# and it is what makes a second run cost nothing.
#
# Measured by hand on 2026-08-30 against the whole library: two tests. Note
# which two are NOT here. The test that the backfill fills three empty rows
# stays green under this break, because a backfill that fills everything still
# fills those. A record measured by expectation rather than by hand would
# probably have named it, and it would have been wrong.
#
# Measured a second time on 2026-09-04, prompted by the count check after plan
# 03-05 added ten tests to this file. Two became three, and the third is not
# about this backfill at all: it opens a database twice and asks that the second
# open changes nothing. A backfill that fills every row rather than the empty
# ones rewrites, on the second open, every conversation id the first open's
# merges settled on, so the second open changes plenty. Two backfills that run
# one after the other on the same open are coupled by the column they share, and
# nobody would have filtered for this record while writing that test.
before = """                 WHERE thread_id IS NULL","""
after = """                 WHERE 1 = 1","""
red = [
    "data::message_cache::messages::tests::test_opening_a_database_that_has_already_been_filled_changes_nothing",
    "data::message_cache::messages::tests::test_the_backfill_never_writes_over_an_id_that_is_there_already",
    "data::message_cache::messages::tests::test_the_backfill_run_again_fills_nothing",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
name = "a local folder name containing the nesting separator survives being stored and read back"
file = "src/application/local_folders.rs"
# The escaped form is the stored identity of a folder on this computer, and the
# typed form is what a person reads. The scheme only works if the escape
# character escapes itself: without that, a name already holding it comes back
# with the following character eaten, and the folder is reachable only under a
# name nobody typed. That is the same failure ImapFolder's own comment warns
# about for a path re-derived from its readable form.
#
# The break removes the escaping of the escape character and leaves the
# escaping of the separator alone, which is the half a scheme written in a
# hurry would have.
#
# Measured by hand on 2026-08-30 against the whole library: two tests, out of
# six that the pair has. The four that stay green are worth naming, because
# guessing would have listed more. The ordinary name, the separator on its own
# and both refusal tests all pass under the break: a name with no escape
# character in it round-trips whether or not the escape character is escaped,
# and refusing a name Windows will not take is a different question that this
# break does not touch.
before = """        if letter == ESCAPES_THE_NEXT || letter == NESTS_WITH {"""
after = """        if letter == NESTS_WITH {"""
red = [
    "application::local_folders::tests::test_every_awkward_name_survives_being_stored_and_read_back",
    "application::local_folders::tests::test_the_escape_character_is_itself_escaped",
]
tests_last_seen = [
    { file = "src/application/local_folders.rs", tests = 30 },
]

[[guard]]
name = "the hierarchy separator is read per mailbox rather than assumed for the server"
file = "src/service/protocols/imap.rs"
# IMAP puts a separator on every LIST line, not one on the connection, and a
# server may answer differently for different parts of its namespace. A reader
# that assumed one would split "Work.2026" on a slash, find nothing, and leave
# it at the top level under its whole name; on the other side it would split a
# mailbox the server gave no separator for at all, and file it under a parent
# that does not exist.
#
# The break assumes a slash for every mailbox, which is what a reader who had
# only ever seen one kind of server would write, and is the exact defect the
# comment this replaced was written to defer.
#
# Measured by hand on 2026-08-30 against the whole library: two tests, 5330
# passing beside them. Nothing in mail_sync reddens, and that is the point
# worth recording rather than assuming: store_folders takes the separator it is
# given and splits on it correctly either way, so the whole of the damage
# happens where the separator is read. Its own tests build folders by hand and
# never go near a LIST response, so measuring by expectation would probably
# have named them.
before = """                let carried = delimiter
                    .as_deref()
                    .filter(|d| !d.is_empty())
                    .map(str::to_string);"""
after = """                let carried = Some("/".to_string());"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_mailbox_the_server_gives_no_separator_for_carries_none",
    "service::protocols::imap::against_a_server_that_answers::test_each_mailbox_carries_the_separator_the_server_gave_for_it",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a folder with folders inside it is deleted deepest first"
file = "src/application/folders_underneath.rs"
# RFC 9051 section 6.3.5: "The DELETE command MUST NOT remove inferior
# hierarchical names." That is the exact opposite of RENAME, which section
# 6.3.6 requires to carry them along, and the two sit two sections apart in one
# document. A reader who had just implemented the rename is the most likely
# person to write the walk the wrong way round, because for a rename the order
# genuinely does not matter.
#
# What the break costs: the first DELETE goes to the folder at the top, which
# still has folders inside it. A server answers NO, the walk stops there, and
# nothing at all is deleted. So the visible symptom is a command that never
# works rather than one that destroys anything, which is the better of the two
# and is still a command that never works.
#
# The break reverses the order and nothing else. It is one call, and it is the
# smallest edit that turns a correct walk into a wrong one, which is what makes
# it worth recording: it would read as a tidy-up in a diff.
#
# Measured by hand on 2026-08-30 against the whole library: five tests, and the
# two beyond the three obvious ones are the point. `where_the_rows_move_to`
# reverses this walk to get shallowest first, so a rename's row updates depend
# on this order as well, in the opposite direction. Measuring by expectation
# would have named the three tests about deleting and missed both of those.
before = "    levels.into_iter().rev().flatten().collect()"
after = "    levels.into_iter().flatten().collect()"
red = [
    "application::folders_underneath::tests::test_a_folder_moved_under_another_takes_its_own_shape_with_it",
    "application::folders_underneath::tests::test_a_three_level_tree_comes_back_deepest_first",
    "application::folders_underneath::tests::test_renaming_a_folder_moves_every_row_underneath_it_too",
    "application::folders_underneath::tests::test_the_separator_each_row_already_had_is_the_one_it_keeps",
    "application::folders_underneath::tests::test_two_branches_of_the_same_depth_both_come_back",
    # Added 2026-09-15 by 08-07's sweep: written 2026-08-30, the same day as
    # this record, in a file it had never named.
    "application::emptying::tests::test_the_reach_on_takes_the_folder_and_everything_under_it_deepest_first",
]
tests_last_seen = [
    { file = "src/application/emptying.rs", tests = 23 },
    { file = "src/application/folders_underneath.rs", tests = 13 },
]

[[guard]]
name = "deleting a folder on a server asks the gate before it builds a command"
file = "src/service/protocols/imap.rs"
# The gate is one line and it is the first line, so it is also the easiest line
# in the file to lose to a merge or to a refactor that reorders a body. What it
# costs here is the most of any write in this file: a delete cannot be undone,
# it takes the mail in the folder with it, and it happens at the server, so
# every device on the account loses it too. Mail writes are off for a new
# install, which is exactly the state somebody trying the program is in.
#
# Measured by hand on 2026-08-30 against the whole library: three tests.
#
# The third is the one worth writing down. `test_every_mailbox_write_is_on_the
# _mail_wire_list` reddens not because it knows about this verb but because
# MAIL_TRANSPORTS' imap floor says 11 and removing a gate leaves ten writes that
# ask it. That floor is what turns a lost gate into a failure rather than a
# quiet count, and 01-01 recorded what happens when it is left behind: with a
# spare write above the floor, the copy_message record lost one of its three
# reddening tests and every run stayed green. So the floor rising with each new
# gated write is not bookkeeping, it is this record's third test.
#
# Re-measured on 2026-08-31: four, not three. Plan 02-01 turned that floor into
# an equality and added a second test reading the same census, so a lost gate is
# now noticed twice over. The paragraph above argued that the floor rising with
# each gated write is this record's third test; the equality is the stronger
# form of the same argument, because a floor can only see a count fall and an
# equality sees it move either way.
before = '''        self.may_i("delete a folder on the server")?;
'''
after = ""
red = [
    "service::outward::completeness::test_every_mailbox_write_is_on_the_mail_wire_list",
    "service::outward::completeness::test_the_read_gate_does_not_move_the_count_of_gated_writes",
    "service::protocols::imap::against_a_server_that_answers::test_deleting_a_folder_with_the_gate_closed_says_nothing_to_the_server",
    "service::protocols::imap::against_a_server_that_answers::test_every_mailbox_write_says_nothing_to_the_server_with_the_gate_closed",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
# What the tree remembers, and where the cursor lands after a rebuild, are keyed
# on a row's identity. Finding that row again on screen is the one step that
# still has to go through words, because the binding gives nothing else:
# `TreeItemId` has no equality and its pointer is not public, so two items
# cannot be compared, and the only question a row answers is what it says.
#
# `where_a_row_sits` is what makes that sound. It collects the words of every
# row above one, and `the_row_on_screen` retraces the same chain with
# `get_item_parent`. Two rows under one parent never read the same, so the chain
# is unique where a single label is not.
#
# The break is the whole of the old behaviour: match on the row's own words and
# ignore what is above it. That is what this tree did until now, and since plan
# 01-03 made the stored folder name the leaf it means `Archive/2026` and
# `Work/2026` are one row as far as every lookup is concerned.
#
# Measured by hand on 2026-08-30 against the whole library: three tests.
#
# Re-measured on 2026-08-31, in 01-14, and it is four now. The record had gone
# stale without anything being wrong with it, which is the failure this file's
# both-directions check exists to catch: 01-08 added
# `test_two_accounts_pinned_inboxes_sit_under_a_branch_each_rather_than_side_by_side`
# for D-29, whose second half asks `where_a_row_sits` for the chain above each
# pinned copy and asserts the two differ. That reaches this rule, so it reddens
# here, and nobody re-measured after adding it. It was found by re-running this
# record after touching a neighbouring part of the same module, which is the
# only thing that finds this: no test failed, and the record read as true.
#
# What stayed green, and why, because guessing would have named more.
# `test_a_top_level_row_sits_under_nothing_but_itself` is correct either way: a
# row at the top level has nothing above it, so its chain really is its own
# label and the break cannot change it. Every test about identities themselves
# stays green too, because `WhichRow::stored` is untouched by this: the
# identities are still distinct, and the break is that nothing can work out
# which of them the row on screen belongs to.
#
# The test that shows the real cost is not here, because it needs a control.
# `tests/folder_tree_rows_pair_with_the_control.rs` puts the cursor on
# `Sent/2026`, applies this same break, and reads the row back as
# `Archive/2026`: the cursor on one folder and the program acting on another.
# `scripts/guards.sh` runs `cargo test --lib` and never builds that.
name = "the row on screen is found by the words above it and not by its own"
file = "src/presentation/folder_tree.rs"
before = """    for above in rows[..at].iter().rev() {"""
after = """    for above in rows[..0].iter().rev() {"""
#
# A fifth arrived on 2026-09-02 with 02.1-08, and it is the ordinary way a
# record falls behind: that plan gave two same-named accounts distinct labels,
# and the test holding that property asks it through this same walk. Nobody
# working on account labels would have filtered for a record about pairing a
# row back to a control. The count check caught it and the run named it.
red = [
    "presentation::folder_tree::accounts_that_read_alike::test_no_two_rows_of_a_tree_with_two_same_named_accounts_sit_in_one_place",
    "presentation::folder_tree::tests::test_every_row_of_a_tree_sits_somewhere_no_other_row_does",
    "presentation::folder_tree::tests::test_two_accounts_pinned_inboxes_sit_under_a_branch_each_rather_than_side_by_side",
    "presentation::folder_tree::tests::test_two_folders_reading_the_same_are_told_apart_by_what_is_above_them",
    "presentation::folder_tree::tests::test_where_a_row_sits_names_every_row_above_it_and_itself_last",
]
tests_last_seen = [
    { file = "src/presentation/folder_tree.rs", tests = 97 },
]

[[guard]]
# No row of the folder tree hangs its identity off the control.
#
# `wxdragon` keeps tree item data in a process-global map. `store_item_data`
# inserts into a static registry, `delete_all_items` calls the raw FFI and
# removes nothing from it, and `cleanup_all_custom_data`, the escape hatch,
# removes nothing either: it walks the tree through `clean_item_and_children`,
# and that function calls `remove_item_data` nowhere at all, for a leaf or for a
# branch. This tree is emptied and rebuilt whenever a sync finishes, on a timer,
# so one call here leaks an entry per folder per sync for the life of the
# process.
#
# This is a source read rather than a behaviour test because this tree is drawn
# inside a running application, so no test can fence a build of it and count
# what it left. It reads the shipping half of each file with `what_ships`,
# because this codebase has test modules sitting between stretches of shipping
# code and cutting at the first `#[cfg(test)]` would call the rest clean.
#
# Its exception list is empty as of 02.1-05. The two dialogs on it, the
# destination picker and the conversation view, hold their rows beside the
# control now.
#
# The break is the obvious way somebody would write this feature: put the
# identity on the item, where it looks like it belongs.
#
# Measured by hand on 2026-08-30 against the whole library: one test.
#
# One, and the reason given here for that was wrong. It said the leak has no
# observable behaviour, so a source read was the only thing that could see it.
# The leak is observable: `wxdragon`'s registry hands out its keys from a
# monotonic counter and `store_item_data` and `get_item_data` are both public,
# so a throwaway entry either side of a build fences the keys that build issued
# and asking the registry for each of them counts what is still there.
# `tests/tree_rows_leave_no_registry_entry.rs` does that, and the two records at
# the bottom of this file are its measurements.
#
# What is true is narrower and still holds for this record: this tree is drawn
# inside a running application rather than by a builder a test can call, so
# nothing can fence a build of it, and the source read is what stands between it
# and an unbounded leak. If this record ever reddens nothing, the check has
# stopped reading the file rather than the file having stopped calling it, and
# `test_the_reading_can_see_such_a_call_when_there_is_one` beside it is what
# says which.
name = "no row in the folder tree hangs data off the control"
file = "src/presentation/wx_app.rs"
before = """        if row.expandable && !collapsed.contains(&row.identity.stored()) {"""
after = """        tree.set_custom_data(&item, row.identity.stored());
        if row.expandable && !collapsed.contains(&row.identity.stored()) {"""
red = [
    "presentation::folder_tree::nothing_hangs_off_the_control::test_no_row_of_a_tree_hangs_its_identity_off_the_control",
]
tests_last_seen = [
    { file = "src/presentation/folder_tree.rs", tests = 97 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
# An exception to the settings guard that has stopped being true is noticed.
#
# `test_every_setting_somebody_can_change_is_offered_by_a_screen` asks whether
# every stored setting appears on the settings screen, and three settings
# honestly do not: they are offered by the account manager or by a menu item
# instead. Each is excepted by name, and each exception names the file that
# offers it.
#
# An exception list is the part of a check most likely to go quietly wrong,
# because nothing re-asks whether its reasons still hold. Take the control out
# of the account manager and the setting becomes stored and offered by nothing,
# which is exactly the defect the guard exists for, while the guard sits there
# green because an exception says not to look.
#
# The break points one exception at a file that does not offer the setting,
# which is what deleting the control amounts to as far as any reader of source
# text can tell.
#
# Measured by hand on 2026-08-30 against the whole library: one test.
name = "a stored setting that no screen offers is caught"
file = "src/data/config.rs"
before = """        ("directories", "src/presentation/wx_account_manager.rs"),"""
after = """        ("directories", "src/presentation/wx_settings.rs"),"""
red = [
    "data::config::every_setting_is_acted_on::test_a_setting_said_to_be_offered_elsewhere_really_is",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
]

[[guard]]
# Moving an account never reaches a server.
#
# D-14 says where somebody's accounts sit is a fact about their own list, and
# FOLDER-03 says the same of pinning a folder. No mail protocol has a notion of
# either, so there is nothing to send and nowhere to send it, and a command that
# quietly opened a connection to record a preference would be doing something
# nobody asked for on a metered or a watched network.
#
# A source read rather than a behaviour test, and the reason is that the thing
# being defended is an absence. A connection nobody opened leaves no trace for a
# test to find, so there is no behaviour to assert; the only question that can be
# asked is whether the code says so. It reads the shipping half of both files
# with `what_ships`, and matches call syntax rather than bare words, because the
# paragraphs explaining this rule name every one of the things it forbids.
#
# The break is the obvious way somebody would get this wrong: reach for the mail
# layer from inside the command, as every other command on that menu does.
#
# Measured by hand on 2026-08-30 against the whole library: one test.
#
# One, and that is what it should be. `test_the_reading_can_see_such_a_call_when_there_is_one`
# beside it is what says whether a run that reddens nothing means the file
# stopped calling out or the check stopped reading.
name = "moving an account never reaches a server"
file = "src/presentation/wx_app.rs"
before = """    let after = moved(&in_order, &which, direction);"""
after = """    let _ = crate::service::protocols::imap::mailbox_name::encode(&which);
    let after = moved(&in_order, &which, direction);"""
red = [
    "application::account_order::nothing_here_reaches_a_server::test_moving_an_account_makes_no_call_that_leaves_this_machine",
]
tests_last_seen = [
    { file = "src/application/account_order.rs", tests = 11 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
# The merge never puts away a folder that still holds mail.
#
# `folders` cascades to `messages`, so deleting a folder row destroys every
# message still in it. The merge deletes each per-account Sent, Outbox, Drafts,
# Junk and Trash once its mail has landed in the shared one, and "once its mail
# has landed" is the whole of what makes that safe.
#
# It is not a theoretical case. The loop carries on past a message it could not
# move, deliberately: one unmovable message in one account's Trash would
# otherwise leave every other account's mail unmerged on every open for ever.
# So a folder with something left in it is a state the code really reaches, and
# the emptiness test is what stands between that and losing the mail this
# program holds the only copy of.
#
# The test is part of the DELETE rather than a check before it. Asking first
# and deleting second is two statements with a gap, and the answer can change
# in the gap.
#
# The break is the one somebody would write while tidying: the folder is empty
# by now, so why ask.
#
# Measured by hand on 2026-08-30 against the whole library: two tests.
name = "the merge never puts away a folder that still holds mail"
file = "src/data/message_cache/shared_folders.rs"
before = """                    "DELETE FROM folders
                     WHERE id = ?1
                       AND NOT EXISTS (SELECT 1 FROM messages WHERE folder_id = ?1)","""
after = """                    "DELETE FROM folders
                     WHERE id = ?1","""
red = [
    "data::message_cache::shared_folders::tests::test_a_folder_that_still_holds_mail_is_never_put_away",
    "data::message_cache::shared_folders::tests::test_a_merge_that_stops_partway_has_landed_what_it_moved_and_lost_nothing",
]
tests_last_seen = [
    { file = "src/data/message_cache/shared_folders.rs", tests = 11 },
]

[[guard]]
# A moved message is given a number the folder it lands in is not already using.
#
# `messages` carries `UNIQUE(folder_id, uid)`. IMAP numbers are per mailbox and
# POP ones per download, so two accounts' local Trash both holding uid 42 is
# expected rather than hypothetical, and the merge of the local folders brings
# exactly those two together. Carrying the old number across loses the second
# message: D-40 is the decision that it must not.
#
# The number is not chosen in the merge. It comes from `move_message`, which is
# where an ordinary move gets one too, and that is the point of the record: the
# guard sits on the shared path, so the three tests below it that are about
# ordinary moves redden alongside the two that are about the merge. A copy of
# this logic written separately for the migration would have been a second
# answer to one question, and it would have had to be guarded twice.
#
# The break is the one the plan for this work actually proposed, on the belief
# that nothing here had ever rewritten a uid: keep the number the message
# already had.
#
# Measured by hand on 2026-08-30 against the whole library: five tests, three of
# them about ordinary moves rather than about the merge. That spread is the
# evidence the two really do share one implementation.
#
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: a sixth test went red, in a third family
# again. Emptying a folder moves its messages to the trash through this same
# path, so the spread the paragraph above calls evidence is wider than it was
# when that paragraph was written.
name = "a moved message is renumbered for the folder it lands in"
file = "src/data/message_cache/messages.rs"
before = """        let was = self.uid_of(message_id)?;
        let numbering = self.numbering_in(into_folder)?;
        let uid = match numbering {"""
after = """        let was = self.uid_of(message_id)?;
        let numbering = self.numbering_in(into_folder)?;
        let uid = was.unwrap_or(1);
        let _unused = match numbering {"""
red = [
    "application::local_delete::tests::test_emptying_several_folders_does_them_all_and_names_them_in_order",
    "application::moves_waiting::tests::test_a_crossing_made_here_moves_the_row_into_the_other_accounts_folder_and_waits",
    "application::moves_waiting::tests::test_two_crossings_are_replayed_in_the_order_they_were_asked",
    "data::message_cache::messages::tests::test_a_message_filed_into_a_folder_a_server_fills_leaves_the_servers_numbers_alone",
    "data::message_cache::messages::tests::test_a_message_moved_into_a_folder_on_this_computer_still_counts_upward",
    "data::message_cache::messages::tests::test_two_messages_moved_into_one_folder_do_not_collide",
    "data::message_cache::moves_waiting::tests::test_the_row_is_put_where_the_server_holds_it_with_the_marker_off",
    "data::message_cache::shared_folders::tests::test_afterwards_no_account_keeps_a_copy_of_a_folder_that_is_shared",
    "data::message_cache::shared_folders::tests::test_two_messages_numbered_the_same_both_survive_and_one_records_its_old_number",
]
tests_last_seen = [
    { file = "src/application/local_delete.rs", tests = 15 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/data/message_cache/moves_waiting.rs", tests = 15 },
    { file = "src/data/message_cache/shared_folders.rs", tests = 11 },
]
# Re-measured 2026-09-20 by 11-11.1.3 when the count check named it for a
# test added to `messages.rs`: three short, none of them that plan's. All
# three are 11-07.1's and 11-07.2's of 2026-09-19, in `moves_waiting` on
# both layers, new modules no record named, so no count check fired for
# them; a move made here renumbers the row through this function and a
# crossing does it twice.

[[guard]]
# D-32 says a rename keeps a pin and a real deletion takes it. A rename
# rewrites a folder's path, `set_folder_path` says so in as many words, so a
# pin stored against `(account_id, path)` is orphaned by the rename D-32
# promises it survives unless the database moves it. `ON UPDATE CASCADE` is
# what moves it, and it is one clause of one statement that nothing else in
# the tree would notice missing.
#
# This is the clause the shape of the table turned on. The plan for this work
# specified the key and never mentioned the cascade, and without it the
# rename half of D-32 is quietly false while every other test stays green.
#
# The break is the shape the plan described: the key without the clause that
# makes it survive a rename.
#
# Measured by hand on 2026-08-30 against the whole library: one test.
name = "a renamed folder keeps its pin because the database moves it"
file = "src/data/message_cache/mod.rs"
before = """                FOREIGN KEY (account_id, path) REFERENCES folders(account_id, path)
                    ON DELETE CASCADE ON UPDATE CASCADE"""
after = """                FOREIGN KEY (account_id, path) REFERENCES folders(account_id, path)
                    ON DELETE CASCADE"""
red = [
    "data::message_cache::folders::tests::test_a_renamed_folder_keeps_its_pin_under_its_new_name",
]
tests_last_seen = [
    { file = "src/data/message_cache/folders.rs", tests = 61 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
# The other half of D-32, and T-01-33. A pin that outlived its folder would
# pre-pin whatever turned up at that path next, which for a mailbox name a
# server reuses is somebody else's folder sitting in somebody's favourites.
#
# It is done in the same statement that removes the folder rather than by a
# sweep afterwards, so there is no window in which a pin names a folder
# nothing has. That is why the break is the clause and not a missing call:
# there is no call to leave out.
#
# Measured by hand on 2026-08-30 against the whole library: two tests, the
# second of which is the one that matters, since a pin merely hidden from the
# read would still pass the first.
name = "a folder that is really gone takes its pin with it"
file = "src/data/message_cache/mod.rs"
before = """                FOREIGN KEY (account_id, path) REFERENCES folders(account_id, path)
                    ON DELETE CASCADE ON UPDATE CASCADE"""
after = """                FOREIGN KEY (account_id, path) REFERENCES folders(account_id, path)
                    ON UPDATE CASCADE"""
red = [
    "data::message_cache::folders::tests::test_a_folder_made_again_at_the_same_path_is_not_pinned_already",
    "data::message_cache::folders::tests::test_a_folder_that_is_really_gone_takes_its_pin_with_it",
]
tests_last_seen = [
    { file = "src/data/message_cache/folders.rs", tests = 61 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
# D-30 makes pinning a copy: the folder stays in its account's branch and turns
# up at the top as well. The obvious implementation of a favourites group is
# the other one, where pinning moves a folder, and it is what most other mail
# programs do, so this is the shape somebody would reintroduce while tidying
# rather than while breaking something.
#
# It matters because a move cannot be undone by unpinning without knowing where
# the folder used to be. A copy can: the original was never touched.
#
# The break is that implementation, filtering a pinned folder out of the branch
# it belongs to.
#
# Measured by hand on 2026-08-30 against the whole library: three tests. Two of
# the three are about closing and about unpinning rather than about the copy
# itself, which is the evidence that D-30 is load-bearing for more than the one
# rule it states.
name = "a pinned folder is still in its own account branch"
file = "src/presentation/folder_tree.rs"
before = """        let mine: Vec<&FolderInTheTree> = folders
            .iter()
            .filter(|folder| folder.account == account.id)
            .collect();"""
after = """        let mine: Vec<&FolderInTheTree> = folders
            .iter()
            .filter(|folder| {
                folder.account == account.id
                    && !pins.iter().any(|pin| {
                        pin.account == folder.account && pin.path == folder.path
                    })
            })
            .collect();"""
red = [
    "presentation::folder_tree::tests::test_a_pinned_folder_is_still_in_its_own_account_branch",
    "presentation::folder_tree::tests::test_the_group_and_the_account_branch_are_closed_apart",
    "presentation::folder_tree::tests::test_unpinning_leaves_the_account_branchs_row_untouched",
]
tests_last_seen = [
    { file = "src/presentation/folder_tree.rs", tests = 97 },
]

[[guard]]
# FOLDER-03 says pinning writes only on this computer, never to the server, and
# never passes through `Allowed`. What is being defended is an **absence**, and
# an absence leaves nothing for a behaviour test to count: there is no call to
# instrument, so a test asserting "zero calls were made" passes identically
# against a working pin and against a body that does nothing at all.
#
# So the check reads the source of the pinning path instead, in call syntax
# rather than bare words, the same shape `account_order` uses for the same
# reason. Its companion tests prove the reading can see such a call, that it
# does not fire on the paragraphs explaining the rule, and that reading one
# function of wx_app.rs stops at that function rather than running on into the
# many places that do reach a server.
#
# The break puts the service layer on the pinning path. It is a mention rather
# than a live call because the break has to compile, and a mention is exactly
# what the check reads for; the companion beside it is what ties that reading
# to a real call.
#
# Measured by hand on 2026-08-30 against the whole library: one test.
name = "pinning a folder never reaches a server"
file = "src/presentation/wx_app.rs"
before = """    let done = match pinning {
        true => cache.pin_row(&account, &path),
        false => cache.unpin_row(&account, &path),
    };"""
after = """    let _ = std::mem::size_of::<crate::service::protocols::imap::ImapFolder>();
    let done = match pinning {
        true => cache.pin_row(&account, &path),
        false => cache.unpin_row(&account, &path),
    };"""
red = [
    "application::favourites::nothing_here_reaches_a_server::test_pinning_a_folder_makes_no_call_that_leaves_this_machine",
]
tests_last_seen = [
    { file = "src/application/favourites.rs", tests = 19 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

[[guard]]
# D-33 and threat T-01-36. Emptying a folder is a great many deletes, so the
# question "does this remove the message or move it to the Trash" is one the
# emptying module could easily answer for itself. It must not: `deleting`
# already carries the per-account "Let me delete mail on this computer"
# permission, so routing through it means emptying is gated without a second
# gate existing for somebody to forget, and it already knows a message in the
# Trash has nowhere further to go.
#
# The break writes that rule again here, in the shape somebody reaching for it
# would write: ours if the path is local, the server's otherwise. It compiles,
# it keeps the same four-arm match underneath, and it drops both the permission
# and the move-versus-remove rule, which is exactly what a second answer to this
# question loses.
#
# Measured by hand on 2026-08-30 against the whole library: three tests.
name = "emptying asks the one function that decides what deleting means"
file = "src/application/emptying.rs"
before = """    match local_folders::deleting(folder, protocol, Deleting::ToTrash, allowed) {
        Some(LocalDelete::Refuse(why)) => WhatEmptyingDoes::Refuse(why),"""
after = """    match match local_folders::is_local(folder) {
        true => Some(LocalDelete::RemoveFromThisComputer),
        false => None,
    } {
        Some(LocalDelete::Refuse(why)) => WhatEmptyingDoes::Refuse(why),"""
red = [
    "application::emptying::tests::test_a_refusal_carries_the_words_the_delete_gate_supplies",
    "application::emptying::tests::test_emptying_the_trash_removes_and_emptying_the_inbox_moves",
    "application::emptying::tests::test_the_permission_gates_emptying_here_and_says_nothing_about_a_folder_on_a_server",
]
tests_last_seen = [
    { file = "src/application/emptying.rs", tests = 23 },
]

[[guard]]
# D-37 and threats T-01-39 and T-01-40. The number in front of the Empty
# confirmation is what this computer is holding, counted at the moment the
# question is asked. `folders.total_count` is the obvious thing to reach for
# instead: it is one column, already read, and it is a different question with
# a stale answer, written by the last sync. A confirmation quoting it would be
# describing a folder as it was, and the direction that matters is understating
# what is about to be destroyed.
#
# The break reads that column. It compiles and it returns a number, which is
# what makes it worth guarding: nothing about the shape of the code says which
# of the two questions was asked.
#
# Ten tests, and two of them are the reason this record is worth keeping.
# `local_delete`'s report counts what was left behind through this same
# function, so breaking the count breaks the sentence a stopped empty says, in
# a module whose name has nothing to do with counting. Neither would have been
# guessed from reading the call site.
#
# Measured by hand on 2026-08-30 against the whole library: nine tests. A tenth
# was added on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own. It asks whether marking a folder gone
# leaves the message rows alone, which is a third question this one count
# answers.
name = "the count in front of the confirmation comes from this computer and makes no round trip"
file = "src/data/message_cache/messages.rs"
before = """                    "SELECT COUNT(*) FROM messages WHERE folder_id = ?1 AND deleted = 0","""
after = """                    "SELECT total_count FROM folders WHERE id = ?1","""
red = [
    "application::local_delete::tests::test_emptying_a_folder_here_moves_every_message_to_the_trash",
    "application::local_delete::tests::test_emptying_with_the_permission_off_moves_nothing_and_says_which_setting",
    "data::message_cache::folders::tests::test_marking_a_folder_gone_changes_no_message_row",
    "data::message_cache::messages::tests::test_a_folder_holding_nothing_counts_nothing_and_so_does_no_folder_at_all",
    "data::message_cache::messages::tests::test_a_message_already_deleted_is_neither_counted_nor_walked_again",
    "data::message_cache::messages::tests::test_emptying_the_same_folder_twice_finds_nothing_the_second_time",
    "data::message_cache::messages::tests::test_the_count_in_front_of_the_confirmation_adds_up_every_folder_it_is_given",
    "data::message_cache::messages::tests::test_the_count_is_the_messages_really_here_and_not_the_number_the_last_sync_wrote",
    "data::message_cache::messages::tests::test_the_count_leaves_out_folders_it_was_not_given",
    "data::message_cache::messages::tests::test_the_rows_an_empty_walks_are_the_rows_the_count_counted",
]
tests_last_seen = [
    { file = "src/application/local_delete.rs", tests = 15 },
    { file = "src/data/message_cache/folders.rs", tests = 61 },
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
# D-27 and threat T-01-42. A folder the server has stopped listing is marked and
# nothing else. The mail cached in it stays where it is, readable, until
# somebody has been asked and has answered, and that is the whole reason a modal
# raised by a background sync was acceptable at all: the worst outcome of the
# question never being answered is a tree that tells the truth about a folder.
#
# A sync runs on a timer, so anything this destroyed would be destroyed with
# nobody there to have agreed to it. And `folders` cascades into `messages`, so
# the destructive version of this is one statement away at all times, which is
# what makes it worth writing down.
#
# The break makes marking a folder gone delete the mail in it. It compiles, it
# is one statement, and it leaves the folder in the tree, so nothing about the
# shape of the code says which of the two was written.
#
# Measured by hand on 2026-08-30 against the whole library: four tests. Three of
# them are about the mark reading back rather than about the mail, because the
# break also stops the mark being written; the count is the one that is about
# the rule, and the other three are what says the break really applied.
name = "nothing is removed for a folder the server stopped listing until somebody has answered"
file = "src/data/message_cache/folders.rs"
before = """                "UPDATE folders SET the_server_stopped_listing_it = ?1 WHERE id = ?2","""
after = """                "DELETE FROM messages WHERE folder_id = ?2 AND ?1 IS NOT NULL","""
red = [
    "data::message_cache::folders::tests::test_a_folder_that_came_back_stops_being_marked_gone",
    "data::message_cache::folders::tests::test_a_folder_the_server_stopped_listing_reads_back_and_one_it_still_lists_does_not",
    "data::message_cache::folders::tests::test_marking_a_folder_gone_changes_no_message_row",
    "data::message_cache::folders::tests::test_one_accounts_gone_folders_are_not_another_accounts",
]
tests_last_seen = [
    { file = "src/data/message_cache/folders.rs", tests = 61 },
]

[[guard]]
# D-27 and threat T-01-43. One question about every folder waiting, never one
# per folder and never one per account syncing at the same moment. That is a
# property of there being one set rather than a queue of windows, so the break
# is at the set: it keeps the last folder added instead of adding to what is
# there.
#
# The failure it defends against is not hypothetical here. Reminder alerts
# opened one over another about a minute apart, each covering the one being
# read, because the clock that opened them kept running inside the modal. A
# sync is a timer too, and five folders going missing at once is the ordinary
# case rather than the odd one: a server that has moved its mailboxes moves all
# of them.
#
# Measured by hand on 2026-08-30 against the whole library: four tests. Two are
# about the question covering the whole set and two are about how it words the
# accounts, which only differ once more than one folder is in it. A weaker
# break, truncating the list of ids where the question is built, reddened two of
# these four and was set aside for this one.
name = "one question is asked about every folder waiting rather than one per folder"
file = "src/presentation/one_question_at_a_time.rs"
before = """                self.waiting.push(folder);"""
after = """                self.waiting = vec![folder];"""
red = [
    "presentation::one_question_at_a_time::tests::test_a_question_naming_two_accounts_says_which_account_each_folder_is_in",
    "presentation::one_question_at_a_time::tests::test_five_folders_going_missing_in_one_sync_produce_one_question_naming_five",
    "presentation::one_question_at_a_time::tests::test_one_accounts_folders_are_not_read_out_with_the_account_after_each",
    "presentation::one_question_at_a_time::tests::test_two_accounts_syncing_at_the_same_time_produce_one_question",
]
tests_last_seen = [
    { file = "src/presentation/one_question_at_a_time.rs", tests = 21 },
]
[[guard]]
# D-02, and the mechanism rather than the wording. A conversation row's value
# and the value the list sorts by have to be one thing, and what makes them one
# is that `conversations_query` fills every field from the same method the
# ORDER BY is built from. Break that and the two can differ while both still
# look right on their own.
#
# So the break fills one field from another column's arm. The Unread field then
# holds how many messages there are while the ORDER BY still uses how many are
# unread: the list comes back in the right order and every row says "Unread",
# which is the shape this is about. A sort that is right and a cell that is
# wrong reads as a working list.
#
# Measured by hand on 2026-08-30 against the whole library: three tests. The
# agreement test catches it through its own check that the two rows read
# differently, which is the check that stops an ordering proving anything by
# luck. The count test catches the number itself. The query test catches the
# missing expression, because a rule that fills no field is not in the query at
# all.
name = "a conversation column's display and its sort come from the same enum arm"
file = "src/data/message_cache/messages.rs"
before = """        unread = MessageColumn::Unread.conversation_sort_expression(),"""
after = """        unread = MessageColumn::Thread.conversation_sort_expression(),"""
red = [
    "data::message_cache::messages::tests::test_every_value_in_the_conversation_query_is_bound_rather_than_written_in",
    "data::message_cache::messages::tests::test_sorting_by_a_column_agrees_with_what_that_column_says",
    "data::message_cache::messages::tests::test_the_unread_count_is_the_unread_messages_by_the_same_reach",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
# D-08 and threat T-01-50. On Gmail a label is a mailbox and All Mail holds a
# copy of every message, so counting a conversation across the account without
# leaving those copies out reports twice its size, on the largest provider there
# is, for every conversation. The number stays plausible while being wrong,
# which is worse than one that is obviously wrong.
#
# The rule moved on 2026-09-04 and this record moved with it. It used to be an
# exclusion of the folder, in the reach; the break was that exclusion made
# always true. Excluding the folder also excluded the messages that are in it
# and nowhere else, which is what archiving without a label leaves behind, so
# the exclusion is now over the rows that are really duplicates: an all-mail row
# whose message some other folder in reach already holds.
#
# The break has the same shape as the one it replaces. It leaves the join and
# the clause in place and makes the condition always true, so only the exclusion
# goes and nothing reddens for a second reason.
#
# Measured by hand on 2026-09-04 against the whole library: five tests. The two
# written for this rule, and three of the four written for the archived-mail one
# next to it, because the fixture there has a labelled message whose copy is in
# All Mail and every number it asserts moves by one when that copy is counted
# again. The one that does not move is the presence assertion: a conversation
# that was in the list is still in the list when something is counted twice.
#
# Re-measured an hour later in the same session, because the plan's second task
# added tests that reach this rule. Five became seven: both of the tests about a
# copy on a server that offers no identifier of its own, which is this same rule
# asked of the other arm of `WHICH_MESSAGE_THIS_ROW_IS`. Nothing about this
# guarded code changed between the two measurements.
name = "a folder holding a copy of every message is left out of a conversation count"
file = "src/data/message_cache/messages.rs"
# The break's text moved on 2026-09-20 when 11-11.1.3 keyed a copy by its
# account as well as its message, so the one scope could hold every inbox
# at once; the break is the same, the exclusion taken out.
before = """               AND (mf.holds_all_mail = 0
                    OR mf.account_id || ':' || {WHICH_MESSAGE_THIS_ROW_IS}
                       NOT IN (SELECT message FROM elsewhere))"""
after = """               AND (mf.holds_all_mail = 0 OR mf.holds_all_mail = 1)"""
red = [
    "data::message_cache::messages::tests::test_a_copy_on_a_server_that_gives_no_identifier_of_its_own_is_counted_once",
    "data::message_cache::messages::tests::test_a_delete_reaches_the_message_archived_with_no_label",
    "data::message_cache::messages::tests::test_a_folder_holding_a_copy_of_every_message_does_not_double_the_count",
    "data::message_cache::messages::tests::test_a_message_archived_with_no_label_counts_toward_its_conversation",
    "data::message_cache::messages::tests::test_a_row_still_holding_the_bracketed_spelling_is_the_same_message_as_its_copy",
    "data::message_cache::messages::tests::test_the_row_is_still_there_when_the_folder_being_read_is_the_all_mail_one",
    "data::message_cache::messages::tests::test_the_unread_count_holds_a_message_archived_with_no_label",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
# D-01 and threat T-01-52. This one number is the whole of the accessibility
# argument for keeping the message list a native virtual control. Virtual mode
# means the control's size is whatever it is told, and that size is what UI
# Automation reports, so it is what a screen reader says when it announces "3
# of 40". Told the message count while forty conversation rows are on screen, it
# announces a mailbox five times the size of the one being read, and nothing on
# screen looks wrong to anybody who can see it.
#
# The break is in the rule rather than at the call site. Breaking the call site
# instead only reddens a source-reading test, which proves that a line changed
# and not that an answer did.
#
# Measured by hand on 2026-08-30 against the whole library at 5761 tests: one
# test. One, because the count in message mode is the same expression either
# way, so the break cannot reach the other half of the rule.
name = "the message list is told how many conversations it is showing"
file = "src/presentation/view_state.rs"
before = """        Showing::Conversations => conversations,"""
after = """        Showing::Conversations => messages,"""
red = [
    "presentation::view_state::tests::test_the_count_in_conversation_mode_is_the_conversation_count",
]
tests_last_seen = [
    { file = "src/presentation/view_state.rs", tests = 57 },
]

[[guard]]
# D-11. Switching to conversations and back must give exactly the messages that
# were selected, and the obvious implementation loses it: a conversation cannot
# say which of its messages was chosen, so recomputing the message selection
# from the conversation selection turns two of five into five. Somebody who
# picked two messages and pressed Delete would destroy three they never picked.
#
# The break is that recomputation, written as what it really amounts to: every
# row of the list that the kept conversations cover. It is deliberately not a
# body that returns nothing, because an empty answer is the shape an absence
# assertion is already vacuously green against.
#
# Measured by hand on 2026-08-30 against the whole library at 5761 tests: two
# tests. The second is the one that reads as vacuous on its own, an empty
# selection surviving as empty, and it is red here because this break invents a
# selection where there was none. That is the pairing paying off rather than a
# coincidence: a negative assertion earns its place by going red under a break
# that a positive one cannot see.
name = "switching the view back selects the messages that were selected, not their whole conversations"
file = "src/presentation/view_state.rs"
before = """    let mut rows: Vec<usize> = kept
        .messages()
        .iter()
        .filter_map(|chosen| message_ids_in_row_order.iter().position(|id| id == chosen))
        .collect();"""
after = """    let mut rows: Vec<usize> = (0..message_ids_in_row_order.len()).collect();"""
red = [
    "presentation::view_state::tests::test_a_selection_of_nothing_survives_as_nothing",
    "presentation::view_state::tests::test_two_of_a_conversation_s_five_messages_come_back_as_those_two",
]
tests_last_seen = [
    { file = "src/presentation/view_state.rs", tests = 57 },
]

[[guard]]
name = "a merged conversation settles on the same name whatever order its messages arrived in"
file = "src/application/thread_identity.rs"
# Pitfall 6 and D-39, moved here on 2026-09-04 because the break it used to
# carry stopped discriminating that day, and the reason is worth the whole of
# this comment.
#
# It used to live in `merge_what_this_message_connects` and read: the defect is
# an incremental join that adopts the conversation id it happened to find,
# rather than the one the arriving message's own chain names, because adoption
# disagrees with the derivation and the same set of messages then comes out
# under different names depending on which arrived first. Its break was that
# adoption, written as `found.last().map_or(conversation, ...)`. Measured on
# 2026-08-31 it reddened three tests.
#
# Plan 03-05 changed the winner from the arriving message's own conversation to
# the earliest identifier among all of them, because the old rule had no answer
# for a chain naming only its parent and gave a different one depending on
# arrival order. That makes adoption harmless: the winner is now a function of
# the SET of conversations being merged, the arriving message's own is always in
# that set because its row is written before the lookup is asked, and adopting
# any member of a set does not change the set. Measured, not assumed: run
# against the tree of 2026-09-04 the old break left all three of its named tests
# green.
#
# So the defect it guarded is not unguarded, it is impossible, and what replaced
# it is the property below: the answer depends on the set and not on the order.
# The record is repointed rather than deleted, so the file keeps the reason.
#
# The break puts the old rule back, which is the most plausible way somebody
# would undo this: take the first of the list, which is the arriving message's
# own conversation, instead of the earliest.
#
# Measured by hand on 2026-09-04 against the whole library, which passes 6124 at
# 0 failed with 1 ignored on `cargo test --lib`: three tests, and each one is a
# different reason. The chain naming only its parent disagrees across arrival
# orders, which is the case the old rule could not answer. The root arriving
# late disagrees across arrival orders, which is the case that only became
# reachable when the identifier table arrived. And the winner test asserts the
# rule directly.
#
# Nothing in `data::message_cache` reddens, which is the same finding from the
# other side: the storing path cannot tell the two rules apart, because both are
# asked the same set.
red = [
    "application::thread_identity::tests::a_chain_naming_only_its_parent_agrees_in_every_arrival_order",
    "application::thread_identity::tests::a_late_message_merging_two_conversations_agrees_in_every_arrival_order",
    "application::thread_identity::tests::the_winner_is_the_earliest_identifier_the_merging_conversations_know_between_them",
]
tests_last_seen = [
    { file = "src/application/thread_identity.rs", tests = 39 },
]
#
# Rewritten onto the new text on 2026-09-19 (11-08.1): the earliest rule now
# sits inside the closure the server's word falls through to, and the break
# is the same one place, `.min()` to `.next()`, one level further in.
before = """        merging
            .iter()
            .copied()
            .min()"""
after = """        merging
            .iter()
            .copied()
            .next()"""

[[guard]]
name = "a merge reaches the losing conversation in every folder it is filed in"
file = "src/data/message_cache/messages.rs"
# THREAD-02. A conversation is not a folder's property: its messages sit in
# whatever folders they were filed into, and the folder being synced is only
# one of them. A rewrite that stops at that folder leaves the same conversation
# under two names, which is the exact defect merging exists to remove, and it
# leaves it in the half nobody was looking at.
#
# The break keeps the account scoping and takes one folder out of it, which is
# what "only the folder being synced" amounts to. Deliberately not a break that
# removes the rewrite: that would redden every merge test and prove only that
# merging happens at all.
#
# Measured by hand on 2026-08-31 against the whole library, which passes 5803
# at 0 failed with 1 ignored on `cargo test --lib`:
# two tests. The second was not expected and is the better of the pair. The batch
# test seeds two folders because it is comparing one storing path against
# another, and one folder each is enough to catch a rewrite that stops at the
# first: the two paths come out under different names, and the test that is
# nominally about batching fails for a reason that is nothing to do with
# batching.
red = [
    "data::message_cache::messages::tests::test_a_conversation_gets_the_same_id_stored_as_a_batch_or_one_at_a_time",
    "data::message_cache::messages::tests::test_the_merge_reaches_the_losing_conversation_wherever_it_is_filed",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]
before = """               AND folder_id IN (SELECT id FROM folders WHERE account_id = ?)\""""
after = """               AND folder_id IN (SELECT id FROM folders WHERE account_id = ? LIMIT 1)\""""

[[guard]]
name = "a merge stays inside one account"
file = "src/data/message_cache/messages.rs"
# T-01-56. A conversation's root is a `Message-ID` a stranger chose, so it can
# collide with another account's by accident or on purpose. Merging across
# accounts on one is a disclosure: mail from one account appears inside a
# conversation being read in another, and the thing that decided it was a
# header somebody else wrote.
#
# The break keeps the parameter and stops it meaning anything, so the shape of
# the statement and its bound values are untouched and only the scoping goes. A
# break that dropped the clause outright would change the parameter arithmetic
# and redden for a second reason.
#
# Measured by hand on 2026-08-31 against the whole library, which passes 5803
# at 0 failed with 1 ignored on `cargo test --lib`:
# one test, and one is right. The two accounts in that test share a conversation
# root and nothing else, so no other test can see this clause at all: every
# other merge test lives inside one account, where the clause is true whether
# it is enforced or not.
red = [
    "data::message_cache::messages::tests::test_a_conversation_in_another_account_is_never_merged_into_this_one",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]
before = """               AND folder_id IN (SELECT id FROM folders WHERE account_id = ?)\""""
after = """               AND ? IS NOT NULL\""""

[[guard]]
name = "an arrival repaints the rows that changed and not the ones that did not"
file = "src/presentation/view_state.rs"
# THREAD-02's criterion that rethreading on arrival "does not re-announce rows
# the user is not on". The list is virtual, so it re-announces every row it is
# told to refresh; repainting rows that did not change is therefore not waste,
# it is the failure.
#
# The break is in the rule and not at the call site, for the reason the
# neighbouring D-01 record gives: breaking the call site reddens a
# source-reading test, which proves that a line changed and not that an answer
# did. Here the rule says every row changed, which is what repainting the whole
# list amounts to, and the call site is left exactly as it is.
#
# Measured by hand on 2026-08-31 against the whole library, which passes 5803
# at 0 failed with 1 ignored on `cargo test --lib`:
# three tests. The third is the one worth reading, and it is the same lesson the
# selection record two entries above records: the test that asserts nothing is
# repainted when nothing changed looks vacuous on its own, and it goes red here
# because this break invents a repaint where there was none. A negative
# assertion earns its place by going red under a break a positive one cannot
# see. That test was green against the do-nothing body this rule was written
# against, so without this record there would be nothing saying it checks
# anything.
red = [
    "presentation::view_state::tests::test_a_conversation_growing_repaints_that_one_row",
    "presentation::view_state::tests::test_a_merge_repaints_the_run_it_touched_and_not_the_whole_list",
    "presentation::view_state::tests::test_nothing_changing_repaints_nothing_and_leaves_the_count_alone",
]
tests_last_seen = [
    { file = "src/presentation/view_state.rs", tests = 57 },
]
before = """        rows: (0..now.len())
            .filter(|row| was.get(*row) != Some(&now[*row]))
            .collect(),"""
after = """        rows: (0..now.len()).collect(),"""

[[guard]]
# Moving between accounts is a selection, not a rebuild.
#
# This is what holding every account at once (01-14) is for. Before it the tree
# drew one account's folders, so reaching another account's meant a rebuild:
# `delete_all_items` and five cache reads per account on the interface thread,
# because `MessageCache` wraps a connection that is not `Sync`. Held
# multi-account the other account's folders are already on screen, so arriving
# at one is an arrow key.
#
# The break is the wrong fix somebody reaches for, and it is worth spelling out
# because it does not look like a mistake. Landing on another account's folder
# has to change whose mail the next command acts on. The obvious way to make
# everything downstream agree is to read the tree back, which is what every
# other command in this file does after changing something. Here it would put a
# whole-tree rebuild on every press of the down arrow: the cost paid per
# keystroke instead of per switch, which is worse than what this plan removed.
#
# So the break replaces asking the row whose it is with reading the tree back,
# in one edit, which is exactly the shape of that mistake. A second candidate
# was measured and kept out: removing the call and adding nothing reddens one
# test rather than two, and says nothing about the rebuild half.
#
# A source read rather than a behaviour test. The handler is a closure inside
# the window builder and wxWidgets allows one application per process, so no
# test in this crate can reach it. What that buys is narrow: the handler names
# the pure answer and names neither rebuild. Whether a running window rebuilds
# is a running window.
#
# Measured by hand on 2026-08-31 against the whole library: two tests.
#
# Two, and both halves are needed. A handler that rebuilds nothing and never
# notices whose row it is passes the absence test and reads the wrong account's
# mail; a handler that notices and rebuilds passes the other and costs a
# rebuild per keystroke. Neither test alone is the property.
#
# The two companions beside them stay green under this break and are what say
# the reading means anything: one proves the slice can see a rebuild when there
# is one, the other proves the slice really covers the handler rather than a
# prefix of it. That second one exists because `the_window_itself` in this same
# file used to cut at the first `#[cfg(test)]` and read 9,500 lines of 24,000.
name = "moving between accounts is a selection and not a rebuild"
file = "src/presentation/wx_app.rs"
before = """                            if let Some(whose) =
                                folder_tree::the_account_a_row_belongs_to(&which)
                            {
                                s.active_account_id = Some(whose);
                            }
                            (
                                s.folder_ids.get(&identity).copied(),"""
after = """                            read_the_tree_back(&folder_cache, &state, &ui_tx);
                            (
                                s.folder_ids.get(&identity).copied(),"""
red = [
    "presentation::wx_app::moving_between_accounts_does_not_rebuild_the_tree::test_landing_on_a_row_asks_which_account_it_belongs_to",
    "presentation::wx_app::moving_between_accounts_does_not_rebuild_the_tree::test_landing_on_a_row_does_not_read_the_tree_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── The read dimension on Allowed, and the gate it reaches ──────────────────
#
# Three records from plan 02-01, each taken red by hand against the whole
# library before it was written down, and each written from what really went
# red rather than from what was expected. Two of the three reddened a test in
# `application::mail_controller`, which is not a module anybody working on
# `application::allowed` would have filtered for.

[[guard]]
name = "a body fetch asks whether this account may read"
file = "src/service/protocols/imap.rs"
before = """        self.may_i_read("read the text of a message")?;
        self.require_selected()?;"""
after = """        self.require_selected()?;"""
red = [
    "service::outward::completeness::test_the_read_gate_is_told_apart_from_the_write_census",
    "service::protocols::imap::against_a_server_that_answers::test_a_session_not_allowed_to_read_says_so_before_it_says_no_folder_is_open",
    "service::protocols::imap::against_a_server_that_answers::test_a_session_not_allowed_to_read_will_not_fetch_a_message",
]
tests_last_seen = [
    { file = "src/service/outward.rs", tests = 38 },
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# ── The constant whose name stopped matching its shape ──────────────────────
#
# `Allowed::NOTHING` holds `reading: true`, which reads like a mistake and is
# the whole point. The first-run choice labelled "Read my mail, change nothing"
# resolves to this constant, and so does `--read-only`. Flipping the field is a
# one-character edit that compiles, and it would take mail away from the most
# cautious person in the user base with nothing on screen having changed.

[[guard]]
name = "the constant that changes nothing still reads mail"
file = "src/application/allowed.rs"
before = """        personal_information: false,
        reading: true,"""
after = """        personal_information: false,
        reading: false,"""
red = [
    "application::allowed::tests::test_combining_is_order_independent",
    "application::allowed::tests::test_reading_is_narrowed_the_same_independent_way_as_the_two_writes",
    "application::allowed::tests::test_the_choice_that_promises_reading_still_reads",
    "application::mail_controller::against_a_server_that_answers::test_a_command_that_names_a_different_folder_opens_that_one_first",
    "presentation::command_line::tests::test_nothing_on_the_command_line_turns_reading_off",
    "presentation::first_run::tests::test_the_choice_that_says_read_my_mail_reads_mail",
]
tests_last_seen = [
    { file = "src/application/allowed.rs", tests = 27 },
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/presentation/command_line.rs", tests = 28 },
    { file = "src/presentation/first_run.rs", tests = 16 },
]

# ── The settings file every machine already has ────────────────────────────
#
# `Allowed` is serialised into `app_config.json` and there is one `from_str`
# for the whole file, so a field that does not handle the absent case does not
# only lose its own answer: it takes every other setting on that machine with
# it. A bare `#[serde(default)]` compiles, parses, and answers false, which is
# this field inverted.

[[guard]]
name = "a settings file written before reading was a setting still loads"
file = "src/application/allowed.rs"
before = """    #[serde(default = "default_reading")]
    pub reading: bool,"""
after = """    #[serde(default)]
    pub reading: bool,"""
red = [
    # `test_a_command_that_names_a_different_folder_opens_that_one_first` was
    # named here until 2026-09-15 and taken off by 08-07's sweep: it stayed
    # green with the guard broken on the runners and again here. It is the
    # test `scripts/guards.py` records as failing only in company, and it was
    # red in company on 2026-08-31 when this record was written; it is not
    # about a settings file and never guarded this rule on purpose.
    "data::config::tests::test_a_settings_file_written_before_reading_was_a_setting_still_loads",
]
tests_last_seen = [
    { file = "src/application/allowed.rs", tests = 27 },
    { file = "src/data/config.rs", tests = 68 },
]

# ── What a saved search covers, said rather than only worked out ───────────
#
# The break is not deleting the call. It is keeping the call and throwing the
# answer away, which is the defect the sentence exists to prevent and is what
# an earlier version of the test could not see: it looked for the call and
# went on passing while the number was computed and never said. The break
# below is the one that found that.
#
# Re-measured 2026-09-01, when 02-05 folded the scope sentence into the same
# update and the recorded text stopped naming a place in the tree. The old
# shape put the `say` inside `if let Some(covers)`, so throwing the answer away
# deleted the `say` with it and the check saw an update go missing. It does not
# any more: the answer is an argument, and a `None` handed in its place leaves
# the update exactly where it was. So the break below keeps the call, keeps the
# sentence, and hands `None`, which is sharper than the old one and was invisible
# to the check until the same commit added the assertion that the answer is bound
# to a name rather than to `_`.
#
# `let _ =` is the point of that assertion. With the answer bound, handing `None`
# leaves an unused variable and `-D warnings` refuses the build, so the compiler
# holds the flow; `let _ =` is the one spelling that keeps the call, discards the
# answer and still compiles, and it is what this record now breaks with. Measured:
# `let covers = ...` with `None` passed does not build, and the `let _ =` form
# reddens exactly the one test named below.

[[guard]]
name = "what a saved search covers is said, not merely worked out"
file = "src/presentation/wx_app.rs"
before = """        let covers = coverage_before(&cache, &account_id, &search);
        say(UIUpdate::StatusUpdated(what_a_search_says_as_it_opens(
            &search, covers,
        )));"""
after = """        let _ = coverage_before(&cache, &account_id, &search);
        say(UIUpdate::StatusUpdated(what_a_search_says_as_it_opens(
            &search, None,
        )));"""
red = [
    "presentation::wx_app::what_a_saved_search_says_before_it_runs::test_the_coverage_sentence_is_said_before_the_mail_is_gathered",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── Eviction leaves the search index alone, on purpose ─────────────────────
#
# The tidy-up this stops is a one-line call that looks like a bug fix: make
# the index forget what the body table forgot, so the two agree. It takes a
# working search away. A message would become unfindable at the moment its
# text is evicted rather than merely unsearchable by that text.
#
# Corrected on 2026-09-02 from the run of 2026-09-01, and from that run rather
# than from a hand break of its own: seven more tests went red than this named,
# against a record written days earlier. That is how fast this happens, and the
# seven are worth reading rather than counting.
#
# One is findability again, asked at the search table instead of at the body
# table. The other six are not about findability at all. They are the coverage
# figure and the offer to fetch, which say how much of the mail a search could
# look inside and offer to go and get the rest. That feature is built on the
# search index, so re-indexing an evicted message moves its arithmetic, and
# every one of those six now depends on eviction leaving the index alone.
#
# Written down as it was measured, and the spread is the point rather than a
# problem with the break: the break is one line at the one place the rule
# lives. What changed is what has come to depend on it. Six tests in a module
# about searching from a box now rest on a decision recorded in the body
# cache, and nobody who wrote either would have looked for the other.

[[guard]]
name = "evicting a message's text leaves it findable by the search box"
file = "src/data/message_cache/bodies.rs"
before = """                .map_err(|e| Error::Other(format!("Failed to evict message body: {}", e)))?;
            total -= bytes;"""
after = """                .map_err(|e| Error::Other(format!("Failed to evict message body: {}", e)))?;
            self.index_message_for_search(message_id)?;
            total -= bytes;"""
red = [
    "data::message_cache::bodies::tests::test_an_evicted_message_stays_findable_by_a_word_from_its_text",
    "data::message_cache::searching::finding_things::test_a_message_whose_text_was_evicted_is_still_text_the_box_can_look_inside",
    "presentation::managers::tests::test_a_search_from_the_box_offers_the_fetch_the_saved_search_offers",
    "presentation::managers::tests::test_a_search_from_the_box_says_how_much_of_the_mail_it_could_look_inside",
    "presentation::managers::tests::test_a_search_from_the_box_that_found_nothing_still_says_what_it_could_look_inside",
    "presentation::managers::tests::test_a_search_that_reads_no_message_text_is_not_given_a_coverage_figure",
    "presentation::managers::tests::test_a_search_that_reads_no_message_text_is_offered_no_fetch",
    "presentation::managers::tests::test_no_offer_is_made_when_there_is_no_account_open_to_fetch_from",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "src/data/message_cache/searching.rs", tests = 26 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── Get Older Messages answers the key before it hands to the download ──────
#
# Until 2026-09-17 this record was "choosing the offer starts the backfill
# and says what it did", on the missing-text fetch: a button that started a
# run and threw its report away. 10-05 retired that command for #23, because
# the download of everything is what it did, and rewrote the record and its
# test in place onto the arm that survives: Shift+F9, Get Older Messages,
# which hands to the download with the folder on screen first. The download's
# own lines are steps, silent under Say what arrived, so the arm answers the
# key before the hand-over or pressing it is silence for as long as a server
# takes. The break drops the answer and keeps the hand-over, which is the
# shape that gets past a guard written to look for the call.
#
# The first version of the old record's test was green through a break of
# that shape, because the routine imported both function names on one `use`
# line. The names are written out in full at their call sites, and the
# comment above them says why.
#
# Measured 2026-09-17 on the branch the rewrite landed on, through
# `scripts/guards.sh --remeasure`, the whole library.

[[guard]]
name = "get older messages answers the key before it hands to the download"
file = "src/presentation/wx_app.rs"
before = """                                    send_status(&ui_tx, &runtime, DOWNLOADING_THIS_FOLDER_FIRST);
                                    start_the_download(app);"""
after = """                                    start_the_download(app);"""
red = [
    "presentation::wx_app::what_a_saved_search_says_before_it_runs::test_get_older_messages_says_this_folder_comes_first_before_handing_to_the_download",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── A dialog that offers a vocabulary of its own ────────────────────────────
#
# The filter rule editor held its own list of six fields and its own list of
# six ways of matching, while the engine answered questions about eleven of
# each. Five fields could not be asked about at all, and the six that could
# were offered as the column names they are stored under.
#
# The break is the defect coming back: a list written into the dialog instead
# of read from the engine's constant. Not a shorter version of the constant,
# because the check compares what the dialog offers against that same constant
# and both sides would move together, which is the way this kind of guard
# stops guarding. Measured: adding a twelfth field to the constant makes the
# dialog offer twelve without the dialog being touched, and the check stays
# green through it, correctly.
#
# It runs against its own suite rather than the library, because it builds the
# real dialogs and wxWidgets allows one application per process.
#
# Re-measured 2026-09-01. Plan 02-06 added a second dialog offering the same
# eleven fields and pulled the four lines this record used to name out into
# `the_words_for_every_field`, which both dialogs now read. So the break moved
# from one dialog's own construction to the shared builder, and it now takes
# both dialogs down at once rather than one.
#
# Still one test named, and that is not the red set shrinking: every check that
# builds a window lives in the one `#[test]` that file is allowed, so a break
# reaching two dialogs reddens the same single function. Measured by hand
# again after the move rather than assumed from the old measurement.

[[guard]]
name = "the filter dialog offers what the engine answers rather than a list of its own"
file = "src/presentation/wx_managers.rs"
suite = "manager_dialog_labels"
before = """fn the_words_for_every_field() -> Vec<String> {
    A_FIELD_A_RULE_MAY_NAME
        .iter()
        .filter_map(|field| the_words_for_a_field(field))
        .map(str::to_string)
        .collect()
}"""
after = """fn the_words_for_every_field() -> Vec<String> {
    ["Subject", "From", "To", "Cc"]
        .iter()
        .map(|s| (*s).to_string())
        .collect()
}"""
red = [
    "test_the_manager_dialogs_name_their_controls_and_offer_what_the_engine_answers",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/manager_dialog_labels.rs", tests = 1 },
]

# The "In" list offered four answers, the search that ran read all four, and
# the saved search kept none of them: choosing From Only and saving gave back a
# search over the subject, the sender and the recipients.
#
# The break is dropping the scope argument, because that is the wrong fix
# somebody would reach for. Shrinking the constant would not do: the tests that
# are about the unnarrowed answer read it too, so both sides move together and
# nothing reddens. Ignoring the argument leaves the compiler quiet, the
# unnarrowed answer right, and only the two narrowed ones wrong, which is
# exactly how this defect looked when it was there.
#
# The round trip is in the red list because it is the only one that proves the
# narrowed set survives both tables. Neither sentence test reddens, correctly:
# the sentence is built from whatever questions it is handed, so it says the
# truth about a set the writer got wrong.
[[guard]]
name = "a saved search keeps the question set the In box was narrowed to"
file = "src/application/saved_searches.rs"
before = """    match looking_in {
        WhereToSearch::SubjectOnly => &["subject"],
        WhereToSearch::SenderOnly => &["from"],
        WhereToSearch::EveryFolder | WhereToSearch::OneFolder(_) => &WHAT_A_TYPED_SEARCH_LOOKS_AT,
    }"""
after = """    let _ = looking_in;
    &WHAT_A_TYPED_SEARCH_LOOKS_AT"""
red = [
    "application::saved_searches::tests::test_from_only_saves_one_question_about_the_sender",
    "application::saved_searches::tests::test_subject_only_saves_one_question_about_the_subject",
    "data::message_cache::saved_searches::tests::test_a_search_saved_from_a_narrowed_search_box_comes_back_narrowed",
]
tests_last_seen = [
    { file = "src/application/saved_searches.rs", tests = 76 },
    { file = "src/data/message_cache/saved_searches.rs", tests = 27 },
]

# ── A search that asks nothing, joined by "all" ─────────────────────────────
#
# A list of conditions that all have to match is true of every message when
# the list is empty. So a saved search with no question in it, joined by All,
# would take the whole mailbox, and the row somebody opened expecting a handful
# of messages would hold everything they own.
#
# The guard is one line in `selects`, and it has been there since that function
# was written. What had never been true until D-2-01 is that anybody could
# reach the case: a search made from the search box always carries three
# questions, and `Join::All` had never been written outside a unit test. The
# rule editor makes both reachable at once, because somebody can delete every
# condition in it.
#
# The break is the fall-through somebody would write if they took the line for
# a needless check, and `Join::Any` is why it reads as safe: over an empty list
# `any` is false, so the Any arm goes on taking nothing and only the All arm
# floods. Half the behaviour looks right.
#
# Measured against the whole library, 2026-09-01: two tests, one at the
# `selects` level that was already there and one at the `run_over` level added
# by plan 02-06. The second is the one that matters, because `run_over` is the
# one way to run a search and is what a rule editor's results would come
# through.

[[guard]]
name = "a saved search that asks nothing takes nothing, whichever way it joins"
file = "src/application/saved_searches.rs"
before = """    pub fn selects(&self, message: &CachedMessage) -> bool {
        if self.questions.is_empty() {
            return false;
        }
        let mut answered = self"""
after = """    pub fn selects(&self, message: &CachedMessage) -> bool {
        let mut answered = self"""
red = [
    "application::saved_searches::tests::test_a_search_with_no_question_in_it_takes_nothing_rather_than_everything",
    "application::saved_searches::tests::test_an_all_join_with_no_questions_takes_nothing_rather_than_everything",
]
tests_last_seen = [
    { file = "src/application/saved_searches.rs", tests = 76 },
]

# ── One group in the tree, however a saved search was made ──────────────────
#
# D-2-02. A saved search has two doors: the search box, which writes three
# questions about the subject, the sender and the recipients, and the rule
# editor plan 02-07 opens, which writes any of the eleven fields the filter
# engine answers. Everything stored appears under one heading.
#
# The alternative was checked rather than assumed, and it is not merely
# different, it is worse. Because the two doors edit one object, a typed search
# opened in the rule editor and given a condition about the message text would
# have to move groups, or stay in a group that no longer describes it.
# Grouping by which door made a thing breaks the moment the other door can edit
# it.
#
# The break is that second group: the searches split in two, each half under a
# heading of its own. It is the wrong fix somebody reaches for the next time a
# door is added, which is what makes it the right break to record. Nothing in
# the stored search or in the tree row says which door made it, and two tests
# hold both shapes to that by naming every field with no `..`, so a field
# added to carry it would stop the file compiling before it ever reached here.
#
# Measured against the whole 5,958-test library, 2026-09-01: four. Two are
# about saved searches and two are not, and neither of those two is a test
# anybody working on this feature would have filtered for.

[[guard]]
name = "one heading holds every saved search, however it was made"
file = "src/presentation/folder_tree.rs"
before = """        out.extend(branch.things.into_iter().map(|search| {
            plain_row(
                WhichRow::SavedSearch {
                    account: search.account.clone(),
                    id: search.id.clone(),
                },
                crate::application::saved_searches::a_row_for(&search.name),
                2,
                false,
            )
        }));
    }
    out
}"""
after = """        let (typed, built) = branch.things.split_at(branch.things.len() / 2);
        for (heading, group) in [
            (crate::application::saved_searches::THE_HEADING, typed),
            ("Smart Folders", built),
        ] {
            out.push(plain_row(
                WhichRow::SavedSearches,
                heading.to_string(),
                1,
                true,
            ));
            out.extend(group.iter().map(|search| {
                plain_row(
                    WhichRow::SavedSearch {
                        account: search.account.clone(),
                        id: search.id.clone(),
                    },
                    crate::application::saved_searches::a_row_for(&search.name),
                    2,
                    false,
                )
            }));
        }
    }
    out
}"""
# Re-measured by hand on 2026-09-01, against the shape D-2-05 gave this group:
# the searches now hang off a branch per account, so the break splits them
# inside a branch rather than at the top level. Four reddened before and four
# redden now, and two of the four changed.
#
# `test_a_search_whose_questions_have_no_name_is_announced_like_any_other` has
# stopped reddening, and that is this plan's doing: it asserted a depth as a
# number, the number moved when the branch went in between, and it was rewritten
# to compare one search's depth with another's. Both are still equal under the
# break. `test_the_top_level_reads_in_the_order_somebody_meets_it` has stopped
# too, because the extra headings now sit at depth one and it reads depth nought.
#
# Two arrived: the two written for D-2-05, which see the branch count and the
# heading count directly.
red = [
    "presentation::folder_tree::tests::test_an_account_with_no_saved_searches_gets_no_branch",
    "presentation::folder_tree::tests::test_no_two_rows_of_one_tree_share_an_identity",
    "presentation::folder_tree::tests::test_one_heading_holds_every_saved_search_however_it_was_made",
    "presentation::folder_tree::tests::test_two_accounts_with_searches_give_one_heading_and_a_branch_each",
]
tests_last_seen = [
    { file = "src/presentation/folder_tree.rs", tests = 97 },
]

# ── A saved search run against the account somebody was last looking at ──────
#
# The break is not an invention: it is the code as it stood before 02-08. The
# account a saved-search command acts on used to come from `active_account_id`,
# and Set Active changes that and leaves the folder tree's cursor where it was,
# so a search under one account's branch ran against another account's mail.
#
# Taken red by hand against the whole library on 2026-09-01: exactly two.
[[guard]]
name = "a saved search takes its account from its row and not from the one last looked at"
file = "src/presentation/wx_app.rs"
before = """    let theirs = state.saved_searches.get(account)?;
    theirs
        .searches"""
after = """    let theirs = state.saved_searches.get(account)?;
    let account = &state.active_account_id.clone().unwrap_or_default();
    theirs
        .searches"""
red = [
    "presentation::wx_app::tests::test_a_saved_search_runs_against_the_account_its_row_names_not_the_one_last_looked_at",
    "presentation::wx_app::tests::test_renaming_a_saved_search_is_checked_against_its_own_accounts_names",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── An upgraded database learning what its own search index holds ───────────
#
# The column arrives holding nought for every row somebody already had, and the
# index cannot be asked to fill it in: `message_search` is contentless, so its
# body column reads as NULL however much text is in it. The backfill reads
# `message_bodies` instead. Without it, everybody upgrading with a fully
# downloaded mailbox is told the search box can look inside none of it.
#
# Taken red by hand against the whole library on 2026-09-01: exactly two. It
# was one until the same day, when the fix for the backfill asking a looser
# question than the live writer added a test that holds the two answers against
# each other. That test asserts an ordinary message reads as text the box can
# look inside, so a backfill that fills nothing in reddens it too. Re-measured
# rather than reasoned about, which is what found the second name.
[[guard]]
name = "a database written before the coverage column is told what its index holds"
file = "src/data/message_cache/mod.rs"
before = """                    "UPDATE messages SET {column} = 1"""
after = """                    "UPDATE messages SET {column} = 0"""
red = [
    "data::message_cache::searching::finding_things::test_a_database_written_before_this_column_is_told_what_its_index_holds",
    "data::message_cache::searching::finding_things::test_the_backfill_and_the_live_writer_agree_about_a_body_row_holding_no_text",
]
tests_last_seen = [
    { file = "src/data/message_cache/mod.rs", tests = 23 },
    { file = "src/data/message_cache/searching.rs", tests = 26 },
]

# ── A narrowed search not paying for a coverage figure it cannot use ────────
#
# Subject Only and From Only each restrict the index to one column, so neither
# can match on message text. Saying how much of it is here would be an answer
# to a question nobody asked, on every narrowed search, and working it out
# would be a count nobody needed.
#
# Taken red by hand against the whole library on 2026-09-01: exactly two. The
# second is in the window layer rather than beside the value, which is the
# shape the guard notes at the top of this file warn about: the filter somebody
# would naturally pick for this subject reaches only one of them.
#
# **Measured again on 2026-09-01 and it had already fallen behind, within the
# day it was written.** The same answer now gates the offer to fetch as well as
# the coverage sentence, so the work that added the offer added a third test
# reaching this rule, and the record said two. Nothing failed while it was
# wrong, which is the whole reason this file exists: a record shorter than the
# truth is a guard weaker than it claims and nothing announces it. Taken red by
# hand against the whole library: exactly three.
[[guard]]
name = "a search that reads no message text is not asked what message text is here"
file = "src/data/message_cache/searching.rs"
before = "            Self::SubjectOnly | Self::SenderOnly => false,"
after = "            Self::SubjectOnly | Self::SenderOnly => true,"
red = [
    "data::message_cache::searching::finding_things::test_a_search_that_reads_no_message_text_is_not_asked_what_text_it_covers",
    "presentation::managers::tests::test_a_search_that_reads_no_message_text_is_not_given_a_coverage_figure",
    "presentation::managers::tests::test_a_search_that_reads_no_message_text_is_offered_no_fetch",
]
tests_last_seen = [
    { file = "src/data/message_cache/searching.rs", tests = 26 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── One place wording each of the two coverage sentences ────────────────────
#
# There are two searches here covering different amounts of one mailbox, and
# the way they come to disagree is a sentence about one of them worded where
# that one is wired. The break is a copy of the box's opening in the layer that
# wires it, which is the most natural place for somebody to put it.
#
# Taken red by hand against the whole library on 2026-09-01: exactly one.
[[guard]]
name = "the layer that wires the search box does not word its coverage sentence itself"
file = "src/presentation/managers.rs"
before = """                        Some(covered) => format!("{count} {covered}"),"""
after = """                        Some(covered) => format!(
                            "{count} The search box reads the text of your messages, and \\
                             {covered}"
                        ),"""
red = [
    "application::saved_searches::tests::test_one_place_builds_the_coverage_sentence",
]
tests_last_seen = [
    { file = "src/application/saved_searches.rs", tests = 76 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── The download saying it has started before it opens a connection ─────────
#
# Until 2026-09-17 this record was "the fetch says what it is doing before it
# opens a connection", on the missing-text fetch chosen from the File menu.
# 10-05 retired that command for #23 and rewrote the record and its test in
# place onto the download of everything, which starts on its own after every
# check: its first step is a sign-in that takes as long as a mail server
# takes, and a status bar that says nothing until then reads, to somebody who
# cannot see the connection light, as a program doing nothing.
#
# The break is the one that keeps the call and throws the words away, because
# an ordering check on its own is satisfied by saying nothing at all.
#
# Taken red by hand against the whole library on 2026-09-01 as first written:
# exactly one. Measured 2026-09-17 on the branch the rewrite landed on,
# through `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the download says it has started before it opens a connection"
file = "src/presentation/wx_app.rs"
before = """const STARTING_THE_DOWNLOAD: &str = "Downloading the mail that is not on this computer yet...";"""
after = """const STARTING_THE_DOWNLOAD: &str = "";"""
red = [
    "presentation::wx_app::what_a_saved_search_says_before_it_runs::test_the_download_says_it_has_started_before_it_opens_a_connection",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# Sixteen checks in tests/wired.rs read the main window's source. Twelve of them
# used to cut it at the first `#[cfg(test)]` and four used a line-walking reader
# of their own; all sixteen now ask `common::what_ships`. The break is a prefix
# cut put back.
#
# It is put back into the one site where it can be seen, and that is a finding
# rather than a convenience. Measured by hand on 2026-09-02: putting the cut
# back into `test_no_two_items_on_one_menu_claim_the_same_letter`, one of the
# twelve, reddens nothing at all, because that check reaches the same verdict
# over 19,728 lines as over 20,117. The same is true of the other eleven. So
# nothing in this file would notice a reader going back to a prefix cut except
# the one test written to ask that question directly, and this record is the
# measurement that it would.
#
# What this cannot see, said plainly: whether the other fifteen sites still use
# the one answer. No break reddens anything if they stop. What holds them is the
# absence of the idiom, which is a reading of the source nobody has written a
# check for.
#
# Taken red by hand against `cargo test --test wired` on 2026-09-02: exactly
# one, out of 59.
[[guard]]
name = "the main window is read whole rather than cut at its first test module"
file = "tests/wired.rs"
suite = "wired"
before = """    let ship = what_ships(&app);

    assert!(
        ship.contains("fn block_the_sender("),"""
after = """    let ship = app[..app.find("\\n#[cfg(test)]").unwrap_or(app.len())].to_string();

    assert!(
        ship.contains("fn block_the_sender("),"""
red = [
    "test_the_reading_of_the_main_window_reaches_past_its_first_test_module",
]
tests_last_seen = [
    { file = "tests/wired.rs", tests = 77 },
]

# ── The claim that a test cannot build a window ─────────────────────────────
#
# wxWidgets allows one live window per process. Ten comment blocks in this tree
# wrote that down as though it meant no test could build one at all, and two of
# them said so flatly while `tests/theme_reach.rs` had been building live
# dialogs since before either was written. A false premise in a comment steers
# work away from a technique that works, and nothing tests a comment.
#
# The break puts the flat claim back into `wx_folder_choice.rs`, whose dialog
# `tests/theme_reach.rs` really does open. It takes away the wording that
# exempts that comment at the same time, which is deliberate: the two halves of
# the rule are that the claim is refused and that stating the budget is what
# lifts the refusal, and one edit exercises both.
#
# What this cannot see, and it is the hole every source-reading guard has:
# whether the other nine corrected blocks stay corrected. Putting the claim back
# into any one of them reddens the same single test, so this record measures the
# reading rather than the ten sites. The reading itself is bounded by
# `test_the_window_claim_check_can_tell_the_two_apart`, which feeds it both a
# claim and a correction and fails if it answers nothing or answers everything.

[[guard]]
name = "a comment claiming a test cannot build a window is still refused"
file = "src/presentation/wx_folder_choice.rs"
suite = "house_style"
before = """/// itself is opened in a test: wxWidgets allows one live window per process,"""
after = """/// itself cannot be opened in a test, because no test can build a window,"""
red = [
    "test_no_comment_says_a_test_cannot_build_a_window",
]
tests_last_seen = [
    { file = "src/presentation/wx_folder_choice.rs", tests = 21 },
    { file = "tests/house_style.rs", tests = 74 },
]

# ── The two integration guards that named no source file ────────────────────
#
# Both records below were written on 2026-09-02 for a reason that is new. A
# record's `file` and `suite` used to be read only by `scripts/guards.sh`, to
# know where to apply a break and which suite to run it against. `scripts/check.sh`
# now reads the same two fields the other way round: given a changed source
# file, which integration targets guard it, so that a guard living under
# `tests/` runs on the commits that could break it rather than only on the
# commits that change the test file.
#
# So a guard with no record here is a guard the gate cannot find. These two had
# none, and both cover a `src/presentation` module from outside it.
#
# Each suite holds exactly one `#[test]` function, for the one-live-window-per-
# process budget `tests/theme_reach.rs` explains: wxWidgets allows one
# application per process and `cargo test` gives each file under `tests/` its
# own process, so every window check in a file shares one test function. A break
# reaching several dialogs still reddens one name there. That is not a red list
# that shrank, and without this note the next re-measurement would read it as
# one, the same way the `manager_dialog_labels` record above has to say it.

# The item form built both of its check boxes with an empty label and named them
# through `set_accessible_name`, which writes MSAA and nothing else. Windows
# takes a native control's UI Automation name from its own window text, so the
# control was named for NVDA and unnamed for Narrator: correct on the channel
# somebody was listening on, and silent on the other.
#
# Measured by hand on 2026-09-02. The break reddens the one check in the suite,
# and the whole library passes through it untouched: 5,997 tests, none of them
# able to see a name that only exists on one accessibility channel, because
# seeing it needs a real control built by the platform. `house_style` and
# `wired` pass through it too. That is the point of an integration guard, and it
# is also why leaving it uncoupled cost so much: nothing else in the tree would
# ever have spoken.

[[guard]]
name = "a check box carries its own label rather than borrowing the text beside it"
file = "src/presentation/wx_item_form.rs"
suite = "checkbox_labels"
before = """            let c = CheckBox::builder(parent).with_label(field.label).build();"""
after = """            let c = CheckBox::builder(parent).build();"""
red = [
    "test_every_check_box_in_a_form_carries_its_own_label",
]
tests_last_seen = [
    { file = "src/presentation/wx_item_form.rs", tests = 14 },
    { file = "tests/checkbox_labels.rs", tests = 1 },
]

# The same rule, on the second screen that walk now reaches, and the record
# exists so the gate knows it does. The plan for this task said no record was
# owed because the one above still describes its own break, which is true and is
# about the wrong thing: without this, `checkbox_labels` runs when
# `wx_item_form.rs` changes and on no commit touching the settings screen it now
# also guards. That is guardrail 4, a check that reads as covering something it
# is never asked about.
#
# The break is the shape that passes every source-reading test and every
# listening pass done with one screen reader: a real accessible name, set through
# the call that writes MSAA, on a control whose own text is empty. NVDA reads it
# correctly. Narrator reads an unnamed check box, because Windows supplies its
# own UI Automation provider for a native check box and that provider takes the
# name from the window's text.
#
# Measured by hand on 2026-09-13 against the target: the one test in the suite
# went red naming all three per-event controls, each with the label it should
# have carried.
[[guard]]
name = "a check box on the settings screen carries its own label too"
file = "src/presentation/wx_settings.rs"
suite = "checkbox_labels"
before = """        let tick = CheckBox::builder(panel).with_label(label).build();"""
after = """        let tick = CheckBox::builder(panel).with_label("").build();"""
red = [
    "test_every_check_box_in_a_form_carries_its_own_label",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/checkbox_labels.rs", tests = 1 },
]

# What a manager window says after a delete, counted against the list as it is
# afterwards rather than as it was before.
#
# The break is the old length: `left` read before the row is removed instead of
# after. Only a condition list counts out loud, so a tag, a filter or a
# signature going says the same words either way and the defect is invisible
# there. A saved search with three conditions, one deleted, then says "3
# conditions now" over a list holding two.
#
# The file's name says the window stays open, and the checks in it are not about
# that: they are about `delete_selected` and `delete_selected_contact` removing
# the right row, marking the state changed, repainting the list, and saying the
# right sentence. The decision the name refers to, `what_it_still_needs` and
# `nothing_stops_this_closing`, is in the modal loop and nothing in that file
# reaches it. The break was chosen from what the file really asserts rather than
# from what its name suggests, and the first plausible break, closing the window
# on an emptied list, would have reddened nothing.
#
# Measured by hand on 2026-09-02. The whole library passes through this break,
# 5,997 tests, and so do `house_style`, `wired` and `manager_dialog_labels`. The
# sentence builder's own tests in `manager_words` are given the number, so they
# cannot see the caller handing them the wrong one. That is the gap this file's
# own comment names and it is measured here rather than assumed.

[[guard]]
name = "a delete says how many rows are left rather than how many there were"
file = "src/presentation/wx_managers.rs"
suite = "manager_delete_stays_open"
before = """        let mut s = state.borrow_mut();
        s.working.remove(idx);
        s.changed = true;
        drop(s);
        let left = state.borrow().working.len();"""
after = """        let mut s = state.borrow_mut();
        let left = s.working.len();
        s.working.remove(idx);
        s.changed = true;
        drop(s);"""
red = [
    "test_delete_removes_the_right_row_against_real_widgets",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/manager_delete_stays_open.rs", tests = 1 },
]

[[guard]]
# The conversation view starts hanging the message id off the row again.
#
# The obvious way to write this feature, and what it did until 02.1-05: the id
# looks like it belongs on the item. It does not go on the item.
# `set_custom_data` puts it in a process-global map in `wxdragon` and stores the
# map key on the row, and nothing takes it out again: `delete_all_items` goes
# straight to wxWidgets, which has never heard of the map, and
# `cleanup_all_custom_data` walks the tree through `clean_item_and_children`,
# which calls `remove_item_data` nowhere at all. So every opening of this dialog
# left one entry per message behind for the life of the program.
#
# The record above `no row in the folder tree hangs data off the control` says
# that leak has no observable behaviour and that a source read is therefore the
# only thing that can see it. That was wrong, and this record is the evidence.
# The registry hands out its keys from a monotonic counter and both
# `store_item_data` and `get_item_data` are public, so a throwaway entry either
# side of a build fences the keys that build issued, and asking the registry for
# each of them counts what is still there.
#
# Measured by hand on 2026-09-02 against this target: one test, which is the
# only test in it. The break leaves three entries behind for a three-message
# conversation, and the failure says so.
name = "the conversation view hangs its message ids off the control"
file = "src/presentation/wx_thread_view.rs"
suite = "tree_rows_leave_no_registry_entry"
before = """        items[row.node] = Some(item);"""
after = """        tree.set_custom_data(&item, nodes[row.node].message_id);
        items[row.node] = Some(item);"""
red = [
    "test_neither_tree_dialog_leaves_an_entry_in_the_process_global_registry",
]
tests_last_seen = [
    { file = "src/presentation/wx_thread_view.rs", tests = 9 },
    { file = "tests/tree_rows_leave_no_registry_entry.rs", tests = 1 },
]

[[guard]]
# The destination picker starts hanging the folder identifier off the row again.
#
# The same leak as the record above, through the other call.
# `append_item_with_data` reads as though the identifier travels with the row,
# and this dialog's own comment used to argue for it on those grounds. It goes
# into the same process-global map, so every opening of the move dialog left one
# entry per folder behind.
#
# Measured by hand on 2026-09-02 against this target: one test, which is the
# only test in it. The break leaves two entries behind for a two-folder account,
# and the failure says which dialog left them.
#
# Re-measured on 2026-09-07. The row a place hangs under is no longer always
# the account: since 04.1-01 a place at depth one hangs under the place above
# it, so the break's first line moved and the record stopped naming any place
# in the tree at all. What it defends is unchanged and it still reddens the one
# test in that target.
name = "the destination picker hangs its folder identifiers off the control"
file = "src/presentation/wx_destination.rs"
suite = "tree_rows_leave_no_registry_entry"
before = """                let Some(row) = tree.append_item(&under[depth], &place.name, None, None) else {"""
after = """                let Some(row) = tree.append_item_with_data(
                    &under[depth],
                    &place.name,
                    place.id.clone(),
                    None,
                    None,
                ) else {"""
red = [
    "test_neither_tree_dialog_leaves_an_entry_in_the_process_global_registry",
]
tests_last_seen = [
    { file = "src/presentation/wx_destination.rs", tests = 6 },
    { file = "tests/tree_rows_leave_no_registry_entry.rs", tests = 1 },
]

[[guard]]
# An account heading starts answering with the first folder under it.
#
# The row somebody landed on is an account name, which is not a place mail can
# go. Answering with a folder under it files the message somewhere they never
# named, and everything reports success, so this is the worst thing this dialog
# can do and it is the reason the entry for an account row is empty.
#
# The break is the plausible convenience: rather than pushing nothing for the
# heading, push the first place under it, so a stray Enter on the account still
# "works".
#
# The pure tests beside the dialog cannot see this. They ask what a position
# means against a fixture they write themselves, and this break is in what the
# build puts in the vector. Only a live control answers it, which is what this
# target is for.
#
# Measured by hand on 2026-09-02 against this target: one test, which is the
# only test in it. It reddens in three places at once, the vector's contents and
# both account rows, and names each.
name = "an account row in the destination picker answers with a folder"
file = "src/presentation/wx_destination.rs"
suite = "tree_dialogs_resolve_the_row_somebody_is_on"
before = """            destinations.push(None);"""
# The break used to push `place.id.clone()`, which is what the vector held
# before 04.1-01. It now holds the whole destination, so the same break is the
# whole place. The defect and the red list are unchanged; only the spelling is.
after = """            destinations.push(branch.places.first().cloned());"""
red = [
    "test_choosing_a_row_in_either_dialog_resolves_to_that_row",
]
tests_last_seen = [
    { file = "src/presentation/wx_destination.rs", tests = 6 },
    { file = "tests/tree_dialogs_resolve_the_row_somebody_is_on.rs", tests = 1 },
]

[[guard]]
# The conversation view resolves a selection to the row after the one chosen.
#
# The pairing between the walk and the vector beside it is exact or it is
# nothing, and an off-by-one is how it stops being exact: every row opens the
# message under it and the last opens the whole conversation. Nothing about that
# looks wrong in the source.
#
# A companion to the record above rather than a repeat of it. That one is about
# what the build puts in the vector; this is about how a position is read back
# out, and the two fail in different places. Both are needed here because this
# target covers two dialogs and a record measures one break.
#
# The library's own tests see this break too, and they are not in this list
# because a record is measured against one target: `--lib` would name
# `test_a_row_resolves_to_its_own_message` beside `wx_thread_view`. What this
# record adds is that the break is caught against a real control, where the
# position comes from the tree rather than from a test's arithmetic.
#
# Measured by hand on 2026-09-02 against this target: one test, which is the
# only test in it. It names all three conversation rows.
name = "the conversation view answers with the row after the one chosen"
file = "src/presentation/wx_thread_view.rs"
suite = "tree_dialogs_resolve_the_row_somebody_is_on"
before = """    match position.and_then(|position| ids.get(position)) {"""
after = """    match position.and_then(|position| ids.get(position + 1)) {"""
red = [
    "test_choosing_a_row_in_either_dialog_resolves_to_that_row",
]
tests_last_seen = [
    { file = "src/presentation/wx_thread_view.rs", tests = 9 },
    { file = "tests/tree_dialogs_resolve_the_row_somebody_is_on.rs", tests = 1 },
]

[[guard]]
# Criterion 2 of phase 2.1, and threat T-2.1-17. Safety is stored as a word so
# a mailbox can be read by a person, and the alphabet does not agree with how
# bad the words are: ordinary, phishing, spam, suspicious. The conversation
# view has ranked the four since it was written. The message view ordered the
# words, so descending put suspicious on top and phishing third, and one column
# meant two different things depending on which view somebody was in.
#
# The break puts the bare column back into the message arm, which is the defect
# itself. That it is the defect is the tell that it is the right break: the two
# arms are built from one ranking now, so shrinking or editing that ranking
# moves both sides together and the guard would stay green while looking as
# though it had been measured.
#
# Measured by hand on 2026-09-02 against the whole library: five tests. Four
# read row order out of a real database. The fifth is the only one that reads
# the expression, and it reads it for a different reason: a `CASE` is not a
# column name, and `By::term` appends a direction to it.
name = "the message view ranks safety by how bad it is rather than by the alphabet"
file = "src/presentation/message_columns.rs"
before = """            MessageColumn::Safety => how_bad_the_safety_word_is!(),"""
after = """            MessageColumn::Safety => "m.safety","""
red = [
    "data::message_cache::messages::tests::test_a_safety_word_this_build_does_not_know_sorts_as_the_mildest",
    "data::message_cache::messages::tests::test_both_views_of_the_safety_column_rank_the_verdicts_the_same_way",
    "data::message_cache::messages::tests::test_sorting_messages_by_safety_puts_the_worst_at_the_top",
    "data::message_cache::messages::tests::test_the_two_directions_of_a_safety_sort_are_exact_reverses",
    "presentation::message_columns::tests::test_a_safety_sort_is_a_ranking_with_a_direction_on_the_end_of_it",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/presentation/message_columns.rs", tests = 43 },
]

[[guard]]
# Criterion 3 of phase 2.1, and threat T-2.1-18. Mail arriving through
# `mail_parser` has its angle brackets taken off before it reaches the store; a
# draft this program files keeps them, because `draft_message::message_id_for`
# builds the whole header value. So one column held two spellings of the same
# thing, and anything comparing a stored identifier with one somebody handed
# over found nothing.
#
# The break makes the normalising call a no-op at the one production write. It
# is applied there rather than in each caller, so this is also what checks that
# the rule stays where a writer added later cannot miss it.
#
# Measured by hand on 2026-09-02 against the whole library: five tests. Four
# are new. The fifth, `test_a_message_is_found_by_the_id_its_sender_gave_it`,
# predates this and is the interesting one: it goes red because the lookup
# trims and the write no longer does, which is the state a correction stopping
# at one end leaves behind.
name = "every writer of a message identifier writes one spelling of it"
file = "src/data/message_cache/messages.rs"
before = """        let identifier = crate::application::thread_identity::bare(&incoming.message_id);"""
after = """        let identifier = incoming.message_id.as_str();"""
red = [
    "data::message_cache::messages::tests::test_a_message_is_found_by_the_id_its_sender_gave_it",
    "data::message_cache::messages::tests::test_an_identifier_arriving_with_brackets_is_stored_without_them",
    "data::message_cache::messages::tests::test_an_identifier_arriving_with_spaces_round_it_is_stored_trimmed",
    "data::message_cache::messages::tests::test_looking_a_draft_up_by_the_identifier_its_header_carries_still_finds_it",
    "data::message_cache::messages::tests::test_the_identifier_a_filed_draft_is_listed_under_is_the_one_a_reader_hands_over",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
# Threat T-2.1-19. The backfill rewrites values in a column that shipped, on
# every open, over the only copy of somebody's mail. What makes that safe is
# that it selects only the rows that need it: the count then means something, a
# second run finds nothing, and a large cache is not written over on every
# open.
#
# The break drops the `WHERE` clause, so it selects every row. Nothing about
# the result looks wrong, which is the point: every value still ends up right,
# and what goes is the count's meaning and the idempotence.
#
# There is one narrowing to drop, and that is deliberate. A first draft
# narrowed in SQL and again in Rust, and neither half could be taken red on its
# own, because whichever one was dropped the other still answered.
#
# Measured by hand on 2026-09-02 against the whole library: four tests.
name = "the identifier backfill selects only the rows that need it"
file = "src/data/message_cache/messages.rs"
before = """                "SELECT id, message_id FROM messages
                 WHERE message_id != TRIM(message_id, ' ' || char(9) || char(10) || char(13))
                    OR message_id LIKE '<%'
                    OR message_id LIKE '%>'","""
after = """                "SELECT id, message_id FROM messages","""
red = [
    "data::message_cache::messages::tests::test_running_the_identifier_backfill_twice_changes_nothing_the_second_time",
    "data::message_cache::messages::tests::test_the_identifier_backfill_counts_only_the_rows_it_changed",
    "data::message_cache::messages::tests::test_the_identifier_backfill_leaves_a_row_with_no_identifier_alone",
    "data::message_cache::messages::tests::test_the_rows_the_backfill_selects_are_exactly_the_rows_that_change",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
# Guardrail 1: a feature is done when a non-test path reaches it. The backfill
# is reached from `MessageCache::open` and from nowhere else, so a test calling
# it by hand says only that it works, never that it runs.
#
# The break leaves the `if let` in place and gives it a result the migration
# never produced, so the code still compiles, every test that calls the
# migration by hand still passes, and the only thing that changes is that
# opening a database no longer corrects anything. That is the state this
# project keeps finding: written, tested, and wired to nothing.
#
# Measured by hand on 2026-09-02 against the whole library: one test, and it is
# the only one that opens a database rather than calling the migration.
name = "the identifier backfill is reached when a database is opened"
file = "src/data/message_cache/mod.rs"
before = """        if let Err(e) = cache.backfill_message_identifiers() {"""
after = """        if let Err(e) = Result::<usize>::Ok(0) {"""
red = [
    "data::message_cache::messages::tests::test_a_database_holding_bracketed_identifiers_is_corrected_when_it_is_opened",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
# THREAD-02's remaining half, and broken windows ledger 7. A merge could only
# ask whether some stored message's own identifier was one of the arriving
# message's, so a conversation root arriving after a message that already named
# it was invisible: the knowledge sat in that message's stored `refs_header`,
# and no index over a header can answer a question about a name inside it.
# `identifiers_a_message_names` is that knowledge written down, and the second
# half of the lookup is what reads it.
#
# The break narrows the new question back to the old one: a recorded name
# counts only when it is that message's own identifier, which is exactly what
# the message column already answered. It keeps the shape of the statement and
# every bound value, so the parameter arithmetic is untouched and nothing
# reddens for a second reason.
#
# Measured by hand on 2026-09-04 against the whole library, which passes 6123
# at 0 failed with 1 ignored on `cargo test --lib`: one test, and one is right,
# because the six arrival orders are the defect and that test is all six.
#
# What stays green is the finding. The three backfill tests do not redden,
# because the backfill walks every stored message and asks about each one in
# turn, so the connector's own chain names both roots and the old question is
# enough there. The table is what an ARRIVAL needs, since an arrival asks about
# one message and nothing ever re-asks the ones already stored. This record and
# the one below therefore cover two different halves and neither stands in for
# the other.
name = "the conversation lookup asks what a message named, not only what it is called"
file = "src/data/message_cache/messages.rs"
before = """                   AND n.identifier IN ({named_placeholders})"""
after = """                   AND n.identifier IN ({named_placeholders}) AND n.identifier = m.message_id"""
red = [
    "data::message_cache::messages::tests::test_a_root_arriving_after_the_message_that_names_it_joins_its_conversation",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

[[guard]]
# Guardrail 1: a feature is done when a non-test path reaches it. A table that
# starts empty makes this change invisible to every conversation already
# stored, so the backfill is the half somebody who has been using this program
# actually sees. It is reached from `MessageCache::new` and from nowhere else,
# so a test calling it by hand says only that it works, never that it runs.
#
# The break is the same shape as the record above it: the `if let` stays, it is
# given a result the backfill never produced, everything still compiles, and
# the only thing that changes is that opening a database joins nothing. That is
# the state this project keeps finding, and `bodies.rs`'s module header records
# what it cost the last time: a function with no non-test caller while the
# cache grew without limit.
#
# Measured by hand on 2026-09-04 against the whole library, which passes 6123
# at 0 failed with 1 ignored on `cargo test --lib`: three tests, which is every
# test that opens a database holding a split conversation. The second-open one
# is among them, and that is what its first assertion is for: without it, "a
# second open changes nothing" is satisfied by a first open that did nothing.
name = "the conversations stored apart are joined when the database is opened"
file = "src/data/message_cache/mod.rs"
before = """        if let Err(e) = cache.backfill_identifiers_a_message_names() {"""
after = """        if let Err(e) = Result::<usize>::Ok(0) {"""
red = [
    "data::message_cache::messages::tests::test_a_conversation_split_before_there_was_an_identifier_table_joins_when_opened",
    "data::message_cache::messages::tests::test_opening_a_database_that_has_already_been_filled_changes_nothing",
    "data::message_cache::messages::tests::test_the_backfill_moves_conversations_and_not_mail",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

[[guard]]
# Criterion 2.1-06. `one_question_at_a_time::what_to_raise` decides when the
# question about vanished folders may be put, and it is tested without a window
# against every combination of its three inputs. Nothing tested that it is
# asked the right question. Both of the answers it is given are worked out in
# the body of `ask_about_the_folders_that_have_gone`, and either could be
# replaced by a constant with the whole library still green.
#
# The break replaces the gate's answer with `true`, which is what "the moment is
# free" looks like when nobody asked. A question then opens on top of the
# reminder alert that is already on screen. Replacing the other argument with
# `false` is the same shape and reddens the same one test, measured separately.
#
# Measured by hand on 2026-09-02, both breaks, against `cargo test --test wired`
# and against the whole library. One test in `wired`; the library notices
# neither break, which is why this record names that suite.
name = "the vanished-folders question is put at a moment that is free"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """    let Some(question) = what_to_raise(&waiting.borrow(), an_editor_has_focus, turn.is_none())"""
after = """    let Some(question) = what_to_raise(&waiting.borrow(), an_editor_has_focus, true)"""
red = [
    "test_the_question_about_vanished_folders_is_put_at_a_moment_that_is_free",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
# Criterion 2.1-13. `trim_trailing_fwd` wants a parenthesised token longer than
# one character, so the probe built on it cannot see Hungarian's one-letter
# `I:`, and the code then concludes reply because the two sets do not overlap.
# A second probe reads the same set out of `thread_name`'s bracket handling,
# where a forward prefix and a reply prefix are treated differently, and it can
# see a one-character word.
#
# The break removes the second probe and leaves the first, which is the state
# this was in from 01-11 until 2026-09-02. It compiles, and thirty-nine of the
# forty markers are still read correctly.
#
# Measured by hand on 2026-09-02 against the whole library: four, two on the
# classification and two on what a person sees, which is what a marker read
# wrongly costs. Replying wrote no reply marker and forwarding wrote a second
# forward marker.
name = "a one-letter forward marker is read as a forward marker"
file = "src/application/conversations.rs"
before = """    let bracketed = thread_name(&format!("[{word}: {A_WORD_THAT_IS_NOT_A_MARKER}]"))
        == A_WORD_THAT_IS_NOT_A_MARKER;
    trailing || bracketed"""
after = """    let bracketed = false;
    trailing || bracketed"""
red = [
    "application::conversations::tests::test_a_one_letter_forward_marker_is_a_forward_marker",
    "application::conversations::tests::test_every_marker_the_dependency_knows_is_read_as_the_kind_it_is",
    "presentation::wx_compose::tests::test_forwarding_a_one_letter_forward_marker_does_not_stack_a_second",
    "presentation::wx_compose::tests::test_replying_to_a_one_letter_forward_marker_says_it_is_a_reply",
]
tests_last_seen = [
    { file = "src/application/conversations.rs", tests = 26 },
    { file = "src/presentation/wx_compose.rs", tests = 43 },
]

# ── A row that is not a folder offered a folder's commands ───────────────────
#
# The break is not an invention. It is the answer the folder tree's closure
# gave from 01-14 until 02.1-08: a saved search gets its own menu and every
# other row gets a folder's, so nine kinds of row out of twelve were offered
# fetching older messages on a row with no messages to fetch, and choosing
# which folders are kept up to date on rows that name no account to choose for.
#
# Written as an early return rather than as a catch-all arm on purpose. A
# catch-all would make the arms below unreachable and change what the compiler
# says about the file, and the point of the record is to measure the tests
# rather than the warnings. This shape compiles clean and is exactly the old
# behaviour.
#
# What the number says, and it is the reason this record exists: measured by
# hand against the whole 6,039-test library on 2026-09-02, five, and all five
# are the tests written in the same plan. Nothing else in the library notices a
# folder tree that offers a folder's commands on a heading, which is what the
# fault being live since 01-14 already told us.

[[guard]]
name = "a row that is not a folder is not offered a folder's commands"
file = "src/presentation/folder_tree.rs"
before = """    use crate::application::context_menu::Focus;
    match row {"""
after = """    use crate::application::context_menu::Focus;
    if !matches!(row, WhichRow::SavedSearch { .. }) {
        return Some(Focus::MailFolders);
    }
    match row {"""
red = [
    "presentation::folder_tree::which_menu::test_a_command_that_reads_the_open_account_is_offered_only_where_the_row_set_it",
    "presentation::folder_tree::which_menu::test_a_folder_and_a_pinned_copy_of_one_answer_the_mail_folders_menu",
    "presentation::folder_tree::which_menu::test_a_row_with_nothing_of_its_own_has_no_menu_rather_than_a_folders",
    "presentation::folder_tree::which_menu::test_getting_older_messages_is_offered_only_where_a_folder_is_opened",
    "presentation::folder_tree::which_menu::test_rows_that_want_different_commands_do_not_share_a_menu",
]
tests_last_seen = [
    { file = "src/presentation/folder_tree.rs", tests = 97 },
]

# ── A stored condition this build cannot show, opened anyway ─────────────────
#
# Criterion 10 and D-2.1-02. Both rule editors read their two lists back with
# `unwrap_or_default`, so a stored word this build has no words for selects
# nothing in the list and pressing OK stores the empty string in its place.
# That is a silent rewrite of somebody's stored rule, which is the shape behind
# every data-losing bug this project has found. Plan 02-04 closed the half that
# was reachable, where the editor could not show five of the eleven fields it
# does know; what is left is a rule written by a later version.
#
# The break is the refusal removed at its source: the one answer both editors
# ask, at both ends, saying there is nothing it cannot show. That is what the
# code did before this plan, so the break is the old behaviour rather than an
# invention.
#
# Measured by hand against the whole 6,060-test library on 2026-09-02: five,
# all of them written in this plan. Nothing else in the library notices, which
# is what a defect that has been live since the editors were written already
# says.
#
# One thing this record does not reach, measured the same day and written down
# here rather than left for somebody to rediscover. Taking the pre-open refusal
# out of `show_rule_edit` instead, so the dialog really does open on a
# condition it cannot show, reddens exactly one test:
# `test_both_condition_editors_refuse_before_they_open_and_before_they_hand_one_back`.
# That test reads the two editors' source, because every path that tells a
# refusal from an opening ends at `show_modal`, which blocks with nobody to
# answer it, so a test that opened the editor would hang the commit gate rather
# than fail it. One test is thin for a record of its own, so that break has none;
# the test lives beside the code and `--lib` reaches it on any commit touching
# that file.

[[guard]]
name = "a condition this build cannot show is refused rather than opened and emptied"
file = "src/presentation/wx_managers.rs"
before = """    let cannot =
        crate::application::saved_searches::what_a_condition_cannot_read(field, match_type)?;
    Some(format!(
        "This {kind} cannot be shown: {}\\n\\nNothing has been lost: it is still in the list, \\
         exactly as it was stored. You can delete it and write a new one here. If a newer \\
         version of Wixen Mail wrote it, it still works there.",
        cannot.why()
    ))"""
after = """    let _ = (kind, field, match_type);
    None"""
red = [
    "presentation::wx_managers::tests::test_a_condition_naming_a_field_this_build_cannot_show_is_refused_by_name",
    "presentation::wx_managers::tests::test_a_condition_that_cannot_be_shown_is_still_a_row_that_says_what_it_holds",
    "presentation::wx_managers::tests::test_a_condition_with_no_field_at_all_is_refused_without_a_hole_in_the_sentence",
    "presentation::wx_managers::tests::test_a_way_of_matching_this_build_cannot_show_is_refused_by_name_too",
    "presentation::wx_managers::tests::test_the_refusal_is_worded_by_the_search_rather_than_a_second_time_here",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
]

# ── A server that says nothing about its numbering has not renumbered ────────
#
# Criterion 1 and T-03-03. `sync_folder` discards every cached message in a
# folder when the server's UIDVALIDITY differs from the stored one, which is
# right: the server has reused the numbers, so every uid held names a different
# message or none. The whole decision is one `matches!` pattern requiring both
# values to be present and to differ.
#
# The break is the one edit a later reader would plausibly make to that
# pattern, and it is the worst one available: treat an absent server value as a
# change. Servers that answer no UIDVALIDITY exist, and against one of them the
# widened pattern empties every folder on every sync and refills it from the
# network. Nothing about it looks like a data-losing edit. It makes the pattern
# shorter.
#
# Measured by hand against the whole 6,075-test library on 2026-09-03: one, the
# test written for this arm in the same commit. Nothing else in the library
# notices, and that is the finding rather than an aside. The arm had no test at
# all until now, so the entire defence of it is the single test named below,
# and a run that reddened only that one is what says so.

[[guard]]
name = "a server that gives no numbering at all has not renumbered anything"
file = "src/application/mail_sync.rs"
before = """        (Some(now), Some(before)) if now != before
    );"""
after = """        (now, Some(before)) if now != Some(before)
    );"""
red = [
    "application::mail_sync::tests::test_a_server_that_gives_no_numbering_at_all_discards_nothing",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── A sign-in that stopped going round the helper ────────────────────────────
#
# SCALE-02's fourth deliverable. How many places in the main window build their
# own `MailController` and connect with it used to be a list of line numbers in
# a requirements document, and it went stale twice: it said eight, by 2026-09-03
# all eight of those lines had moved and none of them was a connect site any
# more, and the truth was twelve. The twelve were then written down as line
# numbers too and seven of them had moved by the next day. So the count lives in
# a test now, which reads the tree rather than repeating a number.
#
# The break used to be the change the census exists to notice: the site that
# marks one message read going through `application::mail_session::a_session_at`
# instead of dialling for itself. Plan 03-06 made that change for real, so the
# text the record named stopped being in the file and the record became
# unmeasurable, which the commit gate refused before anything else could happen.
#
# It is turned round rather than moved to another site, because moving it would
# only have to be turned round later anyway. Once every site goes through the
# helper there is nothing left to convert, and the change worth noticing is the
# opposite one: somewhere new dialling for itself. So the break puts the old
# sign-in back at that same site, and the census answers one more than it was
# told. That is the break the record keeps for good: it discriminates while the
# count is eleven and it still discriminates when it is nought.
#
# This record couples the census to `wx_app.rs` for a second reason as well. The
# census lives under `tests/`, so without a record `scripts/check.sh` would run
# it only on the commits that change the census itself, which is every commit
# except the ones that could change the number.
#
# Measured by hand twice, both times on 2026-09-04. First against the break that
# converted the site, and then again against the break that puts it back, which
# is what the record now carries. Both runs covered the census target,
# `tests/house_style`, `tests/wired` and the whole library, and both gave the
# same answer: one test went red, the one named below, and nothing else did.
# house_style 64 passed and wired 61 passed in each.
#
# The second run's library was 6,127 passed with 3 failed, and those three are
# not this break. They are 03-06's own red half, still red at the commit this
# was measured at, and they fail identically with the break applied and taken
# away again.
#
# That one test is the finding rather than an aside. It says this part of
# `wx_app.rs` has nothing else reading it for this property, so the whole
# defence of the count is the single test named here.

[[guard]]
name = "the count of sign-ins that go round the helper is read from the tree"
file = "src/presentation/wx_app.rs"
suite = "one_sign_in_per_piece_of_work"
before = """        let controller =
            match handle.block_on(crate::application::mail_session::the_session_at(&account)) {
                Ok(session) => session,
                Err(why) => {
                    answer_a_failed_push(why);
                    return;
                }
            };

        let outcome = match flag {"""
after = """        let controller = MailController::new();
        let _ = handle.block_on(controller.connect_imap(
            account.imap_server.clone(),
            143,
            account.username.clone(),
            crate::service::protocols::MailAuth::Password(String::new()),
            account.imap_use_tls,
            &account.id,
        ));

        let outcome = match flag {"""
red = [
    "test_the_places_that_sign_in_for_themselves_are_counted_and_named",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/one_sign_in_per_piece_of_work.rs", tests = 7 },
]

# ── One sign-in for an account, not one per piece of work ───────────────────
#
# SCALE-02. Marking one message read used to be a TLS handshake, a CAPABILITY,
# a LOGIN and a SELECT, and twelve places in the main window worked that way.
# The census under `tests/` says nothing new goes round the helper; this says
# the helper actually holds what it hands out, which is a different claim and
# the one a person feels.
#
# The break is the version that would pass a census and fail a person: the
# holder still hands out a session, and signs in for a fresh one every time it
# is asked. Every site still goes through it, so the count stays at nought.
#
# It reddens the connection budget as well, and that is why the budget has no
# record of its own. Two pieces of work signing in twice is three connections
# where two were budgeted, so one break covers both and a second record would
# only redden the same tests, which is the duplication this file's header warns
# about.
#
# Measured by hand on 2026-09-04 against the whole library: 6,133 passed, the
# two below failed, one ignored.
#
# What stayed green is the finding. Closing an account and closing the program
# both still send LOGOUT, because the break only stops the held session being
# handed back and still writes one down. So this record is about reuse and says
# nothing about the closing, which has no record and is defended by its two
# tests alone.

[[guard]]
name = "one sign-in serves every piece of work for an account"
file = "src/application/mail_session.rs"
before = """    let mut holding = held.lock().await;
    if let Some(session) = holding.as_ref() {
        return Ok(session.clone());
    }"""
after = """    let mut holding = held.lock().await;"""
red = [
    "application::mail_session::tests::test_the_connections_open_for_one_account_stay_within_the_budget",
    "application::mail_session::tests::test_two_pieces_of_work_for_one_account_sign_in_once",
]
tests_last_seen = [
    { file = "src/application/mail_session.rs", tests = 14 },
]

# ── Once means once ─────────────────────────────────────────────────────────
#
# A connection that has gone is signed in again and the work tried once more.
# The number that matters is the second one: Gmail allows fifteen connections
# to an account and punishes one that opens more, so a client that keeps trying
# against a server that keeps dropping is what gets somebody locked out.
#
# The break is one more attempt rather than a loop, deliberately. A loop against
# the server this is measured with never ends, so the run would hang rather than
# report, and a guard that hangs says nothing. Two retries where one was
# recorded is the same defect at a size that terminates.
#
# Measured by hand on 2026-09-04 against the whole library: 6,134 passed, the
# one below failed, one ignored. It counted three sign-ins where two were
# recorded, at the server rather than here.
#
# The two tests about a connection that drops once are not in the list and could
# not be: a second retry that is never reached changes nothing about a server
# that answers the first one. Only a server that drops every time tells one
# retry from two, which is why that fixture exists.

[[guard]]
name = "a dropped connection is tried once more and not twice"
file = "src/application/mail_controller.rs"
before = """        match again {
            Err(Error::Network(_)) => Err(the_second_attempt_failed_too()),
            other => other,
        }"""
after = """        if !matches!(&again, Err(Error::Network(_))) {
            return again;
        }
        if $self.sign_in_for(&account).await.is_err() {
            return Err(the_second_attempt_failed_too());
        }
        let third = {
            let mut guard = $self.require_imap().await?;
            let $session = &mut *guard;
            $work
        };
        match third {
            Err(Error::Network(_)) => Err(the_second_attempt_failed_too()),
            other => other,
        }"""
red = [
    "application::mail_session::tests::test_a_server_that_drops_every_connection_is_tried_once_more_and_no_more",
]
tests_last_seen = [
    { file = "src/application/mail_controller.rs", tests = 71 },
    { file = "src/application/mail_session.rs", tests = 14 },
]

# ── A folder listing reads no message text ─────────────────────────────────
#
# The break is not a body table joined into the listing, because nobody would
# write that. It is the plausible one: the snippet column falling back to the
# inline body text when a row has no snippet, which is a change somebody makes
# to fix an empty preview and which reads every body in the folder to show one
# screen.
#
# It is also the break that shows why this guard reads columns rather than
# tables. `messages` is a table a listing is allowed to read, so a check over
# the table names in a query plan is green through this edit. `body_plain`
# shipped in the original CREATE TABLE and cannot be dropped, so the inline
# columns are a place message text can be read from for as long as this program
# exists.
#
# Measured by hand on 2026-09-04: the whole library, 6,081 passed and exactly
# one failed, which is the guard. Nothing else in the tree notices, because the
# fallback changes no answer for any row that has a snippet and every fixture
# here has one. That one test is therefore the whole defence.

[[guard]]
name = "a folder listing reads no message text"
file = "src/data/message_cache/messages.rs"
before = """                m.to_addr, m.cc, m.reply_to, m.date, m.snippet, m.size_bytes,"""
after = """                m.to_addr, m.cc, m.reply_to, m.date,
                COALESCE(m.snippet, m.body_plain), m.size_bytes,"""
red = [
    "data::message_cache::messages::a_listing_reads_no_message_text::test_no_query_a_folder_listing_runs_reads_message_text_or_a_table_it_may_not",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# ── The migration moves every row it found ─────────────────────────────────
#
# The break passes over a row whose plain text is there, which is the shape a
# migration acquires when somebody adds a condition to it: the rows are still
# found, still counted, and some of them are left where they were. `moved` goes
# on reporting the number found, so the count is no defence.
#
# Measured by hand on 2026-09-04, the whole library, 6,084 passed and four
# failed: the three tests over a database written from the old schema that read
# text back, count what is still inline, or say which copy wins, and the older
# one over a database this program wrote itself.
#
# It was five within the hour. The commit after this one added a test asking
# that text put back inline is moved by the next open however finished the file
# looks, which reaches the same rule and reddens on the same break, and the
# count check caught it in the same session it was written. That is the failure
# mode `CLAUDE.md` describes happening at its shortest possible range, and the
# name is on the list now.
#
# Two of the tests beside them stay green through it, and that is worth writing
# down. The count of messages does not move, because the break loses text and
# not rows. And the second-open comparison passes because it compares one
# broken migration with the same broken migration: both opens leave the same
# thing behind, so nothing differs. A test that two runs agree cannot see a
# fault that is the same both times.

[[guard]]
name = "the inline body migration moves every row it found"
file = "src/data/message_cache/bodies.rs"
before = """        let moved = pending.len();
        for (id, plain, html) in pending {
            self.save_message_body(id, plain.as_deref(), html.as_deref())?;"""
after = """        let moved = pending.len();
        for (id, plain, html) in pending {
            if plain.is_some() {
                continue;
            }
            self.save_message_body(id, plain.as_deref(), html.as_deref())?;"""
red = [
    "data::message_cache::bodies::a_database_from_the_schema_that_kept_text_inline::test_a_row_holding_text_in_both_places_keeps_the_inline_copy",
    "data::message_cache::bodies::a_database_from_the_schema_that_kept_text_inline::test_every_message_that_had_text_still_has_it_after_the_open",
    "data::message_cache::bodies::a_database_from_the_schema_that_kept_text_inline::test_the_inline_columns_are_empty_afterwards_so_the_space_is_reclaimed",
    "data::message_cache::bodies::tests::test_existing_inline_bodies_are_migrated_not_lost",
    "data::message_cache::bodies::the_migration_that_runs_on_every_open::test_text_put_back_inline_is_moved_by_the_next_open_however_finished_the_file_looks",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
]

# ── A sync brings down no attachment ───────────────────────────────────────
#
# SCALE-04's third deliverable. The break is not a file fetched during a sync,
# because a sync has nothing to fetch one with: a header fetch does not read a
# message's structure, so `ImapMessage` carries `has_attachments` and no list.
# It is the other half of the same promise, and a sync really could do it:
# writing an attachment row from the one bit it does know, for a message nobody
# has opened. That is a description with no file behind it, in the table the
# reader reconciles against, on every message in the mailbox.
#
# Measured by hand on 2026-09-04: the whole library, 6,087 passed and one
# failed, which is this guard. Nothing else in the tree asks whether a sync
# writes attachments, so this one test is the whole of it.

[[guard]]
name = "a sync writes no attachment for a message nobody has opened"
file = "src/application/mail_sync.rs"
before = """    let arrived = cache.upsert_messages(&arriving)?;
"""
after = """    let arrived = cache.upsert_messages(&arriving)?;
    for (row, message) in arrived.iter().zip(arriving.iter()) {
        if message.has_attachments {
            cache.replace_attachments(
                *row,
                &[crate::data::message_cache::CachedAttachment {
                    id: 0,
                    message_id: *row,
                    filename: "attachment".to_string(),
                    mime_type: "application/octet-stream".to_string(),
                    size: message.size_bytes.unwrap_or_default(),
                    content_id: None,
                    description: crate::service::mime::WhatTheSenderSaid::Nothing,
                }],
            )?;
        }
    }
"""
red = [
    "application::mail_sync::tests::test_a_sync_over_a_message_with_an_attachment_stores_no_attachment_and_no_file",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── The read of every message on every open ────────────────────────────────
#
# `migrate_inline_bodies` runs on every open of every database and always will,
# because the two columns it empties shipped in the original CREATE TABLE and a
# column that shipped is never dropped here. What makes that free is an index
# whose condition is the query's condition exactly.
#
# The break is the edit somebody makes while tidying: narrowing the index to
# one of the two columns. It still exists, still has a sensible name, and
# SQLite can no longer use it, because a query asking "A or B" does not imply
# an index built for "A". Nothing about the schema looks wrong afterwards and
# every open is a full read of the messages table again.
#
# Measured by hand on 2026-09-04, the whole library: 6,090 passed and two
# failed, both of them the plan assertions. Nothing else in the tree notices,
# because the answer is the same either way and only the cost changes. That is
# the whole reason these two tests exist: a cost is not something the rest of a
# suite can see.

[[guard]]
name = "the migration that runs on every open reads an index rather than every message"
file = "src/data/message_cache/mod.rs"
before = """            "CREATE INDEX IF NOT EXISTS idx_messages_inline_body
                 ON messages(id)
                 WHERE body_plain IS NOT NULL OR body_html IS NOT NULL","""
after = """            "CREATE INDEX IF NOT EXISTS idx_messages_inline_body
                 ON messages(id)
                 WHERE body_plain IS NOT NULL","""
red = [
    "data::message_cache::bodies::the_migration_that_runs_on_every_open::test_a_database_with_no_messages_at_all_opens_and_is_asked_the_same_way",
    "data::message_cache::bodies::the_migration_that_runs_on_every_open::test_the_migration_reads_an_index_rather_than_every_message_in_the_file",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

# ── Nothing remembers that the migration has been done ─────────────────────
#
# The break is the optimisation the next person will reach for, and it is the
# one the index exists to make unnecessary: take something already in the file
# as a marker that the work is finished, and stop asking. Here that marker is
# "the body table holds something", which is true of every database that has
# been opened once.
#
# What it costs is not a slow open. It is message text left in the inline
# columns that nothing else will ever move, in a file the user believes holds
# their mail. Which is why the answer to the cost was an index and not a
# marker: an index makes the question free, and a marker makes it skippable.
#
# Measured by hand on 2026-09-04, the whole library: 6,090 passed and two
# failed. One is the test written for exactly this, and the other is the
# both-places test from the plan before, which puts a body row and an inline
# copy on the same message and so trips the same shortcut. Everything else
# stays green, including every test that migrates a database with no body rows
# in it yet, because the shortcut does not fire there.

[[guard]]
name = "nothing remembers that the inline body migration has been done"
file = "src/data/message_cache/bodies.rs"
before = """    pub fn migrate_inline_bodies(&self) -> Result<usize> {
        let mut stmt = self"""
after = """    pub fn migrate_inline_bodies(&self) -> Result<usize> {
        // Anything already moved means the rest were too.
        if self.cached_body_bytes()? > 0 {
            return Ok(0);
        }
        let mut stmt = self"""
red = [
    "data::message_cache::bodies::a_database_from_the_schema_that_kept_text_inline::test_a_row_holding_text_in_both_places_keeps_the_inline_copy",
    "data::message_cache::bodies::the_migration_that_runs_on_every_open::test_text_put_back_inline_is_moved_by_the_next_open_however_finished_the_file_looks",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
]

# ── Which end of a folder's numbering a filed row takes ────────────────────
#
# The dispatch is what makes the `next_local_uid` wrap unreachable, and phase 1
# deferred that wrap as a defect in the function. It is not one the function can
# fix: the harm is a row filed into a folder a server fills being given the
# number that server is about to issue, which hides a real message forever and
# lets a later sync write over the copy. `next_reserved_uid`'s own doc comment
# is where that reasoning is written down.
#
# The break makes every folder count up, whatever its path. It compiles, it
# reads as a simplification, and it is the whole defect.
#
# Measured by hand on 2026-09-04, the whole library: 6,096 passed and five
# failed. The dispatch test written for it, and four that were already there
# and had been holding this rule from four different directions without
# anything saying so: an import into a folder a server fills, a sent copy filed
# here, and two in the cache about a message filed into such a folder.

[[guard]]
name = "which end of a folder's numbering a filed row takes"
file = "src/data/message_cache/messages.rs"
before = """        Ok(match path {
            Some(path) => WrittenDownAs::for_folder(&path),"""
after = """        Ok(match path {
            Some(_) => WrittenDownAs::FiledHereCountingUp,"""
red = [
    "application::importing_messages::end_to_end::test_a_message_imported_into_a_folder_the_server_fills_leaves_its_numbers_alone",
    "application::moves_waiting::tests::test_a_crossing_made_here_moves_the_row_into_the_other_accounts_folder_and_waits",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_cannot_be_asked_answers_not_reached_and_the_bytes_stay",
    "application::moves_waiting::tests::test_a_read_of_the_destination_neither_forgets_nor_doubles_what_a_waiting_move_holds",
    "application::moves_waiting::tests::test_a_server_that_hung_up_answers_not_reached_and_the_row_goes_on_waiting",
    "application::moves_waiting::tests::test_made_here_moves_the_row_and_keeps_the_move_waiting",
    "application::sent_copy::tests::test_a_copy_kept_here_takes_a_number_the_server_will_never_issue",
    "data::message_cache::messages::tests::test_a_message_filed_into_a_folder_a_server_fills_is_marked_as_one_filed_here",
    "data::message_cache::messages::tests::test_a_message_filed_into_a_folder_a_server_fills_leaves_the_servers_numbers_alone",
    "data::message_cache::messages::the_dispatcher_is_what_keeps_the_two_numberings_apart::test_a_folder_a_server_names_counts_down_from_the_top",
    "data::message_cache::moves_waiting::tests::test_the_row_is_put_where_the_server_holds_it_with_the_marker_off",
]
tests_last_seen = [
    { file = "src/application/importing_messages.rs", tests = 41 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
    { file = "src/application/sent_copy.rs", tests = 22 },
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/data/message_cache/moves_waiting.rs", tests = 15 },
]
# Re-measured 2026-09-20 by 11-11.1.3 when the count check named it for a
# test added to `messages.rs`: six short, none of them that plan's, all
# 11-07.1's and 11-07.2's of 2026-09-19 in `moves_waiting` on both layers,
# new modules no record named. A move made here files the row under the
# marker this decides, and a replay reads the marker back.

# ── Nothing new asks for the counting-up numbering directly ────────────────
#
# `03-RESEARCH.md` names the thing that makes the wrap reachable: one new direct
# caller pointed at a folder a server fills, which brings it within one row
# rather than four billion. So the break is that caller, written at the most
# plausible place for it, where a sent copy is filed.
#
# Measured by hand on 2026-09-04, the whole library: 6,099 passed and two
# failed. The census, naming the new site, and the sent-copy test that already
# asserted the number is one the server will never issue.
#
# The second is worth reading before concluding the census is redundant. It
# covers this one site, by its behaviour, and it is the only such test: the
# census covers every site there could be, including ones written into modules
# that do not exist yet, which is what nothing else here can do.

[[guard]]
name = "nothing new asks for the counting-up numbering without going through the dispatcher"
file = "src/application/sent_copy.rs"
before = """    let uid = cache
        .next_uid_for_filing(row.id)
        .map_err(|e| e.to_string())?;"""
after = """    let uid = cache.next_local_uid(row.id).map_err(|e| e.to_string())?;"""
red = [
    "application::sent_copy::tests::test_a_copy_kept_here_takes_a_number_the_server_will_never_issue",
    "data::message_cache::messages::the_dispatcher_is_what_keeps_the_two_numberings_apart::test_only_the_dispatcher_and_a_pop_inbox_ask_for_the_counting_up_numbering",
]
tests_last_seen = [
    { file = "src/application/sent_copy.rs", tests = 22 },
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# ── Mail archived with no label counts toward its conversation ─────────────
#
# D-08 asks which folders a conversation is counted across, and it used to
# answer by dropping every folder holding a copy of all mail. On Gmail that is
# All Mail, and archiving a message without giving it a label leaves it in All
# Mail and nowhere else. So the message was excluded along with the folder, and
# a conversation with nothing outside All Mail had no rows at all and left the
# list.
#
# The break is that exclusion, put back exactly as it was. It reads as a
# simplification of a query that now says the same thing in a longer way, and it
# is the whole defect.
#
# Worth knowing before trusting the agreement record further down: this break is
# invisible to it. The two queries still agree under it, because both lose the
# same rows. A guard asking whether two answers match cannot see the case where
# both are wrong in the same way, which is why this record exists separately.
#
# Measured by hand on 2026-09-04, the whole library: 6,101 passed and four
# failed, which are the four written for it. The first of them is the one to
# read, because it is the only one that fails on a row being absent rather than
# on a number being short.
#
# Re-measured an hour later in the same session, because the plan's second task
# added tests that reach this rule. Four became five, and the fifth is worth
# reading: it is about a message with no identifier of its own, and its fixture
# puts two such messages in All Mail, so excluding the folder takes them both.
# Nothing about identity is what makes it fail. It is here for the same reason
# every other one is, that the folder holds the only copy.

[[guard]]
name = "mail archived with no label counts toward its conversation"
file = "src/data/message_cache/messages.rs"
# The break's text moved on 2026-09-20 when 11-11.1.3 made the reach's
# clause one of the four parts a scope supplies; the break is the same,
# the all-mail folder dropped from the reach under the whole account.
before = """    reached: "account_id = ?1 AND (?3 = 1 OR id = ?2)","""
after = """    reached: "account_id = ?1 AND (?3 = 0 OR holds_all_mail = 0) AND (?3 = 1 OR id = ?2)","""
red = [
    "data::message_cache::messages::tests::test_a_conversation_archived_with_no_label_is_still_in_the_list",
    "data::message_cache::messages::tests::test_a_delete_reaches_the_message_archived_with_no_label",
    "data::message_cache::messages::tests::test_a_message_archived_with_no_label_counts_toward_its_conversation",
    "data::message_cache::messages::tests::test_the_unread_count_holds_a_message_archived_with_no_label",
    "data::message_cache::messages::tests::test_two_messages_with_no_identity_of_their_own_are_two_messages",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# ── The two conversation queries decide which messages exist with one text ──
#
# D-07 needs the number named before a deletion to be the number of messages
# that go. The doc comment above these two queries has said since it was written
# that they carry the same reach and the same qualifier on purpose, and that two
# queries agreeing today would drift the first time somebody changed one of
# them. It said it, and nothing checked it.
#
# They are one text now. `conversation_scope` is the whole `WITH` and each query
# is that plus its own `SELECT`, so the drift the comment warns about needs
# somebody to take one of them off the shared text first.
#
# Which is what the break is, and the reason is worth reading before anybody
# writes a simpler one. Both sides read `conversation_scope`, so editing the
# scope moves both together and they still agree: a guard over two things built
# from one source can never be falsified by editing that source. The break has
# to reintroduce the divergence the sharing removed, and that is also the defect
# the guard exists to prevent, which is the tell that it is the right one. It is
# written as the most plausible version of that mistake rather than an arbitrary
# one: the query as it stood before this change, with the thread qualifier left
# out.
#
# Measured by hand on 2026-09-04, the whole library: 6,108 passed and three
# failed. The reading, the agreement, and a delete on the archived-mail fixture,
# which is the one that says what a disagreement costs somebody: the question
# names two messages and the deletion takes three.

[[guard]]
name = "the two conversation queries decide which messages exist with one text"
file = "src/data/message_cache/messages.rs"
before = """pub(super) fn messages_in_one_conversation() -> String {
    format!(
        "{scope}
         SELECT m.id
         FROM here m
         WHERE m.thread_id = ?4
         ORDER BY m.received_at ASC, m.id ASC",
        scope = conversation_scope()
    )
}"""
after = """pub(super) fn messages_in_one_conversation() -> String {
    "WITH reach AS (
             SELECT id FROM folders
             WHERE account_id = ?1
               AND (?3 = 1 OR id = ?2)
         )
         SELECT m.id
         FROM messages m
         WHERE m.folder_id IN (SELECT id FROM reach)
           AND m.deleted = 0
           AND m.thread_id = ?4
         ORDER BY COALESCE(m.internaldate, m.date) ASC, m.id ASC"
        .to_string()
}"""
red = [
    "data::message_cache::messages::tests::test_a_delete_reaches_the_message_archived_with_no_label",
    "data::message_cache::messages::tests::test_both_conversation_queries_are_built_on_one_scope",
    "data::message_cache::messages::tests::test_the_row_and_the_action_agree_over_every_conversation_fixture_here",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# ── A message with no identity of its own is only ever itself ───────────────
#
# The last arm of `WHICH_MESSAGE_THIS_ROW_IS`. A message carrying neither
# Gmail's identifier nor a Message-ID has nothing about it to be recognised by,
# and the answer has to be its own row: a message merged with a stranger is a
# message that disappears out of somebody's conversation.
#
# The break makes that arm answer a constant, so every row with no identity is
# the same message as every other one. It is the shape of mistake the arm exists
# to prevent, and it looks harmless, because a placeholder is still a value.
#
# Measured by hand on 2026-09-04, the whole library: 6,110 passed and one
# failed. One test is the whole defence of this arm, which is what the record is
# for. Every other fixture in the module gives its messages identifiers, so the
# arm decides nothing for them and a break in it changes none of their answers.

[[guard]]
name = "a message with no identity of its own is only ever itself"
file = "src/data/message_cache/messages.rs"
before = """                     ELSE 'row:' || m.id"""
after = """                     ELSE 'row:0'"""
red = [
    "data::message_cache::messages::tests::test_two_messages_with_no_identity_of_their_own_are_two_messages",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# ── A copy is recognised by the sender's message identifier when the server
#    gives none ───────────────────────────────────────────────────────────────
#
# The \All attribute is RFC 6154 and holds_all_mail is set from it whoever sent
# it, so a server that is not Gmail can hold a copy of every message and never
# say X-GM-MSGID. With only the Gmail arm and the row fallback, the two rows are
# two messages, and every conversation on such a server reports twice its size.
# That is the harm the folder exclusion used to prevent, so taking the exclusion
# out without this arm trades one defect for another.
#
# The plan for 03-04 said to reuse searching.rs's COALESCE(m.gmail_msgid, m.id),
# which is the Gmail arm and the row fallback with nothing between them. The
# break is therefore that plan as written, which is worth saying: it is not a
# hypothetical mistake, it is the one that was nearly made.
#
# Measured by hand on 2026-09-04, the whole library: 6,111 passed and two
# failed. The copy counted twice, and the row still holding the bracketed
# spelling, which is the second half of the arm: the column held both spellings
# until 02.1-06 and the backfill that corrects the older rows is not fatal when
# it fails.

[[guard]]
name = "a copy is recognised by the sender's message identifier when the server gives none"
file = "src/data/message_cache/messages.rs"
before = """                     WHEN TRIM(TRIM(m.message_id), '<>') <> ''
                         THEN 'header:' || TRIM(TRIM(m.message_id), '<>')
                     ELSE 'row:' || m.id"""
after = """                     ELSE 'row:' || m.id"""
red = [
    "data::message_cache::messages::tests::test_a_copy_on_a_server_that_gives_no_identifier_of_its_own_is_counted_once",
    "data::message_cache::messages::tests::test_a_row_still_holding_the_bracketed_spelling_is_the_same_message_as_its_copy",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# ── The send path reads the switch, rather than only calling the decision ───
#
# SCALE-05, and the live defect it starts from. `offline_mode` was declared,
# initialised, toggled and mirrored on an update, and read by nothing that
# decided anything. The View menu said "Offline mode enabled - outgoing mail
# will be queued" and the composer's Send queued the message and handed the
# whole queue to a server in the next line, whatever the switch said.
#
# The break is the half-fix, not the absent one: the decision is called, with a
# constant instead of what the window believes. The call is there, the promise
# is still false, and a check that only asked whether `when_it_goes` is named
# would pass on it. That is why the census asks about `reachability_of` as
# well.
#
# Measured by hand on 2026-09-05, every target with --no-fail-fast: 6,183
# library tests passed and one test failed in the whole tree.
#
# That one test is the finding rather than an aside. Reaching this arm needs a
# window, a frame and a composer, so nothing in the library reads it, and the
# whole defence of the switch being read is the single source-reading test
# named below. The same shape 03-02 found for the sign-in census.

[[guard]]
name = "the send path reads what the window believes about the network"
file = "src/presentation/wx_app.rs"
suite = "nothing_leaves_the_outbox_unasked"
before = """                        reachability_of(state),"""
after = """                        crate::application::sending_later::Reachability::Online,"""
red = [
    "test_the_send_path_asks_whether_the_message_goes_before_it_flushes",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/nothing_leaves_the_outbox_unasked.rs", tests = 7 },
]

# ── A network loss is said once, because it is a change and not an answer ───
#
# SCALE-05 and guardrail 5. One unplugged cable is many failures: a sync walks
# several folders and each one asks a server, and a watch loop is sitting on a
# connection that has gone. Said per failure, that is a screen reader saying
# the same sentence eight times over somebody trying to read a message.
#
# The break is the shape that gets this wrong, and it is the shape somebody
# reaches for first: answer from the observation rather than from the change.
# It is also what the red half of this task was written against.
#
# Measured by hand on 2026-09-05, every target with --no-fail-fast, against a
# tree with nothing else red: 6,189 tests passed and four failed. The counting,
# the laptop opened where there has never been a signal, the repeat while
# nothing has changed, and the two round trips.
#
# --no-fail-fast matters and is not decoration. Without it the runner stops at
# the first failing target and the list that comes out is a prefix, which reads
# exactly like a complete answer and is wrong in the direction this file exists
# to catch.
#
# What this cannot see: whether the sentence is heard once. It says the state
# hands out one answer, not that the queue speaks it once, and not that a
# person hears it as a state rather than as an error. That is ledger 78.

[[guard]]
name = "the network is spoken about when it changes, not when it is asked"
file = "src/application/the_network_coming_and_going.rs"
before = """        let was = std::mem::replace(&mut self.there_is_a_network, there_is_a_network);
        match (was, there_is_a_network) {
            (true, false) => WhatToDoAboutIt::SayItWentAndGoOffline,
            (false, true) => WhatToDoAboutIt::OfferToGoBackOnline,
            _ => WhatToDoAboutIt::Nothing,
        }"""
after = """        self.there_is_a_network = there_is_a_network;
        match there_is_a_network {
            false => WhatToDoAboutIt::SayItWentAndGoOffline,
            true => WhatToDoAboutIt::OfferToGoBackOnline,
        }"""
red = [
    "application::the_network_coming_and_going::tests::test_a_network_that_was_already_gone_at_the_start_is_not_a_change",
    "application::the_network_coming_and_going::tests::test_asking_again_while_the_network_is_still_there_says_nothing",
    "application::the_network_coming_and_going::tests::test_ten_failures_from_one_loss_are_announced_once",
    "application::the_network_coming_and_going::tests::test_the_network_coming_back_and_going_again_is_one_of_each_each_time",
]
tests_last_seen = [
    { file = "src/application/the_network_coming_and_going.rs", tests = 10 },
]

# ── The network coming back is an offer, and an offer sends nothing ────────
#
# SCALE-05 and guardrail 7, which exists here because a push to main once cut
# two releases nobody asked for. Mail leaving this computer is publishing, and
# the network returning is the moment it is easiest to empty the Outbox and the
# moment it is most wrong to: nobody asked, and mail that has gone cannot be
# brought back. Wired straight to the flush, closing a laptop on a train and
# opening it in an office sends whatever was written in between.
#
# Until this branch that rule held by accident. flush_outbox had two callers,
# both of them a key somebody pressed, so the Outbox was never flushed unasked
# because nothing automatic flushed it at all.
#
# The break is the wiring somebody would write first: the arm that raises the
# offer sends the mail as well, which makes the offer a description of
# something that has already happened.
#
# Measured by hand on 2026-09-05, every target with --no-fail-fast: two tests
# went red, the guardrail-7 assertion and the count of places that hand mail to
# a server, which reads four.
#
# What this cannot see: whether a person hears the offer, finds it, and
# understands that pressing it sends. That is ledger 79 and 80.

[[guard]]
name = "the network coming back offers rather than sending"
file = "src/presentation/wx_app.rs"
suite = "nothing_leaves_the_outbox_unasked"
before = """            back_online_offer.panel.show(true);
            // The list below it has changed size."""
after = """            back_online_offer.panel.show(true);
            flush_outbox(AppHandles { state, tx, rt });
            // The list below it has changed size."""
red = [
    "test_the_network_coming_back_sends_nothing",
    "test_the_places_that_hand_mail_to_a_server_are_the_ones_counted",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/nothing_leaves_the_outbox_unasked.rs", tests = 7 },
]

# ── The window that asks which copy to keep ─────────────────────────────────
#
# SCALE-06 and guardrail 2. Both records are measured against
# `tests/the_conflict_choice_can_be_heard.rs`, which reads the source, because
# building that window needs a display and a running application and nothing in
# the library reaches it. The `suite` coupling is what makes the target run on
# the commits that could break it rather than only on the ones that change it.
#
# Measured by hand on 2026-09-05, each break applied on its own.

[[guard]]
# The defect `CLAUDE.md` records as having compiled and passed 324 tests.
# `set_name` sets an internal wxWidgets identifier and reaches neither MSAA nor
# UI Automation, so a control named that way is read out as its type and
# nothing else. Sixteen widgets were once named that way here.
#
# The break is one control swapped to the call that names nothing, which is
# what somebody reaching for the obvious method would write. Two tests: the
# count of controls against the count of names, and the refusal of `set_name`
# outright.
name = "every control in the choosing window is named where a screen reader reads"
file = "src/presentation/wx_conflict_choice.rs"
suite = "the_conflict_choice_can_be_heard"
before = """    set_accessible_name(&keep_here, KEEP_WHAT_IS_HERE);"""
after = """    keep_here.set_name(KEEP_WHAT_IS_HERE);"""
red = [
    "test_every_control_in_the_choosing_window_is_named_where_a_screen_reader_reads",
    "test_the_choosing_window_never_reaches_for_the_call_that_names_nothing",
]
tests_last_seen = [
    { file = "src/presentation/wx_conflict_choice.rs", tests = 0 },
    { file = "tests/the_conflict_choice_can_be_heard.rs", tests = 5 },
]

[[guard]]
# A window nothing opens is a window nobody has, which is guardrail 1 and the
# defect this project keeps finding. The sync's own sentence says to open
# Contacts and choose, so an emptied menu arm turns that sentence into a
# direction to nowhere.
#
# The first measurement of this reddened nothing, and that is the finding. The
# check read the whole file for a mention of `ask_which_copy_to_keep`, and the
# mention survived the break because it lives in the function the arm used to
# call. It reads what follows the arm now. Measured again after that and it
# reddens.
name = "the choosing window can be reached from the menu"
file = "src/presentation/wx_app.rs"
suite = "the_conflict_choice_can_be_heard"
before = """                            choose_which_copy_to_keep(
                                &state,
                                &message_cache,
                                &frame,
                                &ui_tx,
                                &runtime,
                            );"""
after = """                            let _ = &message_cache;"""
red = [
    "test_the_window_can_be_reached_from_the_menu",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_conflict_choice_can_be_heard.rs", tests = 5 },
]

# ── The calendar asks the same question ─────────────────────────────────────
#
# SCALE-06. A calendar item changed here and changed at the server had its
# disagreement resolved in this computer's favour and shown to nobody: the read
# skipped any event with a change waiting, so the server's copy was dropped on
# the floor. The opposite direction from the contacts defect and the same
# failure, which is that nobody was asked.
#
# Measured by hand on 2026-09-05, the whole library with --no-fail-fast.

[[guard]]
# The break is the skip as it was written before this branch: any event with a
# change waiting is passed over whatever the server's etag says. One test, and
# it is the sync-path one rather than any of the pure ones, because the pure
# decision is still right when nothing consults it.
name = "a calendar item both sides changed is held rather than dropped"
file = "src/application/caldav_sync.rs"
before = """            if crate::application::calendar_conflict::both_copies_moved(
                held,
                remote.etag.as_deref(),
            ) {"""
after = """            if false {"""
red = [
    "application::calendar_conflict::tests::test_a_calendar_item_changed_here_and_at_the_server_is_held_rather_than_dropped",
]
tests_last_seen = [
    { file = "src/application/calendar_conflict.rs", tests = 5 },
    { file = "src/application/caldav_sync.rs", tests = 67 },
]

# ── A flag change made with no server ───────────────────────────────────────
#
# SCALE-06's mail half, and guardrail 7. Marking a message read or starring it
# is applied here at once and pushed straight after; every failure used to take
# the same path back, so starring a message on a train starred it, un-starred
# it a moment later, and said why. Nobody had refused the change. Nobody had
# been asked.
#
# Measured by hand on 2026-09-05, each break applied on its own.

[[guard]]
# The distinction, and the break is the version somebody would write reaching
# for a wildcard: everything that is not this computer's own gate is the server
# having answered. Four tests, and two of them drive a loopback server rather
# than a built error, which is the half that proves the mapping holds through
# the library's own error type.
name = "a lost connection is told apart from a server that said no"
file = "src/application/flag_changes_waiting.rs"
before = """        Error::Network(_) | Error::Authentication(_) => WhyThePushFailed::TheServerWasNeverAsked,
        Error::Security(_) => WhyThePushFailed::ThisComputerRefusedIt,
        Error::Config(_)
        | Error::Protocol(_)
        | Error::Api { .. }
        | Error::Other(_)
        | Error::InPlainWords(_) => WhyThePushFailed::TheServerSaidNo,"""
after = """        Error::Security(_) => WhyThePushFailed::ThisComputerRefusedIt,
        _ => WhyThePushFailed::TheServerSaidNo,"""
red = [
    "application::flag_changes_waiting::tests::test_a_lost_connection_is_the_server_never_having_been_asked",
    "application::flag_changes_waiting::tests::test_a_server_that_drops_the_connection_is_read_as_never_having_been_asked",
    "application::flag_changes_waiting::tests::test_a_sign_in_that_did_not_happen_leaves_the_change_worth_keeping",
    "application::flag_changes_waiting::tests::test_a_waiting_change_the_server_still_cannot_be_asked_about_goes_on_waiting",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 30 and a
    # measurement here that found the same three. A waiting move asks the same
    # question of the same answer, so a server that hung up has to be told
    # apart from one that said no there too, in a file this record had never
    # named.
    "application::moves_waiting::tests::test_a_server_never_reached_leaves_the_move_waiting",
    "application::moves_waiting::tests::test_a_server_that_hung_up_answers_not_reached_and_the_row_goes_on_waiting",
    "application::moves_waiting::tests::test_a_server_that_hung_up_on_the_copy_leaves_it_waiting",
]
tests_last_seen = [
    { file = "src/application/flag_changes_waiting.rs", tests = 16 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
]

[[guard]]
# Guardrail 7. The break is the wiring somebody actually writes, and finding
# that out took two goes.
#
# The first version of this test asked only whether the arm called the sending
# itself, and it stayed green against the break below. Nobody wires the
# network's return straight to a flag change; what they write is a mail check,
# because a check when the network returns sounds obviously useful, and a check
# now sends the waiting changes on the session it opens. The test reads a list
# of everything that ends at a server now.
#
# The second version stayed green too, for a different reason: it searched for
# the bare variant name and found the line that *sends* the update, hundreds of
# lines above the arm that handles it, so it read nine hundred characters of an
# unrelated function. It matches the arm's own opening now.
#
# Both were measured by putting the break in and watching the file stay green.
# A test that cannot fail proves nothing, and this one could not fail twice.
#
# Re-written on 2026-09-22 by 12-02.1, after run 35520204784 could not build
# it. The runner said, at `wx_app.rs:19635` as the file stood at `0ad66e48`,
# `error[E0061] this function takes 4 arguments but 3 arguments were supplied`,
# with `argument #2 of type Vec<account::Account> is missing`. The recorded
# `after` is the wiring somebody writes, so it has to be the wiring this tree
# would accept: `spawn_mail_sync` gained the accounts it is to check, and a
# call that does not compile is a break nobody can judge rather than a break
# that reddens nothing.
#
# The accounts are read the way the nearby `FolderHasChanged` arm reads them,
# from the locked state, so the break is still a call that would really reach
# a server rather than one that compiles and does nothing.
#
# The commit that moved it: `6ec0ec60`, 10-06's watch, schedule and startup
# check, by `git log -S` over the signature's `accounts: Vec<Account>` line.
# One commit, so it is both the introduction and the mover.
name = "the network coming back starts nothing that reaches a server"
file = "src/presentation/wx_app.rs"
suite = "nothing_sends_a_flag_change_unasked"
before = """        UIUpdate::TheNetworkIsBack => {
            back_online_offer.panel.show(true);"""
after = """        UIUpdate::TheNetworkIsBack => {
            let every: Vec<Account> = lock_state(state).accounts.to_vec();
            spawn_mail_sync(
                AppHandles { state, tx, rt },
                every,
                None,
                crate::application::mail_sync::WhatThisSyncIsFor::WhateverHasChanged,
            );
            back_online_offer.panel.show(true);"""
red = [
    "test_the_network_coming_back_starts_nothing_that_reaches_a_server",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/nothing_sends_a_flag_change_unasked.rs", tests = 3 },
]

# ── What the sender said an attachment is ───────────────────────────────────
#
# `Content-Description` is the one fact about an attachment that a person wrote
# rather than a file system, and for a picture it is the only thing that can say
# what is in it. It reached this program on every message that carried one and
# was dropped at `described`, which built an `AttachmentInfo` out of three
# accessors and ignored the rest of the part.
#
# Measured with `--no-fail-fast` over the whole library on 2026-09-05, twice.
# The first measurement reddened one test, because the reading happens at one
# boundary and one table test covered it. The second reddened two: the same
# afternoon added the lookup that borrows a description off the `img` naming
# the part, and a part whose header is dropped now falls through to that lookup
# and comes back with the picture's words instead. The precedence test sees it.
#
# Worth leaving written down. This record went stale inside the session that
# wrote it, which is the shape `CLAUDE.md` says is the common one and never
# announces itself; the only reason it was caught is that the second task
# re-ran `--remeasure` rather than assuming the first task's record still held.

[[guard]]
name = "a description the sender gave survives the boundary"
file = "src/service/mime.rs"
before = """        size: part.contents().len(),
        description: WhatTheSenderSaid::read(part.content_description()),"""
after = """        size: part.contents().len(),
        description: WhatTheSenderSaid::Nothing,"""
red = [
    "service::mime::tests::test_what_a_sender_writes_in_content_description_becomes",
    "service::mime::tests::test_the_description_the_sender_wrote_outranks_the_one_on_the_markup",
]
tests_last_seen = [
    { file = "src/service/mime.rs", tests = 52 },
]

# The accessible half of the same feature, and the half likelier to be tidied
# away. An attachment row ends with a clause about the description whether or
# not there is one, because a row that stops after the size reads exactly like
# a row whose description this program dropped, and the reader cannot tell an
# undescribed picture from a broken one.
#
# The break is the tidy-up: answer nothing where there is nothing to say.

[[guard]]
name = "an attachment nobody described says so rather than trailing off"
file = "src/presentation/reader_text.rs"
before = """            WhatTheSenderSaid::Nothing => crate::application::long_text::NO_DESCRIPTION.to_string(),"""
after = """            WhatTheSenderSaid::Nothing => String::new(),"""
red = [
    "presentation::reader_text::tests::test_an_attachment_reads_as_a_name_a_kind_a_size_and_what_the_sender_said",
    "presentation::reader_text::tests::test_an_attachment_row_the_sender_described_with_nothing_says_so_in_words",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# ── Borrowing a description off the picture that names the part ─────────────
#
# An image with no `Content-Description` of its own takes the `alt` on the
# `img` that names it, which is where most senders who describe a picture at
# all put it. The plausible future mistake is letting the borrowed one win: it
# is a guess about which element meant which part, and an explicit header is
# the sender saying it outright.
#
# The break lets the lookup overwrite a description the sender wrote. Measured
# with `--no-fail-fast` over all 6,263 tests on 2026-09-05: one test reddens,
# and it is the one this is about.

[[guard]]
name = "the sender's own description of a picture outranks a borrowed one"
file = "src/service/mime.rs"
before = """        one.content_id.is_some() && one.description == WhatTheSenderSaid::Nothing"""
after = """        one.content_id.is_some()"""
red = [
    "service::mime::tests::test_the_description_the_sender_wrote_outranks_the_one_on_the_markup",
]
tests_last_seen = [
    { file = "src/service/mime.rs", tests = 52 },
]

# The other half of the same lookup, and the worse failure of the two. Matching
# by content id is what stops one picture's description landing on another
# part: a description said in the sender's voice about the wrong file is worse
# than no description, because nothing about it reads as a guess.
#
# The break takes the first alt in the body whatever it names, which is the
# shape a "close enough" version of this would have. Measured with
# `--no-fail-fast` over all 6,263 tests on 2026-09-05: two tests redden, and
# the second is the malformed-markup one, because a body the parser welds into
# one element yields a content id no part has.

[[guard]]
name = "a borrowed description comes from the picture that names that part"
file = "src/service/mime.rs"
before = """        if let Some(alt) = called.get(named) {"""
after = """        if let Some(alt) = called.values().next() {"""
red = [
    "service::mime::tests::test_a_part_no_picture_in_the_markup_names_stays_undescribed",
    "service::mime::tests::test_markup_too_broken_to_read_leaves_the_part_undescribed_rather_than_guessed_at",
]
tests_last_seen = [
    { file = "src/service/mime.rs", tests = 52 },
]

# ── The mailing-list warning, and the three ways it can go back to silence ──
#
# `blocking::MayBlock::YesButFirst` had a written sentence, three fixtures and
# an arm of `may_block` from the day blocking was built, and no shipped build
# ever returned it. `List-Unsubscribe` was never parsed, so the one production
# construction of `WhatIsAlreadyTrue` passed `None`, and the arm was unreachable
# through 344 commits with everything green.
#
# Three records because the value crosses three places where it can be dropped
# without anything failing, and each of the three is a different kind of
# silence. The parse can stop reading the header. The fetch can stop asking a
# server for it. And the handler can go back to its literal. Each of the other
# two stays correct while any one of them goes, which is exactly why it lasted.
#
# All three were measured by hand with `--no-fail-fast` on 2026-09-05, and the
# passing totals are recorded beside each.

[[guard]]
name = "what a message said about leaving a list survives the parse"
file = "src/service/mime.rs"
before = """        list_unsubscribe: how_to_leave_the_list(&message),"""
after = """        list_unsubscribe: None,"""
# Measured by hand: 6265 passed, 5 failed, 1 ignored.
red = [
    "service::mime::tests::test_what_a_message_said_about_leaving_the_list_arrives_with_its_brackets_on",
    "service::mime::tests::test_a_way_out_that_is_there_and_empty_is_not_a_way_out_that_is_absent",
    "service::mime::tests::test_a_way_out_carrying_things_that_are_not_writing_arrives_as_writing",
    "service::mime::tests::test_a_way_out_too_long_to_be_one_is_cut_rather_than_read_out",
    "service::mime::tests::test_a_way_out_of_ordinary_length_is_not_cut",
]
tests_last_seen = [
    { file = "src/service/mime.rs", tests = 52 },
]

# The far end, and the quietest of the three. `HEADER_FIELDS` names the headers
# an IMAP fetch asks a server for, one at a time, because a whole header block
# carries DKIM signatures and a Received chain. A header that is not on it never
# arrives, so every hop after the fetch can be correct and operating on nothing,
# and no test on either side of that hop can see it: the parse parses the header
# when it is there, and the column stores it when it is given.
#
# The break is the state the tree was in until 2026-09-05, which is the header
# simply absent from the list.
#
# Measured against the census rather than the library on purpose. The whole
# library was run against this break as well and stayed green, 166 passed and
# nothing failed under `--lib service::protocols::imap::`, which is the finding
# rather than an aside: nothing in the library can see this at all.
[[guard]]
name = "the header fetch asks a server for the header the warning is built on"
file = "src/service/protocols/imap.rs"
suite = "the_list_warning_reads_the_message"
before = """     X-MICROSOFT-ANTISPAM DISPOSITION-NOTIFICATION-TO RETURN-RECEIPT-TO \\
     LIST-UNSUBSCRIBE";"""
after = """     X-MICROSOFT-ANTISPAM DISPOSITION-NOTIFICATION-TO RETURN-RECEIPT-TO";"""
# Measured by hand: 7 passed, 1 failed.
red = [
    "test_the_header_fetch_asks_for_the_header_the_warning_is_built_on",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
    { file = "tests/the_list_warning_reads_the_message.rs", tests = 8 },
]

# The near end, and the defect itself. The break is the line the shipped build
# really carried: a field handed a literal instead of the row's own value.
#
# The same measurement as above and the same finding, one file along: the whole
# of `--lib presentation::wx_app::` was run against this break and 199 tests
# passed. A struct literal spelled out inside a key handler cannot be reached
# from a test without standing up a window, and a field set to nothing compiles
# exactly like a field set correctly. That is why this needs a source read at
# all, and why the read has three companions of its own saying it can tell a
# literal from a value and does not count a fixture below the `#[cfg(test)]`.
[[guard]]
name = "the block handler asks the message rather than a constant"
file = "src/presentation/wx_app.rs"
suite = "the_list_warning_reads_the_message"
before = """        how_to_leave_the_list: message.list_unsubscribe.as_deref(),"""
after = """        how_to_leave_the_list: None,"""
# Measured by hand: 7 passed, 1 failed.
red = [
    "test_the_block_handler_reads_the_message_rather_than_a_constant",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_list_warning_reads_the_message.rs", tests = 8 },
]

# ── The two arrival paths, which are two struct literals a field can vanish in ─
#
# Written after the feature, and the tests were green on arrival, which is said
# here rather than left to be inferred. Both breaks were taken by hand
# afterwards and are the whole of the evidence that either test would notice.
#
# The shape is the one 04-01 found in `content_id`: a conversion spelled out as
# a struct literal with thirty fields, where a field written `None` compiles
# exactly like a field carried across, and the site that records attachments had
# written over a content id the parse held for the whole life of that code. Two
# writers rather than one because a fact carried by only one of them is a fact
# that depends on which protocol the account speaks.
#
# Measured by hand on 2026-09-05, each with `--no-fail-fast` over the whole
# library.

[[guard]]
name = "a mailing list found over IMAP reaches the row with its way out on"
file = "src/application/mail_sync.rs"
before = """        list_unsubscribe: message.list_unsubscribe.clone(),"""
after = """        list_unsubscribe: None,"""
# Measured by hand: 6271 passed, 1 failed, 1 ignored.
red = [
    "application::mail_sync::tests::test_what_a_list_said_about_leaving_it_reaches_the_row_this_sync_stores",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

[[guard]]
name = "a mailing list downloaded over POP reaches the row with its way out on"
file = "src/application/pop_sync.rs"
before = """        list_unsubscribe: parsed.list_unsubscribe.clone(),"""
after = """        list_unsubscribe: None,"""
# Measured by hand: 6271 passed, 1 failed, 1 ignored.
red = [
    "application::pop_sync::tests::test_what_a_list_said_about_leaving_it_reaches_a_row_downloaded_over_pop",
]
tests_last_seen = [
    { file = "src/application/pop_sync.rs", tests = 49 },
]

# ── What a message says about its own form, and who is told ──────────────────
#
# `analyze_message_security` answers eight things about every message read and
# `body_safety::from_body` read two of them. So the program worked out that a
# message was PGP-encrypted on every read and told nobody, and opening one gave
# a screenful of armour with no explanation beside it.
#
# The break is the state the tree was in until 2026-09-05, written as the
# question answering "nothing about its form" whatever the body holds. Measured
# against the whole library with --no-fail-fast, because a stopping point
# under-reports and always in the direction that makes a record look right.
[[guard]]
name = "what a message says about its own form is read from the message rather than answered ordinary"
file = "src/application/body_safety.rs"
before = """    if SecurityService::detect_pgp_encrypted(&whole) {
        return WhatTheFormSays::EncryptedWithPgp;
    }
    if SecurityService::detect_pgp_signed(&whole) {
        return WhatTheFormSays::SignedWithPgp;
    }
    WhatTheFormSays::Nothing"""
after = """    let _ = &whole;
    WhatTheFormSays::Nothing"""
# Measured by hand with --no-fail-fast: 6280 passed, 11 failed, 1 ignored.
#
# **Re-measured 2026-09-06 and it had gone short, inside the plan that made it
# short.** 04-09 taught this build to open a PGP message, and everything on that
# path asks this one question first: `opening_pgp` asks it to decide whether to
# offer the armour to a key at all, and the reader's four sentences are only
# reached through the answer. So a break that answers `Nothing` to everything
# reddens three more tests than it did, in two modules that did not exist when
# the record was written.
#
# This is the shape 04-08 recorded and it arrived the same way: a later task in
# the same plan added tests reaching a rule an earlier record was about, and
# nothing failed. What caught it is the count check flagging the file and the
# scoped re-measure it printed. 14 tests in 6,488.
#
# **Short again on 2026-09-16, the same way, inside 09-07.** The composition
# every surface asks, `application::reading_a_message`, asks this question
# through `opening_pgp` before it offers anything to the key, so its three
# readings that meet armour redden, and so does the preview's reading of why
# a message did not open. Four more; 18 now. Found by the count check's
# remedy, written down from what went red.
red = [
    "application::body_safety::tests::test_an_armoured_message_says_it_is_encrypted",
    "application::opening_pgp::tests::test_an_armoured_message_is_handed_to_the_opener",
    "application::reading_a_message::tests::test_a_message_encrypted_to_the_imported_key_is_shown_as_its_words",
    "application::reading_a_message::tests::test_an_armoured_message_with_no_key_here_says_why_and_keeps_its_armour",
    "application::reading_a_message::tests::test_the_reason_a_pgp_message_did_not_open_reaches_the_bar_through_the_fold",
    "presentation::reader_text::encryption_tests::test_the_preview_says_why_a_pgp_message_did_not_open",
    "presentation::reader_text::encryption_tests::test_each_reason_a_pgp_message_did_not_open_is_said_in_its_own_words",
    "presentation::reader_text::encryption_tests::test_the_reasons_are_pairwise_different_and_none_is_the_smime_sentence",
    "application::body_safety::tests::test_a_signature_block_alone_says_the_message_is_signed",
    "application::body_safety::tests::test_a_message_carrying_both_is_reported_as_the_one_that_cannot_be_read",
    "application::body_safety::tests::test_the_formatted_half_alone_is_read_for_armour",
    "application::body_safety::tests::test_a_signed_message_is_not_mistaken_for_an_encrypted_one",
    "presentation::reader_text::encryption_tests::test_an_encrypted_message_explains_its_armour_instead_of_leaving_it_there",
    "presentation::reader_text::encryption_tests::test_a_signed_message_says_so_without_saying_the_signature_was_checked",
    "presentation::reader_text::encryption_tests::test_an_encrypted_message_is_not_reported_as_an_unsafe_one",
    "presentation::reader_text::encryption_tests::test_a_filters_warning_keeps_the_top_of_the_bar_and_the_form_goes_under_it",
    "presentation::reader_text::encryption_tests::test_the_form_is_said_before_the_message_even_when_a_signature_verdict_follows",
    "presentation::reader_text::encryption_tests::test_opening_one_message_as_a_page_says_it_too",
]
tests_last_seen = [
    { file = "src/application/body_safety.rs", tests = 20 },
    { file = "src/application/opening_pgp.rs", tests = 5 },
    { file = "src/application/reading_a_message.rs", tests = 7 },
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# The half-fix, which is the one a check for the question alone cannot see. The
# break leaves the question asked and drops the answer, so the composer still
# names the call and nothing reaches the bar.
#
# This is not hypothetical. The first version of the census asked only whether
# the question was asked, and passed against this exact break when it was
# measured. The census was widened, and this record is what holds it to the
# wider reading.
#
# Measured against the census rather than the library on purpose: the library
# sees this too, and the census is what would see a third composer arriving with
# neither half.
[[guard]]
name = "the reader folds what a message said about its form into the bar rather than dropping it"
file = "src/presentation/reader_text.rs"
suite = "an_encrypted_message_is_not_left_unexplained"
before = """    // by `read_whole`.
    .with_encryption(form)
}"""
after = """}"""
# Measured by hand with --no-fail-fast: 9 passed, 1 failed.
red = [
    "test_every_composer_asks_what_form_the_message_is_in",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
    { file = "tests/an_encrypted_message_is_not_left_unexplained.rs", tests = 10 },
]

# The other half-fix, and the one that reads best in a diff. The question is
# asked, the fold is written, and the fold is handed a named variant, so the
# question decides nothing and every test of the fold itself still passes.
#
# The break spells the type by its full path on purpose, because that is how the
# composers spell it, and a check anchored on the short spelling would be
# answered by whichever one somebody happened to write.
[[guard]]
name = "the fold is handed what the message said rather than a named variant"
file = "src/presentation/reader_text.rs"
suite = "an_encrypted_message_is_not_left_unexplained"
before = """    // by `read_whole`.
    .with_encryption(form)
}"""
after = """    // by `read_whole`.
    .with_encryption(crate::application::body_safety::WhatTheFormSays::Nothing)
}"""
# Measured by hand with --no-fail-fast: 9 passed, 1 failed.
red = [
    "test_every_composer_asks_what_form_the_message_is_in",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
    { file = "tests/an_encrypted_message_is_not_left_unexplained.rs", tests = 10 },
]

# ── A reading gate that admits everything ───────────────────────────────────
#
# `how_it_reads` is the one answer the reader window's gate and the worker's
# routing both read, so widening it too far is a spreadsheet opened in a text
# control rather than a sentence saying it cannot be read. The failure has no
# other symptom: everything still builds, the tab still opens, and what is in
# it is a screenful a screen reader reads out character by character.
#
# Measured by hand 2026-09-05 with `cargo test --all-targets --no-fail-fast`.
# One test went red. `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
# was red in that run as well and is not listed, because it was already red
# before the break was applied: four records were carrying counts from before
# this task added tests, and they were re-measured afterwards.

[[guard]]
name = "the reading gate admits text and still refuses what it cannot read"
file = "src/presentation/reader_text.rs"
before = """            return Some(HowItReads::Picture);
        }
        None"""
after = """            return Some(HowItReads::Picture);
        }
        Some(HowItReads::Text)"""
red = [
    "presentation::wx_reader::tests::test_everything_else_is_not_pretended_to_be_readable",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
    { file = "src/presentation/wx_reader.rs", tests = 14 },
]

# ── A picture whose sender said nothing, and a preview that says nothing ────
#
# The whole of what a reader who cannot see a picture is given is what the
# sender wrote about it, and the commonest case by a wide margin is that they
# wrote nothing. Saying so is the accessible half of the feature: a gap where
# those words would be is indistinguishable from this program having dropped
# them, and the reader is left unsure whether to go looking. It fails silently,
# because a preview with a blank line in it still opens, still reads, and still
# says the kind and the size.
#
# Measured by hand 2026-09-05 with `cargo test --all-targets --no-fail-fast`,
# against a tree with no other failure in it. One test went red.

[[guard]]
name = "a picture nobody described says so rather than leaving the line blank"
file = "src/presentation/reader_text.rs"
before = """        WhatTheSenderSaid::Nothing => format!(
            "This picture came with {}, so nothing here can say what is in it.",
            crate::application::long_text::NO_DESCRIPTION
        ),"""
after = """        WhatTheSenderSaid::Nothing => String::new(),"""
red = [
    "presentation::reader_text::picture_preview_tests::test_a_picture_the_sender_described_with_nothing_says_so_before_anything_else",
    # Arrived one commit after this record was written, and the record was one
    # commit stale as a result. The picture's accessible name is composed from
    # the same sentence, so emptying that sentence leaves the bitmap announced
    # as "graphic", which is the same defect reaching a different surface.
    "presentation::reader_text::picture_shown_tests::test_the_picture_is_named_from_what_the_sender_said",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# ── A bound that is asked after the thing it was supposed to prevent ────────
#
# The declared size is what a picture claims about itself before anything reads
# it, and refusing on it is the only refusal that costs nothing. Taking it away
# does not obviously break anything: the strict width and height limits handed
# to the decoder still refuse the same file, a little later and with somebody
# else's sentence, and every other test stays green. That is what makes it worth
# a record. A bound that has quietly become a comment looks exactly like a bound
# that works.
#
# Measured by hand 2026-09-05 with `cargo test --all-targets --no-fail-fast`
# against a tree with no other failure in it. One test went red.

[[guard]]
name = "a picture is refused on the size it declares, before anything decodes it"
file = "src/service/picture.rs"
before = """    refuse_what_the_header_declares(bytes)?;"""
after = """    // the declared size is not looked at"""
red = [
    "service::picture::tests::test_a_picture_declaring_more_than_the_bound_is_refused_on_its_declaration",
]
tests_last_seen = [
    { file = "src/service/picture.rs", tests = 14 },
]

# ── A fault and a fact that sound the same ──────────────────────────────────
#
# Three things can be true of a picture in a preview: it is shown, it could not
# be read, or this build draws none of that kind. The middle one is a fault, of
# the file or of this program; the last is a statement about the build. Somebody
# told the wrong one goes looking for damage that is not there, or shrugs at
# damage that is.
#
# It fails silently in the worst way: the tab still opens, still describes the
# picture, still says its kind and size, and reads perfectly well. Only somebody
# who knew which of the two had happened would notice.
#
# This program has paid for collapsing an answer into a failure to ask twice
# before, in `spellcheck::WhatThisMachineOffers` and in the certificate answer.
#
# Measured by hand 2026-09-05 with `cargo test --all-targets --no-fail-fast`
# against a tree with no other failure in it. One test went red.
#
# The break is at the place that chooses, not at the constant. Breaking the
# constant was tried first and reddens two tests rather than one, because the
# census over the three sentences sees them become equal. That is a weaker
# record: it would go on passing if somebody left the constants alone and
# returned the wrong one of them, which is the likelier mistake by far. This
# break is the likelier mistake.

[[guard]]
name = "a picture that could not be read does not say there was never one"
file = "src/presentation/reader_text.rs"
before = """            (None, THE_PICTURE_COULD_NOT_BE_READ)"""
after = """            (None, NO_PICTURE_TO_SHOW)"""
red = [
    "presentation::reader_text::picture_shown_tests::test_a_picture_that_could_not_be_read_says_so_and_draws_nothing",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# ── The verdict a rule asks about is the row's, not a constant ──────────────
#
# A rule may name `safety`, and the arm answering it is one line. The failure
# this is about is not the arm being missing, which four tests would catch at
# once through the list-and-reading pair. It is the arm being there and
# answering the same thing about every message, which is what a half-finished
# version of this looks like: the field is offered in both editors, a rule
# naming it stores and loads, a saved search runs, and every message comes back
# ordinary.
#
# So the break is a constant rather than a removal. It is the shape plan 03-08
# found matters: a removal is caught by the guards that already hold the list
# and the reading in agreement, and a constant is caught by nothing unless
# something asserts that a real verdict reaches the engine.
#
# Measured by hand 2026-09-05 with
# `cargo test --all-targets --no-fail-fast` against a tree with no other
# failure in it. Three tests went red, and which three is the point:
#
#   - the fixture one, where a message is handed to the matcher already
#     carrying a verdict;
#   - the arrival one, which goes through `mail_sync::apply_rules` and
#     `MessageCache::get_message`;
#   - the saved search one, which goes through `messages_a_saved_search_reads`.
#
# The two that did not redden are worth naming as well.
# `test_an_ordinary_message_is_a_verdict_a_rule_can_ask_for_rather_than_a_blank`
# stays green because the constant is `ordinary` and that test asks for
# ordinary; `test_every_field_the_list_names_is_one_the_reading_really_handles`
# stays green because a constant is still a non-empty answer. Both are about
# the arm existing, which this break leaves alone.

[[guard]]
name = "a rule about the verdict reads the message's own, not a constant"
file = "src/application/filters.rs"
before = """            "safety" => Some(Some(message.safety.as_str())),"""
after = """            "safety" => Some(Some(crate::service::safety::Safety::Ordinary.as_str())),"""
red = [
    "application::filters::the_fields_a_rule_may_name::test_a_rule_may_ask_how_safe_a_message_was_judged_to_be",
    "application::filters::the_fields_a_rule_may_name::test_a_rule_naming_the_verdict_fires_on_mail_as_it_arrives",
    "application::filters::the_fields_a_rule_may_name::test_a_saved_search_reads_the_verdict_along_with_the_message",
]
tests_last_seen = [
    { file = "src/application/filters.rs", tests = 49 },
]

# ── This program's own reading says it was this program that read it ────────
#
# Four things can judge a message here, and until this was written three of
# them named themselves and one did not. "A link points at a bare numeric
# address rather than a name" is a guess made on this computer, and it was read
# out in the same voice as a filter's verdict.
#
# `from_analysis`'s own doc has argued since it was written that this program's
# reading may never say the word phishing, because a heuristic wearing somebody
# else's authority is how a warning becomes the thing people click past. Not
# sounding like the provider is the same argument one step further, so this is
# the guard for it.
#
# The break keeps the sentence and takes the name out of it, which is the shape
# a later reword would take. It is not a removal: the reading still produces a
# sentence, still bounded to one, and every existing test about what was found
# goes on passing.
#
# Measured by hand 2026-09-06 with `cargo test --all-targets --no-fail-fast`
# against a tree with no other failure in it. Five tests went red. Two are the
# attribution assertions, two are the bar and the merge asking every sentence
# who said it, and the fifth is the whole-sentence assertion about an indicator
# nobody has reworded, which pins the opening words as a side effect of pinning
# the whole sentence.

[[guard]]
name = "this program's own reading names itself in the bar"
file = "src/service/safety.rs"
before = """            "Wixen Mail read this message on your computer and found {}.","""
after = """            "There is something to look at in this message: {}.","""
red = [
    "service::safety::tests::test_an_indicator_nobody_has_reworded_still_reaches_somebody",
    "service::safety::tests::test_every_sentence_in_a_bar_carrying_three_sources_says_which_one_said_it",
    "service::safety::tests::test_merging_keeps_every_sentence_with_the_one_that_said_it",
    "service::safety::tests::test_several_things_found_here_are_one_sentence_rather_than_one_each",
    "service::safety::tests::test_this_programs_own_reading_says_that_it_was_this_program_that_read_it",
]
tests_last_seen = [
    { file = "src/service/safety.rs", tests = 34 },
]

# ── And says it once, however many things it found ──────────────────────────
#
# The other half, and the half that is easy to lose. Guardrail 5 asks for
# feedback that is distinct and bounded. Attribution written as a clause on
# every sentence satisfies the first and breaks the second: somebody hearing
# five findings gets the same eight words five times, which is repetition
# rather than attribution, and a bar people learn to talk past is a bar that
# has stopped working.
#
# So the break is the version somebody would write while doing this correctly
# in every other respect: one sentence per finding, each properly attributed.
# It is a separate record from the one above because the two failures are
# separate and the pair above cannot see this one.
#
# Measured by hand 2026-09-06, same command and same clean tree. One test went
# red, and only one, which is the finding worth recording: nothing else in the
# tree asserts that the bar stays short. The four that stayed green are the
# four the record above names, and they stay green because this break keeps
# every word of the attribution.

[[guard]]
name = "what this program found is one sentence rather than one each"
file = "src/service/safety.rs"
before = """        reasons: vec![format!(
            "Wixen Mail read this message on your computer and found {}.",
            one_after_another(&found)
        )],"""
after = """        reasons: found
            .iter()
            .map(|one| format!("Wixen Mail read this message on your computer and found {one}."))
            .collect(),"""
red = [
    "service::safety::tests::test_several_things_found_here_are_one_sentence_rather_than_one_each",
]
tests_last_seen = [
    { file = "src/service/safety.rs", tests = 34 },
]

# ── Landing on a misspelling offers more than one thing to try ──────────────
#
# The sentence said when the caret lands on a misspelled word is the only thing
# that happens: no dialog opens and there is no list of suggestions to arrow
# into. So one suggestion is one guess, which is exactly what F7's dialog
# already says and what this key exists to improve on. Somebody who disagrees
# with the first guess has been told nothing.
#
# The break is the bound cut back to one. It is the version somebody would
# write who had read the constant as a tidy limit rather than as the feature:
# every other word of the sentence is untouched, and the count clause is left
# alone, so the only thing that changes is how many things somebody is offered.
#
# Measured by hand 2026-09-06 with `cargo test --all-targets --no-fail-fast`
# against a tree with no other failure in it, 33 targets. Two tests went red
# and nothing else did. Both are in the module the constant lives in: the one
# that asks for more than one suggestion by name, and the one that pins the
# whole sentence for a word with more suggestions than the bound.
#
# What stays green under it, and is worth knowing. The two sentence tests that
# do not involve the bound at all: a word with no suggestions, which never
# reaches the take, and the pair asserting the dialog's sentence and this one
# are different, which they still are. So nothing outside `spell_session`
# notices, which is the finding: the bound is asserted in one place.
#
# The fixtures do not move with the constant. Both hold literal suggestion
# lists of two and seven, so widening or narrowing the break cannot carry the
# fixture with it, which is the way plan 03-07's bound test passed through the
# defect it was written for.

[[guard]]
name = "landing on a misspelling offers more than one thing to try"
file = "src/application/spell_session.rs"
before = """pub const SUGGESTIONS_SAID: usize = 3;"""
after = """pub const SUGGESTIONS_SAID: usize = 1;"""
red = [
    "application::spell_session::tests::test_a_word_with_more_suggestions_than_are_said_says_how_many_there_are",
    "application::spell_session::tests::test_landing_on_a_word_offers_more_than_one_thing_to_try",
]
tests_last_seen = [
    { file = "src/application/spell_session.rs", tests = 36 },
]

# ── The two walk keys are inverses of each other ────────────────────────────
#
# Alt+F7 goes to the next misspelling and Alt+Shift+F7 to the one before it,
# and the thing that makes the pair worth having is that the second undoes the
# first. Somebody who goes one word too far presses the other key once.
#
# The break is dropping `.rev()`, which is not a slip: it is what a backwards
# search reads like when it is written from the forward one. `find` over a list
# in reading order answers "the earliest word before here", so pressing the
# backward key at the end of a message goes to its first misspelling and the
# next press says there are no more. The message is walked backwards in one
# stride and nothing reports anything wrong.
#
# Measured by hand 2026-09-06 with `cargo test --all-targets --no-fail-fast`
# against a tree with no other failure in it, 33 targets. Four tests went red
# and nothing else did:
#
#   the inverse property, which is the one written for this
#   landing on the last misspelling from the end of the message
#   pressing the backward key again reaching the one before that
#   the sentence being the same whichever direction somebody arrived from
#
# That last one is red for a reason worth writing down, because it is not what
# the test is about: it reaches a different word, so it compares the wrong two
# sentences. It counts as red here and it is not a second guard on the pair.
#
# What stays green: both boundary sentences, because a message walked in one
# stride still stops at both ends, and the ignored-word assertion, because the
# word it reaches happens to be the same one either way.
#
# The property test cannot pass through this. Measured before the real answer
# was written, by running the naive version against it: the forward walk
# collects five words and the backward walk collects one.

[[guard]]
name = "the two walk keys are inverses of each other"
file = "src/application/spell_session.rs"
before = """    found
        .iter()
        .rev()
        .find(|finding| from.is_none_or(|from| finding.at < from))"""
after = """    found
        .iter()
        .find(|finding| from.is_none_or(|from| finding.at < from))"""
red = [
    "application::spell_session::tests::test_landing_on_a_word_backwards_says_what_landing_on_it_forwards_says",
    "application::spell_session::tests::test_pressing_the_backward_key_again_moves_to_the_one_before_that",
    "application::spell_session::tests::test_the_backward_key_from_the_end_lands_on_the_last_misspelling",
    "application::spell_session::tests::test_the_two_directions_are_inverses_over_a_message",
]
tests_last_seen = [
    { file = "src/application/spell_session.rs", tests = 36 },
]

# ── A batch of files is not all or nothing ─────────────────────────────────
#
# WRITE-01, and the rule that makes a drop or a paste of six files worth
# having. `choose_all` calls `Chosen::at` per path and keeps whatever reads, so
# a folder dropped among five photos is refused by name and the photos still go
# on. The alternative, refusing the whole batch on the first bad path, is what
# a `collect::<Result<Vec<_>, _>>()` gives for free and reads as the careful
# answer: somebody who dropped six files and got none would have to work out
# for themselves which one was the problem, with nothing on screen to say.
#
# The break is that shape exactly: the refusals are still gathered and still
# said, so the announcement is unchanged and only the files are gone. A check
# that asked whether a bad path is refused would pass on it.
#
# Measured by hand on 2026-09-06, every target with --no-fail-fast, from a tree
# that was green first: 6,375 library tests passed and three failed in the whole
# tree. Nothing outside this module noticed, which is the finding rather than an
# aside: three tests are the whole defence of the rule, and every route that
# attaches a file goes through the line they are about.
#
# Measured twice on the same day, and the second time is the point of the
# `tests_last_seen` machinery. The first measurement was two tests. A test
# pinning the whole sentence a batch says was added an hour later, for an
# unrelated reason, and it reddens under this break as well; the commit that
# added it was refused, `--remeasure` refused, and a plain run named the third
# test. The record was two short of the truth for as long as it took to read
# the message, rather than for three commits.

[[guard]]
name = "one path that cannot go on does not take the rest of the batch with it"
file = "src/application/attaching.rs"
before = """    Batch { chosen, refused }"""
after = """    match refused {
        Some(why) => Batch {
            chosen: Vec::new(),
            refused: Some(why),
        },
        None => Batch {
            chosen,
            refused: None,
        },
    }"""
red = [
    "application::attaching::tests::test_a_file_that_cannot_be_read_among_them_does_not_stop_the_others",
    "application::attaching::tests::test_a_folder_among_them_is_refused_by_name_and_the_others_still_go_on",
    "application::attaching::tests::test_what_a_batch_of_three_says_out_loud",
]
tests_last_seen = [
    { file = "src/application/attaching.rs", tests = 42 },
]

# ── Every way a file goes on a message ends at one model ───────────────────
#
# WRITE-01. A file goes on a message three ways now: picked in a dialog, pasted
# off the clipboard, and dropped on the window. `attach_from_paths` is what all
# three call and `attaching::choose_all` is what it calls, so the folder
# refusal, the unreadable refusal and `safe_file_name` are written once. The
# paths a drop hands over were chosen by whoever did the dragging, which is a
# stranger's input in exactly the sense the threat table means, so a route with
# rules of its own is the defect the census is written against.
#
# The break is the door itself going by hand rather than a fourth route doing
# it, because that is what compiles: a route building a chosen file needs the
# attachment list in scope and the drop callback does not capture it. It is the
# worse defect anyway. The door keeps its name and its three callers, and
# underneath it every path becomes an attachment with no folder refused, no
# name cleaned and every size reported as zero.
#
# Measured by hand on 2026-09-06, every target with --no-fail-fast, from a tree
# that was green first: 6,376 library tests passed and one test failed in the
# whole tree.
#
# That one test is the finding rather than an aside. Nothing in the library can
# reach this: it needs a window, a dialog and a web view. The single
# source-reading test named below is the whole defence of the rule, which is the
# same shape 03-02 found for the sign-in census and 03-08 for the outbox.

[[guard]]
name = "every route that attaches a file goes through the one model"
file = "src/presentation/wx_compose.rs"
suite = "every_way_a_file_goes_on_a_message"
before = """            let batch = crate::application::attaching::choose_all(&paths);"""
after = """            let batch = crate::application::attaching::Batch {
                chosen: paths
                    .iter()
                    .map(|path| crate::application::attaching::Chosen {
                        name: path.display().to_string(),
                        bytes: 0,
                        path: path.clone(),
                    })
                    .collect(),
                refused: None,
            };"""
red = [
    "test_the_one_door_is_the_only_place_a_path_becomes_an_attachment",
]
tests_last_seen = [
    { file = "src/presentation/wx_compose.rs", tests = 43 },
    { file = "tests/every_way_a_file_goes_on_a_message.rs", tests = 7 },
]

# ── The drop the composer cannot catch ──────────────────────────────────────
#
# The message body is a WebView2 control and registers its own drop target, so a
# file dropped there never reaches the composer's. What matters is not that the
# drop is missed but what the engine does with it instead: AllowExternalDrop
# defaults to true, the drag reaches the page, and a browser handed a file
# navigates to it. The half-written message goes off the screen.
#
# Turning that setting off is the direct fix and is out of reach. It lives on
# `ICoreWebView2Controller4`; wxWidgets 3.3.2 keeps the controller in
# `wx/msw/private/webview_edge.h` and `GetNativeBackend` hands out the
# `ICoreWebView2_2` underneath it, which cannot be asked for its controller. So
# the refusal is written in the page, and this is what says it is still there.
#
# One line, and the rest of the handler still runs without it: the count is
# still posted and the composer still says where a drop lands, which is exactly
# how this would come out green while the draft was being lost. Two tests
# notice, and they notice differently. One reads the page and one runs it.
#
# Measured by hand on 2026-09-06 with `--all-targets --no-fail-fast`: exactly
# those two, nothing else in 6,400.

[[guard]]
name = "a file dropped on the message is turned away rather than opened"
file = "src/presentation/editor_document.rs"
before = """    event.preventDefault();
    var moved = event.dataTransfer;"""
after = """    var moved = event.dataTransfer;"""
red = [
    "presentation::editor_document::tests::test_a_file_dropped_on_the_message_is_refused_rather_than_opened",
    "presentation::editor_page_harness::tests::test_a_file_dropped_on_the_message_is_turned_away_when_the_page_runs",
]
tests_last_seen = [
    { file = "src/presentation/editor_document.rs", tests = 83 },
    { file = "src/presentation/editor_page_harness.rs", tests = 36 },
]

# ── A description that is lost quietly ──────────────────────────────────────
#
# Criterion 2 of phase 4 says a picture inserted into a message keeps its
# description across a draft save and a reload, and until 04-08 nothing
# asserted any of it. The trip has four stages and the description can be lost
# at any of them: the escaping when it is written, the sanitiser when the body
# leaves the editor, the drafts table, and the sanitiser again when the body
# goes back into the page.
#
# The break is the sanitiser forgetting `alt` on an `img`, because that is the
# one that loses the description and keeps the picture. Somebody sighted sees
# no change at all. The picture is still there, still shown, and the person it
# was described for is told nothing, which is the exact defect this whole
# module exists to prevent.
#
# It is written as a line added to `cleaner`, which is the shape of the real
# mistake: somebody tightening the allowed attributes and taking `alt` with
# them. Removing the whole attribute filter was tried first and is a much
# larger break, reddening the security tests too, so it would go on passing if
# somebody left the filter alone and narrowed the attribute list, which is the
# likelier mistake.
#
# Measured by hand on 2026-09-06 with `WIXEN_TEST_THREADS=4 cargo test
# --all-targets --no-fail-fast` against a tree where `scripts/check.sh all` had
# just passed. Exactly nine tests went red in 6,401, six of them the new round
# trip and three that already stood over `alt` from one end or the other.

[[guard]]
name = "a picture's description survives the trip from the composer to the page"
file = "src/presentation/html_renderer.rs"
before = """        let mut builder = ammonia::Builder::default();"""
after = """        let mut builder = ammonia::Builder::default();
        builder.rm_tag_attributes("img", ["alt"]);"""
# One name taken out on 2026-09-22 by 12-02.1, after run 35520204784 found it
# green on a runner and a measurement here found the same:
# `test_a_decorative_picture_is_left_alone_when_the_reader_asked_for_silence`
# cannot see this break and never could.
#
# Measured by hand with the break in place rather than reasoned about. Under
# `Announcing::Silently` that test asks whether the mark was rewritten, and it
# compares the reading's answer with its own input. The break does not rewrite
# the mark; it takes the mark's meaning away, and the answer under silence is
# byte for byte the input either way,
# `<img src="data:image/png;base64,iVBORwECAwQ=" alt="">`, because a
# decorative picture and an undescribed one are both left alone there. Only a
# reader who asked to be told can see the difference, and the sibling that
# asks that, `test_a_decorative_picture_is_said_to_be_there_when_the_reader_asked_for_that`,
# does redden and stays named. So the test is right about its own subject and
# was never about this guard.
red = [
    "data::message_cache::drafts::tests::test_a_picture_and_its_description_come_back_out_of_a_real_database",
    "presentation::html_renderer::tests::test_a_decorative_picture_is_said_to_be_there_when_the_reader_asked_for_that",
    "presentation::html_renderer::tests::test_a_decorative_picture_survives_the_whole_trip_and_is_still_carried",
    "presentation::html_renderer::tests::test_a_described_picture_is_untouched_whatever_the_reader_chose",
    "presentation::html_renderer::tests::test_a_held_back_picture_leaves_the_senders_words_in_its_place",
    "presentation::html_renderer::tests::test_a_message_on_its_way_out_keeps_its_mark_whatever_this_reader_chose",
    "presentation::html_renderer::tests::test_a_picture_and_its_description_survive_the_whole_trip",
    "presentation::html_renderer::tests::test_a_picture_the_message_carries_is_shown",
    "presentation::html_renderer::tests::test_an_awkward_description_survives_the_whole_trip_unmangled",
    "presentation::html_renderer::tests::test_an_empty_description_is_kept_and_is_not_the_same_as_having_none",
    "presentation::html_renderer::tests::test_image_alt_text_survives_sanitizing",
    "presentation::html_renderer::tests::test_only_the_decorative_picture_changes_when_two_are_shown",
    "presentation::html_renderer::tests::test_the_rewrite_is_anchored_to_the_attribute_and_not_to_a_substring",
    "presentation::html_renderer::tests::test_two_pictures_keep_their_own_descriptions_in_order",
]
tests_last_seen = [
    { file = "src/data/message_cache/drafts.rs", tests = 6 },
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]

# ── An empty description quietly becoming a decorative mark ─────────────────
#
# The defect the whole decorative design is arranged to avoid. There is one
# writer of an `img` tag and it now has two answers, and the moment an empty
# `InWords` reaches the arm that writes an empty `alt`, "somebody said there
# is nothing to say" and "somebody typed nothing" become the same thing. The
# picture goes out marked decorative, every other client's screen reader skips
# it, and nobody is told.
#
# It is one character of a match arm, which is why it needs a guard: it reads
# as tidying up two arms that do the same thing, and only one of them does.
#
# Measured by hand on 2026-09-06 with `WIXEN_TEST_THREADS=4 cargo test
# --all-targets --no-fail-fast` from a tree green in the library. Exactly one
# library test in 6,416.

[[guard]]
name = "an empty description does not become a decorative mark"
file = "src/application/pictures.rs"
before = """        WhatThePictureSays::Decorative => String::new(),"""
after = """        WhatThePictureSays::InWords(_) | WhatThePictureSays::Decorative => String::new(),"""
red = [
    "application::pictures::tests::test_a_picture_being_sent_must_be_described",
]
tests_last_seen = [
    { file = "src/application/pictures.rs", tests = 44 },
]

# ── A narrowing that has stopped narrowing ──────────────────────────────────
#
# `could_be_furniture` decides where the decorative question is asked. It is a
# judgement about size and it exists to keep the question away from a
# photograph, where the only honest answer is a description. A narrowing that
# says yes to everything asks the question over every picture, and the question
# has an answer on it that sends a photograph with nothing said.
#
# The break keeps the `None` guard, so a picture with no readable size is still
# refused. That isolates the two bounds themselves, which is what this record
# is about; a break removing the whole function would also redden the GIF test
# and say less.
#
# Measured by hand on 2026-09-06 the same way. Four library tests in 6,416, and
# the fourth is worth noting: the SVG case reddens because the break drops
# `worth_carrying`, which is part of the same expression.

[[guard]]
name = "the decorative question is not offered over a photograph"
file = "src/application/pictures.rs"
before = """    worth_carrying(kind, how_big)
        && how_big <= MOST_FURNITURE_MAY_BE
        && width.min(height) <= MOST_FURNITURE_MAY_MEASURE"""
after = """    let _ = (kind, how_big, width, height);
    true"""
red = [
    "application::pictures::tests::test_a_kind_that_cannot_be_carried_at_all_is_never_offered_it",
    "application::pictures::tests::test_a_photograph_and_a_screen_capture_are_never_offered_it",
    "application::pictures::tests::test_the_furniture_bound_on_disk_is_a_bound_and_not_decoration",
    "application::pictures::tests::test_the_furniture_bound_on_size_is_a_bound_and_not_decoration",
]
tests_last_seen = [
    { file = "src/application/pictures.rs", tests = 44 },
]

# ── Enter answering the question that costs somebody else ───────────────────
#
# Enter is how a person working by keyboard answers everything, so it is
# already on its way while a question is still being read out. On this one the
# two answers do not cost the same. Describing a picture that turned out to be
# a spacer costs a moment; marking a picture decorative that turned out to say
# something costs the person receiving it the fact that it was there, and they
# cannot ask about what they were never told.
#
# Both spellings compile and neither is obviously wrong in a diff, which is why
# this is a guard rather than a comment. Nothing in the library can see it: the
# composer's dialog code needs a person to answer it, so the census is the
# suite, and the record names it.
#
# Measured by hand on 2026-09-06 the same way. One test in the census, nothing
# else in 6,416.

[[guard]]
name = "enter does not mark a picture decorative"
file = "src/presentation/wx_compose.rs"
suite = "a_picture_is_only_decorative_because_somebody_said_so"
before = """    .with_style(crate::presentation::asking::yes_no_where_enter_answers_no())"""
after = """    .with_style(crate::presentation::asking::yes_no_where_enter_answers_yes())"""
red = [
    "test_the_decorative_question_is_one_enter_answers_by_asking_for_a_description",
]
tests_last_seen = [
    { file = "src/presentation/wx_compose.rs", tests = 43 },
    { file = "tests/a_picture_is_only_decorative_because_somebody_said_so.rs", tests = 14 },
]

# ── A setting that decides nothing ──────────────────────────────────────────
#
# `announce_decorative_pictures` moves the final say about a sender's
# decorative mark to the person reading. A renderer that answers the same way
# whatever the setting says is the shape that passes every test of the words
# themselves: the line appears, it is worded correctly, it is escaped
# correctly, and somebody who turned it off still hears it on every message
# with a spacer in it.
#
# The break is the arm that returns early, not the field, because removing the
# field would not compile and a compile error is a weaker red than a stubbed
# body. It only breaks one direction, which is the honest one to break: a
# setting stuck on is noise somebody can hear past, and a setting stuck off is
# the loss this whole feature exists to prevent, so the noisy direction is the
# one a test can be sure about on any machine.
#
# Measured by hand on 2026-09-06 with `WIXEN_TEST_THREADS=4 cargo test
# --all-targets --no-fail-fast` from a tree green in the library. One test in
# 6,427.

[[guard]]
name = "a reader who asked for silence about decorative pictures gets it"
file = "src/presentation/html_renderer.rs"
before = """        if self.announcing == Announcing::Silently {
            return tag.to_string();
        }"""
after = """        let _ = self.announcing;"""
red = [
    "presentation::html_renderer::tests::test_a_decorative_picture_is_left_alone_when_the_reader_asked_for_silence",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]

# ── This reader's words reaching somebody else's message ────────────────────
#
# `sanitize_html` is one call doing two jobs, and its own comment says so: it
# cleans a stranger's message on the way in and it cleans this person's message
# on the way out. Words written there would be sent to the recipient, and the
# mark the composer just made would be overwritten by a sentence about it.
#
# That is why the announcing seam is in `hold_back_what_would_be_fetched`,
# which only runs when a message is being read. The break is the version
# somebody would write who had not read that comment: the same call, one layer
# up, where it reads as tidier.
#
# Three tests notice and they notice differently. One puts a decorative picture
# through the outgoing path and finds the mark gone. One runs the whole trip
# from the composer to the page and finds the description changed on the way.
# One is the sanitiser assertion the decorative mark rests on.
#
# Measured by hand on 2026-09-06 the same way. Three tests in 6,427.
#
# Two of the three read this machine's profile. `editor_document` builds its
# renderer through `HtmlRenderer::new`, which takes the announcing answer
# from the stored settings, and on 2026-09-19 the profile the tests read on
# this machine held `announce_decorative_pictures` false, so the break's
# rewrite answered silence and the two trips stayed green with the guard
# broken: 1 of 3 here, 3 of 3 on a runner, whose profile is fresh and whose
# default is on. Measured that day through `scripts/guards.sh --remeasure`
# with `WIXEN_MAIL_DATA` pointed at an empty folder, which is a fresh
# profile: the three named, as recorded. A record that reads the profile is
# measured that way from here on, and says so.

[[guard]]
name = "a message on its way out does not pick up this reader's own words"
file = "src/presentation/html_renderer.rs"
before = """        cleaner().clean(html).to_string()"""
after = """        self.say_where_a_decorative_picture_is(&cleaner().clean(html).to_string())"""
red = [
    "presentation::html_renderer::tests::test_a_decorative_picture_survives_the_whole_trip_and_is_still_carried",
    "presentation::html_renderer::tests::test_a_message_on_its_way_out_keeps_its_mark_whatever_this_reader_chose",
    "presentation::html_renderer::tests::test_an_empty_description_is_kept_and_is_not_the_same_as_having_none",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]

# ── An owner of credential store entries that nobody registered ─────────────
#
# `application::forget::entries_for` is the whole list of credential store
# entries an uninstall erases, written out by hand from what were four owners
# and are now five. Its own comment records this list coming apart from another
# one once already: it named an account's tokens, the single-account delete
# path did not, and a removed account left its refresh token on the machine.
#
# Eighteen tests were beside it and every one of them asserts that a
# *particular* known entry is present. None asked whether anything was missing,
# so a fifth owner added and never registered passed all eighteen. The fifth
# owner is the OpenPGP private key, which is the highest-value secret this
# program will hold, and left behind it is unreadable, belongs to nothing, and
# is invisible to the uninstaller that would have removed it.
#
# The break is the omission itself: the loop that asks `service::pgp` for its
# entries, gone. That is what forgetting to register an owner looks like.
#
# **Three tests redden and the third is the one worth reading.**
# `test_the_master_key_is_always_forgotten` asserts an exact count of the
# entries an installation with no accounts has, so it notices a machine-level
# entry disappearing without being about the private key at all. It is a census
# and `CLAUDE.md` warns that a census weakens the guards around it once it
# becomes a floor. This one is an equality, so it stays load-bearing, and it
# went red when the private key arrived as well as when it was taken away.
#
# **A second candidate break was measured and is why the record uses this one.**
# Keeping the loop and neutering it, `pgp::keyring_entries().into_iter()
# .take(0)`, reddens two rather than three, and the completeness guard is not
# among them: the source still says `pgp`, so the reading still finds the
# owner. That is the known limit of a guard that reads source, and what covers
# the gap is `test_the_private_key_entry_is_one_uninstalling_erases`, which
# asks for the entry itself and went red under both breaks. Recorded because
# the first plausible break was the weak one and one extra run is what said so.
#
# Measured by hand on 2026-09-06 against a green tree of 6,434 tests, with
# `cargo test --no-fail-fast --lib`. Three tests in 6,434.

[[guard]]
name = "an owner of credential store entries that uninstalling never names"
file = "src/application/forget.rs"
before = """    for (service, user) in pgp::keyring_entries() {
        entries.push(CredentialEntry { service, user });
    }"""
after = """"""
red = [
    "application::forget::tests::test_every_owner_of_credential_entries_is_named_where_uninstalling_reads",
    "application::forget::tests::test_the_master_key_is_always_forgotten",
    "application::forget::tests::test_the_private_key_entry_is_one_uninstalling_erases",
]
tests_last_seen = [
    { file = "src/application/forget.rs", tests = 24 },
]

# ── An encrypted message opened as a blank message with nothing said ────────
#
# An S/MIME enveloped message has no `text/*` part at all, so `mime::parse`
# yields nothing for either body and the reader says "This message has no text,
# or it has not been downloaded yet" about a message that has text and did
# arrive. Somebody acting on that fetches it again and gets the same nothing.
#
# The question that decides it is asked once, of the raw bytes, on the way in.
# The break is that question always answering no, which is what it did for the
# whole of this program's life until 04-09: `layout_of` has recognised an
# enveloped message all along and nothing called it.
#
# What goes red is the whole chain and not only the question. The recognition
# tests, the mark in the cache, and the reader's body all fail, which is what
# says the wiring is really there rather than the function merely existing.
#
# **The reader's own tests do not appear here and that is not an omission.**
# They hand `with_smime_envelope` an answer directly, so they never ask this
# question and stay green under this break. What covers that half is the mark
# in the cache, `test_a_message_that_arrived_encrypted_is_recorded_as_having_done_so`,
# which is the one hop between the two and goes red here. Measured rather than
# assumed: the first draft of this note said the reader would redden.
#
# Measured by hand on 2026-09-06 against a green tree of 6,465 tests, with
# `cargo test --no-fail-fast --lib`.

[[guard]]
name = "an encrypted message that nothing ever recognised as encrypted"
file = "src/service/signed_mail.rs"
before = """pub fn claims_encryption(raw: &[u8]) -> bool {
    let (headers, _) = split_headers_from_body(raw);
    matches!(
        header_value(headers, "content-type").and_then(|content_type| layout_of(&content_type)),
        Some(SmimeLayout::Encrypted)
    )
}"""
after = """pub fn claims_encryption(raw: &[u8]) -> bool {
    let _ = raw;
    false
}"""
red = [
    "data::message_cache::how_it_arrived::tests::test_a_message_that_arrived_encrypted_is_recorded_as_having_done_so",
    "service::signed_mail::the_cheap_first_question::test_a_message_naming_a_p7m_file_and_nothing_else_still_claims_encryption",
    "service::signed_mail::the_cheap_first_question::test_an_enveloped_message_says_it_claims_encryption",
    "service::signed_mail::the_cheap_first_question::test_capitals_in_enveloped_data_still_claim_encryption",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 32 and a
    # measurement here that found the same one. What a message in the cache is
    # read as saying about its own form goes through the same claim, in a file
    # this record had never named.
    "application::reading_a_message::tests::test_a_signed_message_in_the_cache_has_its_verdict_and_an_enveloped_one_its_sentence",
]
tests_last_seen = [
    { file = "src/application/reading_a_message.rs", tests = 7 },
    { file = "src/data/message_cache/how_it_arrived.rs", tests = 9 },
    { file = "src/service/signed_mail.rs", tests = 113 },
]

# ── The sentence reached the bar and the body still said "no text" ──────────
#
# The other half of the same defect, and the half a reader meets. The bar is
# spoken as a message opens and the body is where somebody lands, and only the
# body was ever wrong: it said "This message has no text, or it has not been
# downloaded yet" about a message that has text and did arrive.
#
# So this break leaves the bar alone and puts the body back. Everything about
# the message is right, the sentence is spoken on open, and the one thing
# missing is the one thing somebody reads. That is what makes it worth a record
# of its own rather than leaving the chain to the break above: a fix that
# reached only the bar would have looked complete.
#
# Measured by hand on 2026-09-06 against a green tree of 6,465 tests, with
# `cargo test --no-fail-fast --lib`.

[[guard]]
name = "an encrypted message whose body still says it may not have been downloaded"
file = "src/presentation/reader_text.rs"
before = '''    let empty = format!("{}\n", nothing_to_read());
    match text.strip_suffix(&empty) {
        Some(above) => format!("{above}{sentence}\n"),
        None => format!("{}\n\n{sentence}\n", text.trim_end()),
    }'''
after = '''    let _ = sentence;
    text.to_string()'''
# Four since 2026-09-16 (09-07): one message opened as a page now folds its
# envelope in through the same sentence, and that reading holds the body to
# no longer saying the message may not have arrived.
red = [
    "presentation::reader_text::encryption_tests::test_an_encrypted_message_reads_as_the_sentence_rather_than_as_nothing_to_read",
    "presentation::reader_text::encryption_tests::test_an_envelope_that_could_not_be_read_still_says_the_message_is_encrypted",
    "presentation::reader_text::encryption_tests::test_the_same_sentence_is_spoken_as_the_message_opens",
    "presentation::reader_text::encryption_tests::test_opening_one_message_as_a_page_folds_what_is_said_in_order",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# ── A PGP key nobody could import, so no message ever opened ────────────────
#
# The decryption path is written, tested against a key and a message GnuPG
# made, and reachable from nothing unless a person can put their key on this
# computer. Without the menu item every PGP message goes on saying there is no
# private key here, for ever, and every test still passes: guardrail 1's exact
# shape, a feature that compiles and is reached by nothing.
#
# The break is the menu item gone. The arm that catches it and the function it
# calls are left in place, so the code is all still there and the only thing
# missing is the way in, which is what the defect looks like.
#
# **Three go red and the third was not expected.**
# `test_every_handled_command_has_something_that_raises_it` is an older guard
# that reads every menu arm and asks whether anything ever raises its id, and it
# catches this break without being about PGP at all. That is the guard doing
# what it was written for, and it is written down here because the first draft
# of this record named two and the run said three.
#
# Measured by hand on 2026-09-06 against a green tree, with
# `cargo test --no-fail-fast --test wired`.

[[guard]]
name = "a PGP private key somebody can import"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = '''            .append_item(
                ID_IMPORT_PGP_KEY,
                "Import PGP Private &Key... (experimental)",
                crate::application::allowed::READING_PGP_MAIL_IS_EXPERIMENTAL,
            )'''
after = ''''''
red = [
    "test_every_handled_command_has_something_that_raises_it",
    "test_importing_a_pgp_private_key_is_on_a_menu_and_reaches_the_importer",
    "test_the_menu_says_reading_pgp_mail_is_experimental_before_it_is_chosen",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# ── The armour was never offered to the key on this computer ────────────────
#
# The other half of the same wiring. A message can only open where something
# hands its armour to `service::pgp`, and the reader window is where somebody
# opens mail. Without this call the whole path is present, tested and never
# reached from the one surface it exists for.
#
# The break leaves the reading aloud path wired, which is what makes it worth
# recording separately: two surfaces open mail and wiring one is exactly how
# the two come to disagree about the same message.
#
# Rewritten 2026-09-16 (09-07, #51): the two calls this break took out moved
# into `application::reading_a_message`, and the text reader asks the window's
# one seam to it, `what_a_message_shows_and_says`. The break now builds the
# text reader's answer by hand with nothing said, which is the bypass the
# missing surfaces had. Measured again the same day through
# `scripts/guards.sh --remeasure`, `cargo test --test wired`.
#
# Measured by hand on 2026-09-06 against a green tree, with
# `cargo test --no-fail-fast --test wired`.

[[guard]]
name = "a PGP message the reader window never offers to the key"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = '''    let shown = what_a_message_shows_and_says(cache, message, body_as_written(stored));
    // The list row does not carry the attachments, only whether there are any,'''
after = '''    let shown = crate::application::reading_a_message::WhatAMessageShowsAndSays {
        body: body_as_written(stored),
        said: crate::application::reading_a_message::WhatIsSaidAboutIt::nothing(),
    };
    // The list row does not carry the attachments, only whether there are any,'''
red = [
    "test_opening_a_message_tries_the_pgp_key_and_says_why_it_did_not_open",
    # Since the guard's companion arrived the same day: it reads the tree
    # before splicing and refuses to splice over a real bypass.
    "test_a_surface_that_stopped_asking_the_composition_is_named",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# ── The four reasons a PGP message did not open collapsed into one ──────────
#
# Four different pieces of news and four different things to do next: set the
# program up, accept that the message was meant for somebody else, import the
# key again, or ask the sender to send it again. Collapsed into one sentence
# they are worth nothing: somebody hearing "this message could not be opened"
# learns only what they can already see.
#
# The break is the wrong-key arm answering with the no-key sentence, which is
# the way this really goes wrong. Nobody merges four arms; somebody reaches for
# the sentence next to the one they need.
#
# Measured by hand on 2026-09-06 against a green tree of 6,488 tests, with
# `cargo test --no-fail-fast --lib`.

[[guard]]
name = "the reasons a PGP message did not open stay four different sentences"
file = "src/presentation/reader_text.rs"
# The four arms moved out of `with_pgp` into `the_reason_it_did_not_open` on
# 2026-09-16 (09-07), so a thread can say the reason at the message it is
# about. The break is the same wrong-key arm; measured again the same day.
before = '''        WhatOpeningItFound::TheKeyHereDoesNotOpenIt => Some(THE_PGP_KEY_HERE_DOES_NOT_OPEN_IT),'''
after = '''        WhatOpeningItFound::TheKeyHereDoesNotOpenIt => Some(NO_PGP_KEY_ON_THIS_COMPUTER),'''
red = [
    "presentation::reader_text::encryption_tests::test_each_reason_a_pgp_message_did_not_open_is_said_in_its_own_words",
    "presentation::reader_text::encryption_tests::test_the_reasons_are_pairwise_different_and_none_is_the_smime_sentence",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# ── Undo Send: the hold reaching the row, and the clock letting it go ────────
#
# Both measured by hand on 2026-09-06 against a green tree of 6,502 tests, with
# `WIXEN_TEST_THREADS=4 cargo test --all-targets --no-fail-fast`, one break at a
# time and the tree green in between.
#
# Read the red lists as short and know why. Neither break can be caught by a
# behavioural test, because both lines live in `queue_for_sending` and in the
# poll timer closure, and reaching either from a test means a `#[test]` in
# `src/presentation/wx_app.rs`. Forty-two records name that file, so one test
# there costs forty-two builds and forty-two full library runs. The honest
# statement is that these two guards are source-reading, that a source-reading
# guard cannot see whether the line is reached, and that what the line does once
# reached is driven over a real database in `data::message_cache::outbox`.

[[guard]]
# The defect itself, in the shape it really had: the value worked out and then
# not handed to the call it was worked out for. `waiting_on` stays computed and
# stays returned, so the compiler is satisfied, `when_it_goes` downstream still
# branches on it, and everything reads as though a decision about waiting had
# reached storage. It had not. That shipped for two phases and every automatic
# check in this tree read the feature as wired.
name = "the composer's Send hands the queue the hold it worked out"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """        .queue_outbox_message_to_go(&queued, &waiting_on)"""
after = """        .queue_outbox_message_to_go(
            &queued,
            &crate::application::sending_later::GoAfter::AsSoonAsPossible,
        )"""
red = ["test_the_composers_send_passes_the_hold_to_the_queue"]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
# Wiring a hold without wiring a clock is a worse defect than the one being
# fixed: the message waits its ten seconds and then waits forever, having told
# somebody it was going in ten seconds. Every other caller of `flush_outbox` is
# a person pressing something.
#
# This break reddens a second test in a second target, and a record names one
# suite, so it is recorded here rather than in the list above:
# `test_the_places_that_hand_mail_to_a_server_are_the_ones_counted` in
# `tests/nothing_leaves_the_outbox_unasked.rs` also goes red, because the break
# takes away the fourth place mail is handed to a server and that file counts
# them. Measured, not assumed.
name = "the clock lets held mail go rather than only working out that it may"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """                        if held_mail_is_due {
                            flush_outbox(app);
                        }"""
after = """                        if held_mail_is_due {
                            let _ = held_mail_is_due;
                        }"""
red = ["test_the_clock_lets_held_mail_go"]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
# -- A time that has gone is refused, not moved to now ----------------------
#
# The break is the clamp, and the clamp is what a later change is most likely
# to reintroduce, because it looks like a kindness. Somebody picks yesterday
# by mistake; being helpful about it means sending, immediately, the message
# they had just said they wanted delayed, and telling them it was set. There
# is no way back from that: it is in a stranger's mailbox.
#
# The neighbouring inconsistency is the reason this needs a guard rather than
# a comment. `Hold::of_seconds` clamps rather than refusing, deliberately, and
# says so in its own doc. A reader who has just met that one will reasonably
# ask why this one does not, and "make them consistent" is a one-line change
# that passes every other check in this tree. What tells them apart is where
# the value came from: a hold is read out of a settings file that survived a
# restart and must never be able to stop this program sending mail, and this
# is a time somebody chose seconds ago in a dialog that is still open.
#
# The third test in the red list is the one worth explaining. The clamp also
# reddens the just-missed case, because that case asserts both halves of the
# grace period: twenty seconds gone is accepted and an hour gone is refused.
# Clamping makes the second half pass a time that should have been turned
# down. Measured with --no-fail-fast over the whole library, not predicted.
name = "a time that has gone is refused rather than quietly moved to now"
file = "src/presentation/wx_send_later.rs"
before = """        Scheduling::SetFor(at) => WhatThePickerDoes::SetFor(at),"""
after = """        Scheduling::SetFor(at) => WhatThePickerDoes::SetFor(at),
        Scheduling::AlreadyPast => WhatThePickerDoes::SetFor(now),"""
red = [
    "presentation::wx_send_later::tests::test_a_time_only_just_missed_is_accepted_rather_than_refused",
    "presentation::wx_send_later::tests::test_a_time_that_has_gone_is_refused_with_the_reason_and_the_next_move",
    "presentation::wx_send_later::tests::test_every_refusal_names_a_next_move_rather_than_only_saying_no",
]
tests_last_seen = [{ file = "src/presentation/wx_send_later.rs", tests = 8 }]

# ── The type on an outgoing part is worked out, not read off the name ───────
#
# The defect this whole plan exists to stop, in the exact shape it had. The
# content type saying a meeting reply is a reply was written, tested and
# correct, and the sending path never asked for it, so every answer went out
# declared as a plain calendar file and no organiser's client recorded one.
#
# So the break is not deleting the call, which only asks whether the call
# exists and which nobody doubts. It keeps the call and throws the answer
# away, which is what a later change reaching for the simpler-looking
# `content_type(&name)` would do, and which every check on the call site is
# blind to.
#
# The red list is one test and that is the measurement rather than an
# oversight. The four tests over the derivation itself call `content_type_of`
# directly, so the break cannot touch them: it is at the call site, and only a
# test that goes the whole way from a document to a `Ready` can see the answer
# being discarded. That is exactly the discrimination wanted here. Measured
# with --all-targets --no-fail-fast from a green tree of 6,521 tests.

[[guard]]
name = "the type on an outgoing part is worked out from the document, not read off the name"
file = "src/application/attaching.rs"
before = """                content_type: content_type_of(&name, &bytes),"""
after = """                content_type: {
                    let _ = content_type_of(&name, &bytes);
                    content_type(&name)
                },"""
red = [
    "application::attaching::tests::test_a_reply_arrives_declared_as_a_reply_so_the_organisers_client_records_it",
]
tests_last_seen = [{ file = "src/application/attaching.rs", tests = 42 }]

# ── An answer worked out for the calendar is really put on the calendar ─────
#
# The defect this plan closed, in the exact shape it had for three months.
# `what_the_calendar_should_hold` worked out the whole entry, correctly, and
# was called by nothing at all, so accepting a meeting sent the answer and left
# the calendar empty. Nothing anywhere said so, in either direction, which is
# what made it worse than a documented limit.
#
# So the break is not deleting the working out. That only asks whether the call
# exists, which the compiler would answer anyway. It keeps the whole working
# out, every field of it, and drops the row on the floor instead of saving it,
# which is precisely what shipped.
#
# The red list is thirteen tests and all thirteen are the measurement rather
# than a net cast wide. Each one reads a row back out of a real database, so
# every one of them can see the row not being there. The three that stay green
# are the three that are not about a row arriving: an answer that never left the
# machine, a database written before this shipped, and the stored vocabulary.
# Measured by hand with --all-targets --no-fail-fast from a green tree of 6,534
# library tests.
#
# It was measured as twelve, and it was twelve when it was measured. The
# thirteenth is the test the record below needed, which was written later in the
# same session, and a test written after a record is measured is exactly how a
# record comes to name too few. `tests_last_seen` could not have caught it: the
# count was written down after the test was added, so the number matched.
# `scripts/guards.sh` caught it, which is the only thing that can.

[[guard]]
name = "an answer worked out for the calendar is really put on the calendar"
file = "src/application/answered_meetings.rs"
before = """    cache.save_calendar_event(&the_row)?;"""
after = """    let _ = &the_row;"""
red = [
    "application::answered_meetings::tests::test_accepting_puts_the_meeting_on_the_calendar_taking_up_its_time",
    "application::answered_meetings::tests::test_each_answer_decides_whether_the_time_is_taken_and_says_it_in_the_stored_word",
    "application::answered_meetings::tests::test_a_change_to_a_meeting_already_accepted_is_answered_as_a_change_to_it",
    "application::answered_meetings::tests::test_answering_the_same_meeting_three_times_leaves_one_entry",
    "application::answered_meetings::tests::test_an_invitation_older_than_the_one_answered_does_not_move_the_meeting_back",
    "application::answered_meetings::tests::test_answering_the_same_version_again_writes_nothing_at_all",
    "application::answered_meetings::tests::test_the_entry_is_marked_as_a_change_this_computer_has_made",
    "application::answered_meetings::tests::test_the_meetings_own_status_is_not_the_guests_answer",
    "application::answered_meetings::tests::test_the_meeting_is_filed_under_a_calendar_made_for_an_account_that_has_none",
    "application::answered_meetings::tests::test_a_meeting_named_after_one_on_another_accounts_calendar_leaves_that_one_alone",
    "application::answered_meetings::tests::test_an_invitation_that_names_no_end_is_stored_with_one_rather_than_a_blank",
    "application::answered_meetings::tests::test_a_meeting_of_whole_days_keeps_its_dates_rather_than_gaining_a_clock_reading",
    "application::answered_meetings::tests::test_the_version_answered_is_written_against_the_row_the_calendar_already_holds",
    "application::answered_meetings::tests::test_a_held_answer_is_filed_on_the_calendar_while_it_waits_to_go",
]
tests_last_seen = [
    { file = "src/application/answered_meetings.rs", tests = 17 },
]

# ── The version answered is written against the row that is really here ─────
#
# This record exists because measuring the one above found the tests could not
# tell two implementations apart. The plan said a second answer minting a fresh
# identity would write a second row. It does not: `save_calendar_event` matches
# on the account, the calendar and the meeting's own name as well as on the
# identity, so the row is found either way. Every test in the module passed with
# a fresh identity minted every time.
#
# What a fresh identity really breaks is one step later and invisible. The
# version answered is written by identity, so written against one no row
# carries, it records nothing. The calendar goes on saying the version before
# last was the one answered, and the next invitation at that version is taken
# for a change and writes over whatever is there.
#
# Seeing that takes three answers. Two at one version stop at "nothing new"
# before any of this is reached, and two at different versions leave one row
# either way. The red list is one test because one test is what discriminates,
# and it was written after the break was measured rather than before.
# Measured by hand with --all-targets --no-fail-fast from a green tree of 6,534
# library tests: 6,533 passed, one failed.

[[guard]]
name = "the version answered is written against the row the calendar already holds"
file = "src/application/answered_meetings.rs"
before = """    let id = match already.as_ref() {
        Some(row) => row.id.clone(),
        None => uuid::Uuid::new_v4().to_string(),
    };"""
after = """    let id = uuid::Uuid::new_v4().to_string();"""
red = [
    "application::answered_meetings::tests::test_the_version_answered_is_written_against_the_row_the_calendar_already_holds",
]
tests_last_seen = [
    { file = "src/application/answered_meetings.rs", tests = 17 },
]

# ── The count of held-back pictures, kept and said ──────────────────────────
#
# The break is not deleting the call to `sanitize_and_count_held_back`. Delete
# that and there is no markup at all, everything falls over, and the run says
# nothing about this. The break is the defect that really shipped and shipped
# for as long as the function existed: keep the call and take `.0`, throwing
# the count away. Both production callers did that while the function's own doc
# described a caller that wanted both halves.
#
# A call-site check cannot see it. The function has a caller, the caller uses
# what it returns, nothing is unused, and the whole suite was green.
#
# Only `wrap_body` is broken, not `render_thread`, so the run says which of the
# two reading paths lost the count rather than both at once.
#
# Measured by hand with --all-targets --no-fail-fast from a green tree.
# Rewritten 2026-09-19 onto the branch as 11-10.1 left it, the plain arm now
# one call to `plain_text_as_a_page`, and measured again through
# `scripts/guards.sh --remeasure`: the same three.

[[guard]]
name = "the count of held-back pictures reaches the reader"
file = "src/presentation/html_renderer.rs"
before = """            MessageBody::Html(html) | MessageBody::Multipart { html, .. } => {
                let (markup, held_back) = self.sanitize_and_count_held_back(html);
                format!(
                    "{}{markup}",
                    self.what_a_reader_is_told_was_held_back(held_back)
                )
            }
            MessageBody::Plain(text) => plain_text_as_a_page(text),
        };
        self.wrap_prepared(&content, "Back to message list (Escape)")"""
after = """            MessageBody::Html(html) | MessageBody::Multipart { html, .. } => {
                self.sanitize_and_count_held_back(html).0
            }
            MessageBody::Plain(text) => plain_text_as_a_page(text),
        };
        self.wrap_prepared(&content, "Back to message list (Escape)")"""
red = [
    "presentation::html_renderer::tests::test_a_reader_is_told_how_many_were_held_back_and_where_the_switch_is",
    "presentation::html_renderer::tests::test_one_held_back_picture_is_not_reported_in_the_plural",
    "presentation::html_renderer::tests::test_a_message_being_written_is_not_told_its_own_sender_would_have_learned",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]
[[guard]]
name = "the composer asks for the renderer that does not name a sender"
file = "src/presentation/html_renderer.rs"
before = """    pub fn for_a_message_being_written() -> Self {
        let (fetching, announcing, describing) = what_the_settings_say();
        Self {
            plain_text_only: false,
            fetching,
            announcing,
            describing,
            whose: crate::application::pictures::WhoseMessage::BeingWrittenHere,
        }
    }"""
after = """    pub fn for_a_message_being_written() -> Self {
        let (fetching, announcing, describing) = what_the_settings_say();
        Self {
            plain_text_only: false,
            fetching,
            announcing,
            describing,
            whose: crate::application::pictures::WhoseMessage::SomebodyElseSent,
        }
    }"""
red = [
    "presentation::html_renderer::tests::test_the_constructor_the_composer_calls_is_the_one_that_stays_quiet",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]

# ── An ordinary message grows nothing ───────────────────────────────────────
#
# The other half of saying it, and the half that would have gone unmeasured.
# `test_a_message_with_nothing_held_back_says_nothing` was rewritten against
# the document and was green on arrival, because at the time nothing said the
# sentence at all. A test that has never been red proves nothing, so it was
# taken red by hand and this is that measurement.
#
# It could not have been, as it stood. Its two assertions look for the two
# phrases the sentence carries, and a sentence going out empty carries neither:
# every message would have picked up an empty paragraph where the count goes
# and the test would have passed on both sides of the break. A third assertion,
# on the class rather than on the words, is what makes the break visible, and
# it went in for this reason.
#
# Measured by hand with --all-targets --no-fail-fast from a green tree.
# Rewritten 2026-09-20 by 11-11.0 onto the joined sentence, the pictures'
# and the hidden blocks' in one paragraph (#90), and measured again through
# `scripts/guards.sh --remeasure`: the same one.

[[guard]]
name = "an ordinary message grows nothing where the count would be"
file = "src/presentation/html_renderer.rs"
before = """        .join(" ");
        if said.is_empty() {
            return String::new();
        }
        format!("""
after = """        .join(" ");
        format!("""
red = [
    "presentation::html_renderer::tests::test_a_message_with_nothing_held_back_says_nothing",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]

# ── Unblocking one address, by taking off the domain block over it ──────────
#
# The mistake that cannot be undone by asking again. Somebody asks for one
# person to be let back in, the wider rule catching them is deleted instead,
# and everybody at that domain is unblocked with nothing said.
#
# The break is the lookup taking the first rule whose block merely catches this
# sender, rather than the rule that is exactly this block.
#
# Which rule that finds depends on the order the rules come back in, and that
# is the whole reason the fixture is written the way it is. Rules are ordered
# by name, so "Blocked: everyone at example.com" comes before
# "Blocked: zoe@example.com" and after "Blocked: ada@example.com". Measured
# with `ada`, this break reddens nothing at all: the exact rule is the first
# one a widening lookup meets, so it returns the right answer by accident. The
# first version of both tests was written that way and passed against the
# defect they were written for. `zoe` is what makes the widening bite.

[[guard]]
name = "unblocking one address never takes the domain block over it"
file = "src/application/blocking.rs"
before = """    match the_rule_that_blocks(account_id, &chosen.what, rules) {"""
after = """    match rules
        .iter()
        .filter(|rule| rule.account_id == account_id)
        .find(|rule| the_block_in(rule).is_some_and(|held| covers(&held, &chosen.what)))
    {"""
red = [
    "application::blocking::tests::test_unblocking_one_address_leaves_the_domain_block_that_covers_it_alone",
]
tests_last_seen = [
    { file = "src/application/blocking.rs", tests = 67 },
]

# ── Unblocking a domain, by taking off the address block under it ───────────
#
# The mirror, and it fails differently rather than less. The domain block stays
# in place, so the person believes a whole domain is unblocked and their mail
# goes on being filed into Junk. Its own record because the break above does
# not redden it: the two are one rule and two failures, and a single record
# would have left one of them guarded by nothing.
#
# The fixture is the other way round for the same reason: `ada` sorts before
# "everyone at", so the narrower rule is the one a first-match lookup meets.

[[guard]]
name = "unblocking a domain never takes the address block under it"
file = "src/application/blocking.rs"
before = """    match the_rule_that_blocks(account_id, &chosen.what, rules) {"""
after = """    match rules
        .iter()
        .filter(|rule| rule.account_id == account_id)
        .find(|rule| the_block_in(rule).is_some_and(|held| covers(&chosen.what, &held)))
    {"""
red = [
    "application::blocking::tests::test_unblocking_a_domain_leaves_the_narrower_block_underneath_it_alone",
]
tests_last_seen = [
    { file = "src/application/blocking.rs", tests = 67 },
]

# ── The direction a page sends is read, rather than posted and dropped ──────
#
# The shape this defect really had, rather than the one that is easy to write.
# Two things can break here. Deleting `back` from the script injected into the
# preview is caught by the source read in tests/wired.rs, which reads that line
# out of wx_app.rs. Keeping the field and ignoring it at the reader is caught by
# nothing that reads source, because the source still says everything it should:
# the page posts the direction, the host asks for it, and the answer is a
# constant. That is exactly what `is_leaving` was for as long as the preview has
# existed, and it is why Shift+F6 silently became F6 the moment focus was inside
# the page.
#
# So the break keeps the field and fixes the answer to one direction.
#
# Measured by hand on 2026-09-07 with `cargo test --all-targets --no-fail-fast`
# against ec2a5f0: 6563 passed, one failed, every other target green. One test
# is the whole red list, and that is worth saying rather than hiding. The three
# other tests of this reader all pass against the break by construction: two pin
# the property that a leave message with no direction still leaves, which the
# break preserves, and one pins that nothing else is read as leaving, which it
# also preserves. Only the payload carrying `back: false` tells the two apart.

[[guard]]
name = "the direction a page sends is read rather than posted and dropped"
file = "src/presentation/panes.rs"
before = """    match value.get("back").and_then(serde_json::Value::as_bool) {
        Some(false) => Some(Direction::Forward),
        Some(true) | None => Some(Direction::Back),
    }"""
after = """    Some(Direction::Back)"""
red = [
    "presentation::panes::tests::test_which_key_left_the_preview_survives_the_crossing",
]
tests_last_seen = [
    { file = "src/presentation/panes.rs", tests = 13 },
]

[[guard]]
# `docs/KEYBOARD_SHORTCUTS.md` has promised since the key was documented that
# `Alt+R` in the Columns dialog puts back "the default columns for this kind of
# folder". It put back the inbox's in every folder, because the one production
# caller passed `FolderKind::Inbox` as a literal. `98546f8` corrected the same
# literal where a folder is switched and left this one, and
# `message_columns.rs:517` is that commit's own note about what it had found.
#
# The break is the defect where it now lives. The parameter that carried the
# literal is gone, so the mistake cannot be made at a call site any more; what
# is left is this one function, and returning `Inbox` from it is exactly what
# used to be passed.
#
# Measured by hand on 2026-09-07 with `cargo test --no-fail-fast` over every
# target: one test. That is the whole red list and it is worth saying why. The
# other four tests of this module are about the dialog's rows and its second
# case pins the answer for a layout that does not say which folder it belongs
# to, which is `Inbox` and which the break preserves. Only a layout that does
# say tells the two apart.
name = "restore defaults in the columns dialog asks the layout which folder it is in"
file = "src/presentation/wx_columns.rs"
before = """    current.kind.unwrap_or(FolderKind::Inbox)"""
after = """    let _ = current;
    FolderKind::Inbox"""
red = [
    "presentation::wx_columns::tests::test_restore_defaults_puts_back_the_columns_for_the_folder_somebody_is_in",
]
tests_last_seen = [
    { file = "src/presentation/wx_columns.rs", tests = 5 },
]

[[guard]]
# Moving to a folder that wants different columns used to call `defaults_for`,
# so a hand-arranged inbox was thrown away by a look in Sent and a trip back.
# That is the half of this defect somebody meets without restarting, and it is
# separate from the stored string not saying which folder a layout was made in.
#
# The break is that call, put back. It is read out of the source because the
# folder change sits inside a closure inside the window builder, where reaching
# it needs a window, an account with stored credentials and a mail server. What
# the source cannot see is whether the answer is right, and that is driven in
# `presentation::message_columns::moving_between_folder_kinds`.
#
# Measured by hand on 2026-09-07 with `cargo test --no-fail-fast` over every
# target: one test. The break also leaves an unused-variable warning on the
# clone into the closure, which would fail the commit gate as well. That is a
# second line of defence and not a substitute: dropping the `keep` call while
# keeping `arriving_at` breaks the same feature and warns about nothing.
name = "a change of folder kind restores the layout rather than rebuilding it"
file = "src/presentation/wx_app.rs"
suite = "columns_belong_to_the_folder_they_were_arranged_in"
before = """                                let mut seen = layouts_per_kind.borrow_mut();
                                seen.keep(&layout);
                                *layout = seen.arriving_at(kind);"""
after = """                                *layout = ColumnLayout::defaults_for(kind);"""
red = [
    "test_a_change_of_folder_kind_restores_the_layout_rather_than_rebuilding_it",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/columns_belong_to_the_folder_they_were_arranged_in.rs", tests = 3 },
]

[[guard]]
# The reader that pairs `docs/KEYBOARD_SHORTCUTS.md` with the bindings took keys
# beginning `Ctrl+` or `Alt+` and nothing else, so no unmodified function key
# was ever asked about. Three keys hid in that gap at once: `F8` reaching the
# composer's toolbar, `Delete` taking a file off a message and `F6` closing the
# conversation window. Its own comment gave the reason, and the reason is right
# about `Space` and `Home` and wrong about `F8`, which is an unambiguous string.
#
# The break is the narrowing put back, which is the move that has to be caught
# rather than a mistake somebody would make by accident. A widened check that
# reports something is a check somebody is tempted to narrow, and `CLAUDE.md`
# carries the worked example: `test_no_status_page_names_a_version_the_code_does
# _not_ship` iterates over nothing, because a page took the advice that guard's
# own comment gave. Narrowing here is silent in the same way. The whole check
# still passes with the filter back, because the two keys it reports, `F7` and
# `Shift+F10`, are allow-listed.
#
# So the allow list is what notices. Every entry on it has to be a key the
# reader really reported, and narrowing takes `F7` and `Shift+F10` off that
# report while leaving their entries behind, which is an allow list covering
# nothing.
#
# Measured by hand on 2026-09-07 with `cargo test --all-targets --no-fail-fast`:
# one test, and every other target green. That is the whole red list. The other
# direction of the pair, `test_the_shortcuts_document_and_the_menus_agree`, does
# not read this function at all, and the per-surface target added beside it
# carries its own reader, so neither can tell the two implementations apart.
name = "the reader of documented keys still sees a plain function key"
file = "tests/wired.rs"
suite = "wired"
before = """    let last = piece.rsplit('+').next().unwrap_or(piece);
    let Some(number) = last.strip_prefix('F') else {
        return false;
    };
    matches!(number.parse::<u8>(), Ok(1..=12))"""
after = """    let _ = piece;
    false"""
red = [
    "test_the_shortcuts_document_names_no_key_the_code_has_never_heard_of",
]
tests_last_seen = [
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
# `Delete` on the composer's attachments list takes the file somebody is on off
# the message. It is bound on an ordinary wxWidgets control rather than in a
# menu table or in the injected page, so no reader in `tests/wired.rs` could see
# it, and it was undocumented until the shortcuts page gained it. Now that the
# page promises it, the binding going away would leave the page promising a key
# that does nothing, which is the defect this whole phase is about.
#
# The break inverts the comparison rather than deleting the arm, so nothing goes
# unused and no warning stands in for the failure. With it applied, `Delete`
# skips and every other key takes a file off the message.
#
# The record exists for a second reason as well, and it is the one `CLAUDE.md`
# names: an integration target that guards a `src/` module from outside is run
# by the scoped gate only when its own file changes, which is every commit
# except the ones that could break it. This record is the coupling that gets it
# run on a commit touching the composer.
#
# Measured by hand on 2026-09-07 with `cargo test --all-targets --no-fail-fast`:
# one test, and every other target green. The target's other two cases are about
# the composer's page and the conversation window and read different files, so
# neither can see this.
name = "delete on the attachments list still takes a file off the message"
file = "src/presentation/wx_compose.rs"
suite = "a_key_is_documented_where_the_surface_that_binds_it_is"
before = """            if key.event.get_key_code() != Some(KEY_DELETE) {"""
after = """            if key.event.get_key_code() == Some(KEY_DELETE) {"""
red = [
    "test_the_key_that_takes_a_file_off_a_message_is_in_the_composers_section",
]
tests_last_seen = [
    { file = "src/presentation/wx_compose.rs", tests = 43 },
    { file = "tests/a_key_is_documented_where_the_surface_that_binds_it_is.rs", tests = 3 },
]

[[guard]]
# The Scrolling section of Settings asks whether Windows has overruled smooth
# scrolling. It used to decide from the machine alone and then hand the question
# a literal `true`, so somebody who had never ticked the box was told, under a
# box they had left alone, that Windows had overruled them.
#
# The break is that literal, put back, and it is the only shape this defect
# takes: the function itself was never wrong, and its three no-op answers were
# tested and passing the whole time it could not be reached with the arguments
# that produce one. So the tests on the function cannot tell the two
# implementations apart and this is read out of the source instead, over what a
# release build compiles.
#
# Measured by hand on 2026-09-07 with `cargo test --all-targets --no-fail-fast`:
# one test, 6571 passed, and every other target green.
name = "the settings screen asks about smooth scrolling with the setting somebody has"
file = "src/presentation/wx_settings.rs"
before = """    if let Some(said) = what_the_machine_has_overruled(config.smooth_scrolling, system_motion()) {"""
after = """    if let Some(said) = what_the_machine_has_overruled(true, system_motion()) {"""
red = [
    "application::scrolling::tests::test_the_settings_screen_asks_about_the_setting_somebody_has",
]
tests_last_seen = [
    { file = "src/application/scrolling.rs", tests = 10 },
    { file = "src/presentation/wx_settings.rs", tests = 0 },
]

[[guard]]
# The move window builds its own folder list again, and gives every folder a
# depth of nought.
#
# That is what shipped until 04.1-01. The picker read the folders itself and
# passed nought for every one, so `Archive/2026` and `Work/2026` were two rows
# reading the same word under one heading, and `Destination.depth`'s own doc
# said the field built the tree while nothing read it.
#
# Measured against `tests/one_hierarchy_two_views.rs`, which drives one fixture
# through the sidebar's builder and through the picker's and requires the two
# to agree.
name = "the move window gives every folder a depth of nought"
file = "src/application/destinations.rs"
suite = "one_hierarchy_two_views"
before = """                    depth: row.depth.saturating_sub(1),"""
after = """                    depth: 0,"""
red = [
    "test_a_folder_sits_at_one_depth_and_not_two",
    "test_the_picker_offers_the_folders_the_sidebar_shows",
]
tests_last_seen = [
    { file = "src/application/destinations.rs", tests = 37 },
    { file = "tests/one_hierarchy_two_views.rs", tests = 12 },
]

[[guard]]
# The move window decides for itself what an account is called.
#
# The sidebar puts an address on an account only when a second account reads
# the same, so somebody hears "Work" for an account nobody else is named after
# and "Work <ada@example.com>" when two are. The picker used to call nothing:
# the mail path passed the address always and the PIM path passed the label
# always, so two accounts both called Work were one name in one of those two
# windows and two in the other.
#
# The break is the plausible one, which is the picker answering the question
# again from the accounts it was handed rather than taking the answer it was
# given.
name = "the move window decides for itself what an account is called"
file = "src/application/destinations.rs"
suite = "one_hierarchy_two_views"
before = """                let called = row.name;"""
after = """                let called = accounts
                    .iter()
                    .find(|account| account.id == id)
                    .map_or_else(String::new, |account| account.name.clone());"""
red = [
    "test_two_accounts_that_read_alike_are_named_alike_in_both",
]
tests_last_seen = [
    { file = "src/application/destinations.rs", tests = 37 },
    { file = "tests/one_hierarchy_two_views.rs", tests = 12 },
]

[[guard]]
# Every folder lands in the first account's branch.
#
# The worst thing this function can get wrong, and the one nothing in the tree
# could see before this target existed: a folder filed under an account it does
# not belong to is a path sent to a server that never had it.
#
# The break keeps the lookup and drops the only thing in it that is about
# identity, which is the shape this defect really takes. It is the reason
# `test_a_folder_belongs_to_one_account_in_both` exists, since that test is
# green on arrival and this is where its red was taken.
#
# Measured twice, and the first measurement is the reason four tests are named
# here rather than two. This break reddened *nothing* on the first run, because
# the target flattened every branch away before comparing and read the account
# off each place rather than off the branch it was drawn under. Those two agree
# in working code and are not the same claim: the place still carried its own
# account while sitting under the wrong heading, so the comparison came out
# identical. The target now reads the branch, and
# `test_a_place_says_the_account_of_the_branch_it_is_drawn_under` was written to
# hold the two together. The second run reddens four.
name = "every folder the move window offers lands in the first account's branch"
file = "src/application/destinations.rs"
suite = "one_hierarchy_two_views"
before = """                    .find(|branch| branch.account_id == account)"""
after = """                    .find(|branch| !branch.account_id.is_empty())"""
# Measured a third time on the same day, and it fell behind twice in one
# session. A test added afterwards asks this same question from the caller's
# side, and a record shorter than the truth is what this file exists to stop.
#
# Measured a fourth time in 04.1-02, and it had fallen behind again for the
# same reason: three tests reading which accounts the window offers reach this
# rule too, because a branch lookup that has lost the account puts every folder
# in one branch and the other accounts then have nothing in them and are
# dropped. Five to eight. This is the record CLAUDE.md's paragraph about a
# record perishing is written from, and it has now perished three times.
red = [
    "test_a_folder_belongs_to_one_account_in_both",
    "test_a_folder_the_server_has_stopped_listing_is_still_somewhere_mail_can_go",
    "test_a_message_whose_folder_is_not_known_is_still_offered_everywhere",
    "test_a_place_says_the_account_of_the_branch_it_is_drawn_under",
    "test_every_account_with_somewhere_to_put_it_is_offered",
    "test_the_folder_the_message_is_in_is_taken_out_of_its_own_account_and_no_other",
    "test_the_picker_offers_the_folders_the_sidebar_shows",
    "test_the_window_offers_the_folders_of_the_account_the_message_is_in",
]
tests_last_seen = [
    { file = "src/application/destinations.rs", tests = 37 },
    { file = "tests/one_hierarchy_two_views.rs", tests = 12 },
]

[[guard]]
# The move window draws its folders as a list under a heading.
#
# Every place appended as a direct child of its account, whatever its depth
# said, which is what the window did for as long as `Destination.depth` existed.
# The tests beside the dialog cannot see it: they ask what a position means
# against a vector they write themselves, and the walk meets the same rows in
# the same order whether the tree is nested or flat. Only a live control says
# which of the two was built, which is what this target is for.
name = "the move window draws its folders as a list under a heading"
file = "src/presentation/wx_destination.rs"
suite = "tree_dialogs_resolve_the_row_somebody_is_on"
before = """                let depth = place.depth.min(under.len() - 1);"""
after = """                let depth = 0;"""
red = [
    "test_choosing_a_row_in_either_dialog_resolves_to_that_row",
]
tests_last_seen = [
    { file = "src/presentation/wx_destination.rs", tests = 6 },
    { file = "tests/tree_dialogs_resolve_the_row_somebody_is_on.rs", tests = 1 },
]

[[guard]]
# The move window builds its folder list from the account on screen again.
#
# The defect that shipped. All Inboxes reads every account's inbox as one list,
# so the account on screen and the account a row belongs to are routinely
# different, and this half took the one on screen while `spawn_folder_move` sent
# the command to the one `owner_of` names. The path was chosen on one server and
# the command went to another, where a mailbox of that name is a different
# mailbox.
#
# Invisible to everything else in the tree: the code compiles, every test
# passes, and the two accounts differ only in a running program with All Inboxes
# open. What catches it is the wiring check that reads this file for the
# question being asked, and that check named six handlers and not this one until
# 04.1-01, which is why nothing saw it.
#
# The test strengthened on 2026-09-22 by 12-02.1, after the break reddened
# nothing. Run 35520204784 found this guard green on a runner, and a
# measurement here on 2026-09-22 agreed: 1 of 1 named test stayed green with
# the guard broken, rebuild 18 s and run 2 s. The cause was the test and not
# the record. It asked whether `owner_of(` appears anywhere in
# `move_or_copy_message`, and that function calls it twice: once for the
# account the destinations are built from, which is what this record is about,
# and once per message for the account each row is moving out of. Taking the
# first away left the second, so the body still read as asking.
#
# `test_everything_aimed_at_a_message_asks_which_account_that_message_is_in`
# now reads the `account` binding the folder list is built from rather than the
# whole body. Taken red by hand under this break on 2026-09-22 before it was
# made green, `cargo test --test wired`: 76 passed and exactly this one failed,
# on the sentence about destinations chosen on one server and the command sent
# to another. The red list is unchanged, because the test that notices is the
# same test.
name = "the move window builds its folder list from the account on screen"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """        owner_of(
            &s.messages,
            &s.accounts,
            row_id,
            s.active_account_id.as_deref(),
        )
    }) else {
        return send_refusal(
            tx,
            rt,
            "This message is not in an account this program knows about.",
        );"""
after = """        s.active_account_id
            .as_deref()
            .and_then(|id| s.accounts.iter().find(|a| a.id == id).cloned())
    }) else {
        return send_refusal(
            tx,
            rt,
            "This message is not in an account this program knows about.",
        );"""
red = [
    "test_everything_aimed_at_a_message_asks_which_account_that_message_is_in",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

[[guard]]
# The move window stops asking where the last one went.
#
# Two things at once, which is why the break is worth having. Filing twenty
# messages into one folder is twenty walks through the tree again, which is what
# opening on the last one exists to save. And the census in `config.rs` that
# asks whether every stored setting is read by something answers through a
# reader function here, so this call is the second hop that census follows: with
# it gone, `last_filed_into` is a setting the program takes an answer for and
# ignores, which is the shape that census was written to catch.
#
# It is therefore also the proof that the widened census can still see a
# violation. The reading gained a second hop in 04.1-01 because a setting whose
# stored value holds two facts gets a pair of functions and stops being named
# anywhere but the file that owns it.
name = "the move window stops asking where the last one went"
file = "src/presentation/wx_app.rs"
before = """    let last_used = settings
        .as_ref()
        .and_then(|mgr| mgr.app_config().where_the_last_one_went(&account_id));"""
after = """    let last_used: Option<crate::data::config::FiledInto> = None;"""
red = [
    "data::config::every_setting_is_acted_on::test_every_setting_somebody_can_change_is_read_by_something",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── A copy across accounts, sent as a copy inside one ────────────────────────
#
# Two accounts are two mail servers and no IMAP command reaches across them.
# `COPY` names a mailbox on the connection it is sent down, so a crossing sent
# as an ordinary copy goes to the account the message is already in, naming a
# folder path that belongs to a different server.
#
# What that does depends on the source server and none of the answers is one
# somebody could act on. Usually it fails naming a mailbox the person can see
# in the window they just used. Where the two accounts happen to hold a folder
# at the same path, which is the ordinary case for `Archive`, `Sent` and
# `INBOX`, it succeeds and files the message into the wrong account, and the
# sentence afterwards says it went where it was asked to go.
#
# The break is the comparison itself rather than one of its arms, because a
# function with two values answers something whatever it does and the reading
# that ignores its arguments compiles.

[[guard]]
name = "the crossing decision compares nothing and every destination is the same account"
file = "src/application/mail_across_accounts.rs"
before = """    if the_message_is_in == it_is_going_to {
        Crossing::TheSameAccount
    } else {
        Crossing::AnotherAccount
    }"""
after = """    let _ = (the_message_is_in, it_is_going_to);
    Crossing::TheSameAccount"""
red = [
    "application::mail_across_accounts::tests::test_a_folder_in_the_same_account_and_one_in_another_are_not_the_same_answer",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
]

# ── Every account built, and all but one thrown away ─────────────────────────
#
# What the move and copy window did before 04.1-02, in the line it did it on.
# `where_mail_can_go` has always answered with every account's branches; the
# window kept the one the message was in, so a folder on another account was
# somewhere nobody could choose and copying to another account was unreachable
# whatever the code underneath could do.
#
# The break reddens nothing in the library, which is the point of the record.
# The window is what narrowed, and the only tests that can see the difference
# are the ones reading the assembly from outside.

[[guard]]
name = "the move window offers only the account the message is in"
file = "src/application/destinations.rs"
suite = "one_hierarchy_two_views"
before = """    offer(
        where_mail_can_go(accounts, folders),
        the_folder_it_is_in.map(|path| FolderInAnAccount {"""
after = """    offer(
        where_mail_can_go(accounts, folders)
            .into_iter()
            .filter(|branch| branch.account_id == the_account_it_is_in)
            .collect(),
        the_folder_it_is_in.map(|path| FolderInAnAccount {"""
red = [
    "test_every_account_with_somewhere_to_put_it_is_offered",
    "test_the_folder_the_message_is_in_is_taken_out_of_its_own_account_and_no_other",
    "test_a_message_whose_folder_is_not_known_is_still_offered_everywhere",
]
tests_last_seen = [
    { file = "src/application/destinations.rs", tests = 37 },
    { file = "tests/one_hierarchy_two_views.rs", tests = 12 },
]

# ── A window that opens every account it is given ────────────────────────────
#
# What the destination window did for as long as it only ever held one account,
# where it was right: a lone closed branch is a window that reads as empty.
#
# With eight accounts it is every folder of every account between somebody and
# the last one. A screen reader user meets rows in order and cannot skim, so
# rows they will not use are not clutter to look past, they are time spent
# listening. Closed, the same window is eight rows, each announced as collapsed
# and with a count of what is inside it.
#
# Nothing else in the tree can see this. The window builds, every row is
# present, every position resolves to the right destination, and the only
# difference is how long it takes to get to the last one. That is why the target
# that notices it is the one holding a live control: `is_expanded` is asked of
# the tree, not of the code that built it.

[[guard]]
name = "the destination window opens every account rather than one"
file = "src/presentation/wx_destination.rs"
suite = "tree_dialogs_resolve_the_row_somebody_is_on"
before = """            if open_branch == Some(branch.account_id.as_str()) {
                tree.expand(&under[0]);
            }"""
after = """            if open_branch.is_some() || open_branch.is_none() {
                tree.expand(&under[0]);
            }"""
red = [
    "test_choosing_a_row_in_either_dialog_resolves_to_that_row",
]
tests_last_seen = [
    { file = "src/presentation/wx_destination.rs", tests = 6 },
    { file = "tests/tree_dialogs_resolve_the_row_somebody_is_on.rs", tests = 1 },
]

# ── The removal sent before the destination has the message ──────────────────
#
# The one break in this phase that loses somebody's mail. A move across accounts
# is an append at one server and a removal at another, and nothing joins them:
# there is no transaction, no two-phase anything, and no way to undo either half
# once it has gone. The only safeguard available is the order.
#
# Removal last means every failure is recoverable. The destination refuses and
# the message is where it always was. The source will not let go and the message
# is in two places, which somebody can see and tidy up. Removal first means a
# refused append leaves the message nowhere, with a sentence saying it moved and
# nothing anywhere saying where it went.
#
# The break puts the removal at the top of the sequence and throws its answer
# away, which is where a rewrite that tidied the branching would arrive without
# meaning to: the removal has to happen in every arm that reaches it, so hoisting
# it looks like taking out a repetition.
#
# Measured 2026-09-08 against the whole library.
#
# Moved on 2026-09-22 by 12-02.1, after run 35520204784 could not build it. The
# runner said `error[E0599] no method named take_it_off_the_server found for
# reference &impl TheAccountItIsIn`, and suggested restricting the type
# parameter with `+ TheAccountItIsLeaving`.
#
# That error is the safeguard working. `fetch_and_keep`, where the break used
# to sit, takes only the reading trait, which has no write in it, so no body it
# can be given can send a change to the source. Widening its bound to make the
# break compile would edit a signature to make a guard applicable, which is a
# larger change than the rule needs and the kind that goes stale first, and it
# would also weaken the thing being guarded for the length of the run.
#
# So the break moves to where the order is decided and where the removal is
# already reachable: `move_it_across` takes both traits and calls
# `remove_at_the_source` after the append. Hoisting that call above the append
# is the same mistake the paragraph above describes, written where somebody
# tidying the branching would really write it.
#
# The commit that moved it: `e72a822c`, 11-07.2's failing cases for a crossing
# as a waiting move with its bytes, by `git log -S` over `fetch_and_keep`'s own
# signature. One commit, so it is both the introduction and the mover. Before
# it, the two lines the break was anchored on sat in `the_crossing`, bounded by
# `impl TheAccountItIsIn + TheAccountItIsLeaving`, and the break compiled;
# 11-07.2 extracted them into a function bounded by the reading trait alone.
#
# Measured on 2026-09-22 rather than predicted, and the list below is that
# run's answer.

[[guard]]
name = "the source is told to give the message up before the destination has it"
file = "src/application/mail_across_accounts.rs"
before = """        Ok(
            match append_and_ask(the_account_it_is_going_to, &fetched.message).await {
                Appended::ItLanded => {
                    remove_at_the_source(the_account_it_is_in, &fetched.message).await
                }"""
after = """        let _ = remove_at_the_source(the_account_it_is_in, &fetched.message).await;
        Ok(
            match append_and_ask(the_account_it_is_going_to, &fetched.message).await {
                Appended::ItLanded => {
                    remove_at_the_source(the_account_it_is_in, &fetched.message).await
                }"""
red = [
    "application::mail_across_accounts::tests::test_nothing_is_said_to_the_source_that_changes_it_until_the_append_has_gone_out",
    "application::mail_across_accounts::tests::test_a_destination_that_refuses_the_append_leaves_the_source_untouched",
    "application::mail_across_accounts::tests::test_a_refusal_is_an_answer_so_a_message_already_there_is_not_read_as_an_arrival",
    "application::mail_across_accounts::tests::test_a_message_that_was_already_there_is_not_read_as_the_one_just_sent",
    "application::mail_across_accounts::tests::test_a_folder_that_could_not_be_read_beforehand_settles_nothing_afterwards",
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_that_is_not_found_removes_nothing",
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_about_a_message_with_no_identifier_removes_nothing",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
]

# ── An append nobody answered, counted as one that landed ────────────────────
#
# The second way to lose a message, and it is quieter than the first because the
# order is still right. The append goes out, the connection drops, and nothing
# ever says whether the server took it. Treating that as success sends the
# removal, and if the message never arrived it is now nowhere.
#
# What makes it a question rather than a guess is asking the destination
# afterwards, and what makes the answer worth anything is that the folder was
# read before the message went as well. A `Message-ID` is a header a stranger
# wrote, and a folder that already held a message carrying it would otherwise
# confirm an arrival that never happened.
#
# The break makes the question answer yes to everything, which is the shape
# somebody arrives at by simplifying a function whose three answers look like
# two. Under it a message with an identifier is always taken to have landed,
# whatever the destination really holds and whether or not it could be read.
#
# Measured 2026-09-08 against the whole library, and measured again the same day
# when 04.1-04 gave the question a second caller. It reddened three more tests
# than the record named, which is the shape this file exists to catch: the same
# question is now asked by a move finished on a later run, so a version of it
# that answers yes to everything sends somebody's message to a destination that
# already has it. Two of the three say exactly that. The third is the check that
# every ending lets go of the kept bytes, which fails because a question
# answering yes leaves two of the six endings unreachable.
#
# Re-measured 2026-09-08.

[[guard]]
name = "an append the destination never answered is counted as one that landed"
file = "src/application/mail_across_accounts.rs"
before = """    match the_account_it_is_going_to
        .which_messages_carry(into, message_id)
        .await
    {
        Ok(now) if now.iter().any(|uid| !was_there_before.contains(uid)) => {
            WhetherItLanded::ItIsThere
        }
        Ok(_) => WhetherItLanded::ItIsNotThere,
        Err(why) => WhetherItLanded::ItCannotBeAsked(why.to_string()),
    }"""
after = """    let _ = (the_account_it_is_going_to, into, message_id, was_there_before);
    WhetherItLanded::ItIsThere"""
red = [
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_that_is_not_found_removes_nothing",
    "application::mail_across_accounts::tests::test_a_message_that_was_already_there_is_not_read_as_the_one_just_sent",
    "application::mail_across_accounts::tests::test_a_folder_that_will_not_answer_cannot_be_asked",
    "application::mail_across_accounts::tests::test_nothing_new_carrying_the_identifier_is_not_there_rather_than_unanswerable",
    "application::mail_across_accounts::tests::test_an_append_with_no_answer_that_is_found_afterwards_is_removed_at_the_source",
    "application::mail_across_accounts::tests::test_a_resumed_move_asks_the_destination_before_it_sends_anything",
    "application::mail_across_accounts::tests::test_a_resumed_move_the_destination_does_not_have_is_sent_again_then_removed",
    "application::mail_across_accounts::tests::test_no_way_a_crossed_move_can_end_leaves_the_bytes_behind",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 34 and a
    # measurement here that found the same four. 11-07.2 made a crossing a
    # waiting move, so the same question about an unanswered append is asked
    # again from `moves_waiting`, in a file this record had never named.
    "application::moves_waiting::tests::test_a_crossing_resumed_from_held_bytes_fetches_nothing_from_the_source",
    "application::moves_waiting::tests::test_a_destination_that_already_held_the_identifier_reads_only_a_new_number_as_the_arrival",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_cannot_be_asked_answers_not_reached_and_the_bytes_stay",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_does_not_hold_it_answers_refused",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
]

# ── A move finished on a later run, sent again without asking ────────────────
#
# The one way keeping the bytes can do harm that not keeping them would not,
# and the only reason the whole store needed a question built first.
#
# A row that outlived the program means this program stopped somewhere between
# writing the bytes and letting go of them. That is not the same as an append
# that never went: it may have gone and landed and the answer may have been on
# its way back when the process ended. Sending it again because a row exists
# gives somebody two copies of their message, at an account they cannot easily
# tell apart, with nothing anywhere saying why.
#
# The break skips the question and always sends. It is not an absence but the
# obvious shape: a row is there, so the move did not finish, so finish it. That
# reading is wrong about exactly one thing and it is the thing that matters.
#
# Measured 2026-09-08 against the whole library.

[[guard]]
name = "a move finished on a later run appends without asking the destination"
file = "src/application/mail_across_accounts.rs"
before = """    let landed = whether_the_destination_has_it(
        the_account_it_is_going_to,
        &unfinished.to_folder,
        the_identifier_to_ask_about(Some(&unfinished.identifier)),
        unfinished.was_there_before.as_deref(),
    )
    .await;"""
after = """    let landed = WhetherItLanded::ItIsNotThere;
    let _ = the_identifier_to_ask_about(Some(&unfinished.identifier));"""
red = [
    "application::mail_across_accounts::tests::test_a_resumed_move_asks_the_destination_before_it_sends_anything",
    "application::mail_across_accounts::tests::test_a_resumed_move_the_destination_already_has_is_only_removed",
    "application::mail_across_accounts::tests::test_a_resumed_move_the_destination_does_not_have_is_sent_again_then_removed",
    "application::mail_across_accounts::tests::test_a_resumed_move_that_cannot_be_asked_about_sends_nothing_to_either_server",
    "application::mail_across_accounts::tests::test_a_resumed_move_whose_folder_nobody_read_settles_nothing",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
]

# ── A finished move leaving its message on the disk ──────────────────────────
#
# What it costs a person: a whole message of theirs, unencrypted, sitting in
# the cache folder after the move it was written for finished perfectly well,
# with nothing anywhere saying it is there. On the next start they are asked
# whether to finish a move that is already done, and answering yes speaks to
# both servers again about it.
#
# The break takes away the one call that lets go of the bytes. It is placed on
# the way out of the whole crossing rather than in each ending, so there is one
# line to remove and one line to forget, which is the shape that was chosen
# precisely because six endings and two early returns are six and two chances
# to forget.
#
# Measured 2026-09-08 against the whole library.

[[guard]]
name = "a move that reached an ending leaves its message on the disk"
file = "src/application/mail_across_accounts.rs"
before = """    if let Some(cache) = recording.cache
        && let Err(e) = cache.the_move_is_over(recording.row)
    {
        tracing::warn!("The bytes kept for a finished move could not be let go of: {e}");
    }
    ended"""
after = """    let _ = recording;
    ended"""
red = [
    "application::mail_across_accounts::tests::test_no_way_a_crossed_move_can_end_leaves_the_bytes_behind",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
]

# ── A moved day of a series showing on both dates ────────────────────────────
#
# What it costs a person: a meeting that was moved from the Tuesday to the
# Thursday is on the calendar on both days, and only one of them is real. They
# either turn up to a meeting that is not happening or miss the one that is.
#
# The break is the half-fix rather than the deletion, because the deletion is
# not the mistake anybody makes. `one_day_kept_out_of_the_series` does two
# writes and the second one is the one that carries the de-duplication: it
# saves the series with that day called off, which is the EXDATE
# `occurrences::falls_on` filters the expansion by. Somebody writing the
# obvious half of this stores the changed day and stops, and the calendar looks
# right until the series is expanded over the date the day used to be on.
#
# `let _ = (series, the_day_it_was, who)` is in the break because the three
# arguments are then unused and the gate lints with `-D warnings`, so the honest
# half-fix does not build without it. It changes nothing about what the function
# does.
#
# Two records rather than one, because the break reddens tests in two places
# that cannot be measured together. A record carrying `suite` is scoped to that
# one integration target: `scripts/guards.py` runs `--test <suite>` and nothing
# else, and `tests/house_style.rs` resolves every name in its red list to
# `tests/<suite>.rs`, so a library test path named here is looked for inside the
# integration file and reported gone. The library half is the record below.
#
# Measured 2026-09-08 with `cargo test --test a_moved_day_is_shown_once`.

[[guard]]
name = "a day moved out of a series is still shown on the date it left"
file = "src/application/calendar.rs"
suite = "a_moved_day_is_shown_once"
before = """    cache.save_calendar_event(that_day)?;
    let called_off =
        crate::service::caldav::the_called_off_value_for(the_day_it_was, series.is_all_day);
    let (after, _) = with_one_more_day_called_off(series, &called_off);
    cache.save_calendar_event(&CalendarEventEntry {
        pending: series.pending || who == WhoTookTheDayOut::SomebodyHere,
        ..after
    })"""
after = """    let _ = (series, the_day_it_was, who);
    cache.save_calendar_event(that_day)"""
red = [
    "test_a_day_moved_out_of_a_series_is_on_the_calendar_once",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "tests/a_moved_day_is_shown_once.rs", tests = 2 },
]

# ── A changed day of a series that sounds like all the others ───────────────
#
# What it costs a person: they arrow down a calendar past fifty-two Tuesdays,
# one of which was moved or changed on its own, and nothing they hear tells
# them which. Every other thing the row says about that day is the same as the
# ordinary days say about themselves.
#
# Two records, because two separate things can go quiet and breaking one does
# not catch the other. This one is the row forgetting it stands apart:
# `from_entry` stops reading the stored columns and answers that every row is
# an ordinary day. The constant rather than the removal, because a field that
# is simply gone does not build and is nobody's mistake; a field wired to a
# plausible default is what a half-finished version looks like, and it is green
# everywhere except here.
#
# Only two tests answer to it. The tests about what is said build their rows
# directly with the field set, so they say nothing about where the field came
# from. That is the record after it, and finding out that one break did not
# reach both halves is the reason there are two.
#
# Measured 2026-09-08 with `WIXEN_TEST_THREADS=4 cargo test --lib
# --no-fail-fast` on a clean tree.

[[guard]]
name = "a row stops knowing it is a day of a series that was changed on its own"
file = "src/presentation/ui_types.rs"
before = """            changed_on_its_own: entry.cut_from_event_id.is_some()
                || entry.provider_recurrence_id.is_some(),"""
after = """            changed_on_its_own: false,"""
red = [
    "presentation::ui_types::tests::test_a_day_changed_on_its_own_is_known_from_either_stored_column",
    "presentation::ui_types::tests::test_every_day_a_series_is_shown_on_says_whether_that_day_was_changed",
]
tests_last_seen = [
    { file = "src/presentation/ui_types.rs", tests = 79 },
]

# ── The same fact, known and then not said ──────────────────────────────────
#
# The other half of the record above. The row knows the day was changed and the
# words for it go quiet, which is the failure this project's second guardrail
# is about: the structure is there, the tests about the field pass, and the
# person moving down the list hears nothing.
#
# The break is the clause answering nothing whatever it is asked, which is a
# half-written function rather than a deleted one. It reddens five tests across
# the two places the clause is heard: the row cell a screen reader reads while
# arrowing, and the short reading Space gives. Both call the one function, so
# one break reaches both, which is the point of there being one function.
#
# Measured 2026-09-08 with `WIXEN_TEST_THREADS=4 cargo test --lib
# --no-fail-fast` on a clean tree.

[[guard]]
name = "the clause about a day changed on its own goes quiet"
file = "src/presentation/read_aloud.rs"
before = """    match changed_on_its_own {
        true => "changed just for this day",
        false => "",
    }
}"""
after = """    let _ = changed_on_its_own;
    ""
}"""
red = [
    "presentation::pim_rows::tests::test_a_changed_day_out_of_hours_is_told_both_things",
    "presentation::pim_rows::tests::test_a_day_changed_on_its_own_says_so_in_the_row_itself",
    "presentation::read_aloud::tests::test_a_changed_day_of_a_series_nobody_can_work_out_is_told_both_things",
    "presentation::read_aloud::tests::test_a_day_changed_on_its_own_says_so_in_the_short_reading",
    "presentation::read_aloud::tests::test_the_full_reading_does_not_say_the_day_was_changed_over_again",
]
tests_last_seen = [
    { file = "src/presentation/pim_rows.rs", tests = 25 },
    { file = "src/presentation/read_aloud.rs", tests = 51 },
]

# ── The same break as the moved-day record, seen from the library ───────────
#
# The suite-scoped record further up is run with `--test <suite>` and nothing
# else, so it can say nothing about the library. This one carries the rest of
# what the same break reddens, measured on its own with
# `WIXEN_TEST_THREADS=4 cargo test --lib --no-fail-fast` on a clean tree on
# 2026-09-08.
#
# Worth reading rather than skipping: seven tests across three modules answer
# to one pair of writes. The CalDAV read, the Google read and the screen that
# changes one day all reach this function, which is the point of it existing
# once, and it is also why breaking it quietly would go wrong in three places
# at once.

[[guard]]
name = "a day moved out of a series is still shown on the date it left, seen from the library"
file = "src/application/calendar.rs"
before = """    cache.save_calendar_event(that_day)?;
    let called_off =
        crate::service::caldav::the_called_off_value_for(the_day_it_was, series.is_all_day);
    let (after, _) = with_one_more_day_called_off(series, &called_off);
    cache.save_calendar_event(&CalendarEventEntry {
        pending: series.pending || who == WhoTookTheDayOut::SomebodyHere,
        ..after
    })"""
after = """    let _ = (series, the_day_it_was, who);
    cache.save_calendar_event(that_day)"""
red = [
    "application::caldav_sync::tests::test_a_caldav_series_and_its_moved_day_in_one_resource_leaves_two_linked_rows",
    "application::calendar::tests::test_a_day_moved_at_google_is_one_meeting_at_its_new_time",
    "application::calendar::tests::test_a_day_of_a_series_is_folded_in_even_when_google_names_it_first",
    "application::calendar::tests::test_who_took_the_day_out_decides_whether_the_series_is_still_waiting_to_be_sent",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_cut_out_of_a_series_kept_only_here_is_not_refused_over_its_zone",
    "presentation::managers::changing_one_day_of_a_series::test_a_day_taken_off_a_series_is_no_longer_shown_and_the_others_still_are",
    "presentation::managers::changing_one_day_of_a_series::test_changing_one_day_stores_the_changed_day_and_the_series_with_that_day_called_off",
]
tests_last_seen = [
    { file = "src/application/caldav_sync.rs", tests = 67 },
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# The step Previous period and Next period make, broken to a constant rather
# than removed. A week that answers the day it was already on is what a
# half-finished version looks like: the buttons are enabled, they are pressed,
# the list is drawn again and the heading written again, and everything says
# what it said before. Nothing that only asks whether the buttons are enabled
# can see it.
#
# Measured twice, and the first measurement is why this record names two tests.
# It reddened one, the one comparing the dates by hand, and nothing asked
# whether the words somebody hears after pressing Prev had changed. That is a
# thinner guard than the button deserves, so
# `test_the_heading_names_a_different_week_after_a_step` was written, taken red
# against this same break, and the break measured again.
[[guard]]
name = "the step to the week before and after answers the week it is already on"
file = "src/presentation/ui_types.rs"
before = """            (CalendarView::Week, Step::Back) => self.day - chrono::Duration::days(7),
            (CalendarView::Week, Step::Forward) => self.day + chrono::Duration::days(7),"""
after = """            (CalendarView::Week, _) => self.day,"""
red = [
    "presentation::ui_types::tests::test_the_week_before_and_after_move_by_exactly_seven_days_at_both_ends",
    "presentation::ui_types::tests::test_the_heading_names_a_different_week_after_a_step",
]
tests_last_seen = [
    { file = "src/presentation/ui_types.rs", tests = 79 },
]

# The month step made by adding and taking off thirty days, which is the naive
# version and the one somebody writes without thinking about it.
#
# The plan that asked for this record named it for the year boundary, and the
# measurement says that is the wrong thing to name it for: thirty days back from
# the fourteenth of January is the fifteenth of December, which is the right
# month, and thirty days on from the fourteenth of December is the thirteenth of
# January, which is also the right month. The naive step is correct across a year
# boundary and wrong about the day that does not exist in the shorter month, so
# this record guards that and the record below guards the boundary, on a break
# that can actually reach it.
[[guard]]
name = "the month step counts thirty days instead of going to the month next door"
file = "src/presentation/ui_types.rs"
before = """            (CalendarView::Month, Step::Back) => the_same_day_of(
                the_first_of_the_month_holding(self.day) - chrono::Duration::days(1),
                self.day,
            ),
            (CalendarView::Month, Step::Forward) => the_same_day_of(
                the_last_day_of_the_month_holding(self.day) + chrono::Duration::days(1),
                self.day,
            ),"""
after = """            (CalendarView::Month, Step::Back) => self.day - chrono::Duration::days(30),
            (CalendarView::Month, Step::Forward) => self.day + chrono::Duration::days(30),"""
red = [
    "presentation::ui_types::tests::test_stepping_back_from_the_thirty_first_lands_in_the_shorter_month_and_forward_returns",
]
tests_last_seen = [
    { file = "src/presentation/ui_types.rs", tests = 79 },
]

# December's next month taken as January of the same year, which is the
# off-by-one every piece of month arithmetic is one keystroke away from. It
# reverses December's own window, so the far end falls before the near one and
# nothing can be inside it, and it puts the month after December a year early.
#
# This is the break the record above was meant to be and could not be. Written
# in the helper that answers what the month next door is, because that is the
# one place the year is decided.
[[guard]]
name = "the month after December is taken as January of the same year"
file = "src/presentation/ui_types.rs"
before = """        12 => (first.year() + 1, 1),"""
after = """        12 => (first.year(), 1),"""
red = [
    "presentation::ui_types::tests::test_the_month_after_december_is_january_of_the_year_after",
    "presentation::ui_types::tests::test_the_month_before_january_is_december_of_the_year_before",
]
tests_last_seen = [
    { file = "src/presentation/ui_types.rs", tests = 79 },
]

# ── A copy that writes over the row it read ─────────────────────────────────
#
# What it costs a person: they press Ctrl+Shift+Y on a task, hear "Book the
# dentist copied to Next week", and This week has lost it. A copy that quietly
# became a move is not something anybody working by ear can see, and the
# sentence they heard says the opposite of what happened.
#
# It is the failure that arrives by sharing one function too far. Moving and
# copying are one write, one chooser and one read, and the whole of what makes
# them different is three answers on `Filing`. Answering the third the way a
# move answers it turns the copy back into a move.
#
# Two records, and the second is not a duplicate of the first. A record is also
# what couples an integration suite to the source it guards: `check.sh` reads
# `file` to decide which commits run the suite, so the record below on
# `destinations.rs` is what makes a commit touching the decision run these
# tests, and the record after it does the same for the file that carries out
# the write. Without the second, a commit changing the write would not run the
# suite that watches it.
#
# Measured 2026-09-08 with `WIXEN_TEST_THREADS=4 cargo test --all-targets
# --no-fail-fast` on a clean tree with nothing else building. No library test
# goes red for either break, which is a fact about where the coverage is rather
# than a red list to pad: `Filing` has no unit tests of its own and is measured
# through the real write instead, which is the stronger of the two.

[[guard]]
name = "a copy makes a new row rather than writing over the one it read"
file = "src/application/destinations.rs"
suite = "a_copy_leaves_the_original_where_it_was"
before = """        match self {
            Self::Moving => false,
            Self::Copying => true,
        }
    }
}"""
after = """        match self {
            Self::Moving | Self::Copying => false,
        }
    }
}"""
red = [
    "test_a_task_copied_into_another_list_is_on_both_lists",
    "test_copying_a_task_a_provider_holds_leaves_the_provider_owed_nothing",
    "test_the_copy_of_a_note_a_backend_holds_makes_no_claim_on_the_backends_own",
    "test_the_copy_of_a_task_a_provider_holds_is_this_computers_own",
    "test_the_copy_of_an_event_a_provider_holds_makes_no_claim_on_the_providers_own",
]
tests_last_seen = [
    { file = "src/application/destinations.rs", tests = 37 },
    { file = "tests/a_copy_leaves_the_original_where_it_was.rs", tests = 9 },
]

# The same failure one step later, at the write rather than at the decision.
# The copy keeps the identifier the provider gave the original, so the store
# writes over the row it read and the next push asks the provider to update its
# own task instead of creating a second one.
[[guard]]
name = "a copied task is given an identifier of its own"
file = "src/presentation/managers.rs"
suite = "a_copy_leaves_the_original_where_it_was"
before = """                task.id = new_id("task");
                task.remote_updated = None;"""
after = """                task.remote_updated = None;"""
red = [
    "test_a_task_copied_into_another_list_is_on_both_lists",
    "test_copying_a_task_a_provider_holds_leaves_the_provider_owed_nothing",
    "test_the_copy_of_a_task_a_provider_holds_is_this_computers_own",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "tests/a_copy_leaves_the_original_where_it_was.rs", tests = 9 },
]

# ── A copy line missing from a panel where copying works ────────────────────
#
# What it costs a person: they arrow down the menu on a task, find no way to
# make a second copy of it in another list, and the only route left is to type
# it again. The menu and the command have to agree in both directions, and this
# is the direction that reads as an absence rather than as a mistake.
#
# **This record was wrong from 2026-09-08 until 2026-09-10, and how it went
# wrong is the thing worth keeping.** Its break used to add a second copy line
# to the reminders menu, on the reasoning that a reminder is sorted into a
# bucket worked out from when it is due and a bucket is not a place. 05-05 gave
# a reminder the one container it really has, its account, and put
# "Cop&y to another account" on that menu. From that commit the break was
# adding a line the menu already had, so it reddened
# `test_no_menu_offers_the_same_thing_twice` and
# `test_no_two_entries_in_one_menu_share_a_mnemonic`, which are about
# duplicates, and left the test this record names green. A record whose break
# reddens two checks it does not name and not the one it does is a guard
# nobody wrote down standing where a guard somebody wrote down used to be.
#
# Nothing said so, and nothing could have. The count check saw 17 tests before
# and 17 after, because 05-05 added no test to this file. Only the runner asks
# the question this failed, and it was asked on 2026-09-10 because a different
# plan added a test here and the count check printed the remedy.
#
# The break is now the direction the name promises: the copy line taken off the
# task menu, which is the edit somebody makes while tidying a list they think
# is repetitive.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 7,106 passed
# and two failed. The ordering check is the second, and it belongs: it asserts
# that a copy sits next to the move it is the twin of, so a menu that loses its
# copy line loses the pairing too.
[[guard]]
name = "the copy line is on exactly the menus whose command accepts one"
file = "src/application/context_menu.rs"
before = """static TASKS: &[Entry] = &[
    entry("&New task", Action::NewItem),
    entry("Mo&ve to another list", Action::MoveItem),
    entry("Cop&y to another list", Action::CopyItem),"""
after = """static TASKS: &[Entry] = &[
    entry("&New task", Action::NewItem),
    entry("Mo&ve to another list", Action::MoveItem),"""
red = [
    "application::context_menu::tests::test_a_copy_is_offered_exactly_where_it_means_something",
    "application::context_menu::tests::test_a_copy_sits_beside_the_move_it_is_the_twin_of",
]
tests_last_seen = [
    { file = "src/application/context_menu.rs", tests = 18 },
]

# ── A contact left in a group it was only passing through ───────────────────
#
# What it costs a person: they move somebody out of Team A and into Team B, are
# told nothing happened because she was not in Team A after all, and she is now
# in Team B anyway. Nobody working by ear sees that. The sentence they heard
# said the move did not happen, and a membership appeared.
#
# Whether she is really in the group she is leaving is answered by the take-out
# itself, by how many rows it removed, rather than by a read before the write,
# because a membership read first can be stale by the time the write runs. That
# answer therefore arrives after the put-in has already happened, so the put-in
# has to be undone, and the transaction is the only thing that undoes it.
#
# The break is the transaction losing its hold, written as the one edit that
# does it: committing before the early answer, which is what somebody adding a
# "make sure we commit" line writes. Running the two statements on `self.conn`
# instead was tried first and is NOT the break, because it is not one: the
# statements go through the same connection, so SQLite runs them inside the
# open transaction whichever handle issued them, and the tests all stay green.
#
# The record is also what couples this suite to the write it guards. `check.sh`
# reads `file` to decide which commits run which tests, and the green commit
# that added the write ran `data::message_cache::contacts` and not the suite,
# because no record named the file yet.
#
# Measured 2026-09-08 with `WIXEN_TEST_THREADS=4 cargo test --all-targets
# --no-fail-fast` on a clean tree with nothing else building. Exactly one test,
# tree-wide. The other four in the suite reach a route where both halves
# succeed, so a transaction and two loose statements are the same thing to them.
[[guard]]
name = "a contact is not left in the group it was moving to when the move did not happen"
file = "src/data/message_cache/contacts.rs"
suite = "a_contact_moved_between_groups"
before = """            return Ok(MovedBetweenGroups::NotInTheGroupItWouldLeave);
        }"""
after = """            let _ = moving.commit();
            return Ok(MovedBetweenGroups::NotInTheGroupItWouldLeave);
        }"""
red = [
    "test_a_contact_that_is_not_in_the_group_it_would_leave_is_not_put_in_the_other_one",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
    { file = "tests/a_contact_moved_between_groups.rs", tests = 8 },
]

# ── A chooser that reads out every group in the account ─────────────────────
#
# What it costs a person: they press Ctrl+Shift+V on a contact who is in two
# groups and are read a list of twenty. Nothing has crashed and no wrong answer
# has been given, so somebody hearing it assumes they pressed the wrong key or
# landed on the wrong row. Choosing a group the contact is not in then asks for
# a move that cannot happen.
#
# Two records, because the defect has two independent homes and the first
# guards only one of them. The answer to "which groups could this contact
# leave" is a pure function with unit tests; the question being asked of that
# answer is a call site in a window handler with no unit tests at all, because
# it lives in `managers.rs`, which 42 records fingerprint. The regression
# somebody actually writes is the second: one word at the call site, `&groups`
# where `&leaving` was, which leaves the pure function and every one of its
# tests untouched.
#
# Two candidate breaks were measured for the first record, both with
# `WIXEN_TEST_THREADS=4 cargo test --all-targets --no-fail-fast` on a clean
# tree on 2026-09-08, and they redden exactly the same two tests: the filter
# widened to every group, and the filter negated to the groups the contact is
# not in. The widened one is kept because it is the defect this record is named
# for and the state the code was in before the filter was written; the negation
# produces a different wrong list. That they are indistinguishable is worth
# recording: the tests are sensitive to the set being wrong at all rather than
# to it being too wide.
#
# No library test reddens for the second break and no integration test reddens
# for the first, which is the measurement that made this two records rather
# than one.
[[guard]]
name = "the groups a contact can leave are the ones it is in"
file = "src/application/contact_groups.rs"
before = """    groups
        .iter()
        .filter(|group| is_in(contact_id, group))
        .cloned()
        .collect()
}"""
after = """    let _ = contact_id;
    groups.to_vec()
}"""
red = [
    "application::contact_groups::tests::test_the_groups_a_contact_can_leave_are_the_ones_it_is_in",
    "application::contact_groups::tests::test_a_contact_in_no_group_can_leave_none_of_them",
]
tests_last_seen = [
    { file = "src/application/contact_groups.rs", tests = 30 },
]

# The same defect one step later, at the call site rather than at the answer.
# The filter is still right and nothing is asked of it.
[[guard]]
name = "the question about which group a contact is leaving is asked of the groups it is in"
file = "src/presentation/managers.rs"
suite = "a_contact_moved_between_groups"
before = """            &leaving,
            "Which group should this contact come out of?","""
after = """            &groups,
            "Which group should this contact come out of?","""
red = [
    "test_each_group_question_is_asked_of_the_groups_that_question_has_an_answer_in",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "tests/a_contact_moved_between_groups.rs", tests = 8 },
]

# ── The one column a reminder's move turns on ───────────────────────────────
#
# `save_reminder` is an upsert whose `ON CONFLICT(id) DO UPDATE SET` list names
# eight columns and not `account_id`. So the move anybody writes first, read the
# row, change the field, save it, writes every other column, reports success and
# leaves the reminder in the account it started in. The break is that mistake
# rather than a deletion: the UPDATE keeps its WHERE clause and stops naming
# `account_id`, which is a write that moves nothing while answering `Moved`.
#
# Two records for one break, because the red list spans two cargo targets. A
# record carrying `suite` runs one target and resolves every name in it, so the
# library test and the three integration tests cannot be written as one.
#
# Measured 2026-09-08 with `WIXEN_TEST_THREADS=4 cargo test --all-targets
# --no-fail-fast` on a clean tree with nothing else building. Four tests
# tree-wide, and the split is 1 and 3.
#
# Two things it does not redden, and both were predicted wrong before the run.
# `test_a_move_to_the_account_it_is_already_in_leaves_the_row_exactly_as_it_was`
# stays green in both targets: the WHERE clause still refuses a row already in
# that account, so nothing is written either way and the answer is unchanged.
# And the copy stays green, because the copy is `save_reminder` under a new
# identifier and never touches this statement.
[[guard]]
name = "a reminder's move writes the account rather than every column but the account"
file = "src/data/message_cache/reminders.rs"
suite = "a_reminder_moved_to_another_account"
before = """                "UPDATE reminders SET account_id = ?2, updated_at = ?3
                 WHERE id = ?1 AND account_id <> ?2","""
after = """                "UPDATE reminders SET updated_at = ?3
                 WHERE id = ?1 AND account_id <> ?2","""
red = [
    "test_a_reminder_moved_to_another_account_is_in_that_one_and_not_the_one_it_left",
    "test_a_moved_reminder_carries_everything_about_it_except_the_time_it_changed",
    "test_a_move_leaves_every_other_reminder_and_account_where_they_were",
]
tests_last_seen = [
    { file = "src/data/message_cache/reminders.rs", tests = 9 },
    { file = "tests/a_reminder_moved_to_another_account.rs", tests = 7 },
]

# The same break, read from the library. This half is the one that asserts the
# trap itself: that `save_reminder` still ignores `account_id`, and that the
# move does not.
[[guard]]
name = "the write that moves a reminder is not the upsert that cannot"
file = "src/data/message_cache/reminders.rs"
before = """                "UPDATE reminders SET account_id = ?2, updated_at = ?3
                 WHERE id = ?1 AND account_id <> ?2","""
after = """                "UPDATE reminders SET updated_at = ?3
                 WHERE id = ?1 AND account_id <> ?2","""
red = [
    "data::message_cache::reminders::tests::test_a_move_between_accounts_is_the_one_column_save_reminder_will_not_write",
]
tests_last_seen = [
    { file = "src/data/message_cache/reminders.rs", tests = 9 },
]

# Re-measured by hand on 2026-09-09 with `cargo test --all-targets
# --no-fail-fast` on a clean tree, after `05-08`, and **this guard is now much
# weaker than the record above it used to say.** That is the finding, not the
# red list.
#
# The break is the half-fix, not the deletion. `moving_can_be_told` is asked
# twice, once in `where_it_could_go` before the chooser opens and once here
# before anything is written, and somebody tidying up a duplicated question
# takes out the second. That leaves the refusal working from the menu and gone
# from every other route into the write, which is the state the second question
# exists to prevent. Deleting the refusal outright would redden far more and
# would be noticed by anybody reading the diff; this is the edit that looks like
# housekeeping.
#
# It used to redden five tests across three targets, four of them about a task.
# It now reddens **one**, tree-wide, and the one is about an event. `05-08`
# lifted the refusal for a task, so a task no longer passes through this question
# at all: `file_under` sends it to `move_a_task_the_provider_holds` on a branch
# of its own, and taking this block out changes nothing about a task. What is
# left here is the event, which is still refused for the reason a task used to
# be, and one library test is the whole of what would notice.
#
# Measured rather than predicted, and the prediction would have been wrong in the
# usual direction: the four task tests that used to redden here were rewritten by
# `05-08` to assert that the move happens, and a move that happens goes on
# happening with this block gone.
#
# The earlier note about a `suite` still holds. Without one a record runs
# `cargo test --lib`, so a library test is the only kind its red list can hold,
# and the event has no integration file of its own anyway.
[[guard]]
name = "the second question before a move is written is asked as well as the first"
file = "src/presentation/managers.rs"
before = """    if filing.needs_the_holder_told()
        && let Err(refused) = moving_can_be_told(cache, kind, id, "", account_id)
    {
        return Err(Error::Other(refused));
    }"""
after = """"""
red = [
    "presentation::managers::tests::test_moving_an_event_the_provider_holds_is_refused_and_writes_nothing",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# Measured by hand on 2026-09-09 with `cargo test --lib --no-fail-fast` on a
# clean tree, and measured against a second candidate before it was chosen.
#
# The break moves the stamp out of the arm that runs when the provider took the
# deletion, so every deletion is written down as taken whether it landed or not.
# That is what a half-finished version of this loop produces: the call and the
# bookkeeping look like two steps of one action rather than one step conditional
# on the other.
#
# What it costs is the recovery this whole plan rests on. A delete the provider
# refused would be recorded as done, so nothing would ever ask again, and the
# provider would keep its copy of a task that had already moved. The move would
# be half finished for ever with nothing left saying so.
#
# It reddens exactly one test, and that is the reason this candidate was kept.
# The other measured was the pending row's create not being retried, broken by
# clearing the flag when the create failed. That reddens three, and two of them
# are older tests about a list going and about a priority nothing understands.
# So that break is already guarded by tests that predate this plan and a record
# for it would be a second guard on a defect that has one. This break has none.
[[guard]]
name = "a deletion the provider refused is not written down as taken"
file = "src/application/tasks_sync.rs"
before = """        match sent {
            Ok(()) => {
                let _ = cache.the_provider_took_the_deletion_of_a_task(
                    &gone.id,
                    &crate::application::deletions::written(chrono::Utc::now()),
                );
                result.sent += 1;
            }"""
after = """        let _ = cache.the_provider_took_the_deletion_of_a_task(
            &gone.id,
            &crate::application::deletions::written(chrono::Utc::now()),
        );
        match sent {
            Ok(()) => {
                result.sent += 1;
            }"""
red = [
    "application::tasks_sync::tests::test_a_move_whose_delete_is_refused_is_finished_by_the_sync_after_it",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
]

# Measured by hand on 2026-09-09 with `cargo test --all-targets --no-fail-fast`
# on a clean tree, after `05-08` lifted the refusal.
#
# The break is one word, and it is the way this would most plausibly be undone.
# `moving_can_be_told`'s task arm answered `tasks_sync::a_provider_holds(id)`
# from the day the chooser was written until 2026-09-09, and every other arm in
# that match still answers a question rather than a constant. A `false` sitting
# among them reads like something half-finished, and somebody resolving a merge
# between this branch and anything written before it puts the call back without
# reading why it went.
#
# What it costs is the whole of this plan. With the call back, a task a provider
# holds is refused before the chooser opens and again before anything is written,
# which is where this program was for a year, and the sentence it says stops
# being true the moment the arm is right.
#
# Deliberately not a record on `file_under`'s branch. That branch answers the
# same question, and a break there leaves the chooser offering a move the write
# then carries out the wrong way, which is a different defect worth its own
# record if anybody writes one. This is the arm the chooser and the write both
# read.
[[guard]]
name = "a task a provider holds is not refused a move"
file = "src/presentation/managers.rs"
before = """        ItemKind::Task => false,"""
after = """        ItemKind::Task => crate::application::tasks_sync::a_provider_holds(id),"""
#
# The red list grew from 3 to 10 inside the same plan, and that is left on the
# record rather than tidied away. Task 1 measured it at 3 and the measurement was
# right that day. Task 2 then added seven tests about the ways this move fails,
# and every one of them starts by making the move, so with the refusal back every
# one panics where it sets up rather than failing on what it is about. Found by
# `--remeasure` hours after the record was written, and predicted by nobody.
red = [
    "presentation::managers::tests::test_a_task_the_provider_holds_is_offered_the_chooser_like_any_other",
    "presentation::managers::tests::test_moving_a_task_the_provider_holds_writes_the_new_copy_and_the_note",
    "application::tasks_sync::tests::test_a_task_google_holds_moves_list_and_two_syncs_carry_it_out",
    "application::tasks_sync::tests::test_a_move_held_by_allow_changes_is_counted_apart_from_one_held_by_the_copy",
    "application::tasks_sync::tests::test_a_move_interrupted_by_the_program_closing_is_finished_when_it_opens_again",
    "application::tasks_sync::tests::test_a_move_into_a_list_made_here_is_never_sent_and_the_note_waits",
    "application::tasks_sync::tests::test_a_move_that_needs_a_sign_in_is_not_reported_as_a_problem",
    "application::tasks_sync::tests::test_a_move_whose_create_is_refused_sends_no_delete_and_keeps_both_halves",
    "application::tasks_sync::tests::test_a_move_whose_delete_is_refused_is_finished_by_the_sync_after_it",
    "application::tasks_sync::tests::test_a_task_microsoft_holds_moves_list_the_same_way",
]
tests_last_seen = [
    { file = "src/application/tasks_sync.rs", tests = 119 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# Measured by hand on 2026-09-09 with `cargo test --all-targets --no-fail-fast`.
#
# The break is the sentence made uniform: every filing gets the clause, which is
# the tidy-up somebody writes when four arms look like three arms and a special
# case. It is the version of this defect that ships, because the clause is
# genuinely right most of the time and saying it always looks like consistency.
# What it costs is the thing the clause was added for: a note and a task filed
# into a list made on this computer are told the account has not had them yet,
# when no account is ever going to.
#
# Four tests, all of them in the same module, and three of them older than this
# feature. That is the shape worth noting: the ones that catch this are the
# plain ones asserting what a move says, not the new one asserting what it says
# when something is waiting. A guard asking only whether the clause can appear
# would have stayed green through the whole break.
[[guard]]
name = "the clause about the account is said only where something really is waiting"
file = "src/application/pim_command.rs"
before = """        Waiting::StaysHere => landed,"""
after = """        Waiting::StaysHere => format!("{landed}, and has not reached the account yet"),"""
red = [
    "application::pim_command::tests::test_a_copy_says_it_was_copied_and_never_that_it_moved",
    "application::pim_command::tests::test_a_filing_that_stays_on_this_computer_says_nothing_about_waiting",
    "application::pim_command::tests::test_a_move_of_an_untitled_row_still_says_where_it_went",
    "application::pim_command::tests::test_a_move_says_what_moved_and_where_it_went",
]
tests_last_seen = [
    { file = "src/application/pim_command.rs", tests = 38 },
]

# The same defect one step earlier, at the deciding rather than at the wording,
# and a second record because the two are different edits in different files
# and neither break applies to the other.
#
# It reddens exactly one test, measured on 2026-09-09 rather than predicted, and
# that is worth leaving on the record rather than widening until it looks
# healthier. The reason it is one: every other test of the sentence hands
# `filed` a `Waiting` directly and never comes through here, and the integration
# tests that do decide something ask `will_have_to_be_sent`, which is the other
# half of the question. So this one test is the whole of what stands between
# `what_is_waiting` and answering "waiting" for everything.
[[guard]]
name = "nothing is waiting when nothing was going to be sent"
file = "src/application/pim_command.rs"
before = """        (false, _) => Waiting::StaysHere,"""
after = """        (false, _) => Waiting::ToBeSent,"""
red = [
    "application::pim_command::tests::test_nothing_is_waiting_when_nothing_was_going_to_be_sent",
]
tests_last_seen = [
    { file = "src/application/pim_command.rs", tests = 38 },
]

# ── A move that did not happen, leaving both halves of one behind ───────────
#
# What it would cost a person, once `05-08` makes the calls: they move a task
# their account holds, a sync between their reading of it and the write decides
# the account no longer has it, and the move answers that there was nothing
# there to move. What is left behind is a second copy of the task nobody asked
# for, in a list they were moving it to, and a note telling the next sync to
# delete the first one at the account. Nothing said any of that happened.
#
# Whether the task is really still here is answered by the removal itself, by
# how many rows it took away, and not by a read before the write, because a read
# first can be stale by the time the write runs. That answer therefore arrives
# after the new copy and the note are already written, so both have to be undone,
# and the transaction is the only thing that undoes them.
#
# The break is the transaction losing its hold, written as the one edit that
# does it: committing before the early answer, which is what somebody adding a
# "make sure we commit" line writes. Moving the statements onto `self.conn` is
# NOT the break and would leave every test green, because SQLite scopes a
# transaction to a connection rather than to the handle a statement was issued
# through, so they run inside the open transaction either way. That was measured
# on 2026-09-08 for the identical break on
# `MessageCache::move_contact_between_groups` and is recorded there too.
#
# Measured 2026-09-09 with `WIXEN_TEST_THREADS=4 cargo test --all-targets
# --no-fail-fast` on a clean tree with nothing else building, 47 targets. Exactly
# one test, tree-wide, and the same count the contacts version came out at. The
# other seven in the suite reach a route where every write succeeds, so a
# transaction and four loose statements are the same thing to them.
[[guard]]
name = "a move of a task the provider holds that did not happen leaves neither half behind"
file = "src/data/message_cache/tasks.rs"
suite = "a_half_finished_task_move"
before = """            return Ok(MovedWhatTheProviderHolds::ItIsNotHereToMove);
        }"""
after = """            let _ = moving.commit();
            return Ok(MovedWhatTheProviderHolds::ItIsNotHereToMove);
        }"""
red = [
    "test_a_move_of_a_task_that_is_no_longer_here_writes_neither_half",
]
tests_last_seen = [
    { file = "src/data/message_cache/tasks.rs", tests = 22 },
    { file = "tests/a_half_finished_task_move.rs", tests = 8 },
]

# ── A note's body, on its way through storage ───────────────────────────────
#
# The break is the tidy-up nobody argues about: trim the body before storing
# it. It is written here because everything a person meets treats a note's body
# as the exact text they typed. The editor calls its box "Body, in Markdown",
# the reader looks for whatever structure is in it, and a backend after this
# has to hand the same bytes to a server. A trim is one word and it makes all
# three quietly untrue for a note ending in a space or a newline.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building. 6,732 passed and exactly two failed.
#
# One of the two predates this record and is here by accident rather than by
# design. `test_get_note_returns_the_full_body` is about the list preview being
# truncated while the editor's copy is not, and its fixture happens to end in a
# newline, so a trim reddens it without anybody having meant it to. That is
# worth writing down rather than counting as coverage: the accident goes the
# moment somebody tidies that fixture, and then this record would be short by
# one and nothing would say so. The round-trip test beside it names the wrong
# implementation each of its four fixtures is aimed at, so it does not depend
# on an accident.
[[guard]]
name = "a note's body is stored the bytes it was typed as"
file = "src/data/message_cache/notes.rs"
before = """                    n.title,
                    n.body,"""
after = """                    n.title,
                    n.body.trim(),"""
red = [
    "data::message_cache::notes::tests::test_a_notes_body_comes_back_the_bytes_it_went_in_as",
    "data::message_cache::notes::tests::test_get_note_returns_the_full_body",
]
tests_last_seen = [
    { file = "src/data/message_cache/notes.rs", tests = 32 },
]

# ── The word beside a note saying what its body is written in ───────────────
#
# Two records rather than one, because the round trip has two halves and each
# has its own edit. `AddressBook` is the shape both follow: a word this code
# does not recognise is still somebody else's answer, and forgetting it
# rewrites their row on the next save without anybody asking for that.
#
# The first plausible break was taken and then a second was measured beside
# it, because `CLAUDE.md` says the first is often the weak one and it was:
# losing the word on the way in reddens one test, losing it on the way out
# reddens two. Both are edits somebody writes while tidying, and neither is
# covered by the other, so both are written down.
#
# Both measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a
# clean tree with nothing else building. 6,736 passed with one red for the
# first and 6,735 passed with two for the second.
# The break here is reading the column and then dropping what it said, which is
# the shape that still reads as consulting it. A break that stopped consulting
# it altogether would be caught by the compiler and would prove nothing.
[[guard]]
name = "a stored form this build did not write survives being read"
file = "src/data/message_cache/notes.rs"
before = """            "plain" => NoteBody::AsTyped,
            other => NoteBody::Other(other.to_string()),"""
after = """            "plain" => NoteBody::AsTyped,
            _ => NoteBody::AsTyped,"""
red = [
    "data::message_cache::notes::tests::test_a_stored_form_this_build_did_not_write_survives_being_written_back",
]
tests_last_seen = [
    { file = "src/data/message_cache/notes.rs", tests = 32 },
]

# The other half. The word is still read correctly and is thrown away on the
# way back to the database, which is the half a read-only test cannot see.
[[guard]]
name = "a stored form this build did not write survives being written back"
file = "src/data/message_cache/notes.rs"
before = """            NoteBody::AsTyped => "plain",
            NoteBody::Other(word) => word,"""
after = """            NoteBody::AsTyped => "plain",
            NoteBody::Other(_) => "plain","""
red = [
    "data::message_cache::notes::tests::test_a_stored_form_this_build_did_not_write_survives_being_written_back",
    "data::message_cache::notes::tests::test_what_this_program_says_a_notes_body_is_written_in_is_one_word",
]
tests_last_seen = [
    { file = "src/data/message_cache/notes.rs", tests = 32 },
]

# ── A menu offering a sync that cannot happen, or hiding one that can ───────
#
# What it costs a person: they arrow onto a note folder, meet "Sync notes now",
# choose it, and nothing happens, because their account has no notes backend
# and never had one. Or the opposite, which is quieter and worse: their notes
# do reach a server and the menu never says so, so the only way to send them is
# to wait and hope. Both are the note editor stub in a different coat, which is
# the failure this project has already shipped once.
#
# The menu used to hold a constant and a comment saying notes go nowhere. Both
# were true and neither could stop being true, because there was nothing to
# ask. `application::notes_backend` is the answer now and this menu asks it.
#
# The break is the two arms swapped, which offers the sync exactly where it
# cannot happen and hides it exactly where it can. Two other breaks were
# considered and rejected. Returning the same list from both arms is caught by
# the compiler before any test runs, because `NOTE_FOLDERS_WITH_SYNC` then has
# no reader and `-D warnings` refuses it, so it would prove nothing. Putting
# the constant back into `entries_for` reddens nothing at all today, because
# every account answers the same, and that is written down here rather than
# left for somebody to rediscover: this record does not cover it.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 7,107 passed
# and exactly two failed. The second is the older absence check, which is kept
# rather than replaced precisely because it asserts against a literal: it goes
# red here while an assertion computed from the seam would not.
[[guard]]
name = "the note folder menu offers a sync exactly where the seam says one exists"
file = "src/application/context_menu.rs"
before = """    match notes.goes_somewhere_else() {
        true => NOTE_FOLDERS_WITH_SYNC,
        false => NOTE_FOLDERS,
    }"""
after = """    match notes.goes_somewhere_else() {
        true => NOTE_FOLDERS,
        false => NOTE_FOLDERS_WITH_SYNC,
    }"""
red = [
    "application::context_menu::tests::test_notes_are_not_offered_a_sync_they_cannot_do",
    "application::context_menu::tests::test_the_note_folder_menu_offers_a_sync_exactly_where_the_seam_says_one_exists",
]
tests_last_seen = [
    { file = "src/application/context_menu.rs", tests = 18 },
]

# ── Two places deciding where one account's notes go ────────────────────────
#
# The other direction, and a different property. The record above holds the
# menu to the seam's answer. This one holds the seam to being the only answer:
# where a new note is filed, what the note folder menu offers, and the sentence
# the settings screen says all read the same function, so none of them can come
# to believe something the others do not.
#
# That is not hypothetical here. `new_item::supports` used to answer for notes
# with a `false` and a comment giving the reason per provider, and the menu
# held a constant with the same reason written out again. Two copies of one
# fact, in two files, is how a menu comes to offer a sync a screen calls
# impossible. `supports` asks here now.
#
# The break is the seam's fallback arm answering that a backend exists, which
# is what a half-finished first backend looks like: `05.1-03` wires CalDAV and
# puts it in the arm that catches everything rather than in the arm for the
# provider that has it. It is the constant rather than the removal on purpose,
# because a removal is caught by the compiler.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 7,104 passed
# and five failed. Three are the seam's own; the fourth is the note folder menu
# reached through `entries_for`, which asks about an account nobody named; and
# the fifth is in `new_item`, which is the one that proves `supports` really
# asks here rather than keeping its own answer.
[[guard]]
name = "where a new note is filed and what its menu offers come from one answer"
file = "src/application/notes_backend.rs"
before = """        // mail server, and a note "in" one would live in a database on this
        // computer while claiming to belong somewhere else.
        _ => NotesBackend::ThisComputer,"""
after = """        // mail server, and a note "in" one would live in a database on this
        // computer while claiming to belong somewhere else.
        _ => NotesBackend::CalDavJournal,"""
red = [
    "application::context_menu::tests::test_notes_are_not_offered_a_sync_they_cannot_do",
    "application::new_item::tests::test_a_container_goes_where_the_things_it_holds_go",
    "application::notes_backend::tests::test_a_default_account_that_is_not_there_is_the_same_as_none",
    "application::notes_backend::tests::test_an_account_whose_provider_this_build_does_not_recognise_keeps_its_notes_here",
    "application::notes_backend::tests::test_an_account_with_no_provider_at_all_keeps_its_notes_here",
]
tests_last_seen = [
    { file = "src/application/context_menu.rs", tests = 18 },
    { file = "src/application/new_item.rs", tests = 42 },
    { file = "src/application/notes_backend.rs", tests = 25 },
]

# ── A sentence that says the same thing whatever the answer was ─────────────
#
# What it costs a person: they open Settings, read that their notes are kept
# on this computer and that this account has no notes backend, and their notes
# have been going to a server for weeks. Or the reverse, which is what ships
# today: the sentence is right by accident because every account answers the
# same, and nothing would notice when one stops.
#
# This is the note editor stub in the shape a sentence can take. That editor
# filled itself with "Note 1" and "(Note content loaded here)" on every
# selection, and it looked finished. A sentence that ignores what it was told
# looks finished in exactly the same way, and reads as an answer rather than as
# a placeholder, which is worse.
#
# The break is the one somebody writes by copying the arm above and forgetting
# to change the words. It is the constant rather than the removal on purpose:
# a function that stopped taking the answer at all would not compile.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 7,114 passed
# and exactly one failed.
#
# Re-measured the same day, because `05.1-03` changed the words this break
# replaces and the per-commit check said so at once: the sentence now says it is
# experimental and that no build has ever sent a note to a real server, which is
# a warning that has to reach somebody before they rely on it. The break is the
# same one, the arm copied from its neighbour, and it now reddens two tests
# rather than one.
[[guard]]
name = "the settings sentence says where the notes really go"
file = "src/application/notes_backend.rs"
before = '''        NotesBackend::CalDavJournal => format!(
            "Notes in {account_named} are kept on this computer and sent to that \
             account's calendar server. This is experimental. No build has ever \
             sent a note to a real server, so expect problems, and turning on \
             Allow Changes is what lets a note go at all."
        ),'''
after = '''        NotesBackend::CalDavJournal => format!(
            "Notes in {account_named} are kept on this computer. This account has no \
             notes backend, so nothing sends them anywhere."
        ),'''
red = [
    "application::notes_backend::tests::test_an_account_whose_notes_reach_a_server_is_told_that_has_never_been_tried",
    "application::notes_backend::tests::test_the_sentence_says_where_the_notes_really_go",
]
tests_last_seen = [
    { file = "src/application/notes_backend.rs", tests = 25 },
]

# ── A sentence only one screen reader hears ─────────────────────────────────
#
# The other half, and a different failure. The one above is the words being
# wrong. This is the words being right and one of the two people who need them
# never hearing them.
#
# Windows has two accessibility channels and this program needs both. A static
# text's own label serves UI Automation, which is what Narrator reads.
# `set_accessible_name` writes the MSAA name, which is what NVDA reads. Drop
# the second line and the sentence is on screen, is read by Narrator, and is
# silent to the screen reader most of this program's users have. Nothing about
# the panel looks wrong.
#
# This project has already shipped that mistake at scale: sixteen widgets were
# "named" with `set_name()`, which sets an internal wxWidgets identifier and
# never reaches the accessibility tree at all. It compiled and passed 324
# tests. Guardrail 2 is written about it.
#
# The break is the `set_accessible_name` line taken out, which is what somebody
# writes when they decide the label is enough.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 7,114 passed
# and exactly one failed. What it cannot see, said here rather than left to be
# assumed: it reads the screen's source. A control built correctly and never
# given focus, or a name a real screen reader announces differently, keeps this
# green. Only a screen reader answers that, and nobody has run one.
[[guard]]
name = "the settings sentence about notes reaches both windows accessibility channels"
file = "src/presentation/wx_settings.rs"
before = """    set_accessible_name(&notes_answer, &where_notes_go);
    notes_sec.add(&notes_answer, 0, SizerFlag::Expand | SizerFlag::All, 4);"""
after = """    notes_sec.add(&notes_answer, 0, SizerFlag::Expand | SizerFlag::All, 4);"""
red = [
    "data::config::every_setting_is_acted_on::test_where_an_accounts_notes_go_is_said_by_a_screen",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
    { file = "src/presentation/wx_settings.rs", tests = 0 },
]

# A change made by the one writer that does not go through `save_note` is still
# sent.
#
# `toggle_note_pin` writes its own `UPDATE`, so an invariant kept only in
# `save_note` is bypassed by it and nothing says so. Pinning is a change
# somebody made and would expect their backend to be told about, and the shape
# of the bug is already in this project's changelog against a task: a move
# changed a row, nothing marked it as waiting, nothing ever pushed it, and the
# status line said the change had been made.
#
# The break is the half-fix rather than a wholesale removal: the flag stays in
# `save_note` and comes out of the one writer that does not use it, which is
# exactly what somebody writes who read `save_note` and stopped there.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 6,769 passed
# in the library and exactly one failed.
#
# A second candidate was considered and rejected: taking the flag out of
# `save_note` instead. That reddens the storage round trip as well, so it
# measures "the flag is written at all" rather than "the writer that bypasses
# the upsert sets it too", which is the thing nothing else would catch.
[[guard]]
name = "a note changed by the writer that bypasses the upsert is still sent"
file = "src/data/message_cache/notes.rs"
before = '''                "UPDATE notes SET pinned = NOT pinned, updated_at = ?1, pending = 1
                 WHERE id = ?2",'''
after = '''                "UPDATE notes SET pinned = NOT pinned, updated_at = ?1 WHERE id = ?2",'''
red = [
    "data::message_cache::notes::tests::test_pinning_a_note_leaves_it_waiting_to_be_sent",
]
tests_last_seen = [
    { file = "src/data/message_cache/notes.rs", tests = 32 },
]

# A copy of a note makes a second note at the backend rather than writing over
# the first.
#
# A note carries what its backend calls it in a column of its own, the way an
# event does, so minting a new identifier is not enough on its own. Kept, the
# push asks the backend to change its copy of the original rather than make a
# second one, and the original is the one that moves. That is data loss at
# somebody's server rather than a wrong row here, which is why the event and
# task arms above this one in `file_under` both carry a comment about it.
#
# The break is what an executor writes who follows a plan that says to add the
# columns and does not mention the copy branch, which is what nearly happened:
# the arm carried a comment saying a note has no provider identity, and that
# comment stopped being true in the same commit as the columns arrived.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. Exactly one
# test failed, in this suite, and the library was untouched.
[[guard]]
name = "a copied note makes a second note at the backend rather than overwriting the first"
file = "src/presentation/managers.rs"
suite = "a_copy_leaves_the_original_where_it_was"
before = '''                note.id = new_id("note");
                note.known_as = None;
                note.known_version = None;'''
after = '''                note.id = new_id("note");'''
red = [
    "test_the_copy_of_a_note_a_backend_holds_makes_no_claim_on_the_backends_own",
]
tests_last_seen = [
    { file = "tests/a_copy_leaves_the_original_where_it_was.rs", tests = 9 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# A note somebody deleted is not written back down by the read that follows.
#
# `application::deletions` states the rule and this follows it: the record of
# the deletion outlives the row, and every read asks the records before it
# writes anything down. Without that a note deleted here comes back on the next
# read, under the backend's own identifier, with nothing left saying it was ever
# deleted, and it reads as the deletion having quietly failed.
#
# The break is the check taken out, which is the edit somebody makes while
# tidying a loop they think is over-defensive. The reorder the plan describes,
# asking the record after the write instead of before, is the same defect and
# could not be written as one contiguous replacement here, because the question
# and the write are at opposite ends of the loop body. Said rather than left as
# a smaller break presented as the plan's.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 6,783 passed
# and exactly one failed.
[[guard]]
name = "a note deleted here is not written back down while the backend still names it"
file = "src/application/notes_sync.rs"
before = '''        if deleted_here.holds(&one.named) {
            continue;
        }
        let ours = here'''
after = '''        let ours = here'''
red = [
    "application::notes_sync::tests::test_a_note_deleted_here_is_not_written_back_down_while_the_backend_still_names_it",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
]

# The losing copy of a note is held for somebody rather than written over.
#
# A backend that says its copy moved first has written nothing and handed the
# question over, which is what the seam's contract requires of it. Doing nothing
# with that answer is not neutral: the note is still waiting, so the read in the
# same sync sees the markers differ, fetches the backend's copy and writes it
# over the change nobody sent. An edit made and lost looks exactly like an edit
# that never saved.
#
# The break used to be exactly that, an arm that takes the answer and does
# nothing, which is what somebody writes who reads `ItMovedFirst` as "nothing to
# do here". **It stopped reddening anything and nothing said so**, which is the
# finding this record now carries rather than the measurement it used to.
#
# Measured by hand on 2026-09-11 while closing ledger 274. Gutted, the arm
# leaves the note waiting, and the read's own hold, added by the record named
# "a change nobody has sent is not written over by the read", then catches
# exactly the same case and holds the two copies anyway. Both named tests stayed
# green. Gutting both arms at once reddens both tests, which is what proves the
# second arm is what was covering for the first.
#
# Nothing about the scoping work of 2026-09-11 caused it: neither the arm nor
# the read's hold condition was touched. The two records were written the same
# day, 2026-09-10, and the second one made the first one's break harmless within
# hours. The count check could not see it, because both records already named a
# tree with 17 tests and the overlap changes no count. Only a run asks this, and
# it was asked today because five tests arrived in this file and the count check
# printed the remedy. That is the second time in this file a record went quiet
# this way, and the reminder-menu record above tells the first.
#
# The break is now the direction the name promises: the arm takes the backend's
# word for it and marks the note as no longer waiting, which is what somebody
# writes who reads `ItMovedFirst` as "the backend won". That clears the flag the
# read's hold is conditioned on, so the read writes the backend's copy over the
# change nobody sent and neither arm can cover for the other.
#
# Measured 2026-09-11 with `cargo test --lib application::notes_sync::` at the
# default eight threads: 20 passed and exactly the two named failed. The second
# one is the load-bearing half of the pair: it says the note is still waiting and
# still says what was typed here after the first sync, which is what stops "it
# was not offered again" passing against a sync that gave the change up.
[[guard]]
name = "a note that moved in two places is held rather than written over"
file = "src/application/notes_sync.rs"
before = '''            Ok(WhatTheBackendSaid::ItMovedFirst { version_now }) => {
                hold_both_copies_of(cache, service, container, &note, version_now, result).await;
            }'''
after = '''            Ok(WhatTheBackendSaid::ItMovedFirst { version_now }) => {
                let _ = version_now;
                let _ = cache.a_backend_took_the_note(
                    &note.id,
                    note.known_as.as_deref().unwrap_or_default(),
                    None,
                );
            }'''
red = [
    "application::notes_sync::tests::test_a_note_held_for_a_choice_is_not_offered_again_on_the_next_sync",
    "application::notes_sync::tests::test_a_note_that_moved_in_both_places_is_held_rather_than_written_over",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
]

# A note's body comes back the bytes it went out as.
#
# The calendar's own reader trims a property's value, which is right for a zone
# name or a status word and wrong for a note: a body is somebody's text and a
# trailing space is part of it. RFC 5545 section 3.1 puts the value immediately
# after the delimiter colon, so there is nothing there to strip that was not in
# the value.
#
# The break is using the calendar's reader, which is what somebody writes who
# notices there is already a routine for this and does not ask what it does to
# the ends of a value. It is how the defect arrived in the first place: the
# first version of this file used `value_named_on` and the trailing-space
# fixture found it.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 6,796 passed
# and exactly two failed. The second is the folding fixture, whose body ends in
# a space for a different reason and catches this too.
[[guard]]
name = "a note's body comes back from a document the bytes it went out as"
file = "src/service/note_document.rs"
# The break moved from the title's line to the body's on 2026-09-15, after
# 08-07's sweep found it reddening nothing on the runners and again here: the
# two tests named are about the body, and no fixture's title ends in a space,
# so trimming the title was a break nothing could see. The title's own
# reading is guarded by nothing, said here rather than left to be assumed.
before = '''        body: inside
            .iter()
            .find_map(|line| the_whole_value_on(line, "DESCRIPTION"))'''
after = '''        body: inside
            .iter()
            .find_map(|line| value_named_on(line, "DESCRIPTION"))'''
red = [
    "service::note_document::tests::test_a_body_long_enough_to_be_broken_across_lines_is_put_back_together",
    "service::note_document::tests::test_a_note_written_as_a_document_comes_back_the_bytes_it_went_in_as",
]
tests_last_seen = [
    { file = "src/service/note_document.rs", tests = 9 },
]

# Changing a note keeps everything in the server's document this program does
# not own.
#
# A whole document written from nothing is the shorter code and throws away
# every property this program has never modelled: categories somebody set in
# another program, the class, the status. It is a change made here destroying
# something at somebody's server, which is worse than a wrong row on this
# computer, and it is silent.
#
# The break leaves the server's own summary and description in the document with
# the new ones written beside them. That failure really happened here, to the
# calendar writer: it read `SUMMARY ` as a different name from `SUMMARY`, left
# the server's title in place and wrote the new one after it, and two titles
# went to somebody's calendar.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 6,796 passed
# and exactly two failed.
#
# No third record was added for matching a property name without regard to case.
# `src/service/note_document.rs` is on `FILES_THAT_READ_OR_WRITE_A_DOCUMENT` in
# `src/service/caldav.rs`, so
# `test_nothing_that_reads_or_writes_a_calendar_document_matches_a_name_by_case`
# already reads this file's source and holds it to that rule, and a second
# record for one rule is the shape `03-09` warned about.
[[guard]]
name = "changing a note keeps what the server has that this program does not own"
file = "src/service/note_document.rs"
before = '''        if inside && is_one_of_ours(line) {
            continue;
        }
        written.push(line.clone());'''
after = '''        written.push(line.clone());'''
red = [
    "service::note_document::tests::test_a_property_the_server_wrote_in_another_case_is_replaced_rather_than_doubled",
    "service::note_document::tests::test_changing_a_note_keeps_what_this_program_does_not_own",
]
tests_last_seen = [
    { file = "src/service/note_document.rs", tests = 9 },
]

# Choosing Sync now on a note folder really runs a sync.
#
# This is the stub shape this project has met before: eight update variants were
# handled in the window and sent by nothing, and five modules rendered empty in
# every build. A handler that reports success and calls nothing looks finished
# from every side, and a test asking only whether the menu offers the command
# passes against it.
#
# The break is exactly that: the arm keeps its status line and stops calling the
# sync. It builds and it lints, because the handle it stops using is used by the
# arms beside it.
#
# The guard reads the window's source, which is what `data::config`'s own screen
# checks do and for the same reason: a window needs a display and a running
# application. What it cannot see, said here rather than assumed: it does not
# press a key, and nobody has heard what comes back.
#
# Measured 2026-09-10 with `cargo test --all-targets --no-fail-fast` on a clean
# tree with nothing else building, at the default eight threads. 6,801 passed in
# the library and exactly one failed.
#
# The line moved from `send_status` to `send_progress` on 2026-09-17 (#38),
# when a sync request became a step; the same break, the call to the sync
# taken out, on the new lines, measured again through `--remeasure`.
[[guard]]
name = "asking for a notes sync reaches the sync rather than reporting one"
file = "src/presentation/wx_app.rs"
before = '''                                PimModule::Notes => {
                                    send_progress(&ui_tx, &runtime, "Notes sync requested...");
                                    spawn_notes_sync(app);
                                }'''
after = '''                                PimModule::Notes => {
                                    send_progress(&ui_tx, &runtime, "Notes sync requested...");
                                }'''
red = [
    "application::notes_backend::tests::test_asking_for_a_notes_sync_reaches_the_sync_rather_than_reporting_one",
]
tests_last_seen = [
    { file = "src/application/notes_backend.rs", tests = 25 },
    { file = "src/presentation/wx_app.rs", tests = 199 },
]

# ── 05.1-04: what a second implementation of the notes seam guards ──────────
#
# All three were taken by hand on 2026-09-10 on a clean tree with nothing else
# building, at eight threads, with `--no-fail-fast`. Each was measured twice
# over: `cargo test --lib` and then the suite.
#
# **The library saw none of them.** 6,802 library tests passed under every one
# of these three breaks. That is the sentence this plan exists to produce: the
# properties below are the ones a single implementation could not guard,
# because with one backend behind a seam there is nothing to disagree with.
#
# Each carries `suite`, so it runs on a commit that changes the source it is
# about rather than only on one that changes the test file.

# Measured 2026-09-10: 6,802 passed in the library, exactly two failed in the
# suite. The push is where a clash is normally found, and the push does not
# always run; the read had no guard at all, so a change the setting held was
# written over inside the same sync that counted it as waiting.
[[guard]]
name = "a change nobody has sent is not written over by the read"
file = "src/application/notes_sync.rs"
suite = "the_notes_seam_takes_a_second_kind_of_backend"
before = '''        if let Some(here) = ours
            && here.pending
            && (here.title != said.title || here.body != said.body)
        {
            hold_these_two_copies(cache, here, container, &said, None, result);
            continue;
        }
        match write_it_down(cache, folder, ours, &said) {'''
after = '''        match write_it_down(cache, folder, ours, &said) {'''
red = [
    "test_the_setting_and_a_document_that_moved_do_not_lose_the_change",
    "test_the_setting_and_a_page_that_moved_do_not_lose_the_change",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "tests/the_notes_seam_takes_a_second_kind_of_backend.rs", tests = 35 },
]

# Measured 2026-09-10: 6,802 passed in the library, exactly two failed in the
# suite. The break is the arm exactly as `05.1-03` wrote it, which is the shape
# somebody reaches for: a name the backend does not know is reported, and the
# change waits here for ever while the same sentence arrives on every sync.
[[guard]]
name = "a note the backend no longer holds is made again rather than reported for ever"
file = "src/application/notes_sync.rs"
suite = "the_notes_seam_takes_a_second_kind_of_backend"
before = '''            Ok(WhatTheBackendSaid::ItIsNotThere) => {
                offer_it_as_a_new_note(cache, service, container, &note, result).await;
            }'''
after = '''            Ok(WhatTheBackendSaid::ItIsNotThere) => result.errors.push(format!(
                "Note {}: the backend no longer holds it, so nothing was sent",
                note.id
            )),'''
red = [
    "test_a_document_the_backend_no_longer_holds_is_made_again",
    "test_a_page_the_backend_no_longer_holds_is_made_again",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "tests/the_notes_seam_takes_a_second_kind_of_backend.rs", tests = 35 },
]

# Measured 2026-09-10: 6,802 passed in the library, exactly nine failed in the
# suite. The break reads a container as an address and keeps its last part,
# which is what "a container is one identifier" looks like when somebody writes
# it down. The five that stayed green are the ones whose work is all on the
# push side, which the break does not touch.
#
# The plan asked for a record whose break is the sync naming a concrete backend
# type. That one cannot be measured: the sync is generic over the trait, so
# naming a type there does not redden a test, it stops the crate compiling, and
# a run has no red list to compare. This is the same property with a break a run
# can see.
[[guard]]
name = "the sync hands a container back exactly as it was given"
file = "src/application/notes_sync.rs"
suite = "the_notes_seam_takes_a_second_kind_of_backend"
before = '''    let there = match service.notes_it_holds(container).await {'''
after = '''    let container = container.rsplit('/').next().unwrap_or(container);
    let there = match service.notes_it_holds(container).await {'''
red = [
    "test_a_backend_that_gives_no_marker_loses_a_change_made_at_the_other_end_and_says_nothing",
    "test_a_change_at_a_section_of_pages_arrives_here",
    "test_a_collection_address_arrives_as_it_was_given",
    "test_a_marker_that_stood_still_while_the_note_moved_hides_the_change_from_the_read",
    "test_a_note_changed_here_reaches_a_section_of_pages",
    "test_a_note_made_here_reaches_a_section_of_pages",
    "test_a_page_only_the_backend_has_arrives_here",
    "test_a_page_the_backend_no_longer_holds_is_made_again",
    "test_a_page_the_service_only_touched_is_not_written_down_again",
    "test_a_three_part_section_arrives_as_it_was_given",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "tests/the_notes_seam_takes_a_second_kind_of_backend.rs", tests = 35 },
]

# Measured 2026-09-10: 6,803 passed in the library, exactly four failed in the
# suite. The break is the call site dropping the answer, which is the shape the
# code had before this plan and the one somebody restores by deleting a line
# that looks like bookkeeping.
#
# **The red list was one short from the day it was written**, corrected
# 2026-09-11 while closing ledger 274.
# `test_a_page_the_service_only_touched_is_not_written_down_again` asserts
# `unchanged` is 1, and under this break the read never happens, so it was
# always going to go red and was never named. Nothing about the scoping work of
# that day caused it: neither the call site nor that test's assertions were
# touched, and the test reaches the break through a path the scoping does not
# cross. Only a run asks this question, and it was asked because five tests
# arrived in this file and the count check printed the remedy.
#
# What it defends is not a count. Without this call the copy here and the copy
# at the backend differ from the moment of the first push with nothing said, and
# the first thing that moves a marker at the backend brings the backend's
# version of somebody's note down over theirs.
[[guard]]
name = "what a backend could not keep is written down rather than reported as taken"
file = "src/application/notes_sync.rs"
suite = "the_notes_seam_takes_a_second_kind_of_backend"
before = '''                    Ok(()) => {
                        result.sent += 1;
                        keep_what_the_backend_could(cache, &note, what_it_could_keep, result);
                    }'''
after = '''                    Ok(()) => {
                        result.sent += 1;
                    }'''
red = [
    "test_a_note_a_section_of_pages_could_not_keep_says_exactly_which_bytes_moved",
    "test_a_page_the_service_only_touched_is_not_written_down_again",
    "test_a_section_of_pages_that_could_not_keep_a_note_says_so_rather_than_reporting_it_took_it",
    "test_the_summary_says_a_backend_could_not_keep_a_note_only_when_one_really_could_not",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "tests/the_notes_seam_takes_a_second_kind_of_backend.rs", tests = 35 },
]

# Measured 2026-09-10: exactly one failed in the library and the suite was
# untouched. The break is the comparison stopping being one on bytes, which is
# the change somebody makes to stop a backend reporting a loss it really has:
# put the two line endings into the same shape first and the loss disappears
# from the answer while going on happening to somebody's note.
#
# **The first candidate break proved nothing and is written down rather than
# discarded.** Trimming both sides before comparing reddened not one test in
# 6,803, because a carriage return inside a body is not at either end and `trim`
# never sees it. A weaker break than the defect is the commonest way a guard
# comes to guard nothing, and this one was found by measuring rather than by
# reasoning about it.
[[guard]]
name = "what a backend kept is compared on bytes and on nothing parsed"
file = "src/service/caldav_journal.rs"
before = '''            Ok(back) if back.title == title && back.body == body => None,'''
after = '''            Ok(back)
                if back.title == title
                    && back.body.replace("\r\n", "\n") == body.replace("\r\n", "\n") =>
            {
                None
            }'''
red = [
    "service::caldav_journal::tests::test_a_body_this_format_cannot_carry_is_said_rather_than_reported_as_taken",
]
tests_last_seen = [
    { file = "src/service/caldav_journal.rs", tests = 6 },
]

# Measured 2026-09-10 on a clean tree with nothing else building, at the default
# eight threads: 6,812 passed and exactly one failed.
#
# The break is the whole file export writing a property of its own beside the
# card the shared writer gave it, which is what a second writer looks like while
# it is being born. Somebody wants one more property in the exported file, the
# loop is where they are standing, and they add it there. From that moment a
# card written to a file and a card sent to a server are two different cards.
#
# **6,812 library tests passed under it, and that is the finding.** Every export
# test in `contacts.rs` asks whether the file contains a line, so an extra
# property is invisible to all of them. Only a test comparing the whole file
# against the per card writer's own answer can see the two coming apart, and
# before this plan there was no per card writer to compare against.
#
# The break deliberately does not change what `vcard_block_from_contact` writes.
# Changing that changes both callers at once, which is the thing this record is
# about not happening, so it reddens tests about `NICKNAME` and says nothing
# about whether the two agree.
[[guard]]
name = "the file export and a card sent to a server are written by one writer"
file = "src/data/message_cache/contacts.rs"
before = '''        for c in contacts {
            output.push_str(&Self::vcard_block_from_contact(&c));
        }'''
after = '''        for c in contacts {
            output.push_str(&Self::vcard_block_from_contact(&c));
            if let Some(ref nick) = c.nickname {
                output.push_str(&Self::fold_vcard_line(&format!(
                    "X-NICKNAME:{}",
                    Self::escape_vcard_text(nick)
                )));
            }
        }'''
red = [
    "service::carddav::tests::test_the_whole_file_export_and_the_one_card_writer_give_the_same_card",
]
tests_last_seen = [
    { file = "src/data/message_cache/contacts.rs", tests = 103 },
    { file = "src/service/carddav.rs", tests = 35 },
]

# Measured 2026-09-10 on a clean tree with nothing else building, at the default
# eight threads: 6,821 passed and exactly one failed.
#
# The break is the resource type test replaced by asking only whether the block
# is a collection at all, which is what a reader written in a hurry does. Every
# folder on the server is then offered as an address book, and the first sync
# writes somebody's contacts into whichever one they picked.
#
# A test that only counts what came back is green against that break and also
# green against a reader that returns nothing, which is why the test this record
# names holds one address book and one calendar in the same answer and asserts
# both the count and which one came back.
[[guard]]
name = "a folder that is not an address book is not offered as one"
file = "src/service/carddav.rs"
before = '''        if !names_an_address_book(block) {
            continue;
        }'''
after = '''        if !block.contains("collection") {
            continue;
        }'''
red = [
    "service::carddav::tests::test_a_collection_that_is_not_an_address_book_is_left_out",
]
tests_last_seen = [
    { file = "src/service/carddav.rs", tests = 35 },
]

# Measured 2026-09-10 on a clean tree with nothing else building, at the default
# eight threads: 6,832 passed and exactly one failed.
#
# The break is a line joining step added here, dropping the lines the card
# format broke in two instead of leaving them to the reader that knows what a
# fold is. That is the second card reader being born: nobody sits down to write
# one, somebody adds a little card handling at the place they are standing, and
# from then on a card read from a file and a card read from a server mean
# different things.
#
# **The break the plan asked for was replaced, and the reason is worth keeping.**
# It asked for a property read inline here. Reading one property inline does not
# change anything a test can see, because the shared reader has already filled
# that field with the same text; to make it visible the inline read has to also
# be wrong, which means writing a whole second reader rather than making the one
# edit somebody really makes. This break is one edit and it is wrong in the way
# the real thing is wrong.
#
# The first candidate was joining the folded lines here rather than dropping
# them. It reddened nothing, because joining them correctly is what the reader
# does anyway, so the result was the same card by a longer road.
[[guard]]
name = "a card from a server is read by the reader a file import uses, not here"
file = "src/service/carddav.rs"
before = '''        let Some(card) = the_card_in(block) else {
            continue;
        };'''
after = '''        let Some(card) = the_card_in(block) else {
            continue;
        };
        let card = card
            .lines()
            .filter(|line| !line.starts_with(' '))
            .collect::<Vec<_>>()
            .join("\n");'''
red = [
    "service::carddav::tests::test_a_cards_text_goes_through_the_reader_a_file_import_uses",
]
tests_last_seen = [
    { file = "src/service/carddav.rs", tests = 35 },
]

# ── Two address books that are one address book ─────────────────────────────
#
# Every question the contact merge asks is keyed on the word
# `AddressBook::as_stored` returns: `this_address_book_is_still_owed_the_change`
# compares it, and so do `ContactEntry::id_in`, `told` and `no_longer_in`. Two
# CardDAV address books filed under one word are therefore one address book to
# all of them. A change waiting to go to the first reads as waiting for the
# second, and a contact in both carries one identity where it needs two.
#
# The break is what `05-RESEARCH.md` told whoever read it to write: "no schema
# change is needed, `AddressBook::Other("carddav")` already round-trips". It is
# right about naming one address book on a contact and it is what makes two
# collapse into one. It is one edit, in the one function whose whole job is the
# decision, and it is the edit somebody makes while simplifying a word that
# looks needlessly long.
#
# Measured by hand on 2026-09-10 against a green tree, with
# `cargo test --lib --no-fail-fast` at the default eight threads: 6,845 passed
# and exactly two failed.
[[guard]]
name = "two address books on one account that are one address book to the merge"
file = "src/data/message_cache/address_books.rs"
before = '''        AddressBook::Other(format!("carddav-{}", self.id))'''
after = '''        AddressBook::Other("carddav".to_string())'''
red = [
    "data::message_cache::address_books::tests::test_two_address_books_on_one_account_are_two_address_books",
    "data::message_cache::address_books::tests::test_the_word_an_address_books_contacts_are_filed_under_survives_the_database",
]
tests_last_seen = [
    { file = "src/data/message_cache/address_books.rs", tests = 11 },
]

# ── An address book's copy written over a change somebody made here ─────────
#
# The losing side is held, not written over. 03-09 is the plan that established
# that for contacts, and what it replaced is exactly this break: the address
# book's copy went straight over the edit, and somebody was told afterwards
# which of their words had gone.
#
# The break is deleting the arm that holds both copies, which is the edit
# somebody makes while simplifying a branch that looks redundant: the line above
# it already decides that the address book wins, so holding reads as an extra
# step on the way to the same write.
#
# Two go red and both are worth having. The one about holding is the behaviour;
# the one about deciding nothing is the shape, and it reddens because the call
# to `hold_both_copies_of` is one of the two calls it reads for. That pairing is
# deliberate: a break that removed the call and kept the words would still be
# caught.
#
# Measured by hand on 2026-09-10 against a green tree, with
# `cargo test --lib --no-fail-fast` at the default eight threads: 6,864 passed
# and exactly two failed.
[[guard]]
name = "an address book's copy written over a contact somebody edited here"
file = "src/application/carddav_sync.rs"
before = '''    if answer == WhoseCopyWins::TakeTheAddressBooksOverAChangeMadeHere
        && contacts_sync::the_copy_here_was_written_here(local)
    {
        return contacts_sync::hold_both_copies_of(
            cache,
            local,
            &card.contact,
            filed_under,
            card.version.as_deref(),
            result,
        );
    }
'''
after = ''''''
red = [
    "application::carddav_sync::tests::test_a_contact_changed_in_both_places_is_held_for_somebody_to_choose",
    "application::carddav_sync::tests::test_nothing_here_decides_a_winner_of_its_own",
]
tests_last_seen = [
    { file = "src/application/carddav_sync.rs", tests = 14 },
]

# ── A second answer to which copy of a contact wins ─────────────────────────
#
# The CardDAV sync feeds the merge contacts already have; it does not fork it.
# Two answers to one question is the defect this project paid for in the
# calendar, where a reader and the writer beside it disagreed.
#
# The break is deciding a winner inline from the two markers, which is what
# somebody writes who has just read `whose_copy_wins` and thinks the call is a
# long way round to a comparison they can make here. It looks right: for a
# contact nobody edited here it gives the same answer. What it drops is every
# question about work waiting on this computer, which is the whole of what the
# shared decision is for.
#
# Measured by hand on 2026-09-10 against a green tree, with
# `cargo test --lib --no-fail-fast` at the default eight threads: 6,864 passed
# and exactly two failed. The same two as the record above, and that is not a
# duplicate: this break leaves the holding arm in place and the holding arm
# never runs, because the answer it tests for is never produced.
[[guard]]
name = "a second answer to whose copy of a contact wins, written in the CardDAV sync"
file = "src/application/carddav_sync.rs"
before = '''    let answer = contacts_sync::keep_a_change_this_sync_could_not_send(
        contacts_sync::whose_copy_wins(
            contacts_sync::the_copy_here_holds_work_nobody_has_sent(local),
            card.version.as_deref(),
            last_seen.as_deref(),
        ),
        local,
        result,
        still_built_on_an_old_copy,
    );'''
after = '''    let answer = match card.version.as_deref() == last_seen.as_deref() {
        true => WhoseCopyWins::NeitherCopyMoved,
        false => WhoseCopyWins::TakeTheAddressBooks,
    };'''
red = [
    "application::carddav_sync::tests::test_a_contact_changed_in_both_places_is_held_for_somebody_to_choose",
    "application::carddav_sync::tests::test_nothing_here_decides_a_winner_of_its_own",
]
tests_last_seen = [
    { file = "src/application/carddav_sync.rs", tests = 14 },
]

# ── An address book added by a screen that never asked a server ─────────────
#
# The stub shape this project has met before, and the one its first guardrail
# is about: the handler writes the row, says "added", and nothing ever asks the
# server what is there. Everything reads as working until the first sync goes
# to an address nobody checked, or finds nothing at an address that was never
# an address book. Five modules once rendered empty in every build for want of
# exactly this.
#
# The break is the asking replaced by an answer made out of what was typed,
# which is what somebody writes while making the screen easier to try by hand.
# It keeps the waiting window, the timeout and the thread, so every other
# assertion about this path stays green: only the question of whether anything
# was asked goes red.
#
# Measured by hand on 2026-09-11 against a green tree, with
# `cargo test --lib --no-fail-fast` at the default eight threads: 6,881 passed
# and exactly one failed.
[[guard]]
name = "an address book added by a screen that never asked a server what was there"
file = "src/presentation/managers.rs"
before = '''        let asking = address_book_source::what_a_server_has(&address, &user_name, &password);'''
after = '''        let asking = std::future::ready(Ok::<_, String>(vec![address_book_source::Offer {
            name: address.clone(),
            address: address.clone(),
        }]));
        let _ = (&user_name, &password);'''
red = [
    "application::address_book_source::tests::test_adding_an_address_book_really_goes_and_looks",
]
tests_last_seen = [
    { file = "src/application/address_book_source.rs", tests = 16 },
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── A page sent with elements OneNote has never named ───────────────────────
#
# What OneNote accepts in a page's body is a published list, and `ammonia`'s
# default allowlist is a browser's idea of safe HTML rather than that list. It
# holds `pre`, `code`, `blockquote` and `hr`, none of which the reference
# names. Sending one is not refused and not reported: the service does
# something with it that is written down nowhere, and what somebody's note
# looks like afterwards is unknown rather than measured.
#
# The break is a widening rather than a deletion, because widening is what
# somebody does to make a construct survive. Two names are added to the list of
# elements this program sends, and everything else about the file stays: both
# conversions still run, the round trip still holds, the sanitising still
# removes a script. Only the question of whether the page was checked against
# the published set goes red.
#
# One test of 6,906 reddens, and that is the guard rather than a shortfall in
# it. One test reads a produced page against the reference's own list, and it
# is the only place that comparison is made. Measured by hand on 2026-09-11
# against a green tree with `cargo test --lib --no-fail-fast` at the default
# eight threads: 6,905 passed and exactly one failed, in 63.86 seconds.
[[guard]]
name = "a page is checked against the elements OneNote names"
file = "src/service/onenote_page.rs"
before = '''const ELEMENTS_THIS_PROGRAM_SENDS: [&str; 28] = [
    "div", "p", "h1", "h2", "h3", "h4", "h5", "h6", "ol", "ul", "li", "table", "tr", "td", "span",
    "br", "a", "img", "b", "i", "u", "em", "strong", "strike", "sup", "sub", "del", "cite",
];'''
after = '''const ELEMENTS_THIS_PROGRAM_SENDS: [&str; 30] = [
    "div", "p", "h1", "h2", "h3", "h4", "h5", "h6", "ol", "ul", "li", "table", "tr", "td", "span",
    "br", "a", "img", "b", "i", "u", "em", "strong", "strike", "sup", "sub", "del", "cite", "pre",
    "code",
];'''
#
# Re-measured on 2026-09-11, later the same day, when the fidelity measurement
# added 24 tests to the file this record names. The red list is unchanged at one
# test, measured rather than assumed: none of the new tests reads a produced
# page's elements, so widening the list leaves all of them green. 6,929 passed
# and one failed.
red = [
    "service::onenote_page::tests::test_the_page_holds_nothing_the_reference_does_not_name",
]
tests_last_seen = [
    { file = "src/service/onenote_page.rs", tests = 48 },
]

# ── A page's body read without being taken out of its divs ──────────────────
#
# The finding that makes the fidelity measurement about OneNote rather than
# about this program's own reader. `long_text::from_markup`'s block pass has one
# arm for `p` and `div` together, so a `div` is read as a single paragraph and
# every heading, list and blank line inside it is concatenated into one run of
# text. OneNote wraps all body content in at least one `div`, so a page handed
# over unflattened comes back as one paragraph and a fidelity table measured
# through that path reports every construct as lost and blames Microsoft for it.
#
# The break is the flattening replaced by the ordinary clean that was there
# before anybody noticed, which is what somebody writes who reads the
# sanitising as the point of this function and the tag removal as tidying.
#
# Measured by hand on 2026-09-11 against a green tree, with
# `cargo test --lib --no-fail-fast` at the default eight threads: 6,919 passed
# and 11 failed, in 62.08 seconds.
#
# The break this plan's own text asked for was measured first and reddens
# nothing. It was to compare a round trip on parsed structures rather than on
# bytes, which is the change somebody makes to turn a red table green. It
# cannot redden anything while the table records what really happens: a looser
# comparison passes a true row as readily as a strict one. 6,930 passed and
# none failed. Written down rather than left out, because a candidate break
# that reddens nothing is a finding about the break.
#
# ── And the record itself retired on 2026-09-22, by 12-02.1 ─────────────────
#
# Run 35520204784 found this record's break reddening nothing on a runner, and
# a measurement here on 2026-09-22 found the same: all 11 of 11 named tests
# stayed green with the guard broken, rebuild 45 s and run 50 s. So the guard
# had no bite on either machine.
#
# Why. `d470a105`, 11-11.0's newsletter work for #90, split the shared `p` and
# `div` arm the paragraph above describes, and a `div` now goes to
# `blocks_or_a_paragraph`, which walks a container holding a block child as
# blocks. Every shape a OneNote page has is such a container, so the stripping
# here no longer decides anything those 11 tests read.
#
# Measured rather than read off that, by putting each shape through
# `long_text::from_markup_to_edit` twice, once with the divs stripped and once
# with them kept:
#
#     <div><h1>H</h1><p>p</p></div>      both "# H\n\np"
#     <div>words <p>a para</p></div>     both "words\n\na para"
#     <div><div><h1>H</h1></div></div>   both "# H"
#     <div>only words</div>              both "only words"
#     <div>a <b>b</b> c</div>            stripped "a\n\nb\n\nc", kept "a b c"
#     <div>a <a href="https://x">link</a> here</div>
#                                        stripped "a\n\nlink\n\nhere",
#                                        kept "a [link](https://x) here"
#
# So the stripping is redundant wherever a div holds a block, and where a div
# holds only a line of words it now cuts that line at every inline element and
# takes a link's address away with it. That is a loss on a round trip through
# the editor, which is the one thing `from_markup_to_edit` exists to prevent.
#
# No break to this function can have bite while that is true, because the only
# behaviour left to guard is the loss, and a test asserting it would write the
# defect down as the rule. So the record is retired rather than rewritten, and
# `.planning/WINDOWS.md` carries what the reading found, with the shapes above,
# for whoever decides whether the stripping comes out.

# A note a backend holds is copied into the new folder and the old container is
# owed a removal, rather than having its row changed in place.
#
# The break is the routing branch taken out, which is the shape the note arm had
# before 2026-09-11 and the one somebody restores by deleting a condition that
# reads like a special case. With it gone the row keeps the name the backend
# gave it in the container the note is leaving, so the next push asks the
# backend to change that copy rather than make one in the new container. That is
# somebody's note lost at their server, and it is the thing this whole
# arrangement is arranged around.
#
# Two records with one break, because it reddens two suites and a record runs
# one cargo target. This one is the new suite; the one below it is the older
# file whose note assertions changed with the behaviour.
#
# Measured 2026-09-11 on a tree with nothing else building, at the default eight
# threads.
[[guard]]
name = "a note a backend holds is copied into the new folder rather than moved in place"
file = "src/presentation/managers.rs"
suite = "a_note_moves_between_folders"
before = '''            if filing.needs_the_holder_told() && note.known_as.is_some() {
                return move_what_a_backend_holds(cache, &note, into);
            }
            note.folder_id = Some(into.to_string());'''
after = '''            note.folder_id = Some(into.to_string());'''
red = [
    "test_a_note_a_backend_holds_is_created_in_the_new_folder_before_the_old_one_is_removed",
    "test_a_note_moved_into_a_folder_on_this_computer_reaches_no_backend",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "tests/a_note_moves_between_folders.rs", tests = 5 },
]

# The same break, measured against the older suite. Its note assertions were
# right under the arrangement where an account had one container and are about
# this one now.
[[guard]]
name = "a note moved between folders gives up the name the old container knew it by"
file = "src/presentation/managers.rs"
suite = "a_copy_leaves_the_original_where_it_was"
before = '''            if filing.needs_the_holder_told() && note.known_as.is_some() {
                return move_what_a_backend_holds(cache, &note, into);
            }
            note.folder_id = Some(into.to_string());'''
after = '''            note.folder_id = Some(into.to_string());'''
red = [
    "test_a_note_moved_to_another_folder_is_left_waiting_to_be_sent",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "tests/a_copy_leaves_the_original_where_it_was.rs", tests = 9 },
]

# A OneNote write goes out through the transport the client already holds, so
# the Allow Changes gate is applied to it.
#
# The break is what somebody writes who wants a different timeout, or who adds
# the next OneNote call from memory: a client built where the request is sent.
# It compiles, it would work against a real server, and it takes the account's
# Allow Changes answer out of the path entirely. The gate is applied where the
# client is built, so a client built somewhere else is the whole of how it stops
# applying.
#
# What stayed green matters as much as what went red. `outward.rs`'s write
# census is the check somebody would expect to catch this, and it does not. It
# recognises a write by the gate marker inside the method, so taking the marker
# out takes the method off the census rather than failing it, and the
# `MEASURED_ON_THE_WIRE` row only asks that the method still exists and that
# some test asserts its request line, both still true. The one test below is
# therefore the whole of the evidence that this gate applies.
#
# Measured 2026-09-11 by hand on a clean tree with nothing else building, at the
# default eight threads: 6,980 passed, 1 failed, 69.99 seconds. The red test was
# new with that change and was the only place the assertion was made.
#
# Re-measured 2026-09-12, when `05.2-03` put a notes backend on top of this
# client and gave it a test of its own asking the same question one layer up.
# The break now reddens two, and the second one is the finding: adding a caller
# to a gated method widens what this guard covers, and no count of tests per
# file can see that. It was found by the remeasure refusing the record rather
# than by anybody thinking to look.
[[guard]]
name = "a onenote page is made through the gated transport rather than a client of its own"
file = "src/service/microsoft_graph.rs"
before = '''        let resp = self
            .http
            .changing(reqwest::Method::POST, &url, "add a note to this account")?
            .bearer_auth(token)'''
after = '''        let resp = reqwest::Client::new()
            .post(&url)
            .bearer_auth(token)'''
red = [
    "service::microsoft_graph::tests::test_an_account_that_may_only_be_read_makes_no_onenote_page",
    "service::onenote_notes::tests::test_an_account_that_may_only_be_read_makes_no_page_and_says_why",
]
tests_last_seen = [
    { file = "src/service/microsoft_graph.rs", tests = 57 },
    { file = "src/service/onenote_notes.rs", tests = 13 },
]

# A walk of a notebook stops going deeper into section groups, and says it
# stopped.
#
# The break raises the bound by one, which is what somebody writes who reads the
# comparison as off by one. The walk then asks about a group the fixture has no
# answer for, so the test fails on the request rather than on the sentence about
# the bound, and the message is about a socket. That is worth knowing before
# somebody reads a future failure of this record as a network problem. It is
# also the fixture doing something the code stopped doing: a fixed number of
# canned answers bounds the walk even when the walk has no bound of its own,
# which is why this fails fast instead of running until it is killed.
#
# A second candidate was measured rather than assumed, because the first
# plausible break is often the weak one. Taking out the `whole = false` leaves
# the bound in place and the read claiming to be whole, which is the other half
# of this rule. It reddens exactly the same one test, on the assertion that
# names the rule: 6,987 passed, 1 failed, 66.25 seconds. So that single test
# covers both halves, which is the finding, and the record keeps the break
# against the bound because the bound is what holds a hostile answer off.
#
# Measured 2026-09-11 by hand on a clean tree with nothing else building, at the
# default eight threads: 6,987 passed, 1 failed, 65.54 seconds. The red test is
# new with this change.
[[guard]]
name = "a walk of a notebook stops at the bound and says it stopped"
file = "src/service/microsoft_graph.rs"
before = '''                if groups_deep == MOST_SECTION_GROUPS_DEEP {'''
after = '''                if groups_deep == MOST_SECTION_GROUPS_DEEP + 1 {'''
red = [
    "service::microsoft_graph::tests::test_a_section_group_nested_past_the_bound_is_not_followed_and_the_read_says_so",
]
tests_last_seen = [
    { file = "src/service/microsoft_graph.rs", tests = 57 },
]

# A change to a page is built from a read of that page taken immediately before
# it, and from nothing else.
#
# The break builds the commands from the page this change is about to send
# instead of from the page as it stands. That is the caching defect in its
# plainest form: a page from some other moment, used to name things Graph has
# generated names for since. The reference says those names "might change after
# a page update", so a client working from any read but the last one names
# things the page no longer answers to, and a fake with fixed identifiers can
# never show it. The fixture moves them between reads for that reason.
#
# It is also exactly what somebody writes who thinks they know what is on the
# page because they just put it there, which is why it is this break rather than
# a contrived cache.
#
# Three tests red rather than one, and each is a different half. The second
# change names nothing the second read said, the read before the write does not
# happen at all, and the read-only client's test loses the read it waits for.
# All three are new with this change.
#
# Measured 2026-09-11 by hand on a clean tree with nothing else building, at the
# default eight threads: 6,993 passed, 3 failed, 57.65 seconds.
[[guard]]
name = "a change to a page is built from the read immediately before it"
file = "src/service/microsoft_graph.rs"
before = '''        let there_now = self.page_content_with_identifiers(token, page_id).await?;'''
after = '''        let there_now = crate::service::onenote_page::the_page_for(note);'''
red = [
    "service::microsoft_graph::tests::test_a_change_reads_the_page_before_it_writes",
    "service::microsoft_graph::tests::test_a_second_change_names_what_the_second_read_said_and_not_the_first",
    "service::microsoft_graph::tests::test_an_account_that_may_only_be_read_changes_no_page",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-11, the same day as
    # this record, in a file it had never named.
    "service::onenote_notes::tests::test_the_marker_handed_back_after_a_change_is_the_one_the_page_carries_afterwards",
]
tests_last_seen = [
    { file = "src/service/microsoft_graph.rs", tests = 57 },
    { file = "src/service/onenote_notes.rs", tests = 13 },
]

# What a backend could not keep is read off its own copy, not worked out from
# what this program believes that backend does.
#
# Requirement 2 of the seam's section on bytes, and the break is the saving
# anybody would reach for: the pure mapping in `onenote_page` already says what
# a page does to a note, so putting the note through it costs nothing and saves
# a request. What it costs instead is the only thing in this backend that could
# ever notice Microsoft behaving differently from a model read off three
# documentation pages. A backend answering from a rule about itself goes on
# claiming a byte survived after the day it stops surviving.
#
# Three tests red and only one of them is about the rule. The other two starve:
# the break takes a request away, and their fixtures wait for one that never
# comes, so they fail on a socket rather than on an assertion. Written down as
# measured rather than trimmed to the one that was predicted.
#
# A second candidate was measured, because the first plausible break is often
# the weak one. Answering "kept it all" whenever the title survived is the other
# shape of the same mistake, and it reddens exactly one test, the one about the
# rule: 7,010 passed, 1 failed, 68.01 seconds. The break kept here is the wider
# one, because what it defends is the read happening at all.
#
# Measured 2026-09-12 by hand on a clean tree with nothing else building, at the
# default eight threads: 7,008 passed, 3 failed, 64.59 seconds. All three tests
# were new with that change.
#
# Re-measured the same day, after the conflict work added a fourth test to this
# file. It starves the same way the other two do, which is the second time in
# one plan that a record widened because a test was added near what it guards
# rather than because anything about the rule changed. Four now.
[[guard]]
name = "what a onenote page could not keep is read off the page rather than off a model of it"
file = "src/service/onenote_notes.rs"
before = '''        let there = the_note_on(&self.client.page_content(&self.token, page_id).await?);'''
after = '''        let there = the_note_on(&the_page_for(asked));'''
red = [
    "service::onenote_notes::tests::test_a_note_made_here_reaches_onenote_as_a_page_in_the_section_it_was_given",
    "service::onenote_notes::tests::test_a_note_reaches_onenote_through_the_sync_that_names_no_backend",
    "service::onenote_notes::tests::test_the_marker_handed_back_after_a_change_is_the_one_the_page_carries_afterwards",
    "service::onenote_notes::tests::test_what_onenote_could_not_keep_is_read_back_off_the_page_rather_than_worked_out",
]
tests_last_seen = [
    { file = "src/service/onenote_notes.rs", tests = 13 },
]

# A clash is two copies that disagree, and a marker that moved is not that.
#
# The break compares the markers instead of the words, which is what the backend
# below already did to decide there was a clash at all: the whole point of this
# rule is that the marker's answer is not the question. It is what somebody
# writes who reads "the backend says its copy moved first" as "the copies
# differ", and for an entity tag the two really are the same sentence. For a
# clock the service owns they are not, and OneNote has only a clock.
#
# What it costs when it is wrong is a question with one answer given twice: a
# title and a body read aloud, identical on both sides, and two buttons that do
# the same thing. Somebody who fixed the same typo in both places meets it.
#
# Measured 2026-09-12 by hand on a clean tree with nothing else building, at the
# default eight threads: the library untouched at 7,014 passed, and exactly 2
# failed in the suite, 65.84 seconds. Both tests are new with this change and
# both backends fail it, so this was never a second backend's problem.
[[guard]]
name = "two copies of a note that say the same thing are not held for anybody to choose"
file = "src/application/notes_sync.rs"
suite = "the_notes_seam_takes_a_second_kind_of_backend"
before = '''    if said.title == note.title && said.body == note.body {'''
after = '''    if said.known_as.version == note.known_version {'''
red = [
    "test_a_document_changed_to_what_the_server_already_says_is_not_a_question",
    "test_a_page_changed_to_what_onenote_already_says_is_not_a_question",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "tests/the_notes_seam_takes_a_second_kind_of_backend.rs", tests = 35 },
]

# A note is its title as well as its body, and comparing one of them is not
# comparing the note.
#
# The other half of the record above, and it earns a record of its own because
# it reddens different tests. The break compares the bodies alone, which is what
# somebody writes who is thinking about the body because the body is where the
# words are. A note renamed in two places then has one of its names thrown away
# with nothing said.
#
# **This break reddened nothing when it was first measured, and that was the
# finding rather than a reason to drop it.** Every test in this repository
# passed with the comparison weakened to the body, so the rule was stronger than
# anything asserting it. The two tests below were written for that gap and the
# break was measured again against them.
#
# Measured 2026-09-12 by hand on a clean tree with nothing else building, at the
# default eight threads: nothing at all failed before the two tests existed, and
# afterwards the library was untouched at 7,014 passed with exactly 2 failed in
# the suite, 67.74 seconds.
[[guard]]
name = "a note renamed at both ends is still two copies that disagree"
file = "src/application/notes_sync.rs"
suite = "the_notes_seam_takes_a_second_kind_of_backend"
before = '''    if said.title == note.title && said.body == note.body {'''
after = '''    if said.body == note.body {'''
red = [
    "test_a_document_renamed_at_both_ends_is_still_a_question",
    "test_a_page_renamed_at_both_ends_is_still_a_question",
]
tests_last_seen = [
    { file = "src/application/notes_sync.rs", tests = 23 },
    { file = "tests/the_notes_seam_takes_a_second_kind_of_backend.rs", tests = 35 },
]

# A consumer Gmail account's notes stay on this computer, and nothing widens
# that by looking at the wrong side of the question.
#
# The break sends them to OneNote, which is the over-widening anybody makes
# once one provider arm changes and the two arms sit next to each other. It
# promises a sync that cannot happen: Google Keep's API is Workspace only, so a
# consumer account cannot use it at all, and OneNote has no idea who this
# person is. What somebody would see is a Sync now they can press, a settings
# screen saying their notes go to OneNote, and nothing ever arriving.
#
# Five tests red, and the spread is the point: the seam's own answer, the
# sentence the settings screen says, where a new note is filed, whether a sync
# is asked for at all, and the "no notes backend" sentence that has to go on
# being true after all three backends ship. That last one is why this arm is
# not a placeholder waiting to be filled in.
#
# Measured 2026-09-12 by hand on a clean tree with nothing else building, at the
# default eight threads: 7,012 passed, 5 failed, 69.27 seconds. Two of the five
# are new with this change and three were already there, which is what a guard
# on an existing promise should look like.
[[guard]]
name = "a consumer gmail account's notes stay on this computer after all three backends ship"
file = "src/application/notes_backend.rs"
before = '''        Some("gmail") => NotesBackend::ThisComputer,'''
after = '''        Some("gmail") => NotesBackend::OneNote,'''
red = [
    "application::new_item::tests::test_a_note_is_filed_where_that_providers_answer_says_and_a_reminder_never_leaves",
    "application::new_item::tests::test_a_note_or_reminder_is_local_even_on_a_gmail_default",
    "application::notes_backend::tests::test_a_gmail_accounts_notes_stay_on_this_computer",
    "application::notes_backend::tests::test_an_account_whose_notes_stay_here_is_never_asked_for_a_backend",
    "application::notes_backend::tests::test_an_account_with_no_notes_backend_is_told_so_rather_than_told_not_yet",
]
tests_last_seen = [
    { file = "src/application/new_item.rs", tests = 42 },
    { file = "src/application/notes_backend.rs", tests = 25 },
]

# The half-fix rather than the absent one, which is the break this rule is
# really about. An icon shipped to one folder and named in another satisfies
# every presence check anybody would write: the [Files] section installs an icon
# and both [Icons] entries name one. Windows then falls back in silence, and
# nobody finds out until somebody looks at a real Start Menu.
#
# `file` is not a Rust file, so the count check skips the count for it and takes
# the counts off the red list instead, which with `suite` set resolves to
# tests/installer.rs. Checked in tests/house_style.rs at the line that pushes
# the guarded file only when it ends `.rs`, rather than assumed.
#
# Measured 2026-09-12 by hand on the final tree, `cargo test --all-targets
# --no-fail-fast` at the default eight threads: 7,436 passed and exactly these
# two failed. Both are new with this change, so this break is measuring this
# guard rather than an older test catching it by accident.
[[guard]]
name = "the icon a shortcut names is the one the installer really put there"
file = "installer/Wixen-Mail-Setup.iss"
suite = "installer"
before = '''Source: "..\assets\icon.ico"; DestDir: "{app}"; Flags: ignoreversion'''
after = '''Source: "..\assets\icon.ico"; DestDir: "{app}\assets"; Flags: ignoreversion'''
red = [
    "test_apps_and_features_shows_the_icon_the_installer_ships",
    "test_the_icon_each_shortcut_names_is_the_one_the_installer_ships",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# ── 07-03 ─────────────────────────────────────────────────────────────────
#
# The half-fix rather than the absent one. The disclosure still appears and
# still names the announcements half; where the accessible-names sentence
# belongs it repeats the opening line instead. That is the failure premise 3 of
# 07-03 names: a disclosure derived only from the old `NativeBridgeStatus`
# would have said that announcements do not reach a screen reader and said
# nothing about every control in the window reaching the accessibility tree
# with no name, which is the larger of the two failures.
#
# Measured 2026-09-12 by hand on the final tree, after the version bump and the
# changelog were in place, with `cargo test --all-targets --no-fail-fast` at the
# default eight threads: 7,029 passed in the library and exactly these two
# failed, with nothing else red across 7,444 passing tests. Both are new with
# this change, so this break measures this guard rather than an older test
# catching it by accident.
#
# Two tests in the same file stay green under it and that is correct rather
# than a short list. `test_a_build_with_both_halves_of_the_bridge_has_nothing_to_disclose`
# asks about the pair (true, true), which this break does not reach, and
# `test_the_about_dialog_asks_this_build_what_it_does_not_do` reads wx_app.rs
# rather than the sentences.
[[guard]]
name = "the disclosure names both halves of the accessibility bridge"
file = "src/presentation/accessibility/platform_bridge.rs"
before = '        said.push(CONTROLS_WITH_NO_VISIBLE_LABEL_HAVE_NO_NAME);'
after = '        said.push(SOME_OF_IT_DOES_NOT_WORK);'
red = [
    "presentation::accessibility::platform_bridge::tests::test_a_build_with_no_bridge_names_both_halves",
    "presentation::accessibility::platform_bridge::tests::test_a_build_that_only_cannot_name_a_control_says_only_that",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/platform_bridge.rs", tests = 9 },
]

# The disclosure appearing where there is nothing to disclose, which is the
# threat this one is really about. A warning that is wrong every time it appears
# teaches people to ignore warnings, and this one would appear on every start of
# every Windows build, where both halves of the bridge work.
#
# The break makes the answer unconditional rather than deleting the call, which
# is what the failure really looks like: somebody who believes the answer can
# come back empty and supplies one. The reading that refuses it lists `.or(`,
# `.or_else(` and the `.unwrap_or` family, and a fixture drives each of them, so
# the guard and the reading cannot drift apart.
#
# Measured 2026-09-12 by hand on the final tree, after the version bump and the
# changelog were in place, with `cargo test --all-targets --no-fail-fast` at the
# default eight threads: 7,031 passed in the library and exactly this one
# failed, with nothing else red across 7,446 passing tests. It is new with this
# change, so this break measures this guard rather than an older test catching
# it by accident.
#
# Taken twice. The first run also reddened
# `test_every_guard_record_says_how_many_tests_the_files_it_names_held`, from
# the record above needing its count corrected rather than from the break. That
# correction was run first and the break measured again on the clean tree, so
# the one name here is the whole of what it does.
[[guard]]
name = "the startup line says nothing on a build whose accessibility bridge is whole"
file = "src/main.rs"
before = '    if let Some(missing) = platform_bridge::what_this_build_does_not_do() {'
after = '    if let Some(missing) = platform_bridge::what_this_build_does_not_do().or_else(|| Some("Some of what Wixen Mail tells a screen reader does not work in this build.".to_string())) {'
red = [
    "presentation::accessibility::platform_bridge::tests::test_starting_the_program_says_it_before_anybody_has_opened_a_menu",
]
tests_last_seen = [
    { file = "src/main.rs", tests = 0 },
    { file = "src/presentation/accessibility/platform_bridge.rs", tests = 9 },
]

# ── 07-04 ─────────────────────────────────────────────────────────────────
#
# The half-fix rather than the absent one. The split on the plus is still
# there and the numbers still decide first; what the break adds is the whole
# string as a tiebreaker, so two builds of one version stop being one version.
# That is what somebody reaching for "but these two strings are different"
# writes, and it is the shape that matters: it tells everybody running
# 0.5.0+g64c73dd that 0.5.0 is a different version from the one they have.
#
# Deleting the split was the other candidate and it was rejected on what it
# proves. Without the split nothing parses at all, so the answer becomes
# "could not read" and the record would be measuring whether a function is
# called rather than whether the rule it holds is right.
#
# Measured 2026-09-12 by hand on the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads.
# Both tests named are new with this change, so this break measures this guard
# rather than an older test catching it by accident.
#
# Three tests in the same file stay green under it and that is correct rather
# than a short list. `test_the_same_version_is_neither_newer_nor_older` and the
# tag test compare strings that are equal or that sort the same way as their
# versions, so the tiebreaker never changes their answer, and
# `test_a_string_that_is_not_a_version_is_refused_rather_than_crashing` returns
# before the comparison is reached.
#
# Re-measured the same day and the red list grew from two to three. The offer
# decision arrived after this record was written and routes through the same
# comparison, so a row of its table where two versions differ only after a plus
# turns from "nothing newer" into an offer under this break. Two tests were
# right when it was written and short by the afternoon, which is what a record
# being a measurement with a date means here.
[[guard]]
name = "a build identifier is not part of the version"
file = "src/common/version.rs"
before = '    match left.cmp(&right) {'
after = '    match left.cmp(&right).then_with(|| version.cmp(with)) {'
red = [
    "common::version::tests::test_a_build_identifier_after_a_plus_is_not_part_of_the_version",
    "common::version::tests::test_two_builds_of_one_version_are_the_same_version",
    "common::version::tests::test_whether_a_release_is_an_offer_depends_on_the_channel_somebody_chose",
    # Added 2026-09-15 by 08-07's sweep: written 2026-09-12, the same day as
    # this record, in a file it had never named.
    "service::update_check::tests::test_a_published_release_that_is_not_an_offer_is_reported_as_this_being_the_newest",
]
tests_last_seen = [
    { file = "src/common/version.rs", tests = 27 },
    { file = "src/service/update_check.rs", tests = 23 },
]

# The channel check passing everything through, which is the half-fix rather
# than the absent one: the type still exists, the setting still stores which
# channel somebody chose, the offer decision still asks the question, and the
# answer is yes for everybody. That offers an alpha to somebody who asked only
# for released versions, and once 07-09 lands it sends them to fetch an
# installer built from one.
#
# Deleting the type was the other candidate and it is worse as a measurement,
# because a missing type is a build failure rather than a wrong answer, and
# what needs guarding here is the answer.
#
# One test rather than several, and that is the whole of it rather than a short
# list. Only `test_whether_a_release_is_an_offer_depends_on_the_channel_somebody_chose`
# varies the channel; every other test in the file either asks one channel or
# asks about the ordering underneath. It is new with this change.
#
# Measured 2026-09-12 by hand on the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads: 7,051
# passed in the library and exactly this one failed, with every other target
# green.
[[guard]]
name = "the public channel never offers a prerelease"
file = "src/common/version.rs"
before = '            Self::PublicReleases => false,'
after = '            Self::PublicReleases => true,'
red = [
    "common::version::tests::test_whether_a_release_is_an_offer_depends_on_the_channel_somebody_chose",
]
tests_last_seen = [
    { file = "src/common/version.rs", tests = 27 },
]

# A fresh installation asking GitHub for a version list nobody asked for. The
# break turns the default on rather than taking the field out, which is the
# half-fix: the setting is still there, the control still offers three answers,
# the check still does what it says. It just does it for somebody who never
# chose that it should, and once plan 07-09 lands that means an installer is
# fetched over whatever connection they are on.
#
# Deleting the field was the other candidate and it is worse as a measurement,
# because a missing field is a build failure rather than a wrong answer, and
# what needs guarding here is the answer. Every other way of getting this wrong
# routes through this one line: `channel_for` asks the setting, and the setting
# has one default.
#
# One test rather than several, and that is the whole of it rather than a short
# list. Only `test_a_fresh_installation_is_not_looking_for_new_versions` asks
# what a new installation's answer is. Its neighbour about an older settings
# file reads `WhichUpdates::default()` through `#[serde(default)]` rather than
# `AppConfig::default()`, so it stays green under this break, which is worth
# knowing: this record guards the fresh-installation half and nothing here
# guards the upgrade half, because nothing can break it without breaking the
# type's own default in `src/common/version.rs`.
#
# Measured 2026-09-12 by hand on the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads: 7,065
# passed in the library and exactly this one failed, with every other target
# green.
[[guard]]
name = "a fresh installation asks nobody about new versions"
file = "src/data/config.rs"
before = '            which_updates: crate::common::version::WhichUpdates::default(),'
after = '            which_updates: crate::common::version::WhichUpdates::PublicReleases,'
red = [
    "data::config::permission_tests::test_a_fresh_installation_is_not_looking_for_new_versions",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
]

# Nothing published reading as up to date, which is T-07-19 in one line. The
# break makes the 404 arm answer that this is the newest version rather than
# deleting the arm: the check still runs, still asks, still says something, and
# what it says is that everybody is current. That is the half-fix somebody
# writes when they read a 404 as "no news", and it is the one answer this
# repository can actually give today, because no release has ever been published
# from it. So the break is not hypothetical: it is what the feature would have
# done on every machine, for every person, until the first tag.
#
# Deleting the arm was the other candidate and it is worse as a measurement. A
# missing arm falls through to the catch-all and answers "could not be fetched",
# which is wrong but is not dangerous, and what needs guarding here is
# specifically the answer that claims a state nobody learned.
#
# One test rather than several, and that is the whole of it rather than a short
# list. Only `test_nothing_published_is_neither_an_error_nor_being_up_to_date`
# drives a 404 at all. The pairwise sentence test stays green under the break,
# and correctly so: it compares five sentences built by hand and never asks the
# mapping which one a status produces, which is the split between the two tests
# and the reason both exist.
#
# The development channel's half of the same fact goes through a different line,
# `Ok(published) if published.is_empty()`, because that endpoint answers 200
# with an empty array rather than 404. This record does not cover it. That was
# measured on 2026-09-12 rather than assumed, and it is the sort of thing a
# second record would be for if the two ever stop agreeing.
#
# Measured 2026-09-12 by hand on the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads: 7,073
# passed in the library and exactly this one failed, with every other target
# green.
[[guard]]
name = "nothing published does not read as being up to date"
file = "src/service/update_check.rs"
before = '        404 => return Answer::NothingPublishedYet { channel },'
after = '        404 => return Answer::ThisIsTheNewest { channel },'
red = [
    "service::update_check::tests::test_nothing_published_is_neither_an_error_nor_being_up_to_date",
]
tests_last_seen = [
    { file = "src/service/update_check.rs", tests = 23 },
]

# ── 07-07 ─────────────────────────────────────────────────────────────────
#
# The half-fix rather than the absent one. The glob is still there, still
# names a file in `dist/`, and still reads like something the build writes. It
# has simply stopped matching, which is the shape a real drift takes: somebody
# shortens a name in the step that writes it and the list that publishes it is
# left behind, or the other way round. Removing the glob is the easy case and
# no release would ever fail that way.
#
# Until this change the consequence was silence. `fail_on_unmatched_files` was
# false, whose own documentation calls it the "Indicator of whether to fail if
# any of the files globs match nothing", so the release published three files,
# went green, and the fourth was found by somebody trying to download it.
#
# `file` is not a Rust file, so the count check skips the count for it and
# takes the counts off the red list instead, which with `suite` set resolves to
# tests/installer.rs.
#
# One test rather than several, and that is the whole of it. `house_style`
# reads this workflow, because `ours()` collects `.github` with extension
# `yml`, but its rules are about prose and a glob is not prose.
#
# Measured 2026-09-12 by hand on the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads:
# 7,077 passed in the library, 49 other targets green, and exactly this one
# test failed.
#
# One other test failed in two of the three whole-tree runs taken that day and
# it is not this break's. `tests/a_move_says_what_has_not_been_sent.rs` cannot
# reach the Windows credential store on this machine every time it asks: it
# fails with "No default store has been set", it was a different test of that
# file in each run, and the file passes on its own on an unbroken tree. Written
# down rather than filtered out, because a run that anybody trims to the
# failures they expected is a run that cannot find the one they did not.
[[guard]]
name = "a published glob still matches a name the release really writes"
file = ".github/workflows/release.yml"
suite = "installer"
before = '        dist/Wixen-Mail-*-windows.zip'
after = '        dist/Wixen-Mail-*-win.zip'
# Two tests as of 07-08, and it was one when this record was written a day
# earlier. The census of what has to be signed reads the same published list
# and asserts the globs it holds, so a glob that changes shape reddens it as
# well. Measured by hand on 2026-09-12 before the count check was cleared:
# `cargo test --test installer --no-fail-fast` with the break applied, 11
# passed and exactly these two failed.
#
# Left as the worked example of a record going stale inside a single phase.
# Nothing about 07-07's own work moved; a plan one wave later added a test
# that reaches the same rule, and the record it made short is not the record
# that plan was writing. That is the case CLAUDE.md warns costs the most,
# because the author of the new test is the only person who will ever be
# looking at this file at the right moment.
red = [
    "test_every_file_the_release_promises_is_one_it_really_produces",
    "test_the_census_of_what_has_to_be_signed_counts_seven_things",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# Guardrail 7, which is the one this project has already been bitten by: a push
# to `main` triggered this workflow, cut two releases nobody asked for and
# promoted an alpha to beta. The test that holds it was green the day it was
# written, so it has no red half of its own and this record is the substitute.
#
# The break swaps the trigger rather than adding one beside it, because that is
# a single line and expresses the same thing: after it, a release happens
# without anybody asking for one. The `inputs:` block below is left orphaned,
# which a text reading does not care about and a real run would reject, and
# that is fine for a break whose only job is to make the rule speak.
#
# Measured 2026-09-12 by hand on the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads:
# 7,077 passed in the library, 50 other targets green, and exactly this one
# test failed. The credential-store flake described in the record above did
# not appear in this run.
[[guard]]
name = "a release still happens only when somebody asks for one"
file = ".github/workflows/release.yml"
suite = "installer"
before = '  workflow_dispatch:'
after = '  push:'
red = [
    "test_a_release_still_happens_only_when_somebody_asks_for_one",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# ── 07-08 ─────────────────────────────────────────────────────────────────
#
# The half-fix rather than the absent one. The census still runs, still reads
# both lists, still counts, and has simply stopped seeing one kind of file.
# That is the shape a real narrowing takes: somebody trims a filter to make an
# awkward entry go away, or writes it a little too narrow to begin with, and
# the census goes on reading as a complete answer. Deleting the test would be
# caught by anybody. A census that counts six of seven and says nothing is the
# failure this whole task exists to prevent, applied to the instrument.
#
# `.dll` is the extension dropped because it is the one somebody is most
# likely to think does not need signing. wixen_mail_search.dll is loaded into
# the Windows indexer's own process, which is a higher bar for a signature
# than the executables beside it rather than a lower one.
#
# Measured by hand on 2026-09-12 against the tree this commit makes, with
# `cargo test --all-targets --no-fail-fast` at the default eight threads:
# 7,077 passed in the library, 49 other targets green, and exactly this one
# test failed. The count check was also red in that run, for the unrelated and
# expected reason that this commit adds two tests to a file three records
# fingerprint; it was cleared by `--remeasure` afterwards and is not part of
# this break. The credential-store flake of ledger 328 did not appear.
#
# `test_the_census_can_see_an_artefact_that_arrived_without_being_signed` does
# not redden here, and that is worth knowing rather than worth papering over.
# Its fixtures are all `.exe`, so it proves the reading can see an artefact
# that arrived and cannot see the filter narrowing underneath it. The census
# over the real tree is the half that catches this one, which is the reason
# the pair is a pair.
[[guard]]
name = "the census still sees every kind of file that has to be signed"
file = "tests/installer.rs"
suite = "installer"
before = 'const WHAT_AUTHENTICODE_SIGNS: [&str; 2] = [".exe", ".dll"];'
after = 'const WHAT_AUTHENTICODE_SIGNS: [&str; 1] = [".exe"];'
red = [
    "test_the_census_of_what_has_to_be_signed_counts_seven_things",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# The break is the half-fix, not the deletion. Taking the publisher comparison
# out altogether would leave a hole any reader spots; making it always succeed
# leaves a function that still looks like a check, still has its name, still has
# its doc comment, and accepts an installer anybody signed. That is the shape a
# real mistake takes here, and it is the one plan 07-09's threat register calls
# T-07-41.
#
# Measured by hand on 2026-09-12 at 0.117.0, `cargo test --lib --no-fail-fast`
# at eight threads: 7,103 passed, three failed, and they are exactly the three
# below. The whole library rather than a filter, because a filter chosen for the
# subject is how a record comes out short.
#
# Note which test does *not* redden: `test_a_refused_installer_is_deleted`
# stays green, and that is right rather than a gap. Its fixture is a few bytes
# with no signature at all, so Windows refuses it one step earlier, at
# `whether_windows_trusts_it`, and never reaches the comparison this breaks.
# The test that catches this break against a real signature is the first one
# listed, which asks Windows about a file Microsoft really signed.
[[guard]]
name = "an installer is refused unless this project signed it"
file = "src/service/update_download.rs"
before = """    if name.eq_ignore_ascii_case(WHO_SIGNS_THIS) {
        return Ok(());
    }"""
after = """    if name.eq_ignore_ascii_case(name) {
        return Ok(());
    }"""
red = [
    "service::update_download::tests::test_a_file_somebody_else_signed_is_refused_for_that_reason",
    "service::update_download::tests::test_a_name_that_merely_contains_ours_is_refused",
    "service::update_download::tests::test_a_signature_from_anybody_else_is_refused_however_valid_it_is",
]
tests_last_seen = [
    { file = "src/service/update_download.rs", tests = 40 },
]

# The break is a second way to make a checked installer, and it is the shape a
# real mistake takes rather than a deletion. Somebody wanting to run an
# installer from a path they already have adds a constructor, and it compiles,
# and no call site anywhere changes, and every behaviour test stays green,
# because nothing about the *behaviour* of this module changes. What changes is
# that the question and the handover can both now be handed a file nothing
# looked at, which is the entire design of the file undone in five lines.
#
# **One test reddens, and that is the finding rather than a thin record.** No
# behaviour test can see this break, by construction: the rule is carried in the
# type and a type cannot be observed by running the program. The guard reading
# the source is the only thing in the tree that can, which is exactly why it
# exists and why it has two companions proving the reading can see both a second
# constructor and a public function handing one out.
#
# Measured by hand on 2026-09-12 at 0.118.0, `cargo test --lib --no-fail-fast`
# at eight threads: 7,113 passed, one failed, and it is the one below.
[[guard]]
name = "nothing but the check can make a checked installer"
file = "src/service/update_download.rs"
before = """impl Verified {
    /// Where the checked file is.
    pub fn at(&self) -> &Path {"""
after = """impl Verified {
    /// Take a file's word for it.
    pub fn from_a_path(at: PathBuf) -> Self {
        Verified {
            at,
            version: String::new(),
        }
    }

    /// Where the checked file is.
    pub fn at(&self) -> &Path {"""
red = [
    "service::update_download::tests::test_only_the_check_can_make_a_checked_installer",
]
tests_last_seen = [
    { file = "src/service/update_download.rs", tests = 40 },
]

# The person chose whether the day comes before the month and whether the month
# is a word. The machine supplies the names inside that choice and decides
# nothing else. The plausible wrong version of this module asks Windows for its
# own idea of a date, which throws away both preferences on every machine whose
# locale disagrees with them, and on an English machine it looks fine.
#
# The break makes every shape answer the same picture, which is that mistake in
# its smallest form. Measured by hand on 2026-09-13 against this branch: four
# tests red, all four new with this module, 7131 of 7136 still passing. The two
# that did not redden are worth naming, because a record that expected them
# would rot on the next change. `test_a_day_no_month_has_is_still_written_rather
# _than_lost` is about the English fallback, which this break does not reach,
# and `test_the_twelve_names_for_a_list_come_from_the_machine` does not go
# through a picture at all, which is the whole reason there are two mechanisms.
#
# **Stale within its own plan, and the count check could not see it.** The
# four above were measured when this module had no callers. Task 2 gave it
# three, in `date_display`, and every reading downstream of those, in seven
# more files, asserts a date this break rewrites. Re-measured on 2026-09-13
# during task 3: 48 red, the four plus 44 in eight files. None of those eight
# gained a test between the two measurements, so the check that counts tests
# per named file had nothing to say; this is the case `CLAUDE.md` names as the
# first of three the count check cannot see, met here two tasks after the
# record was written. Found only because task 3 changed this file's own test
# count and the remedy it printed ran this record.
[[guard]]
name = "the shape the person chose decides the picture"
file = "src/common/how_the_machine_writes_dates.rs"
before = """            Shape::DayMonthYear(_) => "d MMMM yyyy",
            Shape::MonthDayYear(_) => "MMMM d, yyyy",
            Shape::DayMonth => "d MMMM",
            Shape::MonthDay => "MMMM d","""
after = """            Shape::DayMonthYear(_) => "d MMMM yyyy",
            Shape::MonthDayYear(_) => "d MMMM yyyy",
            Shape::DayMonth => "d MMMM yyyy",
            Shape::MonthDay => "d MMMM yyyy","""
red = [
    "application::due::tests::test_an_alert_for_a_day_reminder_says_the_day_not_an_hour_count",
    "application::due::tests::test_an_alert_says_what_it_is_and_when_it_was_due",
    "application::due::tests::test_an_overdue_task_says_so_and_names_the_day",
    "application::sending_later::tests::test_a_scheduled_message_says_when_it_will_go",
    "application::sending_later::tests::test_every_row_in_the_outbox_says_what_it_is_doing",
    "common::how_the_machine_writes_dates::tests::test_a_birthday_on_the_twenty_ninth_of_february_is_written_rather_than_refused",
    "common::how_the_machine_writes_dates::tests::test_a_french_machine_gets_a_french_month",
    "common::how_the_machine_writes_dates::tests::test_a_month_inside_a_date_is_a_different_word_from_the_month_named_alone",
    "common::how_the_machine_writes_dates::tests::test_an_english_machine_still_gets_the_english_this_program_always_wrote",
    "presentation::date_display::tests::test_a_birthday_on_the_twenty_ninth_of_february_is_still_read_as_a_day",
    "presentation::date_display::tests::test_a_birthday_with_a_year_is_still_read_as_a_whole_date",
    "presentation::date_display::tests::test_a_birthday_with_no_year_follows_the_order_the_locale_uses",
    "presentation::date_display::tests::test_a_birthday_with_no_year_is_read_as_a_day_and_a_month",
    "presentation::date_display::tests::test_a_date_with_no_year_reaching_the_ordinary_reading_is_read_as_words",
    "presentation::date_display::tests::test_a_french_machine_gets_a_french_month_in_the_order_the_person_chose",
    "presentation::date_display::tests::test_a_future_timestamp_is_never_dressed_up_as_relative",
    "presentation::date_display::tests::test_a_whole_day_in_the_future_stays_a_date_under_the_relative_style",
    "presentation::date_display::tests::test_a_whole_day_under_the_relative_style_is_a_date_and_never_an_hour_count",
    "presentation::date_display::tests::test_absolute_style_never_goes_relative",
    "presentation::date_display::tests::test_beyond_a_week_gives_the_date",
    "presentation::date_display::tests::test_month_first_order",
    "presentation::date_display::tests::test_one_stored_date_is_spoken_the_same_way_wherever_it_appears",
    "presentation::date_display::tests::test_the_clock_can_run_to_twenty_four",
    "presentation::date_display::tests::test_the_four_stored_combinations_still_read_as_they_always_did",
    "presentation::managers::tests::test_moving_an_events_time_leaves_a_moment_that_is_read_out_as_a_date",
    "presentation::pim_rows::tests::test_a_missing_due_date_is_blank_not_the_word_none",
    "presentation::pim_rows::tests::test_a_note_row_says_when_it_was_last_changed",
    "presentation::pim_rows::tests::test_a_reminder_with_a_due_date_says_the_date_rather_than_nothing",
    "presentation::pim_rows::tests::test_an_all_day_event_says_so_instead_of_a_time",
    "presentation::read_aloud::tests::test_a_birthday_this_week_is_still_a_date_and_not_how_long_ago_it_was",
    "presentation::read_aloud::tests::test_a_birthday_with_no_year_is_read_aloud_as_a_day_and_a_month",
    "presentation::read_aloud::tests::test_a_due_date_in_a_reading_is_spoken_in_words_not_as_stored_digits",
    "presentation::read_aloud::tests::test_a_full_reminder_reading_carries_the_description_the_column_hides",
    "presentation::read_aloud::tests::test_a_full_task_reading_carries_the_description_the_column_hides",
    "presentation::read_aloud::tests::test_a_note_updated_on_a_bare_date_is_a_date_under_the_shipped_default",
    "presentation::read_aloud::tests::test_a_reminder_due_on_a_day_is_a_date_not_an_hour_count_under_the_shipped_default",
    "presentation::read_aloud::tests::test_a_reminder_reads_its_own_due_field_in_the_short_form",
    "presentation::read_aloud::tests::test_a_task_due_today_is_a_date_not_an_hour_count_under_the_shipped_default",
    "presentation::read_aloud::tests::test_a_timed_event_says_when_it_starts_in_the_short_reading",
    "presentation::read_aloud::tests::test_a_whole_day_keeps_its_shape_and_an_unreadable_moment_is_still_said",
    "presentation::read_aloud::tests::test_an_all_day_event_says_so_in_the_short_reading_too",
    "presentation::read_aloud::tests::test_an_event_missing_one_of_its_times_does_not_trail_off_on_the_word_to",
    "presentation::read_aloud::tests::test_an_event_on_a_bare_date_is_a_date_not_an_hour_count_under_the_shipped_default",
    "presentation::read_aloud::tests::test_every_shape_a_stored_moment_takes_is_read_as_a_date",
    "presentation::reader_text::tests::test_a_composed_message_carries_its_date_in_the_wording_the_reader_asked_for",
    "presentation::ui_types::tests::test_a_contact_detail_reads_a_birthday_that_has_a_year_as_a_whole_date",
    "presentation::ui_types::tests::test_a_contact_detail_reads_a_birthday_with_no_year_as_words",
    "presentation::ui_types::tests::test_contact_detail_text_includes_every_populated_field",
    "presentation::ui_types::tests::test_the_heading_for_a_week_names_the_week_even_when_nothing_is_in_it",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
    { file = "src/application/sending_later.rs", tests = 47 },
    { file = "src/common/how_the_machine_writes_dates.rs", tests = 17 },
    { file = "src/presentation/date_display.rs", tests = 47 },
    { file = "src/presentation/managers.rs", tests = 137 },
    { file = "src/presentation/pim_rows.rs", tests = 25 },
    { file = "src/presentation/read_aloud.rs", tests = 51 },
    { file = "src/presentation/reader_text.rs", tests = 125 },
    { file = "src/presentation/ui_types.rs", tests = 79 },
]

# ── The catalogue, 06-03 task 3 ──────────────────────────────────────────────
#
# Five records for the one function that builds every bundle and the two that
# format a sentence, one per setting the module comment lists and one for the
# completeness check. Each break is the setting put back to the library's
# default, or the plausible tidying of a line, which is the shape the module
# comment says somebody will do. Every red list below was measured on
# 2026-09-13 through `scripts/guards.sh --remeasure` against this branch, and
# the prediction each was measured against is in 06-03-SUMMARY.md.

# Fluent's default wraps every placeable in U+2068 and U+2069. Turned back on,
# every exact-string assertion about a sentence with a number in it goes red,
# in the catalogue, in date_display, and in one read_aloud test that asserts
# "Updated: 3 hours ago" through the shipped reading.
[[guard]]
name = "a sentence out of the catalogue carries no isolation mark"
file = "src/common/catalogue.rs"
before = """    bundle.set_use_isolating(false);"""
after = """    bundle.set_use_isolating(true);"""
red = [
    "application::due::tests::test_an_all_day_event_ahead_has_no_clock_to_say",
    "application::due::tests::test_an_event_ahead_says_how_soon_then_its_name_then_its_time",
    "application::due::tests::test_an_event_that_has_started_says_how_long_ago",
    "common::catalogue::tests::test_a_count_of_one_takes_the_singular_form",
    "common::catalogue::tests::test_a_locale_nobody_has_rules_for_takes_english_plurals_silently",
    "common::catalogue::tests::test_a_machine_with_no_catalogue_in_its_language_gets_english",
    "common::catalogue::tests::test_a_number_inside_a_sentence_is_written_the_way_the_language_writes_it",
    "common::catalogue::tests::test_a_polish_resource_produces_the_polish_forms",
    "common::catalogue::tests::test_a_russian_resource_produces_the_four_russian_forms",
    "common::catalogue::tests::test_no_sentence_out_of_the_catalogue_carries_an_isolation_mark",
    "common::catalogue::tests::test_the_four_english_sentences_are_the_ones_this_program_always_said",
    "common::catalogue::tests::test_the_three_future_sentences_say_in",
    "presentation::date_display::tests::test_a_computer_whose_language_has_no_catalogue_hears_english",
    "presentation::date_display::tests::test_how_long_ago_is_reachable_without_the_relative_style",
    "presentation::date_display::tests::test_how_soon_is_said_in_the_catalogues_words",
    "presentation::date_display::tests::test_relative_wording_within_the_week",
    "presentation::read_aloud::tests::test_a_note_updated_hours_ago_still_reads_relatively_under_the_shipped_default",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
    { file = "src/common/catalogue.rs", tests = 15 },
    { file = "src/presentation/date_display.rs", tests = 47 },
    { file = "src/presentation/read_aloud.rs", tests = 51 },
]

# A count passed as a string matches no plural category and falls through to
# `*[other]` with no error, so 1 is "1 days ago". Only an assertion that sees
# a singular, or a form other than `other`, can tell; a count of 2 or 3 in
# English reads the same either way, and the formatter never sees a string.
[[guard]]
name = "a count reaches the catalogue as a number and not as a string"
file = "src/common/catalogue.rs"
before = """        args.set(counts, count);"""
after = """        args.set(counts, count.to_string());"""
red = [
    "common::catalogue::tests::test_a_count_of_one_takes_the_singular_form",
    "common::catalogue::tests::test_a_locale_nobody_has_rules_for_takes_english_plurals_silently",
    "common::catalogue::tests::test_a_number_inside_a_sentence_is_written_the_way_the_language_writes_it",
    "common::catalogue::tests::test_a_polish_resource_produces_the_polish_forms",
    "common::catalogue::tests::test_a_russian_resource_produces_the_four_russian_forms",
    "common::catalogue::tests::test_the_four_english_sentences_are_the_ones_this_program_always_said",
    "common::catalogue::tests::test_the_three_future_sentences_say_in",
    "presentation::date_display::tests::test_a_computer_whose_language_has_no_catalogue_hears_english",
    "presentation::date_display::tests::test_how_soon_is_said_in_the_catalogues_words",
    "presentation::date_display::tests::test_relative_wording_within_the_week",
]
tests_last_seen = [
    { file = "src/common/catalogue.rs", tests = 15 },
    { file = "src/presentation/date_display.rs", tests = 47 },
]

# Without the formatter Fluent writes every number as `f64` writes it, "1234"
# in every language. Under sixty that agrees with Windows everywhere, which is
# why one test reddens and it is the one written with 1234 under de-DE.
[[guard]]
name = "a number inside a sentence is written by Windows"
file = "src/common/catalogue.rs"
before = """    bundle.set_formatter(Some(as_this_locale_writes_a_number));"""
after = """    bundle.set_formatter(None);"""
red = [
    "common::catalogue::tests::test_a_number_inside_a_sentence_is_written_the_way_the_language_writes_it",
]
tests_last_seen = [
    { file = "src/common/catalogue.rs", tests = 15 },
]

# The error list `format_pattern` fills is the only place a missing argument
# is visible: the sentence itself comes back as "{$minutes} minutes ago" with
# no other sign. Ignore the list and that sentence is spoken.
[[guard]]
name = "a sentence the catalogue could not say is an error and not a sentence"
file = "src/common/catalogue.rs"
before = """        if !errors.is_empty() {
            return Err(Error::Other(format!(
                "{} could not be said: {errors:?}",
                message.id()
            )));
        }
        Ok(text.into_owned())"""
after = """        Ok(text.into_owned())"""
red = [
    "common::catalogue::tests::test_a_missing_argument_is_an_error_and_not_a_sentence_with_a_brace_in_it",
]
tests_last_seen = [
    { file = "src/common/catalogue.rs", tests = 15 },
]

# `LanguageIdentifier::default()` is `und`, and today negotiation turns `und`
# into English because English is the default catalogue, so through the
# public reading the two look alike. They stop looking alike with a second
# catalogue, which is why the fallback is asked for by name and held by two
# tests on the function that answers it rather than on what a machine hears.
[[guard]]
name = "a locale name that cannot be read asks for English by name"
file = "src/common/catalogue.rs"
before = """        .unwrap_or_else(english)"""
after = """        .unwrap_or_default()"""
red = [
    "common::catalogue::tests::test_a_locale_name_that_is_not_one_asks_for_english",
    "common::catalogue::tests::test_a_locale_this_computer_could_not_name_asks_for_english",
]
tests_last_seen = [
    { file = "src/common/catalogue.rs", tests = 15 },
]

# A message the code can ask for that the catalogue does not hold. The break
# declares one more rather than deleting one from the `.ftl`, because a
# record breaks a source file whose tests this file counts, and the catalogue
# is not one. The check that walks `ALL` reddens, and so do the two tests
# that walk `ALL` to format every message, because the extra one cannot be
# said, and the companion that plants a short resource and expects exactly
# the declared ones missing now finds one more. Four messages when this was
# written; seven since 2026-09-14, when the three "in" messages joined, and
# the break moved to the end of the list.
[[guard]]
name = "every message the code can ask for is in the English catalogue"
file = "src/common/catalogue.rs"
before = """    /// Within the week ahead.
    InDays = "dates-in-days" counting "days",
}"""
after = """    /// Within the week ahead.
    InDays = "dates-in-days" counting "days",
    /// Declared and never written.
    Never = "dates-never",
}"""
red = [
    "common::catalogue::tests::test_every_message_formats_with_its_argument_and_no_error",
    "common::catalogue::tests::test_no_sentence_out_of_the_catalogue_carries_an_isolation_mark",
    "common::catalogue::tests::test_the_completeness_reading_sees_a_message_missing_and_a_message_nobody_asks_for",
    "common::catalogue::tests::test_the_english_catalogue_is_complete_in_both_directions",
]
tests_last_seen = [
    { file = "src/common/catalogue.rs", tests = 15 },
]

# ── Day names and the signing date, 06-03 task 4 ─────────────────────────────
#
# Three records. Two put an English site back the way it was, which is the
# break the source-reading guard exists for, so each reddens that guard as
# well as the forced-locale test beside the site. The third is the off-by-one
# nobody would see on a Monday.

# The day names in a repeat-series sentence, answered from an English match
# again. The guard that reads every shipped string literal for an English day
# name sees the seven come back, and the French test hears "Tuesday".
[[guard]]
name = "a repeat-series sentence names its days the way this computer does"
file = "src/application/occurrences.rs"
before = """fn weekday_called(which: WhichLocale<'_>, day: &chrono::Weekday) -> String {
    crate::common::how_the_machine_writes_dates::a_day_name(which, *day)
}"""
after = """fn weekday_called(_which: WhichLocale<'_>, day: &chrono::Weekday) -> String {
    match day {
        chrono::Weekday::Mon => "Monday",
        chrono::Weekday::Tue => "Tuesday",
        chrono::Weekday::Wed => "Wednesday",
        chrono::Weekday::Thu => "Thursday",
        chrono::Weekday::Fri => "Friday",
        chrono::Weekday::Sat => "Saturday",
        chrono::Weekday::Sun => "Sunday",
    }
    .to_string()
}"""
red = [
    "application::occurrences::tests::test_a_french_computer_hears_french_day_names_inside_the_english_frame",
    "common::how_the_machine_writes_dates::tests::test_no_shipping_file_names_a_month_or_a_day_in_english",
]
tests_last_seen = [
    { file = "src/application/occurrences.rs", tests = 63 },
    { file = "src/common/how_the_machine_writes_dates.rs", tests = 17 },
]

# The signing date written with chrono's %B again, which is English on every
# machine. The same two shapes of witness: the guard sees the directive, and
# the French test hears "August".
[[guard]]
name = "a signature sentence writes its date the way this computer does"
file = "src/service/signed_mail.rs"
before = """    a_date(
        which,
        Shape::DayMonthYear(moment.year()),
        moment.month(),
        moment.day(),
    )
}"""
after = """    let _ = (which, Shape::DayMonthYear(moment.year()), a_date);
    moment.format("%-d %B %Y").to_string()
}"""
red = [
    "common::how_the_machine_writes_dates::tests::test_no_shipping_file_names_a_month_or_a_day_in_english",
    "service::signed_mail::tests::test_a_french_computer_hears_the_signing_date_in_french",
]
tests_last_seen = [
    { file = "src/common/how_the_machine_writes_dates.rs", tests = 17 },
    { file = "src/service/signed_mail.rs", tests = 113 },
]

# Counting the week from Sunday, which is how a calendar on the wall is
# printed and how chrono's other counter counts. Every day name is then a
# real name for the wrong day, and a test written on a Monday alone would
# not see it; the ones that assert both ends of the week do.
[[guard]]
name = "day names are counted from Monday, which is where Windows starts them"
file = "src/common/how_the_machine_writes_dates.rs"
before = """    day.num_days_from_monday() as usize"""
after = """    day.num_days_from_sunday() as usize"""
red = [
    "application::occurrences::tests::test_a_french_computer_hears_french_day_names_inside_the_english_frame",
    "application::occurrences::tests::test_a_series_says_how_often_it_repeats_and_when_it_stops",
    "application::occurrences::tests::test_every_weekday_a_rule_can_name_is_read_as_the_day_it_names",
    "common::how_the_machine_writes_dates::tests::test_a_french_machine_gets_french_day_names_counted_from_monday",
    "common::how_the_machine_writes_dates::tests::test_an_english_machine_gets_the_english_day_names_this_program_always_wrote",
]
tests_last_seen = [
    { file = "src/application/occurrences.rs", tests = 63 },
    { file = "src/common/how_the_machine_writes_dates.rs", tests = 17 },
]

# 06-05. One rule says whether this is a moment to open something modal, and
# both the folders question and the reminder ask it. The break makes it answer
# Free while somebody is typing, which is the state the reminder was in before
# this plan: a window over somebody in the middle of a word.
#
# Measured by hand on 2026-09-14 against the whole library: two tests, one
# asking the rule directly and one asking `what_to_raise`, which now goes
# through it. The reading in `tests/wired.rs` stays green, because it reads
# the call site and not the rule.
[[guard]]
name = "the rule about opening a modal does not answer free while somebody is typing"
file = "src/presentation/one_question_at_a_time.rs"
before = """        (false, true) => Moment::SomebodyIsTyping,"""
after = """        (false, true) => Moment::Free,"""
red = [
    "presentation::one_question_at_a_time::tests::test_nothing_is_raised_while_an_editor_has_focus_and_it_is_raised_once_focus_leaves",
    "presentation::one_question_at_a_time::tests::test_the_rule_tells_somebody_typing_apart_from_something_already_up",
]
tests_last_seen = [
    { file = "src/presentation/one_question_at_a_time.rs", tests = 21 },
]

# 06-05, guardrail 5. The reminder window's tone comes back once a minute
# until focus reaches the window, and stops for good the first time it does.
# The break turns the latch into a reading, so focus leaving the window again
# starts the tone again: a person who heard it and went back to what they
# were doing is nagged for it.
#
# Measured by hand on 2026-09-14 against the whole library: two tests, the
# one about focus leaving and the one about a window that opens with focus,
# because with the latch gone the second ask of that test finds no focus and
# sounds.
[[guard]]
name = "the reminder tone does not come back once focus has reached the window"
file = "src/presentation/wx_reminder_alert.rs"
before = """        self.focus_has_arrived |= window_has_focus;"""
after = """        self.focus_has_arrived = window_has_focus;"""
red = [
    "presentation::wx_reminder_alert::tests::test_a_window_that_opens_with_focus_never_sounds_again",
    "presentation::wx_reminder_alert::tests::test_the_tone_stops_for_good_once_focus_has_arrived_even_if_it_leaves",
]
tests_last_seen = [
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
]

# ── 06-06 ─────────────────────────────────────────────────────────────────
#
# Task 2. The scan looks at every window a fresh profile can reach, thirty
# targets where there were ten, and the workflow's own list is what says
# which of them the scan is asked for. The break takes one of the new
# windows out of that list and leaves its variant in the code, which is the
# exact shape of forgetting the workflow: the window has a name, the program
# opens it on request, and nothing ever requests it. The test that reads the
# workflow is a unit test in src/, so before 06-06's first task a commit
# touching only the workflow never ran it; now the folder answers `all`.
#
# Measured by hand on 2026-09-14 against the whole library at eight threads,
# `cargo test --lib --no-fail-fast`: 7,183 passed, 1 ignored, and exactly
# this one test failed, naming which-days.
[[guard]]
name = "a window the program can open for the scan is one the workflow asks for"
file = ".github/workflows/accessibility.yml"
before = """          'which-days', 'send-later', 'add-address-book', 'about',"""
after = """          'send-later', 'add-address-book', 'about',"""
red = [
    "presentation::scan_target::tests::test_the_workflow_asks_for_every_target",
]
tests_last_seen = [
    { file = "src/presentation/scan_target.rs", tests = 11 },
]

# Guardrail 4, on the scanner itself. The workflow downloads one named
# release and refuses to run it unless the zip's SHA-256 is the one written
# beside the tag, so the coverage list 06-07 writes is a claim about one
# binary rather than about a name somebody can move. The break is the fix
# somebody reaches for when the hash stops matching: compare the download
# with itself. The line still says Get-FileHash and SHA256, the check still
# runs, and it can no longer refuse anything.
#
# Measured by hand on 2026-09-14 against the whole library at eight threads,
# `cargo test --lib --no-fail-fast`: 7,183 passed, 1 ignored, and exactly
# this one test failed, saying the workflow never says what the hash has to
# be.
[[guard]]
name = "the scanner's checksum is compared with a number written down and not with itself"
file = ".github/workflows/accessibility.yml"
before = """        $expected = 'aeca43f41c89b3ffb1db84011539e609ecd7cb3badd6e78fada2ada327d10a64'"""
after = """        $expected = (Get-FileHash -Algorithm SHA256 axe-cli.zip).Hash.ToLowerInvariant()"""
red = [
    "presentation::scan_target::tests::test_the_workflow_pins_the_scanner_to_a_release_and_its_checksum",
]
tests_last_seen = [
    { file = "src/presentation/scan_target.rs", tests = 11 },
]

# The coverage page, docs/wcag-coverage.md, says which of the fifty-five WCAG
# 2.2 Level A and AA criteria each scan channel can produce a finding
# against, and the three the Axe.Windows rules cite live in
# presentation::what_the_scans_can_judge as code. The break is the page
# going stale by hand: the 2.1.1 row's Axe.Windows cell changed to "no" with
# the code untouched, which is the shape the five WebView2 findings went
# stale in. Two go red rather than one, because the companion that plants a
# fourth criterion expects exactly one disagreement and finds two.
#
# Measured by hand on 2026-09-14 against the whole library at eight threads,
# `cargo test --lib --no-fail-fast`: 7,190 passed, 1 ignored, and exactly
# these two failed.
[[guard]]
name = "the coverage page cannot say a criterion is unjudged while the code says the scan judges it"
file = "docs/wcag-coverage.md"
before = """| 2.1.1 Keyboard | A | yes | yes, 9 rules, all about whether an element reports itself as keyboard focusable given its control type | no | no |"""
after = """| 2.1.1 Keyboard | A | yes | no | no | no |"""
red = [
    "presentation::what_the_scans_can_judge::tests::test_the_page_says_yes_for_exactly_the_criteria_the_code_names",
    "presentation::what_the_scans_can_judge::tests::test_a_fourth_criterion_marked_yes_on_the_page_is_reported",
]
tests_last_seen = [
    { file = "src/presentation/what_the_scans_can_judge.rs", tests = 8 },
]

# ── One window for every due thing, 06-09 task 1 ─────────────────────────────
#
# Six records, one per rule the model gained on 2026-09-14 when a Due stopped
# being only a reminder. Each was measured by hand that day against the whole
# library at eight threads, `cargo test --lib --no-fail-fast`, 7,221 tests
# with one ignored, and each reddened exactly what it names and nothing else.
# The counts are quoted per record, because a "nothing else" that was not
# measured is the claim this file exists to refuse.

# A row is heard one word at a time, and in a list of reminders, tasks and
# events the word that sorts them is the one to hear first. The break drops
# the word from the task's on-time sentence, which is the sentence a person
# meets most: "Due today: file the report" says what and not which. Two go
# red: the task's own sentence, and the sweep over every kind on time and
# late. Measured 2026-09-14: 7,219 passed, exactly these two failed.
[[guard]]
name = "a due row says its kind before its title"
file = "src/application/due.rs"
before = """                    format!("Task due today: {title}")"""
after = """                    format!("Due today: {title}")"""
red = [
    "application::due::tests::test_a_task_says_task_before_anything_else",
    "application::due::tests::test_every_kind_says_its_word_first_on_time_and_late",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
]

# A task and a reminder can share a row id, and `already` and the hold are
# both keyed by identity. The break is the plausible shortcut: equality and
# hashing on the id alone, which is what the session set was keyed by until
# this plan. Both tests that were green on arrival in the red commit, because
# the derive carried them, go red here, which is what earns them their place.
# Measured 2026-09-14: 7,219 passed, exactly these two failed. The other half
# of the identity trap, a day of a series carrying its series' id, is composed
# by the event feed and not here; its record belongs to the commit that writes
# that feed.
[[guard]]
name = "the same id under two kinds is two identities"
file = "src/application/due.rs"
before = """#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Identity {
    pub kind: Kind,
    pub id: String,
}"""
after = """#[derive(Debug, Clone, Eq)]
pub struct Identity {
    pub kind: Kind,
    pub id: String,
}

impl PartialEq for Identity {
    fn eq(&self, other: &Self) -> bool {
        self.id == other.id
    }
}

impl std::hash::Hash for Identity {
    fn hash<H: std::hash::Hasher>(&self, state: &mut H) {
        self.id.hash(state);
    }
}"""
red = [
    "application::due::tests::test_a_hold_on_one_kind_does_not_hold_the_same_id_under_another",
    "application::due::tests::test_the_same_id_under_two_kinds_is_two_things",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
]

# "Event started three hours ago" about a meeting that finished two hours ago
# is noise, and a machine asleep through the morning would say it about every
# meeting it missed. The break compares the end with the alert instant instead
# of with now, which is never true for an event that ends after its alert, so
# nothing is ever refused for having ended. Measured 2026-09-14: 7,220 passed,
# exactly this one failed.
[[guard]]
name = "an event whose end has passed is not raised"
file = "src/application/due.rs"
before = """            if candidate.ends.is_some_and(|end| end <= now) {"""
after = """            if candidate.ends.is_some_and(|end| end <= at) {"""
red = [
    "application::due::tests::test_an_event_whose_end_has_passed_is_not_raised",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
]

# A task snoozed until five is not mentioned at four and is mentioned at
# five, and its own row is never written; the hold is a rule here so that it
# is tested without a table. The break flips the comparison, so a hold that
# has not run out is ignored and one that has is obeyed. Measured 2026-09-14:
# 7,220 passed, exactly this one failed.
[[guard]]
name = "a held thing is not due until its hold runs out"
file = "src/application/due.rs"
before = """                .is_some_and(|until| *until > now)"""
after = """                .is_some_and(|until| *until < now)"""
red = [
    "application::due::tests::test_a_held_thing_is_not_due_until_its_hold_runs_out",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
]

# An event happens whether or not somebody went, and marking it done would
# write a fact its calendar does not hold, so the window never offers it. The
# break answers yes for every kind. One test today; the window that reads
# this answer to enable or disable its button is task 3's, and its records
# will name this rule again. Measured 2026-09-14: 7,220 passed, exactly this
# one failed.
[[guard]]
name = "an event cannot be done"
file = "src/application/due.rs"
before = """            Kind::Reminder | Kind::Task => true,
            Kind::Event => false,"""
after = """            Kind::Reminder | Kind::Task => true,
            Kind::Event => true,"""
red = [
    "application::due::tests::test_an_event_cannot_be_done_and_the_other_two_can",
    # Added 2026-09-15 by 08-07's sweep: the due window's two tests, written
    # 2026-09-14 by 06-09's task 2 in a file this record from task 1 had
    # never named.
    "presentation::wx_reminder_alert::tests::test_done_is_refused_on_an_event_row_and_taken_on_a_task_row",
    "presentation::wx_reminder_alert::tests::test_mark_done_and_details_say_why_they_are_unavailable",
]
tests_last_seen = [
    { file = "src/application/due.rs", tests = 46 },
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
]

# The future twin chooses its unit the way the past reading does, so "in 90
# minutes" is never said where "90 minutes ago" would have been "1 hour ago".
# The break widens the minutes band to two hours in the future twin only. Two
# go red: the boundary test that asks for the unit and the count, and the
# English wording through the catalogue, which asks for "in 1 hour" at sixty
# minutes. Measured 2026-09-14: 7,219 passed, exactly these two failed.
[[guard]]
name = "the future wording chooses hours where the past one would"
file = "src/presentation/date_display.rs"
before = """    if minutes < 60 {
        return Some((Message::InMinutes, Some(minutes)));
    }"""
after = """    if minutes < 120 {
        return Some((Message::InMinutes, Some(minutes)));
    }"""
red = [
    "presentation::date_display::tests::test_how_soon_is_said_in_the_catalogues_words",
    "presentation::date_display::tests::test_which_sentence_a_moment_ahead_gets_and_the_count_that_goes_in_it",
]
tests_last_seen = [
    { file = "src/presentation/date_display.rs", tests = 47 },
]

# A status line built with a label of one space is a control whose name is a
# space on both accessibility channels, and the sizing grip after it takes the
# same name. The scan of 2026-09-14 reported seven such across five windows,
# all from three lines of code. The break puts the space back on the manager
# shell's status line, which is the line the other two were copied from. The
# guard is its own target, `tests/no_label_is_only_a_space.rs`, so the suite is
# what is measured rather than the library.
[[guard]]
name = "no status line is built with a label that is only a space"
file = "src/presentation/wx_managers.rs"
suite = "no_label_is_only_a_space"
before = """    set_accessible_name(&list, holds);
    // Empty until there is something to say, not a space: an empty label is
    // one line tall and reaches the accessibility tree with no name, where a
    // space reached it as a name that says nothing, and named the sizing grip
    // after it the same way.
    let status = StaticText::builder(&dialog).with_label("").build();"""
after = """    set_accessible_name(&list, holds);
    let status = StaticText::builder(&dialog).with_label(" ").build();"""
red = [
    "test_no_control_is_built_with_a_label_that_is_only_a_space",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/no_label_is_only_a_space.rs", tests = 7 },
]

# ── 06-09 task 2: the hold table, and done on a task ────────────────────────
#
# A snoozed task or event waits in `held_alerts` until a moment. Holding one
# that is already held has to replace its moment: the latest snooze is the one
# somebody meant, and a table that kept the first would bring the thing back
# at the old time. The break turns the replacement into a no-op.
[[guard]]
name = "a hold held again replaces its moment rather than keeping the first"
file = "src/data/message_cache/held_alerts.rs"
before = """                 ON CONFLICT(kind, id) DO UPDATE SET until = excluded.until","""
after = """                 ON CONFLICT(kind, id) DO NOTHING","""
red = [
    "data::message_cache::held_alerts::tests::test_holding_an_alert_again_replaces_its_moment",
]
tests_last_seen = [
    { file = "src/data/message_cache/held_alerts.rs", tests = 5 },
]

# A hold ending at exactly the moment is kept for one more look, so the table
# and `due::what_is_due` agree about the boundary without the table knowing
# the rule. The break lets go of the row on the boundary.
[[guard]]
name = "a hold that ended is let go and one that has not is kept"
file = "src/data/message_cache/held_alerts.rs"
before = """                "DELETE FROM held_alerts WHERE until < ?1","""
after = """                "DELETE FROM held_alerts WHERE until <= ?1","""
red = [
    "data::message_cache::held_alerts::tests::test_a_hold_that_ended_is_let_go_and_one_that_has_not_is_kept",
]
tests_last_seen = [
    { file = "src/data/message_cache/held_alerts.rs", tests = 5 },
]

# The kind is a word so that a fourth kind is a fourth word. A read that kept
# only the words this build knows would drop a hold written by a later version
# and bring a snoozed message back at somebody who went back to this build.
# The break narrows the read to the three kinds that exist today.
[[guard]]
name = "a hold under a kind word this build does not know survives a read"
file = "src/data/message_cache/held_alerts.rs"
before = """            .prepare_cached("SELECT kind, id, until FROM held_alerts ORDER BY until, kind, id")"""
after = """            .prepare_cached("SELECT kind, id, until FROM held_alerts WHERE kind IN ('reminder', 'task', 'event') ORDER BY until, kind, id")"""
red = [
    "data::message_cache::held_alerts::tests::test_a_kind_word_this_build_does_not_know_survives_a_read_and_a_prune",
]
tests_last_seen = [
    { file = "src/data/message_cache/held_alerts.rs", tests = 5 },
]

# A task and a reminder can share a row id. The key is the kind and the id
# together; keyed by the id alone, the second hold under a shared id would be
# refused by the conflict clause, which names a key that no longer exists. The
# break is in the schema, which the test template builds once per process.
[[guard]]
name = "the same id under two kinds is two holds in the table"
file = "src/data/message_cache/mod.rs"
before = """                PRIMARY KEY (kind, id)"""
after = """                PRIMARY KEY (id)"""
red = [
    "data::message_cache::held_alerts::tests::test_a_hold_read_back_is_the_hold_written",
    "data::message_cache::held_alerts::tests::test_holding_an_alert_again_replaces_its_moment",
    "data::message_cache::held_alerts::tests::test_the_same_id_under_two_kinds_is_two_holds",
    "data::message_cache::held_alerts::tests::test_a_hold_that_ended_is_let_go_and_one_that_has_not_is_kept",
    "data::message_cache::held_alerts::tests::test_a_kind_word_this_build_does_not_know_survives_a_read_and_a_prune",
]
tests_last_seen = [
    { file = "src/data/message_cache/held_alerts.rs", tests = 5 },
    { file = "src/data/message_cache/mod.rs", tests = 23 },
]

# The due window answers "done", not "the other way". A writer that set done
# on a row already done would report a change for a task the phone finished
# in between, and stamp over the moment it was really finished. The break
# drops the guard on the row's state.
[[guard]]
name = "completing a task already done changes nothing"
file = "src/data/message_cache/tasks.rs"
before = """                 WHERE id = ?2 AND NOT is_completed","""
after = """                 WHERE id = ?2","""
red = [
    "data::message_cache::tasks::tests::test_completing_a_task_sets_done_once_and_never_toggles_it_back",
]
tests_last_seen = [
    { file = "src/data/message_cache/tasks.rs", tests = 22 },
]

# ── 06-09 task 3: one window, every due thing a row ─────────────────────────
#
# A row's text is its whole sentence, so the kind is the first word a screen
# reader says and the three channels agree. The break lists the title alone,
# which is what the old window showed a braille display off its first line.
# The suite is the live target that builds the real dialog, which couples that
# target to this file in `check.sh --suites-for`.
[[guard]]
name = "a due row's text is its sentence and not its title"
file = "src/presentation/wx_reminder_alert.rs"
suite = "the_due_window_holds_every_kind"
before = """        list.append(&row.spoken(now, dates));"""
after = """        list.append(&row.title);"""
red = [
    "test_the_due_window_holds_one_row_of_each_kind_and_offers_what_each_can_take",
]
tests_last_seen = [
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
    { file = "tests/the_due_window_holds_every_kind.rs", tests = 1 },
]

# Mark Done on an event would write a fact its calendar does not hold. The
# label and the state are decided together, and the break makes the button
# available for every kind with its plain label.
[[guard]]
name = "mark done is unavailable on an event row and its label says so"
file = "src/presentation/wx_reminder_alert.rs"
before = """    if kind.can_be_done() {
        ButtonState {
            label: MARK_DONE.to_string(),"""
after = """    if true {
        ButtonState {
            label: MARK_DONE.to_string(),"""
red = [
    "presentation::wx_reminder_alert::tests::test_mark_done_and_details_say_why_they_are_unavailable",
]
tests_last_seen = [
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
]

# Snooze all answers for every row still listed, not only the selected one.
# The break answers the first row alone, which is what a handler that read
# the selection and forgot the rest would do.
[[guard]]
name = "snooze all answers for every row still listed and not only one"
file = "src/presentation/wx_reminder_alert.rs"
before = """        for row in self.listed.drain(..) {
            self.answered.push((row.identity, answer));
        }"""
after = """        for row in self.listed.drain(..self.listed.len().min(1)) {
            self.answered.push((row.identity, answer));
        }"""
red = [
    "presentation::wx_reminder_alert::tests::test_snooze_all_answers_for_every_row_still_listed_and_not_only_the_selected_one",
]
tests_last_seen = [
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
]

# Closing the window by Escape or the title bar dismisses what is left, as
# it always did: the rows stay as they are and are not raised again this
# session. Treating them as edited would take them out of `already` and
# bring them back at the next look at somebody who had just closed them.
[[guard]]
name = "closing the due window dismisses whatever is still listed"
file = "src/presentation/wx_reminder_alert.rs"
before = """            self.answered.push((row.identity, Answer::Dismissed));"""
after = """            self.answered.push((row.identity, Answer::Edited));"""
red = [
    "presentation::wx_reminder_alert::tests::test_closing_the_window_dismisses_whatever_is_still_listed",
]
tests_last_seen = [
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
]

# Done on an event is refused by the bookkeeping as well as by the button,
# because a handler is one line from answering the wrong row. The break
# takes the refusal out.
[[guard]]
name = "done on an event row is refused by the bookkeeping itself"
file = "src/presentation/wx_reminder_alert.rs"
before = """        if answer == Answer::Done && !row.identity.kind.can_be_done() {
            return Answered::NotForThisKind;
        }"""
after = """        if false && !row.identity.kind.can_be_done() {
            return Answered::NotForThisKind;
        }"""
red = [
    "presentation::wx_reminder_alert::tests::test_done_is_refused_on_an_event_row_and_taken_on_a_task_row",
]
tests_last_seen = [
    { file = "src/presentation/wx_reminder_alert.rs", tests = 15 },
]

# ── 06-09 task 4: the feeds, and what an event's stored alert means ────────
#
# Every shown day of a series carries the series' id, so an identity made
# from the id alone would make dismissing today's standup dismiss every
# standup for the session, and snoozing it hold every one. The break composes
# the identity from the id alone. Ledger 404's record, deferred from task 1
# because the composition did not exist until the feed did.
[[guard]]
name = "a day of a series is its own due thing and not the series"
file = "src/presentation/ui_types.rs"
before = """            id: format!("{}|{}", self.id, self.start),"""
after = """            id: self.id.clone(),"""
red = [
    "presentation::ui_types::tests::test_two_days_of_one_series_are_two_due_identities_and_one_day_is_one",
]
tests_last_seen = [
    { file = "src/presentation/ui_types.rs", tests = 79 },
]

# The one mistake worse than a missed alert: an event whose owner switched
# the alert off, interrupted anyway. The break gives an explicit off the
# default lead, which is what reading the column as two states did.
[[guard]]
name = "an event stored as off is never given the default lead"
file = "src/application/event_alerts.rs"
before = """        StoredAlert::Off => None,"""
after = """        StoredAlert::Off => Some(default_lead),"""
red = [
    "application::event_alerts::tests::test_the_default_fills_silence_and_never_overrides_an_explicit_off",
]
tests_last_seen = [
    { file = "src/application/event_alerts.rs", tests = 4 },
]

# Pratik's other half: the default fills silence. The break leaves an event
# nobody said anything about silent, which is the column-only reading he
# was offered and did not choose.
[[guard]]
name = "an event nobody said anything about is given the default lead"
file = "src/application/event_alerts.rs"
before = """        StoredAlert::Unknown => Some(default_lead),"""
after = """        StoredAlert::Unknown => None,"""
red = [
    "application::event_alerts::tests::test_the_default_fills_silence_and_never_overrides_an_explicit_off",
]
tests_last_seen = [
    { file = "src/application/event_alerts.rs", tests = 4 },
]

# Microsoft's pull knows the alert is off and says so. The break stores
# nothing for off, as the pull did until 2026-09-14, which the due window
# would read as nobody's answer and fill with the default. Reddens the
# module's own test and the pull's test in calendar.rs.
[[guard]]
name = "microsofts pull stores off when the reminder is off"
file = "src/application/event_alerts.rs"
before = """        false => Some(NO_ALERT.to_string()),"""
after = """        false => None,"""
red = [
    "application::event_alerts::tests::test_microsofts_pull_stores_off_when_the_reminder_is_off",
    "application::calendar::tests::test_an_alert_is_kept_only_when_microsoft_says_it_is_on_and_gives_a_lead_time",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/event_alerts.rs", tests = 4 },
]

# Google's "useDefault false with no overrides" is the event never alerting.
# The break stores nothing for it, so the calendar's default and never are
# the same silence again. Reddens the module's own test and the pull's.
[[guard]]
name = "googles pull stores off only when google said the event never alerts"
file = "src/application/event_alerts.rs"
before = """        return (!reminders.use_default).then(|| NO_ALERT.to_string());"""
after = """        return None;"""
red = [
    "application::event_alerts::tests::test_googles_pull_stores_off_only_when_google_said_the_event_never_alerts",
    "application::calendar::tests::test_google_event_to_local",
]
tests_last_seen = [
    { file = "src/application/calendar.rs", tests = 194 },
    { file = "src/application/event_alerts.rs", tests = 4 },
]

# The editor is the third writer that knows the alert is off: a person took
# the last one away. The break stores nothing for an emptied list, as it did
# until 2026-09-14.
[[guard]]
name = "the editor stores off when the last alert is taken away"
file = "src/presentation/managers.rs"
before = """    Some(if kept.is_empty() {
        crate::application::event_alerts::NO_ALERT.to_string()
    } else {
        serde_json::Value::Array(kept).to_string()
    })"""
after = """    (!kept.is_empty()).then(|| serde_json::Value::Array(kept).to_string())"""
red = [
    "presentation::managers::tests::test_clearing_the_only_alert_stores_off_rather_than_nothing",
]
tests_last_seen = [
    { file = "src/presentation/managers.rs", tests = 137 },
]

# ── Every number carries its command and its date, 08-01 task 1 ─────────────
#
# docs/development/measurements.md is the one page a figure about this tree
# is written on, and every row must say how and when it was taken. The break
# is a row losing its date: the commits row's date cell emptied by hand, with
# the rest of the row intact. Five go red rather than one, because each of
# the four companions reads the real page and asserts it is clean before
# splicing its own omission in, so a page that already disagrees fails them
# all. That is the companion refusing to prove something other than what it
# says, not a record to narrow.
#
# Measured by hand on 2026-09-14 against the target itself,
# `cargo test --test every_number_carries_its_command_and_its_date`: 5
# passed and exactly these five failed. The suite is the target rather than
# the library because the reading lives under tests/, which `--lib` never
# builds.
[[guard]]
name = "a figure on the measurements page cannot lose its date"
file = "docs/development/measurements.md"
suite = "every_number_carries_its_command_and_its_date"
before = """| Commits on `main` | 2,025 | `git rev-list --count HEAD` | 2026-09-14 | 7b2482b1 |"""
after = """| Commits on `main` | 2,025 | `git rev-list --count HEAD` |  | 7b2482b1 |"""
red = [
    "test_every_row_on_the_measurements_page_carries_its_command_its_date_and_its_commit",
    "test_the_reading_can_see_a_row_that_appears_twice",
    "test_the_reading_can_see_a_row_whose_command_is_missing",
    "test_the_reading_can_see_a_row_whose_commit_is_missing",
    "test_the_reading_can_see_a_row_whose_date_is_missing",
]
tests_last_seen = [
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# ── Phase 8, plan 02: a figure on any page carries its date and its source ──
#
# The provenance reading walks docs/, README.md and CLAUDE.md, less the
# changelog and docs/plans/, and requires a count, a percentage or a duration
# to sit in a paragraph with a date and a command or named source. The break
# is the date taken off a narrative in CLAUDE.md: the day 01-02's writer
# record was written, which is the only date in its paragraph. Five go red
# for the reason the record above gives: the four companions that splice a
# figure into the README assert the real walk is clean first.
#
# Measured by hand on 2026-09-14 against the target,
# `cargo test --test every_number_carries_its_command_and_its_date`: 14
# passed and exactly these five failed.
[[guard]]
name = "a figure on a page cannot lose the date it was taken"
file = "CLAUDE.md"
suite = "every_number_carries_its_command_and_its_date"
before = """it was written on 2026-08-30, 17 a day later, 21 the day after, and 31 by the"""
after = """it was written, 17 a day later, 21 the day after, and 31 by the"""
red = [
    "test_every_figure_on_a_page_carries_its_date_and_its_source",
    "test_the_provenance_reading_can_see_a_count_without_a_date",
    "test_the_provenance_reading_can_see_a_duration_without_a_source",
    "test_the_provenance_reading_can_see_a_percentage_without_a_date",
    "test_the_provenance_reading_excuses_a_line_that_names_a_target",
]
tests_last_seen = [
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# A line that says `target` is stating an aim and is excused from the date. The
# break takes the word off the one such line in the tree, the coverage aim in
# docs/architecture.md, so the reading sees a bare percentage. The same five
# as the record above, for the same reason.
#
# Measured by hand on 2026-09-14 against the target: 14 passed and exactly
# these five failed, each naming `docs/architecture.md:400`.
#
# ── Re-anchored on 2026-09-22 by 12-02.1, after it reddened nothing ─────────
#
# Run 35520204784 found this break reddening nothing on a runner, and a
# measurement here on 2026-09-22 agreed: 5 of 5 named tests stayed green with
# the guard broken, rebuild 1 s and run 1 s.
#
# Why. `f15f4671`, 08-09's pass that judged each target against its row, gave
# the paragraph around `docs/architecture.md:400` its own date and its own
# backticked command. The reading excuses a figure twice over, by
# `names_a_target` on its line and separately by (dated, sourced) on its
# paragraph, so taking the word away left the second excuse standing. The
# guard still read the rule; the site it was anchored on had stopped
# discriminating.
#
# And no other document site is available. A scan of every page under `docs/`
# and of `README.md` and `CLAUDE.md` on 2026-09-22, replaying the three figure
# patterns and the date and source readings, found no line holding the word
# and a figure of a matched kind whose paragraph carries neither. Two shapes
# were tried by hand before this one and neither reddened anything:
# `docs/wcag-coverage.md:29`'s "conformance target is 31 + 24 = 55 criteria",
# because the count pattern matches a number before `tests`, `records`,
# `mutants` or `lines` and not before `criteria`; and the original site again.
#
# So the break moves to where the rule lives, the reading's own excuse, on the
# pattern of the sixteen records that already break a file under `tests/`. It
# reddens exactly one test, the companion that plants a line saying `target`
# and then the same line without the word.
#
# Measured by hand on 2026-09-22 against the target,
# `cargo test --test every_number_carries_its_command_and_its_date`: 24 passed
# and exactly this one failed.
[[guard]]
name = "the word target is the only thing excusing a figure from its date"
file = "tests/every_number_carries_its_command_and_its_date.rs"
suite = "every_number_carries_its_command_and_its_date"
before = '''/// A line that says `target` is stating an aim, not a measurement, and an aim
/// has no date of being taken.
fn names_a_target(line: &str) -> bool {
    line.to_lowercase().contains("target")
}'''
after = '''/// A line that says `target` is stating an aim, not a measurement, and an aim
/// has no date of being taken.
fn names_a_target(line: &str) -> bool {
    line.to_lowercase().contains("no line on any page holds this")
}'''
red = [
    "test_the_provenance_reading_excuses_a_line_that_names_a_target",
]
tests_last_seen = [
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# The agreement reading holds a test count on the three pages that state one
# to a row on the measurements page. The break moves the status page's count
# one off the row's value. Two go red: the reading, and the companion that
# asserts the pages agree before it plants a count no row holds. The
# provenance companions stay green because the paragraph keeps its date and
# its command.
#
# Measured by hand on 2026-09-14 against the target: 17 passed and exactly
# these two failed.
[[guard]]
name = "a test count on a page is the value of a row on the measurements page"
file = "docs/IMPLEMENTATION_STATUS.md"
suite = "every_number_carries_its_command_and_its_date"
before = """7,697 tests: 7,245 unit tests and 452 integration tests, counted 2026-09-14 at"""
after = """7,698 tests: 7,245 unit tests and 452 integration tests, counted 2026-09-14 at"""
red = [
    "test_the_agreement_reading_can_see_a_count_no_row_holds",
    "test_the_three_pages_that_state_the_test_count_quote_one_row",
]
tests_last_seen = [
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# The share of history before red/green is computed on every commit, and no
# site states it as two absolutes any more. The break puts the two-absolutes
# form back into scripts/mutants.sh in the shape it really had there, the
# count at the end of one comment line and the total behind the `#` of the
# next, which is the shape a line grep never saw. Three go red: the check, and
# the two companions, each of which asserts the four sites are clean before it
# plants its own form.
#
# Measured by hand on 2026-09-14 against the target,
# `cargo test --test every_number_carries_its_command_and_its_date`: 19 passed
# and exactly these three failed, each naming `scripts/mutants.sh:9`.
[[guard]]
name = "no site states the share of history before red/green as two absolutes"
file = "scripts/mutants.sh"
suite = "every_number_carries_its_command_and_its_date"
before = """# This project needs to know the difference. Red/green started at 18a02454 on
# 2026-07-26, so most of the tests here were written after the code they cover,"""
after = """# This project needs to know the difference. Red/green started at commit 182 of
# 344 on 2026-07-26, so most of the tests here were written after the code they cover,"""
red = [
    "test_the_ratio_reading_can_see_the_one_line_form",
    "test_the_ratio_reading_can_see_the_wrapped_form",
    "test_the_share_of_history_before_red_green_is_computed_and_printed",
]
tests_last_seen = [
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# A figure in prose that restates a constant the code holds is held to it.
# The break moves the privacy page's largest-attachment figure by one
# megabyte, so the page says 26 where LARGEST_ATTACHMENT_KEPT_BYTES says 25.
# Three go red: the reading and its two companions, each of which asserts the
# twelve pairs agree before it moves a figure or removes a phrase.
#
# Measured by hand on 2026-09-14 against the target,
# `cargo test --test every_number_carries_its_command_and_its_date`: 22 passed
# and exactly these three failed.
[[guard]]
name = "a page cannot restate a constant with a figure the code does not hold"
file = "docs/privacy.md"
suite = "every_number_carries_its_command_and_its_date"
before = """share the computer or carry it around. A single file is kept up to 25 MB, and all of them"""
after = """share the computer or carry it around. A single file is kept up to 26 MB, and all of them"""
red = [
    "test_every_figure_that_restates_a_constant_agrees_with_it",
    "test_the_constants_reading_can_see_a_figure_that_moved",
    "test_the_constants_reading_refuses_a_phrase_that_has_gone",
]
tests_last_seen = [
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# The same rule from the other side: the constant moves and the page does not.
# The break lands on source, which is the point of the record: it couples
# src/data/message_cache/attachment_content.rs to this target, so a commit
# that changes the constant runs the reading that holds the page to it. The
# same three go red.
#
# What the whole library said under the same break is worth writing down.
# `cargo test --lib -- --test-threads=8` on 2026-09-14: 7,244 passed, none
# failed, 73 s. No unit test pins the constant's value; the tests beside it
# are written relative to it, `LARGEST_ATTACHMENT_KEPT_BYTES + 1`, and stay
# green at any size. So this target's reading is the only thing in the tree
# that holds the number, and there is no library-suite record to write
# beside this one because a record whose red list is empty measures nothing.
#
# Measured by hand on 2026-09-14 against the target: 22 passed and exactly
# these three failed.
[[guard]]
name = "a constant cannot move away from the page that restates it"
file = "src/data/message_cache/attachment_content.rs"
suite = "every_number_carries_its_command_and_its_date"
before = """pub const LARGEST_ATTACHMENT_KEPT_BYTES: i64 = 25 * 1024 * 1024;"""
after = """pub const LARGEST_ATTACHMENT_KEPT_BYTES: i64 = 26 * 1024 * 1024;"""
red = [
    "test_every_figure_that_restates_a_constant_agrees_with_it",
    "test_the_constants_reading_can_see_a_figure_that_moved",
    "test_the_constants_reading_refuses_a_phrase_that_has_gone",
]
tests_last_seen = [
    { file = "src/data/message_cache/attachment_content.rs", tests = 22 },
    { file = "tests/every_number_carries_its_command_and_its_date.rs", tests = 25 },
]

# ── The usable line, and the harness that reads it ──────────────────────────
#
# PERF-02's cold-start figure is read off one log line the application writes
# when its message list first holds a row: `the message list is usable: N rows,
# M ms after start`. A harness parses it, so its shape is held byte for byte.
# The break changes one word, which is what a well-meaning rewording of a log
# line looks like. Two go red: the byte-for-byte test and the once test, which
# looks for the line's prefix.
#
# Measured by hand on 2026-09-14, the whole library at eight threads: 7,248
# passed and exactly these two failed.

[[guard]]
name = "the usable line keeps the shape the harness parses"
file = "src/common/started.rs"
before = """        "the message list is usable: {rows} rows, {} ms after start","""
after = """        "the message list is ready: {rows} rows, {} ms after start","""
red = [
    "common::started::tests::test_the_usable_line_is_held_byte_for_byte",
    "common::started::tests::test_the_usable_line_is_said_once_per_start",
]
tests_last_seen = [
    { file = "src/common/started.rs", tests = 7 },
]

# The line is written once per process. The handler that asks for it fires on
# every load, including a folder change and a sort, and without the once a
# harness reading the newest line would take a sort for a start. The break
# spends the flag and ignores what it held, so every load with rows gets a
# line. One goes red.
#
# Measured by hand on 2026-09-14, the whole library at eight threads: 7,249
# passed and exactly this one failed.

[[guard]]
name = "the usable line is said once per process"
file = "src/common/started.rs"
before = """        if self.said_usable.swap(true, Ordering::SeqCst) {
            return None;
        }
        Some(usable_line(rows, since_start))"""
after = """        self.said_usable.swap(true, Ordering::SeqCst);
        Some(usable_line(rows, since_start))"""
red = [
    "common::started::tests::test_the_usable_line_is_said_once_per_start",
]
tests_last_seen = [
    { file = "src/common/started.rs", tests = 7 },
]

# ── The harness that takes the three numbers ────────────────────────────────
#
# The profile the measurement runs against is defined in the target's header:
# a plain-text body of about 2 KB per message, among other things. The break
# halves the body, which is what a well-meaning "make the fixture smaller" edit
# looks like, and the profile stops being the profile the page's numbers were
# taken under. One goes red: the test that opens a ten-row profile and holds
# it to the definition.
#
# Measured by hand on 2026-09-14 against the target,
# `cargo test --test the_numbers_the_targets_ask_for`: 13 passed and exactly
# this one failed.

[[guard]]
name = "the measurement profile keeps the body size its definition gives"
file = "tests/the_numbers_the_targets_ask_for.rs"
suite = "the_numbers_the_targets_ask_for"
before = """const ABOUT_TWO_KILOBYTES: usize = 2_048;"""
after = """const ABOUT_TWO_KILOBYTES: usize = 1_024;"""
red = [
    "test_the_profile_has_the_shape_the_definition_gives",
]
tests_last_seen = [
    { file = "tests/the_numbers_the_targets_ask_for.rs", tests = 16 },
]

# The parser in the harness and the line the application writes are two
# halves of one shape. The break is the same reworded line as the library
# record above, and the point of a second record is where it lands: on
# `src/common/started.rs` with this target as its suite, so a commit that
# changes the line runs the harness's parser against it. One goes red: the
# test that builds the line with the library function and parses it back.
#
# Measured by hand on 2026-09-14 against the target: 13 passed and exactly
# this one failed. The library's own answer to this break is the record
# above, two red.

[[guard]]
name = "the harness can still parse the line the application writes"
file = "src/common/started.rs"
suite = "the_numbers_the_targets_ask_for"
before = """        "the message list is usable: {rows} rows, {} ms after start","""
after = """        "the message list is ready: {rows} rows, {} ms after start","""
red = [
    "test_the_usable_line_the_library_writes_parses_back",
]
tests_last_seen = [
    { file = "src/common/started.rs", tests = 7 },
    { file = "tests/the_numbers_the_targets_ask_for.rs", tests = 16 },
]

# ── The line Settings writes, and the harness that reads it (#34) ───────────
#
# The Settings dialog writes `settings built in N ms` once per open, from the
# top of the window's `ID_SETTINGS` arm to just before `show_modal`, and
# `tests/the_settings_dialog_opens_in.rs` parses that line back off the
# release binary's log. The break rewords the line, which is what a tidy-up
# of the wording looks like, and the harness would then read nothing off a
# real log. One goes red in the library: the test that holds the line byte
# for byte.
#
# Measured on 2026-09-16 through `scripts/guards.sh --remeasure`, the whole
# library at eight threads: exactly this one failed and nothing else did.

[[guard]]
name = "the settings built line keeps the shape the harness parses"
file = "src/common/started.rs"
before = """    format!("settings built in {} ms", building.as_millis())"""
after = """    format!("settings built after {} ms", building.as_millis())"""
red = [
    "common::started::tests::test_the_settings_built_line_is_held_byte_for_byte",
]
tests_last_seen = [
    { file = "src/common/started.rs", tests = 7 },
]

# The same break, landing on the harness: this record couples
# `src/common/started.rs` to the target, so a commit that changes the line
# runs the harness's parser against it. One goes red there: the test that
# builds the line with the library function and parses it back.
#
# Measured by hand on 2026-09-16 against the target,
# `cargo test --test the_settings_dialog_opens_in`: 1 passed, 1 ignored and
# exactly this one failed. The library's own answer to this break is the
# record above.

[[guard]]
name = "the harness can still parse the settings built line the application writes"
file = "src/common/started.rs"
suite = "the_settings_dialog_opens_in"
before = """    format!("settings built in {} ms", building.as_millis())"""
after = """    format!("settings built after {} ms", building.as_millis())"""
red = [
    "test_the_settings_built_line_the_library_writes_parses_back",
]
tests_last_seen = [
    { file = "src/common/started.rs", tests = 7 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# Every figure the harness prints carries its unit. The summary and the page
# quote these lines, and a number without a unit is a number somebody guesses
# at. The break drops the unit from the figure line, which is what the first
# draft of the harness did. One goes red: the test that builds a line from
# five known runs and reads its first and median back.
#
# Measured by hand on 2026-09-16 against the target: 1 passed, 1 ignored and
# exactly this one failed.

[[guard]]
name = "a figure line the settings harness prints carries its unit"
file = "tests/the_settings_dialog_opens_in.rs"
suite = "the_settings_dialog_opens_in"
before = """        "{what}: first {first} ms, median {median} ms, the {}: {each} ms","""
after = """        "{what}: first {first}, median {median}, the {}: {each}","""
red = [
    "test_a_figure_line_carries_its_unit_and_parses_back",
]
tests_last_seen = [
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# The same rule for the line that says what the first visit of each later
# tab costs, which is where the cost of the open went. The break drops the
# unit. One goes red: the test that builds the line from two tabs, one with
# spaces in its name, and reads both back.
#
# Measured on 2026-09-17 against the target: exactly this one failed and
# nothing else did.

[[guard]]
name = "the first-visits line the settings harness prints carries its units"
file = "tests/the_settings_dialog_opens_in.rs"
suite = "the_settings_dialog_opens_in"
before = """        .map(|(name, took)| format!("{name} {} ms", took.as_millis()))"""
after = """        .map(|(name, took)| format!("{name} {}", took.as_millis()))"""
red = [
    "test_the_first_visits_line_carries_its_units_and_parses_back",
]
tests_last_seen = [
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# ── What the before rows justified (#34, 09-09) ──────────────────────────────
#
# The settings screen says which checker will check spelling, and it used to
# build a Windows checker to ask it; letting go of that checker cost about a
# fifth of a second on every open, measured on 2026-09-16. The answer is now
# decided the same way with nothing built, and the test holds the two answers
# to each other on the machine it runs on. The break names the built-in list
# where Windows checks the tag. One goes red on a machine where Windows
# checks the default language, which this one is; on a runner with no spell
# checking feature both answers are the built-in list and the break is
# invisible, which the test's own comment says.
#
# Measured on 2026-09-17 through `scripts/guards.sh --remeasure`, the whole
# library at eight threads: exactly this one failed and nothing else did.

[[guard]]
name = "the spelling source named without a checker is the one a checker would report"
file = "src/service/spellcheck/mod.rs"
before = """pub fn source_for_language(tag: &str) -> Source {
    if windows_checks(tag) {
        return Source::Windows;
    }"""
after = """pub fn source_for_language(tag: &str) -> Source {
    if windows_checks(tag) {
        return Source::Builtin;
    }"""
red = [
    "service::spellcheck::tests::test_the_source_named_without_a_checker_is_the_one_a_checker_built_would_report",
]
tests_last_seen = [
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# The same sentence, at the screen: the break puts the checker back, which is
# exactly the line that was there before 09-09. One goes red: the reading of
# `add_language_and_spelling` in the settings harness's target. The record
# couples `wx_settings.rs` to that target.
#
# Measured on 2026-09-17 against the target,
# `cargo test --test the_settings_dialog_opens_in`: exactly this one failed
# and nothing else did.

[[guard]]
name = "the spelling sentence on the settings screen is worded without building a checker"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """    let source = crate::service::spellcheck::source_for_language(&config.language);"""
after = """    let source = crate::service::spellcheck::for_language(&config.language).source();"""
red = [
    "test_the_spelling_sentence_is_worded_without_building_a_checker",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# The dialog is frozen while it is built. `wxChoice` resizes itself after
# every item it is handed unless it is frozen, and a child added under a
# frozen window is frozen with it; measured on 2026-09-16 with the parent
# window shown, the typeface list alone cost about a second unfrozen and a
# tenth of that frozen. The break takes the freeze out, which is what a
# tidy-up that saw nothing drawn before the show would do. One goes red: the
# reading that holds the freeze before the first page and the thaw after the
# layout.
#
# Measured on 2026-09-17 against the target: exactly this one failed and
# nothing else did.
#
# ── And it does not return when broken, which is what ended shard 40 ────────
#
# This is the record that cost run 35520204784 a whole shard on 2026-09-20.
# Shard 40 logged 2,637 s of measured work, printed this name, never printed a
# timing line, and the job's 360-minute cap ended it 18,978 s later with nine
# records unmeasured. It was re-dispatched on its own as run 35726150488 on
# 2026-09-22 and died the same way: twelve headers, eleven verdicts, and six
# hours inside this one.
#
# Measured here on 2026-09-22 by 12-02.1, under the per-record limit that plan
# built: "it did not return within its budget of 1800 s, and was still running
# when the budget ran out at 1800 s", the kill took the tree, and the run went
# on to the next four records and measured all of them. So the record has no
# verdict from either machine, and it is left without one rather than given a
# guessed one.
#
# The hang is itself evidence about what this guard defends. The record is
# about the dialog being frozen while its pages are built, and an unfrozen
# build repaints per control: `wxChoice` resizes itself after every item it is
# handed. The measurement of 2026-09-16 above put the typeface list alone at
# about a second unfrozen against a tenth of that frozen, over one list; the
# break takes the freeze off the whole dialog, and what happens then is not a
# slower run, it is a run nothing has been seen to finish. That is a stronger
# statement about the freeze than the reading that names it, and it is not a
# verdict, because nobody has watched this break redden anything since
# 2026-09-17.
#
# What it would take to measure it: a way to run that suite with the break in
# place and a bound on the repainting, which nothing here has. It is in
# `.planning/WINDOWS.md` as work rather than as a lowered record. Do not lower
# the record and do not guess a verdict.

[[guard]]
name = "the settings dialog is frozen while its pages are built"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """    dlg.freeze();

    let root_sizer = BoxSizer::builder(Orientation::Vertical).build();"""
after = """    let root_sizer = BoxSizer::builder(Orientation::Vertical).build();"""
red = [
    "test_the_dialog_and_each_later_page_are_built_frozen",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# The six pages after General are built when their tab is first shown, and
# that is what took the build from 2.2 seconds to what the after rows on
# docs/development/measurements.md say. The break builds every page before
# the dialog is shown, in place of the handler that builds one when its tab
# is reached, which is the shape the dialog had before 09-09. One goes red:
# the test that finds controls on the Reading page before its tab was shown.
#
# Measured on 2026-09-17 against the target: exactly this one failed and
# nothing else did.

[[guard]]
name = "a settings page after the first is built when its tab is first shown and not before"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """    notebook.on_page_changed({
        let later = Rc::clone(&later);
        move |event| {
            if let Some(reached) = event
                .get_selection()
                .and_then(|tab| usize::try_from(tab).ok())
            {
                later.build_the_page_for(reached);
            }
        }
    });"""
after = """    for tab in THE_COMPOSE_TAB..=THE_ADVANCED_TAB {
        later.build_the_page_for(tab);
    }"""
red = [
    "test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# A page nobody showed is written back as it was stored. `read_settings`
# starts from the configuration the dialog opened with and each page that
# was built overwrites its own fields; the break starts from the defaults
# instead, which is what a simplification that forgot the unbuilt pages
# would do, and every setting on a page nobody visited would then be reset by
# pressing OK (T-09-31). One goes red: the test that stores a sort order,
# never shows the Reading tab, and reads what OK would write.
#
# Measured on 2026-09-17 against the target: exactly this one failed and
# nothing else did.

[[guard]]
name = "pressing OK keeps the stored answer for a settings page nobody showed"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """pub fn read_settings(w: &SettingsWidgets, base: &AppConfig) -> AppConfig {
    let mut cfg = base.clone();"""
after = """pub fn read_settings(w: &SettingsWidgets, base: &AppConfig) -> AppConfig {
    let mut cfg = AppConfig::default();"""
red = [
    "test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# The window fills the module it opens on at startup. Found on 2026-09-14 by
# the harness waiting 60 s for a usable line that never came: every fill came
# from a module switch, a switch to the module already on screen is refused,
# so the folder tree came up empty and cached mail was not listed until a
# sync finished or somebody switched away and back. The break takes the one
# startup fill out and leaves its inputs bound, which is what a tidy-up that
# thought the switch handled it would look like. One goes red: the reading of
# the startup section of `run`.
#
# The library under the same break: `cargo test --lib -- --test-threads=8`
# on 2026-09-14, 7,250 passed and none failed, 71 s. No unit test reaches the
# startup closure, which is why the reading lives in the target and why this
# record couples `wx_app.rs` to it; the commit that made the fix ran the
# window's 199 tests and not this target, because no record said to.
#
# Measured by hand on 2026-09-14 against the target: 13 passed and exactly
# this one failed.

[[guard]]
name = "the module the window opens on is filled at startup"
file = "src/presentation/wx_app.rs"
suite = "the_numbers_the_targets_ask_for"
before = """                load_module_data(module, &message_cache, account_id, &scan_tx, showing);"""
after = """                let _ = (module, account_id, showing);"""
red = [
    "test_the_module_the_window_opens_on_is_filled_at_startup",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_numbers_the_targets_ask_for.rs", tests = 16 },
]

# ── The message list paints from memory ─────────────────────────────────────
#
# PERF-03's second line asks for a test that the virtual text callback issues
# no query. Half of that is the type: `virtual_rows::text_for` takes slices
# and copies and is handed no connection. That half cannot be a record,
# because its break is a compile error and this runner measures a break by
# which tests go red. The other half is a reading in
# `tests/the_list_reads_only_memory.rs` that refuses the function and the
# closure naming a database, and it is recordable because its break compiles.
#
# The break here is the one line a function with no connection can still
# write: a call to the cache type's constructor with an empty path, result
# discarded. Five go red on the target: the reading, which quotes the line,
# and the four companions that each require the real files to be clean before
# they break one on purpose. The library was not run under this break, on
# purpose: the planted call would open a cache at an empty path from inside
# every test that paints a cell, and where that lands is not worth finding
# out for a suite the record does not name.
#
# Measured by hand on 2026-09-14 against the target,
# `cargo test --test the_list_reads_only_memory`: 1 passed and exactly these
# five failed. The `before` rewritten on 2026-09-19 by 11-10, which made the
# function read the column's index as well as the column for the phrase it
# prefixes to the first cell, and measured again through
# `scripts/guards.sh --remeasure`, the target named.

[[guard]]
name = "the function the list paints with cannot open the cache"
file = "src/presentation/virtual_rows.rs"
suite = "the_list_reads_only_memory"
before = """) -> String {
    let Some((at, column)) = usize::try_from(column)"""
after = """) -> String {
    let _ = crate::data::message_cache::MessageCache::new(std::path::PathBuf::new(), None);
    let Some((at, column)) = usize::try_from(column)"""
red = [
    "test_the_message_list_paints_from_memory_and_its_closure_only_calls_the_function",
    "test_a_function_that_opened_the_cache_would_be_named",
    "test_a_closure_doing_its_own_work_would_be_named",
    "test_a_third_registration_would_be_counted",
    "test_the_comment_naming_the_words_as_forbidden_is_not_a_violation",
]
tests_last_seen = [
    { file = "src/presentation/virtual_rows.rs", tests = 6 },
    { file = "tests/the_list_reads_only_memory.rs", tests = 6 },
]

# The closure half. The break puts the old dispatch back inside the message
# list's closure, which compiles with one unused-import warning, so the
# closure does its own work and calls nothing in `virtual_rows`. That is what
# the reading is for: a function nothing calls holds nothing. The same five go
# red on the target, and the reading's complaint is that the closure does not
# call `text_for(`. This record is the one that couples `wx_app.rs` to the
# target, so a commit that touches the closure runs the reading.
#
# Measured by hand on 2026-09-14 against the target: 1 passed and exactly
# these five failed. The break's text rewritten 2026-09-20 (11-11.1.1, #91)
# when the closure began reading the dates from the state rather than a
# captured local, and measured again the same day.

[[guard]]
name = "the message list's closure calls the function rather than painting for itself"
file = "src/presentation/wx_app.rs"
suite = "the_list_reads_only_memory"
before = """                    let columns = column_layout.borrow().visible();
                    virtual_rows::text_for(
                        virtual_rows::Listed {
                            showing: state.showing,
                            messages: &state.messages,
                            conversations: &state.conversations,
                        },
                        &columns,
                        row,
                        column,
                        state.dates,
                        chrono::Local::now(),
                    )
                }
            });"""
after = """                    let columns = column_layout.borrow().visible();
                    let Some(c) = columns.get(column as usize).copied() else {
                        return String::new();
                    };
                    let now = chrono::Local::now();
                    match state.showing {
                        view_state::Showing::Conversations => {
                            match state.conversations.get(row as usize) {
                                Some(conversation) => message_rows::conversation_cell_text(
                                    conversation,
                                    c,
                                    state.dates,
                                    now,
                                ),
                                None => message_rows::PLACEHOLDER.to_string(),
                            }
                        }
                        view_state::Showing::Messages => match state.messages.get(row as usize) {
                            Some(message) => {
                                message_rows::cell_text(message, c, state.dates, now)
                            }
                            None => message_rows::PLACEHOLDER.to_string(),
                        },
                    }
                }
            });"""
red = [
    "test_the_message_list_paints_from_memory_and_its_closure_only_calls_the_function",
    "test_a_function_that_opened_the_cache_would_be_named",
    "test_a_closure_doing_its_own_work_would_be_named",
    "test_a_third_registration_would_be_counted",
    "test_the_comment_naming_the_words_as_forbidden_is_not_a_violation",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_list_reads_only_memory.rs", tests = 6 },
]

# ── The harness over the list at scale ──────────────────────────────────────
#
# The measurement behind `#[ignore]` prints rows for the measurements page,
# and the format-holding test at 2,000 rows runs the same code on every
# commit and holds each row to the page's six columns. The break merges two
# cells with a semicolon, which is what a tidy-up of the format string looks
# like, and a row with five cells is a row the page's reading refuses. Three
# go red: the format test over the scale rows, since 2026-09-17 the format
# test over the read-path rows, which 10-02 added, and since the same day
# the format test over the All Inboxes rows, which 10-02.1 added; all three
# walk the same `the_row`. The plan's other candidate, halving the batch
# size, reddens nothing, because chunks of any size write every row.
#
# Measured by hand on 2026-09-14 against the target,
# `cargo test --test the_list_at_two_hundred_thousand_rows`: 4 passed and
# exactly this one failed. Measured again on 2026-09-17 through
# `--remeasure` after the read-path test arrived: it came out one short, the
# new test was added here, and the run after that reddened exactly the two.
# Measured again the same day after the All Inboxes test arrived, the third
# test written here first and the run reddening exactly the three.

[[guard]]
name = "the rows the scale harness prints keep the page's six columns"
file = "tests/the_list_at_two_hundred_thousand_rows.rs"
suite = "the_list_at_two_hundred_thousand_rows"
before = """        "| {} | {} | {} | {} | {} | {} |","""
after = """        "| {} | {} | {} | {} | {}; {} |","""
red = [
    "test_every_row_the_measurement_prints_has_the_pages_shape_and_names_what_it_timed",
    "test_every_read_path_row_has_the_pages_shape_and_says_which_step_it_timed",
    "test_every_all_inboxes_row_has_the_pages_shape_and_names_its_order",
]
tests_last_seen = [
    { file = "tests/the_list_at_two_hundred_thousand_rows.rs", tests = 10 },
]

# ── Shards from two commits, read as one run ─────────────────────────────────
#
# `cargo mutants --shard k/n` divides the list the tool builds from the tree
# it runs in, so two shards taken at two commits are two lists: a mutant can
# fall between them or land in both, and their sum reads as a whole-tree run
# with nothing in it saying otherwise. Every shard writes its commit before it
# starts and the merger compares them. The check is in the reader and not in
# the shell, because the shell has no test; the break below makes the merger
# accept the two-commit example in its own docstring.
#
# Measured by hand on 2026-09-15 against `house_style` with the break
# applied: exactly this one test failed.

[[guard]]
name = "shards from two commits are not read as one run"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """        if said.commit != first.commit:"""
after = """        if said.commit != said.commit:"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# The same rule one input along. `--shard k/n` divides whatever `--file` left
# in, so two shards over two globs are two lists as surely as two commits are,
# and the merger holds every shard to the first one's glob. The break makes
# it accept the two-glob example in its own docstring.
#
# Measured by hand on 2026-09-15 against `house_style` with the break
# applied: exactly this one test failed.

[[guard]]
name = "shards over two file filters are not read as one run"
file = "scripts/mutants_report.py"
suite = "house_style"
before = """        if said.files != first.files:"""
after = """        if said.files != said.files:"""
red = [
    "test_the_mutation_report_still_obeys_its_own_examples",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A dispatched shard that fails an hour in, on a runner ────────────────────
#
# The mutation workflow can be dispatched from the Actions tab since
# 2026-09-15, one runner per shard. Its shard step hands `scripts/mutants.sh`
# flags, and a flag the script does not accept fails after the checkout, the
# toolchain and the build, which on a runner is the expensive place to find
# out and on 248 runners at once is 248 times that. The reading holds the
# step's flags to the arms of the script's own `case`. The break misspells the
# one flag whose absence would also cost a second build per shard.
#
# Measured by hand on 2026-09-15 against `house_style` with the break
# applied: exactly this one test failed.

[[guard]]
name = "a dispatched shard hands the mutation script a flag it accepts"
file = ".github/workflows/mutants.yml"
suite = "house_style"
before = """      run: scripts/mutants.sh --shard ${{ matrix.k }}/${{ inputs.shards }} --in-place ${FILE:+--file "$FILE"} -- --all-targets"""
after = """      run: scripts/mutants.sh --shard ${{ matrix.k }}/${{ inputs.shards }} --inplace ${FILE:+--file "$FILE"} -- --all-targets"""
red = [
    "test_the_mutation_workflow_dispatch_hands_the_script_flags_it_accepts",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A CI job that runs the tests on a checkout of one commit ─────────────────
#
# `actions/checkout` fetches one commit unless told otherwise, and one target
# reads the history: the share of commits before red/green is computed by
# `git merge-base` against `18a02454`, which on a one-commit checkout is not a
# valid object name. The push of `main` at `0fa393ba` on 2026-09-15 failed
# its Test Suite job on that alone, 7,263 library tests green beside it. The
# reading holds every job that runs `cargo test` to `fetch-depth: 0`, and the
# break is the depth the action would have used anyway.
#
# Measured by hand on 2026-09-15 against `house_style` with the break
# applied: exactly this one test failed.
#
# Re-anchored 2026-09-17 on the last line of the Test Suite job's own comment,
# because the Build job's checkout gained the same `fetch-depth: 0` that day
# and a break has to name one place. Re-measured through `--remeasure`.

[[guard]]
name = "a CI job that runs the tests checks out the whole history"
file = ".github/workflows/ci.yml"
suite = "house_style"
before = """      # other test green. mutants.yml already checks out this way and says why.
      uses: actions/checkout@v4
      with:
        fetch-depth: 0"""
after = """      # other test green. mutants.yml already checks out this way and says why.
      uses: actions/checkout@v4
      with:
        fetch-depth: 1"""
red = [
    "test_every_job_that_runs_the_tests_checks_out_the_whole_history",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── A CI job that runs the build script on a checkout of one commit ──────────
#
# Since 2026-09-17 `scripts/build-installer.sh` counts the commits since the
# version was set, stamps the count into the build, and refuses a checkout
# that does not hold the version's commit. The Build job runs the script on
# every push and checked out one commit until that day; the reading above was
# widened to jobs that run the script, and this is the break for that half of
# it: the Build job's checkout back to the depth the action would have used.
#
# Measured 2026-09-17 through `--remeasure` on the branch the record was
# written on, `cargo test --test house_style`.

[[guard]]
name = "the CI job that builds the installer checks out the whole history"
file = ".github/workflows/ci.yml"
suite = "house_style"
before = """      # is cheap enough to fetch on every push.
      uses: actions/checkout@v4
      with:
        fetch-depth: 0"""
after = """      # is cheap enough to fetch on every push.
      uses: actions/checkout@v4
      with:
        fetch-depth: 1"""
red = [
    "test_every_job_that_runs_the_tests_checks_out_the_whole_history",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# ── The guard sweep's shard step hands the runner a flag it accepts ──────────
#
# The sweep runs on GitHub's runners in shards since 2026-09-15, and the shard
# step of `.github/workflows/guards.yml` spells `scripts/guards.py`'s flags. A
# flag the script does not accept fails on the runner after the checkout and
# the build, which is the expensive place, so the reading in
# `tests/the_guard_sweep_runs_on_runners.rs` holds the step to the flags read
# off the script's own `add_argument` calls. The break is the misspelling the
# mutation workflow's record above uses too: one letter, and a shard job that
# would have run for nothing. A target of its own rather than two more tests
# in `house_style`, which twenty-five records name as their suite.
#
# Measured by hand on 2026-09-15 against `the_guard_sweep_runs_on_runners`
# with the break applied: exactly this one test failed, and the companion,
# which reads its own text, stayed green.

[[guard]]
name = "the guard sweep's shard step hands the runner a flag it accepts"
file = ".github/workflows/guards.yml"
suite = "the_guard_sweep_runs_on_runners"
before = """--shard ${{ matrix.k }}/${{ inputs.shards }} --log"""
after = """--shards ${{ matrix.k }}/${{ inputs.shards }} --log"""
red = [
    "test_the_guard_sweep_dispatch_hands_the_script_flags_it_accepts",
]
tests_last_seen = [
    { file = "tests/the_guard_sweep_runs_on_runners.rs", tests = 2 },
]

# ── A window holding a browser waits for the browser before it goes ──────────
#
# WebView2 makes the browser inside a `WebView` after `build()` returns, and
# wxWidgets 3.3.2 delivers the completions to the control whether or not it
# still exists (wxWidgets #26491, fixed upstream for the unreleased 3.3.4).
# A window destroyed inside that moment took the application down with exit
# code 0xc000041d and no message, and `tests/theme_reach.rs` died that way on
# every run on GitHub's runners on 2026-09-15. `presentation::browser_ready`
# is the answer, and three records hold its three halves.
#
# The first is the state machine: the browser's report is what frees what was
# waiting. The break makes a report leave the browser unready, so a wait
# queued after it never runs and one queued during the drain is lost. Three of
# the module's four tests go red; the fourth, that a second report runs
# nothing twice, is satisfied by a report that runs nothing at all.
#
# Measured by hand on 2026-09-15 against the whole library with the break
# applied: exactly these three failed.

[[guard]]
name = "the browser's report is what frees a window waiting on it"
file = "src/presentation/browser_ready.rs"
before = """            state.reported = true;"""
after = """            state.reported = false;"""
red = [
    "presentation::browser_ready::tests::test_nothing_waiting_runs_before_the_browser_reports",
    "presentation::browser_ready::tests::test_once_the_browser_has_reported_a_new_wait_runs_at_once",
    "presentation::browser_ready::tests::test_a_wait_queued_by_something_that_was_waiting_runs_too",
]
tests_last_seen = [
    { file = "src/presentation/browser_ready.rs", tests = 4 },
]

# The second is the live half: a window handed to `destroy_when_ready` before
# its browser exists is not destroyed until the browser reports. The break
# destroys it at once, which is what every caller did before 2026-09-15, and
# the child process the test spawns dies with 0xc000041d. The parent reads the
# exit status, so a crash is a failing test here and not a run that never
# finished. The reading over `wx_compose` in the same target stays green,
# because the dialogs there still go through the watch.
#
# Measured by hand on 2026-09-15 against `closing_a_window_before_its_browser_exists`
# with the break applied: exactly this one test failed.

[[guard]]
name = "a window handed over before its browser exists is not destroyed until the browser reports"
file = "src/presentation/browser_ready.rs"
suite = "closing_a_window_before_its_browser_exists"
before = """        self.when_ready(move || window.destroy());"""
after = """        window.destroy();"""
red = [
    "test_closing_a_window_before_its_browser_exists_does_not_kill_the_process",
]
tests_last_seen = [
    { file = "src/presentation/browser_ready.rs", tests = 4 },
    { file = "tests/closing_a_window_before_its_browser_exists.rs", tests = 3 },
]

# The third is the two dialogs a person can close early, Compose and Preview
# Before Send. Both are modal, so no test can drive them, and the reading
# holds each close path to the watch rather than to `destroy()`. The break
# puts the preview's direct `destroy()` back, the line as it stood before
# 2026-09-15. The live half above stays green, because it does not read
# `wx_compose` at all.
#
# Measured by hand on 2026-09-15 against `closing_a_window_before_its_browser_exists`
# with the break applied: exactly this one test failed.

[[guard]]
name = "the two dialogs holding a browser close through the watch on it"
file = "src/presentation/wx_compose.rs"
suite = "closing_a_window_before_its_browser_exists"
before = """    browser.destroy_when_ready(dlg);"""
after = """    dlg.destroy();"""
red = [
    "test_the_two_dialogs_holding_a_browser_are_closed_through_the_browser_watch",
]
tests_last_seen = [
    { file = "src/presentation/wx_compose.rs", tests = 43 },
    { file = "tests/closing_a_window_before_its_browser_exists.rs", tests = 3 },
]

# ── What the mutation run of 2026-09-15 found nothing watching ───────────────
#
# Two areas went through `scripts/mutants.sh` in shards on GitHub's runners at
# `3e633252`: the mail protocols under src/service/protocols/ and the CalDAV
# client. docs/plans/20260915-whole-tree-mutation-run.md lists every survivor.
# The records below are the ones killed by a test: each `after` is the mutant
# as the tool wrote it, so the record's break is the very change the suite
# had let through, and the test named is the one written to notice it. A test
# written to kill a mutant is a guard by definition, and the mutant is its
# break.
#
# Each was measured on 2026-09-16 on the whole library through
# `scripts/guards.sh --remeasure`, with the verdict written beside it.

[[guard]]
name = "every LIST attribute the parser knows is spelled the way IMAP spells it"
file = "src/service/protocols/imap.rs"
before = """        NameAttribute::Trash => "\\\\Trash".to_string(),
"""
after = ""
red = [
    "service::protocols::imap::tests::test_every_list_attribute_the_parser_knows_is_spelled_and_none_is_dropped",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "the session says which folder it opened"
file = "src/service/protocols/imap.rs"
before = """    pub fn selected_folder(&self) -> Option<&str> {
        self.selected.as_deref()
    }"""
after = """    pub fn selected_folder(&self) -> Option<&str> {
        None
    }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_folder_that_was_opened_is_the_one_the_session_says_is_open",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "the UIDs above one are asked for and answered from the server"
file = "src/service/protocols/imap.rs"
before = """    pub async fn uids_above(&mut self, after: u32) -> Result<Vec<u32>> {
        self.search_uids(&format!("UID {after}:*")).await
    }"""
after = """    pub async fn uids_above(&mut self, after: u32) -> Result<Vec<u32>> {
        Ok(vec![])
    }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_asking_for_the_uids_above_one_asks_the_server_for_exactly_that_range",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a folder's counts are the totals the server gave"
file = "src/service/protocols/imap.rs"
before = """    pub async fn folder_counts(&mut self, path: &str) -> Result<FolderCounts> {
        let mailbox = with_timeout("""
after = """    pub async fn folder_counts(&mut self, path: &str) -> Result<FolderCounts> {
        return Ok(Default::default());
        let mailbox = with_timeout("""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_counting_a_folder_reads_the_totals_the_server_gave",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a command that needs a folder is refused when none is open"
file = "src/service/protocols/imap.rs"
before = """    fn require_selected(&self) -> Result<()> {
        if self.selected.is_none() {
            return Err(Error::Protocol("No folder is open".into()));
        }
        Ok(())
    }"""
after = """    fn require_selected(&self) -> Result<()> {
        Ok(())
    }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_session_with_no_folder_open_refuses_a_search_rather_than_searching_nothing",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a fresh IMAP session may not change anything until told"
file = "src/service/protocols/imap.rs"
before = """    pub const fn may_change(&self) -> bool {
        self.may_change
    }"""
after = """    pub const fn may_change(&self) -> bool {
        true
    }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_fresh_session_may_not_change_anything_until_it_is_told_it_may",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a server that takes an introduction is given one"
file = "src/service/protocols/imap.rs"
before = """    async fn introduce_ourselves(&mut self) {
        if !self.abilities.id {
            return;
        }"""
after = """    async fn introduce_ourselves(&mut self) {
        if true {
            return;
        }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_server_that_takes_an_introduction_is_given_one",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "a nested folder is named by its last segment"
file = "src/service/protocols/imap.rs"
before = """                let leaf = match delimiter.as_deref().filter(|d| !d.is_empty()) {"""
after = """                let leaf = match delimiter.as_deref().filter(|d| d.is_empty()) {"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_nested_folder_is_named_by_its_last_segment",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "the highest modseq a server names on opening is heard"
file = "src/service/protocols/imap.rs"
before = """                Response::Data {
                    status: Status::Ok,
                    code: Some(ResponseCode::HighestModSeq(modseq)),
                    ..
                } => Some(WhatOpeningAFolderSaid::HighestModSeq(*modseq)),
                _ => None,"""
after = """                _ => None,"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_a_server_that_names_the_highest_modseq_on_opening_is_heard",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "the idle window stays inside what RFC 2177 allows"
file = "src/service/protocols/imap.rs"
before = """const IDLE_WINDOW: Duration = Duration::from_secs(29 * 60);"""
after = """const IDLE_WINDOW: Duration = Duration::from_secs(29 + 60);"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_the_idle_window_stays_inside_what_the_standard_allows",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "stopping a watch waits for the connection to sign out"
file = "src/service/protocols/imap.rs"
before = """    pub async fn stop(mut self) -> Result<()> {
        if let Some(stop) = self.stop.take() {
            let _ = stop.send(());
        }
        let _ = self.task.await;
        Ok(())
    }"""
after = """    pub async fn stop(mut self) -> Result<()> {
        Ok(())
    }"""
red = [
    "service::protocols::imap::against_a_server_that_answers::test_stopping_a_watch_ends_it_and_says_so",
]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

[[guard]]
name = "STAT's two numbers are read from the server"
file = "src/service/protocols/pop3.rs"
before = """    pub async fn stat(&mut self) -> Result<(usize, usize)> {
        let said = self.command("STAT", &[]).await?;"""
after = """    pub async fn stat(&mut self) -> Result<(usize, usize)> {
        return Ok((0, 0));
        let said = self.command("STAT", &[]).await?;"""
red = [
    "service::protocols::pop3::against_a_server_that_answers::test_stat_reads_the_count_and_the_bytes_the_server_gave",
]
tests_last_seen = [
    { file = "src/service/protocols/pop3.rs", tests = 10 },
]

[[guard]]
name = "a POP3 listing is read from the text block the server sends"
file = "src/service/protocols/pop3.rs"
before = """    async fn read_text_data(&mut self) -> Result<Vec<String>> {
        Ok(self"""
after = """    async fn read_text_data(&mut self) -> Result<Vec<String>> {
        return Ok(vec![]);
        Ok(self"""
red = [
    "service::protocols::pop3::against_a_server_that_answers::test_the_listing_pairs_every_message_with_its_size_and_its_identifier",
]
tests_last_seen = [
    { file = "src/service/protocols/pop3.rs", tests = 10 },
]

[[guard]]
name = "a reset is said to the server"
file = "src/service/protocols/pop3.rs"
before = """    pub async fn reset(&mut self) -> Result<()> {
        self.command("RSET", &[]).await?;
        Ok(())
    }"""
after = """    pub async fn reset(&mut self) -> Result<()> {
        Ok(())
    }"""
red = [
    "service::protocols::pop3::against_a_server_that_answers::test_reset_tells_the_server_to_undo_the_deletions",
]
tests_last_seen = [
    { file = "src/service/protocols/pop3.rs", tests = 10 },
]

[[guard]]
name = "a clock of letters is not dressed up as a time"
file = "src/service/caldav.rs"
before = """        if time.len() >= 6 && all_digits(&time[..6]) {"""
after = """        if time.len() >= 6 || all_digits(&time[..6]) {"""
red = [
    "service::caldav::tests::test_a_clock_that_is_not_digits_is_not_dressed_up_as_a_time",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a collection is told from a name that starts the same way"
file = "src/service/caldav.rs"
before = """fn names_a_collection(block: &str) -> bool {
    ["<d:collection", "<D:collection"].iter().any(|opening| {"""
after = """fn names_a_collection(block: &str) -> bool {
    return false;
    ["<d:collection", "<D:collection"].iter().any(|opening| {"""
red = [
    "service::caldav::tests::test_a_collection_is_recognised_by_its_element_and_not_by_a_name_that_starts_the_same_way",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a document whose first line is indented keeps that line"
file = "src/service/caldav.rs"
before = """            Some(carried_on) if !lines.is_empty() => {"""
after = """            Some(carried_on) if true => {"""
red = [
    "service::caldav::tests::test_a_document_whose_first_line_is_indented_keeps_that_line",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a home set answered with a plain 200 is still read"
file = "src/service/caldav.rs"
before = """        // has "the network failed" can tell them none of them.
        if !response.status().is_success() && response.status().as_u16() != 207 {"""
after = """        // has "the network failed" can tell them none of them.
        if !response.status().is_success() || response.status().as_u16() != 207 {"""
red = [
    "service::caldav::discovery_tests::test_a_home_set_answered_with_a_plain_200_is_still_read",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a report the server refuses is a refusal and not an empty calendar"
file = "src/service/caldav.rs"
before = """            .map_err(|e| Error::Network(format!("CalDAV REPORT failed: {}", e)))?;

        if !response.status().is_success() && response.status().as_u16() != 207 {"""
after = """            .map_err(|e| Error::Network(format!("CalDAV REPORT failed: {}", e)))?;

        if !response.status().is_success() && response.status().as_u16() == 207 {"""
red = [
    "service::caldav::discovery_tests::test_a_report_the_server_refuses_is_a_refusal_and_not_an_empty_calendar",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

[[guard]]
name = "a journal listing is refused unless the server answered it"
file = "src/service/caldav.rs"
before = """            .map_err(|e| Error::Network(format!("CalDAV PROPFIND failed: {}", e)))?;

        if !response.status().is_success() && response.status().as_u16() != 207 {
            let status = response.status();
            return Err(Error::Api {
                status: status.as_u16(),
                provider: CALENDAR_SERVER.to_string(),
                message: format!("PROPFIND returned {status}"),
            });
        }

        let body = response
            .text()
            .await
            .map_err(|e| Error::Network(format!("CalDAV response read error: {}", e)))?;

        let here = resolved_against("", collection_url);"""
after = """            .map_err(|e| Error::Network(format!("CalDAV PROPFIND failed: {}", e)))?;

        if response.status().is_success() && response.status().as_u16() != 207 {
            let status = response.status();
            return Err(Error::Api {
                status: status.as_u16(),
                provider: CALENDAR_SERVER.to_string(),
                message: format!("PROPFIND returned {status}"),
            });
        }

        let body = response
            .text()
            .await
            .map_err(|e| Error::Network(format!("CalDAV response read error: {}", e)))?;

        let here = resolved_against("", collection_url);"""
red = [
    "service::caldav::discovery_tests::test_a_journal_listing_answered_with_a_plain_200_is_still_read",
    "service::caldav::discovery_tests::test_a_journal_listing_the_server_refuses_says_which_status_it_refused_with",
]
tests_last_seen = [
    { file = "src/service/caldav.rs", tests = 170 },
]

# ── 09-01: the version is 1.0.0-alpha.1 ─────────────────────────────────────
#
# The test naming the step from 0.125.1 to 1.0.0-alpha.1 to 1.0.0 was green
# the day it was written, because the ordering was right already, so it has no
# red half of its own and this record is the substitute. The break moves the
# release stage to the front of the enum whose declaration order is the
# comparison order, so a prerelease sorts above the release it stages: 1.0.0
# reads as older than 1.0.0-alpha.1, and the update check would offer the
# alpha to somebody already on the release. `is_prerelease` still answers
# correctly under it, because it matches the variant by name, so what this
# breaks is the order and nothing else.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads. The first
# reading named three tests and the runner found a fourth: the offer decision
# routes through the same comparison, so a row of its table where a release
# is compared with its own prerelease turns over too. Nothing in
# src/service/update_check.rs went red, because its tests compare versions
# that differ in their numbers rather than in their stage. Written down as
# the runner reported it, the same day, and measured again to earn the
# counts below.
[[guard]]
name = "a prerelease stays below the release it stages"
file = "src/common/version.rs"
before = """enum Stage {
    Alpha,
    Beta,
    Rc,
    /// Not a prerelease at all, which is why it is last: a release is newer
    /// than every prerelease that staged it.
    Release,
}"""
after = """enum Stage {
    /// Not a prerelease at all, which is why it is last: a release is newer
    /// than every prerelease that staged it.
    Release,
    Alpha,
    Beta,
    Rc,
}"""
red = [
    "common::version::tests::test_the_step_from_the_last_0_x_version_to_1_0_0_is_ordered_both_ways",
    "common::version::tests::test_a_release_is_newer_than_the_prerelease_that_staged_it",
    "common::version::tests::test_a_published_tag_carries_a_v_and_is_still_a_version",
    "common::version::tests::test_whether_a_release_is_an_offer_depends_on_the_channel_somebody_chose",
]
tests_last_seen = [
    { file = "src/common/version.rs", tests = 27 },
]

# The half-fix rather than the absent one: the stamp is still written, still
# a version, and still the wrong one. Copying the loaded value through is what
# `save` did until 2026-09-16, so a profile named the build that created it
# and not the one that last wrote it, and a settings file sent with a bug
# report could not say which build it came from. The other test in the pair,
# a file this build created keeping its own stamp, stays green under the break
# on purpose: it holds that `load` does not rewrite the stamp, which the break
# does not touch.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads.
[[guard]]
name = "a saved settings file names the build that wrote it"
file = "src/data/config.rs"
before = """            version: env!("CARGO_PKG_VERSION").to_string(),
            ..self.app_config.clone()"""
after = """            version: self.app_config.version.clone(),
            ..self.app_config.clone()"""
red = [
    "data::config::tests::test_a_saved_settings_file_names_the_build_that_wrote_it",
]
tests_last_seen = [
    { file = "src/data/config.rs", tests = 68 },
]

# The reading in tests/installer.rs is a second copy of the script's
# arithmetic, and this is what holds it to the first: the stage numbers come
# off the script's own `case` arms, so a script whose alpha arm says 2 makes
# the reading say 1.0.0.27070 for 1.0.0-alpha.1+70 and the table test fails
# by row. Since 2026-09-17 the ordering test reads the same arms and goes red
# beside it: with alpha weighed as beta, the last alpha at its caps (38999)
# ranks above the first beta (27000). The first-alpha test stays green under
# it on purpose, because 1.0.0.27000 is still above 0.125.1.52000 by the
# first field, which is the only field that test is about.
#
# `file` is not a Rust file, so the count check skips the count for it and
# takes the counts off the red list instead, which with `suite` set resolves
# to tests/installer.rs.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, `cargo test --test installer`; re-measured 2026-09-17 when the
# table test was rewritten for the counter and the ordering test arrived.
[[guard]]
name = "the installer's alpha arm is the first stage"
file = "scripts/build-installer.sh"
suite = "installer"
before = '  *-alpha.*) stage=1 ;;'
after = '  *-alpha.*) stage=2 ;;'
red = [
    "test_the_four_field_version_follows_the_scripts_own_table",
    "test_a_later_build_orders_above_an_earlier_one_and_below_the_next_stage",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# ── The build counter reaches the file version ───────────────────────────────
#
# Since 2026-09-17 the fourth field is `stage * 13000 + step * 1000 +
# counter`, the counter being the commits since the version was set, so Apps
# and Features orders two builds of one version the way the string does. The
# reading takes the expression off the script, and this break drops the
# counter from it: the reading then names the expression as missing, and the
# three tests that go through it (the table, the ordering and the first-alpha
# test) all fail on that sentence. The reader's companion reads a snippet of
# its own and stays green.
#
# Measured 2026-09-17 through `--remeasure` on the branch the record was
# written on, `cargo test --test installer`.
[[guard]]
name = "the build counter reaches the Windows file version"
file = "scripts/build-installer.sh"
suite = "installer"
before = 'VERSION_INFO="${major:-0}.${minor:-0}.${patch:-0}.$((stage * 13000 + step * 1000 + counter))"'
after = 'VERSION_INFO="${major:-0}.${minor:-0}.${patch:-0}.$((stage * 13000 + step * 1000))"'
red = [
    "test_the_four_field_version_follows_the_scripts_own_table",
    "test_a_later_build_orders_above_an_earlier_one_and_below_the_next_stage",
    "test_the_first_alpha_of_1_0_0_sits_above_the_last_0_x_version_the_way_windows_orders_it",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# ── A clone that cannot count is refused ─────────────────────────────────────
#
# The refusal is the `exit 1` inside the branch for an empty VERSION_SET_AT;
# without it the two sentences print and the build goes on, stamping a build
# that cannot say its order, which is what the counter exists to prevent.
# The installer test in house_style reads the branch and asks for the exit.
#
# Measured 2026-09-17 through `--remeasure` on the branch the record was
# written on, `cargo test --test house_style`.
[[guard]]
name = "a clone without the commit that set the version is refused a build"
file = "scripts/build-installer.sh"
suite = "house_style"
before = '''  echo "A shallow clone has no history to search; fetch the whole history and build again." >&2
  exit 1
fi'''
after = '''  echo "A shallow clone has no history to search; fetch the whole history and build again." >&2
fi'''
red = [
    "test_the_installer_says_how_far_back_the_version_was_set",
]
tests_last_seen = [
    { file = "tests/house_style.rs", tests = 74 },
]

# The form no longer offering the level, in the shape that would really
# happen: the line is still there and spelled wrong, so the dispatch form
# offers a word the step does not branch on and cargo-release does not know.
# Without the level, from a tree at 1.0.0-alpha.1 the workflow can publish
# -alpha.2 and never -alpha.1. The companion beside the test splices the real
# line out and is the same absence taken the other way.
#
# `file` is not a Rust file, so the count check skips the count for it and
# takes the counts off the red list instead, which with `suite` set resolves
# to tests/installer.rs.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, `cargo test --test installer`.
[[guard]]
name = "the release workflow offers a level that publishes the version as it stands"
file = ".github/workflows/release.yml"
suite = "installer"
before = '          - as-is'
after = '          - as_is'
red = [
    "test_the_release_workflow_can_publish_the_version_the_tree_already_carries",
]
tests_last_seen = [
    { file = "tests/installer.rs", tests = 24 },
]

# The middle arm of `language_to_use`, which is the whole of the fix for #21:
# a bare stored "en" on a machine set to English (United States) resolves to
# en-US rather than to the first English Windows lists, which is the
# Caribbean. The break is the shape the old code had: the machine's own
# region is found and the first of the family is taken anyway. The exact
# stored tag, the machine set to another language, and a language nothing
# offers all stay green under it, because none of them reaches this arm.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads.
[[guard]]
name = "a bare stored language resolves to the region this machine is set to, not to the first Windows lists"
file = "src/service/spellcheck/mod.rs"
before = """        return Some(own_region);"""
after = """        return first_available_in_the_family_of(&stored, offered);"""
red = [
    "service::spellcheck::tests::test_a_bare_stored_language_resolves_to_the_region_this_machine_is_set_to",
    "service::spellcheck::tests::test_a_bare_stored_language_follows_the_machine_to_whichever_region_it_has",
    "service::spellcheck::tests::test_the_stored_tag_and_the_machines_tag_are_matched_without_regard_to_case",
    "service::spellcheck::tests::test_a_bare_language_is_checked_in_the_machines_own_region_rather_than_windows_first",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 42 and a
    # measurement here that found the same one. The settings screen's row for
    # a bare stored tag is the resolver's answer, in a file this record had
    # never named.
    "presentation::which_language_row::tests::test_a_bare_tag_is_resolved_to_the_row_the_checker_would_use",
]
tests_last_seen = [
    { file = "src/presentation/which_language_row.rs", tests = 6 },
    { file = "src/service/spellcheck/mod.rs", tests = 64 },
]

# The settings screen's language row, put back the way it was before
# 2026-09-16: the screen selects row 0 whatever is stored, which on a machine
# set to English (United States) is English (Caribbean) in Windows' order,
# and what the tester saw for a profile from before 2026-09-03 storing a bare
# "en" (#21). The break drops the rule's answer and takes row 0, so all three
# of the target's cases go red inside its one test: the bare "en", the chosen
# en-AU, and the tag nothing offers; on a runner with no spell checking the
# built-in list has "en" at row 0 and the other two still redden. The rule
# itself, `presentation::which_language_row`, has a record of its own on the
# library; this one holds the screen to asking it.
#
# `suite` names the target that builds the screen, which couples the target
# to this file for `check.sh --suites-for`, and `wx_settings.rs` holds no
# unit tests of its own.
#
# Rewritten 2026-09-18 by 11-01, whose call replaced the line the old break
# sat on; measured that day through `--remeasure` on the branch the record
# was rewritten on, `cargo test --test the_language_the_screen_shows_is_the_one_used`.
[[guard]]
name = "the settings screen shows a stored language nothing offers as itself, not as row 0"
file = "src/presentation/wx_settings.rs"
suite = "the_language_the_screen_shows_is_the_one_used"
before = """    match which_row_shows(stored, system_language().as_deref(), &rows) {"""
after = """    let _ = which_row_shows(stored, system_language().as_deref(), &rows);
    match RowToShow::Existing(0) {"""
red = [
    "test_the_language_the_screen_shows_is_the_one_the_checker_uses",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_language_the_screen_shows_is_the_one_used.rs", tests = 1 },
]

# The snippet of an HTML-only body taken from the markup as it is rather than
# through the reader the message goes through, which is the shape of #32 one
# step cruder: the stylesheet, the script and the tags themselves all reach
# the column the list reads aloud on every row. The pass that puts stored
# snippets right derives through the same function, so its rows come out
# wrong the same way and its test reddens with the save's. So does the index
# test, which the first draft of this list did not name and the runner did:
# the index row carries the snippet column, and a snippet holding the
# stylesheet is findable by `padding` whatever the body column holds.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads. The break
# rewritten 2026-09-19 by 11-09.2, when the snippet started reading the
# reader's pieces as lines rather than its words: the markup as it is is
# now one paragraph piece holding the whole of it. Measured again that day
# through `--remeasure` on the branch, the whole library.
[[guard]]
name = "a snippet of an HTML-only body is its words through the reader, not the markup as it is"
file = "src/data/message_cache/bodies.rs"
before = """                .map(long_text::pieces_of_markup)"""
after = """                .map(|html| vec![long_text::Piece::Paragraph(html.to_string())])"""
red = [
    "data::message_cache::bodies::tests::test_a_snippet_of_an_html_only_body_is_its_words_and_not_its_stylesheet",
    "data::message_cache::bodies::tests::test_a_snippet_of_a_heading_and_a_list_holds_the_words_and_no_markers",
    "data::message_cache::bodies::tests::test_an_html_only_body_still_gives_a_snippet",
    "data::message_cache::bodies::tests::test_stored_snippets_are_put_right_once_index_included_plain_bodies_too",
    "data::message_cache::searching::finding_things::test_an_html_only_body_is_indexed_by_its_words_and_not_by_its_stylesheet",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "src/data/message_cache/searching.rs", tests = 26 },
]

# The index row for an HTML-only body taking the markup as it is, so a search
# for a word inside a stylesheet finds every newsletter, which is the second
# half of #32 and the one the issue did not name. The pass that puts stored
# snippets right rebuilds each row's index entry through this arm, so its
# search for `padding` finds the newsletter again under the break.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads.
[[guard]]
name = "the index takes an HTML-only body as its words through the reader, not as the markup"
file = "src/data/message_cache/searching.rs"
before = """                (None, Some(html)) => crate::application::long_text::words_of_markup(&html),"""
after = """                (None, Some(html)) => html,"""
red = [
    "data::message_cache::searching::finding_things::test_an_html_only_body_is_indexed_by_its_words_and_not_by_its_stylesheet",
    "data::message_cache::bodies::tests::test_stored_snippets_are_put_right_once_index_included_plain_bodies_too",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "src/data/message_cache/searching.rs", tests = 26 },
]

# The pass recording itself as done under a name nothing asks about, which is
# the marker never written in the shape a real slip would take: the second
# open reads every HTML-only body again, and the test that plants a second
# wrong row after the first pass finds it put right when it should have been
# left alone. The first pass's own test stays green, because a first pass
# does not read the marker.
#
# Measured 2026-09-16 through `--remeasure` on the branch the record was
# written on, the whole library at the default eight threads. The marker's
# name changed on 2026-09-19 with 11-09.2, which widened the pass to every
# stored body under a name of its own; the break is the same and the record
# was measured again that day through `--remeasure` on the branch.
[[guard]]
name = "the snippet pass records itself as done, not under a name nothing asks about"
file = "src/data/message_cache/bodies.rs"
before = """                rusqlite::params![SNIPPETS_ARE_THE_FIRST_RELEVANT_WORDS, now()],"""
after = """                rusqlite::params!["never", now()],"""
red = [
    "data::message_cache::bodies::tests::test_snippets_are_put_right_once_and_a_second_call_reads_no_body",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
]

# Undo Send taken off the Edit menu, which is the shape the tester met on
# 2026-09-15 (#44): the one undo this program has was on Tools, between the
# address book commands and Flush Outbox, and Edit opened on Cut. The break
# removes the item and the separator after it, so the chain still builds and
# the first item on Edit is Cut again. The reading goes red on that, and both
# companions go red with it, because each plants by finding the item's call
# and there is no call to find.
#
# `suite` names the target that reads the menu chains, which couples the
# target to this file for `check.sh --suites-for`; `tests/wired.rs` holds the
# item to its handler by id and says nothing about the menu.
#
# Measured 2026-09-16 by hand on the branch the record was written on,
# `cargo test --test undo_send_is_where_somebody_looks`.
[[guard]]
name = "Undo Send is first on the Edit menu, not off it and back where the tester found it"
file = "src/presentation/wx_app.rs"
suite = "undo_send_is_where_somebody_looks"
before = '''            .append_item(
                ID_UNDO_SEND,
                "&Undo Send\tCtrl+Shift+Z",
                "Take back the message you just sent, while it is still being held",
            )
            .append_separator()
            // The four every Windows program has.'''
after = '''            // The four every Windows program has.'''
red = [
    "test_undo_send_is_the_first_item_on_the_edit_menu_and_not_on_tools",
    "test_the_reading_complains_when_undo_send_is_put_back_on_tools",
    "test_the_reading_complains_when_undo_send_is_offered_on_both_menus",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/undo_send_is_where_somebody_looks.rs", tests = 7 },
]

# The composer's schedule routine described as reached by Alt+E, which is what
# its doc comment said from the day the key was Alt+H until #56 read it. A
# comment is where the next person learns which key to test, so a wrong one
# sends them to a key that does nothing. The break puts the stale key back;
# the reading goes red on it, and the companion goes red because its plant of
# the same stale key then changes nothing.
#
# `suite` names the target that reads the comment, which couples the target
# to this file, so the reading runs on the commits that could edit the comment
# and not only on the ones that edit the reading.
#
# Measured 2026-09-16 by hand on the branch the record was written on,
# `cargo test --test undo_send_is_where_somebody_looks`.
[[guard]]
name = "the schedule routine's comment names Alt+H, the key that reaches it, not Alt+E"
file = "src/presentation/wx_compose.rs"
suite = "undo_send_is_where_somebody_looks"
before = """/// One routine for both ways in, the toolbar button and Alt+H out of the"""
after = """/// One routine for both ways in, the toolbar button and Alt+E out of the"""
red = [
    "test_the_schedule_routines_comment_names_the_key_that_reaches_it",
    "test_the_reading_complains_when_the_comment_names_a_key_that_does_nothing",
]
tests_last_seen = [
    { file = "src/presentation/wx_compose.rs", tests = 43 },
    { file = "tests/undo_send_is_where_somebody_looks.rs", tests = 7 },
]

# A queued meeting answer worded as delivered, which is the sentence the
# tester heard on 2026-09-15 (#56): "Ada Lovelace has been told", said at
# once for an answer that sat in the outbox for ten seconds like any other
# message. The break puts that ending back in place of the composer's own
# sentence for the same queue row, so the three endings a queued answer can
# have, the countdown, "Sending to ...", and the offline sentence, all go
# red; the refused answer's sentence is untouched and stays green.
#
# Measured 2026-09-16 by hand on the branch the record was written on, the
# whole library at the default eight threads.
[[guard]]
name = "a queued meeting answer says what the composer's Send says, not that the organiser has been told"
file = "src/application/answering.rs"
before = """                crate::application::sending_later::what_send_did(
                    *goes,
                    waiting_on,
                    now,
                    how_to_say(&self.organiser)
                )"""
after = """                format!("{} has been told.", how_to_say(&self.organiser))"""
red = [
    "application::answering::tests::test_a_held_answer_says_which_answer_went_and_the_countdown_every_send_says",
    "application::answering::tests::test_an_answer_with_the_hold_off_says_it_is_sending_and_not_that_anybody_has_heard",
    "application::answering::tests::test_an_answer_queued_while_offline_says_it_waits_for_a_network",
]
tests_last_seen = [
    { file = "src/application/answering.rs", tests = 37 },
]

# A queued meeting answer left off the calendar, which is what the filing
# rule did with any variant but the retired `Sent`: every answer is queued
# now, so the break files only an answer that never left and files nothing
# that is on its way. The held-answer test goes red on that, and so does
# every test that answers through the fixture, because the fixture queues
# under the default hold as pressing the button does; the never-left test
# goes red the other way, because the break files what it refuses.
#
# Measured 2026-09-16 by hand on the branch the record was written on, the
# whole library at the default eight threads.
[[guard]]
name = "a queued meeting answer is filed on the calendar while it waits, not left off it"
file = "src/application/answered_meetings.rs"
before = """    if matches!(how_it_went, HowItWent::DidNotSend { .. }) {"""
after = """    if !matches!(how_it_went, HowItWent::DidNotSend { .. }) {"""
red = [
    "application::answered_meetings::tests::test_a_held_answer_is_filed_on_the_calendar_while_it_waits_to_go",
    "application::answered_meetings::tests::test_a_change_to_a_meeting_already_accepted_is_answered_as_a_change_to_it",
    "application::answered_meetings::tests::test_a_meeting_named_after_one_on_another_accounts_calendar_leaves_that_one_alone",
    "application::answered_meetings::tests::test_a_meeting_of_whole_days_keeps_its_dates_rather_than_gaining_a_clock_reading",
    "application::answered_meetings::tests::test_accepting_puts_the_meeting_on_the_calendar_taking_up_its_time",
    "application::answered_meetings::tests::test_an_answer_that_never_left_the_machine_is_not_filed_as_though_it_had",
    "application::answered_meetings::tests::test_an_invitation_older_than_the_one_answered_does_not_move_the_meeting_back",
    "application::answered_meetings::tests::test_an_invitation_that_names_no_end_is_stored_with_one_rather_than_a_blank",
    "application::answered_meetings::tests::test_answering_the_same_meeting_three_times_leaves_one_entry",
    "application::answered_meetings::tests::test_answering_the_same_version_again_writes_nothing_at_all",
    "application::answered_meetings::tests::test_each_answer_decides_whether_the_time_is_taken_and_says_it_in_the_stored_word",
    "application::answered_meetings::tests::test_the_entry_is_marked_as_a_change_this_computer_has_made",
    "application::answered_meetings::tests::test_the_meeting_is_filed_under_a_calendar_made_for_an_account_that_has_none",
    "application::answered_meetings::tests::test_the_meetings_own_status_is_not_the_guests_answer",
    "application::answered_meetings::tests::test_the_version_answered_is_written_against_the_row_the_calendar_already_holds",
]
tests_last_seen = [
    { file = "src/application/answered_meetings.rs", tests = 17 },
]

# Answering a meeting with the hold off told "Sending to ..." and handed to
# nothing, which is the shape the handler had until #56: the send loop runs
# on a clock only for rows carrying a moment, so a row with nothing on it
# waited for the next Send of anything. The break takes the flush out and
# leaves the question, so the reading goes red on the missing flush and its
# companion goes red because its plant of the same removal then changes
# nothing.
#
# `suite` names the target that reads the handler, which couples the target
# to this file for `check.sh --suites-for`.
#
# Measured 2026-09-16 by hand on the branch the record was written on,
# `cargo test --test undo_send_is_where_somebody_looks`.
[[guard]]
name = "answering a meeting with the hold off hands the answer to the server, not to the next Send of anything"
file = "src/presentation/wx_app.rs"
suite = "undo_send_is_where_somebody_looks"
before = """    } = &went
    {
        flush_outbox(app);
    }"""
after = """    } = &went
    {
    }"""
red = [
    "test_answering_a_meeting_with_the_hold_off_hands_the_answer_to_the_server_as_send_does",
    "test_the_reading_complains_when_the_answer_is_no_longer_flushed",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/undo_send_is_where_somebody_looks.rs", tests = 7 },
]

# "Then by" was built into Dates and Times at the bottom of the Reading tab,
# so the tester heard it straight after "Write the month as", with the
# folders and reading sections between it and "Default sort order" (#36). The
# break plants that neighbour back: a static labelled "Write the month as:"
# built straight before "Then by", so it is no longer the tab stop after the
# sort and the month setting is behind it again. Not the plan's break, which
# was to build it into the date section again: `date_sec` is declared well
# below the new site, in the Dates and Times section, so that edit does not
# compile as one replacement.
#
# `suite` names the target that builds the dialog and walks the panel's
# children, which couples the target to this file for `check.sh --suites-for`.
#
# Measured 2026-09-16 by hand on the branch the record was written on,
# `cargo test --test the_sort_controls_sit_together`.
[[guard]]
name = "Then by is the tab stop after Default sort order, not after Write the month as"
file = "src/presentation/wx_settings.rs"
suite = "the_sort_controls_sit_together"
before = """    let sort_then = labelled_choice(
        panel,
        &list_sec,"""
after = """    let _planted = StaticText::builder(panel)
        .with_label("Write the month as:")
        .build();
    let sort_then = labelled_choice(
        panel,
        &list_sec,"""
red = [
    "test_then_by_is_the_tab_stop_after_default_sort_order",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_sort_controls_sit_together.rs", tests = 1 },
]

# The Sort submenu was seven radio items in four runs with a separator
# between each run, and wxWidgets ends a radio group at a separator and
# checks the first item of each group as it is made, so the menu showed four
# ticks before anybody had sorted anything (#39). The break puts one of the
# three separators back, after Date (Oldest First), which splits the group
# in two again. The companion that plants a separator there stays green,
# because it plants a second and the complaint is the same.
#
# `suite` names the target that reads the chain, which couples the target to
# this file for `check.sh --suites-for`.
#
# Measured 2026-09-16 by hand on the branch the record was written on,
# `cargo test --test one_sort_is_checked`.
[[guard]]
name = "the seven sort items are one radio group, not four with a tick each"
file = "src/presentation/wx_app.rs"
suite = "one_sort_is_checked"
before = """            .append_radio_item(
                ID_SORT_SENDER_AZ,"""
after = """            .append_separator()
            .append_radio_item(
                ID_SORT_SENDER_AZ,"""
red = [
    "test_the_seven_sort_items_are_one_radio_group",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/one_sort_is_checked.rs", tests = 5 },
]

# ── 09-05: the five editors reached, every checkbox named ───────────────────
#
# Task 1. Five editors that open only from inside a manager became scan
# targets on 2026-09-16 (#42, #40): the manager targets stopped at Add, so
# the scan had never looked at the windows two testers met an unnamed
# checkbox in. The break is the shape of forgetting the workflow for one of
# them, the same shape 06-06's record takes for which-days: the variant
# stays, the program opens the editor on request, and the workflow never
# asks for it, so it is never scanned and nothing says so.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh` on the whole library at eight threads.
[[guard]]
name = "an editor the program can open for the scan is one the workflow asks for"
file = ".github/workflows/accessibility.yml"
before = """          'contact-editor', 'condition-editor', 'filter-editor', 'signature-editor', 'account-editor'"""
after = """          'contact-editor', 'condition-editor', 'filter-editor', 'account-editor'"""
red = [
    "presentation::scan_target::tests::test_the_workflow_asks_for_every_target",
]
tests_last_seen = [
    { file = "src/presentation/scan_target.rs", tests = 11 },
]

# The other half of the same coupling: the name the workflow asks for has to
# be the name the program answers to. The break misspells one editor's
# command-line name in `as_name`, so `--scan-target signature-editor` is
# refused as unknown while the workflow goes on asking for it. Two go red:
# the list of windows a fresh profile can reach no longer finds the name,
# and the workflow's list no longer matches the program's.
#
# Measured 2026-09-16 the same way as the record above.
[[guard]]
name = "the name the scan asks an editor by is the name the program answers to"
file = "src/presentation/scan_target.rs"
before = """            Self::SignatureEditor => "signature-editor","""
after = """            Self::SignatureEditor => "signatures-editor","""
red = [
    "presentation::scan_target::tests::test_every_window_a_fresh_profile_can_reach_has_a_name",
    "presentation::scan_target::tests::test_the_workflow_asks_for_every_target",
]
tests_last_seen = [
    { file = "src/presentation/scan_target.rs", tests = 11 },
]

# Task 2. Four of the five checkboxes two testers reported are built by one
# helper now, `add_checkbox`, which names the box outright and leaves the
# label cell to a sizer spacer. The break is the simplification somebody
# reaches for on reading that a checkbox carries its own label: the naming
# call taken out of the helper, on the reasoning that Windows names the box
# anyway. It does, on UI Automation; on the channel NVDA reads the name is
# then the framework's fallback rather than one this code set, which is the
# state the tester met. `suite` names the target that builds the five
# editors and asks each checkbox for its accessible object, which couples
# the target to this file for `check.sh --suites-for`.
#
# Measured 2026-09-16 on the branch the record was written on,
# `cargo test --test checkbox_labels`: four rows in the one complaint.
[[guard]]
name = "a checkbox in an editor grid is named outright, not left to the fallback"
file = "src/presentation/wx_managers.rs"
suite = "checkbox_labels"
before = """    let check = CheckBox::builder(parent).with_label(label).build();
    set_accessible_name(&check, &name_from_label(label));
    leave_the_cell_empty(sizer);"""
after = """    let check = CheckBox::builder(parent).with_label(label).build();
    leave_the_cell_empty(sizer);"""
red = [
    "test_every_check_box_in_a_form_carries_its_own_label",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/checkbox_labels.rs", tests = 1 },
]

# The other half of the same rule, on the box the tester reported on the
# contact editor's Basic tab (#40, point 5). The break puts the spacer back
# as it was: an empty static text built straight before the Favourite box
# to hold the label column open, which is a nameless window in the tree and
# the one Windows picks when it names what follows from the nearest static.
# The naming call stays, so only the spacer reading goes red.
#
# Measured 2026-09-16 the same way as the record above: one row in the one
# complaint.
[[guard]]
name = "no empty static text is built before the Favourite box as a spacer"
file = "src/presentation/wx_managers.rs"
suite = "checkbox_labels"
before = """    let fav_label = "&Favorite";
    let fav_check = CheckBox::builder(&basic_panel)"""
after = """    let fav_label = "&Favorite";
    let fav_spacer = StaticText::builder(&basic_panel).with_label("").build();
    basic_fields.add(&fav_spacer, 0, SizerFlag::All, 4);
    let fav_check = CheckBox::builder(&basic_panel)"""
red = [
    "test_every_check_box_in_a_form_carries_its_own_label",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/checkbox_labels.rs", tests = 1 },
]

# Task 3. The same spacer put back, read from the source rather than from
# the built tree: the whole-tree check now refuses an empty static text that
# is placed in a sizer and never touched again, in any file under src/, so
# the shape cannot come back in a window no built-tree test covers. Read on
# the tree as it stood at 1d934e26, before task 2, the reading refused
# exactly the five spacers in this file the tester's two boxes were among,
# and none of the eleven lines something fills later; it does not see a
# spacer handed on in a tuple, which is how the account editor built its
# six, and its module comment says so. `suite` names the target, which
# couples it to this file for `check.sh --suites-for`; the target is in the
# whole-tree list as well, so the script answers with the list and not with
# the name (ledger 442), and the coupling was shown with a copy of the
# script that had the target taken out of that list.
#
# Measured 2026-09-16 on the branch the record was written on,
# `cargo test --test no_label_is_only_a_space`.
[[guard]]
name = "no empty static text nothing fills is placed in a sizer, read from the source"
file = "src/presentation/wx_managers.rs"
suite = "no_label_is_only_a_space"
before = """    let fav_label = "&Favorite";
    let fav_check = CheckBox::builder(&basic_panel)"""
after = """    let fav_label = "&Favorite";
    let fav_spacer = StaticText::builder(&basic_panel).with_label("").build();
    basic_fields.add(&fav_spacer, 0, SizerFlag::All, 4);
    let fav_check = CheckBox::builder(&basic_panel)"""
red = [
    "test_no_empty_static_is_built_that_nothing_fills",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/no_label_is_only_a_space.rs", tests = 7 },
]

# The Settings tab row answers its own arrow keys (#33). The tester heard the
# reached tab spoken twice; `scripts/uia-events.ps1` on 2026-09-16 showed the
# native tab control's own arrow handler raising EVENT_OBJECT_FOCUS twice on
# the same tab per key, and the selection moved through TCM_SETCURSEL raising
# it once. `answer_the_arrows_on` takes the arrow on the notebook and moves
# the selection through wxWidgets' SetSelection, which goes that way. The
# break takes the one call that installs it, so the control's own handler
# runs again and the built dialog raises two focus events for one real
# WM_KEYDOWN, which is what the target counts with an in-context win-event
# hook. `suite` names the target, which couples it to this file for
# `check.sh --suites-for`; the file has no unit tests of its own.
#
# Measured 2026-09-16 on the branch the record was written on,
# `cargo test --test the_settings_tab_row_says_each_tab_once`.
[[guard]]
name = "the Settings tab row answers its own arrow keys, so a tab is raised once"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_tab_row_says_each_tab_once"
before = """    let notebook = Notebook::builder(&dlg).build();
    answer_the_arrows_on(&notebook);"""
after = """    let notebook = Notebook::builder(&dlg).build();"""
red = [
    "test_one_arrow_on_the_settings_tab_row_raises_one_focus_event",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_tab_row_says_each_tab_once.rs", tests = 3 },
]

# ── What a message shows and says, decided once (#51) ────────────────────────
#
# Two of six surfaces offered a PGP message to the key and said what an
# envelope and a signature were worth; the default reader, both conversation
# readings and the preview did not. `application::reading_a_message` is the
# one composition every surface now asks. Two things about it are worth a
# record because both were right on the two working surfaces only by each
# carrying a comment.
#
# The opening: the armour is offered to the key and the body handed on is the
# words where it opened. The break keeps the armour as the body, which is the
# state every missing surface was in, and the reading against the message
# GnuPG encrypted to Alice is what reddens.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "a message the key opens is shown as its words and not as its armour"
file = "src/application/reading_a_message.rs"
before = """    let body = opening_pgp::the_body_to_show(body, opened.as_ref());"""
after = """    // the armour stays where it was"""
red = [
    "application::reading_a_message::tests::test_a_message_encrypted_to_the_imported_key_is_shown_as_its_words",
]
tests_last_seen = [
    { file = "src/application/reading_a_message.rs", tests = 7 },
]

# The order of the fold: a signature verdict puts "More about this signature:"
# into the bar and the reader speaks only what is above it, so the envelope
# goes in first. The text reader and Shift+Space each carried that as a
# comment; `with_what_is_said` carries it as code, once. The break folds the
# signature before the envelope, and the reading holds the envelope sentence
# inside the spoken cut.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "the envelope is folded in before the signature, so it is spoken"
file = "src/presentation/reader_text.rs"
before = """        self.with_pgp(said.opened.as_ref())
            .with_smime_envelope(&said.envelope)
            .with_signature(&said.signature)"""
after = """        self.with_pgp(said.opened.as_ref())
            .with_signature(&said.signature)
            .with_smime_envelope(&said.envelope)"""
red = [
    "application::reading_a_message::tests::test_the_envelope_is_folded_in_before_the_signature_so_it_is_spoken",
    # Two since the same day's task 2: the page's composer folds through the
    # same function, and its reading holds the same order. Written when the
    # record was a few hours old, which is the shape CLAUDE.md warns of.
    "presentation::reader_text::encryption_tests::test_opening_one_message_as_a_page_folds_what_is_said_in_order",
]
tests_last_seen = [
    { file = "src/application/reading_a_message.rs", tests = 7 },
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# The preview pane's bar. The reader window and the conversation window put
# the bar in a control above the page; the preview is one WebView and had no
# bar at all, so a signed message previewed as unsigned and a PGP message as
# raw armour with nothing said. `preview_html` composes the document the text
# reader would show, keeps the top of its bar, and renders it into the page
# as a region. The break renders the page with no bar, which is what the
# preview did; both preview readings redden, because for one message the
# reason a PGP message did not open is in the bar and nowhere else.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "the preview pane carries the bar above the message"
file = "src/presentation/reader_text.rs"
before = """    HtmlRenderer::new().render_thread_under_a_bar(bar.as_deref(), subject, &thread_parts(parts))"""
after = """    HtmlRenderer::new().render_thread_under_a_bar(None, subject, &thread_parts(parts))"""
red = [
    "presentation::reader_text::encryption_tests::test_the_preview_carries_the_bar_above_the_message",
    "presentation::reader_text::encryption_tests::test_the_preview_says_why_a_pgp_message_did_not_open",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# A finding about one message of several, said where that message begins.
# One bar over a thread would be heard as covering every message in it, so
# the bar stays off a thread and each message's own findings go under its
# own heading, on both conversation surfaces: the whole thread in the text
# reader and the page as headings. The break drops the reason a PGP message
# did not open from what is said there, so a thread holding one goes back to
# showing its armour with nothing said, and both thread readings redden.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "one message of several says why it did not open where it begins"
file = "src/presentation/reader_text.rs"
before = """    let reason = the_reason_it_did_not_open(part.said.opened.as_ref());"""
after = """    let reason: Option<&str> = None;"""
red = [
    "presentation::reader_text::encryption_tests::test_a_whole_conversation_says_where_a_message_did_not_open_and_heads_it_with_no_verdict",
    "presentation::reader_text::encryption_tests::test_a_page_of_several_messages_says_where_one_did_not_open",
]
tests_last_seen = [
    { file = "src/presentation/reader_text.rs", tests = 125 },
]

# The guard that names every surface. It named two of six until 2026-09-16,
# which is how four surfaces came to bypass the key while it stayed green
# (#51); it reads a table of six now, each with the call its function has to
# make. The break takes the composition out of the preview's function and
# hands it the answer for nothing said, which is the bypass the preview had,
# and the guard names the preview pane. Its companion, which splices the
# same bypass in memory, goes red with it: it reads the tree first and
# refuses to splice over a real bypass, because then it could not tell its
# own break from the tree's. Written down as "unmoved" when the record was
# first drafted and corrected by the first measurement. `suite` names the
# target, which couples it to this file for `check.sh --suites-for`.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test wired`.
[[guard]]
name = "a surface that shows a message without asking the composition is named"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """    let shown = what_a_message_shows_and_says(cache, &message, body.clone());"""
after = """    let shown = crate::application::reading_a_message::WhatAMessageShowsAndSays {
        body: body.clone(),
        said: crate::application::reading_a_message::WhatIsSaidAboutIt::nothing(),
    };"""
red = [
    "test_opening_a_message_tries_the_pgp_key_and_says_why_it_did_not_open",
    "test_a_surface_that_stopped_asking_the_composition_is_named",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# Mail out of an Outlook data file goes through the one place that files an
# imported message, which is the one place that puts the marker on a row
# this program filed itself. Without the marker the next check for mail sees
# a message the server no longer has and takes it away, with the only copy
# of its text. The break files the row through the sync's own writer
# instead, which is exactly the row a sync would then delete, and the
# reading that asks the row for its marker reddens. Nothing else does: the
# row is there, the folder is there, and a second import still finds it.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "mail out of a data file is filed with the marker the sync leaves alone"
file = "src/application/importing_an_outlook_data_file.rs"
before = """            counted.count_one_written(file_one_imported_message(cache, message, folder_id));"""
after = """            let row = crate::application::filing::a_row_filed_here(
                &message.message,
                folder_id,
                1,
                0,
                crate::application::filing::AlreadyRead::No,
                "2026-09-17T00:00:00Z",
            );
            let _ = cache.upsert_message(&row);
            counted.count_one_written(WhetherItWasWrittenDown::ItIsInTheFolder);"""
red = [
    "application::importing_an_outlook_data_file::tests::test_mail_out_of_the_data_file_lands_under_imported_with_the_marker",
]
tests_last_seen = [
    { file = "src/application/importing_an_outlook_data_file.rs", tests = 8 },
]

# Every kind of thing out of a data file moves exactly one count, so the
# closing sentence adds up to what the file held. The break stops the four
# kinds that are not mail from being counted when they are saved, so an
# appointment, a contact, a task and a note all land and the sentence says
# none of them did. The three readings that count a saved kind redden.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "each kind out of a data file is counted when it is saved"
file = "src/application/importing_an_outlook_data_file.rs"
before = """        *kind += 1;"""
after = """        let _ = kind;"""
red = [
    "application::importing_an_outlook_data_file::tests::test_each_of_the_four_kinds_lands_under_the_local_account_and_not_the_servers",
    "application::importing_an_outlook_data_file::tests::test_a_folder_with_a_name_this_computer_cannot_use_keeps_its_mail_and_hands_over_the_rest",
    "application::importing_an_outlook_data_file::tests::test_a_refusal_from_the_reader_ends_the_folder_and_is_kept_to_be_said",
]
tests_last_seen = [
    { file = "src/application/importing_an_outlook_data_file.rs", tests = 8 },
]

# An Outlook data file chosen in the picker goes to its own reader. It went
# to the archive reader until 2026-09-16 and was told it was not a mailbox
# archive (#53). The break takes the arm out, so a file that begins the way
# Outlook's do falls through to the archive reader again, and the reading
# that hands one to what_was_chosen reddens.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "an Outlook data file is sent to its own reader rather than the archive's"
file = "src/application/import_tree.rs"
before = """    if opens_with.starts_with(crate::service::outlook_data_file::HOW_ONE_BEGINS) {"""
after = """    if opens_with.starts_with(b"never") {"""
red = [
    "application::import_tree::tests::test_an_outlook_data_file_goes_to_its_own_reader",
]
tests_last_seen = [
    { file = "src/application/import_tree.rs", tests = 33 },
]

# The name Save As offers is the subject with every path separator made
# harmless before it goes anywhere near the folder somebody chose. The
# break leaves the separators in, so the one function that knows what a
# name may hold keeps only the last segment of the subject, and "Invoices/
# March" is offered as March. The reading that hands the decision a subject
# holding a slash reddens; a plain subject and an empty one are unchanged.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --lib`.
[[guard]]
name = "Save As takes the path out of a subject before offering it as a name"
file = "src/application/importing_messages.rs"
before = """            '/' | '\\\\' => '_',"""
after = """            '/' | '\\\\' => letter,"""
red = [
    "application::importing_messages::tests::test_save_as_takes_the_path_out_of_a_subject_before_offering_it_as_a_name",
]
tests_last_seen = [
    { file = "src/application/importing_messages.rs", tests = 41 },
]

# File, Save As reaches a handler that writes, held by a reading of the main
# window. The command sent "Save As: no message selected" whatever was
# selected from the day it was added until #53, and the break puts that
# line back in the arm. The reading names the stub, and its companion,
# which reads the tree before splicing and refuses over a real stub, goes
# red with it. `suite` names the target, which couples it to the window's
# source for `check.sh --suites-for`.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test wired`.
[[guard]]
name = "Save As reaches a handler that writes rather than a status line"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """                            save_the_message_as(
                                &state,
                                &message_cache,
                                &frame,
                                &ui_tx,
                                &runtime,
                                &a11y,
                            );"""
after = """                            send_status(&ui_tx, &runtime, "Save As: no message selected");"""
red = [
    "test_save_as_writes_the_chosen_message_rather_than_sending_a_status_line",
    "test_the_reading_of_save_as_can_see_the_stub_put_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# The import worker hands an Outlook data file to the module that reads
# one. The break keeps the third answer and does nothing with it, so a data
# file is recognised and then imported by nothing, which is the shape the
# reader was in for eleven days. The reading that asks the worker for the
# call reddens.
#
# Measured 2026-09-16 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test wired`.
[[guard]]
name = "an Outlook data file the worker recognises is handed to its reader"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """        import_tree::WhatWasChosen::AnOutlookDataFile => {
            return crate::application::importing_an_outlook_data_file::brought_in(
                cache,
                account,
                at,
                &|so_far| say(UIUpdate::StatusUpdated(so_far.to_string())),
            );
        }"""
after = """        import_tree::WhatWasChosen::AnOutlookDataFile => {}"""
red = [
    "test_importing_mail_sends_each_kind_of_file_to_its_own_reader",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# The folder import's picker answers with a folder and the handler hands it
# to the worker. The break keeps the item, the arm and the folder picker and
# drops the hand-over, so a folder is chosen and imported by nothing, which
# is the shape a menu item that opens a dialog and does nothing has: the
# most convincing kind of stub. The reading that asks the handler for the
# call reddens, and so does its companion, which reads the tree before it
# splices and refuses a tree that already holds a break.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test wired`.
[[guard]]
name = "a chosen folder of messages is handed to the import worker"
file = "src/presentation/wx_app.rs"
suite = "wired"
before = """    let Some(chosen) = picker.get_path() else {
        refuse_to_import(ui_tx, runtime, a11y, "No folder was chosen.");
        return;
    };
    mail_brought_in_from(chosen, account, ui_tx, runtime, a11y);"""
after = """    let Some(_chosen) = picker.get_path() else {
        refuse_to_import(ui_tx, runtime, a11y, "No folder was chosen.");
        return;
    };
    let _ = account;"""
red = [
    "test_a_folder_of_messages_can_be_chosen_and_goes_to_the_import_worker",
    "test_the_reading_of_the_folder_import_can_see_the_hand_over_taken_out",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/wired.rs", tests = 77 },
]

# The wait before asking a mail server again grows with each failure. The
# break answers the first wait every time, which is a client that asks a
# server that has said no every thirty seconds for as long as the program
# runs: the reconnect storm the rule exists to prevent. The test that lists
# the whole shape reddens, and so does the one holding the seventh answer
# to the cap.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the wait before asking a mail server again grows with each failure"
file = "src/application/trying_again.rs"
before = """        FIRST_WAIT
            .saturating_mul(1u32 << doublings)
            .min(LONGEST_WAIT)"""
after = """        let _ = doublings;
        FIRST_WAIT"""
red = [
    "application::trying_again::tests::test_each_failure_doubles_the_wait_until_the_cap",
    "application::trying_again::tests::test_the_wait_never_goes_past_thirty_minutes",
]
tests_last_seen = [
    { file = "src/application/trying_again.rs", tests = 9 },
]

# The wait before asking a mail server again never goes past thirty
# minutes. The break drops the cap, so the seventh failure waits thirty-two
# minutes, the tenth over four hours, and a watch that failed overnight is
# next tried some time next week. The test that lists the whole shape
# reddens on the seventh answer, and the one that asks a hundred times
# reddens on the first answer past the cap.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the wait before asking a mail server again never goes past thirty minutes"
file = "src/application/trying_again.rs"
before = """            .saturating_mul(1u32 << doublings)
            .min(LONGEST_WAIT)"""
after = """            .saturating_mul(1u32 << doublings)"""
red = [
    "application::trying_again::tests::test_each_failure_doubles_the_wait_until_the_cap",
    "application::trying_again::tests::test_the_wait_never_goes_past_thirty_minutes",
]
tests_last_seen = [
    { file = "src/application/trying_again.rs", tests = 9 },
]

# A download of everything brings the folder on screen down first, then the
# inbox, then the rest in the tree's order. The break sorts by the tree's
# order first and by what is on screen second, so the inbox comes before
# the folder somebody is looking at, which is a person watching an empty
# list while mail lands somewhere else. The test that reads the whole order
# reddens; the one with nothing on screen does not, because the two keys
# agree when nothing is on screen.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the folder on screen comes down before the inbox"
file = "src/application/bringing_everything_down.rs"
before = """    (not_on_screen, kind, name)"""
after = """    (kind, not_on_screen, name)"""
red = [
    "application::bringing_everything_down::tests::test_the_folder_on_screen_comes_first_then_the_inbox_then_the_tree_order",
]
tests_last_seen = [
    { file = "src/application/bringing_everything_down.rs", tests = 23 },
]

# A text budget somebody chose ends the text run when the next chunk would
# take the kept text past it. The break ignores the bound, so a choice of
# one gigabyte fetches without limit, which is T-10-04: the bound is in the
# settings screen and not in the run. The test that ends a run on the
# budget reddens, and so does the one that shrinks the last chunk to fit.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a text budget somebody chose ends the text run"
file = "src/application/bringing_everything_down.rs"
before = """        TextBudget::UpTo(bytes) => bytes.saturating_sub(text.kept_bytes),"""
after = """        TextBudget::UpTo(_) => u64::MAX,"""
red = [
    "application::bringing_everything_down::tests::test_the_budget_ends_a_text_run_and_says_what_it_would_have_needed",
    "application::bringing_everything_down::tests::test_a_chunk_of_text_shrinks_to_what_the_budget_still_allows",
]
tests_last_seen = [
    { file = "src/application/bringing_everything_down.rs", tests = 23 },
]

# A folder is reported as stopped only when a chunk brought nothing new.
# The break reports it as stopped after any chunk at all, so a folder
# that is coming down is left at its second chunk and said to have
# stopped, which is a server that answered being reported as one that did
# not. The pure test of the rule reddens on the case where the count
# moved.
#
# Until 2026-09-17 the old whole-folder loop asked the same rule, stopped
# at its second chunk under the break, and so reddened its carry-on test
# and its last-thing-said test as well; 10-05 retired that loop with its
# module when the download of everything took the command's place, so the
# two names came off this record by hand that day and it was measured again.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library; measured again the
# same day after the correction.
[[guard]]
name = "a folder is reported as stopped only when a chunk brought nothing new"
file = "src/application/bringing_everything_down.rs"
before = """    here.held < here.total_on_server && held_before_the_last_chunk == Some(here.held)"""
after = """    here.held < here.total_on_server && held_before_the_last_chunk.is_some()"""
red = [
    "application::bringing_everything_down::tests::test_a_finished_folder_brings_nothing_new_and_is_not_a_server_that_stopped",
]
tests_last_seen = [
    { file = "src/application/bringing_everything_down.rs", tests = 23 },
]

# The list of messages with no text here carries each row's size, so a
# chunk can be bounded by bytes before a fetch. The break selects nought
# for every row, which is every message reading as free and the byte bound
# never met, and the run can say nothing about how much is still to come.
# The test that stores a size and reads it back reddens; the one about a
# row from before sizes were kept does not, because nought is its answer.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the list of messages with no text here carries each row's size"
file = "src/data/message_cache/bodies.rs"
before = """                "SELECT m.id, f.path, m.uid, COALESCE(m.size_bytes, 0)"""
after = """                "SELECT m.id, f.path, m.uid, 0"""
red = [
    "data::message_cache::bodies::tests::test_a_listed_message_carries_the_size_the_server_gave_it",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
]

# A chunk of the text pass stops before the next message when it is asked
# to. The break asks and ignores the answer, which is a Pause item that
# does nothing until the chunk is over, and a chunk of fifty messages with
# photos in them is minutes. The test that stops after two reddens.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a chunk of the text pass stops when it is asked to"
file = "src/application/mail_sync.rs"
before = """        if stop() {
            done.ended = Ending::Stopped { after: attempted };
            break;
        }"""
after = """        let _ = stop();"""
red = [
    "application::mail_sync::tests::test_a_chunk_stops_before_the_next_message_when_asked_to",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# Three failures in a row that are not the gate's refusal end the chunk,
# which is the provider's answer read per chunk rather than per message.
# The break counts them and never ends the chunk, so a server that has
# started refusing is asked for every message in it and each refusal is
# reported as one message that could not be fetched, which is the shape
# the text pass had until 2026-09-17. The six-asks test reddens, and so
# does the whole-list run's, which folds the chunk endings and so goes on
# asking too.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "three refusals in a row end the chunk rather than being counted one at a time"
file = "src/application/mail_sync.rs"
before = """                if failures_in_a_row >= REFUSALS_THAT_MEAN_THE_SERVER_HAS_STOPPED {"""
after = """                if failures_in_a_row > chunk.len() {"""
red = [
    "application::mail_sync::tests::test_a_server_that_refuses_three_in_a_row_ends_the_chunk_after_six_asks_and_not_ten",
    "application::mail_sync::tests::test_the_whole_list_run_stops_when_the_server_stops_answering_rather_than_asking_for_every_message",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# What a person hears when the server stops answering is this program's
# clause for the kind of failure, never the server's text. The break ends
# the chunk through the other early ending, the one that carries a
# sentence, with the error's own text in it, so a server-controlled string
# is read aloud, which is T-10-03's mirror on the spoken side. The test
# that asks a refusing server for a chunk and reads the sentence reddens,
# and so do the two that hold the ending's shape.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "no sentence about the server stopping carries a string the server sent"
file = "src/application/mail_sync.rs"
before = """                    done.ended = Ending::TheServerStoppedAnswering {
                        after: attempted + 1 - failures_in_a_row,
                        because: WhyTheServerStopped::from_the_kind_of(&e),
                    };"""
after = """                    done.ended = Ending::ReadingWasTurnedOff(e.to_string());"""
red = [
    "application::mail_sync::tests::test_no_sentence_about_the_server_stopping_carries_a_string_the_server_sent",
    "application::mail_sync::tests::test_a_server_that_refuses_three_in_a_row_ends_the_chunk_after_six_asks_and_not_ten",
    "application::mail_sync::tests::test_the_whole_list_run_stops_when_the_server_stops_answering_rather_than_asking_for_every_message",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── Every Settings check box stays a check box (#67) ──────────────────────────
#
# A later Settings panel is painted at the end of its own build, after its
# controls exist. From 09-09 (`8162ef57`) until 10-01.1 the six empty panels
# were painted when the dialog was built, and every checkbox created on one
# later inherited its text colour at creation and was made owner-drawn by
# wxWidgets, which Windows' accessible object reports as a push button with
# no checked state: the probe that diagnosed #67 read 45 owner-drawn rows at
# `7d57cd49` (both build paths and a re-read of Compose in one run) and 57
# check boxes at `3e633252`; the target reads 20 later-page check boxes per
# theme. The break puts one later panel, Compose, back into the dialog's
# paint, so its three check boxes are created under a painted panel again and
# answer ROLE_SYSTEM_PUSHBUTTON with a state that does not move, which
# reddens the Compose rows of both readings in both themes. `suite` names the
# target, which is what lets `check.sh --suites-for` find it for a commit to
# this file; the file has no unit tests of its own.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built`.
[[guard]]
name = "a later Settings panel is painted after its controls exist, so its check boxes stay check boxes"
file = "src/presentation/wx_settings.rs"
suite = "every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built"
before = """        theme::paint(&general_panel, palette.main_surface());
        theme::paint(&font_size, palette.main_surface());"""
after = """        theme::paint(&general_panel, palette.main_surface());
        theme::paint(&later.compose.panel, palette.main_surface());
        theme::paint(&font_size, palette.main_surface());"""
red = [
    "test_every_checkbox_on_a_later_page_answers_check_button_after_set_selection",
    "test_bm_click_toggles_the_checked_state_on_every_later_page",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built.rs", tests = 3 },
]

# ── A page reached from inside a page hands focus to its first control (#68) ──
#
# `wxNotebook::SetSelection` gives the reached page focus before it sends
# the page-changed event, when the tab row does not hold focus (Ctrl+Tab
# from inside a page); since 09-09 the page is built on that event, so the
# focus landed on the empty panel and a screen reader spoke an unnamed
# pane. After the build, `build_the_page_for` asks whether the panel itself
# holds native focus and, if so, hands it to the page's first control. The
# break takes that hand-off out, so focus stays on the panel and the
# reading that moves the selection from a General control reddens; the
# arrow reading in `the_settings_tab_row_says_each_tab_once` (#33) is the
# other half, holding that a row which had focus keeps it. `suite` names
# the target, which is what lets `check.sh --suites-for` find it for a
# commit to this file; the file has no unit tests of its own.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# a_settings_page_reached_from_inside_a_page_gives_focus_to_its_first_control`.
[[guard]]
name = "a Settings page reached from inside another page hands focus to its first control"
file = "src/presentation/wx_settings.rs"
suite = "a_settings_page_reached_from_inside_a_page_gives_focus_to_its_first_control"
before = """        if panel.has_focus()
            && let Some(first) = first_control
        {
            first.set_focus();
        }"""
after = """        let _ = first_control;"""
red = [
    "test_a_page_reached_from_inside_a_page_puts_focus_on_its_first_control",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/a_settings_page_reached_from_inside_a_page_gives_focus_to_its_first_control.rs", tests = 3 },
]

# ── The list holds everything the folder holds (#24) ─────────────────────────
#
# Until 2026-09-17 `load_folder_messages` read a folder through
# `get_message_list_sorted` with a `LIMIT` of 500 that reset on every folder
# change, so the list showed the newest 500 and a message arriving pushed the
# oldest shown off the end. 10-02 took the page off against the rows on
# `docs/development/measurements.md` named "The list's own read path". The
# reading in `tests/the_list_holds_everything_the_folder_holds.rs` holds the
# shipping half of `wx_app.rs` to passing `None`, to reading the labels by
# folder, and to naming none of the three identifiers the page lived in. The
# break puts `Some(500)` back into the call. One test goes red out of the
# five in the target; the companion plants into a snippet and stays green,
# and the three cache tests do not read the window. `suite` names the target,
# which is what couples it to `wx_app.rs` for `check.sh --suites-for`.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# the_list_holds_everything_the_folder_holds`.
[[guard]]
name = "the message list asks the cache for the whole folder and not a page of it"
file = "src/presentation/wx_app.rs"
suite = "the_list_holds_everything_the_folder_holds"
before = """    match cache.get_message_list_sorted(folder_id, &account_id, order.as_deref(), None) {"""
after = """    match cache.get_message_list_sorted(folder_id, &account_id, order.as_deref(), Some(500)) {"""
red = [
    "test_the_window_asks_for_the_whole_folder",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_list_holds_everything_the_folder_holds.rs", tests = 5 },
]

# ── The labels of a folder are read by folder, all of them ───────────────────
#
# The old labels read sent one bound parameter per row the list held, and
# the bundled SQLite refuses more than 32,766 of them, so a folder past that
# size showed no labels; the row that refused, at 200,000, is on the
# measurements page. `tags_by_message_in_folder` reads them through one
# query joined on `messages.folder_id`, and its two siblings by account and
# across every inbox share the query. The break puts a `LIMIT 100` on the
# shared query, so a folder of 12,872 with a label on one row in ten answers
# 100 labelled rows rather than 1,288, and the 40,000-row read answers 100
# rather than 4,000. Two tests go red out of the five in the target; the
# unit tests in `tags.rs` label two messages and cannot see a limit of 100.
# `file` is `tags.rs` and `suite` names the target, so a commit to the
# query runs the target through `check.sh --suites-for`.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# the_list_holds_everything_the_folder_holds`.
[[guard]]
name = "the labels of a folder are read by folder, every one of them"
file = "src/data/message_cache/tags.rs"
suite = "the_list_holds_everything_the_folder_holds"
before = """                 WHERE {narrowed_to} AND m.deleted = 0
                 ORDER BY mt.message_id, t.name\""""
after = """                 WHERE {narrowed_to} AND m.deleted = 0
                 ORDER BY mt.message_id, t.name LIMIT 100\""""
red = [
    "test_the_labels_of_a_folder_are_read_by_folder",
    "test_a_folder_above_the_variable_limit_reads_its_labels_by_folder_without_an_error",
]
tests_last_seen = [
    { file = "src/data/message_cache/tags.rs", tests = 10 },
    { file = "tests/the_list_holds_everything_the_folder_holds.rs", tests = 5 },
]

# ── All Inboxes is read in the sort that was chosen (#69) ────────────────────
#
# Until 2026-09-17 `unified_inbox` ran a query carrying its own `ORDER BY
# m.date DESC, m.uid DESC` and took no order, while a folder is read through
# `get_message_list_sorted` with the stored sort in the query, so choosing a
# sort with All Inboxes open re-sorted the rows on screen and saved the
# choice, and every return to the row read the fixed order. 10-02.1 gave the
# query the order as `listing_query` takes it. The break hands the query the
# fixed order in place of what was asked for, which is the defect itself.
# Two tests go red out of the five in the target: the one that reads every
# menu sort through `unified_inbox`, and the one that stores Oldest first
# the way the menu does and reads it back the way the window does. The
# newest-first companion reads the fixed order and stays green, and the
# label and search tests read other methods. `suite` names the target,
# which is what couples it to `messages.rs` for `check.sh --suites-for`:
# before this record no integration target was coupled to that file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# all_inboxes_reads_in_the_sort_that_was_chosen`.
[[guard]]
name = "All Inboxes is read in the sort that was chosen, not a fixed one"
file = "src/data/message_cache/messages.rs"
suite = "all_inboxes_reads_in_the_sort_that_was_chosen"
before = """        let order = order_by.unwrap_or(NEWEST_MESSAGE_FIRST);
        let query = unified_inbox_query(order, limit);"""
after = """        let query = unified_inbox_query(NEWEST_MESSAGE_FIRST, limit);"""
red = [
    "test_all_inboxes_answers_in_the_order_it_is_handed",
    "test_the_sort_the_menu_stores_is_read_back_into_the_order_the_cache_takes",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "tests/all_inboxes_reads_in_the_sort_that_was_chosen.rs", tests = 7 },
]

# ── A label view is read in the sort that was chosen (#69) ───────────────────
#
# The same shape as All Inboxes, in `messages_with_label`: an inline query
# with its own `ORDER BY` and no order taken, until 10-02.1 lifted it into
# `label_listing_query` with the order as an argument. The break hands the
# query the fixed order in place of what was asked for. One test goes red
# out of the five in the target, the one that reads every menu sort through
# `messages_with_label` over three labelled rows. `suite` names the target,
# which couples it to `tags.rs` for `check.sh --suites-for`, beside the
# labels-by-folder record above.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# all_inboxes_reads_in_the_sort_that_was_chosen`.
[[guard]]
name = "a label view is read in the sort that was chosen, not a fixed one"
file = "src/data/message_cache/tags.rs"
suite = "all_inboxes_reads_in_the_sort_that_was_chosen"
before = """        let order = order_by.unwrap_or(super::messages::NEWEST_MESSAGE_FIRST);"""
after = """        let order = super::messages::NEWEST_MESSAGE_FIRST;"""
red = [
    "test_a_label_view_answers_in_the_order_it_is_handed",
]
tests_last_seen = [
    { file = "src/data/message_cache/tags.rs", tests = 10 },
    { file = "tests/all_inboxes_reads_in_the_sort_that_was_chosen.rs", tests = 7 },
]

# ── What a saved search found is listed in the sort that was chosen (#69) ────
#
# The same shape in `message_rows_for`: `results_query` carried its own
# `ORDER BY` and took no order, so the newest 500 a search found were listed
# newest first whatever was chosen. 10-02.1 gave `results_query` the order.
# The cut stays the search's; the order is the person's. The break hands the
# query the fixed order in place of what was asked for. One test goes red
# out of the five in the target, the one that reads every menu sort through
# `message_rows_for` over five of the fixture's ids. `suite` names the
# target, which couples it to `saved_searches.rs` for `check.sh
# --suites-for`: before this record no integration target was coupled to
# that file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# all_inboxes_reads_in_the_sort_that_was_chosen`.
[[guard]]
name = "what a saved search found is listed in the sort that was chosen, not a fixed one"
file = "src/data/message_cache/saved_searches.rs"
suite = "all_inboxes_reads_in_the_sort_that_was_chosen"
before = """        let order = order_by.unwrap_or(super::messages::NEWEST_MESSAGE_FIRST);"""
after = """        let order = super::messages::NEWEST_MESSAGE_FIRST;"""
red = [
    "test_what_a_saved_search_found_answers_in_the_order_it_is_handed",
]
tests_last_seen = [
    { file = "src/data/message_cache/saved_searches.rs", tests = 27 },
    { file = "tests/all_inboxes_reads_in_the_sort_that_was_chosen.rs", tests = 7 },
]

# ── The window asks the three listings for the sort that was chosen (#69) ────
#
# The cache taking an order is half of it; the other half is the window
# asking for one. `load_every_inbox`, `load_messages_with_label` and
# `run_a_saved_search` each ask `the_sort_as(view_state::Showing::Messages)`
# and pass it where their read takes it, as `load_folder_messages` has since
# the layout carried a sort. The reading in the target holds all three
# loaders and the folder loader to asking and passing; the break takes one,
# `load_every_inbox` passing `None` in place of `order.as_deref()`, because
# a record applies one edit, and the reading names each loader separately
# so a break in any of the three would be named. One test goes red out of
# the seven in the target; the companion plants into a snippet and the five
# cache tests do not read the window. `suite` names the target, which
# couples it to `wx_app.rs` for `check.sh --suites-for`, the thirteenth
# target coupled to that file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# all_inboxes_reads_in_the_sort_that_was_chosen`.
[[guard]]
name = "the window asks All Inboxes, a label view and a saved search for the sort that was chosen"
file = "src/presentation/wx_app.rs"
suite = "all_inboxes_reads_in_the_sort_that_was_chosen"
before = """    match cache.unified_inbox(order.as_deref(), None) {"""
after = """    match cache.unified_inbox(None, None) {"""
red = [
    "test_the_window_reads_all_inboxes_a_label_and_a_search_in_the_sort_that_was_chosen",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/all_inboxes_reads_in_the_sort_that_was_chosen.rs", tests = 7 },
]

# ── A garbled size for the message text kept reads as All, not as a bound (#23) ──
#
# The setting that says how much message text stays on this computer is
# read from the settings file by `TextKept::from_stored`, and what it answers
# for a value it cannot read decides whether somebody's downloaded text is
# thrown away (T-10-09): a small bound evicts, All loses nothing. The break
# makes an unreadable value answer `UpTo(0)`, which is every body over
# nought bytes gone at the end of the next folder sync. One test goes red,
# the one that hands it eight garbled values; the round trip, the default and
# the screen's fallback all read values that parse and do not notice.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a garbled size for the message text kept reads as All and not as a bound"
file = "src/application/keeping_message_text.rs"
before = """                .map(TextKept::UpTo)
                .unwrap_or_default(),"""
after = """                .map(TextKept::UpTo)
                .unwrap_or(TextKept::UpTo(0)),"""
red = [
    "application::keeping_message_text::tests::test_a_garbled_value_reads_as_all_because_a_wrong_bound_evicts_and_all_loses_nothing",
]
tests_last_seen = [
    { file = "src/application/keeping_message_text.rs", tests = 10 },
]

# ── The size chosen on the Permissions page is what OK writes back (#23) ────
#
# The two settings guards in `config.rs` hold that `message_text_kept` is
# named in the settings screen's shipping half and in a file that acts on
# it; neither can tell the line that builds the choice from the line that
# reads it back, so a `read_the_permissions_page` that stopped writing the
# field would leave both green and every choice made on that page would be
# saved as whatever was stored before. The break is that: the write replaced
# by a read of the control that writes nothing. One test goes red out of the
# three in the target, the one that chooses each entry and reads it back;
# the never-shown and default readings answer the stored value either way.
# `suite` names the target, which couples it to `wx_settings.rs` for
# `check.sh --suites-for`, the ninth target coupled to that file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# how_much_message_text_stays_is_read_back_from_the_permissions_page`.
[[guard]]
name = "the size chosen on the Permissions page is what OK writes back"
file = "src/presentation/wx_settings.rs"
suite = "how_much_message_text_stays_is_read_back_from_the_permissions_page"
before = """    cfg.message_text_kept = crate::application::keeping_message_text::TextKept::ALL
        .get(sel(&w.message_text_kept) as usize)
        .copied()
        .unwrap_or_default()
        .as_stored();"""
after = """    let _ = &w.message_text_kept;"""
red = [
    "test_a_size_chosen_on_the_permissions_page_is_what_ok_writes_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/how_much_message_text_stays_is_read_back_from_the_permissions_page.rs", tests = 4 },
]

# ── With All chosen, the end of a folder sync evicts no message text (#23) ──
#
# The eviction at the end of every folder sync reads the setting through the
# cache's budget, and `All`, the default, answers nought without reading a
# row. The break makes `All` evict as a bound of nought bytes rather than as
# the old half-gigabyte constant, because a test that could tell the constant
# from All would have to store more than half a gigabyte of bodies, and what
# the record proves is that the All arm is consulted at all: a cache under
# All that evicted anything is the download of everything undone by the next
# folder's sync. One test goes red, the one that opens a cache under All,
# stores three bodies and reads all three back; every other eviction test
# opens its cache with a size or with none, and a cache opened with none is
# `UpTo` the constant.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "with All chosen the end of a folder sync evicts no message text"
file = "src/data/message_cache/bodies.rs"
before = """            TextBudget::All => Ok(0),"""
after = """            TextBudget::All => self.evict_bodies_over(0),"""
red = [
    "data::message_cache::bodies::tests::test_under_all_nothing_is_evicted_and_under_a_size_the_old_rule_runs_at_that_size",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
]

# ── The check's worker opens its cache with how much text stays (#23) ───────
#
# The setting reaches the eviction only because the two workers that run a
# folder sync hand it to their cache before the sync: the check
# (`spawn_mail_sync`) and the whole-folder request. The guard in `config.rs`
# that requires a reader outside the settings files sees the field's name in
# the helper that reads the file and cannot tell a worker's cache line from
# it, so a worker opened without the setting would leave that guard green
# and evict at the old constant whatever was chosen. The break opens the
# check's cache bare. One test goes red out of the four in the target, the
# source reading that takes each worker's body and looks for the seam and
# the setting; the three dialog readings do not read the window. `suite`
# names the target, which couples it to `wx_app.rs` for `check.sh
# --suites-for`, the fourteenth target coupled to that file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# how_much_message_text_stays_is_read_back_from_the_permissions_page`. The
# break moved one indent to the right on 2026-09-18, when 10-06 put the
# check's body inside a walk over the accounts it is handed (#37); the
# lines are the same and the record was measured again that day.
[[guard]]
name = "the check's worker opens its cache with how much message text stays"
file = "src/presentation/wx_app.rs"
suite = "how_much_message_text_stays_is_read_back_from_the_permissions_page"
before = """                Ok(cache) => cache.keeping_bodies_under(text_kept.budget()),
                Err(e) => {
                    fail(format!("Cache error: {}", e));"""
after = """                Ok(cache) => cache,
                Err(e) => {
                    fail(format!("Cache error: {}", e));"""
red = [
    "test_the_two_workers_that_evict_are_handed_the_setting_and_nothing_else_evicts",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/how_much_message_text_stays_is_read_back_from_the_permissions_page.rs", tests = 4 },
]

# ── A step is not spoken under Say what arrived (#38) ───────────────────────
#
# The setting's whole point is the cell of the table that says a progress
# line is silent under the default: a check of fifty folders speaks fifty
# lines without it. The break widens the Progress row so a step is spoken
# under everything but Errors only, which is today's verbosity with a
# setting in front of it. One test goes red, the cell itself; the other
# eleven cells and the stored forms answer the same either way.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, whole library.
[[guard]]
name = "a step is not spoken under Say what arrived"
file = "src/application/what_is_said_while_fetching.rs"
before = """            Kind::Progress => self == HowMuchToSay::EveryStep,"""
after = """            Kind::Progress => self != HowMuchToSay::ErrorsOnly,"""
red = [
    "application::what_is_said_while_fetching::tests::test_under_what_arrived_a_step_is_not_spoken",
]
tests_last_seen = [
    { file = "src/application/what_is_said_while_fetching.rs", tests = 19 },
]

# ── The level chosen on the Feedback page is what OK writes back (#38) ──────
#
# The same shape as the Permissions record above it: the two settings guards
# in `config.rs` see `announce_while_fetching` named in the settings screen
# and in a file that acts on it, and cannot tell the line that builds the
# choice from the line that reads it back, so a `read_the_feedback_page`
# that stopped writing the field would leave both green and every level
# chosen would be saved as whatever was stored before. The break replaces the
# write with a read of the control that writes nothing. The target has one
# test, which builds the real dialog and asks every reading of the Feedback
# page inside it, so that one goes red. `suite` names the target, which is
# already coupled to `wx_settings.rs` by the four records above it.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test every_event_has_a_control`.
[[guard]]
name = "the level chosen on the Feedback page is what OK writes back"
file = "src/presentation/wx_settings.rs"
suite = "every_event_has_a_control"
before = """    cfg.announce_while_fetching =
        crate::application::what_is_said_while_fetching::HowMuchToSay::ALL
            .get(sel(&w.announce_while_fetching) as usize)
            .copied()
            .unwrap_or_default()
            .as_stored();"""
after = """    let _ = &w.announce_while_fetching;"""
red = [
    "test_every_event_is_reachable_and_keeps_what_it_was_given",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/every_event_has_a_control.rs", tests = 1 },
]

# ── A step is spoken only when every step was asked for (#38) ───────────────
#
# The Progress arm is the one place the level governs a step. The break
# takes the ask away and speaks every step, which is the verbosity #38 was
# filed about with a setting in front of it that governs nothing. The arm
# still contains `a11y.announce`, so the window's own reading of arms that
# show and never say passes over it; the reading in the target is what sees
# the ask gone. Two tests go red out of the thirteen in the target: the
# reading of the Progress arm, and its companion, which plants the same
# break into the real text by replacing the ask and refuses to plant into a
# window that has no ask to replace. The first draft of this record named
# the reading alone and said the companion was green either way; the runner
# said otherwise, and the record carries what it measured. `suite` names the
# target, which couples it to `wx_app.rs` for `check.sh --suites-for`, the
# fifteenth target coupled to that file.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# progress_is_shown_and_results_are_said`.
[[guard]]
name = "a step is spoken only when every step was asked for"
file = "src/presentation/wx_app.rs"
suite = "progress_is_shown_and_results_are_said"
before = """            if a11y.how_much_to_say().is_spoken(Kind::Progress) {
                let _ = a11y.announce_topic(said, Priority::Low, "progress");
            }"""
after = """            let _ = a11y.announce_topic(said, Priority::Low, "progress");"""
red = [
    "test_a_step_is_spoken_only_when_every_step_was_asked_for",
    "test_the_reading_complains_when_a_step_is_spoken_without_asking",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/progress_is_shown_and_results_are_said.rs", tests = 15 },
]

# ── The new-mail sound means mail arrived, not that the watch woke (#38) ───
#
# The signal fired from the MailboxChanged arm until 2026-09-17, which is
# reached when the watch wakes: before the folder is read, whether or not
# anything arrives, and never for a check started by F9. The break puts it
# back there, beside the one in the WhatArrived arm, so the sound plays for
# both and means neither. One test goes red out of the thirteen, the reading
# that holds the MailboxChanged arm clear and counts the signal's places as
# one; its companion plants the same line and is green either way.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# progress_is_shown_and_results_are_said`.
[[guard]]
name = "the new-mail sound means mail arrived, not that the watch woke"
file = "src/presentation/wx_app.rs"
suite = "progress_is_shown_and_results_are_said"
before = """        UIUpdate::MailboxChanged(folder) => {
            // Nothing is signalled here since 2026-09-17 (#38). The new-mail"""
after = """        UIUpdate::MailboxChanged(folder) => {
            let _ = a11y.signal(FeedbackEvent::NewMail, "");
            // Nothing is signalled here since 2026-09-17 (#38). The new-mail"""
red = [
    "test_the_new_mail_sound_plays_when_a_check_found_mail_and_not_when_the_watch_woke",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/progress_is_shown_and_results_are_said.rs", tests = 15 },
]

# ── A check that found nothing says nothing about what arrived (#38) ────────
#
# `what_arrived` answers `None` when every count is nought, and the window
# sends a result only for `Some`, so a check that found nothing sends no
# result, plays no sound and says nothing under the default. The break keeps
# the folders with nothing, so every check answers a sentence naming every
# folder with "0 new messages" and the sound plays for every check. Two
# tests go red, the one that asks for `None` and the one that asks for the
# folders with nothing to be left out.
#
# Measured 2026-09-17 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, whole library.
[[guard]]
name = "a check that found nothing says nothing about what arrived"
file = "src/application/mail_sync.rs"
before = """        .filter(|(_, count)| *count > 0)"""
after = """        .filter(|(_, _count)| true)"""
red = [
    "application::mail_sync::tests::test_a_check_that_found_nothing_says_nothing_about_what_arrived",
    "application::mail_sync::tests::test_what_arrived_names_each_folder_that_received_something_with_its_count",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
]

# ── A disabled account is never due a check (#37) ───────────────────────────
#
# The schedule asks over every account row and keeps the enabled ones, so an
# account somebody turned off is not dialled every five minutes for as long
# as the program runs. The break drops the filter on `enabled`, and the test
# with a disabled account in the list reddens; the one that builds the
# schedule's view from the row is green either way, because the field is
# still carried, only no longer read.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a disabled account is never due a check"
file = "src/application/checking_on_a_schedule.rs"
before = """        .filter(|account| account.enabled)
        .filter(|account| {"""
after = """        .filter(|account| {"""
red = [
    "application::checking_on_a_schedule::tests::test_a_disabled_account_is_never_due",
]
tests_last_seen = [
    { file = "src/application/checking_on_a_schedule.rs", tests = 20 },
]

# ── A watch somebody stopped is not started again from the arm (#37) ────────
#
# "the watch was stopped" is the window replacing a watch or closing, and
# whoever asked for the stop knows whether a fresh one is coming. The break
# reads it as a lost connection and answers a wait, so every check for mail,
# which ends by replacing the watch, would also schedule a second one behind
# it: two watches per account, one of them nobody asked for. Two tests
# redden, the one about the stop and the table of every reason.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a watch somebody stopped is not started again from the arm"
file = "src/application/checking_on_a_schedule.rs"
before = """            if said.starts_with(SOMEBODY_STOPPED_IT) || said.starts_with(NOBODY_WAS_LISTENING) =>"""
after = """            if said.starts_with(NOBODY_WAS_LISTENING) =>"""
red = [
    "application::checking_on_a_schedule::tests::test_a_watch_somebody_stopped_is_not_tried_again",
    "application::checking_on_a_schedule::tests::test_every_reason_the_watch_can_end_with_has_an_answer_that_is_not_a_guess",
]
tests_last_seen = [
    { file = "src/application/checking_on_a_schedule.rs", tests = 20 },
]

# ── A stored interval of nought is a minute, not every tick (#37) ───────────
#
# The editor clamps the interval to one minute at the least, and the row can
# still hold nought: written by hand, or by a version that did not clamp.
# The break reads the stored number as it is, so nought is "due at once,
# always", which is a check against somebody else's server on every tick of
# the timer. Two tests redden, the one about nought and the one holding both
# ends of the clamp.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a stored interval of nought is a minute, not every tick"
file = "src/application/checking_on_a_schedule.rs"
before = """    let minutes = minutes.clamp(SHORTEST_INTERVAL_MINUTES, LONGEST_INTERVAL_MINUTES);
    Duration::from_secs(60 * u64::from(minutes))"""
after = """    Duration::from_secs(60 * u64::from(minutes))"""
red = [
    "application::checking_on_a_schedule::tests::test_a_stored_interval_of_nought_is_read_as_one_minute_and_not_every_tick",
    "application::checking_on_a_schedule::tests::test_a_stored_interval_above_sixty_is_read_as_sixty",
]
tests_last_seen = [
    { file = "src/application/checking_on_a_schedule.rs", tests = 20 },
]

# ── A watch that ends is asked about, not broken from (#37) ─────────────────
#
# The Stopped arm hands the reason to the decision, which asks the rule and
# writes a wait or the schedule alone on the account's watch. The break puts
# the arm back the way it stood until 2026-09-18: the reason is logged and
# the loop breaks, and nothing starts another watch, which is the tester's
# "automatic mail fetching is switched off". One test goes red out of the
# fourteen in the target; the companion is over a snippet. `wired`'s reading
# of the same arm reddens too and is not in this suite. This record is also
# what couples the target to `wx_app.rs`, so it runs on the commits that
# could break it.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "a watch that ends is asked about, not broken from"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """                        what_the_watch_does_next(
                            &state_for_task,
                            &tx,
                            &account,
                            &inbox.name,
                            WhyTheWatchEnded::Ended(reason),
                        );
                        break;"""
after = """                        let _ = reason;
                        break;"""
red = [
    "test_a_watch_that_ends_asks_whether_to_watch_again",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── A watch that never started is asked about before the branch returns ─────
#
# The connection, the sign-in or the select failing before any watch exists
# answers an error from `watch_folder` and never reaches Stopped, so the
# question has to be asked in that branch. The break returns without asking,
# which is the tree as it stood until 2026-09-18: a server that could not be
# reached at startup was logged and never tried again. One test goes red out
# of the fourteen; the companion is over a snippet.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "a watch that never started is asked about before the branch returns"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """                // question is asked here, and it is asked before the return.
                tracing::warn!("Could not watch the inbox of {}: {}", account.name, e);
                ask_what_next(WhyTheWatchEnded::NeverStarted(e.to_string()));
                return;"""
after = """                // question is asked here, and it is asked before the return.
                tracing::warn!("Could not watch the inbox of {}: {}", account.name, e);
                return;"""
red = [
    "test_a_watch_that_never_started_asks_too_before_it_returns",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── The network coming back starts the watches at once (#37) ────────────────
#
# Reads follow the network. The back branch asks for a watch on every
# enabled IMAP account and a check of every due one; the break drops both,
# so a watch that died with the network waits out the whole of a wait that
# was about the network, and mail that arrived meanwhile waits for the
# schedule. One test goes red out of the fourteen; the companion is over a
# snippet.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "the network coming back starts the watches at once"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """        watch_every_enabled_account(app);
        check_the_accounts_that_are_due(app, true);"""
after = """        let _ = app;"""
red = [
    "test_the_network_coming_back_starts_the_watches_at_once",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── The timer checks the accounts that are due (#37) ────────────────────────
#
# The schedule arm sits on the main timer's minute look, beside the
# reminders. The break takes it out, so nothing is checked on a schedule: a
# server without IDLE, a POP account and the kept folders the watch does not
# cover are read only when somebody presses F9. One test goes red out of the
# fourteen; the companion is over a snippet.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "the timer checks the accounts that are due"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """                        check_the_accounts_that_are_due(
                            app,
                            the_network.borrow().there_is_a_network(),
                        );"""
after = """                        let _ = the_network.borrow().there_is_a_network();"""
red = [
    "test_the_timer_checks_on_the_accounts_own_interval",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── The schedule reads the interval off the account row (#37) ───────────────
#
# The accounts handed to `which_are_due` are built from the rows by `From`,
# so the Check Interval the account editor has offered since it was written
# is what decides. The break builds them by hand with five minutes for
# everybody, which is the field stopping to act again with nothing red, as
# it did from the day it was written until 2026-09-18. One test goes red out
# of the fourteen, the same one as the timer's record, on a different
# sentence; the companion plants the same five minutes.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "the schedule reads the interval off the account row"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """        let to_check: Vec<AccountToCheck> = s.accounts.iter().map(AccountToCheck::from).collect();"""
after = """        let to_check: Vec<AccountToCheck> = s
            .accounts
            .iter()
            .map(|account| AccountToCheck {
                id: account.id.clone(),
                enabled: account.enabled,
                protocol: account.protocol(),
                check_interval_minutes: 5,
            })
            .collect();"""
red = [
    "test_the_timer_checks_on_the_accounts_own_interval",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── A start checks without a keystroke (#37) ────────────────────────────────
#
# After the module is filled, the start asks for a watch on every enabled
# IMAP account and checks every enabled account. The break drops both, which
# is the tree as it stood until 2026-09-18: nothing checked at startup and
# nothing watched until the first F9. One test goes red out of the fourteen;
# the companion is over a snippet.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "a start checks without a keystroke"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """                watch_every_enabled_account(app);
                check_every_enabled_account(app);"""
after = """                let _ = app;"""
red = [
    "test_a_start_checks_without_a_keystroke",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── A check that went through nowhere asks for no download (#37) ────────────
#
# Found by starting the release binary against the measurement profile,
# whose account has no password saved: the first draft of the walk over the
# accounts asked for the download after every check, so a check in which
# nothing went through started a download that failed the same way and
# waited on its own, two retry rows against one server. The break drops the
# branch that returns when nothing went through, which is that draft. One
# test goes red out of the fourteen, the reading of the check; the companion
# is over two snippets.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, `cargo test --test
# mail_keeps_arriving_on_its_own`.
[[guard]]
name = "a check that went through nowhere asks for no download"
file = "src/presentation/wx_app.rs"
suite = "mail_keeps_arriving_on_its_own"
before = """        if nothing_went_through {
            return;
        }
        // Every check that went through ends by asking for the download of"""
after = """        let _ = nothing_went_through;
        // Every check that went through ends by asking for the download of"""
red = [
    "test_the_check_for_mail_walks_every_enabled_account_and_writes_down_when",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mail_keeps_arriving_on_its_own.rs", tests = 14 },
]

# ── A spelling language chosen for a region is shown as chosen, not as the checker's answer ──
#
# The settings screen's language row shows one row and OK writes that row's
# tag back, so a row chosen for the person rewrites their choice. The checker
# may settle for the nearest dictionary it has, en-US for a stored en-AU on a
# machine with no Australian dictionary, and that is right for the checker
# and wrong for the screen: on such a machine the screen showed English
# (United States) for a stored en-AU and saved it the first time Settings was
# saved. GitHub's runner is such a machine, and the Test Suite job of CI run
# 35336142985 on 2026-09-18 is where it was seen; the machine the testing
# happens on offers en-AU, which is why nobody saw it there. A regression of
# 09-02's fix for #21. The break makes no tag name a region, so every tag
# takes the resolver's answer, which is the tree as it stood before 11-01.
# Two tests go red, the two whose region tag the resolver would resolve
# elsewhere; the case-insensitive one stays green because its row is an
# exact usable match the resolver answers too, and the bare-tag cases never
# reach the half that broke.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a spelling language chosen for a region is shown as chosen, not as the checker's answer"
file = "src/presentation/which_language_row.rs"
before = """        .is_some_and(|(_, region)| !region.is_empty())"""
after = """        .is_some_and(|(_, region)| region.is_empty())"""
red = [
    "presentation::which_language_row::tests::test_a_tag_with_a_region_is_shown_as_its_own_row_even_without_a_dictionary",
    "presentation::which_language_row::tests::test_a_tag_with_a_region_the_machine_does_not_list_is_added_as_stored",
]
tests_last_seen = [
    { file = "src/presentation/which_language_row.rs", tests = 6 },
]

# ── Folders to Keep Up to Date nests its rows the way the folder tree does ──
#
# The tester's first point on #70, 2026-09-17: the list was flat where the
# folder tree in the main window is nested. Since 11-03 the dialog is a tree
# and `nesting` places each row under the row whose path its stored parent
# names, in the order the tree walks its items. The break stops every row
# from finding its parent, so every row sits at the top in the stored order,
# which is the flat list the tester reported. Two tests go red, the two
# that ask for a child under its parent; the three that ask for the stored
# order of siblings, an unlisted parent at the top and a loop of parents at
# the top pass on a flat list too, because a flat list is what each of them
# expects.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a folder in the chooser sits under its parent, not at the top of a flat list"
file = "src/presentation/wx_folder_choice.rs"
before = """                .and_then(|parent| rows.iter().position(|it| it.path == parent))"""
after = """                .and_then(|_parent| None)"""
red = [
    "presentation::wx_folder_choice::tests::test_a_child_comes_after_its_parent_one_deeper_and_names_it",
    "presentation::wx_folder_choice::tests::test_a_grandchild_is_two_deep_and_follows_its_parent_before_the_next_top_row",
]
tests_last_seen = [
    { file = "src/presentation/wx_folder_choice.rs", tests = 21 },
]

# ── A kept folder is ticked in the control's own state, where NVDA reads it ──
#
# The tester's second point on #70: a kept folder was heard as "check box,
# read-only, not checked". The dialog's tree ticks each item through
# `TVM_SETITEMW` from the row's `syncing`, and reading C in the target reads
# every item's state image back through `TVM_GETITEMSTATE` and its MSAA
# state through `AccessibleObjectFromWindow`, which are the two channels NVDA
# and Windows' own tree object answer from. The break unticks every item
# whatever the row says, so the kept row's state image reads 1 and its MSAA
# state carries no CHECKED bit; reading C goes red on both and nothing else
# does, since the companions hold the predicate to a hand-built harvest and
# reading D reads static texts.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a kept folder in the chooser is ticked in the control's own state, not left unticked whatever the row says"
file = "src/presentation/wx_folder_choice.rs"
suite = "a_kept_folder_reads_as_a_checked_check_box"
before = """            native_tree_checks::set_checked(&tree, by_row[row], folder.syncing);"""
after = """            native_tree_checks::set_checked(&tree, by_row[row], false);"""
red = [
    "test_reading_c_the_dialog_is_a_tree_whose_kept_folder_is_checked_where_nvda_reads",
]
tests_last_seen = [
    { file = "src/presentation/wx_folder_choice.rs", tests = 21 },
    { file = "tests/a_kept_folder_reads_as_a_checked_check_box.rs", tests = 8 },
]

# ── The chooser's title names the account, not its identifier ───────────────
#
# The tester's third point on #70: the title was an identifier. The chooser
# puts whatever it is handed after the colon, so the fact lives at the one
# call in `choose_folders`, which hands the account row's `name`. The break
# hands the identifier back, the tree as it stood before 11-03; the reading
# of the window's source in the target goes red and nothing else does.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the folder chooser is handed the account's name for its title, not its identifier"
file = "src/presentation/wx_app.rs"
suite = "a_kept_folder_reads_as_a_checked_check_box"
before = """    let Some(changed) = ask(frame, &account.name, is_gmail, &rows) else {"""
after = """    let Some(changed) = ask(frame, &account_id, is_gmail, &rows) else {"""
red = [
    "test_the_window_hands_the_chooser_the_accounts_name_and_not_its_identifier",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_kept_folder_reads_as_a_checked_check_box.rs", tests = 8 },
]

# ── The log's default follows the version the build carries ─────────────────
#
# #71, Pratik's decision of 2026-09-17: debug while the version says alpha or
# beta, info otherwise, one rule in `logging::default_level_for` that both
# `LoggerConfig::default()` and `AppConfig::default()` read. The break makes
# every version answer info, the tree as it stood before 11-04 with its two
# literals. The rule's own test goes red; the two tests that hold the defaults
# to the rule stay green, because they compare the default with the rule's
# answer and both move together, which is the point of asking the rule.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "the log's default is debug under alpha and beta, not info for every build"
file = "src/common/logging.rs"
before = """    if crate::common::version::is_alpha_or_beta(version) {"""
after = """    if false && crate::common::version::is_alpha_or_beta(version) {"""
red = [
    "common::logging::tests::test_the_default_level_is_debug_under_alpha_and_beta_and_info_otherwise",
]
tests_last_seen = [
    { file = "src/common/logging.rs", tests = 10 },
]

# ── The log filter names this crate and no library ──────────────────────────
#
# A library switched on at debug logs what the library chooses, and for an
# IMAP crate that is the literals on the wire, which is message text (T-11-16).
# The break adds a second target the way somebody debugging a connection
# would, and the filter test goes red on the comma. The first draft named
# that one test; the runner answered two, because the census in
# `service::outward` reads every module for the name of a crate that reaches
# the network, and a module that spells `async_imap` is a module naming a way
# out, which `logging.rs` is not on any list as. So a library named in the
# filter is caught twice, by the filter test and by the census, and the record
# says so rather than being shorter than the truth.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library, twice: once as the draft
# and once as this.
[[guard]]
name = "the log filter names this crate alone, not a library beside it"
file = "src/common/logging.rs"
before = """    format!("wixen_mail={}", level.as_stored())"""
after = """    format!("wixen_mail={},async_imap=debug", level.as_stored())"""
red = [
    "common::logging::tests::test_the_filter_names_this_crate_at_the_level_and_nothing_else",
    "service::outward::completeness::test_every_module_that_names_a_way_out_is_on_one_of_these_lists",
]
tests_last_seen = [
    { file = "src/common/logging.rs", tests = 10 },
    { file = "src/service/outward.rs", tests = 38 },
]

# ── Each folder of a check is written to the log ────────────────────────────
#
# #71: a report written from the log has to say what each check found per
# folder, and until 11-04 the check wrote nothing per folder at any level.
# The break drops the line, the tree as it stood; the reading in the target
# goes red with "writes nothing", and so does its companion, which asserts
# the line is there before planting its absence, so it cannot pass by
# planting nothing. Both are named, as the runner answered.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "each folder of a check is written to the log, not left to the status bar alone"
file = "src/presentation/wx_app.rs"
suite = "the_log_carries_what_a_report_needs"
before = """                        tracing::info!(
                            "Checked {} in {}: {}",
                            folder.name,
                            account.name,
                            crate::application::mail_sync::what_the_folder_sync_did(&result)
                        );"""
after = """                        // The per-folder line, dropped by a guard's break."""
red = [
    "test_each_folder_of_a_check_is_written_at_info",
    "test_the_reading_complains_when_the_folder_line_is_missing_or_at_debug",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_log_carries_what_a_report_needs.rs", tests = 13 },
]

# ── Content held back by mute is written to the log by its length ───────────
#
# #71: a tester who reports silence needs the log to say whether anything was
# held back, and until 11-04 the queue dropped muted content without a word.
# The break drops the line; the reading goes red with "written nowhere", and
# its companion goes red on its anchor, as above.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "content held back by mute is written to the log by its length, not dropped in silence"
file = "src/presentation/accessibility/announcements.rs"
suite = "the_log_carries_what_a_report_needs"
before = """            tracing::info!(
                "Held back {} characters of message text: content is muted",
                announcement.text.len()
            );"""
after = """            // The held-back line, dropped by a guard's break."""
red = [
    "test_what_is_held_back_from_speech_is_written_and_never_the_words",
    "test_the_reading_complains_when_the_muted_line_is_missing_or_writes_the_words",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/announcements.rs", tests = 25 },
    { file = "tests/the_log_carries_what_a_report_needs.rs", tests = 13 },
]

# ── The page's script posts the attachments jump for Alt+A ──────────────────
#
# #84: the formatted message window bound F8 on the browser control, where a
# key binding never fires, and the tester met a dead key. The jumps go through
# the page's own script now, and the script posting nothing for Alt+A is the
# same dead key with a different spelling. The break drops the post; the
# reading in the target goes red with "posts nothing for Alt+A", and its
# companion goes red on its anchor. The module's own unit tests go red too and
# are not named here, because this record runs the target alone; the record
# below runs the library for them.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the page's script posts the attachments jump for alt+a, not nothing"
file = "src/presentation/page_jumps.rs"
suite = "attachments_are_reached_with_alt_a_in_both_views"
before = """        window.contextMenu.postMessage(JSON.stringify({ kind: 'attachments' }));"""
after = """        // The post, dropped by a guard's break."""
red = [
    "test_the_page_window_is_given_the_jumps_and_the_preview_is_not",
    "test_the_reading_complains_when_the_script_or_the_wiring_loses_a_jump",
]
tests_last_seen = [
    { file = "src/presentation/page_jumps.rs", tests = 5 },
    { file = "tests/attachments_are_reached_with_alt_a_in_both_views.rs", tests = 10 },
]

# ── The window reads every kind the page's script posts ─────────────────────
#
# The same module holds the script and the reader of what it posts so the two
# words cannot differ, and this is the break that makes them differ: the
# reader stops answering the warning kind. The test walking every posted kind
# through the reader goes red, and so does the one asking for F7 by name.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "the window reads the warning kind the page's script posts, not only the attachments"
file = "src/presentation/page_jumps.rs"
before = """        "warning" => Some(Jump::Warning),"""
after = """        "a kind no page posts" => Some(Jump::Warning),"""
red = [
    "presentation::page_jumps::tests::test_f7_in_the_page_posts_the_warning_and_the_window_reads_it_back",
    "presentation::page_jumps::tests::test_every_kind_the_script_posts_is_one_the_window_reads",
]
tests_last_seen = [
    { file = "src/presentation/page_jumps.rs", tests = 5 },
]

# ── Alt+A in the page window moves focus to the attachments list ────────────
#
# #84: a jump that speaks and goes nowhere is the promise the tester met,
# said out loud. The break drops the focus call from the attachments arm and
# leaves the sentence; the reading goes red with "does not move focus", and
# its companion goes red on its anchor.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "alt+a in the page window moves focus to the attachments list, not only says so"
file = "src/presentation/wx_app.rs"
suite = "attachments_are_reached_with_alt_a_in_both_views"
before = """                    Some(list) => {
                        list.set_focus();
                        let _ =
                            a11y.announce(&format!("Attachments, {}", attached), Priority::Normal);
                    }"""
after = """                    Some(list) => {
                        let _ = list;
                        let _ =
                            a11y.announce(&format!("Attachments, {}", attached), Priority::Normal);
                    }"""
red = [
    "test_the_page_window_moves_focus_on_each_jump_and_binds_nothing_on_the_browser",
    "test_the_reading_complains_when_focus_stays_put_or_a_key_is_bound_on_the_browser",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/attachments_are_reached_with_alt_a_in_both_views.rs", tests = 10 },
]

# ── The reader's Attachments item is on Alt+A ───────────────────────────────
#
# #84, Pratik's decision: Alt+A in both message windows, F8 retired there.
# The break puts F8 back on the reader's Go menu item; the reading goes red
# with "not on Alt+A", and its companion goes red on its anchor. The reader's
# own count-sentence test is in the library and not named here.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the reader's attachments item is on alt+a, not back on f8"
file = "src/presentation/wx_reader.rs"
suite = "attachments_are_reached_with_alt_a_in_both_views"
before = '''                "&Attachments\tAlt+A",'''
after = '''                "&Attachments\tF8",'''
red = [
    "test_the_readers_item_is_on_alt_a_and_f8_is_gone",
    "test_the_reading_complains_when_the_readers_item_goes_back_to_f8",
]
tests_last_seen = [
    { file = "src/presentation/wx_reader.rs", tests = 14 },
    { file = "tests/attachments_are_reached_with_alt_a_in_both_views.rs", tests = 10 },
]

# ── A message nobody read is never marked read ──────────────────────────────
#
# #25: a row selected for two seconds was marked read, and hearing a row takes
# longer than that, so a walk through a folder by ear marked every message
# stopped on. The rule answers nothing when no reading began, whatever is
# selected. The break is the old behaviour in the rule's own words: when
# nothing began, the selected unread message stands in as though its reading
# had begun just now. The case that selects a row for an hour with no reading
# begun goes red under Immediately, and the case that reads one message and
# then selects another goes red the same way, because the break stands the
# second message in too.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "a message nobody read is never marked read, whatever is selected"
file = "src/application/reading_habits.rs"
before = """    let (message, since) = began.filter(|(message, _)| Some(*message) == selected_unread)?;"""
after = """    let (message, since) = began
        .filter(|(message, _)| Some(*message) == selected_unread)
        .or_else(|| selected_unread.map(|message| (message, now)))?;"""
red = [
    "application::reading_habits::a_message_is_marked_read_after_it_was_read_and_never_after_it_was_selected::test_nothing_began_means_nothing_is_marked_however_long_a_row_is_selected",
    "application::reading_habits::a_message_is_marked_read_after_it_was_read_and_never_after_it_was_selected::test_a_message_that_began_reading_is_not_marked_once_another_is_selected",
]
tests_last_seen = [
    { file = "src/application/reading_habits.rs", tests = 25 },
]

# ── Selecting a row starts no clock ─────────────────────────────────────────
#
# #25: the clock that marks a message read started the moment a row was
# selected, and hearing a row takes longer than the wait, so a walk through a
# folder by ear marked every message stopped on. The break puts the write
# back into the selection handler, for the message the row landed on. The
# reading over the handler goes red with "touches reading_began"; its
# companion reads a snippet rather than the file and stays green, which is
# why one test is named.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "selecting a row starts no clock towards marking it read, whatever the wait"
file = "src/presentation/wx_app.rs"
suite = "moving_through_the_list_marks_nothing_read"
before = """                        s.selected_message_index = Some(idx);"""
after = """                        s.selected_message_index = Some(idx);
                        s.reading_began = s
                            .messages
                            .get(idx)
                            .map(|m| (m.message_id, std::time::Instant::now()));"""
red = [
    "test_selecting_a_row_records_nothing_about_reading",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/moving_through_the_list_marks_nothing_read.rs", tests = 13 },
]

# ── The timer asks the rule before it marks anything ────────────────────────
#
# The decision that turns time into a mark is one rule in reading_habits,
# held by six cases; a timer that decides for itself is a second copy of the
# decision, and the second copy is where the selection clock lived. The
# break drops the ask and marks the selected unread message at once, which
# is what the timer did under Immediately. The reading over the function goes
# red with "never asks whether_to_mark_read"; its companion reads a snippet
# and stays green.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. The break rewritten
# 2026-09-20 (11-11.1.1, #91) when the wait became the state's rather than
# a parameter, and measured again the same day.
[[guard]]
name = "the timer asks whether to mark what was read, not marks the selected one itself"
file = "src/presentation/wx_app.rs"
suite = "moving_through_the_list_marks_nothing_read"
before = """        let Some(row) = crate::application::reading_habits::whether_to_mark_read(
            s.reading_began,
            selected_unread,
            std::time::Instant::now(),
            s.marks_read,
        ) else {
            return;
        };"""
after = """        let Some(row) = selected_unread else {
            return;
        };"""
red = [
    "test_the_timer_asks_the_rule_and_keeps_no_clock_of_its_own",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/moving_through_the_list_marks_nothing_read.rs", tests = 13 },
]

# ── The sentence under Mark as read after is on the built page ──────────────
#
# #25: the setting's seven answers cannot say what the wait is counted from,
# and until 2026-09-18 it was counted from selection. The sentence saying it
# is counted from reading sits under the choice on the Reading tab, and the
# one test that builds that page in a window session reads it there. The
# break builds the sentence and destroys it instead of adding it to the
# section; the reading goes red with "holds no static text saying". Taken by
# hand first on 2026-09-18, then measured.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the sentence saying what mark as read after counts from is on the reading page, not only in the source"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """    read_sec.add(&markread_note, 0, SizerFlag::Expand | SizerFlag::All, 4);"""
after = """    markread_note.destroy();"""
red = [
    "test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# ── Mark as Read says which way it will go ──────────────────────────────────
#
# #27: the command said "Mark as Read" on every surface whatever the message's
# state, and toggled. The words for each state are one rule in
# marking_read, and the break makes both states answer the unread form,
# which is what every surface said until 2026-09-18. The read-state case in
# the module goes red, and so does the message-menu test in context_menu,
# whose second form takes its Mark entry's words from the same rule; the
# mnemonic and context-word cases stay green, because a rule that answers
# one form agrees with itself.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "mark as read says mark as unread on a read message, not the one word whatever the state"
file = "src/application/marking_read.rs"
before = """    if any_unread {
        Wording {"""
after = """    if any_unread || !any_unread {
        Wording {"""
red = [
    "application::marking_read::tests::test_a_read_message_is_offered_mark_as_unread",
    "application::context_menu::tests::test_a_message_menu_has_the_things_you_do_to_a_message",
]
tests_last_seen = [
    { file = "src/application/context_menu.rs", tests = 18 },
    { file = "src/application/marking_read.rs", tests = 5 },
]

# ── The context menu's second form is offered on a read message ─────────────
#
# #27: the context menu has two forms of its message list since 2026-09-18,
# and entries_for_messages picks by the message under the cursor. The break
# answers the unread form for a read message too, which is what the menu did
# before, and the message-menu test goes red on the Mark entry's words.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "the context menu on a read message offers mark as unread, not the unread form"
file = "src/application/context_menu.rs"
before = """    } else {
        MESSAGES_WITH_THE_ONE_UNDER_THE_CURSOR_READ
    }"""
after = """    } else {
        MESSAGES
    }"""
red = [
    "application::context_menu::tests::test_a_message_menu_has_the_things_you_do_to_a_message",
]
tests_last_seen = [
    { file = "src/application/context_menu.rs", tests = 18 },
]

# ── A letter bound on a list is consumed, so the search never gets it ───────
#
# #27: a plain letter on a SysListView32 is the start of the control's own
# search, on the WM_CHAR after the key-down, unless the key-down was handled
# and not skipped. wxdragon resets Skip(true) before every closure, so a
# handler that runs and says nothing is one the search still gets. The break
# drops the skip(false), which is the whole mechanism; the reading on the
# wired list goes red with the selection on Mango, and the companion on the
# list wired with another letter stays green, since it expects the search.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a letter wired on a list is consumed at the key-down, not left to the control's search"
file = "src/presentation/list_keys.rs"
suite = "mark_as_read_says_which_way_it_will_go"
before = """        event.skip(false);
        on_pressed(i64::from(selected));"""
after = """        on_pressed(i64::from(selected));"""
red = [
    "test_reading_a_the_letter_reaches_its_handler_on_a_real_list_and_the_search_does_not_get_it",
]
tests_last_seen = [
    { file = "src/presentation/list_keys.rs", tests = 3 },
    { file = "tests/mark_as_read_says_which_way_it_will_go.rs", tests = 14 },
]

# ── A tool's new label reaches the control ──────────────────────────────────
#
# #27: the toolbar's label follows the state, and wxdragon 0.9.17 offers no
# relabel, so it goes through TB_SETBUTTONINFOW with TBIF_TEXT. The break
# sends the message with no mask, so the control is told nothing; both
# readings of the label after the relabel go red on "Mark Read", through
# TB_GETBUTTONTEXTW and through get_accName, and the companion that read the
# old label before the relabel stays green.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a relabelled tool says the new label where the control and a screen reader read it"
file = "src/presentation/toolbar_text.rs"
suite = "mark_as_read_says_which_way_it_will_go"
before = """            dw_mask: TBIF_TEXT,"""
after = """            dw_mask: 0,"""
red = [
    "test_reading_b_a_relabelled_tool_says_the_new_label_where_the_control_is_asked",
    "test_reading_b_a_relabelled_tool_says_the_new_label_where_a_screen_reader_reads_it",
]
tests_last_seen = [
    { file = "src/presentation/toolbar_text.rs", tests = 1 },
    { file = "tests/mark_as_read_says_which_way_it_will_go.rs", tests = 14 },
]

# ── The words follow the row landed on ──────────────────────────────────────
#
# #27: the command's words are set from the message under the cursor on
# selection, after the toggle and when a flag lands on the row. The break
# drops the refresh from the selection handler, so arrowing onto a read
# message leaves every surface saying Mark as Read; the reading over the
# three sites goes red with "the selection handler never refreshes the
# wording", and its companions read snippets and stay green.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. Rewritten 2026-09-19 by
# 11-07 onto the cursor handler, which is on the focus event and hands the
# refresh the list; the break is the one it was, and it reddened the same
# one test.
[[guard]]
name = "landing on a row makes mark as read say which way it will go, not leave the last row's words"
file = "src/presentation/wx_app.rs"
suite = "mark_as_read_says_which_way_it_will_go"
before = """                    receipt_for_the_open_message(app);
                    // Mark as Read says which way it will go for the rows
                    // selected (#27, #30): the item, the tool and its tip.
                    refresh_mark_read_wording(&frame, toolbar_handle, &state, &list);"""
after = """                    receipt_for_the_open_message(app);
                    let _ = &list;"""
red = [
    "test_the_wording_is_refreshed_on_selection_after_the_toggle_and_when_a_flag_lands",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mark_as_read_says_which_way_it_will_go.rs", tests = 14 },
]

# ── M goes through the helper that consumes it ──────────────────────────────
#
# #27: M is wired on the message list through list_keys::wire_letter, which
# calls skip(false) so the control's search never gets the letter. The break
# wires the key with a bare KEY_DOWN binding that runs the toggle and says
# nothing about skip, so the dispatcher's Skip(true) stands and the search
# moves the cursor after every press; the reading over the key wiring goes
# red with the wire_letter anchor gone. The reading on the built list is
# not in this record's red list because it builds its own list and wires
# the helper itself; the record on list_keys.rs covers the helper.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. Rewritten 2026-09-19 by
# 11-07, the wiring capturing the list and the cache the toggle takes now;
# the break is the one it was, and it reddened the same one test.
[[guard]]
name = "m on the message list is wired through the helper that consumes it, not bound bare"
file = "src/presentation/wx_app.rs"
suite = "mark_as_read_says_which_way_it_will_go"
before = """            crate::presentation::list_keys::wire_letter(&msg_list, 'M', {
                let state = state.clone();
                let ui_tx = ui_tx.clone();
                let runtime = runtime.clone();
                let a11y = a11y.clone();
                let list = msg_list;
                let cache = message_cache.clone();
                move |_row| {"""
after = """            msg_list.bind_internal(EventType::KEY_DOWN, {
                let state = state.clone();
                let ui_tx = ui_tx.clone();
                let runtime = runtime.clone();
                let a11y = a11y.clone();
                let list = msg_list;
                let cache = message_cache.clone();
                move |event| {
                    if event.get_key_code() != Some(77) {
                        return;
                    }"""
red = [
    "test_the_menu_item_and_the_key_run_one_toggle_and_the_key_says_one_word",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/mark_as_read_says_which_way_it_will_go.rs", tests = 14 },
]

# ── The first Space starts no clock ─────────────────────────────────────────
#
# #25 reopened on 2026-09-18 on the tester's word: reading the snippet is
# not reading. The mail read-aloud closure wrote reading_began before the
# cycle had decided which form the press reads, so the first Space, subject,
# sender and snippet, started the clock. 11-05.1 moved the write into a
# second closure the wiring calls only under the whole reading, and made
# which press counts one function over the depth, read_aloud::
# what_a_press_starts. Three records: the write put back where 11-05 had
# it, and the decision answering the whole reading for the short form,
# measured on the library and on the target, because a record has one
# suite and the module's case and the target's case live in different ones.

# The write moved back into the lookup closure, beside the clone of the
# row, where 11-05 wrote it and where it runs for every press. The
# short-form reading goes red with "the first Space starts the clock"; the
# whole-reading reading stays green, since the second closure still writes
# it too, and the companions read snippets. Rewritten onto the lookup as
# 11-08 left it, which asks which message the row stands for rather than
# the flat list at the row's index, and measured again: one red, as
# before.
#
# Measured 2026-09-19 on the branch the record was rewritten on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the first space, the short form, starts no clock towards marking the message read"
file = "src/presentation/wx_app.rs"
suite = "moving_through_the_list_marks_nothing_read"
before = """                            let s = lock_state(&state);
                            let message = s.the_loaded_message_the_row_stands_for(index)?.clone();"""
after = """                            let mut s = lock_state(&state);
                            let message = s.the_loaded_message_the_row_stands_for(index)?.clone();
                            s.reading_began = Some((message.message_id, std::time::Instant::now()));"""
red = [
    "test_the_short_form_records_nothing_about_reading",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/moving_through_the_list_marks_nothing_read.rs", tests = 13 },
]

# The decision answering the whole reading for the short form, which is what
# the stub the red commit carried answered and what the tree did in effect
# before 11-05.1. The module's case goes red on the library.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the library.
[[guard]]
name = "the short form is not the whole reading, on the library"
file = "src/presentation/read_aloud.rs"
before = """        Depth::Short => WhatBegan::Nothing,"""
after = """        Depth::Short => WhatBegan::TheWholeReading,"""
red = [
    "presentation::read_aloud::tests::test_the_short_form_starts_nothing_towards_marking_read",
]
tests_last_seen = [
    { file = "src/presentation/read_aloud.rs", tests = 51 },
]

# The same break on the target, where the case over the real cycle and the
# real decision goes red at the first press.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the short form is not the whole reading, on the target"
file = "src/presentation/read_aloud.rs"
suite = "moving_through_the_list_marks_nothing_read"
before = """        Depth::Short => WhatBegan::Nothing,"""
after = """        Depth::Short => WhatBegan::TheWholeReading,"""
red = [
    "test_a_first_space_marks_nothing_and_a_second_does",
]
tests_last_seen = [
    { file = "src/presentation/read_aloud.rs", tests = 51 },
    { file = "tests/moving_through_the_list_marks_nothing_read.rs", tests = 13 },
]

# ── 11-06.1: where the cursor lands after a delete, and after a re-read ──────
#
# #76: deleting a message put the cursor at the top of the list, because the
# rule that lands it on the next message lived in the window's own record of
# the selection and nothing set the control's focus to it. The rules are
# `presentation::landing_after_a_removal`, held by cases on the library, and
# the control following them is held by the built list in
# `tests/deleting_a_message_lands_on_the_next_one.rs`.

# The row before the set dropped from the answer, so a delete of the last
# row, or of a set that reaches the end, lands nowhere: the cursor is left
# wherever the control puts it, which for a virtual list is no row at all.
# The two cases that end at the last row and the case holding both ends go
# red; the first draft named those three and the runner agreed.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the library.
[[guard]]
name = "deleting the last row lands on the row before it, not nowhere"
file = "src/presentation/landing_after_a_removal.rs"
before = """    Some(
        after_the_set
            .or(before_the_set)
            .unwrap_or(len_after - 1)
            .min(len_after - 1),
    )"""
after = """    after_the_set.map(|row| row.min(len_after - 1))"""
red = [
    "presentation::landing_after_a_removal::tests::test_the_last_row_removed_lands_on_the_row_before_it",
    "presentation::landing_after_a_removal::tests::test_a_set_at_the_end_lands_on_the_row_before_the_first_removed",
    "presentation::landing_after_a_removal::tests::test_a_set_holding_both_ends_lands_on_the_last_row_left",
]
tests_last_seen = [
    { file = "src/presentation/landing_after_a_removal.rs", tests = 16 },
]

# A re-read that moves the cursor on every load, whatever row the cursor's
# message is on now, which is what 10-02 refused: re-selecting moves focus
# and takes the screen reader's cursor with it. The no-move case goes red.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the library.
[[guard]]
name = "a re-read that left the cursor's message where it was moves nothing, not the cursor every load"
file = "src/presentation/landing_after_a_removal.rs"
before = """    new_index.filter(|new| Some(*new) != old_index)"""
after = """    let _ = old_index;
    new_index"""
red = [
    "presentation::landing_after_a_removal::tests::test_a_message_still_on_its_row_is_no_move_and_neither_is_one_that_is_gone",
]
tests_last_seen = [
    { file = "src/presentation/landing_after_a_removal.rs", tests = 16 },
]

# The landing dropped from the control: the helper still answers the row
# the rule chose, the record of the selection still moves, and the control
# is left with whatever the count change left it, which is the tree before
# this plan. On the built list the middle-row landing raises no focus event
# and the last-row landing leaves the control holding nothing; the source
# reading complains that the helper sets nothing on the control. The
# companions read snippets and stay green.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a removal lands the cursor on the control, not only in the record of the selection"
file = "src/presentation/wx_app.rs"
suite = "deleting_a_message_lands_on_the_next_one"
before = """    let row = landing_after_a_removal::where_to_land(removed, len_after)?;
    put_the_cursor_on(list, row);"""
after = """    let row = landing_after_a_removal::where_to_land(removed, len_after)?;
    let _ = list;"""
red = [
    "test_deleting_a_middle_row_lands_the_cursor_on_the_row_that_followed_it",
    "test_deleting_the_last_row_lands_the_cursor_on_the_row_before_it",
    "test_a_removal_lands_the_cursor_on_the_control_through_the_rule",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/deleting_a_message_lands_on_the_next_one.rs", tests = 19 },
]

# A re-read that puts the cursor on its message's row whatever row it was
# on, which moves focus on every load and is what 10-02 refused. On the
# built list the re-read that changed nothing raises a focus event and the
# helper answers a row; the source reading complains that the helper does
# not ask whether_to_move.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a re-read moves the cursor only when its message changed row, not on every load"
file = "src/presentation/wx_app.rs"
suite = "deleting_a_message_lands_on_the_next_one"
before = """    let row = landing_after_a_removal::whether_to_move(old_index, now)?;"""
after = """    let row = now?;
    let _ = old_index;"""
red = [
    "test_a_re_read_that_left_the_cursors_message_where_it_was_raises_no_focus_event",
    "test_a_re_read_keeps_the_cursor_on_its_message_and_moves_it_only_when_its_row_changed",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/deleting_a_message_lands_on_the_next_one.rs", tests = 19 },
]

# #83, the third task: a delete says Delete once at the key and nothing on
# success; the outcome is shown for the eye when the row left and spoken
# when it stayed. Two breaks, each the tree before this plan on one half.

# The success line sent as StatusUpdated again, which is spoken at Normal,
# so "Deleted: subject" follows the landed row's reading as it did before
# 2026-09-18. The outcome reading goes red; the companions read snippets.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a delete that went through is shown for the eye, not spoken over the landed row"
file = "src/presentation/wx_app.rs"
suite = "deleting_a_message_lands_on_the_next_one"
before = """            say(UIUpdate::Shown(next.said));"""
after = """            say(UIUpdate::StatusUpdated(next.said));"""
red = [
    "test_the_outcome_of_a_delete_is_shown_when_the_row_left_and_spoken_when_it_stayed",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/deleting_a_message_lands_on_the_next_one.rs", tests = 19 },
]

# The subject back in the spoken word at the key, "Deleting subject...",
# which is the first of the two sentences the tester was waiting through.
# The one-word reading goes red.
#
# Measured 2026-09-18 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. Rewritten 2026-09-19 by
# 11-07 onto the arm over a set, where the word is said once before the
# loop and the subject comes from the set's shown line; the break is the
# one it was, and it reddened the same one test.
[[guard]]
name = "a delete says the one word delete at the key, not the subject again"
file = "src/presentation/wx_app.rs"
suite = "deleting_a_message_lands_on_the_next_one"
before = """                            say_the_one_word(&a11y, "Delete");"""
after = """                            say_the_one_word(&a11y, &what_is_being_done("Deleting", &chosen));"""
red = [
    "test_a_delete_says_the_one_word_delete_at_the_key_and_writes_the_fuller_line",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/deleting_a_message_lands_on_the_next_one.rs", tests = 19 },
]

# ── 11-06.2: focus arriving on the list lands on a row (#87) ──────────────

# The landing fired on every focus event, not only when the control holds
# no row: coming back to the list with the cursor on row 1 asks the rule
# and moves the cursor to the remembered row, which takes a screen
# reader's cursor away under the person, the fault the folder tree was
# fixed for. The built window's third step goes red, inside the one test.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "focus arriving on a list that holds a row moves nothing, not the cursor on every focus event"
file = "src/presentation/list_arrival.rs"
suite = "tab_from_the_tree_lands_on_the_newest_message"
before = """        if the_focused_row_of(&owner).is_some() {
            return;
        }"""
after = """        let _ = the_focused_row_of(&owner);"""
red = [
    "test_focus_arriving_from_the_tree_lands_on_the_remembered_row_or_the_first_and_an_empty_list_says_so",
]
tests_last_seen = [
    { file = "src/presentation/list_arrival.rs", tests = 0 },
    { file = "tests/tab_from_the_tree_lands_on_the_newest_message.rs", tests = 1 },
]

# The first row whatever is remembered: somebody who left the list on row
# 40 and comes back to it after the control lost the row lands at the top.
# The built window's second step goes red, inside the one test; the rule's
# own case for a remembered row still there goes red on the library too,
# which this record, measured on the target, does not see.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "focus arriving lands on the remembered row when it is still there, not the first row whatever was remembered"
file = "src/presentation/landing_after_a_removal.rs"
suite = "tab_from_the_tree_lands_on_the_newest_message"
before = """    Some(remembered.filter(|row| *row < len).unwrap_or(0))"""
after = """    let _ = remembered;
    Some(0)"""
red = [
    "test_focus_arriving_from_the_tree_lands_on_the_remembered_row_or_the_first_and_an_empty_list_says_so",
]
tests_last_seen = [
    { file = "src/presentation/landing_after_a_removal.rs", tests = 16 },
    { file = "tests/tab_from_the_tree_lands_on_the_newest_message.rs", tests = 1 },
]

# ── 11-07: the list selects a set, and every command acts on it (#30, #27) ──

# The duplicate removal dropped: a message two rows name, which a conversation
# row and a message row can both do, is written twice and counted twice, so
# "4 messages marked read" over three. The held-once case and the batch case
# that counts the set go red; the rest never choose a message twice.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "a message two selected rows name is chosen once, not once per row"
file = "src/application/choosing_messages.rs"
before = """        held.insert(message.row_id) && {"""
after = """        let _ = held.insert(message.row_id);
        {"""
red = [
    "application::choosing_messages::tests::test_a_message_named_by_two_rows_is_held_once_where_it_was_first",
    "application::choosing_messages::tests::test_a_batch_with_a_refusal_says_how_many_went_and_how_many_did_not",
]
tests_last_seen = [
    { file = "src/application/choosing_messages.rs", tests = 18 },
]

# The bound gone: nothing is ever too many, so Shift+End on a folder of
# twelve thousand queues twelve thousand cache writes and server changes and
# the window stops answering keys. The bound case goes red on its own.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "a command over more than the bound is refused with a sentence, not run"
file = "src/application/choosing_messages.rs"
before = """    if count <= MOST_ROWS_WORTH_SELECTING {
        return None;
    }"""
after = """    let most = MOST_ROWS_WORTH_SELECTING.max(count);
    if count <= most {
        return None;
    }"""
red = [
    "application::choosing_messages::tests::test_the_bound_is_select_alls_and_is_refused_only_above_it",
]
tests_last_seen = [
    { file = "src/application/choosing_messages.rs", tests = 18 },
]

# SingleSel back on the message list, which is the tree before this plan:
# Shift+Down moves the selection instead of growing it and every command
# reads one row. The style reading goes red on the target.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the message list selects more than one row, not one however many Shift chose"
file = "src/presentation/wx_app.rs"
suite = "every_command_acts_on_the_selection"
before = """                .with_style(ListCtrlStyle::Report | ListCtrlStyle::HRules | ListCtrlStyle::Virtual)"""
after = """                .with_style(
                    ListCtrlStyle::Report
                        | ListCtrlStyle::SingleSel
                        | ListCtrlStyle::HRules
                        | ListCtrlStyle::Virtual,
                )"""
red = [
    "test_the_message_list_is_built_without_single_selection",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/every_command_acts_on_the_selection.rs", tests = 17 },
]

# The Mark arm reading the cursor row alone, built into a set of one from
# selected_message_index rather than read off the control: Mark as Read
# over five marks one, and a conversation row marks nothing of its thread.
# The flag-commands reading goes red on the target, since toggle_read_state
# no longer reaches chosen_messages.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "mark as read acts on every selected message, not the cursor row alone"
file = "src/presentation/wx_app.rs"
suite = "every_command_acts_on_the_selection"
before = """    let chosen = match chosen_messages(
        state,
        cache,
        list,
        crate::application::conversations::AConversationReaches::TheWholeAccount,
    ) {
        Ok(chosen) => chosen,
        Err(why) => return send_refusal(tx, rt, &why),
    };
    if chosen.is_empty() {
        return send_refusal(tx, rt, "No message selected");
    }"""
after = """    let chosen = {
        use crate::application::choosing_messages::{Members, MessageRef, what_the_selection_holds};
        let s = lock_state(state);
        let rows: Vec<usize> = s.selected_message_index.into_iter().collect();
        what_the_selection_holds(&rows, |row| {
            s.messages.get(row).map(|message| {
                Members::AMessage(MessageRef {
                    row_id: message.message_id,
                    uid: message.uid,
                    subject: message.subject.clone(),
                    read: message.read,
                    starred: message.starred,
                })
            })
        })
    };
    if chosen.is_empty() {
        return send_refusal(tx, rt, "No message selected");
    }"""
red = [
    "test_star_mark_as_read_and_the_labels_act_on_the_selection_with_one_sentence",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/every_command_acts_on_the_selection.rs", tests = 17 },
]

# The bound dropped from the Delete arm: Shift+End on a folder of twelve
# thousand and Delete queues twelve thousand server changes and the window
# stops answering keys. The delete reading goes red on the target.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "delete over a selection refuses above the bound, not runs whatever the size"
file = "src/presentation/wx_app.rs"
suite = "every_command_acts_on_the_selection"
before = """                            if let Some(why) = too_many(chosen.messages.len()) {
                                return send_refusal(&ui_tx, &runtime, &why);
                            }
                            if let Some(question) = deleting_asks(&chosen) {"""
after = """                            let _ = too_many;
                            if let Some(question) = deleting_asks(&chosen) {"""
red = [
    "test_delete_acts_on_the_selection_asks_for_a_conversation_and_says_delete_once",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/every_command_acts_on_the_selection.rs", tests = 17 },
]

# A refusal for a message the destination already holds read as a refusal:
# the server did the move before the restart, or another client did, and
# the replay then puts the row back in a folder the server no longer has the
# message in and says the server refused. The already-done case and the two
# loopback cases where the scripted server refuses and finds the message go
# red (#86, 11-07.1).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "a refused replay of a message already where it was going is read as done, not put back"
file = "src/application/moves_waiting.rs"
before = """            WhereItIsNow::WhereTheMoveWantedIt => Replayed::AlreadyDone,"""
after = """            WhereItIsNow::WhereTheMoveWantedIt => Replayed::Refused(why.to_string()),"""
red = [
    "application::moves_waiting::tests::test_a_refusal_for_a_message_already_where_it_was_going_is_already_done",
    "application::moves_waiting::tests::test_a_refusal_for_a_message_the_destination_already_holds_is_already_done",
    "application::moves_waiting::tests::test_a_refused_copy_the_destination_already_holds_is_already_done",
]
tests_last_seen = [
    { file = "src/application/moves_waiting.rs", tests = 45 },
]

# The marker dropped from the forgetting: a row this program filed is
# deleted the moment a caller names its number as one the server does not
# list, which is a sent copy kept here and, since 11-07.1, a move made here
# and not yet at the server. The two cache cases that hold the marker go
# red. The sync case in `application::moves_waiting` that reads Archive over
# a moved row does not, and the first draft of this record said it would:
# the read leaves marked rows out of the numbers it compares as well, so the
# forgetting is never asked about the row, and no one-place break reaches
# that case through either guard alone (#86, 11-07.1).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library; the first draft's red
# list was corrected by hand from the runner's answer before this.
[[guard]]
name = "a row this program filed is not forgotten when the server does not list its number"
file = "src/data/message_cache/messages.rs"
before = """                "DELETE FROM messages WHERE folder_id = ?1 AND uid = ?2 AND filed_here = 0","""
after = """                "DELETE FROM messages WHERE folder_id = ?1 AND uid = ?2","""
red = [
    "data::message_cache::messages::tests::test_a_message_filed_into_a_folder_a_server_fills_is_marked_as_one_filed_here",
    "data::message_cache::messages::tests::test_a_copy_kept_here_survives_the_sync_that_finds_it_is_not_on_the_server",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# The line for a row that left spoken again, as the outcome was until
# 2026-09-18: "Move", then the landed row, then "Moved to Archive: subject",
# the second sentence the tester was waiting through on every move (#83,
# #86). The made-here reading goes red on the target.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. The break re-indented
# on 2026-09-19 by 11-07.2, which nests the loop one level deeper for the
# asks of several accounts; the code is the same and was measured again.
[[guard]]
name = "a row that left at the key has its line shown, not spoken as well"
file = "src/presentation/wx_app.rs"
suite = "a_move_completes_here_first"
before = """                        take_row_out_of_the_list(state, list, ask.asked.message_row_id);
                        send_shown(tx, rt, &made.shown);"""
after = """                        take_row_out_of_the_list(state, list, ask.asked.message_row_id);
                        send_status(tx, rt, &made.shown);"""
red = [
    "test_the_row_leaves_and_the_line_is_shown_before_any_session_is_asked_for",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_move_completes_here_first.rs", tests = 15 },
]

# The undo dropped from the arm a refusal arrives on: the server said no,
# the sentence says the message is back where it was, and the row stays in
# the folder the server never moved it to until the next read of either
# folder. The refusal reading goes red on the target (#86).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a refused move is undone here, not left made with the refusal spoken over it"
file = "src/presentation/wx_app.rs"
suite = "a_move_completes_here_first"
before = """            if let Err(why) = undo_here(cache, waiting) {
                tracing::error!(
                    "A change the server refused could not be undone here for message {}: {why}",
                    waiting.message_row_id
                );
            }"""
after = """            let _ = waiting.message_row_id;"""
red = [
    "test_a_refusal_puts_the_change_back_here_and_is_said_at_high",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_move_completes_here_first.rs", tests = 15 },
]

# A check wired into the arm that runs when the network comes back, which
# is how a waiting move would reach the server with nobody having asked:
# closing a laptop on a train and opening it in an office would write at
# somebody's mail server. Guardrail 7. The network-back reading goes red on
# the target; `nothing_sends_a_flag_change_unasked` would go red too, on
# its own run, which this record's one suite does not see (#86).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the network coming back replays no waiting move, not a check that would"
file = "src/presentation/wx_app.rs"
suite = "a_move_completes_here_first"
before = """        UIUpdate::TheNetworkIsBack => {
            back_online_offer.panel.show(true);"""
after = """        UIUpdate::TheNetworkIsBack => {
            spawn_mail_sync(
                AppHandles { state, tx, rt },
                lock_state(state).accounts.clone(),
                None,
                crate::application::mail_sync::WhatThisSyncIsFor::WhateverHasChanged,
            );
            back_online_offer.panel.show(true);"""
red = [
    "test_the_network_coming_back_replays_no_waiting_move",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_move_completes_here_first.rs", tests = 15 },
]

# The activation binding dropped from the Move dialog's tree, which is the
# dialog as the tester met it on 2026-09-18: Enter on the chosen folder does
# nothing, since no button is the default and nothing listens to the tree.
# The dialog reading goes red on the target (#86).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "enter on a folder in the move dialog is the move, not nothing"
file = "src/presentation/wx_destination.rs"
suite = "a_move_completes_here_first"
before = """        let destinations = destinations.clone();
        tree.on_item_activated(move |_event| {
            if what_a_selection_means(&destinations, tree_walk::where_the_selection_sits(&tree))
                .is_some()
            {
                dialog.end_modal(ID_OK);
            }
        });"""
after = """        let _ = destinations.clone();"""
red = [
    "test_enter_on_a_folder_in_the_dialog_is_the_move",
]
tests_last_seen = [
    { file = "src/presentation/wx_destination.rs", tests = 6 },
    { file = "tests/a_move_completes_here_first.rs", tests = 15 },
]

# The bytes not kept before the append goes out, as the first step of a
# crossing would be with the store filtered away: the message is fetched
# and appended and nothing here says a crossing is owed, so a restart
# between the fetch and the ending has nothing to resume from and fetches
# again, or finds a source that has stopped answering (#86, 11-07.2). The
# case that reads the store from inside the append goes red, and the
# queue's cases that read what the store holds after a hang-up or through
# the ceiling.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "the bytes of a crossing are kept before the append, not never"
file = "src/application/mail_across_accounts.rs"
before = """    let held = recording.cache.and_then(|cache| {"""
after = """    let held = recording.cache.filter(|_| false).and_then(|cache| {"""
red = [
    "application::mail_across_accounts::tests::test_the_message_is_already_on_the_disk_when_the_append_goes_out",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_cannot_be_asked_answers_not_reached_and_the_bytes_stay",
    "application::moves_waiting::tests::test_a_message_too_large_to_hold_is_fetched_and_the_store_says_so",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
]

# An append nobody could settle read as a refusal: the destination hung up
# and could not be asked, so the message may be in one place or in two, and
# a refusal is undone, which puts the row back here as if the message were
# nowhere else. The one line the crossing's replay must not get wrong
# (T-11-92); the value case and the hang-up case go red (#86, 11-07.2).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "an append nobody could settle is not reached, not a refusal to undo"
file = "src/application/moves_waiting.rs"
before = """        MovedAcross::ItIsNotKnownWhereItIs(_) => Replayed::NotReached,"""
after = """        MovedAcross::ItIsNotKnownWhereItIs(why) => Replayed::Refused(why.clone()),"""
red = [
    "application::moves_waiting::tests::test_an_append_nobody_could_settle_is_not_reached_and_never_a_refusal",
    "application::moves_waiting::tests::test_a_destination_that_hung_up_and_cannot_be_asked_answers_not_reached_and_the_bytes_stay",
]
tests_last_seen = [
    { file = "src/application/moves_waiting.rs", tests = 45 },
]

# The ceiling on a message kept while it moves taken away: a message of any
# size is written to the store, which is the store's own ceiling test, the
# crossing's case over a one-byte ceiling and the queue's reading of the
# store's answer, all red (#86, 11-07.2; the number is the privacy page's
# one number for three stores and is not moved).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "a message over the ceiling is not held while it moves"
file = "src/data/message_cache/moves_in_flight.rs"
before = """        if i64::try_from(moving.raw.len()).is_ok_and(|size| size > self.largest_move_kept) {"""
after = """        if i64::try_from(moving.raw.len()).is_ok_and(|size| size > i64::MAX) {"""
red = [
    "data::message_cache::moves_in_flight::tests::test_a_message_too_large_to_keep_leaves_nothing_to_resume_from",
    "application::mail_across_accounts::tests::test_a_message_too_large_to_keep_still_moves",
    "application::moves_waiting::tests::test_a_message_too_large_to_hold_is_fetched_and_the_store_says_so",
]
tests_last_seen = [
    { file = "src/application/mail_across_accounts.rs", tests = 43 },
    { file = "src/application/moves_waiting.rs", tests = 45 },
    { file = "src/data/message_cache/moves_in_flight.rs", tests = 13 },
]

# A session asked for in the move arm before the change is made here,
# which is the wait the tester felt on every move: the row leaves once a
# server has answered and not before. The arms reading goes red on the
# target (#86, 11-07.2).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a crossing is made here before any session is asked for, not after"
file = "src/presentation/wx_app.rs"
suite = "a_move_across_accounts_completes_here_first"
before = """    let into_for_the_worker = into.clone();"""
after = """    let _ = rt.block_on(crate::application::mail_session::the_session_at(
        &destination_account,
    ));
    let into_for_the_worker = into.clone();"""
red = [
    "test_a_move_or_a_copy_to_another_account_completes_here_first",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_move_across_accounts_completes_here_first.rs", tests = 11 },
]

# The store of held bytes read again for a dialog, which is the question
# at start phase 4.1 asked and 11-07.2 retired: a crossing is what the
# person asked for, and a restart finishes it from the held bytes at the
# next check rather than asking. The retirement reading goes red on the
# target (#86).
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a restart asks no question about a crossing, not a dialog from the held bytes"
file = "src/presentation/wx_app.rs"
suite = "a_move_across_accounts_completes_here_first"
before = """    let every_account: Vec<Account> = lock_state(&state).accounts.clone();"""
after = """    let every_account: Vec<Account> = lock_state(&state).accounts.clone();
    let _ = crate::data::message_cache::MessageCache::new(std::path::PathBuf::new(), None)
        .and_then(|cache| cache.moves_that_did_not_finish());"""
red = [
    "test_a_restart_asks_no_question_about_a_crossing",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_move_across_accounts_completes_here_first.rs", tests = 11 },
]

# The crossings offered to the source account's check alone, so a crossing
# waits for an account whose check may never come while the destination is
# checked every few minutes; the plan put this record on the sync, but the
# sync does not read the table, and the query that answers either account
# does. The table's case, the two of the replay at the destination's check
# and the made-here case, which reads the waiting crossings from the
# destination's side, go red (#86, 11-07.2); the first draft named three
# and the runner answered four, and the record was corrected by hand
# before it was measured again.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, on the library.
[[guard]]
name = "a crossing is offered at a check of either account, not the source alone"
file = "src/data/message_cache/moves_waiting.rs"
before = """                   AND (account_id = ?1 OR to_account_id = ?1)"""
after = """                   AND account_id = ?1"""
red = [
    "data::message_cache::moves_waiting::tests::test_a_crossing_is_offered_at_a_check_of_either_account",
    "application::moves_waiting::tests::test_a_crossing_is_replayed_at_a_check_of_the_destination_account",
    "application::moves_waiting::tests::test_two_crossings_are_replayed_in_the_order_they_were_asked",
    "application::moves_waiting::tests::test_a_crossing_made_here_moves_the_row_into_the_other_accounts_folder_and_waits",
]
tests_last_seen = [
    { file = "src/application/moves_waiting.rs", tests = 45 },
    { file = "src/data/message_cache/moves_waiting.rs", tests = 15 },
]

# The one ordering every expression about the row message reads, put back
# to the newest by arrival, which is what the row was until 11-08: the
# preview, the sender said first, the snippet and the sort all move to the
# last message in the conversation, which is the row the tester reported
# (#31). Every case of the target that reads the rule through the listing
# goes red but the conversation of one, where the newest and the originator
# are the same message: the four read patterns, the cell, the sort, and
# the fetch list's case, which asks which message the row is before it
# asks what is missing. The first draft named six; the measurement at
# task 2's red could not see the seventh, since that case was red under
# its own stub, and the measurement on the green tree found it; corrected
# by hand and measured again.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a conversation row stands for the first unread or the originator, not the newest"
file = "src/presentation/message_columns.rs"
suite = "a_conversation_row_stands_for_one_message"
before = """             ORDER BY r.read ASC, r.received_at ASC, r.id ASC LIMIT 1)\""""
after = """             ORDER BY r.received_at DESC, r.id DESC LIMIT 1)\""""
red = [
    "test_with_nothing_read_the_row_stands_for_the_originator",
    "test_with_later_messages_read_the_row_still_stands_for_the_originator",
    "test_with_the_first_messages_read_the_row_stands_for_the_first_unread",
    "test_with_everything_read_the_row_stands_for_the_originator_again",
    "test_the_correspondent_cell_says_the_row_messages_sender_first_and_everyone_once",
    "test_sorting_by_correspondent_orders_conversations_by_their_row_messages_senders",
    "test_selecting_a_row_asks_for_the_conversations_missing_text_with_the_row_message_first",
]
tests_last_seen = [
    { file = "src/presentation/message_columns.rs", tests = 43 },
    { file = "tests/a_conversation_row_stands_for_one_message.rs", tests = 18 },
]

# The row message's sender dropped from the front of the Correspondent
# cell, so the cell lists the senders in stored order again and the first
# name heard on a thread row is whoever wrote first, not the message the
# row previews (#31). The cell case on the target goes red; the library's
# own cell test would too, and this record measures the target.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the correspondent cell says the row message's sender first, not the senders in stored order"
file = "src/presentation/message_rows.rs"
suite = "a_conversation_row_stands_for_one_message"
before = """            one_first_then_everyone(&conversation.stands_for.from, &conversation.senders)"""
after = """            everyone_in(&conversation.senders)"""
red = [
    "test_the_correspondent_cell_says_the_row_messages_sender_first_and_everyone_once",
]
tests_last_seen = [
    { file = "src/presentation/message_rows.rs", tests = 41 },
    { file = "tests/a_conversation_row_stands_for_one_message.rs", tests = 18 },
]

# The cursor handler reading the flat list at the row's index again, which
# under conversation view is a message unrelated to the row: the preview,
# the body fetch and the receipt all take that message, the defect #31 did
# not name and phase 11's decision 14 fixes (T-11-29). The handler reading
# on the target goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the cursor handler previews the message the row stands for, not the flat row at its index"
file = "src/presentation/wx_app.rs"
suite = "a_conversation_row_stands_for_one_message"
before = """                        let stands_for = s.the_row_stands_for(idx);"""
after = """                        let stands_for = s.messages.get(idx).map(|message| RowMessage {
                            id: message.message_id,
                            uid: message.uid,
                            from: message.from.clone(),
                        });"""
red = [
    "test_the_cursor_handler_previews_the_row_message_and_starts_the_fetch",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_conversation_row_stands_for_one_message.rs", tests = 18 },
]

# The reading gate dropped from the conversation's fetch, so selecting a
# thread row brings text under a Message Text box that forbids it
# (T-11-31). The fetch reading on the target goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a conversation's text is fetched under the reading gate, not whatever the box says"
file = "src/presentation/wx_app.rs"
suite = "a_conversation_row_stands_for_one_message"
before = """    let reading = crate::application::allowed::allowed_for(&account.id).reading;"""
after = """    let reading = true;"""
red = [
    "test_the_conversations_text_is_one_bounded_chunk_under_the_reading_gate",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_conversation_row_stands_for_one_message.rs", tests = 18 },
]

# The conversation tree putting the cursor on the root whatever message
# was asked for, so Enter on arrival opens the whole conversation rather
# than the message the row stood for (#31). The tree reading on the target
# goes red; the pure rule's case stays green, since the rule is untouched.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the conversation tree opens on the message the row stood for, not the root whatever was asked"
file = "src/presentation/wx_thread_view.rs"
suite = "a_conversation_row_stands_for_one_message"
before = """    let opening = where_to_open(nodes, open_on).and_then(|at| items[at].clone());"""
after = """    let opening: Option<TreeItemId> = None;"""
red = [
    "test_enter_opens_the_conversation_on_the_row_message",
]
tests_last_seen = [
    { file = "src/presentation/wx_thread_view.rs", tests = 9 },
    { file = "tests/a_conversation_row_stands_for_one_message.rs", tests = 18 },
]

# ── A conversation is Gmail's own, and a late parent joins it (11-08.1, #88) ─
#
# X-GM-THRID dropped from the Gmail fields again, which is the tree as it
# was until 2026-09-19: the field the tester's threads were split for the
# absence of. The rewritten pinning test goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "the conversation id is asked for where the server has it, not left out"
file = "src/service/protocols/imap.rs"
before = """const GMAIL_FIELDS: &str = "X-GM-MSGID X-GM-THRID X-GM-LABELS";"""
after = """const GMAIL_FIELDS: &str = "X-GM-MSGID X-GM-LABELS";"""
red = ["service::protocols::imap::tests::test_the_thread_id_is_asked_for_where_the_server_has_it"]
tests_last_seen = [
    { file = "src/service/protocols/imap.rs", tests = 102 },
]

# The earliest string beating the server's word in the merge, so a
# conversation Gmail named is renamed after a header root that happens to
# sort first, and two Gmail keeps apart settle under one (T-11-98). The
# four merge cases over the server's word go red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "the server's word wins the merge, not the earliest string"
file = "src/application/thread_identity.rs"
before = """    let the_servers_first = merging.iter().copied().find(|name| is_the_servers(name));"""
after = """    let the_servers_first: Option<&str> = None;"""
red = [
    "application::thread_identity::tests::the_servers_word_wins_the_merge_over_the_earliest_string",
    "application::thread_identity::tests::a_conversation_the_server_named_is_not_rewritten_onto_a_header_root",
    "application::thread_identity::tests::two_conversations_the_server_named_apart_stay_apart",
    "application::thread_identity::tests::a_header_root_between_two_of_the_servers_conversations_joins_the_first_found",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 48 and a
    # measurement here that found the same one. The cache's own reading of a
    # reply whose header names a conversation the server also named goes
    # through this merge, in a file this record had never named.
    "data::message_cache::server_thread_ids::tests::test_naming_a_reply_brings_its_header_named_conversation_under_the_word",
]
tests_last_seen = [
    { file = "src/application/thread_identity.rs", tests = 39 },
    { file = "src/data/message_cache/server_thread_ids.rs", tests = 5 },
]

# The in-memory pass joining two conversations the store keeps apart by
# their headers, so the tree opened from a Gmail row holds a member the
# row never counted and a reply sits at depth one under a root the store
# filed elsewhere. The kept-apart case goes red; the naming cases stay
# green because the name still comes from the word.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "the headers do not join two conversations the store keeps apart"
file = "src/application/threading.rs"
before = """        if let (Some(x), Some(y)) = (word_a, word_b)
            && x != y
        {
            return;
        }"""
after = """        let _ = (word_a, word_b);"""
red = ["application::threading::tests::test_two_messages_the_store_filed_apart_are_not_joined_by_their_headers"]
tests_last_seen = [
    { file = "src/application/threading.rs", tests = 29 },
]

# The in-memory pass handed no conversation again, which is the tree as it
# was until 2026-09-19: a group named after its least Message-ID, which the
# conversation window's lookup by the stored name finds only when the root
# happens to sort first, and on Gmail's names never. The page reading on the
# target goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the page is threaded over the store's word, not over nothing"
file = "src/presentation/wx_app.rs"
suite = "a_conversation_is_gmails_own_and_a_late_parent_joins_it"
before = """            conversation: row.thread_id.clone(),"""
after = """            conversation: None,"""
red = ["test_the_page_is_threaded_over_the_stores_word"]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs", tests = 14 },
]

# The once-only pass dropped from the check, so the tester's 17,753 stored
# messages keep their header roots and the fix shows only on new mail. The
# check reading on the target goes red; its companion holds the two wrong
# moments as well.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the stored mail gets its word in the check, not never"
file = "src/presentation/wx_app.rs"
suite = "a_conversation_is_gmails_own_and_a_late_parent_joins_it"
before = """            if let Err(why) = handle.block_on(
                crate::application::server_thread_ids::fetch_the_server_thread_ids_once(
                    controller.as_ref(),
                    &cache,
                    &account.id,
                ),
            ) {
                tracing::warn!(
                    "The stored mail of {} could not be given its conversation ids this check: {why}",
                    account.display_name()
                );
            }
"""
after = """"""
red = ["test_the_stored_mail_gets_its_word_in_the_check_after_the_replay_and_before_the_listing"]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs", tests = 14 },
]

# The word written on the row and the merge skipped, so a reply that got its
# word leaves the parent its chain had joined it to under the header root,
# and the two show as two rows: the split the tester saw, one row at a time.
# The reply case goes red; the two-words case stays green, because without
# the merge each row simply keeps its own word, which is also the right end.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "a row given the server's word brings what its chain connects under it, not just itself"
file = "src/data/message_cache/server_thread_ids.rs"
before = """        self.merge_what_this_row_connects(row, folder_id, &message_id, refs_header.as_deref(), word)"""
after = """        let _ = (folder_id, message_id, refs_header);
        Ok(0)"""
red = ["data::message_cache::server_thread_ids::tests::test_naming_a_reply_brings_its_header_named_conversation_under_the_word"]
tests_last_seen = [
    { file = "src/data/message_cache/server_thread_ids.rs", tests = 5 },
]

# The pass recorded as done before the server has answered, so a refusal
# on a bad day leaves the account marked done forever and the stored mail
# never gets its word (T-11-97, the other way round). The refusal case goes
# red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "the pass is recorded as done when every folder answered, not before it asked"
file = "src/application/server_thread_ids.rs"
before = """    let started = Instant::now();
    let mut rows_named = 0usize;"""
after = """    cache.record_this_as_done(&marker)?;
    let started = Instant::now();
    let mut rows_named = 0usize;"""
red = ["application::server_thread_ids::tests::test_a_refusal_records_nothing_and_the_next_check_asks_for_what_is_still_missing"]
tests_last_seen = [
    { file = "src/application/server_thread_ids.rs", tests = 7 },
]

# The conversation listing grouped by subject rather than by the stored
# conversation, which is the join this project refuses on purpose and the
# one a listing that stopped reading thread_id would fall back to. The
# plan asked for the header root as the break, and `here` carries neither
# the chain nor the identifier, so that break is a query error and every
# store case goes red for the wrong reason; measured first, 9 red. Under
# subject, the cases where one subject spans two conversations go red: two
# strangers with one subject, a message whose word differs, and the
# re-threading reading whose three rows share a subject.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the conversation listing groups by the stored conversation, not by the subject"
file = "src/data/message_cache/messages.rs"
suite = "a_conversation_is_gmails_own_and_a_late_parent_joins_it"
# The break's text moved on 2026-09-20 when 11-11.1.3 made the grouping an
# argument of the one listing text; the break is the same, the folder's
# listing grouped by subject.
before = """        conversation_scope(),
        "m.thread_id","""
after = """        conversation_scope(),
        "m.subject","""
red = [
    "test_a_message_that_moves_to_another_conversation_changes_both_rows_messages_and_counts",
    "test_on_gmail_a_message_whose_word_differs_is_its_own_conversation",
    "test_two_messages_with_one_subject_and_no_chain_stay_two_conversations",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs", tests = 14 },
]

# The row read on request saying every heading, the six self-describing
# cells included, so Ctrl+Shift+; reads "Unread, Unread" and "Attachment,
# Has attachment": the same word twice on every flag cell that has
# something to say (#26). The composition ignoring the predicate is the
# break; the cases with a self-describing cell present go red, and the
# conversation case stays green because nothing in its row is unread,
# flagged, answered or attached.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the row read on request says a self-describing cell alone, not its heading twice"
file = "src/presentation/message_rows.rs"
suite = "the_rows_columns_are_read_on_request"
before = """            let cell = if column.heading_is_worth_saying() {
                format!("{}, {}", column.heading(), text)
            } else {
                text.to_string()
            };"""
after = """            let cell = format!("{}, {}", column.heading(), text);"""
red = [
    "test_a_message_row_is_composed_from_the_cells_the_list_paints",
    "test_a_row_reads_each_heading_then_its_text_in_the_order_the_columns_are_shown",
    "test_a_self_describing_cell_is_said_as_its_text_alone",
]
tests_last_seen = [
    { file = "src/presentation/message_rows.rs", tests = 41 },
    { file = "tests/the_rows_columns_are_read_on_request.rs", tests = 17 },
]

# The row read on request going out through `announce`, the interface
# channel, instead of `announce_content`: the subject and the sender of
# somebody's message spoken in a room with the mute on (#26, T-11-33). The
# closing brace is in the break because Space's reading ends on the same
# line one indent deeper, and a `before` that matched both would be no
# place at all. The channel reading on the target goes red; the item and
# the arm readings stay green, since neither asks how the row goes out.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the row read on request goes out as content under the mute, not as interface chatter"
file = "src/presentation/wx_app.rs"
suite = "the_rows_columns_are_read_on_request"
before = """    let _ = a11y.announce_content(&text);
}"""
after = """    let _ = a11y.announce(
        &text,
        crate::presentation::accessibility::announcements::Priority::Normal,
    );
}"""
red = [
    "test_the_reading_is_announced_once_as_content",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_rows_columns_are_read_on_request.rs", tests = 17 },
]

# The attachment event's own default dropped, so an event whose words are
# already in the row reaches every channel like any other, and landing on
# a message with an attachment speaks "Has attachment" over the row's own
# Attachment column again (#77). The break folds `the_default_for` to every
# channel for every event. The cases that read the attachment event under
# the default, under the tester's stored profile and under a profile that
# chose silence go red, and the fallback case's last reading with it; the
# fallback's own case for an answer given by hand stays green, since it
# never reads the default.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "an event whose words are on the row has its own default of the tone and the status bar"
file = "src/presentation/accessibility/feedback.rs"
suite = "landing_on_an_attachment_says_it_once"
before = """        if event.text_is_already_on_the_row() {
            [Channel::Earcon, Channel::Visual].into_iter().collect()
        } else {
            Channel::ALL.into_iter().collect()
        }"""
after = """        let _ = event;
        Channel::ALL.into_iter().collect()"""
red = [
    "test_a_profile_that_chose_silence_keeps_it",
    "test_the_attachment_event_reaches_the_tone_and_the_status_bar_and_no_words",
    "test_the_fallback_never_speaks_for_an_event_whose_text_is_on_the_row",
    "test_the_profile_that_filed_the_issue_hears_the_attachment_event_the_same_way",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
    { file = "tests/landing_on_an_attachment_says_it_once.rs", tests = 15 },
]

# The never-sound-alone rule adding braille, and so speech, to a sound-only
# answer for the attachment event, which puts the word back over the row
# the moment somebody ticks only the sound for it (#77, T-11-53). The break
# makes the fallback's order the same for every event. The case that hands
# the event a sound-only answer goes red; the companion for an event
# without the mark stays green, since the order it reads is the one the
# break leaves for everything.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the fallback never speaks for an event whose words are on the row"
file = "src/presentation/accessibility/feedback.rs"
suite = "landing_on_an_attachment_says_it_once"
before = """        let quietest_first: &[Channel] = if event.text_is_already_on_the_row() {
            &[Channel::Visual]
        } else {
            &[Channel::Braille, Channel::Visual, Channel::Speech]
        };"""
after = """        let quietest_first: &[Channel] = &[Channel::Braille, Channel::Visual, Channel::Speech];"""
red = [
    "test_the_fallback_never_speaks_for_an_event_whose_text_is_on_the_row",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
    { file = "tests/landing_on_an_attachment_says_it_once.rs", tests = 15 },
]

# The cursor handler speaking "Has attachment" of its own beside the
# signalled event, which is the duplicate #77 was filed about arriving by a
# route the Feedback tab cannot switch off: the row's own column, the
# event's channels and a third voice hard-wired in the window. The break
# adds the line beside the signal. The reading that holds the handler to
# naming the word in no string goes red; the reading that the handler
# signals what the landing decides stays green, since the signal is still
# there.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the cursor handler speaks nothing of its own about attachments"
file = "src/presentation/wx_app.rs"
suite = "landing_on_an_attachment_says_it_once"
before = """                    for landing_event in landing_events {
                        let _ = a11y.signal(landing_event, "");
                    }"""
after = """                    for landing_event in landing_events {
                        let _ = a11y.signal(landing_event, "");
                        let _ = a11y.announce(
                            "Has attachment",
                            crate::presentation::accessibility::announcements::Priority::Low,
                        );
                    }"""
red = [
    "test_the_cursor_handler_speaks_nothing_of_its_own_about_attachments",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/landing_on_an_attachment_says_it_once.rs", tests = 15 },
]

# The player not asking whether its device has gone before a sound, which
# is the shape the tester met (#81): the stream's error callback sets the
# flag and nothing reads it, so every later sound plays into the ended
# stream and `play` answers true. The break drops the flag from the
# decision and leaves the gap. The cases that lose the device inside a gap
# go red, in this file and in `accessibility.rs`, since the reopen never
# happens and the outage is never told; the cases that wait out the gap
# stay green, since the gap still opens the device again.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "a sound after the device has gone opens the default device again first"
file = "src/presentation/accessibility/feedback.rs"
before = """                output.has_ended() || a_gap_since(last_used)"""
after = """                a_gap_since(last_used)"""
red = [
    "presentation::accessibility::feedback::tests::test_a_device_that_stays_gone_is_asked_for_again_after_a_gap_not_on_every_sound",
    "presentation::accessibility::feedback::tests::test_a_player_whose_device_went_opens_the_default_device_again_before_the_next_sound",
    "presentation::accessibility::feedback::tests::test_an_outage_is_told_once_and_the_sounds_come_back_when_a_device_does",
    "presentation::accessibility::tests::test_a_player_that_cannot_open_a_device_puts_its_complaint_where_the_eye_reads",
]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
]

# The outage told on every refused open rather than once: a device that
# stays gone under a syncing mailbox is forty lines a second in the log and
# a sentence for the eye on every sound (#81, T-11-59). The break tells the
# outage whenever there is no output, which in that arm is always. The
# case counting the log lines across three sounds goes red; the case in
# `accessibility.rs` takes one sentence and stays green, since a sentence
# told again is still a sentence.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "an outage of the sounds is told once, not on every sound"
file = "src/presentation/accessibility/feedback.rs"
before = """                if !inner.outage.told {
                    inner.outage = Outage::told(&why);
                }"""
after = """                inner.outage = Outage::told(&why);"""
red = [
    "presentation::accessibility::feedback::tests::test_an_outage_is_told_once_and_the_sounds_come_back_when_a_device_does",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
]

# `signal` playing the tone and never asking the player for its complaint,
# so the sounds stop and the status bar goes on showing the event's own
# words as if they had played (#81). The break drops the call. The case
# that loses the device for good and reads the status bar goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "signal puts the player's complaint where the eye reads it"
file = "src/presentation/accessibility.rs"
before = """            if let Some(complaint) = self.earcons.take_complaint() {
                shown = Some(complaint);
            }"""
after = """            let _ = &mut shown;"""
red = [
    "presentation::accessibility::tests::test_a_player_that_cannot_open_a_device_puts_its_complaint_where_the_eye_reads",
]
tests_last_seen = [
    { file = "src/presentation/accessibility.rs", tests = 25 },
]

# The plain part taken as written, its words joined and nothing skipped,
# which is the shape the tester met (#82): a message opening with a link
# reads the whole address out on its row, and an opener, a rule of dashes,
# a greeting, a quote and a signature all reach the column before the
# message's own words. The break puts the old cut back for the plain part
# alone. On the target the plain cases go red, and so do the pass's, since
# a snippet recomputed the old way is the old form and nothing is put right;
# the markup cases stay green, since they never touch the plain arm.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a snippet takes the plain part through the rules as its lines, not as written"
file = "src/data/message_cache/bodies.rs"
suite = "a_snippet_is_the_first_relevant_words"
before = """        Some(text) => snippet::first_relevant_words(text.lines(), snippet::LIMIT),"""
after = """        Some(text) => text.split_whitespace().collect::<Vec<_>>().join(" "),"""
red = [
    "test_a_body_that_is_only_a_signature_gives_the_least_bad_line",
    "test_a_database_that_ran_the_older_pass_still_gets_this_one",
    "test_a_reply_gives_the_new_words_and_not_the_quote",
    "test_a_saved_plain_body_gives_its_first_relevant_words",
    "test_a_stored_snippet_in_the_old_form_is_put_right_on_the_next_open_and_once",
    "test_the_pass_over_two_thousand_bodies_finishes_and_says_what_it_cost",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "tests/a_snippet_is_the_first_relevant_words.rs", tests = 10 },
]

# The pass narrowed back to HTML-only bodies, which is the pass of
# 2026-09-16 under the new name: every message with a plain part keeps the
# snippet an older build cut from its text, address and all, and the row
# the tester heard stays as it was until its text is fetched again. The
# break puts the old query's WHERE back. On the target the two put-right
# cases and the timing case go red, since their bodies are plain; the save's
# cases stay green.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the pass recomputes every stored snippet, not the HTML-only ones alone"
file = "src/data/message_cache/bodies.rs"
suite = "a_snippet_is_the_first_relevant_words"
before = """                     INNER JOIN message_bodies b ON b.message_id = m.id","""
after = """                     INNER JOIN message_bodies b ON b.message_id = m.id
                     WHERE b.body_plain_packed IS NULL
                       AND COALESCE(length(trim(b.body_plain)), 0) = 0
                       AND (b.body_html_packed IS NOT NULL
                            OR COALESCE(length(b.body_html), 0) > 0)","""
red = [
    "test_a_database_that_ran_the_older_pass_still_gets_this_one",
    "test_a_stored_snippet_in_the_old_form_is_put_right_on_the_next_open_and_once",
    "test_the_pass_over_two_thousand_bodies_finishes_and_says_what_it_cost",
]
tests_last_seen = [
    { file = "src/data/message_cache/bodies.rs", tests = 50 },
    { file = "tests/a_snippet_is_the_first_relevant_words.rs", tests = 10 },
]

# ── 11-10: a rule can change how a row is announced (#62) ─────────────────
#
# The settle arm for Say this first put back to the empty arm the red
# commit carried: every rule's phrase is dropped before anything is
# written, so no message ever carries one. On the target the case that
# reads the phrase back through the listing and the case where the later
# rule's phrase wins go red; the refusal at the reader, the sound count
# and the schema cases do not read the phrase and stay green. The module's
# own settle case in `filters.rs` would redden too, and is not in the red
# list because the record's suite is the target.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a rule's phrase said first survives settling, not dropped on the way to the message"
file = "src/application/filters.rs"
suite = "a_rule_can_change_how_a_row_is_announced"
before = """            FilterAction::SayFirst(phrase) => outcome.say_first = Some(phrase.clone()),"""
after = """            FilterAction::SayFirst(_) => {}"""
red = [
    "test_a_rule_saying_a_phrase_first_leaves_it_on_the_message_and_the_listing_carries_it",
    "test_when_two_rules_say_a_phrase_first_the_later_rules_phrase_wins",
]
tests_last_seen = [
    { file = "src/application/filters.rs", tests = 49 },
    { file = "tests/a_rule_can_change_how_a_row_is_announced.rs", tests = 20 },
]

# Every match counted as one that sounds, whether or not the rule carries
# the flag: the sound would play after any check where any rule matched,
# and the count the window says would be the count of matches. The break
# drops the filter. On the target the counting case reads five for three
# and the silent-rule case reads one for nought; the phrase cases and the
# schema case do not read the count and stay green.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "only a rule that carries the sound flag counts as a match with a sound"
file = "src/application/mail_sync.rs"
suite = "a_rule_can_change_how_a_row_is_announced"
before = """        done.matches_with_a_sound += matched.iter().filter(|rule| rule.plays_a_sound).count();"""
after = """        done.matches_with_a_sound += matched.len();"""
red = [
    "test_a_check_with_no_sounding_rule_counts_no_match_with_a_sound",
    "test_a_rule_with_a_sound_counts_once_per_message_it_matched_and_one_without_counts_nothing",
]
tests_last_seen = [
    { file = "src/application/mail_sync.rs", tests = 149 },
    { file = "tests/a_rule_can_change_how_a_row_is_announced.rs", tests = 20 },
]

# The prefix dropped: the first visible cell is painted as it was, so the
# phrase a rule kept on a message is heard nowhere unless the Says first
# column is switched on, which it is not by default. The break keeps the
# arm's shape and hands the cell back. On the target the two prefix cases,
# one per view, go red; the case where the Says first column is itself
# first stays green, since nothing is prefixed there either way, and the
# cell cases do not go through text_for.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a rule's phrase is said before the row's first cell, not only shown in its column"
file = "src/presentation/virtual_rows.rs"
suite = "a_rule_can_change_how_a_row_is_announced"
before = """        (Some(phrase), true) => said_first(phrase, &cell),"""
after = """        (Some(_), true) => cell,"""
red = [
    "test_the_phrase_is_said_before_the_first_visible_cell_of_a_conversation_row",
    "test_the_phrase_is_said_before_the_first_visible_cell_of_a_message_row",
]
tests_last_seen = [
    { file = "src/presentation/virtual_rows.rs", tests = 6 },
    { file = "tests/a_rule_can_change_how_a_row_is_announced.rs", tests = 20 },
]

# The count sent per folder as well as once after the loop: the arm runs
# per update it is sent, so a check over a mailbox of matches plays the
# Rule matched sound once per folder that held one, which is the flood
# guardrail 5 exists to stop. The break keeps the one send after the loop
# and adds one inside it, with the folder's own count. On the target the
# reading that holds both halves of the bound goes red at its second
# half; the readings' companions plant their own faults and stay green.
# 10-04's reading in progress_is_shown_and_results_are_said would redden
# too, and is not in the red list because the record's suite is the
# target.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the rule-matched sound plays once per check, not once per folder that held a match"
file = "src/presentation/wx_app.rs"
suite = "a_rule_can_change_how_a_row_is_announced"
before = """                        matches_with_a_sound += result.filtered.matches_with_a_sound;"""
after = """                        matches_with_a_sound += result.filtered.matches_with_a_sound;
                        say(UIUpdate::WhatArrived {
                            what: String::new(),
                            matches_with_a_sound: result.filtered.matches_with_a_sound,
                        });"""
red = [
    "test_a_check_with_matches_plays_the_sound_once_and_never_per_folder_or_per_message",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_rule_can_change_how_a_row_is_announced.rs", tests = 20 },
]

# The event given another event's tone: a match sounds like a search that
# came up empty, so somebody who learned the scheme by ear hears the wrong
# fact. The break copies NothingFound's tone onto RuleMatched. In the
# library the walk over every event that requires each tone its own goes
# red; nothing else reads the pair.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "the rule-matched event has a tone of its own, not another event's"
file = "src/presentation/accessibility/feedback.rs"
before = """            Event::RuleMatched => Tone::new(1175, 80),"""
after = """            Event::RuleMatched => Tone::new(300, 130),"""
red = [
    "presentation::accessibility::feedback::tests::test_every_event_has_its_own_tone",
]
tests_last_seen = [
    { file = "src/presentation/accessibility/feedback.rs", tests = 54 },
]

# The editor opened on a stored rule with the sound, its box left clear:
# OK reads the box back, so editing anything about such a rule turns its
# sound off without a word. The break drops the one line that sets the box
# from the stored rule. On the target the editor reading goes red at that
# rule; the pure readings over the label and the refusal stay green.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the rule editor opens a rule with the sound as it was stored, not with the box clear"
file = "src/presentation/wx_managers.rs"
suite = "a_rule_can_change_how_a_row_is_announced"
before = """        sound_check.set_value(r.plays_a_sound);"""
after = """        sound_check.set_value(false);"""
red = [
    "test_the_editor_offers_the_action_and_the_sound_and_reads_a_stored_rule_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_managers.rs", tests = 44 },
    { file = "tests/a_rule_can_change_how_a_row_is_announced.rs", tests = 20 },
]

# ── 11-10.1: an address written out is a link (#89) ─────────────────────────

# The closing bracket at the end of an address is treated as part of it
# whether or not the address opened it, which is the truncation the
# composer's link rule was written against, turned round: "(see
# https://x.org/s/1)" makes a link to "https://x.org/s/1)", a page that
# does not exist, with nothing said. The break makes the bracket arm answer
# that a closing bracket is always the address's own. The two bracket cases
# go red; the case with a balanced pair stays green, since it was never cut.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "an address keeps the bracket it opened and not the one that closes the sentence round it"
file = "src/application/links_in_text.rs"
before = """            ')' => closes_more_than_it_opens(candidate, '(', ')'),"""
after = """            ')' => false,"""
red = [
    "application::links_in_text::tests::test_a_balanced_bracket_is_kept_and_the_one_closing_the_sentence_is_not",
    "application::links_in_text::tests::test_a_bracket_closing_a_pair_the_address_did_not_open_is_left_outside_it",
]
tests_last_seen = [
    { file = "src/application/links_in_text.rs", tests = 29 },
]

# The shape rule loosened to any word with a dot in it, so a version
# number, a decimal and a bare host are addresses by shape. The message is
# held by a second rule the break does not touch: a word with no scheme is
# given mailto: and the sanitiser refuses a mailto: with nobody in it, so
# "a.b" stays text there and the recogniser's own non-address cases stay
# green. The snippet has no second rule, since a hint drops what looks like
# an address without asking whether it opens, and its cases with a dotted
# word in them go red. The record measures the shape rule where it is bare.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "a word with a dot in it is a word and not an address, by the one shape rule"
file = "src/application/links_in_text.rs"
before = """        || is_a_bare_email_address(&lower)"""
after = """        || lower.contains('.')"""
red = [
    "application::links_in_text::tests::test_the_shape_rule_names_the_five_shapes_and_nothing_looser",
    "application::snippet::tests::test_a_word_with_a_dot_or_an_at_sign_in_it_is_not_an_address",
]
tests_last_seen = [
    { file = "src/application/links_in_text.rs", tests = 29 },
    { file = "src/application/snippet.rs", tests = 39 },
]

# The gate skipped: an address by shape becomes a link whatever the
# sanitiser says of it, so "https://apple.example@evil.example" is a link
# the program made to a page that reads as one site and goes to another,
# which is the one rewriting T-11-99 forbids. The break hands the address
# on without asking. The case that names three refused addresses goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "an address the sanitiser refuses stays text rather than becoming a link the program made"
file = "src/application/links_in_text.rs"
before = """    let href = HtmlRenderer::safe_external_url(&as_followed(shown))?;"""
after = """    let href = as_followed(shown);"""
red = [
    "application::links_in_text::tests::test_an_address_the_sanitiser_refuses_stays_text",
]
tests_last_seen = [
    { file = "src/application/links_in_text.rs", tests = 29 },
]

# The plain branch put back to escaping alone, which is the page as the
# tester met it: a plain-text message is characters and its chapter
# address is not a link. The break is the one call in the branch; both
# plain branches of the renderer go through it, so both lose the links at
# once. On the target the tester's message and the angle-bracket case go
# red; the reply's cases go through the composer's own call and stay green.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a plain-text message shown as a page has its addresses as links, not as characters"
file = "src/presentation/html_renderer.rs"
suite = "an_address_written_out_is_a_link"
before = """        crate::application::links_in_text::as_html(text)
    )"""
after = """        html_escape::encode_text(text)
    )"""
red = [
    "test_a_plain_message_keeps_its_angle_brackets_as_characters_round_a_link",
    "test_the_testers_message_shown_as_a_page_has_its_three_addresses_as_links",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
    { file = "tests/an_address_written_out_is_a_link.rs", tests = 11 },
]

# A code block's text linked like any other text: the recogniser stops
# counting a code block as somewhere it must not look, so an address in a
# fenced block becomes an anchor inside the block. A code span is not the
# same break, since the parser hands a span over as a code event and never
# as text, so no one-line edit here makes a span's address a link. On the
# target the code case goes red on its block half; the note with a bare
# address and the note with a link that has words stay green.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "an address in a note's code block stays code, not a link inside the block"
file = "src/application/long_text.rs"
suite = "an_address_written_out_is_a_link"
before = """            Event::Start(Tag::CodeBlock(_) | Tag::Link { .. } | Tag::Image { .. }) => {"""
after = """            Event::Start(Tag::Link { .. } | Tag::Image { .. }) => {"""
red = [
    "test_a_note_with_an_address_in_code_keeps_it_as_code",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "tests/an_address_written_out_is_a_link.rs", tests = 11 },
]

# tel: gone from the schemes this program opens, spelled as tel:// so the
# array keeps its length: a sender's "call us" link is refused at the gate
# and the page says so beside its words, which is what the page did before
# 2026-09-19 without the note. The corpus case for a telephone link and the
# gate's own tel: case go red; every other scheme's case stays green.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "a telephone link is one this program opens, not one it refuses with a note"
file = "src/presentation/html_renderer.rs"
before = """const SAFE_URL_SCHEMES: [&str; 4] = ["http://", "https://", "mailto:", "tel:"];"""
after = """const SAFE_URL_SCHEMES: [&str; 4] = ["http://", "https://", "mailto:", "tel://"];"""
red = [
    "presentation::html_renderer::tests::test_a_telephone_link_a_sender_writes_is_kept",
    "presentation::html_renderer::tests::test_safe_external_url_allows_a_telephone_number_and_refuses_an_empty_one",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
]

# ── Pictures shown by default, except the ones that look like trackers ──────
#
# 11-11, #28. Since 2026-09-19 a picture a message points at is fetched by
# default, and what stands between a reader and a tracking pixel is one
# rule over the size the sender declared. The break makes the rule answer
# no for everything: the closure compares against a size no declaration
# reaches, so a one-by-one pixel is fetched and reports the opening. On the
# target the beacon cases go red, and from task 2 the newsletter shown
# through the renderer, whose pixel is then fetched and counted by nothing;
# the cases for a picture with no size, for the switch and for the
# descriptions stay green, since none of them asks the size rule to say yes.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a picture a pixel or less on a side is a beacon and is not fetched"
file = "src/application/pictures.rs"
suite = "pictures_show_by_default_except_beacons"
before = """        .any(|pixels| pixels <= MOST_A_BEACON_MAY_MEASURE)"""
after = """        .any(|pixels| pixels == u32::MAX)"""
red = [
    "test_a_newsletters_pictures_show_and_its_beacon_is_held_back_and_counted",
    "test_a_nothing_by_nothing_picture_and_a_one_pixel_strip_are_beacons_too",
    "test_a_one_by_one_picture_is_a_beacon_by_its_declared_size",
]
tests_last_seen = [
    { file = "src/application/pictures.rs", tests = 44 },
    { file = "tests/pictures_show_by_default_except_beacons.rs", tests = 26 },
]

# A description this program invented. The undescribed rule stops asking
# the choice and writes "image" on every picture the sender did not
# describe, which is what the default of nothing exists to refuse: a reader
# who chose to pass over such pictures hears "image" forty times over a
# mailing, and the word is this program's and not the sender's (guardrail
# 9). On the target the case that walks the three answers goes red on
# nothing and on photo, and from task 2 so does the same walk through the
# renderer; the case that counts the words under image stays green, since
# image is the word the break writes.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "an undescribed picture is called what the choice says and not a word this program picked"
file = "src/application/pictures.rs"
suite = "pictures_show_by_default_except_beacons"
before = """                None => with_a_description(picture, read_as.description()),"""
after = """                None => with_a_description(picture, "image"),"""
red = [
    "test_an_undescribed_picture_is_described_as_the_reader_chose_when_shown",
    "test_an_undescribed_picture_is_described_as_the_setting_says",
]
tests_last_seen = [
    { file = "src/application/pictures.rs", tests = 44 },
    { file = "tests/pictures_show_by_default_except_beacons.rs", tests = 26 },
]

# The default moved to a word. The tester's decision in #28 is "" unless
# somebody chooses image or photo, and a fresh profile, an older settings
# file and a garbled value all read as the default, so a default of image
# writes the word on every undescribed picture for everybody who chose
# nothing. The module's own case for the default goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "an undescribed picture is passed over unless somebody chose a word"
file = "src/application/describing_pictures.rs"
before = """    #[default]
    Nothing,
    /// The word "image".
    Image,"""
after = """    Nothing,
    /// The word "image".
    #[default]
    Image,"""
red = [
    "application::describing_pictures::tests::test_the_default_is_nothing_so_an_undescribed_picture_is_passed_over",
    # Added 2026-09-22 by 12-02.1, from run 35520204784's shard 50 and a
    # measurement here that found the same one. The settings guard that reads
    # a file written before these settings existed asserts this default among
    # the others, in a file this record had never named.
    "data::config::permission_tests::test_a_settings_file_written_before_these_existed_reads_the_way_it_should",
]
tests_last_seen = [
    { file = "src/application/describing_pictures.rs", tests = 8 },
    { file = "src/data/config.rs", tests = 68 },
]

# A reader's word sent to a recipient. The undescribed rule moves into
# sanitize_html, which also cleans a message on its way out, so the word
# this reader chose for pictures nobody described is written on the
# pictures in a message being sent, as though the writer had put it there
# (T-11-41). On the target the case that sends a message through the
# cleaner under each answer goes red, and so does the source reading that
# holds the sending path to calling none of the three rules; and seven
# more, because every fixture on the target is cleaned through the same
# call, so under the break each arrives with a description already on it.
# The first draft of this record named two, and the runner named nine.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a description written for reading is never written on a message going out"
file = "src/presentation/html_renderer.rs"
suite = "pictures_show_by_default_except_beacons"
before = """        cleaner().clean(html).to_string()
    }"""
after = """        crate::application::pictures::describe_the_undescribed(
            &cleaner().clean(html).to_string(),
            self.describing,
        )
    }"""
red = [
    "test_a_description_written_for_reading_is_never_sent",
    "test_a_linked_picture_whose_link_has_no_words_stays_undescribed",
    "test_a_linked_picture_with_no_description_takes_the_links_words",
    "test_a_picture_with_no_declared_size_is_fetched",
    "test_a_remote_picture_the_sender_marked_decorative_is_not_fetched",
    "test_an_undescribed_picture_is_described_as_the_setting_says",
    "test_every_picture_in_a_message_is_described_and_the_described_ones_left",
    "test_the_links_words_are_stripped_of_markup_and_escaped_before_they_become_a_description",
    "test_the_sending_path_calls_none_of_the_three_rules_and_the_reading_path_calls_all",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
    { file = "tests/pictures_show_by_default_except_beacons.rs", tests = 26 },
]

# The Reading tab's choice read back as its first entry whatever was
# chosen: the control shows the stored answer, offers the three, and OK
# writes nothing back every time, which is the shape ten settings in this
# file once had and the two settings guards cannot see, since the field's
# name is in the screen and in a reader either way. On the target the
# read-back case goes red.
#
# Measured 2026-09-19 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the word chosen for an undescribed picture is what OK writes back"
file = "src/presentation/wx_settings.rs"
suite = "pictures_show_by_default_except_beacons"
before = """        .get(sel(&w.undescribed_pictures_read_as) as usize)"""
after = """        .get(0)"""
red = [
    "test_a_choice_made_on_the_reading_tab_is_what_ok_writes_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/pictures_show_by_default_except_beacons.rs", tests = 26 },
]

# ── What the sender hid is not read (#90, 11-11.0) ──────────────────────────
#
# A block is dropped by the sender's own hiding and never by what it says
# (guardrail 6), and the hiding rules are exactly six. The two records on
# the module hold the two ways the set was most likely to widen: a rule
# that hides on a declaration a sender writes for something else. opacity:0
# on its own is a fade-in's first frame, and a block hidden by opacity alone
# still takes its space; the newsletter writes it beside display:none, and
# it is display:none that hides. The break makes opacity:0 a rule, and the
# module's companion case for it goes red.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "opacity nought alone is not a hiding"
file = "src/application/hidden_text.rs"
before = """            ("mso-hide", "all") => return Some(Rule::MsoHideAll),"""
after = """            ("mso-hide", "all") | ("opacity", "0") => return Some(Rule::MsoHideAll),"""
red = [
    "application::hidden_text::tests::test_opacity_nought_alone_is_not_a_hiding",
]
tests_last_seen = [
    { file = "src/application/hidden_text.rs", tests = 29 },
]

# max-height:0 hides only together with overflow:hidden: alone, the block
# overflows its nought height and is read; overflow:hidden alone hides
# nothing of a block that fits. The break lets either half hide on its own,
# and the module's case for the pair goes red on both companions.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "max-height nought hides only with overflow hidden beside it"
file = "src/application/hidden_text.rs"
before = """    (max_height_nought && overflow_hidden).then_some(Rule::MaxHeightZeroOverflowHidden)"""
after = """    (max_height_nought || overflow_hidden).then_some(Rule::MaxHeightZeroOverflowHidden)"""
red = [
    "application::hidden_text::tests::test_max_height_nought_hides_only_together_with_overflow_hidden",
]
tests_last_seen = [
    { file = "src/application/hidden_text.rs", tests = 29 },
]

# The drop is the reading path's and never the sending path's. sanitize_html
# cleans a reply's quoted message on its way into the editor and the
# editor's body on its way out, and a block the writer hid is theirs to
# send. The break runs the drop inside sanitize_html, and on the target the
# case that sends a hidden block through the composer's renderer and the
# editor's goes red.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "what the sender hid is dropped on the reading path and never on the sending path"
file = "src/presentation/html_renderer.rs"
suite = "what_the_sender_hid_is_not_read"
before = """        cleaner().clean(html).to_string()"""
after = """        cleaner()
            .clean(&crate::application::hidden_text::drop_what_the_sender_hid(html).0)
            .to_string()"""
red = [
    "test_a_message_on_its_way_out_keeps_what_its_writer_hid",
]
tests_last_seen = [
    { file = "src/presentation/html_renderer.rs", tests = 92 },
    { file = "tests/what_the_sender_hid_is_not_read.rs", tests = 19 },
]

# The one role the cleaner keeps is presentation, on a table's four tags,
# because it is the sender's own claim that a table is layout and a reader
# is better off without a table announced around every block. The break
# lets every role value through on those tags, so a table's grid, row and
# gridcell roles survive the cleaner and reach the page. On the target the
# case that counts the roles goes red; the newsletter's case stays green,
# since its region and its button are not a table's tags.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. Rewritten the same
# day when the allowance moved into hidden_text::keep_the_layout_claim,
# the one place the page's cleaner and the reader's take it from, and
# measured again: the same one.
[[guard]]
name = "no role but presentation survives the cleaner"
file = "src/application/hidden_text.rs"
suite = "what_the_sender_hid_is_not_read"
before = """        builder.add_tag_attribute_values(tag, "role", [LAYOUT_CLAIM]);"""
after = """        builder.add_tag_attributes(tag, ["role"]);"""
red = [
    "test_no_role_but_presentation_survives_and_only_on_a_tables_four_tags",
]
tests_last_seen = [
    { file = "src/application/hidden_text.rs", tests = 29 },
    { file = "tests/what_the_sender_hid_is_not_read.rs", tests = 19 },
]

# A sender's label is a name only where a reader would use it as one, and
# a layout table's label names a grouping. The break keeps every table's
# label whatever its role, so a presentational table reaches the page
# named, which the cleaner lets through because a table may carry a label.
# On the target the case that puts a labelled layout table beside a
# labelled data table goes red. A break on the div arm would redden the
# module's cases and never the page's, since the cleaner drops a div's
# label on its own; this break is the one the page can see.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a layout table's label is not a name a reader would hear"
file = "src/application/hidden_text.rs"
suite = "what_the_sender_hid_is_not_read"
before = """        "table" => !is_a_layout_claim(role),"""
after = """        "table" => true,"""
red = [
    "test_a_label_is_kept_where_it_is_a_name_and_dropped_where_it_named_a_grouping",
]
tests_last_seen = [
    { file = "src/application/hidden_text.rs", tests = 29 },
    { file = "tests/what_the_sender_hid_is_not_read.rs", tests = 19 },
]

# The reader reads a layout table as its blocks in order and never as a
# Table piece (ledger 555): a layout table has no columns to name, and
# read as one the whole newsletter arrived as a single cell with every
# block's last word run into the next block's first. The break sends a
# presentational table back down the table arm, so the reader answers a
# Table piece for it again. On the target the hand-built layout table and
# the newsletter both go red.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a layout table is read as its blocks and never as a table piece"
file = "src/application/long_text.rs"
suite = "what_the_sender_hid_is_not_read"
before = """                        layout_table(child, out, keeping)"""
after = """                        table(child, out, keeping)"""
red = [
    "test_a_layout_table_is_read_as_its_blocks_in_order",
    "test_the_reader_reads_the_newsletter_as_blocks_apart_and_no_layout_table_as_a_table",
]
tests_last_seen = [
    { file = "src/application/long_text.rs", tests = 60 },
    { file = "tests/what_the_sender_hid_is_not_read.rs", tests = 19 },
]

# ── 11-11.1: where a link opens (#80), 2026-09-20 ──────────────────────────
#
# A link activated in the page follows the Reading tab's setting; the
# route is one pure function over the setting and the ask. The break
# sends an activation to the browser whatever the setting says, which is
# what the tree did before the setting existed, and the target's table of
# every cell goes red on the two settings that are not the browser. The
# module's own cases go red as well and are not in the suite named here,
# which is the target, so the record counts the target's one.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "an activated link follows the setting rather than always the browser"
file = "src/application/opening_links.rs"
suite = "a_link_opens_where_the_setting_says"
before = """    match asked {
        Asked::Activated => Route::from(setting),"""
after = """    match asked {
        Asked::Activated => Route::Browser,"""
red = [
    "test_the_route_table_holds_cell_by_cell",
]
tests_last_seen = [
    { file = "src/application/opening_links.rs", tests = 13 },
    { file = "tests/a_link_opens_where_the_setting_says.rs", tests = 20 },
]

# The Reading tab offers the three places, in the module's order, and OK
# writes the chosen one back. The break builds the choice with no entries,
# so the built page offers nothing and the readback answers the default for
# whatever was stored; the target's three readings over the built dialog
# go red together, since the harvest they share cannot choose an entry. The
# settings guard in config.rs that asks whether a screen offers the field
# reads the field's name and stays green under this break: a name on the
# screen and a choice somebody can make are two facts, and this record is
# the second.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the reading tab offers the three places a link can open and writes the chosen one back"
file = "src/presentation/wx_settings.rs"
suite = "a_link_opens_where_the_setting_says"
before = """        &open_links_labels,"""
after = """        &[],"""
red = [
    "test_the_reading_tab_offers_the_browser_then_the_message_view_then_a_separate_window",
    "test_a_stored_separate_window_is_shown_as_the_third_entry_and_written_back_as_itself",
    "test_a_choice_made_on_the_reading_tab_is_what_ok_writes_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/a_link_opens_where_the_setting_says.rs", tests = 20 },
]

# The page's click listener takes the click from the browser before it
# posts the address; without preventDefault the browser navigates as well,
# and the link opens inside the window beside wherever the setting sent
# it, which is the tester's report with the fix on top. The break drops
# the one line; the target's listener reading goes red. The module's own
# case goes red in the library too and is not in the suite named here.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the page's click listener takes the click from the browser before posting the link"
file = "src/presentation/page_links.rs"
suite = "a_link_opens_where_the_setting_says"
before = """    e.preventDefault();
    e.stopPropagation();
    var data = { kind: 'link', href: link.href };"""
after = """    e.stopPropagation();
    var data = { kind: 'link', href: link.href };"""
red = [
    "test_every_page_is_given_the_link_listener_and_it_takes_the_click_from_the_browser",
]
tests_last_seen = [
    { file = "src/presentation/page_links.rs", tests = 7 },
    { file = "tests/a_link_opens_where_the_setting_says.rs", tests = 20 },
]

# Open in Message View asks for the message view whatever the setting says;
# the break sends the item to the browser, which is the setting's default
# and so would look right on every fresh profile. The target's arm reading
# goes red.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "open in message view asks for the message view and not the browser"
file = "src/presentation/wx_app.rs"
suite = "a_link_opens_where_the_setting_says"
before = """                    opening_links::Asked::InMessageView,"""
after = """                    opening_links::Asked::InBrowser,"""
red = [
    "test_each_item_reaches_the_one_route_with_its_own_ask_on_both_surfaces",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_link_opens_where_the_setting_says.rs", tests = 20 },
]

# The sanitiser runs before the route on every path (T-11-68): the break
# takes the address as the page wrote it when the sanitiser refuses it, so
# a javascript: or file: link a sender wrote would reach the route and the
# browser. The target's sanitiser reading goes red.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the sanitiser runs before the route on every path a link takes"
file = "src/presentation/wx_app.rs"
suite = "a_link_opens_where_the_setting_says"
before = """    let Some(safe) = HtmlRenderer::safe_external_url(href) else {"""
after = """    let Some(safe) = HtmlRenderer::safe_external_url(href).or_else(|| Some(href.to_string())) else {"""
red = [
    "test_the_sanitiser_runs_before_the_route_on_every_path",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_link_opens_where_the_setting_says.rs", tests = 20 },
]

# ── A setting saved in Settings applies without a restart (#91, 11-11.1.1) ──
#
# The wait before a message read aloud or opened is marked read lives in the
# window's state, written where the window is built and by the
# MarkReadAfterChanged arm, and the main timer reads it from there. Until
# 2026-09-20 the timer's closure captured a value read once at startup, so
# the setting did nothing however the tester set it until the next start.
# The first break makes the arm write nothing: a save sends the update and
# the timer goes on asking with the old wait. The target's arm reading goes
# red.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a wait saved in settings reaches the timer, because its arm writes the state"
file = "src/presentation/wx_app.rs"
suite = "a_setting_saved_applies_without_a_restart"
before = """        UIUpdate::MarkReadAfterChanged(wait) => {
            lock_state(state).marks_read = *wait;"""
after = """        UIUpdate::MarkReadAfterChanged(wait) => {
            let _ = wait;"""
red = [
    "test_the_update_writes_the_wait_into_the_state",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_setting_saved_applies_without_a_restart.rs", tests = 20 },
]

# The second break puts the capture back: the startup block binds the wait
# into a local instead of writing it into the state, which is the line #91
# met, and the timer reads the state's default from then on. The target's
# no-capture reading goes red on the local.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the wait is written into the state where the window is built, not bound into a local a closure captures"
file = "src/presentation/wx_app.rs"
suite = "a_setting_saved_applies_without_a_restart"
before = """            lock_state(&state).marks_read = stored_config
                .as_ref()"""
after = """            let _marks_read = stored_config
                .as_ref()"""
red = [
    "test_nothing_captures_the_wait_where_the_window_is_built",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_setting_saved_applies_without_a_restart.rs", tests = 20 },
]

# The date settings had the same fault as the wait: captured at startup into
# the row callback, the PIM cells and every read-aloud closure, while the
# calendar heading read the file afresh. The break puts that capture back,
# a local named as it was, and the target's reading over the rows goes red
# on the name: a local is what a closure captures.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the dates are written into the state where the window is built, not captured into a local"
file = "src/presentation/wx_app.rs"
suite = "a_setting_saved_applies_without_a_restart"
before = """            lock_state(&state).dates = stored_config
                .as_ref()
                .map(date_settings_from)"""
after = """            let _date_settings = stored_config
                .as_ref()
                .map(date_settings_from)"""
red = [
    "test_the_rows_and_the_cells_read_the_dates_from_the_state",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_setting_saved_applies_without_a_restart.rs", tests = 20 },
]

# The audit's half. The break drops the update the Settings-saved arm sends
# for the dates, so a save writes the file and the rows keep the dates the
# window started with. Two readings go red: the one over the arm, and the
# audit, which lists the dates as following a save through that update and
# finds the arm no longer sends it. That second red is the audit doing the
# job it exists for: a setting captured at startup that a save no longer
# reaches is exactly #91.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a date setting saved in settings reaches the rows, and the audit says so when it does not"
file = "src/presentation/wx_app.rs"
suite = "a_setting_saved_applies_without_a_restart"
before = """                let _ = tx.try_send(UIUpdate::DateSettingsChanged(dates));"""
after = """                let _ = dates;"""
red = [
    "test_saving_settings_sends_the_dates_after_the_save",
    "test_every_setting_read_at_startup_has_a_disposition_the_tree_honours",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_setting_saved_applies_without_a_restart.rs", tests = 20 },
]

# The log level cannot follow a save: it is set up once when the program
# starts. So its control says so, in a sentence under it named on both
# channels, and the one test that builds the Advanced page in a window
# session reads the sentence off the built page. The break drops the
# sentence and keeps the constant's name in a `let _`, so the import stays
# used and only the page changes; the page-building test goes red with
# "holds no static text saying".
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the sentence saying the log level takes effect at the next start is on the advanced page"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """    a_sentence_under(panel, &log_sec, TAKES_EFFECT_AT_THE_NEXT_START);"""
after = """    let _ = TAKES_EFFECT_AT_THE_NEXT_START;"""
red = [
    "test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# The default sort order is next-start by nature, and the audit lists it
# that way with the sentence under its control as the evidence. The break
# drops the sentence altogether (the import goes unused, which is a warning
# and compiles), and the audit goes red: a setting listed as taking effect
# at the next start with nothing under its control saying so is a setting
# that appears to do nothing.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the sentence saying where the default sort order applies is under its control, and the audit holds it there"
file = "src/presentation/wx_settings.rs"
suite = "a_setting_saved_applies_without_a_restart"
before = """    a_sentence_under(panel, &list_sec, WHERE_THE_DEFAULT_SORT_ORDER_APPLIES);"""
after = """    let _ = &list_sec;"""
red = [
    "test_every_setting_read_at_startup_has_a_disposition_the_tree_honours",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/a_setting_saved_applies_without_a_restart.rs", tests = 20 },
]

# ── Show conversations by default (#92, 11-11.1.2, 2026-09-20) ──────────────
#
# Thread View is on by default, as a setting somebody can turn off. The
# trap in a default that changed is a folder somebody chose flat turning
# threaded under them (T-11-110): a stored nought is a choice, not the
# absence of one, and it stays flat whatever the setting says. The break
# makes a nought answer the setting like an absent row, which is what a
# careless reading of "on by default" would write. Red in the rule's own
# test, which asks with the setting on, and in the cache test that writes
# a nought and reads it back handed the other view.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`.
[[guard]]
name = "a folder chosen flat stays flat whatever show conversations by default says"
file = "src/presentation/view_state.rs"
before = """            Some(0) => Self::Messages,"""
after = """            Some(0) => when_unset,"""
red = [
    "data::message_cache::folders::tests::test_setting_a_folder_flat_again_reads_back_flat_rather_than_never_set",
    "presentation::view_state::tests::test_a_folder_left_flat_comes_back_flat_whatever_the_setting_says",
]
tests_last_seen = [
    { file = "src/data/message_cache/folders.rs", tests = 61 },
    { file = "src/presentation/view_state.rs", tests = 57 },
]

# The setting is read where the folder opens and handed to the rule; a
# landing that hands the rule a constant takes the answer and ignores it,
# which is the shape the read-by-something guard exists for and cannot see
# once the name appears anywhere in the window. The break is that constant.
# Red in the reading over the window's shipping source that every landing
# hands the rule what the setting says; the function then has no caller,
# which is a warning and compiles.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named. The break's text moved
# later the same day, when 11-11.1.3 put the one `from_stored` call in
# `the_view_kept_under` so a folder and All Inboxes land by the same
# answer; the break is the same, the constant in place of the call, and
# now reddens every landing at once.
[[guard]]
name = "the folder landing hands the rule what show conversations by default says, not a constant"
file = "src/presentation/wx_app.rs"
suite = "the_settings_dialog_opens_in"
before = """            what_a_folder_never_set_shows(),"""
after = """            view_state::Showing::Messages,"""
red = [
    "test_the_folder_landing_reads_show_conversations_by_default_where_the_folder_opens",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# A check box read back by nothing is the panel half-working: the box moves,
# OK says it saved, and the next folder opens as before. The break reads
# the box and writes nothing. Red in the one test that builds the Reading
# page in a window session, which clears the box and reads OK's answer
# back, and in the reading over the page's readback function.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "ok writes show conversations by default back from the check box"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """    cfg.show_conversations_by_default = w.show_conversations_by_default.get_value();"""
after = """    let _ = w.show_conversations_by_default.get_value();"""
red = [
    "test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown",
    "test_the_check_box_carries_its_words_on_both_channels_and_is_written_back_on_save",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# The name NVDA hears is the one `set_accessible_name_and_description`
# installs, and a name that is not the label's words is a control heard as
# something other than what is on screen. The break shortens the name and
# leaves the label; UI Automation would still read the label, MSAA would
# read the shorter name, and the two channels would disagree. Red in the
# reading over the builder that the name is the label's words.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the show conversations by default check box is named with its own words on the channel nvda reads"
file = "src/presentation/wx_settings.rs"
suite = "the_settings_dialog_opens_in"
before = """        "Show conversations by default","""
after = """        "Show conversations","""
red = [
    "test_the_check_box_carries_its_words_on_both_channels_and_is_written_back_on_save",
]
tests_last_seen = [
    { file = "src/presentation/wx_settings.rs", tests = 0 },
    { file = "tests/the_settings_dialog_opens_in.rs", tests = 9 },
]

# ── All Inboxes shown as conversations, one row per account (#92, 11-11.1.3) ──
#
# A conversation is an account's, and the same identifier in two inboxes is
# two conversations (T-01-47). The every-inbox listing groups by account and
# conversation; the break groups by conversation alone, which folds the two
# accounts' rows into one row that counts both and says one account's name
# for the other's mail. Red in the cache test that writes the same
# conversation into two inboxes and asks for two rows.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "all inboxes lists a conversation held in two accounts as two rows, one per account"
file = "src/data/message_cache/messages.rs"
before = """        "m.account_id, m.thread_id","""
after = """        "m.thread_id","""
red = [
    "data::message_cache::messages::tests::test_all_inboxes_lists_a_conversation_held_in_two_accounts_as_two_rows",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# A row says where it was read so an act on it asks the scope its count
# came from, and under All Inboxes that is its own account's inbox. The
# break takes the account out of the lookup, so every row names the first
# inbox of any account, and a delete of a conversation in the second
# account would be counted against the first's inbox. Red in the same
# two-rows test, which reads the folder on each row.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "a row of all inboxes says its own account's inbox, not the first inbox of any account"
file = "src/data/message_cache/messages.rs"
before = """    read_in_folder: "(SELECT MIN(i.id) FROM inboxes i WHERE i.account_id = mf.account_id)","""
after = """    read_in_folder: "(SELECT MIN(i.id) FROM inboxes i)","""
red = [
    "data::message_cache::messages::tests::test_all_inboxes_lists_a_conversation_held_in_two_accounts_as_two_rows",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
]

# The message a row stands for is chosen among the rows of `here` in the
# same conversation, and under All Inboxes `here` holds every account's
# rows at once, so the choice has to stay inside the row's own account or
# the second account's row previews, opens and fetches the first account's
# message (T-11-113). The break drops the account from the macro every
# stands-for expression is built from. Red in the two-rows test, which
# reads each row's message id against the ids it wrote.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the whole library.
[[guard]]
name = "the message a row of all inboxes stands for is its own account's"
file = "src/presentation/message_columns.rs"
before = """            " FROM here r WHERE r.thread_id = m.thread_id AND r.account_id = m.account_id \\"""
after = """            " FROM here r WHERE r.thread_id = m.thread_id \\"""
red = [
    "data::message_cache::messages::tests::test_all_inboxes_lists_a_conversation_held_in_two_accounts_as_two_rows",
]
tests_last_seen = [
    { file = "src/data/message_cache/messages.rs", tests = 183 },
    { file = "src/presentation/message_columns.rs", tests = 43 },
]

# What the tester saw (#92): landing on All Inboxes left the view and the
# conversation rows as the last folder had left them, so a folder in
# Thread View put its own rows under the All Inboxes title. The break
# takes the settle out of the All Inboxes arm and leaves the view, the
# rows and the check mark untouched, which is the tree as it stood before
# 2026-09-20. Red in the reading over the arm that the view is read,
# settled and only then the mail asked for.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "landing on all inboxes settles its own view before the mail is asked for"
file = "src/presentation/wx_app.rs"
suite = "all_inboxes_keeps_a_view_of_its_own"
before = """                                settle_the_view_on_arrival(&state, &frame, showing);"""
after = """                                let _ = (&state, &frame, showing);"""
red = [
    "test_landing_on_all_inboxes_reads_its_own_view_settles_it_and_then_asks_for_the_mail",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/all_inboxes_keeps_a_view_of_its_own.rs", tests = 11 },
]

# All Inboxes holds every account's messages at once, and the same thread
# id in two accounts is two conversations (T-01-47); the tree Enter opens
# is built from the loaded rows filtered by the row's account as well as
# its thread id. The break drops the account from the filter, so either
# row would open a tree holding both accounts' mail (T-11-113). Red in the
# case that builds a state with one thread id in two accounts and in the
# reading over the function's text.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the tree a row of all inboxes opens holds its own account's messages alone"
file = "src/presentation/wx_app.rs"
suite = "all_inboxes_keeps_a_view_of_its_own"
before = """        .filter(|m| m.account_id == account_id && m.thread_id.as_deref() == Some(thread_id))"""
after = """        .filter(|m| !account_id.is_empty() && m.thread_id.as_deref() == Some(thread_id))"""
red = [
    "test_one_thread_id_in_two_accounts_opens_one_tree_per_account",
    "test_the_set_the_fetch_and_the_tree_read_the_rows_own_account",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/all_inboxes_keeps_a_view_of_its_own.rs", tests = 11 },
]

# D-11's switch to conversations selects the rows holding what was
# selected, and under All Inboxes a row is named by its account and its
# thread id together, since the same thread id in two accounts is two rows
# (T-01-47). The break keys a message by its thread id with no account, so
# a message of the second account comes back selected on the first
# account's row, and a command on that row would act on the other
# account's mail (T-11-113). Red in the reading over the switch's keys.
# Found on the branch before the merge, by reading the helper the switch
# already used rather than by the plan.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "switching all inboxes to conversations selects the row's own account's conversation"
file = "src/presentation/wx_app.rs"
suite = "all_inboxes_keeps_a_view_of_its_own"
before = """                        .map(|thread| (m.account_id.clone(), thread)),"""
after = """                        .map(|thread| (String::new(), thread)),"""
red = [
    "test_switching_to_conversations_selects_the_rows_own_accounts_conversation",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/all_inboxes_keeps_a_view_of_its_own.rs", tests = 11 },
]

# ── A Markdown marker counts at the start of any line (#79) ─────────────────
#
# The block rule fires on the typed space, and from 2026-07-29 until 11-11.3
# it refused whenever the text node holding the marker had a previous
# sibling, which every line after a line break in a reply to plain text
# has, as does a line after Shift+Enter and a line after Enter on the empty
# first line. The guard asks the line now: nothing before the node, or a
# `<br>`, or a block. The break puts the node question back, so a marker on
# any line after the first is refused again with nothing posted. Red in the
# one reading that drives the real page: steps 2, 3, 4 and 7b of it, which
# are one function.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "a marker is refused when anything at all precedes it in its parent"
file = "src/presentation/editor_document.rs"
suite = "a_marker_counts_at_the_start_of_any_line"
before = """    if (!startsItsLine(at.node)) {{ post({{ kind: 'refused', where: 'line' }}); return; }}"""
after = """    if (at.node.previousSibling) {{ return; }}"""
red = [
    "test_a_marker_typed_at_the_start_of_any_line_makes_its_structure",
]
tests_last_seen = [
    { file = "src/presentation/editor_document.rs", tests = 83 },
    { file = "tests/a_marker_counts_at_the_start_of_any_line.rs", tests = 1 },
]

# The first character typed after a closing inline delimiter is plain. The
# engine puts a caret placed after an inline element back inside it, so
# the word after **bold** was bold and so was every line after it (#79's
# side finding). The applier remembers the element it made and the input
# listener takes the first character typed after the close back out of it
# with `removeFormat`. The break forgets the element, so nothing is taken
# out and the word stays bold, the code span keeps its following word, and
# the line after Enter is bold too. Red in the reading over the real page:
# steps 8a, 8b and 8c of its one function.
#
# Measured 2026-09-20 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the first character typed after a closing delimiter is plain"
file = "src/presentation/editor_document.rs"
suite = "a_marker_counts_at_the_start_of_any_line"
before = """    elementJustMade();
    post({{ kind: 'style', index: hit.style.index }});"""
after = """    post({{ kind: 'style', index: hit.style.index }});"""
red = [
    "test_a_marker_typed_at_the_start_of_any_line_makes_its_structure",
]
tests_last_seen = [
    { file = "src/presentation/editor_document.rs", tests = 83 },
    { file = "tests/a_marker_counts_at_the_start_of_any_line.rs", tests = 1 },
]

# The browser is the first thing in the formatted message window that takes
# the keyboard, so activating the window puts the keyboard in the message and
# a screen reader's browse mode has a document to move through. wxWidgets
# gives an activated window's keyboard to the child it saved, or failing that
# to the first child that wants it (`wxSetFocusToChild`), and this window's
# only such child is the page. The break puts a text control in front of it,
# which is what somebody adding a control to this window does by accident;
# the keyboard then lands on the control, `K` is not a next-link key, and
# NVDA hears nothing. Red in the reading that activates the window, and not
# in its companion over a window whose control really is a text control,
# which is the pair that says the reading can tell them apart.
#
# Measured 2026-09-22 on the branch the record was written on, through
# `scripts/guards.sh --remeasure`, the target named.
[[guard]]
name = "the browser is the first thing in the page window that takes the keyboard"
file = "src/presentation/wx_app.rs"
suite = "the_page_window_keeps_the_document_focused_when_it_comes_back"
before = """    let page = WebView::builder(&frame)
        .with_backend(WebViewBackend::Edge)
        .build();
    set_accessible_name(&page, "Conversation");"""
after = """    let in_front_of_the_page = TextCtrl::builder(&frame).build();
    sizer.add(&in_front_of_the_page, 0, SizerFlag::Expand, 0);
    let page = WebView::builder(&frame)
        .with_backend(WebViewBackend::Edge)
        .build();
    set_accessible_name(&page, "Conversation");"""
red = [
    "test_the_document_keeps_focus_when_the_page_window_comes_back",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/the_page_window_keeps_the_document_focused_when_it_comes_back.rs", tests = 2 },
]

# A separate window shows a page and nothing else, and this is where that is
# decided (#80, 12-02, T-12-04). `--show-page` crosses a process boundary, so
# the address arrives from something this program cannot vouch for: it may
# have been typed, and the sanitiser it went through on the way out was a
# different process's. The break keeps the narrowing to http and https and
# drops the sanitiser, which is the shape somebody reaches for on the day the
# two checks look like one; the process then loads an address dressed to look
# like it goes somewhere it does not, and the window never closes, which the
# target kills and reports.
#
# The break used to be the other way round, keeping the sanitiser and dropping
# the scheme. It reddened, and on the way there it let the target hand a
# `mailto:` to the browser control, which is an address Windows gives to
# whatever reads mail on the machine. Nothing launched, twice, and that was
# luck. A break must not be able to start another program, so this one moves
# the failure onto a web address and the target's list puts that address
# first.
#
# The target rather than the library, because the module's own cases are
# reached by `--lib presentation::page_window::` on any commit that changes
# this file, and this reading is not: without a record naming this file it
# would run only when its own file changed, which is every commit except the
# ones that could break it.
#
# Measured 2026-09-22 on the branch the record was written on, and again the
# same day on the branch that corrected the break.
[[guard]]
name = "a separate window opens a page and refuses everything that is not one"
file = "src/presentation/page_window.rs"
suite = "a_separate_window_is_its_own_process"
before = """    let safe = HtmlRenderer::safe_external_url(address)?;
    let scheme = safe.to_ascii_lowercase();
    (scheme.starts_with("http://") || scheme.starts_with("https://")).then_some(safe)"""
after = """    let scheme = address.trim().to_ascii_lowercase();
    (scheme.starts_with("http://") || scheme.starts_with("https://"))
        .then(|| address.trim().to_string())"""
red = [
    "test_an_address_that_is_not_a_page_opens_no_window",
]
tests_last_seen = [
    { file = "src/presentation/page_window.rs", tests = 4 },
    { file = "tests/a_separate_window_is_its_own_process.rs", tests = 7 },
]

# A page process is decided before the loop that reads the flags of a run,
# because it is not a run (#80, 12-02). The break leaves the early answer in
# place and stops it answering, which is what moving it into the loop would
# do: `--show-page` then falls through to the arm that refuses anything
# beginning with a dash, and the window a person asked for never opens.
#
# Two cases red rather than one: the flag stops being read at all, so the
# refusal for a missing address stops saying what it wanted as well.
#
# Measured 2026-09-22 on the branch the record was written on.
[[guard]]
name = "the flag that opens one page is answered before the flags of a run"
file = "src/presentation/command_line.rs"
before = """    if let Some(at) = args.iter().position(|arg| arg == page_window::FLAG) {
        let Some(address) = args.get(at + 1) else {
            return Command::Refused(format!("{} needs an address", page_window::FLAG));
        };
        return Command::ShowPage(address.clone());
    }"""
after = """    if let Some(at) = args.iter().position(|arg| arg == page_window::FLAG) {
        let _ = at;
    }"""
red = [
    "presentation::command_line::tests::test_show_page_is_answered_before_anything_is_opened",
    "presentation::command_line::tests::test_show_page_with_no_address_is_refused_rather_than_opening_a_window",
]
tests_last_seen = [
    { file = "src/presentation/command_line.rs", tests = 28 },
]

# T-12-05: a page process opens no database, prepares no data folder, opens
# no log file and claims no single-copy marker, and the only thing that makes
# that true is where its arm sits in `main` (#80, 12-02). The break leaves
# the arm where it is and takes the window out of it, which is what somebody
# does on the day they decide a page should open through the ordinary path;
# `main` then names no page process at all and the reading that holds the
# order has nothing to compare.
#
# `src/main.rs` maps to a `--lib` filter that matches nothing, so without
# this record the reading runs on no commit that could break it.
#
# Measured 2026-09-22 on the branch the record was written on.
[[guard]]
name = "a page process is answered before this start claims or prepares anything"
file = "src/main.rs"
suite = "a_separate_window_is_its_own_process"
before = """        Command::ShowPage(address) => {
            std::process::exit(page_window::show(&address));
        }"""
after = """        Command::ShowPage(_address) => {
            std::process::exit(page_window::NOT_A_PAGE);
        }"""
red = [
    "test_a_page_process_is_answered_before_this_start_claims_or_prepares_anything",
]
tests_last_seen = [
    { file = "src/main.rs", tests = 0 },
    { file = "tests/a_separate_window_is_its_own_process.rs", tests = 7 },
]

# The third place a link can open really is the third place (#80, 12-02).
# 11-11.1 left this arm calling `open::that` and telling a line that said
# separate windows arrived with the next build, which was ledger 566; the
# break puts the browser back and takes the process away, which is the shape
# somebody reaches for on the day starting a second copy looks like more than
# it is worth. The window is then the browser again and nothing says so,
# which is the silent version of the same defect.
#
# One test red rather than two: the reading over the two promises is about
# sentences and this leaves them alone.
#
# Measured 2026-09-22 on the branch the record was written on.
[[guard]]
name = "the third place a link can open starts a window of this program's own"
file = "src/presentation/wx_app.rs"
suite = "a_separate_window_is_its_own_process"
before = """        opening_links::Route::SeparateWindow => match a_window_of_its_own(&safe) {"""
after = """        opening_links::Route::SeparateWindow => match open::that(&safe) {"""
red = [
    "test_the_route_starts_this_program_again_rather_than_opening_the_browser",
]
tests_last_seen = [
    { file = "src/presentation/wx_app.rs", tests = 199 },
    { file = "tests/a_separate_window_is_its_own_process.rs", tests = 7 },
]
