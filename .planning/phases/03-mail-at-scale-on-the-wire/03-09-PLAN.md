---
phase: 03-mail-at-scale-on-the-wire
plan: 09
type: execute
wave: 9
depends_on: ["03-08"]
files_modified:
  - src/application/conflict_choice.rs
  - src/application/contacts_sync.rs
  - src/application/caldav_sync.rs
  - src/presentation/wx_app.rs
  - .planning/REQUIREMENTS.md
  - guards/guards.toml
  - docs/changelog.md
autonomous: true
requirements: [SCALE-06]
estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "When a contact has changed here and changed at the address book, the person is shown both versions and chooses, and neither is written over while they decide."
    - "An unresolved conflict is not pushed, so it cannot become a silent overwrite at the provider."
    - "Choosing works from the keyboard alone and the two versions are announced as a labelled pair, not as two unlabelled panes."
    - "A calendar item whose etag says the server copy moved asks the same question, instead of resolving silently and showing nobody anything."
    - "Mail's real defect is recorded as what it is, a change made while the server is unreachable being silently reverted rather than queued, and is not disguised as a both-changed conflict that mail cannot have."
  artifacts:
    - "src/application/conflict_choice.rs: the held conflict, the two versions, and the decision, in a new module with no window type and no sync in it."
    - "src/application/contacts_sync.rs: the losing arm holding the conflict instead of writing over and apologising afterwards."
    - "src/application/caldav_sync.rs: the etag disagreement raising the same question."
    - "src/presentation/wx_app.rs: the choosing surface, keyboard-only, announcing a labelled pair."
    - ".planning/REQUIREMENTS.md: SCALE-06's mail paragraph restated to name the defect mail actually has."
  key_links:
    - "whose_copy_wins (contacts_sync.rs:988) -> the held conflict: the four-armed decision already exists and already identifies the losing case. This plan changes what happens in that arm, not how the arm is chosen."
    - "SyncResult::sent_over_a_newer_copy (contacts_sync.rs:335) -> the sentence at :1674-1690 -> wx_app.rs:15289: the telling already works and is the model for how the new question reaches somebody."
---

<objective>
Put the conflict choice where the conflicting state can actually happen, and stop
the losing copy being written over and apologised for afterwards.

Purpose: criterion 6. SCALE-06's evidence was corrected on 2026-09-03 because
"no conflict resolution path in `src/`" is wrong and getting it wrong would have
had this phase write a second conflict model beside a working one, with the two
disagreeing about who wins. Contacts already resolves: `whose_copy_wins`
(`contacts_sync.rs:988`) returns a four-armed decision built from whether local
work is unsent and whether the address book's version marker moved, with two
production call sites reached from the interface, and the losing case counted and
spoken rather than silent. What is missing is that the losing case is told after
the fact rather than asked before it.

Output: a held conflict, a choice made from the keyboard, a calendar path that
asks the same question, and mail's real defect written down as itself.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mail-at-scale-on-the-wire/03-RESEARCH.md
@CLAUDE.md
</context>

<premise_corrections>
**1. This plan rests on an assumption Pratik has not confirmed.**
`03-RESEARCH.md` asks whether the choice should be built where the state exists,
contacts and CalDAV, rather than where the requirement points. **This plan
assumes yes**, and the argument is in the requirement's own corrected text: mail
flags are optimistic-local with revert-on-failure, nothing queues a mail flag
change, so the "both changed" state this requirement describes largely cannot
arise there. Building a mail conflict chooser would be building a surface for a
state that does not occur.

**2. Do not write a second conflict model.** `whose_copy_wins` decides who wins
and it decides correctly, comparing markers rather than clocks on purpose
(`contacts_sync.rs:977-982`). This plan changes what happens in one of its four
arms. If the work starts to look like a new decision function, stop: that is the
duplicate `03-RESEARCH.md` warns about, and the two would disagree the first time
either changed.

**3. A hundred and thirty-five guard records name
`src/application/contacts_sync.rs`.** That is the most expensive file in this
tree for the count check by a wide margin: the next worst is sixty-eight. Adding
test functions there flags all hundred and thirty-five, and a remeasure at a
build and a full library run each is an overnight job. **So the new type, the
held conflict and the decision go in a new module, `conflict_choice.rs`, which no
record names**, and `contacts_sync.rs` gains as few test functions as the work
allows. That is not tidiness, it is hours, and it is written here so the next
person does not move the module for tidiness. The count for
`contacts_sync.rs` is quoted in the summary before and after so the cost of what
was added is visible.

**4. The telling already works and is the model.**
`SyncResult::sent_over_a_newer_copy` (`:335`) becomes a sentence at `:1674-1690`
and reaches the user through `what_the_contacts_sync_did` at `wx_app.rs:15289`.
The asking should arrive the same way rather than through a new channel.

**5. CalDAV has the markers and not the choice.** `etag` and `If-Match` are at
`caldav_sync.rs:870`, `:1028`, `:3424` and `:3716`, and it resolves automatically
showing the user nothing. So the second half of this plan is wiring an existing
disagreement to an existing question, not building detection.

**6. Mail's real defect is adjacent and is not this.** A flag change made while
the server is unreachable is silently reverted rather than queued
(`wx_app.rs:17381-17404` reverts per flag kind with a sentence). That is worth
recording and it is not a both-changed conflict. Task 3 records it; it does not
fix it, because fixing it means queueing mail flag changes, which is a
different-sized piece of work and would need its own plan.

**7. The five `*_sync.rs` files total over 38,000 lines and none has met a live
account.** Everything here is proved against divergent local states driven through
the same code path the sync uses, which is what SCALE-06's fourth deliverable
asks for and is all that is possible.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A contact that changed in both places is held, not overwritten</name>
  <files>src/application/conflict_choice.rs, src/application/contacts_sync.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/contacts_sync.rs:940-1000`, `WhoseCopyWins` and `whose_copy_wins` in full with the comment at `:977-982` about comparing markers rather than clocks.
    - `src/application/contacts_sync.rs:2280-2300` and `:2475-2495`, the two production call sites, so the held conflict is raised where the decision is already made rather than somewhere new.
    - `src/application/contacts_sync.rs:330-340` and `:1670-1695`, `sent_over_a_newer_copy` and the sentence it becomes, which is the existing telling this replaces with an asking.
    - `src/presentation/wx_app.rs:15285-15295`, where that sentence reaches somebody.
    - `src/application/mod.rs`, for how a module is declared here.
  </read_first>
  <behavior>
    - When local work is unsent and the address book's version marker has moved, the sync neither writes over the local copy nor sends it. The item is held with both versions kept.
    - A held conflict is not pushed on any later sync while it is unresolved, so a second sync does not resolve it by accident.
    - The other three arms of the existing decision behave exactly as they do today, and every existing test for them keeps its expected value.
    - A held conflict carries enough to show both versions: what is here, what the address book has, and which fields differ.
    - Resolving a held conflict by choosing one version leaves exactly that version, sends it if it is the local one, and clears the hold.
    - Two divergent local states driven through the same code path the sync uses produce a held conflict, which is SCALE-06's fourth deliverable and is the only proof available without an account.
  </behavior>
  <action>
    RED first, on a branch. Behaviour one is the defect: today the losing arm
    writes over and counts it. Drive two divergent states through the sync path
    and assert nothing was written and nothing was sent. Write these tests in the
    new module wherever they can live there, and in `contacts_sync.rs` only for
    the ones that genuinely need the sync path.

    Commit RED with `Fails-until-green:` trailers naming each new test plus
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held`, and
    say in the body how many records name `contacts_sync.rs` and that the
    remeasure is an overnight job to be run after the merge.

    GREEN: create `src/application/conflict_choice.rs` holding the conflict type,
    the two versions, the field comparison, and the resolution. No window type, no
    sync in it, no database. That keeps it testable on its own and it is what
    keeps the test functions out of the expensive file.

    Change the losing arm of `whose_copy_wins`'s use, not `whose_copy_wins`
    itself. The arm is already identified correctly; what changes is that instead
    of writing and counting, it raises a held conflict. Keep the count, because a
    number of conflicts found is still worth saying.

    Store the hold somewhere a later sync will see it, and make the later sync
    skip a held item. That is the third deliverable and it is the one that stops a
    conflict becoming a silent overwrite: without it, the next sync resolves what
    the person has not.

    Do not change the other three arms. Every existing test for them keeping its
    expected value is the check that the change was surgical, and the summary
    counts them.

    Add a `guards/guards.toml` record for the hold. Take the break by hand: make
    the losing arm write over as it does today, run the library, write down
    everything that really goes red. Expect this list to be long, since a hundred
    and thirty-five records already name this file; write down all of it anyway,
    which is the rule.
  </action>
  <verify>
    <automated>cargo test --lib application::conflict_choice:: --lib application::contacts_sync::</automated>
  </verify>
  <acceptance_criteria>
    - Both named test filters pass, and `scripts/check.sh` passes for this commit's file list.
    - The nothing-written-nothing-sent case was seen red before the change, and the failure output is quoted.
    - `whose_copy_wins` itself is unchanged, and the summary says so by quoting the function before and after or stating it was not touched.
    - Every existing test for the other three arms keeps its expected value; the summary counts them.
    - A second sync over a held conflict changes nothing, with a test.
    - The summary quotes the test-function count in `contacts_sync.rs` before and after this task, and says how many test functions went into `conflict_choice.rs` instead.
    - The `guards/guards.toml` record was measured by taking the break by hand, with the full red list quoted however long it is.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
  </acceptance_criteria>
  <done>A contact edited in two places is still both, waiting for somebody to choose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Choosing, from the keyboard, with both versions named</name>
  <files>src/presentation/wx_app.rs, src/application/conflict_choice.rs, guards/guards.toml</files>
  <read_first>
    - Task 1's `conflict_choice` module as left.
    - `src/presentation/wx_app.rs:15285-15295`, `what_the_contacts_sync_did`, since the question should arrive where the sentence about the losing copy already arrives.
    - `src/presentation/wx_app.rs:15159-15185`, the offer panel phase 2 built, for a keyboard-reachable offer whose label and accessible name come from one string, and for the topic reasoning.
    - `docs/KEYBOARD_SHORTCUTS.md`, since `CLAUDE.md` requires a shortcut to be documented in the same commit as the shortcut, if this task adds one.
    - `src/presentation/accessibility/names.rs` and `set_accessible_name`'s callers, and `CLAUDE.md`'s note that `set_name` sets an internal wxWidgets identifier and never reaches the accessibility tree. Sixteen widgets were once "named" that way and it compiled and passed 324 tests.
    - Broken windows ledger entries 14, 20, 21 and 22, which record what phase 2 could and could not prove about names and focus in a dialog.
  </read_first>
  <behavior>
    - A held conflict can be reached, read and resolved using the keyboard alone, with no mouse-only or drag-only step.
    - The two versions are announced as a labelled pair: each is introduced by which copy it is, this computer's or the address book's, rather than presented as two unlabelled blocks.
    - The fields that differ are identified, so somebody does not have to compare two whole contacts by ear to find the one changed line.
    - Every control carries an accessible name set through the call that reaches the accessibility tree, not through the one that sets an internal identifier.
    - Choosing resolves the hold and the result is announced.
    - Leaving without choosing leaves the hold in place and nothing is pushed.
  </behavior>
  <action>
    RED first, on a branch. Write what can be proved without a running window
    first: the pair's wording, the labelling, and the field difference, all built
    in `conflict_choice.rs` from the two versions and testable there. Then the
    wiring assertions in whatever this project uses to read `wx_app.rs` for what
    it does, following the nearest existing example rather than inventing one.

    Commit RED with `Fails-until-green:` trailers naming each new test plus the
    count check.

    GREEN: build the wording in `conflict_choice.rs`, so the sentences somebody
    hears are testable without a window. Each version is introduced by which copy
    it is, in plain words: what is on this computer and what the address book
    has. Name the fields that differ. `CLAUDE.md`'s cognitive rule is plain
    language and reduced memory load, and reading two whole contacts aloud to find
    one changed telephone number is exactly the memory load it forbids.

    Build the surface in `wx_app.rs`, keyboard-reachable, following the offer
    panel precedent. Set every accessible name through the call that reaches the
    accessibility tree. `CLAUDE.md` records that `set_name` does not, and that
    sixteen widgets were once named with it, compiled, and passed the suite. If a
    shortcut is added, document it in `docs/KEYBOARD_SHORTCUTS.md` in the same
    commit.

    Announce the pair. Whether the two versions are announced together or on
    arrival at each is a judgement a screen reader settles; build the version this
    plan can defend, which is each introduced by its label as focus reaches it,
    plus one sentence on opening saying what is being asked and how many fields
    differ. Say in a comment that this is the arrangement chosen and that it is
    unverified by ear.

    Leaving without choosing must leave the hold. That is the third deliverable
    again at the interface, and it is the arm somebody will exercise most.

    Add a `guards/guards.toml` record for the accessible names being set through
    the call that reaches the tree. Take the break by hand: change one to the call
    that does not, run the library, write down what goes red. If nothing goes red,
    that is a finding and the guard is written before this task closes, because it
    is the exact defect `CLAUDE.md` names as having compiled and passed.
  </action>
  <verify>
    <automated>cargo test --lib application::conflict_choice:: --lib presentation::wx_app:: --test wired</automated>
  </verify>
  <acceptance_criteria>
    - The named test filters pass, and `scripts/check.sh` passes for this commit's file list.
    - The pair's wording is built in `conflict_choice.rs` and tested there, and the summary quotes both introductory labels in full.
    - The differing fields are named, with a test over a pair differing in one field and a pair differing in several.
    - Every control's accessible name is set through the call that reaches the accessibility tree, held by a guard whose break was taken by hand; if that break reddened nothing, the guard was strengthened before the task closed and the summary says so.
    - Any new shortcut is in `docs/KEYBOARD_SHORTCUTS.md` in the same commit.
    - Leaving without choosing leaves the hold, with a test.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
  </acceptance_criteria>
  <done>Somebody can hear both versions, told which is which, and choose without a mouse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The calendar asks the same question, and mail's real defect is written down</name>
  <files>src/application/caldav_sync.rs, .planning/REQUIREMENTS.md, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/application/caldav_sync.rs:860-880`, `:1020-1040`, `:3415-3435` and `:3705-3725`, the four places `etag` and `If-Match` are used, so the disagreement is detected where it already is rather than in a new check.
    - Task 1's `conflict_choice` module and task 2's wording, since the calendar reuses both.
    - `src/presentation/wx_app.rs:17375-17410`, mail's revert-per-flag-kind path, so the recorded defect is described accurately.
    - `.planning/REQUIREMENTS.md` SCALE-06 in full, especially its mail paragraph.
    - `docs/changelog.md`'s `[Unreleased]` section.
  </read_first>
  <behavior>
    - A calendar item whose etag says the server copy moved while there is unsent local work raises the same held conflict a contact does, using the same module.
    - An item with no local change takes the server copy as it does today.
    - An item the server has not moved sends as it does today.
    - A held calendar conflict is not pushed on a later sync.
    - Two divergent local states driven through the CalDAV path produce a held conflict, proved without a live account.
  </behavior>
  <action>
    RED first, on a branch. Behaviour one fails today: CalDAV resolves
    automatically and shows nobody anything. Drive two divergent states through
    the CalDAV path and assert a hold.

    Commit RED with `Fails-until-green:` trailers naming each new test plus the
    count check.

    GREEN: raise the held conflict from the etag disagreement, reusing task 1's
    module and task 2's wording. The wording will need a word for what the two
    copies are here, since "the address book" is wrong for a calendar; add that as
    a parameter of the wording rather than a second set of sentences, so the two
    cannot come to differ.

    Do not build a second detection. The markers are there and are already
    compared; what changes is what happens when they disagree.

    Then the documentation half, which is not a fix and must not be dressed as
    one. Restate SCALE-06's mail paragraph so it names the defect mail actually
    has: a flag change made while the server is unreachable is silently reverted
    rather than queued. Keep the existing correct description of why the
    both-changed state largely cannot arise in mail. Say which plan or phase would
    own queueing mail flag changes, or say plainly that nothing owns it yet, and
    record it in the broken windows ledger so it stays visible. `CLAUDE.md`'s rule
    about not silently absorbing a gap applies here: a requirement quietly retired
    because it turned out to be about something else is a gap nobody is pressured
    to fix.

    Write the `docs/changelog.md` entry under `[Unreleased]` covering the whole
    plan: when a contact or a calendar item has been changed both here and at the
    provider, Wixen Mail now shows both and asks, and sends nothing until you
    choose. Say the honest limits: none of the five sync paths has ever met a live
    account, and mail flags are not covered because they work differently, with one
    sentence saying how.

    Bump the version.

    Record the accessibility half in the ledger as `unrun-verify`: whether the
    labelled pair is understood by ear, whether announcing each version on arrival
    rather than both at once is the right arrangement, and whether the list of
    differing fields is useful or is a sentence somebody stops hearing. Phase 2's
    entries 26 and 33 are the precedent, both about whether a tally read aloud is
    useful or is noise.

    Add a `guards/guards.toml` record for the calendar raising rather than
    resolving. Take the break by hand.
  </action>
  <verify>
    <automated>cargo test --lib application::caldav_sync:: --lib application::conflict_choice:: --test house_style --test docs_links</automated>
  </verify>
  <acceptance_criteria>
    - The named test filters pass, and `scripts/check.sh` passes for this commit's file list.
    - The calendar hold was seen red before the change, and the failure output is quoted.
    - The wording is parameterised rather than duplicated, and the summary quotes the calendar's version of both labels.
    - The three unchanged behaviours keep their existing tests' expected values, and the summary counts them.
    - SCALE-06's mail paragraph names the revert-rather-than-queue defect and says who owns it or that nobody does; a broken windows ledger entry exists for it either way.
    - `docs/changelog.md` says none of the sync paths has met a live account and says in one sentence why mail flags are not covered.
    - An `unrun-verify` ledger entry exists naming the three things a screen reader run would settle.
    - The version is bumped in the same commit.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
  </acceptance_criteria>
  <done>The calendar asks the same question the address book does, and the thing mail actually gets wrong is written down as itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a provider's copy -> the copy on this computer | one of the two is about to be lost and nothing today asks which |
| an unresolved conflict -> a later sync | a sync that resolves what the person has not is a silent overwrite with extra steps |
| a second conflict model -> the working one | two decisions about who wins disagree the first time either changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-39 | Tampering | the losing copy in `whose_copy_wins`'s fourth arm | critical | mitigate | the arm holds both versions instead of writing over; a test drives two divergent states and asserts nothing written and nothing sent |
| T-03-40 | Elevation of privilege | a later sync pushing an unresolved conflict | critical | mitigate | held items skipped by the sync, with a test that a second sync changes nothing |
| T-03-41 | Repudiation | two versions presented as two unlabelled panes | high | mitigate | wording built and tested in `conflict_choice.rs`, each version introduced by which copy it is, with the differing fields named |
| T-03-42 | Repudiation | an accessible name that never reaches the accessibility tree | high | mitigate | a guard whose break swaps the call for the one that does not reach the tree; strengthened if that break reddens nothing |
| T-03-43 | Repudiation | a requirement retired because it was about something else | medium | mitigate | SCALE-06's mail paragraph restated to name the real defect, plus a ledger entry so it stays visible |
| T-03-44 | Spoofing | a second conflict decision beside the working one | high | mitigate | `whose_copy_wins` is not changed; only what happens in one of its arms |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | no dependency added; `Cargo.toml` changes only its version line |
</threat_model>

<verification>
- `cargo test --lib application::conflict_choice:: --lib application::contacts_sync:: --lib application::caldav_sync::` passes.
- Two divergent local states driven through each sync path produce a held conflict, which is SCALE-06's fourth deliverable.
- Three `guards/guards.toml` records were measured by hand.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` runs in the background after the merge. This branch changes `contacts_sync.rs`, which a hundred and thirty-five records name, and `wx_app.rs`, which sixty-one name. This is the most expensive remeasure in the phase and it must not block the merge.
- The `unrun-verify` ledger entries exist.
</verification>

<success_criteria>
Criterion 6 is closed structurally for contacts and for the calendar: when a local
copy and a server copy have both changed, both are shown, somebody chooses, and
nothing is pushed until they do.

It is not closed by ear, and it is not closed for mail. The accessibility half is
`unrun-verify` in the ledger. Mail is excluded on purpose, because the state the
criterion describes largely cannot arise there, and mail's real defect is recorded
as itself rather than fixed here.

The aim of this plan is an assumption Pratik has not confirmed. If he wants a mail
conflict chooser as well, that is a different plan and it needs mail flag changes
to be queued first, which nothing currently does.
</success_criteria>

<output>
Create `.planning/phases/03-mail-at-scale-on-the-wire/03-09-SUMMARY.md` when done
</output>
