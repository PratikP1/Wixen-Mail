---
phase: 02-search-that-says-what-it-covers
plan: 07
type: execute
wave: 7
depends_on: ["02-06"]
files_modified:
  - src/presentation/manager_words.rs
  - src/presentation/wx_managers.rs
  - src/application/context_menu.rs
  - src/presentation/wx_app.rs
  - tests/manager_dialog_labels.rs
  - guards/guards.toml
  - docs/changelog.md
autonomous: true
requirements: [SEARCH-03]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user opens a saved search from the folder tree and edits its conditions: adding, editing and removing them, reaching any of the eleven fields and any of the eleven match types (D-2-01)."
    - "Every change says one sentence carrying both what happened and how many conditions are left."
    - "A search cannot be saved with no conditions at all."
    - "A search made in the search box and one made in the rule editor sit in the same group in the tree, and a search edited by the other door does not move (D-2-02)."
    - "The list of conditions reports its own size to both accessibility channels from the system's provider, and the tab order does not change as conditions come and go."
  artifacts:
    - "src/presentation/manager_words.rs: the noun for a condition, beside the five that exist."
    - "src/presentation/wx_managers.rs: populate_questions and show_rule_manager_dialog, over run_manager_loop."
    - "src/application/context_menu.rs and src/presentation/wx_app.rs: the entry that opens it, on a saved-search row."
    - "guards/guards.toml: one record that the editor writes through the atomic replace."
  key_links:
    - "the folder tree context menu -> show_rule_manager_dialog -> show_rule_edit: two dialogs deep, the same depth the filter manager already is."
    - "the editor's Close -> the atomic replace: the editor holds a working copy and writes once, so a cancelled edit changes nothing."
    - "run_manager_loop -> the native ListCtrl -> the system's own provider: the set size reaches both channels without set_accessible_name being involved."
---

<objective>
Turn the condition dialog into the second door D-2-01 describes: a manager over
one saved search's conditions, reached from the folder tree, writing back through
the atomic replace, with everything it stores landing in the same one group.

Purpose: SEARCH-03, and roadmap criterion 3. One stored thing, two doors, one
matcher, one group.

Output: a manager dialog over `Question` rows, an entry that opens it, and the
one-group property under test.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-search-that-says-what-it-covers/02-CONTEXT.md
@.planning/phases/02-search-that-says-what-it-covers/02-PATTERNS.md
@.planning/phases/02-search-that-says-what-it-covers/02-RESEARCH.md
@.planning/phases/02-search-that-says-what-it-covers/02-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A manager over one search's conditions</name>
  <files>src/presentation/manager_words.rs, src/presentation/wx_managers.rs</files>
  <read_first>
    - `src/presentation/wx_managers.rs:203-350`, `run_manager_loop` in full: its signature, already generic over the row type; the four buttons it builds at `:221-236`; the Edit arm at `:333-346` showing that Add and Edit end the outer modal with a sentinel id and re-enter; and how `populate` is a free function taking the whole slice.
    - `delete_selected` at `:152-187`, which already computes how many are left and uses it to put the row cursor back, and says a sentence through `said_and_shown`. The count is computed and not spoken today.
    - `show_filter_manager_dialog` at `:2423-2461`, the whole shape to copy: `make_shell`, `insert_column` per column, then one `run_manager_loop` call whose add and edit closures are the same sub-dialog with and without a row.
    - `populate_filters` at `:2502-2520`, which begins with `delete_all_items`.
    - `make_shell` at `:357-381`, which builds the native `ListCtrl` in report mode and sets one accessible name on the control. The item count and position in set come from Windows' own provider on both channels; nothing here computes them, and `"Items"` is generic.
    - `src/presentation/manager_words.rs` in full: the five nouns at `:12-16` and the four sentence builders at `:19-53`.
    - `build_rule_edit_dialog` and `show_rule_edit` as plan 02-06 left them.
    - The two dialogs in `01-05`'s deferred items that hang row data off a tree control and leak a registry entry per row. Do not do that: `run_manager_loop` keeps its rows in a `Vec` beside the control, which is what makes it safe.
  </read_first>
  <behavior>
    - The manager lists one row per condition, with columns naming the field, how it matches, and what it matches against, all in words.
    - Adding a condition appends it and says one sentence carrying both the change and the new count.
    - Editing a condition replaces it in place and says one sentence.
    - Removing a condition says one sentence carrying both the removal and the new count, and puts the row cursor back on the row that moved up.
    - Closing with no conditions left is refused out loud and the dialog stays open.
    - Cancelling leaves the stored search exactly as it was: the manager works on a copy.
    - The list's accessible name says what it holds, rather than the generic word the shell sets by default.
  </behavior>
  <action>
    Take RED from the built dialog: assert the list's row count and the announced sentence, and confirm each fails on content.

    Add a noun for a condition to `manager_words`, beside the five that exist, so every sentence about one is built by the four builders already there. Do not write a sentence in `wx_managers.rs`.

    The count-in-the-same-sentence requirement plugs into `manager_words::deleted` and its siblings rather than becoming a second announcement. `delete_selected` already computes how many are left for the row cursor; make the sentence carry it. A count said separately is a second announcement that interrupts the first, which is worse than not saying it. Whether the count belongs in every builder or only in the ones for adding and removing is a judgement to make and record: what must not happen is two announcements for one change.

    Build the manager as a copy of `show_filter_manager_dialog`'s shape over `Question` rows, with `populate_questions` written the way `populate_filters` is. `run_manager_loop` is already generic over the row type and needs no change to take a `Question`.

    Give the list its own noun rather than the generic accessible name `make_shell` sets. If that means a parameter on `make_shell`, that is a smaller change than a second shell; take it and update the existing callers, which the compiler will name.

    Refuse a save with no conditions, out loud, using `a_sub_dialog_needs` and `event.event.skip(false)` the way the name refusal does. The store refuses it too, from plan 02-06; two refusals is deliberate, because a dialog is where somebody can be told and a store is where nothing gets past.

    Do not replace `run_manager_loop` and do not hand-roll a scrolling stack of rows. The native list is the only shape where the set size reaches both accessibility channels from the system's own provider, the tab order never changes as rows come and go, and there is no drag interaction. A hand-rolled stack would have to set the count with `set_accessible_name`, which reaches one channel only.

    Add the new dialog to `tests/manager_dialog_labels.rs`'s single test.
  </action>
  <verify>
    <automated>cargo test --lib presentation::wx_managers && cargo test --lib presentation::manager_words && cargo test --test manager_dialog_labels && cargo test --test manager_delete_stays_open</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib presentation::wx_managers` passes and includes the seven behaviours as named tests.
    - A test asserts one sentence per change, not two: it fails if a second announcement is added, demonstrated once by hand.
    - A test asserts the announced sentence after a removal contains the number of conditions left.
    - `grep -n 'set_accessible_name' src/presentation/wx_managers.rs` shows the list named with its own noun, and `grep -n '"Items"' src/presentation/wx_managers.rs` no longer names the rule manager's list.
    - `cargo test --test manager_delete_stays_open` still passes, so the existing manager behaviour is unchanged.
    - `cargo test --test manager_dialog_labels` reports exactly one test and covers the new dialog.
    - `grep -n 'set_item_data\|item_data' src/presentation/wx_managers.rs` returns nothing new, so no row data is hung off a control.
  </acceptance_criteria>
  <done>One saved search's conditions can be added to, edited and removed in a native list that reports its own size, with one sentence per change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reached from the tree, written through the replace</name>
  <files>src/application/context_menu.rs, src/presentation/wx_app.rs</files>
  <read_first>
    - `src/application/context_menu.rs`: the `Focus` enum at `:36-45`, `Focus::MailFolders` at `:40`, and how the entries for that focus are built at `:155`. Read how another focus decides which entries apply to the row under the cursor.
    - `src/presentation/wx_app.rs:2923-2940`, where `wire_context_menu(&folder_tree, Focus::MailFolders)` is attached.
    - The saved-search row identity and its handler: `WhichRow::SavedSearch` in `src/presentation/folder_tree.rs:118` and `:156`, and the function in `wx_app.rs` that turns a tree row into a `ChosenSearch` near `:9760`, plus `ChosenSearch` at `:9736-9758`, whose two shapes tell a readable search from one a newer version wrote.
    - `run_a_saved_search` at `:6254`, for how a saved search is fetched and run from a row today.
    - `rename_saved_search` and `delete_saved_search` in `src/data/message_cache/saved_searches.rs`, and wherever `wx_app.rs` already calls them from a tree row, which is the existing precedent for a command on a saved-search row.
    - The atomic replace plan 02-06 added.
    - `show_filter_manager_dialog(parent: &Frame, ...)` at `src/presentation/wx_managers.rs:2423`, which takes a `Frame`, so a manager opened from the frame is two dialogs deep once it opens the condition dialog.
  </read_first>
  <behavior>
    - The context menu on a saved-search row offers to edit its conditions, and the same entry is absent on every other row kind.
    - The entry is absent, or refuses with a sentence, on a search this build cannot read, because its questions cannot be shown without guessing at them.
    - Choosing the entry opens the manager with that search's current conditions listed.
    - Closing with changes writes them through the atomic replace, under the same identifier.
    - Closing without changes writes nothing at all.
    - After a write, the tree and any open results reflect the new conditions without the application being restarted.
    - The editor is reached from the frame, so the deepest stack is the manager and then the condition dialog: two, the same as the filter manager.
  </behavior>
  <action>
    Take RED from the context menu's entry list for each row kind, and from the write path, so each failure is about behaviour.

    Add the entry to the mail-folders focus, shown only for a saved-search row. `02-RESEARCH.md`'s open question 4 recommends reaching the editor from the folder tree's context menu rather than from a saved-search manager, because that keeps the stack two deep and this codebase has never gone three. Take that recommendation and say in the summary that it was a discretionary choice under D-2-06's list.

    Handle the unreadable-search case deliberately. `ChosenSearch` exists precisely because a search written by a newer version is a row somebody can land on, rename and remove but cannot run, and its questions are words this build does not understand. Offering to edit them would mean showing a guess or dropping what it could not read, and dropping is the "half a question list" failure. Refuse with a sentence naming why, using the wording `SAVED_BY_ANOTHER_VERSION` already gives, or leave the entry out for that row and say so.

    Write once, on close, through plan 02-06's replace, from the working copy `run_manager_loop` already keeps. Do not write per change: a manager that writes on every add would leave a search with a half-edited list if the window were closed part-way, which is exactly what the one-transaction rule exists to prevent.

    Refresh the tree and any showing results after a write, using whatever the rename and delete paths already do. A change that is stored and not shown reads as a change that did not happen.

    Errors through `common::Error`, and a failure to write says a sentence rather than being logged and swallowed.
  </action>
  <verify>
    <automated>cargo test --lib application::context_menu && cargo test --lib presentation::wx_app && cargo test --test tree_selection_raises</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib application::context_menu` passes and includes a test that the entry appears for a saved-search row and for no other row kind.
    - `cargo test --lib presentation::wx_app` passes and includes tests for the readable and unreadable cases, the write-on-close case, and the no-changes case.
    - A test asserts nothing is written when the manager is closed without changes; it fails if the write is made unconditional, demonstrated once by hand.
    - A source-reading test over the shipping half of `wx_app.rs`, using `common::what_ships`, asserts the manager is opened from somewhere: it fails if the call is removed.
    - The summary states the dialog depth reached, and it is two.
    - SEARCH-03's third derived criterion holds and is checked rather than assumed: nothing this plan adds reaches a provider or asks an `Allowed` gate. `git diff` for this plan shows no new call to `allowed_for`, no new `may_i`, and no new call into `src/service/`.
    - The summary states whether `docs/KEYBOARD_SHORTCUTS.md` changed. It changes only if the entry carries an accelerator that file lists; a context-menu entry with a mnemonic letter is not one.
    - `cargo test --all-targets` is green.
  </acceptance_criteria>
  <done>The second door is open: a saved search's conditions are edited from the tree and written once, atomically, under the same identifier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One group in the tree, however a search was made</name>
  <files>src/presentation/folder_tree.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/folder_tree.rs:590-606`, where the saved-search heading and its rows are built, and `THE_HEADING` at `src/application/saved_searches.rs:202`.
    - `WhichRow::SavedSearches` and `WhichRow::SavedSearch` at `folder_tree.rs:116-118`, and their stored spellings at `:155-156`.
    - `SavedSearch::announced` and `a_row_for` at `src/application/saved_searches.rs:327` and `:556-568`, which say what a row is every time, because a row that sounds like a folder and is not is the whole risk of putting these in the tree.
    - `02-CONTEXT.md` D-2-02 in full, including why grouping by which door made a search is not merely different but worse.
    - `guards/guards.toml:44-58` and `:167-176`.
  </read_first>
  <behavior>
    - A search whose questions were written by the search box and a search whose questions were written by the rule editor produce rows under the same heading, at the same depth, indistinguishable by anything the tree carries.
    - Editing a search through the rule editor does not move its row.
    - There is exactly one saved-search heading in the tree, whatever mixture of searches exists.
    - A search whose question set the In box has no name for is still announced as a saved search, in the same words as any other.
  </behavior>
  <action>
    Take RED by asserting over `folder_tree::rows` with a mixture of searches, against the current builder, and confirm each assertion is about rows rather than about a missing symbol. Some of these will be green on arrival, because there is only one group today; that is the point. The property is worth a test precisely because the next person to add a second door will be tempted to add a second group, and D-2-02 records that grouping by provenance breaks the moment a thing can be edited by the other door.

    Do not add a field recording which door made a search, and do not add one to the tree row. There is nothing in the stored shape that says which door made a search and there must not be, because a typed search opened in the rule editor and given a body condition would then have to move groups or sit in a group that no longer describes it.

    Write the tests as properties over `rows`, counting headings and comparing rows built from differently-shaped searches, so they fail if a second group is introduced later.

    Record one guard: one heading however a search was made. Take it red by hand by adding a second heading for searches whose question set the In box cannot name, measure which tests redden, and write exactly those into `red`. That break is the wrong fix somebody would reach for, which is what makes it the right break to record. Raise the second sweep number by one in the same edit.

    Add the changelog entry under `[Unreleased]` in this commit, describing the rule editor from a person's side: what they can now ask a saved search, where they open it, and that a search made either way is one thing in one place. Do not bump the version.
  </action>
  <verify>
    <automated>cargo test --lib presentation::folder_tree && cargo test --test folder_tree_rows_pair_with_the_control && cargo test --test house_style && bash scripts/guards.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib presentation::folder_tree` passes and includes all four behaviours as named tests.
    - A test counts the saved-search headings in `rows` output and asserts it is one, for a mixture of search shapes.
    - With comments filtered first, no field records which door made a search: `grep -v '^\s*//\|^\s*///' src/application/saved_searches.rs src/presentation/folder_tree.rs | grep -cE 'made_by|provenance|written_by_the'` returns 0. Comments are filtered because the rule is about a stored field, not about prose explaining why there is none, and an unfiltered count would be disarmed by its own explanation. <!-- planner-discipline-allow: provenance -->
    - `bash scripts/guards.sh` runs unfiltered and passes with the new record.
    - `grep -c '^\[\[guard\]\]' guards/guards.toml` equals the sum of the two sweep numbers.
    - `docs/changelog.md` has an `[Unreleased]` entry added in this commit, and `grep -n '^version' Cargo.toml` still reads 0.46.0.
  </acceptance_criteria>
  <done>One group holds every saved search, and a test fails if a second one is ever added.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Symbol | File | What it is |
|--------|------|------------|
| the condition noun | `src/presentation/manager_words.rs` | Beside the five that exist, so every sentence is built there |
| `populate_questions` and `show_rule_manager_dialog` | `src/presentation/wx_managers.rs` | The manager over one search's conditions, on `run_manager_loop` |
| the list noun parameter on `make_shell` | `src/presentation/wx_managers.rs` | So a manager's list says what it holds |
| the context menu entry | `src/application/context_menu.rs` | Edit conditions, on a saved-search row only |
| the open-and-write path | `src/presentation/wx_app.rs` | Opens the manager, writes once on close through the replace |
| the one-group property tests | `src/presentation/folder_tree.rs` | One heading however a search was made |
| one guard record | `guards/guards.toml` | The one-group rule |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the manager's working copy -> the stored search | User edits crossing into storage, once, on close |
| a search written by a newer version -> the editor | Stored words this build does not understand |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-29 | Repudiation | Editing a search this build cannot read, dropping the parts it did not understand | high | mitigate | The unreadable case is refused with a sentence, or the entry is absent for that row; `ChosenSearch`'s two shapes already make the difference visible |
| T-02-30 | Repudiation | Writing per change, so closing part-way leaves a half-edited search | high | mitigate | One write on close, through the atomic replace, from the working copy `run_manager_loop` keeps |
| T-02-31 | Spoofing | A list whose size a screen reader hears differently on the two channels | high | mitigate | Native `ListCtrl` in report mode, whose count comes from the system's own provider; no hand-rolled row stack and no `set_accessible_name` carrying a count |
| T-02-32 | Denial of Service | Row data hung off a control, leaking a registry entry per row | medium | mitigate | `run_manager_loop` keeps rows in a `Vec` beside the control; the acceptance criteria check that no item data is set, which is the defect `01-05` found in two other dialogs |
| T-02-33 | Information Disclosure | A condition list stored under the wrong search | medium | mitigate | The replace keeps the identifier and account, asserted by plan 02-06's tests, and the editor is opened from a row that names one search |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No dependency added; `Cargo.toml` unchanged |
</threat_model>

<verification>
- `cargo test --all-targets` is green.
- `bash scripts/guards.sh` unfiltered passes.
- A search saved from the search box, opened in the rule editor, given a condition about the message text and closed, runs with that condition and still sits in the same group.
- The deepest dialog stack reached is two, the same as the filter manager.
- No screen reader run is planned here, and none is claimed. What is claimed is that the list is native and that every control carries a name, which is what the tests can settle.
</verification>

<success_criteria>
- Roadmap criterion 3 is met: one stored thing with two doors, one matcher, and one group in the tree however a search was made.
- SEARCH-03 is met for the saved-search path as well as the filter path.
- Nothing records which door made a search.
</success_criteria>

<output>
Create `.planning/phases/02-search-that-says-what-it-covers/02-07-SUMMARY.md` when done.
</output>
