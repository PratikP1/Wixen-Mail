---
phase: 10-all-the-mail-and-what-is-said-while-it-comes
plan: 02.2
subsystem: the installer build script, the Windows file version, CI's Build job, the tests that read the script and the workflow, guards, the versioning rule
tags: [build-installer, version, file-version, build-metadata, ci, guards, changelog, found-16]

requires:
  - phase: 10-all-the-mail-and-what-is-said-while-it-comes
    provides: "10-02.1's tree at 36882a21: 885 records, census 802 + 83, ledger 516; the pattern of recording the checker's corrections in the plan file"
  - phase: 09-the-first-day-of-testing
    provides: "the version at 1.0.0-alpha.1 set at 01ff57bf, the four-field encoding in the script, tests/installer.rs reading the script's stage arms, the release workflow's as-is level"
provides:
  - "scripts/build-installer.sh: the commits since the version was set computed before the build identifier; BUILD as counter.gcommit after the plus; the fourth field as stage * 13000 + step * 1000 + counter with the step held to 12 and the counter to 999, each cap printing a loud line; a clone without the version's commit refused with two sentences and exit 1; no fallback to the word unknown anywhere"
  - "tests/installer.rs: the_fourth_field_rule reading the weights and caps off the script; windows_file_version taking a counter; the table test to the new rows; the ordering test over eleven builds in the order they are made and the largest field under 65535; the reader's companion; two compare companions"
  - "tests/house_style.rs: the installer test holding the refusal and VERSION_SET_AT= before BUILD=; the CI history reader widened to jobs that run the build script, with the companion's case"
  - ".github/workflows/ci.yml: the Build job's checkout with fetch-depth 0 and the reason"
  - "guards/guards.toml: three records on the script (the alpha arm re-measured at two, the counter dropped from the expression, the exit dropped from the refusal), the Test Suite job's record re-anchored, a record for the Build job's checkout; 888 records, census 802 + 86"
  - "CLAUDE.md and docs/changelog.md: the rule with the counter, the date and the reason, the old shape kept and dated; ledger 517"
affects: [10-07 (reads FOUND-16's [S] line in ledger 517 and the changelog entry), the next build handed to the tester, which will read 1.0.0-alpha.1+N.g<commit>, the release workflow, which checks out the history already and now shares the refusal]

actuals:
  tokens: 17900
  tasks: 2
  commits: 5

tech-stack:
  added: []
  patterns:
    - "A rule read off a script's own arithmetic by a test, weights and caps by value, with a companion shown the rule gone, rather than a second copy of the arithmetic"
    - "A build refused rather than made when the fact it exists to carry cannot be computed, with set -e as the second refusal path and no word standing in for a number"
    - "A guard record's text anchor checked against every edit to its file, because an addition that copies a good pattern can make a short anchor name two places"

key-files:
  created: []
  modified:
    - scripts/build-installer.sh
    - tests/installer.rs
    - tests/house_style.rs
    - .github/workflows/ci.yml
    - src/common/version.rs
    - guards/guards.toml
    - CLAUDE.md
    - docs/changelog.md
    - .planning/WINDOWS.md
    - .planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-02.2-PLAN.md

key-decisions:
  - "The stage weighs 13000, the step 1000, the step held to 12 and the counter to 999, largest value 64999: the brief's 10000 puts rc.11 above a release, which is the order the field is for"
  - "The count has no fallback: `|| echo unknown` dropped from the LAG line (the checker's correction) and from the commit line (unreachable once the refusal above it exists), so a failed count stops the script under set -e"
  - "The Test Suite job's record re-anchored on its comment's last line and a sibling written for the Build job, three new records rather than the plan's two, because the plan's own addition to ci.yml duplicated the bare anchor"
  - "One installer built after the merge on the orchestrator's instruction, against the plan's verification line, to read the string and the file version off the built file; handed to nobody"

patterns-established:
  - "When a plan edits a file, list the records whose `before` text lies in it, not only the records whose tests_last_seen or file name it"

requirements-completed: [FOUND-16]

coverage:
  - id: D1
    description: "The script computes the counter before the build identifier, composes counter.gcommit after the plus, and puts stage * 13000 + step * 1000 + counter in the fourth field with the step held to 12 and the counter to 999, each cap saying so on the console; a clone without the commit that set the version is refused with a sentence; nothing is appended at a tag while the file version keeps the counter"
    requirement: FOUND-16
    verification:
      - kind: integration
        ref: "tests/installer.rs#test_the_four_field_version_follows_the_scripts_own_table"
        status: pass
      - kind: integration
        ref: "tests/installer.rs#test_a_later_build_orders_above_an_earlier_one_and_below_the_next_stage"
        status: pass
      - kind: integration
        ref: "tests/installer.rs#test_the_first_alpha_of_1_0_0_sits_above_the_last_0_x_version_the_way_windows_orders_it"
        status: pass
      - kind: integration
        ref: "tests/installer.rs#test_the_reading_can_see_a_fourth_field_rule_that_is_gone"
        status: pass
      - kind: integration
        ref: "tests/house_style.rs#test_the_installer_says_how_far_back_the_version_was_set"
        status: pass
      - kind: other
        ref: "guards/guards.toml, three records with file = scripts/build-installer.sh, measured 2026-09-17: each reddens exactly the tests it names"
        status: pass
      - kind: command
        ref: "bash scripts/build-installer.sh on main at 44bff634, exit 0: dist/Wixen-Mail-Setup-1.0.0-alpha.1+114.g44bff634.exe, file version 1.0.0.14114 by (Get-Item ...).VersionInfo, and target/release/wixen-mail.exe --version saying Wixen Mail 1.0.0-alpha.1+114.g44bff634"
        status: pass
    human_judgment: false
  - id: D2
    description: "CI's Build job checks out the whole history, held by the same house_style reader that holds the Test Suite job; compare reads the new shape as the bare version, held by two companions; the rule in CLAUDE.md and the changelog's opening paragraph carry the counter with the date and the reason, the old shape kept and dated"
    requirement: FOUND-16
    verification:
      - kind: integration
        ref: "tests/house_style.rs#test_every_job_that_runs_the_tests_checks_out_the_whole_history"
        status: pass
      - kind: integration
        ref: "tests/house_style.rs#test_the_history_reading_can_see_a_job_with_one_commit"
        status: pass
      - kind: integration
        ref: "tests/installer.rs#test_a_build_with_a_counter_is_the_same_version_as_the_bare_number"
        status: pass
      - kind: integration
        ref: "tests/installer.rs#test_the_running_builds_shape_with_a_counter_still_reads"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'a CI job that runs the tests checks out the whole history' re-anchored and 'the CI job that builds the installer checks out the whole history', measured 2026-09-17: one red each"
        status: pass
      - kind: command
        ref: "grep -c '1.0.0-alpha.1+42.g59c5b6a4' CLAUDE.md answers 2, grep -c '0.5.0+g64c73dd' CLAUDE.md answers 1; the four document-reading targets pass"
        status: pass
    human_judgment: false
  - id: S1
    description: "Whether the next installer handed to the tester carries the counter in Apps and Features and installs over the alpha.1 build as an upgrade"
    requirement: FOUND-16
    verification: []
    human_judgment: true
    rationale: "FOUND-16's last [S] line, ledger 517; one installer was built and read, none has been installed over anything or handed to anybody"

duration: 1h 05m
completed: 2026-09-17
status: complete
---

# Phase 10 Plan 02.2: A build carries how many commits it is past its version Summary

**A build made by `scripts/build-installer.sh` now carries, after the plus, how many commits it
is past the commit that set its version and then the commit, and the same count ends the
Windows file version: from `main` at `44bff634` the script built
`dist/Wixen-Mail-Setup-1.0.0-alpha.1+114.g44bff634.exe` with file version `1.0.0.14114`, and the
exe inside it answers `--version` with `Wixen Mail 1.0.0-alpha.1+114.g44bff634`. The two builds
the tester had, `+g7d57cd49` and `+g59c5b6a4`, would read `+70` and `+88`. The counter is computed
before the build identifier, the fourth field is `stage * 13000 + step * 1000 + counter` with the
step held to 12 and the counter to 999 and a loud line when either bites, a clone that does not
hold the version's commit is refused rather than built, CI's Build job checks out the history so
it is not that clone, and the version stays `1.0.0-alpha.1` because the counter is build
metadata and `compare` reads `1.0.0-alpha.1+42.g59c5b6a4` as `1.0.0-alpha.1`. The rule is in
`CLAUDE.md` and the changelog with the date, the old shape kept and dated (Pratik's decision of
2026-09-17).** Nothing pushed; nothing handed to anybody.

## Performance

- **Duration:** 1 h 05 min from the branch at 19:28:55Z to the summary commit; of that, about
  17 minutes on the hook's runs over four commits (3 m 21 s on the red, 7 m 07 s on the green,
  which carried `ci.yml` and so ran `all`, 3 m 11 s and 3 m 10 s on the two document commits),
  6 m 53 s on the whole gate on the branch and 6 m 59 s on `main`'s hook at the merge, 1 m 34 s
  on the installer build, and 35 s on the ten guard measurements by their `timed:` lines
- **Started:** 2026-09-17T19:28:55Z (the branch; first commit 19:34Z)
- **Merged:** 2026-09-17T20:10:37Z at `44bff634`
- **Tasks:** 2
- **Files modified:** 10, the plan file among them

## What landed

**The script.** The block that finds `VERSION_SET_AT` moved above the tag test, keeping its
comment and gaining the sentence that says why it is first now. The `-n` branch with
`LAG="unknown"` in its else became `if [ -z "$VERSION_SET_AT" ]` printing two sentences to
standard error and `exit 1`, with a comment naming 2026-09-17 and the reason: a build that
cannot say its order is the one build nobody can place. `LAG=$(git rev-list --count
"$VERSION_SET_AT..HEAD")` with no `|| echo unknown` and no `2>/dev/null`, so a count that fails
stops the script under `set -e` with git's own reason. `BUILD="$LAG.g$commit"`, `.dirty` still
after the commit. `counter=$LAG`, the two caps as `if` blocks each printing three lines, and
`VERSION_INFO` with the new expression. The comment's table is the eight rows premise 8 gave,
with the reasoning: five stages under 65535 leave 13107 each, a thousand per step keeps a
counter under a thousand from reading as the next step, twelve steps keep a step from reading as
the next stage, largest value 64999, and 10000 for the stage would put `rc.11` above a release.
The `unknown)` arm of the `case "$LAG"` went. `find_iscc`, the two builds and the ISCC line are
as they were.

```
grep -n 'VERSION_SET_AT=\|BUILD="\$LAG.g\$commit"\|counter=\$LAG\|stage \* 13000 + step \* 1000 + counter\|"\$step" -gt 12\|"\$counter" -gt 999\|does not hold the commit that set' scripts/build-installer.sh
39:VERSION_SET_AT=$(git log --format=%H -S"version = \"$VERSION\"" -- Cargo.toml 2>/dev/null | head -1)
48:  echo "This clone does not hold the commit that set $VERSION, so the build cannot say how many commits it is past it." >&2
87:  BUILD="$LAG.g$commit"
145:counter=$LAG
146:if [ "$step" -gt 12 ]; then
152:if [ "$counter" -gt 999 ]; then
158:VERSION_INFO="${major:-0}.${minor:-0}.${patch:-0}.$((stage * 13000 + step * 1000 + counter))"
```

(taken at `906280dc`; `VERSION_SET_AT=` at 39 is below `BUILD=` at 87.) `grep -v '^\s*#'
scripts/build-installer.sh | grep -c 'LAG="unknown"\|unknown)\|echo unknown'` answers 0, after
the fallback on the `commit=` line went as well: a clone where HEAD cannot be named has already
been refused above it, so that fallback was unreachable, and the comment on the line says so.
`bash -n` exits 0. The file-version block run alone with `bash -c`, `VERSION=1.0.0-alpha.1
LAG=98`, prints `1.0.0.14098`; with `LAG=1500` the counter's cap speaks and it prints
`1.0.0.14999`; with `VERSION=1.0.0-rc.13 LAG=0` the step's cap speaks and it prints
`1.0.0.51000`; with `VERSION=1.0.0 LAG=3`, `1.0.0.52003`.

**The tests.** `the_fourth_field_rule` reads `$((stage * A + step * B + counter))` and the two
`[ "$name" -gt N ]` lines off the code half of the script into a `FourthField`, naming the first
part it cannot find; `windows_file_version` takes the table, the rule, a version and a counter
and does no arithmetic of its own beyond the rule's shape. The table test asserts the rule by
value, the two `BUILD` and `FULL_VERSION` lines, and eleven rows including both caps biting. The
ordering test walks eleven builds in the order they are made and requires each above the last,
then the largest field the rule can produce at most 65535. The first-alpha test goes through the
widened helper. The reader's companion is shown a snippet with the rule whole, with the counter
cap gone, with `+ counter` gone from the expression, and with the whole rule inside a comment.
The two compare companions hold `Same` for the new shape against the bare version and against
`+70.g7d57cd49`, `Older` against `1.0.0-alpha.2`, and the running build's shape with a counter
appended readable. In `house_style.rs`, the installer test reads the block from `if [ -z
"$VERSION_SET_AT" ]` to its `fi` for `exit 1` and the sentence, and the position of the line
starting `VERSION_SET_AT=` before the line starting `BUILD=` (by line start, since
`WIXEN_BUILD=` contains `BUILD=`); the history reader names a job that runs the build script
without `fetch-depth: 0` with the script's reason, and its companion plants one on one commit
and one over the whole history.

**CI and the doc comments.** The Build job's checkout carries `fetch-depth: 0` with a comment
naming the script, the date and the Test Suite job's reason. `version.rs`'s module doc and
`current`'s doc name `1.0.0-alpha.1+42.g59c5b6a4` and what the number before the `g` is, with
`0.5.0+g64c73dd` kept as the shape until 2026-09-17; nothing else there moved and its 25 tests
pass as before.

**The documents.** `CLAUDE.md`'s "Bump when the software changes" paragraph keeps its first
sentence and says what the script appends since 2026-09-17, where it appears, why after a plus,
that nothing is appended at a tag while the file version keeps the counter there; a second
paragraph gives the decision with the date, the two shapes declined and why, and the old shape
dated; a third gives the caps, the refusal and CI's checkout. The changelog's opening paragraph
carries the new shape with the old one dated; the entry under `[Unreleased]`, `### Changed` is
in the tester's terms with what was wrong, what changed, what the number is and is not, and
four limitations, one of them found by the build below. Ledger 517.

## The built installer, read back

By `bash scripts/build-installer.sh` on `main` at `44bff634`, working tree clean, 20:17:56Z to
20:19:30Z, output to a file and exit 0 read directly. The script printed `== 1.0.0-alpha.1 was
set 114 commits ago ==` (by `git rev-list --count 01ff57bf..HEAD` at that commit, 114, taken
2026-09-17) and `== setup executable, version 1.0.0-alpha.1+114.g44bff634, file version
1.0.0.14114 ==`.

| What was read | How | Answer |
|---|---|---|
| the built exe's own version | `./target/release/wixen-mail.exe --version` | `Wixen Mail 1.0.0-alpha.1+114.g44bff634` |
| the setup file's file version | `(Get-Item 'dist/Wixen-Mail-Setup-1.0.0-alpha.1+114.g44bff634.exe').VersionInfo` | `FileVersion 1.0.0.14114`, `FileVersionRaw 1.0.0.14114`, `ProductVersion 1.0.0-alpha.1+114.g44bff634` |
| the exe's own resource | `(Get-Item 'target/release/wixen-mail.exe').VersionInfo` | `FileVersion 1.0.0-alpha.1`, `FileVersionRaw 1.0.0.0` |

The third row is a finding, not a defect this plan made: `build.rs` stamps `wixen-mail.exe`
through `winresource` with the crate's version and never with the build, so the exe's own
Properties said `1.0.0.0` before this plan and say it after. The counter is on the setup file,
which is what Inno registers and what the tests and premise 6 were about, and in every string.
The changelog's Known limitations say so. The installer is in `dist/`, which is gitignored, and
was handed to nobody; no binary was started against anybody's profile, and `--version` exits
before the program opens anything.

## Task commits

| Commit | What |
|---|---|
| `f9390a3e` | test(10-02.2): failing tests for a build that carries an ordered counter; five named bare and the count check bare |
| `906280dc` | feat(10-02.2): the script, `ci.yml`, the doc comments, ten records measured or re-measured |
| `e07e4d3e` | docs(10-02.2): `CLAUDE.md`, the changelog, ledger 517 |
| `fc78fd62` | docs(10-02.2): the plan corrected where the checker and the tree found it wrong |
| `44bff634` | Merge 10-02.2 into `main` |

Branch `a-build-carries-an-ordered-counter` from `main` at `36882a21`. Not pushed; 135 commits
unpushed before the commit that lands this summary, by `git rev-list origin/main..HEAD --count`
on `main` at `44bff634`.

## Honest RED and GREEN

**The red** named the five tests bare and the count check bare, as the checker corrected the
plan to say, and the gate in `red` mode ran exactly those six red and nothing else. The run at
`36882a21` before the commit: `cargo test --test installer` 21 passed and 3 failed, the table,
ordering and first-alpha tests each on "no `$((stage * A + step * B + counter))` expression was
read off scripts/build-installer.sh"; `cargo test --test house_style` 71 passed and 3 failed,
the installer test on "no branch for not being able to work out when the version was set", the
history test naming "the installer job runs the installer build script on a checkout without
the history", and the count check naming the six records holding `installer.rs` at 20 where it
holds 24. The four companions were green on both trees as premises 5 and 7 said. **The first
attempt at the red commit was refused by clippy** for a `format!` with no arguments in the
reader's error closure; `.to_string()` and the second attempt went through.

**The green** was exactly green on its first run through the hook, `all` because it carried
`ci.yml`: 7,945 passed and none failed over 71 result lines, 7 m 07 s. Before it, `cargo test
--test installer` 24, `--test house_style` 74, `--lib common::version::` 25. One thing was red
on the green tree before the records were written and is described under the records:
`test_every_guard_record_still_names_one_place_in_the_tree`.

## What the gate selected

| File | On the branch |
|---|---|
| `tests/installer.rs`, `tests/house_style.rs` | their own targets on the red |
| `scripts/build-installer.sh` | `affected` with no scoped target; `installer` through its records; `house_style` as a whole-tree guard |
| `.github/workflows/ci.yml` | `all` on the green, through the hook |
| `src/common/version.rs` | `--lib common::version::` (25), inside the green's `all` |
| `guards/guards.toml`, `CLAUDE.md`, `docs/changelog.md`, `.planning/WINDOWS.md`, the plan file | no scoped target; the whole-tree guards on every commit, the document-reading targets on the two `docs_only` commits |

`scripts/check.sh all` on the branch at `fc78fd62`, output to a file and the exit status read
directly, never piped: exit 0, 7,945 passed and none failed over 71 result lines, 413 s from
20:03:24Z to 20:10:17Z, the release build included. Four more than 10-02.1's 7,941, the four
tests `installer.rs` gained. Inside the 275 s to 654 s band on `docs/development/measurements.md`.
`main`'s hook ran `all` again on the merge: 7,945 and none failed, 419 s. The keyring race
(ledger 374) did not appear on either.

## Guard records

885 records by the TOML reader before, 888 after; census 802 + 83 before, 802 + 86 after, the
line at `guards/guards.toml:84` moved in the green. Every measurement through `scripts/guards.sh
--remeasure` with `WIXEN_TEST_THREADS` untouched, the counts written by the runner.

| Record | File and suite | Break | Red | Run |
|---|---|---|---|---|
| the installer's alpha arm is the first stage (re-measured, red list widened by hand first) | the script, `installer` | alpha arm to 2 | 2, "all 2 tests named went red, and nothing else did" | rebuild 4 s, run 1 s |
| the build counter reaches the Windows file version (new) | the script, `installer` | `+ counter` dropped from the expression | 3 | rebuild 1 s, run 1 s |
| a clone without the commit that set the version is refused a build (new) | the script, `house_style` | the `exit 1` line dropped from the refusal | 1 | rebuild 1 s, run 3 s |
| a CI job that runs the tests checks out the whole history (re-anchored, re-measured) | `ci.yml`, `house_style` | the Test Suite job's depth to 1, anchored on its comment's last line | 1 | rebuild 1 s, run 3 s |
| the CI job that builds the installer checks out the whole history (new) | `ci.yml`, `house_style` | the Build job's depth to 1, anchored the same way | 1 | rebuild 1 s, run 3 s |
| the six `installer.rs` records the red commit named (the icon, the glob, the release trigger, the census, the alpha arm above, the as-is level) | `installer` | as before | 2, 2, 1, 1, 2, 1, each "and nothing else did" | rebuild 1 to 4 s, run 1 s each |

**The count check fired once, on the red commit, for the six `installer.rs` records at 20**, and
its remedy ran in the foreground and was read before the green: all six wrote 24 and their red
lists held. **The one-place check fired on the green tree**, which the plan did not foresee:
"a CI job that runs the tests checks out the whole history" anchored its break on the bare line
`        fetch-depth: 0`, and the Build job's checkout now carries the same line, so the record
named two places and could not be measured. It is re-anchored on the last line of the Test
Suite job's comment plus the three lines under it, and a sibling holds the Build job's checkout
the same way. `check.sh --suites-for guards/guards.toml scripts/build-installer.sh` prints
`installer`; it does not print `house_style`, which is a whole-tree guard the mapper never lists
(ledger 442). The count check and the one-place check are green on `main` at `44bff634`.

## Premises the tree contradicted

Every command in the plan's thirteen premises was re-run against `main` at `36882a21` before
anything was built. Twelve held: the script's lines, no tag, the release workflow's checkout,
`01ff57bf` as the commit that set the version with 70 and 88 for the tester's two builds,
`without_build` and the two existing tests, the seven readers of `version::current()`, the
`.iss` lines, the test lines, the 885 records with 6, 3 and 25 naming the three files, the
`+g` in `CLAUDE.md:1001` alone. What moved, each recorded in the plan file at `fc78fd62`:

1. **The checker's two corrections held.** `|| echo unknown` on the `LAG` line dropped so a
   failed count stops the script, and the acceptance grep's pattern gained `echo unknown`; premise
   8's 65999 is 55999 and the order is what breaks the brief's formula, which is what the
   script's comment says. And the count check named bare.
2. **The counter at the branch point is 109**, not 98, the eleven commits of 10-02.1; 114 at
   `44bff634` where the installer was built.
3. **A record the plan did not list went unmeasurable**, above, so three records are new and the
   census moved by three.
4. **`--suites-for` prints `installer` alone**, ledger 442.
5. **An installer was built**, against the plan's verification line and on the orchestrator's
   instruction, once, after the merge, and read; the plan file says so.
6. **The exe's own resource does not carry the counter**, found by reading the built file;
   premise 6 said `build.rs` does not change and it did not, and the changelog now says where
   the counter is and is not.

## Deviations from plan

**1. [Rule 3] The `commit=` line's `|| echo unknown` removed** with the `LAG` line's, since it
was unreachable behind the refusal and the acceptance grep would otherwise answer 1; a comment
on the line says why.

**2. [Rule 3] The `ci.yml` record re-anchored and a sibling written**, because the plan's own
edit to the workflow made the existing anchor name two places and the one-place check refused
the tree.

**3. [Decision] `2>/dev/null` dropped from the `rev-list` line** along with the fallback, so a
count that fails says why on the console rather than stopping silently.

**4. [Decision] The reader's companion gained a fourth case**, the whole rule inside a comment,
so a rule that moved into the script's prose is refused.

**5. [Process] One installer built after the merge**, on the orchestrator's instruction, to read
the string and the file version off the built file; the plan's verification said none would
be. Nothing was handed to anybody and nothing was published.

**6. [Decision] The changelog names where the counter is not**: the exe's own Properties, after
the build showed it.

Everything else executed as written. **No scripted edit touched a tracked file: the exception
set for this plan is zero, and it stayed there.** Every tracked file was changed by Read then
Edit or Write; the script was rewritten whole with Write after being read whole. The only
`sed`, `awk`, `grep`, `cut`, `head` and `tail` in the session read files and logs, and the
Python one-liners read `guards.toml` and counted. Commit messages were written to the
scratchpad and passed with `-F`; `cargo fmt` ran before each code commit. Carriage returns
measured with `tr -cd '\r' | wc -c` on every changed file before each commit: zero on each. No
em-dash in any file this plan wrote, measured with `grep -c` for the byte sequence: zero on
each; none of the six words, `the_words_that_say_nothing` 9 passed. `git commit` and `git
merge`, not `gsd-tools query commit`; never `--no-verify`; `check.sh` never piped. No AI
attribution in any commit. `Cargo.toml`, `Cargo.lock`, `build.rs`, `installer/Wixen-Mail-Setup.iss`
and `.github/workflows/release.yml` untouched, by `git diff --stat 36882a21..44bff634` naming
none of them; no crate, no feature. The version stays `1.0.0-alpha.1`. The tester's profile was
not read and no installed binary was started; `git tag` is empty.

## Threat register

T-10-37: mitigated, the weights and caps read off the script by value, the ordering test over
eleven builds with the largest field under 65535, the two loud lines shown biting by `bash -c`.
T-10-38: mitigated, the refusal held by `house_style` reading the block and its `exit 1`, the
Build job's checkout held by the widened reader and its record. T-10-39: mitigated, the two
companions hold `Same` and `Older` over the new shape and the existing record on `version.rs`
holds `without_build`, unchanged. T-10-40: accepted, one more job fetching the history, with
the Test Suite job's reason quoted in the comment. T-10-41: accepted, a count of commits since a
version was set in a file name and the IMAP ID. T-10-SC: nothing added. No new surface outside
the register.

## Ledger

`.planning/WINDOWS.md` 517 written by hand, both halves, no backslash;
`the_planning_files_agree_with_themselves` green after, 16 passed. 516 before, 517 after; 488
open before, 489 after.

| id | kind | what |
|---|---|---|
| 517 | unrun-verify | what only a build and a machine settle: the next installer handed to the tester carrying the counter in its file name and in Apps and Features, installing over the alpha.1 build he has as an upgrade, `--version` and the log's first line showing it; one installer built and read, none installed over anything, none handed to anybody |

## Known stubs

None. The counter is reached by every build the script makes, through `WIXEN_BUILD` into
`version::current()` and through `/DVersionInfo` into the setup file, both read back off the
built files above; the refusal by any clone without the history; the tests by the gate through
their records. No control, sentence or setting was added.

## For 10-07

- The next build handed to the tester will read `1.0.0-alpha.1+N.g<commit>` with N the commits
  since `01ff57bf`; the alpha page's line about what a build's name means can quote the shape
  from the changelog entry.
- FOUND-16's `[S]` line and ledger 517 close only when a build with the counter has been
  installed over the alpha.1 build on a machine and Apps and Features read; that is a check for
  the listening lines.

## Self-Check: PASSED

`scripts/build-installer.sh` holds `BUILD="$LAG.g$commit"` and the expression once each and no
`echo unknown` in its code half; `tests/installer.rs` holds `fn the_fourth_field_rule` and the
four new test names; `tests/house_style.rs` holds `does not hold the commit that set` and
`scripts/build-installer.sh` in the reader; `.github/workflows/ci.yml` has `fetch-depth: 0` under
the Build job's checkout at line 261; `guards/guards.toml` holds 888 records by the TOML reader
and the census line says 86; `CLAUDE.md` holds `1.0.0-alpha.1+42.g59c5b6a4` twice and
`0.5.0+g64c73dd` once; `docs/changelog.md` holds the line beginning "**A build you are given now
says how far along it is.**"; `.planning/WINDOWS.md` holds 517 in both halves;
`dist/Wixen-Mail-Setup-1.0.0-alpha.1+114.g44bff634.exe` exists. Commits `f9390a3e`, `906280dc`,
`e07e4d3e`, `fc78fd62` and `44bff634` are in `git log --oneline` on `main`.
