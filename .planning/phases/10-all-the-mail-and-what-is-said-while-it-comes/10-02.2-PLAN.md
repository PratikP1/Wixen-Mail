---
phase: 10-all-the-mail-and-what-is-said-while-it-comes
plan: 02.2
type: execute
wave: 5
depends_on: [10-02.1]
files_modified:
  - scripts/build-installer.sh
  - tests/installer.rs
  - tests/house_style.rs
  - .github/workflows/ci.yml
  - src/common/version.rs
  - guards/guards.toml
  - CLAUDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [FOUND-16]
estimate:
  tokens: 18000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A build handed to a tester carries, after the plus, how many commits it is past the commit that set its version, then the commit: 1.0.0-alpha.1+42.g59c5b6a4; two builds of one version can be put in order by reading them (FOUND-16, Pratik's decision of 2026-09-17)."
    - "The Windows file version's fourth field carries the same counter beneath the stage and the step, capped and monotonic, so Apps and Features orders the builds the way the counter does; a build at a tag omits the counter from the string and keeps it in the file version, so it sits between the builds before and after it."
    - "The version proper still moves only by CLAUDE.md's rule: the counter is build metadata, version ordering ignores it, and the update check compares 1.0.0-alpha.1+42.g59c5b6a4 as Same with 1.0.0-alpha.1."
    - "A clone that does not hold the commit that set the version is refused with a sentence rather than handed a build that cannot say its order; CI's Build job checks out the history so it is not that clone."
    - "The caps are said in the script and in the test that reads it, and a build that hits one prints a loud line rather than a quietly wrong number."
  artifacts:
    - "scripts/build-installer.sh: the counter computed before the build identifier, BUILD as counter.gcommit, VERSION_INFO as stage*13000 + step*1000 + counter with step held to 12 and counter to 999, the refusal when the version's commit is absent."
    - "tests/installer.rs: a reader of the fourth-field rule off the script, the table test held to it, the ordering test over builds in the order they are made, the two comparison companions."
    - "tests/house_style.rs: the installer test held to the refusal and to the counter being computed before the build identifier; the CI history reader widened to jobs that run the build script."
    - ".github/workflows/ci.yml: the Build job's checkout with fetch-depth 0 and the reason."
    - "guards/guards.toml: the alpha-arm record re-measured, two records added for the counter and the refusal."
    - "CLAUDE.md, docs/changelog.md: the rule with the counter, the date and the reason; the old shape kept and dated."
  key_links:
    - "VERSION_SET_AT -> LAG -> BUILD and VERSION_INFO: one count feeds both the string and the file version, computed before either."
    - "WIXEN_BUILD -> build.rs -> version::current(): the counter reaches --version, the log's first line, About, the IMAP ID and the update check unchanged."
    - "without_build -> compare: everything after the first plus is dropped, so the counter never reaches version ordering."
---

<objective>
Give every build handed to a tester an ordered number without moving the
version. Pratik decided on 2026-09-17 that builds keep `1.0.0-alpha.1` as the
round's name and gain a counter in the build metadata, the number of commits
since the version was set, with the same counter in the Windows file
version's last part so Apps and Features orders the builds:
`1.0.0-alpha.1+42.g59c5b6a4`. The alternatives he declined: a counter inside
the prerelease (`alpha.1.2`), which `version::parse` refuses, which
cargo-release's `alpha` level would count on from, and which is the
version-as-build-counter CLAUDE.md's rule warns against; and cutting every
tester build as a release.

Purpose: the tester has had two builds of `1.0.0-alpha.1`, `+g7d57cd49` and
`+g59c5b6a4`, and no number on either says which is later; a hash orders
nothing, and Apps and Features showed the same file version for both. The
counter is what the script already computes and prints as `LAG`, turned
into a fact the build carries.

Output: the script computing the counter first and stamping it into the
string and the file version with two caps said out loud, a refusal for a
clone without the history, CI's Build job checking out the history, the
tests in `tests/installer.rs` and `tests/house_style.rs` that hold each
part, three records, and the rule written where the old one was with the
date and the reason.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-17 against `main` at `c5ee5085`, version `1.0.0-alpha.1`,
working tree clean. Command first, then what it said.

**1. The script's lines at HEAD, against the brief's.**

```
cat -n scripts/build-installer.sh | sed -n 29,48p   -> the tag test at 29, BUILD="" at 30, commit at 32, .dirty at 36, BUILD="g$commit" at 38, FULL_VERSION at 41-45, export WIXEN_BUILD at 48
cat -n scripts/build-installer.sh | sed -n 67,75p   -> VERSION_SET_AT at 67, LAG at 69, LAG="unknown" at 74
cat -n scripts/build-installer.sh | sed -n 91,107p   -> the stage case at 92-100, step at 101-102, the cap at 106, VERSION_INFO at 107
cat -n scripts/build-installer.sh | sed -n 140,158p   -> the LAG case, unknown) at 141
cat -n scripts/build-installer.sh | sed -n 172,179p   -> the ISCC line at 176 with /DAppVersion and /DVersionInfo
```

The brief's `:28-45` is `:29-45`; `:67-69` and `:91-107` hold. The counter
is computed after the build identifier today, so the block at `:50-75`
moves above `:29`.

**2. No tag exists in this clone, so the tag branch has only ever run in the
release workflow, which checks out the history.**

```
git tag | wc -l   -> 0
git describe --exact-match --tags HEAD   -> fatal: No names found
grep -n 'fetch-depth\|build-installer' .github/workflows/release.yml   -> 92 (fetch-depth: 0 on the build job), 145 (bash scripts/build-installer.sh)
```

**3. CI's Build job checks out one commit and runs the script on every push,
so a refusal for a clone without the history reddens CI unless that
checkout changes in the same commit.**

```
sed -n 252,277p .github/workflows/ci.yml   -> actions/checkout@v4 with no depth, then bash scripts/build-installer.sh
sed -n 36,44p .github/workflows/ci.yml   -> the Test Suite job's checkout carries fetch-depth: 0 with its reason
sed -n 365,372p scripts/which-checks.sh   -> a change under .github/workflows/ answers all
sed -n 4631,4655p tests/house_style.rs   -> jobs_that_run_the_tests_without_the_history reads jobs running cargo test only; test_every_job_that_runs_the_tests_checks_out_the_whole_history holds ci.yml to it
```

Today that job prints "was set at a commit this clone does not have" and
builds. Under the refusal the brief prefers, it stops. So the Build job's
checkout gains `fetch-depth: 0` in the green commit, the reader in
`house_style.rs` is widened to jobs that run the build script, and the
commit that touches the workflow runs the whole gate.

**4. The counter today, and what the tester's two builds would have said.**

```
git log --format=%H -S'version = "1.0.0-alpha.1"' -- Cargo.toml | head -1   -> 01ff57bf2bbcfd9591de832a7029483fa7f35ad9 (feat(09-01), 2026-09-16)
git rev-list --count 01ff57bf..HEAD   -> 98
git rev-list --count 01ff57bf..7d57cd49   -> 70
git rev-list --count 01ff57bf..59c5b6a4   -> 88
```

So the two builds he has had would have been `1.0.0-alpha.1+70.g7d57cd49`
and `1.0.0-alpha.1+88.g59c5b6a4`, and a build from HEAD is `+98`. The
changelog entries that name the two builds keep the shape they carried.

**5. The comparison the brief asks a test for is already true, so the two
tests are companions and not red.**

```
sed -n 83,89p src/common/version.rs   -> without_build splits at the first + and drops what follows
sed -n 442,453p src/common/version.rs   -> test_a_build_identifier_after_a_plus_is_not_part_of_the_version, test_two_builds_of_one_version_are_the_same_version, over 0.5.0+g64c73dd
sed -n 172,178p src/common/version.rs   -> a prerelease is split once at a dot and whole_number refuses "1.2", so alpha.1.2 is refused as the brief says
```

`compare("1.0.0-alpha.1+42.g59c5b6a4", "1.0.0-alpha.1")` is `Same` at HEAD
and stays so. The two tests go in `tests/installer.rs` beside the four-field
tests rather than in `version.rs`: the file is about what the installer
stamps and how Windows and the update check read it, `compare` is public,
and `version.rs` is named by 3 records whose remedy is a library run each
where `installer.rs`'s 6 records carry `suite = installer` and re-measure in
seconds (premise 9). They are green on arrival and the red trailer does not
name them; what guards `without_build` is the existing record "a build
identifier is not part of the version", unchanged. `version.rs` changes only
in its doc comments, which name the old shape at `:9-12` and `:55`.

**6. The counter reaches every place the version is shown, through one
constant.**

```
sed -n 20,22p build.rs   -> WIXEN_BUILD read and passed as cargo:rustc-env
grep -rn 'version::current()' src --include='*.rs'   -> main.rs:37 (--version), main.rs:110 (the log's first line), wx_app.rs:22520 (About), imap.rs:1667 (the IMAP ID), update_check.rs:225-246, receipts.rs:274, platform_bridge.rs:361
grep -n 'AppVersion\|VersionInfoVersion' installer/Wixen-Mail-Setup.iss   -> 26 AppVersion={#AppVersion}, 27 AppVerName, 28 VersionInfoVersion={#VersionInfo}, 128 OutputBaseFilename
```

Nothing in `build.rs` or the `.iss` changes: the string arrives as it is
composed and the file version arrives as it is computed.

**7. The tests that read the script, and which assertions change.**

```
sed -n 1203,1228p tests/installer.rs   -> asserts the script contains "$((stage * 1000 + step))" and the 999 cap line, then seven table rows through windows_file_version
sed -n 1237,1261p tests/installer.rs   -> orders 1.0.0-alpha.1 above 0.125.1 through the same helper
sed -n 1263,1287p tests/installer.rs   -> the companion over a case block with and without the alpha arm
sed -n 4744,4775p tests/house_style.rs   -> asserts VERSION_SET_AT and LAG="unknown" or LAG=unknown in the code half
sed -n 3455,3459p tests/house_style.rs; sed -n 3300,3310p tests/house_style.rs   -> the version reader takes alphanumerics, dots and dashes after a + or -, so 1.0.0-alpha.1+42.g59c5b6a4 reads as 1.0.0-alpha.1
```

Red first, all rewritten in place under their names: the table test (the
expression, the two caps, the rows); the first-alpha test (through the
widened helper, which reads the rule off the script and cannot at HEAD);
the installer test in `house_style.rs` (the refusal in place of
`LAG=unknown`, and the counter computed before the build identifier). New:
the ordering test and the rule reader's companion in `installer.rs`. The
version reader at `:3271` stops at a `+` after a prerelease, so a page
naming `1.0.0-alpha.1+42.g59c5b6a4` reads as `1.0.0-alpha.1`, which is the
right verdict for the status-page guard it serves; it is not changed and no
test is added to `house_style.rs`.

**8. The file-version formula the brief offered breaks the order it is for,
so this one is chosen and said.** `stage * 10000 + step * 1000 + counter`
puts `rc.11` (41000) above a release (40000) and reaches 65999 at `rc.25`
with 999 commits, past the 65535 Windows allows. Monotonic across stage,
step and counter needs the stage weight above the largest step-and-counter
sum and five stages under 65535: `stage * 13000 + step * 1000 + counter`,
with the step held to 12 and the counter to 999, has a largest value of
`4 * 13000 + 12 * 1000 + 999 = 64999`, and every later build reads higher
than every earlier one: `1.0.0-alpha.1+70.g7d57cd49` is `1.0.0.14070`,
`+88` is `1.0.0.14088`, `1.0.0-alpha.2` set in its own commit is
`1.0.0.15000`, `1.0.0-beta.1` is `1.0.0.27000`, `1.0.0-rc.1` is
`1.0.0.40000`, `1.0.0` at its tag with nothing since is `1.0.0.52000`, and
`1.0.0+3.gabcdef12` is `1.0.0.52003`. A build at a tag omits the counter
from the string, as today, and keeps it in the file version, so a tagged
build sits above the tester builds made before it and below those after;
a counter of zero there would order the release below every build that
staged it, which Apps and Features would read as a downgrade.

**9. Records, by the TOML reader, and what the gate couples.**

```
python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(len(g));[print(f,sum(1 for r in g if any(e['file']==f for e in r.get('tests_last_seen',[]))), sorted({e['tests'] for r in g for e in r.get('tests_last_seen',[]) if e['file']==f})) for f in ['tests/installer.rs','src/common/version.rs','tests/house_style.rs','build.rs','scripts/build-installer.sh']]"
  -> 881; installer.rs 6 [20]; version.rs 3 [25]; house_style.rs 25 [74]; build.rs 0; the script 0 (it is a file, never a tests_last_seen entry)
python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];[print(r['name'],'|',r.get('file'),'|',r.get('suite'),'|',r.get('red')) for r in g if r.get('file')=='scripts/build-installer.sh']"
  -> "the installer's alpha arm is the first stage" | scripts/build-installer.sh | installer | [test_the_four_field_version_follows_the_scripts_own_table]
bash scripts/check.sh --suites-for guards/guards.toml scripts/build-installer.sh   -> installer
grep -l 'build-installer' scripts/*.test.sh   -> (nothing)
sed -n 375,387p scripts/which-checks.sh   -> a .sh under scripts answers affected, which maps to no scoped target
```

The 881 is before 10-02.1; that plan adds four, so this one starts at 885
and a census line of 83, re-taken before writing. No shell suite covers the
build script, and none is added: the script's behaviour is held by
`installer.rs` reading its lines and by `house_style.rs`, which is how it
has been held since the four-field encoding was written, and a suite that
ran the script would need Inno Setup and a release build. Adding tests to
`installer.rs` fires the count check for its 6 records; each is `suite =
installer`, so the remedy is a target run each, seconds. Rewriting in place
in `house_style.rs` fires nothing.

**10. Pages naming the old shape.**

```
grep -rn '+g' CLAUDE.md docs/*.md docs/**/*.md README.md .claude/skills/cutting-a-release/SKILL.md | grep -v '^docs/changelog.md'   -> CLAUDE.md:1001 only
grep -c '+g' docs/changelog.md   -> 19, one of them the opening paragraph at :5 stating the shape, eighteen naming builds testers had
grep -n -i 'version\|build' docs/ALPHA_TESTING.md | grep -c 'g[0-9a-f]\{7,8\}'   -> 0
```

`docs/ALPHA_TESTING.md` names no build shape and is not changed. The
eighteen changelog entries naming builds keep the shape those builds
carried. `src/service/update_download.rs:84` quotes a file name as a
measurement and `src/service/update_check.rs:1103` uses the old shape in a
test string; both stay, being facts about the day they were written.

**11. No version move.** As the phase's rule: no build has been cut, and
this plan changes what a build carries after the plus, which version
ordering ignores.

**12. Waves.** 10-02.1 writes `docs/changelog.md` and `guards/guards.toml`
and so does this plan, so the two are waves 4 and 5 rather than one wave;
the roadmap's phase 10 entry defines a wave as a set of plans sharing no
file. This plan depends on nothing 10-02.1 builds; the dependency is the
two files.

**13. The gate for each file.** `scripts/build-installer.sh`: `affected`
with no scoped target, plus `installer` through its record, plus
`house_style` as a whole-tree guard. `.github/workflows/ci.yml`: `all`.
`tests/installer.rs` and `tests/house_style.rs`: their own targets.
`src/common/version.rs`: `--lib common::version::` (25). `guards/guards.toml`,
`CLAUDE.md`, `docs/changelog.md`, `.planning/WINDOWS.md`: no scoped target;
the whole-tree guards on every commit, the document-reading targets on a
`docs_only` commit.
</premise_corrections>

<tasks>

<task type="tdd">
  <name>Task 1: the build carries an ordered counter in its string and its file version, and refuses a clone that cannot count (FOUND-16)</name>
  <files>tests/installer.rs, tests/house_style.rs, scripts/build-installer.sh, .github/workflows/ci.yml, src/common/version.rs, guards/guards.toml</files>
  <read_first>
    - `scripts/build-installer.sh`, all of it.
    - `tests/installer.rs:1100-1290` (the four-field section: `the_installer_build_script`, `StageTable`, `stage_arm`, `the_stage_table`, `windows_file_version`, the three tests), `:1-60` (the header and what the file holds), and `:1029-1068` for the shape of a test that reads a workflow.
    - `tests/house_style.rs:4348-4353` (`what_it_does_not_what_it_says`), `:4595-4690` (`the_jobs_of`, `jobs_that_run_the_tests_without_the_history`, its test and companion), `:4744-4775` (the installer test).
    - `.github/workflows/ci.yml:30-50` (the Test Suite job's checkout with its reason) and `:245-283` (the Build job).
    - `src/common/version.rs:1-90` (the module doc, `describe`, `without_build`) and `:150-223` (`parse`, `compare`).
    - `guards/guards.toml`, the record "the installer's alpha arm is the first stage", for the shape of a record with `suite = "installer"` and a break in a shell script.
    - `CLAUDE.md`, "Committing the red half" and "Versioning and releases".
  </read_first>
  <behavior>
    - `the_fourth_field_rule(script) -> Result<FourthField, String>`: reads, off the code half of the script, the expression `$((stage * A + step * B + counter))` into `stage_weight` and `step_weight`, and the two cap lines `[ "$step" -gt N ]` and `[ "$counter" -gt N ]` into `step_cap` and `counter_cap`, naming the first thing it cannot find; `windows_file_version(table, rule, version, counter) -> [u64; 4]` computes the fourth field from what was read, the step and the counter each held to its cap, no arithmetic of its own beyond the rule's shape.
    - `test_the_four_field_version_follows_the_scripts_own_table` (rewritten under its name): the rule read off the script is stage weight 13000, step weight 1000, step cap 12, counter cap 999; the script's code half composes `BUILD="$LAG.g$commit"` and `FULL_VERSION="$VERSION+$BUILD"`; and the rows: `("0.5.0", 0)` is `[0, 5, 0, 52000]`, `("1.0.0-alpha.1", 70)` is `[1, 0, 0, 14070]`, `("1.0.0-alpha.1", 88)` is `[1, 0, 0, 14088]`, `("1.0.0-alpha.2", 0)` is `[1, 0, 0, 15000]`, `("1.0.0-beta.1", 0)` is `[1, 0, 0, 27000]`, `("1.0.0-rc.1", 0)` is `[1, 0, 0, 40000]`, `("1.0.0", 0)` is `[1, 0, 0, 52000]`, `("1.0.0", 3)` is `[1, 0, 0, 52003]`, `("0.125.1", 0)` is `[0, 125, 1, 52000]`, `("1.0.0-alpha.1", 1500)` is `[1, 0, 0, 14999]` (the counter held), `("1.0.0-alpha.13", 0)` is `[1, 0, 0, 25000]` (the step held). Red at HEAD: the rule cannot be read.
    - `test_a_later_build_orders_above_an_earlier_one_and_below_the_next_stage` (new): the sequence `("1.0.0-alpha.1", 0)`, `("1.0.0-alpha.1", 70)`, `("1.0.0-alpha.1", 88)`, `("1.0.0-alpha.1", 999)`, `("1.0.0-alpha.2", 0)`, `("1.0.0-alpha.12", 999)`, `("1.0.0-beta.1", 0)`, `("1.0.0-rc.1", 0)`, `("1.0.0", 0)`, `("1.0.0", 3)`, `("1.0.1-alpha.1", 0)` is strictly increasing under the derived order on `[u64; 4]`, and the largest fourth field the rule can produce, stage 4 with the step and counter at their caps, is at most 65535. Red at HEAD.
    - `test_the_first_alpha_of_1_0_0_sits_above_the_last_0_x_version_the_way_windows_orders_it` (rewritten under its name): through the widened helper with a counter of 0 on both sides, as before. Red at HEAD, because the rule cannot be read.
    - `test_the_reading_can_see_a_fourth_field_rule_that_is_gone` (new companion): over a snippet holding the expression and both caps the reader answers the four numbers; with the counter cap line removed it names `counter`; with `+ counter` removed from the expression it names the expression. Green on both trees.
    - `test_a_build_with_a_counter_is_the_same_version_as_the_bare_number` and `test_the_running_builds_shape_with_a_counter_still_reads` (new companions, in `installer.rs`): `compare("1.0.0-alpha.1+42.g59c5b6a4", "1.0.0-alpha.1")` is `Same`, so is the comparison with `1.0.0-alpha.1+70.g7d57cd49`, and against `1.0.0-alpha.2` it is `Older`; and `compare(&format!("{}+42.g59c5b6a4", version::current()), &version::current())` is `Same` and not `CouldNotRead`. Green on both trees (premise 5).
    - `test_the_installer_says_how_far_back_the_version_was_set` in `house_style.rs` (rewritten under its name): the code half contains `VERSION_SET_AT` as before; the block from `if [ -z "$VERSION_SET_AT" ]` to its `fi` contains `exit 1` and the sentence `does not hold the commit that set`; and the position of `VERSION_SET_AT=` is before the position of `BUILD=`, because the counter is part of the build identifier and must exist first. Red at HEAD on the second and third.
    - `test_every_job_that_runs_the_tests_checks_out_the_whole_history` (its reader widened, name kept): a job whose text contains `scripts/build-installer.sh` and no `fetch-depth: 0` is named with a sentence saying the build script counts the commits since the version was set and refuses a checkout without them. Red at HEAD, where the Build job checks out one commit. Its companion `test_the_history_reading_can_see_a_job_with_one_commit` gains a case: a job running the build script over one commit is named and over the whole history is not. Green in the red commit, being test code.
  </behavior>
  <action>
    Red first, on a branch from `main`, with the exception set for scripted
    edits at zero: every file is changed by Read then Edit or Write, and the
    summary says so. The red commit holds every test change above and no
    script change; its trailer names bare, as `scripts/red-commit.sh` reads an
    integration test: `test_the_four_field_version_follows_the_scripts_own_table`,
    `test_a_later_build_orders_above_an_earlier_one_and_below_the_next_stage`,
    `test_the_first_alpha_of_1_0_0_sits_above_the_last_0_x_version_the_way_windows_orders_it`,
    `test_the_installer_says_how_far_back_the_version_was_set` and
    `test_every_job_that_runs_the_tests_checks_out_the_whole_history`, and
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held` by
    its module path, because `installer.rs` gains four tests and six records
    hold it at 20. The message says the four companions are green on both
    trees and why (premises 5 and 7). Run `cargo test --test installer` and
    `cargo test --test house_style` before committing and quote the failures.

    Then the green, in `scripts/build-installer.sh`, and the summary quotes
    the diff. Move the block that computes `VERSION_SET_AT` above the tag
    test, keeping its comment, and replace the branch that sets `LAG` to the
    word unknown with a refusal: when `VERSION_SET_AT` is empty, two lines to
    standard error,
    "This clone does not hold the commit that set $VERSION, so the build
    cannot say how many commits it is past it." and "A shallow clone has no
    history to search; fetch the whole history and build again.", then
    `exit 1`; the comment above says why a build that cannot say its order is
    refused rather than built (it is what the counter exists to prevent) and
    names 2026-09-17. `LAG` stays the name of the count. In the build block,
    `BUILD="$LAG.g$commit"` with `.dirty` still after the commit, and the
    comment gains the counter: the number of commits since the version was
    set, so two builds of one version can be put in order by reading them,
    and the same number goes into the file version below; nothing is
    appended at a tag, as before, and the file version carries the counter
    there too (premise 8's reason). In the file-version block, `counter=$LAG`,
    the two caps as `if` blocks each printing a loud line naming the number
    and the cap ("== the step is $step and the file version holds it to 12
    ==", "== the build counter is $LAG and the file version holds it to 999
    ==", each followed by a line saying Apps and Features cannot order this
    build against another past the cap and the version string still carries
    the true number), and `VERSION_INFO="${major:-0}.${minor:-0}.${patch:-0}.$((stage * 13000 + step * 1000 + counter))"`.
    Rewrite the comment's table to the rows premise 8 gives, and its
    reasoning: five stages under 65535 leave 13107 per stage, the step weight
    is a thousand so a counter under a thousand never reads as the next
    step, and the caps are where the arithmetic would otherwise lie. The arm
    of the `case "$LAG"` block for the word unknown goes, since the refusal
    above it means the count is always a number there; the other arms stay.
    Nothing else in the script moves; `find_iscc`, the builds and the ISCC
    line are as they were.

    In `.github/workflows/ci.yml`, the Build job's checkout gains `with:
    fetch-depth: 0` and a comment above it: the build script counts the
    commits since the version was set, refuses a checkout without them since
    2026-09-17, and the Test Suite job's checkout says why the history is
    cheap enough. In `src/common/version.rs`, the module doc's example at
    `:9-12` and `current`'s doc at `:55` name the new shape,
    `1.0.0-alpha.1+42.g59c5b6a4`, and say the number before the `g` is how
    many commits the build is past the commit that set its version, so two
    builds of one version can be put in order; nothing else there changes.

    Run `cargo test --test installer`, `cargo test --test house_style` and
    `cargo test --lib common::version::` before the green commit. The commit
    that carries `ci.yml` runs the whole gate through the hook, `all`,
    5 to 11 minutes by 10-01.1's and 10-02's runs; write its output to a
    file and read the exit status directly.

    The records, in the green commit. Re-measure "the installer's alpha arm
    is the first stage" through `scripts/guards.sh --remeasure`, because its
    named test was rewritten and the new ordering test reads the same arms;
    write what the runner wrote. Two new records, each `file =
    "scripts/build-installer.sh"`: one with `suite = "installer"`, the break
    being `+ counter` dropped from the expression, `red` what the runner
    says (the table test and the ordering test are the prediction); one with
    `suite = "house_style"`, the break being the `exit 1` line dropped from
    the refusal, `red` the installer test. Measure both, quote each `timed:`
    line, write `tests_last_seen` from the runner (`installer.rs` 24 after
    the four, `house_style.rs` 74). Run the count check's remedy the red
    commit named for the six `installer.rs` records and write their counts.
    Census line moved by two from what it says on the branch (83 after
    10-02.1 to 85). Confirm `bash scripts/check.sh --suites-for
    guards/guards.toml scripts/build-installer.sh` prints `installer` and
    `house_style`.
  </action>
  <verify>
    <automated>cargo test --test installer && cargo test --test house_style && cargo test --lib common::version:: && bash scripts/check.sh --suites-for guards/guards.toml scripts/build-installer.sh</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the five tests bare and the count check by module path in its trailer, and its body quotes the failures from a run.
    - After the green: `grep -n 'VERSION_SET_AT=\|BUILD="\$LAG.g\$commit"\|counter=\$LAG\|stage \* 13000 + step \* 1000 + counter\|"\$step" -gt 12\|"\$counter" -gt 999\|does not hold the commit that set' scripts/build-installer.sh` shows each once, with `VERSION_SET_AT=` on a lower line number than `BUILD=`, quoted in the summary; `grep -v '^\s*#' scripts/build-installer.sh | grep -c 'LAG="unknown"\|unknown)'` is 0, the code half and not the comments.
    - `bash -n scripts/build-installer.sh` exits 0; running the script's first eighty lines is not possible without Inno Setup, so the summary quotes instead the `VERSION_INFO` a `bash -c` of the file-version block prints for `VERSION=1.0.0-alpha.1` and `LAG=98`: `1.0.0.14098`.
    - `sed -n 252,262p .github/workflows/ci.yml` shows `fetch-depth: 0` under the Build job's checkout.
    - The four commands in `<automated>` pass; `cargo test --test installer` reports 24 passed; the last prints `installer` and `house_style`.
    - Three records measured (one re-measured, two new), each `timed:` line quoted, `red` as the runner wrote it; the six `installer.rs` records carry 24; the census line is two above what it said on the branch; `tr -cd '\r' | wc -c` is 0 on every changed file; `Cargo.toml`, `Cargo.lock`, `build.rs` and `installer/Wixen-Mail-Setup.iss` unchanged by `git diff --stat main..HEAD`.
  </acceptance_criteria>
  <done>A build made by the script carries how many commits it is past its version and which commit it is, in the file name, in Apps and Features, in `--version` and in the log, with the file version ordering builds the same way under two caps that speak when they bite; a clone that cannot count is refused; and every part is held by a test that reads the script.</done>
</task>

<task type="auto">
  <name>Task 2: the rule with the counter, written where the old rule was, with the date and the reason</name>
  <files>CLAUDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `CLAUDE.md`, "Versioning and releases", all of it, and especially the paragraph beginning "**Bump when the software changes, not when a build changes hands.**".
    - `docs/changelog.md:1-30`, the opening paragraph and the `[Unreleased]` head with its `### Changed` entry about the version, for the shape of a dated correction and of an entry under Changed.
    - `.planning/WINDOWS.md:1-8` and the rows 10-02.1 wrote, in both halves.
    - `CLAUDE.md`, "Documentation and writing", and `tests/house_style.rs`'s rules the document-reading targets apply (no em dash, none of the six words).
  </read_first>
  <action>
    Documents only, no test first: this task changes no behaviour, which is
    the exception CLAUDE.md lists, and the document-reading targets run on
    the commit. Invoke `writing-craft`.

    In `CLAUDE.md`, the paragraph "Bump when the software changes, not when
    a build changes hands" keeps its first sentence and says what the script
    appends now: since 2026-09-17, how many commits the build is past the
    commit that set its version and then the commit, as
    `1.0.0-alpha.1+42.g59c5b6a4`, in the file name, in Apps and Features, in
    `--version` and in the first line of the log; the same counter in the
    Windows file version's last part under the stage and the step, so Apps
    and Features orders the builds; after a `+` because that is build
    metadata, which version ordering ignores, so the counter moves nothing
    in the version and the update check reads `+42.g59c5b6a4` as the bare
    version. Then the reason, dated: Pratik decided on 2026-09-17, holding
    two builds of `1.0.0-alpha.1` that no number could put in order, that
    the round's name stays and the order goes after the plus; and the two
    shapes declined, a counter inside the prerelease (`alpha.1.2`, which
    `version::parse` refuses, which cargo-release's `alpha` level would count
    on from, and which is the version-as-build-counter the rule above warns
    against) and a release per tester build. Keep the old sentence visible
    and dated: until 2026-09-17 the script appended the commit alone,
    `0.5.0+g64c73dd`, and two builds of one version were tellable apart and
    not orderable. Say the two caps, 12 steps and 999 commits, and that a
    build past either says so on the console and keeps the true number in
    the string. Say that a clone without the commit that set the version is
    refused, that CI's Build job checks out the history for that reason, and
    that nothing is appended at a tag as before while the file version
    keeps the counter there. Keep "See `src/common/version.rs`".

    In `docs/changelog.md`, the opening paragraph's sentence naming
    `1.0.0-alpha.1+g64c73dd` becomes the new shape with the same words about
    build metadata, and the old shape is kept with its date the way the
    paragraph already keeps the `0.x.y` rule with its date. Under
    `[Unreleased]`, `### Changed`, after the version entry, a new entry: a
    bold first sentence in the tester's terms (a build you are given now
    says how far along it is: `1.0.0-alpha.1+88.g59c5b6a4` is the
    eighty-eighth commit past the point the version was set, and Apps and
    Features puts a later build above an earlier one), then what was wrong
    (the builds of 2026-09-17 read `1.0.0-alpha.1+g7d57cd49` and
    `1.0.0-alpha.1+g59c5b6a4`, and no number on either said which was
    later; in Apps and Features both showed the same file version), what
    changed and since when, what the number is and is not (commits since
    the version was set; not a build number anybody assigns, so two builds
    from one commit share it and the commit tells them apart), and Known
    limitations: the file version holds the counter to 999 and the step to
    12, a build past either says so when it is built; a build from a clone
    without the full history is refused; no build with the counter has been
    handed to anybody yet. Nothing under Fixed.

    One ledger entry by hand in `.planning/WINDOWS.md`, both halves, no
    backslash, `open_count` and `total_count` moved: kind `unrun-verify`,
    file `scripts/build-installer.sh`, for what only a build and a machine
    settle: that the next installer handed to the tester carries the counter
    in its file name and in Apps and Features, that installing it over the
    alpha.1 build he has is read by Windows as an upgrade, and that
    `--version` and the log's first line show it; nothing has built an
    installer since the script changed.
    `test_both_halves_of_the_ledger_say_the_same_thing` holds the entry.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves && cargo test --test every_number_carries_its_command_and_its_date</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '1.0.0-alpha.1+42.g59c5b6a4' CLAUDE.md` is at least 1 and `grep -c '0.5.0+g64c73dd' CLAUDE.md` is at least 1, the old shape kept and dated; `grep -n '2026-09-17' CLAUDE.md` shows the versioning paragraph among its lines.
    - `sed -n 5p docs/changelog.md` names the new shape and keeps the old one dated; `grep -c 'Apps and Features puts a later build above an earlier one' docs/changelog.md` is 1, under `### Changed` in `[Unreleased]`.
    - One ledger entry in both halves with the counts moved; the four targets in `<automated>` pass; `tr -cd '\r' | wc -c` is 0 on every changed file; no em dash and none of the six words, measured with `grep -c` for the byte sequence.
  </acceptance_criteria>
  <done>The rule a person reads in CLAUDE.md and in the changelog is the rule the script runs, dated, with what it replaced and why, and what only the next build settles is in the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a git history to a version number | the counter is computed from the clone's history; a clone without it cannot compute the number a build carries |
| a version string to Windows' four fields | three numbers and a stage, a step and a counter have to fit sixteen bits each, in an order Windows compares numerically |
| a build string to the update check | everything after the plus must be dropped before versions are compared, or a tester build would read as newer than its release |
| a workflow file to CI | a checkout depth decides whether the script can count; the Build job runs the script on every push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-37 | Tampering | `VERSION_INFO` overflowing or reordering the four fields | high | mitigate | stage 13000, step 1000, caps 12 and 999, largest value 64999; the ordering test over builds in the order they are made and the largest value under 65535; loud lines when a cap bites |
| T-10-38 | Repudiation | a build that cannot say its order | medium | mitigate | refused with a sentence when the commit that set the version is absent; CI's Build job checks out the history; both held by `house_style` reading the script and the workflow |
| T-10-39 | Tampering | the counter reaching version ordering | medium | mitigate | `without_build` drops everything after the first plus, unchanged; the two companions in `installer.rs` hold `Same` and `Older` over the new shape; the existing record on `version.rs` holds `without_build` |
| T-10-40 | Denial of service | CI's Build job fetching the whole history on every push | low | accept | the Test Suite job has done so since 2026-09-15 for the fraction test and says why; the same clone shape on one more job |
| T-10-41 | Information disclosure | a commit count in a file name and in the IMAP ID | low | accept | the hash was already there; a count of commits since a version was set says nothing a public repository does not |
| T-10-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate and no feature is added; `Cargo.toml` and `Cargo.lock` unchanged, quoted in the summary |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass on the branch; `scripts/check.sh` passes on every commit through the hook, never piped, never `--no-verify`; the commit carrying `ci.yml` runs `all` through the hook; `scripts/check.sh all` once on the branch before the merge, its output to a file and the exit status read directly; the merge onto `main` runs it again.
- Three records measured on the branch through `scripts/guards.sh --remeasure`, and the six `installer.rs` records re-measured when the count check names them; `WIXEN_TEST_THREADS` untouched; every runner line quoted.
- No installer is built and no release is dispatched: the script needs Inno Setup and a release build, and building a file to hand to somebody is Pratik's (guardrail 7). What the script would print is shown by running its version block alone with `bash -c`, quoted in the summary. `git tag` stays empty.
- `Cargo.toml`, `Cargo.lock`, `build.rs`, `installer/Wixen-Mail-Setup.iss` and `.github/workflows/release.yml` unchanged: `git diff --stat main..HEAD` on the branch names none of them. The version stays `1.0.0-alpha.1`.
- No issue is closed: the decision was Pratik's in conversation on 2026-09-17 and is recorded in `CLAUDE.md` and the changelog with the date. Filing an issue is not the executor's.
- In the summary commit, on `main` after the merge: tick FOUND-16 in `.planning/REQUIREMENTS.md` with a "Closed 2026-09-17 by 10-02.2, merged at `<merge commit>`" line naming the tests that hold each `[D]` line, and move its traceability row from Pending to Complete; the roadmap's phase 10 progress row to 5/10 with 10-02.2's one-paragraph note; `STATE.md` by hand, `completed_plans` counted from the `*-SUMMARY.md` files on disk. This plan ticks its own requirement because 10-07's closing read is about the mail.
</verification>

<success_criteria>
- Roadmap criterion 10 closes structurally: the counter in the string and the file version, the caps, the refusal, CI's checkout, the tests and records, the rule written with its date.
- FOUND-16's `[D]` lines each have a named test in the summary; its `[S]` line is untouched until a build is handed to the tester.
- The next installer built by the script is `1.0.0-alpha.1+N.g<commit>` with N the commits since `01ff57bf`, and Apps and Features orders it above the alpha.1 build the tester has.
</success_criteria>

<output>
Create `.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-02.2-SUMMARY.md` when done
</output>
