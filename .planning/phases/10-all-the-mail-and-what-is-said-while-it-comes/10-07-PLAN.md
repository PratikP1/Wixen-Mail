---
phase: 10-all-the-mail-and-what-is-said-while-it-comes
plan: 07
type: execute
wave: 7
depends_on: [10-02, 10-03, 10-04, 10-05, 10-06]
files_modified:
  - docs/ALPHA_TESTING.md
  - docs/privacy.md
  - docs/USER_GUIDE.md
  - docs/manual-accessibility-pass.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [MAIL-01, MAIL-02, MAIL-03, MAIL-04, MAIL-05]
estimate:
  tokens: 18000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The four pages a person reads before trusting this program say what it does now: mail comes down whole and on its own, the text with it unless forbidden or bounded, mail keeps arriving, how much is said is a choice, and none of it has met a real provider except through one account; the privacy page says what a full download tells a provider and puts on the disk."
    - "The five issues are read against the tree clause by clause, each requirement ticked only where every [D] line has a name, the [S] lines left to the tester, and the roadmap, the requirements, the state and the ledger say the same thing."
  artifacts:
    - "docs/ALPHA_TESTING.md, docs/privacy.md, docs/USER_GUIDE.md: the sentences this phase made false corrected by dating, and the new ones."
    - "docs/manual-accessibility-pass.md: the listening lines this phase owes beyond 10-04's two."
    - ".planning/REQUIREMENTS.md: MAIL-01 to MAIL-05 read and ticked or left open with the reason; .planning/ROADMAP.md: the phase's criteria and row; .planning/STATE.md; .planning/WINDOWS.md: the entries for what only a person can settle, both halves."
  key_links:
    - "Each requirement's [D] line -> a test or reading named in a summary of this phase: the closing read is a table of names, not a re-run."
    - "The privacy page's table row for the mail provider -> 10-05's runner: the sentence says what the download asks for."
---

<objective>
Say in the documents what this phase changed, then read the five issues
against the tree and close the phase the way 09-10 closed phase 9.

Purpose: a page that describes the program before this phase is a page that
says only 500 messages come down, that text is fetched on opening, and that
new mail stops arriving after a drop, none of which is true after 10-05 and
10-06. #29 will want the privacy page to list every way a reader of mail can
be tracked, and a client that asks for the whole mailbox is one line of that
list this phase can write now.

Output: four pages corrected by dating, the listening lines, the closing
read in the summary and in the four planning files, and the ledger entries
for what the tester's account alone can settle.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/README.md
@.planning/phases/09-what-the-first-day-of-testing-found/09-10-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-17 against `main` at `7d57cd49`, before any plan of this phase
landed; the executor re-takes every line below against the tree the six
plans left. Command first, then what it said.

**1. Which pages describe fetching, and what they say.**

```
grep -n -i '500\|older messages\|whole folder\|message text\|watch\|automatic' docs/ALPHA_TESTING.md docs/USER_GUIDE.md docs/privacy.md README.md | cut -c1-120
  -> USER_GUIDE.md:82 "waiting for the next automatic sync" (about the three module syncs, which have no schedule either); nothing on the three pages names 500, Get Older Messages or the whole-folder command; KEYBOARD_SHORTCUTS.md:555 named all three and 10-05 rewrote it
sed -n 144,160p docs/privacy.md   -> the table "Who Wixen Mail talks to": "Your mail provider | Checking, reading, sending | The mail itself, over TLS"
sed -n 123,135p docs/ALPHA_TESTING.md   -> "What is already known to be missing or unproven", opening with "Nothing that writes has run against a real account"
```

So the pages under-describe rather than mis-describe: none promises 500 and
none says mail comes down whole. What they lack is any sentence about what
a check brings down, how much is said, and what keeps mail arriving. The
alpha testing page's list gains the download and the watch as things that
have met one account and no other; the privacy table's mail-provider row
gains what the download asks for; the user guide's "Reading and Managing
Email" gains a short section. The executor re-runs the grep after 10-02 to
10-06 and corrects by dating anything they left.

**2. The listening lines this phase owes.**

```
grep -n -i 'sync\|status\|announce\|new mail' docs/manual-accessibility-pass.md | head -12
```

10-04 wrote two. This plan reads the six summaries for every sentence a
person will hear that nobody has heard (the download's progress and result
sentences, the wait sentence, the status line's three states, the Pause
item's two sentences, the F9 line under several accounts) and adds one line
each in the page's shape, with its source and technology.

**3. The requirement ticks and the ledger follow 09-10's rule.**

```
sed -n 332,420p .planning/phases/09-what-the-first-day-of-testing-found/09-10-SUMMARY.md   -> the closing read: a table per requirement, the plan and merge, the [D] lines each with a name, the [S] line left; the criteria table; the progress row
grep -n 'MAIL-0' .planning/REQUIREMENTS.md | head   -> the five, unticked, with their [D] and [S] lines as the planner wrote them on 2026-09-17
```

A requirement is ticked only when every `[D]` line has a name in a summary
of this phase; one whose last `[D]` line names a run nobody has made stays
open with the reason. The ledger entries are written by hand, both halves,
with no backslash in a description, because `gsd-tools windows append`
corrupts one and has no edit.

**4. The gate for each file.** Every file here is a document or a planning
file: `docs/*.md` map to the document-reading targets on a documents-only
commit (`house_style`, `docs_links`, `wired`, `the_words_that_say_nothing`,
`every_number_carries_its_command_and_its_date` among the nine), `.planning/*.md`
to `the_planning_files_agree_with_themselves`, `house_style` and
`the_words_that_say_nothing`. The commit answers `docs_only` and the summary
quotes the mode the hook printed.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: the four pages say what the program does now</name>
  <files>docs/ALPHA_TESTING.md, docs/privacy.md, docs/USER_GUIDE.md, docs/manual-accessibility-pass.md, docs/changelog.md</files>
  <read_first>
    - `docs/ALPHA_TESTING.md:1-30` and `:123-200`, the short version and the known-missing list.
    - `docs/privacy.md:15-60` (where things are, and the unencrypted sentence at `:52`), `:144-160` (the table), `:252-290` (the two sections about a message reaching out, for the register of "what somebody learns").
    - `docs/USER_GUIDE.md:92-172`, "Reading and Managing Email", and `:30-92`, "Account Setup", for where the check interval belongs.
    - `docs/manual-accessibility-pass.md`, the whole shape, and the two lines 10-04 added.
    - The six summaries of this phase, each "What landed" and "Not done here, on purpose".
    - `docs/changelog.md`, the six entries this phase wrote under `[Unreleased]`.
  </read_first>
  <action>
    Documents only, which `CLAUDE.md` lists among the exceptions to
    test-first; the summary says so. Invoke `writing-craft` for all four
    pages and `cognitive-accessibility` for every sentence that describes
    something a person hears.

    `docs/ALPHA_TESTING.md`: in the short version, one paragraph that a
    check brings every message of every kept folder down, the text with it
    unless the Message Text box is off or a size is chosen, that it runs on
    its own after every check and can be paused from Tools, and that this
    has met one Gmail account and no other provider; in the known-missing
    list, the download and the watch as things whose behaviour against a
    provider nobody has observed, in the register of the list's first item;
    a line that how much is said while fetching is a choice on the Feedback
    tab and nobody has listened to the three levels; and the check interval
    on the account editor as a setting that now does what it says.

    `docs/privacy.md`: the mail-provider row of the table says what the
    download asks for, "the whole of every folder you keep up to date, a
    chunk at a time, and the text of each message unless you turn that
    off"; a paragraph under "Where your things are" that with the default
    the text of every message is on this disk, unencrypted as the page
    already says of the mail, and that the Permissions tab is where a size
    is chosen; and, for #29, one sentence that a client asking for a whole
    mailbox tells the provider only that, and nothing about which messages
    were read, which the read flag already tells it as it did before. Where
    a sentence on the page was true until this phase, keep it and date it,
    in the page's own manner.

    `docs/USER_GUIDE.md`: a section "Getting your mail" under "Reading and
    Managing Email": what a check does now, that everything comes down and
    keeps coming, Pause Downloading, Shift+F9 for this folder first, the
    three announcement choices and where, and, under "Account Setup", the
    Check Interval field's sentence. The Keyboard Shortcuts list at `:406`
    stays as it is unless a key changed, and 10-05 changed none.

    `docs/manual-accessibility-pass.md`: the listening lines from premise
    2, each with its source plan and its technology.

    The changelog: read the six entries this phase wrote as one reader
    would, and correct any that a later plan of the phase made stale by
    dating, in the page's manner; add nothing new.

    No em dash, none of the six words, counts beside their dates and
    commands where a page states one (`every_number_carries_its_command_and_its_date`
    reads `docs/`).
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n -i 'pause downloading\|every message\|whole mailbox\|check interval' docs/ALPHA_TESTING.md docs/privacy.md docs/USER_GUIDE.md` finds each page saying its part, quoted in the summary.
    - The privacy table's mail-provider row names the download, and the page carries the #29 sentence.
    - `docs/manual-accessibility-pass.md` has one line per sentence the summaries list as unheard, with the count in the summary.
    - The five targets pass; carriage returns on every page are 0 by `tr -cd '\r' | wc -c`.
  </acceptance_criteria>
  <done>A person reading the alpha page, the privacy page or the guide before trusting the program reads what it does now and what nobody has yet seen it do against a provider.</done>
</task>

<task type="auto">
  <name>Task 2: the five issues read against the tree, and the phase closed</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/phases/09-what-the-first-day-of-testing-found/09-10-SUMMARY.md:332-420`, the closing read, for the shape.
    - `.planning/REQUIREMENTS.md`, the section "All the mail, and what is said while it comes", every line of MAIL-01 to MAIL-05.
    - `.planning/ROADMAP.md`, phase 10's entry and the progress row.
    - `.planning/STATE.md:1-60` and the Current Position section.
    - `.planning/WINDOWS.md:1-20` for the frontmatter's counts and the row shape, and the JSON half at the end of the file for the other half of each entry.
    - The six summaries' coverage blocks.
  </read_first>
  <action>
    Read each of the five issues clause by clause against the six
    summaries and the tree: for every `[D]` line, the test, reading or row
    that closes it, by name and file; for every `[S]` line, the sentence
    that it is the tester's and the ledger entry that says so. Write the
    table into the summary, then into `REQUIREMENTS.md` under each
    requirement and in its traceability row, by hand, keeping each
    requirement's lines as written and adding the dated closing sentence
    above them in FOUND-02's manner. Tick a requirement only when every
    `[D]` line has a name.

    In `ROADMAP.md`, phase 10's criteria each gain the dated closing
    sentence with the plan and the merge commit, the plan list is ticked
    with a line per plan in phase 9's manner, the progress row reads `7/7`
    with the criteria that are open on a clause named in the row, and the
    phase-list line gains "Complete <date>" as phase 9's did.
    `test_the_roadmap_counts_the_files_that_are_on_disk` holds the row to
    the summary files, so the row is written after the summary exists.

    In `STATE.md`, by hand: `current_plan` 7, `status` phase_complete, the
    Current Position paragraph for 10-07 and the phase, `Current Plan: 7`
    and `Total Plans in Phase: 7` at column zero once each,
    `progress.completed_plans` counted from the `*-SUMMARY.md` files on disk
    and `total_plans` from the `*-PLAN.md` files, the session continuity
    lines.

    In `WINDOWS.md`, one entry per thing only a person or a real provider
    can settle, both halves, no backslash: whether Gmail tolerates the
    download of 12,872 messages and their text in chunks; whether it drops
    the watch and the restart carries mail over hours; whether a folder of
    his size reads as one list with his screen reader; what the three
    announcement levels sound like on a check of his 50 folders; whether
    the status line's three states read as states; and whether two
    connections per account are welcome, unless 67 already says it.
    `gsd-tools windows append` may be used for an entry with no backslash;
    the frontmatter's counts follow.

    The "For a person" paragraph, in the summary as 09-10 wrote it: what
    changed in this build over the last one, which issues closed with their
    merge commits, what only a person can settle with the ledger numbers,
    and what is next, which is the next of Pratik's groups, named from the
    README's table.
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*MAIL-0' .planning/REQUIREMENTS.md` plus `grep -c '^- \[ \] \*\*MAIL-0' .planning/REQUIREMENTS.md` is 5, and each open one names the clause and the reason in its traceability row.
    - The roadmap's phase 10 row reads `7/7` and its status names any criterion open on a clause; `cargo test --test the_planning_files_agree_with_themselves` passes.
    - `STATE.md`'s frontmatter and Current Position agree on phase 10 and plan 7, and `progress.total_plans` and `completed_plans` equal the files on disk, quoted with the two `ls | wc -l` commands.
    - Every ledger entry has both halves; the frontmatter's `total_count` and `open_count` are the numbers the file holds.
    - The summary carries the table per requirement, the criteria table, and the "For a person" paragraph.
  </acceptance_criteria>
  <done>The tracker, the requirements, the roadmap, the state and the ledger say the same thing about the five issues, and what only the tester can settle is written down with a number rather than claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a document to a person deciding whether to trust the program | a page that says less than the program does is a person surprised; one that says more is guardrail 3 |
| a requirement tick to the next planner | a tick over a clause nobody closed sends the next phase past a gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-25 | Repudiation | a page describing the program before this phase | medium | mitigate | each sentence corrected by dating, the old wording kept; the greps in premise 1 re-run on the final tree and quoted |
| T-10-26 | Repudiation | a requirement ticked on a clause with no name | medium | mitigate | 09-10's rule, the table in the summary, the planning-files target on the commit |
| T-10-27 | Information disclosure | the privacy page promising less than the download sends | medium | mitigate | the table row and the #29 sentence written from 10-05's runner, not from memory |
| T-10-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; nothing here compiles |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; every commit answers `docs_only` through the hook, never piped, and the summary quotes the mode.
- No guard record is written or measured; nothing under `src/` changes.
- What the tester's account could settle is the ledger's six entries; nothing here claims any of it.
- After the merge, the executor comments on the five issues only where a plan's own close did not already say the merge commit, and comments on #29: `gh issue comment 29 --body "One line of this list is written at <merge commit>: a client that downloads the whole mailbox tells the provider that and nothing more, on docs/privacy.md under Who Wixen Mail talks to. The rest of the page is still this issue's."` Commenting is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 6 closes: the four pages, the listening lines, the closing read, the ledger.
- Every MAIL requirement is ticked or open with its reason on its traceability row.
</success_criteria>

<output>
Create `.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-07-SUMMARY.md` when done
</output>
