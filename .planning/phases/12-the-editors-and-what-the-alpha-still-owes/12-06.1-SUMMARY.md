---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 06.1
subsystem: the name on a spin control's typing field, in the running program
tags: [edit-01, spin-control, msaa, uia, annotation, webview2, "#35", "#73"]
status: complete

requires:
  - phase: 12-the-editors-and-what-the-alpha-still-owes
    provides: "12-06 merged at 01dfbe28: names::name_the_spin_control writing the arrows' words onto the buddy through IAccPropServices, read in a test process and not in the running program"
provides:
  - "src/presentation/accessibility/names.rs: ready_the_annotation_store, the process's first annotation write before any browser; typing_field::carry's failures at warn and one named line at debug"
  - "src/presentation/wx_app.rs: ready_the_annotation_store called first thing in on_init, before the preview's WebView2"
  - "tests/a_spin_controls_field_is_named_where_the_scan_reads_it.rs: the program's condition, a browser first, read from another process by the field's object, by the scan's walk and on UI Automation; 4 tests and 2 child processes"
  - ".github/workflows/accessibility.yml: the program's own log printed and uploaded as accessibility-scan-program-log"
  - "guards/guards.toml: one record written, one re-measured"
affects: [12-07 and 12-08, whose new spin controls go through the same helper and now reach the running program; 12-12, which reads the pages and runs the phase's full gate]

actuals:
  tokens: 61500
  tasks: 4
  commits: 7

tech-stack:
  added: []
  patterns:
    - "A fault about first use is bisected across processes, one probe per process, because a probe that touches the thing under test cures every point after it"
    - "A reading that must see the running program's condition builds what the program builds first (here a WebView2) and reads only from another process"

key-files:
  created:
    - tests/a_spin_controls_field_is_named_where_the_scan_reads_it.rs
    - .planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06.1-SUMMARY.md
  modified:
    - src/presentation/accessibility/names.rs
    - src/presentation/wx_app.rs
    - tests/every_spin_control_names_the_field_a_person_types_in.rs
    - .github/workflows/accessibility.yml
    - Cargo.toml
    - guards/guards.toml
    - docs/changelog.md
    - docs/USER_GUIDE.md
    - .planning/WINDOWS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06-SUMMARY.md

key-decisions:
  - "The cause is a WebView2 built before the process's first annotation write: after it, SetHwndPropStr answers S_OK and keeps nothing, on any window and from any thread. Not explained; ledger 594."
  - "The fix is one write onto a hidden window before the browser, destroyed without ClearHwndProps, because clearing it measured red."
  - "D-01 extended twice on 2026-09-24 by the coordinator, for the startup bisect and then the bisect across processes; both left with the diagnosis."

requirements-completed: []

duration: 170 min
completed: 2026-09-24
---

# Phase 12 Plan 06.1: The Typing Field's Name in the Running Program Summary

**Every spin control's typing field is now named in the running program, on MSAA and, wherever the scan's snapshot reaches it, on UI Automation, proved by Accessibility run 35976411245 on #98. The program built its WebView2 message preview before any dialog named a spin control, and once a browser exists before the process's first annotation write, no later write is kept. `names::ready_the_annotation_store` makes one write first.**

On branch `12-06.1-spin-field-naming` from `main` at `ca2f5e37`, 2026-09-24, first commit at about 06:40Z. The branch name is the brief's, not the plan's.

## What the scan said, run by run

| Run | Commit | What it carried | What it showed |
|---|---|---|---|
| 35966112515 (run 1) | `e9452bda` | per-call diagnosis at warn, `send-later` with every read switched off | every write `S_OK` in a main STA, the field's name unreadable even inside the program straight after the write; no row of the branch table held, so the plan stopped at its checkpoint |
| 35968417797 | `7d00b817` | a throwaway field named and read back at 13 startup points in every process | every point read right, and the probed targets' real fields came out named; `send-later`, unprobed, stayed nameless and its field held only `UxSubclassInfo`, so the write stored nothing. The first probe was the process's first use and cured the rest |
| 35970583171 | `1476b1af` | one probe per process at one assigned point, 36 processes over 13 points (37 assigned; `main` starts without a scan target and probes nothing), `send-later` and `new-event` unprobed | stored at every point up to `after-first-announcement`, stored nothing at every point from `after-webview-preview` on, two or three processes a point, no exception; a plain `SetPropW` on the unstored field stuck, so the service never set its property |
| 35976411245 (run 2) | `e50aee6f` | the fix; the diagnosis gone | `join_the_scan.py --verify` exit 0, every logged spin field named |

Run 1's build step: `Compiling wixen-mail v1.0.0-alpha.1 (D:\a\Wixen-Mail\Wixen-Mail)`. The companion over run 35930014324 listed Send Later's four handles, 1245872, 1770146, 1376932 and 1573554, nameless on both channels, "no log lines at all" for every target, and exited 3.

Run 1, per target, from `join_the_scan.py --row` (exit 2, no row):

| Target | Calls | Service and writes | Read back inside the program | MSAA walk | UI Automation |
|---|---|---|---|---|---|
| settings | 1 | created `0x00000000`, name `0x00000000` | `Font size:`, the label | `Font size:` | `Font size:` |
| new-event | 12 | all `0x00000000` | `""` on 8, the label on 4 | `""` on 8 | None on 7, 4 not in the snapshot |
| send-later | 4 | all `0x00000000` | skipped, the control | `""` on 4 | None on 4 |
| account-editor | 2 | all `0x00000000` | the label with its colon | the label | not in the snapshot |

Run 2, per target, from `join_the_scan.py --verify` (exit 0):

| Target | Fields | MSAA | UI Automation | Scan violations |
|---|---|---|---|---|
| settings | 1 | `Font size` | `Font size` | 0, was 0 |
| new-event | 12 | all their arrows' words | 8 named; Alert minutes before, Last day Day, Last day Year and How many times not in the snapshot | 3, was 9 |
| send-later | 4 | all named | all named | 2, was 6 |
| account-editor | 2 | both named | neither in the snapshot | 0 |

The MSAA walk reported 0 operated controls without a name in all four, against 8 in new-event and 4 in send-later on run 1.

Reproduced here before any fix, in a throwaway example: `Frame`, then an Edge `WebView`, then a write, stored nothing on the main thread, on another thread in an STA or the MTA, on its own window or the interface thread's; without the browser it stored. Creating the service before the browser without writing did not help; one write before it did.

## Commits

| Commit | What | Hook |
|---|---|---|
| `e9452bda` | chore: log each naming call at warn and upload the program's log | `all`, 452 s |
| `7d00b817` | chore: bisect the program's startup (D-01 extended) | `affected`, 145 s |
| `1476b1af` | chore: bisect startup across processes, one first use each (deviation 2) | `all`, 461 s |
| `1151b984` | chore: the diagnosis out, the failures kept at warn | `all`, 455 s |
| `97440d7f` | test: a spin field read where the scan reads it, a browser first | `red`, 124 s, "a red that is exactly the one this commit named" |
| `e50aee6f` | fix: ready the annotation store before the program's first browser | `all`, 485 s; the first attempt stopped in tests after 403 s |
| this commit | docs: the pages, the ledger, EDIT-01, the summary and the four marks | |

The red commit's trailers:

```
Fails-until-green: test_send_later_names_every_typing_field_where_the_scan_reads_it
Fails-until-green: test_the_event_form_names_every_typing_field_where_the_scan_reads_it
```

Red at `97440d7f`, Send Later's four fields came back `own: "", walk: "", uia: ""` against arrows `Send on Day` to `Send at Minute`, and the event form's twelve matched run 1: eight nameless and four carrying their label. The two companions passed by design: a spin control named on its arrows alone reads nameless on its field, and with the store not readied a browser first leaves Send Later's fields nameless, which is the program's condition this rests on.

Fix candidates against the red target, in order: the write then `ClearHwndProps` then the destroy, still red; the write then the destroy, green. The second is the fix.

## Tests

| Target | Before | After |
|---|---|---|
| `tests/a_spin_controls_field_is_named_where_the_scan_reads_it.rs` | new | 4 passed, 2 ignored children, 15 s |
| `tests/every_spin_control_names_the_field_a_person_types_in.rs` | 15 passed, 1 ignored | 15 passed, 1 ignored |
| `presentation::accessibility::names::` | 12 | 12 |

## Guard records

One `scripts/guards.sh --remeasure` call over both, 2026-09-24:

| Record | Break | What went red |
|---|---|---|
| the annotation store is readied before the program's first browser (new) | the hidden window made to fail | "all 2 tests named went red, and nothing else did" |
| every spin control's typing field is named through the annotation service (re-measured, its code changed in `1151b984`) | the write loop over nothing | "all 8 tests named went red, and nothing else did" |

The sweep header's arrived-since count went from 261 to 262, which the first green attempt was refused for.

## What the contract kept

`git grep -n 'tracing::warn!' -- src/presentation/accessibility/names.rs` at `e50aee6f`: `service-failed` with the HRESULT and the apartment, `no-field` with the arrows' handle and the words, `write-failed` with the property, the HRESULT and the field's handle, and two `ready-failed` lines for the new step. One `named` line at debug per naming call. `WIXEN_DIAGNOSE_NO_READBACK`, `WIXEN_DIAGNOSE_PROBE_AT`, the read-back, the check at show and the two `windows` features are gone: `git diff main -- Cargo.lock` is empty and `grep -c '"Win32_' Cargo.toml` is 11. The workflow keeps printing and uploading the program's log.

## Ledger

`head -7 .planning/WINDOWS.md` before: `open_count: 551`, `fixed_count: 42`, `total_count: 593`. After: `open_count: 540`, `fixed_count: 55`, `total_count: 595`. Thirteen fixed, 408, 409, 410, 412, 413, 414, 419, 420, 421, 422, 424, 425 on run 35976411245 and 593 on the new target; two opened, 594 the platform behaviour this works around and does not explain (whether to report it upstream is Pratik's), 595 the six spin fields the scan reads on MSAA only. 592 stays open for the ear. Both halves agree, counted with Python's `json` over the fenced half and a row count over the table.

## Deviations

1. **The plan stopped at its checkpoint after run 1.** No row of the branch table held: every write succeeded and the name was unreadable inside the program too, the case Row A's last bullet sends to the checkpoint.
2. **D-01 extended, 2026-09-24, on the coordinator's instruction:** the startup bisect, `7d00b817`, the same kind of diagnostic lines for the same purpose. It named no step, because its first probe cured every point after it.
3. **Deviation 2 in the coordinator's numbering, 2026-09-24:** the bisect across processes, `1476b1af`, one probe per process with `send-later` and `new-event` as controls, the apartment and a process-wide instance count at each probe, and a plain `SetPropW` test at the first real naming. It named the step.
4. **The cause is not a row of the plan's table**, so the red is the plan's Row F shape without Row F's cause: a new target reading from another process after reproducing the program's condition, and the fix in a new function rather than in `carry`.
5. **EDIT-01's box stays unticked** until #73 and #35 are closed from the merge commit, the last clause of 12-06's third `[D]` line. The three 12-06.1 `[D]` lines are ticked with their names.
6. **The NVDA job on `e50aee6f` failed once**, `account-manager-sign-in-failure.test.js`: after `Down` the last phrase was "Accounts, list" where the account's name was expected. It was rerun; the report gives the verdict.
7. **STATE's Current focus** read "4 of 13 plans merged", stale since 12-03; it now reads 10 of 16 with a dated note. **12-06's summary** said 2026-09-23 for Pratik's split at three places; they now say 2026-09-24, the day of his message.

## Scripted edits

None. Every tracked file was written with Write or Edit, through the diagnostic detour as well as the work; `cargo fmt` formatted Rust files and `scripts/guards.sh` applied and restored its own breaks. The diagnosed files were returned to `main`'s content with `git checkout main -- <files>` before the kept lines were written back by hand. Four throwaway examples were written with Write, run and deleted untracked. Two stray `sed` calls aimed at `/dev/null` changed nothing.

## Decision quoted

D-01, Pratik, 2026-09-24: "Split it + followup with logging level changes that you recommended."

## What only a person can settle

A spin control's field heard once with its name under NVDA, and Narrator on the six fields the snapshot does not reach (ledger 592, 595). `docs/development/measurements.md` gained no row: no figure was taken for it.

## Self-Check: PASSED

The six commits above are on the branch (`git log main..HEAD`), the new target and this summary exist, and `the_planning_files_agree_with_themselves` (17), `house_style` (74) and `the_words_that_say_nothing` (10) passed on the documents before this commit.
