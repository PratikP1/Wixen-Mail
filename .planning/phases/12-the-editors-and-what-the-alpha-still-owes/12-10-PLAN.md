---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 10
type: execute
wave: 13
depends_on: [12-09]
files_modified:
  - src/application/tagging.rs
  - src/data/message_cache/mod.rs
  - src/data/message_cache/tags.rs
  - src/presentation/wx_app.rs
  - src/presentation/managers.rs
  - src/presentation/wx_managers.rs
  - tests/the_label_menu_says_the_labels_an_account_has.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [EDIT-05]
estimate:
  tokens: 29000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Action menu's Label submenu shows the account's labels by their current names in their chosen order with the number key beside each, re-read whenever labels change, and ends with Edit Labels, which opens the manager; a number past the end says there is no such label (EDIT-05, #48)."
    - "The manager, reached from the submenu and from Tools under the one word Label, adds, edits, and moves a label up or down with Alt+Shift+Up and Alt+Shift+Down, the gesture accounts and pinned folders already use, and says which number key each label has."
    - "The order is stored in a position column added without dropping anything, existing rows taking the order they had, and Ctrl+N applies the label the menu shows at N, which today it does not: the menu is built from the built-in five in their own order and the keys apply the stored labels by name order, so Ctrl+2 says Work and applies Later."
  artifacts:
    - "tags.rs: the position column, get_tags_for_account ordered by position then name, move_tag, the once-only numbering; tagging.rs: what_the_menu_says(labels) and moved through reordering::moved with its own wording."
    - "wx_app.rs: the submenu rebuilt from the labels on load and after the manager closes; Edit Labels at its end; Tools' item says Label; wx_managers.rs and managers.rs: the manager with Move Up and Move Down and the key column."
    - "tests/the_label_menu_says_the_labels_an_account_has.rs: the menu's items read back after a rename and a move, the key applying the label at the position shown, the pass over rows without a position."
  key_links:
    - "the tags table -> position -> get_tags_for_account -> what_the_menu_says -> the submenu's items and at_number: one order, read by the menu and by the key."
    - "the manager's Move Up -> reordering::moved -> the positions written -> the submenu rebuilt: the same gesture as accounts and pins."
---

<objective>
Labels the person can see, create, edit and order, with the menu saying
what the keys do.

Purpose: #48, the tester on 2026-09-15: "The action menu allows the user
to apply labels. Currently there are five preconfigured labels. Four
additional slots are available but are not assigned. There is NO UI for
creating these labels. Create a UI for this functionality for additional
labels as well as for editing and moving labels' order." The tree has a
manager under Tools, Tags with no way to it from the submenu, no order,
and a menu that never re-reads its names. Worse than the issue says: the
keys and the menu disagree today (premise 1). #62's decision on
2026-09-19 left the labels' order and naming to this issue.

Output: the column and the pass, the ordering and the menu's words as
rules, the submenu rebuilt, the manager with moves and the key column,
one word, the reading, the pages, the changelog, the ledger. Closes #48
on the tree side.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`. Command first, then what
it said.

**1. The menu and the keys disagree today, which the issue did not
say.**

```
sed -n 6918,6946p src/presentation/wx_app.rs   -> the submenu built once from tagging::TO_BEGIN_WITH by index (Important, Work, Personal, To Do, Later), slots six to nine "Label"; the comment at :6919-6921 says the names "are rewritten from the account's own labels when those load", and nothing does: `grep -n 'labels_menu' src/presentation/wx_app.rs` finds the build at :6927-6941 and the append at :7346 and no other site
sed -n 72,78p src/data/message_cache/tags.rs   -> get_tags_for_account orders `ORDER BY name`, so the stored order is alphabetical, not undefined as the issue said: Important, Later, Personal, To Do, Work
sed -n 4097,4100p src/presentation/wx_app.rs; sed -n 149,155p src/application/tagging.rs   -> Ctrl+N applies labels[N-1] of that alphabetical list through at_number; so the menu shows "Work Ctrl+2" and Ctrl+2 applies Later, "Personal Ctrl+3" applies Personal by coincidence, "To Do Ctrl+4" applies To Do, "Later Ctrl+5" applies Work
grep -n 'CREATE TABLE IF NOT EXISTS tags' -A 7 src/data/message_cache/mod.rs   -> :1802: id, account_id, name, color, created_at, UNIQUE(account_id, name); keyword added later; no position
grep -n 'ID_TAG_MGR' src/presentation/wx_app.rs | head -3   -> :137, :5597 (managers::manage_tags), :7449 ("Ta&gs..." on Tools, help "Labels you can put on messages"): two words for one thing on one menu bar
grep -n 'Tag Manager' src/presentation/wx_managers.rs   -> :3659, the shell titled Tag Manager with Add and Edit; no move
grep -n 'reordering::moved' src/application/account_order.rs src/application/favourites.rs   -> :37 and :224: the gesture and its wording, which labels join as a third caller
grep -n 'Labels\|Ctrl+1' docs/KEYBOARD_SHORTCUTS.md | head -4   -> :755 "### Labels", :758 "Nine labels have a key; a tenth is reached from the Label menu", :777 "| Important | Ctrl+1 |": the page lists the built-in names against the keys, which is the menu's order and not the keys'
```

**2. The word.** The submenu, the shortcuts page and the guide's
sidebar heading say Label; Tools and the manager say Tag. One word:
Label, on the tester's word and the page's, so Tools says "&Labels..."
and the manager is the Label Manager; the `tags` table and the `Tag`
type keep their names, since a column and a type are not something a
person reads.

**3. The gate for each file.** `tagging.rs` 12 tests and 0 records,
cases added freely; `tags.rs` 10 tests and 2 records, one case added
for the pass with the count check named bare; `mod.rs` 23 and 12, the
column only; `wx_app.rs` 199 and 110, none added; `managers.rs` 137 and
52, none; `wx_managers.rs` 44 and 11, none; the new target at zero
records.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the position, the pass, the order as one rule, and the menu's words</name>
  <files>src/application/tagging.rs, src/data/message_cache/mod.rs, src/data/message_cache/tags.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/tagging.rs`, whole; `src/data/message_cache/tags.rs:1-100`; `src/data/message_cache/mod.rs:1800-1812` and `:1675-1700`.
    - `src/application/reordering.rs`, whole; `src/application/account_order.rs:25-45`.
    - 12-09's summary for the once-only pass's shape under `work_done_once`.
  </read_first>
  <behavior>
    - `tags` gains `position INTEGER` through `ensure_column_exists`; `get_tags_for_account` orders by `position` then `name`; `number_the_labels_once()` under the marker `LABELS_HAVE_A_POSITION` writes 1.. in the order rows came back before (name order), per account, once; `move_tag(account_id, tag_id, direction)` swaps positions through `reordering::moved` and writes them; a created tag takes the next position; cases: the order after the pass, a move up and down, the first not moving, a new tag last.
    - `tagging::what_the_menu_says(labels: &[Tag]) -> Vec<MenuLine { id_index, text }>`: one line per label in order with `\tCtrl+N` for the first nine and no key past the ninth; `nothing_there(n)` unchanged; `moved` wording for labels through `reordering::moved` ("Work moved up, second of five") on the pattern of the two callers; cases.
  </behavior>
  <action>
    Red first, on a branch: the `tagging` cases by module path, the
    `tags.rs` case by module path with the count check named bare
    (`test_every_guard_record_says_how_many_tests_the_files_it_names_held`,
    two records name the file). Then the column, the pass, the readers,
    the rule, with `cargo build` between. Invoke `elegant-code`: the
    order lives in one query; the menu's words in one function; the
    move through the one reordering rule.

    Guard records: one on `tags.rs` whose break orders by name alone,
    the order case red; one on `tagging.rs` whose break gives the tenth
    label a key, its case red; each measured. The two records naming
    `tags.rs` re-measured when the count check prints them.
  </action>
  <verify>
    <automated>cargo test --lib application::tagging:: && cargo test --lib data::message_cache::tags::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the count check bare.
    - `grep -c 'ensure_column_exists("tags", "position"' src/data/message_cache/mod.rs` is 1; `grep -c 'ORDER BY position' src/data/message_cache/tags.rs` is at least 1; `grep -c 'ORDER BY name"' src/data/message_cache/tags.rs` is 0.
    - `cargo test --lib application::tagging::` passes with at least 16 tests, quoted; `data::message_cache::tags::` with 11, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>Labels have an order the store keeps, rows from before take the order they had, and the menu's words come from one rule that agrees with the keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the submenu rebuilt from the labels, Edit Labels, the manager's moves and key column, one word</name>
  <files>src/presentation/wx_app.rs, src/presentation/managers.rs, src/presentation/wx_managers.rs, tests/the_label_menu_says_the_labels_an_account_has.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:6915-6950`, `:7340-7350`, `:7445-7452`, `:4095-4105`, `:11229-11260` (`label_the_message`), `:12425-12432` (the labels read on the sidebar's pass), `:10905-10925` (an item relabelled in place, 11-06's shape), `:5597`.
    - `src/presentation/managers.rs:103-185` (`manage_tags`); `src/presentation/wx_managers.rs:3621-3720` (the manager).
    - The gesture's wiring, which is on the main window and not in the account manager (`grep -n 'Alt+Shift' src/presentation/wx_account_manager.rs` finds nothing): the menu items at `wx_app.rs:7190-7200` ("Move &Up\tAlt+Shift+Up", `ID_MOVE_UP` and `ID_MOVE_DOWN`), the arm at `:4337`, and the rule `what_the_gesture_moves` at `folder_tree.rs:755-790` (`WhatMoves`), which the manager's own handler copies in shape and not in code.
    - `~/.cargo/registry/src/*/wxdragon-0.9.17/src/menus/menu.rs:165-320` (`append`, `insert`, `remove`, `delete`, `find_item`).
  </read_first>
  <behavior>
    - `rebuild_the_label_menu(menu, labels)` in `wx_app.rs`: removes every label item, appends one per `what_the_menu_says` line under the same `LABEL_IDS` by index, then the separator, Remove every label, a separator and "&Edit Labels..." (`ID_TAG_MGR`); called after the labels are read on the sidebar's pass and after the manager closes; `label_the_message` unchanged, since `at_number` now reads the same order the menu shows.
    - Tools' item reads "&Labels..." with the same help; the manager's shell is "Label Manager" with the list titled "Labels"; a column "Key" saying `Ctrl+N` for the first nine and nothing past; Move Up (Alt+Shift+Up) and Move Down (Alt+Shift+Down) through `move_tag`, the sentence from task 1's wording at Normal, the list re-read and the cursor kept on the moved row; Add and Edit as before.
    - The target: a cache built in the test with an account and the five built-in labels; the submenu built through the real builder and its items read back (`find_item_by_position`, `get_label`) as Important Ctrl+1, Work Ctrl+2, Personal Ctrl+3, To Do Ctrl+4, Later Ctrl+5 after the pass; a rename and a move written to the cache, the menu rebuilt, the items read again; `at_number(labels, 2)` naming what the second item shows; the Tools item's label read; the manager built and its Key column read; companions that plant a menu built from the built-in five and a key applying by name order and are refused.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare. Then the
    rebuild, the item, the manager, with `cargo build` between. Invoke
    `elegant-code`: the rebuild is one function called from two places;
    the manager's move handlers call one store function; no `unwrap`
    outside the tests. Invoke `naming-review` on the one word: every
    string a person reads says Label.

    Guard records: one on `wx_app.rs` whose break skips the rebuild
    after the labels load, the rename reading red, `suite` the target;
    one on `wx_managers.rs` whose break drops the Key column, its
    reading red; each measured. `wx_app.rs` stays at 199, `managers.rs`
    at 137, `wx_managers.rs` at 44, quoted.
  </action>
  <verify>
    <automated>cargo test --test the_label_menu_says_the_labels_an_account_has && cargo test --lib presentation::wx_app:: && cargo test --lib presentation::managers:: && cargo test --lib presentation::wx_managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare.
    - `grep -c 'fn rebuild_the_label_menu' src/presentation/wx_app.rs` is 1 and `grep -c 'rebuild_the_label_menu(' src/presentation/wx_app.rs` is at least 3; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c '"Ta&gs..."'` is 0; `grep -c 'Edit Labels' src/presentation/wx_app.rs` is at least 1; `grep -v '^\s*//' src/presentation/wx_managers.rs | grep -c '"Tag Manager"'` is 0.
    - The target passes with at least 6 tests, quoted; the three `--lib` filters at 199, 137 and 44, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The Label submenu says the labels the account has in their order with the keys that apply them, leads to the manager, and the manager moves a label with the gesture the tree already has, under one word.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the changelog, the ledger, and the issue closed</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:755-790` (Labels) and the Tools menu rows (`grep -n 'Tags' docs/KEYBOARD_SHORTCUTS.md`).
    - `docs/USER_GUIDE.md:105-125` (the sidebar's Tags line) and the labels paragraph.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: the Labels table no
    longer lists the built-in names against the keys and says instead
    that Ctrl+1 to Ctrl+9 apply the first nine labels in the order the
    Label Manager shows, Edit Labels at the end of the submenu, Alt+Shift+Up
    and Down in the manager; the Tools row says Labels. The guide: the
    sidebar line and the labels paragraph say Label, the manager and
    its moves, and one sentence on what an installation from before
    keeps (its labels, in the order they were shown). The changelog,
    `[Unreleased]`, Changed and Fixed, in the tester's words, #48, with
    the Fixed entry saying plainly that Ctrl+2 said Work and applied
    Later until this build (premise 1) and why; Known limitations:
    nobody has heard the rebuilt submenu.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's
    ear (the submenu's items with their keys, Edit Labels, a move said,
    the Key column).

    After the merge, the executor closes the issue from the merge
    commit: `gh issue close 48 --comment "..."` with the summary's
    findings and the disagreement premise 1 found. Closing an issue is
    not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Edit Labels' docs/KEYBOARD_SHORTCUTS.md` is at least 1; `grep -c '| Important | `Ctrl+1` |' docs/KEYBOARD_SHORTCUTS.md` is 0; `grep -c '#48' docs/changelog.md` is at least 1.
    - The ledger entry in both halves; carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say how labels are ordered and reached, the changelog names the disagreement that was fixed, and the issue is closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the menu to the key | two readers of one order that disagreed for months |
| rows without a position to the new order | a person's labels must keep the order they were shown in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-34 | Tampering | a key applying a label other than the one the menu shows | high | mitigate | one query, one order, read by both; the target reads the menu and `at_number` together; a guard record |
| T-12-35 | Tampering | the pass numbering rows in a different order from the one shown before | medium | mitigate | the pass numbers in name order, which is the order `get_tags_for_account` returned before; the case holds it |
| T-12-36 | Information disclosure | a label's name in a menu item nobody expected | low | accept | the names are the person's own and were always meant to be there; the comment that claimed they were is corrected |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite runs once, in 12-12's full gate (12-03.2; CLAUDE.md, "The merge is the full gate", as 12-03.2 dated it); never `scripts/check.sh all` by hand here. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line to it, and its own hook answers `docs_only`, so 12-12's full gate can place a red among the merges (12-03.2, task 7).
- Four guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the submenu and the manager under NVDA; what the readings prove: the order, the pass, the menu's items after a rename and a move, the key agreeing with the menu.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 10 closes structurally.
- EDIT-05's `[D]` lines each have a named test or reading in the summary and its box is ticked with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, EDIT-05's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-10-SUMMARY.md` when done
</output>
