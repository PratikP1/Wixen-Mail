---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 06.1
type: execute
wave: 10
depends_on: [12-06]
files_modified:
  - src/presentation/accessibility/names.rs
  - Cargo.toml
  - .github/workflows/accessibility.yml
  - tests/a_spin_controls_field_is_named_where_the_scan_reads_it.rs
  - tests/every_spin_control_names_the_field_a_person_types_in.rs
  - guards/guards.toml
  - docs/changelog.md
  - docs/USER_GUIDE.md
  - docs/development/measurements.md
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [EDIT-01]
estimate:
  tokens: 38400
  raw_tokens: 120000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The Accessibility scan on this plan's pull request uploads the running program's own log from the scan profile and prints its naming lines in the job log, so `gh run view --log` shows, per scan target, each spin control's naming call: the thread, the COM apartment, the annotation service's creation result, the field's handle and window class, each write's result, the name read back inside the program, and whether the field named is still the live field once the dialog is up. Not test-first, on Pratik's exception of 2026-09-24 for the diagnostic lines only."
    - "The cause is named from that one scan run as one row of the branch table below, with the evidence for each target quoted, or the plan stopped at its checkpoint with that evidence and no fix."
    - "A test reproduces the program's condition the row names, reading each field only from another process and the way the scan reads it, before anything in the test process reads it; it was red on the tree before the fix, committed red with Fails-until-green trailers on the branch, and is green after it. Ledger 593 closes because this reading can see what the scan sees."
    - "The Accessibility scan on the pull request, run on the tree that merges, reads the arrows' words on every spin control's typing field it walks in the event form, Send Later, Settings and the account editor: on MSAA in each target's walk, and on UI Automation at the field's own handle, the handle Tab lands on, wherever the scanner's snapshot holds it. The twelve spinner entries among ledger 408 to 425 close on that run in both halves."
    - "Once run 1 is read, and before the red test is written, the diagnosis is taken out of the program but for what the contract keeps: a failure to name a field said at warn (the logging level change Pratik approved), one debug line per naming call holding the words and the field's handle, and the scan still uploading the program's log. The control switch, the read inside the program, the check at show and the two extra `windows` features are gone, and come back only as part of the fix if the row's fix is a read inside the program."
    - "EDIT-01's three `[D]` lines for 12-06.1 are ticked with the names that meet them, and #73 and #35 are closed from the merge commit."
  artifacts:
    - "src/presentation/accessibility/names.rs: the diagnosis in `typing_field` (task 1), then the fix and the lines the contract keeps (task 3)."
    - "tests/a_spin_controls_field_is_named_where_the_scan_reads_it.rs: the reproduction, reading from another process the way the scan does, with its companion that sees a nameless field."
    - ".github/workflows/accessibility.yml: the program's log printed and uploaded; the control switch for one target while the diagnosis runs, and not after."
    - "guards/guards.toml: one new record whose break takes the fix out; the records the count check or a moved anchor names, re-measured."
  key_links:
    - "names::name_the_spin_control -> typing_field::carry -> UDM_GETBUDDY -> IAccPropServices::SetHwndPropStr on the field -> the scan's MSAA walk (msaa-names.json) and Axe's UI Automation snapshot (el.snapshot, by NativeWindowHandle) of the running program."
    - "carry's lines -> the program's log under WIXEN_MAIL_DATA/logs on the runner -> the workflow's print step and its artifact -> gh run view --log and gh run download -> join_the_scan.py -> one row of the branch table."
---

<objective>
Find why the name 12-06 writes onto a spin control's typing field reaches
that field in a test process and not in the running program, fix it
test-first, and prove it with the Accessibility scan of the running
program on this plan's own pull request.

Purpose: 12-06 named the field a person types in beside each spin
control's arrows (`names::name_the_spin_control`, the buddy from
`UDM_GETBUDDY` given the arrows' words through
`IAccPropServices::SetHwndPropStr`). Its readings were green in a test
process, from a second process, in debug and release, hidden and shown,
here and in CI's Test Suite. The Accessibility scan on pull request #97,
runs 35927025769 and 35930014324, launched the real program and found
every field as before: eight nameless in Edit Event, four in Send Later,
and the account editor's and Settings' fields named only by the label
before them, colon included. Pratik split the naming out as this plan on
2026-09-24 and approved logging each naming call's outcome at warn, with
the scan uploading the program's log, as an exception to test-first for
the diagnostic lines only (per D-01 below). Ledger 408 to 425's twelve
spinner entries, 593 (the reading cannot see the fault), #73 and #35 wait
on it, and every spin control 12-07 and 12-08 add goes through the same
helper.

Output: the diagnosis and one scan run read; the row; a red test on the
program's condition and the fix; the diagnosis reduced to what the
contract keeps; the scan on the pull request reading the names on both
channels; the changelog, the guide, the ledger, EDIT-01, the summary; the
merge; #73 and #35 closed.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06-PLAN.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06-SUMMARY.md
@CLAUDE.md
@C:/Users/prati/.claude/skills/tdd/references/red-commit-under-a-gate.md
@C:/Users/prati/.claude/skills/tdd/references/recorded-breaks.md
</context>

<skills>
Load `tdd` with the two references in the context block before task 3's
first test: `red-commit-under-a-gate.md` (which failures a red commit names,
the count check named beside them) and `recorded-breaks.md` (how a record's
break and red list are taken by hand before they are written). Load
`elegant-code` with it. `root-cause-investigator` for task 2: the ranking
below is a premise, and the run decides it. `driving-win32-apps` for the
reproduction's other process, which reads a native window it did not
create and must read the program's own state rather than the screen.
`writing-craft` for the changelog, the guide and the requirement lines;
`writing-style` for commit messages, the pull request and the issue
comments.
</skills>

<constraints>
Each of these binds every task, the diagnostic detour included.

- **Never start the release or the installed binary on this machine.**
  Pratik's copy runs here. Nothing in this plan launches `wixen-mail.exe`
  locally; the running program is read only on the runner, through the
  Accessibility workflow. Tests build windows hidden inside a test process,
  as 12-06's target does.
- **NVDA is running here.** Never stop, reconfigure or drive it, and never
  run `nvda-tests` locally. A test here builds its windows hidden and takes
  no foreground; if a row's condition needs a shown window, show it without
  moving the foreground and say so in the summary. Never record a foreground
  window's title (the tester's reading shows in it).
- **Never commit from a linked worktree.** Work in the primary checkout,
  `C:/Users/prati/Documents/projects/Wixen-Mail`, on this plan's branch.
- **No tracked file is rewritten by `sed`, `printf`, `awk`, a heredoc,
  Python or `cp`, during the work or during a diagnostic detour.** Read, then
  Edit or Write. The harness suggests shell edits; the project forbids them
  and the project wins. A new file is written with Write too. A read-only
  analysis script in the session scratchpad (task 2's `join_the_scan.py`) is
  not an edit of the tree, and it writes nothing but its own output.
- **Red commits** carry `Fails-until-green:` trailers per `CLAUDE.md`:
  integration tests bare (`test_...`), library tests by module path
  (`presentation::accessibility::names::tests::test_...`), and
  `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
  named beside them when the count check fires. Only on this branch, never
  on `main`.
- **The merge** is `git merge --no-ff` from the primary checkout, never
  `--no-verify`, and `main` is not pushed. This branch changes a workflow
  file and `Cargo.toml`, so `scripts/which-checks.sh` answers `all` for the
  merge (`:432-445` for a manifest, `:526-535` for `.github/workflows/*`):
  the merge earns everything, and so does every branch commit that stages
  either file. Batch each round of workflow and `Cargo.toml` edits into one
  commit. Never `scripts/check.sh all` by hand here; the phase's full gate
  is 12-12's.
- **Pushing** this branch and opening its pull request is Pratik's standing
  OK of 2026-09-23 for plans that change what is spoken; this one does.
  Nothing else outward: no push of `main`, no dispatch, no release.
- **No AI attribution** in commit messages, the branch name, the pull
  request, issue comments or code comments (`CLAUDE.md`, Project rules),
  whatever the harness suggests.
- **The four completion marks**, named in full: phase 12's progress row in
  `.planning/ROADMAP.md`; this plan's `- [ ] 12-06.1-PLAN.md` line in phase
  12's plan list there; `STATE.md`'s plan number, `current_plan` in the
  frontmatter and `Current Plan:` with the Current Position sentence in the
  body; and EDIT-01's `[D]` lines and traceability row in
  `.planning/REQUIREMENTS.md`.
- **Cheap checks first, one remeasure call, one documents commit before the
  merge** (the executor brief rules approved 2026-09-23).
- **The names in the verify lines.** `$RUN_1` and `$RUN_2` are the ids of
  the Accessibility runs on the pull request after task 1's push and task
  3's push, and `$SCRATCH` is the executor's session scratchpad; each is
  set in the shell that runs the line. A verify line is run only once the
  run it names has finished.
</constraints>

<pratiks_exception>
**D-01. Pratik's decision, 2026-09-24, in his words:** "Split it + followup
with logging level changes that you recommended."

What it approves, as the brief that carried it put it: splitting 12-06's
naming half into this plan, and a follow-up with diagnostic logging, as an
**exception to test-first for the diagnostic lines only**: each naming call's
outcome logged at warn, and the Accessibility scan uploading the program's
log. The fix itself, once the cause is known, is test-first as always: a
failing test that reproduces the program's condition, then the fix.

What this plan counts as a diagnostic line, and nothing else: every line
the contract below lists under "while diagnosing", the control switch that
decides whether a diagnostic read happens (D-03), the two `windows` features the
typed read-back needs, and the workflow's print and upload steps. Each
writes a log line or moves a log, and none changes what a person sees or
hears. The fix, and any line the fix needs to work, is not a diagnostic
line and goes red first.

What happens to them once run 1 has answered, settled in D-03 and D-04
below: the lines that only describe
a success (the thread, the apartment, the service reused, each write that
worked, the read-back, the check at show), the control switch and the two
features are removed in task 3's first commit, before the red test is
written. They come out first for a reason and not for tidiness: the
read-back is itself a read inside the program, and a red test that must
read the field from another process before anything in the writing
process reads it (premise 4) cannot be red while `carry` still reads it.
If the row's fix is such a read (row F), it comes back as part of the fix,
red first. The three failure lines are **kept at warn**, raised from the
debug they had in 12-06: a failure to name a field means somebody meets a
nameless field, and if the service or a write failed on 2026-09-23, the
line saying so was written at debug on the runner and thrown away with it
(premise 2). That is the
"logging level change" his message names. One line per naming call stays
at **debug**, holding the words and the field's handle, because it is what
ties a field a scan reads to the call that named it, and debug is where
this project writes per-item outcomes during a testing round
(`logging.rs:47-67`). The workflow keeps printing and uploading the
program's log.

The date is 2026-09-24, the day of his message (D-05).
</pratiks_exception>

<decisions>
Settled 2026-09-24 on Pratik's answers. None is open, and none waits on
him.

**D-02. "See if you can get around the bug" is not about this plan.** He
said it beside "Use phonenumber", about that library's defect, for 12-07.
It permits no workaround here and is not read as permission for one. A
cause inside Windows' annotation service (rows E and F) is still in scope
as ordinary engineering: a failing test that reproduces it first, then the
fix, like any other fix. Guardrail 9 says not to absorb an upstream failure
silently, so the ledger entry for such a fix names the platform behaviour
it works around and says the mechanism is not explained (task 4).

**D-03. The control switch is part of the diagnostic instrument.**
`WIXEN_DIAGNOSE_NO_READBACK`, the environment variable that turns the
read-back off for the Send Later target, falls under D-01. It is removed
with the other diagnostic lines in task 3's first commit, from the program
and from the workflow.

**D-04. What stays after the fix.** The three failure lines stay at warn,
and one debug line per naming call stays, holding the words and the
field's handle. That is what Pratik approved: "Split it + followup with
logging level changes that you recommended".

**D-05. The split's date is 2026-09-24**, the day of his message.
`STATE.md:48` said 2026-09-23, and the commit that landed this plan
corrected it. Commit `2b651cb3`, made at 01:27 EDT on 2026-09-24, says
2026-09-23 and is left as it is; so is 12-06's summary, written in that
commit, which says 2026-09-23 at `:72`, `:85` and `:169`.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `01dfbe28` (12-06 merged, its split
commit `2b651cb3` before it), clean but for another agent applying the
12-07 and 12-09 amendments. Command first, then what it said. The copies
named below (run 35930014324's log and artifact, the planning files as
`main` held them) were kept under `evidence/` beside this plan in the
planning session's scratchpad, which does not outlive it; take them again.

**1. What the scan read, from its own record.** Both runs said the same;
the second, 35930014324, was read in full.

```
gh run view 35930014324 --json headSha,event,conclusion   -> 83f394d8, pull_request, success (the job is continue-on-error)
gh run view 35930014324 --log | grep 'Build application' | grep -E 'Compiling (wixen-mail|windows )|Dirty wixen-mail|Finished'
   -> "Compiling windows v0.62.2"; "Dirty wixen-mail v1.0.0-alpha.1 ... the file `build.rs` has changed"; "Compiling wixen-mail v1.0.0-alpha.1"; "Finished `release` profile ... in 3m 34s"
   So the scanned binary held 12-06's code and the three new features: a stale build is ruled out for that run.
gh run view 35930014324 --log   (the scan step, per target)
   -> new-event: 9 violations, "MSAA walk: 3483 elements, 2012 of them operated, 8 without a name"
   -> send-later: 6 violations, 4 without a name; settings: 0 and 0; account-editor: 0 and 0
   -> new-event Error 1: ClassName Edit, NativeWindowHandle 787186, ProviderDescription "[pid:8856 ... Main:Nested [pid:5660 ... Annotation(parent link):Microsoft: Annotation Proxy (unmanaged:uiautomationcore.dll); Main:Microsoft: EDIT Proxy ...]"
gh run download 35930014324 -n accessibility-scan-results, then Python's json over each msaa-names.json (role 42 editable text, role 52 spin button, the dialog's own paths only)
   -> send-later: four editable text named ""; spin buttons "Send on Day", "Send on Year", "Send at Hour", "Send at Minute"
   -> settings: editable text "Font size:"; spin button "Font size"
   -> account-editor: fields "Check Interval (min):" and "Then remove it after this many days (0 for never):"; spin buttons "Check Interval (min)," and "Then remove it after this many days (0 for never),"
   -> new-event: eight editable text named ""; twelve spin buttons named, "Starts Day" to "How many times"
Python's zipfile and json over each .a11ytest (a zip whose el.snapshot is every element's UI Automation properties, keyed by property id: 30005 Name, 30012 ClassName, 30020 NativeWindowHandle)
   -> send-later Edits 1245872, 1770146, 1376932, 1573554: Name None; settings 131746: "Font size:"
   -> account-editor: only the four Edits of the page the target shows (IMAP and SMTP server and port); the two spin fields are not in the snapshot
   -> new-event: 787186 None, 1508064 None, 787176 "Start time", 983702 None, and the rest
```

What that settles. The arrows carry the words, and in Send Later only
`name_the_spin_control` writes them (`wx_send_later.rs:157-160`), so the
helper ran in the program and its first half landed; its second half did
not, on either channel. UI Automation asked the annotation proxy running
inside the program's own process (pid 5660) for the field's name and it had
none. And UI Automation for the account editor's two spin fields is read by
no scan, because Axe records only the page the target shows; MSAA reads
them.

**2. The program's log already held one answer, and nothing kept it.**

```
grep -n 'fn default_level_for\|fn filter_for' src/common/logging.rs   -> :61 debug while the version is alpha or beta; :75 "wixen_mail={level}"
grep -n 'fn logs_dir\|fn resolve_with' src/common/paths.rs   -> :118 root/logs; :85 the root is WIXEN_MAIL_DATA when set
grep -n 'WIXEN_MAIL_DATA\|upload-artifact\|path:' .github/workflows/accessibility.yml
   -> the scan sets WIXEN_MAIL_DATA to RUNNER_TEMP/a11y-profile; the one upload step takes a11y-results/ only
grep -n 'tracing::debug' src/presentation/accessibility/names.rs   -> :327 the service not available, :347 no field, :367 a field that could not be named
```

So at `1.0.0-alpha.1` the program in the scan logged at debug under
`$RUNNER_TEMP/a11y-profile/logs/wixen-mail.YYYY-MM-DD.log`, and a failure
to create the service or to write the name was in that file on both runs,
and was discarded with the runner. The workflow kills each target's
process with `Stop-Process -Force`, and the log's writer runs on a worker
thread, so a line written in the last moments can be lost; the naming
lines are written when a dialog is built, at least three seconds before the
kill (the workflow's own `Start-Sleep -Seconds 3` and then the whole Axe
scan).

**3. COM on the naming thread, read and not found wanting.**

```
grep -n 'wxOleInitialize' target/debug/wxWidgets/src/msw/app.cpp   -> :163, inside wxOleInitModule::OnInit (:152-172), a module initialised on the thread that runs wxEntry
sed -n 40,58p target/debug/wxWidgets/include/wx/msw/ole/oleutils.h   -> OleInitialize(nullptr); RPC_E_CHANGED_MODE counted as success
grep -n 'define wxUSE_OLE \|define wxUSE_ACCESSIBILITY ' target/release/wxwidgets_cmake_build/lib/vc_x64_lib/mswu/wx/setup.h   -> :659 wxUSE_OLE 1; :520 wxUSE_ACCESSIBILITY 1
grep -n 'open_for_scanning(' src/presentation/wx_app.rs   -> :6492 the call, inside the wxdragon::main closure; :17240 the function
grep -n 'wxdragon::main(move' tests/every_spin_control_names_the_field_a_person_types_in.rs   -> :855, the test's harvest inside wxdragon::main too
git grep -n 'CoInitialize\|CoUninitialize\|OleInitialize\|RoInitialize\|COINIT_' -- src   -> windows_speller.rs:77-81 only: an STA, and it never calls CoUninitialize (:62)
grep -n 'COINIT\|CoUninitialize' ~/.cargo/registry/src/*/cpal-0.17.3/src/host/wasapi/com.rs   -> :17 an STA; :44-47 uninitialises only what it initialised, at thread exit
grep -n 'WIXEN_NO_AUDIO' .github/workflows/ci.yml .github/workflows/accessibility.yml   -> ci.yml:20 only
sed -n 316,329p src/presentation/accessibility/names.rs   -> the service created once per thread and a failure kept as None for the rest of the thread's life, said at debug
```

wxWidgets 3.3.2 joins OLE, an STA, on the thread that runs `wxEntry`,
before any window, in the program and in the test alike, and the scan
target's dialog is built on that thread inside the same closure shape the
test uses. Nothing in `src` leaves an apartment. The one process-level
difference the reading found is audio: the scan runs the program without
`WIXEN_NO_AUDIO`, so it opens the sound device path CI's Test Suite never
opens; no link from that to the annotation service was found. So
hypothesis (a) has no mechanism on the page and is not ruled out, and a
failure would have been silent for the thread's whole life.

**4. Every green reading from another process followed a read in the test
process.**

```
sed -n 369,395p tests/every_spin_control_names_the_field_a_person_types_in.rs   -> read_the_spinner reads the field in-process on MSAA (msaa_of, :277) and on UI Automation (uia_name_of, :328, ElementFromHandle)
sed -n 465,484p tests/every_spin_control_names_the_field_a_person_types_in.rs   -> the_spinners_under reads every spinner so, and only then starts the other process (:473)
```

The program never reads its own fields. If a read inside the process that
wrote the annotation is what makes it visible to others, 12-06's target
could never show the program's condition, and the diagnosis's own read-back
could hide the fault in the very run meant to find it. Hence the control
target in the contract.

**5. The field in wxWidgets.**

```
grep -n 'MSWCreateWindowAtAnyPosition\|wxSetWindowProc(GetBuddyHwnd\|DestroyWindow( GetBuddyHwnd\|^wxBuddyTextWndProc\|^bool wxSpinCtrl::Reparent' target/debug/wxWidgets/src/msw/spinctrl.cpp
   -> :310 the field created once, in Create; :343 subclassed with wxBuddyTextWndProc; :392 destroyed with the control
sed -n 85,141p target/debug/wxWidgets/src/msw/spinctrl.cpp   -> forwards focus, key, char and help messages to the spin control and adjusts WM_GETDLGCODE; never WM_GETOBJECT
sed -n 617,661p target/debug/wxWidgets/src/msw/spinctrl.cpp   -> Reparent recreates the arrows and keeps the field
```

No toolkit path replaces the field after it is named, and wxWidgets never
answers for it: the field's accessibility requests reach the native edit's
own procedure in both processes.

**6. How the scan reads, against how the test reads.**

```
sed -n 162,200p scripts/msaa-names.ps1   -> AccessibleChildren from each window's client object down, so a field is reached as the child of its window object
sed -n 262,270p scripts/msaa-names.ps1   -> AccessibleObjectFromWindow(top-level window, OBJID_CLIENT) for the root
sed -n 277,283p tests/every_spin_control_names_the_field_a_person_types_in.rs   -> AccessibleObjectFromWindow(field, OBJID_CLIENT) directly
```

Axe walks UI Automation's tree; the test asks `ElementFromHandle`. For UI
Automation the element Axe recorded for 787186 carries the same provider
chain a handle read gets (premise 1), so the difference in path explains an
MSAA miss and not a UI Automation one.

**7. The gate and the records.**

```
sed -n 432,445p scripts/which-checks.sh; sed -n 526,535p scripts/which-checks.sh   -> a Cargo.toml change other than the package's version, and any .github/workflows/* change, answer all
grep -n 'fn test_the_command_line_and_the_workflow_use_the_same_flag\|fn test_the_workflow_asks_for_every_target\|fn test_the_workflow_asks_whether_the_application_is_still_running' src/presentation/scan_target.rs   -> :366, :391, :505 read accessibility.yml
python: tomllib over guards/guards.toml, both readings CLAUDE.md asks for, for every file this plan edits (records_touched.py beside this plan in the planning scratchpad)
   -> 1,058 records at 01dfbe28
   -> names.rs: 1, "every spin control's typing field is named through the annotation service", named by file and anchored in it on `for (which, words) in carries {` with the SAFETY comment under it; tests_last_seen names.rs 12 and the spin target 16
   -> tests/every_spin_control_names_the_field_a_person_types_in.rs: 3 by tests_last_seen (that one; "the event form's minute spinner is named on its typing field too", file wx_item_form.rs; "the check interval spin control holds 1 to 60 minutes as its own range", file wx_account_manager.rs); none anchored in it
   -> accessibility.yml: 3, each named and anchored in it: "a window the program can open for the scan is one the workflow asks for" on the targets line holding 'which-days'; "an editor the program can open for the scan is one the workflow asks for" on the line holding 'contact-editor'; "the scanner's checksum is compared with a number written down and not with itself" on the $expected line
   -> Cargo.toml: 0; scripts/msaa-names.ps1: 0 (not edited); the new target: 0
grep -c '#\[test\]' tests/every_spin_control_names_the_field_a_person_types_in.rs   -> 16
```

Take the count again with the parser before the first commit; other plans
write records between now and then. The edits here leave all four anchor
lines verbatim; if one must move, the record is rewritten and re-measured
in the commit that moves it.

**8. The typed read-back needs two features the tree does not switch on.**

```
sed -n 1634,1635p ~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/UI/Accessibility/mod.rs   -> IAccessible::get_accName is cfg(Win32_System_Ole, Win32_System_Variant)
grep -n '"Win32_' Cargo.toml   -> eleven, neither of those two among them
grep -n 'pub unsafe fn CoGetApartmentType' ~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/System/Com/mod.rs   -> :209, under Win32_System_Com, already on
```

Features of the crate already in the tree, not a crate: `Cargo.lock` does
not move. `GetCurrentThreadId` (Win32_System_Threading), `IsWindow`,
`GetClassNameW` and `GetWindowLongPtrW` (Win32_UI_WindowsAndMessaging) are
already on.

**9. The planning files count the plans.**
`tests/the_planning_files_agree_with_themselves.rs:344-392` holds
`STATE.md`'s `Total Plans in Phase` to the phase's `*-PLAN.md` files and
`:661-750` the roadmap row's denominator to the same, so this plan lands
with the amendments that move both to 16 (`12-06.1-amendments.md`).
</premise_corrections>

<hypotheses_ranked>
Ranked by reading, before any diagnosis. The run in task 2 decides; this
order only says which rows the diagnosis must be able to tell apart.

1. **(a) The service, or its write, fails in the program's process.** The
   only hypothesis that fits both channels failing in every dialog while
   UI Automation's annotation proxy, running inside the program (pid 5660),
   found nothing for 787186 (premise 1). Against it: wxWidgets joins an STA
   on the naming thread before any window (`app.cpp:152-172`,
   `oleutils.h:40-58`, `setup.h:659`) and nothing in `src` leaves an
   apartment (premise 3). For it: a failure would be silent, at debug, and
   kept for the thread's life (`names.rs:316-329`), and the scan's program
   opens an audio path the tests never do (`ci.yml:20`).
2. **(f) A read inside the writing process is what carries the name out.**
   Found here, not in the brief. Every green reading in 12-06's target read
   the field in-process on both channels before its other process read it
   (`every_spin_control...rs:369-395`, `:473`); the program never reads its
   own fields. Consistent with every observation so far; no documented
   mechanism.
3. **(c) The field read is not the field named.** No wxWidgets path replaces
   the field after it is named (`spinctrl.cpp:310`, `:343`, `:392`; Reparent
   `:617-661` keeps it), and the program's scan targets build through the
   same builders the test calls (`wx_app.rs:17318`, `:17465`, `:17635`).
   Settled cheaply by comparing the logged handle with the snapshot's
   `NativeWindowHandle`.
4. **(e) The scan reads by a path the test does not.** The MSAA walk reaches
   the field through `AccessibleChildren` from the dialog's root
   (`msaa-names.ps1:162-200`, `:265`); the test asks for the field's own
   object (`:277-283`). Explains an MSAA miss; does not explain the UI
   Automation miss, whose element had the same provider chain a handle read
   gets.
5. **(b) The program builds these dialogs through a path that skips the
   helper.** Ruled out for run 35930014324: Send Later's arrows answered the
   words only `name_the_spin_control` writes (`wx_send_later.rs:157-160`), so
   the helper ran. The 13 builder sites against 15 calls in 12-06's summary
   are construction sites against naming sites (deviation 4 there), not a
   missed path. Kept as a row so the table is whole.
6. **(d) wxWidgets answers for the field.** Ruled out by reading:
   `wxBuddyTextWndProc` (`spinctrl.cpp:85-141`) never handles `WM_GETOBJECT`,
   in either process.
</hypotheses_ranked>

<diagnostic_contract>
Every line starts with the prefix `spin-field-naming:` so one grep finds
them in a log and in the job log. Handles are written in decimal, as Axe
writes `NativeWindowHandle`. HRESULTs as `0x` and eight hex digits. Never
written: a field's value, a window's title, anything from a message or an
account; the words are the helper's own fixed names.

**While diagnosing (task 1), at warn,** in `typing_field` (the Windows arm
only), numbered per thread by a counter `n`:

- `call=<n>`: the words; `GetCurrentThreadId`; the apartment from
  `CoGetApartmentType` (type and qualifier, or its HRESULT when it fails);
  the arrows' handle; `UDM_GETBUDDY`'s answer (the field's handle, or 0);
  the field's window class (`GetClassNameW`); whether this call created the
  service, reused it, or found it absent; the creation HRESULT on the call
  that tried.
- `write=<n>`: per property written, `name` or `description`, and the
  HRESULT.
- `readback=<n>`: right after the writes, the field's MSAA name read inside
  the program through `AccessibleObjectFromWindow(field, OBJID_CLIENT)` and
  the typed `IAccessible::get_accName` for `CHILDID_SELF`, and whether it
  equals the words; or the HRESULT; or `skipped` under the control switch.
- `at-show=<n>`: from a closure queued with `wxdragon::call_after` at the end
  of the naming call, holding only plain handles, the words and `n`, which
  runs from the dialog's own loop once it is up: whether the arrows are
  still a window, `UDM_GETBUDDY` now and whether it is the handle named,
  whether the handle named is still a window, whether its window procedure
  (`GetWindowLongPtrW` with `GWLP_WNDPROC`) is the one it had when named, and
  its MSAA name now, or `skipped` under the control switch.

**The control switch** is the environment variable
`WIXEN_DIAGNOSE_NO_READBACK`, read once per process. When it is set the
program makes no MSAA read at all: neither the read-back nor the check at
show reads a name. The workflow sets it for the `send-later` target only.
Why: premise 4. If the read-back is what carries the name out, the three
targets that read would come back named and the one that does not would
not, and a run without the control could not tell a fix from a
side effect.

**Once run 1 is read (task 3's first commit, before the red test):**

- kept at **warn**: `service-failed` (the HRESULT and the apartment, when the
  service cannot be created), `write-failed` (the property, the HRESULT and
  the field's handle), `no-field` (the arrows' handle and the words, when
  `UDM_GETBUDDY` answers 0).
- kept at **debug**: `named` (the words and the field's handle), one per
  naming call.
- gone: the `call=`, `write=`, `readback=` and `at-show=` lines, the control
  switch in the program and in the workflow, and the two features. If the
  row's fix reads a name back inside the program (row F), that read and the
  two features return in the fix's green commit, with the fix's reason
  beside them in `Cargo.toml`, and without the diagnosis's log lines.
- the workflow keeps its print step and its upload.

If a failed service is no longer kept for the thread's life (row A's fix),
`service-failed` can repeat once per naming call; that is bounded by the
spin controls in one dialog, twelve at most (the event form).
</diagnostic_contract>

<branch_table>
Read by task 2 from run 1, per scan target that holds spin controls
(new-event, send-later, settings, account-editor; any other target whose
walk shows spin buttons is read the same way). Three sources: **L** the
program's log lines for that target (the lines after its "Opening <target>
for the accessibility scan" line), **M** its `msaa-names.json`, **U** its
Axe snapshot by `NativeWindowHandle`. The rows are tried in this order and
the first whose rule holds for a target is that target's row.

**Row 0. The diagnosis did not reach the run.** The build step shows no
`Compiling wixen-mail`, or the program-log artifact is missing or empty, or
a target whose walk shows spin buttons has no log lines at all (no Opening
line), or has an Opening line and no `call=` lines. Those last two are told
apart, with both counts quoted per target, because they point at different
plumbing: no Opening line means the log does not hold that target's
process at all (never written, lost at the kill, or in another day's file),
and an Opening line with no `call=` lines means the target opened and no
naming line reached the log. Not a finding. Fix the plumbing on the branch
under D-01, push, and read the new run as run 1. Once; a second miss stops
at the checkpoint.

**Row A. The service or its write fails in the program (a).** In L, a call
line with the service absent or a creation HRESULT other than S_OK, or an
apartment that is not an STA (STA or main STA); or a write line with an
HRESULT other than S_OK.
- Red, service: a `names.rs` case on a fresh `std::thread` that has joined
  no apartment: asking for the service fails with the HRESULT L shows,
  asserted by value and not only as a failure. `CO_E_NOTINITIALIZED`,
  `0x800401F0`, is what a thread in no apartment is given; if L shows
  another, the case asserts that one. The same thread then joins an STA and
  asks again and must be given one. Red today, because the thread keeps its
  first failure (`names.rs:316-321`), and the failure line the summary
  quotes shows the HRESULT. And in the new target, the harness puts its
  window thread in the state L shows before `wxdragon::main` (the apartment
  L names, joined with `CoInitializeEx` first, which wx's own
  `OleInitialize` tolerates, `oleutils.h:40-58`), then builds, names and
  has the other process read: red.
- **If the service red comes back green**, the fresh thread was given a
  service, and that means another thread in the test process has joined
  the multithreaded apartment: COM counts a thread that has joined no
  apartment as an implicit member of it, and any test in the process can
  have made one. The case is then no reproduction and is not committed as
  a red. Pin the red instead to a forced failure. The creation step becomes
  a function the thread's cache is handed, a seam that goes into the red
  commit with the behaviour unchanged so the case compiles and runs. The
  case hands it one that answers the HRESULT L shows on its first call and
  calls `CoCreateInstance` on its second, on a thread that has joined an
  STA, and asserts that the first answer carries that HRESULT by value and
  that the second ask is given a service. That is red today for the same
  reason, whatever apartment the rest of the process is in. The new
  target's case is treated the same way: green there means it is not a
  red, the apartment its window thread reported is quoted in the summary,
  and the forced failure carries the red alone.
- Red, write: the new target makes the same call with the arguments L shows
  and reads from the other process: red, with the write's HRESULT from L
  asserted by value.
- Fix: `carry` joins an apartment itself when the thread has none, as
  `windows_speller::ensure_com_initialised` does (`windows_speller.rs:74-90`),
  and no longer keeps a failed service for the thread's life; a write's
  argument corrected to what the red test shows the service accepts. If L's
  apartment is a multithreaded one and nothing inside `carry` turns the red
  green without changing the apartment the program's interface thread runs
  in, stop at the checkpoint: that apartment is a decision about the whole
  program (OLE drag and drop and WebView2 want an STA).
- If every write is S_OK in an STA and the read-back on a reading target is
  not the words, the store refused a write it said it took, and nothing
  here reproduces that: stop at the checkpoint.

**Row B. A path names the arrows without the helper (b).** In M the spin
buttons carry words for which L holds no call line. Red: the new target
opens the window by the scan target's own opener rather than by the
builder, and the other process reads: red. Fix: that path goes through
`name_the_spin_control`.

**Row C. The field read is not the field named (c).** In L an at-show line
says the arrows' field is now another handle, or the handle named is no
longer a window, or its window procedure changed; or in U an Edit with no
name, or with a label's name, whose handle is not among the target's
logged field handles while M shows the target's spin buttons named. Red:
the new target takes the step the at-show line and the handles place
between naming and reading (a show, a page change, a reparent, whatever L
shows) and then the other process reads: red. Fix: the field is named where
it is final, after that step, the red test deciding where.

**Row F. A read inside the program carries the name out (f).** In L every
creation and write is S_OK, every read-back on the reading targets equals
the words, every at-show line says the same live field and procedure, and
the handles are the ones U holds; and M and U show the words on the fields
of the targets that read inside the program (new-event, settings,
account-editor) and no name, or a label's name, on send-later's, the
control. Red: the new target's other process reads before anything in the
test process reads the field: red; its companion reads the same field
after one read in the test process: green, which is the measurement the
fix rests on. Fix: `carry` reads the field's name back once inside the
program after writing, as a named step of the fix and not as a diagnostic
line, with a doc comment citing run 1 and the red test, because that read
is what the scan showed carries the name to other processes. A ledger
entry names the platform behaviour the fix works around, a name written
through Windows' annotation service that other processes do not see until
the writing process has read it, and says the mechanism is not explained
(guardrail 9, D-02).

**Row E. Written and readable inside, unread outside (e).** Everything L
shows is as in row F, and M and U show no name, or a label's name, on the
fields of every target, the three that read inside the program and the
control alike. Red: the new target's reading taken as the scan takes it,
from the other process only, MSAA by the walk from the dialog's client
object and by the field's own object, UI Automation by the field's handle,
nothing read in the test process first: red. If that reading is green, the
test still cannot see what the scan sees: stop at the checkpoint with run
1's table and the green reading's output. Fix, the red test deciding, in
this order, stopping at the first that turns it green: (1) write UI
Automation's own Name property through the same service as well, its
property GUID `Name_Property_GUID` in place of an MSAA one, which
Microsoft's "Using Direct Annotation" page says the service accepts, the
MSAA name kept; (2) give the field an accessible object of its own that
answers `WM_GETOBJECT` for `OBJID_CLIENT` with the standard object's answers
and the words as its name (a subclass on the field, the way `wxAccessible`
answers for wx's own windows), released when the field is destroyed. The
second is written only if the first leaves the test red. As in row F, a
ledger entry names the platform behaviour the fix works around, a name
the annotation service holds for the field that other processes are not
given, and says the mechanism is not explained (guardrail 9, D-02).

**Mixed.** Different rows on different targets: each row's red goes into the
one red commit and each fix follows; the summary says which target took
which row.

**Checkpoint.** No rule holds for a target; two rules hold that cannot both
be true of one target; or the scan reads the words on every target, the
control included, so the fault did not appear in run 1. Stop: return
`## CHECKPOINT REACHED` with the per-target table, every row tried and why it
failed, and the one further line of evidence that would decide. No fix is
written.
</branch_table>

<tasks>

<task type="auto">
  <name>Task 1: the naming call's outcome at warn, the program's log out of the runner, and one scan run</name>
  <files>src/presentation/accessibility/names.rs, Cargo.toml, .github/workflows/accessibility.yml</files>
  <precondition>`git merge-base --is-ancestor 01dfbe28 main` succeeds (12-06 is merged), and `git rev-parse --git-dir` answers `.git` in the directory the work happens in (the primary checkout, not a linked worktree, whose answer is a path under `.git/worktrees/`).</precondition>
  <read_first>
    - `src/presentation/accessibility/names.rs:245-382`, the helper and both arms of `typing_field`.
    - `.github/workflows/accessibility.yml`, the scan step's loop (the `$targets` list, the profile, `Start-Process`) and the upload step.
    - `src/common/logging.rs:47-140` and `src/common/paths.rs:79-120`, for the level and where the file is written.
    - `src/service/spellcheck/windows_speller.rs:49-90`, the one place the program already joins an apartment itself, and how it reads each answer.
    - This plan's `<diagnostic_contract>` and `<pratiks_exception>` (D-01) in full.
  </read_first>
  <action>
    Not test-first, on Pratik's exception of 2026-09-24 (D-01) for the
    diagnostic lines only. Everything this task adds is on the contract's
    "while diagnosing" list and writes a log line or moves a log; if any
    line here would change what a person sees or hears, it is not covered
    and it waits for task 3's red.

    Branch from `main` in the primary checkout:
    `12-06.1-the-typing-field-in-the-running-program`.

    In `names.rs`, Windows arm of `typing_field`, add the contract's lines
    while diagnosing, at warn, with the prefix: in `the_service` the
    creation result, and in `carry` the call line, a write line after each
    `SetHwndPropStr`, the read-back and the queued check at show. Keep the
    `OnceCell` as 12-06 left it, so the diagnosis measures the code that
    shipped. Keep the two lines the names.rs record anchors on (premise 7)
    exactly as they are: add around them, never inside them. The read-back
    uses `AccessibleObjectFromWindow` with `OBJID_CLIENT` and the typed
    `IAccessible::get_accName`; add `Win32_System_Ole` and
    `Win32_System_Variant` to `Cargo.toml`'s `windows` list with a comment
    saying they are for 12-06.1's diagnosis and leave with it. The check at
    show is queued with `wxdragon::call_after` and holds plain handles and
    the words, never a widget. The control switch the contract names is
    read once per process (a `OnceLock<bool>`), and under it no MSAA read is
    made. No `unwrap` or `expect`; every failure is a line, never a panic.
    Invoke `elegant-code`: the diagnosis in one small private submodule of
    `typing_field`, each reading its own function, so task 3 can remove it
    by module.

    In `accessibility.yml`: inside the scan step's loop, just before
    `Start-Process`, set the control switch for the `send-later` target and
    clear it for every other, with a comment dated 2026-09-24 saying it is
    12-06.1's control and leaves with the diagnosis. Leave the `$targets`
    lines and the `$expected` line verbatim (three records anchor there and
    three tests in `scan_target.rs` read the file, premise 7). Add a step
    after the scan, `if: always()`, that looks in
    `$env:RUNNER_TEMP/a11y-profile/logs` and says which of three things it
    found: no logs folder, a log with no naming lines, or the naming lines
    and the scan's Opening lines printed with `Write-Host` and counted. Add
    an upload step, `if: always()`, artifact `accessibility-scan-program-log`,
    path `${{ runner.temp }}/a11y-profile/logs/`, `if-no-files-found: warn`.
    The profile is the scan's throwaway one, so the artifact holds fixture
    data only; say so in the step's comment.

    Build and run the cheap checks first: `cargo build`, then the spin
    target and the `names::` cases (both must pass unchanged; the diagnosis
    changes no answer). One commit,
    `chore(12-06.1): log each naming call at warn and upload the program's log`,
    whose body quotes D-01 in Pratik's words with the date and says the
    commit is not test-first by that exception. The hook answers `all`
    (`Cargo.toml` and a workflow): read its mode line and stage lines and
    quote them; never piped, never `--no-verify`.

    Push the branch and open a pull request into `main` (standing OK),
    titled "12-06.1: the spin controls' typing field named in the running
    program", its body saying what the first run is for and that the
    diagnosis is temporary. Wait for the Accessibility run on the pull
    request to finish (`gh run list --branch <branch> --workflow
    accessibility.yml`, then `gh run watch <id>` in the background or
    `gh run view <id> --json status` until it reads completed); a run that
    has not finished is not read. Its id is run 1.
  </action>
  <verify>
    <automated>cargo test --test every_spin_control_names_the_field_a_person_types_in && cargo test --lib presentation::accessibility::names:: && gh run view "$RUN_1" --repo PratikP1/Wixen-Mail --log | grep -q 'spin-field-naming: call='</automated>
  </verify>
  <acceptance_criteria>
    - The commit's message carries D-01 in Pratik's words with its date, quoted from `git log -1 --format=%B`.
    - The hook's mode line for that commit reads `all`, quoted with its stage lines and total.
    - `grep -c '"Win32_' Cargo.toml` is 13 and `git diff main -- Cargo.lock` is empty.
    - `cargo test --test every_spin_control_names_the_field_a_person_types_in` passes with the same count as before the commit, both quoted; `cargo test --lib presentation::accessibility::names::` passes with 12, quoted.
    - `gh api repos/PratikP1/Wixen-Mail/actions/runs/$RUN_1/artifacts --jq '.artifacts[].name'` lists `accessibility-scan-results` and `accessibility-scan-program-log`.
    - The job log of run 1 holds a `call=` line after each of the Opening lines of `new-event`, `send-later`, `settings` and `account-editor`, the count per target quoted, and `readback=` lines that say `skipped` for `send-later` alone.
  </acceptance_criteria>
  <done>The running program says, in a log the scan hands back, what happened to every name it tried to write, and one scan run on the branch holds that record.</done>
</task>

<task type="auto">
  <name>Task 2: read run 1 and name the row</name>
  <files>none in the tree; the session scratchpad only</files>
  <read_first>
    - This plan's `<branch_table>` and `<hypotheses_ranked>` in full.
    - Premise 1, for what each artifact holds and how the planning session read it.
  </read_first>
  <action>
    Invoke `root-cause-investigator`. The ranking is a premise; the run
    decides it.

    Confirm the build compiled this branch: in `gh run view $RUN_1 --log`,
    the Build application step holds `Compiling wixen-mail`. If not, row 0.

    Download both artifacts into the session scratchpad, never the
    repository: `gh run download $RUN_1 -n accessibility-scan-results -D
    <scratch>/run-1/results` and `-n accessibility-scan-program-log -D
    <scratch>/run-1/log`. Before quoting any line from the program's log,
    read it for anything that is not fixture data or a fixed word of the
    program; the log belongs to the throwaway scan profile, and the check is
    still made.

    With the Write tool, write `join_the_scan.py` into the session
    scratchpad. It reads only, and prints only. Given a run directory
    holding `results/` and `log/`: it splits the program's log by the
    Opening lines into one list of prefix lines per target and reads their
    keys; reads each target's `msaa-names.json` (role 42 editable text and
    role 52 spin button, under the dialog's own title); reads each target's
    `.a11ytest` zip, whose `el.snapshot` holds every element's properties by
    UI Automation property id (30005 Name, 30012 ClassName, 30020
    NativeWindowHandle); and joins them, the MSAA walk by the words and the
    snapshot by the logged field handle. Before each target's table, in
    every mode, it prints two counts for that target from the program's
    log: its Opening lines and its `call=` lines (under `--verify`, its
    `named` lines, since task 3 takes the `call=` lines out before run 2).
    It tells two cases apart and says which in words: "no log lines at all
    for <target>" when the target has no Opening line, and "Opening line
    present, no call= lines for <target>" when it has one or more and none
    of the naming lines. Under `--row` both are row 0 and neither is a
    finding. It prints one table per target:
    the words, the field's handle, the thread and apartment, the service
    result, each write's result, the read-back, the check at show, the MSAA
    name, and the UI Automation name at the handle or "not in the
    snapshot". With `--row` it applies `<branch_table>`'s rules in order and
    prints the row for each target and for the run, exiting 0 when every
    target has one row and the rows can all hold, 2 when a target has none
    or two that cannot both hold, and 3 when the evidence is incomplete (a
    target's log lines, walk or snapshot missing, either of the two log
    cases above included, which it names as row 0 with the case), each said
    in words. With
    `--verify` (task 3) it exits 0 when every logged spin field of the four
    targets carries the words on MSAA and, wherever the snapshot holds its
    handle, on UI Automation, 1 when any does not, and 3 when incomplete,
    listing the fields the snapshot does not hold.

    Prove it can see the fault before trusting it: download run
    35930014324's `accessibility-scan-results` the same way and run the
    script over it; it must list Send Later's four fields as nameless on
    both channels and exit 3, since that run has no program log.

    Then run `--row` over run 1, read every table, and write the evidence
    and the row, per target, into the summary draft in the scratchpad (task
    4 moves it into the summary).

    If the script exits 2, or the tables show what no row describes, stop:
    return `## CHECKPOINT REACHED` with the tables, every row tried and why
    it failed, and the one further line of evidence that would decide. No
    fix is written; the branch and the pull request stay as they are.
  </action>
  <verify>
    <automated>python "$SCRATCH/join_the_scan.py" --row "$SCRATCH/run-1"</automated>
  </verify>
  <acceptance_criteria>
    - The Build application line holding `Compiling wixen-mail` for run 1 is quoted.
    - The companion over run 35930014324 is quoted: Send Later's four handles nameless on MSAA and UI Automation, "no log lines at all" for every target with both counts 0, exit 3.
    - For run 1, each target's Opening-line count and `call=` count are quoted, and any target short of either is named with which of the two cases it is.
    - Every target with spin controls has its table in the summary draft, and the row named for each with the rule it met; or the plan has stopped at its checkpoint with those tables.
  </acceptance_criteria>
  <done>The run has said which row the program is in, from the program's own log and the scanner's own record, or the plan has stopped and said what it saw.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: the diagnosis out, red on the program's condition, the fix, and the scan reading the names</name>
  <files>tests/a_spin_controls_field_is_named_where_the_scan_reads_it.rs, src/presentation/accessibility/names.rs, tests/every_spin_control_names_the_field_a_person_types_in.rs, Cargo.toml, .github/workflows/accessibility.yml, guards/guards.toml</files>
  <read_first>
    - The row task 2 named, in `<branch_table>`, and the summary draft's tables.
    - `tests/every_spin_control_names_the_field_a_person_types_in.rs:57-260` (the MSAA reader and its declarations), `:486-602` (the other process: how it is started, answered and read), `:846-889` (one `wxdragon::main` per process through a `OnceLock` holding a `Result`).
    - `scripts/msaa-names.ps1:137-200`, the walk the new reading takes.
    - `src/presentation/wx_send_later.rs:78-160` and `src/presentation/wx_item_form.rs:270-300`, the builders and the palette the program's scan targets pass.
    - `guards/guards.toml`, the record "every spin control's typing field is named through the annotation service", as the shape of the new one.
  </read_first>
  <behavior>
    - The new target builds Send Later through `wx_send_later::build_the_asking_dialog` and the event form through `build_item_form_dialog` with the stored configuration's palette, as the program's scan targets do, inside one `wxdragon::main` per process, with the harness in the condition the row names; then another process started by the test reads every spin control's field before anything in the test process reads it: on MSAA by the walk `scripts/msaa-names.ps1` takes (`AccessibleChildren` from the dialog's client object down to the editable text whose window, by `WindowFromAccessibleObject`, is the field) and by the field's own object, and on UI Automation by `ElementFromHandle` on the field. Each must answer the arrows' words. Red on the tree before the fix, for the reason the row names.
    - The row's own red, as `<branch_table>` gives it (for row A, the `names.rs` case on a thread that has joined no apartment, asserting the HRESULT L shows by value, or the forced failure in its place when that case reads green).
    - A companion: a bare `SpinCtrl` named on its arrows alone, read the same way, answers no name, so the reading sees the fault ledger 408 describes. For row F, a second companion: the same field read once in the test process first answers the words, which is the measurement the fix rests on.
  </behavior>
  <action>
    First the diagnosis comes out, under D-01, in one commit, before any
    test is written: reduce `typing_field` to what `<diagnostic_contract>`
    keeps once run 1 is read (the failure lines at warn, one debug line per
    naming call), and remove the diagnosis-only lines, the control switch
    from the program and from the workflow, and the two features. It goes
    first because the diagnosis reads the field inside the program, and the
    red below must read it from another process before anything in the
    writing process has (premise 4). `cargo build`, 12-06's target and the
    `names::` cases green, one commit,
    `chore(12-06.1): the diagnosis out, the failures kept at warn`; it
    stages a workflow and `Cargo.toml`, so its hook answers `all`, quoted.

    Red next, on the branch. Write the new target and the row's red; run
    them red for the reason the row names, and only that reason. One red
    commit: the new target's tests bare, a `names.rs` case by module path,
    and the count check beside them when `names.rs` gained a test (one
    record names it, premise 7), with the reason in the message; the
    gate's red mode must print "a red that is exactly the one this commit
    named", quoted. The new target reads exactly as the behaviour says and
    never in the test process before the other process: that order is the
    point of it.

    Then the fix the row gives, found against the red test and nowhere
    else: the first candidate that turns it green is the fix, and the
    candidates tried are listed in the summary. If the fix is a read inside
    the program (row F), it brings back the two features with its own
    reason beside them, and none of the diagnosis's log lines. Rewrite the
    doc comments that say the name does not reach the program
    (`name_the_spin_control`'s, 12-06's target's header and `Cargo.toml`'s)
    to say what run 1 showed, which row, and what the fix is, dated
    2026-09-24, and point 12-06's target's header at the new target as the
    reading that can see the program's condition. `cargo build`, the red
    tests green, 12-06's target and the `names::` cases still green. Green
    commit. The new record goes in this commit: its break takes the fix
    out, its `suite` is the new target, its red list is exactly the new
    target's naming tests, measured by hand first and then recorded by one
    `scripts/guards.sh --remeasure` call naming it and every record the
    count check or a moved anchor names (premise 7's names.rs record if its
    count or anchor moved, the three records naming 12-06's target if its
    count moved).

    Push. Wait for the Accessibility run on the pull request to finish; its
    id is run 2. Download both artifacts into `<scratch>/run-2` and run
    `join_the_scan.py --verify` over it. If it does not exit 0, stop at the
    checkpoint with runs 1 and 2's tables; the diagnosis can be restored
    from the branch's history for a further round, and whether to is
    Pratik's.
  </action>
  <verify>
    <automated>cargo test --test a_spin_controls_field_is_named_where_the_scan_reads_it && cargo test --test every_spin_control_names_the_field_a_person_types_in && cargo test --lib presentation::accessibility::names:: && python "$SCRATCH/join_the_scan.py" --verify "$SCRATCH/run-2"</automated>
  </verify>
  <acceptance_criteria>
    - Three commits in this order, each with its hook's mode line quoted: the diagnosis out (`all`), the red, the green.
    - The red commit's trailers and the gate's red-mode line are quoted; the red tests failed for the row's reason, their failure lines quoted.
    - The new target passes, its test count quoted; the companion that reads a bare spin control sees no name, quoted.
    - `cargo test --lib presentation::accessibility::names::` passes, its count before and after quoted; 12-06's target passes at its count, quoted.
    - `! git grep -q 'WIXEN_DIAGNOSE_NO_READBACK' -- src .github` succeeds.
    - `! git grep -q -e 'readback=' -e 'at-show=' -- src` succeeds.
    - `git grep -n 'tracing::warn!' -- src/presentation/accessibility/names.rs` shows the three failure lines, each quoted with the kind it says.
    - `git diff main -- Cargo.lock` is empty; `grep -c '"Win32_' Cargo.toml` is 11, or 13 with the fix's reason beside the two it keeps.
    - Run 2's `--verify` table is quoted in full: every logged spin field of new-event, send-later, settings and account-editor answers its words on MSAA, and on UI Automation wherever the snapshot holds its handle; the fields the snapshot does not hold are listed by their words.
    - One new guard record and every re-measured record quoted with what went red.
  </acceptance_criteria>
  <done>A test that reads the field the way the scan does was red on the program's condition and is green on the fix, and the scan of the running program on the pull request hears every spin control's field by its name on both channels.</done>
</task>

<task type="auto">
  <name>Task 4: the changelog, the guide, the ledger, EDIT-01, the summary, the merge and the two issues</name>
  <files>docs/changelog.md, docs/USER_GUIDE.md, docs/development/measurements.md, .planning/WINDOWS.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06.1-SUMMARY.md</files>
  <read_first>
    - `docs/changelog.md`, `[Unreleased]`, the entry "Numbers are spin controls." and its Known limitations as `2b651cb3` wrote them (`git show 2b651cb3 -- docs/changelog.md`).
    - `docs/USER_GUIDE.md`, the paragraph `2b651cb3` wrote beginning "The field you type in does not have a name of its own yet" (`git show 2b651cb3 -- docs/USER_GUIDE.md`).
    - `.planning/WINDOWS.md`: the head, entries 408 to 425 and 592 and 593 in both halves.
    - `.planning/REQUIREMENTS.md`: EDIT-01 with its "Not ticked" paragraph, its `[D]` lines (three from 12-06, three from 12-06.1), its `[S]` line and its traceability row.
  </read_first>
  <action>
    Invoke `writing-craft`. Changelog, `[Unreleased]`, `### Fixed`: the
    field a person types in beside a spin control's arrows now carries the
    arrows' name in the running program, on both channels, in Settings, the
    account editor, Edit Event, Send Later and Insert Table; what somebody
    met before (nothing, or the label before the field with its colon);
    what was wrong, in a sentence a person can follow, from the row; that
    the scan of the running program proved it and not only a test. The
    Known limitations sentence `2b651cb3` wrote is corrected by dating, not
    deleted, and a new one says nobody has heard the field's name with a
    screen reader since (ledger 592). The guide's paragraph is corrected the
    same way. `docs/development/measurements.md` gains a row only if a
    figure is taken, with its date and command; the summary says whether one
    was.

    Ledger, by hand, both halves, the head re-counted with `head -7` before
    and after: 408, 409, 410, 412, 413, 414, 419, 420, 421, 422, 424 and 425
    fixed, each with `resolved_at` and a reason naming run 2 and the green
    commit (the merge commit does not exist yet; the report gives it); 593
    fixed, its reason naming the new target and the red commit, since the
    reading now sees what the scan sees. New entries for what the run
    leaves: `todo` for UI Automation on any spin field no snapshot holds
    (the account editor's two, if run 2 confirms it), and for rows E and F
    a `deviation` naming the platform behaviour in Windows' annotation
    service the fix works around and saying it is not explained (D-02), for
    Pratik to decide whether it is reported upstream. 592 stays open for the
    ear.

    EDIT-01: tick 12-06.1's three `[D]` lines with the date and the names
    that meet them (the commit, the target and its tests, the two runs);
    the "Not ticked" paragraph gains a dated sentence saying the naming half
    is met and how; the box is ticked only if every `[D]` line then has its
    names; the traceability row says the same. The `[S]` line stays.

    The summary, from the template: the tables from task 2 and run 2, the
    row and its evidence, the red and green commits with their hook lines,
    runs 1 and 2 with their ids, the records, the exception set for scripted
    edits (zero, through the detour as well as the work), D-01 quoted, and
    the deviations. One documents commit, before the merge.

    Merge with `git merge --no-ff` into `main` from the primary checkout; its
    hook answers `all`, because the branch's diff holds a workflow file and
    `Cargo.toml`; quote its mode line and stage lines. If it refuses on the
    keyring race of ledger 374, `git commit --no-edit`; any other refusal is
    `git merge --abort`, a fix on the branch, and the merge again. `main` is
    not pushed. Then one commit on `main` touching only the summary, adding
    the merge commit and its mode and stage lines; its hook answers
    `docs_only`. Then the four completion marks, named in `<constraints>`.
    Then `gh issue close 73 --comment "..."` and `gh issue close 35 --comment
    "..."`, each quoting the merge commit and run 2 in the issue's own terms;
    closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - `head -7 .planning/WINDOWS.md` before and after, quoted: `fixed_count` at least thirteen higher and `open_count` at least thirteen lower, less the entries this task opens, each counted and named.
    - The twelve entries and 593 read `fixed` in both halves with the same `resolved_at`, quoted by id.
    - `grep -c '^- \[x\] 12-06.1-PLAN.md' .planning/ROADMAP.md` is 1; `STATE.md`'s `current_plan`, `Current Plan:` and the Current Position sentence agree, quoted; EDIT-01's three 12-06.1 `[D]` lines ticked with their names, quoted.
    - `gh issue view 73 --json state --jq .state` and the same for 35 read `CLOSED`.
    - Carriage returns 0 on every file touched, taken with `tr -cd '\r' < FILE | wc -c` per file and quoted; no em or en dash; none of the six words.
  </acceptance_criteria>
  <done>The record says what the running program did, why, what fixed it and what proved it, and the issues are closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the program to its own log | diagnostic lines written where a tester's Send Feedback report attaches an excerpt by default |
| the runner's log to a public artifact | the scan profile's log uploaded from a public repository's workflow |
| the environment to the program | a variable the program reads that turns reads off |
| a field to its accessible object | a fix that writes, or answers for, a window wxWidgets created and does not own on our behalf |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06.1-01 | Information disclosure | the diagnostic lines in a tester's log | medium | mitigate | the contract fixes every field a line may hold: handles, HRESULTs, the apartment, the thread id and the helper's own words; never a value, a title, message text or an account detail; after task 3 only failures at warn and one debug line per naming call remain |
| T-12-06.1-02 | Information disclosure | the program's log as a workflow artifact | medium | mitigate | the upload takes `$RUNNER_TEMP/a11y-profile/logs` only, the scan's throwaway profile holding fixture data; the executor reads the lines before quoting them |
| T-12-06.1-03 | Denial of service | warn lines on every naming call | low | mitigate | diagnosis-only lines removed in task 3; after it, warn only on failure, bounded by one dialog's spin controls (twelve at most) |
| T-12-06.1-04 | Tampering | the control switch left in the program | low | mitigate | removed in task 3; `! git grep -q` over `src` and `.github` in its acceptance |
| T-12-06.1-05 | Denial of service | a read inside the program blocking the interface thread | low | mitigate | `AccessibleObjectFromWindow` on a window of the calling thread is a direct call, not a cross-thread wait; no UI Automation client call is made inside the program |
| T-12-06.1-06 | Elevation of privilege | row E's second candidate answering `WM_GETOBJECT` for the field | medium | mitigate | written only if the first candidate leaves the red test red; the object is released when the field is destroyed and every answer but the name is the standard object's, both held by the red test's readings |
| T-12-06.1-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate added; two features of the `windows` crate already in the tree for the diagnosis, removed with it unless the fix needs them; `Cargo.lock` unchanged, checked in tasks 1 and 3 |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the commits staging `Cargo.toml` or the workflow answer `all` on the branch, and the merge with `git merge --no-ff` answers `all` because the branch's diff holds both; the phase's full gate is 12-12's, and `scripts/check.sh all` is never run by hand here. After the merge, one commit on `main` touching only the summary adds the merge's mode and stage lines (12-03.2, task 7).
- One `scripts/guards.sh --remeasure` call in the green commit; every record it names quoted with what went red.
- What a test proves here: the program's condition, reproduced red and green on the fix, read the way the scan reads. What the runner proves: the names on both channels in the running program, run 2. What only the tester settles: a spin control's field heard once with its name under NVDA (ledger 592).
- NVDA not stopped, reconfigured or driven; no `nvda-tests` run here; the release and installed binaries never started; the tester's profile not read; `main` not pushed.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 6 holds in the running program: the twelve entries close on the scan of the running program, as the criterion's amendment of 2026-09-24 asks.
- EDIT-01's three 12-06.1 `[D]` lines ticked with their names; the requirement's box ticked only if all six `[D]` lines have theirs; the `[S]` line untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and EDIT-01's lines and traceability row.
- The summary reports the exception set for scripted edits, zero, through the diagnostic detour as well as the work, and quotes D-01 in Pratik's words.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06.1-SUMMARY.md` when done
</output>
