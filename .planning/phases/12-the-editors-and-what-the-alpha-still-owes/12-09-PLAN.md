---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 9
type: execute
wave: 10
depends_on: [12-08]
files_modified:
  - src/application/signatures.rs
  - src/application/mod.rs
  - src/data/message_cache/mod.rs
  - src/data/message_cache/signatures.rs
  - src/presentation/managers.rs
  - src/presentation/wx_managers.rs
  - src/presentation/wx_account_manager.rs
  - src/presentation/wx_compose.rs
  - src/presentation/wx_app.rs
  - tests/a_signature_follows_the_from_account.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [EDIT-04]
estimate:
  tokens: 29000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Signatures are one set: the Signature Manager lists every signature whichever account is active and says for each which accounts use it; each email account can be given one on its own edit dialog; one signature is the default and is used for any account with no assignment (EDIT-04, #43)."
    - "Compose starts with the From account's assigned signature, else the default, else none, and when the From account is changed the signature block changes with it as long as the person has not edited it."
    - "The schema change is additive: the account_id and is_default columns stay, an assignment table is added, and a signature stored under an account before this build is that account's assignment, by a once-only pass under a marker; the version does not move, because no build has been cut since 1.0.0-alpha.1."
  artifacts:
    - "src/application/signatures.rs: which_signature(assigned, default_for_all) and whether_to_swap(body, previous, next), pure, with cases; src/data/message_cache/signatures.rs: get_every_signature, assign, assignment_for, set_the_default, signature_for_account, the once-only pass; mod.rs: the signature_assignments table and the marker."
    - "wx_managers.rs and managers.rs: the manager over the whole set with an Accounts column and one Default box meaning for everyone; wx_account_manager.rs: the Signature choice on the edit dialog; wx_compose.rs and wx_app.rs: the From change swapping the block."
    - "tests/a_signature_follows_the_from_account.rs: the store's resolution order against a cache built in the test, the pass over rows written before, the manager's rows, the account dialog's choice, the swap on a From change over the built composer."
  key_links:
    - "the From account -> signature_assignments -> else the default -> sign_off::attach: one resolution, read by compose and by the manager's Accounts column."
    - "rows written before this build (is_default per account) -> the once-only pass -> an assignment per account and one default: nothing an account had changes."
---

<objective>
Signatures assignable per account, with one default for the rest.

Purpose: #43, the tester on 2026-09-15: "Allow signatures to be assigned
by email account. Default should apply if no signature is assigned to a
particular account." The tree has the other shape: signatures live only
under the account active when they were written, the manager shows one
account's set without saying so, a default is per account, and an account
with none gets none.

Output: the resolution as a pure rule, the assignment table and the pass,
the manager over one set, the choice on the account dialog, compose
following the From account, the reading, the guide, the changelog, the
ledger. Closes #43 on the tree side.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`. Command first, then what
it said.

**1. The store and its readers.**

```
grep -n 'CREATE TABLE IF NOT EXISTS signatures' -A 9 src/data/message_cache/mod.rs   -> :1830: id, account_id NOT NULL, name, content_plain, content_html, is_default, created_at, UNIQUE(account_id, name); the issue's :1791 moved
grep -n 'pub fn ' src/data/message_cache/signatures.rs   -> create_signature :9 (clears other defaults for the same account), get_signatures_for_account :32, get_signature :60, get_default_signature :87, update_signature :118, delete_signature :147; nothing reads across accounts
grep -cE '^\s*#\[(tokio::)?test\]\s*$' src/data/message_cache/signatures.rs   -> 2, named by 0 records: cases added here move no record
grep -n 'get_default_signature' src/presentation/wx_app.rs   -> :16601, inside compose's opening, for the active account, with a comment saying it used to read nothing back
sed -n 190,215p src/presentation/managers.rs   -> manage_signatures reads get_signatures_for_account(manager_account), the active account or this computer, and the window says nothing of which
grep -n 'Signature Manager\|&Default signature\|is_default' src/presentation/wx_managers.rs | head -5   -> the shell at :3875, the box at :3969 ("&Default signature", ledger 490 for its NVDA reading), the star column at :3921
grep -n 'account_choice' src/presentation/wx_compose.rs | head -4   -> the From account Choice at :809-820, named "From account"; no handler on its change reaches the signature
grep -n 'fn with_signature\|sign_off::attach' src/presentation/wx_compose.rs   -> :312-323: the signature goes in through sign_off; sign_off::split and carries_one (sign_off.rs:67, :76) can find the block again, which the swap uses
grep -n 'CREATE TABLE IF NOT EXISTS work_done_once' src/data/message_cache/mod.rs   -> :2379: the marker table 11-09.2's snippet pass and 11-08.1's id pass used, and the pass here uses
```

**2. The issue's own two shapes, and the one taken.** "Each email
account can be given a signature (in the Signature Manager, or on the
account's own edit dialog, whichever reads better by ear; the settings
rule says a setting lives where somebody would look for it)". By ear is
the tester's; by the settings rule the account's dialog is where a fact
about one account is looked for, so the choice goes there, and the
manager shows the assignments as a column so both places say the same
thing. Overrulable in a summary with a reason.

**3. The gate for each file.** `application::signatures` new, zero
records; `data::message_cache::signatures` 2 tests and 0 records, cases
added freely; `mod.rs` 23 tests and 12 records, the table and the marker
added, no test added (the cases are in `signatures.rs`); `managers.rs`
137 and 52, none added; `wx_managers.rs` 44 and 11, none;
`wx_account_manager.rs` 14 and 7, none; `wx_compose.rs` 43 and 7, none;
`wx_app.rs` 199 and 110, none; the new target at zero records.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the resolution as a rule, the assignment table, the pass, and the store's readers</name>
  <files>src/application/signatures.rs, src/application/mod.rs, src/data/message_cache/mod.rs, src/data/message_cache/signatures.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/signatures.rs`, whole; `src/data/message_cache/mod.rs:1825-1845` and `:2375-2400`.
    - `src/data/message_cache/bodies.rs:372` (the marker `SNIPPETS_ARE_THE_FIRST_RELEVANT_WORDS`) and `:840-870` (the pass 11-09.2 wrote under it; `grep -n 'SNIPPETS_ARE_THE_FIRST_RELEVANT_WORDS' src/data/message_cache/*.rs` finds it in `bodies.rs`, not `messages.rs`), for the marker's shape.
    - `src/application/sign_off.rs:40-120`.
  </read_first>
  <behavior>
    - `application::signatures::which_signature(assigned: Option<Id>, default_for_all: Option<Id>) -> Option<Id>`: the assignment, else the default, else none; `whether_to_swap(body, previous: &str, next: &str) -> Swap { ReplaceBlock, LeaveAlone }`: replace only when the body's signature block, found by `sign_off::split`, still equals `previous` character for character; cases for each arm and for a body with no block.
    - `mod.rs`: `CREATE TABLE IF NOT EXISTS signature_assignments (account_id TEXT PRIMARY KEY, signature_id TEXT NOT NULL)`; the marker `SIGNATURES_ARE_ONE_SET` under `work_done_once`.
    - `signatures.rs`: `get_every_signature() -> Vec<Signature>` ordered by name; `assign(account_id, Option<signature_id>)` (none removes the row); `assignment_for(account_id)`; `set_the_default(signature_id)` clearing `is_default` on every other row whatever its account; `signature_for_account(account_id) -> Option<Signature>` through `which_signature`; `make_signatures_one_set()`: once under the marker, for each account with a row marked default, write an assignment to it when none exists, then keep the oldest default and clear the rest, so every account keeps the signature it had and one default remains; a delete removes the assignments that named the signature; cases for the resolution, the pass over three accounts with three defaults, the pass running once, and a delete.
  </behavior>
  <action>
    Red first, on a branch: the rule's cases by module path
    (`application::signatures::tests::<name>`) and the store's by module
    path (`data::message_cache::signatures::tests::<name>`). Then the
    rule, the table, the functions, the pass, with `cargo build`
    between. Invoke `elegant-code`: `which_signature` is two lines; the
    pass is one transaction; the readers map rows through the one
    `Signature` reader already there.

    Guard records: one on `application/signatures.rs` whose break
    answers the default before the assignment, its case red; one on
    `data/message_cache/signatures.rs` whose break skips the pass's
    assignment write, the three-accounts case red; each measured.
  </action>
  <verify>
    <automated>cargo test --lib application::signatures:: && cargo test --lib data::message_cache::signatures::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path.
    - `grep -c 'signature_assignments' src/data/message_cache/mod.rs src/data/message_cache/signatures.rs` reports at least 1 for each file; `grep -c 'SIGNATURES_ARE_ONE_SET' src/data/message_cache/signatures.rs` is at least 1.
    - `cargo test --lib application::signatures::` passes with at least 6 tests, quoted; `data::message_cache::signatures::` with at least 8, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The store answers one signature per account by one rule, holds assignments beside the rows it always had, and a database written before this build reads the same way it did.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the manager over one set, the choice on the account dialog, and compose following the From account</name>
  <files>src/presentation/managers.rs, src/presentation/wx_managers.rs, src/presentation/wx_account_manager.rs, src/presentation/wx_compose.rs, src/presentation/wx_app.rs, tests/a_signature_follows_the_from_account.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/managers.rs:185-260` (`manage_signatures` and its rows) and the save arms after it.
    - `src/presentation/wx_managers.rs:3846-3990` (the manager's shell, columns, the editor with its Default box).
    - `src/presentation/wx_account_manager.rs:1368-1420` and `:1620-1660` (the edit dialog's fields near the interval) and `:1240-1345` (the read-back).
    - `src/presentation/wx_compose.rs:300-330`, `:800-830`; `src/presentation/wx_app.rs:16585-16620`.
    - `tests/the_feedback_dialog_shows_what_it_sends_before_it_goes.rs` (12-05) for a composer-adjacent dialog built in a test process.
  </read_first>
  <behavior>
    - The manager: rows from `get_every_signature()` whichever account is active, columns Name, Default (the star), Used by (the account names from the assignments, "everyone else" on the default); the shell's second line says "Every account's signatures"; the editor's "&Default signature" box means the default for every account and its handler calls `set_the_default`; ledger 490's reading of that box is unchanged.
    - The account edit dialog: "Si&gnature:" as a `Choice` listing "Use the default" then every signature by name, named and described ("Used for messages sent from this account; the default is marked in the Signature Manager"), filled from `assignment_for`, written back through `assign` on save.
    - Compose: the opening signature from `signature_for_account(from)`; a handler on the From account `Choice` that reads the next account's signature and, when `whether_to_swap` answers `ReplaceBlock`, replaces the block through `sign_off` and says "Signature changed to <name>" at Normal, and says nothing and leaves the body when it answers `LeaveAlone`.
    - The target: a cache built in the test with two accounts, three signatures, one assigned to the first account and one default; `signature_for_account` answers the assignment for the first and the default for the second; the manager's rows built through `wx_managers` show three names with the Used by column; the account dialog's choice built and read back; the composer built with account one, its body's block read, the From changed to account two through the control, the block read as the default's; the body edited first and the From changed, the block unchanged; companions that plant a swap over an edited body and a manager showing one account's set and are refused.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare. Then the
    manager, the dialog's choice, the composer's handler, with `cargo
    build` between. Invoke `elegant-code`: the swap is one function
    called from the From handler; the manager's Used by column is
    composed by one function over the assignments; no `unwrap` outside
    the tests.

    Guard records: one on `wx_compose.rs` whose break swaps the block
    whatever the body holds, the edited-body reading red, `suite` the
    target; one on `managers.rs` whose break lists the active account's
    signatures only, the rows reading red, `suite` the target; each
    measured. `wx_app.rs` stays at 199, `managers.rs` at 137,
    `wx_managers.rs` at 44, `wx_account_manager.rs` at 14,
    `wx_compose.rs` at 43, quoted.
  </action>
  <verify>
    <automated>cargo test --test a_signature_follows_the_from_account && cargo test --lib presentation::managers:: && cargo test --lib presentation::wx_compose:: && cargo test --lib presentation::wx_account_manager::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare.
    - `grep -c 'get_every_signature' src/presentation/managers.rs` is at least 1; `grep -c 'Use the default' src/presentation/wx_account_manager.rs` is 1; `grep -c 'whether_to_swap' src/presentation/wx_compose.rs src/presentation/wx_app.rs` reports at least 1 for one of them; `grep -c 'get_default_signature(&id)' src/presentation/wx_app.rs` is 0.
    - The target passes with at least 7 tests, quoted; the four `--lib` filters at 137, 43 and 14 (and `presentation::wx_app::` at 199 if run), quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The manager shows one set and who uses each, an account's own dialog chooses its signature, and a message takes the From account's signature and follows a change of account unless the person has edited the block.</done>
</task>

<task type="auto">
  <name>Task 3: the guide, the changelog, the ledger, and the issue closed</name>
  <files>docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`, the signatures paragraph (`grep -n -i 'signature' docs/USER_GUIDE.md | head`).
    - `docs/changelog.md`, `[Unreleased]`.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide: signatures as one set, the
    account dialog's choice, the default, what compose does on a From
    change, and one sentence saying what a database from an earlier
    build keeps. The changelog, `[Unreleased]`, Changed, in the tester's
    words, #43, naming the once-only pass and that the version does not
    move (additive schema, no cut since alpha.1); Known limitations:
    nobody has heard the Used by column or the choice.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's
    ear (the manager's columns, the choice on the account dialog, the
    sentence on a From change).

    After the merge, the executor closes the issue from the merge
    commit: `gh issue close 43 --comment "..."` with the summary's
    findings. Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -i 'Use the default' docs/USER_GUIDE.md` is at least 1; `grep -c '#43' docs/changelog.md` is at least 1.
    - The ledger entry in both halves; carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The guide and the changelog say what signatures are now, and the issue is closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rows written before this build to the new meaning | a person's existing default must stay theirs |
| a From change to the body | a block replaced under a person's edit is lost text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-31 | Tampering | the pass changing which signature an account gets | high | mitigate | the pass writes an assignment for every account that had a default before it clears any; the three-accounts case; a guard record |
| T-12-32 | Tampering | a From change replacing text the person typed | high | mitigate | `whether_to_swap` replaces only a block equal to the previous signature; the edited-body reading; a guard record |
| T-12-33 | Repudiation | a message sent with a signature the person did not see change | low | mitigate | the swap is said at Normal with the signature's name |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the manager, the choice and the sentence under NVDA; what the readings prove: the resolution, the pass, the swap rule, the surfaces.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 9 closes structurally.
- EDIT-04's `[D]` lines each have a named test or reading in the summary and its box is ticked with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, EDIT-04's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-09-SUMMARY.md` when done
</output>
