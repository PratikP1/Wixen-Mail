---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 4
type: execute
wave: 6
depends_on: [12-03.1]
files_modified:
  - src/application/about.rs
  - src/application/mod.rs
  - src/presentation/wx_app.rs
  - LICENSE
  - tests/the_about_dialog_names_its_owners_and_its_links.rs
  - tests/theme_reach.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [ALPHA-01]
estimate:
  tokens: 19000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The About dialog says who holds the copyright, Pratik Patel and the Wixen Project with other contributors, under the MIT licence, in words that agree with LICENSE and are held to it by a reading; the version line keeps the full build string with the counter (ALPHA-01, #78)."
    - "wixen.app and wixen.app/support are two controls a keyboard reaches in order after the copyright, each named with its address on both channels, each opening the default browser through the sanitiser and sending nothing to the page; the Send Feedback button is absent until 12-05 adds it with the dialog it opens, never present and dead."
    - "The tab order is the two links then OK; the dialog's title and its first line say the name and the version; the scan target about walks the new controls at the next push."
  artifacts:
    - "src/application/about.rs: COPYRIGHT, LICENCE_NAME, HOME_PAGE, SUPPORT_PAGE, and what_the_dialog_says() composing the lines; cases holding the words and that the two addresses pass safe_external_url."
    - "src/presentation/wx_app.rs: build_about_dialog reading the texts from application::about, the two link controls, the order."
    - "LICENSE: the copyright line in step with the dialog's, year range and holders."
    - "tests/the_about_dialog_names_its_owners_and_its_links.rs: the built dialog's children in order, the copyright text against LICENSE, the two controls' names over MSAA, the absence of a Send Feedback button until 12-05."
  key_links:
    - "application::about::COPYRIGHT -> the dialog's static text -> LICENSE line 3: one sentence, three readers, held by one test."
    - "a link control activated -> HtmlRenderer::safe_external_url -> open::that: the same gate every link takes."
---

<objective>
Before the public alpha, the About dialog names its owners, its licence
and its two pages, and the reader hears each as what it is.

Purpose: #78, Pratik's on 2026-09-18 while testing
`1.0.0-alpha.1+149.g744d05ef`: the copyright line naming Pratik Patel and
the Wixen Project with other contributors; the full version kept; links to
`wixen.app` and `wixen.app/support` as named controls; a Send Feedback
button that arrives with #64's dialog and not before. The last is 12-05's,
by his decision on #64 of 2026-09-18 ("added in the same commit as the
dialog, not before, so nothing dead sits on About until then"), which is
why this plan is two tasks and 12-05 is three.

Output: the texts in one module, the dialog, `LICENSE` in step, the
reading, the guide's Help section, the changelog, the ledger. Advances
#78 to its last point, which 12-05 closes.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`. Command first, then what
it said.

**1. What the dialog holds, and where.**

```
grep -n 'fn show_about_dialog\|fn build_about_dialog' src/presentation/wx_app.rs   -> :26155 and :26170 (the issue's :23292 was 744d05ef's)
sed -n 26170,26248p src/presentation/wx_app.rs   -> four StaticText lines ("Wixen Mail"; "Version {version::current()}"; "A modern, accessible email client\nbuilt with Rust and wxWidgets."; "Copyright 2024-2026 Wixen Mail Contributors"), the bridge disclosure when the build has one, OK; fixed 380 by 260; painted last
sed -n 3p LICENSE   -> "Copyright (c) 2026 Pratik Patel"; the dialog says 2024-2026 and a different holder, so the two disagree today in both the years and the names
grep -n 'wixen.app' -r src docs README.md --include=*.rs --include=*.md   -> only docs/plans/20260823-earcon-sound-schemes.md; the addresses appear nowhere a person reads
grep -n 'build_about_dialog' tests/theme_reach.rs   -> :887, `check_about`, whose doc says "four StaticText and a button"; 7 tests, 0 records; the doc sentence is corrected in place, the reading unchanged
grep -n "'about'" .github/workflows/accessibility.yml   -> :156; ScanTarget::About at scan_target.rs:145; the new controls are walked at the next push with nothing added here
curl -s -o /dev/null -w '%{http_code}' -A Mozilla/5.0 --max-time 30 https://wixen.app/ ; the same for /support   -> 522 and 522 on 2026-09-20 (Cloudflare answers, the origin does not); the addresses resolve and the pages are not up, so the dialog names them and the guide says the site is coming, and whether it is up before the public alpha is Pratik's (README, Decisions for Pratik)
```

**2. What a link control is to a screen reader, on this toolkit.**

```
grep -n 'pub struct HyperlinkCtrl\|pub fn builder' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/hyperlink_ctrl.rs   -> :76, :83; the control exists in the binding
grep -rn 'HyperlinkCtrl' src --include=*.rs   -> nothing: no dialog here has used one, so what it answers over MSAA (role, name, state) is unmeasured on this tree
grep -n 'AccessibleObjectFromWindow' tests/a_kept_folder_reads_as_a_checked_check_box.rs | head -1   -> the reading over MSAA on a built control this tree already makes, and the shape the reading here takes
```

So the choice between a `HyperlinkCtrl` and a `Button` labelled with the
address is made by a measurement in task 1, not in this plan: the built
control read over MSAA, and the one that answers a role a screen reader
names (link or push button) with the address as its name is the one
kept; the summary quotes both readings. A control that answers nothing
useful on MSAA is not a link to the reader NVDA is.

**3. The gate for each file.** `src/application/about.rs` is new, `--lib
application::about::`, zero records; `wx_app.rs` at 199 tests and 110
records, none added, the dialog rewritten in place;
`tests/the_about_dialog_names_its_owners_and_its_links.rs` new, zero
records, a record whose `suite` names it; `LICENSE` and `docs/*.md` reach
the document-reading targets; `tests/theme_reach.rs` 7 tests, 0 records,
one doc sentence corrected.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the texts in one module, LICENSE in step, the dialog, and the reading over the built dialog</name>
  <files>src/application/about.rs, src/application/mod.rs, src/presentation/wx_app.rs, LICENSE, tests/the_about_dialog_names_its_owners_and_its_links.rs, tests/theme_reach.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:26155-26250`, the dialog as it is.
    - `LICENSE`, whole (it is short).
    - `tests/a_kept_folder_reads_as_a_checked_check_box.rs:190-260` (the MSAA reading's declarations and `role` and `name` reads) and `tests/theme_reach.rs:880-890`.
    - `src/presentation/html_renderer.rs`, `safe_external_url` (`grep -n 'pub fn safe_external_url'`), and one `open::that` site in `wx_app.rs` (`:13272`).
    - `~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/hyperlink_ctrl.rs`, whole.
  </read_first>
  <behavior>
    - `application::about`: `COPYRIGHT` ("Copyright 2024-2026 Pratik Patel and the Wixen Project, with other contributors"), `LICENCE_NAME` ("MIT licence"), `HOME_PAGE` ("https://wixen.app"), `SUPPORT_PAGE` ("https://wixen.app/support"), and `lines() -> [String; 4]` composing name, version (`version::current()`), the one-sentence description and the copyright with the licence; cases: the copyright names both holders and the contributors, the two addresses pass `HtmlRenderer::safe_external_url` unchanged, `lines()[1]` starts with "Version " and contains `version::current()`.
    - `LICENSE:3` reads "Copyright (c) 2024-2026 Pratik Patel and the Wixen Project, with other contributors"; the rest of the file unchanged.
    - `build_about_dialog`: the four lines from `about::lines()`; then two controls in this order, one per address, each labelled with the bare address (`wixen.app`, `wixen.app/support`) and named on both channels with the same words; each activation calls `open::that` with the constant after `safe_external_url`, and a refusal (which cannot happen for a constant, and is still an arm) logs; then OK. Which control kind: measured first (premise 2) on the built dialog; the kept kind answers a link or push button role and the address as its name over MSAA. No Send Feedback button.
    - The target: builds the real dialog with `build_about_dialog(parent, None)`; reads its children in order and asserts the static texts equal `about::lines()`; reads `LICENSE:3` and asserts it holds the same years and the same three names as `COPYRIGHT`; reads the two link controls over MSAA (`AccessibleObjectFromWindow` on each handle) and asserts role and name; asserts no child is labelled "Send Feedback" (a reading 12-05 rewrites in place to assert its presence); companions that plant a stale LICENSE line and a missing link and are refused.
    - `tests/theme_reach.rs:882-884`: the doc sentence says what the dialog holds now; the reading itself unchanged.
  </behavior>
  <action>
    Red first, on a branch: the module cases by module path
    (`application::about::tests::<name>`), the target's readings bare.
    Then the module, registered in `application/mod.rs`; `LICENSE`; the
    dialog, with the measurement of premise 2 taken on the built dialog
    before the control kind is chosen and its two readings quoted in
    the summary; `cargo build --tests` between. Invoke `elegant-code`:
    the dialog reads its words from the module and writes none; one
    small function opens an address through the sanitiser and is called
    by both controls; nothing `unwrap`s outside the tests.

    Guard records: one on `about.rs` whose break changes the year range
    in `COPYRIGHT`, the LICENSE reading red, `suite` the target; one on
    `wx_app.rs` whose break drops the second link control, the order
    reading red, `suite` the target; each measured. `wx_app.rs` stays at
    199, quoted before and after.
  </action>
  <verify>
    <automated>cargo test --lib application::about:: && cargo test --test the_about_dialog_names_its_owners_and_its_links && cargo test --test theme_reach && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the module cases by module path and the readings bare.
    - `grep -c 'Pratik Patel and the Wixen Project' LICENSE src/application/about.rs` reports 1 for each file; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'Wixen Mail Contributors'` is 0.
    - `grep -c 'about::lines()\|about::HOME_PAGE\|about::SUPPORT_PAGE' src/presentation/wx_app.rs` is at least 3; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'Send Feedback'` is 0.
    - `cargo test --lib application::about::` passes with at least 4 tests, quoted; the target with at least 5; `theme_reach` with 7; `presentation::wx_app::` with 199.
    - The summary quotes the MSAA role and name each candidate control answered and says which was kept and why.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The About dialog names its owners and its licence in words held to LICENSE, offers the two pages as controls a screen reader names by their address, keeps the full version, and has no dead button.</done>
</task>

<task type="auto">
  <name>Task 2: the guide, the changelog, the ledger, and the issue advanced</name>
  <files>docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md:1115-1135` (Getting Help), which still says "Help, Documentation" and "Help, Keyboard Shortcuts" for items the menu does not carry by those names (`wx_app.rs:7514-7550`: Contents, one item per help topic, Load Sample Mailbox, Check for Updates, About).
    - `docs/changelog.md`, `[Unreleased]`, Added.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/USER_GUIDE.md`, Getting Help: the
    In-App Help list names the Help menu's real items (Contents on F1,
    the topics, Check for Updates, About) with the two old lines
    corrected by dating in the guide's manner; a short paragraph on
    About: who holds the copyright, the version with the build counter
    and what the counter means (`docs/ALPHA_TESTING.md` already says),
    the two pages, and that the site is being built, dated 2026-09-20
    with what the addresses answered that day, so a reader of an older
    build knows why a link did not open a page. Report Issues keeps its
    three steps and gains one line that 12-05's Send Feedback is coming
    with the next plan, dated, in place of promising it now.

    The changelog, under `[Unreleased]`, Changed: the About dialog's new
    lines and links, #78, with Known limitations: the pages the links
    name are not up yet, dated; nobody has heard the dialog since.

    Ledger, by hand, both halves, no backslash: one `unrun-verify` entry
    for the tester's ear on About (the copyright line read, each link
    heard as a link or button with its address, Tab order, what Enter
    on a link does with the browser); one `todo` for the site answering
    522 at both addresses on 2026-09-20, Pratik's, closed when a fetch
    answers 200.

    After the merge, the executor comments on #78 from the merge
    commit: `gh issue comment 78 --body "Points 1, 2, 3 and 5 landed in <merge commit>: the copyright line names Pratik Patel and the Wixen Project with other contributors under the MIT licence and LICENSE says the same, held by a reading; the version keeps the full build string; wixen.app and wixen.app/support are two controls after the copyright, named by their address on both channels, opening the browser through the same gate every link takes; the order is the two links then OK. Point 4, Send Feedback, arrives with #64's dialog in the next plan, as you decided. What only your ear settles: the two controls under NVDA. What only the site settles: both addresses answered 522 on 2026-09-20."` Commenting is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wixen.app/support' docs/USER_GUIDE.md docs/changelog.md` reports at least 1 for each file; `grep -c 'Help → Documentation' docs/USER_GUIDE.md` is 0 outside a dated correction (`grep -n 'Help → Documentation' docs/USER_GUIDE.md` shows only lines carrying "2026-09").
    - The two ledger entries in both halves; carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The guide describes the Help menu and About as they are, the changelog says what changed and what is not up yet, and the tester's questions are on the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the dialog to the browser | two fixed addresses leave the program through the same opener a message's links use |
| the dialog to the site | a page that is not up is a link to nowhere for the person who pressed it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Information disclosure | the two links telling the site who pressed them | low | mitigate | the addresses carry no query and no identifier; the browser's own request is the browser's; the privacy page's rule that nothing is sent to those pages holds because the program sends nothing |
| T-12-13 | Spoofing | a link constant edited to another host | low | mitigate | the constants are in one module with a case each and pass `safe_external_url`; a guard record holds the LICENSE agreement and the order |
| T-12-14 | Denial of service | a link to a page that answers 522 | low | accept | the guide and the changelog say the site is being built, dated; a `todo` on the ledger is Pratik's to close when the pages answer |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the full gate once, as the hook on the `git merge --no-ff` into `main`, never by hand before it (CLAUDE.md, "The merge is the full gate", 2026-09-23).
- Two guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the two controls and the order under NVDA; what the reading proves: the words, the LICENSE agreement, the roles and names over MSAA, the absence of the dead button.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 4 closes structurally but for its Send Feedback clause, which 12-05 closes.
- ALPHA-01's `[D]` lines each have a named test or reading in the summary and its box is ticked with the date; its `[S]` lines are untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, ALPHA-01's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-04-SUMMARY.md` when done
</output>
