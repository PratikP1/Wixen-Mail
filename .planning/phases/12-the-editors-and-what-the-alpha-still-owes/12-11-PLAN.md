---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 11
type: execute
wave: 11
depends_on: [12-10]
files_modified:
  - docs/plans/20260920-pro-licence.md
  - docs/development/requirements-backlog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [ALPHA-03]
estimate:
  tokens: 19000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A design document under docs/plans, dated in its name as the tree dates designs, settles what a pro licence gates, how a key is checked offline, what happens when it lapses, and what the alpha carries, with every claim about the tree re-read by command on the day and every decision that is Pratik's in one table with the choices and their costs, the merchant among them (ALPHA-03, #65)."
    - "Nothing in the product is gated, keyed, checked or worded by this plan: no field, no setting, no seam, no sentence; the plan's output is the document and the list of decisions, and the product changes only after Pratik decides."
  artifacts:
    - "docs/plans/20260920-pro-licence.md: the sections named in task 1, the decisions table, the merchant table carried from #65's comment with its date and the sentence that the fee pages carry no dates."
    - "docs/development/requirements-backlog.md: one row pointing at the design, so the backlog and the design agree on where the work is."
    - ".planning/WINDOWS.md: one todo naming the decisions as Pratik's and the document as where they are listed."
  key_links:
    - "#65's body and Pratik's two comments -> the document's proposals and his settled prices -> the decisions table: nothing decided for him, nothing he decided restated as open."
---

<objective>
The pro licence as a design, not code: what is gated, how a key is
checked offline, what a lapse does, and the decisions that are Pratik's,
in one document.

Purpose: #65, the tester on 2026-09-16: "plan a pro license with gated
features under pro. Multiple account support. Future RSS reader. PGP
support. Allow working with multiple calendars. Future sound packs for
event announcements. Suggest other potential pro features." Pratik's
decisions of 2026-09-16 on the issue: a $10 a year supporter licence, a
$19 a year pro licence, a $99 perpetual pro licence, a 60-day trial of
pro; and his merchant table of the same day, read from each merchant's
own pages, with Paddle recommended and undecided. The brief's word: a
design plan, not code; nothing gated in the product until he decides.

Output: the document, one backlog row, one ledger entry, the comment on
#65 listing his decisions. Advances #65 to the decisions; the code is a
later phase's once they are made.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@docs/plans/20260823-earcon-sound-schemes.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`. Command first, then what
it said.

**1. Where designs live, and how they are dated.**

```
ls docs/plans/   -> 20260726-mail-at-scale.md, 20260801-mutation-sweep.md, 20260823-earcon-sound-schemes.md, 20260915-whole-tree-mutation-run.md: designs are docs/plans/YYYYMMDD-name.md, the date in the name
grep -n 'docs/plans' tests/every_number_carries_its_command_and_its_date.rs | head -3   -> :596-613: a page under docs/plans is dated by its name, so its figures need their command and not a date each; every other rule of house_style still reads it
ls docs/development/   -> implementation-history.md, measurements.md, requirements-backlog.md, ...: the backlog page is where future work is listed, and it gains one row pointing at the design
```

The brief said "a document under `docs/development/` (or where the tree
keeps designs)"; the tree keeps designs under `docs/plans/`, so that is
where it goes, with a row in the backlog.

**2. What the tree already has that the design leans on, and what it
does not.**

```
grep -rniE 'licen[cs]e key|entitlement' src --include=*.rs   -> nothing: no licence key and no entitlement anywhere in the tree, as the issue said; the issue's own wider grep (`licen[cs]e key|subscription|entitle`) answers 202 lines for `subscription` (iCal and folder subscriptions) and 23 for `entitle` in ten files, none about a licence, among them a type `Entitles` and a field `entitle` at `wx_app.rs:12920-12971`, the page window's title handoff; the design's proposed `Entitlement` seam sits one letter from that name and the document says so and chooses a name that does not collide, or says why the two can coexist
grep -n '^name = "ed25519-dalek"\|^name = "ring"\|^name = "keyring"' Cargo.lock   -> all three are in the tree: an offline Ed25519 check and a credential-store entry need no new crate
grep -n 'pub fn verify' src/service/update_download.rs   -> :858, the publisher check the issue names as the pattern for a signed thing checked offline
grep -n 'pub fn allowed_for\|pub const SETTINGS_SECTION' src/application/allowed.rs   -> :912 and :186: the one-seam pattern (read once, asked where a write starts, a section on Settings) the Entitlement seam would copy
grep -n 'sound_scheme_import' src/presentation/accessibility/mod.rs 2>/dev/null; ls src/presentation/accessibility/ | grep sound   -> sound_scheme.rs, sound_scheme_import.rs: schemes and their import exist, so "sound packs" is a distribution question and not a mechanism
grep -rn 'fn ' src/application/accounts.rs | grep -c pub   -> several accounts are free today; gating them takes something back, which the issue's section 4 answers with a dated grandfather rule and section 1 lists as his to decide
```

**3. His decisions, quoted, and what stays his.** Decided: the four
prices and the trial (his comment of 2026-09-16). Undecided, in his own
words on the issue and the comment: the free/pro line; whether several
accounts are gated at all; the merchant (Paddle recommended, with the
FTC settlement noted; Lemon Squeezy under Stripe; Stripe Managed
Payments; Gumroad too dear; FastSpring unknown); whether revocation is
checked online; whether the perpetual licence carries updates for ever
or for a stated period; whether the trial runs in the program from first
run with no card. Two more that the design raises and does not settle:
whether the $10 supporter tier must deliver a real licence (Paddle's
acceptable-use policy, in his table); and how priority support is
carried in a feedback report (#64's own words: a separate issue when the
pro offering is designed).

**4. The gate.** `docs/plans/*.md` and `docs/development/*.md` reach the
document-reading targets on a documents-only commit (`house_style` 74,
`the_words_that_say_nothing` 9, `docs_links` 6,
`every_number_carries_its_command_and_its_date` 25); no source file is
touched, no test is added, no record is written.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: the design document</name>
  <files>docs/plans/20260920-pro-licence.md, docs/development/requirements-backlog.md</files>
  <read_first>
    - #65's body and its two comments (`gh issue view 65 --json body,comments` from the repository root), whole.
    - `docs/plans/20260823-earcon-sound-schemes.md`, whole, for the shape a design here takes (what exists, the proposal, the decisions, what waits on what).
    - `src/application/allowed.rs:1-120` and `:180-260` (the seam and its Settings section); `src/service/update_download.rs:840-900`.
    - `docs/privacy.md:173-192` (the table the design's privacy line would join) and `docs/development/requirements-backlog.md`, whole.
  </read_first>
  <action>
    Invoke `writing-craft`. Write `docs/plans/20260920-pro-licence.md`
    (the date is the day it is written; if that is not 2026-09-20, the
    file takes that day's date and `files_modified` is corrected in the
    summary) with these sections, each claim about the tree carrying
    the command it was read with:

    1. What this is for, in the tester's words, and what it is not (no
       code, no gate, nothing in the alpha changes on this document).
    2. What exists today: no licence anywhere; several accounts free;
       PGP reading with one key; several calendars syncing with free/busy
       asking one source; sound schemes with an import; no RSS reader;
       the crates an offline check and a stored key would use, already
       in the tree; the one-seam pattern `application::allowed` is.
    3. The proposed free and pro line as a table: one row per feature
       the issue names and each the proposal adds, its state in the
       tree today (free and built; built and partial; not built), which
       side of the line the proposal puts it on, and what gating it
       takes back from somebody who has it (the accounts row says so).
       The product's own line first: nothing a blind person needs to
       work a mailbox is gated, and no accessibility feature ever is.
    4. The licence: a short signed string (Ed25519, the project's public
       half in the binary) carrying the plan, the expiry and an
       identifier; entered on the settings screen in its own section;
       kept in the credential store like every other secret; never
       logged; checked offline; an online revocation check only if
       decided, said on the privacy page; the trial as a dated marker in
       the same store with what a machine-clock change would do to it.
    5. The seam: one `Entitlement` read once and asked where a pro
       feature starts; a gated command visible and greyed with a label
       saying it is a pro feature; a test holding that every gated site
       names the seam and that nothing accessibility-bearing sits behind
       it; the alpha carrying the seam with everything unlocked. The
       document says that the tree already has a type `Entitles` and a
       field `entitle` at `wx_app.rs:12920-12971` (the page window's
       title handoff, nothing to do with a licence), one letter from the
       proposed name, and either proposes a name that does not collide
       or says why the two can live side by side.
    6. A lapse or no licence: nothing deleted, nothing unreadable; extra
       accounts keep receiving and can be read and stop sending and
       syncing changes after a grace period with the sentence saying so;
       imported keys keep reading; the dated grandfather rule for
       accounts set up before the release that introduces licences.
    7. Prices, as decided (his comment of 2026-09-16, quoted).
    8. The merchant table from his comment, carried whole with its date
       and the sentence that the fee pages carry no dates and are
       re-read before anything is signed; what each choice costs in one
       worked $10 and one worked $19 sale; the recommendation as it was
       made (Paddle) and the reasons against each, unchanged.
    9. **Decisions for Pratik**, one table: the decision, the choices,
       what each costs, what the document proposes, and an empty column
       for his answer. The rows are premise 3's, every one, and none is
       answered in the document.
    10. What happens after the decisions: a phase of its own, touching
        Settings, the privacy page, the first-run screen, `--help`, the
        guide and every gated feature's plan; #64's priority-support
        line; the version at which it starts.

    `docs/development/requirements-backlog.md`: one row for the pro
    licence pointing at the design and its decisions table.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test every_number_carries_its_command_and_its_date</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/plans/*-pro-licence.md` finds exactly one file; `grep -c '^## ' docs/plans/*-pro-licence.md` is at least 10; `grep -c 'Decisions for Pratik' docs/plans/*-pro-licence.md` is at least 1; `grep -c 'Paddle' docs/plans/*-pro-licence.md` is at least 3.
    - `grep -rniE 'licen[cs]e key|entitlement' src --include=*.rs` answers nothing before and after this plan (its exit status 1 both times, quoted), which is the reading that nothing was gated; the `Entitles` type at `wx_app.rs:12920` is named in the document beside the seam's proposed name.
    - `git diff --stat` for the plan's commits names no file under `src/`, `tests/` or `guards/`.
    - Carriage returns 0 on both files; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The design is on the record in the place the tree keeps designs, with everything that is Pratik's listed as his and nothing in the product changed.</done>
</task>

<task type="auto">
  <name>Task 2: the ledger and the issue told</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md`, the last three entries in both halves, for the shape.
  </read_first>
  <action>
    Ledger, by hand, both halves, no backslash: one `todo` naming the
    document and saying the decisions in its table are Pratik's, closed
    when a phase is planned from his answers.

    After the merge, the executor comments on #65 from the merge
    commit: `gh issue comment 65 --body "..."`, listing the decisions
    table's rows in one numbered list with the document's path, saying
    that nothing in the product is gated and that the prices and the
    trial are carried as decided. Commenting is not a publish. The
    issue stays open for his answers.
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The ledger entry in both halves; `head -7 .planning/WINDOWS.md` one entry and one open higher, quoted.
    - Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The decisions are on the ledger as Pratik's and on the issue where he reads them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a design to the product | a document that reads as a decision could be built from as if decided |
| a merchant table to a signature | figures read on one day, signed on another |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-37 | Tampering | code gated from a document nobody approved | high | mitigate | the plan touches no source; the acceptance criteria hold `src/`, `tests/` and `guards/` unchanged; the decisions table's answer column is empty |
| T-12-38 | Repudiation | a price or a fee quoted without its date | medium | mitigate | every figure carries his comment's date and the sentence that the merchants' pages are undated and re-read before signing |
| T-12-39 | Information disclosure | a future licence key logged or sent | low | accept | no key exists; the design says never logged and the credential store, and the phase that builds it carries the guard |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing is installed; `Cargo.toml` and `Cargo.lock` untouched; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook (`docs_only`), never piped.
- No guard record written, no test added, nothing under `src/` touched, said in the summary with `git diff --stat`.
- What only Pratik settles: every row of the decisions table.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 11 closes structurally: the document with its ten sections and the table.
- ALPHA-03's `[D]` lines each have the document's section named in the summary and its box is ticked with the date; its `[S]` line, the decisions, is untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, ALPHA-03's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-11-SUMMARY.md` when done
</output>
