---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 03.2
subsystem: the commit gate: stage timings, per-suite shell suites, one cargo call, compiled-in files, a kept guard pre-read, the merge path, and the keyring race
tags: [found-23, gate, check-sh, which-checks, guards-py, keyring, ledger-373, ledger-374, merge-path]
status: complete

requires:
  - phase: 12-the-editors-and-what-the-alpha-still-owes
    provides: "12-03.1: the eighth whole-tree target, the_nvda_cases_wait_for_words_the_program_says, and main at 8eee380a"
provides:
  - "scripts/check.sh: begin_stage and one exit trap printing each stage's seconds; the_inputs_of_a_suite with the other two lists and --shell-suites-owed; the_staged_paths with --no-renames; the_scoped_runs and --scoped-runs-for, one cargo call for the integration targets, the compiling sources, the document list at a merge; the_targets_that_read_documents and the_modules_that_read_documents; decide_the_mode_for_this_commit, MERGE_HEAD and --mode-for-this-commit"
  - "scripts/which-checks.sh: unknown options refused; --merge; --sources-compiling; --documents-among; is_a_document"
  - "scripts/guards.py: the kept pre-read under target/guards-pre-read/, keyed and bounded to two hours"
  - "src/service/secret_store.rs: open_entry, the one opener of a credential entry; src/application/forget.rs opens through it"
  - "tests/the_credential_store_is_ready_for_every_thread.rs: twenty fresh processes of sixteen threads each"
  - "CLAUDE.md, .githooks/commit-msg: the full gate once a phase, its risk, and a premise rule for the closing plan"
affects: [12-04 to 12-12, which commit and merge under this gate; 12-12, whose task 3 is the phase's full gate and closes ledger 585]

actuals:
  tokens: 80000
  tasks: 8
  commits: 22

tech-stack:
  added: []
  patterns:
    - "A gate decides by data it holds, and a case holds the data to what a pattern reads, rather than the gate deciding by the pattern"
    - "A shell case that runs on the common commit path is written without a process per step, because a fork costs tens of milliseconds on Windows"
    - "A red whose trigger is a probability is taken red by hand several times, its odds are written beside its record, and a not-red sweep result is run again locally before the record is called stale"

key-files:
  created:
    - tests/the_credential_store_is_ready_for_every_thread.rs
    - .planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-03.2-SUMMARY.md
  modified:
    - scripts/check.sh
    - scripts/check.test.sh
    - scripts/which-checks.sh
    - scripts/which-checks.test.sh
    - scripts/guards.py
    - .githooks/commit-msg
    - src/service/secret_store.rs
    - src/application/forget.rs
    - tests/every_number_carries_its_command_and_its_date.rs
    - tests/the_planning_files_agree_with_themselves.rs
    - tests/the_words_that_say_nothing.rs
    - guards/guards.toml
    - CLAUDE.md
    - docs/changelog.md
    - docs/development/measurements.md
    - .planning/WINDOWS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
    - .planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-12-PLAN.md

key-decisions:
  - "The merge into main answers what the branch's whole diff earns by the branch rules, with the document-reading list when the diff holds a document; the whole suite, the release build and the audit run once a phase, by hand in the closing plan"
  - "The check suite's cases were rewritten without a process per step when they took the suite from 9 s to 31 s; it grew back to about 32 s with later cases, recorded as ledger 586 rather than trimmed"
  - "which-checks.sh --sources-compiling takes several paths, so check.sh asks once per commit rather than once per path"

metrics:
  duration: about 100 minutes, 15:29Z to about 17:10Z on 2026-09-23
  completed: 2026-09-23
---

# Phase 12 Plan 03.2: The gate costs what the change earns Summary

**It works on this branch, and the merge that lands it is the first merge under the new path.** Every hook run now names its mode and ends with each stage's seconds; the keyring race that refused three of twenty-eight full gates is closed; each shell suite runs only when a commit stages what it reads; the scoped integration targets are one cargo call; a file the program compiles in reaches the tests of the code that compiles it; the guard runner keeps a suite's pre-read for a tree it has read; and a merge into `main` runs what the branch's diff earns, with the whole suite once a phase in 12-12. This branch is not pushed, on Pratik's answer of 2026-09-23. The merge commit, its mode line and its stage line cannot be in this file; the report that closed the plan carries them.

What this plan cannot prove, said plainly: nothing here has run the whole suite over these changes. 12-12's task 3 does, and ledger 585 is that run. The keyring change reaches integration targets this plan's merge will not run; the two that reach the real store were run by hand (below).

## Commits and what each hook run printed

Twenty-one commits on `the-gate-costs-what-the-change-earns` from `8eee380a`, and this summary's documents commit makes twenty-two. One hook run was refused and remade: task 2's first red, for rustfmt.

| Commit | What | Mode | Stage line (seconds) |
|---|---|---|---|
| `3c009cba` | task 1 red | red | none: the old gate |
| `00fb8e4e` | task 1 green | affected | 86: start 1, rustfmt 2, clippy 27, suites 32, scoped 24 |
| refused | task 2 red, first try | red | stopped in rustfmt after 4: start 1, rustfmt 3 |
| `f3e4b8ee` | task 2 red | red | 94: start 1, rustfmt 3, clippy 27, suites 34, named 0, scoped 29 |
| `72c27b6c` | task 2 green | affected | 113: start 2, rustfmt 3, clippy 27, suites 33, scoped 48 |
| `45cb36e4` | task 2 record, ledger | affected | 93: start 1, rustfmt 3, clippy 26, suites 33, scoped 30 |
| `6b880981` | task 3 red | red | 103: start 3, rustfmt 3, clippy 26, suites 48, named 0, scoped 23 |
| `cff44a7f` | task 3 green | affected | 86: start 1, rustfmt 3, clippy 27, suites 30 (check only), scoped 25 |
| `13b42781` | task 3 refactor | affected | 72: start 1, rustfmt 2, clippy 28, suites 17 (check only), scoped 24 |
| `37b98bb3` | task 4 red | red | 79: start 2, rustfmt 3, clippy 26, suites 23 (check), named 1, scoped 24 |
| `5ad48c04` | task 4 green | affected | 76: start 1, rustfmt 3, clippy 26, suites 22 (check), scoped 24 |
| `4dd77e13` | task 5 red | red | 97: start 2, rustfmt 3, clippy 26, suites 41 (check, which-checks), named 0, scoped 25 |
| `221a9d89` | task 5 green, ledger | affected | 97: start 2, rustfmt 3, clippy 26, suites 41 (check, which-checks), scoped 25 |
| `0e985381` | task 6 red | red | 62: start 1, rustfmt 3, clippy 29, suites 3 (none), named 0, scoped 26 |
| `ca2db29d` | task 6 green | affected | 60: start 2, rustfmt 3, clippy 30, suites 0 (none), scoped 25 |
| `28597c01` | task 6 record | affected | 82: start 1, rustfmt 3, clippy 27, suites 26 (check), scoped 25 |
| `940da640` | task 7 pair A red | red | 98: start 2, rustfmt 2, clippy 27, suites 43 (check, which-checks), named 0, scoped 24 |
| `83f797ed` | task 7 pair A green | affected | 97: start 1, rustfmt 2, clippy 26, suites 43 (check, which-checks), scoped 25 |
| `a8596b88` | task 7 pair L red | red | 60: start 2, rustfmt 2, clippy 27, suites 4 (none), named 0, scoped 25 |
| `3db8ec65` | task 7 pair L green | affected | 83: start 2, rustfmt 3, clippy 27, suites 26 (check), scoped 25 |
| `de9d5130` | task 7 pair B red | red | 110: start 3, rustfmt 3, clippy 26, suites 53 (check, which-checks), named 0, scoped 25 |
| `c4798dba` | task 7 pair B green | affected | 120: start 3, rustfmt 2, clippy 27, suites 63 (all four), scoped 25 |

Task 3's green (`cff44a7f`) is the first to run check alone and print a not-run line for the other three: `-- audit: not run, nothing it reads changed; CI runs it on every push`. Task 6's red (`0e985381`) is the first to skip every suite, as the plan's table said; task 6's green and pair L's red skip every suite too.

## What it saved, by mode

From the stage lines above, beside the measurement of nine executors on 2026-09-23 (premise 1). **These commits stage gate scripts far more than a feature plan's do**, so the suite stage is high and the scoped stage low; a feature plan's will read the other way.

| Mode | Runs | Median total | Range | Median stages | Premise 1 |
|---|---|---|---|---|---|
| red | 8 | 95.5 s | 60 to 110 s | clippy 26.5, suites 37.5, scoped 25 | 4.25 min (255 s) |
| affected | 12 | 86 s | 60 to 120 s | clippy 27, suites 31, scoped 25 | 4.2 min (252 s) |
| docs_only | 0 read here | | | | 3.0 min |
| the merge | 0 read here; the report carries it | | | | 6.9 min |

The shell suites: of the twenty runs with a stage line, three ran none (0 to 4 s for the stage), six ran check alone (17 to 30 s), five ran check and which-checks (41 to 53 s), and six ran all four (32 to 63 s), five of those before task 3's green made the rule per suite and the sixth the commit staging the hook. Premise 4 counted 47 of 79 commits in the nine plans before this one owing no suite under the per-suite rule; this plan's commits stage the suites' own inputs by design.

**The finding against the saving, ledger 586.** The check suite that decides which suites run grew with the cases that decide it. By hand, one after another: `audit` 4 s, `check` 9 s, `red-commit` 6 s, `which-checks` 11 s at `8eee380a`; 5.4, 31.8, 7.2 and 18.5 s at `c4798dba`. Task 3's green first took check to 31 s through a process per step in its helpers; the fork-free rewrite at `13b42781` brought it to 16 s; the cases of tasks 4, 5 and 7 each start a `bash` and parse `check.sh`, and took it back to about 32. So a commit staging only `guards/guards.toml` now pays about what all four suites cost before, and a commit staging no suite input pays nothing. The rows are on `docs/development/measurements.md`.

| Task | The report's estimate a plan | What reads it | Read here |
|---|---|---|---|
| 1 timings | none; it is how the rest is read | every stage line | every commit from `00fb8e4e` printed one |
| 2 keyring | about 1.3 min, and 3 of 4 flaky refusals | refusals in 12-04 onward | none here; 20 of 20 fresh processes failed before, none after |
| 3 suites | 3 to 8 min; per suite about 3.2 min against every suite on every commit | the suites stage | 0 to 4 s with none owed, 17 to 33 s for check alone; see 586 |
| 4 one call | 1 to 3 min | the scoped stage on a commit coupling many targets, first in 12-04 | not shown here: no commit coupled more than a handful |
| 5 compiled in | a cost, on rare commits | none | about 0.8 s for the scan on a commit that asks it |
| 6 kept pre-read | 1 to 2 min | the first two pre-read lines | 14 s read and kept, then 7 s kept, on `house_style`, whose pre-read is 5 s |
| 7 the merge | 6.9 min toward an affected run's 4.2 | this plan's merge's stage line | in the report |

The first two pre-read lines from task 6, quoted:

```
Reading what already fails with --test house_style, before anything is broken.
Reading what already fails with --test house_style: kept from 21 s ago for this tree; delete C:\Users\prati\Documents\projects\Wixen-Mail\target\guards-pre-read\test-house_style to read it again.
```

## Tasks

**Task 1.** `begin_stage` closes the previous stage's clock and prints the header; one `trap the_run_ends EXIT`, armed after the mode line, prints `check.sh: <mode> <passed|stopped in <stage>> after N s: ...` and removes the run's files. `grep -nE '^[[:space:]]*trap .*EXIT' scripts/check.sh` finds one line, below every early answer's `exit 0`. `check.test.sh` went from 49 case lines to 54. `bash scripts/check.sh --suites-for guards/guards.toml src/presentation/wx_managers.rs` prints four target names and no stage line.

**Task 2.** `secret_store::open_entry` makes the first `keyring::Entry::new` of a process inside a `OnceLock` initialiser. The hand reds before the fix: 20 of 20 children failed each of three times, 15 of 16 threads each saying "No default store has been set". After: the new target five times, 1 passed each (20 s, then 2 s four times); `a_move_says_what_has_not_been_sent` three times, 10 passed each. `service::secret_store` 6 tests before and after, `application::forget` 24. `grep -rnE '(^|[^A-Za-z_])(keyring::)?Entry::new\(' src --include=*.rs` finds one line, `secret_store.rs:40`. **A person could meet it**: at start the program checks every account and starts each inbox watch at once, and for an account that signs in through a browser both reach `mail_auth::for_account`, which reads the token from the store; the changelog says so under Fixed.

**Task 3.** The lists, the owed rule, `the_staged_paths` with `--no-renames`, and a not-run line per skipped suite. Eighteen cases red, two planted companions green on arrival. From the repository root: `--shell-suites-owed affected src/lib.rs` a `no:` line per suite; `guards/guards.toml` `check yes:` and three `no:`; `.cargo/audit.toml` `audit yes:`, `check yes:`; `scripts/shell-suite.sh` and `all` a `yes:` line per suite; `scripts/guards.py` a `no:` line per suite. The green's staged answer was `check yes: scripts/check.sh is on its list` and three `no:` lines. A probe on a copy of `check.sh` with one path taken off each of three lists named all three (`Cargo.lock` for audit, reached through `audit.sh`; `guards/guards.toml` for check; `scripts/red-commit.sh` for which-checks, reached through `which-checks.sh`), so the reading follows calls.

**Task 4.** `the_scoped_runs` builds one `--lib` line per module and one `--test` line; `run_the_integration_targets` spells `--no-fail-fast` once. For `wx_managers.rs` the answer holds one whole-word `--test` line and no `--no-fail-fast`; the command composed from it, `cargo test --no-fail-fast $words --test does_not_exist_xyz`, answered `error: no test target named 'does_not_exist_xyz'`, not a refused flag. The five files reading `guards_that_read_the_whole_tree` are the five the plan named, and every one ran in the hook.

**Task 5.** `which-checks.test.sh`'s `run_from` moved to a fixture tree first: 52 case lines before and after. The copied include lines matched the real ones exactly, compared read-only (`spellcheck/mod.rs:1050`, `catalogue.rs:153`, `sent_copy.rs:1133`). From the root by hand: `--sources-compiling data/dictionary_en.txt` prints `src/service/spellcheck/mod.rs`; `gsd/x data/dictionary_en.txt` prints `affected`; the scoped run for the dictionary holds `--lib service::spellcheck::`, for `mail_controller.rs` `--lib application::sent_copy::`, for `dates.ftl` `--lib common::catalogue::`. Ledger 373 fixed in both halves on Pratik's answer of 2026-09-23; 582 opened for the real include lines nothing holds.

**Task 6.** Four functions and the constant with worked examples; `python -m doctest -v scripts/guards.py` reports 160 tests, from 136. The red failed 14 of 160 on stubs. `grep -nE 'os\.(environ|putenv).*GIT_INDEX_FILE|environ\.(update|setdefault)' scripts/guards.py` finds nothing; the one `GIT_INDEX_FILE` is `its_own_index["GIT_INDEX_FILE"]`, handed to the git calls. `git status --porcelain` showed only `guards/guards.toml` modified before and after each of the three invocations. `.github/workflows/guards.yml` caches the registry, not `target/`, so a runner shard never finds a kept reading.

**Task 7.** Pair A: an unknown option exits 64 naming it. Pair L: the two arrays and the three readers; the seven records named by the count check re-measured in one call, 34 s, each "went red, and nothing else did". Pair B: `--merge`, `--documents-among`, `--mode-for-this-commit`, the document list at a merge, the closing messages, the dated comments. From the root: `--documents-among docs/changelog.md data/dictionary_en.txt src/lib.rs` prints `docs/changelog.md`; `--merge main src/lib.rs` prints `affected`; `main src/lib.rs` prints `all`; `--not-an-option gsd/x src/lib.rs` exits 64. The word-level check over `CLAUDE.md` against the merge base printed no removed word and exited 0, and "A phase's closing plan runs the full gate once" occurs once. The suites' inputs taken again after tasks 5 and 7: every path the pattern finds is on its suite's list, the only read of `src/` relative to the current directory is `which-checks.sh`'s scan, and every case that reaches it answers from a fixture tree or a repository of its own.

**Task 8.** The measurements rows, FOUND-23's seven `[D]` lines, the ledger.

## Tests, and which were red

Every red commit was accepted by `red-commit.sh`, each hook ending "a red that is exactly the one this commit named".

- Shell cases: `check.test.sh` 49 to 89 case lines, `which-checks.test.sh` 52 to 68; every new case red first except the planted companions and the questions' guards, each named in its message as green on arrival.
- Rust: `test_threads_reaching_the_credential_store_at_once_all_find_it_ready`; `test_the_guard_runner_still_obeys_its_own_examples` (14 examples red); `test_the_measurements_reading_is_found_in_the_documents_array` and its two siblings, with `test_every_guard_record_says_how_many_tests_the_files_it_names_held` named in the same red as `CLAUDE.md` prescribes.

## Guard records

- New: "the first credential entry of a process sets the store up before any other is opened", measured three times (21, 20, 19 s, each red on the one test named). Its comment gives the odds: 120 children failing of 120 across three hand reds and three runs under the break applied by hand, so under 0.025^20 that a run reads not red; measured on this machine only, and a not-red result from a runner shard is run here alone twice before the record is called stale. The runner's log prints no child count, so the counts came from the break applied and taken back by hand with the editor.
- New: "a kept pre-read is used only for the tree it was kept for", measured three times.
- Re-measured: the seven naming `every_number_carries_its_command_and_its_date.rs`.
- 1,044 records by the TOML reader; census 797 swept and 247 since.

## Deviations from Plan

1. **[Rule 1] The check suite took three times as long after task 3.** Found on task 3's green stage line. Rewritten without a process per step at `13b42781`; it grew again with later cases and is ledger 586 rather than trimmed further.
2. **[Rule 3] `the_staged_paths` was defined below the early answer that first used it.** The case "a merge in progress is told to which-checks as a merge" answered `all` before pair B's green was committed; the function moved above `--scoped-runs-for`. Nothing committed carried it.
3. **The brief overrode the plan's commit on `main` after the merge.** No commit follows the merge; the report carries the merge hash, its mode line and its stage line, and FOUND-23's task 7 line says so.
4. **Ledger 583 and 584 were filed with task 7's green, not task 8's commit**, because `CLAUDE.md` names both in that commit. Task 8 filed 585 and 586. `12-12-PLAN.md`'s premise corrections gained a paragraph naming 585 and 586, a file the plan did not list, by `CLAUDE.md`'s rule that an entry handing work to a named plan is filed only when that plan's text mentions it.
5. **`which-checks.sh --sources-compiling` takes several paths**, so `check.sh` asks once per commit.
6. **`THE_DEFAULT_TEST_THREADS`** now names the `8` both `TEST_THREADS` and `the_settings_a_run_reads` use.
7. **The three existing reader companions' anchor moved with the list** (pair L): they took `--test <name>` out of the documents block, which no longer spells one, and now take the name out of the array. Said in that commit.
8. **Ledger timestamps** were first written ahead of the clock and corrected to the times the entries were written before this commit.

## Exception set for scripted edits

**Zero.** Every tracked file was changed with the editor, the diagnostic detours included: the hand-applied keyring break and its reversal, the formatting fix rustfmt asked for, and every ledger edit. `sed`, `awk` and `printf` wrote only to temporary copies and to fixture trees the suites build at run time; `cargo fmt` ran only with `--check`.

## Threat Flags

None beyond the plan's register. `the_working_tree_id` runs `git add -A` into a copy of the index, which stores the working tree's files as loose objects in the real repository; the real index is never written (T-12-03.2-05).

## Ledger

374 fixed; 373 fixed as built; 581 (374's second finding), 582 (the real include lines), 583 (documents read at test time, a hole this plan widened), 584 (the closing-plan check), 585 (12-12's full gate, unrun) and 586 (the check suite's cost) opened. `head -7`: 540 open, 40 fixed, 580 total before; 544 open, 42 fixed, 586 total after.

## Known stubs

None.

## Self-Check: PASSED
