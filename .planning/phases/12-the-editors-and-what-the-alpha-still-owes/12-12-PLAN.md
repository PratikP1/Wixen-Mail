---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 12
type: execute
wave: 14
depends_on: [12-01, 12-02, 12-02.1, 12-03, 12-03.1, 12-04, 12-05, 12-06, 12-07, 12-08, 12-09, 12-10, 12-11]
files_modified:
  - docs/ALPHA_TESTING.md
  - docs/USER_GUIDE.md
  - docs/manual-accessibility-pass.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [FOUND-20, LIST-19, LIST-11, ALPHA-01, ALPHA-02, ALPHA-03, EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05]
estimate:
  tokens: 19000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The alpha page and the user guide say what the editors, the About dialog, the feedback dialog, the separate window and the status bar do now, every older sentence kept and dated, and the listening page carries one line per sentence the phase's summaries list as unheard."
    - "Each of the phase's eleven requirements is read clause by clause against the tree, ticked only when every [D] line has a named test, reading or file found in the tree by grep, its [S] line left with its ledger numbers; the roadmap, the requirements, the state and the ledger say the same thing, and a plan that did not run is said everywhere a tick or a number would otherwise read as done."
  artifacts:
    - "docs/ALPHA_TESTING.md, docs/USER_GUIDE.md, docs/manual-accessibility-pass.md, docs/changelog.md: read as one reader would."
    - ".planning/REQUIREMENTS.md: the eleven requirements with dated closing sentences and their traceability rows; .planning/ROADMAP.md: the criteria closed, the plan list ticked, the progress row, the phase line; .planning/STATE.md; .planning/WINDOWS.md."
  key_links:
    - "each plan's coverage block -> the closing sentence per [D] line: every ref with a # taken as path#name and grepped as fn <name> in that file, 11-12's one-pass check, before any tick is written."
    - "each plan's [S] line -> the listening page -> the ledger: one number per unheard sentence."
---

<objective>
Say on the pages what the editors and the alpha's dialogs do now, write
the listening lines, and read the phase's eleven requirements against
the tree clause by clause.

Purpose: the phase's closing read, on 11-12's pattern: a phase that
closes says so everywhere a number or a tick would otherwise read as
complete, and a closing read checks every name the coverage blocks give
against the tree in one pass rather than trusting the summaries. This
plan runs last so it reads the status bar's sentences as 12-03 left
them and the pages as every plan before it wrote them.

Output: the four pages, the listening lines, the requirements read, the
roadmap, the state and the ledger told, and the For a person paragraph
in the summary.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@.planning/phases/11-reading-and-the-list/11-12-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`, before any plan of the
phase has run; every count is re-taken on the day. Command first, then
what it said.

```
ls .planning/phases/12-the-editors-and-what-the-alpha-still-owes/*-PLAN.md | wc -l   -> 12 (two moved from phase 11, ten written today); the summaries on the day are what the row counts
awk '/^## A\./,/^## B\./' docs/manual-accessibility-pass.md | grep -c '^[0-9]*\. \*\*'   -> 83, 11-12's count; the items this plan appends start at 84 and no item number moves
grep -c '^- \[x\] \*\*LIST-\|^- \[ \] \*\*LIST-' .planning/REQUIREMENTS.md   -> 24 ticked and 3 open at 0ad66e48; LIST-11 and LIST-19 are ticked by 12-03 and 12-02 on their own plans' instruction and re-read here; LIST-02 is phase 11's and stays where 11-12 left it unless its two rows landed
grep -n 'Current Plan:\|Total Plans in Phase:' .planning/STATE.md   -> the two lines at column zero in the Current Position section, one each, which the_planning_files_agree_with_themselves holds to the frontmatter and the disk
head -7 .planning/WINDOWS.md   -> 566 entries, 531 open, 35 fixed before the phase; the plans add and close their own, and the frontmatter is corrected here by count
git rev-list origin/main..HEAD --count   -> 0 at 0ad66e48, the commit the phase was planned against; 1 at 19ad8648, the commit that landed the plans, and higher with each commit until Pratik pushes; the runs FOUND-20 and 12-01 wait for are that push, which is his, and this plan re-takes the count on its day
```

The completion marks a plan leaves are four, and the brief to every
plan in this phase names all four (`CLAUDE.md`, the paragraph added
2026-09-20); this read counts the ticks in the roadmap's plan list
against the progress row and reports any drift as 11-12 did for the
row.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: the four pages and the listening lines</name>
  <files>docs/ALPHA_TESTING.md, docs/USER_GUIDE.md, docs/manual-accessibility-pass.md, docs/changelog.md</files>
  <read_first>
    - Every summary of the phase, each one's `provides`, `[S]` lines and ledger table; 11-12's summary for the shape of the short version and the listening items.
    - `docs/ALPHA_TESTING.md`, the short version and the known-missing list; `docs/manual-accessibility-pass.md`, section A's end; `docs/USER_GUIDE.md`, the sections each plan wrote; `docs/changelog.md`, `[Unreleased]`.
  </read_first>
  <action>
    Invoke `writing-craft`. Every sentence is checked against the tree
    before it is written: the constants and rows a sentence names are
    re-taken with a command. `docs/ALPHA_TESTING.md`: a paragraph in the
    short version saying what the editors, About, Send Feedback, the
    separate window and the status bar gained in this phase and that
    none of it has been heard; in the known-missing list, one entry per
    plan's unheard sentence in the register of the list's first item;
    what would help most gains the walk that fits (a contact typed whole
    and its parts heard filling; a report sent from a real account; an
    event's time moved by a block). `docs/manual-accessibility-pass.md`:
    items from 84 on, one per ledger entry the phase opened for the
    tester's ear, reader, machine, account or mailbox, appended after
    the last so no number moves, the count line corrected by dating.
    `docs/USER_GUIDE.md` and `docs/changelog.md`: read as one reader
    would, each entry the plans wrote carrying its own until-sentence,
    and any sentence a later plan falsified corrected by dating in the
    page's own manner.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## A\./,/^## B\./' docs/manual-accessibility-pass.md | grep -c '^[0-9]*\. \*\*'` is 83 plus the number of ear-and-account ledger entries the phase's summaries opened, quoted, and the count line says the same number.
    - `grep -c -i 'send feedback' docs/ALPHA_TESTING.md` is at least 1; `grep -c 'separate' docs/ALPHA_TESTING.md` is at least 1.
    - Carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The four pages describe the program this phase built rather than the one before it, and every sentence the tester will test by ear is a numbered line on the listening page.</done>
</task>

<task type="auto">
  <name>Task 2: the closing read, and the four planning files told</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - Every summary's coverage block; `.planning/REQUIREMENTS.md`'s phase 12 section and the FOUND-20 line under phase 9's; the LIST-11 and LIST-19 lines; the traceability rows; `.planning/ROADMAP.md`'s phase 12 entry, plan list and progress row, and the phase 11 line and row that name 12-02 and 12-03; `.planning/STATE.md`'s frontmatter and Current Position; `.planning/WINDOWS.md`'s head.
  </read_first>
  <action>
    The one-pass check first: every `ref` in every coverage block of the
    phase holding a `#` taken as `path#name` and grepped as `fn <name>`
    in that file; a name not found is traced to its rename or split and
    said, and no tick is written on a name that is not in the tree. Then
    `.planning/REQUIREMENTS.md`: a paragraph at the head of the phase's
    section saying what the read did and found; each of the eleven
    requirements read clause by clause, its box ticked only when every
    `[D]` line has a name found, otherwise left open with the reason on
    its lines; the `[S]` lines untouched; the traceability rows the
    same; the coverage count re-taken with the file's own command.
    `.planning/ROADMAP.md`: each criterion with its dated closing
    sentence naming the plan, the merge and the ledger numbers; the plan
    list every line ticked whose summary exists, unticked with the
    reason otherwise; the progress row from the disk; the phase line
    ticked only if every plan ran, otherwise open with the reason and
    the date; the milestone paragraph brought forward one sentence.
    `.planning/STATE.md` by hand: the frontmatter, the Current focus
    line, the Current Position paragraph, `Current Plan:` and `Total
    Plans in Phase:` at column zero, `progress.completed_plans` and
    `total_plans` from `ls .planning/phases/*/*-SUMMARY.md | wc -l` and
    the same over `*-PLAN.md`, the session lines. `.planning/WINDOWS.md`:
    the frontmatter counts corrected from the rows and the JSON, and
    one `unrun-verify` for whatever the phase leaves to a person that no
    plan's own entry names.

    The summary's For a person paragraph: what changed in this round
    over the build the tester has, which issues closed, what only a
    person can settle, what is next (the fifth of the seven groups is
    this phase; phase 13 is the sixth, planned when this phase closes).
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - The one-pass check's numbers quoted: refs read, names found, names not found and where each went.
    - `grep -c '^- \[x\] \*\*\(ALPHA\|EDIT\)-' .planning/REQUIREMENTS.md` plus the unticked form equals 8, and the summary says which are which and why; FOUND-20, LIST-11 and LIST-19 read again and left as their plans set them or corrected with the reason.
    - The roadmap's progress row for phase 12 equals the summaries on disk over the plans on disk; the plan list's ticked lines equal the row's numerator, quoted; `STATE.md`'s two lines agree with the frontmatter and the disk; `head -7 .planning/WINDOWS.md` agrees with the rows and the JSON, quoted.
    - Carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The requirements, the roadmap, the state and the ledger say the same thing about the phase, every tick stands on a name in the tree, and what did not run is said where it would otherwise read as done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a summary's claim to a page's sentence | a page describing a program the tree does not hold |
| a coverage block's name to a tick | a tick standing on a test that was renamed or split |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-40 | Repudiation | a page sentence the phase falsified left standing | medium | mitigate | every sentence re-read against the tree with a command; corrections by dating |
| T-12-41 | Tampering | a requirement ticked on a name not in the tree | medium | mitigate | the one-pass grep over every ref before any tick; the numbers quoted |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing installed; documents and planning files only; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook (`docs_only`), never piped; the full gate once, as the hook on the `git merge --no-ff` into `main`, never by hand before it (CLAUDE.md, "The merge is the full gate", 2026-09-23).
- The one-pass check's numbers in the summary; `the_planning_files_agree_with_themselves` green on the branch and on `main` at the merge.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 12 closes: the pages, the listening lines, the eleven requirements read, the four planning files agreeing.
- Every requirement of the phase carries a dated sentence saying whether it stands and on what.
- The completion marks, all four, for this plan and, re-read, for every plan before it.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-12-SUMMARY.md` when done
</output>
