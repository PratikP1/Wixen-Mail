---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 3
type: execute
wave: 4
depends_on: [12-02.1]
files_modified:
  - src/application/status_sentences.rs
  - src/application/mod.rs
  - src/application/checking_on_a_schedule.rs
  - src/application/mail_sync.rs
  - src/application/bringing_everything_down.rs
  - src/application/trying_again.rs
  - src/presentation/wx_app.rs
  - src/presentation/wx_account_manager.rs
  - src/presentation/wx_managers.rs
  - src/presentation/wx_compose.rs
  - src/presentation/wx_settings.rs
  - tests/every_status_sentence_has_one_shape.rs
  - tests/progress_is_shown_and_results_are_said.rs
  - guards/guards.toml
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-11]
estimate:
  tokens: 38000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every sentence the status bar shows is listed from the code and reads to one shape: what happened, to what, and what to do next when there is something to do; a person's words; one style of ending (LIST-11, #75)."
    - "The four refusals for nothing chosen are one wording per kind of thing; no sentence says cache, queue, flush or sync where a person's word exists; no step that is not a step ends in an ellipsis."
    - "A reading holds new sentences to the shape where a reading can, on the pattern of the_words_that_say_nothing; the channel of every line is unchanged from 10-04."
  artifacts:
    - "src/application/status_sentences.rs: the wordings that were four and are one, the words a status sentence may not use, and the shape check, with cases."
    - "the sites: every literal rewritten in place at its call, the built sentences in checking_on_a_schedule, mail_sync, bringing_everything_down and trying_again reworded where they miss the shape; no test added to wx_app.rs."
    - "tests/every_status_sentence_has_one_shape.rs: the census of sites, the reading over every literal handed to a status call, with companions."
    - "docs/changelog.md: the entry, with the sentences that changed most named."
  key_links:
    - "each status call's literal -> the reading's list of words and endings -> the sentence a person hears: one shape read from the tree, not from a page."
    - "10-04's kinds -> unchanged: the reading in progress_is_shown_and_results_are_said still holds every opening and channel, and this plan moves no line between channels."
---

<objective>
Read every sentence the status bar shows, in one pass, and rewrite each
to one shape in a person's words, with a reading that holds new sentences
to it.

Purpose: #75, raised 2026-09-18 on `1.0.0-alpha.1+149.g744d05ef` after
phase 10 put the download's steps and the watch's three state lines on
the bar without speaking them under the default level, so the bar is read
on its own more than it was. The sample in the issue: the same refusal
worded four ways; trailing punctuation used and not used; "Flushing outbox
queue..." and "No cache available for export" where a person's word would
do; steps that say something is happening and not to what. Phase 11's
own plans add sentences; this plan runs after them so it reads theirs
too, and before the closing read.

Output: the census, every sentence to the shape, the four refusals to one
per kind, the reading, the changelog entry. Closes #75 on the tree side;
whether the bar reads well by ear is the tester's.

**Moved into phase 12 on 2026-09-20.** Written on 2026-09-18 as 11-13 and
deferred at phase 11's close on Pratik's decision of 2026-09-20 under his
token budget; it is 12-03 now, after 12-02, so the pass reads the
separate window's two new sentences ("Opening <host> in a separate
window" and the failure line) as well as every sentence phase 11 added.
Its premises were re-taken against `main` at `0ad66e48` the same day,
below the original ones.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-04-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
**Re-taken 2026-09-20 against `main` at `0ad66e48`, with every phase 11
plan but this one and 12-02 merged.** Command first, then what it said.

```
for c in 'send_status(' 'send_refusal(' 'said_and_shown(' 'set_status_text(' 'UIUpdate::StatusUpdated(' 'UIUpdate::Progress(' 'send_progress('; do grep -rn --include='*.rs' -F "$c" src | grep -v '^\s*//' | wc -l; done
  -> 105, 114, 79, 47, 33, 17, 12 (test modules not excluded); the refusals grew by fifteen and the status calls fell by five since 08197657, mostly 11-07's set commands and 11-07.1's queue; the target's own census over `what_ships` is still the number the summary quotes
grep -n 'No message selected\|Choose a message first\|No conversation selected to delete\|Choose at least one message first\|Choose a conversation first' src/presentation/wx_app.rs
  -> 4581, 11256, 20703 "Choose a message first"; 5293 "No message selected to delete"; 10801 "No message selected"; the two "No conversation selected to delete" sites at :13954 and :13968 are gone (11-07 rewrote the conversation delete over the selection), so that pattern finds five sites in three wordings for one kind today; the three wordings in premise 2 stand, and `Kind::Conversation` stays because a conversation row is still a kind of thing a refusal can name
grep -n 'send_refusal' src/presentation/wx_app.rs | grep -i 'select\|choose\|first\|nothing'
  -> the wider pattern the plan check ran on 2026-09-20 finds two more refusals of the same kind the narrow one missed: :17887 "Nothing is selected in the message list" (a selection, 11-07's) and :5072 "Choose a folder first" (a folder, a fourth kind, so `Kind::Folder` joins the module); seven sites in five wordings across four kinds, and the census target's own count on the day is the number the plan holds, not a fixed six
grep -n 'Flushing outbox queue\|No cache available for export' src/presentation/wx_app.rs   -> :5765 and :2543 (were :5227 and :2372)
ls src/application/status_sentences.rs tests/every_status_sentence_has_one_shape.rs   -> neither exists; both are new
grep -n 'fn what_the_status_line_says\|fn what_arrived\|fn what_to_say_before_waiting' src/application/checking_on_a_schedule.rs src/application/mail_sync.rs src/application/trying_again.rs   -> :233, :182, :97
grep -n 'PROGRESS_OPENINGS' tests/progress_is_shown_and_results_are_said.rs | head -2   -> :38 (six openings), :346
grep -cE '^\s*#\[(tokio::)?test\]\s*$' src/presentation/wx_app.rs   -> 199, 110 records; none added here, the sentence tests rewritten in place
the same for wx_account_manager.rs 14 (7 records), wx_managers.rs 44 (11), wx_compose.rs 43 (7), wx_settings.rs 0 (26), checking_on_a_schedule.rs 20 (3), mail_sync.rs 149 (14), bringing_everything_down.rs 23 (4), trying_again.rs 9 (2), status_line.rs 3 (2)
```

Two sentences 12-02 adds are in the pass by name: "Opening <host> in a
separate window" (an answer, Normal) and "The separate window could not
be started: <err>; opened in the browser" (High). Both already read to
the shape; the reading holds them like every other.

**Taken 2026-09-18 against `main` at `08197657`; every count is re-taken on
the day, after 11-03 to 11-11.3 have added their sentences.** Command first,
then what it said.

**1. The sites, counted the way the issue counted them.**

```
for c in 'send_status(' 'send_refusal(' 'said_and_shown(' 'set_status_text(' 'UIUpdate::StatusUpdated(' 'UIUpdate::Progress(' 'send_progress('; do grep -rn --include='*.rs' -F "$c" src | grep -v '^\s*//' | wc -l; done
  -> 110, 99, 82, 45, 34, 17, 12 at 08197657, test modules not excluded here; the issue's 109, 98, 72, 45, 34, 17, 11 at the same commit had them excluded, and the difference is the test modules' own calls
```

The reading in task 1 excludes test modules through `what_ships` and
prints its own census, which is the number the summary quotes. Some
sentences are built in one place and sent from many: the three status
lines in `checking_on_a_schedule::what_the_status_line_says`, the result
line in `mail_sync::what_arrived`, the runner's six sentences and the
wait's wording in `bringing_everything_down` and `trying_again`; those are
read where they are built.

**2. The inconsistencies the issue names, confirmed.**

```
grep -n 'No message selected\|Choose a message first\|No conversation selected to delete' src/presentation/wx_app.rs   -> 4135 and 10348 "Choose a message first"; 4856 "No message selected to delete"; 4913 "No message selected"; 13954 and 13968 "No conversation selected to delete"
grep -n 'Flushing outbox queue\|No cache available for export' src/presentation/wx_app.rs   -> 5227, 2372
```

Six sites for one refusal in two kinds of thing (a message, a conversation),
and 11-07's set commands add a third kind (a selection). One wording per
kind: "Choose a message first.", "Choose a conversation first.", and for
a set "Choose at least one message first."; the working phrases become
"Sending the mail in the Outbox." and "Nothing is open to export from."
(Added 2026-09-20 from the re-take above: a fourth kind, a folder, with
"Choose a folder first.", which is already the wording at `:5072` and
becomes the module's.)

**3. What a reading can hold and what it cannot.** A reading over the
source can hold: no literal handed to a status call ends in "..." unless
the call is `send_progress` or `UIUpdate::Progress` (a step); every
literal handed to one of the seven calls ends in a full stop or a
question mark, or is one of the openings the step reading already knows;
no literal holds one of the words a person does not use for these things
(`cache`, `queue`, `flush`, `sync` as a noun, `expunge`, `IDLE`, `uid`),
with an exception table for the words that are the thing's name on a page
(Sync is a menu item's name, and stays where the sentence names the item);
the refusal for nothing chosen is one of the three wordings. A reading
cannot hold "what happened, to what, what next" as a grammar; that is
done by hand once, sentence by sentence, and the summary lists every
sentence that changed with its old and new form, which is the record.

**4. The channel of every line is 10-04's and does not move.**

```
grep -n 'PROGRESS_OPENINGS' tests/progress_is_shown_and_results_are_said.rs | head -2   -> the openings the answer channel may not use
```

A sentence reworded keeps its call; a step stays a step and an answer an
answer. 10-04's reading holds the openings, so a reworded step whose new
opening is not in `PROGRESS_OPENINGS` is added there in the same commit,
which 10-04's summary asked for.

**5. The gate for each file.** `src/application/status_sentences.rs` maps
to itself; `src/presentation/wx_app.rs` to `--lib presentation::wx_app::`,
199, none added, its tests that assert a sentence's old words rewritten in
place and each named in the summary; `wx_account_manager.rs` (14, 6
records), `wx_managers.rs` (44, 10), `wx_compose.rs`, `wx_settings.rs` (0,
19) to their filters and coupled targets, none gaining a test; the four
application modules to their filters, their sentence tests rewritten in
place; the new target to itself. `checking_on_a_schedule.rs` has 3
records, `mail_sync.rs` 13, `bringing_everything_down.rs` 4,
`trying_again.rs` 0; a sentence test rewritten in place moves no count.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the census, the words a status sentence may not use, and the shape a reading can hold</name>
  <files>src/application/status_sentences.rs, src/application/mod.rs, tests/every_status_sentence_has_one_shape.rs, guards/guards.toml</files>
  <read_first>
    - `tests/the_words_that_say_nothing.rs`, all of it: the stem list, the walk, the exception shape, the companions.
    - `tests/progress_is_shown_and_results_are_said.rs`, the openings and the call reader that reads a `send_status(` to its closing bracket.
    - `src/presentation/wx_app.rs:4578-4584`, `:11253-11258`, `:20700-20705` ("Choose a message first"), `:5290-5296` ("No message selected to delete"), `:10798-10802` ("No message selected"), `:5763-5767` ("Flushing outbox queue..."), `:2541-2545` ("No cache available for export"), and the two the widened pattern found, `:17885-17889` ("Nothing is selected in the message list") and `:5070-5074` ("Choose a folder first"): the sites in the 2026-09-20 re-take, at `0ad66e48`.
  </read_first>
  <behavior>
    - `status_sentences::NOTHING_CHOSEN`: one wording per kind (`Kind::Message`, `Kind::Conversation`, `Kind::Selection`, `Kind::Folder`, the last for "Choose a folder first" at `wx_app.rs:5072`, which the 2026-09-20 re-take's pattern missed and a wider one over every `send_refusal` naming a choice found), through `nothing_chosen(kind) -> &'static str`; the number of sites is the census's and not a fixed six.
    - `status_sentences::WORDS_A_PERSON_DOES_NOT_USE`: `cache`, `queue`, `flush`, `expunge`, `IDLE`, `uid`, and `sync` as a noun, with `reads_as_a_persons_sentence(sentence) -> Result<(), Complaint>` answering which word or which ending is wrong: a step (`is_step: bool`) may end in "..." and an answer may not; an answer ends in "." or "?"; the words above are refused unless the sentence names a menu item or a page that carries the word, which the caller says.
    - The target: a census over `what_ships` of every file under `src/presentation` and `src/application` for the seven calls, each literal argument read to its closing bracket, printed as a table (file, line, call, sentence) in the summary's shape; every literal held to `reads_as_a_persons_sentence` with the call's kind; every literal that is a refusal for nothing chosen held to be one of the three; the built sentences in the four application modules read through their own functions over fixtures; an exception table with file, line and reason for any sentence a reading cannot judge; companions that plant a "..." on an answer, a `queue`, and a fourth wording, each refused.
  </behavior>
  <action>
    Red first, on a branch: the target bare (red on the day's census: the
    refusal sites the census lists, seven on 2026-09-20 across four kinds,
    the two working phrases, and whatever else the reading finds, each
    quoted in the commit as the day's list) and the
    module's cases by module path. Then the module, registered in
    `application/mod.rs`; the target stays red until task 2 rewrites the
    sites, which is the honest stopping point.

    Guard records: one on `status_sentences.rs` whose break lets `queue`
    through, measured on the library.
  </action>
  <verify>
    <automated>cargo test --lib application::status_sentences:: && cargo test --test every_status_sentence_has_one_shape</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target bare and the module's cases by module path, and quotes the day's census: how many sites per call outside test modules, and every sentence the reading refused.
    - `cargo test --lib application::status_sentences::` passes with at least eight tests, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>Every status sentence in the tree is listed with its call, and the words and endings a reading can judge are one rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: every sentence to the shape, by hand, in place</name>
  <files>src/presentation/wx_app.rs, src/presentation/wx_account_manager.rs, src/presentation/wx_managers.rs, src/presentation/wx_compose.rs, src/presentation/wx_settings.rs, src/application/checking_on_a_schedule.rs, src/application/mail_sync.rs, src/application/bringing_everything_down.rs, src/application/trying_again.rs, tests/every_status_sentence_has_one_shape.rs, tests/progress_is_shown_and_results_are_said.rs, guards/guards.toml</files>
  <read_first>
    - The census the target printed in task 1, which is the work list.
    - 10-04's summary, "Every status line in wx_app.rs, and its kind", the table that sorts every site by channel; a sentence keeps the row it is in.
    - `src/application/checking_on_a_schedule.rs`, `what_the_status_line_says`; `src/application/mail_sync.rs:174-198`, `what_arrived`; `src/application/bringing_everything_down.rs`, the six sentences; `src/application/trying_again.rs`, `what_to_say_before_waiting`.
  </read_first>
  <behavior>
    - Every literal in the census is rewritten in place to the shape: what happened, to what, and what to do next when there is something to do; a person's words; an answer ending in a full stop, a step ending in "..." only when it is a step; every refusal site for nothing chosen the census lists through `nothing_chosen(kind)` (seven on 2026-09-20: `wx_app.rs:4581`, `:11256`, `:20703`, `:5293`, `:10801`, `:17887`, `:5072`); the two working phrases to the wordings in premise 2; a step that said only that something was happening says to what ("Loading Inbox..." not "Loading..."; "Contacts sync requested." not "sync requested"), where the object is at hand.
    - The built sentences follow the same shape where they miss it, with their own tests rewritten in place (the three status lines, the result line, the runner's six, the wait's wording), count unchanged in each module.
    - No line changes channel; `tests/progress_is_shown_and_results_are_said.rs`'s `PROGRESS_OPENINGS` gains any new opening a reworded step uses, in the same commit.
    - Every `wx_app.rs` test that asserts an old wording is rewritten in place onto the new one, count 199 unchanged; the same in the other files.
    - The target's reading is green at the end of this task; the exception table holds only sentences that name a menu item or a page with a reason each.
  </behavior>
  <action>
    Green, on the branch, in commits of a manageable size (one per
    file group is fine, each green through the hook), each naming the
    sentences it changed old and new in its message, because the record
    of the pass is the list. Invoke `cognitive-accessibility` for every
    sentence: plain words, the thing named, the next step when there is
    one, nothing a person did not press.

    The rewording is by hand and is the work; `status_sentences` holds
    what a reading can hold, and the summary carries the full table of
    old and new for the rest.

    Guard records: one on `wx_app.rs` whose break puts a fourth "nothing
    chosen" wording back at the Delete arm, `suite` the target; one on
    `checking_on_a_schedule.rs` whose break puts a working word into a
    status line, measured on the library; each measured. `wx_app.rs` stays
    at 199, quoted before and after; `bash scripts/check.sh --suites-for
    guards/guards.toml src/presentation/wx_app.rs` names the target.

    The records anchored on sentences this task rewrites, the second
    reading `CLAUDE.md` asks for under "Tests that would notice". One is
    known on 2026-09-20: "mark as read acts on every selected message,
    not the cursor row alone" (`file = src/presentation/wx_app.rs`), whose
    `before` ends on the block `return send_refusal(tx, rt, "No message
    selected");` at `wx_app.rs:10801`, the site this task rewrites onto
    `nothing_chosen(Kind::Message)`; its `before` and `after` are
    rewritten onto the new call in the same commit as the site and the
    record is re-measured, or
    `test_every_guard_record_still_names_one_place_in_the_tree` refuses
    the commit. The census is longer than that one probe, so before the
    first rewrite the executor runs the needle over the register for
    every literal the census lists:
    `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];n='<literal>';[print(r['name'],r.get('file')) for r in g if n in (r.get('before','') or '') or n in (r.get('after','') or '')]"`,
    one run per literal (six needles on 2026-09-20, the five refusal
    wordings and the two working phrases with "No message selected"
    covering its longer sibling, answered one hit, on `No message
    selected`, and nothing on the rest; the remainder of the census is
    the executor's), and names in the summary every
    record whose anchor moved, each rewritten and re-measured in the
    commit that moved it.
  </action>
  <verify>
    <automated>cargo test --test every_status_sentence_has_one_shape && cargo test --test progress_is_shown_and_results_are_said && cargo test --lib presentation::wx_app:: && cargo test --lib application::checking_on_a_schedule:: && cargo test --lib application::mail_sync:: && cargo test --lib application::bringing_everything_down:: && cargo test --lib application::trying_again:: && cargo test --lib presentation::wx_managers:: && cargo test --lib presentation::wx_account_manager::</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'No message selected\|No conversation selected to delete' src/presentation/wx_app.rs` is 0 outside comments (`grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'No message selected\|No conversation selected to delete'`); `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'nothing_chosen('` equals the number of nothing-chosen refusal sites the census listed in `wx_app.rs`, quoted (seven on 2026-09-20).
    - `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'Flushing outbox queue\|No cache available for export'` is 0.
    - The target passes with its exception table quoted in the summary; `cargo test --lib presentation::wx_app::` passes with 199 tests, quoted; every other filter's count quoted before and after, unchanged.
    - Two guard records measured and quoted; the record "mark as read acts on every selected message, not the cursor row alone" rewritten and re-measured, and every other record the needle found named in the summary with the same treatment; `cargo test --test house_style` passes at 74.
    - The summary's table lists every sentence changed, old and new, with its file and call.
  </acceptance_criteria>
  <done>Every status sentence reads to one shape in a person's words, the refusals are one per kind, and no line changed channel.</done>
</task>

<task type="auto">
  <name>Task 3: the changelog, the ledger, and the issue closed</name>
  <files>docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - The summary's table from task 2, and `docs/changelog.md`'s `[Unreleased]` section as the phase left it, for any entry that quotes a sentence this plan reworded.
  </read_first>
  <action>
    Invoke `writing-craft`. The changelog entry under `[Unreleased]`,
    Changed: the tester's words; that every status sentence was read in
    one pass and rewritten to one shape, with the shape stated; the four
    refusals now one per kind, with the three wordings; the two working
    phrases named old and new; that no sentence changed which channel it
    goes to; Known limitations: nobody has read the bar by ear since. Any
    earlier entry in `[Unreleased]` that quotes a sentence this plan
    changed is corrected by dating in the entry's own manner.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only
    the tester's ear settles (the bar read on its own with NVDA+End, the
    three watch lines and the download's lines under the new wording, and
    a refusal heard as one sentence).
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The changelog entry names #75 and the three refusal wordings; carriage returns 0; no em dash; none of the six words.
    - The ledger entry is in both halves.
  </acceptance_criteria>
  <done>The pass is on the record with every sentence it changed, and the person who reads the bar knows what changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a sentence to its channel | a rewording that moves a line between step and answer changes what is spoken under the default level, which 10-04 settled |
| a sentence to a test that quotes it | a reworded sentence with a test still asserting the old words is a red the pass has to own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Tampering | a line moved between channels by the rewording | medium | mitigate | every literal keeps its call; 10-04's reading holds the openings and the arms; `PROGRESS_OPENINGS` extended for a new step opening |
| T-12-10 | Repudiation | a sentence changed with no record of the old one | low | mitigate | every commit names old and new; the summary carries the table |
| T-12-11 | Information disclosure | a reworded sentence naming what the old one did not | low | mitigate | the object named is the folder, the account or the count, as before; no subject or body enters a status line |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Three guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the bar read on its own under the new wording; what the reading proves: the words and endings, the three refusals, the channels unchanged.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 75 --comment "Fixed in <merge commit>: every sentence the status bar shows was read in one pass and rewritten to one shape (what happened, to what, and what to do next when there is something to do), in a person's words, with one style of ending; the four refusals for nothing chosen are one wording per kind; the working phrases are gone; a reading now holds every new sentence to the words and endings it can judge, and the full list of what changed is in the plan's summary. Nothing changed which channel a line goes to. What only your ear settles: the bar read on its own with NVDA+End."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Phase 11's roadmap criterion 14 and phase 12's criterion 3 close structurally: the census, the shape by hand, the reading, the refusals, the channels unchanged.
- LIST-11's `[D]` lines each have a named test or reading in the summary, and the box is ticked by this plan with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, LIST-11's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-03-SUMMARY.md` when done
</output>
