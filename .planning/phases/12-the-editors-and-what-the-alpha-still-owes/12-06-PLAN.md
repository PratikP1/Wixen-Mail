---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 6
type: execute
wave: 7
depends_on: [12-05]
files_modified:
  - src/presentation/accessibility/names.rs
  - src/presentation/wx_account_manager.rs
  - src/presentation/wx_settings.rs
  - src/presentation/wx_item_form.rs
  - src/presentation/wx_compose.rs
  - src/application/reading_habits.rs
  - Cargo.toml
  - tests/every_spin_control_names_the_field_a_person_types_in.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [EDIT-01]
estimate:
  tokens: 29000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every number a person sets in the account editor and in Settings is a spin control: Up and Down change it, Page Up and Page Down by more, typing still works, the range and step are the control's own and are said; the check interval first, 1 to 60 minutes (EDIT-01, #73, #35)."
    - "A spin control's typing field has a name on both channels, the same words as its arrows, on every spin control in the tree, held by a reading over MSAA on the built dialogs; the twelve spinner entries among ledger 408 to 425 (408, 409, 410, 412, 413, 414, 419, 420, 421, 422, 424, 425), the Edit Event and Send Later spinners whose fields had no name, close on that reading."
    - "Mark read after offers Immediately, After a number of seconds, and Never as a choice, with a spin control for the seconds enabled for the middle one; the stored value keeps its shape and every setting is still offered by a screen."
  artifacts:
    - "names.rs: name_the_spin_control(spin, name), naming the arrows through the existing accessible and the typing field through the annotation service, one call per spin control in the tree."
    - "wx_account_manager.rs: the check interval as a spin control 1..=60 with its sentence beneath, the spin closure taking a range; wx_settings.rs: Font size 8..=72, Default reminder 0..=1440, Mark read after as a choice and a spin; wx_item_form.rs and wx_compose.rs: their spin controls named through the helper."
    - "tests/every_spin_control_names_the_field_a_person_types_in.rs: every msctls_updown32 in the built account editor, Settings, the item form and the table asker found by class, its buddy read, the buddy's MSAA name equal to the arrows'; the ranges read back."
  key_links:
    - "SpinCtrl.get_handle() -> UDM_GETBUDDY -> the typing field's handle -> IAccPropServices::SetHwndPropStr(PROPID_ACC_NAME) -> the name on MSAA and UI Automation both."
    - "the check interval spin -> check_interval_minutes -> checking_on_a_schedule::which_are_due: the value 10-06 made act."
---

<objective>
Numbers in the editors as spin controls, and every spin control's typing
field named where a screen reader reads it.

Purpose: #73 (the tester on 2026-09-18: the account editor's check
interval "should be a spin control, so Up and Down change it and the
bounds are the control's own") and #35 (2026-09-15: "where numbers are
expected, spin boxes/controls should be used so that users can use
up/down arrow keys to make changes"), which Pratik joined on 2026-09-18
("Same rule, same reading over MSAA for the buddy edit's name (ledger
408); worth fixing together"). Both accessibility. The buddy finding is
older than either: phase 6's scan found the typing field of every
spinner in the Edit Event and Send Later windows nameless on both
channels (twelve entries among ledger 408 to 425), and a new spin
control would add to that class, so the class is fixed first and every
site goes through the fix.

Output: the helper, the check interval, the three Settings numbers, the
other seven sites named, the reading, the pages, the changelog, the
ledger. Closes #73 and #35 on the tree side, and the twelve ledger
entries on the reading.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`. Command first, then what
it said.

**1. The sites, and what each is today.**

```
grep -n 'Check &Interval\|check_interval_minutes\|let spin = \|let tf_with_description\|clamp(1, 60)' src/presentation/wx_account_manager.rs
   -> :1639 `tf_with_description("Check &Interval (min):", "5", WHAT_THE_INTERVAL_DOES)`, a TextCtrl (the issue's line held); :1855 filled as a string; :1254 read back `.parse().unwrap_or(5).clamp(1, 60)`; :1488-1499 the `spin` closure, range fixed at 0..=3650 with `set_accessible_name(&c, &name_from_label(label))` on the arrows
grep -n 'font_size\|default_reminder\|mark_read_after\|undo_send_hold\|draft_autosave\|working_day' src/presentation/wx_settings.rs | head -20
   -> Font size a TextCtrl at :1109-1112, saved at :3397-3402 with `.clamp(8, 72)`; Default reminder a TextCtrl at :2472-2473, saved at :3669-3674 with `.min(1440)`; Mark read after a Choice at :1511 and :1834 over `reading_habits::MarkRead::ALL` (7 entries, `offered_index` at reading_habits.rs:99); the undo-send hold and the autosave interval are SpinCtrls at :1337 and :1380; the working day is two hour lists (Choice) at :2492 onward, which are times and not typed numbers, so they stay
grep -rn 'SpinCtrl::builder' src --include=*.rs | grep -v '^\s*//' | cut -d: -f1 | sort | uniq -c
   -> wx_account_manager.rs 1, wx_compose.rs 2 (the table asker's rows and columns, :3210-3229), wx_item_form.rs 5 (build_date_fields :863 and :875, build_time_fields :917 and :924, build_control :1062), wx_settings.rs 2: ten today, four more after this plan
grep -n 'build_date_fields\|build_time_fields' src/presentation/wx_send_later.rs   -> :132 and :142: Send Later's spinners are the item form's builders, so naming them there names both windows
```

**2. The buddy, and what names it.**

```
awk -F'|' '$2 ~ /^ 408 $/' .planning/WINDOWS.md   -> "the text field of the Starts Day spinner has no name on either channel. set_accessible_name names the up-down arrows, and a Windows spinner is two windows; the field a person types in is the other one. What it takes: get the buddy through UDM_GETBUDDY on the spinner handle from get_handle, then IAccPropServices::SetHwndPropStr with PROPID_ACC_NAME, which the Annotation proxy carries to both channels; needs the Win32_UI_Accessibility, Win32_UI_Controls and Win32_UI_WindowsAndMessaging features. Or a visible static label before each field"
awk -F'|' '$2 ~ /^ 424 $/' .planning/WINDOWS.md   -> the Start time Hour field is named "Start time" on UI Automation from the static label before it and nothing on MSAA: the visible-label route names one channel, the one NVDA does not read for an edit, so the annotation route is the one taken
grep -rn 'UDM_GETBUDDY\|SetHwndPropStr\|IAccPropServices' src --include=*.rs   -> nothing: the helper is new
grep -n 'Win32_' Cargo.toml   -> the eight features listed in 12-05's premise 2; `Win32_UI_Accessibility` (IAccPropServices, PROPID_ACC_NAME; `CLSID_AccPropServices` is the GUID constant at windows-0.62.2/src/Windows/Win32/UI/Accessibility/mod.rs:902 and `SetHwndPropStr` the method at `:1370` of the same file), `Win32_UI_Controls` (UDM_GETBUDDY) and `Win32_UI_WindowsAndMessaging` (SendMessageW) are features of the crate already in the tree, not a crate; the typed COM call is preferred over a hand-written vtable, and the three features go into Cargo.toml with the reason beside them
grep -n 'AccessibleObjectFromWindow' src/presentation/accessibility/names.rs tests/a_kept_folder_reads_as_a_checked_check_box.rs | head -2   -> the MSAA name read the reading reuses
```

**3. What a spin control does with keys, on this toolkit.**

```
grep -n 'impl crate::event::WindowEvents for SpinCtrl' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/spinctrl.rs   -> :201: on_key_down is available on a SpinCtrl
grep -n 'pub fn ' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/spinctrl.rs | head -12   -> value, set_value, set_range, min, max; no set_increment, so a step other than one is a key handler (12-08's, not this plan's; the step here is one)
```

Up, Down, Page Up and Page Down are the native control's; nothing here
binds a key.

**4. The gate for each file.** `names.rs` 11 tests and 0 records, the
helper's pure part (the name it composes) one case added; `wx_account_manager.rs`
14 tests and 7 records, none added; `wx_settings.rs` 0 tests and 26
records, none added; `wx_item_form.rs` 14 and 1; `wx_compose.rs` 43 and
7; `reading_habits.rs` (`grep -cE '^\s*#\[(tokio::)?test\]\s*$'` on the
day) with `offered_index` rewritten in place; the new target at zero
records. The two settings guards in `config.rs` (68 tests, 11 records)
hold that every field is still offered; no field is added, so neither
goes red and neither is touched.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the helper that names both halves of a spin control, applied to every site, and the reading that proves it</name>
  <files>src/presentation/accessibility/names.rs, Cargo.toml, src/presentation/wx_item_form.rs, src/presentation/wx_compose.rs, src/presentation/wx_settings.rs, src/presentation/wx_account_manager.rs, tests/every_spin_control_names_the_field_a_person_types_in.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/accessibility/names.rs:100-230` (the accessible object, `set_accessible_name`, `name_from_label`) and `:1-100` (the file's head and its declarations).
    - `src/presentation/wx_item_form.rs:829-960` and `:1055-1100` (the five spinners and how they are named today).
    - `tests/a_kept_folder_reads_as_a_checked_check_box.rs:190-260` and `tests/a_marker_counts_at_the_start_of_any_line.rs:97-125` (`EnumChildWindows`, `GetClassNameW`).
    - `~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/UI/Accessibility/mod.rs`, the `IAccPropServices` methods (`grep -n 'fn SetHwndPropStr'`).
  </read_first>
  <behavior>
    - `names::name_the_spin_control(spin: &SpinCtrl, name: &str)`: `set_accessible_name(spin, name)` for the arrows; then, on Windows, the buddy from `UDM_GETBUDDY` on `spin.get_handle()`, and `IAccPropServices::SetHwndPropStr(buddy, OBJID_CLIENT, CHILDID_SELF, PROPID_ACC_NAME, name)`, the service created once per process and kept; a failure logged at debug and never a panic; on other platforms the first half only. `spin_field_name(name) -> String` pure: the words the field carries (the same as the arrows'), one case.
    - Every `SpinCtrl::builder` site in the tree calls the helper in place of `set_accessible_name` on the spin: the five in `wx_item_form.rs` (day, year, hour, minute, the `build_control` one), the two in `wx_compose.rs`, the two in `wx_settings.rs`, the one in `wx_account_manager.rs`'s closure.
    - The target: builds, in one `wxdragon::main`, the account edit dialog, the Settings dialog, the item form for an event and the table asker; walks each with `EnumChildWindows` for class `msctls_updown32`; for each, reads the buddy through `UDM_GETBUDDY` and the buddy's MSAA name through `AccessibleObjectFromWindow` and asserts it equals the arrows' MSAA name and is not empty; asserts the count of spinners found is the number this plan expects per dialog (quoted, so a spinner added without the helper is a red); a companion that builds a bare `SpinCtrl` named through `set_accessible_name` alone and asserts the reading sees its buddy nameless, so the reading is shown to see the defect the ledger describes.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare, the `names.rs`
    case by module path with the count check named bare
    (`test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    fires for a test added to `names.rs` if any record names it; premise
    4 says none does today, so name it only if the check prints it).
    Then the three features in `Cargo.toml` with a comment saying which
    call each is for; the helper; the ten sites; `cargo build` between.
    Invoke `elegant-code`: one helper, called ten times, no site names a
    spin control any other way; the COM service is created through the
    crate's typed interface; nothing `unwrap`s outside the tests.

    Guard records: one on `names.rs` whose break skips the buddy half of
    the helper, the target red, `suite` the target; one on
    `wx_item_form.rs` whose break names the minute spinner through
    `set_accessible_name` alone, the target red; each measured.
  </action>
  <verify>
    <automated>cargo test --test every_spin_control_names_the_field_a_person_types_in && cargo test --lib presentation::accessibility::names:: && cargo test --lib presentation::wx_item_form::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare and the module case by module path.
    - `grep -rn 'SpinCtrl::builder' src --include=*.rs | grep -v '^\s*//' | wc -l` and `grep -rn 'name_the_spin_control(' src --include=*.rs | grep -v '^\s*//' | grep -v 'pub fn' | wc -l` answer the same number, quoted (ten before task 2, fourteen after it).
    - `grep -c '^\s*"Win32_UI_Accessibility"\|^\s*"Win32_UI_Controls"\|^\s*"Win32_UI_WindowsAndMessaging"' Cargo.toml` is 3 and `grep -c '^\s*"Win32_' Cargo.toml` is 11; `grep -c '^\[dependencies\]' Cargo.toml` is 1 and no new `name = ` line appears in `Cargo.lock`'s diff (`git diff --stat Cargo.lock` is empty).
    - The target passes with at least 5 tests, quoted, and its summary line names how many spinners it found per dialog.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>Every spin control in the tree names the field a person types in, on the channel NVDA reads, and a reading over the built dialogs would notice one that does not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the check interval, Font size, Default reminder and Mark read after as spin controls</name>
  <files>src/presentation/wx_account_manager.rs, src/presentation/wx_settings.rs, src/application/reading_habits.rs, tests/every_spin_control_names_the_field_a_person_types_in.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_account_manager.rs:1240-1260`, `:1405-1500`, `:1630-1645`, `:1850-1860`.
    - `src/presentation/wx_settings.rs:1100-1150`, `:1500-1520`, `:1825-1845`, `:2465-2480`, `:3395-3403`, `:3665-3675`, and the `MarkRead` read-back (`grep -n 'mark_read_after' src/presentation/wx_settings.rs`).
    - `src/application/reading_habits.rs:1-120` (`MarkRead`, `ALL`, `offered_index`, the stored forms).
    - `src/data/config.rs`, the two settings guards in `mod every_setting_is_acted_on` (`:2533`): `test_every_setting_somebody_can_change_is_read_by_something` at `:2627` and `test_every_setting_somebody_can_change_is_offered_by_a_screen` at `:3004` (`grep -n 'mod every_setting_is_acted_on\|fn test_every_setting_somebody_can_change' src/data/config.rs`).
  </read_first>
  <behavior>
    - The account editor: "Check &Interval (min):" is a spin control from the `spin` closure with a range parameter, 1..=60, initial 5, named through the helper, `WHAT_THE_INTERVAL_DOES` beneath it as its description; filled from `check_interval_minutes` with `set_value`; read back with `value()` and no parse; the three port fields stay text fields, and the summary says why in one sentence (a port is typed, not stepped).
    - Settings: Font size a spin control 8..=72 (the save's own clamp, now the control's range), Default reminder in minutes a spin control 0..=1440 (the save's own bound), each named through the helper, each read back with `value()`; the save's parse-and-clamp lines go. The widget keeps the field name `font_size` (`SettingsWidgets` at `:165`, painted at `:685` as `theme::paint(&font_size, palette.main_surface());`), because the record "a later Settings panel is painted after its controls exist, so its check boxes stay check boxes" anchors on that paint line; with the name kept the anchor holds and the record is re-measured only if the count check names it, and if the executor renames the field the record is rewritten and re-measured in the same commit.
    - Mark read after: a `Choice` with three entries (Immediately, After a number of seconds, Never) and a spin control "Seconds" 1..=600 beside it, enabled only while the middle entry is chosen; `reading_habits` gains `MarkRead::parts(stored) -> (Which, seconds)` and `MarkRead::from_parts` so the stored string keeps its shape (`immediately`, `never`, or the number) and every value the old list offered maps to itself; `offered_index` rewritten in place onto `parts`, its cases rewritten in place (count unchanged); `WHAT_MARK_READ_COUNTS_FROM` stays under the row.
    - The target from task 1 gains the four new spinners in its count and reads the four ranges back (`min()`, `max()`); the two settings guards in `config.rs` stay green (`cargo test --lib data::config::` at 68).
  </behavior>
  <action>
    Red first, on a branch: the `reading_habits` cases by module path
    (rewritten in place where a case asserted the seven-entry list), the
    target's range readings bare. Then the controls, with `cargo build`
    between. Invoke `elegant-code`: the closure takes its range rather
    than growing a second closure; a read-back is `value()` and nothing
    parses a number a spin control already holds; the Choice and the
    spin are wired by one enable-on-change handler.

    Guard records: one on `wx_account_manager.rs` whose break sets the
    interval's range to 0..=3650, the range reading red, `suite` the
    target; one on `reading_habits.rs` whose break maps `never` to
    zero seconds, its case red; each measured. `wx_account_manager.rs`
    stays at 14 and `wx_settings.rs` at 0, quoted.
  </action>
  <verify>
    <automated>cargo test --test every_spin_control_names_the_field_a_person_types_in && cargo test --lib application::reading_habits:: && cargo test --lib data::config:: && cargo test --lib presentation::wx_account_manager::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the readings bare.
    - `grep -c 'tf_with_description("Check &Interval' src/presentation/wx_account_manager.rs` is 0; `grep -c '\.clamp(1, 60)' src/presentation/wx_account_manager.rs` is 0; `grep -c '\.clamp(8, 72)\|\.min(1440)' src/presentation/wx_settings.rs` is 0.
    - `cargo test --lib data::config::` passes with 68, quoted; `application::reading_habits::` with its count quoted before and after, unchanged.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The check interval and the three Settings numbers are spin controls with their ranges as the controls' own, Mark read after still offers Immediately and Never, and nothing stored changed shape.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the changelog, the ledger's twelve entries, and two issues closed</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`, the Settings section's rows for Font size, Default reminder and Mark read after, and the account editor's fields (`grep -n 'Check Interval\|Font size\|Default reminder\|Mark read after' docs/USER_GUIDE.md`).
    - `docs/KEYBOARD_SHORTCUTS.md:640-647` (Account Management) and the Settings subsection (`grep -n 'Settings' docs/KEYBOARD_SHORTCUTS.md | head`).
    - `.planning/WINDOWS.md`, entries 408 to 414 and 419 to 425, both halves.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide: each of the four rows says it is
    a spin control, its range, and that Up and Down move it by one and
    Page Up and Page Down by more, and that typing over the value still
    works; the account editor's field the same; the ports said to be
    typed. The shortcuts page: one sentence in the Settings and Account
    Management subsections on how a spin control is worked, once each,
    not a row per control. The changelog, `[Unreleased]`, Changed, in
    the tester's words, #73 and #35, naming the four controls and the
    class fix for every spinner's typing field; Known limitations:
    nobody has heard a spin control's field name since.

    Ledger, by hand, both halves: entries 408, 409, 410, 412, 413, 414,
    419, 420, 421, 422, 424 and 425 marked fixed on task 1's reading,
    each with `resolved_at` and the merge commit in its reason (the
    tools have no edit; both halves by hand, the JSON's `status` and
    `resolved_at` and the row's cells); 411, 415, 418 and 423 are the
    AM or PM lists' and the Send on Month list's value elements, not
    spinners, and stay open untouched (the summary says they are not
    this plan's); 416 and 417 the same. One `unrun-verify` for
    the tester's ear: the check interval's field and arrows heard with
    one name, Up and Down said as values, Mark read after's three
    entries and the seconds.

    After the merge, the executor closes both issues from the merge
    commit: `gh issue close 73 --comment "..."` and `gh issue close 35
    --comment "..."`, each with the summary's findings in the issue's
    own numbering and the sentence on the ports. Closing an issue is not
    a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'spin control' docs/USER_GUIDE.md` is at least 5; `grep -c 'spin control' docs/changelog.md` is at least 1.
    - Twelve ledger rows read `fixed` in both halves with the same `resolved_at`, quoted by id; `head -7 .planning/WINDOWS.md` shows `fixed_count` twelve higher and `open_count` eleven lower (twelve fixed, one new open), quoted.
    - Carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say which numbers are spin controls and how to work them, the scan's twelve findings are closed on a reading rather than on the scan, and the two issues are closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a spin control to the accessibility tree | a name written through the annotation service onto a handle wx did not create |
| a typed number to a stored setting | the control's range is now the only bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-21 | Tampering | a stored `mark_read_after` value changing shape | medium | mitigate | `parts` and `from_parts` round-trip every old value; the older-file test and the two settings guards stay green |
| T-12-22 | Denial of service | a buddy handle that is not an edit (a spin control built before its buddy exists) | low | mitigate | the helper reads the buddy after the control is built and logs a zero handle at debug rather than failing; the reading counts the spinners found |
| T-12-23 | Information disclosure | a name annotated onto the wrong handle | low | mitigate | the reading asserts the buddy's name equals the arrows' on every spinner in four dialogs |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; three features of the `windows` crate already in the tree are switched on, `Cargo.lock` unchanged; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the field's name and the values under NVDA; what the accessibility workflow settles at the next push: the twelve findings gone from the scan; what the reading proves: the buddy's name on every spinner over MSAA, the ranges.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 6 closes structurally.
- EDIT-01's `[D]` lines each have a named test or reading in the summary and its box is ticked with the date; its `[S]` lines are untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, EDIT-01's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-06-SUMMARY.md` when done
</output>
