---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 8
type: execute
wave: 8
depends_on: [12-07]
files_modified:
  - src/application/time_blocks.rs
  - src/application/mod.rs
  - src/data/config.rs
  - src/presentation/wx_settings.rs
  - src/presentation/wx_item_form.rs
  - src/presentation/managers.rs
  - tests/event_times_move_in_blocks.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [EDIT-03]
estimate:
  tokens: 29000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new event, task or reminder starts at the next block boundary after now and ends one block later, the block being 15 minutes, 30 minutes or 1 hour from a setting on the Calendar and PIM tab, 30 by default (EDIT-03, #41)."
    - "On the minute control Up and Down move the time by the block, to a boundary, with the hour rolling; Left and Right move it by one minute so any time is reachable; typing over the value still works; the same in the three editors."
    - "When the start moves, the end moves with it by the same amount until the person has edited the end, and then it stays where they put it."
    - "The setting is read where the form opens and never captured at startup; the two settings guards go red on arrival, one green with the control and the other only with a reader outside config.rs and wx_settings.rs, so the field, the control and the form-open read are one green commit; a settings file written before the key existed still loads with 30."
  artifacts:
    - "src/application/time_blocks.rs: Block, next_boundary, step_by_block, step_by_minute, end_after, follow, with cases at every boundary of the day."
    - "config.rs: event_length_minutes with its default and the older-file test; wx_settings.rs: the choice on the Calendar and PIM tab; wx_item_form.rs: the key handling on the minute control and the start-moves-end rule; managers.rs: the new item's start and end."
    - "tests/event_times_move_in_blocks.rs: the built form driven with Up, Down, Left and Right on the minute field, the values read back, the end following the start, a new item's start and end from a fixed now."
  key_links:
    - "the minute control's key -> time_blocks::step_by_block or step_by_minute -> the hour and minute controls set -> the end moved by follow() unless edited: the rule pure, the wiring thin."
    - "Settings, Calendar and PIM -> event_length_minutes -> load_stored where the form opens -> Block: read on use, as 11-11.1.1's audit requires."
---

<objective>
Event times move in blocks, with a default length, and single minutes on
Left and Right.

Purpose: #41, the tester on 2026-09-15: "each event should be blocked
for 30 minutes by default unless the user has indicated otherwise in
settings ... The configuration should allow for 15 minutes, 30 minutes,
or 1 hour ... the start time and end time should move by the given 15,
30, or 1 hour blocks respectively. Start time and end time should begin
and end corresponding to the block, e.g., 8:00, 8:15, 8:30, 9:00, etc.
Pressing up and down arrow keys when picking time should move in those
blocks. However the user should be able to choose other times minutely
using left and right arrow keys." His four answers of 2026-09-15: a new
event starts at the next boundary after now (14:37 with 30 gives 15:00,
not 14:30); the end moves with the start unless the person has edited
the end explicitly; Left and Right stepping single minutes is accepted,
typing over the selected value still works; the same block, setting and
keys apply to the task and reminder editors. Nothing is left open.

Output: the pure module, the setting, the key handling, the new item's
times, the reading, the pages, the changelog, the ledger. Closes #41 on
the tree side.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@.planning/phases/11-reading-and-the-list/11-11.1.1-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `0ad66e48`. Command first, then what
it said.

**1. The time controls, as they are.**

```
sed -n 901,946p src/presentation/wx_item_form.rs   -> build_time_fields: the hour spin (0..=23 or 1..=12), the minute spin 0..=59 stepping one, both anchored on `now`'s hour and minute or on the stored HH:MM; nothing aligns, nothing sets an end from a start
grep -n 'fn build_time_fields\|fn build_date_fields\|fn as_stored_time\|fn hour_from' src/presentation/wx_item_form.rs   -> :901, :829, :1535, :1560
grep -n 'wx_item_form::ask_for' src/presentation/managers.rs   -> :1213 and :2733: the events and the tasks and reminders all open through ask_for, so one change reaches three editors, as the tester's fourth answer asks
sed -n 1900,1912p src/presentation/managers.rs   -> the default reminder lead is read with load_stored() where the form opens: the pattern this setting follows
grep -n 'impl crate::event::WindowEvents for SpinCtrl' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/spinctrl.rs   -> :201; on_key_down exists on the control, and no `set_increment`, so the step is a handler
```

Whether a key pressed in the spinner's typing field reaches a handler
bound on the `SpinCtrl` is measured first on a built form (task 2),
the way 11-06 measured M on a real list: `WM_KEYDOWN` posted to the
buddy from 12-06's `UDM_GETBUDDY` read, the handler's arrival recorded.
If it does not arrive, the handler goes on the buddy's own window
through the same mechanism that names it, and the summary says which.

**2. The setting, and the guards that make it red on arrival.**

```
grep -n 'pub event_length\|event_length_minutes' src/data/config.rs   -> nothing: no such setting; default_reminder_minutes at :543 is the reminder lead
grep -n 'mod every_setting_is_acted_on\|fn test_every_setting_somebody_can_change_is_read_by_something\|fn test_every_setting_somebody_can_change_is_offered_by_a_screen\|fn test_a_settings_file_written_before_reading_was_a_setting_still_loads' src/data/config.rs
   -> the module at :2533, the read-by guard at :2627, the offered-by guard at :3004, the older-file test at :1450; a field added is red on arrival in both guards; the read-by guard's reader census (:2533-2620) excludes data/config.rs and wx_settings.rs by name (:2613-2615, "neither is anybody acting on the setting"), so the control alone leaves it red, and only a reader outside those two files, the form-open read in wx_item_form, turns it green; 11-11.1.2 found the same: no green commit can hold fewer than the field, its first reader and the control, which is why task 1 carries all three
grep -n 'Calendar && PIM\|fn build_calendar_pim_tab' src/presentation/wx_settings.rs   -> :578 (the page), :2429 (the builder; :1550 is its controls' doc comment): the tab the choice joins, beside the reminder lead
grep -n 'load_stored' src/presentation/wx_app.rs | wc -l   -> the read-on-use sites; 11-11.1.1's reading in tests/a_setting_saved_applies_without_a_restart.rs holds every startup capture to an allowlist, so this setting is read where the form opens and joins no capture
```

**3. The gate for each file.** `time_blocks.rs` new, zero records;
`config.rs` 68 tests and 11 records, the older-file test rewritten in
place and no test added; `wx_settings.rs` 0 tests and 26 records;
`wx_item_form.rs` 14 and 1 (15 after 12-07), no test added, the wiring
read by the target; `managers.rs` 137 and 52, none added; the new target
at zero records.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the block rules as a pure module, the setting on its tab, and the form-open read that makes the guards green</name>
  <files>src/application/time_blocks.rs, src/application/mod.rs, src/data/config.rs, src/presentation/wx_settings.rs, src/presentation/wx_item_form.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/config.rs:430-560` (the fields around `show_conversations_by_default` and `default_reminder_minutes`, their defaults), `:1440-1470` (the older-file test), and `:2533-2640` and `:3004-3030` (the two guards in `mod every_setting_is_acted_on`, and the reader census's exclusion of `data/config.rs` and `wx_settings.rs` at `:2613-2615`).
    - `src/presentation/wx_settings.rs:2429-2500` (`build_calendar_pim_tab` and its controls; the doc comment at `:1550`), `:3655-3675` (their save).
    - `src/presentation/wx_item_form.rs:271-300` (`ask_for`, where the form opens and the block is read) and `src/presentation/managers.rs:1900-1912` (the reminder lead read with `load_stored()` on use, the pattern).
    - `.planning/phases/11-reading-and-the-list/11-11.1.2-SUMMARY.md`, the paragraph on the three things one green commit must hold.
  </read_first>
  <behavior>
    - `time_blocks::Block { Fifteen, Thirty, Sixty }` with `minutes()`, `from_setting(u32)` answering `Thirty` for anything but 15, 30 and 60, `ALL` and a label each ("15 minutes", "30 minutes", "1 hour"); `Time { hour, minute }`; `next_boundary(now: Time-with-seconds, block) -> Time`: strictly after now, 14:37 with Thirty gives 15:00, 14:30:00 gives 15:00, 23:50 with Fifteen gives 00:00 the next day (`Rolled { time, next_day: bool }`); `step_by_block(time, block, up: bool) -> Rolled`: from a boundary one block, from off a boundary to the next boundary in that direction (14:37 Up gives 15:00, Down gives 14:30), the hour rolling at midnight both ways; `step_by_minute(time, forward: bool) -> Rolled`; `end_after(start, block) -> Rolled`; `follow(old_start, new_start, end, end_edited: bool) -> Time`: the end moved by the same signed minutes unless edited; cases at every boundary named above and at 12-hour faces (11:45 AM Up with Thirty gives 12:00 PM).
    - `config.rs`: `event_length_minutes: u32` with `default_event_length_minutes()` answering 30 through `#[serde(default = ...)]`, doc comment naming #41 and that it is read where a form opens; the older-file test rewritten in place to assert 30 for a file without the key.
    - `wx_settings.rs`: on the Calendar and PIM tab, beside the reminder lead, "New events, tasks and reminders &last:" as a `Choice` over `Block::ALL`'s labels, named and described (the description says the arrow keys move a time by this much), read back into the field.
    - `wx_item_form.rs`: `ask_for` reads the setting once where the form opens, `Block::from_setting(ConfigManager::load_stored().map(|s| s.app_config().event_length_minutes).unwrap_or(30))`, and holds the `Block` for task 2's handlers and opening times; that read is the reader outside `config.rs` and `wx_settings.rs` the read-by guard's census counts, so the field, the control and this read are one green commit (11-11.1.2's finding); nothing reads it at startup.
  </behavior>
  <action>
    Red first, on a branch: the module cases by module path
    (`application::time_blocks::tests::<name>`), the older-file test
    rewritten in place by module path
    (`data::config::tests::test_a_settings_file_written_before_reading_was_a_setting_still_loads`),
    and both settings guards by module path, since they go red the
    moment the field lands:
    `data::config::every_setting_is_acted_on::test_every_setting_somebody_can_change_is_read_by_something`
    and
    `data::config::every_setting_is_acted_on::test_every_setting_somebody_can_change_is_offered_by_a_screen`.
    Then the module and, in one green commit, the field, the control,
    the read-back and the form-open read in `ask_for` (the read-by guard
    excludes `config.rs` and `wx_settings.rs` from its census, so
    without the form-open read that guard stays red and the commit
    cannot be green), with `cargo build` between. Invoke
    `elegant-code`: every rule is a function over `Time` and `Block`,
    none touches a control; `Rolled` carries the day change rather than
    a second return; the read is one expression at the form's opening.

    Guard records: one on `time_blocks.rs` whose break makes
    `next_boundary` answer the nearest instead of the next, the 14:37
    case red; one whose break moves the end when it was edited, the
    `follow` case red; each measured on the library.
  </action>
  <verify>
    <automated>cargo test --lib application::time_blocks:: && cargo test --lib data::config:: && cargo test --lib presentation::wx_item_form::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the module cases, the older-file test and both settings guards by module path, the guards as `data::config::every_setting_is_acted_on::test_every_setting_somebody_can_change_is_read_by_something` and `data::config::every_setting_is_acted_on::test_every_setting_somebody_can_change_is_offered_by_a_screen`.
    - `grep -c 'pub event_length_minutes' src/data/config.rs` is 1; `grep -c 'event_length_minutes' src/presentation/wx_settings.rs` is at least 2; `grep -c 'event_length_minutes' src/presentation/wx_item_form.rs` is at least 1 and `grep -c 'load_stored' src/presentation/wx_item_form.rs` is at least 1.
    - `cargo test --lib application::time_blocks::` passes with at least 12 tests, quoted; `data::config::` with 68, quoted, both guards among them; `presentation::wx_item_form::` with 15, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The block arithmetic is pure and held at its boundaries, the length is a setting on the tab a person would look in, and the form reads it on use, so both settings guards are green in the commit that adds the field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the keys on the minute control, the end following the start, a new item's times, in the three editors</name>
  <files>src/presentation/wx_item_form.rs, src/presentation/managers.rs, tests/event_times_move_in_blocks.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_item_form.rs:200-230` (`OfferedTime`), `:271-430` (`ask_for`), `:901-946`, `:969-1100` (`build_control`, the time fields' naming), `:1535-1580`.
    - `src/presentation/managers.rs:1195-1230` and `:2725-2750` (the two `ask_for` calls and what they prefill).
    - `tests/mark_as_read_says_which_way_it_will_go.rs` (a key posted to a real control and consumed) and `tests/a_marker_counts_at_the_start_of_any_line.rs:97-160`.
    - 12-06's summary for `name_the_spin_control` and the buddy handle.
  </read_first>
  <behavior>
    - Measured first, on the built form in the target's process: `WM_KEYDOWN` for Up posted to the minute spinner's buddy, and whether a `on_key_down` bound on the `SpinCtrl` sees it; the summary quotes the answer, and the handler is bound where the key arrives.
    - On each time's minute control: Up and Down call `step_by_block` with the `Block` task 1's `ask_for` read when the form opened (once per form, never at startup), set the hour and minute controls (and the AM or PM choice on a 12-hour face) and consume the key so the native step of one does not also run; Left and Right call `step_by_minute` and consume the key; every other key is the control's own, so typing over the selection still works; a change to the start time through these keys or by typing moves the end by `follow` unless the end was edited (a flag set by any key or text event on the end's controls that is not a programmatic set); the hour control keeps its native step.
    - A new event, task or reminder (no `existing` time) opens with the start at `next_boundary(now, block)` and the end at `end_after(start, block)`, the date rolled when the boundary crosses midnight; an existing item opens on its stored times, untouched.
    - The target: builds the item form for an event with a fixed `now` (14:37:10) and the block Thirty and reads the start 15:00 and the end 15:30; posts Up to the start's minute field and reads 15:30 and the end 16:00; posts Down twice and reads 14:30 and 15:00; posts Right and reads 14:31 and 15:01; types 16:00 into the end, posts Up on the start, and reads the end still 16:00; the same form for a task and for a reminder opening at the same times; a companion that plants a step of one on Up and is refused.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare (the measurement
    of where the key arrives is written into the summary and into the
    target's head comment, not asserted). Then the handlers and the
    opening times, with `cargo build` between. Invoke `elegant-code`:
    the handlers call the module and set controls; the edited flag is
    one `Cell<bool>` per time; `follow` is called from one place.

    Guard records: one on `wx_item_form.rs` whose break stops
    consuming Up after stepping (the native step runs too, 15:31), the
    target's Up reading red, `suite` the target; one on `managers.rs`
    whose break opens a new event on `now` rather than the boundary,
    the opening reading red, `suite` the target; each measured.
    `wx_item_form.rs` stays at 15 (12-07 added one) and `managers.rs`
    at 137, quoted.
  </action>
  <verify>
    <automated>cargo test --test event_times_move_in_blocks && cargo test --lib presentation::wx_item_form:: && cargo test --lib presentation::managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare.
    - `grep -c 'time_blocks::' src/presentation/wx_item_form.rs` is at least 4; `grep -c 'next_boundary\|end_after' src/presentation/managers.rs src/presentation/wx_item_form.rs` reports at least 1 for one of them.
    - The target passes with at least 7 tests, quoted; `presentation::wx_item_form::` with 15; `presentation::managers::` with 137.
    - Two guard records measured and quoted; the summary says where the key arrived.
  </acceptance_criteria>
  <done>In all three editors the minute control steps by the block on Up and Down and by a minute on Left and Right, a new item opens on the next boundary with its end one block later, and the end follows the start until it is edited.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the changelog, the ledger, and the issue closed</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:172-190` (The Event Window) and the task and reminder editors' rows if any (`grep -n 'Task Window\|Reminder Window\|Edit Task\|Edit Reminder' docs/KEYBOARD_SHORTCUTS.md`).
    - `docs/USER_GUIDE.md`, the calendar's new-event paragraph and the Settings rows for the Calendar and PIM tab.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: in The Event Window, rows
    for Up and Down (by the block), Left and Right (a minute), typing
    over the value, and a sentence that the task and reminder editors
    take the same keys; the guide: the setting's row on the Calendar and
    PIM tab, the new item's start and end, the end following the start.
    The changelog, `[Unreleased]`, Added, in the tester's words, #41,
    with his four answers as the rules; Known limitations: nobody has
    heard a value after a key.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's
    ear (the value spoken after Up, after Left, the end heard following,
    the setting's list).

    After the merge, the executor closes the issue from the merge
    commit: `gh issue close 41 --comment "..."` with the summary's
    findings and his four answers each named as built. Closing an issue
    is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Left\|Right' docs/KEYBOARD_SHORTCUTS.md` grew by at least 1 in The Event Window section (`awk '/^### The Event Window/,/^### The Reader Window/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'Left'` is at least 1).
    - The ledger entry in both halves; carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The keys are documented where the surface that binds them is, the changelog carries the rules in the tester's words, and the issue is closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a key in a native control to a handler | a consumed key that the native control would also have acted on |
| a stored setting to three editors | one number read on use in three places |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-28 | Tampering | Up stepping twice (the handler and the native step) | medium | mitigate | the key is consumed after the step; a guard record holds it; the target reads 15:30 and not 15:31 |
| T-12-29 | Tampering | an end the person set moved by a later start change | medium | mitigate | the edited flag; `follow`'s case; a guard record |
| T-12-30 | Denial of service | Left and Right no longer moving the caret in the typed digits | low | accept | the tester accepted it on 2026-09-15; typing over the selection still works and the shortcuts page says so |
| T-12-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the values after each key under NVDA; what the readings prove: the arithmetic at the boundaries, the keys on the built form, the opening times, the end following.
</verification>

<success_criteria>
- Phase 12's roadmap criterion 8 closes structurally.
- EDIT-03's `[D]` lines each have a named test or reading in the summary and its box is ticked with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, EDIT-03's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-08-SUMMARY.md` when done
</output>
