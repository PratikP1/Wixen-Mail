---
phase: 12-the-editors-and-what-the-alpha-still-owes
plan: 03.2
type: execute
wave: 6
depends_on: [12-03.1]
files_modified:
  - scripts/check.sh
  - scripts/check.test.sh
  - scripts/which-checks.sh
  - scripts/which-checks.test.sh
  - scripts/guards.py
  - .githooks/commit-msg
  - src/service/secret_store.rs
  - src/application/forget.rs
  - tests/the_credential_store_is_ready_for_every_thread.rs
  - tests/every_number_carries_its_command_and_its_date.rs
  - tests/the_planning_files_agree_with_themselves.rs
  - tests/the_words_that_say_nothing.rs
  - guards/guards.toml
  - CLAUDE.md
  - docs/changelog.md
  - docs/development/measurements.md
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
autonomous: true
requirements: [FOUND-23]
estimate:
  tokens: 104000
  raw_tokens: 325000
  tasks: 8
  confidence: low

must_haves:
  truths:
    - "A merge into `main` made with `git merge --no-ff` runs formatting, clippy and what the branch's whole diff earns by the same rules a branch commit is judged by, and not the whole suite and not the release build; a commit on `main` that is not a merge and touches code still earns everything; the whole suite, the release build and the security audit run once a phase, by hand, in the phase's closing plan, and 12-12 is phase 12's; a red one is fixed on the closing plan's branch as a small red and green pair and the gate run again before the merge, on Pratik's answer of 2026-09-23."
    - "`check.sh` knows a merge by `MERGE_HEAD` and tells `which-checks.sh` so with a flag; the branch's diff is the staged diff of the merge commit, which equalled the merge-base-to-tip diff on every one of the fourteen merges read on 2026-09-23; `which-checks.sh` refuses an option it does not know rather than reading it as a branch name."
    - "Each shell suite runs when a path on its own list in `check.sh` is staged; every suite runs for `scripts/shell-suite.sh`, for `.githooks/commit-msg` and for a path under `scripts/` or `.githooks/` that no list places, in every `all` and `all_but_slow` run, and in CI on every push and pull request; a suite runs in `red` when a named test is one of its cases; a script on the list no suite reads runs none; each suite not run gets one line saying so and why. Until Pratik's answer of 2026-09-23 to decision for Pratik 3 this read that the four suites run together when a staged path is under `scripts/` or `.githooks/`, or is `guards/guards.toml` or `.cargo/audit.toml`. A case in `check.test.sh` holds each list to what its suite and the scripts it reaches name, and check's list to every other list; the lists were taken again after tasks 5 and 7 and not only at `481a7918`, and no case in any suite reads this repository's `src/`, because every case that reaches the compiled-in scan answers from a fixture tree of its own. The staged list is read with `--no-renames`, so a staged move lists the path it left as well as the one it reached, and a move out of `scripts/` still owes the suites that read the file it left."
    - "Every integration target the scoped run reaches, changed, coupled by `guards/guards.toml` or read-the-whole-tree, runs in one `cargo test --no-fail-fast` call, each target once, with the flag spelled once, by the runner's line; a test file the commit deleted is left out rather than refusing the whole call; and `red-commit.sh` judges a red exactly as before."
    - "A file the program compiles in with `include_str!` or `include_bytes!` reaches the tests of every source that compiles it: `data/dictionary_en.txt` reaches `service::spellcheck`, `locales/en-US/dates.ftl` reaches `common::catalogue`, and `src/application/mail_controller.rs` reaches `application::sent_copy` as well as its own module. A compiled-in `.md` or `.txt` is not a document."
    - "`scripts/guards.py` keeps each suite's pre-read for a tree it has already read, keyed on the working tree's content with `guards/guards.toml` left out, the records' own text with only their written counts left out, the suite, the settings that move a run or a build (every `WIXEN_*` variable, `RUSTFLAGS`, `CARGO_BUILD_*` and `CARGO_TARGET_DIR`, each present or absent) and the compiler's version, for at most two hours, and says when it used one and which file to delete to read again; `HEAD` and the history, the Windows credential store and the user profile are outside the key, and the two hours bound them."
    - "Two threads reaching the credential store at once both find it ready: the first `keyring::Entry::new` in the process runs once under a lock before any other, on both sites that open an entry (ledger 374's race)."
    - "Every run of `check.sh` that is not a question answered early (`--suites-for`, `--scoped-runs-for`, `--shell-suites-owed`, `--mode-for-this-commit`) names its mode before its first stage and ends, pass or fail, with one line giving each stage's seconds and the total; the questions print only their answers, and a run refused before its mode is settled (an unknown mode, or `which-checks.sh` refusing a red commit on `main`) prints no stage line."
    - "A merge whose diff holds a document runs the document-reading targets as well as what its code earns, so a broken link in a branch that also changed code is found at its merge rather than at the phase's close. The document-reading list is held once in `check.sh`, as two one-line arrays beside `guards_that_read_the_whole_tree`, and the three targets that ask whether they are on it read the array; `which-checks.sh --documents-among` is the one place that says which paths are documents."
  artifacts:
    - "scripts/check.sh: the stage timer and its exit line; `the_staged_paths`, reading the index with `--no-renames`; `--shell-suites-owed`, one line per suite, and the lists it answers from, `the_inputs_of_a_suite`, `what_every_suite_is_owed_for` and `what_no_suite_reads`; `--scoped-runs-for` and the one cargo call, whose runner line alone spells `--no-fail-fast`; the compiled-in sources added to what changed; `--mode-for-this-commit` and the `--merge` flag passed when `MERGE_HEAD` exists; the document-reading list held once as `the_targets_that_read_documents` and `the_modules_that_read_documents`, run by `docs_only` and by a merge holding a document; the three closing messages and the notes on `all` saying where the whole suite runs now; the comment about the suites' cost corrected by dating."
    - "scripts/which-checks.sh: unknown options refused; `--merge`; `--sources-compiling`; `--documents-among`; one rule for what a document is, used on `main`, on a branch and by `check.sh` through `--documents-among`."
    - "tests/every_number_carries_its_command_and_its_date.rs, tests/the_planning_files_agree_with_themselves.rs, tests/the_words_that_say_nothing.rs: `the_documents_only_targets` reads the one-line array, each with a companion over a planted script."
    - "scripts/check.test.sh and scripts/which-checks.test.sh: the cases for each change, each red before its change, the fixture trees every case reaching the compiled-in scan answers from, and the reading that holds each suite's list to what the suite and the scripts it reaches name."
    - "scripts/guards.py: the kept pre-read and its worked examples, and the comment above the pre-read rewritten by dating."
    - "src/service/secret_store.rs and src/application/forget.rs: one guarded opener for a credential entry; tests/the_credential_store_is_ready_for_every_thread.rs: the race taken red in fresh processes."
    - "CLAUDE.md: the gate paragraphs rewritten by dating and a rule under 'Premises a plan checks before execution' that a phase's closing plan runs the full gate and fixes a red one on its branch; .githooks/commit-msg and which-checks.sh's header: their comments the same."
    - "guards/guards.toml: two records, and the seven naming `every_number_carries_its_command_and_its_date` in `tests_last_seen` re-measured after task 7's list pair; docs/development/measurements.md: the per-mode hook rows; .planning/WINDOWS.md: 374 fixed, 373 fixed as built on Pratik's answer of 2026-09-23 with a dated closing note (it read 'given a dated note and left for Pratik's yes or no' until then), the entries tasks 2, 5, 7 and 8 open (task 5's, a reading of the real include lines, was missing from this list until the same day); .planning/REQUIREMENTS.md: FOUND-23 ticked on its `[D]` lines."
  key_links:
    - "git merge --no-ff -> commit-msg -> check.sh sees MERGE_HEAD -> which-checks.sh --merge main <the merge's staged paths> -> the what-changed rules -> affected or docs_only (or all for a dependency, a workflow or the installer script) -> the whole suite waits for the closing plan's scripts/check.sh all."
    - "a staged path, both halves of a move listed by `the_staged_paths` with `--no-renames` -> the_shell_suites_owed, one answer per suite from the lists in check.sh -> run each owed suite and print why each other did not run -> CI's Clippy job runs every suite on every push and pull request whatever the hook did."
    - "data/dictionary_en.txt staged -> which-checks.sh --sources-compiling -> src/service/spellcheck/mod.rs -> not a document -> affected -> the scoped run treats mod.rs as changed -> --lib service::spellcheck:: and its guards.toml couplings."
    - "src/application/mail_controller.rs staged -> the scoped run asks --sources-compiling -> src/application/sent_copy.rs -> --lib application::sent_copy:: beside --lib application::mail_controller::."
    - "a merge whose staged paths hold docs/x.md -> which-checks.sh --documents-among names it -> the_targets_that_read_documents join the one --no-fail-fast call and the_modules_that_read_documents run one --lib each -> the same two arrays docs_only runs, which the three readers read."
---

<objective>
Make the four gate changes and three repairs Pratik approved on
2026-09-23 from the measurement of nine executors, so that a plan
spends less of its time in the commit hook and the full gate runs once
a phase rather than at every merge, and say plainly what that costs.

Purpose: on 2026-09-23 nine executors (11-11.1.1 to 12-03) took 1,294
minutes, 144 a plan, and 360 of those minutes, 28%, were the hook on
81 commits and merges. The measurement's report sits in the session
scratchpad and does not outlive it, so its figures are quoted in
premise 1 below. `481a7918` already removed the hand run of the full
gate before each merge. This plan goes one step further on the same
paragraph: the merge stops being the full gate too, and the full gate
moves to the phase's closing plan. The other six changes each take
minutes out of the hook, or out of a guard measurement, or make the
gate say where its time goes.

Amended later on 2026-09-23 with Pratik's answers to this plan's four
decisions for him: each shell suite runs only for its own inputs (task
3, premise 4, decision 11); a red phase gate is fixed on the closing
plan's branch as a small red and green pair (12-12's task 3, and the
premise rule task 7 writes); no pushes beyond his standing OK, under
which plans that change what is spoken or shown push their branch and
open a pull request and others do not; and ledger 373 closes as built
(task 5). The estimate rose from 94,000 tokens to 104,000, raw 295,000
to 325,000, at the phase README's factor of 0.32, for the per-suite
lists, five further cases, a second planted companion and the reading
that holds the lists.

Output: the gate scripts changed test-first, one case red before each
change; the keyring race fixed and taken red in fresh processes; a
kept pre-read in the guard runner; `CLAUDE.md` and the hook's comment
rewritten by dating; the full-gate step already in 12-12 (written with
this plan); FOUND-23; the per-mode hook times this plan's own commits
print, on the measurements page.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-02.1-PLAN.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-03.1-SUMMARY.md
@CLAUDE.md
@C:/Users/prati/.claude/skills/tdd/references/red-commit-under-a-gate.md
@C:/Users/prati/.claude/skills/tdd/references/recorded-breaks.md
@C:/Users/prati/.claude/skills/tdd/references/source-reading-checks.md
@C:/Users/prati/.claude/skills/tdd/references/fixture-reads.md
</context>

<skills>
Load `tdd` with `references/red-commit-under-a-gate.md` and
`references/recorded-breaks.md` before the first red of task 1: the
first says which failures a red commit names and how a scoped gate
selects them ("Run the gate the way the hook runs it", "Enumerate the
checks a red commit trips for reasons other than missing code"), the
second how a trailer and a record spell a test. Load
`references/source-reading-checks.md` too before tasks 1, 3 and 4,
because three of their cases read `scripts/check.sh` as text. Load
`references/fixture-reads.md` before tasks 3, 4, 5 and 7, because
they build fixture trees and repositories of their own: "A fixture
derived from a real artefact gets, as its first test, a read-only
comparison with the source" governs task 5's copies of the real
include lines, and "A fixture that creates a repository, database or
config of its own proves it is its own by clearing the environment
that locates the inherited one" governs the throwaway repositories of
tasks 3 and 7. Added 2026-09-23 from the checker's third pass, which
found the plan building both kinds without loading the file. Load
`elegant-code` with it. `writing-craft` for `CLAUDE.md`, the changelog
and the requirement lines; `writing-style` for commit messages.
</skills>

<premise_corrections>
Taken 2026-09-23 against `main` at `481a7918`, clean. Command first,
then what it said. Line numbers carry that commit. 12-03.1 runs before
this plan and edits `scripts/check.sh` (an eighth target in
`guards_that_read_the_whole_tree`, which holds seven names at `:47`,
with its paragraph, 12-03.1 task 3), so every `check.sh` line below
`:14` moves; find each by the name beside it. Corrected the same day
from "a ninth target", which the checker counted against the array.

**1. The measurement this plan acts on.** The report is
`scratchpad/executor-time/report.md` in the session that planned this,
with `commits.txt` beside it holding every commit and merge and what
the hook printed. It does not outlive that session, so what the plan
rests on is quoted here.

```
report.md, the table   -> hook runs on commits and merges 360 min (28%), 81 runs; the model's own turns 356 (28%); guard measurement with waits 243 (19%); cargo by hand 137 (11%); check.sh all by hand 99 (8%); reads and edits 100 (8%)
report.md, hook runs   -> code commits 26 (108 min, 4.2 each); merges into main 13 (89 min, 6.9 each); red commits 20 (85 min, 4.25 each); documents only 20 (60 min, 3.0 each); refused 8 (37 min)
report.md, finding 2   -> an unprinted stage of rustfmt, the four suites and start-up took 25 to 35 s on 09-23 and 95 to 145 s on 09-20 and 09-22, cause not found; in affected mode a change to wx_app.rs couples 25 to 29 integration targets, each its own cargo call; in all mode cargo test --all-targets 73 to 113 s compile and 142 to 170 s run, the release build 77 to 86 s
report.md, finding 3   -> every guards.sh invocation pre-reads the whole suite first, 50 to 100 s for the library; one-record invocations spend half their time there
report.md, finding 4   -> flakes failed 4 of 28 full gates (23 min): the keyring race of ledger 374 three times, a live-window test once
report.md, finding 6   -> across the nine plans the merge's full gate found nothing the branch's scoped checks had missed; its only refusals were the keyring flake
report.md, estimates   -> suites only when inputs change 3 to 8 min a plan; one cargo call 1 to 3; the kept pre-read 1 to 2; the keyring fix about 1.3; the full gate per phase: a break only the full suite sees waits on main until the phase closes, about 4 bisect steps
```

**2. A merge runs the hook with `MERGE_HEAD` present, and its staged
diff is the branch's diff.** Measured in a throwaway repository in the
scratchpad, with a `commit-msg` hook that logged what it saw, under
`git version 2.55.0.windows.3`:

```
git merge --no-ff --no-edit feature          -> hook: MERGE_HEAD=<the branch tip> branch=main staged=b.rs c.md (the branch's two files)
the same with the hook refusing               -> "Not committing merge; use 'git commit' to complete the merge.", .git/MERGE_HEAD still present
git commit --no-edit after it                 -> hook: MERGE_HEAD=<the same tip> branch=main staged=d.rs
main moved by a planning commit, then merged  -> hook staged=e.rs only; git diff --name-only HEAD^1...HEAD^2 -> e.rs; HEAD^1 HEAD -> e.rs
```

And on this repository, over the fourteen merges on `main`'s first
parent from `f497785f` (11-11) to `434cd972`:

```
for m in $(git log --merges --first-parent --format=%h -14 main); do git diff --name-only $m^1 $m vs git diff --name-only $m^1...$m^2 ...
  -> same=yes on all fourteen
  -> 12 of 14 hold a shell-suite input (scripts/, .githooks/, guards/guards.toml, .cargo/audit.toml); 9ee0e9a8 (11-12) and 434cd972 do not
  -> the same twelve hold guards/guards.toml; a503ce77 also holds scripts/guards.py and scripts/guards.sh, f497785f also .cargo/audit.toml
  -> 3 of 14 hold a workflow, the installer script or a manifest: a503ce77 (guards.yml), 70f5435a, 8340e5e6
```

So `git diff --cached --name-only`, which `check.sh` already hands
`which-checks.sh` (`check.sh:269-278`), is the branch's diff at a
merge, and `git rev-parse -q --verify MERGE_HEAD` is how `check.sh`
tells a merge from any other commit on `main`. The brief's "merge-base
to tip" is that diff; nothing new has to be computed. Task 3 reads it
with `--no-renames` from then on, which only adds the path a staged
move left (git lists a move by its destination alone otherwise), so
a merge's list can only grow and its mode only strengthen. The count of
merges holding a suite input read "13 of 14" when this plan was first
written, beside a sentence naming the two merges without one; re-taken
by the checker and again by the planner the same day, it is twelve.

**3. `which-checks.sh` reads an option it does not know as the branch
name.** `which-checks.sh:72-89`: the loop takes `--message-file=` and
`--manifest-diff-file=` and breaks on anything else, and the next word
becomes `branch`. So `which-checks.sh --merge main src/lib.rs` answers
today as a branch called `--merge` with `main` as a changed path, which
is `affected`, the answer the new flag should give. **A red case for
`--merge` would pass by accident.** Task 7 therefore first makes an
unknown option a refusal, red on its own case, and only then adds
`--merge`, whose cases are then red for the honest reason.

**4. The shell suites read more than the brief's list.**

```
grep -noE '\$(root|repo_root)/[A-Za-z0-9_./-]+' scripts/*.test.sh scripts/audit.sh scripts/check.sh scripts/which-checks.sh scripts/red-commit.sh scripts/shell-suite.sh
  -> check.test.sh:27 $root/guards/guards.toml
  -> audit.sh:109 $repo_root/.cargo/audit.toml, read through `audit.sh --accepted` by audit.test.sh:174
  -> every other path is under scripts/
which-checks.sh:99   -> calls red-commit.sh names (under scripts/)
```

**The brief's input list misses `.cargo/audit.toml`**: three cases in
`audit.test.sh` (`:185-226`) read the live accepted list. It is added.
`.githooks/commit-msg` is read by no suite; it stays in the list
because it is what runs them, as Pratik's approval named it.

**That grep was taken before task 5 exists, and task 5 would have
made it false.** `which-checks.test.sh`'s `expect` runs its subject
from `run_from="$root"` (`:32-38`), and `check.test.sh`'s `answer`
runs `check.sh` from whatever directory the suite was started in,
which under the hook is the repository root. Once task 5 makes
`which-checks.sh` and `check.sh --scoped-runs-for` scan `src/**/*.rs`
for `include_str!` and `include_bytes!`, every case asked from there,
the existing document cases such as `gsd/x docs/changelog.md`
included, reads this repository's `src/`. A source-only commit that
moved or rewrapped the include in `spellcheck/mod.rs` would then
redden a suite that task 3's rule says is not owed, and nothing would
see it before a push or 12-12. Found by the checker on 2026-09-23.
**The fix is that no case reads the real `src/`**, rather than
widening the rule to every commit touching an `include_str!`: six
source files include themselves (`wx_app.rs`, `theme.rs`, `imap.rs`,
`pop3.rs`, `page_window.rs`, `sent_copy.rs`), `wx_app.rs` among them,
and this phase's editors change `wx_app.rs` on most commits, so the
wider rule would give back most of task 3's saving. So from task 4
on, every case that reaches the scan answers from a fixture tree in
the suite's scratch directory holding copies of the include lines it
needs, `which-checks.test.sh`'s default `run_from` moves to such a
tree in task 5's red, and the answers for the real tree are the
by-hand acceptance commands of tasks 5 and 7. The input list is taken
again after tasks 5 and 7 (their acceptance criteria), because a list
taken before a plan runs goes stale when a later task in the same
plan adds a case that reads a new input.

**Per suite, added later on 2026-09-23 for Pratik's answer to decision
for Pratik 3.** Each suite's reads, taken at `342b019f`, whose
`scripts/` is `481a7918`'s (`git diff --name-only 481a7918 342b019f`
names planning files only). Command first; comment lines were read by
eye and none matched.

```
grep -noE '\$(root|repo_root)/[A-Za-z0-9_./-]+|\$\(dirname "\$0"\)/[A-Za-z0-9_./-]+' scripts/*.sh
  audit.test.sh:30, :33               -> scripts/audit.sh, scripts/shell-suite.sh
  audit.sh:109, :423                  -> .cargo/audit.toml; Cargo.lock at :423, in the real run, which no case reaches (the cases ask --awaiting-a-decision, --accepted, --verdict and --acceptances-still-apply, which exit at :105, :206, :275 and :376)
  red-commit.test.sh:12, :15          -> scripts/red-commit.sh, scripts/shell-suite.sh; red-commit.sh names no file
  which-checks.test.sh:17, :20, :303  -> scripts/which-checks.sh, scripts/shell-suite.sh
  which-checks.sh:99                  -> scripts/red-commit.sh, reached by every case handed --message-file (which-checks.test.sh:440-481)
  check.test.sh:26, :27, :30          -> scripts/check.sh, guards/guards.toml, scripts/shell-suite.sh; :518, :568, :593, :617 the same harness, sourced by the fixture suites it writes
  check.sh:274, :483, :512, :521, :592 -> scripts/which-checks.sh, guards/guards.toml, scripts/red-commit.sh twice, scripts/audit.sh (in `all` only)
  guards.sh:119, mutants.sh:72, build-installer.sh:10 -> the repository root, from scripts no suite reaches
git ls-files scripts .githooks        -> 20 files: the nine above; ten no suite reaches (guards.py, guards.sh, mutants.sh, mutants_report.py, build-installer.sh, make-brand.py, make-icon.py, render_svg.py, msaa-names.ps1, uia-events.ps1); .githooks/commit-msg, which only execs check.sh
```

What `check.test.sh` reaches through `check.sh` grows inside this plan:
today only `--suites-for` and the mode refusal, neither of which calls
another script; from task 3, `--shell-suites-owed red` reads names
through `red-commit.sh`; from task 5, `--scoped-runs-for` asks
`which-checks.sh --sources-compiling`; from task 7,
`--mode-for-this-commit` and `--documents-among` call `which-checks.sh`.
So the lists are written as they stand after task 7, not as the suites
read today, and a case holds them to the reading on every commit that
owes check (task 3, decision 11). What each staged path owes by them:

```
guards/guards.toml                                   -> check
.cargo/audit.toml, Cargo.lock                        -> audit, check
scripts/check.sh, scripts/check.test.sh              -> check
scripts/which-checks.sh, scripts/which-checks.test.sh -> which-checks, check
scripts/red-commit.sh                                -> red-commit, which-checks, check
scripts/red-commit.test.sh                           -> red-commit, check
scripts/audit.sh, scripts/audit.test.sh              -> audit, check
scripts/shell-suite.sh, .githooks/commit-msg         -> every suite
the ten scripts no suite reaches                     -> none
any other path under scripts/ or .githooks/          -> every suite, until a list places it
any other path                                       -> none
```

The saving is the record file. Over the nine plans of premise 1, read
as the commits that exist:

```
for c in $(git rev-list 8340e5e6..434cd972); do git diff --name-only --no-renames $c^1 $c; done, each commit classified by the approved rule and by the lists above
  -> 79 commits and merges from 11-11.1.1 to 12-03, 11 of them merges, each merge read by its first-parent diff as task 7 reads one
  -> the approved rule: 36 owe the four suites, 144 suite runs; 43 owe none
  -> per suite: 32 owe check alone, 31 of them staging guards/guards.toml as their only suite input and a503ce77 (12-02.1's merge) also scripts/guards.py and scripts/guards.sh; 47 owe none, the 43 and four 12-02.1 commits staging only scripts/guards.py or scripts/guards.sh; 32 suite runs
```

That is not premise 1's 81 hook runs: those were runs, refused ones
among them, and these are the commits that exist. At premise 6's times,
about 25 s for the four suites and 7 s for check alone, the approved
rule spends about 900 s over the 79 and the per-suite one about 224 s:
about 11 minutes over nine plans beyond the approved rule, about 1.25 a
plan, and about 29 minutes, 3.2 a plan, against every suite on every
commit, which is what the hook does today (1,975 s). Premise 1's
unprinted stage of 95 to 145 s on 2026-09-20 and 09-22 says the suites
have cost more on other days, and nothing here says how much of that
stage they were.

**5. CI runs the suites whatever the hook does.** `ci.yml:161-170`, in
the Clippy job, `for suite in scripts/*.test.sh; do bash "$suite";
done` under Actions' `bash -eo pipefail`, on push and pull request to
`main` and `develop` (`:3-7`). Confirmed; no change.

**6. Where the suites' cost is stated.** `check.sh:297-298` says they
run in every mode "because they cost milliseconds"; `CLAUDE.md:429-438`
records 108 s on 2026-09-10 and 43 s and 47 s on 2026-09-19. Today, by
hand, one after the other:

```
bash scripts/which-checks.test.sh   -> exit 0 in 9 s, 52 case lines (51 cases and the verdict line)
bash scripts/check.test.sh          -> exit 0 in 7 s, 49 case lines
```

Added later on 2026-09-23 for the per-suite rule: the other two suites,
at `342b019f`, by hand, one after the other, with 12-03.1's executor
building in the same checkout, so these two are the high side.

```
bash scripts/audit.test.sh          -> exit 0 in 3.9 s, 20 case lines
bash scripts/red-commit.test.sh     -> exit 0 in 5.1 s, 26 case lines
```

So the four took about 25 s that day, 9, 7, 3.9 and 5.1, and check
alone 7.

**7. Text readings of `check.sh` that constrain its shape.**

```
check.test.sh:432-457   -> the first 'for suite in' must come before the first 'if [ "$mode" = '
check.test.sh:473-505   -> the line running a suite matches '^[[:space:]]*bash "$suite"' and holds '||' and '>> "$run_log"'; '^run_log=' comes before it
house_style.rs:6354     -> test_one_failing_target_does_not_hide_the_rest: a check.sh line naming more than one --test carries --no-fail-fast
grep -rnF 'guards_that_read_the_whole_tree' tests   -> four readers, one a file: every_number_carries_its_command_and_its_date.rs:502, no_label_is_only_a_space.rs:410, the_planning_files_agree_with_themselves.rs:1259, the_words_that_say_nothing.rs:593 (house_style reads it nowhere)
which-checks.test.sh:25-26  -> "Two traps on EXIT are one trap: the second replaces the first"
check.sh:319 and :511   -> two traps on EXIT today, the second replacing the first in red mode
```

The readers' line numbers first written here were not the ones at
`481a7918`, and the list claimed five targets; corrected the same day
from the checker's grep, re-taken by the planner.

So the array keeps its one-line spelling, the combined call spells
`--no-fail-fast`, the decision whether the suites run is made before
the first mode branch and does not spell `if [ "$mode" = `, and the
stage timer's exit line joins the existing cleanup in one trap rather
than a second one.

**Three more readings, found by the checker on 2026-09-23 and missing
from the list above until then.**

```
grep -rn 'fn the_documents_only_targets' tests
  -> every_number_carries_its_command_and_its_date.rs:473, the_planning_files_agree_with_themselves.rs:1230, the_words_that_say_nothing.rs:564
     each: from a line that begins, at column 0, `if [ "$mode" = "docs_only" ]; then` to the first line beginning `fi`, the `--test NAME` tokens outside comments; each file's test_this_target_runs_on_the_commits_that_could_break_it asserts its own name is there (every_number :513, the_planning_files :1266, the_words :600)
check.sh:389 and :397   -> the block's two `--lib` runs, `help_page::` and `presentation::what_the_scans_can_judge::`, which no test reads
python tomllib over guards/guards.toml   -> 7 records name every_number_carries_its_command_and_its_date.rs in tests_last_seen; 0 name either of the other two; the one record anchored in every_number ("the word target is the only thing excusing a figure from its date", on names_a_target) lies outside the_documents_only_targets
```

Task 7's merge runs the documents list too, so the list would be
needed in a second place. Copying it would make a second list no
reader watches, which is the drift those readers were written to
catch; moving it into an array without changing the readers would
turn all three red. So task 7 holds it once, as two one-line arrays
beside `guards_that_read_the_whole_tree`, and changes the three
readers to read the target array, each red first over a planted
script (decision 10).

**The ordering case reads the first `for suite in` in the file.**
`check.test.sh:432-433` takes `grep -n 'for suite in' | head -1` and
the first `if [ "$mode" = `. The real loop is `check.sh:324`; a second
`for suite in` sits at `:487`, inside `run_the_tests_that_reach_what_changed`,
below the loop only because that function is defined late. Tasks 3, 4
and 5 add functions that must be defined above the seams at `:173`,
so a builder looping over `guards_that_read_the_whole_tree` as `for
suite in` would be found first and the two ordering cases would pass
wherever the real loop went. Task 3's red therefore narrows the
reading to the loop's own spelling, `for suite in
"$(dirname "$0")"/*.test.sh`, with a companion over a planted decoy;
and no new code above the loop spells `for suite in` or
`if [ "$mode" = `.

**`house_style`'s exemption counts whole words.** `runs_the_suite` and
`targets_named_one_by_one` (`house_style.rs:6280-6303`) exempt a
`cargo test` line only when it spells exactly one `--test` or `--lib`
as a word. A runner line such as `cargo test "${arguments[@]}"` or
`cargo test $line` counts none, so it is refused unless it carries
`--no-fail-fast` itself, and that includes the per-module `--lib`
runs, which are exempt today only because they spell `--lib`. So
every `cargo test` line in `check.sh` that does not spell exactly one
`--lib` or `--test` carries `--no-fail-fast` on the same line.

**And the flag has one owner, because cargo refuses it twice.**
Measured at `481a7918` from the repository root: `cargo test
--no-fail-fast --no-fail-fast --test does_not_exist_xyz` answered
`error: the argument '--no-fail-fast' cannot be used multiple times`
in 0.1 s, before selecting a target, and `cargo test --no-fail-fast
--test house_style --test wired --test does_not_exist_xyz` answered
`error: no test target named` in 0.5 s without building anything. So
a runner line carrying the flag for `house_style`'s sake and a seam
line also starting with it would make every scoped run fail at task
4's own green commit. The runner's target line owns the flag, and the
seam's argument lines never hold it (task 4). Until the checker's
third pass on 2026-09-23 this plan required both, and nobody had
composed the two halves into the one command cargo is handed.

**The readers of `guards_that_read_the_whole_tree` are five at the
tip this plan starts from, not four.** 12-03.1 runs first and adds
`the_nvda_cases_wait_for_words_the_program_says`, whose
`test_this_target_runs_on_the_commits_that_could_break_it` reads the
array (12-03.1-PLAN.md `:534`, `:598`). Re-count at the tip with `grep
-rln 'guards_that_read_the_whole_tree' tests` before task 4, and run
every file it names; task 4's verify line names the five.

**8. A combined call keeps the lines `red-commit.sh` reads.**
`red-commit.sh:137-158` reads `test NAME ... ok|FAILED|ignored` lines
anywhere in the run log. `ee6a1058` (12-02.1) named
`test_the_guard_runner_still_obeys_its_own_examples`, which only the
combined tree-guard call reaches (`check.sh:486-503`, `cargo test
--no-fail-fast --test house_style --test wired ...`), and the hook
accepted it (`commits.txt`, 12-02.1 #4, "a red that is exactly the one
this commit named"). `git log -1 --format=%B ee6a1058` carries the
trailer. So one call for every integration target changes nothing the
verdict reads.

**9. What the program compiles in, and what reads documents from a
test.**

```
grep -rn -A2 'include_str!\|include_bytes!' src build.rs | grep -E '"[^"]+\.(txt|md|ftl|ico|toml|json|html|css)"'
  -> src/service/spellcheck/mod.rs:1050 "../../../data/dictionary_en.txt"
  -> src/common/catalogue.rs:153 "../../locales/en-US/dates.ftl"
  -> src/presentation/art.rs:28 "../../assets/icon.ico"
grep -rn 'include_str!\|include_bytes!' src   -> Rust files compiled in by another module: sent_copy.rs:1104 (../service/protocols/imap.rs), :1133 and :1197 (mail_controller.rs), :1187 (mail_session.rs); self-includes in wx_app.rs:27602, theme.rs:1306, imap.rs:2488, pop3.rs:546, page_window.rs:570, sent_copy.rs:1166
which-checks.sh:140 and :379   -> '*.md | *.txt' is a document, on main and on a branch
check.sh:389-415               -> the document-reading targets run in docs_only only, so a commit or a merge mixing code and documents runs none of them
.planning/WINDOWS.md:390       -> ledger 373: locales/ maps to no target; "the whole gate at the merge and CI are what cover it"
grep -rn 'read_to_string(' src --include=*.rs | grep -E '\.md"'
  -> application/contact_groups.rs:538 and service/update_check.rs:990 read docs/privacy.md; service/onenote_page.rs:1174 reads docs/development/the-notes-seam.md; presentation/help_page.rs:421 reads docs/ALPHA_TESTING.md
```

`help_page` is in the documents-only list (`check.sh:389`); the other
three are not, so a commit changing only `docs/privacy.md` never runs
the two tests that read it before the whole gate. **That hole and
ledger 373's both widen under task 7**, because "the whole gate at the
merge" stops being true. Task 5's rule covers an `.ftl` compiled in,
so the hole 373 names is closed in the code; the entry itself waits
for Pratik (decision 6; this said "373 is closed by task 5's rule"
until the checker's second pass on 2026-09-23). Pratik answered later
that day that 373 closes as built, so task 5's green closes the entry
too (decision 6). The documents read at test time
are not compiled in and are ledgered for a later plan (task 8), and the
risk sentence in `CLAUDE.md` names them.

Two more holes widen under task 7, both found by the checker on
2026-09-23. A Rust file another module compiles in: a commit changing
only `mail_controller.rs` never runs `application::sent_copy`'s
readings of it, and the merge's full gate was what caught that; task
5's mapping therefore covers every compiled-in path, Rust or not (a
self-include adds nothing, since the module is already its own run).
And a merge mixing code and documents answers `affected`, which runs
no document-reading target; task 7 has a merge whose diff holds a
document run that list too. That list is paid once a merge, not on
every branch commit, because nearly every green commit here carries a
changelog line beside its code and would pay it every time.

**10. The keyring race, read in the crate.**

```
~/.cargo/registry/src/*/keyring-4.1.5/src/v1.rs:47-55   -> Entry::new: compare_exchange(false, true) on SET_CREDENTIAL_STORE; the winner calls set_credential_store(); every loser goes straight to keyring_core::Entry::new
v1.rs:93-107   -> set_credential_store builds windows_native_keyring_store::Store::new() and only then calls keyring_core::set_default_store
grep -rn 'Entry::new' src --include=*.rs   -> secret_store.rs:23 (the non-test backing) and forget.rs:146 (the uninstall's own store module)
secret_store.rs:219-250   -> test_only_the_seam_and_the_uninstall_open_a_credential_entry_of_their_own allows keyring:: in those two files only
secret_store.rs:18 and :50 -> the in-memory seam is cfg(test), so an integration target reaches the real store (ledger 374's second finding)
```

So the loser's `keyring_core::Entry::new` runs before the winner has
set the store, which is "No default store has been set". A lock that
lets one thread make the first entry while the others wait closes it,
and it has to cover both sites, because two threads reaching one each
race the same flag.

**11. The guard runner's pre-read.**

```
grep -n 'def what_is_already_failing' scripts/guards.py   -> :1160; called once per suite from main at :2340, after wait_until_quiet at :2321-2325
grep -c '>>>' scripts/guards.py; python -m doctest -v scripts/guards.py | tail -3   -> 136; "136 tests in 74 items. 136 passed."
tests/house_style.rs:5084-5131   -> test_the_guard_runner_still_obeys_its_own_examples, floor 40 at :5126
scripts/guards.sh:130   -> python -m doctest scripts/guards.py before the exec, so a red example stops every guards.sh invocation
```

**12. Guard records on the files this plan edits, by the TOML reader
(1,038 records).**

```
python tomllib: records whose file is the file / whose tests_last_seen names it / whose before appears in it
  scripts/check.sh, which-checks.sh, shell-suite.sh, red-commit.sh, check.test.sh, which-checks.test.sh, .githooks/commit-msg   -> 0 / 0 / 0
  scripts/guards.py            -> 1 / 0 / 1: "a record's own time limit reaches the cargo call it is supposed to bound", anchored on `finished = run_to_completion(\n        ["cargo", *arguments],\n        budget,` in cargo(); suite house_style
  src/service/secret_store.rs  -> 0 / 1 / 0: "nothing but the seam and the uninstall sweep opens a credential entry of its own" counts 6 tests there
  src/application/forget.rs    -> 1 / 1 / 1: "an owner of credential store entries that uninstalling never names", anchored on the pgp::keyring_entries() loop, 24 tests
  CLAUDE.md                    -> 1 / 0 / 1: "a figure on a page cannot lose the date it was taken", suite every_number_carries_its_command_and_its_date
```

No shell rule has a record, because `scripts/guards.py` measures a
cargo suite and a shell case is not one; the four suites are what hold
the gate scripts. `CLAUDE.md` is read for figures, so every count,
percentage and duration this plan writes there sits beside its date
and its source.

**13. The plans after this one.** `grep -n 'the full gate once'` over
12-04 to 12-12 finds the verification line `481a7918` wrote in 12-04
to 12-10 and 12-12; 12-11 has none. This plan makes that line false,
so it was rewritten in each when this plan was written, and 12-12
gained its full-gate task the same day. 12-03.1 keeps its line: it
runs before this plan, and its merge is still the full gate.
</premise_corrections>

<decisions_made_here>
Each overrulable with a reason in the summary.

1. **The merge answers what its whole diff earns, by the same rules as
   a branch commit.** So a branch that changed a dependency, a workflow
   or the installer script still runs everything at its merge, three
   of the last fourteen merges; the rest run what reaches the diff.
2. **A commit on `main` that is not a merge and touches code still
   answers `all`.** Code reaches `main` only through a merge here; a
   commit that breaks that rule pays the most, which is the safe
   direction. Planning files the orchestrator commits on `main` stay
   `docs_only`.
3. **The phase's full gate is `scripts/check.sh all` by hand on the
   closing plan's branch, after its last commit and before its merge,
   with `main` unmoved since the branch was cut**, so the tree it reads
   is the tree `main` holds after the merge and a red is found before
   the phase is called closed. A red one is fixed on that branch, a
   small red and green pair, and the gate run again before the merge,
   not a plan of its own: Pratik's answer of 2026-09-23 to decision
   for Pratik 2.
4. **This plan's own merge is the first under the new rule.** It is the
   live test of the merge path, and the whole suite over this plan's
   changes runs in 12-12. The keyring change is the one that reaches
   integration targets the merge will not run, so task 2 runs the
   target the race was found in by hand three times.
5. **`.cargo/audit.toml` joins the suites' inputs** (premise 4).
6. **The compiled-in rule covers every `include_str!` and
   `include_bytes!` in `src/`**, whatever the file: a compiled-in file
   earns what its compiling source earns, and a compiled-in `.md` or
   `.txt` is not a document. Pratik's approval named the dictionary;
   the same rule was applied to the `.ftl`, the `.ico` and the Rust
   files `sent_copy.rs` reads, because each is the same shape of hole
   and every one of them widens checking rather than narrowing it.
   **Ledger 373 is not closed by this plan.** It had deferred the
   `locales/` mapping to version 2's questions, and his approval named
   the dictionary, not the catalogue, so applying the rule there goes
   past what he approved even though it only adds checks. The plan
   builds the rule for every compiled-in file and gives 373 a dated
   note saying so; closing it is decision for Pratik 4, a yes or a no.
   The note says too that if version 2 loads catalogues at run time
   instead of compiling them in, the rule stops reaching them. This
   read "It closes ledger 373 too" until the checker's second pass on
   2026-09-23. **Answered later on 2026-09-23: yes.** Pratik's answer
   to decision for Pratik 4 closes 373 as built, the rule covering
   every file the program compiles in, the date catalogue included, so
   task 5's green closes the entry in both halves and keeps the version
   2 sentence in its closing note.
7. **The kept pre-read lasts two hours**, shorter than the 144-minute
   mean plan, so a kept reading seldom outlives the plan that took it,
   and when it does the tree it describes has not moved. What the key
   cannot see (ignored files such as `oauth.toml` and `.env`, `HEAD`
   and the history, `LANG`, the Windows credential store every
   integration target reaches (ledger 374's second finding), the user
   profile a test may read, the machine's state, a flaky test's luck)
   is what the limit bounds. Anybody who knows one of those moved
   deletes the kept file, `target/guards-pre-read/<suite>`, and the
   next invocation reads the suite again; the kept line names that
   file. A file rather than a flag, because deleting it needs nothing
   new in the runner's arguments. The credential store, the profile
   and the way to discard were added on 2026-09-23 from the checker's
   third pass.
8. **A merge whose diff holds a document runs the document-reading
   targets as well** (premise 9). Only at a merge, because on a branch
   nearly every green commit carries a changelog line beside its code.
9. **No shell case reads this repository's `src/`** (premise 4): the
   cases that reach the compiled-in scan answer from fixture trees,
   rather than widening task 3's rule to every commit touching an
   `include_str!`.
10. **The document-reading list is held once** (premise 7), as
   `the_targets_that_read_documents=(...)` and
   `the_modules_that_read_documents=(...)`, each on one line beside
   `guards_that_read_the_whole_tree`, and the three readers read the
   first. The other shape, a merge running the `docs_only` block
   through code that keeps the block's literal spelling, was set
   aside: the readers key on the block's opening line at column 0, so
   the block could only run at a merge by reassigning `$mode` inside
   it, which is the harder of the two to read. Holding the list once
   costs three reader files, a companion each, and re-measuring the
   seven records that name `every_number_carries_its_command_and_its_date`.
11. **Each shell suite runs for its own inputs** (premise 4), on
   Pratik's answer of 2026-09-23 to decision for Pratik 3. Three
   readings of it are the planner's, each overrulable.
   `.githooks/commit-msg` owes every suite, as the approved list had
   it: no suite reads it, but it is what runs them and hands them the
   commit's environment, the #85 hazard, and it changed in 4 of 2,543
   commits (`git log --oneline -- .githooks/ | wc -l` against the same
   over the tree, 2026-09-23). A path under `scripts/` or `.githooks/`
   that no list places owes every suite, the gate's answer to what it
   cannot place, as `which-checks.sh` answers `all`: a script added
   later costs seconds until somebody places it, rather than skipping
   a suite that reads it. And the reading that holds the lists follows
   every call and every `$root/` and `$repo_root/` path rather than
   guessing which of them a case reaches, which puts `Cargo.lock` on
   audit's list and `.cargo/audit.toml` and `Cargo.lock` on check's, a
   cost on a commit that stages either, none of premise 4's 79. The
   lists are data in `check.sh` rather than read out of the suites
   when the gate runs, because a gate deciding by a pattern over text
   leaks where the text is spelled another way (`CLAUDE.md` on the
   comma in a case name); the pattern holds the lists instead of making
   them. Check's list holds every other list because that reading
   lives in check's suite, so check runs for any gate script, and the
   saving is where premise 4's count put it, the commits whose only
   suite input is `guards/guards.toml`.
</decisions_made_here>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: every run names its mode and ends with each stage's seconds</name>
  <files>scripts/check.sh, scripts/check.test.sh</files>
  <read_first>
    - `scripts/check.sh`, whole, at the tip 12-03.1 left; at `481a7918` the stage headers are `echo "== rustfmt =="` `:282`, `"== clippy =="` `:285`, `"== the scripts that decide what runs =="` `:321`, the documents list `:372`, red's two headers `:514` and `:517`, affected's `:535`, `"== security advisories =="` `:591`, `"== tests =="` `:594`, `"== release build =="` `:603`; the two traps `:319` and `:511`; the mode validation `:253-263`.
    - `scripts/check.test.sh`: `ask_check_sh_for_a_mode` `:382-384` (it runs `check.sh` from an empty directory, where it stops at `touch src/lib.rs`), the mode loop `:416-425`, the text cases `:432-505`.
    - Premise 7.
  </read_first>
  <behavior>
    - The early answers come first and stay as they are: `--suites-for` today, and the questions tasks 3, 4 and 7 add (`--shell-suites-owed`, `--scoped-runs-for`, `--mode-for-this-commit`), each answered and exited before the trap is armed and before the mode line, so their output is only their answer; every `check.test.sh` case compares a question's whole output. Each is answered next to `--suites-for` (`:173-177`), **above the hooks-path note at `:194-205`**, which prints on stdout whenever the configured hooks directory holds no `commit-msg`: every fixture root and throwaway repository in the suites is such a directory, the latter because its hooks path is pinned to an empty one, so an answer given below the note would lead with it and every whole-output comparison would be red for a reason that is not the answer. Logic a question shares with the run, the mode decision at `:234-278` that `--mode-for-this-commit` reuses among it, becomes a function defined above `:194` and called from both. Added 2026-09-23 from the checker's third pass.
    - After the mode is known (given and validated at `:253-263`, or decided by `which-checks.sh` at `:269-278`) and before `touch src/lib.rs`, the run prints `check.sh: mode <mode>`, with `, a merge into main` added once task 7 lands, and arms the trap. That is before the first stage, not the first line: the hooks-path note at `:194-205` prints before it wherever `core.hooksPath` is unset, which includes `check.test.sh`'s empty directory (measured: `git config core.hooksPath` outside the repository exits 1).
    - Each stage starts through one function, which closes the previous stage's clock; the stages are start (from the script's first line to rustfmt), rustfmt, clippy, the scripts that decide what runs, and every header the modes print today.
    - A run refused before the trap is armed prints no stage line: an unknown mode (`:253-263`) and `which-checks.sh` refusing, a red commit on `main` among them (`:274-277`), exit with their refusal alone, as today. The trap is armed only once the mode is settled, so a refusal cannot claim to have run a stage.
    - One exit trap prints, on every exit after it is armed, pass or fail, one line: `check.sh: <mode> <passed|stopped in <stage>> after <N> s: start <a> s, rustfmt <b> s, ...`. The run log's and red mode's clean-up move into that same trap through a list of files to remove, so no second `trap ... EXIT` replaces it.
    - Seconds from bash's `SECONDS`, whole seconds, because the question is minutes.
  </behavior>
  <action>
    Red first, on a branch from `main` after 12-03.1's merge. One
    commit adding to `check.test.sh`:
    - `a run names the mode it was given before it starts`: from the
      empty directory, `docs_only`'s output holds a line
      `check.sh: mode docs_only` (holds, not begins with: the
      hooks-path note prints above it there).
    - `a run that stops before its first stage still says how long it ran`:
      the same run's output holds `stopped in start after`.
    - `a question answered before the run prints no stage line`:
      `--suites-for` over a made-up registry prints no line starting
      `check.sh: mode` or holding `after` and ` s:`. Green on arrival,
      since no stage line exists yet; it is the guard on the green, and
      the message says so and does not name it.
    - `every stage check.sh announces is timed`: no line of `check.sh`
      outside the timing function matches `^[[:space:]]*echo "== `, and
      the function is called at least eight times; this reads the
      script as text, so it carries a companion over a planted script
      that still holds one bare header and must be refused
      (`source-reading-checks.md` rule 5).

    ```
    test(12-03.2): failing cases for the mode line and the stage timings

    Fails-until-green: check::a run names the mode it was given before it starts
    Fails-until-green: check::a run that stops before its first stage still says how long it ran
    Fails-until-green: check::every stage check.sh announces is timed
    ```

    The planted companion and the question case are green on arrival;
    the message says so and names neither. This red
    runs under the gate as 12-03.1 left it, where the suites run in
    every mode, so `check.test.sh`'s failures reach the verdict.

    Green: the function, the trap, the headers through it. From this
    commit on every commit prints its own stage line; copy each into
    the plan's working notes, because the summary is built from them.
  </action>
  <verify>
    <automated>bash scripts/check.test.sh && bash scripts/which-checks.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - The red commit carries the three trailers and `red-commit.sh` accepted it, the hook's line quoted.
    - `bash scripts/check.test.sh` passes with more case lines than the 49 it printed at `481a7918`, quoted.
    - `grep -nE '^[[:space:]]*trap .*EXIT' scripts/check.sh` prints exactly one line, read by the line, and it sits below the last early answer's `exit 0`; a comment mentioning a trap is not counted, which a bare `grep -c 'trap '` would do. The green commit's own hook output ends with the stage line, quoted.
    - `bash scripts/check.sh --suites-for guards/guards.toml src/presentation/wx_managers.rs` prints no line starting `check.sh: mode`, quoted.
    - Carriage returns 0 on both files by `tr -cd '\r' < <file> | wc -c`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>Every hook run from here on says which mode it ran and where its seconds went, which is what every later task's saving is read from.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: two threads reaching the credential store at once both find it ready</name>
  <files>tests/the_credential_store_is_ready_for_every_thread.rs, src/service/secret_store.rs, src/application/forget.rs, guards/guards.toml, .planning/WINDOWS.md, docs/changelog.md</files>
  <read_first>
    - Premise 10, and `keyring-4.1.5/src/v1.rs:40-107` in the cargo registry.
    - `src/service/secret_store.rs`, whole: the non-test `backing` `:17-47`, the public `read` `:153-155`, the allow list test `:219-250`; `src/application/forget.rs:130-170`.
    - `.planning/WINDOWS.md`, entry 374 in both halves (`:391`, `"id": 374`) and 365 (`:382`).
    - `red-commit-under-a-gate.md`, "Read the lint configuration instead of believing it": a function that exists only in the non-test build is dead code in the test build, which `clippy --all-targets -D warnings` compiles.
  </read_first>
  <behavior>
    - `test_threads_reaching_the_credential_store_at_once_all_find_it_ready`, in a new integration target, starts its own test binary again (`std::env::current_exe()`, `--exact` with its own name, an environment variable marking the child) twenty times, one after another, so each child is a fresh process with keyring's flag unset. A child starts sixteen threads held at a `std::sync::Barrier`, and each calls `wixen_mail::service::secret_store::read` with a service name no program stores under and a user of its own; the child fails if any answer is an error. The parent waits for each child with a deadline of 60 s, polling `try_wait`, kills a child that passes it and fails naming it, and fails naming every child that exited non-zero with its stderr. Nothing is written to the store; each read asks for an entry that is not there.
    - The target opens with `#![cfg(windows)]` and a doc comment saying why: the race is in keyring's Windows store setup, and on `other-platforms.yml`'s headless Linux runner keyring reaches the Secret Service (`keyring-4.1.5/src/v1.rs:93-107`), which is not there, so every read would be an error and the target would go red for a reason that is not this race. Elsewhere it compiles to no tests.
    - The fix: one opener for a credential entry in the non-test half of `secret_store`, which makes the process's first `keyring::Entry::new` inside a `std::sync::OnceLock` initialiser (or `Once`) so every other caller waits until the store is set; `backing::entry` and `forget.rs`'s `store::delete` both open through it. It is `pub(crate)` and compiled only where it is used, so the test build holds no dead item; the allow-list test keeps passing with the same two files.
  </behavior>
  <action>
    Red first: the target alone, no change to `src/`.

    ```
    test(12-03.2): failing reading for two threads reaching the credential store at once

    Fails-until-green: test_threads_reaching_the_credential_store_at_once_all_find_it_ready
    ```

    Bare, because it lives in an integration target. **Take the red by
    hand three times before committing** (`cargo test --test
    the_credential_store_is_ready_for_every_thread`), and quote how many
    of the twenty children failed each time. The race is a probability,
    so the red is only a red if it is red every time: ledger 374
    measured about one run in three at ten unsynchronised tests, and
    sixteen threads released together should lose far more often. If
    any of the three passes, raise the children or the threads and take
    it again; if it still passes, stop and report, because a red that
    cannot be taken is a task to raise, not one to write without.

    Green: the opener and the two call sites. Then, by hand, the new
    target five times and `cargo test --test
    a_move_says_what_has_not_been_sent` three times, all passing, quoted
    with their times, because the merge under task 7 will not run the
    targets that reach the real store and the keyring change reaches
    all of them.

    Record, measured with `scripts/guards.sh --remeasure` after the
    green: `file = src/service/secret_store.rs`, `suite` the new
    target, the break taking the lock out so the first entry is opened
    directly; predicted red, the new test. **Its red is a probability,
    and `guards.py` requires the red set to match exactly**, so a sweep
    could report it stale by chance. So measure the record three times
    when it is written (`scripts/guards.sh "<its name>"`, one after the
    other) and quote each result with the failing-child count its log
    shows; all three must be red, and if any is not, raise the children
    or the threads in the test and measure again before writing the
    record, as for the hand reds. From the hand reds' and the three
    measurements' failing-child counts, give in the record's comment the
    chance that a whole run of twenty children has none failing, with
    the arithmetic, and say what a not-red result for this record in a
    sweep means: the race not taken that run, not a stale record, until
    the record run alone twice more is still not red; only then is it
    measured again by hand. **The comment also says where those odds
    were taken**: on this machine, 24 logical cores, by the hand reds
    and the three measurements, and nowhere else. The phase-end sweep
    runs on GitHub's `windows-latest` runners
    (`.github/workflows/guards.yml:117`), a different machine on which
    the race's odds were never measured and may be far lower. So a
    not-red result for this record from a runner shard is not yet a
    finding about the record: the record is run here, alone, twice, and
    the rule above applies to those local runs. Added 2026-09-23 from
    the checker's third pass. The record naming `secret_store.rs` in
    `tests_last_seen` (premise 12) is unaffected: no test is added to
    that file.

    Whether a person could meet the race: read how the program starts
    its account workers and whether two threads can open a credential
    entry at once without a test (the sync start, the OAuth refresh,
    PGP). If they can, `docs/changelog.md` under `[Unreleased]`,
    `### Fixed`, says what a person could have met (an account that
    failed to sign in at start with "Could not reach the credential
    store") and that it was found by the tests, not reported. If they
    cannot, no entry, and the summary names the paths read.

    Ledger, by hand, both halves: 374 fixed for its race with the
    commit and the three reds; a new `todo` carrying 374's second
    finding, that an integration target reaches the real credential
    store because the in-memory seam is `cfg(test)`.
  </action>
  <verify>
    <automated>cargo test --test the_credential_store_is_ready_for_every_thread && cargo test --lib service::secret_store:: && cargo test --lib application::forget::</automated>
  </verify>
  <acceptance_criteria>
    - The three by-hand reds quoted with each run's failing-child count, each above 0; the red commit names the test bare and `red-commit.sh` accepted it.
    - Five greens of the new target and three of `a_move_says_what_has_not_been_sent` quoted.
    - `grep -rnE '(^|[^A-Za-z_])(keyring::)?Entry::new\(' src --include=*.rs` finds exactly one line, the opener's, read by the line, so a `use keyring::Entry;` spelling cannot hide a second call; at `481a7918` it finds two, `secret_store.rs:23` and `forget.rs:146`. `test_only_the_seam_and_the_uninstall_open_a_credential_entry_of_their_own` passes.
    - `cargo test --lib service::secret_store::` and `cargo test --lib application::forget::` each pass with the same count after the task as before it, both counts taken at the start of the task and quoted, since another plan may have moved them since `481a7918`.
    - One record measured three times, all three red, each quoted with its timing line and failing-child count; its comment gives the chance of a run with no child failing and what a not-red sweep result means, says the odds were measured on this machine and not on the `windows-latest` runners the sweep uses, and says a not-red result from a runner shard is run again locally, alone, twice, before the record is called stale, read by the line; entry 374 fixed in both halves; the new entry in both halves; `head -7 .planning/WINDOWS.md` re-counted.
    - Carriage returns 0 on every file touched; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The race that refused three of the last twenty-eight full gates cannot happen in this program, and a test that takes it red in fresh processes holds it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: each shell suite runs only when what it reads has changed</name>
  <files>scripts/check.sh, scripts/check.test.sh, CLAUDE.md</files>
  <read_first>
    - Premises 4 to 7.
    - `scripts/check.sh`: the comment block `:288-316` (the milliseconds sentence at `:297-298`), the loop `:321-330`, the stopped-short check `:337-346`, the refusal `:349-358`.
    - `scripts/red-commit.sh:60-109` (`names`), and `scripts/shell-suite.sh:69-79` for how a suite's name is taken from its file.
    - `CLAUDE.md:423-446`, the paragraphs about the suites and their cost.
    - Premise 4's per-suite reading and decision 11, added for Pratik's answer of 2026-09-23 to decision for Pratik 3; `scripts/audit.sh:98-109`, `:204-206`, `:272-276`, `:373-377` and `:423` (the modes a case reaches, and the one no case reaches); `scripts/check.sh:274`, `:483`, `:512`, `:521` and `:592`, the calls the lists follow.
  </read_first>
  <behavior>
    - `check.sh --shell-suites-owed <mode> [--message-file=F] [--staged | changed-path ...]`, answered beside `--suites-for`, before anything with a side effect and above the hooks-path note (task 1), prints one line per suite, in the order `"$(dirname "$0")"/*.test.sh` gives them, `<suite> yes: <why>` or `<suite> no: <why>`, `<suite>` being the file's name without `.test.sh` as `shell-suite.sh` takes it, and exits 0. One function, `the_shell_suites_owed`, answers for every suite at once, and it loops as `for candidate in`, never `for suite in` (premise 7). Until Pratik's answer of 2026-09-23 this printed one line for the four together, `yes: <why>` or `no: <why>`. `--staged` takes the paths from the index of the repository it is asked in, through the same function the run fills its changed list from, so a case can ask it of a throwaway repository and reach the run's own reading.
    - One function, `the_staged_paths`, defined above the questions, reads the index as `git diff --cached --name-only --no-renames`, and the run's changed list (`check.sh:269-273`, read without the flag today) is filled from it. Git lists a staged move by its destination alone: measured 2026-09-23 under `git version 2.55.0.windows.3`, after `git mv scripts/shell-suite.sh tools/shell-suite.sh` the plain form printed only `tools/shell-suite.sh` and the form with `--no-renames` printed both paths. Without the flag a move out of `scripts/` owes nothing, and a move from `src/` to `docs/` reads as documents only. The flag only ever adds paths, so it only strengthens the owed rule and the mode decision. A deleted file is listed either way, which task 4's builder answers. Found by the checker's third pass on 2026-09-23.
    - The lists are data in `check.sh`, beside `guards_that_read_the_whole_tree`, each on one line at column 0, with the reason for each path in a comment above them (premise 4, decision 11): `declare -A the_inputs_of_a_suite`, then `the_inputs_of_a_suite[audit]="scripts/audit.test.sh scripts/audit.sh .cargo/audit.toml Cargo.lock"`, `the_inputs_of_a_suite[check]="scripts/check.test.sh scripts/check.sh guards/guards.toml scripts/which-checks.sh scripts/red-commit.sh scripts/audit.sh .cargo/audit.toml Cargo.lock scripts/audit.test.sh scripts/red-commit.test.sh scripts/which-checks.test.sh"`, `the_inputs_of_a_suite[red-commit]="scripts/red-commit.test.sh scripts/red-commit.sh"` and `the_inputs_of_a_suite[which-checks]="scripts/which-checks.test.sh scripts/which-checks.sh scripts/red-commit.sh"`; `what_every_suite_is_owed_for=(scripts/shell-suite.sh .githooks/commit-msg)`; and `what_no_suite_reads=(scripts/guards.py scripts/guards.sh scripts/mutants.sh scripts/mutants_report.py scripts/build-installer.sh scripts/make-brand.py scripts/make-icon.py scripts/render_svg.py scripts/msaa-names.ps1 scripts/uia-events.ps1)`. Check's list is written as it stands after task 7, not as `check.test.sh` reaches it today: `--shell-suites-owed red` reads names through `red-commit.sh` from this task, `--scoped-runs-for` asks `which-checks.sh --sources-compiling` from task 5, and `--mode-for-this-commit` and `--documents-among` call `which-checks.sh` from task 7. It also names every other suite's file and list, because the reading below lives in check's suite and reads them all.
    - A suite is owed, the first reason found giving its `<why>`: the mode is `all` or `all_but_slow`; the mode is `red` and a name `red-commit.sh names` reads from the message begins with `<suite>::`; a changed path is on `what_every_suite_is_owed_for`; a changed path is on its own list; or a changed path is under `scripts/` or `.githooks/` and on no list at all, neither a suite's nor `what_every_suite_is_owed_for` nor `what_no_suite_reads`, which the gate cannot place and so answers with every suite, as `which-checks.sh` answers `all` for what it cannot place (decision 11). Not owed otherwise, a path on `what_no_suite_reads` among the rest. A suite with no list is read as holding an empty one, spelled `${the_inputs_of_a_suite[$name]-}`, because `check.sh` runs under `set -u` and a bare lookup of a suite added later would stop the gate on every commit; `every file a suite reads is on its list` is what names such a suite. Until Pratik's answer of 2026-09-23 this bullet made the four suites owed together, for the same two modes, for a red naming a shell case, and for any changed path under `scripts/` or `.githooks/` or equal to `guards/guards.toml` or `.cargo/audit.toml`.
    - `check.test.sh` holds the lists to the reads with one function, `the_reads_no_list_names <root> <check.sh>`, written whole in the red commit, since it is the test's own reading. For each `<root>/scripts/*.test.sh` it takes the files the suite reaches: the suite itself, then, in every reached file under `scripts/` ending `.sh`, every `$root/<path>`, every `$repo_root/<path>` and every `"$(dirname "$0")/<path>"` (resolved from `scripts/`, `..` taken off) on a line that is not a comment, again for each file newly reached until nothing is new. It prints, one per line: a suite with no `the_inputs_of_a_suite[<suite>]=` line in the given `check.sh`; each path a suite reaches that is on neither its list nor `what_every_suite_is_owed_for`; when a suite named `check` exists, each path on another suite's list missing from check's, a rule written rather than found because check's own reads of every suite are spelled `"$root"/scripts/*.test.sh`, which the pattern does not follow; a path on a suite's list and on `what_no_suite_reads` at once; and a reach it cannot resolve to one file, such as a path built from a variable, with its line. It reads more than any case reaches, on purpose: `check.sh` calls `audit.sh` only in `all`, and `audit.sh` names `Cargo.lock` only in its real run, but a list wider than the reads costs seconds on the rare commit that stages one of them, while a list narrower than the reads skips a suite that should have run.
    - The run asks the same function once, before the loop, and the loop keeps its spelling and its `bash "$suite"` line (premise 7). The stage is announced through the stage function as today; each suite the answer owes runs under `-- <suite>`, and each it does not prints `-- <suite>: not run, <why>; CI runs it on every push` in its place. The stopped-short check and the refusal read only the suites that ran. Until Pratik's answer of 2026-09-23 this bullet had one line for the four together, `== the scripts that decide what runs: not run, <why>; CI runs them on every push ==`.
    - The loop stays above the first mode branch, and the decision is spelled with `case`, so the ordering case at `check.test.sh:432-457` still reads a true property; that case's comment gains a dated sentence saying the property is now "decided before any mode branch, and run when owed".
    - The ordering reading finds the suites' own loop and not another (premise 7). It becomes one function in `check.test.sh` taking a script's path, in two steps across the pair. **The red commit** moves today's reading (`check.test.sh:432-433`) into that function unchanged, still the first `for suite in` and the first `if [ "$mode" = `, and calls it on `check.sh` for the existing ordering cases, which stay green, and on the planted decoy for the new case, which is red. **The green** narrows the function to `for suite in "$(dirname "$0")"/*.test.sh` read as a fixed string. So the red case calls a function that exists at the red, and fails because of what the function reads rather than because it is missing. No function this plan adds above the loop spells `for suite in` or `if [ "$mode" = `: the builder of task 4 loops as `for target in`, and the owed decision is a `case`.
    - The comment at `:297-298` is corrected by dating: the old sentence kept, what the suites cost on 2026-09-23 (premise 6) and why they now run only when owed.
  </behavior>
  <action>
    Red first. One commit adding to `check.test.sh` a case per rule of
    the per-suite answer, the owed half and the not-owed half both, one
    reading that the run says so when it skips a suite, and one reading
    that holds the lists to what the suites read:

    - `every suite is owed in an all run`
    - `every suite is owed when nothing was said about the change`
    - `the guard records owe the check suite alone`
    - `the accepted advisories owe the audit suite and the check suite`
    - `the red-commit reader owes the three suites that reach it`
    - `a suite's own file owes that suite and the check suite`
    - `the shared harness owes every suite`
    - `the hook owes every suite`
    - `a script no list places owes every suite`
    - `a script no suite reads owes no suite`
    - `a red commit naming a shell case owes that suite`
    - `a red commit naming only a rust test owes no suite`
    - `a source file alone owes no suite`
    - `documents alone owe no suite`
    - `when a suite is not owed the run says so`
    - `the ordering reading finds the loop over the suites and not another loop`
    - `a staged move out of the scripts folder owes the suites that read the file it left`
    - `every file a suite reads is on its list`

    The first fourteen ask the seam from the suite's scratch directory,
    through one helper that runs `bash "$subject" --shell-suites-owed`
    there and fails the case unless the answer is one line per
    `$root/scripts/*.test.sh`, in that order, each the suite's name then
    ` yes: ` or ` no: `; otherwise it prints the names answered yes,
    sorted and joined by spaces, and the case compares that whole with
    what it wants. `check` for `affected guards/guards.toml`; `audit
    check` for `affected .cargo/audit.toml`; `check red-commit
    which-checks` for `affected scripts/red-commit.sh`; `check
    which-checks` for `affected scripts/which-checks.test.sh`; every
    suite for `all`, for `all_but_slow` with no path, and for `affected`
    with `scripts/shell-suite.sh`, with `.githooks/commit-msg` and with
    `scripts/a-new-helper.sh`, which no list names, "every suite" being
    the names of `$root/scripts/*.test.sh` taken when the case runs
    rather than four written down; nothing for `affected` with
    `scripts/guards.py`, with `src/lib.rs` and with `docs/changelog.md`;
    `which-checks` for `red` with a message file in the scratch
    directory naming `which-checks::a case` and the path `src/lib.rs`;
    nothing for `red` with one naming
    `application::allowed::tests::test_a` beside `src/lib.rs`. All
    fourteen are red because the question does not exist yet:
    `check.sh` refuses it as an unknown mode, and what reaches stdout
    from the scratch directory is at most the hooks-path note, which is
    not one line per suite. The shape check is what makes the four that
    want nothing red for that reason rather than green on arrival, since
    an empty answer names no suite.

    `when a suite is not owed the run says so` cannot be asked of a
    run: from the scratch directory `check.sh` stops at `touch
    src/lib.rs` before the suites' stage, and a run from the repository
    root would start the gate that runs this suite. So it reads
    `check.sh` as text: inside the suites' own loop, before its `bash
    "$suite"` line, a branch prints a line holding `not run` and
    `$suite_name`, and it is red because no such branch exists. It
    carries a planted companion, `a script that skips a suite in silence
    is refused`: the same reading over a planted script whose loop skips
    a suite with `continue` and prints nothing must refuse it
    (`source-reading-checks.md` rule 5).

    `the ordering reading finds the loop over the suites and not another
    loop` runs the ordering reading over a planted script holding a
    decoy `for suite in "${guards_that_read_the_whole_tree[@]}"` in a
    function above the first mode branch, with the real suite loop
    below that branch, and requires the ordering to be refused. **The
    red commit makes the reading one function first**, unchanged (the
    first `for suite in`, the first `if [ "$mode" = `), and calls it on
    `check.sh` for the existing ordering cases and on the decoy for this
    one, so this case is red because the unchanged function takes the
    decoy and finds the planted order sound, and not because the
    function is missing; the existing ordering cases must still pass in
    that run, which the gate holds to, since nothing unnamed may fail.
    The green narrows the function to the loop's own spelling. It is the
    one case in this task about the test's own reading rather than about
    `check.sh`, and it goes first because tasks 3 to 5 are what add
    functions above the loop.

    `a staged move out of the scripts folder owes the suites that read
    the file it left` asks the seam as `--shell-suites-owed affected
    --staged` from a throwaway repository in the suite's scratch
    directory, built the way `which-checks.test.sh:252-266` builds one
    (hooks path pinned to an empty directory, identity set locally,
    `git init -b main`), under the harness that clears the five git
    variables, and never pointing at this repository
    (`fixture-reads.md`, the paragraph on a fixture that creates a
    repository): a committed `scripts/red-commit.sh`, any content, moved
    with `git mv` to `tools/red-commit.sh` and staged, answered `check
    red-commit which-checks`. It is red today because the question does
    not exist, and after the green it goes red again if
    `the_staged_paths` loses `--no-renames`, since the index then lists
    `tools/red-commit.sh` alone, which is outside `scripts/` and on no
    list, and the answer is no suite. Task 7's pair B reuses the same
    repository helper.

    `every file a suite reads is on its list` runs
    `the_reads_no_list_names "$root" "$subject"` and requires it to
    print nothing; it is red because `check.sh` holds no list yet, so
    every suite is printed as having none. Its planted companion, `a
    suite reading a file its list does not name is refused`, builds a
    tree in the scratch directory holding `scripts/planted.test.sh`,
    which names `$root/scripts/planted.sh`,
    `$root/scripts/shell-suite.sh` and `$root/data/planted.txt` in a
    quoted heredoc so `$root` stays text, beside a planted `check.sh`
    whose one list, `the_inputs_of_a_suite[planted]`, names the first
    two and whose `what_every_suite_is_owed_for` names the harness; the
    reading must print `data/planted.txt` for `planted`.

    The two companions are green on arrival, and the message says so
    and names neither. Write the descriptions in full: no comma, no
    ` ... `, none shared (`shell-suite.sh:104-128`). Every red case is
    named in the trailer, one `Fails-until-green: check::<desc>` line
    each. Until Pratik's answer of 2026-09-23 this list held thirteen
    cases for one rule over the four suites together, answered `yes:`
    or `no:` once for all four, and its move case moved an unlisted
    `scripts/x.sh`.

    This red runs under task 1's gate, where the suites still run in
    every mode. The green runs under its own rule and stages
    `scripts/check.sh` and `scripts/check.test.sh`, so check is owed and
    the other three are not, and its own hook output prints a not-run
    line for each of those three; say both in the messages. (This
    paragraph said the green's "suites are owed" until Pratik's answer
    of 2026-09-23 made the rule per suite.) `CLAUDE.md`'s suites
    paragraphs change in the green, by dating: "runs on every invocation
    of `check.sh`, in every mode" was true until this commit; the new
    rule, each suite for its own list, every suite for the shared
    harness, the hook or a script no list places, none for a script no
    suite reads, every suite in `all` and `all_but_slow`, with the
    reason for `.cargo/audit.toml` and for check's list holding every
    other; the reading that holds the lists; CI as the net; Pratik's
    answer of 2026-09-23 as why the rule is per suite; and the figures
    from premises 4 and 6 with their dates and conditions. Every figure
    there sits beside its date and source, because
    `every_number_carries_its_command_and_its_date` reads the file.

    Before the green commit, stage it and run `bash scripts/check.sh
    --shell-suites-owed affected --staged` from the repository root,
    which reads the staged list the way the hook would
    (`red-commit-under-a-gate.md`, "Run the gate the way the hook runs
    it"), and quote the answer: `check yes:` and a `no:` line for each
    other suite, because the green stages `scripts/check.sh` and
    `scripts/check.test.sh` and nothing on another list. It read "`yes:`
    because the green stages `scripts/check.sh`" until Pratik's answer
    of 2026-09-23.
  </action>
  <verify>
    <automated>bash scripts/check.test.sh && bash scripts/which-checks.test.sh && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - The red names all eighteen cases and `red-commit.sh` accepted it; the two planted companions and the existing ordering cases, now calling the extracted reading, quoted green in the same run. This read "thirteen cases" and "the planted companion" until Pratik's answer of 2026-09-23.
    - `grep -n 'git diff --cached --name-only' scripts/check.sh`, read by the line, prints one line, inside `the_staged_paths`, and it carries `--no-renames`; the run's changed list is filled from that function.
    - `grep -cF 'for suite in "$(dirname "$0")"/*.test.sh' scripts/check.test.sh` is at least 1 and the ordering cases pass; `grep -n 'for suite in' scripts/check.sh` read by the line shows one match above the first mode branch, the suites' loop.
    - From the repository root, each quoted, "a line per suite" being `ls scripts/*.test.sh`, taken at the start of the task and quoted (four at `342b019f`): `bash scripts/check.sh --shell-suites-owed affected src/lib.rs` prints a `no:` line per suite; `bash scripts/check.sh --shell-suites-owed affected guards/guards.toml` prints `check yes:` and a `no:` line for each other suite; `bash scripts/check.sh --shell-suites-owed affected .cargo/audit.toml` prints `audit yes:`, `check yes:` and a `no:` line for each other; `bash scripts/check.sh --shell-suites-owed affected scripts/shell-suite.sh` and `bash scripts/check.sh --shell-suites-owed all` print a `yes:` line per suite; `bash scripts/check.sh --shell-suites-owed affected scripts/guards.py` prints a `no:` line per suite. Until Pratik's answer of 2026-09-23 this bullet asked for one `no:` line for `src/lib.rs` and one `yes:` line for `.cargo/audit.toml`.
    - `test "$(grep -c '^the_inputs_of_a_suite\[' scripts/check.sh)" -eq "$(ls scripts/*.test.sh | wc -l)"` exits 0, and the lines it counts are read; `every file a suite reads is on its list` passes over the real tree in the green's run, and its companion prints `data/planted.txt` for the planted suite, both quoted.
    - The old sentence is kept, found wrap-tolerantly with the comment markers taken off first, since the dating may rewrap it and a comment line joined to the next reads `cost # milliseconds`: `test "$(sed 's/^[[:space:]]*#[[:space:]]*//' scripts/check.sh | tr '\n' ' ' | grep -o 'cost milliseconds' | wc -l)" -eq 1` exits 0, and the one site, read by the line, sits inside a dated correction. The `sed` here reads and prints; it edits nothing.
    - The green commit's own hook output runs check and prints a not-run line for each other suite, and the first commit after it that owes no suite prints one for every suite; by `<which_commit_runs_under_which_gate>` that is task 6's red, which stages only `scripts/guards.py`; both quoted in the summary. This bullet named task 7 pair L's red as the first commit to stage no suite input until Pratik's answer of 2026-09-23 put `scripts/guards.py` on the list no suite reads.
    - Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>A commit that touches nothing the suites read no longer pays for them, a commit that does still does, and the run says which.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: every integration target the scoped run reaches builds in one cargo call</name>
  <files>scripts/check.sh, scripts/check.test.sh</files>
  <read_first>
    - `scripts/check.sh:438-505`, `run_the_tests_that_reach_what_changed`, and `:78-166`, the coupling mapping.
    - Premises 7 and 8; `house_style.rs:6280-6303` (`runs_the_suite`, `targets_named_one_by_one`) and `:6354-6380`.
  </read_first>
  <behavior>
    - `check.sh --scoped-runs-for <registry> [changed-path ...]`, answered beside `--suites-for`, before any side effect and above the hooks-path note (task 1), prints one line per cargo invocation the scoped run would make, as the arguments the builder hands the runner: one `--lib <module>:: -- --test-threads=4` line per changed source module, as today, and at most one line of `--test <target>` pairs, for every changed test file that still exists under the directory asked from, every target `guards/guards.toml` couples to a changed file, and every target in `guards_that_read_the_whole_tree`, each once. No line of the answer holds `--no-fail-fast`: the runner's line adds it (premise 7).
    - A changed `tests/*.rs` whose file is gone, deleted or moved away, which the staged list holds either way and `--no-renames` now lists for a move too (task 3), is left out of the target line. Today such a path is `cargo test --test <gone>` on its own call, which fails and refuses the commit; in one combined call cargo would refuse the whole call before any target ran (`error: no test target named`, premise 7), so a branch that deleted an integration target would hide every other target's result and could not be committed or merged. Found by the checker's third pass on 2026-09-23.
    - `run_the_tests_that_reach_what_changed` runs exactly what that function builds, from the same arrays, and still prints `-- <name>` per module and target so the log reads as it did. Nothing in `check.sh` calls `cargo test --test "$...` on a single target any more.
    - The array keeps its one-line spelling `guards_that_read_the_whole_tree=(...)`.
    - The builder is defined above the seams and loops as `for target in`, never `for suite in` (premise 7). **`--no-fail-fast` has one owner, the runner's target line**, `cargo test --no-fail-fast "${targets[@]}"`, which spells it once: `house_style` counts whole words and a line built from an array counts none, so the runner's line must carry it, and cargo refuses it given twice, so the seam's lines must not (premise 7). The per-module runner line, `cargo test --lib "${module}::" -- --test-threads=4`, spells `--lib` as a word and is exempt. Until the checker's third pass on 2026-09-23 this bullet had the runner carry the flag "whatever it is handed" while the seam's target line also started with it, which composed to the flag twice.
  </behavior>
  <action>
    Every case below asks `--scoped-runs-for` from a fixture root in
    the suite's scratch directory, `( cd "$tree" && bash "$subject"
    --scoped-runs-for "$registry" ... )`, with the real registry passed
    by path, never from the repository root: from task 5 on the seam
    scans `src/` under the directory it is asked from (premise 4), and a
    case asked from the root would then read this repository's sources.
    One helper does it, and `answer`, which asks `--suites-for`, moves
    to the same root in the same commit so no seam case reads the real
    tree. The fixture holds no `src/` in this task; task 5 adds the
    files its cases need. It does hold an empty `tests/<name>.rs` for
    each test file a case names as changed, `tests/house_style.rs`
    among them, because the builder leaves out a changed test file that
    does not exist under the directory it is asked from; `tests/gone.rs`
    is the one it deliberately does not hold.

    Red first, cases in `check.test.sh`: `the scoped integration
    targets are one cargo call` (a coupled source file and a changed
    test file give exactly one line naming a `--test` target, matched as
    a whole word, `(^| )--test( |$)`, the way `house_style`'s
    `targets_named_one_by_one` matches it, so `--test-threads=4` on a
    `--lib` line is not counted, and no line of the answer holds
    `--no-fail-fast`, which the runner's line owns);
    `every target the scoped run reached before is
    still reached` (that line's targets equal the changed target, the
    coupled ones from the real registry and every whole-tree target,
    compared as a set); `a target named twice is run once` (a changed
    `tests/house_style.rs`); `a changed source module is still its own
    library run`; `a changed lib.rs is still no library run`; `a
    deleted test file is not handed to cargo` (a changed
    `tests/gone.rs`, which the fixture root does not hold, gives a
    target line with no `--test gone` on it); `no integration target is
    run by a cargo call of its own`, reading `check.sh` as text with a
    planted companion that must be refused. One
    `Fails-until-green: check::<desc>` line each.

    **Every seam case also asserts that the answer holds exactly one
    line holding `--test` as a whole word**, matched as
    `(^| )--test( |$)`. Today `--scoped-runs-for` is refused as an
    unknown mode (`check.sh:253-263`) and prints nothing on stdout, so
    a case asserting only an absence, `a changed lib.rs is still no
    library run`, the "not twice" half of `a target named twice is run
    once` and `a deleted test file is not handed to cargo`, would hold
    on arrival, and `red-commit.sh` refuses a named test that passed.
    Every scoped answer has that one line, because the whole-tree
    targets are always in it, so the assertion is true after the green
    and false before it for the honest reason. Found by the checker on
    2026-09-23, and reworded the same day from "exactly one line
    starting `--no-fail-fast`" by its third pass, because that spelling
    put the flag in the seam's line as well as the runner's (premise
    7).

    Green: the function, the runner on it. Stage and read what
    `--scoped-runs-for` answers for the staged list before committing.
    Then, still before committing, compose the command the runner will
    hand cargo from the seam's own answer for a real file and ask
    cargo's flag parse of it against a target that does not exist (the
    third acceptance bullet), so a flag spelled twice is seen before
    the green commit rather than by the hook refusing it. The seam's
    cases hold the seam's half and `house_style` holds the runner's
    half; only the composed command holds the two together.
    The first later commit that changes a file coupled to several
    targets (task 7 does not; 12-04 will) is where the saving shows;
    here, quote the stage line of this task's green.
  </action>
  <verify>
    <automated>bash scripts/check.test.sh && cargo test --test house_style && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test no_label_is_only_a_space && cargo test --test the_planning_files_agree_with_themselves && cargo test --test the_words_that_say_nothing && cargo test --test the_nvda_cases_wait_for_words_the_program_says</automated>
  </verify>
  <acceptance_criteria>
    - The red names all seven cases; `red-commit.sh` accepted it.
    - `bash scripts/check.sh --scoped-runs-for guards/guards.toml src/presentation/wx_managers.rs | grep -cE -- '(^| )--test( |$)'` is 1 and `bash scripts/check.sh --scoped-runs-for guards/guards.toml src/presentation/wx_managers.rs | grep -c -- '--no-fail-fast'` is 0, each quoted with the output; the same output also holds the `--lib presentation::wx_managers:: -- --test-threads=4` line, which is why the count is of the whole word and not the substring `--test`.
    - Before the green commit, with the green staged: the runner's target line read by the line and quoted (`grep -nE '^[[:space:]]*cargo test --no-fail-fast' scripts/check.sh`), and the command it will issue composed from the seam's answer and asked of cargo's flag parse against a missing target. With the runner's line as this task writes it, `cargo test --no-fail-fast "${targets[@]}"`, that is `words="$(bash scripts/check.sh --scoped-runs-for guards/guards.toml src/presentation/wx_managers.rs | grep -E -- '(^| )--test( |$)')" && [ -n "$words" ] && cargo test --no-fail-fast $words --test does_not_exist_xyz 2>&1 | grep -F 'no test target named'`, exit 0 and quoted; if the runner's line is spelled otherwise, the composition follows its spelling. A flag given twice answers `cannot be used multiple times` instead, and the `grep` fails. Nothing is built: cargo stops at target selection (premise 7).
    - `test_one_failing_target_does_not_hide_the_rest` passes (`house_style`), and every target that reads the array passes: `grep -rln 'guards_that_read_the_whole_tree' tests` re-taken at the start of the task and quoted, five files after 12-03.1 (`every_number_carries_its_command_and_its_date`, `no_label_is_only_a_space`, `the_planning_files_agree_with_themselves`, `the_words_that_say_nothing` and 12-03.1's `the_nvda_cases_wait_for_words_the_program_says`), each run by the verify line; a file the grep names that the verify line does not is run too and named in the summary.
    - Every `cargo test` line in `check.sh` that does not spell exactly one `--lib` or `--test` as a word carries `--no-fail-fast` on the same line (premise 7), read by the line.
    - `grep -n -- '--scoped-runs-for\|--suites-for' scripts/check.test.sh` shows every line that runs `$subject` with either question inside the one helper that changes to the fixture root first (at `481a7918` there is one such line, `answer` at `:37`), read by the line.
    - Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>A change that couples to twenty-five targets builds them in one cargo invocation instead of twenty-five, and a red is judged by the same lines as before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: a file the program compiles in reaches the tests of the code that compiles it</name>
  <files>scripts/which-checks.sh, scripts/which-checks.test.sh, scripts/check.sh, scripts/check.test.sh, .planning/WINDOWS.md, .planning/phases/12-the-editors-and-what-the-alpha-still-owes/README.md</files>
  <read_first>
    - Premise 9.
    - `scripts/which-checks.sh:127-149` (main) and `:372-387` (the document rule on a branch); `which-checks.test.sh:34-57` (`expect`, `expect_from`) and `:252-272` (a repository of its own, the shape a fixture tree copies).
    - `.planning/WINDOWS.md` entry 373 in both halves, and decision 6.
  </read_first>
  <behavior>
    - `which-checks.sh --sources-compiling <path>` prints every `src/**/*.rs` under the directory it is run from whose `include_str!` or `include_bytes!` argument resolves, relative to that source file, to `<path>`, one per line, with the argument allowed on the line after the macro's name, which is how rustfmt wraps a long one. It reads `src/` relative to the current directory, as `check.sh` already does with `touch src/lib.rs`, because the hook runs from the top of the work tree; the comment says so, since it is what lets a suite ask it of a fixture tree.
    - One function says what a document is: `*.md` or `*.txt` that no source compiles in. `main`'s loop and the branch's loop both ask it.
    - `check.sh`'s scoped run treats each source that compiles a changed path in as changed too, for every changed path, Rust or not, for its `--lib` module and for its `guards/guards.toml` couplings, through `--scoped-runs-for`. The addition is made in the builder `--scoped-runs-for` shares with the run, not inside `the_suites_that_guard_what_changed`, so `--suites-for` still reads no source file. A source that compiles itself adds nothing new, and a compiling source counted twice runs once.
    - What it does not reach, in `which-checks.sh`'s comment: a test in another module that depends on the file's content (`tests/integration_tests.rs` uses the spellchecker), which a change to the compiling source does not reach either; a file read at test time with `read_to_string` (premise 9), which task 8 ledgers.
  </behavior>
  <action>
    **The fixture trees first, in the red commit** (premise 4, decision
    9). `which-checks.test.sh` builds one tree in its scratch directory
    at the top of the suite: `src/service/spellcheck/mod.rs` holding a
    copy of the dictionary's include line, `src/common/catalogue.rs`
    holding a copy of the date catalogue's, a third source holding an
    include wrapped the way rustfmt wraps a long one, and the files they
    name, each empty. Its default `run_from` moves from `"$root"` to
    that tree, so every existing case, the document cases such as `gsd/x
    docs/changelog.md` included, answers from it; the one case that
    needs a repository keeps `expect_from` its own. Run the suite with
    only that move made, before any new case, and quote it green with
    the same count of case lines as before the move: that is the
    evidence no existing case needed the real tree. `check.test.sh`'s
    fixture root from task 4 gains `src/service/spellcheck/mod.rs`,
    `src/common/catalogue.rs` and a copy of `sent_copy.rs`'s include of
    `mail_controller.rs`, the same way. **Before the fixtures' own
    cases are written, each copied include line is compared with the
    real one** by a read-only command that prints both, and the two
    are quoted side by side (`fixture-reads.md`, "A fixture derived from
    a real artefact gets, as its first test, a read-only comparison with
    the source"). After that the copies can drift from the real include
    lines, and **nothing holds the real mapping once this plan is
    done**: the fixtures test the rule, and the real tree's answers are
    the by-hand acceptance commands below, which run once. If the
    dictionary's include changed shape, for instance to
    `concat!(env!("CARGO_MANIFEST_DIR"), ...)`, the suites would stay
    green and a change to the dictionary would silently read as a
    document again. So the green says so in `which-checks.sh`'s comment
    beside the scan, and opens a ledger entry, a `todo` in both halves,
    for a reading of the real include lines that runs on the commits
    that could break them. Not built here: a unit test beside the
    include in `service::spellcheck` or `common::catalogue` would add a
    test to files that 32 and 6 guard records name in `tests_last_seen`
    (the TOML reader, 2026-09-23, at `481a7918`), setting off the count
    check for every one of them, and it would hold a second spelling of
    the shape the scan accepts, which a change to `which-checks.sh`
    does not reach. Found by the checker's third pass on 2026-09-23;
    until then this paragraph called the drift accepted without saying
    that nothing would see it.

    Red first. `which-checks.test.sh`: `a dictionary the program
    compiles in is not a document` (`gsd/x data/dictionary_en.txt`,
    `affected`); `a compiled-in dictionary on main earns everything`
    (`main`, `all`); `the sources compiling the dictionary are named`;
    `the sources compiling the date catalogue are named`; `a file
    compiled in by a macro rustfmt wrapped is not a document`; all from
    the fixture tree. Two companions are green on arrival, and the
    message says so and names neither: `a text file nothing compiles in
    is still a document`, and `the cases here answer from a tree of
    their own`, which asserts the default `run_from` is not `"$root"`
    and holds the fixture's `src/`. `check.test.sh`: `a compiled-in
    dictionary reaches the spellchecker's tests`, `a date catalogue
    reaches the catalogue's tests` and `a Rust file another module
    compiles in reaches that module's tests` (a changed
    `src/application/mail_controller.rs` gives a `--lib
    application::sent_copy::` line beside its own), through
    `--scoped-runs-for` from the fixture root. Each red because the
    question or the answer does not exist yet; one trailer line each,
    `which-checks::<desc>` or `check::<desc>`.

    Green: the function in `which-checks.sh`, the two loops on it, the
    scoped run adding the compiling sources. Ledger 373 is fixed with
    this commit, in both halves, on Pratik's answer of 2026-09-23 to
    decision for Pratik 4 (README decision 16), and its closing note,
    dated with the commit, says: his approval named the dictionary and
    his answer took the rule to every file the program compiles in, the
    date catalogue included, so a change to `dates.ftl` now reaches
    `common::catalogue`; and if version 2 loads catalogues at run time
    rather than compiling them in, the rule stops reaching them. The
    summary reports 373 closed on his answer. Until that answer this
    paragraph kept 373 open with a note ending that closing it waited on
    his yes or no, and had the summary ask him in its opening section,
    in plain words. The phase README's "known gate-mapper holes"
    paragraph gains a dated sentence that `locales/`, `data/` and the
    Rust files `sent_copy.rs` reads now reach the modules that compile
    them in.

    **The suites' inputs, taken again after the green.** The premise 4
    grep over the suites and the scripts they call, and `grep -n 'src/'
    scripts/which-checks.sh scripts/check.sh` for every path read
    relative to the current directory; each hit read, and for each,
    either it is on the list of every suite whose cases reach it (task
    3's lists, per suite since Pratik's answer of 2026-09-23; this read
    "under an input task 3's rule already names" until then), or every
    case that reaches it answers from a fixture tree. Quote both, and
    `every file a suite reads is on its list` green in the same run.
  </action>
  <verify>
    <automated>bash scripts/which-checks.test.sh && bash scripts/check.test.sh && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The move of `run_from` quoted green with the same case-line count before and after it, both quoted.
    - The red names every red case and `red-commit.sh` accepted it; the two companions are quoted green in the same run.
    - From the repository root, by hand: `bash scripts/which-checks.sh --sources-compiling data/dictionary_en.txt` prints `src/service/spellcheck/mod.rs`; `bash scripts/which-checks.sh gsd/x data/dictionary_en.txt` prints `affected`, both quoted.
    - From the repository root, by hand: `bash scripts/check.sh --scoped-runs-for guards/guards.toml data/dictionary_en.txt` holds a line starting `--lib service::spellcheck::`, and `bash scripts/check.sh --scoped-runs-for guards/guards.toml src/application/mail_controller.rs` holds a line starting `--lib application::sent_copy::`, both quoted.
    - The inputs re-derived after the green, both greps quoted, with no case left reading the real `src/`, each hit on its suite's list, and `every file a suite reads is on its list` quoted green.
    - Each copied include line quoted beside the real one it was copied from, compared before the fixtures' cases were written.
    - `which-checks.sh`'s comment beside the scan says nothing holds the real include lines after this plan and names the new ledger entry, read by the line; that entry open in both halves, and `head -7 .planning/WINDOWS.md` re-counted.
    - Entry 373 fixed in both halves with the green commit and carrying the closing note above, `head -7 .planning/WINDOWS.md` re-counted after it, and the summary reporting it closed on Pratik's answer of 2026-09-23. This bullet read "still open in both halves, carrying the dated note above; the summary's opening section asks Pratik decision 4" until that answer. Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>A commit changing only the dictionary, the date catalogue or the icon runs the tests of the code that compiles it before it lands, on a branch and on `main`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: the guard runner keeps a suite's pre-read for a tree it has already read</name>
  <files>scripts/guards.py (the functions, their examples, and the comment above the pre-read in `main`), guards/guards.toml</files>
  <read_first>
    - Premise 11; `scripts/guards.py:1160-1190` and `:2300-2376`; the record on `cargo()` (premise 12), whose anchor this task must not touch.
    - `12-02.1-PLAN.md` premise 12 and its task 1 action: a worked example is the red here, named through `test_the_guard_runner_still_obeys_its_own_examples`, and while it is red `scripts/guards.sh` cannot run.
    - `.github/workflows/guards.yml`, whether it caches `target/` between jobs.
  </read_first>
  <behavior>
    - `the_pre_read_key(tree, records, suite, settings, compiler) -> str`: a hash over the tree id, the records' text without their counts, the suite's arguments, the settings that move a run or a build, and the compiler (`rustc -V`), in a fixed order.
    - The settings are `the_settings_a_run_reads(environ) -> list[tuple[str, str | None]]`, pure, with worked examples: every variable whose name starts `WIXEN_`, and `RUSTFLAGS`, `CARGO_TARGET_DIR` and every `CARGO_BUILD_*`, each as its name and its value or `None` when absent, sorted by name; `WIXEN_TEST_THREADS` as its effective value, the default `8` when absent (`guards.py:82`), so setting it to its own default does not move the key. The `WIXEN_*` family is taken by prefix because the program reads the OAuth identities, `WIXEN_SAFE_BROWSING_KEY`, `WIXEN_TRUSTED_DOMAINS`, `WIXEN_BUILD` and `WIXEN_NO_AUDIO` (`grep -rhoE 'env::var(_os)?\("[A-Z_]+"' src tests build.rs` on 2026-09-23), and a variable added later is covered without anybody remembering. A value enters the key only through the hash, and the kept file holds the key and the failing names, never a value, since some of these are secrets. `LANG` is left out and named as outside the key, because `system_language` reads it only in a build that is not Windows (`spellcheck/mod.rs:427-433` at `481a7918`) and every shell sets it.
    - What stays outside the key, named in the docstring and in T-12-03.2-04 beside the ignored files: `HEAD` and the history. `every_number_carries_its_command_and_its_date` runs `git rev-list --count` and `git merge-base` (`:1264-1305`), and three targets run `git rev-parse HEAD` (`the_list_at_two_hundred_thousand_rows.rs:730`, `the_numbers_the_targets_ask_for.rs:724`, `the_settings_dialog_opens_in.rs:203`); a commit between two invocations moves `HEAD` and leaves the tree id where it was. Putting `HEAD` in the key would throw the reading away at every commit, which is most of the saving, so the two-hour limit is what bounds it, and the docstring says so. Two outside inputs sit beside it, named in the same two places: the Windows credential store, which every integration target reaches because the in-memory seam is `cfg(test)` (ledger 374's second finding, premise 10) and whose handling task 2 changes, and the user profile a test may read. The two hours bound them too, and deleting the kept file discards a reading sooner (the last bullet). Added 2026-09-23 from the checker's third pass.
    - The tree id is `git write-tree` over a copy of the real index (found with `git rev-parse --git-path index`) into which the working tree was added with `git add -A` and from which `guards/guards.toml` was removed, the copy named by `GIT_INDEX_FILE` in that one subprocess's environment and never in `os.environ`. So it covers tracked changes and untracked files that are not ignored.
    - The records' text is `guards/guards.toml` read with `tomllib`, every record with its `tests_last_seen` key dropped, dumped with sorted keys. `--remeasure` writes only `tests_last_seen` (`guards.py:483-540`, both the multi-line and the one-line spelling), so a re-measurement leaves the key unmoved, and a hand edit to any other part of a record moves it. That matters because three `house_style` tests read the file: `test_every_guard_record_says_how_many_tests_the_files_it_names_held`, `test_every_test_a_guard_record_names_is_a_test_that_exists` and `test_every_guard_record_still_names_one_place_in_the_tree`, and records exist whose red list names one of them: the planner's `tomllib` scan on 2026-09-23 over the `red` lists for those three names found one, "a break whose text has gone is not passed off as one that still points somewhere"; the checker's scan the same day counted three, over a list of readers it did not spell out, so the executor takes the count again with the reader's list written down. What stays outside the key, said in the docstring and in T-12-03.2-04: a count written by `--remeasure` can turn the first of those three from red to green within the two hours, and a kept reading would then still excuse it; no break reddens it, since a break changes source and not how many tests a file holds, so the excuse can hide no break's result. Ignored files the tests may read (`oauth.toml`, `.env`) are outside it too, and the expiry bounds them.
    - `the_records_without_their_counts(text) -> str`, pure, with worked examples: two texts differing only in a `tests_last_seen` block, in either spelling, give the same answer; two differing in a `red` entry or a `before` give different ones.
    - `a_kept_pre_read_answers(kept, key, now, longest) -> set[str] | None`: the kept failing names when the key matches and `0 <= now - taken_at <= longest`; `None` for another key, an entry older than the limit, one taken in the future, and a malformed one.
    - `THE_LONGEST_A_PRE_READ_IS_KEPT` is 7,200 s, its comment giving the reason (decision 7).
    - `main` asks for a kept reading before `what_is_already_failing`, prints `Reading what already fails with <suite>: kept from <N> s ago for this tree; delete <file> to read it again.` when it uses one, `<file>` being the kept file's path, and writes one after a pre-read that returned. A pre-read given up on or refused is never kept. Kept under `target/guards-pre-read/`, one file per suite. **Deleting that file is how a kept reading is discarded before its two hours are up**, by somebody who knows an input outside the key has moved, such as an entry left in the credential store or a profile setting a test reads (decision 7). A file rather than a flag, so the runner's arguments gain nothing; the line printed at each use is what makes the way known.
  </behavior>
  <action>
    Red first: worked examples for the four functions and the constant,
    written before the code, committed red:

    ```
    test(12-03.2): failing examples for a pre-read kept for its tree

    Fails-until-green: test_the_guard_runner_still_obeys_its_own_examples
    ```

    Bare, because it is in `tests/house_style.rs`. Nothing else may
    fail. **The green follows at once and nothing else is attempted
    between them**, because every `scripts/guards.sh` invocation runs
    the examples first (premise 11). This commit stages
    `scripts/guards.py` alone, which no suite reads, so under task 3's
    rule no suite runs and the hook prints a not-run line for each,
    quoted; it is the first commit of the plan to skip every suite. This
    read "so under task 3's rule the suites run too" until Pratik's
    answer of 2026-09-23 made the rule per suite.

    Green: the code, with `elegant-code`: the git reads in one small
    function, the decision pure, nothing that `unwrap`s. **The comment
    above the pre-read in `main` (`guards.py:2313-2317` at `481a7918`,
    "The pre-read is taken again on every invocation ... a pre-read kept
    from the log would blame or excuse the wrong failures") is an
    earlier decision this task overturns, and it is rewritten by
    dating, its old sentence kept**: the tree moving between
    invocations is now what the key answers, since the key is the
    working tree's content and the records' text; blaming or excusing
    the wrong failures is what remains of that argument for what the
    key cannot see, which the two-hour limit bounds; and the reading is
    still never taken from the log, only from the kept file for a
    matching key. That comment is outside the record anchored on
    `cargo()` (premise 12).

    Then, by hand, three `scripts/guards.sh --remeasure` invocations one
    after the other on the record written below: the first reads the
    suite and keeps it, the second prints the kept line naming the file
    to delete, and the third, run after deleting that file, reads the
    suite again; quote all three pre-read lines with their seconds, and
    `git status --porcelain` before and after each showing only
    `guards/guards.toml` moved. That is the reading of the part no
    example can reach, the saving, and the way to discard.

    Record: `file = scripts/guards.py`, `suite = house_style`, the
    break making `a_kept_pre_read_answers` ignore the key; predicted
    red, `test_the_guard_runner_still_obeys_its_own_examples`. Anchor on
    the longest unique context.
  </action>
  <verify>
    <automated>python -m doctest scripts/guards.py && cargo test --test house_style && cargo test --test the_guard_sweep_runs_on_runners</automated>
  </verify>
  <acceptance_criteria>
    - `python -m doctest -v scripts/guards.py | tail -3` reports more than the 136 examples it ran at `481a7918`, quoted.
    - The three by-hand pre-read lines quoted, the second saying kept and naming the file to delete, the third reading the suite again after that file was deleted, with `git status --porcelain` around each.
    - The docstring and T-12-03.2-04 name the credential store and the user profile among the inputs outside the key, beside `HEAD`, the history and the ignored files, read by the line.
    - The record measured with its timing line and quoted; "a record's own time limit reaches the cargo call it is supposed to bound" still names one place (`test_every_guard_record_still_names_one_place_in_the_tree` passes).
    - `grep -nE 'os\.(environ|putenv).*GIT_INDEX_FILE|environ\.(update|setdefault)' scripts/guards.py` finds nothing, read by the line; the one `GIT_INDEX_FILE` the task writes sits in a dictionary handed to one subprocess.
    - The worked examples of `the_settings_a_run_reads` show a `WIXEN_*` variable added, `RUSTFLAGS` set, and `WIXEN_TEST_THREADS` set to `8` giving the same settings as absent, quoted from the doctest run.
    - The comment above the pre-read keeps its old sentence inside a dated correction naming the key and the limit, read by the line.
    - Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>A second guard invocation on a tree the first already read skips the pre-read, and a tree that moved, a setting that changed, two hours passing or the kept file deleted read it again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: a merge runs what its diff earns, and the whole suite runs once a phase</name>
  <files>scripts/which-checks.sh, scripts/which-checks.test.sh, scripts/check.sh, scripts/check.test.sh, tests/every_number_carries_its_command_and_its_date.rs, tests/the_planning_files_agree_with_themselves.rs, tests/the_words_that_say_nothing.rs, guards/guards.toml (the seven records re-measured), .githooks/commit-msg, CLAUDE.md, .planning/WINDOWS.md; after the merge, on `main`, the summary and docs/development/measurements.md</files>
  <read_first>
    - Premises 2, 3 and 13; decisions 1 to 4.
    - `scripts/which-checks.sh:59-157`; `which-checks.test.sh:71-90` and `:252-345` (the fixture repository and the two #85 cases, the rule every fixture repository follows).
    - `scripts/check.sh:173-177` (`--suites-for`, the shape of an early answer), `:265-278`, and the three closing messages `481a7918` wrote (`:360-366`, `:416-421`, `:542-547`).
    - `.githooks/commit-msg:36-47`.
    - `CLAUDE.md:298-316` ("What runs depends on where you are", with "On a branch nobody builds, the slow half waits for the merge" at `:309`), `:338-353` ("The obvious version of that rule is wrong here", ending "On a branch the slow two wait for the merge, where they run once rather than once per commit" at `:351-353`), `:355-369` ("The merge is the full gate, and it runs once") and `:876` onward ("Premises a plan checks before execution").
    - Every other sentence that says the whole gate runs at or before a merge, each rewritten by dating in this task's green: `check.sh:207-210` ("Pass `all` ... which is what merging a branch into main does first"), `check.sh:581-583` ("`all` is what every code commit on main and every pre-merge run does"), `which-checks.sh:9-11` ("A branch nobody builds can defer, because the full gate runs once before the merge"). Found by `grep -n -i merge` over the three files on 2026-09-23; take it again at the tip, wrap-tolerantly (`tr '\n' ' '` first), since the three scripts will have moved.
    - `scripts/check.sh:371-422`, the documents-only block, which a merge holding a document now also runs, with its two `--lib` runs at `:389` and `:397` and its one `--no-fail-fast` call at `:411-414`.
    - The three readers of that block (premise 7): `the_documents_only_targets` at `every_number_carries_its_command_and_its_date.rs:473`, `the_planning_files_agree_with_themselves.rs:1230` and `the_words_that_say_nothing.rs:564`, and each file's `test_this_target_runs_on_the_commits_that_could_break_it`.
  </read_first>
  <behavior>
    - `which-checks.sh` refuses an option it does not know, naming it, exit 64; the known ones are `--message-file=`, `--manifest-diff-file=`, `--merge`, `--sources-compiling` and `--documents-among`.
    - `which-checks.sh --documents-among <path> ...` prints each given path that is a document by task 5's one rule, one per line in the order given, and exits 0; it answers from the directory it is run from, as `--sources-compiling` does. It is how `check.sh` asks whether a merge holds a document, so the rule has one spelling.
    - The document-reading list is held once in `check.sh`: `the_targets_that_read_documents=(house_style docs_links wired checkbox_labels manager_delete_stays_open the_planning_files_agree_with_themselves the_words_that_say_nothing every_number_carries_its_command_and_its_date)` and `the_modules_that_read_documents=(help_page:: presentation::what_the_scans_can_judge::)`, each on one line beside `guards_that_read_the_whole_tree`, with the block's comments on why each is there moved above them. The `docs_only` block keeps its opening line and runs one `cargo test --lib` per module and one `cargo test --no-fail-fast` over the targets. The three readers' `the_documents_only_targets` read `the_targets_that_read_documents=(` the way `the_whole_tree_targets` reads its array, and each file's `test_this_target_runs_on_the_commits_that_could_break_it` asserts its own name is there as before.
    - `--merge` on `main` or `master` skips `main`'s own block and answers by the branch rules over the paths given: `docs_only`, `affected`, or `all` for a manifest beyond the version, an `.iss` or a workflow. A merge into `main` with no path given answers `all`, as `main` does with no file list, because a merge that says nothing about what it changed cannot be scoped. A red marker on a merge into `main` is refused as on any `main` commit. On `HEAD` or no name, `all`. On any other branch, what that branch answers.
    - A commit on `main` without `--merge` answers as today: `all` for code, `docs_only` for documents.
    - `check.sh` passes `--merge` when `git rev-parse -q --verify MERGE_HEAD` succeeds, and `check.sh --mode-for-this-commit [--message-file=F]` prints the mode it would run, without running anything. The mode line names a merge. **`--mode-for-this-commit` is answered beside `--suites-for`, above the hooks-path note at `:194-205`** (task 1), through one function holding the mode decision the run makes at `:234-278`: it reads the staged list through task 3's `the_staged_paths`, asks for `MERGE_HEAD`, and calls `which-checks.sh`, and the run calls the same function. The note prints on stdout whenever the hooks directory holds no `commit-msg`, and the pair B repositories pin theirs to an empty directory, so an answer given below the note would lead with it and the whole-output comparisons would be red for the wrong reason. Found by the checker's third pass on 2026-09-23. All three closing messages that say the slow half waits for the merge, `all_but_slow`'s (`:360-366`), `docs_only`'s (`:416-421`) and `affected`'s (`:542-547`), say instead that the whole suite runs once, at the end of the phase, in its closing plan.
    - At a merge answering `affected` whose staged paths hold a document, which `check.sh` learns by asking `which-checks.sh --documents-among` over them, the scoped run adds both arrays: every name in `the_targets_that_read_documents` joins the one `--no-fail-fast` call, each once beside the whole-tree targets it shares, and every module in `the_modules_that_read_documents` is its own `--lib` run, since `cargo test` takes one `--lib` filter. `--scoped-runs-for` takes `--merge` before the registry to show it. A branch commit mixing code and documents runs as before.
  </behavior>
  <action>
    **Three pairs, in this order**: A because of premise 3, L because B
    runs the document list at a merge and the list has to be held once
    first (premise 7, decision 10), then B.

    Pair A, red: `which-checks.test.sh` gains `an option this script
    does not know is refused`, which answers `affected` today.
    `Fails-until-green: which-checks::an option this script does not know is refused`.
    Green: the refusal. From here `check.sh` must pass only known
    options; it passes `--message-file` alone until pair B.

    Pair L, red: each of the three readers gains a companion over a
    planted script whose document list is spelled only as the one-line
    array, `the_targets_that_read_documents=(house_style <its own name>
    docs_links)` above an `if [ "$mode" = "docs_only" ]; then` block
    whose cargo line runs `"${arguments[@]}"`, and asserts that
    `the_documents_only_targets` finds its own name there:
    `test_the_measurements_reading_is_found_in_the_documents_array` in
    `every_number_carries_its_command_and_its_date.rs`,
    `test_the_planning_reading_is_found_in_the_documents_array` in
    `the_planning_files_agree_with_themselves.rs`, and
    `test_the_words_reading_is_found_in_the_documents_array` in
    `the_words_that_say_nothing.rs`. Each is red because the reader
    still reads the block's `--test` tokens and the planted block has
    none. Seven records name
    `every_number_carries_its_command_and_its_date.rs` in
    `tests_last_seen` (premise 7), so adding a test there turns
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    red on arrival; by `CLAUDE.md`'s rule for exactly this collision
    the red names it with the three and says why in the message:

    ```
    test(12-03.2): failing readings of the document list as one array

    The count check is red because seven guard records name
    every_number_carries_its_command_and_its_date.rs and it gains a test;
    they are re-measured and written in the green commit.

    Fails-until-green: test_the_measurements_reading_is_found_in_the_documents_array
    Fails-until-green: test_the_planning_reading_is_found_in_the_documents_array
    Fails-until-green: test_the_words_reading_is_found_in_the_documents_array
    Fails-until-green: test_every_guard_record_says_how_many_tests_the_files_it_names_held
    ```

    Green: the two arrays in `check.sh`, the `docs_only` block on them,
    the three readers reading the array; and, before that commit is
    made, because the count check stays red until the counts are
    written, `scripts/guards.sh --remeasure` on the records the count
    check names, with `guards/guards.toml` staged in the green, which task 6's
    kept pre-read makes cheaper, each record's red list read first and
    corrected by hand if it comes out short (`CLAUDE.md`, "Tests that
    would notice"); quote the timing lines. The existing
    `test_this_target_runs_on_the_commits_that_could_break_it` in each
    file is green again over the real `check.sh` and is the other half
    of the evidence.

    Pair B, red: `which-checks.test.sh` gains `a merge into main with
    code changed` (`affected`), `a merge into main with only documents
    changed` (`docs_only`), `a merge into main with a workflow changed`
    (`all`), `a merge into main with a version bump and code`
    (`affected`), `a merge into a detached HEAD earns everything`
    (`all`), `a merge into main that names no path earns everything`
    (`all`), and `only the documents among the paths are named`
    (`--documents-among docs/x.md src/lib.rs data/dictionary_en.txt`
    from the fixture tree prints exactly `docs/x.md`, compared whole, so
    an empty answer is red). `a red commit on a merge into main is
    refused` asserts the red refusal's own wording, `cannot be made`
    (`which-checks.sh:103`), on stderr as well as a non-zero exit,
    through a helper beside `expect_refused`; today the unknown-option
    refusal answers it with other words, so it is red for the honest
    reason and is named in the trailer. The exit-status form alone was
    not enough: it would pass on pair A's refusal, never having been
    red for the reason its name gives (found by the checker on
    2026-09-23). `check.test.sh` gains `a merge in progress
    is told to which-checks as a merge` and `a commit on main that is not
    a merge earns everything`, each asking `--mode-for-this-commit`,
    and comparing its whole output with the one expected mode, from
    a throwaway repository in the suite's scratch directory with a
    `git merge --no-ff --no-commit` in progress, built the way
    `which-checks.test.sh:252-266` builds one (hooks path pinned to an
    empty directory, identity set locally), made with `git init -b main`
    because this machine's `init.defaultBranch` is `master` and the
    cases' names say `main`, under the harness that clears the five git
    variables, and never pointing at this repository. It also gains `a
    merge holding a document runs the document-reading targets`
    (`--scoped-runs-for --merge` over a code file and a `.md` holds
    `--test docs_links`) from task 4's fixture root; its companion `a
    branch commit holding a document and code runs no document-reading
    target` is green on arrival, and the message says so and does not
    name it. One trailer line per red case.

    Green: `--merge` in `which-checks.sh` and `check.sh` passing it, in
    the **same commit**, so no commit exists in which one side passes a
    flag the other refuses; `--mode-for-this-commit`; `--documents-among`;
    the document list at a merge; the three closing messages
    (`all_but_slow`, `docs_only`, `affected`); the hook's comment at `:40-43`
    rewritten by dating; the three script comments named in
    `<read_first>` (`check.sh`'s note on passing `all`, its note on the
    security advisories, `which-checks.sh`'s header), each keeping its
    old sentence and gaining a dated one saying the whole gate now runs
    once a phase in the closing plan and not at or before each merge;
    and `CLAUDE.md`, by dating, nothing deleted:

    - "On `main`, all four for anything that touches code" gains a dated
      sentence: on `main`, a commit that is not a merge still earns
      everything for code, and a merge earns what the branch's whole
      diff earns by the branch rules, with the document-reading targets
      when its diff holds a document. The same paragraph's "On a branch
      nobody builds, the slow half waits for the merge" (`:309`) gains
      "(until 2026-09-23; from then, for the phase's closing plan)".
    - "The obvious version of that rule is wrong here" ends "On a branch
      the slow two wait for the merge, where they run once rather than
      once per commit" (`:351-353`); it gains a dated sentence that from
      2026-09-23 they wait for the phase's closing plan and run once a
      phase.
    - "The merge is the full gate, and it runs once" keeps its text and
      gains a paragraph dated 2026-09-23: the merge is no longer the full
      gate; it runs formatting, clippy and what the branch's whole diff
      earns; the whole suite, the release build and the audit run once a
      phase, `scripts/check.sh all` by hand in the closing plan on its
      branch before its merge with `main` unmoved, output to a file and
      its exit status read directly. **The risk, plainly**: a break only
      the whole suite sees waits on `main` until the phase closes, up to
      nine merges in phase 12 (12-03.2 to 12-11; 12-03.1 and every plan
      before it merged under the old full gate), and finding its commit
      then is a bisection of about four steps. The evidence, with its
      date and source: across nine plans, 11-11.1.1 to 12-03, the
      merge's full gate found nothing the branch's scoped checks had
      missed, and its only refusals were the keyring race (the
      measurement of 2026-09-23). Its limit: nine plans is a small
      sample, and the scoped checks have known holes the merge used to
      cover, which now wait for the phase's close: the documents `src/`
      reads at test time with `read_to_string` (premise 9: `privacy.md`
      through `contact_groups.rs` and `update_check.rs`,
      `the-notes-seam.md` through `onenote_page.rs`), `src/main.rs` and
      `wx_settings.rs` mapping to filters that match nothing, and the
      rest of the phase README's list. Two holes this plan closed rather
      than named: a Rust file another module compiles in (task 5) and a
      merge mixing code and documents (this task). What narrows it: CI's
      full Test Suite on the pushes and pull requests Pratik's standing
      OK of 2026-09-23 allows, under which plans that change what is
      spoken or shown push their branch and open a pull request and
      others do not. He chose that day to push nothing more, because a
      pull request waits for CI's runs, which on the push of 2026-09-23
      took about 20 minutes for the Test Suite and 24 for NVDA; read
      those durations again with `gh run list` and `gh run view` before
      writing them, and date them, since
      `every_number_carries_its_command_and_its_date` reads the file. A
      red phase gate is fixed on the closing plan's branch, a small red
      and green pair, not a plan of its own. Both are his answers of
      2026-09-23 (decisions for Pratik 1 and 2); this bullet ended "What
      narrows it: CI's full Test Suite on every push and pull request,
      and each is Pratik's to cause" until them.
    - "Premises a plan checks before execution" gains a rule, dated
      2026-09-23, opening as the rules beside it do with a bold
      sentence, **A phase's closing plan runs the full gate once, by
      hand, before its merge.**, which the acceptance finds by those
      words: a phase's closing plan, its highest wave, carries a
      task that runs `scripts/check.sh all` by hand once on its branch
      before its merge, with `main` unmoved, output to a file and the
      exit status read directly, a red one fixed on that branch as a
      small red and green pair and the gate run again before the merge
      (Pratik's answer of 2026-09-23 to decision for Pratik 2), and the
      checker refuses a phase whose closing plan has none. It says that nothing checks this yet and
      names the ledger entry opened for the check in the same commit,
      because a rule that lives only in this file is the weak kind the
      file keeps naming. That entry, by hand in both halves, is a `todo`
      for a check in `tests/the_planning_files_agree_with_themselves.rs`
      that the highest-wave plan of every phase from 12 on names
      `scripts/check.sh all`, to be written beside the widening that
      file's completion-marks paragraph already owes the same test. It
      is not built here: on the real tree it is green on arrival, since
      12-12 already carries the task, so its only red would be a planted
      companion, and it belongs with the tick count in the same reading.

    This green is the first commit that can take the new merge path; it
    is not a merge, so it runs under the rule for a branch. The merge
    of this plan is the first merge under it (decision 4).

    **The suites' inputs, taken again after this green**, the same two
    greps as task 5's, because this task adds cases that build a
    repository and ask a document list; quote both, say that no case
    reads this repository's `src/` or its index, and quote `every file a
    suite reads is on its list` green, since pair B's cases reach
    `which-checks.sh` through `check.sh`, which check's list has named
    since task 3.

    **After the merge.** The merge commit is the last thing this plan's
    branch cannot record, since the summary and task 8's commit land on
    the branch before it. So, once the merge is made, one more commit on
    `main` that touches only the summary and
    `docs/development/measurements.md`, which `which-checks.sh` answers
    `docs_only` for as a commit on `main` that is not a merge, adds the
    merge's mode line and stage line to the summary and fills the merge
    row of task 8's table and its measurements row. The plans after this
    one do the same (their verification lines, written with this plan),
    so 12-12's full gate can place a red among the merges from the
    summaries.
  </action>
  <verify>
    <automated>bash scripts/which-checks.test.sh && bash scripts/check.test.sh && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves && cargo test --test the_words_that_say_nothing && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - Pair A's red names one case, pair L's red names its four tests, pair B's red names every red case including `a red commit on a merge into main is refused` and `only the documents among the paths are named`, and `red-commit.sh` accepted all three; the green-on-arrival companion is quoted green in pair B's red run.
    - `grep -c '^the_targets_that_read_documents=(' scripts/check.sh` is 1 and `grep -c '^the_modules_that_read_documents=(' scripts/check.sh` is 1; every line `grep -nE -- '--test (house_style|docs_links|checkbox_labels|manager_delete_stays_open)( |$)' scripts/check.sh` prints is a comment (at `481a7918` it prints the `docs_only` call at `:411` and `:412` and the comment at `:491`), so no name of the list is spelled a second time on a cargo line; each read by the line.
    - The records the count check named after pair L re-measured, each with its timing line quoted, and `test_every_guard_record_says_how_many_tests_the_files_it_names_held` passes.
    - `bash scripts/which-checks.sh --documents-among docs/changelog.md data/dictionary_en.txt src/lib.rs`, from the repository root, prints exactly `docs/changelog.md`, quoted.
    - `bash scripts/which-checks.sh --merge main src/lib.rs` prints `affected`, `bash scripts/which-checks.sh main src/lib.rs` prints `all`, and `bash scripts/which-checks.sh --not-an-option gsd/x src/lib.rs` exits 64, all three quoted.
    - Nothing this plan wrote in `CLAUDE.md` deletes a word. The paragraphs it dates are "What runs depends on where you are" (`:298-316` at `481a7918`), "The obvious version of that rule is wrong here" (`:338-353`), "The merge is the full gate, and it runs once" (`:355-369`) and task 3's suites paragraphs (`:423-446`); the premise rule is new. A line diff cannot judge that, because inserting "(until 2026-09-23; from then, for the phase's closing plan)" inside the sentence on `:309` rewrites the line, which a line diff shows as one removed and one added. So the check is by word, against the commit the branch was cut from, over the whole file, which covers all four paragraphs and task 3's edits together: `base=$(git merge-base main HEAD) && ! git diff --quiet "$base" -- CLAUDE.md && ! git diff --word-diff=porcelain --word-diff-regex='[[:alnum:]_]+|[^[:alnum:]_[:space:]]' "$base" -- CLAUDE.md | grep -E '^-' | grep -v '^--- a/'`, exit 0 and quoted. The regex makes each punctuation mark its own word, so a sentence appended after "ten" does not show "ten" as removed and rewritten as "ten."; a line rewrapped shows nothing; a deleted word shows a line starting `-`. Taken in a throwaway repository on 2026-09-23: an insertion inside a line, a rewrap and a sentence appended after a full stop gave no `-` line, and one word changed gave `-ten`. The first half, `! git diff --quiet`, makes a diff that is empty because the base is wrong fail rather than pass. The new paragraph names nine plans, the date and the risk, and `every_number_carries_its_command_and_its_date` passes.
    - `grep -c 'MERGE_HEAD' scripts/check.sh` is at least 1, outside a comment, read by the line.
    - `CLAUDE.md:309` and `:351-353`, `check.sh`'s two notes, `which-checks.sh`'s header and the hook's comment each keep their old sentence and carry a dated one, read by the line. The new premise rule is found wrap-tolerantly, since its bold lead may wrap: `test "$(tr '\n' ' ' < CLAUDE.md | grep -o "A phase's closing plan runs the full gate once" | wc -l)" -eq 1` exits 0, and the rule, read by the line under "Premises a plan checks before execution", names the ledger entry. A single-line `grep -n 'closing plan'` was the criterion until the checker's third pass on 2026-09-23, and it could miss the rule on a line break.
    - The inputs re-derived after the green, both greps quoted, and `every file a suite reads is on its list` green.
    - After the merge of this plan: a commit on `main` touching only the summary and the measurements page, whose own hook output names `docs_only`, adds the merge commit's mode line (saying a merge, and not `all`) and stage line to the summary, both quoted there.
    - Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The merge into `main` costs what the branch's diff earns, the whole suite has one place a phase where it runs, and the file that governs the gate says what that risks.</done>
</task>

<task type="auto">
  <name>Task 8: what the gate costs now, FOUND-23 ticked, the ledger</name>
  <files>docs/development/measurements.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/development/measurements.md`, "How to add a row" and the figures table from `:346`; the full-gate row at `:367`.
    - The stage lines copied since task 1, and premise 1's per-mode figures.
    - `.planning/REQUIREMENTS.md`, FOUND-23 (`grep -n 'FOUND-23'`), its row and the coverage block.
  </read_first>
  <action>
    **The savings, re-measured.** From the stage lines this plan's own
    commits printed, a table in the summary by mode: `red`, `affected`,
    `docs_only`, and the merge once task 7 lands, each with its count of
    runs, the median and the range of the total, and the median of each
    stage; beside it premise 1's figure for that mode (4.25, 4.2, 3.0
    and 6.9 minutes), with the warning that this plan's commits touch
    scripts more than a feature plan's do. The shell-suite stage's
    seconds when run, which suites each commit ran and which it skipped,
    and how many commits skipped every suite, beside premise 4's count
    for the nine plans before this one. The first two
    pre-read lines from task 6. What the report estimated per task:

    | Task | The report's estimate a plan | What reads it |
    |---|---|---|
    | 1 timings | none; it is how the rest is read | every stage line |
    | 2 keyring | about 1.3 min, and 3 of 4 flaky refusals | refusals in 12-04 onward |
    | 3 suites | 3 to 8 min; likely the low end, since twelve of fourteen merges and most record commits stage `guards.toml`. Per suite, on Pratik's answer of 2026-09-23: premise 4's count at premise 6's times gives about 3.2 min a plan against every suite on every commit, about 1.25 of it from running check alone for `guards.toml` | the suites stage, per suite, run and skipped |
    | 4 one call | 1 to 3 min | the scoped stage on a commit coupling many targets, first in 12-04 |
    | 5 compiled in | a cost, on rare commits | none |
    | 6 kept pre-read | 1 to 2 min | the first two of task 6's pre-read lines |
    | 7 the merge | a merge from 6.9 min toward an affected run's 4.2 | this plan's merge's stage line, filled by the commit on `main` after the merge (task 7) |

    Rows on `docs/development/measurements.md`, one per mode read,
    each with its command (the stage line and how it was collected),
    its date, its commit and its conditions (warm or cold, what the
    commit staged, NVDA running). The full-gate row stays and gains a
    dated sentence in its conditions that from 2026-09-23 it is taken
    once a phase.

    **FOUND-23**: tick each of its seven `[D]` lines this plan owns with
    the date and the names of the cases, the test and the records; the
    eighth is 12-12's task 3, so the box and the row stay open with a
    sentence saying so; the `[S]` lines untouched.

    **Ledger**, by hand, both halves, `head -7` re-counted before and
    after: a `todo` for the documents `src/` reads at test time that
    no scoped run reaches (premise 9, the three sites), named as a hole
    task 7 widened; an `unrun-verify` for 12-12's full gate, the first
    under the new rule, naming what it would mean if it were red and
    that a red one is fixed on 12-12's own branch as a small red and
    green pair (Pratik's answer of 2026-09-23 to decision for Pratik 2).
    No `todo` for running a suite only when its own inputs move: Pratik
    answered decision for Pratik 3 on 2026-09-23 and task 3 builds it.
    Until then this paragraph ended "and, only if the summary finds the
    per-suite split worth it from its own numbers, a `todo` for running
    a suite only when its own inputs move (decision for Pratik 3)".
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - The summary's table has a row per mode read with its run count, and the measurements page gains at least as many rows as the modes the table read, each passing `every_number_carries_its_command_and_its_date`.
    - FOUND-23's seven `[D]` lines for 12-03.2 carry a tick date and names, its eighth names 12-12's task 3 and is open, and `grep -c '^- \[ \] \*\*FOUND-23' .planning/REQUIREMENTS.md` is 1 until 12-12 ticks it.
    - `head -7 .planning/WINDOWS.md` counts at least two more entries than before the task, quoted before and after.
    - This commit owes no suite, and its hook output shows a not-run line for each suite, quoted. This read "stages no suite input, and its hook output shows the suites' not-run line" until Pratik's answer of 2026-09-23 made the line per suite.
    - Carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>What each change saved is on the record from this plan's own commits, next to what the report predicted, and the requirement says what was built on what.</done>
</task>

</tasks>

<which_commit_runs_under_which_gate>
The hook runs `scripts/check.sh` from the working tree, and `check.sh`
runs `which-checks.sh` from beside it, so **a commit runs the gate as
its own working tree holds it**, staged or not. Two consequences. A
gate script edited and not staged would judge a commit that does not
contain it, so before every commit `git status --porcelain scripts
.githooks` shows nothing unstaged. And each commit below runs under
the version its own content makes:

| Commit | Runs under |
|---|---|
| task 1 red | the gate 12-03.1 left: suites in every mode, one call per target |
| task 1 green, task 2 both | task 1's: timed, suites in every mode |
| task 3 red | task 1's |
| task 3 green onward | each suite only when owed, by its own list, from a staged list that names both paths of a move: task 3's green and task 4's pair run check alone; task 5's pair, task 7's pair A and pair B's red run which-checks and check; task 6's red and green stage `scripts/guards.py`, which no suite reads, so its red is **the first commit to skip every suite**, and the commit that writes its record runs check, for `guards/guards.toml`, as pair L's green does; pair B's green stages `.githooks/commit-msg` and runs every suite. This row read "every commit of tasks 3 to 6, and of task 7 except pair L's red, stages a suite input and runs them" until Pratik's answer of 2026-09-23 |
| task 4 green onward | one cargo call, `--no-fail-fast` spelled once by the runner's line, a deleted test file left out |
| task 5 green onward | compiled-in files mapped |
| task 7 pair A green onward | unknown options refused; `check.sh` passes none until pair B |
| task 7 pair L red | pair A's green; red on the three readings and the count check, all four named. It stages only three `tests/*.rs` files, no suite's input, so it skips every suite and prints a not-run line for each, as task 6's red does before it. This row called it **the first commit to skip the suites**, and the commit task 3's acceptance quotes for the first commit that stages no suite input, until Pratik's answer of 2026-09-23 put `scripts/guards.py` on the list no suite reads |
| task 7 pair L green onward | the document list held once in two arrays; the three readers read it; the seven records re-measured in the green commit itself, since the count check is red until they are |
| task 7 pair B green, task 8 | the merge path exists; pair B's green stages `.githooks/commit-msg` and so runs every suite; task 8 owes no suite and skips every one, as task 6's red and pair L's red do before it (this row called it the second to skip the suites, after pair L's red, until Pratik's answer of 2026-09-23) |
| the merge of this plan | the new merge path, `affected` over the branch's diff with the document-reading targets, since the diff holds documents, and every shell suite, since it holds `.githooks/commit-msg`; the first merge under it |
| the commit on `main` after the merge | `docs_only`, a commit on `main` that is not a merge and touches only documents; it records the merge's stage line |

No commit leaves the gate unable to run: each red adds failing cases
only; the only commit that adds a flag one script passes and another
must accept holds both sides (task 7 pair B's green); the one flag
cargo is handed by a builder and a runner together, `--no-fail-fast`,
has one owner, the runner's line, and the composed command's flag
parse is asked before task 4's green is committed (premise 7); the
guard runner is red for one commit only (task 6).

The table said until the checker's third pass on 2026-09-23 that every
commit of tasks 3 to 7 stages `scripts/` and that task 8 is the first
to skip the suites. Pair L's red stages only its three `tests/*.rs`
files, so it is the first. Later the same day Pratik's answer to
decision for Pratik 3 made each suite run only for its own list, so
task 6's red, which stages only `scripts/guards.py`, is the first to
skip every suite, and task 3's own green, which owes check alone, is
the first to skip some.
</which_commit_runs_under_which_gate>

<decisions_for_pratik>
Pratik answered all four on 2026-09-23, after this plan was written.
Each question is kept as it was put, with his answer under it and where
the plan now carries it.

1. **Whether to push, or open a pull request, before a phase closes**,
   to narrow how long a break only the whole suite sees can sit on
   `main`. CI's Test Suite runs on both. Each is an outward push and
   runner time, his word each time. Without it the plans rely on the
   closing plan's full gate, and the risk is as `CLAUDE.md` will say.
   **Answered: no pushes or pull requests beyond his standing OK of the
   same day**, under which plans that change what is spoken or shown
   push their branch and open a pull request and others do not. He
   chose the option that saves time, since a pull request waits 20 to
   24 minutes for CI. This plan changes nothing spoken or shown, so it
   pushes nothing (`<verification>`), and the risk task 7 writes into
   `CLAUDE.md` names the standing OK as what narrows it.
2. **What happens when a phase's full gate is red.** 12-12's new task
   stops and asks rather than closing the phase red: a small fix on
   12-12's own branch as a red and green pair, or an inserted plan, is
   his to schedule. **Answered: fixed on the closing plan's branch**, a
   small red and green pair there, not a plan of its own. 12-12's task
   3 fixes a red and runs the gate again rather than stopping to ask,
   and the premise rule task 7 writes into `CLAUDE.md` says the same
   for every phase.
3. **Whether to narrow the suites further, one suite per input.**
   Approved was the four suites together when any input moves.
   `guards/guards.toml` is read by `check.test.sh` alone, and it was in
   twelve of the last fourteen merges' diffs (premise 2), so running
   only `check.test.sh` for it is where the rest of task 3's saving
   would be. Not built; task 8's numbers say what it would buy.
   **Answered: yes, one suite per input**: only the `scripts/*.test.sh`
   suite whose own inputs are staged runs. Task 3 builds it (premise 4,
   decision 11), so "Not built" held until his answer, and task 8
   opens no `todo` for it.
4. **Whether ledger 373 is closed, yes or no.** His approval named the
   dictionary. Task 5 applies the same rule to every file the program
   compiles in, the date catalogue `locales/en-US/dates.ftl` among
   them, because it is the same hole and the rule only adds checks.
   373 had left the `locales/` mapping for version 2's questions. Yes
   closes 373 as built. No leaves 373 open for version 2, and a small
   red and green pair takes the files other than the dictionary out of
   the rule, which means a change to `dates.ftl` alone reaches no test
   until the phase's full gate. Until he answers, 373 stays open with a
   dated note (decision 6; README decision 16). **Answered: yes, closed
   as built**: the compiled-in-file rule applies to every file the
   program compiles in, the date catalogue included. Task 5's green
   closes 373 in both halves.
</decisions_for_pratik>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the hook to the working tree | the gate that judges a commit is the one on disk, not the one committed |
| `main` to the phase's close | a break the scoped checks cannot see now stays on `main` until the closing plan |
| a kept reading to a later run | a pre-read answer used for a tree it does not describe |
| a test to the credential store | an integration target touches the real Windows Credential Manager |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03.2-01 | Tampering | a merge answering a weaker mode than its diff earns | high | mitigate | the merge goes through the same rules as a branch commit; cases for a workflow, a version bump and code, and documents at a merge; a non-merge code commit on `main` still answers `all` |
| T-12-03.2-02 | Repudiation | a break only the whole suite sees sitting on `main` for a phase | medium | accept | named in `CLAUDE.md` with its evidence and its limit; 12-12 runs the full gate before the phase closes and fixes a red one on its branch; the holes it widens ledgered; pushing earlier was Pratik's (decision for Pratik 1), and he answered on 2026-09-23 with no pushes beyond his standing OK, under which plans that change what is spoken or shown push their branch and open a pull request and others do not |
| T-12-03.2-03 | Denial of service | a shell suite skipped when something it reads changed | medium | mitigate | each suite's list taken from what it and the scripts it reaches name (premise 4), `.cargo/audit.toml` added, and taken again after tasks 5 and 7; a case holds every list to that reading, and check's list to every other, on every commit that owes check (task 3); `scripts/shell-suite.sh` and `.githooks/commit-msg` owe every suite, and so does a path under `scripts/` or `.githooks/` that no list places (decision 11); no case reads this repository's `src/`, because every case reaching the compiled-in scan answers from a fixture tree (decision 9); the staged list read with `--no-renames`, so a move out of a listed path still owes that path's suites, held by a case over a throwaway repository (task 3); `all` and `all_but_slow` run every suite; CI runs every suite on every push and pull request. Per suite since Pratik's answer of 2026-09-23, and one mitigation for the four together until then |
| T-12-03.2-04 | Tampering | a kept pre-read excusing failures of a different tree | medium | mitigate | the key is the working tree's content with `guards.toml` left out, that file's records with only their written counts left out, the suite, the run's settings and `rustc -V`; so a hand edit to a record moves it and a re-measurement does not. Accepted: three `house_style` tests read `guards.toml`, and a count `--remeasure` writes can turn the count check green within the two hours while a kept reading still excuses it, which hides no break's result since a break changes source and not how many tests a file holds; ignored files (`oauth.toml`, `.env`), `HEAD` and the history, `LANG`, the Windows credential store every integration target reaches, and the user profile are outside the key and bounded by the limit, and deleting the kept file, which each use names, discards a reading sooner; the settings in the key are every `WIXEN_*`, `RUSTFLAGS`, `CARGO_TARGET_DIR` and `CARGO_BUILD_*`, present or absent, hashed and never written out. Two hours at most; only a pre-read that returned is kept; every use printed |
| T-12-03.2-05 | Tampering | the temporary index reaching the real repository | high | mitigate | `GIT_INDEX_FILE` set in one subprocess's environment and never in `os.environ`; the copy removed after; `git status --porcelain` read around the two by-hand invocations (#85's lesson) |
| T-12-03.2-06 | Information disclosure | the keyring test reading the real credential store | low | mitigate | it reads only an entry under a service name no program uses, which is not there; nothing written; no value printed |
| T-12-03.2-07 | Denial of service | a child process that never returns hanging the target | medium | mitigate | a 60 s deadline per child with `try_wait`, the child killed and named |
| T-12-03.2-08 | Repudiation | a red that passes by accident because a new option was read as a branch | medium | mitigate | unknown options refused first, as its own pair (premise 3) |
| T-12-03.2-09 | Tampering | a gate script edited and unstaged judging a commit | medium | mitigate | `git status --porcelain scripts .githooks` empty before every commit |
| T-12-03.2-10 | Denial of service | a flag passed by `check.sh` that `which-checks.sh` refuses, refusing every merge | high | mitigate | `--merge` added to both sides in one commit; `--mode-for-this-commit` read before that commit |
| T-12-03.2-11 | Denial of service | `--no-fail-fast` handed to cargo twice, once by the seam's line and once by the runner's, refusing every scoped run and the commits and merges that make one | high | mitigate | one owner, the runner's target line; the seam cases assert exactly one whole-word `--test` line and no `--no-fail-fast`; the composed command's flag parse asked of cargo against a missing target before task 4's green (premise 7) |
| T-12-03.2-12 | Denial of service | a real include line changing shape so a compiled-in file silently reads as a document again | medium | accept | the fixtures test the rule and the by-hand commands test the real tree once; nothing holds the real lines after this plan, which `which-checks.sh`'s comment says and a `todo` ledgers (task 5); a test beside the include was set aside because it sets off the count check for 32 and 6 records |
| T-12-03.2-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate, package or tool added; `Cargo.toml` and `Cargo.lock` untouched, and the summary says so |
</threat_model>

<verification>
- The commands in all eight `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped, its stage line copied from every commit after task 1.
- This plan's merge is made with `git merge --no-ff` and runs the new merge path; its mode line and stage line are then added to the summary by a commit on `main` touching only the summary and the measurements page, which answers `docs_only` (task 7). **Not by hand before it, and not `scripts/check.sh all` at all in this plan**: the whole suite over this plan's changes runs in 12-12's full gate (decision 4).
- Every record the count check flagged re-measured and read; the two new records measured and quoted.
- What a case here proves: each answer of the two scripts and the seams, the red race in fresh processes, the kept pre-read's decision. What only a run proves: the stage lines, the skipped suites, the kept pre-read used, the merge path; each quoted from a real run. What only 12-12 proves: that nothing broke that only the whole suite sees.
- NVDA not touched; no NVDA case run; the product never started; the tester's profile not read; nothing pushed, dispatched or commented on GitHub. This plan changes nothing spoken or shown, so Pratik's standing OK of 2026-09-23, under which plans that change what is spoken or shown push their branch and open a pull request and others do not, does not reach it, and his answer to decision for Pratik 1 the same day adds no push.
</verification>

<success_criteria>
- FOUND-23's seven `[D]` lines for this plan ticked with their names; its eighth, its box and its row left for 12-12's task 3; its `[S]` lines left for that full gate and the next plans' stage lines.
- Ledger 374 fixed; 373 fixed as built with its dated closing note, on Pratik's answer of 2026-09-23 to decision for Pratik 4 (this read "373 open with its dated note and asked of Pratik as decision 4 in the summary's opening section" until then); the new entries open in both halves.
- `CLAUDE.md` (`:309`, `:351-353`, the merge paragraph and the new premise rule), the hook's comment, `check.sh`'s messages and its two notes, and `which-checks.sh`'s header say the same thing about where the whole suite runs, each old sentence kept beside a dated one.
- The completion marks, all four: phase 12's progress row, this plan's `- [ ]` line in phase 12's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and FOUND-23's lines and traceability row.
- The summary reports the exception set for scripted edits, zero, through the diagnostic detours as well as the work, and the savings table of task 8.
</success_criteria>

<output>
Create `.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-03.2-SUMMARY.md` when done
</output>
