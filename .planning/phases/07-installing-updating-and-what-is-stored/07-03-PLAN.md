---
phase: 07-installing-updating-and-what-is-stored
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - src/presentation/accessibility/platform_bridge.rs
  - src/presentation/accessibility/screen_reader.rs
  - src/presentation/accessibility/names.rs
  - src/presentation/accessibility.rs
  - src/presentation/wx_app.rs
  - src/main.rs
  - guards/guards.toml
  - docs/changelog.md
  - Cargo.toml
autonomous: true
requirements: [SHIP-06]
estimate:
  tokens: 74000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a build where the accessibility bridge is not compiled in, the About dialog says which parts of the accessibility layer do nothing there, rather than presenting a client that looks accessible and is not."
    - "On such a build the same fact is said at startup, on the stream somebody launching from a terminal is reading."
    - "On Windows neither says anything, because there is nothing to disclose, so the disclosure costs nothing where the bridge works."
    - "Whether there is a bridge is answered by whether a bridge module is compiled in, so writing one for another platform removes the warning without anybody remembering to."
    - "The disclosure covers both halves of the bridge: the announcements nobody would hear, and the accessible names nothing would report."
  artifacts:
    - "src/presentation/accessibility/platform_bridge.rs: the derivation, the sentences, and the tests, in a file no guard record fingerprints."
    - "src/presentation/accessibility/screen_reader.rs: a `native` module for each platform carrying its own answer, so `NativeBridgeStatus` stops being a platform list of one."
    - "src/presentation/accessibility/names.rs: a named answer to whether an accessible name reaches the accessibility tree on this build."
    - "src/presentation/wx_app.rs and src/main.rs: the two places the fact is said, both reached from a non-test path."
  key_links:
    - "Accessibility::native_bridge_status -> nothing: `grep -rn native_bridge_status src/ tests/` returns exactly one hit, its own definition, and `ScreenReaderBridge::status` has one caller which is that definition. Both are pub, so no dead_code warning fires and `tests/wired.rs` cannot see them because it is about command ids. This plan gives them a caller or replaces them."
    - "names.rs -> the disclosure: `grep -n 'cfg(target_os' src/presentation/accessibility/names.rs` returns nothing. The accessible-name half of the bridge has no marker at all, so a disclosure derived only from `NativeBridgeStatus` would describe half the problem."
    - "build_about_dialog -> the disclosure: the dialog is a pure builder with an existing test seam, already reached by `tests/theme_reach.rs`, and already carries `version::current()`. It is where a fact about this build belongs."
---

<objective>
Give the two dead accessors a caller, widen what they answer to cover both
halves of the bridge, and say the answer where somebody meets it.

Purpose: success criterion 6. It reads as though nothing exists and most of it
does: the status is derived at `ScreenReaderBridge::default`, every call into
the native layer is gated, and `Accessibility::native_bridge_status` wraps the
accessor. What is missing is a caller. `grep -rn native_bridge_status src/ tests/`
returns one hit, its own definition. That is guardrail 1 and guardrail 3 in the
same function: built, compiling, reachable by nothing, and describing a client
that looks accessible and is not.

Two things this plan does that the criterion asks for and the existing code does
not give. The derivation is `cfg!(target_os = "windows")`, which is a hardcoded
platform list of one, so writing a macOS bridge would leave the warning
appearing. And the status covers announcements only, while the other half of the
bridge, the accessible names, has no marker anywhere.

Output: one answer to what this build's accessibility layer does not do,
answered by which bridge module compiled, said in the About dialog and at
startup, and silent on Windows.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-RESEARCH.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-06, command first.

**1. Both accessors really are unreached, checked in both directions.**

```
grep -rn "native_bridge_status" src/ tests/ --include=*.rs
grep -rn "NativeBridgeStatus" src/ tests/ --include=*.rs
```

The first returns one line, `src/presentation/accessibility.rs:429`, its own
definition. The second returns six, all inside `screen_reader.rs` and that one
wrapper: the enum at `:425`, the field at `:436`, the accessor at `:636`, the two
variants constructed at `:656` and `:658`, and the wrapper. **No variant is
matched on anywhere.** So the type is written, the value is computed on every
start, and nothing has ever asked for it.

**2. The requirement's caveat is right and this is what it costs.**
`ScreenReaderBridge::default` sets the status with
`if cfg!(target_os = "windows")`. The requirement says that is a platform list of
one and that deriving it from whether a bridge function is present is what would
actually satisfy the criterion. It is right, and the shape that does it is
already half there: `screen_reader.rs` has `#[cfg(target_os = "windows")] mod
native` and a `#[cfg(not(target_os = "windows"))]` arm inside `deliver` that
does nothing but discard its arguments. Give the fallback its own `native`
module carrying the same names and its own answer, and the status reads
`native::REACHES_A_SCREEN_READER` rather than asking what platform this is.
Then a macOS bridge is a third `mod native` and the status follows.

**3. The bridge has two halves and only one of them has a marker. This is not in
the research and it changes what the disclosure says.**

```
grep -n "cfg(target_os" src/presentation/accessibility/names.rs
```

returns nothing. `set_accessible_name` calls wxdragon's `set_accessible`
unconditionally, and the module's own header says why that is still Windows
only: "`wxAccessible` is implemented against Microsoft Active Accessibility and
has no GTK or macOS counterpart in wxWidgets, so on those platforms these calls
are accepted and have no effect." `CLAUDE.md` says the same about both halves.

So a disclosure derived only from `NativeBridgeStatus` would say announcements
do not reach a screen reader and say nothing about every control in the window
reaching the accessibility tree with no name. The second is the larger failure.
The disclosure has to name both, and `names.rs` needs a marker of its own before
it can.

**4. `names.rs` already contains the sentences to reuse, measured rather than
guessed.** Its header records what was read side by side through both APIs
against the running composer, and it names the two ports a real bridge would
need: `setAccessibilityLabel:` on the NSView for macOS, an ATK name for GTK.
Read that header in full before writing a word of the disclosure. Do not invent
wording where measured wording exists.

**5. Guard-record cost. Re-measured 2026-09-11 against `main` at `febe8e4`,
version 0.112.0, by parsing `tests_last_seen` rather than by grepping for the file
name.** The "at `c256197`" column is what this plan was written with.

| file | records fingerprinting it | at `c256197` | records breaking it | `#[test]` today | at `c256197` |
|---|---|---|---|---|---|
| `src/presentation/accessibility/platform_bridge.rs` | **0**, it does not exist | 0 | 0 | 0 | 0 |
| `src/presentation/accessibility/screen_reader.rs` | 3 | 3 | 3 | 22 | 22 |
| `src/presentation/accessibility/names.rs` | 4 | 4 | 4 | 32 | 32 |
| `src/presentation/accessibility.rs` | 7 | 7 | 6 | 23 | 23 |
| `src/presentation/wx_app.rs` | **48** | 42 | **43** | 199 | 199 |
| `src/main.rs` | 1 | 1 | 1 | **0** | 0 |
| `tests/theme_reach.rs` | 0 | 0 | 0 | 7 | 7 |

**Corrected 2026-09-11. The paragraph that used to sit above this table said:**
"The tree held 632 records at commit `9611b70` on `main`, version 0.75.0 ... of
the files below only `wx_app.rs` and `tests/wired.rs` moved, which is the same
stability the earlier reading found ... A `grep -c '#\[test\]'` says 73 for
`tests/house_style.rs` where the checker says 64."

The tree holds **720** records. Every file in this plan's own table is unchanged
except `wx_app.rs`, which went from 42 records to **48** and from 37 breaking to
**43**. The grep-versus-checker figures for `tests/house_style.rs` are now **76**
and **67**; that file is not in this table and the sentence is kept only because
it explains why this column is read out of `tests_last_seen` rather than counted.

**The `#[test] today` column is read out of the `tests_last_seen` blocks rather
than counted with a grep**, because the per-commit count check refuses any record
that disagrees with the tree, which makes those stored numbers the checker's own
current answer.

**Add no `#[test]` to `wx_app.rs`**, which would now fire **48** records rather
than 42, so **about 76 minutes** rather than the "roughly an hour" this used to
say. **Corrected 2026-09-11**, and the rate is corrected with a measured figure
rather than withdrawn: the old text derived 1.4 to 1.6 minutes a record from an
undated `CLAUDE.md` figure, and `tests/house_style.rs:5219` now carries **95
seconds a record**, re-measured 2026-09-10 at `eda2719` on one machine with
`WIXEN_TEST_THREADS` at 8 and a warm `target/`. 95 seconds is inside the old band,
so **only the record count moved.** Add none to `accessibility.rs` either, which
would fire 7. Tests for the wiring go in the new
module and read `wx_app.rs` and `main.rs` as text, which is exactly what
`first_run.rs` already does through its own `the_window_that_draws_it` helper and
its comment saying why.

Adding a `pub mod` line to `accessibility.rs` and a call to `wx_app.rs` changes
no test count, so the count check does not fire for either.

**6. `test_every_guard_record_says_how_many_tests_the_files_it_names_held` runs
inside the commit gate**, so its remedy, `scripts/guards.sh --remeasure`, is on
the critical path. Landing this plan's `screen_reader.rs` and `names.rs` tests in
one commit fires 3 + 4 = 7 records once, about eleven minutes; two commits pay it
twice. Run the remedy detached. **Re-checked 2026-09-11: both record counts are
unchanged, the seven are seven distinct records with no overlap, and at the 95
seconds a record `tests/house_style.rs:5219` measured on 2026-09-10 the eleven
minutes is right.**

**7. The About dialog is a pure builder with a test seam, and it already carries
a fact about this build.** `wx_app.rs:21270`, `build_about_dialog(parent,
palette)`, is split out from `show_about_dialog` for exactly the reason its doc
comment gives: a test can build the real dialog without calling `show_modal`.
`tests/theme_reach.rs:837` already does. It formats `Version {}` from
`crate::common::version::current()` at `wx_app.rs:21276`, so a second line about
this build belongs beside that one.

**Corrected 2026-09-11, and the `wx_app.rs` number was wrong on the day it was
written rather than merely stale.** It said `wx_app.rs:20173`. At `c256197`, the
tree this plan says it measured, `build_about_dialog` was at **20441**, and line
20173 was `let mut total_created = 0usize;` in an unrelated loop. Today it is at
21270. `tests/theme_reach.rs:834` was right then and is 837 now, ordinary drift.

**Why both are worth a correction rather than a bumped number.** A reader who
diffs only against HEAD sees one number that moved by 1,097 lines and another that
moved by 3, and reads both as drift in a 21,000-line file. Nothing then asks
whether the first was ever right. Find them by symbol:

```
grep -n 'pub fn build_about_dialog' src/presentation/wx_app.rs
grep -n 'build_about_dialog' tests/theme_reach.rs
```

**8. `main.rs` has a working non-Windows output path already, and the conclusion
holds for a different reason than this premise gave.** `put`, at `main.rs:495`,
tries the stream it was given first, then a console it can attach to, then a
dialog. So a startup line on a Linux or macOS build reaches the terminal without
anything new being built, through `wrote_to_stream` at `:517`, which is ungated
and is the first thing `put` tries.

**Corrected 2026-09-11: this used to say `put`'s "`#[cfg(not(target_os =
"windows"))]` arm is a plain `eprintln!`". `put` has no such arm.** The only
`#[cfg(not(target_os = "windows"))]` in the file is at `:448`, inside
**`show_dialog`**, which is `put`'s third fallback rather than `put` itself. And
`put`'s console step is gated `#[cfg(windows)]` at `:500`, not
`#[cfg(target_os = "windows")]`. The two spellings mean the same thing here; the
point is that the arm the premise sends the executor to read is in a different
function, and task 2's `read_first` sends them to `put` to find it.

```
grep -n 'fn put\|fn show_dialog\|fn wrote_to_stream\|cfg(not(' src/main.rs
```

`main.rs:84` already logs `Starting Wixen Mail v{}` through `tracing`.
A log line is not the criterion: it says the application "says so", and a log
somebody has to go and find is not that.

**9. Criterion 6 says "at startup and in Help", and `--help` is a third thing.**
The Help menu is at `wx_app.rs:6824` to `6853` with Help Contents, the topics, a
Load Sample Mailbox item, and About at `:6849`.

**Corrected 2026-09-11, and the old range was wrong on the day it was written, not
stale.** It said "The Help menu is at `wx_app.rs:6437` to `6473` with Help
Contents, topics and About." At `c256197` the Help menu began at **6525**, and
lines 6437 to 6473 were the **Tools** menu: `ID_SYNC_CALENDAR`, "Sync Calen&dar",
"See and edit the contacts stored for this account". An executor following that
range would have read the wrong menu and found nothing to add an item beside. The
menu also carries a fourth group the old sentence does not name,
`ID_LOAD_SCALE_SAMPLE`, "Load &Sample Mailbox". Find it by symbol:

```
grep -n 'ID_ABOUT' src/presentation/wx_app.rs
```

`--help` is `command_line::HELP`, a `const &str` at `command_line.rs:90` printed by
`main.rs:29`, and a constant cannot carry a conditional sentence without becoming
a function or being appended at print time. Decide in task 2 whether it goes
there too and say what was decided; the criterion does not require it.

**10. This is a disclosure, not a port, and no wording may read as one.**
Criterion 6 says so in as many words: "This closes on the disclosure, not on a
working bridge." Nothing in this plan makes anything work on any platform. Say
that in the changelog and in the summary.

**11. `cargo test --lib a --lib b` does not run.** "the argument '--lib' cannot
be used multiple times". The form that runs is `cargo test --lib -- a b`.
</premise_corrections>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One answer to what this build's accessibility layer does not do, said in the About dialog</name>
  <files>src/presentation/accessibility/platform_bridge.rs, src/presentation/accessibility/screen_reader.rs, src/presentation/accessibility/names.rs, src/presentation/accessibility.rs, src/presentation/wx_app.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `src/presentation/accessibility/screen_reader.rs`: `mod native` and its cfg, `NativeBridgeStatus` and both variants, the `status` field, `ScreenReaderBridge::status`, `Default for ScreenReaderBridge`, `deliver` in full including the `#[cfg(not(target_os = "windows"))]` arm, and the other cfg-gated call sites so it is clear what a second `native` module would have to provide.
    - `src/presentation/accessibility/names.rs`, the whole module header, especially the paragraph saying `wxAccessible` has no GTK or macOS counterpart and naming the two ports a real bridge would need, and `set_accessible_name` and `set_accessible_name_and_description`.
    - `src/presentation/accessibility.rs`: the `pub mod` list, the `Accessibility` struct, and `native_bridge_status`.
    - `src/presentation/wx_app.rs`, `build_about_dialog` and `show_about_dialog`, and where `Accessibility::new()` is called on the startup path.
    - `src/presentation/first_run.rs`, `the_window_that_draws_it` and its comment, which is the pattern for a test that reads a window file as text and says what it cannot see.
    - `CLAUDE.md`, the paragraph on `wxAccessible` and `UiaRaiseNotificationEvent` existing only on Windows and silently doing nothing elsewhere.
  </read_first>
  <behavior>
    - On a build with the Windows bridge compiled in, the answer is that nothing is missing, and the About dialog gains no extra line.
    - On a build with no bridge compiled in, the answer names both halves: that announcements do not reach a screen reader, and that controls without a visible label reach the accessibility tree with no name.
    - The answer is derived from which bridge module compiled, not from a comparison against a platform name, so a test can drive both answers without pretending to be another platform.
    - Adding a bridge module for a third platform changes the answer with no edit to the code that says it. **Corrected 2026-09-11: this used to end "which a test asserts by construction rather than by comment", and no test in this tree can assert it.** No third platform module exists to add, and there is no compile-fail harness to express what a module that does not exist would cause: `grep -n trybuild Cargo.toml` returns nothing. What makes the property true is the shape the action block already prescribes, two `bool` constants supplied by whichever platform module compiled and a pure function of them, with nothing asking what platform this is. Hold it with that shape and a comment saying so, and say in the summary that it is structural rather than tested. No acceptance criterion demanded the test, so nothing else changes.
    - The About dialog on a bridge-less build carries the sentences; on Windows it is unchanged.
    - The sentences say what does not work. They do not say the program is inaccessible, and they do not promise a port.
  </behavior>
  <action>
    RED first, on a branch. A red commit is refused on `main`.

    Tests go in the new `src/presentation/accessibility/platform_bridge.rs`,
    which no record fingerprints, and in `screen_reader.rs` and `names.rs`,
    which 3 and 4 records fingerprint. **Put all three files in one commit** so
    the count check fires once over 7 records rather than twice over 3 and 4.
    Confirm those counts with the `tests_last_seen` parse rather than quoting
    this plan.

    The hard part is making both answers reachable from one test run, because a
    test compiled for Windows cannot ask what a Linux build would say. Decide
    how and say why in the module's comment. The shape that works is to keep the
    two facts as `bool` constants supplied by whichever platform module
    compiled, and to write the sentence-building as a pure function of those two
    booleans, so a test passes `(false, false)` and reads the sentences without
    anything being cross-compiled. Then one further test asserts that the
    constants this build really has are the ones the platform module supplies,
    which is the only part that cannot be driven both ways.

    Say plainly in that comment what the split cannot see: on Windows, the
    bridge-less sentences are only ever built from arguments a test chose, and
    nothing here proves a Linux build would produce them. That is a real limit
    and it belongs in `.planning/WINDOWS.md` as well.

    A trap to walk by hand: a test asserting the About dialog "does not mention
    the bridge" is green on Windows for the wrong reason, because it is green
    both when the disclosure is correctly silent and when the disclosure was
    never written. Write the assertion that goes red, which is about the pure
    function's output for the bridge-less case and about `wx_app.rs` naming the
    accessor.

    Commit RED with `Fails-until-green:` trailers at column 0 naming every new
    test plus
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held`. Say in
    the commit message that the count check is named because the fingerprints
    cannot be corrected before the tests exist.

    GREEN, in this order.

    Give `screen_reader.rs` a `native` module for the case where there is no
    bridge, carrying the same public names the Windows one does and answering
    them as no-ops, plus a constant saying whether announcements reach a screen
    reader. Move `Default`'s `cfg!(target_os = "windows")` to read that constant.
    Replace the `#[cfg(not(target_os = "windows"))]` arm in `deliver` with a call
    into the fallback module, so `deliver` has one shape rather than two. Say in
    the comment that adding a third `mod native` is what a port looks like from
    here, and that the status follows it without anybody remembering.

    Give `names.rs` its own constant saying whether an accessible name reaches
    the accessibility tree on this build, with a doc comment recording that this
    is a statement about wxWidgets rather than about this code, and pointing at
    the module header that measured it.

    Write `platform_bridge.rs`: the two constants read in one place, the pure
    sentence-building function, and one public answer the rest of the program
    calls.

    Decide what happens to `Accessibility::native_bridge_status` and
    `ScreenReaderBridge::status`. Either give them the caller they never had, or
    take them out because the new answer replaces them. **Do not leave them
    there uncalled**, which is the state this plan exists to end. Say which was
    done and why in the summary, and if either is removed, say what
    `cargo clippy -D warnings` said about it.

    Wire the About dialog. Add the line to `build_about_dialog` beside the
    version line, and add no `#[test]` to `wx_app.rs`. The test that it is wired
    is a text test in `platform_bridge.rs` reading `wx_app.rs`, in the shape
    `first_run.rs` already uses, with the same honesty about what it cannot see.

    Write the sentences carefully. They are read by somebody who has just
    started a program that describes itself as an accessible mail client, on a
    platform where large parts of that are not true. Say what does not work and
    what that means in practice. Do not say the program is inaccessible, which
    overstates it, and do not promise a port, which nothing here delivers.
    Reuse the wording `names.rs`'s header already measured rather than writing
    new claims.

    Write the `docs/changelog.md` entry under `[Unreleased]`. Say plainly that
    the program has always known this and never said it. Say that this is a
    disclosure and not a port, in as many words, because a changelog entry about
    the accessibility bridge on Linux will be read as a port otherwise. Bump the
    version in the same commit.

    Re-measure the guard records the count check names, detached:

        scripts/guards.sh --remeasure "<each name the failure prints>"

    Add a `guards/guards.toml` record for the derivation following the module
    rather than the platform. Take the break by hand: make the answer read
    `cfg!(target_os = "windows")` again, which is the half-fix and is exactly the
    state this task is undoing, rather than deleting the constant. Run every
    target with `--no-fail-fast` and write down everything that really went red.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::accessibility::platform_bridge:: presentation::accessibility::screen_reader:: presentation::accessibility::names::</automated>
  </verify>
  <acceptance_criteria>
    - The named test filter passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch, names the count check among its trailers, and `scripts/red-commit.sh` accepted it.
    - The disclosure names both halves of the bridge, announcements and accessible names, and the summary quotes both sentences in full as somebody reads them.
    - The derivation reads a constant supplied by the platform module, and the summary says what adding a third platform module would take and what it would not take.
    - A test drives the bridge-less answer without cross-compiling, and the module's comment says plainly what that cannot prove.
    - `Accessibility::native_bridge_status` and `ScreenReaderBridge::status` are either called from a non-test path or removed. The summary says which, and `grep -rn native_bridge_status src/ tests/` is re-run and its output quoted.
    - No `#[test]` was added to `wx_app.rs` or to `accessibility.rs`; their counts are reported before and after and are unchanged.
    - All three files' tests landed in one commit, and `scripts/guards.sh --remeasure` was run detached over the records the count check named, with the command quoted.
    - The changelog says this is a disclosure and not a port, in as many words.
    - The guard record's break restores the platform comparison rather than deleting the constant, was measured by hand with `--no-fail-fast`, and its full red list is quoted.
  </acceptance_criteria>
  <done>On a build with no accessibility bridge, the About dialog says which parts of the accessibility layer do nothing there, and on Windows it says nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The same thing is said at startup, before anybody has opened a menu</name>
  <files>src/main.rs, src/presentation/accessibility/platform_bridge.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `src/main.rs`: `put` at `:495` in full with its three fallbacks and the comment explaining them, `wrote_to_stream` at `:517` which is the ungated first one, `say`, `complain`, `show_dialog` at `:413` **including its `#[cfg(not(target_os = "windows"))]` arm at `:448` using `eprintln!`**, and the startup sequence around `tracing::info!("Starting Wixen Mail v{}")` at `:84`. **Corrected 2026-09-11: this used to list that arm as though it were inside `put`. It is inside `show_dialog`; premise 8 carries the correction.**
    - `src/presentation/command_line.rs`, `HELP` and `Command::Help`, and how `main.rs:29` prints it, for the `--help` decision.
    - `tests/command_line_output.rs`, which is the existing target covering where output goes and which no guard record fingerprints.
    - Task 1's `platform_bridge.rs` in full.
  </read_first>
  <behavior>
    - On a build with no bridge compiled in, starting the program puts the disclosure where somebody launching it from a terminal will see it.
    - On Windows nothing extra is printed, opened or shown, so a normal start is unchanged.
    - It is said once per start, not once per window and not once per announcement, so it cannot flood.
    - It does not go to a dialog somebody has to dismiss before the program will run.
    - The wording is the same wording the About dialog uses, from one source, so the two cannot drift.
  </behavior>
  <action>
    RED first, on a branch.

    Tests go in `platform_bridge.rs`, which no record fingerprints, and read
    `src/main.rs` as text for the wiring. **Add no `#[test]` to `main.rs`**,
    which holds none today and which one record fingerprints, so the first test
    added there would fire it.

    Choose the channel deliberately and argue for it in one paragraph in the
    summary. The candidates, with what each costs:

    - the stream, through `put` or `say`. Reaches a terminal, reaches a redirect,
      and on Windows falls through to a dialog if there is no stream, which is
      the case to be careful about. Since the disclosure is silent on Windows,
      that fallback is unreachable for it, and the summary should say so rather
      than leave it as a risk.
    - a `tracing` line. Cheap, and not the criterion: a log somebody has to go
      and find is not the application saying something.
    - a dialog. Guardrail 5's bounded half rules it out. It is a modal
      interruption on every start of a program somebody is trying to use.

    The stream is the answer unless reading `put` says otherwise. Read it first.

    A trap to walk by hand: a test asserting "nothing is printed on Windows" is
    green today for the wrong reason, because nothing is printed on Windows
    either way. The assertion that goes red is about the bridge-less path.

    Commit RED with `Fails-until-green:` trailers naming each new test.

    GREEN. Say it once, from the same source as the About dialog's wording.
    Decide, and say what was decided, about `--help`: `command_line::HELP` is a
    `const &str` and cannot carry a conditional sentence, so it would have to be
    appended at print time in `main.rs`. That is two lines and it puts the fact
    in front of somebody who typed `--help` to find out what this is. It is not
    required by the criterion. Say which way it went and why.

    Extend the `docs/changelog.md` entry from task 1. Bump the version.

    Add a `guards/guards.toml` record for the startup line being silent on a
    build that has a bridge. Take the break by hand: make the answer
    unconditional, which is the failure that would put a false warning in front
    of every Windows user, rather than deleting the call. Run every target with
    `--no-fail-fast` and write down everything that really went red.

    Check reachability before claiming this works. Name in the summary the
    non-test function at every hop from process start to the words reaching a
    stream, and say what a person would have to do to see it.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::accessibility::platform_bridge::</automated>
  </verify>
  <acceptance_criteria>
    - The named test filter passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
    - The channel is argued for in one paragraph naming the two rejected candidates and what each would have cost.
    - The About dialog and the startup line take their wording from one source, and the summary names it.
    - The summary says what was decided about `--help` and why.
    - A test drives the bridge-less path and asserts the words; a test drives the bridge-present path and asserts silence.
    - No `#[test]` was added to `src/main.rs`; its count is reported before and after and is still zero.
    - The reachability list names a non-test function at every hop from process start to the words reaching a stream.
    - The guard record's break makes the disclosure unconditional rather than deleting it, was measured by hand with `--no-fail-fast`, and its full red list is quoted.
    - `docs/changelog.md` carries the entry and the version is bumped in the same commit.
  </acceptance_criteria>
  <done>Starting the program on a build with no accessibility bridge says so, before anybody has opened a menu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| what the program claims about its accessibility to somebody who depends on it | this is the product's reason to exist, and a client that looks accessible and is not is the failure guardrail 3 names |
| a compile-time fact to a sentence a person reads | the fact is decided by which module compiled, and a sentence that disagrees with the build is worse than no sentence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Repudiation | a build presenting itself as an accessible mail client on a platform where announcements are discarded and no control has a name | high | mitigate | the whole of this plan. Both halves are named, both are derived from what compiled, and the fact is said in two places a person meets rather than in a log. |
| T-07-09 | Repudiation | a false warning shown on Windows, where the bridge works, teaching people to ignore it | high | mitigate | the guard record in task 2 breaks by making the disclosure unconditional, so the case where it appears wrongly is the case the guard is about. A test drives the bridge-present path and asserts silence. |
| T-07-10 | Repudiation | the disclosure going stale after a real bridge is written, so a working platform keeps apologising | medium | mitigate | the derivation reads a constant supplied by whichever platform module compiled, so a new module carries its own answer. Task 1's guard record breaks by restoring the platform comparison. |
| T-07-11 | Denial of service | a startup disclosure shown as a modal dialog on every start | medium | mitigate | rejected explicitly in task 2 with the reason, which is guardrail 5's bounded half. The channel is the stream. |
| T-07-12 | Information disclosure | a sentence overstating the failure, so somebody who could have used the program with a different reader gives up | medium | accept | the wording names what does not work rather than judging the whole, and reuses what `names.rs` measured through both APIs rather than inventing a claim. Whether it reads that way to somebody who needs it is `unrun-verify`. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | this plan adds no package. `Cargo.toml` is touched only by version bumps and the diff is read rather than assumed. |
</threat_model>

<verification>
- `cargo test --lib -- presentation::accessibility::platform_bridge:: presentation::accessibility::screen_reader:: presentation::accessibility::names::` passes.
- `cargo test --test theme_reach` passes without that file being edited, which is the check that the About dialog still builds.
- `grep -rn "native_bridge_status" src/ tests/ --include=*.rs` either names a caller outside its own definition or returns nothing because the accessor was removed. Report the command and its output.
- `scripts/check.sh` passes on every commit through the hook, with nothing using `--no-verify`.
- Two `guards/guards.toml` records were measured by hand with `--no-fail-fast`, with their full red lists quoted.
- No `#[test]` was added to `wx_app.rs`, `accessibility.rs` or `main.rs`; their counts are reported before and after.
- The `screen_reader.rs` and `names.rs` tests landed in one commit, and `scripts/guards.sh --remeasure` was run detached over the 7 records the count check named.
- **Corrected 2026-09-11: this used to say "Commits touching `Cargo.toml` answer `all` and run the whole gate. Run them detached." That stopped being true on 2026-09-09**, in commit `0e866e3`. `scripts/which-checks.sh` now carries `only_the_packages_own_version_moved` and escalates only when a changed manifest line is something other than this package's own `version = "..."`. On a branch these commits answer `affected`. **The danger is the other direction**: an executor who believes the old sentence reports a full gate that never ran, then skips the `scripts/check.sh all` the branch owes before its merge. Run that once, before the merge.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed after the merge and must not block it. This branch changes `screen_reader.rs`, `names.rs`, `accessibility.rs`, `wx_app.rs` and `main.rs`, which 3, 4, 7, **48** and 1 records fingerprint. **Corrected 2026-09-11: `wx_app.rs` used to be 42.**
</verification>

<success_criteria>
**Criterion 6 closes structurally, all three clauses.** The application says what
its accessibility layer does not do, at startup and in Help, on a build where
the bridge is absent. It is derived from which bridge module compiled rather than
from a hardcoded platform list, so writing a real bridge removes the warning
without anybody remembering to. And it closes on the disclosure: nothing here
makes anything work on any platform, and the changelog says so.

**It is not closed on the platform it is about.** Record as `unrun-verify` in
`.planning/WINDOWS.md`, which ends at entry **301** before this phase
(**corrected 2026-09-11 from 146**; phases 5, 5.1 and 5.2 added 155 entries and
the file's own frontmatter carries the count): no Linux or
macOS build has ever been made, so the sentences have only ever been produced
from arguments a test on Windows chose. Whether they appear on a real
non-Windows start, and whether the About dialog draws them without the layout
breaking, is unverified. Plan 07-06 is the first thing that could answer the
first half of that and it does not answer the second.

**It is not closed by ear either.** Whether somebody who depends on a screen
reader hears these sentences as useful information or as a wall of apology is
something only a person using one can say, and this project has a word for that.

**Two accessors that had never been called now have callers, or are gone.** Say
which in the summary. If the plan's answer is removal, that is guardrail 1
closing rather than the requirement being dodged, and the disclosure that
replaces them is what SHIP-06 asked for.
</success_criteria>

<output>
Create `.planning/phases/07-installing-updating-and-what-is-stored/07-03-SUMMARY.md` when done
</output>
