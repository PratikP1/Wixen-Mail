---
phase: 07-installing-updating-and-what-is-stored
plan: 08
type: execute
wave: 8
depends_on: ["07-02", "07-07"]
files_modified:
  - scripts/build-installer.sh
  - installer/Wixen-Mail-Setup.iss
  - .github/workflows/release.yml
  - tests/installer.rs
  - guards/guards.toml
  - docs/ALPHA_TESTING.md
  - docs/BETA_RELEASE.md
  - docs/installing.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: false
requirements: [SHIP-01]
decisions: [D-06]
user_setup:
  - service: azure-artifact-signing
    why: "D-06. Signing needs a certificate, and only Pratik can create the account, pass identity verification and pay for it. Nothing in this repository can produce a key."
    env_vars:
      - name: AZURE_SIGNING_ENDPOINT
        source: "Azure portal -> the Trusted Signing / Artifact Signing account -> its regional endpoint URI"
      - name: AZURE_SIGNING_ACCOUNT
        source: "Azure portal -> the signing account's name"
      - name: AZURE_SIGNING_PROFILE
        source: "Azure portal -> the certificate profile created under that account"
      - name: AZURE_TENANT_ID
        source: "Azure portal -> Microsoft Entra ID -> Overview"
      - name: AZURE_CLIENT_ID
        source: "the app registration or managed identity given the Trusted Signing Certificate Profile Signer role"
    dashboard_config:
      - task: "Create the signing account and complete identity verification, which is the part with a waiting time"
        location: "Azure portal -> Trusted Signing / Artifact Signing"
      - task: "Create a certificate profile of the public-trust individual type, so the subject carries Pratik's own name"
        location: "Azure portal -> the signing account -> Certificate profiles"
      - task: "Grant the signing identity the Trusted Signing Certificate Profile Signer role, which is a separate step from creating it and is the one people miss"
        location: "Azure portal -> the signing account -> Access control (IAM)"
      - task: "Add the same values as repository secrets, and a federated credential so the release workflow can sign without a stored password"
        location: "GitHub -> Settings -> Secrets and variables -> Actions, and Azure portal -> the app registration -> Federated credentials"
      - task: "Authenticate this machine to the signing account, so a local build can sign and be verified without dispatching a release"
        location: "a terminal on Pratik's machine"

estimate:
  tokens: 94000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every executable this project distributes carries a valid Authenticode signature naming Pratik Patel as the publisher, and a timestamp countersignature so the signature outlives the certificate."
    - "All six artefacts are signed, not the two the requirement's wording suggests, and the uninstaller Inno generates is the seventh and is not forgotten."
    - "A signed build was really produced and really verified before any document says the installer is signed."
    - "What SmartScreen does is still stated rather than promised, so nothing added here trips the guard plan 07-01 built."
    - "No key, password or token reaches the repository or the build log."
  artifacts:
    - "scripts/build-installer.sh: the three binaries signed between building them and running ISCC."
    - "installer/Wixen-Mail-Setup.iss: a SignTool directive and SignedUninstaller, with the prompting behaviour settled from the local help rather than assumed."
    - ".github/workflows/release.yml: the signing identity reaching the build through a federated credential, and a release that fails rather than publishing something unsigned."
    - "tests/installer.rs: the census of what must be signed, held to what the scripts really sign."
    - "docs/ALPHA_TESTING.md, docs/BETA_RELEASE.md, docs/installing.md: corrected, after a signed build exists and not before."
  key_links:
    - "the artefact census -> the two scripts: six things need signing and the requirement's own wording says two. A test that enumerates from the .iss [Files] block and release.yml's files: block, rather than from a list somebody typed, is what catches a seventh appearing later."
    - "SignedUninstaller -> a CI job that cannot answer a prompt: 07-RESEARCH.md's assumption A3 says Inno signs the uninstaller in two passes and may prompt. A prompt in a job with no console is a hang, not a failure, so this is settled from the local Inno Setup 6 help before anything is wired."
    - "the corrected documents -> plan 07-01's guard: 07-01 makes the build fail if a shipped page promises that signing removes the Windows warning. Signing changes the publisher name and does not remove the warning, so the new wording has to say the first without implying the second."
    - "PrivilegesRequired=lowest -> what the signature is worth: installer/Wixen-Mail-Setup.iss:39 means a per-user install shows no UAC prompt at all, so the 'Unknown publisher' line a signature fixes is not on the path most people take. Smart App Control is the real gain and the documents should say so."
---

<objective>
Sign everything this project hands to somebody, with the certificate D-06 chose,
and correct the documents that say it is unsigned only after a signed build has
really been produced and verified.

Purpose: success criterion 1's first clause, the one no earlier plan could close.
"The published installer and the executable inside it both carry a valid
Authenticode signature with a timestamp countersignature, verified against the
published release asset."

**This plan is deliberately last but one, and everything before it closes without
it.** Plans 07-01 through 07-07 do not need a certificate. If the Azure account
takes weeks to verify, the phase is not held up; only this plan and 07-09 wait.
That ordering is the answer to guardrail 3: nothing signing-shaped is written
until there is a key, so nothing here is machinery that has never signed anything.

Output: six artefacts and an uninstaller signed, a test that says which, a real
verified signature, and three documents that stopped being wrong on the day they
stopped being right.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-RESEARCH.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-02-SUMMARY.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-07-SUMMARY.md
@.planning/decisions-2026-09-06.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-06, command first.

**1. The decision is made and the account is not.** `.planning/decisions-2026-09-06.md`
decision 6 chooses Azure Artifact Signing, records the cost at about $10 a month
and the publisher name as Pratik's own, and confirms the United States or Canada
residence requirement is met, asked and confirmed on 2026-09-06. So
`07-RESEARCH.md`'s assumption A1 is settled and the three rejected options are
recorded in SHIP-01's evidence by plan 07-07.

What is not settled is whether the account exists. Nothing in this repository can
create it, pass identity verification or pay for it. **That is the checkpoint in
task 2 and it is the only thing in this plan Claude cannot do.**

**2. The service has been renamed and a search for the old name finds stale
pages.** "Azure Trusted Signing" is now "Azure Artifact Signing". The resource
provider is still `Microsoft.CodeSigning`. Fetch current documentation rather
than recalling it, and record what was read and when, because this plan's whole
subject is an external system that has already been renamed once during this
project's life.

**3. Six things need signing, not two, and a seventh is generated.** Verified
against the tree:

```
grep -n "Source:" installer/Wixen-Mail-Setup.iss     # what goes inside
sed -n '110,140p' .github/workflows/release.yml      # what is published beside it
```

Inside the installer: `target/release/wixen-mail.exe`,
`search-handler/target/release/wixen_mail_search.dll` and
`search-handler/target/release/wixen-mail-search-setup.exe`. Published beside it:
the setup itself, a portable copy of the same binary and a zip of it. Plus the
uninstaller Inno generates, which is the seventh and is the awkward one.

**A plan that signs "the installer and the exe" leaves five things unsigned, two
of which are executables a user runs.** That is why task 1 writes a census rather
than a list.

Note that the portable copy and the zip are copies of `wixen-mail.exe`. If it is
signed before they are made, they inherit the signature and need no second
signing, and if they are made first they do not. **Check the order in
`release.yml` rather than assuming**, and say in the summary which it is, because
"we signed the binary" and "the thing we published is signed" are different
claims and this is exactly where they come apart.

**4. `SignedUninstaller` is an unverified assumption and a prompt in CI is a hang
rather than a failure.** `07-RESEARCH.md` records at MEDIUM confidence that Inno's
`SignedUninstaller` signs in two passes and may prompt, and says plainly that the
Inno help was read through a search summary rather than fetched. **It stays an
assumption until somebody reads the local Inno Setup 6 help**, which is task 1's
first job.

This is not pedantry. A signing tool that prompts, in a job with no console to
answer from, does not fail the run; it holds it until the job times out, and the
failure reads as an infrastructure problem rather than a configuration one. Settle
it before anything is wired.

**5. `PrivilegesRequired=lowest` means the warning a signature fixes is usually
not shown.** `installer/Wixen-Mail-Setup.iss:39`. A per-user install shows no
elevation prompt, so the "Unknown publisher" line on the UAC prompt only appears
on an all-users install. The one everybody meets is SmartScreen, which no
certificate removes on day one.

**So be careful what the corrected documents claim.** The true statements after
this plan are: the installer is signed; the publisher name shown is Pratik Patel
rather than Unknown publisher; SmartScreen may still warn until reputation
accrues; and Smart App Control on Windows 11 stops blocking it. The false one,
which plan 07-01 built a failing build to catch, is that signing makes the warning
go away.

**6. Plan 07-01's guard is the thing most likely to redden in this plan, and that
is it working.** 07-01 adds a check that no shipped page promises signing removes
the Windows warning. Every document this plan touches is one that page-checker
reads. Write the new wording, run `cargo test --test house_style` before
committing, and if it reddens, the wording is wrong rather than the guard.

**7. No key material may enter the repository or the build log, and SHIP-01 says
so.** Azure Artifact Signing satisfies this by holding the key in a service and
signing by calling it, so nothing to protect is ever on disk. What does reach the
build is an identity. Use a federated credential from GitHub Actions rather than a
stored client secret, because a secret is a thing that can leak and a federated
credential is not one. If federation cannot be made to work, say so and stop
rather than falling back to a stored secret quietly.

**Grep the log after the first signed CI run.** A signing tool that echoes its
arguments is how endpoints and account names end up in a public log. That is not
key material and it is still not something to publish by accident.

**8. Guard-record cost, re-measured on 2026-09-06 by parsing `tests_last_seen`.**
632 records at commit `9611b70` on `main`, version 0.75.0.

| file | records fingerprinting it | records breaking it | `#[test]` today |
|---|---|---|---|
| `tests/installer.rs` | created by 07-02 | | |
| `installer/Wixen-Mail-Setup.iss` | 0 | 0 | not Rust |
| `scripts/build-installer.sh` | 0 | 0 | not Rust |
| `.github/workflows/release.yml` | 0 | 0 | not Rust |
| `tests/house_style.rs` | 18 | 6 | 64 |

`tests/installer.rs` does not exist in the tree these figures were taken from;
07-02 creates it and 07-07 adds to it. **Re-measure it with the parse before
committing** rather than trusting any number written here, and remember that a
grep for the file name answers roughly double.

Every test this plan writes goes in `tests/installer.rs`. Add none to
`tests/house_style.rs`, which would cost 18 records and about half an hour, and
none is needed: what this plan asserts is about the build scripts, which is
`installer.rs`'s subject.

**9. This plan can make a release fail and must not make one happen.** Guardrail 7.
`release.yml`'s trigger stays `workflow_dispatch` only. Nothing added here may
tag, publish or push, and the `on:` block is unchanged. Confirm with `git diff`
and quote it. Adding a signing step to an existing job is not a change to when a
release happens; adding a trigger, a schedule or a token would be.

**10. `cargo test --lib a --lib b` does not run.** "the argument '--lib' cannot
be used multiple times". The form that runs is `cargo test --lib -- a b`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: What has to be signed, settled and written down before anything signs</name>
  <files>tests/installer.rs, installer/Wixen-Mail-Setup.iss, guards/guards.toml, .planning/WINDOWS.md</files>
  <read_first>
    - `installer/Wixen-Mail-Setup.iss` in full: `[Setup]` including `PrivilegesRequired`, `AppPublisher`, `OutputBaseFilename` and the `#ifndef AppVersion` guard; the whole `[Files]` block; `[Icons]` as plan 07-02 left it.
    - `scripts/build-installer.sh` in full, especially where each binary is built, where the version and build metadata are computed, and where ISCC is invoked and with what flags.
    - `.github/workflows/release.yml`, `Build the release binary and the setup executable`, `Prepare the portable download` and `Publish GitHub release assets`, in the order the steps really run, so it is known whether the portable copy is taken before or after the binary would be signed.
    - `tests/installer.rs` as plans 07-02 and 07-07 left it, and its module doc about what a text-reading test cannot see.
    - The local Inno Setup 6 help, on `SignTool` and `SignedUninstaller`. This is the one external fact this task exists to settle, and it must come from the help rather than from a search summary.
    - `build.rs` and whatever sets `CompanyName`, so the name in the binary's version resource and the name on the certificate can be compared.
  </read_first>
  <behavior>
    - Every file the installer script installs that is an executable or a library appears in the census, derived from the `[Files]` block rather than typed out.
    - Every file the release workflow publishes that is an executable or an archive of one appears in the census, derived from its `files:` block.
    - The census counts seven things to sign, and a test says so, so a file added to either block later without being signed fails.
    - Whether the uninstaller prompts when `SignedUninstaller` is set is a settled fact with a source, not an assumption.
    - Whether the portable copy is taken before or after the binary is signed is a settled fact, so it is known whether it needs its own signing.
    - Nothing in this task signs anything or configures a signing tool.
  </behavior>
  <action>
    RED first, on a branch. A red commit is refused on `main`.

    **Read the Inno help before writing a line of test.** `SignTool` and
    `SignedUninstaller`. Write down, in the summary and in a comment in the `.iss`
    beside where `SignedUninstaller` will go: whether it signs in two passes,
    whether the second pass can prompt, and what makes it not prompt. Say where in
    the help it was read. If the help contradicts `07-RESEARCH.md`'s assumption
    A3, the contradiction is the finding and it goes in the summary; if it agrees,
    say so, because an assumption confirmed is worth as much as one corrected and
    costs the same to record.

    Then settle the portable-copy ordering by reading `release.yml`'s steps in
    order. Write which it is. This decides whether the census counts a thing that
    inherits a signature or a thing that needs its own.

    Write the census test in `tests/installer.rs`. **Derive both halves, do not
    type them.** Parse the `[Files]` block for `Source:` entries and the `files:`
    block for globs, filter to what is executable, and assert the count and the
    names. The version that does not work is a test holding a list of seven
    strings, which passes when an eighth file is added to the installer and never
    mentions it.

    Take it red by hand before trusting it. Add a fake eighth `Source:` line to
    the `.iss`, watch the census fail and name it, then take it out. **Quote the
    failure.** A census that has never seen a violation is this project's
    guardrail 4 and `CLAUDE.md` records a version guard that was disarmed by its
    own recommended workaround; do not add another.

    Commit RED with `Fails-until-green:` trailers naming each new test.

    GREEN. Whatever makes the census pass against the tree as it is: the census is
    the assertion and the tree is already correct, so the green half here is mostly
    making the parse right. If the parse turns out to need a rule about which
    `Source:` entries are executables, put that rule in the test with a comment
    saying what it excludes and why, because an over-narrow filter is how a census
    stops seeing things.

    **Sign nothing.** No `SignTool` directive, no `SignedUninstaller`, no signing
    step. There is no key yet, and machinery that has never signed anything is
    guardrail 3. The comment recording what the help says is not machinery.

    Record in `.planning/WINDOWS.md` that seven things need signing and that the
    requirement's own wording says two, as a scope finding against SHIP-01 rather
    than as a defect.

    Do not bump the version and do not write a changelog entry. Nothing here is
    user-visible.

    Add a `guards/guards.toml` record for the census seeing a new artefact. Take
    the break by hand: narrow the filter so one real entry stops being counted,
    which is the half-fix that makes a census stop seeing something, rather than
    deleting the test. Run every target with `--no-fail-fast` and write down
    everything that really went red.
  </action>
  <verify>
    <automated>cargo test --test installer</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test installer` passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
    - `SignedUninstaller`'s prompting behaviour is settled from the local Inno Setup 6 help, the summary says where it was read and what it says, and whether it agrees with `07-RESEARCH.md`'s assumption A3.
    - Whether the portable copy is taken before or after the binary would be signed is stated, with the step order quoted from `release.yml`.
    - The census derives both halves by parsing the `.iss` and the workflow, not from a typed list, and the summary quotes the parse.
    - The census was taken red by hand by adding an eighth entry, and the failure is quoted.
    - Nothing was signed and no signing tool was configured; `git diff` shows no `SignTool`, no `SignedUninstaller` and no signing step. Quote the diff.
    - `tests/installer.rs`'s guard-record cost was re-measured with the `tests_last_seen` parse and reported, not quoted from this plan.
    - No `#[test]` was added to `tests/house_style.rs`; its count is reported before and after.
    - The guard record's break narrows the filter rather than deleting the test, was measured by hand with `--no-fail-fast`, and its full red list is quoted.
  </acceptance_criteria>
  <done>What has to be signed is derived from the build rather than remembered, and the one unverified fact about how Inno signs an uninstaller is settled from the help.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: the signing account, which only Pratik can create</name>
  <what-claude-cannot-do>
    Create an Azure subscription, pass an identity verification that names a real
    person, pay about $10 a month, and grant a role in a tenant. None of this is a
    repository operation and none of it can be automated from here.
  </what-claude-cannot-do>
  <context>
    D-06 is taken: Azure Artifact Signing, Pratik's own name as publisher, no
    hardware, residence requirement met. This checkpoint is not the decision. It
    is the setup the decision implies, and it is here rather than at the start of
    the plan because task 1 is real work that does not need it.

    **The part with a waiting time is identity verification**, which is done
    through a third-party verifier against the Azure billing account. Start it
    first; everything else is minutes.

    **The step people miss is the role.** Creating the certificate profile and
    creating the identity that signs with it are two things, and the identity has
    to be granted the signing role on the profile explicitly. A signing attempt by
    an identity without it fails with an authorisation error that reads like a bad
    endpoint.

    **Prefer a federated credential over a stored client secret** for the release
    workflow. A secret is a thing that can leak from a settings page or a log; a
    federated credential is a trust relationship with no material to leak. SHIP-01
    requires that no key reaches the repository or the build log, and the identity
    is the nearest thing to a key in this design.

    **Also authenticate the machine this plan runs on.** Task 3 has to produce a
    real signed build and verify it, and dispatching a release to do that would be
    publishing as a side effect of a plan, which guardrail 7 forbids. A local
    signed build proves the signing works and publishes nothing.
  </context>
  <steps>
    1. Create or choose an Azure subscription and create an Artifact Signing
       account, formerly called Trusted Signing, in a region it is offered in.
    2. Complete identity verification for an individual. This is the step that
       waits.
    3. Create a certificate profile of the public-trust individual type, so the
       certificate subject carries Pratik's own name rather than an organisation.
    4. Create the identity that will sign: an app registration for GitHub Actions
       with a federated credential, and whatever is needed for a local signed
       build on this machine.
    5. Grant that identity the Trusted Signing Certificate Profile Signer role on
       the certificate profile. This is a separate step from step 4.
    6. Add the endpoint, account name, profile name, tenant id and client id as
       repository secrets in GitHub.
    7. Authenticate this machine so a local build can sign.
  </steps>
  <resume-signal>
    Say that the account exists, verification passed, and the profile and role are
    in place. Give the certificate subject name exactly as it appears, because that
    is the name a user will see and it has to match `AppPublisher` and
    `CompanyName` or be reconciled with them deliberately. Confirm whether this
    machine can sign, since task 3 needs that rather than a release.
  </resume-signal>
  <acceptance_criteria>
    - The checkpoint was not auto-approved, whatever `workflow.auto_advance` says.
    - The certificate subject name is recorded exactly, and compared with `AppPublisher` in `installer/Wixen-Mail-Setup.iss` and `CompanyName` in `build.rs`. If they differ, the summary says which changes and why, and that it is a deliberate choice about the name users see.
    - Whether this machine can sign locally is stated, because task 3's shape depends on it and the alternative is dispatching a release, which this phase must not do.
    - No secret value appears anywhere in the repository, in a plan, in a summary or in a commit message. Only the names of the secrets.
  </acceptance_criteria>
  <done>A certificate exists that this project may sign with, and the identity that will use it has the role it needs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sign all seven, prove one, then correct the documents</name>
  <files>scripts/build-installer.sh, installer/Wixen-Mail-Setup.iss, .github/workflows/release.yml, tests/installer.rs, guards/guards.toml, docs/ALPHA_TESTING.md, docs/BETA_RELEASE.md, docs/installing.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `tests/installer.rs` as task 1 left it, especially the census and its parse.
    - Task 1's summary and the `.iss` comment recording what the Inno help says about `SignedUninstaller`.
    - Current Azure Artifact Signing documentation for signing from a build, fetched this session, for how the tool is invoked and what it needs. The service was renamed, so a search for the old name finds stale pages.
    - `docs/ALPHA_TESTING.md` around line 150, `docs/BETA_RELEASE.md` around line 9, and `docs/installing.md` around line 9: the three shipped pages that say the build is unsigned.
    - Plan 07-01's guard on shipped pages promising signing removes the warning, in whatever file 07-01 put it, so the new wording is written against it rather than into it.
    - `docs/changelog.md`'s `[Unreleased]` section as the earlier plans left it.
  </read_first>
  <behavior>
    - Each of the three binaries inside the installer is signed after it is built and before ISCC runs.
    - The setup executable is signed by Inno through a `SignTool` directive.
    - The uninstaller is signed, without prompting, by whatever task 1's reading of the help says makes that true.
    - The portable copy and the zip carry a signature, whether inherited or applied, and the summary says which.
    - Every signature carries a timestamp countersignature, so it stays valid after the certificate expires.
    - A release whose signing step fails does not publish anything.
    - A local build with no signing credential still builds, and says plainly that it is unsigned, rather than failing or silently producing something that looks signed.
    - A test holds the census from task 1 to what the scripts really sign.
    - The three shipped documents say the installer is signed, name the publisher, and do not say the warning is gone.
  </behavior>
  <action>
    RED first, on a branch. A red commit is refused on `main`.

    The RED half is the test that every artefact in task 1's census is signed by
    something in the scripts. It reads the `.iss` and the two scripts as text and
    pairs each census entry with the step that signs it. It is red because nothing
    signs anything yet. Name it in the trailers.

    **Write it to pair, not to count.** A test asserting that the word appears
    seven times passes with seven signings of the same file. Each census entry must
    be matched to a specific step, and an entry with no step must name itself in
    the failure.

    GREEN, and in this order, because each step can be proven before the next is
    written.

    First, sign the three binaries in `scripts/build-installer.sh`, between the
    build and ISCC. Run it locally with the credential from task 2's checkpoint.
    **Then verify by hand before going further**, with the platform's own
    verifier, and quote the output: the subject name, that the chain validates, and
    that a timestamp countersignature is present. A signature with no timestamp
    stops being valid when the certificate expires, and it is the clause the
    criterion names explicitly.

    Second, add the `SignTool` directive to the `.iss` and register the tool with
    ISCC through `/S`. Rebuild locally and verify the setup executable the same
    way.

    Third, set `SignedUninstaller` per what task 1 read in the help, and prove the
    uninstaller is signed, by installing locally and verifying the generated
    uninstaller. **If it prompts, stop and say so**; that is the failure task 1
    existed to predict, and working around it by disabling verification is the
    wrong answer.

    Fourth, handle the portable copy and the zip according to what task 1 found
    about the step order. If they inherit, say so and prove it by verifying the
    published copy rather than the source binary. If they do not, sign them.

    Fifth, wire the release workflow: the federated credential, the signing step,
    and a failure that stops the run. Put the signing before the asset check plan
    07-07 added, so an unsigned artefact fails at the same place a missing one
    does.

    **A build without a credential must still build.** A contributor, or this
    machine before task 2, must be able to run `scripts/build-installer.sh` and get
    an unsigned installer with a clear message saying it is unsigned. It must not
    fail, and it must not quietly produce something a later step treats as signed.
    Say in the summary how that is distinguished and test it by running the script
    with the credential unset.

    Now, and only now, correct the three documents. **The order matters and is the
    point of this task's shape**: nothing says the installer is signed until a
    signature has been verified by hand and the output quoted.

    Write the new wording carefully against plan 07-01's guard. True after this
    plan: the installer is signed; the publisher shown is the certificate subject
    rather than "Unknown publisher"; SmartScreen may still warn on a new version
    until reputation accrues, so the walkthrough in `docs/installing.md` stays; and
    Smart App Control on Windows 11 no longer blocks it. Not true, and the thing
    07-01 fails the build over: that signing makes the warning go away.

    Say plainly in `docs/installing.md` what changes for somebody using a screen
    reader, since that is what the walkthrough is for: the box may still appear and
    the buttons behave the same way, and what is different is the name in it.

    Remember `PrivilegesRequired=lowest`. The UAC prompt a signature fixes is not
    shown on a per-user install, which is the common path. Do not describe a
    benefit most people never see as though it were the main one.

    Write the `docs/changelog.md` entry under `[Unreleased]`. Say what is signed,
    what a user will see differently, and what has not changed. Bump the version.

    Grep the build log from the first signed run for the endpoint, the account and
    the profile names. None is key material and none should be published by
    accident. Say what was found.

    Add a `guards/guards.toml` record for an unsigned artefact not being publishable.
    Take the break by hand: remove one census entry's signing step, which is the
    half-fix that leaves one of seven unsigned, rather than removing signing
    entirely. Run every target with `--no-fail-fast` and write down everything
    that really went red.

    Record in `.planning/WINDOWS.md` what only a real release settles: whether the
    federated credential works from a dispatched run, which cannot be tested from
    a local build, and whether SmartScreen's behaviour on a signed first download
    matches what Microsoft's page says.
  </action>
  <verify>
    <automated>cargo test --test installer</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test installer` passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
    - The test pairs each census entry with the step that signs it, rather than counting occurrences, and the summary quotes the pairing.
    - A real signature was produced and verified by hand, and the verifier's output is quoted: subject name, chain validation, and a timestamp countersignature present. This is quoted before any document is changed, and the summary shows that order.
    - All seven are signed, each verified, and the summary says for the portable copy and the zip whether the signature was inherited or applied and how that was proven.
    - `SignedUninstaller` did not prompt, and the summary says what made that true, referring to what task 1 read in the help.
    - A build with the credential unset produces an unsigned installer with a clear message and does not fail. The summary quotes the run and the message.
    - `.github/workflows/release.yml`'s `on:` block is unchanged, confirmed by `git diff` and quoted. No trigger, schedule or new token was added.
    - The signing step runs before plan 07-07's asset check, so an unsigned artefact fails where a missing one does, and the summary says why that ordering was chosen.
    - `cargo test --test house_style` passes after the document changes, so plan 07-01's guard is satisfied rather than worked around. If it reddened at any point, the summary says what wording caused it.
    - The three documents say the installer is signed and name the publisher; none says or implies the warning is gone; `docs/installing.md` keeps its walkthrough and says what is different for a screen reader user.
    - No secret value appears in the repository, a summary or a commit message; the build log was grepped for the endpoint, account and profile names and the result is reported.
    - The guard record's break removes one entry's signing step rather than removing signing, was measured by hand with `--no-fail-fast`, and its full red list is quoted.
  </acceptance_criteria>
  <done>Everything this project hands to somebody is signed with Pratik's own certificate, a real signature has been verified, and the documents stopped saying otherwise on the same day.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a signing identity to a public build log | the identity is the nearest thing to a key in this design, and a build log is world-readable on a public repository |
| a signed artefact to somebody trusting the name in the box | after this plan a user's decision to run the installer rests partly on a name, so a signature applied to the wrong thing is worse than none |
| the build machine to the signing service | anything that can call the service can sign as Pratik, for as long as the credential lasts |
| a document to a user deciding whether to trust a download | a page saying the installer is signed, when it is not, is the failure this plan's ordering exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-34 | Spoofing | five of seven artefacts unsigned because the requirement's wording says two | high | mitigate | task 1's census is derived from the `[Files]` and `files:` blocks rather than typed, task 3's test pairs each entry with a signing step, and the guard record's break removes one entry's step. |
| T-07-35 | Information disclosure | a signing credential in the repository or a build log | critical | mitigate | a federated credential rather than a stored secret, no key material on disk at any point because the key stays in the service, and the log is grepped for the endpoint and account names after the first signed run. If federation cannot be made to work, the plan stops rather than falling back to a stored secret. |
| T-07-36 | Repudiation | a signature with no timestamp countersignature, which stops verifying when the certificate expires | high | mitigate | the criterion names it, the verification step quotes it from the verifier's own output, and no document is corrected until that output exists. |
| T-07-37 | Tampering | a document saying the installer is signed before anything has been signed | high | mitigate | the document changes are last in the task, after a verified signature is quoted, and the acceptance criteria require the summary to show that order. |
| T-07-38 | Denial of service | Inno's uninstaller signing prompting in a job that cannot answer, hanging the run until it times out | medium | mitigate | task 1 settles the behaviour from the local help before anything is wired, and task 3 stops rather than disabling verification if it prompts anyway. |
| T-07-39 | Elevation of privilege | a change to the release workflow that lets something publish without a person asking | critical | mitigate | the `on:` block is unchanged and an acceptance criterion says so, confirmed by `git diff`. Every change here can only make a release fail, never happen. |
| T-07-40 | Spoofing | a build with no credential producing something a later step treats as signed | medium | mitigate | an unsigned build says so, does not fail, and is distinguishable by the publishing path; tested by running the script with the credential unset. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package is added. The signing tool comes from the platform and from Azure's own tooling rather than from a language registry, and `Cargo.toml` is touched only by the version bump. |
</threat_model>

<verification>
- `cargo test --test installer` passes.
- `cargo test --test house_style` passes, which is what proves plan 07-01's guard is satisfied by the new document wording rather than worked around.
- `scripts/check.sh` passes on every commit through the hook, with nothing using `--no-verify`.
- A real signature was verified by hand and the verifier's output quoted, including the timestamp countersignature, before any document was changed.
- `git diff` on `.github/workflows/release.yml` shows no change to the `on:` block, to `permissions:`, or to anything that could tag, publish or push. Quote it.
- No secret value anywhere; the build log was grepped for the endpoint, account and profile names and the result reported.
- `tests/installer.rs`'s guard-record cost was re-measured with the `tests_last_seen` parse rather than by grepping for the file name.
- No `#[test]` was added to `tests/house_style.rs`; its count is reported before and after.
- Two `guards/guards.toml` records were measured by hand with `--no-fail-fast`, with their full red lists quoted.
- Commits touching `Cargo.toml` answer `all` and run the whole gate. Run them detached.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed after the merge and must not block it.
</verification>

<success_criteria>
**Criterion 1's first clause closes, with one honest qualification.** The
published installer and the executable inside it carry a valid Authenticode
signature with a timestamp countersignature. The verification in this plan is
against a locally built artefact rather than a published release asset, because
dispatching a release to prove a signature would be publishing as a side effect
of a plan, which guardrail 7 forbids. **The published-asset half is recorded as
`unrun-verify` and closes on the first deliberate release.** Say that in the
summary rather than letting the criterion read as fully closed.

**Everything the criterion asks about wording still holds.** SmartScreen's
behaviour is stated rather than promised, `docs/installing.md` keeps the
walkthrough, and plan 07-01's guard is green rather than edited.

**SHIP-01 closes, and it is bigger than the requirement said.** Seven artefacts
rather than two. The summary should say so, because the requirement's own text
would have let a plan sign two things and call it done.

**Three things only a real release settles**, all recorded in
`.planning/WINDOWS.md`: whether the federated credential works from a dispatched
run, which a local build cannot show; whether the signature survives the publish
path intact, which is the difference between "we signed the binary" and "the thing
we published is signed"; and what SmartScreen really does on a signed first
download, which no amount of reading Microsoft's page settles.

**The account is a dependency outside this repository.** If it does not exist,
this plan does not start, and nothing before it in the phase is held up. That is
the ordering's whole purpose and the summary should say whether it held.
</success_criteria>

<output>
Create `.planning/phases/07-installing-updating-and-what-is-stored/07-08-SUMMARY.md` when done
</output>
