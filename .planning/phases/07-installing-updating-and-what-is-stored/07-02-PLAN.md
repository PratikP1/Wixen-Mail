---
phase: 07-installing-updating-and-what-is-stored
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - scripts/which-checks.sh
  - scripts/which-checks.test.sh
  - installer/Wixen-Mail-Setup.iss
  - tests/installer.rs
  - guards/guards.toml
  - docs/changelog.md
  - Cargo.toml
autonomous: true
requirements: [SHIP-03]
estimate:
  tokens: 66000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A commit that changes only the installer script runs the tests that read the installer script, instead of running none of them."
    - "The Start menu entry and the desktop shortcut both name an icon file the installer put there, so the picture on the shortcut does not depend on the executable's resource table being right."
    - "A test that reads the installer script for that runs on the commit that could break it, which is the thing the three existing installer-reading tests do not have."
  artifacts:
    - "scripts/which-checks.sh: an installer script earns the full gate, for the reason the manifest rule beside it already gives."
    - "scripts/which-checks.test.sh: cases for an .iss on a branch, an .iss beside a source file, and an .iss on main."
    - "installer/Wixen-Mail-Setup.iss: assets\\icon.ico shipped into {app}, and IconFilename on both [Icons] entries."
    - "tests/installer.rs: a target that reads the installer script, costing no guard-record re-measurement because nothing fingerprints a new file."
  key_links:
    - "which-checks.sh -> check.sh: `affected` reaches `run_the_tests_that_reach_what_changed`, which maps only src/*.rs and tests/*.rs to targets. An .iss matches neither, so today the only tests an .iss commit runs are the two in `guards_that_read_the_whole_tree` at check.sh:15. Answering `all` instead is what makes any .iss-reading test run."
    - "[Files] -> [Icons]: IconFilename names a file on the user's machine, so the icon has to be installed before it can be named. Inno's own help: 'If this parameter is not specified or is blank, Windows will use the file's default icon.' A [Files] line without the [Icons] change ships a file nothing uses; the [Icons] change without the [Files] line names a path that does not exist."
    - "assets/icon.ico -> build.rs and the installer: build.rs embeds that exact file into the executable and the installer would ship that exact file, so there is one source image and the two copies cannot drift."
---

<objective>
Make an installer-script change run the tests that read the installer script,
then use that to give both shortcuts an icon that does not depend on the
executable's resource table.

Purpose: success criterion 3, and the gate hole underneath it. The criterion is
two attributes on two lines. The reason it is worth a plan is that three tests
already read `installer/Wixen-Mail-Setup.iss` and none of them runs on a commit
that changes only that file, so a fourth would inherit the same hole. That is
`CLAUDE.md`'s "a guard runs on every commit except the ones that could break it",
applied to a build input rather than to a Rust module.

Output: an installer script that earns the full gate, and two shortcuts whose
icon is a file the installer put there.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-RESEARCH.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-06, command first.

**Re-checked 2026-09-11 against `main` at `febe8e4`, version 0.112.0, after
phases 5, 5.1 and 5.2 landed. Nothing in this plan has executed.** Five of the ten
premises below needed correcting and each carries its own. **What held, so nobody
re-checks it:** `build.rs:34` still calls `res.set_icon("assets/icon.ico")` and
the comment at `build.rs:30` still records the executable once having no icon at
all; `SetupIconFile=..\assets\icon.ico` is still the only `.ico` line in the
installer script, still at line 49, and still the wizard's own icon rather than an
installed file; `tests/installer.rs` still does not exist and is still
fingerprinted by no record; `tests/house_style.rs` is still 18 records; and
`cargo test --lib a --lib b` is still refused with "the argument '--lib' cannot be
used multiple times", with `cargo test --lib -- a b` really running.

**1. The gate hole is real and the research's reason for it is wrong, which
matters because the wrong reason points at the wrong fix.** The research says
"Neither `house_style` nor `wired` reads the `.iss`". `house_style` does:
`ours()` in `tests/house_style.rs` calls
`collect(Path::new("installer"), &["iss"], &mut found)`. So a prose rule in
`house_style` already reads the installer script on every commit.

The hole is real for a different reason. `scripts/which-checks.sh` answers
`affected` for an `installer/*.iss` change, and `check.sh`'s
`run_the_tests_that_reach_what_changed` maps only `src/*.rs` to `--lib module::`
and `tests/*.rs` to `--test target`. An `.iss` path matches neither arm, so no
scoped target is chosen, and what runs is formatting, clippy, the suites
`guards/guards.toml` couples to a changed source file (none, see below), and
`guards_that_read_the_whole_tree`, which `scripts/check.sh:26` sets to four
targets. **Corrected 2026-09-11 from `scripts/check.sh:15` and `(house_style
wired)`**; the list is quoted below.

Verify both halves rather than trusting this:

```
grep -n 'Path::new("installer")' tests/house_style.rs   # 54
grep -n 'run_the_tests_that_reach_what_changed' scripts/check.sh   # 387
grep -n 'guards_that_read_the_whole_tree=' scripts/check.sh        # 26
```

**Corrected 2026-09-11: all three coordinates moved and one answer changed.** The
`ours()` call is at `tests/house_style.rs:54`, not in lines 33 to 50, which are
now a doc comment about `.planning` having been outside this reading until
2026-09-07. `run_the_tests_that_reach_what_changed` begins at `scripts/check.sh:387`,
not 363 to 385. And `guards_that_read_the_whole_tree` is at `:26` and holds
**four** targets, not the two this plan says:

```
guards_that_read_the_whole_tree=(house_style wired the_planning_files_agree_with_themselves the_words_that_say_nothing)
```

The third reads `.planning` and the fourth reads prose for the six words
`CLAUDE.md` bans, both added on 2026-09-07 for this paragraph's own reason. **It
makes this premise's conclusion stronger rather than weaker**: an `.iss`-only
commit runs four whole-tree targets today and still none of the three tests that
read the installer script, because all three are unit tests in `src/`.

**The correction changes the options.** A test placed in `tests/house_style.rs`
would already run on an `.iss` commit, so widening `which-checks.sh` is not the
only way. It is still the right way, for two reasons given in task 1.

**2. The three existing installer-reading tests, located by symbol.**

```
grep -rn "Wixen-Mail-Setup.iss" src/ tests/ --include=*.rs
```

returns exactly three call sites: `src/application/forget.rs:650`,
`src/application/running.rs:228`, `src/presentation/first_run.rs:409`. All three
are unit tests in `src/`, so all three are invisible to an `.iss`-only commit.
Widening the gate fixes all four at once, which is the second reason to do it
that way. **Corrected 2026-09-11: `forget.rs` was `:555`; the other two are
unchanged.** The grep above is what to run, and is why this premise gives a
command rather than only the numbers.

**3. No guard record mentions the installer, checked several ways.**

```
grep -niE "installer|build-installer|\.iss" guards/guards.toml
```

returns one hit and it is a false positive: a comment at `guards.toml:16148`
reading "is invisible to the uninstaller that would have removed it", in the
record about credential store owners, matching on "uninstaller". So no suite is
coupled to the installer script through `guards.toml` either.

**Corrected 2026-09-11, and this one was wrong when written rather than stale.**
It used to say the single hit was "the string `frame.iconize(false)` inside an
unrelated record's `before` block, matching on 'icon'". The pattern given contains
no "icon" alternative, so it cannot match `frame.iconize`, and running it at
`c256197` returns the same "uninstaller" line this one does. `frame.iconize` is
really in the file, at `:11260`, which is what makes the sentence read as though
somebody had looked. **The conclusion is unaffected and was re-checked: no guard
record names the installer script or the build script.**

**4. `which-checks.sh` already has the rule to copy, with its reasoning written
out. Corrected 2026-09-11: the line numbers and the shape have both moved.** This
used to say "Lines 132 to 157 make `Cargo.toml` and `Cargo.lock` answer `all`".
The comment is now at `:159`, the collection loop at `:268`, and the escalation at
`:277`, and it is no longer one rule: a manifest answers `all` **unless**
`only_the_packages_own_version_moved` at `:190` proves the whole staged diff is
this package's own version line.

The reasoning is unchanged and is still the sentence to copy: a manifest reaches
every target, something reads it as data, and a manifest change answering
`affected` ran no tests at all because no module maps to it. The file says so
itself: "it is the markdown rule at the top of this file read the other way
round."

The existing case matches `Cargo.toml | Cargo.lock | */Cargo.toml | */Cargo.lock`,
collected into a `manifests` array and then tested. **An `.iss` rule is simpler
than that and should not copy its shape**, because there is no `.iss` equivalent
of "only the version moved": every line of an installer script is a build input.
So write it as the plain path loop the document rule at the bottom of the file
uses, not as a second collect-then-prove pair.

An installer case is `installer/*.iss` or `*.iss`; decide which and say why. An
`.iss` anywhere in the tree is a setup script and there is only one, so the wider
pattern costs nothing and cannot miss a second one added later.

**5. The suite that holds `which-checks.sh` exists, and a case in it can be
committed red.** `scripts/which-checks.test.sh` sources
`scripts/shell-suite.sh`, reports each case through `suite_case_passed` and
`suite_case_failed`, and ends at `suite_verdict`. The name a red commit carries
is `which-checks::<the case description>`, and `shell-suite.sh` refuses a
description that is empty, that holds ` ... `, or that holds a comma. Read
`CLAUDE.md`'s paragraph on this before writing the descriptions; the comma rule
was found by a commit that tried to name a case with one in it.

The existing manifest cases are the shape to copy. **Corrected 2026-09-11: they
no longer read as this plan quotes them.** They used to be:

```
expect all "Cargo.toml" gsd/x Cargo.toml
expect all "a manifest beside a source file" gsd/x src/lib.rs Cargo.toml
```

Every one of them now hands over a fixture diff, because a case that reads the
real index would answer differently depending on what happened to be staged while
it ran, which for this project is a version bump often enough to matter:

```
expect all "Cargo.toml" --manifest-diff-file="$a_feature_too" gsd/x Cargo.toml
expect all "a manifest beside a source file" \
    --manifest-diff-file="$a_feature_too" gsd/x src/lib.rs Cargo.toml
expect affected "a version bump and nothing else in the manifest" \
    --manifest-diff-file="$version_bump" gsd/x Cargo.toml Cargo.lock
```

**An `.iss` case needs no fixture**, because nothing about an `.iss` answer reads a
diff, so the plain two-argument form is still right for the new cases. But read
the `manifest_diff` helper and the block of five `expect all` cases guarding the
exception before writing them: they are the file's own worked example of a rule
that has to be proven not to leak, which is what task 1's load-bearing negative
case is for.

**6. The shortcuts are not iconless today, and the change is smaller than the
requirement's title suggests. Say so rather than claiming a fix.**
`build.rs:34` calls `res.set_icon("assets/icon.ico")`, so `wixen-mail.exe`
carries the icon in its resources. Inno's own help for the `[Icons]` section:
"If this parameter is not specified or is blank, Windows will use the file's
default icon."
[CITED: jrsoftware.org/ishelp/topic_iconssection.htm] So both shortcuts already
show that icon.

**7. There is no `.ico` installed to `{app}` today, which the research does not
say and which decides what `IconFilename` can point at.**

```
grep -n "ico\b|\.ico|assets" installer/Wixen-Mail-Setup.iss
```

returns one line, `SetupIconFile=..\assets\icon.ico` at line 49, and that is the
wizard's own icon, compiled into the setup executable rather than installed. The
`[Files]` section installs `wixen-mail.exe`, `LICENSE`, `README.md`,
`docs\*.md`, the search handler DLL, the search handler setup and the search
handler README. No image at all.

So there are two readings of "IconFilename to the bundled icon" and they differ:

| | what it means | what it buys | what it costs |
|---|---|---|---|
| point at the executable | `IconFilename: "{app}\wixen-mail.exe"` | nothing. It names the same icon Windows already uses by default, so the picture is identical and the line is a comment that compiles | one line, no new file |
| ship the image | a `[Files]` line for `..\assets\icon.ico` and `IconFilename: "{app}\icon.ico"` | the shortcut keeps its icon if the executable ever loses its resource, which has happened here before: `build.rs:30` records "The executable had no icon at all, so Windows drew the generic one" | one file in `{app}`, removed by uninstall with everything else |

**Take the second.** The first is honestly worth nothing and should not be
written up as a fix. The second is testable from a Rust test in the way the
first is not, because a test can assert the installer ships the file the way
`first_run.rs::test_the_installer_ships_the_page_beside_the_program` already
asserts it ships the docs, and no test in this repository can assert the
executable's resource table.

**There is no drift risk in shipping a second copy**, which is the obvious
objection. `build.rs` embeds `assets/icon.ico` and the installer would ship
`..\assets\icon.ico`. One source file, two consumers, nothing to disagree.

**8. `tests/installer.rs` does not exist and costs nothing.** Measured on
2026-09-06 by parsing `tests_last_seen`: no record fingerprints a file that does
not exist, so a new target starts at zero and the count check does not fire.
Compare `tests/house_style.rs`, which 18 records fingerprint. With
`which-checks.sh` widened, an `.iss` commit answers `all` and the whole suite
runs, so the new target is picked up without being named anywhere.

**9. Corrected 2026-09-11, and this changes task 1's design. It used to read:
"`Cargo.toml` in this plan's file list makes every commit answer `all`. That is
the manifest rule from premise correction 4 working as intended, and it means each
version-bump commit runs the full gate. Warm, `CLAUDE.md` measures that at around
five minutes; on a cold tree it can exceed a ten minute cap. Run those commits
detached."**

It stopped being true on 2026-09-09, in commit `0e866e3`, "a manifest diff that is
only the package version stops escalating". `scripts/which-checks.sh` now carries
`only_the_packages_own_version_moved`, reads the staged manifest diff, and lets a
commit through when every changed line is this package's own `version = "..."`.
Measured on a branch with such a diff: `affected`, not `all`.

**Two things follow and the second is task 1's.** An executor who believes the old
sentence reports a full gate that never ran, so run `scripts/check.sh all` once
before the merge. And the exception now sits directly in the path an `.iss` rule
has to reach: see the corrected verification bullet at the end of this plan for
what task 1 must not let the exception skip past, and add the case it names.

**10. `cargo test --lib a --lib b` does not run.** "the argument '--lib' cannot
be used multiple times". The form that runs is `cargo test --lib -- a b`.
</premise_corrections>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: An installer script earns the full gate</name>
  <files>scripts/which-checks.sh, scripts/which-checks.test.sh, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `scripts/which-checks.sh` in full, especially the markdown rule at the top with its two-real-breaks reasoning, and the manifest rule at lines 132 to 157 with the comment explaining why a manifest earns everything.
    - `scripts/which-checks.test.sh` in full: `expect`, `expect_refused`, the manifest cases, and how the file ends.
    - `scripts/shell-suite.sh`: `suite_case_passed`, `suite_case_failed`, `suite_verdict`, and the three descriptions it refuses.
    - `scripts/check.sh`: line 15, the `docs_only` branch, and `run_the_tests_that_reach_what_changed` in full, so it is clear exactly what an `affected` answer runs for an `.iss`.
    - `scripts/red-commit.sh`, so the shape of a shell-suite name in a trailer is right before the commit is attempted.
    - `CLAUDE.md`, the paragraphs on committing a shell-suite case red and on the three things a suite must do.
  </read_first>
  <behavior>
    - `which-checks.sh` answers `all` for a change to the installer script, on a branch.
    - It answers `all` for an installer script named beside a source file.
    - It still answers `all` on `main`, which it did before and which must not be broken by the new case.
    - It still answers `docs_only` for a markdown-only change, and `affected` for a source-only change on a branch, so the new case did not widen anything it should not.
    - Each new case has a description a commit can name: not empty, no ` ... `, no comma.
  </behavior>
  <action>
    RED first, on a branch. A red commit is refused on `main`.

    Write the cases in `scripts/which-checks.test.sh` before touching
    `scripts/which-checks.sh`. Put them beside the manifest cases and copy their
    shape. Include at least one case that must keep answering what it answers
    today, so the commit proves the new rule did not widen anything: a
    markdown-only branch commit answering `docs_only` is the one to pick,
    because that is the answer the new case sits closest to.

    Run the suite and watch it fail:

        bash scripts/which-checks.test.sh

    Commit RED. The trailers name shell-suite cases, in the form
    `which-checks::<the case description>`, at column 0, one per line. Every
    named case must really fail and nothing else may fail, which
    `scripts/red-commit.sh` checks in all three directions. Read `CLAUDE.md`'s
    paragraph on this before writing the descriptions.

    GREEN. Add the case to `which-checks.sh`, in the same loop as the manifest
    case or in one beside it, and write the comment in the same voice as the one
    above it: what reads an `.iss` as data, what answering `affected` really runs
    today, and why that is the hole `CLAUDE.md` names. Name the three existing
    tests that read the file so the next reader can see the size of what this
    closes.

    Decide `installer/*.iss` against `*.iss` and say which and why in the
    comment.

    Then measure what it costs and put the number in the summary. Time
    `bash scripts/which-checks.sh <branch> installer/Wixen-Mail-Setup.iss` for
    the answer, and say what an `all` run costs on this machine, warm, quoting
    the conditions the way `CLAUDE.md` asks. An installer change is rare, which
    is the same argument the manifest rule makes, and the summary should make it
    with a number rather than by assertion.

    Write the `docs/changelog.md` entry under `[Unreleased]`. This is a
    developer-facing change rather than a user-visible one, so consider whether
    it belongs in the changelog at all and say what was decided. Bump the version
    only if something user-visible changed in this commit.

    Add no `guards/guards.toml` record here. A guard record applies a break to a
    source file and names the Rust tests that go red; this behaviour is held by a
    shell suite, which is a different mechanism. Say so in the summary rather
    than leaving it looking like an omission.
  </action>
  <verify>
    <automated>bash scripts/which-checks.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/which-checks.test.sh` passes, ends at `suite_verdict`, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch, names each new case as `which-checks::<description>`, and `scripts/red-commit.sh` accepted it.
    - At least one case asserts an answer that must not change, and the summary says which and why that one is the load-bearing negative.
    - Every case description is one a commit can carry: not empty, no ` ... `, no comma.
    - The comment in `which-checks.sh` names the three existing tests that read the installer script and says what an `affected` answer really runs for an `.iss`.
    - The summary says whether the pattern is `installer/*.iss` or `*.iss`, and why.
    - The summary gives the measured cost of the `all` answer on this machine, warm, with the conditions stated.
    - The summary says why no `guards/guards.toml` record was added.
  </acceptance_criteria>
  <done>A commit that changes only the installer script runs the tests that read the installer script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Both shortcuts name an icon the installer put there</name>
  <files>installer/Wixen-Mail-Setup.iss, tests/installer.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `installer/Wixen-Mail-Setup.iss`: the whole `[Setup]` block including `SetupIconFile` and `UninstallDisplayIcon`, the `[Tasks]` block with the `desktopicon` task, the whole `[Files]` block with its comments about why each thing is shipped, and the `[Icons]` block.
    - `build.rs`, the icon embedding and the comment above it recording that the executable once had no icon at all.
    - `src/presentation/first_run.rs::test_the_installer_ships_the_page_beside_the_program`, which is the pattern for a Rust test reading the installer script, including its comment about what such a test cannot see.
    - `src/application/forget.rs` and `src/application/running.rs`, the other two installer-reading tests, so the new target does not restate what they already assert.
  </read_first>
  <behavior>
    - The installer ships `assets\icon.ico` into the application folder.
    - The Start menu entry names that installed file as its icon.
    - The desktop shortcut names that installed file as its icon.
    - The path both shortcuts name is the path the `[Files]` line installs to, so the two agree rather than each being right on its own.
    - The desktop shortcut is still gated on the `desktopicon` task, which it is today, so somebody who unticks the box still gets no desktop shortcut.
  </behavior>
  <action>
    RED first, on a branch. Tests go in a new `tests/installer.rs`, which no
    guard record fingerprints because it does not exist yet. Confirm that with
    the `tests_last_seen` parse rather than quoting this plan.

    Write the test to read the file as text, which is what the three existing
    ones do and is the only thing available: no test here can run ISCC, install
    anything, or look at a shortcut. Say that limit in the module's own doc
    comment, the way `test_the_installer_ships_the_page_beside_the_program` says
    it, so nobody reads a green test as evidence that a shortcut on a real
    machine has an icon.

    The assertion that ties the two halves together is the one worth writing
    carefully: parse the destination the `[Files]` line installs to and the path
    each `[Icons]` entry names, and assert they are the same. Two separate
    assertions, one per section, would both pass with the icon shipped to one
    folder and named in another.

    A trap to walk by hand: an assertion that the `[Icons]` block contains the
    string `IconFilename` is red today and green after a change that names a path
    nothing installs. Write the assertion about the agreement, not about the
    keyword.

    Commit RED with `Fails-until-green:` trailers naming each new test.

    GREEN. Add the `[Files]` line for `..\assets\icon.ico`, with a comment
    saying why the image is shipped rather than the shortcut pointing at the
    executable: `build.rs` has already shipped an executable with no icon once,
    and a shortcut that names an installed file survives that. Set
    `IconFilename` on both `[Icons]` entries.

    Consider `UninstallDisplayIcon`, which currently names
    `{app}\wixen-mail.exe` and so has the same dependence on the resource table.
    Decide whether it moves too and say what was decided; it is one line and it
    is the icon somebody sees in Apps and Features.

    Say plainly in the summary and in the changelog that the shortcuts were not
    iconless before. `build.rs` embedded the icon and Windows used the
    executable's default, and Inno's own help says an omitted `IconFilename`
    means exactly that. What changed is that the picture no longer depends on the
    resource table being right. A changelog entry claiming shortcuts now have an
    icon would be false.

    Bump the version in the same commit.

    Add a `guards/guards.toml` record for the two paths agreeing. Take the break
    by hand: change the `[Files]` destination so the shortcut names a path
    nothing installs, which is the half-fix rather than the absent one. Run every
    target with `--no-fail-fast` and write down everything that really went red
    rather than what you expected. Note that this is a record whose `file` is not
    a Rust file, which the count check handles by skipping the count for it;
    check that rather than assuming it.
  </action>
  <verify>
    <automated>cargo test --test installer</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test installer` passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
    - The test asserts the installed destination and the named icon path are the same value, not two separate presence checks, and the summary quotes the assertion.
    - The new target's module doc says what a text-reading test cannot see, and the summary repeats it.
    - The summary states plainly that the shortcuts already showed the icon, cites Inno's own wording for the default, and says what actually changed.
    - The changelog entry does not claim shortcuts gained an icon they did not have.
    - The summary says what was decided about `UninstallDisplayIcon` and why.
    - `tests/installer.rs`'s guard-record cost was measured with the `tests_last_seen` parse and reported.
    - The guard record's break redirects the destination rather than deleting a line, was measured by hand with `--no-fail-fast`, and its full red list is quoted.
    - The summary confirms, by running it, that the new target really runs on a commit touching only the `.iss`, now that task 1 has landed.
  </acceptance_criteria>
  <done>Both shortcuts name an icon file the installer installed, and a test that says so runs on the commit that could break it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the commit gate to what actually gets checked | a rule that decides which tests run is the thing that decides whether every other check is real, so a mistake here is invisible and silences other work |
| the installer script to the installed machine | this file is not compiled or type checked by anything, so every claim about it is a claim about text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Repudiation | widening the gate rule so it answers `all` for things it should not, which nobody would notice because `all` never fails wrongly, it only costs time | medium | mitigate | task 1's cases include at least one asserting an answer that must not change, and the suite already covers markdown, source and refusal cases. The RED commit names cases, so `red-commit.sh` proves nothing else broke. |
| T-07-06 | Tampering | an installer that names an icon path nothing installs, so the shortcut falls back silently and nobody sees it until a real install | medium | mitigate | the test asserts the two paths agree rather than asserting each exists, and the guard record's break is exactly that mismatch. |
| T-07-07 | Denial of service | the gate answering `all` on a file somebody edits often, making every commit pay the full suite | low | accept | an `.iss` change is rare, which is the same argument the manifest rule already makes and won on. Task 1 reports the measured cost so the trade is on the record rather than assumed. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | this plan adds no package. `Cargo.toml` is touched only by version bumps and the diff is read rather than assumed. |
</threat_model>

<verification>
- `bash scripts/which-checks.test.sh` passes and ends at `suite_verdict`.
- `cargo test --test installer` passes.
- `bash scripts/which-checks.sh <a branch name> installer/Wixen-Mail-Setup.iss` answers `all`, run and quoted in the summary.
- `scripts/check.sh` passes on every commit through the hook, with nothing using `--no-verify`.
- One `guards/guards.toml` record was measured by hand with `--no-fail-fast`, with its full red list quoted.
- No `#[test]` was added to `tests/house_style.rs` or to any file a guard record fingerprints, so the count check does not fire. Report the count for `tests/installer.rs` before and after, which is zero to whatever it becomes, and confirm no record names it.
- **Corrected 2026-09-11: this used to say "Commits touching `Cargo.toml` answer `all` and run the whole gate. Run them detached." That stopped being true on 2026-09-09**, in commit `0e866e3`. `scripts/which-checks.sh` now carries `only_the_packages_own_version_moved` and escalates only when a changed manifest line is something other than this package's own `version = "..."`. On a branch these commits answer `affected`. **The danger is the other direction**: an executor who believes the old sentence reports a full gate that never ran, then skips the `scripts/check.sh all` the branch owes before its merge. Run that once, before the merge. Measured 2026-09-11 by handing the script a staged diff holding only this package's version line: on a branch, `scripts/which-checks.sh` plus that bump answers `affected`, and so does `installer/Wixen-Mail-Setup.iss` plus that bump.

```bash
bash scripts/which-checks.sh --manifest-diff-file=<a diff holding only the version line> <branch> <the file list>
```

**The second of those is the case task 1 has to get right, and it is new since this plan was written.** The version-bump exception now sits between the manifest collection and everything after it, so an `.iss` change that arrives beside a version bump reaches whatever comes next. If task 1 puts the `.iss` rule in the fall-through path after the manifest block, such a commit answers `all` as intended. If it puts it inside the manifest branch, or anywhere the exception can skip past, an `.iss` commit that also bumps the version quietly answers `affected` and the hole this plan exists to close stays open for exactly the commits this project makes most often, since the convention here bumps the version in the same commit as the change. **Add a case for it**: an `.iss` beside a version bump, on a branch, answering `all`.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed after the merge and must not block it. This branch changes no source file a record breaks, so the scoped set should be small; report what it is rather than assuming.
</verification>

<success_criteria>
**Criterion 3 closes, and it is smaller than it reads.** Both `[Icons]` entries
name an installed icon file, which is what the criterion asks for word for word.
What it does not do is change what anybody sees, because `build.rs` already
embedded the icon and Windows already used it. The summary says that plainly.
The real change is that the picture no longer depends on the executable's
resource table, which has failed here before.

**The gate hole closes for four tests, not one.** The three tests that already
read the installer script were invisible to a commit that changed it, and the
one this plan adds would have been the fourth. After task 1 all four run.

**It is not closed on a real machine.** Record as `unrun-verify` in
`.planning/WINDOWS.md`, which ends at entry **301** before this phase
(**corrected 2026-09-11 from 146**; phases 5, 5.1 and 5.2 added 155 entries and
the file's own frontmatter carries the count): no test here
runs ISCC, installs anything, or looks at a shortcut, so whether the Start menu
entry and the desktop shortcut really show the icon after a real install is
unverified. That is a real installer run on a real machine, and it belongs with
the other things this project can only settle by doing.
</success_criteria>

<output>
Create `.planning/phases/07-installing-updating-and-what-is-stored/07-02-SUMMARY.md` when done
</output>
