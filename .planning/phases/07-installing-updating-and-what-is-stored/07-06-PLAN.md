---
phase: 07-installing-updating-and-what-is-stored
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - .github/workflows/other-platforms.yml
  - .github/workflows/ci.yml
  - tests/house_style.rs
  - .planning/WINDOWS.md
  - docs/changelog.md
  - Cargo.toml
autonomous: false
requirements: [SHIP-05]
estimate:
  tokens: 54000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Somebody can find out, by dispatching one workflow, whether this crate builds and its suite passes on Linux and on macOS, without a merge or a push doing anything."
    - "The answer is written down with the date, the runner image and the version of everything that mattered, so it is a measurement and not an impression."
    - "If it builds, a job that says so runs on every push, so a Windows-only assumption fails on the commit that adds it."
    - "If it does not build, that is recorded as a finding with what failed, and nothing pretends SHIP-05 is closed."
    - "No permanently red job is added to the checks people look at, because a check that is always failing is a check nobody reads."
  artifacts:
    - ".github/workflows/other-platforms.yml: a workflow_dispatch-only build of the main crate on ubuntu-latest and macos-latest."
    - "tests/house_style.rs: the new workflow added to the list test_one_failing_target_does_not_hide_the_rest reads, which is a hardcoded pair today."
    - ".planning/WINDOWS.md: the measurement, whichever way it went."
  key_links:
    - "test_one_failing_target_does_not_hide_the_rest -> the workflow files it reads: the list is the literal `[\"scripts/check.sh\", \".github/workflows/ci.yml\"]`. A new workflow file is invisible to it, so a step that hides later failures could be added there with nothing noticing. Adding the file to that list is part of adding the workflow, not a follow-up."
    - "ci.yml's env -> WIXEN_NO_AUDIO: it is set once at workflow level in ci.yml, so a job in a different file does not inherit it. The Ubuntu runner ships no audio driver either, and the failure shape is a crash that takes several thousand tests with it and reports two."
    - "wxdragon = { version = \"=0.9.17\" } -> the whole question: whether SHIP-05 is a CI change or a port is decided by whether that pinned version has prebuilt libraries for these two platforms. Upstream says it downloads prebuilt wxWidgets rather than building from source; whether that holds for 0.9.17 on these runners is exactly what the probe measures."
---

<objective>
Find out whether this crate builds and passes on Linux and on macOS, by
building it there once, and then act on the answer.

Purpose: success criterion 5. The requirement reads as though this is two CI
jobs. It might be, and it might be a port, and nobody knows because nothing has
ever compiled this crate off Windows. `.planning/REQUIREMENTS.md` says so
itself: "That should be decided after somebody has tried `cargo build` on Linux
once, not before."

So the measurement is the work, and it goes first. Guessing costs a plan written
against the wrong shape.

Output: a workflow anybody can dispatch to get the answer, the answer written
down with its conditions, and either two jobs that run on every push or a
recorded finding that this is a port.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-RESEARCH.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-06, command first. The upstream
facts were fetched this session rather than recalled.

**1. The research's cost claim is probably wrong, and the correction is the
reason to measure rather than to reason.** `07-RESEARCH.md` says wxDragon
"vendors and statically builds wxWidgets from source rather than linking a
system library", so "a Linux CI job is a source build of a C++ GUI toolkit plus
a system package install, not a `cargo test`". Upstream's own README says the
opposite about the build time: "wxDragon automatically downloads pre-built
wxWidgets libraries during the first compilation, reducing build times from 20+
minutes to under 3 minutes."
[CITED: github.com/AllenDang/wxDragon README, fetched 2026-09-06]

Both could be true of different versions. This project pins
`wxdragon = { version = "=0.9.17", features = ["aui", "richtext", "webview"] }`
at `Cargo.toml:75`, and the README describes whatever is on that project's main
branch. **Whether 0.9.17 has prebuilt libraries for `ubuntu-latest` and
`macos-latest` is not knowable from here and is the whole question.** Do not
write a plan around either answer. Measure it.

**2. The Linux package list, quoted rather than paraphrased.** From the same
README, for Ubuntu and Debian:

```
libclang-dev pkg-config libgtk-3-dev libpng-dev libjpeg-dev libgl1-mesa-dev
libglu1-mesa-dev libxkbcommon-dev libexpat1-dev libtiff-dev cmake libssl-dev
libwebkit2gtk-4.1-dev
```

`libwebkit2gtk-4.1-dev` is there for the `webview` feature, which this project
enables. macOS is listed with no platform-specific packages beyond Rust, CMake
and a C++ compiler.

**3. The Windows-only code is smaller than the number of cfg gates suggests, and
that is a reason for optimism rather than a conclusion.**

```
grep -rn 'cfg(target_os = "windows")\|cfg!(target_os = "windows")' src/ --include=*.rs | wc -l
grep -rln "use windows::\|use winreg" src/ --include=*.rs
```

71 gates, and only three files import a Windows crate at all:
`src/application/running.rs`, `src/service/outlook_data_file.rs` and
`src/service/spellcheck/windows_speller.rs`. `Cargo.toml:285` puts the `windows`
crate behind `[target.'cfg(windows)'.dependencies]` and `:301` does the same for
`winresource`. **That says nothing about wxWidgets**, which is the part nobody
has tried, and it is the part that decides the answer.

**4. The search handler is a second crate and is a Windows COM server. Do not
build it.** `search-handler/` has its own target folder, is built separately by
`scripts/build-installer.sh` and linted separately at `ci.yml:150-153`, and every
cargo command in the main crate walks straight past it. SHIP-05 is about the main
crate. Say that in the workflow's own comment rather than leaving somebody to
discover it from a red job.

**5. `WIXEN_NO_AUDIO` is set once at workflow level in `ci.yml` and a new
workflow file does not inherit it.** `ci.yml:20`, with a long comment: the
runners ship no audio driver, `rodio` 0.22.2 opens something anyway and faults on
the first write, and about 3,500 tests were lost that way while the summary named
two. `ubuntu-latest` has no audio device either. **Set it in the new workflow**,
and copy the comment's substance so nobody takes it out reading it as a way to
skip the sound tests. `CLAUDE.md` is explicit that it is not: every test that
plays still runs and still asserts.

**6. The guard that holds CI steps honest reads a hardcoded pair of files, so a
new workflow is invisible to it.**

```
sed -n '5584,5595p' tests/house_style.rs
```

`test_one_failing_target_does_not_hide_the_rest` iterates
`["scripts/check.sh", ".github/workflows/ci.yml"]`. Its own comment records what
it exists to catch: a job stopping at its first failing step, so the search
handler steps reported `skipped` on every run from 2026-07-31 to 2026-09-03.
**Adding the new file to that list is part of adding the workflow.** Leaving it
out builds the same hole in a new place, which is guardrail 4.

That costs a change to `tests/house_style.rs`. Adding a **string to an existing
array** is not a new `#[test]`, so the count check does not fire and the
18-record re-measurement is not owed. Confirm that by looking at the diff rather
than assuming it: if a `#[test]` ends up in that file, the cost is about half an
hour, detached.

**7. A permanently red job is the failure guardrail 4 names, so the probe is
dispatch-only until it passes.** `CLAUDE.md`: CI was red for sixty runs across
five weeks while looking maintained. Adding a Linux job to `ci.yml` on push
before anybody knows whether it builds recreates that exactly. The probe is
`workflow_dispatch` only, run deliberately, and it becomes a push job in task 3
only if it passed.

**8. This is not a release workflow and changes nothing about when a release can
happen.** Say that in the summary in as many words. `.github/workflows/release.yml`
is `workflow_dispatch` only and this plan does not touch it. Nothing added here
tags, publishes or pushes anything, and the new workflow has
`permissions: contents: read` and no token that could.

**9. Guard-record cost, measured on 2026-09-06 by parsing `tests_last_seen`.**
632 records at commit `9611b70` on `main`, version 0.75.0, re-measured on 2026-09-06 after phases 4 and 4.2 landed. The total moved from 617; of the files below only `wx_app.rs` and `tests/wired.rs` moved, which is the same stability the earlier reading found and is not a reason to skip the re-measure. **The `#[test] today` column is read out of the `tests_last_seen` blocks rather than counted with a grep**, because the per-commit count check refuses any record that disagrees with the tree, which makes those stored numbers the checker's own current answer. A `grep -c '#\[test\]'` says 73 for `tests/house_style.rs` where the checker says 64, since the checker requires a trimmed line equal to the attribute and this tree writes the attribute inside doc comments. `tests/house_style.rs` is
fingerprinted by 18 records and holds 64 tests. No record names a workflow file.

**10. Requirement scope, quoted, so nothing is over-read.** SHIP-05's second
`[D]` line says: "No criterion here claims the application is accessible on
Linux or macOS, or usable there. Building is not the same claim." Whatever this
plan finds, nothing it writes may imply otherwise. Plan 07-03's disclosure is the
other side of that and the two should be read together.

**11. The declared Rust floor is 1.88 and the workflow must not pin below it.**
`Cargo.toml:15` says `rust-version = "1.88"`, moved from 1.87 by `pgp = "0.20"`,
which declares 1.88. `ci.yml` uses `dtolnay/rust-toolchain@stable` at all six of
its call sites, so stable satisfies it and the new workflow should do the same
rather than naming a version. **If a version is ever pinned here, pinning below
1.88 fails to build and pinning at a newer one changes what clippy says.** Plan
04-09 measured the second: raising the floor turned on a clippy lint family across
seven pre-existing sites in six files it never opened, and clippy runs with
`-D warnings`, so that is a build failure rather than a warning.

This plan is unlikely to meet either. It is written down because the cost is
invisible until it lands and because a workflow that pins a toolchain is the
obvious thing to reach for when a build behaves differently on one platform.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: A workflow anybody can dispatch to find out</name>
  <files>.github/workflows/other-platforms.yml, tests/house_style.rs, docs/changelog.md</files>
  <read_first>
    - `.github/workflows/ci.yml` in full: the triggers, the workflow-level `env` with `WIXEN_NO_AUDIO` and its whole comment, the `test` job's steps and their comments about `--no-fail-fast` and `if: always()`, the caching steps, and the `audit` job at line 166, which is the only non-Windows job that exists.
    - `.github/workflows/release.yml:1-10`, so it is clear what a deliberately dispatched workflow looks like here and what its comment says about why.
    - `tests/house_style.rs::test_one_failing_target_does_not_hide_the_rest`, in full, including `stops_at_the_first_failure` and `steps_an_earlier_failure_would_skip`.
    - `Cargo.toml`, the `wxdragon` line with its features, and both `[target.'cfg(windows)']` blocks.
    - `CLAUDE.md`, guardrail 4 and the paragraph about CI being red for sixty runs, and the `WIXEN_NO_AUDIO` paragraph in full.
  </read_first>
  <behavior>
    - The workflow runs only when somebody dispatches it. No push, no pull request, no schedule.
    - It builds the main crate and runs its test suite on `ubuntu-latest` and on `macos-latest`, as two jobs that do not depend on each other, so one failing still gives the other's answer.
    - It does not build the search handler, and says why in a comment.
    - It sets `WIXEN_NO_AUDIO`, with the reason, so a missing audio device does not end the run partway and report two failures out of several thousand tests.
    - It installs the packages upstream names for Linux, and nothing beyond Rust and a C++ toolchain for macOS.
    - It runs the suite with `--no-fail-fast` and `--all-targets`, matching `scripts/check.sh`, so a red run says everything that is wrong rather than the first thing.
    - It has read-only permissions and no token that could tag, publish or push.
  </behavior>
  <action>
    This is a configuration-only file, which `CLAUDE.md` lists as one of the
    exceptions to test-first. There is still a testable half and it is task 1's
    second file: the new workflow has to join the list
    `test_one_failing_target_does_not_hide_the_rest` reads, and that change can be
    taken red by hand.

    So: write the workflow, then add its path to the guard's list, then take the
    guard red by hand by putting a step in the new workflow that hides later
    failures, watch it fail, and take it out again. Report that in the summary.
    A guard extended to cover a new file and never shown to see a violation in it
    is a guard that reads as covered.

    Write the workflow. `workflow_dispatch` only, and say in a comment at the top
    why, in the shape `release.yml`'s comment uses: a job nobody knows will pass
    must not be added to the checks people look at, because a permanently red
    check is worse than none. Say that it becomes a push job when it passes, and
    name task 3.

    Two jobs, `ubuntu-latest` and `macos-latest`, neither `needs:` the other.

    Install the Linux packages upstream names, quoting the list from the README
    in a comment with the date it was read, because it is an external fact in a
    project file and this project has already been caught by one of those going
    stale with no commit here.

    Build, then test. `cargo build --all-targets` and
    `cargo test --all-targets --no-fail-fast`, matching what `ci.yml`'s test job
    runs and what `scripts/check.sh` runs, so a difference in the answer is about
    the platform rather than about the command.

    Do not add `working-directory: search-handler` anywhere, and say why in a
    comment.

    Set `permissions: contents: read` at the workflow level.

    Add caching if it is cheap, and say what was decided. A first run with no
    cache is the honest measurement anyway, and that is what task 2 reads, so
    caching is for the second run onward.

    Decide whether the changelog gets an entry. Nothing user-visible changed.
    Say what was decided rather than leaving it.

    Do not bump the version. Nothing user-visible changed and `CLAUDE.md` says a
    bump goes with the change that needs it.
  </action>
  <verify>
    <automated>cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test house_style` passes, and `scripts/check.sh` passes for this commit's file list.
    - The new workflow path is in the list `test_one_failing_target_does_not_hide_the_rest` reads, and the guard was taken red by hand against the new file with the failure quoted in the summary.
    - No `#[test]` was added to `tests/house_style.rs`; its count is reported before and after and is unchanged. If one was added, the summary says why and `scripts/guards.sh --remeasure` was run detached over the 18 records.
    - The workflow triggers on `workflow_dispatch` and nothing else, verified by reading the file, and the summary quotes the `on:` block.
    - The workflow has `permissions: contents: read` and no step that could tag, publish or push, and the summary says so.
    - `WIXEN_NO_AUDIO` is set with a comment saying it is not a way to skip the sound tests.
    - The Linux package list is quoted from upstream with the date it was read.
    - The search handler is excluded with a comment saying why.
    - The summary says plainly that nothing here changes when a release can happen.
  </acceptance_criteria>
  <done>Anybody can dispatch one workflow and find out whether this crate builds and passes on Linux and on macOS.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: run it, on both platforms, and read what happened</name>
  <what-pratik-does>
    Dispatch the `Other platforms` workflow from the Actions tab, on the branch
    this plan is on. Let both jobs finish, whichever way they go. Then report
    four things per platform:

    1. Whether `cargo build --all-targets` succeeded, and if not, the first
       error, in full, including which crate it came from.
    2. Whether `cargo test --all-targets --no-fail-fast` succeeded, and if not,
       how many tests ran, how many failed, and the names of the first few.
    3. How long each job took, wall clock, from the run summary.
    4. The runner image label the job reported, so the measurement carries the
       conditions it was taken under.

    If the build failed on the wxWidgets step specifically, that is the answer
    the whole plan was written to get, and the error text matters more than
    anything else here.
  </what-pratik-does>
  <context>
    **Why this is a person and not a script.** Nothing in this repository can
    build for another platform, and nothing here may dispatch a workflow: this
    project's seventh guardrail says publishing and anything that reaches
    outward happens on purpose, and an agent triggering a GitHub Actions run is
    exactly the side effect that guardrail exists to prevent. The workflow is
    dispatch-only for the same reason.

    **Why this is not last in the plan.** Everything before it is the code, so
    nothing is held up by it. The one thing after it is the response to the
    answer, and there is no version of that which can be written or run without
    knowing what the answer was.

    **What is being asked is narrow.** This is not a decision about whether to
    support Linux. It is a measurement, and it is the measurement
    `.planning/REQUIREMENTS.md` already says should happen before anything is
    planned: "That should be decided after somebody has tried `cargo build` on
    Linux once, not before."

    **Both platforms, even if the first fails.** The two jobs do not depend on
    each other for exactly this reason. macOS and Linux fail differently and one
    answer is not the other.
  </context>
  <resume-signal>
    Paste the four answers for each platform, or the run's URL and a sentence
    saying which jobs passed.
  </resume-signal>
  <acceptance_criteria>
    - Both jobs were dispatched and both finished, whichever way they went.
    - The four answers are recorded per platform rather than in aggregate.
    - Where a job failed, the error text is quoted rather than summarised.
    - No agent dispatched the workflow.
  </acceptance_criteria>
  <done>Somebody has run this crate's build on Linux and on macOS, and what happened is written down.</done>
</task>

<task type="auto">
  <name>Task 3: Act on the answer, whichever it was</name>
  <files>.github/workflows/other-platforms.yml, .github/workflows/ci.yml, tests/house_style.rs, .planning/WINDOWS.md, docs/changelog.md</files>
  <read_first>
    - Task 2's answers.
    - `.github/workflows/ci.yml`, the `test` job in full, so a new job matches its shape.
    - `.planning/WINDOWS.md`, the last few entries, so the new one matches the ledger's format. It ends at entry 146 before this phase.
    - `.planning/REQUIREMENTS.md`, SHIP-05 in full, including the second `[D]` line saying building is not a claim about being accessible or usable.
  </read_first>
  <behavior>
    - Whatever happened is in `.planning/WINDOWS.md` with the date, the runner images, the wxdragon version and the durations, so it is a measurement with its conditions rather than an impression.
    - If both platforms built and passed, a job for each runs on every push and pull request, so a Windows-only assumption fails on the commit that adds it.
    - If either failed, nothing claims SHIP-05 is closed, and the ledger entry says what failed, at which step, with the error.
    - No permanently red job exists on any push trigger.
  </behavior>
  <action>
    Write the ledger entry first, before deciding anything, so the measurement is
    recorded whatever is done with it. Include the date, both runner image
    labels, the pinned `wxdragon` version and its features, the two durations,
    and the exact commands run. `CLAUDE.md` asks that a duration be quoted with
    its conditions, and a first uncached run is a condition that matters a lot
    here.

    **If both built and both suites passed.** Add a job per platform to
    `.github/workflows/ci.yml`, matching the shape of the existing `test` job:
    the same `cargo test --all-targets --verbose --no-fail-fast`, the same
    caching steps, and no search handler step. `WIXEN_NO_AUDIO` is already at
    workflow level in that file, so the new jobs inherit it; confirm that by
    reading rather than assuming.

    Then decide what to do with `other-platforms.yml`. Leaving both is two places
    to keep right. Removing it loses the way to run this deliberately on a
    branch. Say which and why, and if it is removed, take its path back out of
    the guard's list in the same commit so the list does not name a file that is
    not there.

    Run `cargo test --test house_style` after the `ci.yml` change, because
    `test_one_failing_target_does_not_hide_the_rest` reads that file and a new
    job's steps are exactly what it is about.

    Write the changelog entry. This is developer-facing; say what was decided
    about whether it belongs there. Do not write that Wixen Mail runs on Linux
    or macOS. It builds and its tests pass, and SHIP-05's own second `[D]` line
    says those are not the same claim. Plan 07-03's disclosure says what does
    not work there, and the two should agree.

    **If either failed.** Do not add anything to `ci.yml`. Record in the ledger
    what failed, at which step, with the error text, and say what it would take
    to get past it as far as the error allows. Update `.planning/REQUIREMENTS.md`
    SHIP-05's evidence block with the measurement and the date, replacing the
    sentence that says the cost has not been measured.

    Then say plainly, in the summary and in `.planning/STATE.md`, that SHIP-05 is
    a port rather than a CI change, that it is a different phase, and that
    criterion 5 does not close. That is the outcome `.planning/REQUIREMENTS.md`
    already names as possible, so it is a finding rather than a failure. Do not
    leave a half-added job behind as a placeholder.

    **Either way**, keep `other-platforms.yml` dispatch-only or remove it. Do not
    leave a job that is known to fail on a push trigger. That is guardrail 4 and
    it is the specific way CI here went red for five weeks.
  </action>
  <verify>
    <automated>cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test house_style` passes, and `scripts/check.sh` passes for this commit's file list.
    - `.planning/WINDOWS.md` carries the measurement with the date, both runner image labels, the pinned wxdragon version and features, both durations and the exact commands, whichever way it went.
    - If both passed: `ci.yml` carries a job per platform matching the existing `test` job's shape, the summary says whether `other-platforms.yml` stayed and why, and the guard's file list agrees with what exists.
    - If either failed: nothing was added to `ci.yml`, SHIP-05's evidence block in `.planning/REQUIREMENTS.md` carries the measurement and the date, and the summary and `.planning/STATE.md` both say plainly that this is a port and criterion 5 does not close.
    - No job that is known to fail is on a push or pull-request trigger anywhere.
    - Nothing anywhere claims the application is accessible or usable on Linux or macOS, and the summary quotes SHIP-05's second `[D]` line saying why.
    - The summary says plainly that nothing in this plan changes when a release can happen.
  </acceptance_criteria>
  <done>What happens when this crate is built on Linux and on macOS is written down, and the checks match what is really true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a CI workflow to what people believe is checked | a job that is red or skipped while looking maintained is this project's guardrail 4, and it has already cost five weeks here |
| a workflow file to the repository's permissions | a workflow with a write token is a path to tagging and publishing, which guardrail 7 says happens on purpose only |
| upstream package lists to a build somebody trusts | the package list comes from a README and is an external fact in a project file, which this project has been caught by before |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Repudiation | a permanently red Linux job added before anybody knew it would pass, so CI looks checked and is not | high | mitigate | the probe is `workflow_dispatch` only until it passes, and task 3 adds a push job only on a green answer. This is guardrail 4 and it is the exact failure this project spent five weeks in. |
| T-07-24 | Repudiation | a new workflow whose steps hide later failures, invisible to the guard that would catch it | high | mitigate | the guard's file list is a hardcoded pair, so the new path joins it in the same commit and the guard is taken red by hand against the new file. |
| T-07-25 | Elevation of privilege | a workflow with a write token becoming a path to a release nobody asked for | high | mitigate | `permissions: contents: read` at workflow level, no `GITHUB_TOKEN` passed to any step, nothing that tags or publishes, and an acceptance criterion that says so. Nothing in this plan touches `release.yml`. |
| T-07-26 | Denial of service | a test run ended partway by a missing audio device, reporting two failures out of several thousand tests that never ran | high | mitigate | `WIXEN_NO_AUDIO` set in the new workflow with the reason copied, and `--no-fail-fast` so a red run says everything rather than the first thing. |
| T-07-27 | Repudiation | a green build read as "Wixen Mail works on Linux" | high | mitigate | SHIP-05's second `[D]` line is quoted in the acceptance criteria, the changelog is held to it, and plan 07-03's disclosure says what does not work there. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | no cargo package is added. The Linux job installs system packages from Ubuntu's own repositories, quoted from upstream with the date read, and installs nothing from a script fetched at run time. |
</threat_model>

<verification>
- `cargo test --test house_style` passes after each of tasks 1 and 3.
- `scripts/check.sh` passes on every commit through the hook, with nothing using `--no-verify`.
- The guard's file list names every workflow file that exists, and was taken red by hand against the new one.
- No `#[test]` was added to `tests/house_style.rs`; its count is reported before and after.
- `grep -rn "workflow_dispatch\|on:" .github/workflows/other-platforms.yml` shows no push, pull_request or schedule trigger while the answer is unknown. Report the command and its output.
- `.github/workflows/release.yml` is unchanged in this plan, confirmed by `git diff`.
- `Cargo.toml` is not touched, so these commits answer `affected` rather than `all` and run the document-reading and whole-tree guards. Confirm with `bash scripts/which-checks.sh <branch> <the file list>` and quote it.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed after the merge and must not block it. This branch changes `tests/house_style.rs`, which 18 records fingerprint.
</verification>

<success_criteria>
**Criterion 5 closes only if the measurement says it can, and this plan does not
decide that in advance.** If both platforms build and pass, `ci.yml` carries a
job for each and the criterion is met as written: "The crate builds and the suite
passes on Linux and on macOS in CI." If either does not, the criterion does not
close, SHIP-05 is a port rather than a CI change, and that is written down as a
finding rather than as a failure, because `.planning/REQUIREMENTS.md` already
names it as a possible outcome.

**Either way the measurement is the deliverable.** The question "does this build
off Windows" has been open through every phase of this milestone and has never
been asked. After this plan it has an answer with a date and its conditions, in
`.planning/WINDOWS.md`, which ends at entry 146 before this phase.

**Nothing here claims the application works there.** SHIP-05's own second `[D]`
line says building is not the same claim as being accessible or usable, and plan
07-03 is the disclosure that says what does not work. Read together they are the
honest pair: it compiles, and here is what does nothing when it runs.

**Nothing here changes when a release can happen.** The release workflow is
untouched, the new workflow is dispatch-only and read-only, and no step in it
tags, publishes or pushes.
</success_criteria>

<output>
Create `.planning/phases/07-installing-updating-and-what-is-stored/07-06-SUMMARY.md` when done
</output>
