---
phase: 11-reading-and-the-list
plan: 11
type: execute
wave: 20
depends_on: [11-10]
files_modified:
  - src/application/describing_pictures.rs
  - src/application/pictures.rs
  - src/application/long_text.rs
  - src/application/mod.rs
  - src/data/config.rs
  - src/presentation/html_renderer.rs
  - src/presentation/wx_settings.rs
  - src/presentation/wx_app.rs
  - tests/pictures_show_by_default_except_beacons.rs
  - guards/guards.toml
  - docs/privacy.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-09, LIST-10]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A picture a message points at is shown by default; a picture that looks like a tracking pixel by its declared size, and a picture the sender marked decorative, are not fetched; the switch that holds every remote picture back is still there, off by default (LIST-09, #28, Pratik's decision)."
    - "A picture inside a link with no description of its own takes the link's text as its description; a picture with no description and no link text is described as nothing by default, so a screen reader passes over it, unless Settings says image or photo; a sender's own description is untouched."
    - "The privacy page lists every way a reader of mail can be tracked, what this program does about each by default, what a person can change, and what it cannot protect against, each read from the code with the file named; the stale sentence saying there is no setting is corrected by dating (LIST-10, #29)."
    - "Every new setting reaches the Reading tab in this plan; the two settings guards go red on arrival and green with the control."
  artifacts:
    - "src/application/describing_pictures.rs: UndescribedPicture, Nothing, Image or Photo, with label, stored form, parse and default; src/data/config.rs: undescribed_pictures_read_as and the flipped default."
    - "src/application/pictures.rs: looks_like_a_beacon, the link's text as a description, what an undescribed picture is described as; src/presentation/html_renderer.rs: the shown arm applying both; src/application/long_text.rs: the same answer for a picture in a note."
    - "src/presentation/wx_settings.rs: the choice under the two picture boxes on the Reading tab."
    - "tests/pictures_show_by_default_except_beacons.rs: the fixtures through the real cleaner and renderer, the settings guards, the dialog reading."
    - "docs/privacy.md: the pictures section rewritten and dated, the tracking section; docs/USER_GUIDE.md; docs/changelog.md."
  key_links:
    - "hold_back_remote_pictures false by default -> Fetching::Allowed -> what_to_do_about: fetched unless looks_like_a_beacon or the decorative mark says no; the count at the top of the message names what was held back."
    - "undescribed_pictures_read_as -> UndescribedPicture -> the alt written on a picture with none, on the reading path only, never on a message going out."
    - "docs/privacy.md -> the code: every claim on the tracking section names its file, as the page's closing line promises."
---

<objective>
Show remote pictures by default except tracking pixels and pictures the
sender marked decorative, describe an undescribed picture by rule, and
make the privacy page list every way a reader of mail can be tracked.

Purpose: #28, the tester on 2026-09-15: "By default, only beacons should be
avoided along with decorative images. Photo links should have the link
text as the default alt for the photo unless there's an associated alt.
Photos without descriptions should automatically be given "" as the alt
by default unless the user specifically chooses either 'image' or 'photo'
in settings." And his comment: the decision goes on the privacy page,
which #29 widens to every way a reader can be tracked; 10-07 wrote one
line of that list and left the rest.

Output: the three rules, the setting on the Reading tab, the renderer
applying them on the reading path only, the fixtures through the real
cleaner, the privacy page's two sections, the guide and the changelog.
Closes #28 and #29 on the tree side; what a shown picture and a passed-over
one sound like in his reader are his.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef`, working tree clean, before
11-01 to 11-10 land. Command first, then what it said.

**1. Every remote picture is held back by one setting, on by default; the
tester has already turned it off.**

```
grep -n 'hold_back_remote_pictures' src/data/config.rs   -> 89 (the field, #[serde(default = "default_true")]), 715 (the default)
sed -n 555,572p src/application/pictures.rs   -> what_to_do_about: cid or carried -> ItIsCarried; http, https or // -> ItWillBeFetched under Allowed, HeldBack under Blocked; anything else HeldBack
sed -n 464,486p src/presentation/html_renderer.rs   -> hold_back_what_would_be_fetched: a held-back picture becomes a span with what_stands_in_for_it; a shown one goes through say_where_a_decorative_picture_is
sed -n 508,540p src/presentation/html_renderer.rs   -> only an alt that is present and empty is rewritten, on the reading path, never in sanitize_html, for the reason in that function's comment
python -c "import json;d=json.load(open(r'C:\Users\prati\AppData\Local\wixen-mail\config\app_config.json'));print(d['hold_back_remote_pictures'], d['announce_decorative_pictures'])"   -> False False (the tester's profile, read 2026-09-18 through Python, read-only)
```

The issue's `config.rs:89` holds. His profile already fetches every
remote picture, so on his machine this plan's change is the beacon rule
and the descriptions; a fresh profile gets the new default. Nothing here
touches the sending path: the rewrite stays where the existing decorative
rewrite is, after the clean and only when a message is shown.

**2. A beacon can be told by its declared size and by nothing else the
cleaner leaves.**

```
sed -n 142,175p src/presentation/html_renderer.rs   -> ammonia's default allowed set with data: and cid: admitted for our own pictures; no style attribute survives
grep -n 'fn one_attribute' src/presentation/html_renderer.rs   -> the attribute reader the shown arm already uses for src and alt
```

ammonia keeps `width` and `height` on `img` and drops `style`, so an
invisible placement cannot be read after the clean and a declared size
can: a picture whose `width` or `height` parses to at most 1 is a beacon
by the rule this plan writes, and one with no declared size is fetched.
A tracker shaped like an ordinary picture is fetched, which the privacy
page says as the cost of the default the tester chose. A known-host list
was considered and declined: it goes stale the day it is written and
nothing here can keep it current. Decision, overrulable with a reason.

**3. What an undescribed picture is read as today, in mail and in a note.**

```
sed -n 508,540p src/presentation/html_renderer.rs   -> a picture with no alt at all is left as it is; the browser's reader says whatever it says for an image with no name
sed -n 411,420p src/application/long_text.rs   -> Piece::Image("") -> "image with no description", "said rather than skipped"
grep -n 'fn test_an_image_with_no_words_of_its_own_is_still_reported' src/application/long_text.rs   -> 1189
grep -rn 'a_settled_piece_said(' src --include='*.rs' | grep -v 'fn '   -> long_text.rs:325, one caller inside the module
```

Two paths, two answers, and neither is the tester's rule. `long_text`
reads notes and event descriptions rather than mail, but the decision the
issue names is that module's test, so it follows the same setting: its
reader takes the answer and its one test is rewritten in place to the
three answers, count unchanged; the callers hand it the stored setting
the way `html_renderer` reads the two picture settings today.

**4. A linked picture's description is nowhere taken from the link.**

```
grep -n 'anchor_tag_re\|<a\\\\b' src/presentation/html_renderer.rs   -> 120: an anchor matcher exists for another purpose
```

The rule: for an `<a>` whose content is one `<img>` with no `alt` and
some text, the text (tags stripped, whitespace collapsed) becomes the
`alt`; an anchor whose only content is the picture has no text and the
picture is undescribed. Written as a pure function over the cleaned
markup in `pictures.rs`, beside the others.

**5. The privacy page, and what each row of the tracking list reads
from.**

```
sed -n 314,330p docs/privacy.md   -> the pictures section; :326 "There is no setting for this yet", false since the setting was written
grep -n '^## \|^### ' docs/privacy.md   -> the sections the new one cross-references: Link checking (350), Saving a meeting with people on it (297), Asking whether there is a newer version (417), Downloading an update (467), A whole mailbox, a chunk at a time (183), Logging (508)
sed -n 19646,19730p src/presentation/wx_app.rs   -> receipt_for_the_open_message says on opening whether the sender asked and that Send Read Receipt sends one; nothing is sent unasked
grep -n 'rsa' .cargo/audit.toml | head -2   -> 65: the accepted advisory and its reasoning about this channel
sed -n 1,10p docs/privacy.md   -> "There is no analytics, no telemetry and no crash reporting service"
```

Each row of the new section names its file: remote pictures
(`pictures.rs`, `html_renderer.rs`), read receipts (`wx_app.rs`'s
`receipt_for_the_open_message` and `send_receipt_for_the_open_message`,
the `rsa` advisory's sentence), links and link checking (the existing
section, cross-referenced), invitations (cross-referenced), the update
check and the download (cross-referenced), the whole-mailbox download
(10-07's section, cross-referenced), and what is never sent, read from
the code as a census of every place that opens an outbound HTTP client,
not as a grep for the words the header sentence uses (a grep answering
nought for `telemetry` shows only that nobody wrote the word):

```
grep -rn 'reqwest::Client::\|reqwest::blocking::Client\|reqwest::get(' src --include='*.rs' | grep -v '^\s*//' | cut -d: -f1 | sort -u
  -> 14 files on 2026-09-18: common/answering.rs, common/what_ships.rs, service/caldav.rs, carddav.rs, free_busy.rs, google_api.rs, ical_subscription.rs, microsoft_graph.rs, oauth.rs, outward.rs, safebrowsing/client.rs, tasks_api.rs, update_check.rs, update_download.rs (38 sites)
```

Each of those is a provider the person signed in to, a calendar or
address book they added, link checking they switched on, the update check
and download, or the one place a meeting answer goes; the "never" row on
the page lists them by name and says the list is every outbound client
the tree has, re-taken on the day, and names the command. `what_ships.rs`
is a source reader and `answering.rs` sends a meeting reply, each said as
what it is.

**6. The gate for each file.** `src/application/describing_pictures.rs`
maps to itself; `src/application/pictures.rs` to `--lib application::pictures::`,
44 tests, 4 records, no test added (the cases live in the target);
`src/application/long_text.rs` to `--lib application::long_text::`, 60, 18
records, one test rewritten in place; `src/data/config.rs` 68, 11, the
older-file test rewritten in place; `src/presentation/html_renderer.rs`
83, 6, none added; `src/presentation/wx_settings.rs` to its coupled
targets; `src/presentation/wx_app.rs` 199, none added, for the two
settings' reads at startup and on OK if they are held on the window.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the three rules and the setting, pure and held by fixtures</name>
  <files>src/application/describing_pictures.rs, src/application/pictures.rs, src/application/mod.rs, src/data/config.rs, tests/pictures_show_by_default_except_beacons.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/pictures.rs:20-90` and `:540-640`; `src/application/what_is_said_while_fetching.rs` for the shape of a string-valued setting with one enum (10-04).
    - `src/data/config.rs:76-130`, `:600-612`, `:1660-1760` (the older-file test and the two settings guards).
  </read_first>
  <behavior>
    - `describing_pictures::UndescribedPicture`: `Nothing`, `Image`, `Photo`; `ALL` in that order; `label` "Nothing, so it is passed over", "The word image", "The word photo"; `as_stored` `nothing`, `image`, `photo`; `from_stored` answering `Nothing` for anything else; `Default` `Nothing`; `description(self) -> &'static str` answering "", "image", "photo"; `offered_index`.
    - `AppConfig.undescribed_pictures_read_as: String`, serde default `nothing`; `hold_back_remote_pictures` serde and struct default `false`, with the doc comment rewritten: shown by default since 2026-09-18 by the tester's decision, the beacon rule, and what the switch still does; the older-file test asserts both.
    - `pictures::looks_like_a_beacon(tag: &str) -> bool`: a `width` or `height` attribute parsing to at most 1 (digits, optionally `px`); anything else false, including no size. This is the second size rule in the module and its doc says how it differs from the first: `could_be_furniture` (`pictures.rs:459-467`) reads the decoded pixels of a picture this program carries, on the sending side, with a shorter side up to `MOST_FURNITURE_MAY_MEASURE` (200); this reads the declared attributes of a picture a stranger points at, on the reading side, before anything is fetched, because fetching it to measure it is the report the rule exists to prevent, and a pixel or less is the bound because a tracker that large is invisible and a picture that small is not one.
    - `pictures::what_to_do_about` takes the tag's facts as well as the address: `Showing::HeldBackAsABeacon` for a fetched-from address whose tag looks like a beacon; `Showing::HeldBackAsDecorative` for one whose `alt` is present and empty; `ItWillBeFetched` otherwise under `Allowed`; `HeldBack` under `Blocked` as before.
    - `pictures::the_links_text_as_a_description(cleaned: &str) -> String`: for every anchor holding one picture with no `alt` and some text, the picture gains `alt="{text}"` with the text escaped; an anchor with no text leaves the picture undescribed.
    - `pictures::describe_the_undescribed(cleaned: &str, as_: UndescribedPicture) -> String`: every `img` with no `alt` attribute gains `alt="{description}"`; one with an `alt`, empty or not, is untouched.
    - `what_was_held_back` gains the beacon count: "2 pictures that looked like tracking pixels were not fetched." alone, or beside the held-back sentence when the switch is on.
    - The target's cases: each rule over fixtures, including a 1x1, a `0x0`, a `width="1px"`, a picture with no size (fetched), a decorative remote picture, a linked picture with and without text, an undescribed picture under each of the three answers, a described one under each (untouched), and the settings guards failing on arrival for the new field.
  </behavior>
  <action>
    Red first, on a branch: the target's cases bare and the module's
    cases by module path, against stubs; the two settings guards by module
    path (red between the field and the control, as 10-04's were); the
    count check for `config.rs`'s eleven records not fired, because the
    older-file test is rewritten in place. Then the module, the rules,
    the field and the flipped default, with `cargo build` between.

    Guard records: one on `pictures.rs` whose break makes `looks_like_a_beacon`
    false for everything, `suite` the target; one whose break makes
    `describe_the_undescribed` write "image" under `Nothing`, `suite` the
    target; one on `describing_pictures.rs` whose break defaults to
    `Image`; each measured; the four `pictures.rs` records re-measured
    if its count moved (it should not).
  </action>
  <verify>
    <automated>cargo test --test pictures_show_by_default_except_beacons && cargo test --lib application::describing_pictures:: && cargo test --lib application::pictures:: && cargo test --lib data::config::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the two settings guards.
    - `grep -c 'pub fn looks_like_a_beacon\|pub fn the_links_text_as_a_description\|pub fn describe_the_undescribed' src/application/pictures.rs` is 3.
    - `grep -c 'hold_back_remote_pictures: default_true()' src/data/config.rs` is 0.
    - `cargo test --lib application::pictures::` passes with 44 tests and `data::config::` with 68, quoted; the settings guards named in the summary as red at the field and green at the control.
    - Three guard records measured and quoted.
  </acceptance_criteria>
  <done>What is fetched, what is held back and why, and what an undescribed picture is called are three rules held by fixtures, and the setting exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the renderer and the note reader apply the rules, and the Reading tab offers the choice</name>
  <files>src/presentation/html_renderer.rs, src/application/long_text.rs, src/presentation/wx_settings.rs, src/presentation/wx_app.rs, tests/pictures_show_by_default_except_beacons.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/html_renderer.rs:180-240` (the settings read, `HtmlRenderer`'s fields), `:385-540` (the two sanitise paths and the shown arm).
    - `src/application/long_text.rs:300-330` and `:405-425`; `src/presentation/wx_settings.rs:1840-1880` (the two picture boxes) and 10-04's `labelled_choice` with a sentence under it; `src/presentation/wx_app.rs`, the two places 10-04 set a level at startup and on OK, for whether the renderer reads the file itself (it does) or is handed the value.
    - `tests/how_much_message_text_stays_is_read_back_from_the_permissions_page.rs`, the dialog reading shape for a choice read back.
  </read_first>
  <behavior>
    - `HtmlRenderer` reads the third setting with the two it reads; `hold_back_what_would_be_fetched` runs, over the cleaned markup and on the reading path only: the link-text rule, then per picture `what_to_do_about` with the tag's facts (a beacon and a decorative remote picture become an empty span, a shown one keeps its tag), then `describe_the_undescribed` with the setting; the message-top sentence counts beacons; `sanitize_html`, the sending path, is unchanged and a test in the target holds a message sent through it to carry no rewritten `alt`.
    - `long_text`'s reader takes `UndescribedPicture` and `a_settled_piece_said` answers "image, {description}" or, under `Nothing`, leaves the piece unsaid; the one test rewritten in place to the three answers; the module's callers read the setting the way `html_renderer` does.
    - The Reading tab: under the two picture boxes, a `labelled_choice` "An undescribed &picture is read as:" with the three labels, the stored one selected, a `StaticText` under it on both channels: "A picture the sender described keeps its description. A picture inside a link takes the link's words."; `read_the_reading_page` writes the field; the hold-back box's label reads "Do not &fetch any picture a message only points at" with its description saying the default fetches them except tracking pixels.
    - The target's readings: the built dialog offers the three in order and each read back through `read_settings`; a source reading that `sanitize_html` calls none of the three rules.
  </behavior>
  <action>
    Red first, on a branch: the dialog reading and the renderer cases in
    the target bare, the `long_text` test by module path. Then the
    renderer, the reader, the control and the read-back, with
    `cargo build` between. `wx_settings.rs` and `wx_app.rs` gain no test.
    `html_renderer.rs`'s `test_a_tracking_pixel_is_not_fetched` (`:963-966`)
    says "the test that says the default protects against it" while passing
    `Fetching::Blocked` by hand; after the flip the default is `Allowed`, so
    it is rewritten in place, count 83 unchanged: it builds the renderer
    from the default and holds a `1x1` remote picture to not fetched by the
    beacon rule, and its comment says which rule protects now and that the
    switch is the second line of defence.

    Invoke `cognitive-accessibility` for the choice's label, its three
    answers and the sentence under it.

    The changelog entry under `[Unreleased]`, Changed, in the same commit,
    by `writing-craft`: the tester's words; pictures shown by default
    except tracking pixels and decorative ones, what a tracking pixel is
    here and what is missed; the switch; the link's words; the
    undescribed rule and its setting; that a note's pictures follow it
    too; Known limitations: a tracker the size of a picture is fetched,
    and nobody has heard a passed-over picture.

    Guard records: one on `html_renderer.rs` whose break applies the
    undescribed rule inside `sanitize_html`, `suite` the target; one on
    `wx_settings.rs` whose break writes the default back whatever was
    chosen, `suite` the target; each measured; the six `html_renderer.rs`
    records and the eighteen `long_text.rs` records re-measured only if
    their counts moved.
  </action>
  <verify>
    <automated>cargo test --test pictures_show_by_default_except_beacons && cargo test --lib presentation::html_renderer:: && cargo test --lib application::long_text:: && cargo test --test every_event_has_a_control && cargo test --test checkbox_labels && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings and the rewritten test.
    - `grep -v '^\s*//' src/presentation/html_renderer.rs | grep -c 'describe_the_undescribed('` is 1 and it is inside `hold_back_what_would_be_fetched`, quoted with its line.
    - `grep -c 'undescribed_pictures_read_as' src/presentation/wx_settings.rs` is at least 2.
    - `cargo test --lib presentation::html_renderer::` passes with 83 tests and `application::long_text::` with 60, quoted; `cargo test --lib presentation::wx_app::` with 199.
    - Two guard records measured and quoted; the changelog entry names #28.
  </acceptance_criteria>
  <done>A shown message follows the three rules, a sent one is untouched, a note's pictures follow the same setting, and the setting is on the Reading tab.</done>
</task>

<task type="auto">
  <name>Task 3: the privacy page lists every way a reader of mail can be tracked, from the code</name>
  <files>docs/privacy.md, docs/USER_GUIDE.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/privacy.md`, all of it, with the sections premise 5 names.
    - `src/presentation/wx_app.rs:19646-19760`, the receipt path; `.cargo/audit.toml:60-100`, the advisory's reasoning.
    - The census of outbound clients in premise 5, re-run on the day, for the "never" row; and each named file's header, for what its client is for.
  </read_first>
  <action>
    Invoke `writing-craft`. "Pictures a message points at" is rewritten
    in the page's second person: shown by default since the build of this
    plan, what a tracking pixel is and how it is told (a declared size of
    a pixel or less), that a picture the sender marked decorative is not
    fetched either, what fetching a picture tells its sender, that a
    tracker the size of a picture is fetched and cannot be told from one,
    where the switch is (Settings, Reading) and what it does; the sentence
    "There is no setting for this yet" and the two after it are kept and
    corrected by dating in the page's manner, saying the setting has been
    there since it was written and the page did not know.

    A new section after it, "How a reader of mail can be tracked, and what
    this program does about each": one row or short paragraph per way,
    each with the file it was read from: remote pictures (above); read
    receipts (a sender can ask; on opening, this program says so and sends
    nothing unless you choose Send Read Receipt; the `rsa` advisory's one
    sentence that the channel is blind to decryption today); links (an
    address can carry who you are, and opening it tells the site; link
    checking, if on, sends addresses to the checking service, see that
    section); meeting invitations (replying tells the organiser's server;
    saving one with people on it may write to them, see that section);
    the update check and the download (see those sections); the
    whole-mailbox download and the watch (see that section); and what is
    never sent, with the census of outbound clients in premise 5 re-taken
    on the day and quoted as the evidence, each client named by what it
    is for, so "nothing else" is a list and not a memory. The closing
    line's promise, read from the code rather than measured on the wire,
    holds for the section.

    `docs/USER_GUIDE.md`: the pictures paragraph under Reading and
    Managing Email says the default, the setting for undescribed pictures
    and where the switch is, dated.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only
    the tester's reader settles: a shown picture in the preview, a
    passed-over undescribed one, the link's words as a description, and
    the message-top sentence about tracking pixels.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'How a reader of mail can be tracked' docs/privacy.md` is 1; `grep -c 'There is no setting for this yet' docs/privacy.md` is 1 and the line after it carries a dated correction.
    - Every claim in the new section names a file under `src/` or a section of the page, checked by reading the section once more against the tree.
    - Carriage returns 0 on every page; no em dash; none of the six words; the ledger entry in both halves.
  </acceptance_criteria>
  <done>A person reading the privacy page knows every way reading mail here can tell somebody something, what the default is for each, and what cannot be prevented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's markup to the renderer | every rule here runs over cleaned markup; nothing it writes is a URL or a script |
| a fetched picture to its server | the default now tells a sender the message was opened, by the tester's decision, except where the rule catches a pixel |
| the reading path to the sending path | a description written for reading must never be sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-40 | Information disclosure | a remote picture fetched by default reporting the open | high | accept | the tester's decision, with the beacon and decorative rules, the switch, and the privacy page saying the cost plainly |
| T-11-41 | Tampering | a description written into a message going out | medium | mitigate | the rules run in `hold_back_what_would_be_fetched` only; a case sends a message through `sanitize_html` and holds it untouched; a record measures the break |
| T-11-42 | Tampering | a link's text carrying markup into an `alt` | medium | mitigate | tags stripped and the text escaped before it is written; a fixture with markup in the link |
| T-11-43 | Repudiation | a tracking section that drifts from the code | medium | mitigate | every row names its file; the "never" row is a grep quoted on the page |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Five guard records measured; every record the count check flagged re-measured and read.
- What the tester's reader settles: a shown picture, a passed-over one, the link's words, the sentence about pixels; what the fixtures prove: the three rules through the real cleaner, the sending path untouched, the setting read back.
- After the merge, the executor closes both issues from the merge commit: `gh issue close 28 --comment "Fixed in <merge commit>: pictures a message points at are shown by default; a picture whose declared size is a pixel or less, and one the sender marked decorative, are not fetched; the switch that holds every remote picture back is still on the Reading tab and off by default (yours was already off). A picture inside a link with no description takes the link's words; a picture with no description is passed over unless Settings, Reading says image or photo; a sender's own description is untouched. A tracker the size of a picture is fetched, and docs/privacy.md says so. What only your reader settles: a shown picture in the preview and a passed-over one."` and `gh issue close 29 --comment "Fixed in <merge commit>: docs/privacy.md has the section How a reader of mail can be tracked, and what this program does about each: remote pictures with the new default and its cost, read receipts, links and link checking, meeting invitations, the update check and download, the whole-mailbox download and the watch, and what is never sent, each read from the code with the file named; the stale sentence about there being no picture setting is corrected by dating."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criteria 9 and 10 close structurally: the default, the two held-back kinds, the link's words, the undescribed rule and its setting on the Reading tab, the sending path untouched, the privacy page's two sections.
- LIST-09's and LIST-10's `[D]` lines each have a named test or reading in the summary; their `[S]` lines are untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-11-SUMMARY.md` when done
</output>
