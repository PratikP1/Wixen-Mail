---
phase: 11-reading-and-the-list
plan: 09.2
type: execute
wave: 18
depends_on: [11-09.1]
files_modified:
  - src/application/snippet.rs
  - src/application/mod.rs
  - src/application/long_text.rs
  - src/data/message_cache/bodies.rs
  - src/data/message_cache/mod.rs
  - tests/a_snippet_is_the_first_relevant_words.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-17]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A message row's snippet is the message's first relevant words, chosen by reading the text and not by cutting it at 200 characters: addresses dropped altogether, lines that are only an address or a marker skipped, recognisable opening boilerplate skipped, a bare greeting skipped when something follows it, quoted lines and the signature after the delimiter left out, and the first sentences with words in them taken up to the limit, ending at a sentence boundary where one falls inside it (LIST-17, #82, Pratik's decision of 2026-09-18)."
    - "The rules are written down and tested one by one, not a model; a message whose text has nothing but what the rules skip gives the least bad line rather than nothing."
    - "One function derives the snippet for the save and for the pass that puts stored snippets right, over the plain part's lines or the markup's pieces through the reader the message goes through, and the stored snippets are recomputed once by that pass on the next open."
  artifacts:
    - "src/application/snippet.rs: the rules, one function per rule, and first_relevant_words over lines, with the limit; its tests one per rule."
    - "src/data/message_cache/bodies.rs: snippet_of over the module for both parts; the pass widened to every stored body under a new once-only name; the old name left as it is."
    - "tests/a_snippet_is_the_first_relevant_words.rs: a saved body's snippet read back through the cache, and the pass over an older stored snippet."
    - "docs/USER_GUIDE.md, docs/changelog.md."
  key_links:
    - "save_message_body -> snippet_of -> snippet::first_relevant_words(lines, SNIPPET_LIMIT): the plain part's lines, else long_text::structure(&from_markup(html)) pieces as lines."
    - "MessageCache::new -> the widened pass -> every message with a stored body -> snippet_of -> the row rewritten and its index row reindexed when the snippet changed -> work_done_once under the new name."
---

<objective>
The snippet on a message row is the message's first relevant words, not
its first 200 characters.

Purpose: #82, the tester on 2026-09-18 under NVDA on `744d05ef`: a
snippet holding a link reads the whole address out, character by
character, on a row that exists to give a hint of the message. Pratik's
decision the same day on the issue went past the host: the snippet is
chosen by reading the text, with a written set of rules, and a message
whose text is nothing but what the rules skip gives the least bad line.

Output: the rule module with a test per rule, the derivation used by the
save and the pass, the pass widened to recompute every stored snippet
once, the readings, the guide's sentence and the changelog. Closes #82 on
the tree side; whether the rows now say the message is his ear's. A plan
of its own beside 11-09 because that plan is at three tasks and this one
is three of its own.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `eb5d8517`, working tree clean apart
from the planning files, before 11-04 onward land. Command first, then
what it said.

**1. Where a snippet comes from today, and its one derivation.**

```
sed -n 300,325p src/data/message_cache/bodies.rs   -> SNIPPET_LIMIT = 200; snippet_from splits on whitespace, joins with one space, stops at 200 characters
sed -n 327,347p src/data/message_cache/bodies.rs   -> snippet_of: the plain part when it has words, else long_text::words_of_markup(html); "One function for the save and for the pass"
grep -n 'snippet_of\|snippet_from' src/data/message_cache/bodies.rs   -> 400 (the save), 791 (the pass), 1080 and 1115 onward (tests)
grep -rn 'words_of_markup' src --include=*.rs | grep -v long_text.rs   -> bodies.rs alone calls it
```

The issue's second and third lines hold: the plain part is taken as
written, and nothing looks for an address. The one-function shape is
already there, and this plan keeps it.

**2. What the reader gives for markup, and what a link's words are.**

```
sed -n 28,70p src/application/long_text.rs   -> Piece: Heading, Item, Table, Quote, Image, Paragraph
sed -n 479,481p src/application/long_text.rs; sed -n 500,502p   -> from_markup reads OnlyWhatIsSpoken; words_of_markup is words_of(&structure(&from_markup(html)))
sed -n 1576,1584p src/application/long_text.rs   -> "See the report": a link's address is dropped and its words kept; a link whose words are its address keeps the address as words
```

So for markup the pieces are the units, as the decision says: each
piece's text is one line to the rules, a `Quote` is a quoted line, an
`Image`'s description is not a first relevant word of a message, and a
`Table` is its cells in order. A link whose words are its own address
arrives as an address token in a piece's text and the address rule drops
it there.

**3. The signature delimiter already has a splitter.**

```
sed -n 27,28p src/application/sign_off.rs; sed -n 76,92p   -> DELIMITER "-- "; split(body) -> (before, Some(signature)) at the last separator on a line of its own
grep -c '#\[test\]' src/application/sign_off.rs   -> 18; one record names it
```

The signature rule calls `sign_off::split` rather than finding the
delimiter again. `canonical_delimiter` first, since a plain part written
by another client may carry `--` without its space, and `sign_off` already
knows that.

**4. The pass, its once-only marker and its candidates.**

```
sed -n 350,351p src/data/message_cache/bodies.rs   -> SNIPPETS_PUT_RIGHT: "snippets of HTML-only bodies re-derived through the reader"
sed -n 750,760p src/data/message_cache/bodies.rs   -> work_done_once asked by name; answers 0 without reading a body when the row is there
sed -n 767,779p src/data/message_cache/bodies.rs   -> the candidates: messages whose body has no plain part and has markup
sed -n 1400,1416p src/data/message_cache/mod.rs   -> called from MessageCache::new after the inline move, not fatal, one info line with the count and the milliseconds
grep -n 'fn test_.*put_right\|fn test_snippets' src/data/message_cache/bodies.rs   -> 1199, 1249: the two tests of the pass
```

A database that ran the old pass has its row and would never run again
under the old name, so the widened pass runs under a new name and over
every message with a stored body, plain or not. The old constant stays
and its row is left where it is, since a row in `work_done_once` is a
record that a thing happened. The tester's profile holds 17,753 messages
(read 2026-09-18 through Python, read-only); how many of them have a
stored body is not known from here, and the pass's cost on open is the
one figure this plan asks the executor to take before the merge.

**5. The gate for each file.** `src/application/snippet.rs` is new:
`--lib application::snippet::`, no records yet, so its own tests move no
count check; `src/data/message_cache/bodies.rs` maps to `--lib
data::message_cache::bodies::`, 50 tests, 10 records, its two pass tests
and `snippet_from`'s test at `:1080` rewritten in place and no case
added, the new cases in the target instead; `src/application/long_text.rs`
60 tests, 18 records, one function renamed and reshaped and its test
rewritten in place, count kept; `src/application/sign_off.rs` 18, 1, not
changed; `src/data/message_cache/mod.rs` changes one call. Counts
taken 2026-09-18 at `eb5d8517` with `grep -c '#\[test\]'` and the parser
command in `CLAUDE.md`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the rules, one function and one test each, and the first relevant words over lines</name>
  <files>src/application/snippet.rs, src/application/mod.rs</files>
  <read_first>
    - Issue #82 and Pratik's comment on it, which is the rule list.
    - `src/application/sign_off.rs:27-92` (`DELIMITER`, `split`, `canonical_delimiter`).
    - `src/application/long_text.rs:28-70` (`Piece`) and `:1107` (`first_line`, the nearest neighbour in shape).
    - `src/data/message_cache/bodies.rs:300-347`, what is being replaced.
  </read_first>
  <behavior>
    - `pub fn first_relevant_words<'a>(lines: impl Iterator<Item = &'a str>, limit: usize) -> String`, the one entry point, and `pub const LIMIT: usize = 200` moved here from `bodies.rs` with its comment.
    - `is_an_address(word) -> bool`: `http://`, `https://`, `www.`, `mailto:`, and a bare `name@host.tld`; `without_addresses(line) -> String` drops those words and tidies the punctuation they leave (`(` `)` around a dropped word, a comma left hanging).
    - `is_only_a_marker(line)`: a line that is empty after addresses are dropped, or holds only punctuation, or is a run of three or more `-`, `=`, `_` or `*`, or is a bullet marker alone.
    - `is_opening_boilerplate(line)`: case-insensitive, whole line or a line holding one of "view this email in your browser", "view in browser", "view online", "email not displaying correctly", "unsubscribe", "no images?", "having trouble viewing"; the list is a `const` slice with a doc line saying it is the marketing openers seen in the tester's mail and grows by evidence.
    - `is_a_bare_greeting(line)`: at most five words, first word one of Hi, Hello, Hey, Dear, Greetings, Good (morning, afternoon, evening), with or without a name, ending in a comma, colon, exclamation mark or nothing; skipped only when a later line has words.
    - `is_quoted(line)`: begins with `>` after whitespace; `without_the_signature(text)` is `sign_off::split(&sign_off::canonical_delimiter(text)).0`.
    - The sentences: the surviving lines' words joined with one space; when the joined text is longer than the limit, cut at the last sentence end (`.`, `!` or `?` followed by a space, at least one word before it) inside the limit; when none falls inside it, at the last word boundary inside the limit, as today.
    - The least bad line: when no line survives, the first line that has any word after addresses are dropped; when none has, the first non-empty line as written; when there is no line, empty.
    - One test per rule, named for the rule and for the case, over the examples in the issue and the comment: a bare address, an address in parentheses, one followed by a full stop, a link whose words are its address, `mailto:` alone on a line, a line of dashes, "View this email in your browser" first, "Hi Pratik," with a sentence after it and "Hi Pratik," alone, a quoted reply with one new line above it, a signature after `-- `, a text with two sentences where the second crosses the limit, a text with one sentence longer than the limit, and text with no address (unchanged). A companion for each skipping rule that hands in a line the rule must not skip.
  </behavior>
  <action>
    Red first, on a branch: the module's cases bare by path, no count
    check named since nothing names the new file. Then the rules as small
    functions with one job each, `first_relevant_words` composing them in
    the order the decision lists, with `cargo build` between. Invoke
    `elegant-code`: no regex crate is added, the address test is a few
    `starts_with` checks and one `@` rule, and the boilerplate list is
    data rather than branches. `pub mod snippet;` in
    `src/application/mod.rs` with a doc line that says what a row's
    snippet is for.

    The rules are a fixed list and a fixed order, and the comment on
    `first_relevant_words` names the issue and the decision so the next
    person adding a rule adds a test with it.
  </action>
  <verify>
    <automated>cargo test --lib application::snippet::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases.
    - `grep -c 'pub fn first_relevant_words' src/application/snippet.rs` is 1; `grep -c 'regex' src/application/snippet.rs` is 0; `grep -c 'sign_off::split' src/application/snippet.rs` is 1.
    - `cargo test --lib application::snippet::` passes with its count quoted, at least fourteen.
    - No rule is asserted only in a case that could pass with the rule missing: every skipping rule has its companion.
  </acceptance_criteria>
  <done>The rules exist as functions with a test each, and the first relevant words of a text come out of one entry point with the limit honoured at a sentence boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one derivation for the save and the pass, and the pass widened to every stored snippet once</name>
  <files>src/data/message_cache/bodies.rs, src/data/message_cache/mod.rs, src/application/long_text.rs, tests/a_snippet_is_the_first_relevant_words.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/bodies.rs:300-347`, `:395-405`, `:728-812` (the pass, its query, its marker row) and `:1080-1135`, `:1199-1290` (its tests and the snippet helper).
    - `src/data/message_cache/mod.rs:1396-1416`, the call and its log line.
    - `src/application/long_text.rs:74-90` (`structure`), `:479-515` (`from_markup`, `words_of_markup`, `words_of`) and `:1681-1700` (its test).
    - `tests/` for the nearest integration test that opens a `MessageCache` under `tempfile`, saves a body and reads rows back; copy its opening.
  </read_first>
  <behavior>
    - `snippet_of(body)`: the plain part's `lines()` when it has words, else the pieces of `long_text::pieces_of_markup(html)` as lines (a `Table` its cells joined with a space, an `Image` skipped, a `Quote` given with a leading `>` so the quoted rule sees it), through `snippet::first_relevant_words(lines, snippet::LIMIT)`; `snippet_from` and `SNIPPET_LIMIT` retired from `bodies.rs`.
    - `long_text::words_of_markup` becomes `pieces_of_markup(html) -> Vec<Piece>`, `structure(&from_markup(html))`, with its doc paragraph kept and reworded, and its one test at `:1681` rewritten in place to assert the pieces' words are the words alone without markers or stylesheets; no public function is left with no caller outside tests, and `long_text.rs`'s count stays at 60.
    - The pass is renamed `put_right_the_stored_snippets`, its candidates every message with a stored body (plain or markup, packed or not), its marker `SNIPPETS_ARE_THE_FIRST_RELEVANT_WORDS` with the date in the string, and the old constant kept beside it with a comment saying the row it names is history; the reindex of a changed row is unchanged; the log line in `mod.rs` says "stored snippets" rather than "HTML-only messages".
    - The two pass tests are rewritten in place to the new name and a plain body among the candidates; the `snippet_from` test at `:1080` becomes a test that `snippet_of` of a plain body with an address on its first line has no address in it.
    - The target: a cache under `tempfile`; a plain body opening "View this email in your browser", a line of dashes, "Hi Pratik,", then "Your parcel is on its way. Track it at https://example.com/t?x=1. Thanks." gives the snippet "Your parcel is on its way. Track it at. Thanks." with the address gone and the punctuation tidied (the exact string is the target's to settle and quote); an HTML-only body with the same words gives the same snippet; a row whose stored snippet is the old first-200-characters form is put right by the pass on the next open and the pass records itself once; a body that is only a signature after `-- ` gives the least bad line.
    - The cost: a test in the target saves 2,000 short plain bodies and times the pass, printing the milliseconds, asserting only that it finished; the summary quotes the figure with the command and the date, and the changelog's Known limitations says the first open after the update recomputes every stored snippet once and what that cost on 2,000.
  </behavior>
  <action>
    Red first, on a branch: the target's cases bare, the rewritten
    `bodies.rs` tests by module path, the count check named as one of the
    failures if a case count in `bodies.rs` moves, which it must not (the
    three rewritten in place). Then `snippet_of`, the widened query, the
    new marker, the rename and the call, with `cargo build` between.

    The query: the existing one with its `WHERE` reduced to "a body row
    exists", written so a packed body counts; the candidates still read
    into a `Vec` first for the reason the pass's comment gives.

    Guard records: one on `bodies.rs` whose break makes `snippet_of` take
    the plain part as written, the target's address case red; one whose
    break narrows the pass's candidates back to HTML-only bodies, the
    target's put-right case red; each measured; the ten records naming
    `bodies.rs` re-measured only if the count check names them.
  </action>
  <verify>
    <automated>cargo test --test a_snippet_is_the_first_relevant_words && cargo test --lib data::message_cache::bodies:: && cargo test --lib application::long_text::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases.
    - `grep -c 'fn snippet_from' src/data/message_cache/bodies.rs` is 0; `grep -c 'snippet::first_relevant_words' src/data/message_cache/bodies.rs` is 1; `grep -c 'put_right_the_stored_snippets' src/data/message_cache/mod.rs` is 1.
    - `cargo test --lib data::message_cache::bodies::` passes with 50 tests, quoted; the target passes with its count quoted.
    - The summary quotes the pass's milliseconds over 2,000 bodies with the command and the date.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The save and the pass derive the same first relevant words from one function, and every stored snippet is put right once on the next open.</done>
</task>

<task type="auto">
  <name>Task 3: the pages say what a row says now</name>
  <files>docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md:184-191`, the paragraph on what a row reads as, and `:192-220`, the columns.
    - `docs/changelog.md`, the `[Unreleased]` section's shape.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/USER_GUIDE.md`: after the paragraph at
    `:184-191`, one paragraph on the Snippet column: it holds the
    message's first relevant words, chosen by reading the text, so a link
    is left out rather than spelled, the opening lines marketing mail
    puts above its words are skipped, a greeting on its own line is
    skipped, and a reply's quoted lines and the sender's signature are
    left out; the whole message is in the message itself; dated. The
    changelog entry under `[Unreleased]`, Changed: the tester's words,
    the decision, the rules in one sentence each, that every stored
    snippet is recomputed once on the next open with the figure from
    task 2, and Known limitations: the boilerplate list is the openers
    seen so far and grows by evidence, and nobody has heard the rows yet.

    Ledger, by hand, both halves: one `unrun-verify` entry for the rows
    heard under NVDA saying the message rather than an address.

    Invoke `cognitive-accessibility` for the guide's paragraph: one idea
    per sentence, the word "snippet" defined where it is first used.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - The guide and the changelog carry the dated sentences; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words.
    - The ledger entry in both halves.
  </acceptance_criteria>
  <done>A person reading the guide knows what the Snippet column holds and why an address is not in it, and the changelog says what changed and what it cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a message body to a row | the body is a stranger's text; the rules read it and the row is spoken on every arrow press |
| a stored snippet to the pass | the pass rewrites a column on every message with a body, once, on open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-62 | Denial of service | a body crafted so no line survives and every rule runs over every line | low | mitigate | the rules are linear over the lines and stop at the limit; the least bad line answers in one pass; no regex, no backtracking |
| T-11-63 | Denial of service | the widened pass on a large cache at open | medium | mitigate | once, under a marker written after it finishes, not fatal; the cost measured over 2,000 bodies and quoted; the log line says the count and the milliseconds |
| T-11-64 | Information disclosure | an address a sender hid in link words surfacing in the row | low | accept | the rule drops addresses from the words; what survives is the sender's own text, which the row already spoke |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Two guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: a row saying the message rather than an address; what the cases prove: every rule one at a time, the derivation shared by the save and the pass, the pass once over every stored body.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 82 --comment "Fixed in <merge commit>, the way you decided on the issue rather than the way it was written: the snippet is the message's first relevant words, chosen by reading the text. Addresses are dropped altogether, lines that are only an address or a marker are skipped, the openers marketing mail puts above its words are skipped when they are recognisable, a bare greeting is skipped when something follows it, quoted lines and the signature after the delimiter are left out, and the first sentences with words in them are taken up to the limit, ending at a sentence boundary where one falls inside it. Each rule has its own test. A message that has nothing but those gives its least bad line. Every stored snippet is recomputed once on the next open, which took <n> ms over 2,000 bodies here. What only your ear settles: that the rows now say the message."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 20 closes structurally: the rules, one function, the pass once, the least bad line.
- LIST-17's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-09.2-SUMMARY.md` when done
</output>
