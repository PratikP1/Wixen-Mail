---
phase: 11-reading-and-the-list
plan: "11.0"
type: execute
wave: 23
depends_on: [11-11]
files_modified:
  - src/application/hidden_text.rs
  - src/application/mod.rs
  - src/presentation/html_renderer.rs
  - src/application/long_text.rs
  - tests/fixtures/issue_90_substack_newsletter.html
  - tests/what_the_sender_hid_is_not_read.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/privacy.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-25]
estimate:
  tokens: 52000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Text the sender hid is not read: before the sanitiser runs, an element hidden by the sender's own rule (display:none, visibility:hidden, font-size:0, max-height:0 with overflow:hidden, mso-hide:all, aria-hidden=true) is dropped, and a run of invisible filler characters is stripped from the text; the drop is by the sender's hiding and never by content; a count is said once at the top only when a dropped block held words that were not a preheader, in the shape of the held-back pictures sentence (LIST-25, #90)."
    - "A layout table is not a table to the reader: role=presentation survives the sanitiser on table, tr, td and th, so NVDA announces no table, row or column around a block laid out in one; the fixture's forty-eight hold it."
    - "A sender's grouping is announced only where its name is one a reader would use: aria-label is kept where it is the accessible name of a link or button with no text of its own, or of a table that is not presentational, and dropped on layout elements with the role that made them a region, by one rule and not a list."
    - "Nothing of ours is said twice: the subject once as the page's heading, the sender once per message in its heading with the number only in a conversation, the count line only for a conversation, the landmark and section names short; the plain part is never touched."
    - "The reader's own structure (long_text) reads the same fixture: a layout table's blocks are read as blocks with a space between them rather than one run (ledger 555), and a presentational table is not a Table piece."
  artifacts:
    - "src/application/hidden_text.rs: whether_hidden(tag, style, aria_hidden) -> Hidden, is_filler(char), strip_filler(text), what_a_dropped_block_was(text, position) -> Preheader | Words, the count sentence; a case per rule on hand-built shapes and on the fixture's."
    - "src/presentation/html_renderer.rs: drop_what_the_sender_hid before cleaner().clean on the reading path; role=presentation allowed on the four tags; the aria-label rule in the attribute filter; the page's own markup said once; the count line beside the held-back one."
    - "src/application/long_text.rs: a cell's blocks separated; a presentational table read as its blocks."
    - "tests/fixtures/issue_90_substack_newsletter.html: the tester's Substack message with the tracking addresses and the recipient's token replaced, Pratik's leave of 2026-09-19 recorded at its head in a comment."
    - "tests/what_the_sender_hid_is_not_read.rs: the fixture through wrap_body and render_thread_under_a_bar, the counts before and after, the plain part untouched, and long_text over the same fixture."
    - "docs/USER_GUIDE.md, docs/privacy.md, docs/changelog.md."
  key_links:
    - "MessageBody::Html -> sanitize_and_count_held_back -> hidden_text::drop_what_the_sender_hid (scraper parse, detach by whether_hidden, strip_filler on text nodes, serialise) -> cleaner().clean with role=presentation and the aria-label rule -> hold_back_what_would_be_fetched -> the page."
    - "the dropped blocks -> what_a_dropped_block_was per block -> the count of Words blocks -> one sentence at the top beside the pictures' -> said once."
    - "long_text::from_markup over the same body -> a presentational table's cells as paragraphs with a separator -> Piece::Paragraph per block, no Piece::Table."
---

<objective>
An HTML message read in the formatted view says what the sender showed,
once, and announces no table around a layout.

Purpose: #90, the tester on 2026-09-19 under NVDA on `39537d13`: the
formatted view is verbose, groupings are announced and phrases repeat.
Pratik measured the message he named, a Substack newsletter, on its raw
markup: two `display:none` divs at the top hold the post's subtitle and
two hundred invisible filler characters, Substack's preheader padding,
and `ammonia` strips `style`, so both become visible text, the subtitle
read twice and the filler read as symbols; forty-eight of its forty-nine
layout tables carry `role="presentation"` and the sanitiser drops
`role`, so NVDA announces a table with rows and columns around each
block, 111 cells' worth; a region named "Post header" is announced as a
grouping. 11-09.2's reading of the same message found the reader runs a
layout table's blocks together (ledger 555). And the page's own markup
names the subject and the sender in places the window and the row
already do.

Output: the hiding rules as a module with a case each, the drop before
the sanitiser with its count, `role="presentation"` kept, the region
rule, the page's own markup said once, the reader's table walk, the
fixture with its addresses replaced, the readings over the fixture, the
pages and the changelog. Closes #90 on the tree side; whether the
newsletter now reads once is the tester's ear. After 11-11, which
changes the same renderer for pictures, and before 11-11.1, whose
listener catches a link in the page this plan cleans.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-11-PLAN.md
@.planning/phases/11-reading-and-the-list/11-11-SUMMARY.md
@.planning/phases/11-reading-and-the-list/11-09.2-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-19 against `main` at `ced898eb`, read-only while 11-10
executed on its branch, and read again against `f92871bb` after it
merged: `html_renderer.rs`, `long_text.rs`, `pictures.rs` and
`Cargo.toml` are the same bytes at both heads, and 11-10's own changes
(the listing guard's set gaining `tags`, `virtual_rows::text_for`
prefixing a phrase, the labels select over `here`) touch nothing this
plan reads. Before 11-10.1 and 11-11 land; 11-10.1 routes
`wrap_body`'s plain branch through `links_in_text` and 11-11 rewrites
the renderer's shown arm for pictures and the privacy page's pictures
section, so every `html_renderer.rs` line below is re-run on the day
and 11-11's summary's names win. The fixture was measured from the
scratchpad's copy of the stored body, `issue-90-marcus.html`, 101,756
bytes, with Python, read-only. Command first, then what it said.

**1. The fixture's shapes, counted.**

```
python: re.findall on the file   -> display:none 14 (12 in the <style> block's media rules, 2 on the two preview divs at bytes 26301 and 26480); visibility:hidden 0; font-size:0 6 (all on spacer <td>s with no text); max-height:0 2 (the same two divs); mso-hide 0; aria-hidden 0
python: the two divs   -> <div class="preview" style="display:none;font-size:1px;color:#333333;line-height:1px;max-height:0px;max-width:0px;opacity:0;overflow:hidden;">: the first holds "Actions speak louder than words" then the padding, the second the padding alone; the padding is "&#847; &nbsp; &#8199; &#173;" repeated, U+034F, no-break space, figure space and soft hyphen, 200 joiners in all
python: tables   -> 49 <table>, 111 <td>, 0 <th>; role="presentation" 48; the roles in the body: presentation, region, button; one aria-label, "Post header", on a <div role="region">; role="button" on the subscribe <a>
python: pictures   -> 21 <img>, 6 with alt; two 1x1 beacons (eotrx.substackcdn.com/o/... and email.mg2.substack.com/o/...), which 11-11's beacon rule handles; headings: one <h1>, one <h3>
python: addresses   -> hosts substack.com, substackcdn.com, eotrx.substackcdn.com, email.mg2.substack.com, open.substack.com, substack-post-media.s3.amazonaws.com; 15 substack.com/redirect/ links carrying one recipient token (j=eyJ1IjoiNDFicmYifQ...) and per-link tokens; the two beacons carry a token each; no mail address in the body
```

So the two hidden blocks are one shape, and the third `display:none`
form the issue names (`font-size:0` on a block with words) is not in
this message; the rules are still written for every shape the issue
lists, each held by a hand-built case, and the fixture holds the two.

**2. What the sanitiser does today with style, role and aria-label.**

```
git show main:src/presentation/html_renderer.rs | sed -n 142,168p   -> cleaner(): ammonia's defaults with data and cid schemes; the attribute filter reads src, href and background only and keeps every other allowed attribute as it is
grep -n 'tag_attribute_values\|generic_attributes' ~/.cargo/registry/src/*/ammonia-4.1.4/src/lib.rs   -> 847, 864, 1188, 1201: allowed values per attribute per tag, and attributes allowed on every tag
git show main:src/presentation/html_renderer.rs | sed -n 393,415p   -> sanitize_html: cleaner().clean(html); sanitize_and_count_held_back: the clean, then hold_back_what_would_be_fetched over the cleaned string
git show main:src/presentation/html_renderer.rs | sed -n 462,486p   -> the picture pass is a regex over <img> tags on the cleaned markup, not a parse
```

`ammonia` 4.1.4 drops `style`, `class`, `role` and `aria-*` by
default. `role="presentation"` is one value on four tags through
`add_tag_attribute_values`; `aria-label` is a generic attribute the
filter can decide per element. The hidden-block drop cannot be done in
the filter, which sees attributes and not elements, so it is a pass
before the clean over a parsed document.

**3. The parse the tree already has.**

```
git show main:Cargo.toml | grep -n '^scraper\|^ego-tree\|^ammonia'   -> scraper 0.27.0, ego-tree 0.11.0, ammonia 4.0
git show main:src/application/long_text.rs | sed -n 560,566p   -> from_markup parses with scraper::Html::parse_fragment after its own clean
```

`scraper` is the parser the reader already uses; the drop parses the
body once with it, detaches what `whether_hidden` names, strips filler
in text nodes, and serialises; the cost on the 101 KB fixture is
measured and quoted, since the reading path now parses the body twice
(here and, for pictures, by regex).

**4. Ours: what the page says that the window and the row say.**

```
git show main:src/presentation/html_renderer.rs | sed -n 757,830p   -> render_thread_under_a_bar: the security section, <h1>{subject}, "N messages in this conversation" only when parts.len() > 1, then per part "<h{level}>{position}. {role} from {sender}</h{level}>" and a <p> with the date
git show main:src/presentation/html_renderer.rs | sed -n 700,730p   -> wrap_prepared: <main> around the content, no <title> of its own; the subject in the window title is the frame's
```

For a single message the page says "1. Message from <sender>" with a
number that counts nothing. The heading stays, since a page needs one
and the row is another control; the number goes when there is one
message; the subject stays as the `<h1>`, said once on the page; the
sender's own From line in the body is the sender's.

**5. The reader's structure over the same fixture.**

```
git show main:.planning/WINDOWS.md | grep -n '^| 555 '   -> the reader gives a layout table's cell as one run with no space between the blocks in it; every block's last word runs into the next block's first; "a cell's blocks want a space or a line between them, in the block walk in long_text.rs"; 18 records name the file
git show main:src/application/long_text.rs | sed -n 127,180p   -> OpenTable: cells and columns; a cell's content pushed as one string
```

**6. The gate for each file.** `src/application/hidden_text.rs` is new,
no records; `src/presentation/html_renderer.rs` 83 tests, 6 records, as
11-10.1 and 11-11 leave them, cases added for the cleaner's new
attributes and the count check named; `src/application/long_text.rs` 60
tests, 18 records, the cell walk changed and no case added there, the
behaviour held by the target over `pieces_of_markup`;
`src/application/pictures.rs` 44 tests, 4 records, not changed;
`tests/fixtures/` is new and cargo builds nothing under it; the new
target to itself. 989 records in all by the parser at `f92871bb`, the
per-file counts the same at `ced898eb`. Counts taken 2026-09-19.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the hiding rules as a module with a case each, the fixture with its addresses replaced, and the drop before the sanitiser with its count</name>
  <files>src/application/hidden_text.rs, src/application/mod.rs, src/presentation/html_renderer.rs, tests/fixtures/issue_90_substack_newsletter.html, tests/what_the_sender_hid_is_not_read.rs, guards/guards.toml</files>
  <read_first>
    - The stored body saved read-only by Pratik, at `C:/Users/prati/Documents/projects/wixen-mail-sweep/probe_90/issue-90-marcus.html` (untracked, beside the other probes; the planning session's scratchpad held the original), bytes 26,200 to 27,000 and the first `<table>`.
    - `src/presentation/html_renderer.rs:142-168`, `:393-415`, `:462-486`, `:554-568`, `:757-830` as 11-11 left them; `src/application/pictures.rs:620-632` (`what_was_held_back`, the sentence's shape).
    - `src/application/long_text.rs:555-570`, how a body is parsed with `scraper`.
    - `~/.cargo/registry/src/*/ammonia-4.1.4/src/lib.rs:840-880` and `:1180-1210`.
  </read_first>
  <behavior>
    - The fixture: the saved body copied to `tests/fixtures/issue_90_substack_newsletter.html` with every `substack.com/redirect/...` address replaced by `https://example.com/redirect/N`, the two beacon addresses by `https://example.com/open/N.gif`, the `email.mg2.substack.com/o/...` and `open.substack.com` addresses the same way, the recipient token nowhere in the file (`grep -c 'eyJ1IjoiNDFicmYifQ' tests/fixtures/issue_90_substack_newsletter.html` is 0), the picture addresses on `substackcdn.com/image/...` kept since they name public pictures and no person, and an HTML comment at the head saying whose message it is, the date, and Pratik's leave of 2026-09-19 to keep it in a public repository; a case that the file holds the two preview divs, 49 tables, 48 `role="presentation"` and 200 U+034F, so the fixture cannot drift from what the plan measured.
    - `hidden_text::whether_hidden(tag: &str, style: Option<&str>, aria_hidden: Option<&str>) -> Hidden { Shown, ByStyle(Rule), ByAria }` over the style's declarations split on `;` and trimmed, case-insensitively, `!important` ignored: `display:none`, `visibility:hidden`, `font-size:0` (`0`, `0px`, `0pt`, `0em`), `max-height:0` only together with `overflow:hidden`, `mso-hide:all`, and `aria-hidden="true"`; `opacity:0` alone is not a rule (a fade-in is not hiding) and is said in the doc; a case per rule and a companion per rule that must not fire (`display:block`, `font-size:0.9em`, `max-height:0` without `overflow`, `aria-hidden="false"`).
    - `hidden_text::is_filler(c: char)`: U+034F, U+200B to U+200D, U+2060, U+FEFF, U+00AD; `strip_filler(text) -> Cow<str>` removes them wherever they stand; a text node that is only filler and whitespace is dropped whole; a case for the fixture's padding run and one for a soft hyphen inside a word (removed, the word intact).
    - `hidden_text::what_a_dropped_block_was(text_after_stripping: &str, before_any_visible_text: bool) -> Preheader | Words | Nothing`: `Nothing` when no letter or digit remains; `Preheader` when it stood before any visible text and holds at most 200 characters; `Words` otherwise; `what_was_left_out(words_blocks: usize) -> String`, empty for 0, "1 block the sender did not show was left out." and "{n} blocks the sender did not show were left out." in the pictures sentence's register.
    - `html_renderer::drop_what_the_sender_hid(html) -> (String, usize)`: parses with `scraper::Html::parse_fragment`, walks the tree, detaches every element `whether_hidden` names with its subtree (`<style>` and `<script>` untouched, the cleaner drops them), strips filler in every text node, counts `Words` blocks, serialises; called by `sanitize_and_count_held_back` and `sanitize_html` on the reading path before `cleaner().clean`, never on the sending path (`WhoseMessage::BeingWrittenHere`) and never on a plain part; the count line rendered beside the pictures' through the same `<p class="held-back-count">` shape, once.
    - The target: the fixture through `wrap_body` holds "Actions speak louder than words" once, no U+034F, no `&#847;`, and no `class="preview"` text; a hand-built body with a `display:none` block of words below visible text says "1 block the sender did not show was left out." once and a preheader-only body says nothing; a `font-size:0` cell with words is dropped and counted, an empty spacer cell dropped and not counted; the plain part of a `Multipart` body is byte-identical through `wrap_body`; the drop's cost on the fixture measured (ten runs, the median in milliseconds) and quoted.
  </behavior>
  <action>
    Red first, on a branch: the module's cases bare, the target's cases
    bare, the `html_renderer.rs` cases by module path with the count
    check named for its six records. Then the fixture, the module, the
    pass, the count line, with `cargo build` between. Invoke
    `elegant-code`: the rules are one `match` over a declaration's
    property and value, the walk is one function that detaches and one
    that strips, and no regex is added for hiding.

    The fixture's replacement is done by hand in an editor, not by a
    script over the file, then read back with the case above; the
    commit message says whose message it is and cites Pratik's leave.

    Guard records: one on `hidden_text.rs` whose break makes
    `opacity:0` a rule, its companion red; one whose break drops
    `max-height:0` without `overflow:hidden`, its companion red; one on
    `html_renderer.rs` whose break runs the drop on the sending path,
    the target's plain-part case red; each measured; the six records
    naming `html_renderer.rs` re-measured when the count check names
    them, in the background.
  </action>
  <verify>
    <automated>cargo test --lib application::hidden_text:: && cargo test --test what_the_sender_hid_is_not_read && cargo test --lib presentation::html_renderer::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the count check.
    - `grep -c 'eyJ1IjoiNDFicmYifQ' tests/fixtures/issue_90_substack_newsletter.html` is 0; `grep -c 'substack.com/redirect' tests/fixtures/issue_90_substack_newsletter.html` is 0; `grep -c 'role="presentation"' tests/fixtures/issue_90_substack_newsletter.html` is 48; the file's first line is an HTML comment naming the leave.
    - `grep -c 'fn whether_hidden\|fn strip_filler\|fn what_a_dropped_block_was\|fn what_was_left_out' src/application/hidden_text.rs` is 4; `grep -c 'drop_what_the_sender_hid' src/presentation/html_renderer.rs` is at least 2.
    - `cargo test --lib application::hidden_text::` passes with at least sixteen cases, quoted; the target with its count quoted; `presentation::html_renderer::` with its count quoted; the drop's median milliseconds on the fixture quoted with the date.
    - Three guard records measured and quoted.
  </acceptance_criteria>
  <done>What the sender hid is dropped before the sanitiser by the sender's own rule, invisible filler is gone, a count is said once only when words were left out, the plain part is untouched, and the fixture is in the tree with no address that names a person.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a layout table is not a table, a grouping is announced only where its name is one a reader would use, and nothing of ours is said twice</name>
  <files>src/presentation/html_renderer.rs, tests/what_the_sender_hid_is_not_read.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/html_renderer.rs:142-168` (the cleaner) and `:757-830` (the page's own markup), and its tests at `:1700-1760` and `:1940-2045` for the page's shape.
    - `~/.cargo/registry/src/*/ammonia-4.1.4/src/lib.rs:847-880` (`tag_attribute_values`, `add_tag_attribute_values`) and `:1188-1210` (`generic_attributes`).
  </read_first>
  <behavior>
    - The cleaner allows `role` with the one value `presentation` on `table`, `tr`, `td` and `th` through `add_tag_attribute_values`, and no other value on any tag; the fixture through `wrap_body` holds 48 `role="presentation"` and no other `role`.
    - `aria-label` is a generic attribute the filter decides: kept on `a` and `button`, since a link whose only content is a picture has no other name; kept on `table` when the table does not carry `role="presentation"`; dropped everywhere else, so the fixture's "Post header" region, whose `role` is dropped with every other role, carries no label either; a case for each of the four outcomes, and the rule stated in the filter's comment as the rule and not as a list of senders.
    - The page's own markup: for one message the heading is "Message from <sender>" with no number, for a conversation "N. Message from" as today; the count line only for a conversation, as today, held now by a case for one message; the `<main>` landmark and the security section keep their names; a case that renders one message and asserts the subject appears once in the body's text and the sender once outside the sanitised body.
    - The target: the fixture through `render_thread_under_a_bar` as one message holds no "1. Message from", holds "Message from" once, the subject once as the `<h1>`, `role="presentation"` 48 times, `aria-label` 0 times in the body and once on the security section when a bar is given; a hand-built table with `aria-label` and no `role` keeps it, a link with `aria-label` and a picture inside keeps it.
  </behavior>
  <action>
    Red first, on a branch: the target's cases bare, the
    `html_renderer.rs` cases by module path with the count check named,
    the page-shape tests at `:1940-2045` rewritten in place where the
    numbering moved. Then the two attributes, the rule, the heading,
    with `cargo build` between.

    Guard records: one on `html_renderer.rs` whose break allows every
    `role` value, the target's "no other role" reading red; one whose
    break keeps `aria-label` on a `div`, the region case red; each
    measured.
  </action>
  <verify>
    <automated>cargo test --test what_the_sender_hid_is_not_read && cargo test --lib presentation::html_renderer::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the count check.
    - `grep -c 'add_tag_attribute_values' src/presentation/html_renderer.rs` is at least 1; `grep -c '"presentation"' src/presentation/html_renderer.rs` is at least 1; `grep -v '^\s*//' src/presentation/html_renderer.rs | grep -c 'aria-label'` is at least 2 (the rule and the security section).
    - The target passes with its count quoted; `presentation::html_renderer::` with its count quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>NVDA meets no table around a block laid out in one, hears a sender's grouping only where it is a name, and hears the subject and the sender once from the page's own markup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the reader's structure over the same fixture, the pages, and the close</name>
  <files>src/application/long_text.rs, tests/what_the_sender_hid_is_not_read.rs, guards/guards.toml, docs/USER_GUIDE.md, docs/privacy.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `src/application/long_text.rs:127-190` (`OpenTable`, the cell walk) and `:555-620` (`read_markup`), as 11-09.2 and 11-10.1 left them.
    - `.planning/WINDOWS.md`, ledger 555, both halves.
    - `docs/USER_GUIDE.md`, the reading section; `docs/privacy.md`, the section on what a message can carry (`grep -n -i 'sender\|hidden\|tracking' docs/privacy.md | head`).
  </read_first>
  <behavior>
    - `long_text`'s block walk gives a cell's blocks a space between them (a newline for block elements), so the fixture's cell reads "... for more Top three ways ... seven days Actions speak louder than words Gary Marcus Sep 19 ..." with the words apart; and a table carrying `role="presentation"` is read as its blocks in order, `Piece::Paragraph` per block, never as a `Piece::Table`, since a layout table has no columns to name; a data table without the role is a `Table` piece as today.
    - The target: `pieces_of_markup` over the fixture holds no `Piece::Table`, its paragraphs in order with no two words run together (a case asserting the specific joins ledger 555 quotes are now separated), and the hidden preheader's padding absent because the reader's own clean runs the same drop (the reader calls `hidden_text::drop_what_the_sender_hid` before its parse, so a snippet and a spoken description never see hidden text either); a hand-built data table with `<th>` still a `Table` piece with its columns.
    - Ledger 555 closed by this commit in both halves.
    - `docs/USER_GUIDE.md`: the reading section says what the formatted view leaves out (what the sender hid, the padding senders put before a preview line) and that a line at the top says so when the sender hid words; `docs/privacy.md`: one sentence that hidden text is dropped by the sender's own hiding and never by what it says, and that nothing is sent anywhere by it; the changelog entry under `[Unreleased]`, Fixed: the tester's words, the three causes measured on the message he named, what is dropped and by what rule, the layout tables, the groupings, the page's own markup, the reader's cell walk, Known limitations: a sender who hides one copy of a block for mobile and shows another by a stylesheet media rule now shows the copy the stylesheet hid, since the stylesheet is dropped; nobody has heard the newsletter read once.
  </behavior>
  <action>
    Red first, on a branch: the target's cases bare; no case added in
    `long_text.rs`'s module (18 records), said. Then the cell walk and
    the presentational table, with `cargo build` between. Invoke
    `writing-craft` for the three pages.

    Ledger, by hand, both halves: 555 closed by this commit, and one
    `unrun-verify` entry for the tester: the Substack message read once,
    no table announced, no grouping announced, the subtitle heard where
    it stands and not at the top.

    Guard records: one on `long_text.rs` whose break returns a
    presentational table to a `Table` piece, the target's reading red;
    measured; the eighteen records naming `long_text.rs` re-measured
    only if the count check names them, which a change with no test
    added should not.

    After the merge, the executor closes the issue from the merge
    commit: `gh issue close 90 --comment "Fixed in <merge commit>. On the
    message you named: the two blocks Substack hid at the top, the
    subtitle and two hundred invisible padding characters, are dropped
    before the page is built, by the sender's own hiding rule and never
    by what they say; a line at the top says so only when a hidden block
    held words that were not a preview line. The forty-eight layout
    tables keep role=presentation, so NVDA announces no table, row or
    column around them; the Post header grouping carries no name, since
    a sender's label is kept only where it is a link's or a data table's
    name. The page's own heading no longer numbers a single message. The
    reader's own structure, which snippets and descriptions use, reads
    the same message with its blocks apart. What only your ear settles:
    the newsletter read once, with no table and no grouping announced."`
    Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test what_the_sender_hid_is_not_read && cargo test --lib application::long_text:: && cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases.
    - `cargo test --lib application::long_text::` passes with 60 tests, quoted; the target passes with its count quoted.
    - The three pages carry the dated sentences; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words; ledger 555 closed in both halves and the new entry in both.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>The reader's own structure reads the fixture with its blocks apart and no table where there is only layout, the pages say what the formatted view leaves out and why, and the issue is closed with the ear list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a sender's hiding to the reader | a rule that drops text is a rule that can drop text the sender meant to be read |
| the fixture to the public repository | the tester's mail, with addresses that name him |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-103 | Denial of service | a hidden block that carried words a reader needed | high | mitigate | the drop is only by the sender's own hiding rule, never by content; a dropped block with words is counted and said once at the top; the plain part is never touched; a companion per rule holds what must not fire |
| T-11-104 | Information disclosure | the tester's addresses in a public fixture | high | mitigate | every tracking address and the recipient token replaced by hand before the commit; a case holds the token's absence; the picture addresses kept name public pictures and no person; Pratik's leave recorded at the file's head |
| T-11-105 | Tampering | `role="presentation"` allowed on a data table, hiding its columns from the reader | medium | accept | the value is the sender's own claim, as it is in a browser; a sender who marks a data table presentational hides it from every screen reader already, and the reader's structure still reads the cells as blocks |
| T-11-106 | Spoofing | a sender's `aria-label` naming a link something its text does not say | low | mitigate | kept only on a link or button, where a browser keeps it too; the words are still the link's text; 11-11.1's route reads the href, not the label |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; `scraper` and `ammonia` are already here; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Six guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the newsletter read once with no table or grouping announced; what the cases prove: each hiding rule and its companion, the filler, the count, the plain part, the roles, the labels, the page's own markup, the reader's walk, the fixture's shapes and its addresses' absence.
- After the merge, the executor closes #90 with the comment in task 3.
</verification>

<success_criteria>
- Roadmap criterion 29 closes structurally: the rules, the drop with its count, the filler, `role="presentation"` kept, the region rule, the page's own markup said once, the plain part untouched, the reader's walk, the fixture.
- LIST-25's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-11.0-SUMMARY.md` when done
</output>
