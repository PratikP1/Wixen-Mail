---
phase: 11-reading-and-the-list
plan: "11.1.1"
type: execute
wave: 25
depends_on: [11-11.1]
files_modified:
  - src/presentation/wx_app.rs
  - src/presentation/ui_types.rs
  - src/presentation/wx_settings.rs
  - src/application/reading_habits.rs
  - tests/a_setting_saved_applies_without_a_restart.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-26]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Mark as read after applies the moment Settings is saved: the main timer asks the rule with the setting the window's state holds, written at startup and by the Settings-saved arm, so a value changed while the program runs governs the next tick and no restart is needed (LIST-26, #91)."
    - "Every setting the main window captures once at startup is listed with its line and its disposition, and each either follows a save the way the working day does or says on its control on the settings screen that it takes effect at the next start; a reading holds the list to the tree so a new capture is caught."
    - "The date settings follow a save too: the rows, the cells and the read-aloud closures read them from the state, as the working day is read, and a date style chosen in Settings shows on the next paint."
    - "The changelog names the build and the shape of the regression, a setting changed in Settings that did not apply until the program was started again; the version does not move."
  artifacts:
    - "src/presentation/wx_app.rs: WxUIState::marks_read and WxUIState::dates, written at startup and by the arms for UIUpdate::MarkReadAfterChanged and UIUpdate::DateSettingsChanged; mark_what_was_read reading the state; the row, cell and read-aloud closures reading state for the dates; the startup block's captures reduced to what is startup by nature."
    - "src/presentation/ui_types.rs: the two updates, on WorkingDayChanged's pattern."
    - "src/presentation/wx_settings.rs: the sentence 'Takes effect the next time Wixen Mail starts' under the log level and the default sort order, named on both channels."
    - "tests/a_setting_saved_applies_without_a_restart.rs: the readings that the timer reads the state, that the arm writes it, that no startup binding captures either setting, and the audit list held to the tree."
    - "docs/USER_GUIDE.md, docs/changelog.md."
  key_links:
    - "SettingsResult::Updated -> MarkRead::from_setting(&new_config.mark_read_after) -> UIUpdate::MarkReadAfterChanged -> the arm writes state.marks_read -> the main timer's tick -> mark_what_was_read(app) reads s.marks_read -> whether_to_mark_read."
    - "SettingsResult::Updated -> date_settings_from(&new_config) -> UIUpdate::DateSettingsChanged -> state.dates -> the virtual row callback, the PIM cells and the read-aloud closures read s.dates on every call -> the list refreshed once."
    - "the audit reading -> build_main_window's stored_config block -> every `let <name> = stored_config` and `lock_state(&state).<field> = stored_config` binding -> the allowlist with a disposition each -> a new binding fails the reading."
---

<objective>
A setting saved in Settings applies without a restart, and every setting
that cannot says so where it is set.

Purpose: #91, the tester on 2026-09-20 on `4a09bfc2`, confirmed after a
restart: "Mark as read after" did nothing however he set it, because the
main timer asks the rule with a value read once at startup
(`wx_app.rs:1294-1299` at `390a580c`, `let marks_read = stored_config...`,
captured by the timer's closure and handed to `mark_what_was_read` at
`:6034`), and
saving Settings changes the stored file and nothing the timer holds.
With the setting at 10 seconds from startup, Enter, Space and
Shift+Space all mark, so there is one fault and no second. The same
shape sits on the date settings, captured at `:1255` into the paint and
read-aloud closures while other sites read them afresh, and the audit
below lists every capture in the startup block with what each does
today.

Output: the two settings in the state with their updates on the pattern
the working day already follows, the timer and the closures reading
state, the audit as a reading held to the tree, the sentence on the two
controls that are next-start by nature, the guide, the changelog. Closes
#91 on the tree side. A small insert after 11-11.1 and before 11-11.1.2
(#92's first half, which reads its setting the way this plan's audit
allows); its plan number is written as the string `"11.1.1"` because the
file has to sort between 11-11.1 and 11-11.2.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-05-SUMMARY.md
@.planning/phases/11-reading-and-the-list/11-11.1-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `4a09bfc2`, read-only while 11-11.1
executed on its branch, and re-taken the same day against `main` at
`390a580c` with 11-11.1 merged (`8340e5e6`): every `wx_app.rs` line below
is the second reading, and the shapes are the same bytes as the first.
11-11.1 added `open_links_in` and reads it from the stored file at the
moment a link is followed (`where_links_open()`, `wx_app.rs:12777`, with
a comment saying so), so it is on use and not a capture and needs no row
in the audit; its summary says the same. Command first, then what it said.

**1. The fault, exactly.**

```
sed -n 1294,1299p src/presentation/wx_app.rs   -> let marks_read = stored_config.as_ref().map(|cfg| MarkRead::from_setting(&cfg.mark_read_after)).unwrap_or_default();
grep -n 'marks_read' src/presentation/wx_app.rs   -> 1294 (the binding), 6034 (mark_what_was_read(app, marks_read) on the main timer), 10824 and 10839 (the parameter and its use in the function at 10822): three sites, no other read
sed -n 18269,18335p src/presentation/wx_app.rs   -> SettingsResult::Updated: feedback, how much to say, the sound scheme applied on a11y; the working day, the default alert lead and the calendar view sent as UIUpdate::WorkingDayChanged (18318), DefaultEventAlertLeadChanged, CalendarViewChanged; "Settings saved" and the log line at 18328; the theme's palette returned; mark_read_after not named
sed -n 588,612p src/presentation/ui_types.rs   -> the three updates, with the doc saying a setting that needs the window to change goes this way
sed -n 19484,19490p src/presentation/wx_app.rs   -> the WorkingDayChanged arm writes state.working_day
```

The issue's account holds. Its line numbers (`:1291-1296`, `:6098`,
`:10894-10904`) were the 11-11.1 branch's at `5b99cd7c`; `main` moved
past them at the merge and the lines above are `main`'s. The pattern to
copy is the working day: a state field, an update sent by the arm, the
readers reading state.

**2. The audit, as found: every capture in the startup block, and
what each does today.**

```
sed -n 1255,1380p src/presentation/wx_app.rs   -> the block, from the comment "Read once rather than per row: the paint callback runs for every visible cell and must not touch configuration" to the last stored_config read at 1366; let callback_registered at 1402 ends it
```

| Line | Setting | Where it goes | Follows a save today |
|---|---|---|---|
| 1260 | `date_style`, `date_order`, `date_wording`, `clock_hours` as `date_settings` | captured by value into the virtual row callback (`:1419`), the PIM cells (`:1723-1736`) and the read-aloud closures (`:1811-1887`) | no; four other sites read `date_settings_from_stored_config()` (`:11991`) on use (`:2133`, `:17000`, `:17182`, `:19519`), so a date style changed in Settings shows in the calendar heading and not in the rows |
| 1268 | `working_day_starts`, `working_day_ends` | `state.working_day` | yes, `WorkingDayChanged` |
| 1280 | `default_reminder_minutes` | `state.default_event_alert_lead` | yes, `DefaultEventAlertLeadChanged` |
| 1288 | `calendar_view` | `state.calendar_showing` | yes, `CalendarViewChanged` |
| 1294 | `mark_read_after` as `marks_read` | captured by the timer's closure | no (#91) |
| 1306, 1321 | `message_columns`, `default_sort_order` as `starting_layout` | the `column_layout` cell the chooser owns | no: by design the saved layout carries its own sort and the setting applies only where no layout was saved, and then at the next start |
| 1338 | `feedback_channels` | `a11y.set_feedback_settings` | yes, the arm |
| 1356 | `announce_while_fetching` | `a11y.set_how_much_to_say` | yes, the arm |
| 1366 | `sound_scheme_id` | `a11y.set_sound_scheme` | yes, the arm |

Outside the block: `mute_message_reading` restored at `:940` is a menu
command's memory, offered by no settings control; `font_family` and
`font_size` through `the_chosen_list_font()` (`:1248`, `:1680`) are
re-applied to the six lists after a save (`:5597`, "changing either did
nothing at all until the next start" was fixed there); `theme` follows
through the palette; `keep_running_in_the_tray` is read at close and
re-checked after a save (`follow_the_tray_setting`, `:5589`, `:5815`);
`log_level` is `init_logging` once at start (`logging.rs:119`) with no
reload, so it is next-start by nature, and the arm logs the new level
without applying it (`:18328`); `start_in_all_inboxes` and
`check_default_programs_at_startup` are startup by their own words;
`open_links_in` is read at the route (`:12777`). Read on use, each at the
point it matters: `read_messages_as` (`:13675`, `open_single_message`),
`keep_selected_message_in_view` (`:18852`), `unread_on_a_parent`
(`:12365`, `:17395`), `a_conversation_reaches` (`:15113`, `:18520`),
`empty_reaches_subfolders` (`:9401`), `mark_read_reaches_subfolders`
(`:9675`), `deleting_a_conversation_row` (`:10627`), `read_receipts`
(`:21974`), `look_at_message_contents` (`:22239`, `:24034`),
`check_links_with_google` (`:23625`, `:23646`), `draft_autosave_minutes`
and `preview_before_send` (`:16354-16356`, compose open),
`undo_send_hold_seconds` (`:16819`), `language`, `check_spelling_as_you_type`
and `copy_lines` (`wx_compose.rs`, compose open), `hold_back_remote_pictures`,
`announce_decorative_pictures` and `undescribed_pictures_read_as`
(`html_renderer.rs`, every renderer), `smooth_scrolling`
(`html_renderer.rs`), `message_text_kept` (10-03, on use),
`which_updates`, `directories`, `allowed_changes`,
`keep_sent_mail_on_this_computer`, `check_spelling_before_send`,
`send_contact_changes_everywhere`, `download_folder`,
`add_signature_automatically` (each at its act). The four the issue
named to check: `announce_while_fetching` follows the arm,
`message_text_kept` and `read_messages_as` are read on use, and the
column settings are the layout's by design, said on the control by
task 2. So two captures do not follow a save and are not next-start by
nature: `mark_read_after` and the four date settings; one is by design
and is said nowhere: `default_sort_order`; one is next-start by nature
and is said nowhere: `log_level`.

**3. The gate for each file, at `390a580c`.** `src/presentation/wx_app.rs`
196 tests by `#[test]` and 199 by the runner (11-11.1's summary counts
both), 102 records, none added; `src/presentation/ui_types.rs` 79 tests,
6 records, the arm test rewritten in place for the two new updates;
`src/presentation/wx_settings.rs` 0 tests, 22 records, two sentences;
`src/application/reading_habits.rs` 25 tests, 1 record, a constant for
the next-start sentence's words; 11-05's target
`tests/moving_through_the_list_marks_nothing_read.rs` (13 tests, 2
records) reads `mark_what_was_read` and is rewritten in place where the
signature moves; the new target to itself. 1,011 records in all by the
parser. Counts taken 2026-09-20.

**4. What 11-11.1's summary teaches about the commits here.** Its
handlers went into task 1's green because the read-by-something guard in
`config.rs` and clippy together leave no green commit holding a field
and no reader. The same holds for a `WxUIState` field: `marks_read` and
its arm, its send and its reader in the timer are one green, and `dates`
with its closures another, each red commit naming the readings that
commit turns green and nothing more.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the timer reads the setting from the state, and the Settings-saved arm writes it</name>
  <files>src/presentation/wx_app.rs, src/presentation/ui_types.rs, tests/a_setting_saved_applies_without_a_restart.rs, tests/moving_through_the_list_marks_nothing_read.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:1255-1300`, `:6025-6040`, `:10820-10845`, `:18269-18335`, `:19484-19490` (all at `390a580c`; re-grep on the day), and `WxUIState` (`grep -n 'pub working_day' src/presentation/wx_app.rs`).
    - `src/presentation/ui_types.rs:585-615` and the arm test in `wx_app.rs` that names every quiet arm (`grep -n 'fn test_every_arm_that_shows_something' src/presentation/wx_app.rs`).
    - `tests/moving_through_the_list_marks_nothing_read.rs`, the readings' shape.
  </read_first>
  <behavior>
    - `WxUIState::marks_read: MarkRead`, written in the startup block from `stored_config` where `let marks_read` was, and by a new `UIUpdate::MarkReadAfterChanged(MarkRead)` arm, quiet on purpose; the arm in `SettingsResult::Updated` sends it beside `WorkingDayChanged`; `mark_what_was_read(app)` loses its parameter and reads `s.marks_read` under the lock it already takes; the timer's call passes nothing; `grep -c 'let marks_read' src/presentation/wx_app.rs` is 0.
    - The target's readings over `what_ships`: `mark_what_was_read`'s body reads `marks_read` from the state and takes no `MarkRead` parameter; the `SettingsResult::Updated` arm sends `MarkReadAfterChanged` built from `new_config.mark_read_after`; the `MarkReadAfterChanged` arm writes the state field; no `let marks_read` binding exists in the startup block; a companion for each over a hand-built window.
    - 11-05's target rewritten in place where it names the signature (`THE_TIMER` or its equivalent), count kept at 13.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare, the `ui_types`
    arm test by module path with the count check named for its six
    records if a case is added (rewrite in place first). Then the field,
    the update, the arm, the timer, with `cargo build` between. Invoke
    `elegant-code`: one field, one update, one arm, on the working day's
    pattern, no second way.

    Guard records: one on `wx_app.rs` whose break makes the
    `MarkReadAfterChanged` arm write nothing, the arm reading red; one
    whose break hands the timer a value captured at startup again, the
    "no binding" reading red; each measured.
  </action>
  <verify>
    <automated>cargo test --test a_setting_saved_applies_without_a_restart && cargo test --test moving_through_the_list_marks_nothing_read && cargo test --lib presentation::ui_types:: && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings.
    - `grep -c 'let marks_read' src/presentation/wx_app.rs` is 0; `grep -c 'MarkReadAfterChanged' src/presentation/ui_types.rs` is at least 1 and `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'UIUpdate::MarkReadAfterChanged'` is at least 2 (the send and the arm); `grep -c 'pub marks_read' src/presentation/wx_app.rs` is 1.
    - `cargo test --lib presentation::wx_app::` passes with 196, quoted; `presentation::ui_types::` with 79 or 80, quoted; 11-05's target with 13.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>A Mark as read after saved in Settings governs the next tick, and the readings hold the field, the update, the arm and the absence of the startup capture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the audit held to the tree, the date settings following a save, and the two next-start controls saying so</name>
  <files>src/presentation/wx_app.rs, src/presentation/ui_types.rs, src/presentation/wx_settings.rs, src/application/reading_habits.rs, tests/a_setting_saved_applies_without_a_restart.rs, guards/guards.toml</files>
  <read_first>
    - Premise 2's table, and `src/presentation/wx_app.rs:1252-1375` again with 11-11.1's summary beside it for `open_links_in`.
    - `src/presentation/wx_app.rs:1402-1425`, `:1715-1740`, `:1805-1890` (the closures holding `date_settings`), `:11974-12000` (`date_settings_from` and `date_settings_from_stored_config`), and the six lists' refresh after a save at `:5589-5610` (all at `390a580c`; re-grep on the day).
    - `src/presentation/wx_settings.rs`, the Reading tab's `markread_note` (11-05's sentence under Mark as read after, the shape for a sentence under a control) and the log level's row (`grep -n 'log_level' src/presentation/wx_settings.rs`).
    - `src/application/reading_habits.rs:131-136`, `WHAT_MARK_READ_COUNTS_FROM`, the shape for a sentence constant.
  </read_first>
  <behavior>
    - `WxUIState::dates: DateSettings`, written at startup from `date_settings_from` and by a new `UIUpdate::DateSettingsChanged(DateSettings)` arm the Settings-saved arm sends; the virtual row callback, the PIM cell closures and the read-aloud closures read `s.dates` under the lock they already take, and the `let date_settings` capture at `:1255` is gone; the `DateSettingsChanged` arm refreshes the six lists once, the way the font's save path does; `date_settings_from_stored_config`'s four callers are left as they are, since they read on use.
    - `reading_habits::TAKES_EFFECT_AT_THE_NEXT_START: &str`, "Takes effect the next time Wixen Mail starts.", under the log level's control and under the default sort order's control on the settings screen, each a `StaticText` named on both channels as `markread_note` is; the default sort order's sentence adds "and only in folders whose columns you have not arranged", since a saved layout carries its own sort.
    - The audit reading, in the target: it reads the startup block between `let stored_config = ` and `let callback_registered` from `what_ships`, collects every `let <name> = stored_config` and `lock_state(&state).<field> = stored_config` binding and every `stored_config` read into `a11y.set_*`, and holds the set equal to an allowlist written into the test, each entry with a disposition: `FollowsASave("WorkingDayChanged")`, `FollowsASave("DateSettingsChanged")`, `FollowsASave("MarkReadAfterChanged")`, `FollowsASave("the arm")` for the three `a11y` sets, `NextStartAndSaidSo("default_sort_order")` for the layout; a binding not in the list fails with the sentence "a setting captured at startup with no disposition: <name>; either send an update on save or say on its control that it takes effect at the next start"; a companion with a hand-built block holding a new `let` fails that way.
    - The settings readback target (`tests/the_settings_dialog_opens_in.rs` or the one that reads the Reading tab back, found by `grep -ln 'WHAT_MARK_READ_COUNTS_FROM' tests/*.rs`) gains the two sentences read back from the built page, rewritten in place where possible.
  </behavior>
  <action>
    Red first, on a branch: the audit reading bare (red today on
    `date_settings` and `marks_read` with no disposition, and on the
    layout with no sentence), the date readings bare, the two sentences
    read back. Then the field, the update, the closures, the sentences,
    with `cargo build` between. Invoke `elegant-code`: the closures take
    the lock they already take and read one more field; nothing is
    cloned per row.

    11-11.1's `open_links_in`: on its branch at `5b99cd7c`,
    `where_links_open()` (`wx_app.rs:12840-12846` there) reads it from
    the stored file at the moment a link is followed, with a comment
    saying so, and the startup block holds the same twelve
    `stored_config` sites shifted by two lines; so it is read on use and
    needs no row. Confirm against 11-11.1's summary on the day and say
    so in this plan's summary.

    Guard records: one on `wx_app.rs` whose break returns the date
    capture to the startup block, the audit red; one on `wx_settings.rs`
    whose break drops the log level's sentence, the readback red; each
    measured; the twenty-one records naming `wx_settings.rs` re-measured
    only if the count check names them, which a change with no test
    added should not.
  </action>
  <verify>
    <automated>cargo test --test a_setting_saved_applies_without_a_restart && cargo test --test the_settings_dialog_opens_in && cargo test --lib presentation::wx_app:: && cargo test --lib application::reading_habits::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings.
    - `grep -c 'let date_settings = stored_config' src/presentation/wx_app.rs` is 0; `grep -c 'pub dates' src/presentation/wx_app.rs` is 1; `grep -c 'DateSettingsChanged' src/presentation/ui_types.rs` is at least 1; `grep -c 'TAKES_EFFECT_AT_THE_NEXT_START' src/presentation/wx_settings.rs` is 2.
    - The summary holds the audit's table as the reading found it on the day, one row per binding with its disposition, and names 11-11.1's `open_links_in` row.
    - `cargo test --lib presentation::wx_app::` passes with 196, quoted; `application::reading_habits::` with 25.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>Every setting the window captures at startup either follows a save or says on its control that it takes effect at the next start, a new capture fails a reading, and a date style chosen in Settings shows on the next paint.</done>
</task>

<task type="auto">
  <name>Task 3: the guide, the changelog, and the close</name>
  <files>docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`, the Settings section and "When a message counts as read" as 11-05.1 left it.
    - `docs/changelog.md`, the `[Unreleased]` section's Fixed entries and 11-05's entry for the shape.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/USER_GUIDE.md`: the Settings section
    says a setting applies when Settings is saved, and names the two
    that take effect the next time the program starts (the log level,
    and the default sort order for folders whose columns have not been
    arranged); "When a message counts as read" gains one sentence that
    changing the delay applies at once. The changelog entry under
    `[Unreleased]`, Fixed: the build (`1.0.0-alpha.1` at `4a09bfc2`), the
    tester's words, the regression's shape in one sentence ("a setting
    changed in Settings did not apply until the program was started
    again"), which settings had it (Mark as read after, and the date
    settings in the list's rows), that both apply on save now, that
    every other setting was read against the code and either applies on
    save or says on its control that it waits for the next start, and
    that no version moves for it.

    Ledger, by hand, both halves: one `unrun-verify` entry for the
    tester: the delay changed in Settings and a message marked after the
    new wait without a restart.

    After the merge, the executor closes the issue from the merge
    commit: `gh issue close 91 --comment "Fixed in <merge commit>: Mark
    as read after now applies the moment Settings is saved; the timer
    reads the setting the window holds, and saving writes it there, the
    way the working day already worked. The date style in the list's
    rows had the same fault and follows a save now too. Every other
    setting was read against the code: each applies on save or, for the
    log level and the default sort order, says under its control that it
    takes effect the next time the program starts. A reading now fails
    if a setting is ever captured at startup again without one of those
    two. What only your ear settles: the delay changed in Settings and a
    message marked after the new wait, without a restart."` Closing an
    issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - The pages carry the dated sentences; `grep -c 'started again' docs/changelog.md` is at least 1; `grep -c '^version = "1.0.0-alpha.1"' Cargo.toml` is 1; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words; the ledger entry in both halves.
  </acceptance_criteria>
  <done>The pages say which settings wait for the next start and that the rest apply on save, the changelog names the regression and the build, and the issue is closed with the ear list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the stored file to the running window | a value saved by one arm and read by another must be the same value at the same moment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-107 | Repudiation | a setting shown saved and not applied, with nothing said | high | mitigate | the two captures now follow a save; the two next-start settings say so on their controls; the audit reading fails on a new capture with no disposition |
| T-11-108 | Denial of service | the paint callback reading configuration per cell | medium | mitigate | the closures read one more field under the lock they already take; no `load_stored` on the paint path; a record measures the break that reintroduces the capture, and the block's own comment keeps its reason |
| T-11-109 | Tampering | a save applied to the running window before the file was written | low | mitigate | the updates are sent after `mgr.save()` succeeds, as the working day's is; a failed save says so and applies nothing |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the delay changed and a message marked after the new wait without a restart; what the readings prove: the field, the update, the arm, the absence of the capture, the audit's list, the two sentences.
- After the merge, the executor closes #91 with the comment in task 3.
</verification>

<success_criteria>
- Roadmap criterion 30 closes structurally: the setting in the state and applied on save, the date settings the same, the audit held to the tree, the two controls saying next start, the changelog naming the build and the regression's shape, no version move.
- LIST-26's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-11.1.1-SUMMARY.md` when done
</output>
