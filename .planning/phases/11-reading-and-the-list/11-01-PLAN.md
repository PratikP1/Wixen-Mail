---
phase: 11-reading-and-the-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/which_language_row.rs
  - src/presentation/mod.rs
  - src/presentation/wx_settings.rs
  - guards/guards.toml
  - docs/changelog.md
autonomous: true
requirements: [FOUND-17]
estimate:
  tokens: 26000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A spelling language somebody chose is kept exactly as chosen by the Settings screen, whether or not this machine can check it: a stored en-AU is shown as en-AU and written back as en-AU on a machine that offers only en-US (FOUND-17; the Test Suite job of CI run 35336142985 on main at 744d05ef)."
    - "A bare family tag from a profile written before 2026-09-03 is still resolved to this machine's own region, which is #21's fix, and a tag nothing offers is still kept as stored; the integration reading that holds all three passes on the runner and on the tester's machine alike."
    - "CI on main is green again on the first push after this plan merges, which is Pratik's to make."
  artifacts:
    - "src/presentation/which_language_row.rs: the rule for which row the picker shows and what OK writes back, pure over the stored tag, the machine's tag and the offered rows, with a test per case."
    - "src/presentation/wx_settings.rs: language_rows_and_selection asking that rule."
    - "docs/changelog.md: the entry under Unreleased, Fixed, naming it a regression of 09-02 in 1.0.0-alpha.1."
  key_links:
    - "language_rows_and_selection -> which_language_row::which_row_shows -> read_settings: one list built once, so the row shown and the tag written are the same row."
    - "a tag with a region -> the row spelled as stored, available or not, else a row added at the end; a bare tag -> language_to_use's answer: the two rules the integration test's own comments state."
---

<objective>
Make the Settings screen keep a chosen spelling language exactly as chosen
when this machine cannot check it, so the one test that fails on GitHub's
runner passes there and CI on `main` is green again.

Purpose: this morning's push of `744d05ef` ran CI, and the Test Suite job
failed on one target: `tests/the_language_the_screen_shows_is_the_one_used`,
with "stored "en-AU": the screen would keep "en-US", the checker uses
"en-AU"". Every later merge of this phase would land on a red CI until this
is fixed, so it goes first. The same test passes on the tester's machine,
which is why nobody saw it here.

Output: the rule as a module with its tests, the screen asking it, a guard
record, the changelog entry. Closes nothing on the tracker: no issue was
filed for it, and the run is the record.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef`, working tree clean, nothing
unpushed, and against the run itself. Command first, then what it said.

**1. The run, read from the repository root.**

```
gh run view 35336142985 --json name,status,conclusion,headSha   -> CI completed failure 744d05ef
gh run view 35336142985 --json jobs   -> Test Suite failure (job 105571311096); Build (debug), Clippy, Build (release), Rustfmt, Security Audit, Setup Executable all success
gh run view 35336142985 --job 105571311096 --log, saved and grepped   -> line 10868: thread 'test_the_language_the_screen_shows_is_the_one_the_checker_uses' panicked at tests\the_language_the_screen_shows_is_the_one_used.rs:101:5: the settings screen shows a spelling language other than the one the checker uses: stored "en-AU": the screen would keep "en-US", the checker uses "en-AU"; line 10877: test result: FAILED. 0 passed; 1 failed
grep -c 'test result: ok' the saved log   -> 75: every other target passed, because ci.yml runs cargo test --all-targets --no-fail-fast (ci.yml:74)
```

One target, one test, one case of its three: the bare "en" case and the
"zz-ZZ" case passed on the runner.

**2. The same test passes here, so it cannot be the red half of a red
commit on this machine.**

```
cargo test --test the_language_the_screen_shows_is_the_one_used   -> test result: ok. 1 passed; 0 failed (2026-09-18, this machine, exit 0)
```

`scripts/red-commit.sh` requires every test a red commit names to fail
here. This one does not, because this machine offers en-AU and the runner
does not, so the red half of this plan is a unit test over a hand-built row
list in a module of its own, and the integration reading stays as it is,
green here and green on the runner once the rule is right. The commit
message says so.

**3. Where the screen chooses its row, and why en-US wins today.**

```
grep -n 'fn language_rows_and_selection' src/presentation/wx_settings.rs   -> 884
sed -n 884,906p src/presentation/wx_settings.rs   -> selected = language_to_use(stored, system_language(), &rows).and_then(row_of).or_else(|| row_of(stored)); None -> the stored tag is pushed as a row at the end and selected
sed -n 354,380p src/service/spellcheck/mod.rs   -> language_to_use: an offered and available exact match is used as stored; otherwise, if the machine's own tag shares the family and is usable, the machine's region; otherwise the first available in the family
grep -n 'let (languages, _) = language_rows_and_selection' src/presentation/wx_settings.rs   -> 3278, read_settings rebuilds the same list and writes languages[selection].tag
```

For a stored "en-AU" on a machine set to en-US that offers en-US and not an
available en-AU, `language_to_use` answers "en-US" (its second arm, written
for the bare "en" of #21), `row_of("en-US")` is a row, and the `or_else`
that would have kept the stored tag is never reached. The checker's arm is
right for a bare tag and wrong for a tag with a region: the test's own
comment at `:81-83` says "A tag somebody chose is kept exactly as chosen,
whether or not this machine can check it", and 09-02's summary chose that
rule. The rule the screen needs is in two halves: a tag with a region is
kept, as the row spelled that way if the machine lists one (available or
not) and as a row added at the end if it does not; a bare tag is resolved
the way the checker resolves it. `language_to_use` itself is untouched:
the checker resolving en-AU to en-US when there is no en-AU dictionary is
correct for the checker, and the screen's job is to show what was chosen.

**4. Where the rule lives, and what it costs.**

```
grep -cE '^\s*#\[(test|tokio::test)\]\s*$' src/presentation/wx_settings.rs   -> 0; 19 guard records name it at 0 tests, so one test added there flags 19 records
python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(sum(1 for r in g if any(e['file']=='src/service/spellcheck/mod.rs' for e in r.get('tests_last_seen',[]))))"   -> 32 records name the checker's module (the first draft said 30 from phase 9's README; this is today's count)
sed -n 23471,23476p guards/guards.toml   -> the record coupling the language target to wx_settings.rs has before = ".or_else(|| row_of(stored));", exactly the line task 2 replaces
```

So the rule goes in a new module, `presentation::which_language_row`, at
zero records, pure over `(stored, machine, rows)`, and `wx_settings.rs`
gains a call and no test. `tests/the_language_the_screen_shows_is_the_one_used.rs`
is already coupled to `wx_settings.rs` by the record at `guards.toml:23470`,
so a green commit here runs it through the hook.

**5. The gate for each file.** `src/presentation/which_language_row.rs`
maps to itself; `src/presentation/mod.rs` changing runs the whole
presentation layer once; `src/presentation/wx_settings.rs` maps to a `--lib`
filter matching nothing and to its coupled targets, the language target
among them.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: which row the picker shows for a stored tag, as a rule with its cases</name>
  <files>src/presentation/which_language_row.rs, src/presentation/mod.rs, guards/guards.toml</files>
  <read_first>
    - `tests/the_language_the_screen_shows_is_the_one_used.rs`, all of it: the three cases are the rule.
    - `src/presentation/wx_settings.rs:866-906`, `language_rows_and_selection` and its doc comment.
    - `src/service/spellcheck/mod.rs:330-380`, `language_to_use` and `LanguageChoice`; `:269-300`, `language_to_check_in`, for what the checker does with a bare tag.
    - `.planning/phases/09-what-the-first-day-of-testing-found/09-02-SUMMARY.md`, the section on the one resolver and what it decided a chosen tag means.
  </read_first>
  <behavior>
    - `which_language_row::which_row_shows(stored: &str, this_machine: Option<&str>, rows: &[LanguageChoice]) -> RowToShow`, where `RowToShow` is `Existing(usize)` or `Added(String)`.
    - A stored tag that names a region (it has a `-` with something after it): the row whose tag equals it ignoring case, available or not, is `Existing`; when no row spells it, `Added(stored)`. So "en-AU" over rows [en-US available, en-AU not available] is `Existing(1)`, over rows [en-US available] alone is `Added("en-AU")`, and "EN-au" finds "en-AU".
    - A bare tag (no region): `language_to_use(stored, this_machine, rows)`'s answer's row, so "en" on a machine set to en-US over rows [en-029 available, en-US available] is `Existing(1)`; when that answers nothing or names no row, `Added(stored)`.
    - A tag nothing offers, "zz-ZZ", is `Added("zz-ZZ")` under either rule.
    - The doc comment says the two halves and why they differ: the checker may resolve a chosen tag to a dictionary it has, and the screen must never rewrite what was chosen.
  </behavior>
  <action>
    Red first, on a branch: the five cases above by module path, against a
    stub answering `Added(stored)` for everything, so the `Existing` cases
    are red and the `Added` ones green on arrival, said in the commit. Then
    the rule, registered in `presentation/mod.rs` beside `wx_settings`.

    Guard records: one whose break makes a tag with a region take
    `language_to_use`'s answer, which is the tree as it stands today,
    measured on the library.
  </action>
  <verify>
    <automated>cargo test --lib presentation::which_language_row::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the module's tests by module path and says which were green on arrival.
    - `cargo test --lib presentation::which_language_row::` passes with at least five tests, quoted.
    - `grep -c 'pub mod which_language_row' src/presentation/mod.rs` is 1.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>Which row the picker shows is decided by a pure rule whose two halves are each held by a case a runner without the dictionary would fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the screen asks the rule, the runner's case passes, and the changelog says what was wrong</name>
  <files>src/presentation/wx_settings.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/wx_settings.rs:884-906` and `:3272-3283`, the two callers of the list.
    - `guards/guards.toml:23460-23490`, the record coupling the language target to `wx_settings.rs`, whose `before` block may sit on the lines this task changes.
    - `docs/changelog.md`, the `[Unreleased]` section and 09-02's entry, to name the regression against it.
  </read_first>
  <behavior>
    - `language_rows_and_selection` builds `rows` from `available_languages()`, asks `which_row_shows(stored, system_language().as_deref(), &rows)`, and for `Added(tag)` pushes the row with `available: false` and selects it, as it did; its doc comment names the rule's module and keeps its account of #21.
    - `tests/the_language_the_screen_shows_is_the_one_used.rs` is unchanged and passes here; on the runner it will pass because the "en-AU" case now keeps "en-AU", which is what the run at `744d05ef` refused.
    - No test is added to `wx_settings.rs`; the target at zero is the red the runner already gave.
  </behavior>
  <action>
    The green: the call in place of the three-line `selected` expression,
    with `cargo build` between. Run the language target and the six other
    targets coupled to `wx_settings.rs` (`bash scripts/check.sh --suites-for
    guards/guards.toml src/presentation/wx_settings.rs` lists them) before
    the commit, and quote the list.

    Guard records: the record at `guards.toml:23471` ("the settings screen
    shows a stored language nothing offers as itself, not as row 0") has
    its `before` on the very line this task replaces, so it is rewritten
    onto the new call with a break of the same meaning (the rule's answer
    replaced by `Existing(0)`) and re-measured, unconditionally;
    `test_every_guard_record_still_names_one_place_in_the_tree` refuses the
    commit otherwise.

    Invoke `writing-craft` for the changelog entry under `[Unreleased]`,
    Fixed: that a spelling language chosen for a region this computer has no
    dictionary for was shown, and written back on OK, as this computer's own
    region, so a profile set to English (Australia) on a machine that offers
    only English (United States) was rewritten to English (United States) the
    first time Settings was saved; that it is a regression of 09-02's fix for
    #21, in `1.0.0-alpha.1`; that it was found by CI on 2026-09-18 and not by
    anybody, because the machine the testing happens on offers the language;
    and that a chosen tag is kept as chosen now, checked in the nearest
    dictionary as before. No em dash, none of the six words.
  </action>
  <verify>
    <automated>cargo test --test the_language_the_screen_shows_is_the_one_used && cargo test --lib presentation::which_language_row:: && cargo test --test every_event_has_a_control && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'which_row_shows(' src/presentation/wx_settings.rs` is 1.
    - `cargo test --test the_language_the_screen_shows_is_the_one_used` passes here, quoted; the summary names the runner's next run as what settles it there.
    - The changelog entry exists under `[Unreleased]`, Fixed, and names 09-02 and #21.
    - Carriage returns on the changelog are 0 by `tr -cd '\r' | wc -c`.
  </acceptance_criteria>
  <done>The screen keeps a chosen tag, the rule is asked from one place, and the next push of main is what shows CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the stored settings file to the screen | a tag written by hand or by an older build is shown and written back; rewriting it is a silent loss of a choice |
| this machine's language list to the picker | what Windows offers differs by machine, and the runner is the machine that showed the difference |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Tampering | a chosen tag rewritten on save | medium | mitigate | the region rule in `which_row_shows`, held by the module's cases and by the integration reading on the runner |
| T-11-06 | Repudiation | a fix that passes here and not on the runner | medium | mitigate | the red half is a unit case over hand-built rows, so it is red on both machines; the run after the push is the check |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- One guard record measured; the coupled record at `guards.toml:23471` rewritten onto the new call and re-measured.
- What the runner settles: the "en-AU" case at the next push of `main`, which is Pratik's to make; what this machine proves: the rule's five cases and the integration reading as it stands.
- After the merge, no issue to close; the summary quotes run 35336142985's failure line and says the next push is what shows the fix on the runner.
</verification>

<success_criteria>
- Roadmap criterion 11 closes structurally: a chosen tag kept as chosen, the bare tag still resolved, the target green here and written to be green on the runner.
- FOUND-17's `[D]` lines each have a named test in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-01-SUMMARY.md` when done
</output>
