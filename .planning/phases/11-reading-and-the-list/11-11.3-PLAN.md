---
phase: 11-reading-and-the-list
plan: 11.3
type: execute
wave: 26
depends_on: [11-11.2]
files_modified:
  - src/presentation/editor_document.rs
  - src/presentation/wx_compose.rs
  - tests/a_marker_counts_at_the_start_of_any_line.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-18]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Markdown block marker typed with its space at the start of any line of the message body becomes the structure it stands for: on the first line, on a line after a line break in a reply, forward or mailto body that arrived as plain text, after Enter on the empty first line, and after Shift+Enter; each conversion is announced as before; a marker with no space after it is not a marker and never was (LIST-18, #79)."
    - "A refusal that met a marker on a line the rule could not take is written to the log at debug through a posted kind the window handles, never announced, so the next report says which path refused."
    - "A reading drives the real editor page in the hosted WebView with keystrokes and asserts the document's structure after each marker and the ordered posts, so the changelog's promise of 2026-07-28 is held by a test rather than a page; the pages say the space in words and the guide lists the markers."
    - "After **bold** the following text is not bold; and whether - on the empty first line makes a list is measured and either made to or said in the changelog."
  artifacts:
    - "src/presentation/editor_document.rs: startsItsLine in the page script, the refused kind posted, the inline applier leaving the style after its closing delimiter."
    - "src/presentation/wx_compose.rs: the refused kind written at debug."
    - "tests/a_marker_counts_at_the_start_of_any_line.rs: one process, one wx app, the steps of the diagnosis, each asserted on its own on innerHTML and the posts."
    - "docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md."
  key_links:
    - "the typed space -> the input listener -> blockMarkdown -> startsItsLine(at.node) -> blockRule -> execCommand formatBlock -> post({kind:'format', index}) -> wx_compose's Formatted arm -> the announcement."
    - "blockRule matched and startsItsLine false -> post({kind:'refused', where:'line'}) -> parse_message -> tracing::debug! in wx_compose."
---

<objective>
A marker counts at the start of any line, a refusal says so in the
log, and a test types into the real page.

Purpose: #79, the tester on 2026-09-18 under NVDA on `744d05ef`: typed
Markdown made no heading, and he expects the structure. The probe of the
same day, on this machine's WebView2 runtime, reproduced it at neither
build he named: the page is byte for byte the same across the round, and
`## Heading` on the first line of an empty message made an `<h2>` and
posted "Heading level 2". Two states make the rule fail silently and
both fit his words: the marker on a line after a `<br>` in a body that
arrived as plain text, or after Shift+Enter or Enter on the empty first
line, which the guard at `editor_document.rs:742` refuses because the
text node has a previous sibling; and no space after the marker, which
was never a marker. The first is a defect against the documented "at the
start of a line", present since 2026-07-29 in every build; the second is
a sentence the pages never said in words a screen reader voices.

Output: the guard asking the line, the refusal logged, the reading over
the real page, the inline style released after its closing delimiter,
the empty-root list measured, the pages and the changelog. Closes #79 on
the tree side; which of the two states the tester was in is his ear's,
and the summary gives him the by-ear steps of the diagnosis.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `eb5d8517`; the page and its rule
tables carry the same bytes at `744d05ef`, `3e633252` and here (`git
diff --stat 744d05ef HEAD -- src/presentation/editor_document.rs
src/presentation/markdown_input.rs src/presentation/wx_compose.rs
src/presentation/editor_page_harness.rs` prints nothing). The probe's
logs, the engine lens runs and the diagnosis are copied, untracked, to
`C:/Users/prati/Documents/projects/wixen-mail-sweep/probe_79/`
(`79-diagnosis.md`, `probe-79-*.log`, `engine_lens_run1.log`,
`engine_lens_run2.log`, `engine_lens_cdp.py`) and are quoted here by
line; the seed for the reading is
`C:/Users/prati/Documents/projects/wixen-mail-sweep/probe_79/probe_79_markdown_typed.rs`,
uncommitted in the sweep tree beside them and kept out of that tree's
`tests/` so cargo does not build it there. Command first, then what it
said.

**1. The rule, its trigger and its guard.**

```
sed -n 722,724p src/presentation/editor_document.rs   -> typingInput: insertText or insertReplacementText
sed -n 792,804p src/presentation/editor_document.rs   -> the input listener: typingInput and event.data, caretText, the word watch at :800, then event.data === ' ' reaches blockMarkdown at :801
sed -n 741,749p src/presentation/editor_document.rs   -> blockMarkdown: :742 if (at.node.previousSibling) return; :743 blockRule over the line so far; :744 return on no rule; selectBack, delete, formatBlock, post({kind:'format', index}) at :748
sed -n 48,73p src/presentation/markdown_input.rs   -> BLOCK_RULES: ###, ##, #, -, *, >, each with its space; :90 INLINE_STYLES
sed -n 77,81p src/presentation/editor_document.rs   -> escaped_plain_text: every newline of a Plain body becomes a bare <br> inside the editable, no block wrapper
sed -n 1145,1151p src/presentation/wx_compose.rs   -> set_body loads the page for every compose mode; :281 a reply's Plain body; :1228-1231 mailto's
```

The probe's measurements on this engine (runtime 153.0.4234.32, the
tester's): `## Heading here` on the first line of an empty root made
`<h2>` and posted format 4 at both builds; on a line after a `<br>`,
`previousSibling=<br>` and the guard returned with no post; after Enter
on the empty first line, `previousSibling=DIV` and the same; after
Shift+Enter, the same; `##Heading ` stayed text at both builds. The
issue's "trace first" items 1 and 2 are therefore answered: it is the
guard's question, not the input path and not one marker.

**2. What a refusal says today, and where a new kind lands.**

```
sed -n 1658,1660p src/presentation/editor_document.rs   -> parse_message: a match on the kind string, None for one it does not know
sed -n 2185,2210p src/presentation/wx_compose.rs   -> the handler: the Formatted arm at :2200-2208 announces "Heading level 2" from :1576's words
grep -n "kind: 'refused'" src/presentation/editor_document.rs   -> nothing: a refusal posts nothing today
```

**3. What must not move.** The editable's `role="textbox"
aria-multiline="true" aria-label="Message body"` at `:168-169` and the
`<title>` at `:143`, which the phase 6 scan reads; the word watch's
order at `:800`; the whole-line rule that keeps `Hello ## ` text, held by
`editor_page_harness.rs:179-191`; Ctrl+Z putting the characters back,
`docs/KEYBOARD_SHORTCUTS.md:813`.

**4. The two side findings, measured on the way.** After `**bold** ` the
next space and the lines after it stay bold in both builds: the caret
continues the `<strong>` the applier made. And `- ` as the first thing
in an empty root was not measured; July's note says `insertUnorderedList`
did nothing on an empty root.

**5. The gate for each file.** `src/presentation/editor_document.rs`
maps to `--lib presentation::editor_document::`, 82 tests, 1 record;
`src/presentation/wx_compose.rs` 43 tests, 7 records;
`src/presentation/markdown_input.rs` 5, 0, not changed;
`src/presentation/editor_page_harness.rs` 36, 1, not changed and must
stay green; the new target to itself, one `#[test]` per process because
wxWidgets allows one app per process (`tests/theme_reach.rs` explains
the budget). The red for the target and the count check are one commit,
since the record names `editor_document.rs`. Counts taken 2026-09-18 at
`eb5d8517` with `grep -c '#\[test\]'` and the parser command in
`CLAUDE.md`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the reading over the real page, and the guard that asks the line</name>
  <files>tests/a_marker_counts_at_the_start_of_any_line.rs, src/presentation/editor_document.rs, src/presentation/wx_compose.rs, guards/guards.toml</files>
  <read_first>
    - `C:/Users/prati/Documents/projects/wixen-mail-sweep/probe_79/79-diagnosis.md`, sections 2 to 5, and the probe seed named in the premises: how it builds the dialog through `wx_compose::build_compose_dialog`, delivers keys as `WM_CHAR` to the `Chrome_WidgetWin_1` child and `WM_KEYDOWN`/`WM_KEYUP` for Enter and arrows, records the page's events, reads the body with `editor_document::read_body_script()` and decodes posts with `parse_message`; its teardown keeps `ticker.stop()` before `destroy_when_ready`.
    - `src/presentation/editor_document.rs:600-830` (the script: caretText, typingInput, blockMarkdown, the input listener, the load script) and `:1650-1670` (`parse_message`).
    - `src/presentation/wx_compose.rs:2185-2365`, the handler's arms.
    - `tests/theme_reach.rs`, the process budget, and `src/presentation/browser_ready.rs`.
  </read_first>
  <behavior>
    - The target, `test_a_marker_typed_at_the_start_of_any_line_makes_its_structure`, one function, one wx app, the body reset between shapes, under `WIXEN_NO_AUDIO` and a `tempfile` data directory in `WIXEN_MAIL_DATA`, never the tester's profile; it reads `window.__seen.length` first so a delivery failure is told apart from a rule failure. Its steps, each asserted on its own on `innerHTML` and the ordered posts, never on an announcement: (1) empty root, `## Heading `: `<h2>`, post format 4; (2) `format_reply_body(&MessageBody::Plain("Their words\non two lines"))`, ArrowDown, `## Heading `: `<h2>Heading </h2>` between the `<br>`s and nothing else changed, post format 4; (3) the same body, Enter on the empty first line, `## x `: `<h2>`; (4) empty root, `Hello`, Shift+Enter, `## x `: `<h2>`; (5) empty root, `##Heading `: text unchanged and no format post; (6) after (2), `**bold** ` and `- item ` on fresh lines: `<strong>` and `<ul><li>` with posts style 0 and format 7; (7) after (2), the refused post: `## ` typed mid-line after `Their ` posts nothing, and, with the guard still refusing a `<br>` line before the fix, the red shows steps 2, 3 and 4 failing and no others.
    - `startsItsLine(node)` in the page script: true when the node has no previous sibling, or the previous sibling is a `<br>`, or the previous sibling is an element whose computed display is block (the `<div>` Chromium leaves before the caret after Enter on the empty first line; measured by step 3 before the clause is widened, and the summary says what the shape was); `:742` becomes `if (!startsItsLine(at.node)) return` after a `blockRule` match that posts `{kind:'refused', where:'line'}`, so an ordinary space with no marker still posts nothing.
    - `formatBlock` on a text node between two `<br>`s wraps that line alone: step 2 holds it on the real engine; if the engine wraps more, the applier selects the line's nodes first, a few lines in `blockMarkdown` and not a redesign, said in the summary.
    - `parse_message` knows `refused` with its `where`; `wx_compose`'s handler writes it with `tracing::debug!("The editor refused a block marker: {where}")` and no announcement; a unit test in `editor_document.rs`'s module for `parse_message` over the new kind, rewritten in place if one exists for an unknown kind, else one added with the count said.
  </behavior>
  <action>
    Red first, on a branch, one commit: the target named by its one
    function and the count check named with the reason in the message
    (`CLAUDE.md`, "the red gate and the count check collide"). The seed
    is the probe; it is brought into `tests/` under the target's name,
    shaped into steps, and made to assert. Then `startsItsLine`, the
    refused post, `parse_message`, the debug arm, with `cargo build`
    between. `editor_page_harness.rs:179-191` stays green, said.

    Guard record, measured by taking the break by hand: name "a marker
    is refused when anything at all precedes it in its parent", file
    `editor_document.rs`, suite the target, before the `startsItsLine`
    line, after the `previousSibling` line, red the target's function;
    `tests_last_seen` from the parser on the day. A second record on
    `wx_compose.rs` if the debug arm gets a case there, said. The one
    record naming `editor_document.rs` re-measured when the count check
    names it.

    Reachability, so this is not implemented and never wired: the fixed
    page is what `set_body` loads for every compose mode, and
    `--scan-target compose` on a fresh `WIXEN_MAIL_DATA` opens it alone;
    the summary names both lines.
  </action>
  <verify>
    <automated>cargo test --test a_marker_counts_at_the_start_of_any_line && cargo test --lib presentation::editor_document:: && cargo test --lib presentation::editor_page_harness:: && cargo test --lib presentation::wx_compose::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's function and the count check, and its message says which steps were red.
    - `grep -c 'function startsItsLine' src/presentation/editor_document.rs` is 1; `grep -c 'at.node.previousSibling) {{ return; }}' src/presentation/editor_document.rs` is 0; `grep -c "kind: 'refused'" src/presentation/editor_document.rs` is 1; `grep -c 'refused a block marker' src/presentation/wx_compose.rs` is 1.
    - The target passes with 1 test, quoted, and the summary quotes the `innerHTML` after steps 2, 3 and 4; `presentation::editor_document::` passes with 82 or 83, quoted; `editor_page_harness::` with 36; `wx_compose::` with 43 or 44.
    - One guard record measured and quoted, two if the arm got a case.
  </acceptance_criteria>
  <done>A marker with its space at the start of any line becomes its structure on the real page, a refusal that met a marker is in the log, and the reading holds every step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the inline style ends at its closing delimiter, and the list on an empty first line is measured</name>
  <files>tests/a_marker_counts_at_the_start_of_any_line.rs, src/presentation/editor_document.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/editor_document.rs`, the inline applier `blockMarkdown`'s sibling for `INLINE_STYLES` (found by `grep -n "kind: 'style'" src/presentation/editor_document.rs`), and the list applier's `insertUnorderedList` call.
    - The July note the diagnosis cites on `insertUnorderedList` and an empty root, found by `grep -n 'insertUnorderedList' src/presentation/editor_document.rs docs/changelog.md`.
  </read_first>
  <behavior>
    - Step (8) in the target: `**bold** next` on a fresh line: `next` is outside the `<strong>`, the `innerHTML` holding `<strong>bold</strong> next`; red today since the caret continues the element.
    - The inline applier, after wrapping the words, moves the caret after the element and toggles the command off (`execCommand` of the style's command once more when `queryCommandState` says it is on), so typed text after the closing delimiter is plain; the same for every entry of `INLINE_STYLES`, held by step (8) for bold and one more for the code span.
    - Step (9): empty root, `- item `: measured; if the engine makes `<ul><li>item</li></ul>` the step asserts it; if `insertUnorderedList` does nothing on an empty root, the applier first gives the root a block to work in (a `<div>` around the caret's text, the shape Chromium makes on Enter) and then the list, and the step asserts the list; the summary says which it was.
  </behavior>
  <action>
    Red first, on a branch: the two steps added to the target's function
    (one function, so the same name as task 1's red, and the message
    says which steps are new and red), no count check named since the
    target's file is its own. Then the applier's caret move and toggle,
    and the empty-root shape if the measurement says so, with `cargo
    build` between.

    Guard record: one on `editor_document.rs` whose break drops the
    toggle after the wrap, the target red; measured; the records naming
    `editor_document.rs` re-measured when the count check names them.
  </action>
  <verify>
    <automated>cargo test --test a_marker_counts_at_the_start_of_any_line && cargo test --lib presentation::editor_document::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's function and says which steps are new.
    - The summary quotes the `innerHTML` after steps 8 and 9 and says what the empty root did before the fix.
    - The target passes with 1 test, quoted; one guard record measured and quoted.
  </acceptance_criteria>
  <done>Text after a closing delimiter is plain, and a list marker on the empty first line makes a list, both held by the reading.</done>
</task>

<task type="auto">
  <name>Task 3: the pages say the space in words, the guide lists the markers, and the changelog says what was broken since July</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:805-825`, the Markdown section and its table at `:819`.
    - `docs/USER_GUIDE.md`, the composing section, found by `grep -n -i 'compos' docs/USER_GUIDE.md`.
    - `docs/changelog.md:11700-11712`, the promise of 2026-07-28, and the `[Unreleased]` section.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/KEYBOARD_SHORTCUTS.md`, at `:812`: one
    sentence, "Type the marker, then a space, then keep writing", in
    words and not in a code span, and the table's rows read aloud the
    same way (the space named in the row's text where it matters); a line
    saying a marker counts at the start of any line, including a line of
    a quoted reply and a line after Shift+Enter. `docs/USER_GUIDE.md`:
    the composing section gains the list of markers, each with what it
    makes and what is said, and the same sentence about the space; today
    nothing under `docs/` but the changelog names them. The changelog
    entry under `[Unreleased]`, Fixed: that since 2026-07-29 a marker
    typed on any line after the first of a reply, forward or mailto body
    that arrived as plain text, or after Shift+Enter, or after Enter on
    the empty first line, did not count and nothing said so, and now
    counts and a refusal is logged; that text after `**bold**` stayed
    bold and no longer does; what the empty first line did with `- `;
    that the marker needs a space after it, which the pages now say; and
    that the promise of 2026-07-28 needs no retraction because every
    marker it names has a rule. Known limitations: the reading delivers
    keys to the page as posted characters and not through a screen
    reader's hook, so the tester's by-ear steps are still his.

    Ledger, by hand, both halves: one `unrun-verify` entry holding the
    diagnosis's by-ear steps 1 to 8 for the tester, in plain words.

    Invoke `cognitive-accessibility` for the guide's list: one marker per
    row, the space said each time.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Type the marker, then a space' docs/KEYBOARD_SHORTCUTS.md` is 1 and the same grep on `docs/USER_GUIDE.md` is 1; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words.
    - The ledger entry in both halves.
  </acceptance_criteria>
  <done>A person reading the pages knows to type the space, knows a marker counts on any line, and knows what was broken and for how long.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the page script to the window | a posted kind crosses from the document to the handler |
| a quoted body to the rules | a reply's text arrived from a stranger and the rules run over the lines the person types into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-74 | Tampering | a widened guard turning a marker inside a quoted line into structure | medium | mitigate | `blockRule` still requires the whole line so far to be the marker; `Hello ## ` stays text, held by the harness and by step 7 |
| T-11-75 | Denial of service | a refused post on every ordinary space | low | mitigate | the post only after a `blockRule` match; step 7 and the harness hold it |
| T-11-76 | Repudiation | a refusal announced as a conversion | low | mitigate | the refused kind reaches the debug log and no arm that speaks; the reading asserts on posts, never on speech |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Two or three guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the by-ear steps in the ledger; what the reading proves: every step on the real page on this engine.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 79 --comment "Fixed in <merge commit>. The probe could not reproduce this on the first line of a new message at either build: the page is the same bytes in every build of this round, and ## with a space made a heading and said so. Two things did fail, and both fit what you described. A marker typed on any line after the first of a reply to a message that arrived as plain text, or after Shift+Enter, or after Enter on the empty first line, was refused before it was read, silently, since July; now it counts, and a refusal is written to the log. And a marker with no space after it was never a marker: type the marker, then a space, then keep writing, which the pages now say in words. Also fixed on the way: text after **bold** stayed bold. A test now types into the real editor page and checks the structure after each marker. The steps to check by ear are in the plan's summary; the second one is the case that was broken."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 21 closes structurally: the guard asking the line, the refusal logged, the reading over the real page, the inline style released, the empty root measured, the pages.
- LIST-18's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-11.3-SUMMARY.md` when done
</output>
