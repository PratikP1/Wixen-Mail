---
phase: 11-reading-and-the-list
plan: 09.1
subsystem: the earcon channel on by default for every event and the attachment event's own default of the tone and the status bar, read wherever no answer was stored; the never-sound-alone rule unable to speak for an event whose words are on the row; the Feedback tab painting an event's own default; the earcon player opening the default device again on the stream's flag and after a gap, an outage told once in the log and on the status bar; guards, pages, ledger
tags: [earcons, feedback-channels, has-attachment, never-sound-alone, braille, feedback-tab, rodio, cpal, device-reopen, stream-error-callback, status-bar, ledger]

requires:
  - phase: 11-reading-and-the-list
    provides: "11-09 merged at bd5f6929 (the row's cells read on request, which reads the Attachment column this plan keeps); 11-08 (feedback_events_for_landing_on_a_conversation beside feedback_events_for_landing, the cursor handler on on_item_focused); phase 6's FeedbackSettings, Switch and the Feedback tab's One event at a time section; the EarconPlayer of phase 6 with its detached mixer; main clean at fcfe9a15 when the branch left it"
provides:
  - "presentation::accessibility::feedback::Event::text_is_already_on_the_row(self) -> bool: true for HasAttachment alone, with the doc naming the column and why not ThreadLanded; FeedbackSettings::the_default_for(event) -> BTreeSet<Channel>: the tone and the status bar for such an event, every channel otherwise, public so the Feedback tab paints from it; FeedbackSettings::default() with disabled empty and per_event empty, phase 6's reason kept and dated; channels_for reading the event's default for a missing entry and falling back to Visual alone for a marked event; 54 tests, 7 records name the file"
  - "presentation::accessibility::feedback::EarconPlayer: Output holding the mixer, the device and an ended flag the stream's error callback sets; an Opener field; Inner behind one lock with last_played, opened_at, last_attempt and an Outage; new opening through DeviceSinkBuilder::from_default_device with the callback, open_sink_or_fallback and the walk over every other output device; somewhere_to_play opening again on the flag, after REOPEN_AFTER (10 s) since the later of the last sound and the last open, or after REOPEN_AFTER since a refused open; open_the_device_again telling the outage once at warn and the resume once at info; take_complaint(&self) -> Option<String>, the sentence once; whose_device_has_gone_for_good for the case in accessibility.rs"
  - "presentation::accessibility::Accessibility::signal and ::earcon putting the player's complaint where the status bar reads, in place of the event's words, through show(sentence); 25 tests, 9 records"
  - "presentation::wx_settings::ticks_for(event, chosen): an event nobody answered for painted from its own default; the Feedback tab's line beneath still says it is the default"
  - "presentation::accessibility::screen_reader::tests::CapturedLogs pub(crate), with events and has, so feedback.rs's outage case counts log lines"
  - "tests/landing_on_an_attachment_says_it_once.rs: eleven cases over the model and the stored profiles, two readings over what_ships of wx_app.rs with two companions; 15 tests, 3 records name it"
  - "tests/every_event_has_a_control.rs: a_fresh_profile_opens_with_the_sounds_on_and_the_attachment_event_on_its_own_default, reading the built Feedback tab back; 1 test as before"
  - "src/data/config.rs: the older-file test reading an empty feedback_channels as the new default and a file holding off=earcon keeping it; the field's doc; 68 tests"
  - "guards/guards.toml: 981 records, census 798 + 183; six new, measured on the green tree; twelve re-measured at the count check's word"
  - "docs/USER_GUIDE.md: three paragraphs and four steps under Sound Schemes and Earcons; docs/changelog.md: one entry under Changed for #77 and one under Fixed for #81"
  - ".planning/WINDOWS.md: 552 unrun-verify, what only the ear settles for #77; 553 unrun-verify, the reproduction of #81 and the sounds heard again after a real device change"
affects: [11-10, which adds Event::RuleMatched and inherits the default of every channel for it and the sound on; 11-09.2, which changes what a row's snippet says beside the Attachment column this plan keeps; 11-13, which reads the one new status bar sentence, The sounds have stopped; 11-12, which reads LIST-13's and LIST-16's lines and the pages; whoever next adds an event whose words are already on the row, who adds it to text_is_already_on_the_row on purpose]

actuals:
  tokens: 29093
  tasks: 3
  commits: 8

tech-stack:
  added: []
  patterns:
    - "A default that changes lives in the function that answers 'nothing was chosen', never in the constructor's stored fields, because a serialised profile overwrites the fields and never goes through the constructor; the reporter's own stored profile is the first case to write"
    - "A seam the stream owns (the error callback's flag) is proved by a scripted opener and a helper that sets the flag the callback would, so every case holds on a runner with no device, and what a real device does is a ledger entry with its steps"
    - "A red commit under a dead-code lint carries the first slice of the behaviour that reads every item it introduces, with the cases that slice leaves green named in the message; inert stubs are refused by clippy before any test runs"

key-files:
  created:
    - tests/landing_on_an_attachment_says_it_once.rs
  modified:
    - src/presentation/accessibility/feedback.rs
    - src/presentation/accessibility.rs
    - src/presentation/accessibility/screen_reader.rs
    - src/presentation/accessibility/sound_scheme.rs
    - src/presentation/wx_settings.rs
    - src/presentation/wx_reminder_alert.rs
    - src/data/config.rs
    - tests/every_event_has_a_control.rs
    - guards/guards.toml
    - docs/USER_GUIDE.md
    - docs/changelog.md
    - .planning/WINDOWS.md

key-decisions:
  - "The attachment event's exception is the event's own default, FeedbackSettings::the_default_for, read wherever no answer was stored, and not an entry in a fresh profile's per-event list as the plan wrote it: the tester's stored profile is off= with no per-event group, the plan's premise 2 quotes it, and under the plan's shape the person who filed #77 would have gone on hearing the duplicate until he opened Settings; a fresh profile stores off= and the Feedback tab says the event is using the default, which is true"
  - "Braille is not in the attachment event's default and the fallback never adds it for a marked event, because a set holding Braille calls the one screen reader notification and the words are spoken; the doc on the_default_for says where to add it, and the close comment on #77 says so; one channel added is the overrule"
  - "The gap-based reopen is the second mechanism, REOPEN_AFTER ten seconds: opening the default device cost a median of 11 ms and at worst 26 ms, under the plan's 50 ms line; Windows' own notice was not written and no feature was added to the windows crate"
  - "The same gap paces a refused device: an outage asks for the device again REOPEN_AFTER after it last refused, not on every sound (T-11-59), which the plan's one-open-per-play bound left open under a syncing mailbox"
  - "A machine with no device at start is said in the log at info and not on the status bar, since nothing has stopped; the sentence The sounds have stopped is for an outage after a device was open, and the resume is told once at info either way"
  - "signal puts the complaint in place of the event's words on the status bar whatever the event's channels, and earcon does the same, since a person whose only sounded events go through that route would otherwise never see why the sounds stopped; the event's words still go to the screen reader"
  - "Task 2's readings and the Feedback tab readback were committed green with their companions rather than red: once task 1 landed, the handler had signalled the events since phase 6 and the painter knew the event's default, so nothing was left for a red to name; the companions plant six faults and are where those readings were red"
  - "The log-capturing subscriber in screen_reader.rs's tests is made pub(crate) rather than copied, so the outage case counts warn lines rather than reading the told flag"

patterns-established:
  - "A changed default is proved against the reporter's stored profile before the constructor's default; observation 725 in the skill log"
  - "A red under clippy -D warnings carries the first slice that reads what it introduces; observation 726"

requirements-completed: [LIST-13, LIST-16]

coverage:
  - id: D1
    description: "FeedbackSettings::default() enables every channel; HasAttachment's own default of the tone and the status bar is the_default_for, read wherever no answer was stored (the [D] line amended 2026-09-19 to say where the default lives); phase 6's reason kept and dated; from_stored(off=earcon) keeps the sounds off"
    requirement: LIST-13
    verification:
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_a_fresh_profile_hears_every_tone"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_attachment_event_reaches_the_tone_and_the_status_bar_and_no_words"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_every_other_event_still_reaches_every_channel_under_the_default"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_nobody_has_chosen_anything_for_a_fresh_profile"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_profile_that_filed_the_issue_hears_the_attachment_event_the_same_way"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_a_profile_that_chose_silence_keeps_it"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_a_stored_string_with_nothing_in_it_answers_the_new_default"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_speech_can_be_turned_back_on_for_the_attachment_event_and_survives_a_restart"
        status: pass
      - kind: unit
        ref: "src/presentation/accessibility/feedback.rs#test_the_tones_are_on_until_someone_turns_them_off"
        status: pass
      - kind: unit
        ref: "src/data/config.rs#test_a_settings_file_written_before_these_existed_reads_the_way_it_should"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'an event whose words are on the row has its own default of the tone and the status bar' at 4 red on the target, measured 2026-09-19 at the green, at task 2's commit and on the tree after task 3"
        status: pass
      - kind: command
        ref: "cargo test --test landing_on_an_attachment_says_it_once -> 15 passed; --lib presentation::accessibility::feedback:: -> 54; --lib data::config:: -> 68; grep -c 'disabled: \\[Channel::Earcon\\]' src/presentation/accessibility/feedback.rs -> 0; grep -c 'fn text_is_already_on_the_row' -> 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "An event whose text is already on the row never gains speech or braille from the never-sound-alone rule, only the status bar; the older-file test and the Feedback tab readback rewritten in place; the landing arm held to signalling and nothing spoken"
    requirement: LIST-13
    verification:
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_only_the_attachment_event_says_its_text_is_already_on_the_row"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_fallback_never_speaks_for_an_event_whose_text_is_on_the_row"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_fallback_still_adds_braille_first_for_an_event_whose_text_is_not_on_the_row"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_cursor_handler_signals_what_the_landing_decides"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_cursor_handler_speaks_nothing_of_its_own_about_attachments"
        status: pass
      - kind: integration
        ref: "tests/landing_on_an_attachment_says_it_once.rs#test_the_window_readings_complain_when_the_handler_speaks_or_stops_signalling"
        status: pass
      - kind: integration
        ref: "tests/every_event_has_a_control.rs#test_every_event_is_reachable_and_keeps_what_it_was_given"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'the fallback never speaks for an event whose words are on the row' at 1 red on the target and 'the cursor handler speaks nothing of its own about attachments' at 1 red on the target, measured 2026-09-19"
        status: pass
      - kind: command
        ref: "cargo test --test every_event_has_a_control -> 1 passed; --lib presentation::wx_app:: -> 199; grep -v '^\\s*//' src/presentation/wx_app.rs | grep -c 'FeedbackEvent::HasAttachment' -> 2, one per landing function since 11-08 (the plan's 1 predates the conversation twin)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The player's output is opened through the builder with a callback that sets an ended flag; play_at opens the default device again on the flag and after a gap, the open having measured cheap; a failed reopen is one warn line and one take_complaint sentence that signal hands to the visual channel; cases over the flag, the gap, a refusing opener and the resume; the probe's median and worst quoted"
    requirement: LIST-16
    verification:
      - kind: unit
        ref: "src/presentation/accessibility/feedback.rs#test_a_player_whose_device_went_opens_the_default_device_again_before_the_next_sound"
        status: pass
      - kind: unit
        ref: "src/presentation/accessibility/feedback.rs#test_two_sounds_inside_the_reopen_gap_ask_for_the_device_once"
        status: pass
      - kind: unit
        ref: "src/presentation/accessibility/feedback.rs#test_a_sound_after_a_gap_opens_the_default_device_again"
        status: pass
      - kind: unit
        ref: "src/presentation/accessibility/feedback.rs#test_an_outage_is_told_once_and_the_sounds_come_back_when_a_device_does"
        status: pass
      - kind: unit
        ref: "src/presentation/accessibility/feedback.rs#test_a_device_that_stays_gone_is_asked_for_again_after_a_gap_not_on_every_sound"
        status: pass
      - kind: unit
        ref: "src/presentation/accessibility.rs#test_a_player_that_cannot_open_a_device_puts_its_complaint_where_the_eye_reads"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'a sound after the device has gone opens the default device again first' at 4 red, 'an outage of the sounds is told once, not on every sound' at 1 red, 'signal puts the player's complaint where the eye reads it' at 1 red, all measured 2026-09-19 on the green tree with the library as the suite"
        status: pass
      - kind: command
        ref: "cargo test --lib presentation::accessibility::feedback:: -> 54 passed; --lib presentation::accessibility::tests:: -> 25; grep -c 'with_error_callback' src/presentation/accessibility/feedback.rs -> 2; grep -v '^\\s*//' feedback.rs | grep -c 'open_default_sink()' -> 0; grep -c 'fn take_complaint' feedback.rs -> 1; grep -c 'take_complaint' src/presentation/accessibility.rs -> 2; probe_81_device_open_cost, 2026-09-19: open_default_sink median 11.5 ms worst 25.8 ms, from_default_device + open_sink_or_fallback median 11.7 ms worst 13.4 ms"
        status: pass
    human_judgment: false
  - id: S1
    description: "Whether the row is heard once with the tone, and the tone alone with the status bar off; whether a fresh profile's sounds are welcome"
    requirement: LIST-13
    verification: []
    human_judgment: true
    rationale: "Ledger 552; the close comment on #77 lists it; nobody has heard the row with the tone"
  - id: S2
    description: "Whether what silenced the tester's sounds after hours was the device going or something else; the sounds heard again after a real device change"
    requirement: LIST-16
    verification: []
    human_judgment: true
    rationale: "Ledger 553 with the reproduction steps; the close comment on #81 lists it; no device was pulled on a running build, and the tester's copy is what runs on this machine"

duration: 110min
completed: 2026-09-19
status: complete
---

# Phase 11 Plan 09.1: The sounds on by default, the attachment said once, and the earcons following the device Summary

**The Earcon channel is on by default for every event, Pratik's decision of 2026-09-18 on #77,
and landing on a message with an attachment says the word once, from the row's Attachment
column: the event's own default is the tone and the status bar with no words,
`FeedbackSettings::the_default_for`, read wherever no answer was stored for the event, so the
tester's stored profile of `off=` reaches it as a fresh one does and a profile that stored
`off=earcon` keeps its silence. The never-sound-alone rule falls back to the status bar alone
for such an event, never speech and never braille, because both ride the one screen reader
notification; the decision's "and braille" is the one clause the tree cannot carry without the
words being spoken, and the close comment says so. The Feedback tab paints an event nobody has
answered for from its own default, with the line beneath still saying it is the default. The
earcon player opens its output with the stream's own error callback marking the device gone,
and before a sound opens the default device again on that flag or after ten seconds' quiet, the
gap chosen over Windows' notice by a measured open of 11 ms median and 26 ms worst; an outage
is told once at warn and once on the status bar, "The sounds have stopped: no audio output
device could be opened", asked for again after each gap, and the resume told once. Nobody has
heard the row with the tone or the sounds come back after a real device change; #77 and #81 are
closed from the merge with the ear lists.**

## Performance

- **Duration:** 110 min from the start recorded at 14:48:27Z, after the reading of the README,
  the plan, `CLAUDE.md`, the workflow and the summaries had begun, to the merge's hook finishing
  at 16:38:08Z; the branch's first commit at 15:00:59Z; the issues closed at 16:39Z; the
  summary and the planning files after. About 23 min 35 s was guard measurement in four
  foreground runs (51 s for task 1's two records at the green, 12 s to read what already fails
  then 19 s and 20 s; 47 s for task 2's first run, whose break did not build, then 32 s for the
  corrected record, 17 s then 15 s; 21 min 25 s for task 3's three records and the twelve the
  count check named, 1,283 s across the timed lines, the ten library-suite records 80 to 99 s
  each and the target-suite ones 16 to 20 s). About 19 min 16 s the seven hook runs on the
  branch (170 s, 220 s, 137 s, 167 s, 158 s, 147 s, 157 s); 325 s the first whole gate, red on
  one test that read the old default as a fixture; 393 s the second, green; 316 s `main`'s hook
  at the first merge attempt, refused by the keyring race of ledger 374 on a test this plan
  never touched; 405 s the second, green. About 25 s the probe, built and run twice in the
  sweep worktree. 0 s waiting for the desktop: no live-window test went red on any run.
- **Started:** 2026-09-19T14:48:27Z
- **Merged:** 2026-09-19T16:38:08Z at `517a2a4c`
- **Tasks:** 3
- **Files modified:** 12, one created; `Cargo.toml` and `Cargo.lock` untouched by
  `git diff --stat fcfe9a15..3e6f361b -- Cargo.toml Cargo.lock` (T-11-SC), no crate and no
  feature added
- **Actuals:** `tokens: 29093` is `git diff fcfe9a15..3e6f361b | wc -c`, 116,371 characters
  over four, the branch's own diff against the commit it left `main` at; the estimate's
  `tokens` was 39,000 and `raw_tokens` 90,000, so the factor is 0.75 against the former and
  0.32 against the latter.

## The measurement that chose the mechanism

`C:/Users/prati/Documents/projects/wixen-mail-sweep/probe_81/probe_81_device_open_cost.rs`,
run from the sweep worktree's `tests/` with
`cargo test --test probe_81_device_open_cost -- --nocapture` on 2026-09-19 at 14:53Z and again
at 14:54Z, the second run's output kept beside it as `probe-81-device-open-cost.log`. Twenty
opens of the default output device, each sink dropped before the next, through
`rodio::DeviceSinkBuilder::open_default_sink()`, then twenty through
`from_default_device()` with an error callback and `open_sink_or_fallback()`, the route the
player now takes:

| Route | First run, median | First run, worst | Second run, median | Second run, worst |
|---|---|---|---|---|
| `open_default_sink` | 11.5 ms | 25.8 ms (the first open) | 10.6 ms | 23.7 ms |
| `from_default_device` + `open_sink_or_fallback` | 11.7 ms | 13.4 ms | 10.3 ms | 12.2 ms |

Under the plan's 50 ms line by a factor of four, so the second mechanism is the reopen before
the first sound after a gap, `REOPEN_AFTER` of ten seconds, the issue's "simplest and always
right"; Windows' own notice through `IMMNotificationClient` was not written and
`Win32_Media_Audio` was not added to the `windows` crate's features. The figure is on the
constant with its command and date. This machine has one output device; a machine with a
Bluetooth headset may pay more for an open, and the constant's doc says the gap is what to
move if it does.

## What landed

**Task 1.** `Event::text_is_already_on_the_row` is one `matches!` on `HasAttachment`, its doc
naming the Attachment column, the two readers of the mark and why the conversation event is
not one. `FeedbackSettings::the_default_for(event)` answers the tone and the status bar for a
marked event and every channel otherwise, public because the Feedback tab paints from it;
`channels_for` reads it for a missing entry and, for a marked event, falls back to `Visual`
alone and adds nothing when that is off. `Default` has `disabled` and `per_event` both empty,
with phase 6's reason dated as the reason until 2026-09-18 and the decision beside it, and a
paragraph saying why the exception is not in the per-event list. The module doc's first rule
gains the clause. `sound_scheme.rs`'s doc that said off by default is corrected; the comment on
`wx_reminder_alert.rs`'s fixture too. `config.rs`'s field doc says what empty means now, and
its older-file test takes `feedback_channels` out with the rest, reads the empty string as the
new default with the sounds on and no speech for the attachment event, and reads a file holding
`off=earcon` as silence kept. `wx_settings::ticks_for` takes the event and paints an untouched
event from `the_default_for`, so the attachment event shows the sound and the status bar with
the words off under the line saying it is the default; `paint_the_shown_event`'s doc corrected.
In `accessibility.rs` the two tests that read the default as sounds off switch them by hand;
`feedback.rs`'s default test is rewritten in place as
`test_the_tones_are_on_until_someone_turns_them_off`, and the comment on the stored-off test
corrected. 49 before and after in `feedback.rs` at this task, 68 in `config.rs`.

The target, `tests/landing_on_an_attachment_says_it_once.rs`: the fresh profile hearing every
tone, the attachment event's channels, every other event untouched, nothing chosen on a fresh
profile and `to_stored` writing `off=`, the tester's profile reaching the same default, a
profile that chose silence, the empty string as the new default, speech turned back on
surviving a restart, the mark decided over `Event::ALL`, the fallback that never speaks for the
event and yields to the sound alone when the status bar is off too, and the companion holding
the order Braille, Visual, Speech for an unmarked event. Two records on `feedback.rs`, the
target named.

**Task 2.** Two readings over `what_ships` of `wx_app.rs`: the cursor handler, cut between
`msg_list.on_item_focused({` and `msg_list.on_column_click({`, begins its landing loop by
signalling the event, and both landing functions answer `HasAttachment`; and the handler names
the word attachment in no string literal, read line by line with the comment cut off first.
Two companions plant a spoken "Has attachment", a comment naming the word, a string outside
the handler, the signal removed, the loop gone, the conversation's event dropped and the
handler renamed. `every_event_has_a_control.rs` gains
`a_fresh_profile_opens_with_the_sounds_on_and_the_attachment_event_on_its_own_default`, reading
the built tab: the global sound box on, the attachment event's boxes as the sound and the
status bar with the words off, the line beneath saying default, the line under that naming the
sound and the status bar and not an announcement. One record on `wx_app.rs`, the target named.
The guide's Sound Schemes and Earcons section and the changelog's entry under Changed, in the
tester's words; ledger 552. Both readings and the readback were green on arrival, said in the
commit and in the decisions: the handler has signalled the events since phase 6, and task 1
taught the painter the event's default.

**Task 3.** `Output` holds the mixer, the device and the `ended` flag; `on_the_default_device`
opens through `DeviceSinkBuilder::from_default_device`, `with_error_callback` (one warn line
naming the `StreamError`, the flag set) and `open_sink_or_fallback`, then the walk
`open_default_sink` takes over every other output device whose driver is not "null", with
`log_on_drop(false)` since the program chooses its drops; `the_whole_reason` joins an error's
sources because rodio's own words say nothing about why. `Inner` sits behind one lock with
`last_played`, `output`, `opened_at`, `last_attempt` and an `Outage` of `told` and
`complaint`; `Outage::told` is the warn line and the sentence, `Outage::from_the_start` the info
line for a machine with no device at start. `somewhere_to_play` opens again when the flag is
set, when more than `REOPEN_AFTER` has passed since the later of the last sound and the last
open, or when there is no output and more than `REOPEN_AFTER` has passed since it was last
asked for; `open_the_device_again` asks the opener once, tells the outage once, and on a
success after an outage says "The sounds are back" once and clears it. `take_complaint` hands
the sentence out once. `signal` takes the complaint after playing and shows it in place of the
event's words, whatever the event's channels; `earcon` does the same; both through `show`.
The scripted opener in the tests counts asks, refuses on demand and keeps every source;
`the_device_goes` sets the flag the callback would. `feedback.rs` 49 to 54, `accessibility.rs`
24 to 25. Three records, the library as the suite. The guide's paragraph, the changelog's entry
under Fixed, ledger 553 with the reproduction steps.

## Honest RED and GREEN

Three reds, three greens, one test commit whose tests were green on arrival with the companions
that made them red, one documents commit and one test rewrite, on branch
`landing-on-an-attachment-says-it-once` from `main` at `fcfe9a15`.

`b780d8ca`, task 1's red, 170 s through the hook in `red` mode: ten cases named bare, one from
`feedback.rs` and one from `config.rs` by module path, under a stub
`text_is_already_on_the_row` answering false. One green on arrival and said: speech turned back
on surviving a restart, which `set_event_channels` already carried. The two tests in
`accessibility.rs` and the fixture comment in `wx_reminder_alert.rs` rewritten to switch the
sounds by hand were green on both trees. No file a record names gained a test and the count
check printed no remedy.

`5d5d7575`, task 1's green, 220 s: the mark, the default, the fallback, the painter, two
records measured at the green.

`7897b75b`, task 2, 137 s: two readings, two companions, the readback and the pages, green on
arrival for the reason above; committed as `test(11-09.1)` without a marker. One record
measured, task 1's two re-measured since the target went from 11 to 15.

`27567a25`, task 3's red, 167 s: four cases in `feedback.rs` and one in `accessibility.rs`
named by path, the count check named bare since both files gained tests. One green on arrival
and said: two sounds inside the gap ask for the device once. The shape was committed as the
first slice that reads every item it introduces (open only when there is no output, paced by
`REOPEN_AFTER`), because the plan's inert stubs were refused by clippy's dead-code lint before
any test ran; observation 726.

`c545d2d5`, task 3's green, 158 s: the flag read, the gap, the outage, the resume, the
complaint through `signal` and `earcon`, three records measured and twelve re-measured in one
run of 21 min 25 s. `7a0c391a`, the pages and the ledger, 147 s.

`3e6f361b`, 157 s: `test_a_reminder_can_be_said_and_sounded_without_its_window` in
`wx_reminder_alert.rs` read a fresh `Accessibility` as one with the sounds off for its second
half. It went red on the whole gate and on no commit, because its file changed only on task
1's red, where the default had not yet moved, and the green did not touch it; the hook's
scoping is by file and the default is read by a fixture two modules away. Rewritten to switch
the sounds off by hand, as its first half switches them on.

Under the TDD gate's own terms, `test(11-09.1)` precedes `feat(11-09.1)` twice, for tasks 1
and 3; task 2 has no `feat`, since the tree already did what its readings hold.

## Guard records

975 by the TOML reader before, 981 after: six new, none retired, none rewritten; the census
798 + 177 before, 798 + 183 after. `scripts/guards.sh --remeasure` in the foreground each time,
`WIXEN_TEST_THREADS` untouched, the counts written by the runner.

| Record | File | Break | Red | Timed |
|---|---|---|---|---|
| an event whose words are on the row has its own default of the tone and the status bar (new, `suite` the target) | `feedback.rs` | `the_default_for` folded to every channel | 4, "all 4 tests named went red, and nothing else did": the event's channels, the tester's profile, the profile that chose silence, the fallback case | rebuild 18 s, run 1 s at the green; 15 s and 1 s at task 2; 19 s and 1 s after task 3 |
| the fallback never speaks for an event whose words are on the row (new, `suite` the target) | `feedback.rs` | the fallback's order the same for every event | 1: the fallback case | 19 s and 1 s; 15 s and 1 s; 18 s and 1 s |
| the cursor handler speaks nothing of its own about attachments (new, `suite` the target) | `wx_app.rs` | `a11y.announce("Has attachment", ...)` beside the signal, `Priority` by its full path | 1: the string reading | 14 s and 1 s; the first draft with a bare `Priority` did not build, corrected and measured again |
| a sound after the device has gone opens the default device again first (new) | `feedback.rs` | `output.has_ended() \|\|` dropped from the decision | 4: the device-went case, the outage case, the stays-gone case, and the status bar case in `accessibility.rs` | rebuild 39 s, run 51 s |
| an outage of the sounds is told once, not on every sound (new) | `feedback.rs` | the `if !inner.outage.told` dropped | 1: the outage case | 37 s and 49 s |
| signal puts the player's complaint where the eye reads it (new) | `accessibility.rs` | the `take_complaint` call dropped from `signal` | 1: the status bar case | 43 s and 49 s |

Every first draft but one was a prediction the runner agreed with; the exception is the
`wx_app.rs` break, whose `Priority` was out of scope at that line. The twelve records the count
check named at task 3's red (seven on `accessibility.rs`, five on `feedback.rs`, two of them
this plan's) were re-measured in the same run, every one as recorded, 80 to 99 s each for the
library-suite ones. Counts written: `feedback.rs` 54, `accessibility.rs` 25, the target 15,
`wx_app.rs` 199, `automation.rs` 2. `wx_app.rs` is at 199 before and after, quoted by
`cargo test --lib presentation::wx_app::` at the gate, and holds 196 `#[test]` attributes; 99
records name it now, one more than 98. `feedback.rs` 54 and 7 records; `accessibility.rs` 25
and 9; `config.rs` 68 and 11, unchanged; `every_event_has_a_control.rs` 1 and 5, unchanged,
their breaks not moved by the new function. The count check printed its remedy once, at task
3's red, where it was named in the trailer and run in the foreground before the green.

## What the tree contradicted

Every command in the plan's six premises was re-run against `main` at `fcfe9a15` before the
branch. The line numbers had moved since `08197657` (`feedback_events_for_landing` at `21511`,
the cursor handler at `3175`, `FeedbackSettings::default` at `503`, `channels_for` at `581`);
the shapes held, but for these:

1. **The plan's exception would not have reached the tester.** Premise 2 quotes his profile as
   `off=`, every channel on and no per-event group, and the behaviour block puts the
   attachment event's set into `FeedbackSettings::default()`'s `per_event` list, which
   `from_stored("off=")` never reads. The exception is the event's own default,
   `the_default_for`, read wherever no answer was stored, decision 1; LIST-13's `[D]` line is
   amended to say so, and the target's case for the tester's profile is the one that would
   have been red under the plan's shape.
2. **`grep -c 'FeedbackEvent::HasAttachment'` is 2, not the plan's 1.** 11-08 added
   `feedback_events_for_landing_on_a_conversation` beside `feedback_events_for_landing`, and
   both answer the event; the reading holds both.
3. **Task 2 had nothing to commit red.** Once task 1 landed the handler signalled and the
   painter knew the default; the readings and the readback were green on arrival, decision 7.
4. **The plan's stubs could not be committed red.** Fields stored and never read, a constant
   used by tests alone and a method answering `None` are dead code under clippy's
   `-D warnings`, which the hook runs before any test; the red carries the first slice of the
   behaviour instead, said in its message. Observation 726.
5. **A test two modules away read the old default as a fixture and the hook did not run it.**
   `wx_reminder_alert.rs`'s second half went red on the whole gate alone; rewritten, `3e6f361b`.
6. **`Priority` is not in scope at the landing loop.** The record's `after` names it by its
   full path; the plan's break as written did not build.
7. **The one-open-per-play bound is not enough for a device that stays gone.** T-11-59's
   mitigation says one open per play at most; under a sync that is eight opens a second
   against a device that is not there. The outage's retry is paced by the same gap, decision 4.
8. **A machine with no device at start is not an outage.** The plan's sentence "The sounds have
   stopped" is for a device that was open; at start it is said in the log at info and not on
   the status bar, decision 5.

## Deviations from plan

**1. [Rule 2 - Correctness] The exception is the event's own default, not a fresh profile's
stored answer**, contradiction 1 and decision 1, with
`test_the_profile_that_filed_the_issue_hears_the_attachment_event_the_same_way` and
`test_nobody_has_chosen_anything_for_a_fresh_profile`.

**2. [Decision] Task 2 committed green with its companions**, contradiction 3 and decision 7.

**3. [Decision] The red for task 3 carries the first slice of the behaviour**, contradiction 4.

**4. [Rule 1 - Bug] `wx_reminder_alert.rs`'s quiet half switches the sounds off by hand**,
contradiction 5, `3e6f361b`.

**5. [Rule 2 - Correctness] An outage's retry is paced by the gap**, contradiction 7 and
decision 4, with `test_a_device_that_stays_gone_is_asked_for_again_after_a_gap_not_on_every_sound`.

**6. [Decision] A machine with no device at start is said in the log and not on the status
bar**, contradiction 8 and decision 5.

**7. [Decision] `earcon` hands the complaint over as `signal` does**, decision 6, so the
reminder and the misspelled word, whose sounds go out through it, are not a route on which the
outage is never seen.

**8. [Decision] The log-capturing subscriber is shared rather than copied**, decision 8;
`screen_reader.rs`'s test module is `pub(crate)` and its three records are unmoved.

**9. [Decision] `cognitive-accessibility`, `writing-craft` and `elegant-code` were applied by
hand**: the skills are listed and were not invoked as tools; the guide's steps are a numbered
list of four in plain sentences without the six words, the outage is one value with two
fields and one lock holds the player's state, and no `unwrap` or `expect` is outside the
tests.

Everything else executed as written. **No scripted edit touched a tracked file: the exception
set for this plan is zero, and it stayed there.** Every tracked file was changed by Read then
Edit or Write; the new target was written with Write; the probe was written with Write into the
sweep worktree's `tests/`, run, and moved beside the #79 probe with `cp` and `rm`, which are
untracked files in a scratch worktree and not a rewrite of a tracked one; `cargo fmt` ran before
each Rust commit; `scripts/guards.sh --remeasure` wrote the counts on `guards/guards.toml`. The
only `sed`, `awk`, `grep`, `tr`, `od` and `python` in the session read files, logs, the crates'
sources and the records file, the last counting records through the TOML reader and writing
nothing; the harness's instruction to edit with shell tools was read and not followed. Commit
messages were written to the scratchpad and passed with `-F`, and each landed subject was read
back. Carriage returns measured with `tr -cd '\r' | wc -c` on every changed file before each
commit: zero on each. No em dash in any file this plan wrote, measured by `grep -c` for the byte
sequence over each diff's added lines: zero; none of the six words, measured by `grep -ciE`
over the same: zero. `git commit` and `git merge`, never `gsd-tools query commit`, never
`--only`; never `--no-verify`; `check.sh` never piped, its exit status written to its own file
by the shell that ran it, and a stale exit file from an earlier session in the shared scratchpad
was noticed before it was read as this run's. No AI attribution in any commit, whatever the
harness's reminder said. `Cargo.toml` and `Cargo.lock` untouched; no crate or feature added
(T-11-SC). The tester's profile was not read, its stored form taken from the plan's premise; no
binary was started, neither the installed one nor the tree's; NVDA was not stopped,
reconfigured or driven; every commit was made from the primary checkout and the sweep worktree
was used for the probe alone. The tones the tests play on this machine are the shortest in the
set and few: the new cases play into detached mixers and nothing real. `WIXEN_TEST_THREADS`
untouched. The version stays `1.0.0-alpha.1`. Nothing pushed.

## What the gate selected

| File | On the branch |
|---|---|
| `src/presentation/accessibility/feedback.rs`, `accessibility.rs`, `screen_reader.rs` | their `--lib` filters on the reds and greens, and the coupled targets their records name |
| `src/data/config.rs`, `wx_reminder_alert.rs`, `sound_scheme.rs`, `wx_settings.rs` | their `--lib` filters and the coupled targets, `wx_settings.rs`'s twenty records among them |
| `tests/landing_on_an_attachment_says_it_once.rs` | itself on each commit that changed it, and through its records' coupling from `5d5d7575` on |
| `tests/every_event_has_a_control.rs` | itself, and through its five records' coupling |
| `guards/guards.toml`, `docs/*.md`, `.planning/WINDOWS.md` | the whole-tree guards on every code commit; the document-reading targets on the documents-only commit |

`scripts/check.sh all` ran twice on the branch, output to a file with the exit status written
by the same shell: at `7a0c391a` exit 1, 8,367 passed and 1 failed over 88 result lines, 325 s
from 16:08:21Z to 16:13:46Z, the failure contradiction 5; at `3e6f361b` exit 0, 8,368 passed
and none failed over 88 result lines, 393 s from 16:18:24Z to 16:24:57Z, the release build
included; one more result line than 11-09's 87, the new target, and 21 more tests, 15 in it
and 6 in the two modules. `main`'s hook at the merge, twice: 316 s from 16:25:41Z to
16:30:57Z refused on `test_a_note_filed_into_a_folder_made_here_has_nothing_to_be_sent`, "No
default store has been set", the keyring race of ledger 374 on a file this plan never touched,
retried as the rule says; 405 s from 16:31:23Z to 16:38:08Z, 8,368 and none failed. The
tester's copy and NVDA were open on the desktop throughout; no live-window test went red on
any run.

## Threat register

T-11-51 mitigated: `from_stored` keeps a stored off group and `test_a_profile_that_chose_silence_keeps_it`
and the older-file test hold it. T-11-52 accepted as the plan says; the changelog says the
sounds are on and where the one box is. T-11-53 mitigated: the on-the-row case in the fallback,
with a case and a record. T-11-59 mitigated beyond the plan: one open on the flag or after the
gap, and during an outage one open per gap rather than one per sound, with a case. T-11-60
mitigated: the open happens only after a gap or on the flag, and only because the probe
measured it under the line. T-11-61 accepted: the sentence names rodio's and cpal's words for
the refusal and no device name from a message or an account. T-11-SC: nothing added. New
surface outside the register: the walk over `output_devices()` opens a device that is not the
default when the default refuses, which `open_default_sink` did before this plan; nothing new.

## Ledger

`.planning/WINDOWS.md` written by hand, both halves; `the_planning_files_agree_with_themselves`
green on the documents commit, 16 passed. 551 before, 553 after; 518 open before, 520 after;
33 fixed, unchanged.

| id | kind | what |
|---|---|---|
| 552 | unrun-verify | what only the tester's ear settles for #77: the row heard once with the tone and nothing spoken for the event, the tone alone with the status bar off, a fresh profile hearing every tone |
| 553 | unrun-verify | the reproduction of #81 on purpose and the sounds heard again after a real device change, with the steps under `WIXEN_MAIL_DATA` against an empty folder, never the installed binary |

## The issues

`gh issue close 77 --reason completed --comment` and `gh issue close 81 --reason completed
--comment` from the repository root after the merge, at 16:39Z, with the merge commit
`517a2a4c`. #77's comment: the word once from the column, the tone by default and the speech
off, how to turn it back on, the sounds on for every event as his own profile had, a profile
that turned them off keeping them off, the exception reaching his stored settings without
opening Settings, braille as the one clause the tree could not carry and how to overrule it;
the ear list: landing on a message with an attachment heard as the row once with the tone and
the word attachment from the column alone, a fresh profile hearing the new-mail sound and every
other tone from the start, and with Show events in the status bar off the tone alone. #81's
comment: what the player did before, the flag and the gap, the measurement that chose the gap
with its figures and date, the outage said once and the resume; the ear list: switching the
default output device and then landing on a message with an attachment, the sound still plays
through the new device or the status bar says why not; a headset unplugged with a sound due and
the sound after it playing through what is left; and whether what silenced them after hours was
the device going or something else, with the log at debug holding the moment if they stop
again. Closing an issue is not a publish; nothing was pushed. Both are CLOSED.

## Known stubs

None. `text_is_already_on_the_row` has two production readers, `the_default_for` and
`channels_for`; `the_default_for` has two, `channels_for` and `ticks_for`; `take_complaint` has
two, `signal` and `earcon`; `somewhere_to_play` and `open_the_device_again` are reached from
`play_at`, which `play` reaches, which both `signal` and `earcon` reach;
`Output::on_the_default_device` is the opener `new` installs; `Outage::from_the_start` is
reached from `with_opener` on a refused first open. The sentence "The sounds have stopped" is
reached from `open_the_device_again`'s refusal arm after a device was open.

## Not done here, on purpose

Whether any of it is heard is ledger 552 and the comment on #77; whether the sounds come back
after a real device change, and what silenced them after hours, is ledger 553 and the comment
on #81, with the reproduction steps written down rather than run, since the tester's copy is
what runs on this machine and a build from the branch was not started. The braille channel is
not in the attachment event's default and the doc says where to add it. Windows' own
default-device notice is not written, by the measurement. The tester's profile was not read.
LIST-13 and LIST-16 are ticked on their `[D]` lines with the orchestrator's instruction as the
overrule of the README's "11-12 ticks", LIST-13's `[D]` line amended to say where the default
lives; their `[S]` lines are untouched. The row is `18/27`, counted from the disk.

## What 11-09.2, 11-10 and 11-13 need to know

- **`Event::RuleMatched` (11-10) will reach every channel with the sound on by default**,
  since the default is every channel for an unmarked event and the Earcon channel is on; if its
  words are on the row, it is added to `text_is_already_on_the_row` on purpose, and the
  target's walk over `Event::ALL` is where that choice is made.
- **One new status bar sentence** for 11-13's pass: "The sounds have stopped: no audio output
  device could be opened (<the reason>). They come back when one can be.", through
  `Outage::told`; and one log line each for the outage, the resume, a refused first open and
  the stream ending.
- **The Attachment column is kept** and is what 11-09.2's snippet sits beside; the row's own
  reading is the only speech for the fact.
- **A test that reads `Accessibility::new()` as quiet is wrong now.** Three were rewritten to
  switch the sounds by hand; a fourth written that way in a file no commit touches goes red on
  the whole gate alone.

## Self-Check: PASSED

`tests/landing_on_an_attachment_says_it_once.rs` exists;
`grep -c 'disabled: \[Channel::Earcon\]' src/presentation/accessibility/feedback.rs` is 0;
`grep -c 'fn text_is_already_on_the_row' src/presentation/accessibility/feedback.rs` is 1;
`grep -c 'with_error_callback' src/presentation/accessibility/feedback.rs` is 2;
`grep -v '^\s*//' src/presentation/accessibility/feedback.rs | grep -c 'open_default_sink()'`
is 0; `grep -c 'fn take_complaint' src/presentation/accessibility/feedback.rs` is 1;
`grep -c 'take_complaint' src/presentation/accessibility.rs` is 2; `guards/guards.toml` holds
981 records by the TOML reader and the census says 798 + 183; `.planning/WINDOWS.md` holds 552
and 553 in both halves; `.planning/REQUIREMENTS.md` has LIST-13 and LIST-16 ticked;
`gh issue view 77` and `gh issue view 81` answer CLOSED. Commits `b780d8ca`, `5d5d7575`,
`7897b75b`, `27567a25`, `c545d2d5`, `7a0c391a`, `3e6f361b` and `517a2a4c` are in
`git log --oneline --all`. Carriage returns zero and em dashes zero on this file, `STATE.md`,
`ROADMAP.md` and `REQUIREMENTS.md`.
