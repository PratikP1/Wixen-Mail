---
phase: 11-reading-and-the-list
plan: "11.1.3"
type: execute
wave: 27
depends_on: [11-11.1.2]
files_modified:
  - src/data/message_cache/messages.rs
  - src/application/conversations.rs
  - src/presentation/wx_app.rs
  - src/presentation/message_rows.rs
  - src/presentation/view_state.rs
  - src/presentation/virtual_rows.rs
  - tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs
  - tests/a_conversation_row_stands_for_one_message.rs
  - tests/a_rule_can_change_how_a_row_is_announced.rs
  - tests/the_rows_columns_are_read_on_request.rs
  - tests/all_inboxes_keeps_a_view_of_its_own.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-27]
estimate:
  tokens: 56000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All Inboxes has a view of its own: stored under its own row identity, switched with Ctrl+T like a folder, answering Show conversations by default when nothing was stored, and read at the landing rather than inherited from whatever the last folder left (LIST-27, #92, second half)."
    - "The Thread View check mark says the view of what is on screen, All Inboxes, a folder, a label and a saved search included."
    - "All Inboxes showing conversations lists every inbox's conversations, one row per account and conversation, since a thread id is an account's; a conversation whose messages sit in two accounts is two rows, each counting its own account's messages, and every act on a row reads the row's own account and folder."
    - "A label view and a saved search keep one row per message; Ctrl+T there says so, naming All Inboxes as a place conversations can be shown."
  artifacts:
    - "src/data/message_cache/messages.rs: conversations_in_every_inbox(reach, order) over every inbox grouped by account and thread id, each row carrying where it was read."
    - "src/application/conversations.rs: ConversationItem::read_in with the account and the folder."
    - "src/presentation/wx_app.rs: the identity whose view is kept answering All Inboxes; switch_the_view storing under it; the landing on All Inboxes reading it; load_every_inbox loading conversations when they are shown; conversation_nodes and the two readers of the open folder reading the row's own; the check mark synced at every landing and the saved search's arrival; the refusal sentence for a label and a saved search."
    - "tests/all_inboxes_keeps_a_view_of_its_own.rs: the readings over what ships."
    - "docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md."
  key_links:
    - "landing on All Inboxes -> cache.folder_view(WhichRow::AllInboxes.stored()) -> Showing::from_stored(that, when_nobody_set_one(setting)) -> s.showing, conversations cleared, the check mark -> load_every_inbox -> MessagesLoaded and, when showing conversations, conversations_in_every_inbox -> ConversationsLoaded."
    - "Ctrl+T on All Inboxes -> the identity function answers all-inboxes -> cache.set_folder_view(all-inboxes, toggled) -> the same loads."
    - "a conversation row in All Inboxes -> row.read_in -> messages_in_conversation(thread, account, folder, reach) and conversation_nodes filtered by account -> the act and the tree are the row's own account's."
---

<objective>
All Inboxes shows conversations or messages because somebody chose, not
because of which folder was open before.

Purpose: #92's second half, Pratik's decision of 2026-09-20. Today
`switch_the_view` refuses on All Inboxes ("Open a folder first"), because
`the_folder_being_looked_at` answers nothing for a row that opens no
folder, yet landing on All Inboxes leaves `s.showing` as the last folder
left it and `load_every_inbox` reads messages only, so with a threaded
folder open before, All Inboxes draws the previous folder's conversation
rows under its own title and the check mark says whichever that was. The
tester saw it. The view goes under All Inboxes' own row identity, the key
the tree's collapsed state and the landing already use; it answers the
setting 11-11.1.2 added when nothing was stored; and showing conversations
there lists every inbox's, one row per account and conversation, each row
carrying where it was read so acting on it reaches the right account.

Output: the cache query and the row's `read_in`, the window reading and
storing All Inboxes' view, the check mark following what is on screen, the
label and saved-search sentence, the readings, the pages, the changelog;
closes #92 on the tree side. Its plan number is the string `"11.1.3"`.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-11.1.2-SUMMARY.md
@.planning/phases/11-reading-and-the-list/11-08.1-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `4a09bfc2`, read-only while 11-11.1
executed on its branch, and re-taken the same day against `main` at
`390a580c` with 11-11.1 merged; the lines below are the second reading.
11-11.1.1 and 11-11.1.2 land before this runs and each moves lines in
`wx_app.rs`, so every line below is re-run on the day by the `grep`
given. 11-11.1's summary touches nothing here: `PageHost` is what a
browser surface is and the page window shows no list, `open_links_in` is
read at the route, and the navigating event's lack of an address is the
separate window's business (11-11.2). Command first, then what it said.

**1. The refusal, the landing, and the accident, exactly.**

```
sed -n 15144,15162p src/presentation/wx_app.rs   -> switch_the_view: the_folder_being_looked_at or the refusal "Open a folder first. Conversations are shown a folder at a time." (15158), with the check mark cleared
sed -n 15509,15525p src/presentation/wx_app.rs   -> the_folder_being_looked_at (15519): row.opens().map(stored); its doc: "None for a row that opens no folder at all: All Inboxes, an account branch, a label, a saved search"
sed -n 2947,2951p src/presentation/wx_app.rs   -> WhichRow::AllInboxes: selected_folder set, the title, load_every_inbox, return; s.showing untouched, s.conversations uncleared, no sync_menu_check
sed -n 7565,7589p src/presentation/wx_app.rs   -> load_every_inbox: the_sort_as(Messages), cache.unified_inbox, apply_threading, MessagesLoaded; nothing for conversations
sed -n 1402,1421p src/presentation/wx_app.rs   -> the paint callback reads state.showing and state.conversations per row
sed -n 21744,21751p src/presentation/wx_app.rs   -> tell_the_list_how_many: how_many_rows(showing, messages, conversations)
```

So with a threaded folder open before, All Inboxes is told the previous
folder's conversation count and paints the previous folder's conversation
rows: not All Inboxes threaded, the last folder's rows under All Inboxes'
title. The issue's "shows conversations when the account's inbox is in
Thread View" is that, and worse than it says.

**2. The key, and what "the key the sort uses" is.**

```
sed -n 334,355p src/data/message_cache/folders.rs   -> set_folder_view and folder_view: tree_state(identity, thread_view), keyed by the row identity string, ON CONFLICT update; no schema change for a new identity
sed -n 142,144p src/presentation/folder_tree.rs   -> WhichRow::AllInboxes.stored() is "all-inboxes"
grep -n 'AllInboxes.stored()' src/presentation/wx_app.rs   -> 4774 (View, All Inboxes selects the row by it), 17970 (start_in_all_inboxes lands by it), and three tests
sed -n 15073,15083p src/presentation/wx_app.rs   -> the_sort_as reads one message_columns layout for every listing; #69 made All Inboxes read it, and it is keyed by nothing
```

The issue's "the `WhichRow::AllInboxes.stored()` identity the sort already
uses" is the row identity the tree state and the landing use; the sort has
no per-row key. The view goes under that identity in `tree_state`, beside
the collapsed state, and the Thread column's hand choice
(`folder_thread_column`, keyed the same way) follows for free once the
identity function answers All Inboxes.

**3. Conversations are per account and per folder today, and the row
does not say whose it is.**

```
sed -n 188,222p src/data/message_cache/messages.rs   -> conversation_scope: ?1 the account, ?2 the folder, ?3 whether the reach is the whole account; "here" is the messages of that account within reach whose thread id is in folder ?2
sed -n 2362,2390p src/data/message_cache/messages.rs   -> conversations_in(folder_id, account_id, reach, order)
sed -n 467,483p src/data/message_cache/messages.rs   -> unified_inbox_query: every folder with folder_type = 'Inbox', m.deleted = 0
sed -n 6866,6898p src/data/message_cache/messages.rs   -> test_two_accounts_do_not_share_a_conversation (T-01-47): "acc" and "other" each hold "root@example.com", 2 and 1 messages; the fixture for the two-rows reading
sed -n 200,230p src/application/conversations.rs   -> ConversationItem: thread_id, subject, messages, unread, dates, snippet, ...; no account, no folder
git grep -c 'ConversationItem {' -- src tests   -> 29 literal sites in 9 files: conversations.rs 1, messages.rs 2, message_rows.rs 15, view_state.rs 2, virtual_rows.rs 2, four test files 7
sed -n 13860,13868p src/presentation/wx_app.rs   -> conversation_nodes filters s.messages by thread id alone
grep -n 'the_open_folder_and_its_account(' src/presentation/wx_app.rs   -> 10526 (the fn: None for a saved search or All Inboxes by its doc), 10580 (chosen_messages, 10545: a conversation row's members for every act, refusing with the same sentence at 10581), 24165 (spawn_conversation_text_fetch, 24156, the #31 fetch, silently returning)
sed -n 120,125p src/presentation/ui_types.rs   -> MessageItem::account_id, "taken from the row rather than from whichever account is open"
```

`MessageItem` already carries its account for exactly this reason; the
conversation row gets the same, as one field `read_in` holding the account
and the folder the row was read in, so the twenty-nine literal sites change
by one field and the compiler lists them.

**4. A label and a saved search.**

```
sed -n 2953,2968p src/presentation/wx_app.rs   -> WhichRow::Label: selected_folder, the title, load_messages_with_label; s.showing untouched, like All Inboxes
sed -n 18778,18792p src/presentation/wx_app.rs   -> SavedSearchRan: s.showing = Messages (18789) with the comment "a saved search runs across folders, so its results are a list of messages and not of one folder's conversations"; no sync_menu_check, so the check mark keeps saying the last folder's
sed -n 7516,7529p src/presentation/wx_app.rs   -> load_messages_with_label reads one account's messages under a label, across its folders
```

Both keep one row per message: a saved search by its own comment, a label
because it groups messages of many folders in one account and a
conversation there would be neither one folder's nor every inbox's. The
refusal sentence stays for both, reworded to name All Inboxes; landing on a
label sets Messages and the check mark the way the saved search's arm sets
Messages, and both arms sync the check mark.

**5. Two things nearby that are not this plan's.** `reread_folder_if_open`
(`:18574-18597`) re-reads only a folder whose id matches
`folder_on_screen` (`:18418`), which is None on All Inboxes, so mail
arriving while All Inboxes is open refreshes nothing today in either
view; unchanged here and ledgered. Apply View To Other Folders (`:15318`,
`:15326`) refuses from All Inboxes; unchanged.

**6. The gate for each file, at `390a580c`.**
`src/data/message_cache/messages.rs` 179 tests, 25 records;
`src/application/conversations.rs` 26 tests, 1 record;
`src/presentation/wx_app.rs` 196 tests by `#[test]` and 199 by the
runner, 102 records; `message_rows.rs`, `view_state.rs` (55, 3),
`virtual_rows.rs` (6, 2) and the four test files change a literal each
and no test count; the new target to itself. 1,011 records by the
parser; counts taken 2026-09-20, wx_app's before two plans land on it.

**7. What 11-11.1's summary teaches about the commits here.** Its
handlers went into task 1's green because the read-by-something guard
and clippy leave no green commit holding a field and no reader. Here
`read_in` is read by `conversation_row` and the every-inbox query in the
same file it is added in, so task 1's green holds the field and its first
reader together; the window's readers are task 2's, whose green adds
readers and takes none away.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: every inbox's conversations, one row per account and conversation, each saying where it was read</name>
  <files>src/data/message_cache/messages.rs, src/application/conversations.rs, src/presentation/message_rows.rs, src/presentation/view_state.rs, src/presentation/virtual_rows.rs, tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs, tests/a_conversation_row_stands_for_one_message.rs, tests/a_rule_can_change_how_a_row_is_announced.rs, tests/the_rows_columns_are_read_on_request.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/messages.rs:188-240` (the scope and the one-conversation narrowing), `:278-340` (conversations_query and conversation_row), `:467-483`, `:2362-2390`, `:6866-6898`.
    - `src/application/conversations.rs:195-260`; `src/presentation/view_state.rs:451-480` (the test helper that builds a row).
  </read_first>
  <behavior>
    - `ConversationItem::read_in: ReadIn { account_id: String, folder_id: i64 }`, filled by `conversation_row` from two more selected columns; `conversations_in` selects them from the row's own message (the account of `?1`, the folder `?2`); every literal site gains the field, test helpers with a fixed pair.
    - `MessageCache::conversations_in_every_inbox(reach, order) -> Result<Vec<ConversationItem>>`: the scope is every folder with `folder_type = 'Inbox'`, each account's reach applied per account (the inbox alone, or the whole account under `TheWholeAccount`, as `conversation_scope` does for one), grouped by `(f.account_id, m.thread_id)`, ordered as `conversations_query` orders, then by account and thread id; the `here` set excludes All Mail copies the way the scope does.
    - Cache tests: over `test_two_accounts_do_not_share_a_conversation`'s fixture, `conversations_in_every_inbox(TheWholeAccount, None)` answers two rows for "root@example.com", `read_in.account_id` "acc" with 2 messages and "other" with 1, in that order; a message in a non-inbox folder of "acc" is counted only under `TheWholeAccount`; an account with an empty inbox contributes no row; an inbox row's `read_in.folder_id` is its inbox; `conversations_in`'s rows carry `read_in` equal to the folder and account asked for.
  </behavior>
  <action>
    Red first, on a branch: the cache tests bare, with
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    named in the red since `messages.rs` gains tests and twenty-five
    records name it. Then the field (the compiler lists the twenty-nine
    sites; change each by one field and nothing else), the query as one
    more function beside `conversations_query` sharing its column
    expressions, the method. Invoke `elegant-code`: no second copy of the
    column list; the every-inbox scope is the one-folder scope with the
    folder clause replaced, written once.

    Guard records: one on `messages.rs` whose break groups by thread id
    alone, the two-rows test red; one whose break reads `read_in` from the
    folder asked for rather than the row's, the folder-id test red; each
    measured; the twenty-five records naming `messages.rs` re-measured as
    the count check names them, in the background.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::messages:: && cargo test --lib application::conversations:: && cargo build --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the tests and the count check.
    - `grep -c 'pub read_in' src/application/conversations.rs` is 1; `grep -c 'fn conversations_in_every_inbox' src/data/message_cache/messages.rs` is 1; `cargo build --all-targets` is clean.
    - `cargo test --lib data::message_cache::messages::` passes with 179 plus the added, quoted; `application::conversations::` with 26.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The cache lists every inbox's conversations one row per account, each row says whose it is and where it was read, and the compiler has visited every place a row is built.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All Inboxes' own view, the check mark, and a row acted on as its own account's</name>
  <files>src/presentation/wx_app.rs, tests/all_inboxes_keeps_a_view_of_its_own.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs`: `switch_the_view`, `the_folder_being_looked_at`, `show_the_thread_column_if_it_is_wanted`, the folder landing and the All Inboxes, Label and SavedSearch arms in the tree handler, `load_every_inbox`, `load_folder_conversations`, `conversation_nodes`, `chosen_messages`, `spawn_conversation_text_fetch`, the `SavedSearchRan` and `ConversationsLoaded` arms, `sync_menu_check`; each by `grep -n` on the day.
    - `tests/a_setting_saved_applies_without_a_restart.rs` and `tests/moving_through_the_list_marks_nothing_read.rs`, the readings' shape over `what_ships`.
  </read_first>
  <behavior>
    - One function answers the identity whose view and Thread column are kept: a folder's `stored()` for a row that opens one, `WhichRow::AllInboxes.stored()` for All Inboxes, None for an account branch, a label and a saved search; `switch_the_view` and `show_the_thread_column_if_it_is_wanted` read it, so Ctrl+T on All Inboxes toggles, stores under "all-inboxes" and announces as a folder does, and the Conversations arm loads `conversations_in_every_inbox` there and `load_folder_conversations` in a folder.
    - Landing on All Inboxes reads `folder_view("all-inboxes")` through `Showing::from_stored(that, when_nobody_set_one(setting))`, the setting read at the landing as 11-11.1.2's folder landing reads it; sets `s.showing`, clears `s.conversations` and the kept selection, syncs the check mark; `load_every_inbox` loads messages and, when showing conversations, the every-inbox rows with `SomebodyAskedForThem`.
    - Landing on a label sets Messages, clears, syncs the check mark off; the `SavedSearchRan` arm syncs it off beside the `Messages` it already sets.
    - The refusal in `switch_the_view` for a label and a saved search: "Open a folder or All Inboxes first. A label and a saved search show one row per message."; `chosen_messages` for a conversation row reads the row's `read_in` and no longer refuses on All Inboxes; `spawn_conversation_text_fetch` takes the row's `read_in`; `conversation_nodes` filters `s.messages` by the row's account as well as its thread id, so two rows for one thread id open two trees.
    - `tests/all_inboxes_keeps_a_view_of_its_own.rs`, readings over `what_ships`: the identity function names `AllInboxes`; the All Inboxes arm reads `folder_view` and calls `sync_menu_check`; `load_every_inbox` names `conversations_in_every_inbox`; `switch_the_view`'s refusal sentence names All Inboxes; the Label arm and the `SavedSearchRan` arm call `sync_menu_check`; `conversation_nodes` names `account_id`; `chosen_messages` reads `read_in`; and a hand-built `WxUIState` with two messages of one thread id in two accounts answers one node per account from `conversation_nodes`.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare. Then the identity
    function (replacing `the_folder_being_looked_at`, whose doc changes
    with it), the landing, the loads, the sentence, the three readers of
    the row, with `cargo build` between. Invoke `elegant-code`: the
    All Inboxes landing does what the folder landing does, in the same
    order (view before the mail is asked for, because the count the
    control is told depends on it), through the same functions; no second
    way to sync the check mark.

    Guard records: one on `wx_app.rs` whose break returns `s.showing`
    untouched at the All Inboxes landing, the landing reading red; one
    whose break drops the account filter in `conversation_nodes`, the
    two-accounts case red; each measured.
  </action>
  <verify>
    <automated>cargo test --test all_inboxes_keeps_a_view_of_its_own && cargo test --lib presentation::wx_app:: && cargo test --test a_setting_saved_applies_without_a_restart</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings.
    - `grep -c 'Open a folder first. Conversations are shown a folder at a time' src/presentation/wx_app.rs` is 0; `grep -c 'Open a folder or All Inboxes first' src/presentation/wx_app.rs` is 1; `grep -c 'conversations_in_every_inbox' src/presentation/wx_app.rs` is at least 2 (the landing's load and the switch's); `grep -c 'fn the_folder_being_looked_at' src/presentation/wx_app.rs` is 0.
    - `cargo test --lib presentation::wx_app::` passes with its count quoted (196 plus what earlier plans added); 11-11.1.1's audit reading green (nothing captured at startup).
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>All Inboxes comes up in its own view, Ctrl+T there switches and keeps it, the check mark says what is on screen everywhere, and a conversation row there acts on and opens its own account's messages.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the changelog, the ledger, and the close</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`, Thread View as 11-11.1.2 left it, and any All Inboxes paragraph (`grep -n -i 'all inboxes' docs/USER_GUIDE.md`; none at `390a580c`); `docs/KEYBOARD_SHORTCUTS.md:95-106` (the All Inboxes section) and `:706` (the Thread View row).
    - `docs/changelog.md`, `[Unreleased]`, 11-11.1.2's entry.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/USER_GUIDE.md`, Thread View: All Inboxes
    keeps a view of its own, switched with Ctrl+T like a folder and
    following Show conversations by default until you switch it; a
    conversation whose messages are in two of your accounts is two rows
    there, one per account, because a conversation belongs to an account;
    a label and a saved search show one row per message, and Ctrl+T there
    says so. `docs/KEYBOARD_SHORTCUTS.md:95-106` gains the view sentence
    and `:706` "Kept per folder, and for All Inboxes". The changelog entry
    under `[Unreleased]`, Fixed and Changed: the build, what the tester saw
    (All Inboxes drawing the last folder's conversation rows under its own
    title, the check mark saying whichever), the own view and its key, the
    two-rows rule, the label and saved-search sentence, no version move
    (the counter moves at the first behaviour change after a cut, and none
    has been cut).

    Ledger, by hand, both halves: one `unrun-verify` entry for the tester
    (All Inboxes threaded on a fresh profile, Ctrl+T there kept across a
    folder and back, a two-account conversation heard as two rows); one
    `open` entry that mail arriving while All Inboxes is open refreshes
    nothing in either view (`reread_folder_if_open` re-reads a folder
    only), found while planning #92 and not its fault.

    After the merge, the executor closes the issue from the merge commit:
    `gh issue close 92 --comment "Fixed in <merge commit>, with the first
    half in 11-11.1.2's: All Inboxes keeps a view of its own, stored under
    its own key, switched with Ctrl+T, showing conversations by default
    like a folder, and read when you land on it rather than inherited from
    the last folder; the Thread View check mark says what is on screen
    everywhere. Showing conversations there lists every inbox's, one row
    per account and conversation, so a conversation in two of your
    accounts is two rows, each its own account's, and acting on a row
    reaches that account. A label and a saved search stay one row per
    message and Ctrl+T there says so. Mail arriving while All Inboxes is
    open still does not refresh the list, in either view; that is older
    than this and is ledgered. What only your ear settles: All Inboxes
    threaded on a fresh profile, and the view kept when you come back to
    it."` Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -i 'all inboxes' docs/USER_GUIDE.md` is at least 2; `grep -c 'one per account' docs/USER_GUIDE.md` is at least 1; `grep -c '^version = "1.0.0-alpha.1"' Cargo.toml` is 1; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words; the two ledger entries in both halves.
  </acceptance_criteria>
  <done>The pages say what All Inboxes shows and why a conversation can be two rows there, the changelog names what the tester saw, and the issue is closed with the ear list and the older gap named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| one list over several accounts | a row acted on must reach the account it came from, never the account that happens to be active |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-113 | Elevation of privilege | a conversation row in All Inboxes acted on with the active account, or opened as the other account's messages (T-01-47's shape, one list wider) | critical | mitigate | rows are grouped by account and thread id; `read_in` travels on the row; `chosen_messages`, `spawn_conversation_text_fetch` and `conversation_nodes` read it; the two-rows cache test and the two-trees reading; two records measure the breaks |
| T-11-114 | Tampering | the previous folder's conversation rows drawn under All Inboxes' title | high | mitigate | the landing sets the view and clears the rows before the mail is asked for, in the folder landing's order; a reading holds the arm; a record measures the break |
| T-11-115 | Information disclosure | the check mark saying a view the rows are not in | medium | mitigate | synced at every landing and at the saved search's arrival, through the one `sync_menu_check`; readings hold the four sites |
| T-11-116 | Denial of service | the every-inbox conversation query over every account's mail on the interface thread | medium | mitigate | run where `load_every_inbox` runs today; the scope is the inbox rows unless the reach is the whole account, as one folder's is; its cost on a two-account cache said in the summary beside `unified_inbox`'s |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read, the twenty-five on `messages.rs` in the background.
- What the tester's ear settles: All Inboxes threaded on a fresh profile, the view kept, a two-account conversation as two rows; what the readings prove: the key, the landing, the loads, the sentence, the check mark's four sites, the row's own account.
- After the merge, the executor closes #92 with the comment in task 3.
</verification>

<success_criteria>
- Roadmap criterion 31 closes structurally with 11-11.1.2: All Inboxes' own view, on by default from the setting, switched and kept, the check mark true, the cross-account rows one per account, the label and saved-search sentence, the pages, the changelog.
- LIST-27's `[D]` lines for this plan each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-11.1.3-SUMMARY.md` when done
</output>
