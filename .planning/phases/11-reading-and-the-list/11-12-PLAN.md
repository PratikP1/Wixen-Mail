---
phase: 11-reading-and-the-list
plan: 12
type: execute
wave: 15
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05, 11-06, 11-06.1, 11-07, 11-08, 11-09, 11-09.1, 11-10, 11-11, 11-13]
files_modified:
  - docs/ALPHA_TESTING.md
  - docs/USER_GUIDE.md
  - docs/manual-accessibility-pass.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-01, LIST-02, LIST-03, LIST-04, LIST-05, LIST-06, LIST-07, LIST-08, LIST-09, LIST-10, LIST-11, LIST-12, LIST-13]
estimate:
  tokens: 26000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The alpha page and the user guide describe the list and the reader the phase built, every older sentence kept and dated, and the listening page carries one line per sentence the phase's summaries list as unheard."
    - "Each LIST requirement is read clause by clause against the tree, ticked only when every [D] line has a named test, reading or row, its [S] line left with its ledger numbers; the roadmap, the requirements, the state and the ledger say the same thing."
  artifacts:
    - "docs/ALPHA_TESTING.md, docs/USER_GUIDE.md, docs/manual-accessibility-pass.md, docs/changelog.md: read as one reader would."
    - ".planning/REQUIREMENTS.md: the thirteen LIST requirements ticked with dated closing sentences and their traceability rows; .planning/ROADMAP.md: the criteria closed, the plan list ticked, the progress row; .planning/STATE.md; .planning/WINDOWS.md."
  key_links:
    - "each plan's coverage block -> the closing sentence per [D] line: a name checked to exist in the tree with grep before it is written, 10-07's rule."
    - "each plan's [S] line -> the listening page -> the ledger: one number per unheard sentence, so the tester's list and the record agree."
---

<objective>
Say on the pages what the list and the reader do now, write the listening
lines, and read the phase's ten requirements against the tree clause by
clause.

Purpose: fourteen plans changed what a row says, what a key does, what a
picture shows and where a setting is; the pages a tester reads before the
next build describe the program before them. 10-07's shape: every sentence
read against the tree first, every older sentence kept and dated, and the
closing read a table of names.

Output: the four pages, the listening lines, the ticks, the roadmap's
criteria and row, the state, the ledger, and the phase's closing read in
the summary. Closes the phase.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-07-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef`; every command below is
re-run against `main` as it stands when this plan starts, after the
fourteen merges (11-13, the status sentences, is the last of them, so
this plan reads the bar's words as that pass left them), and the summary
quotes what moved.

**1. What the pages say today about the things this phase changes.**

```
grep -n -i 'mark.*read\|column header\|remote picture\|log level\|folders to keep\|select' docs/ALPHA_TESTING.md | head   -> the known-missing list's rows and the short version, none naming the list's read state, the selection, the columns key or the picture default
grep -n 'Forty\|Fifty\|items' docs/manual-accessibility-pass.md | head -3   -> the count line says fifty-seven after 10-07
grep -n '^## \|^### ' docs/USER_GUIDE.md | head -30   -> the sections each plan wrote into; this plan reads them as one
```

**2. What each plan left for this one.** Each summary's "For 11-12"
section and its coverage block; the ledger entries each plan opened
(`grep -n '^| 5[3-9][0-9] ' .planning/WINDOWS.md` on the day); the
`[S]` lines of LIST-01 to LIST-13.

**3. The state and the roadmap.** `.planning/STATE.md`'s frontmatter and
Current Position as 11-13 left them; the roadmap's phase 11 entry with
its sixteen criteria and the plan list; the progress row at `14/15` before
this plan and `15/15` after.

**4. The gate.** Documents and planning files only: every commit answers
`docs_only` through the hook and runs the document-reading targets;
`the_planning_files_agree_with_themselves` holds the state, the roadmap
row and the ledger's two halves.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: the pages say what the list and the reader do now, and the listening lines</name>
  <files>docs/ALPHA_TESTING.md, docs/USER_GUIDE.md, docs/manual-accessibility-pass.md, docs/changelog.md</files>
  <read_first>
    - The fourteen summaries of this phase, each "For 11-12" section and coverage block.
    - `docs/ALPHA_TESTING.md`, all of it; `docs/USER_GUIDE.md`, the sections each plan wrote; `docs/manual-accessibility-pass.md`, section A and its count line; `docs/changelog.md`, the `[Unreleased]` section as the eleven plans left it.
    - `.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-07-SUMMARY.md`, "What landed", for the manner.
  </read_first>
  <action>
    Every sentence written is read against the tree first; every older
    sentence that a plan falsified keeps its words and gains "Until the
    build of <date>" or a dated correction, in each page's own manner.

    Invoke `writing-craft`. `docs/ALPHA_TESTING.md`: the short version
    says what the list does now (nothing marked read by moving; M; a
    selection; a thread row's message; the columns key; the chooser on
    Tools as a tree; pictures shown by default except pixels; the log at
    Debug under the alpha); the known-missing list gains what nobody has
    heard, one entry per plan's `[S]` line, in the register of the list's
    first item; what would help most gains the walk through an unread
    folder, a selection of many, and a shown picture. `docs/USER_GUIDE.md`:
    read as one reader would from Reading and Managing Email to Rules,
    the paragraphs the plans wrote joined where two say one thing, and
    nothing that describes the program before the phase left undated.
    `docs/manual-accessibility-pass.md`: a subsection at the end of
    section A, "Reading, and the list", one item per sentence the
    summaries list as unheard, each with its technology and its ledger
    numbers, appended and not spliced (10-07's rule, so no item number
    moves), and the count line moved with the correction dated.
    `docs/changelog.md`: the entries this phase wrote read as one; a
    sentence one plan wrote that a later plan falsified is corrected by
    dating in the entry's own manner.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - `docs/manual-accessibility-pass.md`'s section A holds a subsection "Reading, and the list" whose items are numbered from 58, and the count line matches `awk '/^## A\./,/^## B\./' docs/manual-accessibility-pass.md | grep -c '^[0-9]*\. \*\*'`.
    - `grep -n -i 'columns\|Ctrl+Shift+;\|selection\|tracking pixel\|Debug' docs/ALPHA_TESTING.md` finds each on the page.
    - Carriage returns 0 on every page; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages describe the program the phase built, and every unheard sentence has a listening line and a ledger number.</done>
</task>

<task type="auto">
  <name>Task 2: the closing read</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`, the section "Reading, and the list" and the traceability rows for LIST-01 to LIST-13, FOUND-17 and FOUND-18.
    - `.planning/ROADMAP.md`, the phase 11 entry and the progress table.
    - `.planning/STATE.md`, the frontmatter and Current Position; `.planning/WINDOWS.md`, the entries this phase opened.
    - `.planning/phases/10-all-the-mail-and-what-is-said-while-it-comes/10-07-SUMMARY.md`, "The phase's closing read", the table shape.
  </read_first>
  <action>
    For each of LIST-01 to LIST-13: every `[D]` line against the plan's
    coverage block and the tree, each name checked to exist with
    `grep -n 'fn <name>'` or the page's grep before it is written; ticked
    only when every `[D]` line has a name; the closing sentence above the
    lines in the manner 10-07 used, naming the plans and merges; the `[S]`
    line left with its ledger numbers; the traceability row saying
    Complete with the plans, the merges and the numbers. FOUND-17 and
    FOUND-18 were ticked by 11-01 and 11-02 and are not re-read.

    `.planning/ROADMAP.md`, by `Edit`, never a whole-file write: each
    criterion gains its dated closing sentence with the plans and merge
    commits and the ledger numbers; the plan list ticked; the progress row
    `15/15`, Complete, the date; the phase line in the list ticked; the
    milestone paragraph's last sentence brought forward: phase 11 closed
    and groups 5 to 7 still later phases. `.planning/STATE.md` by hand:
    the frontmatter, the Current Position paragraph, `Current Plan: 12`
    and `Total Plans in Phase: 12` at column zero, `progress.completed_plans`
    and `total_plans` counted from the disk, the session lines.
    `.planning/WINDOWS.md`: an entry only for what no open entry says,
    both halves by hand.

    The summary carries the closing read as a table, the phase's final
    counts (records by the TOML reader, the census line, tests on the last
    gate, the ledger's counts, plans and summaries on disk, commits
    unpushed), the "For a person" paragraph, and "What is next": group 5
    from the README's table.
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*LIST-' .planning/REQUIREMENTS.md` is 13 and `grep -c '^- \[ \] \*\*LIST-' .planning/REQUIREMENTS.md` is 0, or the summary says which stayed open and why.
    - The roadmap's phase 11 row reads `15/15` and `the_planning_files_agree_with_themselves` passes.
    - `.planning/STATE.md`'s `progress.total_plans` and `completed_plans` equal `ls .planning/phases/*/*-PLAN.md | wc -l` and the same over `*-SUMMARY.md`.
  </acceptance_criteria>
  <done>The requirements, the roadmap, the state and the ledger say the same thing, and each tick names what closed it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the pages to the tester | a sentence that describes the program before the phase reads as a promise the build breaks |
| the closing read to the requirements | a tick without a name is a claim nobody can re-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-44 | Repudiation | a page sentence falsified by the phase and left standing | medium | mitigate | every sentence a plan falsified kept and dated; the greps in task 1 re-run and quoted |
| T-11-45 | Repudiation | a requirement ticked on a name that does not exist | medium | mitigate | each name grepped before it is written; `the_planning_files_agree_with_themselves` on every commit |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing added; documents only; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook as `docs_only`, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- No guard record changes; no source changes.
- After the merge, no issue is closed by this plan: each earlier plan closed or commented on its own; the summary lists the thirteen issues (#70, #71, #25, #27, #30, #31, #26, #62, #28, #29, #75, #76, #77) and their state by `gh issue view N --json state`.
</verification>

<success_criteria>
- Roadmap criterion 13 closes: the pages, the listening lines, the closing read in the summary and the four planning files; criteria 14 to 16 are read here too, from 11-13, 11-06.1 and 11-09.1.
- Every LIST requirement's `[D]` lines are named and its `[S]` line left.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-12-SUMMARY.md` when done
</output>
