---
phase: 11-reading-and-the-list
plan: 06.2
type: execute
wave: 10
depends_on: [11-06.1]
files_modified:
  - src/presentation/landing_after_a_removal.rs
  - src/presentation/list_arrival.rs
  - src/presentation/mod.rs
  - src/presentation/wx_app.rs
  - tests/tab_from_the_tree_lands_on_the_newest_message.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-20]
estimate:
  tokens: 26000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When the message list takes focus by Tab, F6 or a click and no row is focused, the cursor lands on a row: the remembered row for this folder when there is one and it is still there, else the first row under the sort; Selected and Focused set on the control and the viewport following, so a screen reader reads that row on arrival and once (LIST-20, #87)."
    - "Nothing moves while focus is elsewhere: MessagesLoaded still touches no selection, the folder tree's fix stays, and a list with no rows takes focus without a row and says No messages once."
    - "Which row is decided by one function with cases beside 11-06.1's landing rule, and the wiring is one module a reading drives on a built window."
  artifacts:
    - "src/presentation/landing_after_a_removal.rs: where_to_land_on_arrival(remembered, len) with cases."
    - "src/presentation/list_arrival.rs: wire(list, choose, on_empty) binding the list's SET_FOCUS and landing the cursor when no item is focused."
    - "src/presentation/wx_app.rs: the mail list wired once, the closure over the state's remembered index and the rows' count, the empty sentence on both channels."
    - "tests/tab_from_the_tree_lands_on_the_newest_message.rs: a built tree and list, focus moved from the tree by the list's own SET_FOCUS path, the focused item read back from the control; the companion with no rows."
    - "docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md."
  key_links:
    - "Tab or F6 or a click -> the list's SET_FOCUS -> list_arrival::wire's handler -> no focused item -> choose() -> where_to_land_on_arrival(selected_message_index, messages.len()) -> set_item_state Selected and Focused, ensure_visible, selected_message_index written."
    - "MessagesLoaded -> unchanged: no selection touched while focus is in the tree."
---

<objective>
Tab from the folder tree lands on the newest message, not on a list
with no row under the cursor.

Purpose: #87, the tester on 2026-09-18 under NVDA on `7573a812`: Tab
from the folder tree to the message list lands on the list itself and
reads as "list" with no row; Down then lands on the first row. Opening a
folder loads the rows and, by a stated and right rule, selects nothing
while focus is in the tree; nothing lands a row when focus later
arrives. `put_the_selection_back` is the shape and 11-06.1 uses it after
a delete; this plan uses it when the list takes focus with no focused
row. A plan of its own beside 11-06.1 because that plan is at three
tasks with #83's.

Output: the rule with cases, the wiring module, the mail list wired, the
reading over a built window, the pages and the changelog. Closes #87 on
the tree side; whether the newest message is read once on arrival is the
tester's ear.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-06.1-PLAN.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `4d9f14bf`, working tree clean, before
11-06.1 lands; 11-06.1 rewrites `take_row_out_of_the_list` and the
`MessagesLoaded` arm, so every line below is re-run on the day. Command
first, then what it said.

**1. The rule that selects nothing on load, and the two ways focus
arrives.**

```
sed -n 17745,17758p src/presentation/wx_app.rs   -> MessagesLoaded: "Only the viewport moves: the selection is deliberately not touched, because re-selecting would move focus and take the screen reader's cursor with it"; ensure_visible(chosen) under keep_selected_message_in_view
grep -n 'Pane::List => ' src/presentation/wx_app.rs   -> 1454 (msg_list.set_focus()), 4407 (list.set_focus()): F6 and Escape's way in
grep -n 'SET_FOCUS' src/presentation/wx_app.rs   -> nothing: the mail list binds no focus event; the composer's buttons do (wx_compose.rs:1438, on_set_focus)
grep -n 'SetFocus => set_focus' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/event/window_events.rs   -> 326: the event exists on every window
```

The issue's lines hold, moved by six. Tab is the toolkit's own navigation
and a click is the control's, so the list's focus event is the one path
all three ways in share, which is why the wiring goes on `SET_FOCUS` and
not on a key.

**2. The shape that lands a row, and how a focused item is read back.**

```
sed -n 14396,14420p src/presentation/wx_app.rs   -> put_the_selection_back: set_item_state Selected on each row, Focused on the first, ensure_visible, selected_message_index written
grep -n 'pub fn get_next_item\|pub fn get_item_state\|pub fn set_item_state\|pub fn ensure_visible' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/list_ctrl.rs   -> 531, 520, 485, 604
sed -n 12679,12679p src/presentation/wx_app.rs   -> get_next_item(-1, ListNextItemFlag::All, ListItemState::Selected) is already how the window asks the control; the same with Focused answers the focused item, -1 when none
```

**3. What is remembered, and what an empty list says.**

```
sed -n 172,172p src/data/config.rs; sed -n 733,733p   -> keep_selected_message_in_view, true by default
grep -c 'selected_message_index = ' src/presentation/wx_app.rs   -> 5 sites write it; MessagesLoaded reads it as the row to keep in view
sed -n 12134,12142p src/presentation/wx_app.rs   -> what_a_mailbox_holds: "{n} messages, {u} unread", said when the rows arrive; for an empty folder "0 messages, 0 unread"
```

"The remembered row for that folder" is `selected_message_index` when it
is still inside the rows the list holds, which is what `MessagesLoaded`
already trusts for the viewport; a folder just opened has none, and the
first row is the newest under the default sort and the sort's first row
under any other. An empty list says "No messages" on arrival, one
sentence on both channels, rather than the count line's "0 messages, 0
unread", which is said when the rows arrive and not when focus does.

**4. The gate for each file.** `src/presentation/landing_after_a_removal.rs`
is 11-06.1's new module, `--lib presentation::landing_after_a_removal::`,
its count and records as 11-06.1 leaves them, one function and its cases
added here with the count check named; `src/presentation/list_arrival.rs`
is new, no records; `src/presentation/wx_app.rs` 196 tests by `#[test]`
at `4d9f14bf`, 81 records, none added; the new target to itself. 935
records in all by the parser. Counts taken 2026-09-18.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the rule, the wiring, and the reading that moves focus from the tree to the list</name>
  <files>src/presentation/landing_after_a_removal.rs, src/presentation/list_arrival.rs, src/presentation/mod.rs, src/presentation/wx_app.rs, tests/tab_from_the_tree_lands_on_the_newest_message.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/landing_after_a_removal.rs` as 11-06.1 wrote it, whole.
    - `src/presentation/wx_app.rs:14396-14420`, `:17736-17770`, `:1440-1460`, `:4400-4410`, and the mail list's construction (`grep -n 'let msg_list = ' src/presentation/wx_app.rs`).
    - `tests/deleting_a_message_lands_on_the_next_one.rs` as 11-06.1 wrote it: how it builds a frame and a virtual list, pumps events and reads the focused item; copy its opening.
    - `src/presentation/wx_compose.rs:1430-1445`, the one `on_set_focus` in the tree, for the binding's shape.
  </read_first>
  <behavior>
    - `landing_after_a_removal::where_to_land_on_arrival(remembered: Option<usize>, len: usize) -> Option<usize>`: `None` when `len` is 0; `remembered` when it is `Some(i)` with `i < len`; else `Some(0)`; four cases.
    - `list_arrival::wire(list: &ListCtrl, choose: impl Fn() -> Option<usize> + 'static, on_empty: impl Fn() + 'static)`: binds the list's `SET_FOCUS`, skips the event so the control keeps its own focus handling, and when `get_next_item(-1, All, Focused)` answers -1 calls `choose()`; `Some(row)` sets `Selected` and `Focused` on it and `ensure_visible`; `None` calls `on_empty()`; when an item is already focused nothing happens, which is the case of coming back to the list.
    - The mail list wired once where it is built, with `choose` reading the state's `selected_message_index` and `messages.len()` through the rule and writing `selected_message_index` back when it landed, and `on_empty` saying "No messages" at Normal and on the status line; the PIM lists are not wired here (their lists have their own arrival and are another issue if the tester meets it), said in the summary.
    - The target: a frame with a `TreeCtrl` holding one item and a virtual list of three rows wired through `list_arrival::wire` with a `choose` over a cell; focus set on the tree, then `list.set_focus()` and the events pumped; the focused item read back from the control is row 0 and it is selected; with the cell holding `Some(2)` the focused item is 2; with focus already on row 1 and `set_focus` called again nothing moves; the companion with no rows: no focused item, `on_empty` called once. One `#[test]` per process (`tests/theme_reach.rs`'s budget), the shapes as steps inside it.
  </behavior>
  <action>
    Red first, on a branch: the rule's cases by module path with the
    count check named (11-06.1's records name the module), the target
    bare. Then the rule, the module, the wiring, with `cargo build`
    between. Invoke `elegant-code`: `wire` is one binding and one
    `match`; the landing is `put_the_selection_back`'s three calls in a
    function the two can share if 11-06.1 left one, said either way.

    Guard records: one on `list_arrival.rs` whose break lands the row
    even when an item is already focused, the target's third step red;
    one on `landing_after_a_removal.rs` whose break answers the first row
    when a remembered row is in range, the target's second step red;
    each measured; the records naming the module re-measured when the
    count check names them.
  </action>
  <verify>
    <automated>cargo test --test tab_from_the_tree_lands_on_the_newest_message && cargo test --lib presentation::landing_after_a_removal:: && cargo test --lib presentation::list_arrival:: && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the count check.
    - `grep -c 'fn where_to_land_on_arrival' src/presentation/landing_after_a_removal.rs` is 1; `grep -c 'list_arrival::wire' src/presentation/wx_app.rs` is 1; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'No messages'` is at least 1.
    - The target passes with 1 test, quoted, and the summary quotes the focused item after each step; `presentation::wx_app::` passes with its count unchanged from the day's head, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>Focus arriving on the list with no focused row lands on the remembered row or the first, a list already on a row stays there, an empty list says so, and a built window proves each.</done>
</task>

<task type="auto">
  <name>Task 2: the pages and the close comment</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md`, the section on moving between panes (`grep -n 'F6' docs/KEYBOARD_SHORTCUTS.md | head`) and the message list section.
    - `docs/USER_GUIDE.md`, the reading section's paragraph on the folder tree and the list.
    - `docs/changelog.md`, the `[Unreleased]` section's Fixed entries.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/KEYBOARD_SHORTCUTS.md`: under the pane
    keys, a sentence that Tab or F6 into the message list lands on the
    row you were on in that folder, or on the first row, and that an
    empty folder says No messages; dated. `docs/USER_GUIDE.md`: the same
    in the reading section's words. The changelog entry under
    `[Unreleased]`, Fixed: the tester's words, that the list took focus
    with no row under the cursor because opening a folder selects nothing
    on purpose while focus is in the tree, and that the cursor now lands
    when focus arrives; Known limitations: nobody has heard the row read
    once on arrival, and the other modules' lists are not changed.

    Ledger, by hand, both halves: one `unrun-verify` entry for Tab from
    the tree reading the newest message once, and F6 the same.

    After the merge, the executor closes the issue from the merge commit:
    `gh issue close 87 --comment "Fixed in <merge commit>: Tab, F6 or a
    click into the message list lands on the row you were on in that
    folder, or on the first row under the sort, with the row selected and
    focused so it is read on arrival; opening a folder still selects
    nothing while you are in the tree, and an empty folder says No
    messages. A built window proved the four cases. What only your ear
    settles: the newest message read once on arrival, and not twice."`
    Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - The pages carry the dated sentences; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words.
    - The ledger entry in both halves.
  </acceptance_criteria>
  <done>The pages say where Tab lands, and the issue is closed with the ear list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a focus event to the cursor | a landing on the wrong event moves a screen reader's cursor under the person, which is the fault the tree was fixed for |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-79 | Denial of service | the landing firing on every focus event and re-reading the row | medium | mitigate | the handler lands only when no item is focused; the target's third step and a record hold it |
| T-11-80 | Tampering | a remembered index past the end of a shorter list | low | mitigate | `where_to_land_on_arrival` answers the first row when the index is out of range; a case holds it |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Two guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the newest message read once on arrival; what the built window proves: the four steps.
- After the merge, the executor closes #87 with the comment in task 2.
</verification>

<success_criteria>
- Roadmap criterion 23 closes structurally: the rule, the wiring on the focus event, the built window's four steps, the pages.
- LIST-20's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-06.2-SUMMARY.md` when done
</output>
