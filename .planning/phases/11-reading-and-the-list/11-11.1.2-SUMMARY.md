---
phase: 11-reading-and-the-list
plan: "11.1.2"
subsystem: Thread View on by default, as Show conversations by default on the Reading tab beside Default sort order, on unless turned off; Showing::from_stored taking what a folder never set shows and keeping a stored nought flat; the folder landing reading the setting where the folder opens, never at startup and never per row; the check box after Then by, named on both channels and read back on OK; the D-09 sentences dated in the code and on the pages; guards, changelog, ledger
tags: [thread-view, conversations, settings, reading-tab, show-conversations-by-default, view-state, folder-landing, d-09, ledger-562]

requires:
  - phase: 11-reading-and-the-list
    provides: "11-11.1.1 merged at 051c3529 (main at 0b8fb4d7 when the branch left it); its audit in tests/a_setting_saved_applies_without_a_restart.rs holding every capture in the startup block to a list, which is why the setting is read at the landing; 11-11.1's first-reader rule (the read-by-something guard skips the settings screen, so a field, its first reader and its control are one green); D-09's per-folder view store in folders.rs; 11-04's pattern for a setting (field, older-file test, page reader, a reading in tests/)"
provides:
  - "data::config: AppConfig::show_conversations_by_default, default_true when absent and in the struct's default, in the older-file test with its absent answer held to the struct's; 68 tests as before, 11 records"
  - "presentation::view_state: Showing::when_nobody_set_one(bool) and Showing::from_stored(Option<i64>, Showing), Some(1) conversations, Some(0) messages whatever the setting, None and an unknown number the setting; the #[default] on Messages kept for the list before any row opens, its doc saying so; six D-09 tests where there were five; 56 tests, 4 records"
  - "wx_app.rs: what_a_folder_never_set_shows() beside the_sort_as, load_stored once per landing and never in the startup block; the folder landing handing it to from_stored; the menu's D-09 comment dated; 199 tests by the runner and 196 by #[test] as before, 107 records"
  - "presentation::wx_settings: ReadingTabControls::show_conversations_by_default, built after Then by in the Message List section with the label's words as its name and a description on the channel NVDA reads, ticked from the file, written back in read_the_reading_page; 0 tests as before, 26 records"
  - "data::message_cache::folders: folder_view's doc and the never-set test's message dated; six test calls rewritten for the new signature, each handed the other view so the stored answer is what comes back; 61 tests as before, 7 records"
  - "tests/the_settings_dialog_opens_in.rs: the page-building test finds the check box on the built Reading page by its caption, reads it ticked for a file that says on, clears it the way a click does and reads OK's answer; a reading that the landing reads the setting through the rule and every from_stored call in the window hands it in; a reading that the builder captions and names the box with the same words and the Reading page's readback writes it; 9 tests, 12 records name it"
  - "guards/guards.toml: 1,021 records, census 798 + 223; four new, each measured on the green tree; twelve re-measured at the count check's word, all still right"
  - "docs/USER_GUIDE.md: the Thread View section saying what a folder never set shows, where the setting is, and that it was flat until 2026-09-20; docs/KEYBOARD_SHORTCUTS.md: the Thread View row; docs/ALPHA_TESTING.md: the short version; src/presentation/first_run.rs: one sentence in the first paragraph; docs/changelog.md: the entry naming #92, the build and the decision; .planning/WINDOWS.md: 562"
affects: [11-11.1.3, which reads from_stored as this plan leaves it and hands what_a_folder_never_set_shows() in at the All Inboxes landing, and whose reading in the settings target already requires every from_stored call in the window to do so; 11-13, which reads no new status sentence from here; 11-12, which reads LIST-27's lines and the four pages; whoever opens a folder never set on a fresh profile]

actuals:
  tokens: 13472
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A rule takes a view and never a configuration: the setting is turned into a view at the one place that reads it, and the module's tests hand the view in"
    - "A stored nought is a choice and the absence of a row is not, so a default that changed undoes nobody's choice"
    - "A field, its first reader and its control are one green when one guard wants a reader that acts and another wants a screen that offers"
    - "A test that moves a native control below the toolkit sends the notification the toolkit mirrors state from, or the readback answers the old state"

key-files:
  created: []
  modified:
    - src/data/config.rs
    - src/presentation/view_state.rs
    - src/presentation/wx_app.rs
    - src/presentation/wx_settings.rs
    - src/data/message_cache/folders.rs
    - src/presentation/first_run.rs
    - tests/the_settings_dialog_opens_in.rs
    - guards/guards.toml
    - docs/USER_GUIDE.md
    - docs/KEYBOARD_SHORTCUTS.md
    - docs/ALPHA_TESTING.md
    - docs/changelog.md
    - .planning/WINDOWS.md

key-decisions:
  - "The red's field reads false when absent and false in the struct's default, so the older-file test says what the green must make true and no unrelated test breaks; the green flips both to default_true"
  - "Six D-09 tests where there were five, on and off each their own case, so view_state moved from 55 to 56 and its three records were re-measured rather than folding two answers into one test to hold a count"
  - "The check box is named with set_accessible_name_and_description, the neighbour's shape, the description saying that View, Thread View still chooses for one folder; the name is the label's words on both channels, which is what the plan asked and the reading holds"
  - "The check box's readback is proved in the window session by clearing the real control and asking read_settings, not by a public field: BM_SETCHECK and then the click notification to the parent, since wxWidgets keeps a check box's state itself"
  - "The reading over the window requires every Showing::from_stored call to hand what_a_folder_never_set_shows() in, not exactly one call, so 11-11.1.3's All Inboxes landing is covered on arrival rather than rewriting the reading"
  - "The guide names the setting in the Thread View section, where the guide describes settings beside what they change; it has no Settings section with Reading rows, which the plan's task 2 assumed"
  - "The first-run sentence moved no test: 137 characters under the 900 bound, prose in a constant, and no test pins a sentence of prose that is not a security fact"
  - "The pages in the green rather than task 2 where the green made them true (the guide's section and the changelog), as 11-11.1.1 did; the shortcuts row, the alpha page, the first-run sentence and the ledger in the documents commit"

patterns-established:
  - "A test that pokes a wrapped native control must send the toolkit its notification; observation 741 in the skill log"

requirements-completed: []

coverage:
  - id: D1
    description: "The setting: the field with default true, absent reads on, in the older-file test; the two settings guards red on arrival and green in the one commit that adds the reader and the screen"
    requirement: LIST-27
    verification:
      - kind: unit
        ref: "src/data/config.rs#permission_tests::test_a_settings_file_written_before_these_existed_reads_the_way_it_should"
        status: pass
      - kind: unit
        ref: "src/data/config.rs#every_setting_is_acted_on::test_every_setting_somebody_can_change_is_read_by_something"
        status: pass
      - kind: unit
        ref: "src/data/config.rs#every_setting_is_acted_on::test_every_setting_somebody_can_change_is_offered_by_a_screen"
        status: pass
      - kind: command
        ref: "cargo test --lib data::config:: -> 68; grep -c 'pub show_conversations_by_default' src/data/config.rs -> 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "The rule: when_nobody_set_one and from_stored with the setting's answer, a stored nought flat whatever the setting, an unknown number the setting"
    requirement: LIST-27
    verification:
      - kind: unit
        ref: "src/presentation/view_state.rs#tests::test_a_folder_nobody_has_ever_set_shows_conversations_when_the_setting_is_on"
        status: pass
      - kind: unit
        ref: "src/presentation/view_state.rs#tests::test_a_folder_nobody_has_ever_set_shows_messages_when_the_setting_is_off"
        status: pass
      - kind: unit
        ref: "src/presentation/view_state.rs#tests::test_a_folder_left_flat_comes_back_flat_whatever_the_setting_says"
        status: pass
      - kind: unit
        ref: "src/presentation/view_state.rs#tests::test_a_number_from_a_later_version_is_read_as_the_unset_answer_rather_than_guessed"
        status: pass
      - kind: unit
        ref: "src/data/message_cache/folders.rs#tests::test_setting_a_folder_flat_again_reads_back_flat_rather_than_never_set"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'a folder chosen flat stays flat whatever show conversations by default says' at 2 red on the library, measured 2026-09-20"
        status: pass
      - kind: command
        ref: "cargo test --lib presentation::view_state:: -> 56; --lib data::message_cache::folders:: -> 61; grep -c 'fn when_nobody_set_one' src/presentation/view_state.rs -> 1"
        status: pass
    human_judgment: false
  - id: D3
    description: "The landing: the setting read where the folder opens through what_a_folder_never_set_shows, handed to from_stored, captured nowhere at startup and read on no row"
    requirement: LIST-27
    verification:
      - kind: integration
        ref: "tests/the_settings_dialog_opens_in.rs#test_the_folder_landing_reads_show_conversations_by_default_where_the_folder_opens"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_every_setting_read_at_startup_has_a_disposition_the_tree_honours"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'the folder landing hands the rule what show conversations by default says, not a constant' at 1 red on the target, measured 2026-09-20"
        status: pass
      - kind: command
        ref: "cargo test --test a_setting_saved_applies_without_a_restart -> 20; --lib presentation::wx_app:: -> 199; grep -n show_conversations_by_default src/presentation/wx_app.rs -> 15130 and 15132, both inside what_a_folder_never_set_shows, outside the startup block at 1292-1444"
        status: pass
    human_judgment: false
  - id: D4
    description: "The screen: the check box after Then by in the Message List section, captioned and named with the same words, ticked from the file, written back on OK"
    requirement: LIST-27
    verification:
      - kind: integration
        ref: "tests/the_settings_dialog_opens_in.rs#test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown"
        status: pass
      - kind: integration
        ref: "tests/the_settings_dialog_opens_in.rs#test_the_check_box_carries_its_words_on_both_channels_and_is_written_back_on_save"
        status: pass
      - kind: integration
        ref: "tests/the_sort_controls_sit_together.rs#test_then_by_is_the_tab_stop_after_default_sort_order"
        status: pass
      - kind: integration
        ref: "tests/every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built.rs#test_every_checkbox_on_a_later_page_answers_check_button_after_set_selection"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'ok writes show conversations by default back from the check box' at 2 red and 'the show conversations by default check box is named with its own words on the channel nvda reads' at 1 red, on the target, measured 2026-09-20"
        status: pass
      - kind: command
        ref: "cargo test --test the_settings_dialog_opens_in -> 8 passed, 1 ignored (the release binary, run by nobody here); --test every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built -> 3; --test the_sort_controls_sit_together -> 1; --test wired -> 77; grep -c 'Show conversations by default' src/presentation/wx_settings.rs -> 2; grep -c show_conversations_by_default src/presentation/wx_settings.rs -> 7"
        status: pass
    human_judgment: false
  - id: D5
    description: "The pages dated, the changelog naming the build and the decision, the ledger, no version move"
    requirement: LIST-27
    verification:
      - kind: command
        ref: "grep -c 'Show conversations by default' docs/USER_GUIDE.md -> 2; grep -c 'until 2026-09-20' docs/USER_GUIDE.md -> 1; grep -c 'Thread View' src/presentation/first_run.rs -> 1; grep -c '^version = \"1.0.0-alpha.1\"' Cargo.toml -> 1; grep -c 'is flat' src/presentation/view_state.rs src/data/message_cache/folders.rs -> 0 and 0, the sentences now 'was flat until 2026-09-20' and 'Until 2026-09-20 that was flat'; cargo test --lib presentation::first_run:: -> 16; --test house_style -> 74; --test the_words_that_say_nothing -> 9; --test docs_links -> 6; --test the_planning_files_agree_with_themselves -> 16; carriage returns 0 on every file by tr -cd '\\r' | wc -c; em dashes 0 and the six words 0 in every added line; ledger 562 in both halves"
        status: pass
    human_judgment: false
  - id: S1
    description: "That a folder never set is heard as conversations on a fresh profile, that turning the setting off makes the next folder flat without a restart, that a folder set flat stays flat, and that the box is heard by its name and state after Then by, is the tester's ear"
    requirement: LIST-27
    verification: []
    human_judgment: true
    rationale: "Ledger 562; the comment on #92 lists it; nothing here has been heard, and the tester's copy is what runs on this machine"

duration: 71min
completed: 2026-09-20
status: complete
---

# Phase 11 Plan 11.1.2: A folder never set shows conversations, as a setting on the Reading tab Summary

**A folder nobody has set shows one row per conversation, because what such a folder shows
is a setting, Show conversations by default, on the Reading tab beside Default sort order and
on unless somebody turns it off: Pratik's decision of 2026-09-20 on #92 with his amendment
the same day. The rule takes the setting's answer as a value and keeps a stored nought flat,
so a folder somebody chose flat stays flat and a folder set through View, Thread View keeps
its choice whatever the setting says; the folder landing reads the setting as the folder
opens, so a change applies to the next folder with no restart and the startup block captures
nothing, which 11-11.1.1's audit holds; a settings file from before this build reads as on.
The check box sits after Then by, is named with its own words on both channels, and OK writes
it back. The pages say what a folder never set shows and that it was flat until 2026-09-20.
Nobody has heard any of it; #92 stays open for All Inboxes' own view, which is 11-11.1.3.**

## Performance

- **Duration:** 71 min from the branch at 07:56:38Z on 2026-09-20, after the reading of the
  README, the plan, `CLAUDE.md`, the workflow, and the summaries (11-11.1.1's and 11-11.1's in
  full, the rest by their handoff sections), to the merge's hook finishing at 09:07:21Z; the
  comment on #92 at 09:07:52Z; this summary and the planning files after. About 12 min 41 s
  was guard measurement in three foreground runs: 219 s for the four new records (79 s and
  6 s reading what already fails, then 82 s, 18 s, 18 s, 14 s), 374 s for the three
  `view_state` records the count check named (86 s reading, then 85 s, 104 s, 100 s), 154 s
  for the nine settings-target records it named (18 s reading, then 13 s, 14 s, 5 s, 14 s,
  25 s, 22 s, 16 s, 12 s, 12 s). About 18 min the three hook runs on the branch (352 s red;
  560 s green; 172 s docs), every one green on its first run; 430 s the whole gate, from
  08:52:25Z to 08:59:35Z, green on its first; 426 s `main`'s hook at the merge, from
  09:00:15Z to 09:07:21Z, green on its first. No live-window test went red on any run and the
  keyring race of ledger 374 was not seen, with the tester's copy and NVDA open throughout.
- **Started:** 2026-09-20T07:56:38Z
- **Merged:** 2026-09-20T09:07:21Z at `ae0fa4d2`
- **Tasks:** 2
- **Files modified:** 13, none created; `Cargo.toml` and `Cargo.lock` untouched by
  `git diff --stat 0b8fb4d7..1d86667 -- Cargo.toml Cargo.lock` (T-11-SC), no crate and no
  feature added
- **Actuals:** `tokens: 13472` is `git diff 0b8fb4d7..1d86667 | wc -c`, 53,889 characters
  over four; the estimate's `tokens` was 43,000 and `raw_tokens` 100,000, so the factor is
  0.31 against the former and 0.13 against the latter.

## What landed

**Task 1.** `config.rs`: `show_conversations_by_default` after `default_sort_order`, its doc
dating the decision, `default_true` when absent and in the struct's default, the older-file
test taking it out and holding the absent answer to on and to the struct's. `view_state.rs`:
`when_nobody_set_one(bool)`, `from_stored(Option<i64>, Showing)` with `Some(1)`, `Some(0)` and
the rest as the plan's behaviour says, both `const`; the type's doc, the default variant's doc
and the rule's doc dated; six D-09 tests. `wx_app.rs`: `what_a_folder_never_set_shows()`
beside `the_sort_as`, `load_stored` once and the struct's default when the file cannot be
read; the landing handing it in; the menu's comment dated. `wx_settings.rs`: the field, the
box after Then by with `"&Show conversations by default"`, `set_accessible_name_and_description`
with the label's words and one sentence, ticked from the file, added to the Message List
section; the readback in `read_the_reading_page`. `folders.rs`: `folder_view`'s doc and the
never-set test's message dated; the six test calls each handed the other view; the
per-folder test asserting `None` for the other folder outright. The target's page-building
test and its two readings, with `child_labelled`, `is_checked` and `set_checked` in
`windows_of`. Four records measured, twelve re-measured. The guide's Thread View section and
the changelog entry.

**Task 2.** The shortcuts page's Thread View row, the alpha page's short version, the first-run
screen's first paragraph, the guide's sentence reworded so the setting's name appears twice
and the date once as the plan's greps ask. Ledger 562 in both halves.

## Honest RED and GREEN

One red, one green and a documents commit on branch `show-conversations-by-default` from
`main` at `0b8fb4d7`.

`0aa1b9db`, the red, 352 s through the hook in `red` mode on its first run. Nine named, each
red for the reason intended, quoted from the run: the read-by-something guard, "1 setting(s)
can be changed and are read by nothing"; the offered-by-a-screen guard, "1 setting(s) are
stored and survive a restart and no screen offers any of them"; the older-file test, "an
absent key answered no, so everybody upgrading would go on seeing every folder they never set
as flat", since the red's field read false when absent; two of the six D-09 tests against
stubs answering flat whatever they were handed (on-setting, and the unknown number); the
page-building test, "the built Reading page holds no check box labelled"; the landing
reading, "fn what_a_folder_never_set_shows( is not in the source"; the builder reading, on the
missing caption; and the count check, named bare, since the target went from 7 to 9 under
nine records and `view_state.rs` from 55 to 56 under three. Green on arrival and said: the
two D-09 tests that hold the old answer (off means messages, a stored nought stays flat), the
two kept tests, and `folders.rs` at 61 with its calls rewritten for the signature. The
landing handed the rule a constant so the tree built.

`478f5885`, the green, 560 s on its first run: the absent answer and the default flipped to
true, the two functions, the reader and the landing, the box, the name, the readback, four
records measured and twelve re-measured, the guide's section and the changelog. Between
writing and committing, two things the runner found: the readback reading looked in
`read_settings`, which delegates per page, and the Reading page's function is
`read_the_reading_page`; and clearing the box with `BM_SETCHECK` left `read_settings`
writing `true`, because wxWidgets keeps a check box's state in a field of its own and reads
the control only when the parent receives `BN_CLICKED`, so the test sends that notification
after the message, the way a click would.

`1d86667`, the documents and the ledger, 172 s.

Under the TDD gate's own terms, `test(11-11.1.2)` precedes `feat(11-11.1.2)`. The first-run
sentence in the documents commit changed a constant of prose and moved no test.

## Guard records

1,017 by the TOML reader before, 1,021 after: four new, none rewritten, none retired; the
census 798 + 219 before, 798 + 223 after. `scripts/guards.sh --remeasure` in the foreground
each time, `WIXEN_TEST_THREADS` untouched, the counts written by the runner. Every run was
under `WIXEN_MAIL_DATA` on an empty folder, since the target builds the settings dialog,
which reads the profile.

| Record | File | Break | Red | Timed |
|---|---|---|---|---|
| a folder chosen flat stays flat whatever show conversations by default says (new, library) | `view_state.rs` | `Some(0) => when_unset,` | 2: the rule's flat-keeps-flat test and the cache test that writes a nought and reads it back handed the other view | 82 s |
| the folder landing hands the rule what show conversations by default says, not a constant (new, `suite` the target) | `wx_app.rs` | `view_state::Showing::Messages,` in place of the call | 1: the landing reading | 18 s |
| ok writes show conversations by default back from the check box (new, `suite` the target) | `wx_settings.rs` | `let _ = w.show_conversations_by_default.get_value();` | 2: the page-building test and the builder reading | 18 s |
| the show conversations by default check box is named with its own words on the channel nvda reads (new, `suite` the target) | `wx_settings.rs` | the name shortened to `"Show conversations",` | 1: the builder reading | 14 s |

Every first draft of a red list was a prediction and the runner agreed with each. The twelve
the count check named at the red, re-measured at the green and all still right: on
`view_state.rs` at 56, "the message list is told how many conversations it is showing" (1),
"switching the view back selects the messages that were selected, not their whole
conversations" (2) and "an arrival repaints the rows that changed and not the ones that did
not" (3), 85 s, 104 s and 100 s; on the target at 9, the nine records from "the harness can
still parse the settings built line the application writes" to "the sentence saying the log
level takes effect at the next start is on the advanced page", 1 red each, 5 s to 25 s.
`wx_app.rs` 199 tests by the runner and 196 `#[test]` attributes by `grep -c`, as before, 107
records; `config.rs` 68 and 11; `wx_settings.rs` 0 and 26; `view_state.rs` 56 and 4;
`folders.rs` 61 and 7; `first_run.rs` 16 and 3; the target 9 and 12. The count check printed
its remedy once, at the red (named); it was run in the foreground at the green in three
runs and read before the commit.

## What the tree contradicted

Every command in the plan's four premises was re-run against `main` at `0b8fb4d7` before the
branch. The lines had moved with 11-11.1.1 (the landing's comment at `wx_app.rs:3131`, the
`from_stored` call at `:3139`, the default at `:647`, the menu comment at `:6823`,
`build_reading_tab` at `wx_settings.rs:1590`, the readback at `:3555`, `read_by_something` at
`config.rs:2595` and `offered_by_a_screen` at `:2972` as the plan said); the shapes held,
but for these:

1. **`read_settings` reads each page through a function of its own**, and the Reading page's
   readback is in `read_the_reading_page`; the plan's `:3530-3540` pointed at the right lines
   under the wrong function name, and the reading written from the plan looked in the wrong
   body until the runner said so.
2. **`BM_SETCHECK` moves the control and not the toolkit.** wxWidgets keeps a check box's
   state in `m_state` and reads the control only in `wxCheckBox::MSWCommand` on `BN_CLICKED`,
   so a test that clears the box with the message alone reads the old value back through
   `read_settings`; the test sends the notification to the parent after the message.
   Observation 741 in the skill log.
3. **The guide has no Settings section with Reading rows.** Its headings are the modules and
   the acts, and a setting is described beside what it changes (Open links at `:280`, the
   picture switches at `:326`); the plan's "the Settings section's Reading rows gain the
   setting" had nothing to add to, and the Thread View section names the setting and where it
   is instead.
4. **`view_state.rs` moved from 55 to 56**, because the plan's behaviour lists four new names
   and two kept where there were five, while its acceptance quotes 55; the orchestrator's
   brief said to expect it, and the three records were re-measured.
5. **The plan's "grep -c 'is flat' ... counts only dated sentences" answers 0 in both files**:
   the dated sentences say "was flat until 2026-09-20" and "Until 2026-09-20 that was flat",
   which the plan's grep does not match; quoted above.
6. **`wx_settings.rs` is named by 24 records, not the plan's 22**, and 26 now; `wx_app.rs` by
   106, not 102, and 107 now; the target by 9, and 12 now. The plan's premise 4 counted at
   `390a580c`, before 11-11.1.1's six records.

## Deviations from Plan

**1. [Decision] The red's field with a plain absent answer of false**, decision 1: the plan's
"the field alone turns the two guards red" holds, and the older-file test is red in the same
commit for the absent answer the green makes true.

**2. [Decision] Six D-09 tests**, decision 2 and contradiction 4.

**3. [Decision] Name and description in one call**, decision 3: the plan's
`set_accessible_name`; the neighbour `Start in All Inboxes` carries a description and this
box's consequence, that View, Thread View still chooses for one folder, is worth one sentence
after the name.

**4. [Rule 1 - Bug] The click notification sent after `BM_SETCHECK`**, decision 4 and
contradiction 2; a fault in the test, found by running it.

**5. [Rule 3 - Blocking] The readback reading over `read_the_reading_page`**, contradiction 1.

**6. [Decision] The window reading over every `from_stored` call rather than exactly one**,
decision 5, so 11-11.1.3 adds a caller without rewriting the reading.

**7. [Decision] The guide's sentence in the Thread View section**, decision 6 and
contradiction 3.

**8. [Decision] The first-run sentence with no red**, decision 7: prose in a constant, under
the bound, pinned by nothing.

**9. [Decision] The pages in the green where the green made them true**, decision 8.

The skills the plan names (`elegant-code` for the rule and the box, `writing-craft` for the
pages) are listed and were not invoked as tools; the rule is two `const fn`s with a `match`
each and takes a view, the reader is one function with one `load_stored`, the box is built
the way its neighbours are with no second sizer shape, and the guide's paragraph is one idea
a sentence.

Everything else executed as written. **No scripted edit touched a tracked file: the
exception set for this plan is zero, and it stayed there.** Every tracked file was changed by
Read then Edit or Write; `cargo fmt` ran before each Rust commit; `scripts/guards.sh
--remeasure` wrote the counts on `guards/guards.toml`. The only `sed`, `awk`, `grep`, `tr`,
`wc`, `tail`, `cut` and `python` in the session read files, logs, the records file (the
Python counting records through the TOML reader and writing nothing) and the observation log
outside the tree; the harness's instruction to edit with shell tools was read and not
followed. Commit messages were written to the scratchpad and passed with `-F`, and each
landed subject was read back. Carriage returns measured with `tr -cd '\r' | wc -c` on every
changed file before each commit: zero on each. No em dash in any file this plan wrote,
measured by `grep -c` for the byte sequence over the added lines: zero; none of the six
words, measured by `grep -ciE`: zero. `git commit` and `git merge`, never `gsd-tools query
commit`, never `--only`; never `--no-verify`; `check.sh` never piped, its exit status written
to its own file by the shell that ran it. No AI attribution in any commit, whatever the
harness's reminder said. `Cargo.toml` and `Cargo.lock` untouched; no crate or feature added
(T-11-SC). The tester's profile was not read. No binary was started, neither the installed
one nor the tree's; the one ignored test in `the_settings_dialog_opens_in` that would start
it stayed ignored; NVDA was not stopped, reconfigured or driven. Every commit was made from
the primary checkout and no linked worktree was used. `WIXEN_TEST_THREADS` untouched;
`WIXEN_MAIL_DATA` set to an empty scratchpad folder for the test and guard runs and nothing
else. The version stays `1.0.0-alpha.1`. Nothing pushed.

## What the gate selected

| File | On the branch |
|---|---|
| `src/data/config.rs` | its `--lib` filter and, through the records' coupling, the eleven targets those records name |
| `src/presentation/view_state.rs`, `wx_app.rs`, `wx_settings.rs` | their `--lib` filters and the forty-odd coupled targets the hook listed, `wired` and `a_setting_saved_applies_without_a_restart` among them |
| `src/data/message_cache/folders.rs` | its `--lib` filter and its seven records' targets |
| `src/presentation/first_run.rs` | its `--lib` filter and the whole-tree guards |
| `tests/the_settings_dialog_opens_in.rs` | itself on each commit that changed it, and through its records' coupling |
| `guards/guards.toml`, `docs/*.md`, `.planning/WINDOWS.md` | no scoped target; the whole-tree guards on every code commit |

`scripts/check.sh all` ran once on the branch at `1d86667`, output to a file with the exit
status written by the same shell: exit 0, 8,633 passed and none failed over 95 result lines,
430 s from 08:52:25Z to 08:59:35Z, the release build included; the same 95 result lines as
11-11.1.1's and 3 more tests, one in `view_state` and two in the target. `main`'s hook at the
merge, 426 s from 09:00:15Z to 09:07:21Z, the same 8,633 and none failed, green on its first
run. The tester's copy and NVDA were open on the desktop throughout; no live-window test went
red on any run and the keyring race of ledger 374 was not seen.

## Threat register

T-11-110 mitigated: `Some(0)` answers `Messages` whatever `when_unset` says, the rule's test
and the cache's test hold it with the setting on, and a record measures the break at 2 red.
T-11-111 mitigated: the setting is read once per landing inside `what_a_folder_never_set_shows`,
beside the folder's own stored view; nothing in the paint callback and nothing in the startup
block, held by 11-11.1.1's audit (20 passed after the change) and by the landing reading,
whose record breaks the landing. T-11-112 accepted as the plan says: an unknown number
answers the setting, said in `from_stored`'s doc and held by the later-version test. T-11-SC:
no crate added. New surface outside the register: none; the check box writes a bool the
landing already reads through the same file every other setting is read from.

## Ledger

`.planning/WINDOWS.md` written by hand, both halves, no backslash;
`the_planning_files_agree_with_themselves` green after, 16 passed. 561 before, 562 after;
527 open before, 528 after; 34 fixed, unchanged.

| id | kind | what |
|---|---|---|
| 562 | unrun-verify | what only the tester's ear settles for the first half of #92: a folder never set opened on a fresh profile and heard as conversation rows with the check mark on; the setting turned off, OK, and the next folder never set heard as one row per message without a restart; a folder set flat by hand still flat with the setting on; the check box heard by its name and state after Then by |

## The issue

`gh issue comment 92` from the repository root after the merge, at 2026-09-20T09:07:52Z: the
plan's sentence with the merge commit `ae0fa4d2`, what the tree holds, and the ear list. The
issue stays open for 11-11.1.3, which closes it. Commenting is not a publish; nothing was
pushed.

## Known stubs

None. `grep -n 'TODO\|FIXME\|placeholder\|coming soon\|not available'` over the files this
plan changed answers nothing added by it. The field is read by `what_a_folder_never_set_shows`,
which the landing calls on every folder opened; the box is built on the Reading page and its
value written back by the page's readback, which `read_settings` calls when the page was
built and which the window's Settings-saved arm saves; `when_nobody_set_one` has one non-test
caller and `from_stored` one in the window and six in the cache's tests.

## Requirements

LIST-27 is not ticked: its one `[D]` line covers both halves of #92 and names 11-11.1.3's
work beside this plan's. The evidence line records what 11-11.1.2 held, dated at `ae0fa4d2`,
its `[S]` line untouched, and the traceability row says in progress. 11-12 still reads it.
The roadmap's plan line for 11-11.1.2 is ticked and the row is `26/31`, counted from the disk.

## What 11-11.1.3, 11-13 and 11-12 need to know

- **`Showing::from_stored(stored, when_unset)` takes the answer for a folder never set**, and
  `what_a_folder_never_set_shows()` in `wx_app.rs` is the one place that turns the setting
  into one; the All Inboxes landing hands it in the same way, and the reading
  `test_the_folder_landing_reads_show_conversations_by_default_where_the_folder_opens` in
  `tests/the_settings_dialog_opens_in.rs` already requires every `Showing::from_stored(` call
  in the window's shipping half to pass `what_a_folder_never_set_shows()`, so a landing that
  hands a constant fails on arrival and one that hands the call needs no change to the reading.
- **A stored nought is flat whatever the setting says**, so All Inboxes' own key holding a
  nought means somebody chose flat there and the setting does not override it.
- **The check box is `ReadingTabControls::show_conversations_by_default`**, private; a test
  that wants to clear it does so through the control with `windows_of::set_checked`, which
  sends the click notification as well.
- **No new status sentence** for 11-13's pass: the box's name and description are a control's
  words on the settings screen, and nothing announces on a save.
- **The tester's ear is ledger 562**, and the guide's Thread View paragraph is what 11-12
  reads for it.

## Self-Check: PASSED

`grep -c 'pub show_conversations_by_default' src/data/config.rs` is 1; `grep -c 'fn
when_nobody_set_one' src/presentation/view_state.rs` is 1; `grep -c 'Show conversations by
default' src/presentation/wx_settings.rs` is 2 and `grep -c show_conversations_by_default`
there is 7; `guards/guards.toml` holds 1,021 records by the TOML reader and the census says
798 + 223; `.planning/WINDOWS.md` holds 562 in both halves; `gh issue view 92` answers OPEN
with two comments. Commits `0aa1b9db`, `478f5885`, `1d86667` and `ae0fa4d2` are in `git log
--oneline --all`. Carriage returns zero and em dashes zero on this file, `STATE.md`,
`ROADMAP.md` and `REQUIREMENTS.md`.
