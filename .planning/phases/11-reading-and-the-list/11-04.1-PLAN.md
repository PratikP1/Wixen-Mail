---
phase: 11-reading-and-the-list
plan: 04.1
type: execute
wave: 5
depends_on: [11-04]
files_modified:
  - src/presentation/wx_reader.rs
  - src/presentation/wx_app.rs
  - tests/attachments_are_reached_with_alt_a_in_both_views.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-15]
estimate:
  tokens: 26000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Alt+A reaches the attachments of an open message in both views, the plain-text reader and the formatted page window, and Alt+A again goes back; F8 is retired in both, kept only where it means something else (Columns in the main window, the toolbar in the composer) (LIST-15, #84, Pratik's decision of 2026-09-18)."
    - "In the formatted page window the key reaches the window through the injected script, the way Escape and F6 already do, because a KEY_DOWN binding on a WebView never fires while the browser has focus; the handler moves focus and says where it landed; the binding that never fired is gone."
    - "The sentence read on open says Alt+A for them, and the pages say the same, with F8 dated as retired."
  artifacts:
    - "src/presentation/wx_reader.rs: the Attachments item on Alt+A both ways; src/presentation/wx_app.rs: the page window's script posting Alt+A and F7, the handler moving focus and speaking, the KEY_DOWN binding on the page gone, the open sentence."
    - "tests/attachments_are_reached_with_alt_a_in_both_views.rs: the script names Alt+A and F7, the handler acts on them, the reader's item carries Alt+A, F8 is on neither window."
    - "docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md."
  key_links:
    - "the page's keydown listener -> post({kind, key}) -> on_script_message_received -> the attachments list's set_focus and an announcement: the route Escape and F6 already take (wire_the_way_out), extended by two keys."
    - "the reader frame's accelerator -> ID_GO_ATTACHMENTS: a native frame, where a menu accelerator works as written; only the key changes."
---

<objective>
Make Alt+A reach the attachments of an open message in both views and
retire F8 there, so the default reading view stops promising a key that
never arrives.

Purpose: #84, the tester on 2026-09-18 under NVDA on `744d05ef`: in an
open message F8 does not reach the attachments. Under the Formatted style,
the default, the message opens in the page window, which binds F7 and F8
as KEY_DOWN on the WebView itself; a WebView2 control delivers no wx key
event while the browser has focus, which is the fault the preview met with
Escape and F6 and answered with an injected script. The page window has
that script and it posts only Escape and F6. So F8 never left the page
under Formatted, and the sentence "F8 for them" was false there. Pratik's
decision: Alt+A in both windows, F8 retired there.

Output: the reader's item on Alt+A, the page window's script and handler,
the reading, the pages. Closes #84 on the tree side; that Alt+A lands on
the list and NVDA says where is his ear's. Early, after 11-04, because it
is a regression-class fault in the default reading view.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `eb5d8517` (11-03 merged), working
tree clean, before 11-04 lands; 11-04 adds log lines elsewhere in
`wx_app.rs`, and every line here is re-taken on the day. Command first,
then what it said.

**1. Two views, two routes, one key that reaches one of them.**

```
sed -n 316,317p src/presentation/wx_reader.rs   -> append_item(ID_GO_ATTACHMENTS, "&Attachments\tF8", ..) on the reader frame's Go menu, a native frame with a rich text control
sed -n 618,660p src/presentation/wx_reader.rs   -> F8 both ways, Enter on a row saves; this path works as written
grep -n 'fn show_conversation_as_page' src/presentation/wx_app.rs   -> 20976, the page window a single message opens into under Formatted (open_single_message at 12447 calls it with one part) and a conversation opens as
sed -n 21201,21230p src/presentation/wx_app.rs   -> page.bind_internal(EventType::KEY_DOWN, ..) matching 346 (F7) and 347 (F8) on the WebView itself
grep -n 'F8 for them' src/presentation/wx_app.rs   -> 21264, the sentence read on open
grep -n 'wire_the_way_out(' src/presentation/wx_app.rs   -> 1412 (the preview), 21079 (the page window); 11959 the function
sed -n 11972,11992p src/presentation/wx_app.rs   -> the injected keydown listener posts Escape and F6 (with back for Shift+F6) and nothing else
```

The issue's `:20941`, `:21144-21160`, `:21201-21226` and `:21263` are
`20976`, the list beside the page, `21201-21230` and `21264` at this
commit. A WebView2 control does not deliver wx key events while the
browser has focus; the binding at `21201` has never fired under
Formatted, which is why the tester met a dead key.

**2. The composer's page already posts a key of its own, which is the
shape.**

```
grep -n "kind: 'toolbar'" src/presentation/editor_document.rs   -> 311: the editor page posts F8 back to its window as a message
```

**3. Alt+A is free on the reader frame and on the page window.**

```
sed -n 300,325p src/presentation/wx_reader.rs   -> the reader's menus are &File and &Go; the items' chords are Ctrl+W, Esc, Ctrl+Down, Ctrl+Up, F8, Ctrl+F; no Alt chord and no menu on A
grep -n 'Alt+A' docs/KEYBOARD_SHORTCUTS.md   -> 145 (the due window), 529 and 578 (the main window's Action menu), 852 and 908 (the composer's Attach File): different windows, none of them the reader or the page window
```

Alt+A opens the Action menu in the main window and attaches a file in the
composer; neither window is these two, and a person in an open message
has neither bar in front of them. `test_no_two_menu_items_claim_the_same_shortcut`
(`tests/wired.rs:343`) reads the main window's bar; the reader's own
letters are held by the reading in task 1.

**4. What "the way back" means in each view.** In the reader, Alt+A from
the list returns to the message as F8 did (`wx_reader.rs:618-660`). In
the page window, Alt+A from the attachments list returns focus to the
page's document, which the window does through the same call it uses
after Escape from the list today; the summary quotes it.

**5. The pages.**

```
sed -n 188p docs/KEYBOARD_SHORTCUTS.md   -> "Either way, `F8` reaches the attachments and `Ctrl+S` saves the one you are on."
sed -n 214p docs/KEYBOARD_SHORTCUTS.md   -> the Attachments row, `F8`
sed -n 225,226p docs/KEYBOARD_SHORTCUTS.md   -> "`F8` jumps to the list from anywhere in the message, and `F8` again goes back"
```

**6. The gate for each file.** `src/presentation/wx_reader.rs` maps to
`--lib presentation::wx_reader::`, 14 tests, 1 record, its test that
names F8 rewritten in place; `src/presentation/wx_app.rs` to
`--lib presentation::wx_app::`, 199, 74 records, none added, the test at
`:21264`'s sentence rewritten in place if one asserts it; the new target
to itself; `docs/*.md` to the document-reading targets.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the page window's script posts Alt+A and F7, the handler moves focus and says where, the dead binding goes; the reader's item on Alt+A</name>
  <files>src/presentation/wx_app.rs, src/presentation/wx_reader.rs, tests/attachments_are_reached_with_alt_a_in_both_views.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:11959-12010` (`wire_the_way_out` and its script), `:1414-1500` (`on_script_message_received`, the arm that acts on Escape and F6 for the preview), `:20976-21300` (the page window: the list, the bar, the two bindings, the open sentence, its own script message arm).
    - `src/presentation/wx_reader.rs:300-330` and `:610-670`.
    - `src/presentation/editor_document.rs:295-315`, the composer's post.
    - `tests/wired.rs`, the readings of `wire_the_way_out`'s script from phase 10 (`grep -n 'wire_the_way_out' tests/wired.rs`), for the shape.
  </read_first>
  <behavior>
    - The injected script's keydown listener posts, beside Escape and F6, `{kind: 'attachments'}` for Alt+A (`e.altKey && e.key === 'a'`, either case) and `{kind: 'warning'}` for F7, each with `preventDefault`; the page window's script message arm moves focus to the attachments list on `attachments` when the message has any and says "Attachments, N" through the same announce path Escape's landing uses, and to the warning bar on `warning` when there is one, saying its first words; with nothing to go to, the arm says "No attachments" or "No warning" at Normal. Alt+A on the attachments list (a native `ListBox`, where a wx KEY_DOWN does fire) returns focus to the page and says "Message". The `KEY_DOWN` binding on the page at `:21201` is removed whole.
    - The open sentence at `:21264` says "Alt+A for them"; the reader's own count sentence (`wx_reader.rs:238`, "F8 for the list") says "Alt+A for the list".
    - `wx_reader.rs`: the Attachments item reads "&Attachments\tAlt+A"; the way back from the list answers Alt+A where it answered F8; the doc comments at `:618-625` say Alt+A.
    - The target: readings over `what_ships` holding the script text to naming `'attachments'` and `'F7'` and no `F8`; the page window's arm to matching `"attachments"` and `"warning"` and calling `set_focus` on the list and the bar; the page holding no `bind_internal(EventType::KEY_DOWN` for the WebView itself (the list's and the bar's stay); the reader's item to `Alt+A` and the file to no `F8`; each with a companion planting the opposite.
  </behavior>
  <action>
    Red first, on a branch: the readings bare (red on F8 in both files
    and the script naming neither key). Then the script, the arm, the
    removal, the sentences and the reader's item, with `cargo build`
    between. `wx_app.rs` stays at 199; a test in its module that asserts
    "F8 for them" is rewritten in place and named.

    Invoke `cognitive-accessibility` for the three landing sentences:
    where focus is now, how many, in that order.

    Guard records: one on `wx_app.rs` whose break drops `'attachments'`
    from the script, `suite` the target; one whose break drops the
    `set_focus` from the arm, `suite` the target; one on `wx_reader.rs`
    whose break puts F8 back on the item, `suite` the target; each
    measured; the `wx_reader.rs` record re-measured if its count moved.
  </action>
  <verify>
    <automated>cargo test --test attachments_are_reached_with_alt_a_in_both_views && cargo test --lib presentation::wx_reader:: && cargo test --lib presentation::wx_app:: && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare.
    - `grep -v '^\s*//' src/presentation/wx_reader.rs | grep -c 'F8'` is 0 and `grep -c 'Attachments\\tAlt+A' src/presentation/wx_reader.rs` is 1.
    - `grep -c 'F8 for them' src/presentation/wx_app.rs` is 0 and `grep -c 'Alt+A for them' src/presentation/wx_app.rs` is 1; `awk 'NR>=20976 && NR<=21300' src/presentation/wx_app.rs | grep -c 'page.bind_internal(EventType::KEY_DOWN'` is 0.
    - `cargo test --lib presentation::wx_app::` passes with 199 tests and `presentation::wx_reader::` with 14, quoted.
    - Three guard records measured and quoted.
  </acceptance_criteria>
  <done>Alt+A reaches the attachments in both views through a route that fires, F8 is off both, and the sentence read on open is true.</done>
</task>

<task type="auto">
  <name>Task 2: the pages, the old key dated, the changelog, the issue</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:176-230`, the Reader Window section, and `:338-375`, the preview and the way out, for the manner a key that moved is written.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/KEYBOARD_SHORTCUTS.md`: `:188` says
    "Either way, `Alt+A` reaches the attachments"; the row at `:214` says
    `Alt+A` and that it works in the formatted view too; `:225-226` say
    `Alt+A` both ways; one dated sentence that until this build the page
    said F8 and F8 reached the attachments only in the plain-text reader,
    never in the formatted view, and that F8 still means Columns in the
    main window and the toolbar in the composer. The changelog entry
    under `[Unreleased]`, Fixed: the tester's words; Alt+A in both views;
    why F8 never worked under Formatted (a key bound on a browser control
    is a key the browser keeps); F8 retired there; Known limitations:
    nobody has heard the landing sentences.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only
    the tester's ear settles (Alt+A landing on the list under Formatted
    with the count said, Alt+A back, F7 to the warning bar, the same in
    the plain reader).
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'F8' docs/KEYBOARD_SHORTCUTS.md` is quoted before and after; every remaining F8 on the page is Columns, the composer's toolbar, or the dated sentence.
    - The changelog entry names #84; carriage returns 0; no em dash; none of the six words.
    - The ledger entry is in both halves.
  </acceptance_criteria>
  <done>The pages promise the key that works, and say which key stopped and where it still means something.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the page's script to the window | a message posted from a page is a string the window matches; a sender's markup cannot post one, because the script is injected after the clean and scripts are stripped |
| a key on a browser control | a key bound where it never fires is a promise on a page nobody can keep |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-54 | Repudiation | a dead KEY_DOWN binding kept beside the working route | low | mitigate | removed; the reading holds the page to none |
| T-11-55 | Tampering | a posted kind the window does not know | low | accept | `on_script_message_received` ignores what it does not match, as it does today |
| T-11-56 | Denial of service | Alt+A taken by a menu on the same frame | low | mitigate | the reader's bar has no menu on A; the page window has no bar; the reading holds the reader's letters |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Three guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the landing under each view; what the readings prove: the script's keys, the arm, the reader's item, F8 gone.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 84 --comment "Fixed in <merge commit>: Alt+A reaches the attachments of an open message in both views, the plain-text reader and the formatted page window, and Alt+A again goes back; F7 reaches the warning bar in the formatted window too. F8 never reached them under Formatted because it was bound on the browser control, which keeps its keys; it is retired in both message windows and still means Columns in the main window and the toolbar in the composer. The sentence read on open and the shortcuts page say Alt+A. What only your ear settles: the landing sentence and the count."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 18 closes structurally: Alt+A in both views through a route that fires, F8 retired there, the pages dated.
- LIST-15's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-04.1-SUMMARY.md` when done
</output>
