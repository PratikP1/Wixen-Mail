---
phase: 11-reading-and-the-list
plan: 05
type: execute
wave: 5
depends_on: [11-04]
files_modified:
  - src/application/reading_habits.rs
  - src/presentation/wx_app.rs
  - src/presentation/wx_settings.rs
  - tests/moving_through_the_list_marks_nothing_read.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-03]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Moving through the message list never marks a message read, however long a row stays selected; a message is marked read only after it has been read aloud from the list with Space or Shift+Space, or opened in its own window, and then after the delay the Mark as read after setting names (LIST-03, #25)."
    - "The setting keeps its four kinds of answer and its default, and its meaning is written under it where somebody meets it: counted from the moment a message is read, never from moving onto it."
    - "The one place that marks a message read on its own asks one pure rule, and that rule is held by cases in milliseconds."
  artifacts:
    - "src/application/reading_habits.rs: whether_to_mark_read(began, selected_unread, now, setting), with its cases."
    - "src/presentation/wx_app.rs: the reading act recorded where Space reads a mail row and where the reader opens; the selection recording nothing; the timer's mark asking the rule."
    - "src/presentation/wx_settings.rs: the sentence under the choice."
    - "tests/moving_through_the_list_marks_nothing_read.rs: readings holding the three sites, with companions, coupled to wx_app.rs."
    - "docs/USER_GUIDE.md and docs/changelog.md: when a message counts as read, and the tester's words."
  key_links:
    - "Space on a mail row -> reading_began -> whether_to_mark_read on the timer -> MessageReadToggled and the server change: the same write as today, started by reading and not by selecting."
    - "open_single_message -> reading_began: opening is reading; the preview pane cannot be entered (wx_app.rs:1339), so the reading acts are Space and Enter."
    - "on_item_selected -> nothing about read state: the row's landing is signalled and its body loaded, and the clock is not touched."
---

<objective>
Stop the message list marking messages read as somebody moves through it,
and mark a message read only once it has been read aloud from the list or
opened, after the delay the setting names.

Purpose: #25, the tester on 2026-09-15 under NVDA: "Automatic read/unread
status should not be linked to the list traversal for mail. It should be
either when a message is previewed or when a message is opened." Today a
row selected for two seconds is marked read, and hearing a row's sender,
subject and date takes longer than two seconds, so moving through a folder
by ear marks every message stopped on. His own profile says `never` now,
which is the only defence the program offered.

Output: the rule, the three sites, the sentence under the setting, the
reading target, the guide's paragraph, the changelog entry. Closes #25 on
the tree side; whether the unread count now survives a walk through his
inbox is his to hear.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef`, working tree clean, before
11-01 to 11-04 land; 11-04 adds log lines in other functions of `wx_app.rs`.
Command first, then what it said.

**1. The clock starts on selection, from the timer, and nothing else marks
a message read on its own.**

```
grep -n 'fn mark_the_open_one_read\|mark_the_open_one_read(' src/presentation/wx_app.rs   -> 10035 (the function), 5576 (called from the main timer's poll)
sed -n 10046,10070p src/presentation/wx_app.rs   -> the selected unread message starts the clock the first time the poll sees it; the same message keeps it running; a different one restarts it
grep -n 'opened_at' src/presentation/wx_app.rs   -> 5463 (the RefCell on the timer), 5576, 10038, 10054, 10059, 10078: the clock is written by this function alone
grep -n 'FlagChange::Read(true)' src/presentation/wx_app.rs | grep -v 'tests\|assert'   -> 10098, the write at the end of the clock; 4862's arm writes Read(new_read) for the command
```

The issue's "about 9990" and "about 5585" are `10035` and `5576`. The
name says "the open one" and the body reads "the selected one": nothing
about opening is in it.

**2. "Previewed" cannot mean focus entering the reading pane, because the
pane cannot take focus, by design.**

```
grep -n 'preview.set_can_focus(false)' src/presentation/wx_app.rs   -> 1339
sed -n 1317,1338p src/presentation/wx_app.rs   -> "Focus is kept off the preview wherever this application decides it ... The way this application reads a message aloud is Space and Shift+Space on the list, which is where focus stays."
grep -n 'The preview is not one of these' src/presentation/panes.rs   -> 21: F6 does not stop at the preview, and focus arriving there is put back out
```

The issue's own reading, "previewed means focus has moved into the
reading pane, not that the pane is showing the selected message beside the
list", names the act of reading rather than the pane's contents, and in
this program that act is Space or Shift+Space on the row, which reads the
message aloud through `wire_read_aloud`, and Enter, which opens it in its
own window. The preview showing a body beside the list is not reading and
starts nothing, which is what the issue asks. Pratik's decision is applied
to the acts the program has; the summary says so and the close comment
says it in his words.

**3. Opening a message in its own window marks nothing today.**

```
grep -n 'fn open_single_message' src/presentation/wx_app.rs   -> 12447
awk 'NR>=12447 && NR<=12560' src/presentation/wx_app.rs | grep -n 'read\b\|Read(' | head   -> nothing writes the read flag; the reader shows the message and that is all
```

So the second act the issue names does not exist as a mark either. Task 2
makes both acts start the clock.

**4. The setting and its default.**

```
sed -n 20,60p src/application/reading_habits.rs   -> Immediately, After(2, 5, 10, 30, 60), Never; label, as_stored, from_setting
sed -n 118,127p src/application/reading_habits.rs   -> Default: After(2), "long enough that arrowing past a message does not mark it"
sed -n 1755,1768p src/presentation/wx_settings.rs   -> "&Mark as read after:", built from MarkRead::ALL, no sentence under it
python -c "import json;print(json.load(open(r'C:\Users\prati\AppData\Local\wixen-mail\config\app_config.json'))['mark_read_after'])"   -> never (the tester's profile, read 2026-09-18 through Python, read-only)
```

The default's own comment gives the reason the issue refutes: two seconds
is not longer than a row takes to hear. The default stays `After(2)`,
because counted from reading it means what it says; the comment is
rewritten to the new meaning, and the choice is overrulable with a reason
in the summary. The tester's `never` stays his until he changes it, and
the close comment tells him the setting means something else now.

**5. The gate for each file.** `src/application/reading_habits.rs` maps
to `--lib application::reading_habits::`, 19 tests, no record;
`src/presentation/wx_app.rs` to `--lib presentation::wx_app::`, 199, none
added, and its coupled targets; `src/presentation/wx_settings.rs` to a
filter matching nothing and its coupled targets, a `StaticText` adding no
test; the new target to itself.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: whether to mark a message read, as a rule over when reading began</name>
  <files>src/application/reading_habits.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/reading_habits.rs`, all of it.
    - `src/presentation/wx_app.rs:10035-10100`, the function the rule replaces the decision of.
  </read_first>
  <behavior>
    - `reading_habits::whether_to_mark_read(began: Option<(i64, Instant)>, selected_unread: Option<i64>, now: Instant, setting: MarkRead) -> Option<i64>`: `None` when nothing began, whatever is selected and however long ago; `None` when the message that began reading is not the selected unread one; `Some(id)` at once under `Immediately`; under `After(n)`, `None` before `n` seconds from `began` and `Some(id)` at or after; `None` under `Never` always; the doc says the clock starts when a message is read aloud or opened and never when it is selected, with #25 named and the date.
    - `MarkRead::default()`'s comment says two seconds counted from reading, not from selection.
  </behavior>
  <action>
    Red first, on a branch: the six cases by module path against a stub
    answering `None`, so the two `Some` cases are red and the four `None`
    cases green on arrival, said in the commit. Then the rule.

    Guard records: one whose break makes a began of `None` answer the
    selected id, which is the tree's behaviour today in other words,
    measured on the library.
  </action>
  <verify>
    <automated>cargo test --lib application::reading_habits::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path.
    - `cargo test --lib application::reading_habits::` passes with at least 25 tests, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>The decision that turns a selection into a read mark is a rule that answers nothing for a selection alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reading starts the clock, selecting does not, and the window asks the rule</name>
  <files>src/presentation/wx_app.rs, tests/moving_through_the_list_marks_nothing_read.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/wx_app.rs:3006-3095` (`on_item_selected`), `:3433-3455` (the mail read-aloud wiring), `:9985-10020` (`wire_read_aloud`), `:10035-10100`, `:12447-12560` (`open_single_message`), `:5455-5470` and `:5560-5580` (the timer's clock and poll).
    - `tests/progress_is_shown_and_results_are_said.rs`, the reading shape.
  </read_first>
  <behavior>
    - `WxUIState.reading_began: Option<(i64, Instant)>` replaces the timer's `opened_at`; it is written in two places and read in one: the mail read-aloud closure writes it for the row it is about to read, before the text is announced; `open_single_message` writes it for the message it opens; `mark_the_open_one_read`, renamed `mark_what_was_read`, reads it, asks `whether_to_mark_read` with the selected unread message and `Instant::now()`, and on `Some(id)` clears it and does exactly the write it does today (the state, `MessageReadToggled`, the server change, no announcement).
    - `on_item_selected` touches neither field; a selection that moves to another message leaves `reading_began` as it was, and the rule's "not the selected one" answer is what keeps the old message unmarked.
    - `tests/moving_through_the_list_marks_nothing_read.rs`: readings over `what_ships` holding that the `on_item_selected` closure names `reading_began` nowhere; that the mail read-aloud closure writes `reading_began = Some(`; that `open_single_message` writes it; that the timer's function calls `whether_to_mark_read(` and holds no `Instant::now()` comparison of its own; each with a companion planting the opposite; and one reading that the file names `opened_at` nowhere.
  </behavior>
  <action>
    Red first, on a branch: the readings bare. Then the field, the two
    writes, the rename and the ask, with `cargo build` between. The
    function's doc comment is rewritten: it described a clock started by
    selection and the reason two seconds was chosen; it says now that the
    clock starts when a message is read aloud or opened, why selection
    starts nothing (a row takes longer to hear than two seconds), and names
    #25.

    The changelog entry under `[Unreleased]`, Fixed, in the same commit,
    by `writing-craft`: the tester's words; that moving through the list
    marks nothing now; that a message is marked read after it is read
    aloud with Space or opened, and then after the delay the setting
    names; that the setting's four kinds of answer are unchanged and its
    default is still two seconds, counted from reading; that a profile set
    to Only when I say so keeps that; Known limitations: nobody has walked
    an inbox by ear against this yet.

    Guard records: one on `wx_app.rs` whose break puts the write of
    `reading_began` back into `on_item_selected`, `suite` the new target;
    one whose break drops the ask from the timer's function and marks the
    selected one, `suite` the new target; each measured. `wx_app.rs` stays
    at 199, quoted before and after.
  </action>
  <verify>
    <automated>cargo test --test moving_through_the_list_marks_nothing_read && cargo test --lib presentation::wx_app:: && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare.
    - `grep -c 'opened_at' src/presentation/wx_app.rs` is 0; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'reading_began = Some('` is 2; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'whether_to_mark_read('` is 1.
    - `cargo test --lib presentation::wx_app::` passes with 199 tests, quoted.
    - Two guard records measured and quoted.
    - The changelog entry exists and names #25.
  </acceptance_criteria>
  <done>Selecting a row starts nothing, reading one starts the clock, and the mark is written the way it always was once the rule says so.</done>
</task>

<task type="auto">
  <name>Task 3: the setting says what it counts from, and the guide says when a message counts as read</name>
  <files>src/presentation/wx_settings.rs, docs/USER_GUIDE.md, .planning/WINDOWS.md, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_settings.rs:1750-1770`, the choice, and 10-03's `WHAT_A_SIZE_DOES` pattern for a sentence under a choice on both channels.
    - `docs/USER_GUIDE.md`, the Reading and Managing Email section, for where a paragraph on read state belongs.
  </read_first>
  <action>
    A `StaticText` under the choice on the Reading tab, named on both
    channels, from a constant `WHAT_MARK_READ_COUNTS_FROM` in
    `reading_habits.rs`: "Counted from when you read a message aloud with
    Space or open it, never from moving onto it." Invoke
    `cognitive-accessibility` for the sentence. `wx_settings.rs` gains no
    test; the constant's presence in the built dialog is read by
    `tests/every_event_has_a_control.rs`'s sibling readings if one of them
    walks the Reading tab, else by a one-line addition to the reading in
    `tests/the_language_the_screen_shows_is_the_one_used.rs`'s manner
    inside an existing target coupled to `wx_settings.rs`, count unchanged.

    Invoke `writing-craft` for `docs/USER_GUIDE.md`: a short paragraph
    under Reading and Managing Email, "When a message counts as read":
    reading it aloud with Space or Shift+Space, or opening it with Enter,
    then the delay under Settings, Reading, Mark as read after; moving
    through the list never marks anything; Only when I say so for marking
    by hand alone; dated. The stale context-menu list at `:225-231`, which
    says Mark as Unread, is corrected by 11-06 and not here.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only
    the tester's ear settles: a walk through an unread folder leaving the
    count where it was, Space followed by the count moving after two
    seconds, and the sentence under the choice read once.
  </action>
  <verify>
    <automated>cargo test --test every_event_has_a_control && cargo test --test checkbox_labels && cargo test --test the_settings_dialog_opens_in && cargo test --test house_style && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WHAT_MARK_READ_COUNTS_FROM' src/presentation/wx_settings.rs` is at least 1 and `src/application/reading_habits.rs` defines it.
    - `docs/USER_GUIDE.md` holds a heading or bold lead "When a message counts as read"; carriage returns 0; no em dash; none of the six words.
    - The ledger entry is in both halves.
  </acceptance_criteria>
  <done>The setting says what it counts from where somebody meets it, and the guide says when a message counts as read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the timer to the read flag | a mark written on the wrong message, or on one nobody read, is a state change nobody asked for and a server write |
| the reader window to the list's selection | the message opened and the message selected can differ after a conversation opens; the rule requires them to agree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-19 | Tampering | a mark on a message that was only selected | high | mitigate | `whether_to_mark_read` answers nothing without a began; the reading holds the selection handler clear of it; a record measures the break |
| T-11-20 | Tampering | a mark on the wrong message after the selection moved | medium | mitigate | the rule requires the began id to be the selected unread one, with a case |
| T-11-21 | Repudiation | the setting's old meaning kept by a page | low | mitigate | the sentence under the choice and the guide's paragraph, dated |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Three guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: that arrowing through a folder leaves the unread count alone and that Space then the delay moves it; what the readings and the rule prove: the three sites and the six cases.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 25 --comment "Fixed in <merge commit>: moving through the message list marks nothing read, however long you stay on a row. A message is marked read after you read it aloud with Space or Shift+Space, or open it with Enter, and then after the delay under Settings, Reading, Mark as read after, which still defaults to two seconds and now counts from reading. The preview pane cannot take focus in this program by design, so reading aloud from the list is what previewing is here; the setting says so under itself. Your profile says Only when I say so, which still means that. What only your ear settles: that the unread count stays put on a walk through the inbox."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 3 closes structurally: selection marks nothing, reading aloud and opening start the clock, the rule held by cases, the sentence under the setting.
- LIST-03's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-05-SUMMARY.md` when done
</output>
