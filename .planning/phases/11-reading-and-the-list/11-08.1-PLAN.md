---
phase: 11-reading-and-the-list
plan: 08.1
type: execute
wave: 16
depends_on: [11-08]
files_modified:
  - src/service/protocols/imap.rs
  - src/application/mail_sync.rs
  - src/application/mail_controller.rs
  - src/application/thread_identity.rs
  - src/data/message_cache/messages.rs
  - src/data/message_cache/mod.rs
  - src/presentation/wx_app.rs
  - tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/privacy.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-23]
estimate:
  tokens: 52000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On a server that advertises Gmail's extension, X-GM-THRID is asked for in the fetch already made and handed to threading as the server's thread id: a conversation here is the conversation Gmail shows, the stored thread_id follows the server's, and a message whose id changes moves; the cost per message is measured and written down (LIST-23, #88)."
    - "Messages stored before this build on a Gmail account get their server thread id once, in a pass that asks for the one field over the kept folders' stored uids on the next check, recorded as done per account, so the tester's existing mail is threaded Gmail's way without a re-download."
    - "On every server the late-parent case is traced against the loopback servers, newest first as the download brings a folder: a child placed alone joins its parent when the parent lands, a sibling landing with a fuller References chain joins the tree, a reply carrying only In-Reply-To joins through the store's rejoin; what the trace finds is fixed, and the in-memory threading of a loaded page is handed the same server id so its parents and depths agree with the stored grouping."
    - "Subject matching stays refused and its test stands; the conversation row's count and the row message 11-08 chooses in SQL follow whichever conversation a message ends up in, held by readings; a guard record each."
  artifacts:
    - "src/service/protocols/imap.rs: GMAIL_FIELDS with X-GM-THRID, ImapMessage::gmail_thread_id read from AttributeValue::GmailThrId, the pinning test rewritten in place, a scripted-server case; thread_ids_of(folder, uids) for the once-only pass."
    - "src/application/thread_identity.rs: conversation_root and rejoin taking the server's id, which wins when present, with cases."
    - "src/data/message_cache/messages.rs and mod.rs: messages.server_thread_id (additive), carried in IncomingMessage and MessageListRow, the upsert writing thread_id from the server's id when present, the once-only pass under work_done_once per account."
    - "src/application/mail_sync.rs: the id carried from the fetch; the pass on a Gmail account's check before its listing."
    - "src/presentation/wx_app.rs: apply_threading handing server_thread_id from the row."
    - "tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs: the trace as cases against the scripted servers, newest first, and the readings over the row count and the row message after a re-threading."
    - "docs/USER_GUIDE.md, docs/privacy.md, docs/changelog.md."
  key_links:
    - "header_query(gmail) -> X-GM-THRID in the FETCH -> AttributeValue::GmailThrId -> ImapMessage.gmail_thread_id -> IncomingMessage.server_thread_id -> conversation_root(message_id, refs, server) -> messages.thread_id = the server's -> the conversation query groups by it -> 11-08's row message and count."
    - "a Gmail account's check -> the once-only pass: UID FETCH of the stored uids for X-GM-THRID alone, per kept folder -> the rows rerooted onto the server's id -> work_done_once(account)."
    - "load_folder_messages -> MessageListRow.server_thread_id -> apply_threading -> ThreadInput::server_thread_id -> the union in memory matches the store's grouping."
---

<objective>
A conversation on Gmail is the conversation Gmail shows, and on every
server a child placed before its parent joins it when the parent lands.

Purpose: #88, the tester on 2026-09-19 on `6911018d` against Gmail:
messages of one thread show as several with the same subject. Threads
are built from `References` and `In-Reply-To` alone, subject matching
refused on purpose; the model already prefers a server thread id, and
nothing supplies one, because `X-GM-THRID` is deliberately not asked
for. The comment saying so is out of date: it was written when the
library's own reader hid the answer, and the answer is read here now,
and `imap-proto` 0.16.7 parses the attribute. The other half is the
download's order: 10-05 brings a folder newest first, so a child is
stored before its parent exists, and whether the store's rejoin joins
them when the parent lands is what the trace settles.

Output: the field asked for and read, the store and the threading
handed the server's id with the server's word winning, a once-only pass
for mail already stored, the late-parent trace against the loopback
servers with what it finds fixed, the readings over 11-08's row, the
pages and the changelog. Closes #88 on the tree side; whether the
tester's split threads become one is his account's. After 11-08,
because that plan chooses the row message in SQL over `thread_id` and
its target is the regression for a re-threading that moves rows between
conversations.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-08-PLAN.md
@.planning/phases/11-reading-and-the-list/11-08-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-19 against `main` at `1962e341`, read-only while 11-07.2
executed on its branch, and read again against `38ebcb86` after it
merged: `threading.rs`, `thread_identity.rs`, `imap.rs`, `messages.rs`
and the row loader are the same bytes at both heads, `mail_sync.rs`
gained four comment lines from 11-07.2 so its forgetting sits at
`:1405`, and `apply_threading` sits at `wx_app.rs:13413` with its `None`
at `:13428`. 11-08 has not run and rewrites the conversation queries in
`messages.rs` and the arms that read a thread row, so every line below
in those is re-run on the day and 11-08's summary's names win. Command
first, then what it said.

**1. Threading today: two answers, and neither has a server id.**

```
git show main:src/application/threading.rs | sed -n 1,31p   -> JWZ from References and In-Reply-To; subject matching refused; ThreadInput::server_thread_id "Preferred over anything computed here"
git show main:src/application/threading.rs | sed -n 78,95p   -> thread_messages unions by server_thread_id when present
git show main:src/application/threading.rs | grep -n 'fn test_subjects_are_never_used_to_thread'   -> 485, the refusal held
git grep -n 'server_thread_id' main -- src | grep -v threading.rs   -> wx_app.rs:13428 alone (13441 at 1962e341): apply_threading passes None
git show main:src/application/thread_identity.rs | sed -n 1,40p   -> the stored thread_id is conversation_root(message_id, refs), the root of the chain, "answered from that message alone"; thread_messages runs in memory over a loaded page for parents and depths and "must not be used for a value that gets written down"
git show main:src/data/message_cache/messages.rs | sed -n 982,1000p; sed -n 1116,1150p   -> upsert_message derives the conversation, then merge_what_this_message_connects asks the cache for the identifiers worth asking about, and rejoin reroots the conversations found onto the earliest
git show main:src/application/thread_identity.rs | sed -n 200,260p   -> rejoin: the winner is the earliest string among the conversations an arrival proved to be one; nothing is renamed when nothing new is proved
```

So the store already joins a child to a parent that lands later, by
naming a conversation after the root the chain points at whether or not
the root is here, and rejoins a reply with a cut chain through what its
identifiers are already filed under. The trace in task 2 is what proves
that on the loopback servers in the download's order, and the plan
names the two places a failure could sit: the rejoin's rule, and the
in-memory threader being handed no server id.

**2. The field, the library and the cost.**

```
git show main:src/service/protocols/imap.rs | sed -n 106,120p   -> GMAIL_FIELDS "X-GM-MSGID X-GM-LABELS"; X-GM-THRID "deliberately not asked for. It was unreachable while the library's own reader was in the way; now that the answer is read here it could be had"
git show main:src/service/protocols/imap.rs | sed -n 2036,2046p   -> header_query(gmail): the fields appended only where the server advertises the extension, because a server that does not know an attribute refuses the whole FETCH
git show main:src/service/protocols/imap.rs | sed -n 1918,1935p   -> the attributes read here: AttributeValue::GmailMsgId and GmailLabels into ImapMessage
sed -n 41,47p ~/.cargo/registry/src/*/imap-proto-0.16.7/src/parser/gmail.rs   -> gmail_thrid parses "X-GM-THRID <number>" into AttributeValue::GmailThrId(u64)
git show main:src/service/protocols/imap.rs | sed -n 2497,2503p   -> test_the_thread_id_is_not_asked_for pins the absence, "If this starts failing, the library grew the accessor"
```

The accessor is there: the attributes are read directly and the parser
has the variant. The field is one number per message in a FETCH already
made, about thirty bytes on the wire ("X-GM-THRID 1278455344230334865 ");
task 1 measures it on the scripted server rather than trusting that.

**3. What is stored, and what a check re-fetches.**

```
git show main:src/data/message_cache/messages.rs | grep -n 'pub struct MessageListRow' -A 12   -> the row carries message_id and refs_header; no server id
git show main:src/data/message_cache/messages.rs | sed -n 1036,1042p   -> on conflict thread_id = excluded.thread_id: a re-listed message's conversation follows the newest derivation
git show main:src/application/mail_sync.rs | sed -n 1360,1405p   -> a check lists the folder and stores what is new; stored uids are not fetched again
```

A message already stored on the tester's Gmail account is never
re-fetched, so without a pass his 17,753 messages would stay
header-threaded and the fix would show only on new mail. The pass is
one `UID FETCH <stored uids> (X-GM-THRID)` per kept folder on the next
check, before the listing and after `replay_the_moves_that_were_waiting`
(11-07.1 and 11-07.2 put the replay of moves and crossings there, taking
the accounts), recorded per account in `work_done_once`; at about forty
bytes a message it is under a megabyte for his account.

**4. What 11-08 leaves.** From its plan: the row message chosen in SQL by
`read ASC, received_at ASC, id ASC` inside a conversation grouped by
`thread_id`, the preview and the window following it, the
conversation's text as one chunk; its target reads the row message back
through the cache. A re-threading changes `thread_id` and the queries
follow, which is why 11-08's target is this plan's regression for the
row and the count and why this plan comes after it.

**5. The gate for each file.** `src/service/protocols/imap.rs` 35 tests,
46 records, the pinning test rewritten in place and one scripted case
added, the count check named; `src/application/thread_identity.rs` 31
tests, 1 record, cases added and the count check named;
`src/application/threading.rs` 26 tests, 0 records, not changed;
`src/data/message_cache/messages.rs` 179 tests, 24 records, no case
added, the pass's case in the target; `src/application/mail_sync.rs`
149 tests, 13 records, no case added; `src/presentation/wx_app.rs` 196
tests by `#[test]`, 93 records, none added; the new target to itself.
960 records in all by the parser at `38ebcb86`. Counts taken 2026-09-19
at that head.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: X-GM-THRID asked for and read where the server has it, the store and the threading handed the server's id, and the cost measured</name>
  <files>src/service/protocols/imap.rs, src/application/mail_controller.rs, src/application/mail_sync.rs, src/application/thread_identity.rs, src/data/message_cache/messages.rs, src/data/message_cache/mod.rs, src/presentation/wx_app.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/protocols/imap.rs:100-125`, `:394-430`, `:1900-1945`, `:2030-2050`, `:2490-2510`, `:2700-2725`.
    - `src/application/thread_identity.rs`, whole; `src/application/threading.rs:1-95`.
    - `src/data/message_cache/messages.rs:455-480`, `:980-1045`, `:1115-1160`, and `IncomingMessage` (`grep -n 'pub struct IncomingMessage'`).
    - `src/presentation/wx_app.rs:13413-13432` (`apply_threading`) and the row loader that fills `MessageListRow`.
    - 11-08's summary for the names the conversation queries shipped with.
  </read_first>
  <behavior>
    - `GMAIL_FIELDS` is `X-GM-MSGID X-GM-THRID X-GM-LABELS`; `ImapMessage::gmail_thread_id: Option<u64>` from `AttributeValue::GmailThrId`; `test_the_thread_id_is_not_asked_for` becomes `test_the_thread_id_is_asked_for_where_the_server_has_it` in place: asked for under `gmail`, absent otherwise; a scripted-server case in the shape of `test_gmail_message_id_and_labels_reach_the_message_when_the_server_sent_them` with the attribute in the reply and the id read back.
    - `IncomingMessage::server_thread_id: Option<String>` and `messages.server_thread_id` (a column added with `ensure_column_exists`, additive), written on upsert; `MessageListRow::server_thread_id` read with the row; `mail_sync` fills it from the fetch as `format!("gm:{id}")` so the stored value says where it came from.
    - `thread_identity::conversation_root(message_id, refs, server: Option<&str>)` answers the server's id when it is present and not empty, else the root as today, still from the message alone; `rejoin(the_arriving_conversation, found, arriving_is_the_servers: bool)` makes the server's id the winner when the arriving message carries one, so conversations found by header chains settle under the server's name, and otherwise keeps the earliest rule; the guard that nothing is renamed when nothing new is proved is unchanged; cases for each.
    - `apply_threading` passes `row.server_thread_id.clone()` as `ThreadInput::server_thread_id`, so parents and depths in memory are computed over the same union the store groups by.
    - The cost: a case on the scripted server measures the FETCH reply's length for one message with and without the field and prints both; the summary quotes the difference per message and the date.
  </behavior>
  <action>
    Red first, on a branch: the rewritten pinning test and the scripted
    case by module path with the count check named for `imap.rs`'s
    records, the `thread_identity` cases by path with its record's count
    check named. Then the field, the attribute, the column, the row, the
    derivation, the winner, `apply_threading`, with `cargo build`
    between. The comment on `GMAIL_FIELDS` is rewritten: why the field
    was not asked for, when that stopped being true, what it costs.
    Invoke `elegant-code`: the server's id is one `Option<&str>` through
    the existing functions, not a parallel path.

    Guard records: one on `imap.rs` whose break drops `X-GM-THRID` from
    `GMAIL_FIELDS`, the rewritten test red; one on `thread_identity.rs`
    whose break lets the earliest string beat the server's id, its case
    red; one on `wx_app.rs` whose break passes `None` again, the target's
    reading in task 2 red; each measured; the records naming `imap.rs`
    and `thread_identity.rs` re-measured when the count check names
    them, in the background.
  </action>
  <verify>
    <automated>cargo test --lib service::protocols::imap:: && cargo test --lib application::thread_identity:: && cargo test --lib application::threading:: && cargo test --lib data::message_cache::messages::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the count checks.
    - `grep -c 'X-GM-MSGID X-GM-THRID X-GM-LABELS' src/service/protocols/imap.rs` is 1; `grep -c 'GmailThrId' src/service/protocols/imap.rs` is at least 1; `grep -c 'server_thread_id' src/data/message_cache/messages.rs` is at least 3; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'server_thread_id: None'` is 0.
    - `cargo test --lib service::protocols::imap::` passes with 36, quoted; `application::thread_identity::` with its count quoted; `application::threading::` with 26.
    - The summary quotes the field's cost per message from the scripted server with the command and the date.
    - Three guard records measured and quoted.
  </acceptance_criteria>
  <done>A Gmail message arrives with its thread id, the store names its conversation by it, an arriving id wins over what the headers said, and the loaded page is threaded over the same union.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the late-parent trace against the loopback servers, newest first, and the once-only pass for mail already stored</name>
  <files>src/application/mail_sync.rs, src/application/mail_controller.rs, src/service/protocols/imap.rs, src/data/message_cache/messages.rs, src/data/message_cache/mod.rs, src/application/thread_identity.rs, tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/mail_sync.rs`, the folder sync and its tests against `against_a_server_that_answers` (`grep -n 'a_server_that_can' src/application/mail_sync.rs | head`), and `src/service/protocols/imap.rs:2808-2880`.
    - `src/data/message_cache/messages.rs:255-320` as 11-08 left them (the conversation query and the row message), and `bodies.rs`'s once-only pass under `work_done_once` for the shape.
    - 11-08's target, for how it reads a conversation's row message and count back.
  </read_first>
  <behavior>
    - The trace, as cases in the target against a scripted server that lists a folder newest first, each read back through the cache's conversation query and through `apply_threading`'s placements: (a) a child with `References: <p>` stored before its parent `<p>` lands, then the parent: one conversation, the parent the root, the child at depth 1, the row count 2; (b) A with `References: <r>`, B with `References: <r> <a>`, then C with `In-Reply-To: <a>` alone: one conversation; (c) a reply whose chain was cut to its parent only when the parent's own chain is longer: one conversation; (d) the same four messages on a server with the Gmail extension where every message carries one `X-GM-THRID`: one conversation named by it, whatever the headers say; (e) a message whose `X-GM-THRID` differs from its siblings': its own conversation, the headers notwithstanding, which is what "the server's word wins" means and is said in the doc; (f) two messages with the same subject and no chain: two conversations, `test_subjects_are_never_used_to_thread`'s rule read back through the store.
    - What the trace finds is fixed where it sits: the expectation from the premises is that (a) to (c) already pass through `conversation_root` and `rejoin`, and the summary says which passed before any change and which needed one, with the change named; a case that fails is red first and the fix is the smallest that makes it green.
    - The once-only pass: `MailController::thread_ids_of(folder, uids) -> Vec<(u32, u64)>` over `UID FETCH <set> (X-GM-THRID)` only where the server has the extension; `MessageCache::messages_without_a_server_thread_id(account_id) -> per folder the stored uids`; the pass on a Gmail account's check, before its first listing and after 11-07.1's replay, writes `server_thread_id` and reroots each message's conversation onto it through the same `rejoin` path, recorded under `work_done_once` as `server thread ids fetched for <account_id>`; a pass that could not finish is tried again at the next check; the log says how many rows moved and the milliseconds; a case against the scripted server with three stored rows and one FETCH answer.
  </behavior>
  <action>
    Red first, on a branch: the target's cases bare; a fix that touches
    a module named by records is named with its count check. Then the
    trace run, the fixes, the pass, with `cargo build` between. The
    summary records each case's state before and after in a table, which
    is the trace the issue asks for.

    Guard records: one on `mail_sync.rs` whose break runs the pass after
    the listing, its ordering case red; one on `messages.rs` whose break
    skips the reroot when the server's id arrives for a stored row, case
    (d)'s stored-rows variant red; each measured.
  </action>
  <verify>
    <automated>cargo test --test a_conversation_is_gmails_own_and_a_late_parent_joins_it && cargo test --lib application::mail_sync:: && cargo test --lib data::message_cache::messages::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases.
    - The target passes with its count quoted, at least seven; the summary holds the trace table with each case's state before and after.
    - `grep -c 'fn thread_ids_of' src/application/mail_controller.rs` is 1; `grep -c 'server thread ids fetched for' src/data/message_cache/messages.rs src/application/mail_sync.rs` totals at least 1.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>A child stored before its parent joins it when the parent lands, a fuller sibling and a cut reply join their tree, a Gmail id names the conversation over the headers, and mail already stored gets its id once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the row count and the row message follow a re-threading, the pages, and the close</name>
  <files>tests/a_conversation_is_gmails_own_and_a_late_parent_joins_it.rs, guards/guards.toml, docs/USER_GUIDE.md, docs/privacy.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - 11-08's target and its readings of the row message and the count.
    - `docs/USER_GUIDE.md`, the conversations section (`grep -n -i 'conversation' docs/USER_GUIDE.md | head`); `docs/privacy.md`'s section on what is asked of a server (`grep -n -i 'gmail\|label' docs/privacy.md | head`).
  </read_first>
  <behavior>
    - Two readings in the target over the cache after a re-threading: a message that moved from one conversation to another because its server id arrived changes the row message 11-08's SQL chooses for both conversations and their counts, read back through 11-08's own query; and a conversation row's count after the late parent lands is two, not one, through the same query.
    - `docs/USER_GUIDE.md`: the conversations section says that on Gmail a conversation is the one Gmail shows, and on other servers it is built from the headers a reply carries, so a reply a sender's program sent without them stands alone; dated. `docs/privacy.md`: the section on what is asked of a server gains one sentence: on Gmail the conversation id is asked for with each message's other fields, and it is Gmail's own grouping, not a new thing told to Gmail. The changelog entry under `[Unreleased]`, Fixed: the tester's words, why threads split (headers alone, and the download's order), what happens now on Gmail and elsewhere, the once-only pass and what it cost on the scripted server, and Known limitations: a message with no headers joining it to anything stands alone on every server but Gmail, and nobody has seen the tester's split threads become one.
  </behavior>
  <action>
    Red first, on a branch: the two readings bare. Then whatever they
    find, and the pages with `writing-craft`.

    Ledger, by hand, both halves: one `unrun-verify` entry for the
    tester: the split threads one conversation after the next check,
    and a conversation opened showing the same messages Gmail shows.

    Guard record: one on `messages.rs` whose break groups the
    conversation query by the header root rather than `thread_id`, the
    re-threading reading red; measured.

    After the merge, the executor closes the issue from the merge
    commit: `gh issue close 88 --comment "Fixed in <merge commit>: on
    Gmail the conversation id (X-GM-THRID) is now asked for with each
    message and names the conversation here, so a thread is the thread
    Gmail shows; mail already stored gets its id once at the next check.
    On every server a child stored before its parent joins it when the
    parent lands, a reply with a shorter chain joins its tree, and the
    trace of those cases against the loopback servers is in the summary
    with what it found. Subject matching stays off. What only your
    account settles: your split threads becoming one after the next
    check, and a conversation matching Gmail's."` Closing an issue is not
    a publish.
  </action>
  <verify>
    <automated>cargo test --test a_conversation_is_gmails_own_and_a_late_parent_joins_it && cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings.
    - The pages carry the dated sentences; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words; the ledger entry in both halves.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>The row and its count follow a re-threading, the pages say what a conversation is on Gmail and elsewhere, and the issue is closed with the ear list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a server's thread id to the store | a number the server sends names a conversation here and moves rows |
| a stranger's headers to the join | a References chain is written by the sender and decides what joins what on every server without the extension |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-95 | Tampering | a References chain crafted to join a stranger's mail to a conversation | medium | accept | the rejoin joins only through identifiers this account's stored messages carry, as today; subject matching stays refused; on Gmail the server's id wins and Gmail's grouping is the sender's problem to abuse there |
| T-11-96 | Denial of service | the FETCH refused on a server that does not know X-GM-THRID | high | mitigate | the field is appended only under `header_query(gmail)`, as the other two are; the rewritten test holds both branches |
| T-11-97 | Denial of service | the once-only pass fetching every stored uid on every check | medium | mitigate | recorded per account under `work_done_once` after it finished; per kept folder; the log says the count and the milliseconds; a case holds the marker |
| T-11-98 | Tampering | an arriving header chain renaming a conversation the server already named | medium | mitigate | the server's id wins in `rejoin` when the arriving message carries one, and a stored server id is not overwritten by a header root; cases (d) and (e) and a record hold it |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Six guard records measured; every record the count check flagged re-measured and read.
- What the tester's account settles: the split threads one after the next check; what the cases prove: the field read, the server's id winning, the late parent joining, the fuller sibling, the cut reply, the pass, the row and the count following.
- After the merge, the executor closes #88 with the comment in task 3.
</verification>

<success_criteria>
- Roadmap criterion 27 closes structurally: the field, the server's id in the store and the threading, the pass, the trace with its findings fixed, the readings over 11-08's row, subject matching still refused.
- LIST-23's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-08.1-SUMMARY.md` when done
</output>
