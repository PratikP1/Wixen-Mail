---
phase: 11-reading-and-the-list
plan: 07
type: execute
wave: 10
depends_on: [11-06, 11-06.1]
files_modified:
  - src/application/choosing_messages.rs
  - src/application/mod.rs
  - src/presentation/wx_app.rs
  - tests/every_command_acts_on_the_selection.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/development/measurements.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-05, LIST-04]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Shift with Up, Down, Home and End extends the selection in the message list the way every Windows list does, Ctrl+A selects everything shown up to the bound Select All already has, and NVDA says selected, not selected and the count on its own (LIST-05, #30)."
    - "Delete, Delete Permanently, Move to, Copy to, Mark as Read or Unread, Star or Unstar, and the Label commands act on every selected message with one announcement saying how many; Reply, Forward, Open, Save As, the read receipt and Copy to a task, event or note act on the row the cursor is on, as before."
    - "A conversation row that is selected contributes every message in the conversation, so Mark as Read on a thread row marks the whole thread and the announcement says how many (LIST-04's third line, #27)."
    - "No command runs over more than the bound; above it, one sentence says so and nothing is written."
  artifacts:
    - "src/application/choosing_messages.rs: what a selection contributes, the count sentence, the bound, with cases."
    - "src/presentation/wx_app.rs: the list multi-select; chosen_messages read from the control; the seven set commands over it; the cursor commands unchanged."
    - "tests/every_command_acts_on_the_selection.rs: readings holding each arm to the set or the cursor, with companions; the 5,000-row timing."
    - "docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/development/measurements.md, docs/changelog.md."
  key_links:
    - "the control's selected state -> chosen_messages -> what_the_selection_holds -> the arm: the set is read from the control at the moment of the command, never kept in the model, because the list is virtual."
    - "a conversation row -> conversation_nodes -> every message of it: the same walk the conversation window uses."
    - "one command -> one cache write per message, one server change per message through the queue single messages use, one announcement."
---

<objective>
Let the message list select more than one message, make every command
that acts on messages act on the selection with one announcement saying
how many, and make a conversation row stand for every message in it.

Purpose: #30, the tester on 2026-09-15: "Shift+arrow keys should allow the
user to select multiple messages." The list is built single-selection, so
Shift with an arrow moves the selection instead of extending it, and every
command reads one index. And the last sentence of #27: "If a thread has
the focus, then the entire thread should be marked", which is the
one-conversation case of the same change.

Output: the rule for what a selection holds and what is said, the list
multi-select, seven commands over the set, the readings, the timing at the
bound, the pages. Closes #30 and #27.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef`, working tree clean, before
11-01 to 11-06 land; 11-06 moves the Mark as Read arm's body into
`toggle_read_state`, which this plan then widens. Command first, then what
it said.

**1. The list is single-selection and 27 sites read one index; the issue's
count holds.**

```
grep -n 'ListCtrlStyle::SingleSel' src/presentation/wx_app.rs   -> 1108
grep -c 'selected_message_index' src/presentation/wx_app.rs   -> 28, of which 27 before the first #[cfg(test)] at 23687 and 1 in a test
grep -n 'selected_message_index' src/presentation/wx_app.rs | awk -F: '{print $1}' | tr '\n' ' '   -> 339 561 3021 3668 3672 3701 4130 4830 4862 10048 10345 13136 13347 13949 14084 14170 14201 14802 15041 17456 17494 17572 18955 19507 19652 19709 21754 28779
```

Sorted by what each does, read on the day: the field and its default
(339, 561); the selection handler writing it (3021); the commands that
become set commands: Star (3701), Delete and Delete Permanently (4830),
Mark as Read (4862), the Label slots (10345), Move and Copy (18955), and
the conversation row's delete (13949); the commands that stay on the
cursor row: Next and Previous Unread (3668, 3672), Copy to a task, event
or note (4130), the read clock (10048), the invitation answer (13136),
Save As (13347), the view switch (14084), Reply (14802), the reader's
`msg_info` (15041), the read receipt (19652, 19709), the body fetch
(21754); and the bookkeeping that keeps the cursor row after a change
(14170, 14201, 17456, 17494, 17572, 19507). The field stays as the cursor
row; the set is read from the control.

**2. Ctrl+A already selects every row of a focused list and refuses above
five thousand; on this list it selects one.**

```
grep -n 'MOST_ROWS_WORTH_SELECTING' src/application/editing.rs   -> 86: 5_000
sed -n 16515,16524p src/presentation/wx_app.rs   -> Doing::ChooseEveryRow sets Selected on every row and announces "{rows} rows selected"
```

On a `SingleSel` list `set_item_state(Selected)` on each row leaves one
selected. Dropping the style makes the existing command do what its
sentence says. The same bound holds the set commands: a set larger than
`MOST_ROWS_WORTH_SELECTING` (reachable with Shift+End on a folder of
12,872) is refused with one sentence and nothing written, because 5,000
cache writes and 5,000 queued server changes is the most this window can
do while still answering keys, and that is the number Select All already
chose. Task 2 measures marking 5,000 read in the cache so the sentence on
the page is a number.

**3. What a selection holds when it is a conversation row.**

```
grep -n 'fn conversation_nodes' src/presentation/wx_app.rs   -> 12648: every message of the conversation from the loaded rows, the walk the conversation window uses
grep -n 'fn delete_the_conversation_row' src/presentation/wx_app.rs   -> 13940 region: the one command that already acts on a conversation's messages, under DeletingAConversationRow's reach
sed -n 130,160p src/application/conversations.rs   -> DeletingAConversationRow: this folder's messages or the whole conversation, a setting
```

Delete keeps its reach setting; Mark as Read, Star and the Labels over a
conversation row take the whole conversation, which is Pratik's sentence
for the thread; Move and Copy over a conversation row take this folder's
messages, because a move of a conversation's messages out of other folders
is a move nobody asked for. Decision, overrulable with a reason in the
summary.

**4. The cross-account move reports one message at a time.**

```
grep -n 'pub fn what_to_say_about_an_unfinished_move\|pub fn how_to_finish' src/application/mail_across_accounts.rs   -> 713, 603
```

A batch of moves across accounts sums the report: "3 moved, 1 not sent"
in one sentence, through the same functions per message and one
announcement at the end.

**5. NVDA's own words for a multi-selection list are not this program's.**
"selected", "not selected" and "N items selected" are what NVDA says for
a `SysListView32` with `LVS_SINGLESEL` off, from the control's state; the
program adds nothing per row and says only the count after a command.

**6. The gate for each file.** `src/application/choosing_messages.rs`
maps to itself; `src/presentation/wx_app.rs` to `--lib presentation::wx_app::`,
199, none added, and its coupled targets (seventeen by then); the new
target to itself; `docs/*.md` to the document-reading targets.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: what a selection holds, what is said, and the bound</name>
  <files>src/application/choosing_messages.rs, src/application/mod.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/conversations.rs:100-200`, `DeletingAConversationRow` and `AConversationReaches`.
    - `src/application/editing.rs:80-110`, the bound and how Select All refuses.
    - `src/presentation/wx_app.rs:12648-12700`, `conversation_nodes`, for the shape of a conversation's members.
    - `src/application/mail_sync.rs:174-198`, `what_arrived` and `how_many`, the count wording this program uses.
  </read_first>
  <behavior>
    - `choosing_messages::what_the_selection_holds(rows: &[usize], showing: Showing, members_of: impl Fn(usize) -> Members) -> Chosen`, where `Members` is one message for a message row and the conversation's messages for a conversation row, and `Chosen { messages: Vec<MessageRef>, conversations: usize }` with `MessageRef { row_id, uid, subject, read, starred }`; duplicates by `row_id` removed; order the rows' order.
    - `Chosen::reach_for(command: SetCommand, setting: DeletingAConversationRow) -> Reach`: `Delete` follows the setting; `MarkRead`, `Star`, `Label` are `TheWholeConversation`; `Move` and `Copy` are `ThisFolderOnly`.
    - `what_was_done(command: SetCommand, chosen: &Chosen, outcome: Outcome) -> String`: "3 messages marked read", "1 message marked unread", "2 conversations, 9 messages marked read", "4 messages moved to Archive", "2 messages copied to Work", "3 messages deleted", "1 message starred", "3 messages labelled Important", "3 messages moved, 1 not sent" for a cross-account batch with a refusal; through `how_many`, the singular right.
    - `too_many(count: usize) -> Option<String>`: `None` at or below `MOST_ROWS_WORTH_SELECTING`, else "5,001 messages are selected. This can do at most 5,000 at once; select fewer." with the bound read from `editing.rs` and not written again.
    - `what_mark_read_does(chosen: &Chosen) -> bool`: read when any is unread, else unread, which is 11-06's rule over a set.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path against stubs
    answering an empty set, the message-row sentence for everything, and
    `None`. Then the module, registered in `application/mod.rs`.

    Invoke `cognitive-accessibility` for the count sentences and the
    refusal: what was done, how many, where, in that order; no word a
    person did not press.

    Guard records: one whose break drops the duplicate removal; one whose
    break makes `too_many` answer `None` for everything; each measured on
    the library.
  </action>
  <verify>
    <automated>cargo test --lib application::choosing_messages::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path.
    - `cargo test --lib application::choosing_messages::` passes with at least twelve tests, quoted.
    - `grep -c 'MOST_ROWS_WORTH_SELECTING' src/application/choosing_messages.rs` is at least 1 and the file defines no bound of its own.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>What a selection means, what a command over it says, and where it stops are one module held by cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the list selects more than one, and seven commands act on the set</name>
  <files>src/presentation/wx_app.rs, tests/every_command_acts_on_the_selection.rs, guards/guards.toml, docs/development/measurements.md</files>
  <read_first>
    - `src/presentation/wx_app.rs:1100-1112` (the style), `:3006-3030` (the selection handler), and each set arm named in premise 1 with its lines re-taken on the day; `:16515-16524` (Select All); `:18955-19060` (`move_or_copy_message`); `:13940-14010` (`delete_the_conversation_row`); `:19500-19530` (`take_row_out_of_the_list`).
    - `src/application/mail_across_accounts.rs:600-760`, the per-message report.
    - `tests/everything_comes_down_without_being_asked.rs`, the reading shape over `what_ships`.
    - `tests/the_list_holds_everything_the_folder_holds.rs`, for how a cache of 12,872 rows is written in a test, which the timing reuses at 5,000.
  </read_first>
  <behavior>
    - The list is built without `SingleSel`; `chosen_rows(list: &ListCtrl) -> Vec<usize>` walks `get_next_item(-1, ListNextItemFlag::All, ListItemState::Selected)` until it answers below zero; `chosen_messages(state, list) -> Chosen` hands the rows and `conversation_nodes` to `what_the_selection_holds`.
    - Each set arm (Star, Delete, Delete Permanently, Mark as Read from 11-06's `toggle_read_state`, the Label slots and Remove every label, Move to, Copy to) reads `chosen_messages`, refuses through `send_refusal` with `too_many`'s sentence when it answers one, otherwise does per message what it did for one (the state, the cache write, the server change through `spawn_server_change` or the move path) and announces once with `what_was_done` at Normal, signalling `Confirmed` once; the per-message sentence and signal of today are gone from those arms. A cross-account move sums the report. `delete_the_conversation_row` folds into the Delete arm through `reach_for`.
    - The cursor commands and the bookkeeping keep `selected_message_index`, which `on_item_selected` still writes; `take_row_out_of_the_list` takes the set's rows and keeps the cursor on the row after the last removed.
    - 11-06's `refresh_mark_read_wording` reads `what_mark_read_does(&chosen_messages(..))` so the label follows the set.
    - `tests/every_command_acts_on_the_selection.rs`: readings over `what_ships` holding the style clear of `SingleSel`; each of the seven arms calling `chosen_messages(` and `what_was_done(` and `too_many(`; each of the cursor commands calling neither; `on_item_selected` still writing `selected_message_index`; each with a companion. And one `#[ignore]` timing: a cache of 5,000 messages marked read through the same per-message cache write the arm uses, the elapsed time printed as a row in the measurements page's shape, run once on a release build for the row.
  </behavior>
  <action>
    Red first, on a branch: the readings bare. Then the style, the two
    helpers and the arms one at a time with `cargo build` between; the
    line numbers in premise 1 re-taken first, since 11-04 to 11-06 moved
    them. `wx_app.rs` stays at 199; a test in its module that reads a
    changed arm is rewritten in place, and the summary names each.

    Invoke `cognitive-accessibility` for the refusal above the bound.

    The row: `cargo test --release --test every_command_acts_on_the_selection -- --ignored --nocapture --test-threads=1`, nothing else building, output to a file; the elapsed time for 5,000 read marks on the measurements page with the command, the date and the commit, and the sentence on the page that this is the cache alone and the server changes queue behind it.

    Guard records: one on `wx_app.rs` whose break puts `SingleSel` back,
    `suite` the target; one whose break makes the Mark arm act on the
    cursor row alone; one whose break drops `too_many` from the Delete
    arm; each `suite` the target, measured.
  </action>
  <verify>
    <automated>cargo test --test every_command_acts_on_the_selection && cargo test --lib presentation::wx_app:: && cargo test --test mark_as_read_says_which_way_it_will_go && cargo test --test wired && cargo test --test every_number_carries_its_command_and_its_date</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the readings bare.
    - `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'ListCtrlStyle::SingleSel'` is 0; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'chosen_messages('` is at least 8 (the definition and seven arms).
    - `grep -c 'selected_message_index' src/presentation/wx_app.rs` is quoted before and after; the after is smaller.
    - `cargo test --lib presentation::wx_app::` passes with 199 tests, quoted.
    - The measurements page holds the 5,000-row timing with its command, dated.
    - Three guard records measured and quoted.
  </acceptance_criteria>
  <done>The list selects a set, the seven commands act on it with one sentence, the cursor commands are unchanged, and the cost at the bound is a number on the page.</done>
</task>

<task type="auto">
  <name>Task 3: the keys and the count on the pages, and both issues closed</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:497-526` (Select All's paragraph and its bound), `:527-558` (the Action menu), `:686-696` (Message Navigation).
    - `docs/USER_GUIDE.md`, Reading and Managing Email.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/KEYBOARD_SHORTCUTS.md`: under Message
    Navigation, rows for `Shift+Up`, `Shift+Down`, `Shift+Home`, `Shift+End`
    and `Ctrl+A` in the message list, what each selects and that NVDA says
    selected and the count on its own; the Select All paragraph's bound
    sentence gains that every command over a selection has the same bound;
    the Action menu's rows for Delete, Move to, Copy to, Mark as Read, Star
    and Label say they act on every selected message and say how many, and
    that Reply, Forward, Open and Save As act on the row the cursor is on;
    the thread sentence on the Mark as Read row. `docs/USER_GUIDE.md`: a
    paragraph "Selecting more than one message" under Reading and Managing
    Email, with the bound and the conversation-row rule, dated.

    The changelog entry under `[Unreleased]`, Added, for #30 and the thread
    half of #27: the tester's words; the keys; the seven commands and the
    one sentence; a conversation row standing for its messages; the bound
    with the measured number; Known limitations: nobody has heard a
    selection of many read, and a set moved across accounts has met no
    server.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only
    the tester's ear settles: NVDA's own selected and not selected on
    Shift+Down, the count after Ctrl+A, one sentence after Delete over
    several, the thread marked from its row, and the refusal above the
    bound.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Shift+Down' docs/KEYBOARD_SHORTCUTS.md` is at least 1 in the message list's section; `grep -c 'Selecting more than one message' docs/USER_GUIDE.md` is 1.
    - The changelog entry names #30 and #27; carriage returns 0; no em dash; none of the six words.
    - The ledger entry is in both halves.
  </acceptance_criteria>
  <done>The keys, the commands over a set and the bound are on the pages a person reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the control's selection to a destructive command | a set read wrongly deletes or moves messages nobody chose |
| a set of thousands to the cache and the server queue | a command over more than the window can do stops it answering keys, which takes the screen reader with it |
| a conversation row to its messages in other folders | a move that reaches into folders somebody is not looking at |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-25 | Tampering | a set read from a stale model | high | mitigate | the set is read from the control at the command, never kept; duplicates removed; readings hold every arm to `chosen_messages` |
| T-11-26 | Denial of service | a command over more than the bound | high | mitigate | `too_many` refuses above `MOST_ROWS_WORTH_SELECTING` with one sentence; the cost at the bound measured and on the page |
| T-11-27 | Elevation of privilege | Move over a conversation row reaching other folders | medium | mitigate | `reach_for` gives Move and Copy this folder only; Delete keeps its setting; a case per command |
| T-11-28 | Repudiation | one sentence hiding a partial failure in a batch | medium | mitigate | the cross-account report summed with the refusals counted; each refusal still logged per message |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Five guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: NVDA's selection words, the counts, the thread from its row; what the readings and cases prove: the set, the reach, the sentences, the bound, the seven arms.
- After the merge, the executor closes both issues from the merge commit: `gh issue close 30 --comment "Fixed in <merge commit>: Shift with Up, Down, Home and End extends the selection in the message list, Ctrl+A selects everything shown up to 5,000, and Delete, Delete Permanently, Move to, Copy to, Mark as Read or Unread, Star or Unstar and the Label commands act on every selected message with one sentence saying how many. Reply, Forward, Open and Save As act on the row the cursor is on. Above 5,000 a command says so and does nothing; marking 5,000 read costs <the row> in the cache. What only your ear settles: NVDA's own selected and not selected as you extend, and the count after Ctrl+A."` and `gh issue close 27 --comment "Fixed in <merge commit> with 11-06's <merge commit>: the label follows the state on the Action menu, the context menu and the toolbar; M toggles and says read or unread; and a conversation row stands for every message in it, so Mark as Read on a thread marks the whole thread and says how many."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 5 closes structurally, and criterion 4's thread clause with it.
- LIST-05's `[D]` lines and LIST-04's third `[D]` line each have a named test or reading in the summary; their `[S]` lines are untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-07-SUMMARY.md` when done
</output>
