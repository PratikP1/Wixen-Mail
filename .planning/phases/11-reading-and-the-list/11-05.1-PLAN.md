---
phase: 11-reading-and-the-list
plan: 05.1
type: execute
wave: 8
depends_on: [11-06]
files_modified:
  - src/presentation/read_aloud.rs
  - src/presentation/wx_app.rs
  - src/application/reading_habits.rs
  - tests/moving_through_the_list_marks_nothing_read.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-03]
estimate:
  tokens: 26000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The clock towards marking a message read starts only when the whole message is read from the list (the second Space, or Shift+Space) or when the message is opened (Enter); the first Space, the short form of subject, sender and snippet, starts nothing (LIST-03 amended, #25 reopened on the tester's word, 2026-09-18)."
    - "Which press counts is decided by one pure function over the depth the cycle chose, and the record is written where that depth is known, not in the closure that composes both texts before the cycle has decided."
    - "The reading from 11-05 holds it: a first Space marks nothing and a second does, Shift+Space does, moving to another row and pressing once does not; the sentence under Mark as read after and the guide say reading the whole message or opening it."
  artifacts:
    - "src/presentation/read_aloud.rs: what_a_press_starts(depth) -> WhatBegan { Nothing, TheWholeReading }, with cases."
    - "src/presentation/wx_app.rs: wire_read_aloud told what to do when the whole item is read; the mail wiring's write of reading_began inside that, the lookup closure writing nothing; the five other modules passing nothing to do."
    - "src/application/reading_habits.rs: WHAT_MARK_READ_COUNTS_FROM reworded."
    - "tests/moving_through_the_list_marks_nothing_read.rs: the read-aloud reading rewritten to the new place of the write, and the two-press case over SpaceCycle and what_a_press_starts."
    - "docs/USER_GUIDE.md, docs/changelog.md."
  key_links:
    - "Space -> wire_read_aloud -> SpaceCycle::press or press_full -> Depth -> what_a_press_starts(depth) -> TheWholeReading -> the mail wiring's on_whole closure -> WxUIState::reading_began -> mark_what_was_read asks reading_habits::whether_to_mark_read as before."
    - "Enter -> open_single_message -> reading_began, unchanged."
---

<objective>
The first Space starts no clock; the whole reading and opening do.

Purpose: #25, reopened by Pratik on 2026-09-18 on the tester's answer to
the question 11-05's close comment asked him: reading the snippet is not
enough to count as read. 11-05 wrote `reading_began` inside the mail
read-aloud closure (`wx_app.rs:3475` at `5c82f680`), which runs before
`SpaceCycle` has decided whether this press reads the short form or the
whole item, so the first Space, subject, sender and snippet, started the
clock. This plan moves the record to where the depth is known and makes
the decision a function with cases.

Output: the decision, the wiring, the reading's new cases, the sentence
under the setting, the guide's paragraph, the changelog corrected by
addition, and the close comment on #25 with the ear list. Closes #25 on
the tree side for the second time; whether the second Space and not the
first moves the count is the tester's ear.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-05-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `61865f61` (11-05 merged at
`5c82f680`, its summary after), with 11-06 executing on its branch; the
line numbers below are `5c82f680`'s where the issue's comment cites them
and `61865f61`'s otherwise, and 11-06 moves `wx_app.rs` before this plan
starts, so every one is re-run on the day. Command first, then what it
said.

**1. Where the record is written, and why the first Space writes it.**

```
sed -n 3462,3492p src/presentation/wx_app.rs   -> wire_read_aloud(&msg_list, &a11y, &space_cycle, "mail", { ... move |index| { ... s.reading_began = Some((message.message_id, now)); ... Some((message.read_id(), read_the_row(..), read_the_whole_message(..))) } })
sed -n 10018,10062p src/presentation/wx_app.rs   -> wire_read_aloud: on Space, lookup(selected) first, then cycle.press or press_full decides Depth, then the text for the depth is announced
sed -n 58,93p src/presentation/read_aloud.rs   -> SpaceCycle::press: Short on a new item, then Full, then Short again; press_full: Full outright, the next plain Space Short
sed -n 12495,12507p src/presentation/wx_app.rs   -> open_single_message writes reading_began first thing, which stays
```

The comment's account holds: the lookup closure runs before the cycle
is asked, so it cannot know which form this press reads, and it writes
the record for every press. The write belongs after the depth is known,
which is inside `wire_read_aloud`, and `wire_read_aloud` is shared by six
modules of which only mail has a clock.

**2. The reading 11-05 left, and what it asserts about the write.**

```
sed -n 74,111p tests/moving_through_the_list_marks_nothing_read.rs   -> THE_READ_ALOUD_WIRING is the block from the mail wire_read_aloud call to `let preview_visible`; reading_aloud_starts_the_clock requires THE_WRITE ("reading_began = Some(") inside it
grep -c '#\[test\]' tests/moving_through_the_list_marks_nothing_read.rs   -> 11; two records name it
sed -n 198,216p tests/moving_through_the_list_marks_nothing_read.rs   -> the hand-built windows the companions read
```

The reading asks whether the write is in the block, not where in it, so
it stays green when the write moves into the closure this plan adds and
cannot tell a first Space from a second. It is rewritten in place to ask
the sharper question, and the two-press case is a real case over the
cycle and the decision, not a reading.

**3. The sentences.**

```
sed -n 131,136p src/application/reading_habits.rs   -> WHAT_MARK_READ_COUNTS_FROM: "Counted from when you read a message aloud with Space or open it, never from moving onto it."
grep -n 'WHAT_MARK_READ_COUNTS_FROM' tests/the_settings_dialog_opens_in.rs   -> 67, 785, 788: the built Reading page is read for the constant's text, so the readback follows a reword without changing
sed -n 192,204p docs/USER_GUIDE.md   -> "When a message counts as read": "press Space or Shift+Space to hear it from the list, or Enter to open it"
grep -n 'reading a message aloud or opening' docs/changelog.md   -> 147: the 11-05 entry under Fixed
```

**4. The gate for each file.** `src/presentation/read_aloud.rs` maps to
`--lib presentation::read_aloud::`, 49 tests, 5 records, cases added and
the count check named in the red; `src/presentation/wx_app.rs` 196 tests
by `#[test]` at `61865f61` (the brief's 199 is an earlier head), 78
records, none added; `src/application/reading_habits.rs` 25 tests, 1
record, one constant reworded, no test; the target 11 tests, 2 records,
two readings rewritten in place and one case added, said;
`tests/the_settings_dialog_opens_in.rs` 7 tests, 8 records, not changed.
926 records in all by the parser. Counts taken 2026-09-18 at `61865f61`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the decision as a function, the record written where the depth is known, and the reading that tells a first Space from a second</name>
  <files>src/presentation/read_aloud.rs, src/presentation/wx_app.rs, tests/moving_through_the_list_marks_nothing_read.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/read_aloud.rs:20-95` (`Depth`, `SpaceCycle`) and its tests.
    - `src/presentation/wx_app.rs`, the mail `wire_read_aloud` call (`grep -n 'wire_read_aloud(&msg_list'`), the five PIM calls (`grep -n 'wire_read_aloud(&pim_refs'`), the function (`grep -n 'fn wire_read_aloud'`), and `open_single_message`'s first line.
    - `tests/moving_through_the_list_marks_nothing_read.rs`, whole.
    - `.planning/phases/11-reading-and-the-list/11-05-SUMMARY.md`, "What landed" and "Guard records".
  </read_first>
  <behavior>
    - `read_aloud::WhatBegan { Nothing, TheWholeReading }` and `pub fn what_a_press_starts(depth: Depth) -> WhatBegan`: `Short` answers `Nothing`, `Full` answers `TheWholeReading`; two cases in the module, and a doc naming the issue and the tester's word.
    - `wire_read_aloud` gains a parameter after `lookup`, `on_whole: impl Fn(usize) + 'static`, called with the selected index after the depth is decided and only when `what_a_press_starts(depth)` is `TheWholeReading` and the text is not empty; the mail call passes a closure that locks the state, finds the message at the index and writes `reading_began`, with 11-05's comment moved onto it; the lookup closure writes nothing; the five PIM calls pass `|_| {}` with one comment at the first saying only mail has a clock.
    - `open_single_message` is unchanged.
    - The target: `reading_aloud_starts_the_clock` becomes two readings, `the_short_form_starts_nothing` (the lookup closure, from `move |index| {` to the `Some((` it returns, holds no `THE_WRITE`) and `the_whole_reading_starts_the_clock` (the `on_whole` closure holds it, and `wire_read_aloud`'s body calls `on_whole(` under `WhatBegan::TheWholeReading`), each with its companion over a hand-built window; and one case, `test_a_first_space_marks_nothing_and_a_second_does`, over `SpaceCycle::new()` and `what_a_press_starts`: `press("mail", "1")` answers `Nothing`, `press("mail", "1")` again `TheWholeReading`, `press_full("mail", "2")` `TheWholeReading`, and after `press("mail", "3")` on a new row `Nothing` again.
    - The target's count moves from 11 to 13 (one reading split, one case added), said; `wx_app.rs`'s count does not move.
  </behavior>
  <action>
    Red first, on a branch: the module's two cases by path with the
    count check named (five records name `read_aloud.rs`), and the
    target's new readings and case bare with the count check named again
    for the target's two records, one red commit with the reason in the
    message. Then the function, the parameter, the six calls, with
    `cargo build` between. Invoke `elegant-code`: the decision is a
    `match` on `Depth` and nothing else, and `wire_read_aloud` stays one
    function.

    Guard records: one on `wx_app.rs` whose break moves the write back
    into the lookup closure, the short-form reading red; one on
    `read_aloud.rs` whose break answers `TheWholeReading` for `Short`,
    the module case and the target's case red; each measured; the
    records naming `read_aloud.rs` and the target re-measured when the
    count check names them, in the background as the file says; 11-05's
    two records on `wx_app.rs` re-measured by hand since their breaks
    are beside the moved write, and corrected rather than edited if a
    break no longer applies.
  </action>
  <verify>
    <automated>cargo test --test moving_through_the_list_marks_nothing_read && cargo test --lib presentation::read_aloud:: && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the count check.
    - `grep -c 'fn what_a_press_starts' src/presentation/read_aloud.rs` is 1; `grep -c 'what_a_press_starts' src/presentation/wx_app.rs` is at least 1; `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'reading_began = Some('` is 2 (the mail `on_whole` closure and `open_single_message`).
    - The target passes with 13 tests, quoted; `presentation::read_aloud::` with 51; `presentation::wx_app::` with its count unchanged from the day's head, quoted.
    - Two guard records measured and quoted; 11-05's two on `wx_app.rs` re-measured and quoted.
  </acceptance_criteria>
  <done>The first Space records nothing, the second and Shift+Space record that the whole message was read, opening still does, and a case and two readings hold each of those.</done>
</task>

<task type="auto">
  <name>Task 2: the sentence under the setting, the guide, the changelog by addition, and the close comment</name>
  <files>src/application/reading_habits.rs, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `src/application/reading_habits.rs:112-136`, the default's comment and the constant.
    - `docs/USER_GUIDE.md:192-204`, "When a message counts as read".
    - `docs/changelog.md`, the 11-05 entry under Fixed (found by `grep -n 'reading a message aloud or opening' docs/changelog.md`).
    - `.planning/WINDOWS.md`, ledger 539, both halves.
  </read_first>
  <action>
    `WHAT_MARK_READ_COUNTS_FROM` becomes "Counted from when you read the
    whole message aloud, with Space twice or Shift+Space, or open it;
    never from the first Space or from moving onto it." The readback in
    `tests/the_settings_dialog_opens_in.rs` reads the constant and
    follows. The default's comment and the doc on `whether_to_mark_read`
    gain the dated clause: the short form started the clock until this
    plan, #25 reopened on the tester's word.

    Invoke `writing-craft`. `docs/USER_GUIDE.md`, "When a message counts
    as read": the sentence becomes "A message counts as read once you
    have read the whole of it: press Space twice, or Shift+Space, to
    hear the message itself from the list, or Enter to open it in its
    own window. The first Space, which reads the subject, the sender and
    the snippet, does not count." The existing dated sentence about
    selection stays and gains a second: "and from the build of
    2026-09-18 until this one the first Space counted too." The
    changelog: the 11-05 entry is not rewritten; a new entry under
    `[Unreleased]`, Fixed, says the first Space started the clock in the
    build of 2026-09-18, that the tester said reading the snippet is not
    reading, that the clock now starts on the whole reading and on
    opening, and how the decision is held (one function, the reading's
    case). Known limitations: nobody has heard the count move on the
    second Space and not the first.

    Ledger, by hand, both halves: 539 amended with the second Space in
    place of the first in its ear list, dated, or closed and a new entry
    opened if its half already carries an answer; the summary says which.

    After the merge, the executor closes #25 from the merge commit with
    the ear list: `gh issue close 25 --comment "Fixed in <merge commit>:
    the first Space, the short form, starts no clock; the whole reading
    (Space again, or Shift+Space) and opening (Enter) do. The decision
    is one function over what the press reads, held by a case that a
    first Space marks nothing and a second does. The sentence under Mark
    as read after and the guide say reading the whole message or opening
    it. What only your ear settles: Space once on an unread message
    leaving the count where it is; Space again, then the count moving
    after two seconds; Shift+Space the same; Enter the same; moving off
    before the delay runs leaving it unread."` Closing an issue is not a
    publish.
  </action>
  <verify>
    <automated>cargo test --lib application::reading_habits:: && cargo test --test the_settings_dialog_opens_in && cargo test --test house_style && cargo test --test the_words_that_say_nothing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'whole message aloud' src/application/reading_habits.rs` is 1; `grep -c 'The first Space' docs/USER_GUIDE.md` is 1.
    - `cargo test --lib application::reading_habits::` passes with 25 tests, quoted; `the_settings_dialog_opens_in` with 7.
    - The guide and the changelog carry the dated sentences; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words.
    - The ledger's two halves agree; the summary names the entry.
  </acceptance_criteria>
  <done>The setting, the guide and the changelog say that reading the whole message or opening it counts and the first Space does not, and the issue is closed with the ear list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the cycle's answer to the clock | the same keystroke reads two different things depending on history, and only one of them is reading |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-77 | Tampering | a message marked read by a press that read only its row | medium | mitigate | the record written only under `TheWholeReading`; the case over the cycle and the short-form reading hold it; a record measures the break |
| T-11-78 | Denial of service | the clock never starting because the PIM modules' no-op reached mail | low | mitigate | the mail call passes the writing closure and the reading requires the write inside it |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Two guard records measured and 11-05's two re-measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the count staying on the first Space and moving on the second; what the case and the readings prove: the decision, the place of the write, the wiring.
- After the merge, the executor closes #25 with the comment in task 2.
</verification>

<success_criteria>
- Roadmap criterion 3 closes structurally with its amendment: the whole reading or opening starts the clock, the first Space does not, by one rule held by cases.
- LIST-03's amended `[D]` line has a named case and reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-05.1-SUMMARY.md` when done
</output>
