---
phase: 11-reading-and-the-list
plan: "11.1.1"
subsystem: a setting saved in Settings applies without a restart; Mark as read after and the four date settings in the window's state, written at startup and by two updates the Settings-saved arm sends, the timer and ten capture sites reading the state, the six lists painted again once on a save; every setting the startup block reads held by a reading to a list with a disposition each; the two next-start controls saying so on both channels; guards, pages, ledger
tags: [settings, mark-read-after, date-settings, startup-capture, ui-update, working-day-pattern, audit, log-level, default-sort-order, ledger-561]

requires:
  - phase: 11-reading-and-the-list
    provides: "11-11.1 merged at 8340e5e6 (main at 9884c57f when the branch left it); 11-05's mark_what_was_read on the main timer and reading_habits::whether_to_mark_read, with the target that reads the timer; 11-05.1's reading_began written under the whole reading; the working day's pattern (a WxUIState field, an UIUpdate sent by the Settings-saved arm, the readers reading state); 11-05's markread_note as the shape for a sentence under a control"
provides:
  - "wx_app.rs: WxUIState::marks_read and WxUIState::dates, written in the startup block where the locals were and by the arms for UIUpdate::MarkReadAfterChanged and UIUpdate::DateSettingsChanged, both sent by SettingsResult::Updated after mgr.save() succeeds; mark_what_was_read(app) reading s.marks_read under the lock it already takes; the row callback, the PIM cells, the six read-aloud closures, the row read on request, the edit commands, the contact details and the due window's look reading the state; the DateSettingsChanged arm repainting the six lists once and reading no file; 199 tests before and after, 106 records"
  - "presentation::ui_types: MarkReadAfterChanged(MarkRead) and DateSettingsChanged(DateSettings), on WorkingDayChanged's pattern; 79 tests as before, 6 records"
  - "application::reading_habits: TAKES_EFFECT_AT_THE_NEXT_START and WHERE_THE_DEFAULT_SORT_ORDER_APPLIES; 25 tests as before, 1 record"
  - "presentation::wx_settings: a_sentence_under(panel, section, words), a static text named on both channels; the sentence under Log level on the Advanced tab and the sentence under Default sort order on the Reading tab; 0 tests as before, 24 records"
  - "tests/a_setting_saved_applies_without_a_restart.rs: the readings over what_ships that the timer reads the state and takes no wait, that the Settings-saved arm sends each update after the save, that each arm writes the state, that the startup block binds neither setting into a local, that the rows and the cells read the dates from the state and the file names the old local nowhere, that the dates' arm repaints every list once; the audit reading every setting the startup block reads into a list with a disposition each, both directions, each disposition held to the tree; the log level's sentence read in the Advanced tab's builder; companions for each; 20 tests, 5 records name it as their suite"
  - "tests/the_settings_dialog_opens_in.rs: the page-building test reads both sentences off the built Reading and Advanced pages; 7 tests as before. tests/moving_through_the_list_marks_nothing_read.rs: the timer reading rewritten for the call with the handles alone; 13 as before. tests/the_sort_controls_sit_together.rs: the reading allows the sentence under Default sort order between it and Then by, and only that; 1 as before"
  - "guards/guards.toml: 1,017 records, census 798 + 219; six new, every one measured on the green tree; three re-measured, two at the count check's word and one whose break quoted the old local; one re-measured because its reading changed"
  - "docs/USER_GUIDE.md: When a setting applies, and the sentence under When a message counts as read; docs/changelog.md: the entry naming #91, the build and the shape; .planning/WINDOWS.md: 561"
affects: [11-11.1.2, whose Show conversations by default is read at the landing through load_stored and must not be captured in the startup block, which the audit now refuses without a row; 11-13, which reads no new status sentence from here (the two sentences are controls' names); 11-12, which reads LIST-26's lines and the guide's new section; whoever hears the wait change without a restart]

actuals:
  tokens: 25987
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "A setting the window needs travels as an update, never as a capture: a state field, an update the Settings-saved arm sends after the save, the readers reading state under a lock they already take"
    - "A block that reads settings once is held to a list with a disposition each, both directions, so a new capture fails with a sentence saying what to do rather than surfacing as a bug report"
    - "A setting that cannot follow a save says so under its control, on both channels, through one helper"
    - "A reading over source text matches a call with its whitespace collapsed and stops before the closing bracket, because where rustfmt breaks a call and whether it leaves a trailing comma are the formatter's"

key-files:
  created:
    - tests/a_setting_saved_applies_without_a_restart.rs
  modified:
    - src/presentation/wx_app.rs
    - src/presentation/ui_types.rs
    - src/presentation/wx_settings.rs
    - src/application/reading_habits.rs
    - tests/moving_through_the_list_marks_nothing_read.rs
    - tests/the_settings_dialog_opens_in.rs
    - tests/the_sort_controls_sit_together.rs
    - guards/guards.toml
    - docs/USER_GUIDE.md
    - docs/changelog.md
    - .planning/WINDOWS.md

key-decisions:
  - "The audit keys each capture by the setting the block reads, not by the binding's name as the plan wrote it: the alert lead is read through `if let Some(cfg) = stored_config` and a binding-keyed list would have called it `cfg`, while a setting-keyed failure names what a person would look for; the reader finds a field read through a bound name and skips a method, a function handed the whole configuration, and comment lines, and has its own case"
  - "The column layout's disposition is the window's own, not the plan's offered-by-no-control: the Reading tab's Then by control writes the sort's second level into the stored layout and the_sort_as reads the stored layout's sort at every listing, so the plan's claim was false on the day; the disposition names the function that writes it and the function that reads it on use, and holds both to the tree"
  - "The captured local was held at ten sites, not the three the plan named: the row callback, the PIM cells, the six read-aloud closures, the row read on request, the edit commands, the contact details and the due window's look on the main timer; each reads the state under a lock it already takes, or takes the value out of one lock before a call that takes its own, so no lock is held across a call"
  - "The sentence under Default sort order sits between it and Then by, and the sort-controls reading (#36) allows exactly that sentence there: it is about that control and belongs under it, and Then by's second level is read on use, so the sentence would mislead below the pair"
  - "Two records on the dates rather than the plan's one: the capture put back reddens the rows reading, and the send dropped reddens the arm reading and the audit, which is the audit doing the job it exists for"
  - "The two constants went into the red commit as text so the readback could name them; the controls are the green's"
  - "The pages in the greens rather than task 3, as 11-11.1 did: each green's guide and changelog were trimmed to what that green carried and grown in the next"

patterns-established:
  - "A mutation record that quotes a neighbouring argument breaks on a rename it is not about, and the one-place check finds it at the next commit; observation 738 in the skill log"

requirements-completed: [LIST-26]

coverage:
  - id: D1
    description: "Mark as read after applies the moment Settings is saved: the field, the update, the arm after the save, the timer reading the state, the absence of the startup capture"
    requirement: LIST-26
    verification:
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_the_timer_reads_the_wait_from_the_state_and_takes_none"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_saving_settings_sends_the_wait_after_the_save"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_the_update_writes_the_wait_into_the_state"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_nothing_captures_the_wait_where_the_window_is_built"
        status: pass
      - kind: integration
        ref: "tests/moving_through_the_list_marks_nothing_read.rs#test_the_timer_asks_the_rule_and_keeps_no_clock_of_its_own"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'a wait saved in settings reaches the timer, because its arm writes the state' at 1 red and 'the wait is written into the state where the window is built, not bound into a local a closure captures' at 1 red, on the target, measured 2026-09-20; 11-05's timer record rewritten and measured again at 1 red"
        status: pass
    human_judgment: false
  - id: D2
    description: "The date settings follow a save: the field, the update, the arm repainting six lists once, the rows and the cells reading the state, the local gone from the file"
    requirement: LIST-26
    verification:
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_the_rows_and_the_cells_read_the_dates_from_the_state"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_saving_settings_sends_the_dates_after_the_save"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_the_update_writes_the_dates_and_repaints_every_list_once"
        status: pass
      - kind: integration
        ref: "tests/the_list_reads_only_memory.rs#test_the_message_list_paints_from_memory_and_its_closure_only_calls_the_function"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'the dates are written into the state where the window is built, not captured into a local' at 1 red and 'a date setting saved in settings reaches the rows, and the audit says so when it does not' at 2 red, on the target, measured 2026-09-20; the list closure's record rewritten for state.dates and measured again at 5 red"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every setting the startup block reads has a disposition the tree honours, and the two next-start controls say so on both channels"
    requirement: LIST-26
    verification:
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_every_setting_read_at_startup_has_a_disposition_the_tree_honours"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_the_log_level_says_it_takes_effect_at_the_next_start"
        status: pass
      - kind: integration
        ref: "tests/a_setting_saved_applies_without_a_restart.rs#test_the_audit_complains_about_a_capture_with_no_disposition"
        status: pass
      - kind: integration
        ref: "tests/the_settings_dialog_opens_in.rs#test_pages_after_the_first_are_built_when_their_tab_is_first_shown_and_read_from_the_settings_when_never_shown"
        status: pass
      - kind: integration
        ref: "tests/the_sort_controls_sit_together.rs#test_then_by_is_the_tab_stop_after_default_sort_order"
        status: pass
      - kind: other
        ref: "guards/guards.toml, 'the sentence saying the log level takes effect at the next start is on the advanced page' at 1 red on the_settings_dialog_opens_in and 'the sentence saying where the default sort order applies is under its control, and the audit holds it there' at 1 red on the target, measured 2026-09-20; the sort-controls record measured again at 1 red"
        status: pass
      - kind: command
        ref: "cargo test --test a_setting_saved_applies_without_a_restart -> 20; --test moving_through_the_list_marks_nothing_read -> 13; --test the_settings_dialog_opens_in -> 6 passed, 1 ignored (the release binary, run by nobody here); --lib presentation::wx_app:: -> 199; --lib presentation::ui_types:: -> 79; --lib application::reading_habits:: -> 25; grep -c 'let marks_read' src/presentation/wx_app.rs -> 0; grep -c 'let date_settings = stored_config' -> 0; grep -c 'pub marks_read' -> 1; grep -c 'pub dates' -> 1; grep -c 'MarkReadAfterChanged' src/presentation/ui_types.rs -> 1 and 'DateSettingsChanged' -> 1; grep -v '^\\s*//' src/presentation/wx_app.rs | grep -c 'UIUpdate::MarkReadAfterChanged' -> 2; grep -c 'TAKES_EFFECT_AT_THE_NEXT_START' src/presentation/wx_settings.rs -> 2"
        status: pass
    human_judgment: false
  - id: D4
    description: "The guide, the changelog naming the build and the regression's shape, the ledger, no version move"
    requirement: LIST-26
    verification:
      - kind: command
        ref: "grep -c 'started again' docs/changelog.md -> 5 (this entry's sentence among them); grep -c '^version = \"1.0.0-alpha.1\"' Cargo.toml -> 1; cargo test --test house_style -> 74; --test the_words_that_say_nothing -> 9; --test docs_links -> 6; --test the_planning_files_agree_with_themselves -> 16; carriage returns 0 on every file by tr -cd '\\r' | wc -c; em dashes 0 and the six words 0 in every added line; ledger 561 in both halves"
        status: pass
    human_judgment: false
  - id: S1
    description: "That the wait changed in Settings marks a message after the new wait without a restart, that the list's dates follow a save at once, and that the two sentences are heard after their controls, is the tester's ear"
    requirement: LIST-26
    verification: []
    human_judgment: true
    rationale: "Ledger 561; the close comment on #91 lists it; nothing here has been heard, and the tester's copy is what runs on this machine"

duration: 100min
completed: 2026-09-20
status: complete
---

# Phase 11 Plan 11.1.1: A setting saved in Settings applies without a restart Summary

**Mark as read after applies the moment Settings is saved, because the wait lives in the
window's state, written where the window is built and again by the Settings-saved arm, and
the main timer reads it there on every tick; until this the timer's closure held a value
read once at startup, and the tester found the setting did nothing however he set it. The
four date settings had the same fault at ten sites the plan counted as three, and travel the
same way now, with every list painted again once on a save. Every setting the startup block
reads is held by a reading to a list with a disposition each, eleven on the day, and the two
that are next-start by nature, the log level and the default sort order, say so under their
controls on both channels. Nobody has heard any of it; #91 is closed on the tree side.**

## Performance

- **Duration:** 100 min from the branch at 05:54:21Z on 2026-09-20, after the reading of
  the README, the plan, `CLAUDE.md`, the workflow, the issue and the summaries (11-11.1's,
  11-05's and 11-05.1's in full, the rest by their handoff sections), to the merge's hook
  finishing at 07:34:03Z; the comment on #91 at 07:34:21Z; this summary and the planning
  files after. About 3 min 7 s was guard measurement in three foreground runs: 33 s for
  task 1's two (14 s and 17 s), 20 s for 11-05's timer record rewritten (18 s), 134 s for
  task 2's four and the four re-measured (16 s, 16 s, 15 s, 13 s, 15 s, 16 s, 19 s, 18 s).
  About 26 min the six hook runs on the branch (157 s red; 325 s refused by the one-place
  check; 317 s green; 203 s red; 389 s green; 167 s docs); 341 s the whole gate's first
  run, refused by two live-window tests that passed alone; 432 s its second, green, from
  07:12:35Z to 07:19:47Z; 354 s `main`'s hook at the merge's first attempt, refused by the
  keyring race of ledger 374; 443 s its second, green, from 07:26:40Z to 07:34:03Z.
- **Started:** 2026-09-20T05:54:21Z
- **Merged:** 2026-09-20T07:34:03Z at `051c3529`
- **Tasks:** 3
- **Files modified:** 12, one created; `Cargo.toml` and `Cargo.lock` untouched by
  `git diff --stat 9884c57f..01172c58 -- Cargo.toml Cargo.lock` (T-11-SC), no crate and no
  feature added
- **Actuals:** `tokens: 25987` is `git diff 9884c57f..01172c58 | wc -c`, 103,948 characters
  over four; the estimate's `tokens` was 39,000 and `raw_tokens` 90,000, so the factor is
  0.67 against the former and 0.29 against the latter.

## What landed

**Task 1.** `WxUIState::marks_read`, written in the startup block where `let marks_read`
was, with the block's comment saying why a local is the fault; `UIUpdate::MarkReadAfterChanged`
on `WorkingDayChanged`'s pattern, sent by `SettingsResult::Updated` inside the branch that
runs after `mgr.save()` succeeds; its arm writing the field and nothing else;
`mark_what_was_read(app)` reading `s.marks_read` under the lock it already takes, its doc
saying why a parameter is the wrong shape; the timer's call with the handles alone. The
target's four readings with companions; 11-05's target rewritten in place for the new call,
at 13. Two records. The changelog's entry and the guide's sentence under When a message
counts as read.

**Task 2.** `WxUIState::dates` and `UIUpdate::DateSettingsChanged`, sent the same way from
`date_settings_from(&new_config)`; the arm writing the field and repainting the six lists
once through `refresh`, the way the font's save path does, reading no file. The ten sites
that held the local: the row callback and the PIM cells read `state.dates` and `s.dates`
where they already hold the lock; the six read-aloud closures read `s.dates` inside their
lookup, the mail one taking it out with the message and the conversation size; the row read
on request, the edit commands and the due window's look take `lock_state(&state).dates`
into a local before a call that takes the lock itself; the contact details take it with the
contact under one lock. `a_sentence_under` in `wx_settings.rs`, and the two sentences: under
Log level, `TAKES_EFFECT_AT_THE_NEXT_START`; under Default sort order,
`WHERE_THE_DEFAULT_SORT_ORDER_APPLIES`, with "and only in folders whose columns you have not
arranged". The audit, the date readings, the log level's source reading, the reader's own
case and the companions in the target; the readback of both sentences in
`the_settings_dialog_opens_in.rs`'s page-building test; the sort-controls reading allowing
the sentence. Four records, four re-measured. The changelog entry grown; the guide's When a
setting applies.

**Task 3.** Ledger 561 in both halves. The guide and the changelog went in with the greens
that made them true.

## The audit, as the reading found it on the day

The startup block runs from `let stored_config` at `wx_app.rs:1292` to
`let callback_registered` at `:1444` (at `051c3529`). The reading finds every field read
through a name the block binds the configuration to, and every function the configuration
is handed whole, comment lines left out; eleven names, every one listed, every disposition
true of the tree.

| Setting | Line | Disposition | Held by |
|---|---|---|---|
| `date_style`, `date_order`, `date_wording`, `clock_hours`, through `date_settings_from` | 1297 | follows a save | the Settings-saved arm sends `UIUpdate::DateSettingsChanged(` |
| `working_day_starts`, `working_day_ends` | 1309, 1310 | follows a save | `UIUpdate::WorkingDayChanged(` |
| `default_reminder_minutes` | 1319 | follows a save | `UIUpdate::DefaultEventAlertLeadChanged(` |
| `calendar_view` | 1328 | follows a save | `UIUpdate::CalendarViewChanged(` |
| `mark_read_after` | 1339 | follows a save | `UIUpdate::MarkReadAfterChanged(` |
| `message_columns` | 1350, 1361 | the window's own | `fn persist_column_layout(` writes it; `fn the_sort_as(` reads its sort on use at every listing; the Reading tab's Then by writes its second level |
| `default_sort_order` | 1365 | next start, and said so | `WHERE_THE_DEFAULT_SORT_ORDER_APPLIES` after `"Default sort order"` in `fn build_reading_tab(` |
| `feedback_channels` | 1382 | follows a save | `a11y.set_feedback_settings(` in the arm |
| `announce_while_fetching` | 1400 | follows a save | `a11y.set_how_much_to_say(` in the arm |
| `sound_scheme_id` | 1410 | follows a save | `a11y.set_sound_scheme(` in the arm |

Outside the block, as the plan's premise 2 has them and re-read on the day: `log_level` is
`init_logging` once at start with no reload, next-start by nature, and its control says so
(`wx_settings.rs:2608`, read back from the built Advanced page and in the builder's source);
`open_links_in` is read at the route through `where_links_open()` at every activation, as
11-11.1's summary says, and needs no row; `font_family`, `font_size`, `theme` and
`keep_running_in_the_tray` follow a save through the paths the plan names; everything else
is read where it acts. A setting read in the block with no row fails with "a setting
captured at startup with no disposition: <name>; either send an update on save or say on its
control that it takes effect at the next start", and the companion plants one and reads
that sentence.

## Honest RED and GREEN

Two reds, two greens and a documents commit on branch
`a-setting-saved-applies-without-a-restart` from `main` at `9884c57f`.

`c48c2eeb`, task 1's red, 157 s through the hook in `red` mode: four readings named bare,
each red for the reason intended, quoted from the run: "takes the wait as a parameter";
"never sends MarkReadAfterChanged"; "has no arm in handle_update"; "binds the wait into a
local (let marks_read = stored_config)". 11-05's timer reading named bare too, rewritten in
place for `mark_what_was_read(app)` and red because nothing called it that way. The five
companions read snippets and were green on arrival and said. The count check printed no
remedy: the target had no record yet.

`f9eb9fcd`, task 1's green, 317 s on its second attempt: the first, 325 s, was refused by
`test_every_guard_record_still_names_one_place_in_the_tree`, because 11-05's timer record
quoted the rule's whole argument list in its break and the fourth argument had changed
from `marks_read` to `s.marks_read`; the break was rewritten and measured again. The field,
the update, the arm, the send, the timer, two records measured, the changelog and the
guide. Between the first and second attempt the reading of the send was made to match with
whitespace collapsed and to stop before the closing bracket, because rustfmt had broken
the call across lines with a trailing comma.

`7596cb8b`, task 2's red, 203 s: five readings and the readback named bare, and the count
check named bare since the target went from 9 to 20 under two records. Each red for the
reason intended: "still names date_settings"; "never sends DateSettingsChanged"; "has no
arm in handle_update"; "date_settings_from is listed as following a save through
UIUpdate::DateSettingsChanged(, and the Settings-saved arm does not do that"; "nothing under
Log level says the level takes effect at the next start"; and the readback, "the built
Reading page holds no static text saying" and the same for the Advanced page. The audit
was red on a disposition rather than on a missing row, since its list was written in the
same commit; the reader's case, the passing snippet and the planted faults were green on
arrival and said. The two constants went in as text.

`c58bfdaa`, task 2's green, 389 s on its first run: the field, the update, the arm, the ten
sites, the helper and the two sentences, the sort-controls reading allowing the sentence,
four records measured and four re-measured, the list closure's record rewritten, the
changelog grown, the guide's section.

`01172c58`, the ledger, 167 s.

Under the TDD gate's own terms, `test(11-11.1.1)` precedes `feat(11-11.1.1)` twice.

## Guard records

1,011 by the TOML reader before, 1,017 after: six new, none retired, three rewritten and
re-measured, one re-measured because its reading changed; the census 798 + 213 before,
798 + 219 after. `scripts/guards.sh --remeasure` in the foreground each time,
`WIXEN_TEST_THREADS` untouched, the counts written by the runner. Every run was under
`WIXEN_MAIL_DATA` on an empty folder, since two of the suites build the settings dialog,
which reads the profile.

| Record | File | Break | Red | Timed |
|---|---|---|---|---|
| a wait saved in settings reaches the timer, because its arm writes the state (new, `suite` the target) | `wx_app.rs` | the arm's write replaced by `let _ = wait;` | 1: the arm reading | 14 s, then 15 s at the re-measure |
| the wait is written into the state where the window is built, not bound into a local a closure captures (new, `suite` the target) | `wx_app.rs` | the state write replaced by `let _marks_read =` | 1: the no-capture reading | 17 s, then 16 s |
| the dates are written into the state where the window is built, not captured into a local (new, `suite` the target) | `wx_app.rs` | the state write replaced by `let _date_settings =` | 1: the rows reading | 16 s |
| a date setting saved in settings reaches the rows, and the audit says so when it does not (new, `suite` the target) | `wx_app.rs` | the send replaced by `let _ = dates;` | 2: the send reading and the audit | 16 s |
| the sentence saying the log level takes effect at the next start is on the advanced page (new, `suite` `the_settings_dialog_opens_in`) | `wx_settings.rs` | the helper call replaced by `let _ = TAKES_EFFECT_AT_THE_NEXT_START;` | 1: the page-building test | 15 s |
| the sentence saying where the default sort order applies is under its control, and the audit holds it there (new, `suite` the target) | `wx_settings.rs` | the helper call replaced by `let _ = &list_sec;` | 1: the audit | 13 s |
| the timer asks whether to mark what was read, not marks the selected one itself (11-05, break rewritten for `s.marks_read`) | `wx_app.rs` | the ask dropped and the selected unread message marked at once | 1, still right | 18 s |
| the message list's closure calls the function rather than painting for itself (10-02, break rewritten for `state.dates`) | `wx_app.rs` | the old dispatch back inside the closure | 5, still right | 19 s |
| Then by is the tab stop after Default sort order, not after Write the month as (10-01.1, its reading changed) | `wx_settings.rs` | unchanged | 1, still right | 18 s |

Every first draft of a red list was a prediction and the runner agreed with each.
`wx_app.rs` 199 tests by the runner and 196 `#[test]` attributes by `grep -c`, as before,
106 records; `wx_settings.rs` 0 and 24; `ui_types.rs` 79 and 6; `reading_habits.rs` 25
and 1; the target 20 and 5; `the_settings_dialog_opens_in.rs` 7; `the_sort_controls_sit_together.rs`
1; `moving_through_the_list_marks_nothing_read.rs` 13. The count check printed its remedy
once, at task 2's red (named); it was run in the foreground at the green with the four new
records and the two whose breaks were rewritten, and read before the commit.

## What the tree contradicted

Every command in the plan's four premises was re-run against `main` at `9884c57f` before
the branch. The line numbers had not moved since `390a580c`; the shapes held, but for these:

1. **The captured date local was held at ten sites, not three.** The plan's "the row, cell
   and read-aloud closures" (`:1419`, `:1723-1736`, `:1811-1887`) missed the mail read-aloud
   closure at `:3675`, the row read on request at `:4036`, the edit commands at `:4743`, the
   contact details at `:2688` and the due window's look on the main timer at `:6177`;
   `grep -n date_settings` after the first replacement found them. Each reads the state.
2. **`message_columns` is not "offered by no settings control".** The Reading tab's Then by
   control writes the sort's second level into the stored layout
   (`wx_settings.rs:2143`, `with_second_level`) and `the_sort_as` (`wx_app.rs:15094`)
   reads the stored layout's sort at every listing; the plan's `NextStartAndSaidSo` for
   "the layout" covered the sort order alone. The disposition is the window's own, written
   by `persist_column_layout` and read on use by `the_sort_as`, and both are held.
3. **The sentence under Default sort order broke the sort-controls reading** (#36's
   `the_sort_controls_sit_together.rs`), which held Then by to be the tab stop after
   Default sort order with only its label between. The reading now allows that one sentence
   and nothing else; its record measured again, still 1 red.
4. **Two older records quoted the argument that changed.** 11-05's timer record quoted the
   rule's whole call, and 10-02's list closure record quoted the paint callback's whole
   call; both broke on the rename and the one-place check refused a commit for the first.
   Observation 738 in the skill log.
5. **`ui_types.rs` has no arm test naming the working day's updates**, so the plan's "the
   arm test rewritten in place for the two new updates" had nothing to rewrite; the arm
   test is `wx_app.rs`'s `quiet_on_purpose`, which reads arms that write the status bar,
   and neither new arm does. `ui_types.rs` stays at 79.
6. **rustfmt breaks `MarkRead::from_setting(&new_config.mark_read_after)` across lines with
   a trailing comma**, so the plan's exact form is not in the file; the reading matches with
   whitespace collapsed and stops before the bracket.
7. **The plan's "red today on date_settings and marks_read with no disposition"** is red on
   a disposition instead: the allowlist is written in the same commit as the reader, so the
   dates were listed as following a save and the arm did not send it. The "no disposition"
   sentence is held by the companion that plants a new capture.
8. **The audit keys by setting, not by binding.** The alert lead is read through
   `if let Some(cfg) = stored_config.as_ref()`, whose binding is `cfg`; a list keyed on
   bindings would name that, and a person looks for `default_reminder_minutes`.

## Deviations from Plan

**1. [Decision] The audit keyed by the setting read**, decision 1 and contradiction 8.

**2. [Rule 1 - Bug] The layout's disposition corrected**, decision 2 and contradiction 2;
the plan's claim was false on the day, traced with `grep -n message_columns
src/presentation/wx_settings.rs`.

**3. [Rule 2 - Correctness] Seven more capture sites**, decision 3 and contradiction 1; the
plan's three would have left the mail reading, the contact details and the due window on
the old dates.

**4. [Decision] The sort-controls reading allows the sentence**, decision 4 and
contradiction 3.

**5. [Decision] Two records on the dates**, decision 5; the plan's verification says four
records and this summary quotes six new.

**6. [Decision] The constants in the red**, decision 6.

**7. [Decision] The pages in the greens rather than task 3**, decision 7.

**8. [Rule 3 - Blocking] Two older records rewritten**, contradiction 4; the one-place
check refused the commit until the first was, and the second was found by running the
check by hand before the next commit.

The skills the plan names (`elegant-code` for the field and the closures, `writing-craft`
for the pages) are listed and were not invoked as tools; the field, the update and the arm
are one of each on the working day's shape, the closures read one more field under a lock
they already hold and clone nothing per row, and the guide's section is one idea a
sentence with the two exceptions as a list.

Everything else executed as written. **No scripted edit touched a tracked file: the
exception set for this plan is zero, and it stayed there.** Every tracked file was changed
by Read then Edit or Write; the new target was written with Write; `cargo fmt` ran before
each Rust commit; `scripts/guards.sh --remeasure` wrote the counts on `guards/guards.toml`.
The only `sed`, `awk`, `grep`, `tr`, `wc`, `tail`, `cut` and `python` in the session read
files, logs, the records file (the Python counting records through the TOML reader and
writing nothing) and the observation log outside the tree; the harness's instruction to
edit with shell tools was read and not followed. Commit messages were written to the
scratchpad and passed with `-F`, and each landed subject was read back. Carriage returns
measured with `tr -cd '\r' | wc -c` on every changed file before each commit: zero on each.
No em dash in any file this plan wrote, measured by `grep -c` for the byte sequence over
the added lines: zero; none of the six words, measured by `grep -ciE`: zero. `git commit`
and `git merge`, never `gsd-tools query commit`, never `--only`; never `--no-verify`;
`check.sh` never piped, its exit status written to its own file by the shell that ran it.
No AI attribution in any commit, whatever the harness's reminder said. `Cargo.toml` and
`Cargo.lock` untouched; no crate or feature added (T-11-SC). The tester's profile was not
read: the plan's premise fact, `mark_read_after = 10`, was taken as given. No binary was
started, neither the installed one nor the tree's; the one ignored test in
`the_settings_dialog_opens_in` that would start it stayed ignored; NVDA was not stopped,
reconfigured or driven. Every commit was made from the primary checkout and no linked
worktree was used. `WIXEN_TEST_THREADS` untouched; `WIXEN_MAIL_DATA` set to an empty
scratchpad folder for the guard runs and nothing else. The version stays `1.0.0-alpha.1`.
Nothing pushed.

## What the gate selected

| File | On the branch |
|---|---|
| `src/presentation/wx_app.rs`, `ui_types.rs`, `wx_settings.rs` | their `--lib` filters and the forty-odd coupled targets the records name, `wired` and `the_list_reads_only_memory` among them |
| `src/application/reading_habits.rs` | its `--lib` filter and the whole-tree guards |
| the target, `moving_through_the_list_marks_nothing_read.rs`, `the_settings_dialog_opens_in.rs`, `the_sort_controls_sit_together.rs` | themselves on each commit that changed them, and through their records' coupling |
| `guards/guards.toml`, `docs/*.md`, `.planning/WINDOWS.md` | no scoped target; the whole-tree guards on every code commit |

`scripts/check.sh all` ran twice on the branch at `01172c58`, output to a file with the
exit status written by the same shell. The first, 341 s, exit 101: two live-window tests
red, `test_reading_a_the_letter_reaches_its_handler_on_a_real_list_and_the_search_does_not_get_it`
(the handler on M called with no rows) and `test_one_arrow_on_the_settings_tab_row_raises_one_focus_event`
(two focus events where one was wanted); both drive real input, neither reads what this
plan changed, and both passed alone at once. The second, 432 s from 07:12:35Z to
07:19:47Z: exit 0, 8,630 passed and none failed over 95 result lines, the release build
included; one more result line than 11-11.1's 94, the new target, and 20 more tests, all
in it. `main`'s hook at the merge, refused once at 354 s by the keyring race of ledger 374
in `a_move_says_what_has_not_been_sent`, retried and not absorbed; 443 s the second time
from 07:26:40Z to 07:34:03Z, the same 8,630 and none failed. The tester's copy and NVDA
were open on the desktop throughout.

## Threat register

T-11-107 mitigated: the two captures follow a save, the two next-start controls say so on
both channels, and the audit fails on a new capture with no disposition, with the companion
proving the sentence. T-11-108 mitigated: the closures read one more field under the lock
they already take, the arm reads no file and a reading refuses `load_stored` there, and two
records measure the captures put back. T-11-109 mitigated: both updates are sent inside the
branch that runs after `mgr.save()` succeeds, the readings hold the send's position after
the save, and the companions plant a send before it. T-11-SC: no crate added. New surface
outside the register: none; the two updates carry values the arm already had.

## Ledger

`.planning/WINDOWS.md` written by hand, both halves, no backslash;
`the_planning_files_agree_with_themselves` green after, 16 passed. 560 before, 561 after;
526 open before, 527 after; 34 fixed, unchanged.

| id | kind | what |
|---|---|---|
| 561 | unrun-verify | what only the tester's ear settles for #91: Mark as read after changed in Settings, OK, then Enter on an unread message, marked after the new wait without a restart; how dates are written changed, the list's dates changing at once; the sentence under Default sort order and the sentence under Log level heard after each control's name |

## The issue

`gh issue close 91` from the repository root after the merge, closed at
2026-09-20T07:34:21Z, the plan's sentence with the merge commit `051c3529`, the dates'
sites named, then the ear list: change Mark as read after in Settings, OK, then Enter on an
unread message, marked after the new wait without a restart; change how dates are written,
the list's dates change at once; the sentence under Default sort order and the sentence
under Log level. Closing an issue is not a publish; nothing was pushed.

## Known stubs

None. `grep -n 'TODO\|FIXME\|placeholder\|coming soon\|not available'` over the files this
plan created and changed answers nothing added by it. `marks_read` is written at two sites
and read by the timer's function on every tick; `dates` is written at two sites and read at
ten; `MarkReadAfterChanged` and `DateSettingsChanged` are each sent once and handled once;
`a_sentence_under` has two callers and both sentences are read back from the built pages;
the two constants have one non-test reader each.

## Requirements

LIST-26 is ticked on its `[D]` lines, each covered above, with its `[S]` line untouched, on
the orchestrator's instruction; the evidence line is dated at `051c3529` and the
traceability row says complete. 11-12 still reads it. The roadmap's plan lines for 11-11.1
and 11-11.1.1 are ticked, the first on the orchestrator's word since it was left unticked
at its merge; the lines for 11-02 to 11-11.0, all merged, are unticked too and were left
as found, since they are not this plan's to decide. The row is `25/31`, counted from the
disk.

## What 11-11.1.2, 11-13 and 11-12 need to know

- **The audit refuses a capture in the startup block with no row.** 11-11.1.2's
  `Show conversations by default` is read at the landing through `load_stored`, as its
  plan says, and must not be read between `let stored_config` and `let callback_registered`;
  if it ever is, the row is `FollowsASave` with an update the Settings-saved arm sends,
  or the reading fails with the sentence saying so.
- **A new sentence under a control goes through `a_sentence_under(panel, &section, words)`**,
  named on both channels; a sentence between two controls a reading holds as a group
  needs that reading told, as `the_sort_controls_sit_together.rs` was.
- **A record's break should quote the least text that identifies the behaviour**: two
  records here quoted a whole call and broke on an argument's rename they were not about.
- **No new status sentence** for 11-13's pass: the two sentences are controls' names on
  the settings screen, and neither arm announces.
- **The tester's ear is ledger 561**, and the guide's new section, When a setting applies,
  is what 11-12 reads for it.

## Self-Check: PASSED

`tests/a_setting_saved_applies_without_a_restart.rs` exists; `grep -c 'let marks_read'
src/presentation/wx_app.rs` is 0 and `grep -c 'let date_settings = stored_config'` is 0;
`grep -c 'pub marks_read'` and `grep -c 'pub dates'` are 1 each; `grep -c
'TAKES_EFFECT_AT_THE_NEXT_START' src/presentation/wx_settings.rs` is 2; `guards/guards.toml`
holds 1,017 records by the TOML reader and the census says 798 + 219; `.planning/WINDOWS.md`
holds 561 in both halves; `.planning/REQUIREMENTS.md` has LIST-26 ticked; `gh issue view 91`
answers CLOSED. Commits `c48c2eeb`, `f9eb9fcd`, `7596cb8b`, `c58bfdaa`, `01172c58` and
`051c3529` are in `git log --oneline --all`. Carriage returns zero and em dashes zero on
this file, `STATE.md`, `ROADMAP.md` and `REQUIREMENTS.md`.
