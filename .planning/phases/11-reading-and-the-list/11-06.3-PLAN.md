---
phase: 11-reading-and-the-list
plan: 06.3
type: execute
wave: 11
depends_on: [11-06.2]
files_modified:
  - scripts/shell-suite.sh
  - scripts/which-checks.test.sh
  - CLAUDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [FOUND-19]
estimate:
  tokens: 13000
  raw_tokens: 30000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A shell suite that builds a repository of its own acts on that repository whatever git environment the commit hook handed it: shell-suite.sh clears GIT_DIR, GIT_WORK_TREE, GIT_INDEX_FILE, GIT_PREFIX and GIT_COMMON_DIR before any suite's first git, so a hook run from a linked worktree, or during a partial commit, cannot make the suite write to the real repository (FOUND-19, #85)."
    - "A case is red under an exported absolute GIT_DIR and an exported GIT_INDEX_FILE pointing at a throwaway repository, and asserts that repository's HEAD, config and index did not move; it is named which-checks::<description> in the red trailer."
  artifacts:
    - "scripts/shell-suite.sh: the five variables unset at the top, with the comment naming the two incidents of 2026-09-18."
    - "scripts/which-checks.test.sh: the two cases, and the own-repository case unchanged in what it asserts."
    - "CLAUDE.md: the rule about linked worktrees rewritten from a prohibition to what the suites now guarantee, dated; docs/changelog.md."
  key_links:
    - "the commit-msg hook -> check.sh -> bash scripts/*.test.sh with GIT_DIR (absolute in a linked worktree), GIT_INDEX_FILE (a temporary index in a partial commit) exported -> shell-suite.sh sourced first -> the variables gone -> git -C <its own repo> resolves that repo's .git."
---

<objective>
The gate's suites cannot act on the real repository, whatever the hook
hands them.

Purpose: #85, found 2026-09-18 while planning documents were committed
through the hook from the linked worktree `wixen-mail-sweep`. Git hands
a hook an absolute `GIT_DIR` there; `scripts/which-checks.test.sh:246-262`
builds a repository of its own with `git -C <tempdir>`, and under an
absolute `GIT_DIR` those commands act on the real repository: it set
`core.bare`, replaced `core.hooksPath`, wrote a suite identity into
`.git/config` and put a stray commit on `main`, all undone by hand. Later
the same evening a partial commit (`git commit --only <paths>`) from the
primary worktree handed the hook a temporary index as an absolute
`GIT_INDEX_FILE`, and the suite's own-repository case answered `all`
because its subject read the real commit's index. Guardrail 4: a check
that reads as testing itself while writing to the thing it was meant to
leave alone. In the main checkout the relative `.git` is what has kept
the case isolated, by luck.

Output: the harness clearing the git environment for every suite, two
cases red under the two exported variables against a throwaway
repository, the rule in `CLAUDE.md` rewritten, the changelog. Closes #85.
Early, right after 11-06.2, because every executor's hook runs the
suites.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `4d9f14bf`, working tree clean.
Command first, then what it said.

**1. Where the suites get their git environment, and which of them run
git.**

```
sed -n 40,44p .githooks/commit-msg   -> exec "$root/scripts/check.sh" --message-file="$1": the hook is the gate, with git's hook environment intact
sed -n 320,328p scripts/check.sh   -> for suite in scripts/*.test.sh: bash "$suite" >> "$run_log": each suite is a child of the hook and inherits its environment
sed -n 16,20p scripts/which-checks.test.sh   -> root, subject, then `. shell-suite.sh` sourced first
grep -n 'git ' scripts/*.test.sh | grep -v '#'   -> which-checks.test.sh alone: 246-262, the own-repository case
sed -n 195,200p scripts/which-checks.sh   -> the subject reads `git diff --cached`, which under an exported GIT_INDEX_FILE is the hook's index wherever the subject is run from
```

**2. What git exports to a hook.** From git's own documentation of
hooks and environment: `GIT_DIR` (relative `.git` in the main checkout,
the absolute path under `.git/worktrees/<name>` in a linked worktree),
`GIT_INDEX_FILE` (the real index, or the temporary one a partial commit
builds), `GIT_WORK_TREE` where set, `GIT_PREFIX` when run from a
subdirectory, `GIT_COMMON_DIR` in a linked worktree. `git -C <dir>`
changes the directory and not the repository once `GIT_DIR` is absolute.
The two incidents are the two variables; the other three are cleared for
the same reason before they find their own day.

**3. The gate for each file.** `scripts/shell-suite.sh` and
`scripts/which-checks.test.sh` are shell, run by `check.sh` in every
mode; a red case is named `which-checks::<description>` and is measured
by `red-commit.sh` from the `test NAME ... FAILED` line `shell-suite.sh`
prints; a description may hold no comma and no ` ... `. `CLAUDE.md` is a
document read by `tests/house_style.rs` (74 tests). No guard record: the
records file names breaks in Rust, and the suites are their own gate.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the harness clears the git environment, two cases red under the exported variables against a throwaway repository, and the rule rewritten</name>
  <files>scripts/shell-suite.sh, scripts/which-checks.test.sh, CLAUDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `scripts/shell-suite.sh`, whole; `scripts/which-checks.test.sh:1-60` and `:240-270`; `scripts/red-commit.sh:1-40`; `scripts/check.sh:320-345`.
    - `CLAUDE.md`, "A case in a shell suite is committed red the same way" (`grep -n 'which-checks::' CLAUDE.md`).
    - The README's rule "Never commit through the hook from a linked worktree" in `.planning/phases/11-reading-and-the-list/README.md`.
  </read_first>
  <behavior>
    - Two cases in `which-checks.test.sh`, each building a throwaway repository `elsewhere` (`git init` in the scratch directory, one commit, `core.hooksPath` pinned at an empty directory the way the own-repository case does) and then running the own-repository steps with the variable exported for those commands only: (a) `GIT_DIR` set to `elsewhere`'s absolute `.git`; the case asserts afterwards that `elsewhere`'s `HEAD` is the commit it made, that `git -C elsewhere config core.bare` is unset or false and its `core.hooksPath` is the pinned directory, and that the subject run from the own repository answered `affected`; (b) `GIT_INDEX_FILE` set to a copy of `elsewhere`'s index at an absolute path; the case asserts that file's bytes are unchanged and the subject answered `affected`. Both are red before the harness change: under (a) the own-repository `git init`, `config` and `commit` land in `elsewhere` and `HEAD` moves; under (b) the subject reads the exported index and answers `all`, as it did on 2026-09-18.
    - Descriptions that a commit can name, no comma and no ` ... `: "a repository of its own under an absolute GIT_DIR pointing elsewhere leaves that elsewhere untouched" and "a repository of its own under an exported GIT_INDEX_FILE reads its own index".
    - `shell-suite.sh` unsets `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_PREFIX` and `GIT_COMMON_DIR` immediately after `set -uo pipefail`, with a comment naming the two incidents, the date, the reason `-C` is not enough, and that the gate's own decision in `check.sh` is made before the suites run and is not touched by this.
    - Every existing case still passes from the primary worktree with the hook on, and the whole gate is green through the hook.
  </behavior>
  <action>
    Red first, on a branch: the two cases named in the trailer as
    `which-checks::<description>` per `CLAUDE.md`'s shell-suite rule, and
    nothing else red; the message says the real repository is never the
    subject of either case. Then the five `unset`s in `shell-suite.sh`
    with `bash scripts/which-checks.test.sh` run by hand between, its
    exit status read directly, never piped.

    Then the run that reproduces the hook's environment without a hook:
    from the repository root, after the green and never before it,
    `GIT_DIR="$(git rev-parse --absolute-git-dir)" bash scripts/which-checks.test.sh`
    with its exit status read directly, and the real repository's `HEAD`,
    `.git/config` and reflog read before and after; the summary quotes
    the run and the three unchanged. Before the green that command is the
    incident itself, which is why the order is fixed.

    `CLAUDE.md`: the paragraph under "A case in a shell suite is
    committed red the same way" gains two sentences, dated: the suites
    clear the git environment they inherit from a hook, and why; the
    README's "Never commit through the hook from a linked worktree" is
    left as the record of the day and gains one line pointing here. The
    changelog entry under `[Unreleased]`, Fixed, for the gate itself:
    what the suite did on 2026-09-18, what was undone by hand, and what
    the harness now guarantees. Ledger, by hand, both halves: the entry
    the day's README rule opened, if one exists, closed with this
    commit; else none.

    Invoke `writing-style` for the commit messages and `writing-craft`
    for the `CLAUDE.md` sentences.
  </action>
  <verify>
    <automated>bash scripts/which-checks.test.sh && bash scripts/check.test.sh && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the two cases as `which-checks::<description>` and nothing else fails.
    - `grep -c 'unset GIT_DIR' scripts/shell-suite.sh` is 1 and the line also names `GIT_INDEX_FILE`, `GIT_WORK_TREE`, `GIT_PREFIX` and `GIT_COMMON_DIR`; `grep -c 'pointing elsewhere leaves that elsewhere untouched' scripts/which-checks.test.sh` is 1.
    - `bash scripts/which-checks.test.sh` exits 0 with its case count quoted, and `GIT_DIR="$(git rev-parse --absolute-git-dir)" bash scripts/which-checks.test.sh` exits 0 too, both quoted in the summary with the date; the real repository's `HEAD`, `.git/config` and reflog are unchanged across the runs, checked and quoted.
    - `CLAUDE.md` and the changelog carry the dated sentences; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>No suite the gate runs can act on the repository that runs it, two cases would go red if that changed, and the rule says so where the red-commit rule is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the hook's environment to a suite | git's variables for the commit in progress reach every child of the gate |
| a suite's own repository to the real one | one absolute path turns a scratch repository into the real one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-81 | Tampering | a suite's git writing to the real repository | critical | mitigate | the five variables unset in the harness every suite sources; two cases red against a throwaway repository if it stops |
| T-11-82 | Denial of service | a case that tests the incident against the real repository | high | mitigate | both cases use a throwaway `elsewhere`; the real repository is never a subject; the summary quotes the reflog unchanged |
| T-11-83 | Repudiation | the gate's own decision changing because the suites cleared a variable | low | mitigate | the unset is inside the suite's process; `check.sh`'s decision is made before the suites run; the existing cases hold it |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in the `<automated>` block pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- The two runs of the suite, plain and under an absolute `GIT_DIR`, quoted with the real repository unchanged.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 85 --comment "Fixed in <merge commit>: scripts/shell-suite.sh clears GIT_DIR, GIT_WORK_TREE, GIT_INDEX_FILE, GIT_PREFIX and GIT_COMMON_DIR before any suite's first git, so a suite that builds a repository of its own acts on that repository whatever a hook handed it. Two cases are red if that stops: one under an absolute GIT_DIR and one under an exported GIT_INDEX_FILE, both against a throwaway repository, never this one. The same evening's second incident, a partial commit's temporary index read by the suite's subject, is the second case."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 25 closes: the harness clears the variables, the two cases, the rule rewritten.
- FOUND-19's `[D]` lines each have a named case in the summary.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-06.3-SUMMARY.md` when done
</output>
