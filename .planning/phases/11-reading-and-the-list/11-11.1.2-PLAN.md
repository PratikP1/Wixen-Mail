---
phase: 11-reading-and-the-list
plan: "11.1.2"
type: execute
wave: 26
depends_on: [11-11.1.1]
files_modified:
  - src/data/config.rs
  - src/presentation/view_state.rs
  - src/presentation/wx_app.rs
  - src/presentation/wx_settings.rs
  - src/data/message_cache/folders.rs
  - tests/the_settings_dialog_opens_in.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/ALPHA_TESTING.md
  - src/presentation/first_run.rs
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-27]
estimate:
  tokens: 43000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A folder nobody has set shows one row per conversation on a fresh profile, because Showing::from_stored answers a folder never set from the setting Show conversations by default, on by default, read at the moment the folder is opened and never captured at startup (LIST-27, #92, first half)."
    - "A folder whose view was chosen through View, Thread View keeps that choice whatever the setting says, and a stored nought still means flat, so a person who chose flat keeps it."
    - "Show conversations by default is on the settings screen's Reading tab, in the Message List section beside Default sort order, named on both channels, read back on save, and a settings file written before it existed reads as on."
    - "D-09's sentence in the code, the guide, the shortcuts page, the first-run screen and the alpha page say what a new profile shows, corrected by dating rather than rewritten as if it had always been so."
  artifacts:
    - "src/data/config.rs: AppConfig::show_conversations_by_default with serde default true, in the older-file test; the two settings guards green again."
    - "src/presentation/view_state.rs: Showing::when_nobody_set_one(bool) and Showing::from_stored(Option<i64>, Showing) with the D-09 tests moved."
    - "src/presentation/wx_app.rs: the folder landing reading the setting on use and handing it to from_stored."
    - "src/presentation/wx_settings.rs: the check box on the Reading tab, ReadingTabControls::show_conversations_by_default, the readback."
    - "tests/the_settings_dialog_opens_in.rs: the check box read back from the built Reading page."
    - "docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/ALPHA_TESTING.md, src/presentation/first_run.rs, docs/changelog.md."
  key_links:
    - "the folder landing -> ConfigManager::load_stored() -> Showing::when_nobody_set_one(cfg.show_conversations_by_default) -> Showing::from_stored(cache.folder_view(&folder), that) -> s.showing -> the check mark and the count the control is told."
    - "Save on the settings screen -> cfg.show_conversations_by_default = the box -> mgr.save() -> the next landing reads it; nothing in the startup block captures it, which 11-11.1.1's audit reading holds."
---

<objective>
Thread View is on by default, as a setting somebody can turn off.

Purpose: #92, Pratik's decision of 2026-09-20 on `1.0.0-alpha.1+321.g4a09bfc2`
and his amendment the same day: a folder nobody has set shows one row per
conversation; what an unset folder shows is a setting on the Reading tab,
"Show conversations by default", on by default; a folder's own choice
through View, Thread View still wins where one was made; and All Inboxes
takes the setting the same way when its own key holds nothing, which is
11-11.1.3's half. Today `Showing::from_stored(None)` answers flat by D-09
(`view_state.rs:37-46`, "a folder never set is flat"), so a person who
wants threads sets every folder or applies one folder's view to the rest.

Output: the setting, the rule reading it, the folder landing reading the
setting where it acts, the check box on the screen with its readback, the
D-09 tests moved, the pages dated, the changelog. The first half of #92; the
second, All Inboxes' own view and the cross-account rows, is 11-11.1.3,
which reads `from_stored` as this plan leaves it. A small insert after
11-11.1.1 and before 11-11.1.3; its plan number is the string `"11.1.2"`.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@.planning/phases/11-reading-and-the-list/11-11.1.1-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-20 against `main` at `4a09bfc2`, read-only while 11-11.1
executed on its branch, and re-taken the same day against `main` at
`390a580c` with 11-11.1 merged; the lines below are the second reading.
11-11.1.1 lands before this runs and moves lines in `wx_app.rs` (the
startup block loses two bindings), so every `wx_app.rs` line is re-run
on the day. Command first, then what it said.

**1. The rule and its tests hold as the issue says.**

```
sed -n 19,46p src/presentation/view_state.rs   -> Showing with #[default] Messages, "which is what a folder nobody has set does"; from_stored(Option<i64>) answers Conversations for Some(1) and Messages for everything else, with the comment that an unrecognised number reads as flat "rather than a guess"
sed -n 603,631p src/presentation/view_state.rs   -> the D-09 block: test_a_folder_nobody_has_ever_set_is_flat, ..._left_showing_conversations_comes_back_showing_them, ..._left_flat_comes_back_flat, ..._not_written_down_as_the_same_number, ..._from_a_later_version_is_read_as_flat_rather_than_guessed
grep -n 'Showing::default()' src/presentation/wx_app.rs   -> 622, WxUIState's default before any row is opened; the one use
sed -n 3084,3095p src/presentation/wx_app.rs   -> the folder landing, "D-09: the folder comes back in the view it was left in": from_stored(cache.folder_view(&folder).unwrap_or_default()) for a row that opens a folder, Showing::Messages for one that does not; read once per landing, before the mail is asked for
```

**2. The setting's shape, and the two guards that go red on arrival.**

```
sed -n 425,432p src/data/config.rs   -> #[serde(default = "default_true")] on check_spelling_as_you_type is the shape for a bool that is on by default; default_sort_order right after it at 431
grep -n 'fn test_every_setting_somebody_can_change' src/data/config.rs   -> 2595 (..._is_read_by_something, which skips wx_settings.rs on purpose: a reader has to act), 2972 (..._is_offered_by_a_screen): a public field added to AppConfig fails both until something that acts reads it and the settings screen offers it, which CLAUDE.md calls the red half for free
grep -n 'fn test_a_settings_file_written_before_these_existed' src/data/config.rs   -> 1728, the older-file test that gains the field
sed -n 1579,1622p src/presentation/wx_settings.rs   -> build_reading_tab, the Message List section, Default sort order at 1587-1620 with its accessible name set at 1614, the sort's second level built right after it (1624) so it is the next tab stop (#36)
grep -n 'sort_order' src/presentation/wx_settings.rs   -> ReadingTabControls::sort_order 1480, built 2064, read back 3533
grep -n 'WHAT_MARK_READ_COUNTS_FROM' tests/the_settings_dialog_opens_in.rs   -> 67, 785, 788: the reading that the built Reading page holds a sentence, the shape for reading a control back
grep -n 'fn test_every_checkbox_on_a_later_page' tests/every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built.rs   -> 574: every check box on a later page is walked, so a new one is covered by construction and adds no test there
```

**3. The pages, and where D-09's sentence lives.**

```
sed -n 697p docs/USER_GUIDE.md   -> "Each folder remembers its own view, and a folder you have never set is flat."
sed -n 706p docs/KEYBOARD_SHORTCUTS.md   -> the Thread View row, "Kept per folder."
sed -n 130,144p src/presentation/first_run.rs   -> INTRODUCTION says nothing of the view; 16 tests, 3 records
sed -n 9,10p docs/ALPHA_TESTING.md   -> "threading" in the list of what has been used, nothing on what a new profile shows
grep -n 'is flat' src/presentation/view_state.rs src/data/message_cache/folders.rs   -> view_state.rs 22 and 35 (the type's doc and from_stored's), folders.rs 352 (folder_view's doc) and 1170 (a test's message): the sentences to date
```

**4. The gate for each file, at `390a580c`.** `src/data/config.rs` 68
tests, 11 records; `src/presentation/view_state.rs` 55 tests, 3 records,
the D-09 tests rewritten in place; `src/presentation/wx_settings.rs` 0
tests, 22 records; `src/presentation/wx_app.rs` 196 tests by `#[test]`
and 199 by the runner, 102 records, none added;
`src/data/message_cache/folders.rs` 61 tests, 6 records, a comment and
a test's message only; `src/presentation/first_run.rs` 16 tests, 3
records, a sentence in a constant; `tests/the_settings_dialog_opens_in.rs`
gains two cases. 1,011 records by the parser. Counts taken 2026-09-20;
wx_app's are before 11-11.1.1 lands.

**5. Why the code is one task and one green.** 11-11.1's summary found
that its handlers had to go into task 1's green: the read-by-something
guard and clippy leave no green commit holding a field and no reader.
This plan's field has the same shape twice over: the read-by-something
guard wants a reader in a file that acts, which is the folder landing in
`wx_app.rs` (the settings screen is skipped by that guard on purpose),
and the offered-by-a-screen guard wants the control in `wx_settings.rs`.
So the field, its first reader (the landing) and the check box with its
readback are one green commit, task 1, and the red commit before it
names both guards beside the moved tests.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the setting, the rule answering an unset folder from it, the landing reading it, and the check box on the Reading tab: one green</name>
  <files>src/data/config.rs, src/presentation/view_state.rs, src/presentation/wx_app.rs, src/presentation/wx_settings.rs, src/data/message_cache/folders.rs, tests/the_settings_dialog_opens_in.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/view_state.rs:19-80` and `:603-631`; `src/data/config.rs:420-435`, `:1728-1790`, `:2595-2680`, `:2972-3020`.
    - `src/presentation/wx_app.rs`, the folder landing (`grep -n 'D-09: the folder comes back' src/presentation/wx_app.rs`, 3084 at `390a580c`) and 11-11.1.1's audit reading in `tests/a_setting_saved_applies_without_a_restart.rs`, which fails a new `stored_config` binding in the startup block.
    - `src/presentation/wx_settings.rs:1470-1520` (`ReadingTabControls`), `:1579-1660` (the Message List section and the sort's second level, whose comment says why it is built right after the sort), `:2060-2070`, `:3530-3540` (the readback).
    - `tests/the_settings_dialog_opens_in.rs:733-800`, the reading over the built Reading page.
  </read_first>
  <behavior>
    - `AppConfig::show_conversations_by_default: bool`, `#[serde(default = "default_true")]`, documented with the date and Pratik's decision; the older-file test asserts a file without it reads `true`; the two guards at `:2595` and `:2972` are red on the field's arrival and green in the same commit, because the landing reads it and the screen offers it.
    - `Showing::when_nobody_set_one(show_conversations: bool) -> Showing` (Conversations for true); `Showing::from_stored(stored: Option<i64>, when_unset: Showing) -> Showing`: `Some(1)` is Conversations, `Some(0)` is Messages whatever `when_unset` says (a stored nought still means flat, so a person who chose flat keeps it), `None` and any number this version does not recognise answer `when_unset`; the `#[default]` on `Messages` stays for the list before any row is opened (`wx_app.rs:622`) and its doc says that instead of "what a folder nobody has set does".
    - The D-09 tests: `test_a_folder_nobody_has_ever_set_shows_conversations_when_the_setting_is_on`, `..._shows_messages_when_the_setting_is_off`, `test_a_folder_left_flat_comes_back_flat_whatever_the_setting_says`, `test_a_number_from_a_later_version_is_read_as_the_unset_answer_rather_than_guessed`; the two others kept, updated to the new signature; the type's doc, `from_stored`'s doc and `folder_view`'s doc in `folders.rs` (and its test's message at `:1170`) dated: "a folder never set was flat until 2026-09-20 (#92); since then it shows what Show conversations by default says".
    - The folder landing reads `ConfigManager::load_stored()` once per landing, the way `the_sort_as` does per load, and hands `when_nobody_set_one(cfg.show_conversations_by_default)` to `from_stored`; this is the field's first reader, in a file that acts; nothing is added to the startup block, and 11-11.1.1's audit reading stays green.
    - `ReadingTabControls::show_conversations_by_default: CheckBox`, labelled "Show conversations by default", built in the Message List section after the sort's second level (which stays the next tab stop after the sort, #36) and before the section that follows, with `set_accessible_name` so both channels say it; checked from the config on build; read back on save into `cfg.show_conversations_by_default`.
    - The reading in `tests/the_settings_dialog_opens_in.rs`: the built Reading page holds a check box named "Show conversations by default" whose state is the config's, on for a fresh config; and a save with the box cleared writes `false`, read through the same path the dialog saves by. The check box reading in `tests/every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built.rs` covers the new box by construction; its count does not move, said in the summary.
  </behavior>
  <action>
    Red first, on a branch, one red commit: the field alone turns the two
    guards red and they are named; the four D-09 tests, the older-file
    test's new assertion and the two cases in the settings target are
    written bare and named; the count check named if the settings
    target's count moves a record. Then, in one green: the two functions,
    the landing, the check box, the name, the readback, with `cargo build`
    between; the compiler lists every `from_stored` caller. Invoke
    `elegant-code`: the rule stays pure (`view_state` takes a `Showing`,
    not a config), the landing is the one place that turns the setting
    into one, and the box is built the way its neighbours are, no second
    sizer shape. Why one green: premise 5.

    Guard records: one on `view_state.rs` whose break makes `Some(0)`
    answer `when_unset`, the flat-keeps-flat test red; one on `wx_app.rs`
    whose break hands `from_stored` a constant `Messages` at the landing,
    red in 11-11.1.3's target if that plan has landed and otherwise in a
    reading added to this plan's settings target that the landing names
    `show_conversations_by_default` (write it either way, so the record
    has a test to name today); one on `wx_settings.rs` whose break drops
    the readback, the save case red; one whose break drops the accessible
    name, the readback-by-name case red; each measured; the three records
    naming `view_state.rs` and the twenty-two naming `wx_settings.rs`
    re-measured only if the count check names them.
  </action>
  <verify>
    <automated>cargo test --lib presentation::view_state:: && cargo test --lib data::config:: && cargo test --test the_settings_dialog_opens_in && cargo test --test every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built && cargo test --test a_setting_saved_applies_without_a_restart</automated>
  </verify>
  <acceptance_criteria>
    - One red commit on a branch names the two guards, the moved tests and the cases; one green closes them all.
    - `grep -c 'pub show_conversations_by_default' src/data/config.rs` is 1; `grep -c 'fn when_nobody_set_one' src/presentation/view_state.rs` is 1; `grep -c 'is flat' src/presentation/view_state.rs src/data/message_cache/folders.rs` counts only dated sentences (quote them); `grep -c 'Show conversations by default' src/presentation/wx_settings.rs` is at least 2 (the label and the accessible name); `grep -c 'show_conversations_by_default' src/presentation/wx_settings.rs` is at least 3 (the field, the build, the readback).
    - `cargo test --lib presentation::view_state::` passes with 55, quoted; `data::config::` with 68, both guards green, quoted; 11-11.1.1's target green.
    - Four guard records measured and quoted.
  </acceptance_criteria>
  <done>A folder never set answers from the setting, a chosen view and a stored nought are kept, the setting is read where the folder opens and nowhere at startup, and somebody moving by keyboard through the Reading tab meets it beside the sort, hears its name and state on either channel, and a save changes what the next folder opened shows.</done>
</task>

<task type="auto">
  <name>Task 2: the pages dated, the changelog, and the comment on the issue</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/ALPHA_TESTING.md, src/presentation/first_run.rs, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md:663-715` (Thread View) and the Settings section's Reading rows; `docs/KEYBOARD_SHORTCUTS.md:95-106` and `:706`; `docs/ALPHA_TESTING.md:8-12`; `src/presentation/first_run.rs:130-144` and its tests at `:159-400` for what they hold the text to.
    - `docs/changelog.md`, `[Unreleased]`, 11-05's and 11-11.1.1's entries for the shape.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/USER_GUIDE.md:697`: "Each folder
    remembers its own view. A folder you have never set shows
    conversations, or messages if you turn off Show conversations by
    default under Settings, Reading; until 2026-09-20 a folder never set
    was flat." The Settings section's Reading rows gain the setting.
    `docs/KEYBOARD_SHORTCUTS.md:706`: "Kept per folder; a folder never
    set follows Show conversations by default, on unless you turn it off."
    `src/presentation/first_run.rs` `INTRODUCTION`: one sentence after the
    first paragraph, "Your folders show one row per conversation; View,
    Thread View switches a folder, and Settings, Reading changes what a new
    folder shows.", with its tests re-read for what they hold the text to
    (length, the words they look for) and any that the sentence moves
    rewritten in place, red first. `docs/ALPHA_TESTING.md`, under what has
    been used: a new profile shows conversations, and what to do if a
    folder should be flat. The changelog entry under `[Unreleased]`,
    Changed: the build, Pratik's decision, that a folder chosen flat
    stays flat, where the setting is, no version move (the counter moves
    at the first behaviour change after a cut, and none has been cut).

    Ledger, by hand, both halves: one `unrun-verify` entry for the
    tester: a folder never set heard as conversations on a fresh profile,
    and the setting turned off making the next one flat.

    After the merge, the executor comments on the issue from the merge
    commit and leaves it open for 11-11.1.3:
    `gh issue comment 92 --body "First half in <merge commit>: a folder
    nobody has set shows conversations, as Show conversations by default
    on the Reading tab, on unless you turn it off; a folder you chose flat
    stays flat, and a folder's own Thread View choice still wins. All
    Inboxes' own view and the cross-account rows are the next plan, which
    closes this."` Commenting is not a publish.
  </action>
  <verify>
    <automated>cargo test --lib presentation::first_run:: && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Show conversations by default' docs/USER_GUIDE.md` is at least 2; `grep -c 'until 2026-09-20' docs/USER_GUIDE.md` is at least 1; `grep -c 'Thread View' src/presentation/first_run.rs` is at least 1; `grep -c '^version = "1.0.0-alpha.1"' Cargo.toml` is 1; carriage returns 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words; the ledger entry in both halves.
    - `cargo test --lib presentation::first_run::` passes with 16, quoted, or the moved count said.
  </acceptance_criteria>
  <done>The pages say what a new profile shows and since when, the changelog names the decision, and the issue carries the first half with the second named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the settings file to the rule | a value read from disk decides how many rows the control is told, before the mail is asked for |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-110 | Tampering | a folder somebody chose flat turned threaded by the new default | high | mitigate | `Some(0)` answers Messages whatever the setting says; a test holds it both ways; a record measures the break |
| T-11-111 | Denial of service | the setting read per row rather than per landing | medium | mitigate | read once at the landing, beside the view's own read; nothing in the paint callback; nothing in the startup block, held by 11-11.1.1's audit reading |
| T-11-112 | Repudiation | a settings file from a later version carrying a number this build cannot read | low | accept | it answers the unset setting, said in the doc; the same shape the old rule had, with the guess replaced by the person's own default |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: a folder never set heard as conversations on a fresh profile; what the readings prove: the rule, the landing, the box, the readback.
- After the merge, the executor comments on #92 with the text in task 2 and leaves it open.
</verification>

<success_criteria>
- Roadmap criterion 31's first half closes structurally: the setting, the rule reading it, the landing, the screen, the pages dated, the changelog.
- LIST-27's `[D]` lines for this plan each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-11.1.2-SUMMARY.md` when done
</output>
