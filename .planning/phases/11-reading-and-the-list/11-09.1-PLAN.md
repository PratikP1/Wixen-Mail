---
phase: 11-reading-and-the-list
plan: 09.1
type: execute
wave: 12
depends_on: [11-09]
files_modified:
  - src/presentation/accessibility/feedback.rs
  - src/presentation/accessibility.rs
  - src/data/config.rs
  - src/presentation/wx_app.rs
  - tests/landing_on_an_attachment_says_it_once.rs
  - tests/every_event_has_a_control.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-13, LIST-16]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Landing on a message with an attachment says the word once: the Attachment column in the row is what NVDA reads, the earcon plays by default, and the spoken announcement of the event is off by default; the person can turn speech back on for it on the Feedback tab (LIST-13, #77, Pratik's decision of 2026-09-18)."
    - "The earcon channel is on by default for every event, Pratik's decision of 2026-09-18 on the issue, with per-event exceptions still allowed and this event's default being the exception; phase 6's reason for the old default kept and dated; a profile that turned earcons off keeps them off."
    - "The never-sound-alone rule never adds speech for an event whose text is already on the row: for such an event the fallback is the status bar, and never braille either, because braille rides the same screen reader notification as speech in this program, which is why the decision's 'and braille' cannot be sent without the words being spoken and the summary says so."
    - "The earcons keep playing after hours open: when the output device goes away or the stream is invalidated, the player learns it from the stream's own error callback and opens the default device again before the next sound; when the default device changes under a stream still alive, the next sound after a gap opens the device again; a reopen that fails is logged once for the outage and said once on the status bar, and the sounds resume when a device can be opened again (LIST-16, #81)."
  artifacts:
    - "src/presentation/accessibility/feedback.rs: the default channels with the earcon on; HasAttachment's own default set of earcon and status bar; the fallback rule with the on-the-row case; tests rewritten in place; the player's output opened through a builder with an error callback, reopened on the flag or after a gap, the outage told once."
    - "src/presentation/accessibility.rs: signal hands the player's complaint to the visual channel once."
    - "src/data/config.rs: the older-settings-file test rewritten to the new default and to a stored off=earcon kept."
    - "tests/landing_on_an_attachment_says_it_once.rs: the default channels for the event, the fallback, a stored profile kept, and the window adding nothing spoken beyond the signal."
    - "docs/USER_GUIDE.md, docs/changelog.md."
  key_links:
    - "feedback_events_for_landing -> signal(HasAttachment) -> channels_for under the default -> Earcon and Visual, no Speech, no Braille: the row's own reading is the only speech."
    - "FeedbackSettings::default -> disabled empty: a fresh profile hears every earcon; from_stored('off=earcon') -> disabled has Earcon: a profile that chose silence keeps it."
    - "cpal's WASAPI loop -> StreamError::DeviceNotAvailable into the callback given through DeviceSinkBuilder::with_error_callback -> the player's ended flag -> play_at opens the default device again before mixing the sound; a failed open -> one warn line and one status bar sentence through Accessibility::signal and take_visual_feedback."
---

<objective>
Say "attachment" once on landing: the earcon on by default, the spoken
event off by default, the column kept.

Purpose: #77, the tester on 2026-09-18 under NVDA on `744d05ef`: landing on
a message with an attachment says "attachment" more than once, from the
Attachment column NVDA reads in the row, the spoken "Has attachment"
event, and the earcon when on. Pratik's decision the same day: the earcon
on by default for this event, the spoken announcement off by default, the
column kept since it carries the fact in the row.

Output: the defaults, the fallback rule, the readings, the guide and the
changelog. Closes #77 on the tree side; whether the row is heard once with
the tone is his ear's. A plan of its own beside 11-09 because that plan is
at three tasks.

And #81, the same tester the same day: after a few hours open the earcons
go silent and nothing says so. The player opens the default output device
once at startup and holds it for the run; a stream that ends underneath
the mixer makes every later play answer true and play nothing. Task 3
teaches the player to notice and to open the device again, and to say
once, on the status bar and in the log, when it cannot. In this plan
because the earcon channel is on by default from task 1 onward, so a
player that goes quiet after hours is now every fresh profile's player.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `08197657`, working tree clean, before
11-01 to 11-09 land; 11-10 adds `Event::RuleMatched` after this plan and
reads the defaults this plan leaves. Command first, then what it said.

**1. The three places the word comes from.**

```
sed -n 19625,19634p src/presentation/wx_app.rs   -> feedback_events_for_landing: ThreadLanded when in a thread, HasAttachment when has_attachments; signalled from on_item_selected
grep -n 'HasAttachment' src/presentation/accessibility/feedback.rs   -> 131 (the event), 172 ("has_attachment"), 202 ("Has attachment"), 256 (Priority::Low), 304 (a tone of its own)
sed -n 77,80p src/presentation/message_rows.rs   -> the Attachment cell reads "Has attachment"; 140-143 for a conversation row
grep -n 'unread,attachment,subject' src/presentation/message_columns.rs   -> the default layout for every folder kind has the column
```

The issue's lines hold. The column is what NVDA reads on its own; the
event's speech is the duplicate; the earcon is a third only when it is
on.

**2. The channels, the global default and the per-event set.**

```
sed -n 505,516p src/presentation/accessibility/feedback.rs   -> FeedbackSettings::default: disabled = [Earcon], per_event empty, with the reason "an application that starts making noises nobody asked for is one people switch the sounds off in, permanently"
sed -n 581,602p src/presentation/accessibility/feedback.rs   -> channels_for: the event's own set or every channel, filtered by is_channel_enabled; then the never-sound-alone fallback in the order Braille, Visual, Speech
sed -n 604,650p src/presentation/accessibility/feedback.rs   -> to_stored writes off=<channels> and one group per event; from_stored answers Self::default() when neither an off group nor a per-event group was stored
sed -n 1212,1224p src/presentation/wx_app.rs   -> the stored string is read at startup only when it is not empty
python -c "import json;print(json.load(open(r'C:\Users\prati\AppData\Local\wixen-mail\config\app_config.json'))['feedback_channels'])"   -> off= (the tester's profile, read 2026-09-18 through Python, read-only: every channel on, no per-event set)
```

Two shapes for "the earcon on by default", as the issue says, and Pratik
settled it on the issue at 12:24Z the same day: the Earcon channel is
enabled by default for every event (the `:511` default and its reason
change; a fresh profile hears every sound from the start), per-event
exceptions stay allowed through the One event at a time section so a
person can silence any one sound, and for this event the exception is the
default itself. So the global switch keeps its one meaning (off across
every event), a fresh profile hears every tone and turns them all off with
one box on the Feedback tab, and it is what the tester's own profile
already does. Phase 6's reason is kept in the comment and dated as the
reason until 2026-09-18, with Pratik's decision beside it.

**3. The fallback rule can add speech, and braille is speech here: the
decision's "and braille" cannot be sent without the words being spoken.**

```
sed -n 586,600p src/presentation/accessibility/feedback.rs   -> a sound-only set gains the first enabled of Braille, Visual, Speech
sed -n 258,266p src/presentation/accessibility.rs   -> "Speech and braille both ride the one screen reader notification, so announcing once serves either": a set holding Braille calls announce_topic, which the screen reader speaks
sed -n 335,341p src/presentation/accessibility/feedback.rs   -> "Braille is listed separately from speech even though a screen reader usually drives both from one notification. The distinction is a preference, not a transport"
sed -n 449,456p src/presentation/accessibility/feedback.rs   -> the Feedback tab's one switch, SpokenOrBrailled, names both channels: "Words, through the screen reader, in whichever medium it chooses"
grep -n 'braille' src/presentation/accessibility/screen_reader.rs | head -3   -> 6, 190: NVDA routes the one notification to speech and to a braille display; nothing separate exists for braille
```

Pratik's words for this event are "the earcon plus the status bar and
braille, no speech". The tree has no braille route apart from speech: a
set holding `Braille` calls `announce_topic`, the screen reader speaks
the notification, and the same row's Attachment column already sits on
the braille display as it sits in speech. So "braille, no speech" is not
a set this program can send, and a set that names Braille reinstates the
duplicate the issue exists to stop. The plan therefore writes
`HasAttachment`'s default as `{Earcon, Visual}`, the earcon plus the
status bar, and says so in the summary and the close comment as the one
place the tree could not carry the decision's words; if Pratik wants the
words on braille at the cost of them being spoken, that is one channel
added to the default set and he can say so. The fallback rule gains a
case: for an event `whose_text_is_on_the_row` the fallback order is
Visual only; if Visual is off too, the event goes out as the sound alone
rather than as speech, and the doc says why.

**4. What the reading can prove.** `channels_for(HasAttachment)` on a
default settings holds Earcon and Visual and neither Speech nor Braille;
`from_stored("off=earcon")` keeps Earcon off; the window's landing arm
signals the event and announces nothing else; the arm's `signal` under
the default calls no `announce_topic`. The row's own reading is NVDA's,
and the summary says the program's part is that it adds nothing spoken.

**5. The gate for each file.** `src/presentation/accessibility/feedback.rs`
maps to `--lib presentation::accessibility::feedback::`, 49 tests, 3
records, its default and fallback tests rewritten in place and one case
added at most by task 1, said, and task 3's cases added there because the
seam they need is inside the module; `src/data/config.rs` 68, 11, the
older-file test rewritten in place; `src/presentation/wx_app.rs` 199,
none added; `tests/every_event_has_a_control.rs` (1 test, 5 records)
reads the Feedback tab back and is rewritten in place if it asserts the
old default; the new target to itself;
`src/presentation/accessibility.rs` maps to `--lib
presentation::accessibility::`, 24 tests, 7 records, one case added by
task 3 for the complaint reaching the visual channel. Every count taken
2026-09-18 at `eb5d8517` with `grep -c '#\[test\]'` and the parser
command in `CLAUDE.md`.

**6. What the tree and the two crates say about the silent player (#81).**
Taken 2026-09-18 at `eb5d8517`, `rodio` 0.22.2 and `cpal` 0.17.3 read
from the registry.

```
sed -n 764,782p src/presentation/accessibility/feedback.rs   -> _device: Option<rodio::MixerDeviceSink> "held for as long as the player exists and read by nothing"; mixer: Option<Mixer>
sed -n 842,847p src/presentation/accessibility/feedback.rs   -> new: rodio::DeviceSinkBuilder::open_default_sink().ok(), once; the mixer cloned from it
sed -n 850,853p src/presentation/accessibility/feedback.rs   -> "Mixer::add ignores the send when nothing is listening, so a player whose source has gone is silent rather than broken"
sed -n 896,920p src/presentation/accessibility/feedback.rs   -> play_at: the gap, then mixer.add; answers true whether or not anything listens
grep -n 'reopen\|error_callback\|IMMNotificationClient' src/presentation/accessibility/feedback.rs   -> nothing
grep -n 'earcons' src/presentation/accessibility.rs   -> 28 (the field), 58 and 475 (EarconPlayer::new() once per Accessibility), 230 and 256 (play)
sed -n 40,46p src/presentation/accessibility.rs; sed -n 274,280p   -> visual: Mutex<Option<String>>, "the window owns the status bar"; take_visual_feedback takes it
sed -n 5550,5555p src/presentation/wx_app.rs   -> the twenty-a-second timer: take_visual_feedback -> frame.set_status_text(&text, 0)
sed -n 368,383p ~/.cargo/registry/src/*/rodio-0.22.2/src/stream.rs   -> DeviceSinkBuilder::with_error_callback<F: FnMut(cpal::StreamError) + Send + 'static>; open_stream; open_sink_or_fallback needs E: Clone
sed -n 222,262p ~/.cargo/registry/src/*/rodio-0.22.2/src/stream.rs   -> from_default_device; open_default_sink is the static path with the default callback, which only logs; it then tries every other output device whose driver is not "null"
sed -n 164,169p ~/.cargo/registry/src/*/rodio-0.22.2/src/stream.rs   -> default_error_callback: tracing::error! under the tracing feature, else eprintln!
grep -n 'pub use cpal' ~/.cargo/registry/src/*/rodio-0.22.2/src/lib.rs   -> 186: cpal is re-exported; nothing is added to Cargo.toml
sed -n 293,305p ~/.cargo/registry/src/*/cpal-0.17.3/src/error.rs   -> StreamError: DeviceNotAvailable ("disconnected while the program is running"), StreamInvalidated, BufferUnderrun, BackendSpecific
sed -n 382,404p ~/.cargo/registry/src/*/cpal-0.17.3/src/host/wasapi/stream.rs   -> the output loop calls error_callback(err) and breaks; the stream thread ends
sed -n 98,110p ~/.cargo/registry/src/*/cpal-0.17.3/src/host/wasapi/mod.rs   -> AUDCLNT_E_DEVICE_INVALIDATED and AUDCLNT_E_DEVICE_IN_USE map to DeviceNotAvailable
grep -rn 'IMMNotificationClient' ~/.cargo/registry/src/*/cpal-0.17.3/src/host/wasapi   -> nothing: cpal does not follow the default device
grep -n 'Win32_Media_Audio' Cargo.toml   -> nothing: the windows crate here has no audio feature on
```

So the issue's second trace question has its answer from the source: a
device that goes away or is invalidated reaches a callback the builder
accepts, and the stream thread ends after it. The default device
*changing* while the old one stays does not: cpal keeps writing to the
old device and reports nothing. Two mechanisms therefore, and the second
chosen by a measurement the issue asks for: the callback's flag for the
first case, and for the second either a reopen before the first sound
after a gap (if opening the default device is cheap on this machine) or
Windows' own notification (if it is not). The player under
`WIXEN_NO_AUDIO` and in every test has no device and is not touched by
either.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the earcon on by default, the event's own channels, and the fallback that never speaks for it</name>
  <files>src/presentation/accessibility/feedback.rs, src/data/config.rs, tests/landing_on_an_attachment_says_it_once.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/accessibility/feedback.rs:340-400` (`Channel`), `:500-660` (the settings, the stored form), `:1200-1230` and `:1650-1680` (the stored-form tests).
    - `src/presentation/accessibility.rs:243-275` (`signal`).
    - `src/data/config.rs:1660-1760`, the older-file test.
  </read_first>
  <behavior>
    - `FeedbackSettings::default()`: `disabled` empty, `per_event` holding `(Event::HasAttachment, {Earcon, Visual})`; the comment keeps phase 6's reason, dated as the reason until 2026-09-18, and gives Pratik's decision.
    - `Event::text_is_already_on_the_row(self) -> bool`: true for `HasAttachment`, false otherwise, with the doc naming the column; `channels_for`'s fallback for such an event tries `Visual` alone and adds nothing when it is off.
    - `from_stored("off=earcon")` keeps Earcon disabled; `from_stored("off=")` enables every channel; a stored string with no `off` group and no event group answers the new default; `to_stored` of the default writes `off=,has_attachment=earcon+visual`.
    - The older-file test in `config.rs` asserts that an empty `feedback_channels` reads as the new default and that a file holding `off=earcon` keeps it.
    - The target: the four facts in premise 4 as cases over `FeedbackSettings` and `Event`, plus a companion that hands `channels_for` an event without the on-the-row mark and requires Braille first as before.
  </behavior>
  <action>
    Red first, on a branch: the target's cases bare and the rewritten
    `feedback.rs` and `config.rs` tests by module path; the count check
    for `feedback.rs`'s three records named bare if a case is added there
    rather than in the target. Then the default, the mark, the fallback
    case, with `cargo build` between.

    Guard records: one on `feedback.rs` whose break drops the per-event
    default for the attachment event, `suite` the target; one whose break
    lets the fallback add Speech for an on-the-row event, `suite` the
    target; each measured; the three records naming `feedback.rs`
    re-measured if its count moved.
  </action>
  <verify>
    <automated>cargo test --test landing_on_an_attachment_says_it_once && cargo test --lib presentation::accessibility::feedback:: && cargo test --lib data::config::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases.
    - `grep -c 'disabled: \[Channel::Earcon\]' src/presentation/accessibility/feedback.rs` is 0 and `grep -c 'fn text_is_already_on_the_row' src/presentation/accessibility/feedback.rs` is 1.
    - `cargo test --lib data::config::` passes with 68 tests, quoted; `presentation::accessibility::feedback::` with 49 or 50, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>A fresh profile hears the attachment tone and no spoken event for it, a profile that chose silence keeps it, and the fallback cannot speak for an event whose text is on the row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the window adds nothing spoken, the Feedback tab shows the default, and the pages say what a fresh profile hears</name>
  <files>src/presentation/wx_app.rs, tests/landing_on_an_attachment_says_it_once.rs, tests/every_event_has_a_control.rs, guards/guards.toml, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `src/presentation/wx_app.rs:3006-3035` (the landing signals in `on_item_selected`) and `:19625-19634`.
    - `tests/every_event_has_a_control.rs`, the readback of the Feedback tab's rows and the "One event at a time" section.
    - `docs/USER_GUIDE.md:180-195`, the sentence about the attachment showing as a real column, and the feedback section.
  </read_first>
  <behavior>
    - The landing arm is unchanged in shape: `feedback_events_for_landing` still signals `HasAttachment`; a reading over `what_ships` holds that the arm calls `a11y.signal(` for the landing events and no `announce` or `announce_content` with "attachment" in its text, with a companion.
    - `tests/every_event_has_a_control.rs`: the Feedback tab built from `AppConfig::default()` shows the global earcon box on and the attachment event's row with earcon and status bar chosen; rewritten in place, count 1.
    - The pages: `docs/USER_GUIDE.md`'s feedback section says a fresh profile hears every tone, that the attachment event's tone plays and its words do not, that the row's Attachment column is where the word is heard, and how to turn speech back on for it (Settings, Feedback, One event at a time), dated; the changelog entry under `[Unreleased]`, Changed: the tester's words, the decision, what a fresh profile hears now, that a profile that turned earcons off keeps them off, and the column kept; Known limitations: nobody has heard the row with the tone.
  </behavior>
  <action>
    Red first, on a branch: the window reading bare and the
    `every_event_has_a_control` rewrite named bare. Then the reading's
    green is the tree as it stands once task 1 landed (the arm changes
    nothing), said in the commit; the dialog readback follows task 1's
    default.

    Invoke `writing-craft` for the two pages and `cognitive-accessibility`
    for the guide's sentence on how to turn speech back on.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only
    the tester's ear settles: landing on a message with an attachment
    heard as the row once with the tone, and the tone alone when the
    status bar is off.

    Guard records: one on `wx_app.rs` whose break announces "Has
    attachment" beside the signal, `suite` the target, measured; the five
    records naming `every_event_has_a_control.rs` re-measured if the
    rewrite moved their breaks.
  </action>
  <verify>
    <automated>cargo test --test landing_on_an_attachment_says_it_once && cargo test --test every_event_has_a_control && cargo test --lib presentation::wx_app:: && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/presentation/wx_app.rs | grep -c 'FeedbackEvent::HasAttachment'` is 1, in `feedback_events_for_landing`.
    - `cargo test --lib presentation::wx_app::` passes with 199 tests, quoted; `every_event_has_a_control` passes with 1.
    - The guide and the changelog carry the dated sentences; carriage returns 0; no em dash; none of the six words.
    - One guard record measured and quoted; the ledger entry in both halves.
  </acceptance_criteria>
  <done>The program adds nothing spoken to a row that already says attachment, the Feedback tab shows the default, and the pages say what a fresh profile hears.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the earcons come back when the device goes, and say once when they cannot</name>
  <files>src/presentation/accessibility/feedback.rs, src/presentation/accessibility.rs, guards/guards.toml, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `src/presentation/accessibility/feedback.rs:760-960` (the player, `new`, `into_the_air`, `play_at`, `play_file`) and `:1030-1075` (the no-device tests).
    - `src/presentation/accessibility.rs:20-60` (the handle) and `:243-280` (`signal`, `take_visual_feedback`).
    - `~/.cargo/registry/src/*/rodio-0.22.2/src/stream.rs:177-262` and `:364-412` (the builder, the callback, the fallback) and `~/.cargo/registry/src/*/cpal-0.17.3/src/host/wasapi/stream.rs:382-404`.
    - Premise 6 above, and issue #81's "What to trace first".
  </read_first>
  <behavior>
    - The player's output is one value, `Output`, holding the mixer, the open device when there is one, and an `ended: Arc<AtomicBool>` the stream's error callback sets; `new` opens it through `DeviceSinkBuilder::from_default_device()` with `with_error_callback` (the callback logs the `StreamError` at warn, one line, and stores true) and `open_sink_or_fallback`, then, when that fails, walks `rodio::cpal::default_host().output_devices()` the way `open_default_sink` does, skipping the "null" driver, so a machine whose default device cannot be opened still gets the second one it gets today.
    - The opener is a field, `open: Box<dyn Fn() -> Result<Output, String> + Send + Sync>`, so a test hands in one that answers a fresh detached mixer each time and records how many times it was asked; `into_the_air` keeps its shape and hands out an opener that answers the same silence.
    - `play_at` asks `somewhere_to_play(now)` before mixing: it opens again when `ended` is set, or when the last play was longer ago than `REOPEN_AFTER` and the open is cheap (see the measurement below), and otherwise plays through what it holds; the replaced device is dropped with `log_on_drop(false)` so rodio does not print a line for a drop the program chose.
    - A reopen that fails leaves the player without an output, answers false from `play_at`, writes one `tracing::warn!` line for the outage rather than one per sound, and records one sentence for the eye; a later reopen that succeeds writes one `tracing::info!` line and clears the outage so a second outage is told again.
    - `EarconPlayer::take_complaint(&self) -> Option<String>`: the sentence once, taken, then `None`; the sentence is "The sounds have stopped: no audio output device could be opened again (<the error's text>). They come back when one can be." and `Accessibility::signal` calls it after `play` and writes it into `visual` in place of the event's text, so the status bar shows it on the next tick whatever the event's own channels were.
    - Cases in `feedback.rs`'s module tests: a player whose source is dropped and whose `ended` is set plays the next sound into a second mixer the opener handed out, the opener asked twice; a player played twice inside `REOPEN_AFTER` with `ended` clear asks the opener once; a player whose opener answers `Err` records one complaint, logs once across three plays (the count read back through a `tracing` subscriber test layer if one exists in the tree, else the `told` state read directly), and after the opener answers `Ok` again plays and clears; `take_complaint` answers once. One case in `accessibility.rs`: `signal` with a player whose opener fails leaves the complaint in `take_visual_feedback` and the event text nowhere.
    - Every case runs with no device, in a detached mixer, so it holds on the runner too; `test_a_player_with_no_device_behind_it_still_plays_and_still_suppresses` and `test_a_player_that_has_lost_its_listener_is_silent_rather_than_broken` are rewritten in place if the new shape moves them, counts said.
  </behavior>
  <action>
    Measure first, and write the figure into the summary with the command
    and the date: a probe in the sweep worktree beside the #79 one,
    `C:/Users/prati/Documents/projects/wixen-mail-sweep/tests/probe_81_device_open_cost.rs`,
    times `rodio::DeviceSinkBuilder::open_default_sink()` twenty times on
    this machine and prints the median and the worst. Under 50 ms median:
    `REOPEN_AFTER` is 10 seconds and the gap-based reopen is the second
    mechanism, which is the issue's "simplest and always right", and the
    default device changing under a live stream is covered by it. At or
    over 50 ms: the gap-based reopen is not written, and the second
    mechanism is Windows' own notice, `IMMNotificationClient` registered
    through `IMMDeviceEnumerator::RegisterEndpointNotificationCallback`
    with `OnDefaultDeviceChanged` for `eRender` setting the same `ended`
    flag, behind `cfg(target_os = "windows")` with the `Win32_Media_Audio`
    feature added to the `windows` crate's list in `Cargo.toml` (a feature
    on a crate already here, not a new crate) and a fallback that keeps
    the crate building elsewhere. Either way the callback's flag is
    written, and the summary says which second mechanism was chosen and
    the figure that chose it.

    The issue's first trace step, reproducing on purpose by switching
    the default output device, needs a hand on the machine's Sound
    settings under a running build, which the executor does not have and
    which manual testing's standing rule keeps for later: record it, do
    not surface it. So it goes into the ledger with its exact steps rather
    than being run: the program built from the branch and started with
    `WIXEN_MAIL_DATA` set to an empty folder (`src/common/paths.rs:30`,
    the one override the paths module honours), so it touches nothing
    under `%LOCALAPPDATA%\wixen-mail`; earcons on; the default output
    device changed in Windows Sound settings; an event with a sound
    triggered (Settings saved is the nearest); whether it went silent and
    what the log said at `debug`. Never the installed binary, and never
    without the override. What the executor proves instead is the seam:
    the cases below drop the source and set the flag the way the callback
    would, which is the mechanism the crates document.

    Red first, on a branch: the module cases named by path, the
    `accessibility.rs` case named by path, the count check named as one
    of the failures with the reason in the message, since both files are
    named by records. Then `Output`, the opener field, `somewhere_to_play`,
    the complaint, `take_complaint`, the `signal` arm, with `cargo build`
    between. Invoke `elegant-code`: the outage is one small struct with
    its `told` state, not three booleans on the player. Never `unwrap` or
    `expect` outside the tests; a poisoned lock stays silent as `play_at`
    does today.

    Pages, with `writing-craft`: `docs/USER_GUIDE.md`'s Sound Schemes and
    Earcons section gains a paragraph saying the sounds follow the
    device, that a headset or a monitor coming or going does not silence
    them, and what the status bar says when no device can be opened; the
    changelog entry under `[Unreleased]`, Fixed: the tester's words, what
    happened before (the device opened once at startup and never again,
    a stream ended by the device going away leaving every later sound
    played into nothing), what happens now, and Known limitations: the
    reproduction was by switching the default device on the developer's
    machine, and a stream ended by something else after hours has not
    been seen end-to-end; if the sounds stop again, the log at `debug`
    (11-04) holds the moment. Ledger, by hand, both halves: one
    `unrun-verify` entry holding the reproduction steps above and the
    earcons heard again after a real device change.

    Guard records: one on `feedback.rs` whose break stops `play_at` from
    asking `somewhere_to_play` when `ended` is set, the reopen case red;
    one whose break logs on every failed play rather than once, the
    outage case red; one on `accessibility.rs` whose break drops the
    `take_complaint` call from `signal`, the visual case red; each
    measured; the three records naming `feedback.rs` and the seven naming
    `accessibility.rs` re-measured when the count check names them, in
    the background, as the file says.
  </action>
  <verify>
    <automated>cargo test --lib presentation::accessibility::feedback:: && cargo test --lib presentation::accessibility:: && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases and the count check.
    - `grep -c 'with_error_callback' src/presentation/accessibility/feedback.rs` is at least 1; `grep -c 'open_default_sink()' src/presentation/accessibility/feedback.rs` is 0 outside comments (`grep -v '^\s*//'` first).
    - `grep -c 'fn take_complaint' src/presentation/accessibility/feedback.rs` is 1 and `grep -c 'take_complaint' src/presentation/accessibility.rs` is at least 1.
    - The summary quotes the probe's median and worst with the command and the date, and names the second mechanism chosen.
    - `cargo test --lib presentation::accessibility::feedback::` passes with its count quoted; `presentation::accessibility::` with 25, quoted.
    - Three guard records measured and quoted; the guide and the changelog carry the sentences; carriage returns 0; no em dash; none of the six words; the ledger entry in both halves.
  </acceptance_criteria>
  <done>A player whose device has gone opens the default device again before the next sound, a player that cannot says so once on the status bar and once in the log and plays again when it can, and the changelog says what happened before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a default to a stored profile | a default that moves must not move a profile that chose otherwise |
| the fallback rule to speech | a rule written to stop a sound going out alone can put words into a row that already has them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-51 | Tampering | a stored off=earcon read as on | medium | mitigate | `from_stored` keeps a stored off group; the older-file test holds it |
| T-11-52 | Denial of service | every tone on for a fresh profile | low | accept | Pratik's decision and the tester's own configuration; one box on the Feedback tab turns them all off; the changelog says so |
| T-11-53 | Denial of service | the fallback speaking for an on-the-row event | medium | mitigate | the on-the-row case in the fallback with a case and a record |
| T-11-59 | Denial of service | a reopen tried on every sound against a device that stays gone | medium | mitigate | one open per play at most, only on the flag or after `REOPEN_AFTER`; the outage logged once and the complaint given once; the outage case holds it |
| T-11-60 | Denial of service | a device open on the window's thread on every sound | medium | mitigate | the open happens only after a gap or on the flag, and only when the probe measured it cheap; over 50 ms the notification route is taken instead, and the summary says which |
| T-11-61 | Information disclosure | the error's text on the status bar naming a device | low | accept | the device's name is the person's own hardware; nothing from a message or an account is in it |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Six guard records measured; every record the count check flagged re-measured and read.
- What the tester's ear settles: the row heard once with the tone, and the sounds still there after a headset comes or goes; what the cases and readings prove: the default channels, the fallback, a stored profile kept, the arm adding nothing spoken, the player opening again on the flag and after a gap, the outage told once.
- After the merge, the executor closes #81 from the merge commit: `gh issue close 81 --comment "Fixed in <merge commit>: the earcon player used to open the output device once at startup and never again, so a device that went away or was invalidated left every later sound played into nothing, with no error. Now the stream's own error callback marks the device gone and the next sound opens the default device again; <the second mechanism chosen and the figure that chose it>. When no device can be opened, the log says so once for the outage and the status bar says The sounds have stopped, with the reason, and the sounds come back when a device can be opened again. What only your machine settles: the sounds heard again after a real device change, and whether what silenced them after hours was the device going or something else; if they stop again, the log at debug holds the moment."`
- After the merge, the executor closes the issue from the merge commit: `gh issue close 77 --comment "Fixed in <merge commit>: landing on a message with an attachment says the word once, from the Attachment column in the row; the attachment tone plays by default and the spoken event is off by default, and you can turn its speech back on under Settings, Feedback, One event at a time. The sounds are on by default for every event now, which is what your own profile already had; a profile that turned them off keeps them off. One thing the issue's third point had the other way: braille rides the same screen reader notification as speech here, so the text channel added for a sound that would otherwise go out alone is the status bar for this event, never speech and never braille. What only your ear settles: the row heard once with the tone."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 16 closes structurally: the earcon on, the speech off, the column kept, the fallback that never speaks for it, the profile kept.
- LIST-13's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
- Roadmap criterion 19 closes structurally: the player opening again on the flag and after a gap or on the notice, the outage told once in the log and once on the status bar, the resume.
- LIST-16's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-09.1-SUMMARY.md` when done
</output>
