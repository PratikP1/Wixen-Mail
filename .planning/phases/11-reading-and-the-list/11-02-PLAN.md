---
phase: 11-reading-and-the-list
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .github/workflows/nvda.yml
  - nvda-tests/README.md
  - nvda-tests/tests/settings-tabs-read-once.test.js
  - src/presentation/status_line.rs
  - src/presentation/wx_account_manager.rs
  - guards/guards.toml
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [FOUND-18, FOUND-08, FOUND-09]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A run of the NVDA workflow in which a case failed is a failed run, so a case that fails is seen at the badge and not only in a transcript somebody downloads (FOUND-18; guardrail 4; run 35336142908 on main at 744d05ef reported success over a job that failed)."
    - "The Account Manager's sign-in failure reaches a screen reader as one line that carries the sentence, where today the sentence and the event's cue are two notifications a moment apart and the runner's transcript holds only the cue (the case that has failed since its first run on 2026-09-15)."
    - "The Settings tab-row case waits for what the harness can hear: the transcripts of every case that passed begin with what the first key made NVDA say, and the dialog itself speaks, heard by the tester on 2026-09-18 (#33 closed on his word); the Settings dialog is not changed for it."
    - "FOUND-08's walk clause is read from run 35336142914, where the five editors were walked with no unnamed control; FOUND-09 is ticked on the tester's ear, with the runner's case named as the harness's own."
  artifacts:
    - ".github/workflows/nvda.yml: the job's verdict is the run's; the header says why since 2026-09-18."
    - "src/presentation/status_line.rs: shown_and_signalled, the one call that shows a sentence and raises the event that carries it; src/presentation/wx_account_manager.rs: the three failure arms through it."
    - "nvda-tests/tests/settings-tabs-read-once.test.js: the opening wait replaced by a mark; nvda-tests/README.md: the cases that exist and what the log holds."
    - ".planning/REQUIREMENTS.md: FOUND-08 and FOUND-09 ticked with their reasons; docs/changelog.md: the entries; .planning/WINDOWS.md: 489 closed, 492 closed."
  key_links:
    - "npm test's exit code -> the job -> the run: no continue-on-error between them; the summary and the upload keep if: always() so a red run still carries its transcript."
    - "OAuthFlowResult::Failed -> shown_and_signalled -> one notification: the earcon, the visual and the words from one event, on the channels the person's row for it allows."
    - "the case's first wait -> a mark after the settle -> the first Right: the harness's log holds what keys make NVDA say, not what a dialog says as it opens."
---

<objective>
Make the NVDA workflow's verdict the run's, read the two cases that failed
at `744d05ef` and act on what each said in the harness and in the one
place the source is at fault, and read the Accessibility run for the
clause phase 9 left waiting on it.

Purpose: this morning's push ran three workflows. The NVDA run reported
success while its one job failed, because `nvda.yml` sets
`continue-on-error: true` on the job; that is guardrail 4 exactly, a check
nobody reads. Inside it, two of five cases failed: the Settings tab-row
case heard nothing at all on its first ever run, on a dialog the tester
heard speak correctly by hand the same day; and the sign-in case has
failed at every run since 2026-09-15, unnoticed because the badge was
green. The Accessibility run on the same push walked the five editors
FOUND-08 waited on.

Output: the workflow gating a red run, the account manager's failure as one
notification, the settings case waiting for what can be heard, FOUND-08
and FOUND-09 ticked with their reasons, the ledger and the changelog. No
change to the Settings dialog: it speaks, and the harness is what did not
hear.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef` and against the runs, from
the repository root. Command first, then what it said.

**1. The NVDA run is green over a failed job, by design written on
2026-08-16.**

```
gh run view 35336142908 --json name,status,conclusion,headSha   -> NVDA completed success 744d05ef
gh run view 35336142908 --json jobs   -> NVDA screen reader tests | completed | failure; step "Run the NVDA tests" failure, "Summarise findings" success, "Upload NVDA transcripts" success
sed -n 39p .github/workflows/nvda.yml   -> continue-on-error: true
sed -n 18,21p .github/workflows/nvda.yml   -> "The job is non-blocking on purpose, the same as accessibility.yml's. Treat its findings as a work queue, not a gate, and read the uploaded transcript rather than the pass or fail badge."
git log --format='%h %ad %s' --date=short -S'continue-on-error: true' -- .github/workflows/nvda.yml | tail -1   -> 363358b3 2026-08-16 Run the NVDA tests in CI, on a disposable Windows runner
grep -n 'continue-on-error' .github/workflows/accessibility.yml   -> 44: the same, on the scan job
```

The workflow was written non-blocking so that findings would be a work
queue. The queue was never read: the sign-in case failed at `3e633252`
(run 34973255598, 2026-09-15) and at `744d05ef`, and both runs are green.
The decision here: `continue-on-error` comes off the NVDA job, so a failing
case fails the run; the summary step keeps `if: always()` and already tells
"no transcript was written" from "at least one test failed", and the upload
keeps `if: always()`, so a red run still carries the transcript. The header
paragraph is rewritten with the date and this reason. `accessibility.yml`'s
setting is left as it is and recorded in the ledger: its findings are
counts per window that the workflow already reads out (`perwindow`,
`unnamed`, `broken`), and whether a count should fail the run is a decision
for the phase that owns the findings, not this plan.

**2. The two cases, from the job log and the transcripts.**

```
gh run view 35336142908 --job 105571310901 --log, saved and grepped
  -> FAIL tests/settings-tabs-read-once.test.js (33.772 s): never heard all of ["General"] within 15000ms. Everything NVDA said: []   (at settings-tabs-read-once.test.js:164, the first waitToHearAll, before any key)
  -> FAIL tests/account-manager-sign-in-failure.test.js (25.289 s): never heard all of ["Signing in failed","not one Wixen Mail can sign in to through a browser"] within 15000ms   (at :95)
  -> PASS calendar-immediate-actions, PASS filter-manager-delete; Tests: 2 failed, 1 skipped, 2 passed, 5 total
gh run view 34973255598 --log, saved and grepped   -> at 3e633252 on 2026-09-15: FAIL account-manager-sign-in-failure with the same sentence; PASS the other two; 4 total, because the settings case did not exist yet
git log --format='%h %ad' --date=short -- nvda-tests/tests/settings-tabs-read-once.test.js | tail -1   -> a7a54743 2026-09-16: the run at 744d05ef is that case's first run
gh issue view 33 --json state   -> CLOSED, 2026-09-18, on the tester's word: on 1.0.0-alpha.1+149.g744d05ef with NVDA the dialog speaks as it should, each tab once along the row
```

The transcripts, downloaded to the scratchpad from the run's artifact:

```
account-manager-sign-in-failure.json   -> ["Scan target; Email: scan-target at example dot com; Status: Enabled, 1 of 1", "Add Account..., button, Alt plus, a", "Edit..., button, Alt plus, e", "Delete, button, Alt plus, d", "Set Active, button, Alt plus, v", "Set as Default, button, Alt plus, u", "Sign In Again, button, Alt plus, s", "Sign-in needs attention, Scan target"]
settings-tabs-read-once.json   -> []
calendar-immediate-actions.json   -> ["New Event, button, Alt plus, n", "Edit Event, button, Alt plus, e", "Select an event to edit.", ...]
filter-manager-delete.json   -> ["Add..., button, Alt plus, a", "Edit..., button, Alt plus, e", "Delete, button, Alt plus, d", "Select a filter to delete"]
```

Two things the transcripts say that the cases assumed otherwise. First, no
transcript holds a dialog's opening announcement: each begins with what
the first key made NVDA say (the row after Down, a button after Tab), and
the settings case never pressed a key, because its first wait, for
"General" at open, timed out first. So that wait was for something this
harness has never captured, and the case's own comment ("the dialog opens
on General with the row focused, and NVDA says so") was a prediction from
the win-event capture of 2026-09-16, not from a transcript. The dialog
itself speaks: the tester heard it by hand on the same build. The Settings
dialog is therefore not changed here, and the case is: it takes its mark
after the settle and presses Right. Second, the sign-in transcript ends
with the event's words, "Sign-in needs attention, Scan target", and never
holds the sentence, though the code sends the sentence first.

**3. How the settings case launches, and what NVDA on the runner meets
first.**

```
sed -n 45,70p nvda-tests/helpers/launch-app.js   -> spawn(APP_PATH, ["--scan-target", scanTarget], { env: { WIXEN_MAIL_DATA: dataDir } }): a fresh, empty profile per case
sed -n 93,110p nvda-tests/helpers/launch-app.js   -> waitForWindow polls for the main window, then sleeps extraSettleMs (3000 here) for the dialog on top
sed -n 15701,15720p src/presentation/wx_app.rs   -> ScanTarget::Settings calls handle_settings on the frame with the profile's (empty) accounts
gh run view 35336142914 --log, grepped for '--- settings ---' onward   -> the Accessibility scan on the same push: "Walked 2 window(s): 'Settings', 'Wixen Mail'", "MSAA walk: 2812 elements, 1634 of them operated, 0 without a name"
```

So on the runner the dialog opens on a fresh profile with no accounts, the
same way the Accessibility scan saw it minutes earlier, and what NVDA meets
first is that dialog. What the harness's log holds of that is nothing,
by the shape every other transcript shows. The corrected case counts from
a mark taken after the settle: if the tab row holds focus at open, the
first Right reaches Compose and the count begins there; if it does not,
the first Right moves nothing in the notebook and the case fails with
"never heard: Compose", which is a finding about focus at open on a fresh
profile and is written into the ledger as the runner's to show, not
guessed at here.

**4. The sign-in failure is two notifications a moment apart, and the
second is the one that was heard.**

```
sed -n 582,590p src/presentation/wx_account_manager.rs   -> OAuthFlowResult::Failed: said_and_shown(status, a11y, &format!("Signing in failed: {msg}"), Priority::High); then a11y.signal(FeedbackEvent::AccountNeedsAttention, &name)
sed -n 19,27p src/presentation/status_line.rs   -> said_and_shown: line.set_label(said); a11y.announce(said, priority)
sed -n 243,265p src/presentation/accessibility.rs   -> signal: the earcon, then announce_topic(event.text_with(detail), event.priority(), event.key()) when speech or braille is on, then the visual
grep -n 'AccountNeedsAttention =>' src/presentation/accessibility/feedback.rs   -> 203: "Sign-in needs attention"; 246: Priority::Urgent
sed -n 439,446p src/presentation/accessibility/screen_reader.rs   -> Urgent -> ImportantAll; Important (High) -> ImportantMostRecent; both are the processing values a screen reader may cancel current speech for
grep -rn 'AccountNeedsAttention' src --include='*.rs' | grep 'signal(' | grep -v '^\s*//'   -> wx_account_manager.rs:572, :580, :589, the three failure arms and nowhere else
```

The sentence goes out at High and the event's own line at Urgent, on two
topics, within a millisecond, and the runner's NVDA spoke the second. Why
the first is absent from NVDA's own log is not settled by reading this
tree, and `nvda-tests/README.md` says these tests never run on a machine
somebody is using, so it is not settled here either. What is settled: one
outcome sent as two notifications is guardrail 5's flood in miniature, and
one notification cannot cut off another. The fix makes the three arms one
call each, `shown_and_signalled(status, a11y, event, sentence)`, which puts
the sentence on the line and raises the event with the sentence as its
detail, so the words, the earcon, the braille and the visual come from one
event on the channels the person's Feedback row for it allows, and the
line NVDA hears is "Sign-in needs attention, Signing in failed: ..."; the
case's two substrings are both in it. `said_and_shown` and its guard
(`what_this_screen_never_says`, which counts `status.set_label(` in the
screen and requires the one helper to announce) stay as they are; the new
helper lives beside it in `status_line.rs`, so the screen's count of bare
`set_label` stays 0. The existing reading at `wx_account_manager.rs:2787`,
which counts `a11y.signal(FeedbackEvent::AccountNeedsAttention` against the
arms, is rewritten in place to count the new call, so the file's test count
stays 44. If the tester reports on this case by hand before the plan runs,
the coordinator's note goes into the summary beside the transcript.

**5. The Accessibility run walked the five editors with no unnamed
control, which is FOUND-08's second `[D]` line.**

```
gh run view 35336142914 --json jobs   -> Accessibility scan | completed | success
gh run view 35336142914 --log, saved and grepped for 'Walked\|MSAA walk:'
  -> contact-editor: Walked 'Edit Contact', 'Wixen Mail'; MSAA walk: 2915 elements, 1718 operated, 0 without a name
  -> condition-editor: 'Edit Condition'; 1978 elements, 1158 operated, 0 without a name
  -> filter-editor: 'Edit Filter Rule'; 2126, 1242, 0
  -> signature-editor: 'Edit Signature'; 1889, 1114, 0
  -> account-editor: 'Edit Account'; 3120, 1833, 0
  -> All 36 windows scanned, 20 violation(s) in total; new-event 9 violations and 8 without a name, send-later 6 and 4, compose 4, accounts 1
grep -n '^| 489 \|^| 492 ' .planning/WINDOWS.md   -> 489 open (the walk on the five editors left with -1073740791 here), 492 open (the settings case waits for the runner)
```

FOUND-08's second `[D]` line asked for the walk over the five editors on
CI; it ran and found none unnamed, so the line closes and FOUND-08 ticks.
The 20 violations elsewhere are older findings on windows this phase does
not touch (`new-event` and `send-later` carry ledger entries of their own,
408 among them) and are not this plan's. FOUND-09's third `[D]` line asked
for the runner's transcript; the tester's ear settled the requirement on
2026-09-18 and #33 is closed on it, so FOUND-09 ticks on the `[S]` line's
own terms with the runner's case named as the harness's remaining work.

**6. The gate for each file.** `.github/workflows/nvda.yml` maps to `all`;
`nvda-tests/` is JavaScript that no local check runs, by the README's rule,
so the case's change is read and not run here; `src/presentation/status_line.rs`
maps to `--lib presentation::status_line::`, 2 tests, 1 record;
`src/presentation/wx_account_manager.rs` to `--lib presentation::wx_account_manager::`,
44 tests, 10 records, one test rewritten in place.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: the job's verdict is the run's, the settings case waits for what can be heard, and the README says what the log holds</name>
  <files>.github/workflows/nvda.yml, nvda-tests/README.md, nvda-tests/tests/settings-tabs-read-once.test.js, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.github/workflows/nvda.yml`, all of it, and `.github/workflows/accessibility.yml:20-50`, for the paragraph the two share.
    - `nvda-tests/README.md`, all of it: the table of files names two cases and the skipped one, and three more exist.
    - `nvda-tests/tests/settings-tabs-read-once.test.js` and `nvda-tests/helpers/nvda-navigation.js`, for `waitToHearAll` and the mark.
    - `.planning/WINDOWS.md` entries 489 and 492, and the header rule that both halves are written by hand.
  </read_first>
  <action>
    Workflow, test code that no local check runs, and documents; nothing
    under `src/` changes in this task.

    In `nvda.yml`: remove `continue-on-error: true` from the job; leave
    `if: always()` on the summary and the upload; rewrite the header's last
    paragraph to say that since 2026-09-18 a failing case fails the run,
    because the run at `744d05ef` reported success over a job that failed
    and one case had failed at every run since 2026-09-15 with nobody told,
    and that the transcript is still uploaded on a red run. Keep the
    sentence about reading the transcript; it is still the finding.

    In `settings-tabs-read-once.test.js`: the first
    `waitToHearAll(nvda, [TABS[0].heardAs])` is replaced by the settle and
    the mark, the comment above it says why with the run named and that
    the tester heard the dialog by hand on 2026-09-18, the assertion that
    General is heard 0 times going right stays, and nothing else changes.
    Read, not run, by the README's rule.

    In `nvda-tests/README.md`, by `writing-craft`: the table of files lists
    every file under `tests/` with what each holds and whether it runs, the
    two sentences that say "two places" and "the first test described
    above" corrected by dating, and one paragraph on what the run at
    `744d05ef` showed about the harness: the spoken-phrase log begins with
    what the first key made NVDA say and holds no dialog-opening
    announcement, so a case waits for a key's reading and never for an
    opening.

    Ledger, by hand, both halves: 489 closed on run 35336142914 (the five
    editors walked, none unnamed; the walk still crashes here, ledger 390);
    492 closed on the tester's word of 2026-09-18 with the runner's case
    named as what still has to be heard there, and a new `unrun-verify`
    entry for the corrected case's next run (which tab the first Right
    reaches on a fresh profile is what it will show); one new `deviation`
    entry that `accessibility.yml` keeps `continue-on-error` and why. The
    changelog entry under `[Unreleased]`, Changed: the NVDA workflow fails
    when a case fails, with the date and the reason; no em dash, none of
    the six words.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_planning_files_agree_with_themselves && cargo test --test docs_links</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'continue-on-error' .github/workflows/nvda.yml` is 0 and `grep -c 'if: always()' .github/workflows/nvda.yml` is 2.
    - `grep -c '2026-09-18' .github/workflows/nvda.yml` is at least 1.
    - `grep -c 'waitToHearAll(nvda, \[TABS\[0\]' nvda-tests/tests/settings-tabs-read-once.test.js` is 0 and `grep -c 'timesHeard(heardGoingRight, TABS\[0\]' nvda-tests/tests/settings-tabs-read-once.test.js` is 1.
    - `nvda-tests/README.md` names all five files under `nvda-tests/tests/` (`for f in nvda-tests/tests/*.js; do grep -c "$(basename $f)" nvda-tests/README.md; done` prints no 0).
    - Ledger 489 and 492 are fixed in both halves and the two new entries are in both; `the_planning_files_agree_with_themselves` passes.
    - Carriage returns on every file touched are 0 by `tr -cd '\r' | wc -c`.
  </acceptance_criteria>
  <done>A failing NVDA case is a failing run, the settings case waits for what a key makes NVDA say, and the README says what the harness can and cannot hear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one notification for a sign-in failure</name>
  <files>src/presentation/status_line.rs, src/presentation/wx_account_manager.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/status_line.rs`, all of it, and `src/presentation/wx_account_manager.rs:540-600` (the reauthorise arms) and `:2770-2810` (the reading that counts the signals).
    - `src/presentation/accessibility.rs:243-300`, `signal`, and `src/presentation/accessibility/feedback.rs:200-260`, `text_with` and the priorities.
  </read_first>
  <behavior>
    - `status_line::shown_and_signalled(line: &StaticText, a11y: &Accessibility, event: FeedbackEvent, said: &str)` puts `said` on the line and calls `a11y.signal(event, said)` once; its doc says it is for an outcome that is both an answer and an event, so the words and the cue are one notification and neither can cut the other off, with the run at `744d05ef` named.
    - The three arms `NoCreds`, `NotSaved` and `Failed` in the reauthorise function call it with `FeedbackEvent::AccountNeedsAttention` and their sentence, and call neither `said_and_shown` nor `a11y.signal` themselves; the `Authorized` arm and the password-account arm are unchanged.
    - The reading at `wx_account_manager.rs:2787`, rewritten in place: every arm that leaves the account unauthorised calls `shown_and_signalled(` with `AccountNeedsAttention`, the count of those calls equals the count of such arms, and the function holds no separate signal of that event.
    - `status_line.rs` gains one test beside its two: the new helper's body sets the label and signals, read as text the way the existing test reads `said_and_shown`.
  </behavior>
  <action>
    Red first, on a branch: the rewritten reading in `wx_account_manager.rs`
    by module path (red because the arms still signal separately) and the
    new `status_line` test by module path (red because the helper does not
    exist; a stub that sets the label and does not signal keeps it red on
    the assertion rather than on a compile error); the count check for
    `status_line.rs`'s one record named bare. Then the helper and the three
    arms.

    Invoke `cognitive-accessibility` for the one line NVDA will hear:
    "Sign-in needs attention, Signing in failed: {reason}" is the event's
    word then the sentence, which is the order the feedback module already
    composes for every event; do not reword the sentence.

    The changelog entry under `[Unreleased]`, Fixed, by `writing-craft`:
    that a sign-in failure in the Account Manager was two announcements a
    moment apart and the runner's NVDA spoke only the second, and is one
    line now carrying the reason; that it was found by the NVDA workflow,
    which had been reporting success over it since 2026-09-15; Known
    limitations: whether the line is heard whole is the runner's next run
    to show, or the tester's ear.

    Guard records: one on `wx_account_manager.rs` whose break puts
    `said_and_shown` and a separate signal back into the `Failed` arm,
    measured on the library; one on `status_line.rs` whose break drops the
    signal from the helper; the record naming `status_line.rs` re-measured
    because its count moved.
  </action>
  <verify>
    <automated>cargo test --lib presentation::status_line:: && cargo test --lib presentation::wx_account_manager::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the rewritten reading and the new test by module path.
    - `grep -v '^\s*//' src/presentation/wx_account_manager.rs | grep -c 'shown_and_signalled('` is 3.
    - `grep -v '^\s*//' src/presentation/wx_account_manager.rs | grep -c 'a11y.signal(FeedbackEvent::AccountNeedsAttention'` is 0.
    - `cargo test --lib presentation::wx_account_manager::` passes with 44 tests, quoted; `presentation::status_line::` with 3.
    - Two guard records measured and one re-measured, quoted.
  </acceptance_criteria>
  <done>A sign-in failure is one notification carrying the sentence, on the channels the person's row for the event allows.</done>
</task>

<task type="auto">
  <name>Task 3: FOUND-08 ticked on the walk, FOUND-09 on the tester's ear, with the harness's case named</name>
  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`, FOUND-08 (`:2606-2658`) and FOUND-09 (`:2659-2690`) with their traceability rows at `:3467-3468`, and the manner 10-07 used to tick a requirement clause by clause.
    - `.planning/phases/09-what-the-first-day-of-testing-found/09-05-SUMMARY.md` and `09-06-SUMMARY.md`, the sections naming what each left for a push of `main`.
    - #33's closing comment of 2026-09-18 (`gh issue view 33 --json comments`), the tester's words.
  </read_first>
  <action>
    Documents only. FOUND-08: the second `[D]` line gains the dated
    sentence naming run 35336142914, the five editors' windows and the
    five "0 without a name" lines, and the requirement is ticked with its
    traceability row saying Complete, 09-05 at `165fd811` and this plan.
    FOUND-09: the third `[D]` line gains the dated sentence that the case
    ran once at `744d05ef` and heard nothing because it waited for an
    opening announcement the harness never captures, that 11-02 corrected
    the wait and the next push runs it, and that the requirement's own
    `[S]` line was settled by the tester on 2026-09-18 on
    `1.0.0-alpha.1+149.g744d05ef` (#33 closed on his word, quoted); the
    requirement is ticked and its row says Complete on the tester's ear,
    the runner's case pending. Neither `[S]` line is reworded.

    Nothing to close or comment on the tracker: #33 is closed already. The
    summary quotes the comment's timestamp.
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*FOUND-08\*\*' .planning/REQUIREMENTS.md` is 1 and `grep -c '^- \[x\] \*\*FOUND-09\*\*' .planning/REQUIREMENTS.md` is 1.
    - The traceability rows for FOUND-08 and FOUND-09 say what the requirement lines say.
  </acceptance_criteria>
  <done>What the two runs and the tester settled is written where the requirements live, and what is still the runner's says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a workflow's verdict to whoever reads the badge | a green run over a failed job is a claim nobody checks |
| this program to a screen reader, two notifications for one outcome | the second can cancel the first, and the person hears the cue and not the reason |
| the per-event channels to a sign-in failure | a row set to earcon only silences the words of an outcome that is also an answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Repudiation | `nvda.yml` reporting success over a failed job | high | mitigate | `continue-on-error` removed; the summary and upload keep `if: always()`; the header says why |
| T-11-12 | Denial of service | the event's notification cutting off the sentence | medium | mitigate | one call, one notification, the sentence as the event's detail; the reading holds the three arms to it |
| T-11-13 | Information disclosure | the sign-in reason on the line and in the notification | low | accept | the reason is `run_oauth_flow`'s own sentence and names no secret; unchanged |
| T-11-14 | Tampering | a Feedback row turning the words of a sign-in failure off | low | accept | the person's own row for the event, as phase 6 designed; the visual and the line stay |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | `nvda-tests/package-lock.json` unchanged; no crate added; the summary says so |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- The JavaScript case is not run here, by `nvda-tests/README.md`'s rule; the next push of `main` runs it, and that push is Pratik's.
- Two guard records measured, one re-measured; every record the count check flagged re-measured and read.
- What the runner settles: whether the sign-in line is heard whole and which tab the corrected case's first Right reaches on a fresh profile; what the tester settled: the dialog speaks, each tab once; what this machine proves: the arms, the helper and the workflow file's shape.
- After the merge, nothing is closed or commented: #33 is closed on the tester's word already.
</verification>

<success_criteria>
- Roadmap criterion 12 closes structurally: the verdict reaches the run, the two cases are read and acted on where each was at fault, FOUND-08 ticked on the walk, FOUND-09 on the tester's ear with the runner's case named.
- FOUND-18's `[D]` lines each have a named test, reading or run in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-02-SUMMARY.md` when done
</output>
