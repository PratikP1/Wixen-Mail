---
phase: 11-reading-and-the-list
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - src/presentation/wx_folder_choice.rs
  - src/presentation/accessibility/names.rs
  - src/presentation/wx_app.rs
  - src/application/blocking.rs
  - tests/a_kept_folder_reads_as_a_checked_check_box.rs
  - tests/theme_reach.rs
  - tests/wired.rs
  - guards/guards.toml
  - docs/PROVIDER_SETUP.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [LIST-01]
estimate:
  tokens: 39000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Folders to Keep Up to Date shows the account's folders as a tree nested the way the folder tree in the main window nests them, with a check state per folder, and a kept folder is heard as a checked check box and never as read-only (LIST-01, #70)."
    - "The title names the account the way the account manager names it, not its identifier."
    - "A folder the server flagged as holding every message, Gmail's All Mail, is in the tree unticked unless somebody ticked it; when Gmail did not list one, the dialog says so and says where Gmail decides that."
    - "The command is on the Tools menu with a letter nothing else on Tools claims, and every page that said File says Tools, dated."
  artifacts:
    - "src/presentation/wx_folder_choice.rs: a tree with check states, the parent of each row known, Space toggling, the answers read back from the control's own state."
    - "tests/a_kept_folder_reads_as_a_checked_check_box.rs: the reading that decided the control and the reading that holds the built dialog, over MSAA and the control's own state message."
    - "src/presentation/wx_app.rs: the item on Tools, the account's name passed, the rows carrying their parent."
    - "docs/PROVIDER_SETUP.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, src/application/blocking.rs: Tools, then Folders to Keep Up to Date."
  key_links:
    - "cache.folder_parents(account) -> FolderRow.parent -> the tree's nesting: the same parent_id the main window's tree reads, so the two cannot nest differently."
    - "TVM_GETITEMSTATE on each item -> changes(): what is written back is what the control shows, read from the control and not from a snapshot."
    - "the row's check state -> NVDA: read by NVDA from TVM_GETITEMSTATE on a SysTreeView32 with TVS_CHECKBOXES (sysTreeView32.py, read 2026-09-18), so no object this program writes stands between the state and the reader."
---

<objective>
Make Folders to Keep Up to Date a tree with a check box per folder whose
state reaches a screen reader, name the account in the title, show Gmail's
All Mail unticked when the server lists it and say so when it does not, and
move the command to Tools.

Purpose: #70, the tester on 2026-09-17 under NVDA on `1.0.0-alpha.1`: the
list is flat where the folder tree is nested; a kept folder is heard as
"check box, read-only, not checked"; the title is an identifier; All Mail is
missing; and, his fifth point, the command belongs on Tools. The module's own
header says "Still to be confirmed with a screen reader"; this is that
confirmation, and it failed.

Output: the dialog as a tree, the reading that decided which control, the
item on Tools, the four pages and one sentence corrected, the changelog
entry. Closes #70 on the tree side; whether NVDA says "checked" on his
machine is his ear's.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-reading-and-the-list/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-18 against `main` at `744d05ef`, version `1.0.0-alpha.1`,
working tree clean, nothing unpushed, before 11-01 and 11-02 land; neither
touches a file this plan touches. Command first, then what it said.

**1. The command is on Action, This Folder, and has not been on File since
2026-08-26. The issue and three pages say File.**

```
grep -n 'ID_CHOOSE_FOLDERS' src/presentation/wx_app.rs   -> 254 (the id), 4112 (the arm), 6584 (the item, inside the folder_menu builder that starts at 6565)
grep -n 'append_submenu(' -A 2 src/presentation/wx_app.rs | grep -n 'folder_menu' -A 1   -> 6815-6817: message.append_submenu(folder_menu, "&This Folder", ..)
grep -n '\.append(message, \|\.append(file, \|\.append(tools, ' src/presentation/wx_app.rs   -> 6998 file "&File", 7002 message "&Action", 7003 tools "&Tools"
git log --format='%h %ad %s' --date=short -S'"&This Folder"' -- src/presentation/wx_app.rs | tail -1   -> d9503015 2026-08-26 Put what you can do to a thing on the menu the doing lives on
grep -rn 'File, then Folders to Keep Up to Date' docs src --include='*.md' --include='*.rs'   -> docs/PROVIDER_SETUP.md:263, :587; src/application/blocking.rs:603 (WHERE_FOLDERS_ARE_CHOSEN, three tests at :1653, :1676, :1689)
grep -n 'Folders to Keep Up to Date' docs/KEYBOARD_SHORTCUTS.md   -> 555, the Action menu's This Folder row
```

The issue's `wx_app.rs:6563-6567` is the `folder_menu` builder, and a
builder's lines do not say which menu a submenu lands on; the
`append_submenu` call 250 lines later does, and `message` is the Action
menu. The pages said File from 2026-07-30 (`b7240fbb`) and were not
corrected when the item moved. Pratik's decision stands as made: Tools. The
move is from Action, This Folder to Tools, and the pages go from File to
Tools, each with the date and the fact that they said File while the item
was on Action.

**2. The Tools menu's letters, read off the builder.**

```
awk '/let tools = Menu::builder/,/\.build\(\);/' src/presentation/wx_app.rs | grep -o '"[^"]*&[^"]*"'
  -> A (Account Manager), n (Sync Contacts), d (Sync Calendar), T (Sync Tasks), W (Which Copy), C (Contact Manager), F (Message Filters), k (Blocked Senders), i (Signatures), g (Tags), b (Calendar by Address), r (Address Book by Address), O (Flush Outbox), P (Pause Downloading), S (Settings)
```

F is taken, as the issue says; so are O and D. `l`, `e` and `U` are free.
The item is "Fo&lders to Keep Up to Date...", Alt+L on the Tools menu,
placed after Pause Downloading and before Settings, because both are about
the download. `test_no_two_items_on_one_menu_claim_the_same_letter` in the
window's own tests holds it; re-read the letters on the day, since 09-10
found a letter taken that a plan assumed free.

**3. Gmail's All Mail is not in the tester's folder list because his server
did not list it, not because this program drops it.**

```
grep -n 'holds_all_mail' src/application/mail_sync.rs   -> 718 (set_folder_server_facts writes it for every listed folder), 1614 (sync_by_default: never by default), 1649 (cached_folder_syncs reads it)
sed -n 694,718p src/application/mail_sync.rs   -> store_folders saves every folder the server listed except one named like a local folder; nothing else is dropped
python -c "import sqlite3;c=sqlite3.connect(r'file:C:\Users\prati\AppData\Local\wixen-mail\cache\message_cache.db?mode=ro',uri=True);print(c.execute('select count(*), sum(holds_all_mail) from folders').fetchone());print([r[0] for r in c.execute(\"select path from folders where path like '[Gmail]%' order by id\")])"
  -> (50, 0); ['[Gmail]/Drafts', '[Gmail]/Sent Mail', '[Gmail]/Spam', '[Gmail]/Trash', '[Gmail]', '[Gmail]/QC Docs', '[Gmail]/QC Docs/QILC', '[Gmail]/Starred']
```

Read 2026-09-18 through Python, read-only, the one process on this machine
that sees the live profile, for this fact and nothing else. Fifty folders,
none flagged as holding every message, and neither `[Gmail]/All Mail` nor
`[Gmail]/Important` among them. Gmail lets a person hide a label from IMAP
(Gmail's settings, Labels, the "Show in IMAP" box), and a hidden label is
absent from `LIST "" "*"`, which is the only thing this program reads folders
from. So on his account the dialog cannot show a row the server never
sent. What this plan does: a listed `\All` folder is in the tree unticked
unless chosen, held by a test; and when the account is Gmail and no listed
folder holds every message, the dialog says so in one sentence and says where
Gmail decides it, rather than showing a tree that reads as if All Mail did
not exist. Whether his own Gmail hides it is his to look at, said in the
close comment.

**4. What the rows answer today, and why "read-only" is not in it.**

```
sed -n 232,262p src/presentation/accessibility/names.rs   -> CheckedRows::get_state: FOCUSABLE | SELECTABLE, CHECKED when ticked, FOCUSED | SELECTED for the current row; READONLY nowhere
grep -n 'list.check(\|set_accessible_checked_rows(' src/presentation/wx_folder_choice.rs   -> 163 (the ticks, before), 175 (the rows, after)
grep -rn 'set_accessible_checked_rows(' src --include='*.rs' | grep -v names.rs   -> the folder chooser only
```

The object this program writes never answers read-only and ticks the rows
before the snapshot is taken, so either the snapshot is read before the
owner-drawn list has the ticks or NVDA is reading a different object for the
row. Task 1 reads the object this program writes from the built dialog and
reads a native tree's own state, and the summary says which of the two the
tester's report matches. The reading NVDA does for a `SysTreeView32` item was
read from NVDA's own source on 2026-09-18
(`source/NVDAObjects/IAccessible/sysTreeView32.py`): `TVM_GETITEMSTATE` with
`TVIS_STATEIMAGEMASK`, state image 1 unchecked, 2 checked, 3 half; the
level from `accValue`. No object this program writes is in that path.

**5. wxdragon 0.9.17's tree has no check state and no style for one.**

```
grep -n 'Default\|HasButtons\|LinesAtRoot\|NoLines\|Single\|HideRoot\|EditLabels' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/treectrl.rs | head -8   -> seven styles, none a check box
grep -n 'state\|check' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/treectrl.rs | grep 'pub fn'   -> nothing
grep -rn 'SetWindowLongPtrW\|TVM_\|TVS_' src --include='*.rs' | head -3   -> nothing: no tree in this tree uses a native style wx does not offer
```

So the check state is set and read through Windows directly, behind
`cfg(target_os = "windows")` with a fallback that keeps the crate building,
as `CLAUDE.md` asks: `SetWindowLongPtrW` to add `TVS_CHECKBOXES` after the
control is created and before any item is added (which is the order the
style needs), `TVM_SETITEMW` with `TVIS_STATEIMAGEMASK` to tick, and
`TVM_GETITEMSTATE` to read. Raw `extern "system"` declarations on the shape
`tests/every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built.rs`
already uses, so no crate feature reaches the shipping binary.

**6. The hierarchy is stored and the main window's tree reads it.**

```
grep -n 'pub fn folder_parents' src/data/message_cache/folders.rs   -> 661: path -> Option<parent id>, read from parent_id and never from the path
grep -n 'fn nested' src/presentation/folder_tree.rs   -> 1074
grep -n 'pub parent: Option<i64>' src/presentation/folder_tree.rs   -> 324, on FolderInTheTree
```

`FolderRow` gains `parent: Option<String>`, the parent's path, filled in
`choose_folders` from `folder_parents` and the rows' ids, and the tree nests
by it. The separator is never split; that was plan 01-04's rule and the
reason `folder_parents` exists.

**7. The account's name is on the account row the window already holds.**

```
sed -n 19333,19336p src/presentation/wx_app.rs   -> active_account_id is what ask() is handed
grep -n 'pub name: String' src/data/account.rs   -> the display name the account manager shows
```

**8. The gate for each file.** `src/presentation/wx_folder_choice.rs` maps to
`--lib presentation::wx_folder_choice::`, 8 tests, 1 record;
`src/presentation/accessibility/names.rs` to `--lib presentation::accessibility::names::`,
32 tests, 4 records; `src/presentation/wx_app.rs` to `--lib presentation::wx_app::`,
199 tests, 73 records, no test added or removed; `src/application/blocking.rs`
to `--lib application::blocking::`, 67 tests, 2 records, three tests
rewritten in place; `tests/theme_reach.rs` to itself, no record;
`tests/wired.rs` to itself, 77 tests, 18 records, no test added. The new
target is at zero records until its record lands with `suite` naming it.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read what a row answers today and what a native tree with check boxes answers, then hold the dialog to a tree</name>
  <files>tests/a_kept_folder_reads_as_a_checked_check_box.rs, src/presentation/wx_folder_choice.rs, guards/guards.toml</files>
  <read_first>
    - `tests/every_settings_checkbox_reads_as_a_checkbox_after_its_page_is_built.rs`, all of it: the one-session `OnceLock<Result<Harvest, String>>` shape, the user32 and oleacc declarations, `Guid`, the `Variant`, and how a child id is passed to `get_accRole` and `get_accState`.
    - `src/presentation/wx_folder_choice.rs`, all of it, and `src/presentation/accessibility/names.rs:150-380`, the object the rows answer with today.
    - `tests/theme_reach.rs:760-790`, how the chooser is built in a test on a fixture.
    - NVDA's `sysTreeView32.py`, the paragraph in premise 4 above; the reading it does is the reading task 1 takes.
  </read_first>
  <behavior>
    - Reading A, the dialog as it stands, taken before anything changes and kept in the target as a record of the day: `build_folder_choice_dialog` over three rows (INBOX ticked, Archive unticked, a `holds_all_mail` row unticked); `AccessibleObjectFromWindow(list hwnd, OBJID_CLIENT, IID_IAccessible)`; for children 1 to 3, `get_accRole`, `get_accState` and `get_accName`; the harvest carries all three per row. Assert what the code claims: role `ROLE_SYSTEM_CHECKBUTTON` (0x2c) on every row, `STATE_SYSTEM_CHECKED` (0x10) on row 1 and not on rows 2 and 3, `STATE_SYSTEM_READONLY` (0x40) on none. Whether this is green or red on arrival is the finding: green means the object this program writes is right and NVDA is reading something else for the row; red means the snapshot is wrong. Either way the summary quotes the harvest.
    - Reading B, a scratch `SysTreeView32` in the same session: a `TreeCtrl` built on the same frame, `TVS_CHECKBOXES` added through `SetWindowLongPtrW(GWL_STYLE)` before any item, three items with one nested, item 2 ticked through `TVM_SETITEMW` with `TVIS_STATEIMAGEMASK`; assert `TVM_GETITEMSTATE` answers state image 2 on item 2 and 1 on the others, `get_accState` on the item's child id carries `STATE_SYSTEM_CHECKED` for item 2 only, and `get_accValue` on the nested item answers its level; then `WM_KEYDOWN` `VK_SPACE` sent to the tree with item 1 selected, and the state read again: if it moved to 2, the control toggles on its own; if not, the dialog toggles it (task 2's handler), and the summary says which.
    - Reading C, red until task 2: `build_folder_choice_dialog` over rows carrying parents (INBOX; `[Gmail]`; `[Gmail]/QC Docs` under `[Gmail]`; `[Gmail]/QC Docs/QILC` under `[Gmail]/QC Docs`) answers a `TreeCtrl`; the item for QILC has the item for QC Docs as its parent by `get_item_parent`; the ticked row's `TVM_GETITEMSTATE` is 2 and its MSAA state carries CHECKED and not READONLY; every item is expanded; focus is on the first row.
    - A companion hands reading C's predicate a tree where the ticked row's state image is 1 and requires it to complain about the state, and one where QILC is under `[Gmail]` and requires it to complain about the parent.
    - In `wx_folder_choice.rs`, red until task 2 and by module path: `FolderRow.parent: Option<String>`; a pure `nesting(rows) -> Vec<(row index, depth, parent index)>` in the stored order with a child after its parent, a parent absent from the rows treated as the top, so a folder can never vanish because its parent was not listed; `the_all_mail_sentence(is_gmail: bool, any_holds_all_mail: bool) -> Option<String>` answering the one sentence only for Gmail with no such folder; `row_label` unchanged.
  </behavior>
  <action>
    Red first, on a branch: readings A and B are readings and may be green
    on arrival, said in the commit; reading C, the two companions and the
    three module tests are the red, named bare and by module path in the
    trailer, with the count check for `wx_folder_choice.rs`'s one record
    named bare because the file gains tests.

    The target is `cfg(windows)` and copies the probe's declarations rather
    than adding a crate feature (`Cargo.toml` and `Cargo.lock` untouched,
    T-11-SC). It builds every window it reads, sends messages only to those,
    and never calls `show_modal`. If `theme::current` answers `None` the
    harvest is an error naming High Contrast, as the sibling target does.

    Then the pure half of task 2's shape in `wx_folder_choice.rs`: the
    field, `nesting`, `the_all_mail_sentence`, with their tests green and
    reading C still red, which is the honest stopping point for this task.

    Guard records: one on `wx_folder_choice.rs` whose break makes `nesting`
    put every row at the top, measured on the library; the existing record
    naming the file re-measured because its count moved.
  </action>
  <verify>
    <automated>cargo test --test a_kept_folder_reads_as_a_checked_check_box && cargo test --lib presentation::wx_folder_choice::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names reading C, the two companions and the module tests, and says which of readings A and B were green on arrival and what each harvested.
    - The summary quotes reading A's rows (role, state, name per row) and reading B's states before and after Space.
    - `cargo test --lib presentation::wx_folder_choice::` passes with more than 8 tests after the green, the count quoted.
    - Two guard records measured and quoted; `Cargo.toml` and `Cargo.lock` unchanged by `git diff --stat main..HEAD`.
  </acceptance_criteria>
  <done>What the current rows answer over MSAA is on the record, a native tree with check boxes is shown to carry its state where NVDA reads it, and the dialog is held to being that tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the dialog as a tree with a check state per folder, the account's name in the title, All Mail said</name>
  <files>src/presentation/wx_folder_choice.rs, src/presentation/accessibility/names.rs, src/presentation/wx_app.rs, tests/theme_reach.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/wx_app.rs:19320-19400`, `choose_folders`, and `:19400-19440`, the subscription writes it hands to.
    - `src/presentation/folder_tree.rs:1074-1140`, `nested`, for how depth and expandable are computed there; this dialog nests the same way and expands everything.
    - `src/presentation/accessibility/platform_bridge.rs`, for how this crate already declares a Windows call behind `cfg(target_os = "windows")` with a fallback.
    - `tests/theme_reach.rs:712-790`, the two checks that build the Columns dialog and the chooser.
  </read_first>
  <behavior>
    - `build_folder_choice_dialog(parent, account_name, folders, palette) -> (Dialog, TreeCtrl)`: a `TreeCtrl` with `HideRoot`, one item per row in `nesting`'s order under its parent's item, `TVS_CHECKBOXES` added through `SetWindowLongPtrW` before the first item, each item's state image set from `folder.syncing` through `TVM_SETITEMW`, every item expanded, the label `row_label(folder)`, the first item selected and focused. Space toggles the item under the cursor: natively if reading B found the control does it, otherwise a `KEY_DOWN` handler on the tree flips the state image through `TVM_SETITEMW` and does not skip, with the reason in a comment naming reading B. On a platform that is not Windows the tree is built without check states and `ask` answers `None`, with the comment saying the dialog is Windows-only until another bridge exists, as `CLAUDE.md` says of `wxAccessible`.
    - `changes(folders, ticked)` is unchanged in shape; `ask` reads each item's state through `TVM_GETITEMSTATE` at OK, never from a snapshot, so what is written back is what the control shows.
    - The title is "Folders to keep up to date: {account name}"; `choose_folders` passes the account's `name` from the account row the state holds and fills `FolderRow.parent` from `cache.folder_parents(&account_id)` mapped through the rows' ids to paths.
    - Under the tree, when `the_all_mail_sentence` answers one, a `StaticText` on both channels: "Gmail did not list All Mail, so it cannot be kept up to date from here. Gmail's own settings, Labels, Show in IMAP, decide which labels it lists." The explaining sentence above the tree keeps "Space ticks the folder under the cursor" and adds "Right arrow opens a folder that holds others."
    - `set_accessible_checked_rows` and `CheckedRows` lose their only caller. If reading B's native path is taken they are retired with their tests in this commit and the four records naming `names.rs` are re-measured, the count quoted before and after; if the fallback path is taken they are moved onto the tree's items and kept. `set_accessible_name` on the tree stays.
    - `tests/theme_reach.rs`'s folder-chooser check reads the tree's colour where it read the old control's; count unchanged.
  </behavior>
  <action>
    Green for reading C and the module tests, on the branch. The Windows
    calls live in one small module, `presentation::native_tree_checks`
    (or a `cfg(windows)` block at the foot of `wx_folder_choice.rs` if it
    stays under sixty lines), with `SetWindowLongPtrW`, `SendMessageW`, the
    `TVITEMW` layout, `TVIF_STATE`, `TVIS_STATEIMAGEMASK`, `TVM_SETITEMW`,
    `TVM_GETITEMSTATE` and `TVS_CHECKBOXES` declared by hand with their
    values and a comment naming the Windows header each comes from. The tree
    item handle comes from wxdragon's `TreeItemId` if it exposes the raw
    `HTREEITEM`; if it does not, walk the native tree with `TVM_GETNEXTITEM`
    in the order the items were appended, which is the order `nesting`
    answers, and say so in the comment.

    Invoke `cognitive-accessibility` for the two sentences under and above
    the tree: what Space does, what Right arrow does, what Gmail decides and
    where.

    The changelog entry under `[Unreleased]`, Fixed, in the same commit:
    the tester's four findings and his fifth in his words; a tree nested the
    way the folder tree is; a check box per folder whose state is the
    control's own; the account's name in the title; All Mail unticked when
    listed and the sentence when it is not; the move to Tools with the
    letter; the dated correction that the pages said File while the command
    sat on Action, This Folder from 2026-08-26; Known limitations: nobody has
    heard the tree, and whether Gmail lists All Mail is Gmail's setting.

    Guard records: one on `wx_folder_choice.rs` whose break sets every
    item's state image to 1 whatever `syncing` says, with `suite` naming the
    new target; one on `wx_app.rs` whose break passes `account_id` back into
    the title, `suite` the new target if the target reads the window's
    source for it, else the library; each measured. `wx_app.rs` stays at
    199, quoted before and after.
  </action>
  <verify>
    <automated>cargo test --test a_kept_folder_reads_as_a_checked_check_box && cargo test --lib presentation::wx_folder_choice:: && cargo test --lib presentation::accessibility::names:: && cargo test --test theme_reach && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - Reading C and its companions pass; the two module tests for `nesting` and the sentence pass.
    - `grep -v '^\s*//' src/presentation/wx_folder_choice.rs | grep -c 'CheckListBox'` is 0; `grep -v '^\s*//' src/presentation/wx_folder_choice.rs | grep -c 'TreeCtrl'` is at least 3.
    - `grep -n 'ask(frame, ' src/presentation/wx_app.rs` shows the account's name and not `account_id` as the second argument.
    - `grep -v '^\s*//' src/presentation/wx_folder_choice.rs | grep -c 'Show in IMAP'` is 1.
    - `cargo test --lib presentation::wx_app::` passes with 199 tests, quoted.
    - Two guard records measured and quoted; the four `names.rs` records re-measured if the file's count moved, quoted.
    - The changelog entry exists under `[Unreleased]` and names #70.
  </acceptance_criteria>
  <done>The dialog is a tree whose check states are the control's own and reach the channel NVDA reads, the title names the account, All Mail is a row when the server lists it and a sentence when it does not, and nothing this program writes stands between the state and the reader.</done>
</task>

<task type="auto">
  <name>Task 3: the command on Tools, and every page that said File</name>
  <files>src/presentation/wx_app.rs, src/application/blocking.rs, tests/wired.rs, docs/PROVIDER_SETUP.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:6565-6600` (the `folder_menu` builder) and `:6829-6900` (the `tools` builder).
    - `src/application/blocking.rs:595-610` and `:1645-1695`, the sentence and its three tests.
    - `tests/wired.rs:2040-2070` and `:2395-2430`, the two readings that name `ID_CHOOSE_FOLDERS`.
    - `docs/KEYBOARD_SHORTCUTS.md:527-558` (the Action menu section) and the Tools section; `docs/PROVIDER_SETUP.md:258-266` and `:584-592`.
  </read_first>
  <action>
    Move the `append_item(ID_CHOOSE_FOLDERS, ..)` from the `folder_menu`
    builder to the `tools` builder as "Fo&lders to Keep Up to Date..." after
    Pause Downloading, with the separator kept sensible; the arm at `:4112`
    is unchanged because it follows the id. Read the letters off the builder
    again before choosing; if `l` is taken by then, `e`, then `U`. The
    context-menu entries in `context_menu.rs` stay where they are, since the
    menu key on a folder row is a second way in and not the item.

    `blocking.rs`'s `WHERE_FOLDERS_ARE_CHOSEN` becomes "Tools, then Folders
    to Keep Up to Date", and its three tests are rewritten in place, count
    unchanged at 67; the two records naming the file are re-measured only if
    the count check names them.

    `tests/wired.rs`: the File-menu reading already holds the id absent from
    the `file` block and stays; add nothing. If `is_on_a_menu` reads the
    `tools` block it is green already; quote the run.

    The pages, by `writing-craft`: `docs/PROVIDER_SETUP.md:263` and `:587`
    say Tools, then Folders to Keep Up to Date, a tree rather than a ticked
    list, with the dated sentence that until this build the page said File
    while the command was on Action, This Folder from 2026-08-26, and before
    that on File; `:587`'s paragraph says Right arrow opens a folder that
    holds others and that Gmail's All Mail appears only when Gmail lists it.
    `docs/KEYBOARD_SHORTCUTS.md`: the This Folder row at `:555` loses the
    command with a dated clause, and the Tools section gains a row for it
    with its letter. The changelog entry from task 2 gains the sentence
    about the move if it was written without it.

    Ledger, by hand, both halves: one `unrun-verify` entry for what only the
    tester's ear settles for #70 (the tree read as a tree with levels, a
    kept folder heard as checked, Space heard as the new state, the title,
    the All Mail sentence, and whether his Gmail lists All Mail at all).
  </action>
  <verify>
    <automated>cargo test --lib presentation::wx_app:: && cargo test --lib application::blocking:: && cargo test --test wired && cargo test --test house_style && cargo test --test docs_links && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/let tools = Menu::builder/,/\.build\(\);/' src/presentation/wx_app.rs | grep -c 'ID_CHOOSE_FOLDERS'` is 1 and `awk '/let folder_menu = Menu::builder/,/\.build\(\);/' src/presentation/wx_app.rs | grep -c 'ID_CHOOSE_FOLDERS'` is 0.
    - `grep -rn 'File, then Folders to Keep Up to Date' docs src --include='*.md' --include='*.rs'` finds only lines inside dated sentences saying what the page used to say.
    - `cargo test --lib application::blocking::` passes with 67 tests, quoted.
    - Carriage returns on every page touched are 0 by `tr -cd '\r' | wc -c`; no em dash; none of the six words.
    - The ledger entry is in both halves and `the_planning_files_agree_with_themselves` passes.
  </acceptance_criteria>
  <done>The command is on Tools with a letter of its own, and every page and sentence that pointed at File points at Tools with the date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the dialog to the stored choices | a tick read from the wrong row writes a choice against a folder nobody asked about, and the next check downloads or drops it |
| this program to Windows' tree control | a style and two messages sent by hand to a control wx owns; a wrong struct layout is undefined behaviour, not an error |
| the server's folder list to the tree | a folder listed with no parent among the rows must not vanish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Tampering | the state read at OK | high | mitigate | read from `TVM_GETITEMSTATE` per item at OK, never from a snapshot; `changes` writes only what moved; reading C holds the ticked row's state |
| T-11-08 | Denial of service | a wrong `TVITEMW` layout or a message sent to a destroyed window | high | mitigate | the layout declared by hand with the header named and checked against the probe in reading B; every message sent to a window this code built and still holds |
| T-11-09 | Information disclosure | the account's name in a title | low | accept | the name is what the account manager already shows in the main window's tree |
| T-11-10 | Repudiation | a folder with an unlisted parent dropped from the tree | medium | mitigate | `nesting` treats an absent parent as the top, with a test |
| T-11-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the Windows calls are declared by hand; the summary says so |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge, its exit status read directly.
- Four guard records measured; every record the count check flagged re-measured and read.
- The tester's profile read once through Python, read-only, for the fact in premise 3 and nothing else; no binary started.
- What the tester's ear settles: whether NVDA says "check box, checked" on a kept folder, the level on a nested one, the new state on Space, the account's name in the title, and the All Mail sentence; what the readings prove: the state on the channel NVDA reads, the nesting, the title, the row for a listed `\All` folder, the sentence for an unlisted one, the item on Tools.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 70 --comment "Fixed in <merge commit>: Folders to Keep Up to Date is a tree nested the way the folder tree is, with a check box per folder whose state is the control's own and is what NVDA reads for a tree item; the title names the account; All Mail is a row, unticked, when Gmail lists it, and when Gmail does not the dialog says so and says where Gmail decides that (your folder list holds no All Mail, so look at Gmail's settings, Labels, Show in IMAP). The command is on Tools, Folders to Keep Up to Date; the pages said File while it sat on Action, This Folder, and say Tools now. What only your ear settles: that a kept folder is heard as checked, that Space says the new state, and the level on a nested folder."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 1 closes structurally: the tree, the check state on the channel NVDA reads, the title, All Mail when listed and the sentence when not, the item on Tools, the pages dated.
- LIST-01's `[D]` lines each have a named test or reading in the summary; its `[S]` line is untouched.
</success_criteria>

<output>
Create `.planning/phases/11-reading-and-the-list/11-03-SUMMARY.md` when done
</output>
