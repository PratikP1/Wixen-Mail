---
phase: 09-what-the-first-day-of-testing-found
plan: 01
type: execute
wave: 2
depends_on: ["09-11"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - src/common/version.rs
  - tests/installer.rs
  - .github/workflows/release.yml
  - .claude/skills/cutting-a-release/SKILL.md
  - CLAUDE.md
  - docs/changelog.md
  - docs/BETA_RELEASE.md
  - guards/guards.toml
autonomous: true
requirements: [FOUND-01]
estimate:
  tokens: 19500
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cargo.toml and Cargo.lock say 1.0.0-alpha.1, wixen-mail --version prints it, and every fix that follows in this phase lands under that number (FOUND-01, #46)."
    - "A test on common::version names the exact steps 0.125.1 to 1.0.0-alpha.1 to 1.0.0 in both directions, and a test on the installer script's arithmetic holds 1.0.0.1001 above 0.125.1.4000 the way Windows orders four fields."
    - "The Release workflow has a level that publishes the version the tree already carries, so the first alpha Pratik dispatches is 1.0.0-alpha.1 and not 1.0.0-alpha.2, and a test reads that level."
    - "CLAUDE.md, the changelog's opening paragraph, docs/BETA_RELEASE.md and the release skill state one rule for moving inside a prerelease, and it is the rule the phase README states."
  artifacts:
    - "Cargo.toml: version = \"1.0.0-alpha.1\"; Cargo.lock: the same for the wixen-mail package."
    - "src/common/version.rs: one test naming the two steps, each way."
    - "tests/installer.rs: a reading of build-installer.sh's stage arithmetic for the two versions; a reading of release.yml's as-is level."
    - ".github/workflows/release.yml: the new level and its cargo release line."
    - "docs/changelog.md: the opening paragraph says the scheme in force; an [Unreleased] entry says the number moved and why."
    - "CLAUDE.md, docs/BETA_RELEASE.md, .claude/skills/cutting-a-release/SKILL.md: the same rule, the old wording kept with its date."
  key_links:
    - "Cargo.toml -> scripts/build-installer.sh:12 -> installer/Wixen-Mail-Setup.iss: the number is read once and stamped everywhere; Cargo.lock:6805 is the one other copy and cargo rewrites it on the next build."
    - "release.yml's cargo release line -> the tag -> the installer name: a level that bumps before it tags cannot publish the version the tree carries, which is why the as-is level exists."
    - "cargo-release's patch level on a prerelease strips the suffix: a patch dispatch on 1.0.0-alpha.N publishes 1.0.0 as a full release, so the skill says never to dispatch patch on a prerelease."
---

<objective>
Move the tree to `1.0.0-alpha.1`, so every fix in this phase lands under the
number it will ship as, and write down how a version moves inside a
prerelease so nobody has to work it out from a workflow's comments.

Purpose: Pratik decided on 2026-09-15 (#46) that the next builds are the alpha,
beta and release-candidate stages of 1.0.0. Twenty-five `0.1.0-alpha.N`
versions were once cut as a build counter, which is why `CLAUDE.md` forbids
that shape; this plan moves the number once, by hand, and states the rule that
keeps it from becoming a counter again.

Output: the bump, two pins, one workflow level, and four documents saying one
rule. Closes #46 on the tree side. Whether the first alpha is published is a
dispatch and is Pratik's.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-what-the-first-day-of-testing-found/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-16 against `main` at `524ff24f`, version `0.125.1`, working tree
clean. Command first, then what it said.

**1. The number lives in two lines and the installer reads one of them.**

```
grep -n '^version' Cargo.toml                       -> 3:version = "0.125.1"
grep -rn '0\.125\.1' --include='*.toml' --include='*.lock' --include='*.rs' --include='*.md' --include='*.iss' --include='*.sh' --include='*.yml' . | grep -v '^./target\|^./.planning\|changelog'
                                                    -> ./Cargo.lock:6805, ./Cargo.toml:3
grep -n '^version' search-handler/Cargo.toml        -> 3:version = "0.1.0"   (its own crate, untouched)
```

**2. The installer's arithmetic takes a major of 1 and a prerelease step, and
Windows orders the result above today's.** `scripts/build-installer.sh:92-107`:
`stage` is 1 for alpha, 2 beta, 3 rc, 4 for a plain version; `step` is the
number after the dot, capped at 999; `VERSION_INFO` is
`major.minor.patch.(stage*1000+step)`. So `1.0.0-alpha.1` is `1.0.0.1001` and
`0.125.1` is `0.125.1.4000`. Windows compares the four fields as numbers left
to right, so `1.0.0.1001 > 0.125.1.4000` by the first field, and Inno Setup's
`VersionInfoVersion` accepts any field up to 65535. Nothing in
`tests/installer.rs` reads this arithmetic today (`grep -n 'stage\|1001\|4000'
tests/installer.rs` -> nothing); the four tests there that read the script are
about icons, shortcuts and the release's promised files.

**3. `common::version` already orders the step and has no test naming it.**

```
grep -n 'fn test' src/common/version.rs | wc -l    -> 24
sed -n 425,435p src/common/version.rs               -> compare("1.0.0", "0.99.0") == Newer; compare("0.6.0", "0.6.0-alpha.1") == Newer
```

A test for `compare("1.0.0-alpha.1", "0.125.1") == Newer` and
`compare("1.0.0", "1.0.0-alpha.1") == Newer`, each with its reverse, will pass
on arrival. That is a pin, not a red half, and the summary says so; the
record's break is what shows it can go red. Two records name this file.

**4. The Release workflow bumps before it tags, so from a tree at
`1.0.0-alpha.1` the `alpha` level publishes `-alpha.2`.**

```
grep -n 'major\|cargo install cargo-release\|cargo release \${{\|prerelease = if' .github/workflows/release.yml
  -> no major level; 90: cargo install cargo-release --locked; 100: cargo release ${{ inputs.release_level }} --execute --no-confirm --no-publish (continued on the next line with --allow-branch main --push-remote origin); 110: the prerelease capture, inside the step at 109-111
```

(Corrected 2026-09-16 after the plan check: the first draft cited the
`cargo release` line as `:91`; it is `:100`, the install is `:90` and the
capture `:109-111`.)

cargo-release's reference (`docs/reference.md` in `crate-ci/cargo-release`,
read 2026-09-16): `alpha`, `beta`, `rc` "Add/increment pre-release to your
version (1.0.0 -> 1.0.1-rc.1, 1.0.1-alpha -> 1.0.1-rc.1, 1.0.1-rc.1 ->
1.0.1-rc.2)"; `release` "Remove the pre-release extension; if any (0.1.0-alpha.1
-> 0.1.0, 0.1.0 -> 0.1.0)"; `patch` "If version has a pre-release, then the
pre-release extension is removed (0.1.0-alpha.1 -> 0.1.0)". Two consequences
this plan carries. The `0.1.0 -> 0.1.0` example says cargo-release will tag a
version it did not change, which is the as-is publish task 2 adds, given the
version explicitly. And `patch` on a prerelease is a full release in disguise,
which the skill and `CLAUDE.md` must say.

**5. The issue's list of documents naming a version shape is two too long.**

```
grep -n 'alpha\|version\|0\.' src/presentation/first_run.rs   -> "an alpha" as a state; no number
grep -n 'alpha\|version' src/presentation/command_line.rs      -> --version; "experimental"; no number shape
grep -n '0\.x\|alpha' docs/changelog.md | head -3               -> line 5 names the 0.x.y scheme
grep -n '0\.x\|alpha' docs/BETA_RELEASE.md                      -> lines 42-44 name it
grep -n -i 'alpha\.[0-9]\|0\.x\|version' docs/ALPHA_TESTING.md   -> 7, 107, 115: prose; 187 names 0.111.0 as history ("an account set up before version 0.111.0"); no scheme
git ls-files .claude/skills/cutting-a-release/SKILL.md          -> tracked
```

So the first-run screen, `--help` and the alpha testing page need nothing:
the page names one past version as history and no shape. The changelog's
opening paragraph, `docs/BETA_RELEASE.md`, `CLAUDE.md`'s versioning section
and the release skill do.

**6. The changelog has one heading above the old alphas.**

```
grep -n '^## ' docs/changelog.md   -> 7:## [Unreleased]  11403:## [0.1.0-alpha.9]  11424:## [0.1.0-alpha.8]  11442:## [0.1.0-alpha.1]
```

Everything from `0.1.0-alpha.10` to today sits under `[Unreleased]`, and
cargo-release does not touch this file (no `release.toml`, no
`[package.metadata.release]` in `Cargo.toml`). "The changelog's next heading is
`1.0.0-alpha.1`" means: the heading written by hand when the first alpha is
cut. This plan does not write it.

**7. Which tests read the version.** `tests/command_line_output.rs:52` reads
`CARGO_PKG_VERSION` and passes at any number. `tests/house_style.rs:3348`,
`test_no_status_page_names_a_version_the_code_does_not_ship`, reads
`README.md` and `docs/IMPLEMENTATION_STATUS.md`, neither of which names a
version, so it passes at any number too and its companion is what proves it
reads. `tests/installer.rs:362` reads `Cargo.toml`'s version to compose the
installer's name. None goes red on the bump.

**8. The gate for each file.** `Cargo.toml` and `Cargo.lock` map to no
target, so on a branch only the whole-tree guards run for them;
`.github/workflows/` answers `all`; `src/common/version.rs` maps to `--lib
common::version::`; `tests/installer.rs` to `--test installer`; `CLAUDE.md`
and the two `docs/` pages to the document-reading targets; the skill file to
nothing. The commit that changes the workflow pays the whole gate.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the number, and two pins that name the step</name>
  <files>Cargo.toml, Cargo.lock, src/common/version.rs, tests/installer.rs, guards/guards.toml</files>
  <read_first>
    - `src/common/version.rs:156-250`, `parse` and `compare`, and the tests at `:425-480`, for the shape a test naming a step takes.
    - `scripts/build-installer.sh:75-107`, the arithmetic and its comment table.
    - `tests/installer.rs:354-378`, `the_release_workflow` and `the_version`, for how this target reads the script and the manifest, and `:204-230` for a test that reads a script section.
    - `CLAUDE.md`, the paragraph beginning "Every build carries a version Windows can tell apart" is in the changelog at `:11227`; read it for what the four-field encoding was built to do.
  </read_first>
  <behavior>
    - `compare("1.0.0-alpha.1", "0.125.1")` is `Newer` and the reverse is `Older`; `compare("1.0.0", "1.0.0-alpha.1")` is `Newer` and the reverse is `Older`; `compare("1.0.0-alpha.2", "1.0.0-alpha.1")` is `Newer`. One test in `version.rs`, named for the step this project is making.
    - `tests/installer.rs` gains a pure function that applies the script's rule to a version string (stage by suffix word, step capped at 999, `4000` for a plain version) and a test that holds it to the script's own comment table (`0.5.0 -> 0.5.0.4000`, `0.6.0-alpha.1 -> 0.6.0.1001`, `0.6.0-beta.2 -> 0.6.0.2002`, `0.6.0-rc.1 -> 0.6.0.3001`) and to the two versions this plan is about, and a second test that compares the two four-field results the way Windows does, field by field as numbers, and requires `1.0.0-alpha.1`'s above `0.125.1`'s. The function is a reading of the script, so the test also asserts the script still contains the four `stage=` arms and the `* 1000 + step` expression, and fails naming the arm if one is gone; the summary says this is a second copy of the arithmetic held to the first by that reading.
    - A third test reads `Cargo.toml` through `the_version` and requires a major of at least 1, so the scheme cannot drift back to `0.x` without a test saying so. This one is red before the bump and green after; it is the red half of the plan.
  </behavior>
  <action>
    Red first, on a branch. The `installer.rs` major test reddens against
    `0.125.1`; the arithmetic tests are green on arrival, because the script is
    right already, and the version pin is green on arrival too. Say so in the
    summary rather than passing three green tests off as red. Land all three
    in one red commit naming only the one that is red in its
    `Fails-until-green:` trailer, as the gate requires.

    Then the bump: `Cargo.toml` line 3 to `1.0.0-alpha.1` with the Edit tool,
    and `cargo build` to let cargo rewrite `Cargo.lock` (do not edit the lock
    by hand; cargo owns it). Confirm `target/release/wixen-mail.exe --version`
    or `cargo run -- --version` prints `1.0.0-alpha.1`, and quote the line.

    Guard records: one for the version pin, whose break inverts the prerelease
    arm of `compare` so that a prerelease sorts above its release, measured on
    the whole library; one for the arithmetic reading, whose break changes the
    script's alpha arm to `stage=2`, with `suite = "installer"`. Two records
    name `version.rs` and four name `installer.rs`; adding tests to both fires
    the count check, so run the `--remeasure` remedy it prints, detached, and
    read it before committing.

    No changelog entry in this task; task 3 writes the entry for the whole
    plan, because the number and its rule are one change to a reader.
  </action>
  <verify>
    <automated>cargo test --lib common::version:: && cargo test --test installer && grep -c '^version = "1.0.0-alpha.1"' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names `test_the_version_the_tree_carries_is_at_least_one_point_oh` (or the name chosen) in a `Fails-until-green:` trailer and nothing else, and the summary says the other two tests were green on arrival and why.
    - `grep -c '^version = "1.0.0-alpha.1"' Cargo.toml` is 1 and `grep -c 'version = "1.0.0-alpha.1"' Cargo.lock` is 1, rewritten by cargo.
    - `cargo test --lib common::version::` passes with 25 tests where there were 24, and the summary quotes the step test's five assertions.
    - `cargo test --test installer` passes and the summary quotes the four-field results for both versions as the test computed them.
    - Two guard records exist, each measured by hand with exactly the named test red, and every record the count check flagged was re-measured through `--remeasure` with the result quoted.
  </acceptance_criteria>
  <done>The tree says 1.0.0-alpha.1, a test names the step in both directions, and a test holds the installer's four-field version for it above today's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a Release level that publishes the version the tree already carries</name>
  <files>.github/workflows/release.yml, tests/installer.rs, guards/guards.toml</files>
  <read_first>
    - `.github/workflows/release.yml:1-30`, the levels and their comments; `:90`, the install; `:98-101`, the `cargo release` step; `:105-113`, the tag capture with the prerelease decision at `:110`.
    - `tests/installer.rs:613-700`, `the_release_triggers`, `nothing_starts_a_release_but_a_person` and the tests that read the workflow, for the reading shape and for what already holds the trigger.
    - `tests/installer.rs:379-440`, `block_scalar`, `step` and `the_steps`, the YAML readers this target already has.
    - Premise 4 above, for what cargo-release does with each level.
  </read_first>
  <behavior>
    - `release.yml` offers a seventh level, named `as-is`, described in its comment as publishing the version `Cargo.toml` already carries without bumping it, for the case this project now works in: the tree's version is the next build to go to testers and was moved in the commit that changed behaviour, so the workflow has nothing to bump.
    - The `cargo release` step passes the manifest's version instead of a level when `as-is` is chosen, read from `Cargo.toml` in the job with the same line `scripts/build-installer.sh:12` uses, so the tag is the number the tree carries; every other level is unchanged.
    - `tests/installer.rs` reads the workflow and requires: the `as-is` option is in the `release_level` choice list; the step that runs `cargo release` branches on it; and the branch hands cargo-release a version read from `Cargo.toml` rather than a level. A companion splices the option out of the real workflow text and requires the reading to name it.
    - The prerelease capture at `:109-111` still decides `prerelease` from the tag's suffix, so an `as-is` publish of `1.0.0-alpha.1` is a GitHub prerelease; a test holds that line unchanged.
  </behavior>
  <action>
    Red first, on a branch: the reading before the level exists, named in the
    trailer. Then the workflow change. Verify the `cargo release` semantics
    with a dry run on this machine before trusting the reference: install the
    tool the way CI does, `cargo install cargo-release --locked` (a developer
    tool, not a crate in `Cargo.toml`; say so in the summary, and say that
    `release.yml:90` already installs the same tool on every release), then
    from the branch run `cargo release 1.0.0-alpha.1 --dry-run --no-publish
    --allow-branch <branch>` and quote what it plans: it should plan no
    version change and a tag `v1.0.0-alpha.1` (the tag prefix is the one
    `the_names_the_release_writes` expects; check the dry run's tag against
    that test's glob). If the dry run refuses a version equal to the current
    one, the alternative shape is cargo-release's `tag` subcommand, `cargo
    release tag --dry-run`, which tags the version the manifest carries
    without a bump step, followed by a push of the tag; try it the same way
    and the summary records which shape worked and why; do not publish
    anything, do not push anything, and do not run `--execute`.

    A workflow change answers `all` on a branch, so this commit pays the whole
    gate; `tests/installer.rs` is in the run either way.

    One guard record whose break removes the `as-is` option from the choice
    list, `suite = "installer"`, measured by hand.
  </action>
  <verify>
    <automated>cargo test --test installer && grep -c 'as-is' .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new `installer` test in its trailer.
    - `grep -c 'as-is' .github/workflows/release.yml` is at least 2 (the option and the branch).
    - The summary quotes the `cargo release ... --dry-run` output for `1.0.0-alpha.1`, including the planned tag, and states that nothing was executed, pushed or published.
    - `cargo test --test installer` passes, and `test_a_release_still_happens_only_when_somebody_asks_for_one` still passes unchanged.
    - One guard record, measured, exactly the named test red.
  </acceptance_criteria>
  <done>Pratik can dispatch the Release workflow and have it publish 1.0.0-alpha.1 as a prerelease, and a test holds the level that makes that possible.</done>
</task>

<task type="auto">
  <name>Task 3: one rule for moving inside a prerelease, in the four places the old rule was</name>
  <files>CLAUDE.md, docs/changelog.md, docs/BETA_RELEASE.md, .claude/skills/cutting-a-release/SKILL.md</files>
  <read_first>
    - `CLAUDE.md`, the section "Versioning and releases", all of it, for the sentences that describe `0.x.y` development and the `0.6.0-alpha.1` example.
    - `docs/changelog.md:1-6`, the opening paragraph, and `:11040`, the entry "Version numbers stopped pretending to be an alpha programme", which is the record of why the counter shape is forbidden and stays as written.
    - `docs/BETA_RELEASE.md:20-70`, the dispatch instructions and the level table.
    - `.claude/skills/cutting-a-release/SKILL.md`, all of it.
    - `.planning/phases/09-what-the-first-day-of-testing-found/README.md`, the section "How versions move under the alpha", which is the rule these four pages state; the README is the planner's and these are the product's.
  </read_first>
  <action>
    **Documents only, which `CLAUDE.md` lists among the exceptions to
    test-first; the summary says so.** Invoke `writing-craft` for all four:
    plain language, no em dashes, none of the six banned words, and every
    corrected sentence keeps its old wording with the date, in each page's own
    manner, because a page that quietly changes its rule is one nobody can
    audit.

    The rule, stated the same way in each place and in the README's words:
    the tree's version is the next build to go to testers. It stays at
    `1.0.0-alpha.1` through this phase's fixes, because that build has not
    been cut and the number already names something newer than any build
    handed out. After a build is cut, the first behaviour change moves the
    prerelease counter (`-alpha.2`) in the commit that makes it, once; later
    behaviour changes before the next cut do not move it again, because the
    version is a name for the next build and not a count of commits. A hand
    build between cuts carries `+g<commit>` as it always has. `alpha` becomes
    `beta` and `rc` the same way when Pratik says the round has moved on, and
    `release` drops the suffix to `1.0.0` when it closes. After `1.0.0`, minor
    for features and patch for fixes as before. And the trap: `patch`
    dispatched on a prerelease is read by cargo-release as `release` and cuts
    a full release, so it is never dispatched while the version carries a
    suffix; the `as-is` level publishes the version the tree carries, and is
    the level the alpha uses.

    In `CLAUDE.md`, rewrite the paragraph "Development happens on plain
    `0.x.y`" and the one after it to say the scheme is now the 1.0.0 stages,
    keeping the old two paragraphs' sentences with "until 2026-09-16" in the
    file's own style; keep "'Alpha' as a state of the product belongs in the
    product, not in the number" as it stands, because it is still true, and
    add that a version bump inside the alpha follows the rule above. In the
    changelog's opening paragraph, the same, shorter. In `docs/BETA_RELEASE.md`,
    the level table gains `as-is` and the paragraph at `:42` is corrected by
    dating. In the skill, the table gains `as-is` as a GitHub prerelease when
    the version carries a suffix and a full release when it does not, the
    "Before dispatching" list gains "the version carries the suffix you mean
    to publish, and `patch` is not what you want on a prerelease", and the
    "Check `Cargo.toml` before dispatching" paragraph says why it matters more
    now.

    Write the `[Unreleased]` changelog entry for the whole plan: the number
    moved from `0.125.1` to `1.0.0-alpha.1` on Pratik's decision, why (the
    builds going to testers are the stages of 1.0.0), that nothing a person
    uses changed except the number they see, that the Release workflow can
    now publish the version as it stands, and under Known limitations that
    no `1.0.0-alpha.1` has been published and that the first alpha is a
    dispatch. `tests/house_style.rs` reads `CLAUDE.md` and the changelog for
    counts beside dates and for em dashes; `the_words_that_say_nothing` reads
    all four for the six words.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && grep -c 'as-is' docs/BETA_RELEASE.md .claude/skills/cutting-a-release/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md`'s versioning section states the rule above and keeps the old paragraphs' wording dated; `grep -n '0\.6\.0-alpha\.1' CLAUDE.md` still finds the old example inside a dated sentence rather than as the rule.
    - `docs/changelog.md:5` (the opening paragraph) names the 1.0.0 stages and the `+g<commit>` build identifier, and an `[Unreleased]` entry says the number moved, why, and what is not published.
    - `docs/BETA_RELEASE.md` and the skill each list `as-is` with what it publishes, and the skill says never to dispatch `patch` on a prerelease.
    - The four pages carry no em dash and none of the six words; `cargo test --test house_style` and `cargo test --test the_words_that_say_nothing` pass.
    - The summary quotes each page's corrected sentence beside its old one.
  </acceptance_criteria>
  <done>Four pages say one rule for how a version moves inside the alpha, and the old rule is still readable beside it with its date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the tree to the Release workflow | a dispatch turns the number in `Cargo.toml` into a public tag, an installer name and a GitHub release; a wrong number is public |
| cargo-release's levels to the tag | the level's meaning is cargo-release's and not this project's; `patch` on a prerelease is a full release |
| a document to a reader deciding what to dispatch | the skill and `CLAUDE.md` are what a person reads before pressing Run workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | a `patch` dispatch on `1.0.0-alpha.N` publishing `1.0.0` as a full release | high | mitigate | the skill, `CLAUDE.md` and `docs/BETA_RELEASE.md` say never to dispatch `patch` on a prerelease, and the `as-is` level gives the alpha a level of its own; guardrail 7 keeps dispatch a deliberate act |
| T-09-02 | Repudiation | the number moving without the rule moving with it | medium | mitigate | task 3 rewrites four pages in one commit and the changelog entry records the decision, the date and the reason |
| T-09-03 | Elevation of privilege | the dry run in task 2 executing a release | high | mitigate | `--dry-run` only, `--execute` forbidden by the action, nothing pushed; the summary must quote the dry run and state nothing ran |
| T-09-04 | Denial of service | a reading of the workflow that passes over a missing level | low | mitigate | the companion splices the option out and requires the reading to name it |
| T-09-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; `cargo install cargo-release --locked` is a developer tool CI already installs at `release.yml:90`, pinned by `--locked`, and the summary names the version installed |
</threat_model>

<verification>
- `cargo test --lib common::version::` and `cargo test --test installer` pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge.
- `cargo run -- --version` prints `1.0.0-alpha.1`, quoted in the summary.
- Three guard records measured by hand and through `--remeasure`; every record the count check flagged re-measured and read.
- Nothing dispatched, pushed or published; the summary says so in one sentence.
- After the merge, the executor closes the issue from the merge commit: `gh issue close 46 --comment "Moved to 1.0.0-alpha.1 in <merge commit>: the tree, two pins, an as-is Release level and the rule in CLAUDE.md, the changelog, BETA_RELEASE.md and the release skill. Nothing published; the first alpha is a dispatch."` Closing an issue is not a publish.
</verification>

<success_criteria>
- Roadmap criterion 1 closes structurally: the number, the two pins, the level, and four pages saying one rule.
- FOUND-01's four `[D]` lines are each closed by a named test or a named page in the summary; its `[S]` line about the dispatch is untouched.
- Every later plan in this phase writes its changelog entries under `[Unreleased]` with the tree at `1.0.0-alpha.1` and bumps nothing.
</success_criteria>

<output>
Create `.planning/phases/09-what-the-first-day-of-testing-found/09-01-SUMMARY.md` when done
</output>
