---
phase: 09-what-the-first-day-of-testing-found
plan: 08
type: execute
wave: 9
depends_on: ["09-07"]
files_modified:
  - src/application/import_tree.rs
  - src/application/importing_an_outlook_data_file.rs
  - src/application/mod.rs
  - src/application/importing_messages.rs
  - src/application/message_files.rs
  - src/presentation/wx_app.rs
  - src/service/outlook_data_file.rs
  - tests/wired.rs
  - guards/guards.toml
  - docs/changelog.md
  - docs/KEYBOARD_SHORTCUTS.md
autonomous: true
requirements: [FOUND-11]
estimate:
  tokens: 13000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "File, Import Mailbox lists *.pst, and a chosen Outlook data file is read through service::outlook_data_file on the import worker: each Mail item, which the reader already composes as one message through written_as_one_message, goes through each_message_in(bytes, ReadAs::OneMessage) and file_one_imported_message under Imported the way an archive's does; its appointments, contacts, tasks and notes land on this computer through the cache's existing writers; the closing sentence counts each kind and what stayed behind and says the reader has never met a real file (FOUND-11, #53 point 1)."
    - "File, Save As, on the main frame, writes the message under the cursor in the list as .eml through written_as_one_message, through the ordinary save dialog, and refuses with a reason when nothing is selected; an attachment is saved by the reader's existing Save Attachment command, and the shortcuts page says which command does which (FOUND-11, #53 point 2)."
  artifacts:
    - "src/application/import_tree.rs: what_was_chosen answers a third thing, an Outlook data file, from the !BDN signature."
    - "src/application/importing_an_outlook_data_file.rs: the walk over folders and items, one function per kind of item, the counts, and the closing sentence; tests at zero records."
    - "src/presentation/wx_app.rs: the picker's filter; the worker's dispatch on what_was_chosen; the Save As handler."
    - "src/service/outlook_data_file.rs: reached by a production caller for the first time; unchanged otherwise."
    - "tests/wired.rs: the Save As item held to a handler that writes, and the import worker to a dispatch that names all three answers."
  key_links:
    - "outlook_data_file yields ItemInTheDataFile::{Mail(Vec<u8>), Appointment, Contact, Task, Note} with the cache's own entry types (:2176), and composes each Mail item's bytes through message_files::written_as_one_message (:1701): the bytes already arrive as one message, so each_message_in(bytes, ReadAs::OneMessage) is the dispatch and file_one_imported_message the writer, as for a single saved message."
    - "what_was_chosen(a_folder, opens_with) at import_tree.rs:513 is the one seam the worker dispatches on: a third answer is one arm there and one branch in the worker."
    - "ID_SAVE_AS is on the main frame's File menu; the attachment list is in the reader frame, whose own menu already has ID_SAVE_ATTACHMENT (wx_reader.rs:35, :794, save_attachment_now at :878). When the reader has focus the main menu is not active, so a Save As branch for an attachment on the main frame is a branch nothing reaches."
    - "Ledger 97 describes an importer path that does not exist: once it exists, 97's List-Unsubscribe deviation is real and stays open, and the changelog's 'never run against a real file' sentence stays true and is now about something reachable."
---

<objective>
Reach the Outlook data file reader from the picker, and make Save As save.

Purpose: both are the shapes guardrails 1 and 3 forbid, built and reached by
nothing, and a stub presented as a command. The decision here is to wire the
reader rather than retire it, for the reasons in the premises: it already
yields the cache's own types and composes each message through the writer
the archive path reads, the one seam the worker dispatches on exists, and
the product's stated purpose is to replace Outlook, whose data file is what
somebody leaving it holds.

Output: `*.pst` in the picker and read on the worker; Save As writing the
list's message as `.eml`. Closes #53's points 1 and 2; 09-10 takes points 3
and 7 and corrects the documents; points 4 to 6 are later work and the final
comment on #53 is 09-10's.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-what-the-first-day-of-testing-found/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-16 against `main` at `524ff24f`. Command first, then what it
said.

**1. The reader is unreached, and its shape is an importer's.**

```
wc -l src/service/outlook_data_file.rs                 -> 3608
grep -c '#\[test\]' src/service/outlook_data_file.rs   -> 38
grep -rn 'outlook_data_file' src --include='*.rs' | grep -v '^src/service/outlook_data_file.rs'   -> src/service/mod.rs:28: pub mod outlook_data_file;
sed -n 2176,2183p src/service/outlook_data_file.rs      -> pub enum ItemInTheDataFile { Mail(Vec<u8>), Appointment(Box<CalendarEventEntry>), Contact(Box<ContactEntry>), Task(Box<TaskEntry>), Note(Box<NoteEntry>) }
grep -n 'pub fn opened\|pub fn what_it_holds\|pub fn each_item_in\|pub fn what_stayed_behind\|const HOW_ONE_BEGINS' src/service/outlook_data_file.rs
  -> 345 opened, 1897 what_it_holds, 1951 each_item_in, 1936 what_stayed_behind, 104 HOW_ONE_BEGINS = b"!BDN"
```

Its module header says it is `mailbox_archive`'s sibling, built to be handed
to an importer: folders are offered as names, items read one at a time, and
what stayed behind is counted. The cache's writers for the four PIM kinds
exist: `save_calendar_event` (`calendar.rs:157`), `save_contact`
(`contacts.rs:90`), `save_task` (`tasks.rs:144`), `save_note` (`notes.rs:326`),
and `opening::WHERE_IMPORTED_THINGS_GO` (`opening.rs:158`) already names
`new_item::LOCAL_ACCOUNT_ID` as where an imported calendar's events go.

**2. The decision: wire it. Both costs.** Wiring: a third arm in
`what_was_chosen` (`import_tree.rs:513`) keyed on `!BDN`, one branch in
`import_a_mailbox`'s worker (`wx_app.rs:12843-12900`, `spawn_blocking` at
`:12894`), a module that walks `what_it_holds()` and `each_item_in()` and
files each kind, and a sentence; the picker filter gains `*.pst`. About a
day, with the standing condition that no real file exists to run it against,
which the changelog at `:4667` already says. Retiring: delete 3,608 lines and
38 tests, the `outlook-pst` dependency (`Cargo.toml:253`, with its paragraph
on why Microsoft's own crate), correct the same four documents the other way,
and close ledger 97 as moot; about half a day, and the product loses the one
thing somebody leaving Outlook arrives with. Wiring is chosen because the
reader already speaks the cache's types, the seam exists, the risk the crate
paragraph names (a parser over a stranger's file) is the same risk the
archive reader carries today, and "what does it replace" in `CLAUDE.md`
answers Outlook. The executor may overrule with a reason in the summary; the
documents in 09-10 then go the other way.

**3. Each Mail item already arrives as one message, and the first draft of
this premise misread its own command.** Run as written, the grep answers two
lines, not one:

```
grep -rn 'written_as_one_message' src --include='*.rs' | grep -v 'fn written_as_one_message\|message_files.rs'
  -> src/application/export_tree.rs:814:        let written = message_files::written_as_one_message(&rebuilt, &[]);
     src/service/outlook_data_file.rs:1701:    crate::application::message_files::written_as_one_message(&a_message_from(item, went_to), &[])
```

The plan check caught the second line on 2026-09-16. It means the reader
composes each `Mail` item into one message's bytes through the same writer
Save As and the exporter use, so the bytes an importer receives are a saved
message and not a folder of them: the dispatch for a `Mail` item is
`each_message_in(&bytes, ReadAs::OneMessage)` (`importing_messages.rs:291-297`,
which reads one message through `read_one_message_as_it_arrived`) followed
by `file_one_imported_message`, exactly the path a single `.eml` takes. No
new reader of message bytes is written.

**4. Save As is a stub, and the attachment case belongs to another frame.**

```
sed -n 5027,5029p src/presentation/wx_app.rs   -> _ if id == ID_SAVE_AS => { send_status(&ui_tx, &runtime, "Save As: no message selected") }
grep -n 'ID_SAVE_AS' src/presentation/wx_app.rs | head -3   -> 155 (id), 5027 (handler), 6026 ("Save &As...", "Save to a file")
grep -n 'Save As' docs/KEYBOARD_SHORTCUTS.md   -> 485: | Save As | (none) | Save message or attachment to file |
grep -n 'ID_SAVE_ATTACHMENT\|fn save_attachment_now' src/presentation/wx_reader.rs   -> 35 (the id), 295 (its item), 794 (its handler), 878 save_attachment_now
```

`ID_SAVE_AS` is on the main frame's File menu, and the attachment list is
in the reader frame, which has its own menu and its own Save Attachment
command reached by `save_attachment_now`. When the reader has focus the main
frame's menu is not the active one, so a Save As branch on the main frame
for "the selected attachment" would be a branch nothing reaches, which is
the shape this plan exists to remove. So Save As saves the message under the
cursor in the list, and the attachment case is the reader's existing
command; the shortcuts page row says which does which. (The first draft of
this plan wrote the unreachable branch; the plan check caught it.)

**5. Records and the gate.** `outlook_data_file.rs` is named by no record
and `--lib service::outlook_data_file::` reaches its 38 tests. `import_tree.rs`,
`importing_messages.rs` and `message_files.rs`: the executor takes the record
counts with the TOML reader before adding tests. The new module is at zero
records and gets one. `wx_app.rs` 50 records; no test added there.
`tests/wired.rs` 14 records, 72 tests.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: an Outlook data file chosen in the picker is read, and its five kinds of item land here</name>
  <files>src/application/import_tree.rs, src/application/importing_an_outlook_data_file.rs, src/application/mod.rs, src/presentation/wx_app.rs, src/service/outlook_data_file.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/service/outlook_data_file.rs:1-140`, the header and `HowMuchToAllow`, `:340-360`, `opened`, `:1690-1710`, where a `Mail` item is composed through `written_as_one_message`, `:1870-1960`, `OutlookDataFile` and `each_item_in`, and `:2129-2183`, `ItemsInAFolder` and `ItemInTheDataFile`; and how its 38 tests build a file to read, for the fixture the new module's tests reuse.
    - `src/application/import_tree.rs:496-522`, `what_was_chosen`, and `:72-160`, where imported folders go.
    - `src/application/importing_messages.rs:1-60`, the header on the one question that can lose mail, `:144-150`, `ReadAs`, `:291-400`, `each_message_in` and `file_one_imported_message`, and `:389-515`, the counts and the closing sentence.
    - `src/presentation/wx_app.rs:12843-12960`, `import_a_mailbox` and its worker.
    - `src/application/opening.rs:150-220`, `WHERE_IMPORTED_THINGS_GO` and how an imported calendar is filed under the local account.
    - `Cargo.toml:240-253`, the crate paragraph, which stays.
  </read_first>
  <behavior>
    - `what_was_chosen(false, b"!BDN...")` answers a new `WhatWasChosen::AnOutlookDataFile`; the two existing answers are unchanged; a test for each.
    - The new module takes an opened `OutlookDataFile`, the cache and the account the import is for, walks every folder, and for each item: `Mail(bytes)` goes through `each_message_in(&bytes, ReadAs::OneMessage)` and then `file_one_imported_message` under Imported with the folder's name, exactly as one saved `.eml` does, so the sync's marker rule holds (the header's "one question that can lose somebody's mail"); `Appointment`, `Contact`, `Task` and `Note` go through the cache's four `save_*` writers under `new_item::LOCAL_ACCOUNT_ID`, with a task list and a note folder named for the data file's folder where those writers need one. It returns counts per kind, what stayed behind from `what_stayed_behind()`, and whether the file had a password on it. Tests at zero records build a data file the way `outlook_data_file.rs`'s own tests do, with one item of each kind, import it into a `tempfile` cache, and read each back, the mail row carrying the imported marker.
    - A closing sentence, on `what_the_mail_import_did`'s pattern, counts each kind in plain words, says what stayed behind, says the file had a password Outlook would have asked for and this program did not need (when true), and ends with the sentence that no real Outlook file has ever been read by this program; a test holds the sentence for each case.
    - The picker's filter names `*.pst` beside the three; the worker dispatches on the third answer to the new module and says the closing sentence the way the archive path says its own.
  </behavior>
  <action>
    Red first: the `what_was_chosen` arm, the walk's tests, the sentence's
    tests, named with module paths. Then the module, the arm, the picker and
    the worker branch. Do not change `outlook_data_file.rs` except to make
    something `pub` the module needs, and say what; its 38 tests still pass.

    Two guard records on the new module: the mail marker (break: mail filed
    without the imported marker, which the sync would then delete) and the
    counts (break: a kind not counted), measured on the whole library. One
    on `import_tree.rs` for the third arm.

    Changelog entry for #53 point 1, `[Unreleased]`: File, Import Mailbox
    takes an Outlook data file; what comes across and what stays behind; that
    the reader had been in the tree and reachable by nothing since its entry
    at `:4667` (dated sentence added there too); and, unchanged and repeated,
    that no real Outlook file has ever been read by this program.
  </action>
  <verify>
    <automated>cargo test --lib application::importing_an_outlook_data_file:: && cargo test --lib application::import_tree:: && cargo test --lib service::outlook_data_file::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new tests with module paths.
    - `grep -rn 'outlook_data_file::' src --include='*.rs' | grep -v '^src/service/outlook_data_file.rs\|^src/application/importing_an_outlook_data_file.rs'` finds the worker's call, quoted.
    - `grep -c '\*.pst' src/presentation/wx_app.rs` is at least 1.
    - The new module's `Mail` arm names `ReadAs::OneMessage`, quoted, and no new reader of message bytes exists.
    - The three `<verify>` commands pass; the summary quotes the closing sentence for a file with one item of each kind.
    - Three guard records measured; flagged records re-measured and read.
    - The changelog entry is in, with the dated sentence on the older entry.
  </acceptance_criteria>
  <done>Somebody leaving Outlook can point File, Import Mailbox at their data file and have its mail, appointments, contacts, tasks and notes here, told what stayed behind and that nobody has yet done this with a real file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save As writes the list's message as .eml, and the attachment case is the reader's own command</name>
  <files>src/presentation/wx_app.rs, src/application/importing_messages.rs, src/application/message_files.rs, tests/wired.rs, guards/guards.toml, docs/changelog.md, docs/KEYBOARD_SHORTCUTS.md</files>
  <read_first>
    - `src/presentation/wx_app.rs:5020-5035`, the stub, and `:6020-6030`, the item.
    - `src/presentation/wx_reader.rs:30-40`, `:290-300`, `:790-810` and `:878-900`, the reader's Save Attachment id, item, handler and `save_attachment_now`, which this task leaves as they are and names on the shortcuts page.
    - `src/application/message_files.rs:220-260`, `written_as_one_message`, and `:1700-1760`, `what_the_file_holds`; `src/application/export_tree.rs:800-830`, how the exporter rebuilds a `ParsedMessage` from stored bytes and files.
    - `tests/wired.rs`, the readings that hold a menu item to a handler that does something (`grep -n 'send_status' tests/wired.rs | head`), for the shape that catches a stub.
  </read_first>
  <behavior>
    - A pure function in `importing_messages` (or beside the writer) decides what Save As saves, from the message under the cursor in the list: its file name as `.eml` from its subject with path separators and other unsafe characters replaced, or a refusal sentence saying to select a message first when nothing is selected. Tests for a plain subject, a subject holding a slash, an empty subject, and nothing selected.
    - The handler asks it, opens the ordinary save dialog with the suggested name, writes `written_as_one_message` with `FileOnTheMessage::all_on` from the stored bytes the way the exporter rebuilds a message, and says what it wrote and where, once; a refusal is said as a refusal. There is no attachment branch on the main frame: the reader's Save Attachment command is that case, and the handler's comment says so with the reason (the main menu is not active while the reader has focus).
    - A `wired.rs` reading holds `ID_SAVE_AS`'s handler to calling the decision and a writer, so a stub that only sends a status line is a failing test; a companion splices the stub back.
    - `docs/KEYBOARD_SHORTCUTS.md:485` says Save As saves the selected message as `.eml` and has no key, and that an attachment is saved from the reader's own Save Attachment command, named as the page names it.
  </behavior>
  <action>
    Red first: the decision's tests and the `wired.rs` reading, named (module
    paths for the lib tests, bare for `wired`). Then the handler and the
    page. `wired.rs` is named by 14 records; the count check will name them,
    and the remedy is run and read.

    Guard records: one for the decision (break: the slash left in the
    suggested name), one for the `wired` reading with `suite = "wired"`.

    Changelog entry for #53 point 2, `[Unreleased]`: Save As saves the
    selected message as `.eml`; attachments are saved from the reader as
    before; and that the command had sent one status line whatever was
    selected since it was added.
  </action>
  <verify>
    <automated>cargo test --lib application::importing_messages:: && cargo test --lib application::message_files:: && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new tests.
    - `grep -n '"Save As: no message selected"' src/presentation/wx_app.rs` finds nothing; the handler's body names the decision and a writer, quoted, and holds no attachment branch.
    - The three `<verify>` commands pass; the summary quotes the suggested file name for a message whose subject holds a slash.
    - Two guard records measured; flagged `wired.rs` records re-measured and read.
    - The shortcuts page row and the changelog entry are in.
  </acceptance_criteria>
  <done>Save As saves the list's message, the reader saves attachments as it already did, the page says which is which, and a reading refuses a stub behind the item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's data file to the parser | a `.pst` is a file somebody else wrote; a fault in the reader is reachable by whoever made the file, which `Cargo.toml`'s crate paragraph already names |
| imported mail to the sync | a row filed without the imported marker is deleted by the next sync as mail the server no longer has |
| Save As to the disk | a message's bytes leave the cache as a file the person chose; the name suggested must not carry a path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-26 | Denial of service | a data file larger than memory, or one item larger than memory | high | mitigate | `HowMuchToAllow` and the reader's own bounding (its header, "Memory, which is the shape of this module") are reused unchanged; the worker runs off the interface thread as the archive import does |
| T-09-27 | Tampering | imported mail lost to the next sync | high | mitigate | every `Mail` item goes through `file_one_imported_message`, the one writer that sets the marker, and a guard record breaks exactly that |
| T-09-28 | Tampering | a suggested file name that escapes the chosen folder | medium | mitigate | the decision function replaces path separators and a test holds a subject with a slash |
| T-09-29 | Repudiation | a command whose branch nothing reaches | medium | mitigate | the attachment case is the reader's existing command and the `wired` reading holds the main frame's handler to a writer |
| T-09-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `outlook-pst 1.2.0` is already in `Cargo.toml` with its legitimacy paragraph |
</threat_model>

<verification>
- The two `<verify>` commands pass; `scripts/check.sh` through the hook on every commit, never piped; `scripts/check.sh all` once before the merge.
- Five guard records measured; flagged records re-measured and read.
- Changelog entries under `[Unreleased]`, no version bump.
- After the merge: `gh issue comment 53 --body "Points 1 and 2 fixed in <merge commit>: *.pst is in the picker and read on the worker (each message through the same path a saved .eml takes, under Imported; the four item kinds on this computer; the closing sentence counts each and says no real file has been read); Save As saves the selected message as .eml, and attachments are saved from the reader's own command as before. Point 3, the folder picker, and point 7, the guide, are 09-10's, which also corrects the four documents; 4 to 6 are later work. This stays open."`
</verification>

<success_criteria>
- Roadmap criterion 8's first two clauses close structurally; FOUND-11's first two `[D]` lines are named by tests in the summary; its `[S]` line about a real file stays.
</success_criteria>

<output>
Create `.planning/phases/09-what-the-first-day-of-testing-found/09-08-SUMMARY.md` when done
</output>
