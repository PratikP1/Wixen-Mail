---
phase: 09-what-the-first-day-of-testing-found
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/common/paths.rs
  - src/common/logging.rs
  - src/main.rs
  - src/data/config.rs
  - src/presentation/wx_app.rs
  - tests/a_setting_survives_a_restart.rs
  - tests/the_log_outlives_the_second_minute.rs
  - guards/guards.toml
  - docs/changelog.md
  - docs/privacy.md
autonomous: true
requirements: [FOUND-13]
estimate:
  tokens: 19500
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The program says, in its log's first lines and on the settings status line, which folder it is reading and writing, so two launches that write two profiles can be told apart from the log rather than from a forensic read (FOUND-13, #66)."
    - "A setting changed on the Settings screen and confirmed is on disk, in the profile the program said it writes, with the running build's version stamp, and a test proves it through the real load and save against a copy of the July file's shape (FOUND-13, #66 symptom a)."
    - "The settings status line says the path written or the reason it was not, announced at a priority a person hears; nothing says 'saved' over nothing (FOUND-13)."
    - "The release binary run against a profile for longer than two minutes writes a line in its third minute, proved by a test on the launcher 08-03 built, and the log guard is held where main leaves it for the program's life (FOUND-13, #66 symptom b)."
  artifacts:
    - "src/common/paths.rs: the resolved data folder, its origin (the environment variable, or the platform folder) and whether the process runs under a package identity, in one value the log and the status line word."
    - "src/data/config.rs: save re-stamps version with the running build; a test that a July-shaped file loads, saves with the changed field and the new stamp, and holds every field."
    - "src/presentation/wx_app.rs: the settings status line naming the path written, at a priority a person hears."
    - "src/main.rs and src/common/logging.rs: the guard's lifetime made explicit and the first log lines naming the folder."
    - "tests/a_setting_survives_a_restart.rs and tests/the_log_outlives_the_second_minute.rs: two ignored release-binary harnesses on the shape of the_numbers_the_targets_ask_for.rs, each with a not-ignored test on the line it parses."
  key_links:
    - "AppPaths::resolve_with(env WIXEN_MAIL_DATA, dirs::data_local_dir()) at paths.rs:55: the one place the profile's location is decided, and the place that must say what it decided."
    - "handle_settings at wx_app.rs:16499: ConfigManager::new() then load() (a failed load is a warning and the dialog opens on defaults), then save() on OK with send_status at low priority; the status line is what the tester did not hear."
    - "main.rs:103: _log_guard is a local of main and the event loop runs inside main, so by reading the guard should live; the log stopping after two minutes says something the reading does not, and the harness is what finds it."
    - "A process started under a packaged app's identity sees AppData\\Local through that package's LocalCache: every launch from this harness does, so every harness launch sets WIXEN_MAIL_DATA to a temp folder, and a reading of the tester's plain profile is done with cmd, PowerShell or bash, never with Python from here."
---

<objective>
Make the program say where it writes, make a setting survive a restart and
prove it, and keep the log alive past its second minute.

Purpose: nothing the tester reports about a setting can be trusted until
one survives a restart, and #22 cannot be read until the log holds
something, which is why this plan executes before 09-01. What the
premises found is that the tester's machine holds two profiles at one path,
chosen by the identity of the process that launched the program, and that
the July file loads and saves correctly in-process; so the first task is to
make the program say which profile it is in, the second to hold a save to
disk with the right stamp and a status line a person hears, and the third
to hold the log.

Output: the folder named in the log and on the status line; the version
re-stamped on save with a test through a July-shaped file; two
release-binary harnesses; the guard's lifetime held. Advances #66 to the
point where the tester's next report answers which profile it came from.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-what-the-first-day-of-testing-found/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-16 against `main` at `358c7026`. Command first, then what it
said. This plan was added after the first ten, on #66, filed 2026-09-16.

**1. One path, two profiles, chosen by the identity of the process that
reads it.** The issue and its first readers took `%LOCALAPPDATA%\wixen-mail`
for one folder. It is two on this machine:

```
grep -o '"version": *"[^"]*"\|"language": *"[^"]*"' "C:/Users/prati/AppData/Local/wixen-mail/config/app_config.json"
  -> "version": "0.1.0-alpha.22"   "language": "en"        (bash; cmd /c type and PowerShell Get-Content answer the same; 886 bytes, 28 keys, dated 2026-07-30 02:07)
python: json.load(open(r"C:\Users\prati\AppData\Local\wixen-mail\config\app_config.json"))
  -> version 0.7.7, language en-US, 1921 bytes, 52 keys, dated 2026-09-15 18:39
python: GetFinalPathNameByHandleW on that handle  -> \\?\C:\Users\prati\AppData\Local\wixen-mail\config\app_config.json   (the same path, a different file)
find /c/Users/prati/AppData/Local/Packages -path '*wixen-mail*' -name app_config.json
  -> /c/Users/prati/AppData/Local/Packages/Claude_pzs8sxrjxfjjc/LocalCache/Local/wixen-mail/config/app_config.json
powershell: Get-AppxPackage -Name Claude  -> PackageFamilyName Claude_pzs8sxrjxfjjc
```

Python here runs under the Claude desktop application's package identity,
and Windows gives a packaged process its own view of `AppData\Local` inside
the package's `LocalCache`. So a program launched from a shell under that
identity reads and writes the virtualised profile, and a program launched
from the Start Menu reads and writes the plain one. What each holds:

| | plain `%LOCALAPPDATA%\wixen-mail` (cmd, PowerShell, bash) | virtualised, `Packages\Claude_pzs8sxrjxfjjc\LocalCache\Local\wixen-mail` (Python here) |
|---|---|---|
| `config/app_config.json` | 886 bytes, 28 keys, `0.1.0-alpha.22`, `"language": "en"`, 2026-07-30 | 1,921 bytes, 52 keys, `0.7.7` stamp, `"language": "en-US"`, 2026-09-15 18:39 |
| `cache/message_cache.db` | 4,096 bytes 2026-07-30, WAL 2.3 MB 2026-09-14 13:30 | 20,099,072 bytes 2026-09-15 18:56, WAL 6.3 MB 19:43; 12,872 messages; one account, `pratikp1@gmail.com`, Gmail; the `password` column empty |
| `logs/` | 2026-09-14 (24 lines, 0.123.1), 2026-09-15 (0 bytes, 10:40) | 2026-07-30, 2026-09-14 physically; the merged view lists 07-31 to 08-27 and 09-15 (0 bytes) |

So the tester's 2026-09-15 session, the one with the Gmail account and
12,872 messages, ran under the package identity and wrote the virtualised
profile, and its Settings OK at 18:39 wrote a 52-key file with English
(United States): the save worked. A Start Menu launch reads the plain
profile, whose stored `"language": "en"` lands on Caribbean, which is #21's
cause exactly. Both profiles are his. Which launcher he uses on which day is
a question for him and is the one question this plan's summary asks; the
code change is that the program says which folder it is in, so the next
report answers it itself.

**2. The July file loads, validates and saves; the version stamp is copied
through, not re-stamped.** A scratch test in `config.rs` (added with the
Edit tool, run once, removed with `git checkout`) fed the plain file's
exact text to `serde_json::from_str::<AppConfig>` and to a `ConfigManager`
in a temp folder:

```
cargo test --lib data::config::tests::scratch_planning_does_the_july_file_load -- --nocapture
  -> JULY_LOAD=ok version=0.1.0-alpha.22 language=en reading=true validate=Ok(())
     MANAGER_LOAD=Ok(())
     MANAGER_SAVE=Ok(())      (after setting language to en-US)
     WRITTEN_VERSION="0.1.0-alpha.22"  WRITTEN_LANGUAGE="en-US"  FIELDS=56
```

The five fields without a serde default (`version`, `download_folder`,
`theme`, `font_size`, `log_level`) are all in the July file; `Allowed`
defaults its missing `reading`; unknown keys (`check_updates`,
`enable_notifications`, the three `calendar_*`) are ignored (no
`deny_unknown_fields` in `config.rs` or `allowed.rs`). `validate()` at
`:857` refuses only a font size outside 8 to 72 and a log level outside the
five words; 12 and `"info"` pass. And `version` is set from
`CARGO_PKG_VERSION` in `AppConfig::default()` (`:651`) and never on save,
so a file's stamp is the build that created it, not the one that last wrote
it. The issue's "the stored version proves no build after alpha 22 has ever
written this file" is therefore not a proof; what shows the plain file
untouched since July is its 28 keys against the 56 a save writes. The
virtualised file's `0.7.7` stamp is the same effect: created by 0.7.7,
saved since by later builds.

**3. The save path, followed.** `handle_settings` (`wx_app.rs:16499`):
`ConfigManager::new()` creates the folder or returns with "Cannot open
settings"; `mgr.load()` on error is a `tracing::warn!` and the dialog opens
on defaults; `show_settings_dialog` returns `Updated` only when `show_modal`
answered `ID_OK`, which the OK button's `end_modal(ID_OK)` at
`wx_settings.rs:348` does; `*mgr.app_config_mut() = *new_config` then
`mgr.save()` (`config.rs:1024`) which is `validate()` then
`fs::write(app_config_path)`; on error `send_status` "Settings save error:
..." and on success `send_status` "Settings saved". Both through the status
topic at low priority, which the queue coalesces (`wx_app.rs:17214`'s
comment), so a failure said while a sync was talking would not be heard.
`ConfigManager::new()` resolves the folder through `AppPaths::resolve()`
(`paths.rs:54-55`): `WIXEN_MAIL_DATA` if set, else `dirs::data_local_dir()`,
which is the path the package identity redirects.

**4. The log.** `main.rs:103` holds `_log_guard` as a local for the whole of
`main`, and the event loop runs inside `main`, so by reading the guard lives
until exit. `init_logging` (`logging.rs:84-97`) builds a daily rolling
appender and `tracing_appender::non_blocking`, whose default is lossy: when
its channel is full, lines are dropped. The plain profile's 2026-09-14 log
holds startup, "First timer tick", then the due look at 17:31Z and 17:32Z,
then nothing, while the WAL beside it is dated 13:30 local, the same
minutes, so that run may simply have ended; the 2026-09-15 log is 0 bytes in
both profiles and in `%TEMP%\wixen-mail\logs` (the fallback when the data
folder cannot be resolved), while the virtualised database was written from
10:40 to 19:43. So a session that wrote 20 MB of mail wrote no log line at
all, not even "Starting Wixen Mail", which is earlier than any guard drop:
either the appender never opened that file for writing, or the lines were
lost before the first flush. The harness in task 3 is what finds which.

**5. The harness that starts the release binary.**
`tests/the_numbers_the_targets_ask_for.rs:385-390`: `Command::new(env!("CARGO_BIN_EXE_wixen-mail"))`
with `.env("WIXEN_MAIL_DATA", profile)` and `--read-only`, polled for the
usable line, stopped with `taskkill`. `nvda-tests/helpers/launch-app.js:59`
does the same with `WIXEN_MAIL_DATA`. Both new harnesses reuse that shape.
Every process this harness starts inherits the package identity, so
`WIXEN_MAIL_DATA` is set on every launch and no harness ever touches either
of the tester's profiles.

**6. Records and the gate.** `config.rs` 10 records, 66 tests; `paths.rs`
and `logging.rs`: the executor takes their counts with the TOML reader;
`main.rs` maps to a `--lib` filter matching nothing and is coupled to
`wired` by one record; `wx_app.rs` 50 records, no test added. Two new
targets at zero records, one record each.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the program says which folder it is in, in the log and on the settings status line</name>
  <files>src/common/paths.rs, src/main.rs, src/presentation/wx_app.rs, guards/guards.toml, docs/privacy.md</files>
  <read_first>
    - `src/common/paths.rs:1-120`, `AppPaths`, `resolve`, `resolve_with` and `DATA_DIR_ENV`, and its tests.
    - `src/main.rs:96-125`, where logging starts and the first lines are written, and `:344-386`, `prepare_data_folder` and `report_migration`, for what is already said about the folder.
    - `src/presentation/wx_app.rs:16499-16585`, `handle_settings`, and `:17214-17240`, the status handler and its priority.
    - Premise 1's table, which is what the words must let a reader tell apart.
    - `docs/privacy.md`, the section on where data is kept, for the sentence about the package identity.
  </read_first>
  <behavior>
    - `AppPaths` answers, in one value, the data folder it resolved, whether it came from `WIXEN_MAIL_DATA` or the platform folder, and whether the process runs under a package identity (Windows answers `GetCurrentPackageFamilyName`; `APPX_E_NO_PACKAGE_IDENTITY` means no); a pure function words it in one sentence, and a test holds the sentence for each of the four cases (environment or platform, packaged or not), with the packaged case's wording saying that a packaged process sees its own copy of the folder.
    - The log's first lines after "Starting Wixen Mail" name the folder in that sentence, so a log from either profile says which profile it is.
    - The settings status line on success says "Settings saved to <path>" through the same sentence, and on failure "Settings not saved: <reason>, at <path>", both announced at a priority the queue does not coalesce away (the priority the save-failure of a message uses), and a test in `wx_app.rs`'s existing test module is not added: the wording is a pure function in `paths.rs` or `config.rs` with its test there, and the status call is held by a `wired` reading that finds the call in `handle_settings`.
    - `docs/privacy.md`'s section on where data lives gains one paragraph: a copy started from inside another application that Windows packages sees its own copy of the data folder, which is where its mail and settings go, and the log's first lines say which folder a copy is using.
  </behavior>
  <action>
    Red first: the wording function's tests and the `wired` reading, named
    (module paths and bare). Then the value, the sentence, the log lines,
    the status line and the page. The Windows call goes behind
    `#[cfg(target_os = "windows")]` with the other arm answering "not
    packaged", per the platform rule in `CLAUDE.md`.

    Then reproduce premise 1 with the change in place, without touching
    either of the tester's profiles: start the release binary from this
    harness with `WIXEN_MAIL_DATA` unset and `--read-only`, read its first
    log lines, and quote the folder it names (it should be the virtualised
    one, because the harness is packaged); then the same with
    `WIXEN_MAIL_DATA` set to a temp folder, and quote that. Stop it with
    `taskkill` as the 08-03 harness does. The summary then asks Pratik one
    question, in plain words: whether he starts Wixen Mail from the Start
    Menu, from a shortcut, or from a window inside another program, and on
    which days, because the answer says which of his two profiles each
    session used. The plan does not guess the answer.

    Two guard records: the wording (break: the packaged case worded as the
    plain one) and the `wired` reading (break: the status call removed).
  </action>
  <verify>
    <automated>cargo test --lib common::paths:: && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new tests.
    - The two `<verify>` commands pass; the summary quotes the four sentences the wording function produces.
    - The summary quotes the first log lines of a release-binary start from this harness with `WIXEN_MAIL_DATA` unset and with it set, and the folder each names.
    - The summary carries the one question for Pratik and no assumed answer.
    - Two guard records measured; flagged records re-measured and read.
    - `docs/privacy.md` carries the paragraph and `cargo test --test house_style` passes.
  </acceptance_criteria>
  <done>A log or a status line says which of two folders a copy is writing, and the tester's next report can say which profile it came from.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a changed setting is on disk with the running build's stamp after OK, proved through the July file's shape</name>
  <files>src/data/config.rs, tests/a_setting_survives_a_restart.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/data/config.rs:928-1040`, `ConfigManager`, `load`, `save_app_config` and `save`, and `:640-700`, `AppConfig::default` with `version` at `:651`.
    - `tests/the_numbers_the_targets_ask_for.rs:1-120` and `:380-440`, the header and the launcher, for the second harness's shape.
    - `src/presentation/wx_settings.rs:154-180`, `show_settings_dialog` and `read_settings`, for what OK writes; and `tests/checkbox_labels.rs:36-120`, for building the dialog in a test without showing it.
    - Premise 2's scratch output, which is the shape the unit test takes.
  </read_first>
  <behavior>
    - `save_app_config` writes `version` as the running build's `CARGO_PKG_VERSION`, so a file's stamp is the build that last wrote it; a unit test in `config.rs` writes the July file's exact text (premise 2) into a temp folder, loads it through `ConfigManager`, changes `language` to `en-US`, saves, and reads back: `version` equals `env!("CARGO_PKG_VERSION")`, `language` is `en-US`, every field `AppConfig::default()` would write is present, and the July file's unknown keys are gone; a second test loads what was written and requires the same values, which is the restart.
    - `tests/a_setting_survives_a_restart.rs`, an ignored harness on the 08-03 launcher: copies the July text into a temp profile's `config/app_config.json`, starts the release binary with `WIXEN_MAIL_DATA` there and `--read-only`, waits for the usable line or the first log line, stops it, and requires the file unchanged (a start alone must not rewrite settings); then, because no harness here can press OK on the real dialog, the in-process half is the unit test above, and the harness's header says so. A not-ignored test holds the harness's parsing of the line it waits for.
    - The changelog entry says a setting confirmed on the Settings screen is written with the build's version, that a file stamped with an older version now means an older build last wrote it, and what the log's first lines say about the folder.
  </behavior>
  <action>
    Red first: the re-stamp test (red, because save copies the stamp
    today), the read-back test, the harness's parsing test, named with
    module paths and bare. Then the re-stamp in `save_app_config` and the
    harness. `config.rs` is named by 10 records; run the remedy the count
    check prints and read it. If the summary of task 1 has Pratik's answer
    by then, quote it here and say which profile the fix is now known to
    have reached; if not, say it is still open.

    Two guard records: the re-stamp (break: `version` copied through again)
    and the read-back (break: a field dropped from the write).
  </action>
  <verify>
    <automated>cargo test --lib data::config:: && cargo test --test a_setting_survives_a_restart</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the re-stamp test with its module path and says the other two were red for the reason each gives.
    - `cargo test --lib data::config::` passes; the summary quotes the re-stamp test's before-and-after of `version` on the July text.
    - `cargo test --release --test a_setting_survives_a_restart -- --ignored --nocapture` was run once and the summary quotes its lines: the profile folder, the first log line, and that the file was unchanged by a start.
    - Two guard records measured; flagged records re-measured and read.
    - The changelog entry is under `[Unreleased]`.
  </acceptance_criteria>
  <done>A setting confirmed on the screen is on disk with the build's own stamp, a July-shaped profile loads and saves whole, and a start alone rewrites nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the log outlives its second minute, proved on the release binary</name>
  <files>src/main.rs, src/common/logging.rs, tests/the_log_outlives_the_second_minute.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/common/logging.rs:60-140`, `init_logging`, the appender, `non_blocking` and the filter; `tracing_appender`'s `NonBlockingBuilder::lossy` and `buffered_lines_limit` in the crate's docs (Context7 or `~/.cargo/registry`), for what the default drops.
    - `src/main.rs:96-125` and `:170-200`, the guard and every `std::process::exit` after it.
    - `src/presentation/wx_app.rs`, the due look's timer line ("One look at what is due", `grep -n 'One look at what is due' src/presentation/wx_app.rs`), which is the once-a-minute line the harness reads, and its level.
    - `tests/the_numbers_the_targets_ask_for.rs:380-440`, the launcher, and how it reads the log.
  </read_first>
  <behavior>
    - `tests/the_log_outlives_the_second_minute.rs`, an ignored harness: writes a temp profile whose `app_config.json` sets `log_level` to `debug`, starts the release binary with `WIXEN_MAIL_DATA` there and `--read-only`, waits 150 seconds, stops it, and requires the profile's log for today to hold "Starting Wixen Mail", a line stamped in the first minute, and a line stamped in the third minute (the due look's line, which runs once a minute at debug). A not-ignored test holds the harness's parsing of a timestamp into its minute.
    - If the harness is red on the release binary (no third-minute line, or an empty log), the cause is found and fixed: `non_blocking` made non-lossy with a bounded buffer and the guard's lifetime made explicit (a named binding that `main` holds until after the event loop returns, with a comment saying why it is not `_log_guard`), or whatever the harness shows; `logging.rs` gains a unit test for the configuration it now builds.
    - If the harness is green on a fresh temp profile, the summary says so and runs it again with the plain profile's July `app_config.json` (`log_level: "info"`) copied into the temp profile, because the tester's runs were on that shape; the due look's line is debug and would be filtered, so the harness then reads the once-a-minute line at info that task 1 added to the folder sentence, or one added here on purpose, and the summary says which.
    - The changelog says what the log now holds and, under Known limitations, that the 2026-09-15 session's log cannot be recovered.
  </behavior>
  <action>
    Red first: the harness's parsing test, then the harness itself run once
    in release against the binary as it is, with its output quoted whole:
    that run is the measurement #66 asks for and it decides what the fix
    is. Then the fix the run supports, its unit test, and the run again with
    the after output beside the before. Two minutes and a half of wall clock
    per run; say each run's start and end in the summary.

    One guard record for whatever the fix holds (break: the lossy default
    restored, or the guard dropped early), measured on the whole library;
    one for the harness target.
  </action>
  <verify>
    <automated>cargo test --lib common::logging:: && cargo test --test the_log_outlives_the_second_minute</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the parsing test bare.
    - The summary quotes the harness's before run and after run on the release binary, each with the three lines it looked for and their timestamps, or the absence that made it red.
    - `cargo test --lib common::logging::` passes with the new unit test; the summary says what the fix was and what the run showed the cause to be, or that no cause reproduced on a temp profile and what was tried.
    - Two guard records measured; flagged records re-measured and read.
    - The changelog entry is under `[Unreleased]` with its Known limitations line.
  </acceptance_criteria>
  <done>A release binary left running writes its third minute to the log, a test says so, and the reason the tester's log was empty is known or the summary says exactly what was tried.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the process identity to the profile | Windows decides which `AppData\Local` a process sees; the program did not know and could not say |
| the settings screen to the disk | "saved" is a claim about a file; a status line at a coalesced priority is a claim nobody hears |
| the log to a bug report | an empty log is a bug nobody can read, which is guardrail 4 |
| a harness to the tester's profiles | a harness launched here runs packaged; without `WIXEN_MAIL_DATA` it would write the tester's virtualised profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-36 | Tampering | a harness writing into one of the tester's profiles | high | mitigate | every launch sets `WIXEN_MAIL_DATA` to a temp folder and passes `--read-only`; the acceptance criteria quote the folder each run named |
| T-09-37 | Repudiation | "Settings saved" said over a file in another profile | high | mitigate | the status line names the path and the log's first lines name the folder, both worded by one tested function |
| T-09-38 | Information disclosure | the folder sentence or the status line carrying a message body or a credential | low | mitigate | the sentence carries a path and two facts about the process; a test holds its shape |
| T-09-39 | Denial of service | a non-lossy log blocking the interface thread when the disk is slow | medium | mitigate | the buffer is bounded and the unit test holds the configuration; the harness measures the usable line beside it |
| T-09-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `tracing-appender` is already a dependency |
</threat_model>

<verification>
- The three `<verify>` commands pass; `scripts/check.sh` through the hook on every commit, never piped; `scripts/check.sh all` once before the merge.
- Both ignored harnesses run once in release and their output quoted; neither touched a folder outside `%TEMP%`.
- Six guard records measured; flagged records re-measured and read.
- Changelog entries under `[Unreleased]`, no version bump.
- After the merge: `gh issue comment 66 --body "Advanced in <merge commit>. What the profile read found: your machine holds two profiles at one path, the plain %LOCALAPPDATA%\\wixen-mail and a copy Windows keeps for programs started under the Claude desktop application's package identity (Packages\\Claude_pzs8sxrjxfjjc\\LocalCache\\Local\\wixen-mail); the 2026-09-15 session with the Gmail account and 12,872 messages wrote the second, and its Settings OK at 18:39 did save English (United States) there; a Start Menu launch reads the first, still from July. The program now says which folder it is in, in the log's first lines and on the settings status line; a saved file carries the build's own version; <what task 3 found about the log>. One question for you is in 09-11-SUMMARY.md: how you start Wixen Mail on which days."` The issue stays open until his answer and the log's cause agree.
</verification>

<success_criteria>
- Roadmap criterion 10 closes structurally except its last clause, which closes when the harness shows a third-minute line on the release binary; FOUND-13's `[D]` lines are named by tests and quoted runs in the summary; its `[S]` line about which launcher the tester uses waits for him.
</success_criteria>

<output>
Create `.planning/phases/09-what-the-first-day-of-testing-found/09-11-SUMMARY.md` when done
</output>
