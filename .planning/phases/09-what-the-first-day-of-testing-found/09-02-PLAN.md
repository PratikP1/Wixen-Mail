---
phase: 09-what-the-first-day-of-testing-found
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/service/spellcheck/mod.rs
  - src/presentation/wx_settings.rs
  - tests/the_language_the_screen_shows_is_the_one_used.rs
  - src/data/message_cache/bodies.rs
  - src/data/message_cache/searching.rs
  - src/data/message_cache/mod.rs
  - src/application/long_text.rs
  - scripts/check.sh
  - guards/guards.toml
  - docs/changelog.md
autonomous: true
requirements: [FOUND-02, FOUND-03]
estimate:
  tokens: 19500
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A stored bare or unlisted spelling language resolves to this machine's own region when the machine's language is in that family, in one function both the checker and the settings screen ask (FOUND-02, #21)."
    - "The settings screen shows the language that will be used and never index 0 for a value it could not match (FOUND-02, #21)."
    - "A snippet of an HTML-only message is its first words through the same reader the reading path uses, and so is the text the search index holds for it; strip_markup is gone; stored snippets are re-derived once on the first open after the change, non-fatally, with the count logged, and each row put right is reindexed (FOUND-03, #32)."
  artifacts:
    - "src/service/spellcheck/mod.rs: one resolver over (stored tag, machine's tag, what the machine offers), asked by for_language and by the screen."
    - "src/presentation/wx_settings.rs: the language selection through the resolver."
    - "tests/the_language_the_screen_shows_is_the_one_used.rs: the real General tab built with \"en\" stored on an en-US machine and the selection read back."
    - "src/data/message_cache/bodies.rs: the snippet derived through application::long_text; strip_markup deleted; a once-only re-derivation of stored HTML-only snippets that reindexes each row it changes."
    - "src/data/message_cache/searching.rs: index_message_for_search reading an HTML-only body through the same reader, with a test."
    - "src/data/message_cache/mod.rs: the re-derivation run at open beside the other backfills."
  key_links:
    - "config.language (a string, possibly a bare \"en\" from before 2026-09-03) -> spellcheck::for_language and -> wx_settings::build_general_tab: two readers of one stored value that disagree today only by accident of the machine's list order; the resolver is where they agree."
    - "save_message_body -> the snippet column -> every row of the message list: the snippet is read aloud on every row somebody arrows past, so the reader it comes through is what a screen reader user hears first about a message."
    - "MessageCache open -> the backfills at mod.rs:1390-1435: the re-derivation runs there, after the inline-body migration that makes the bodies readable, and must be marked done so it does not read every HTML body on every open."
---

<objective>
Fix the two defects whose cause is a stored value read wrongly: the spelling
language a machine set to English (United States) is shown and checked as
English (Caribbean), and the snippet of a newsletter reads its stylesheet
aloud on every row.

Purpose: both are heard constantly by the person this program is for, one in
every message row and one in every word marked wrong, and both are pure
functions with a stored value behind them, so they prove the phase's flow on
the cheapest ground before the windows.

Output: a resolver for stored language tags, a screen that shows what will be
used, a snippet derived through the reading path's own reader, and the
stored snippets put right once. Closes #21 and #32 on the tree; whether the
tester's profile now reads right is his to confirm.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-what-the-first-day-of-testing-found/README.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-16 against `main` at `524ff24f`. Command first, then what it
said.

**1. #21's two candidate causes both fail on this machine; the cause is a
stored bare `en`, and both symptoms follow from it.** A scratch test added to
`src/service/spellcheck/mod.rs` with the Edit tool, run with `--nocapture`,
then removed with `git checkout -- src/service/spellcheck/mod.rs`:

```
cargo test --lib service::spellcheck::tests::scratch_print_what_this_machine_offers_for_planning -- --nocapture
SYSTEM_LANGUAGE=Some("en-US")
OFFERS=TheseLanguages([("en-029", "English (Caribbean)"), ("en-AU", ...), ... ("en-US", "English (United States)"), ("en-ZA", ...), ("en-ZW", ...)])   (19 tags; en-US is the 17th)
DEFAULT=en-US
```

So `language_to_check_in(Some("en-US"), TheseLanguages(...))` answers `en-US`
here: the exact tag is found and marked available, and the family fallback is
never reached. `default_language()` on a fresh profile on this machine writes
`en-US`. What produces Caribbean is a stored `"en"`, and there is a reason a
profile would hold one:

```
git log --format='%h %ad %s' --date=short -S'language_to_check_in' -- src/data/config.rs
  -> 0cb35dd9 2026-09-03 test(spellcheck): failing test for a machine that could not be asked
     580a334a 2026-09-03 A machine that could not be asked keeps its own language
```

and the doc comment on `default_language` says "It was 'en' for everybody".
With `"en"` stored, two readers disagree with the person's machine:

- `wx_settings.rs:651-654`, `build_general_tab`: `.position(|language|
  language.tag == config.language).unwrap_or(0)`. No tag equals `"en"`, so the
  choice shows index 0, and index 0 in Windows' order is `en-029`.
- `spellcheck::for_language("en")` (`mod.rs:493-516`): Windows refuses the
  bare tag, so `find_regional_variant("en", supported)` takes the first of the
  family Windows lists, `en-029`, and the checker really checks Caribbean
  English. `test_a_bare_language_matches_the_first_regional_variant_windows_offers`
  at `:1634` pins exactly that.

This machine's own profile holds exactly that shape (read 2026-09-16 after
the plan check; the first draft of this premise said a profile could not be
read from here, which was an absence claim nobody had traced):

```
grep -o '"language": *"[^"]*"' "C:/Users/prati/AppData/Local/wixen-mail/config/app_config.json"
  -> "language": "en"
ls -la --time-style=long-iso "C:/Users/prati/AppData/Local/wixen-mail/config/app_config.json"   -> 2026-07-30 02:07
ls "C:/Users/prati/AppData/Local/wixen-mail/logs/" | tail -2   -> wixen-mail.2026-09-14.log, wixen-mail.2026-09-15.log (no 2026-09-16 log; no wixen-mail process running)
```

**That file is evidence of the stored shape and not of the tester's
profile.** Pratik has been testing for two days, added a Gmail account, and
said on 2026-09-16 "My setting to U.S. English was done manually", while this
profile's settings file is dated 2026-07-30, its log for 2026-09-15 is the
last one, and nothing here has run today; so he tests on another machine or
under another Windows account, and his manual change lives there. What can
be said: this machine's profile holds the bare `"en"` the cause needs, written
by the old default; the tester's profile was created by an earlier build
(0.40.0, moved on 2026-08-27 at `f7e34777`, was the last installer before
0.125.1, and `default_language` wrote `"en"` until `580a334a` on 2026-09-03),
so it held the same bare value and landed on Caribbean the same way; and he
has since set English (United States) by hand there, which the fix leaves as
set. The fix below holds for a bare or unlisted stored tag, which resolves to
the machine's own region within the family, and the screen shows the resolved
language rather than index 0; a tag that is stored and offered is taken as it
is, so a hand-set language is untouched.

**2. `best_available_match` and `find_regional_variant` are two readers of
one question.** `best_available_match(wanted, choices)` at `:308` prefers the
exact tag, then the first available family member; `find_regional_variant(tag,
supported)` at `:534` takes the first family member. Neither asks what the
machine's own tag is. One function taking the stored tag, the machine's tag
and the offered list, answering exact stored, else the machine's own tag if it
is in the stored tag's family and offered, else the first offered member of
the family, is what both callers need. 30 guard records name `mod.rs` and 56
tests are in it; the count check will name them and the `--remeasure`
remedy is run, detached, and read.

**3. #32's mechanism is exact and the line moved by one.** `strip_markup` is
at `bodies.rs:317` (the issue said about 316), keeps every character outside
`<...>`, and is `pub(super)` with the doc comment "Crude tag removal".
`save_message_body` at `:378-386` derives the snippet through it when the
plain part is empty. The reading path's reader:

```
grep -n 'pub fn from_markup\|fn read_markup\|ammonia::clean' src/application/long_text.rs
  -> 496: pub fn from_markup(html: &str) -> String
     514: fn read_markup(html: &str, keeping: Keeping) -> String   (ammonia::clean, then scraper::Html::parse_fragment)
```

`ammonia::clean` drops `<style>` and `<script>` content outright and `scraper`
walks the rest; no window is involved, and the unit test at `:1046` holds
`blocks_output("<style>body { color: red }</style>") == ""`. `data` already
reaches `application` (`grep -rn 'crate::application' src/data/ | wc -l` ->
133), so calling `long_text` from `bodies.rs` is not a new direction. The
markdown-ish markers `from_markup` writes (`- ` for an item, `# ` for a
heading) are right for a box somebody edits and wrong for a snippet, so the
snippet takes the words with the markers stripped; `long_text::first_line`
at `:1074` may already be that reader, and the executor reads it before
writing a third.

**3a. The search index reads HTML-only bodies through the same stripper, so
deleting it breaks the build and the index holds stylesheets today too.**
Found by the plan check on 2026-09-16; the first draft of this plan missed
the second caller:

```
grep -rn 'strip_markup' src --include='*.rs'
  -> src/data/message_cache/bodies.rs:317 (the definition), src/data/message_cache/searching.rs:360: (None, Some(html)) => super::bodies::strip_markup(&html),
```

`index_message_for_search` (`searching.rs:335`) writes one row of
`message_search` from the message's subject, sender, snippet and body text,
and for a body with no plain part it takes the HTML through `strip_markup`,
so a newsletter's index row holds its stylesheet as body text and a search
for `padding` finds it. The index goes through the same reader as the
snippet, for the same reason. The once-only re-derivation reindexes each
row it changes by calling `index_message_for_search` for it, because that
function already rebuilds the row from the stored snippet and body and is
the one writer of an index row; a re-derived snippet with a stale index row
would be a search that finds the stylesheet the list no longer shows.
`searching.rs` is named by 3 guard records and holds 25 tests; adding one
names the three for re-measuring.

**4. Where stored snippets are re-derived.** The backfills run from
`MessageCache` open at `mod.rs:1390-1435`, each `if let Err(e) =
cache.something() { tracing::warn!(...) }`, non-fatal, after
`migrate_inline_bodies`. The candidates are rows whose body has no plain part
and has an HTML part: `bodies.rs:193` already writes that predicate for
another reader. Bodies may be packed (`body_html_packed`), so each candidate
is read through the existing unpacking reader and not straight from the
column. Nothing marks a backfill done today; the identifier backfill is
idempotent by its own rule (`:1244`, "written over on every open"). A snippet
re-derivation over every HTML-only body is not cheap enough to run on every
open, so it needs a marker: a row in whatever small key-value table the
cache holds, or a new `CREATE TABLE IF NOT EXISTS` on the `held_alerts`
precedent from 06-09. The executor reads `mod.rs`'s schema for an existing
key-value table before adding one.

**5. Records and tests on the files touched.** By the TOML reader on
2026-09-16: `bodies.rs` 4 records, 43 tests; `searching.rs` 3 records, 25
tests; `long_text.rs` 18 records, 59 tests; `spellcheck/mod.rs` 30 records,
56 `#[test]` lines in that file, and `cargo test --lib service::spellcheck::`
runs 65 because `windows_speller.rs`'s 9 sit under the same path, so the
summary says which of the two it quotes; `wx_settings.rs` 8 records,
0 tests, so `--lib presentation::wx_settings::` matches nothing and the gate
reaches it through `every_event_has_a_control` and `checkbox_labels`. The
screen test goes in a new integration target at zero records, which then
needs a record of its own naming the target as its `suite` so
`check.sh --suites-for` couples it to `wx_settings.rs`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: one resolver for a stored language, preferring this machine's own region</name>
  <files>src/service/spellcheck/mod.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/spellcheck/mod.rs:261-325`, `language_to_check_in` and `best_available_match`, and `:493-539`, `for_language` and `find_regional_variant`, with their tests at `:1493-1560` and `:1634-1660`.
    - `src/data/config.rs:550-570`, `default_language`, which is the one writer of a fresh value and needs no change.
    - Premise 1 and 2 above.
  </read_first>
  <behavior>
    - A new pure function, named for what it answers (the language to use for a stored tag), takes the stored tag, the machine's tag as `Option<&str>`, and the offered `&[LanguageChoice]`, and answers: the stored tag itself when it is offered and available; else the machine's own tag when it shares the stored tag's family and is offered and available; else the first offered and available member of the family; else `None`.
    - `"en"` stored, `Some("en-US")` machine, a list in Windows' order beginning `en-029` and holding `en-US`: answers `en-US`. The same with `Some("en-GB")` answers `en-GB`. `"en"` stored with `Some("fr-FR")`: answers `en-029`, the first. `"en-AU"` stored, any machine: `en-AU`. `"de"` stored and no German offered: `None`. Case is ignored on both sides, as `best_available_match` already does.
    - `for_language(tag)` asks the resolver before `find_regional_variant`, with `system_language()` as the machine's tag, so a bare `"en"` on this machine checks `en-US`; `find_regional_variant` is retired if nothing else calls it, and its test at `:1634` is rewritten to the resolver's rule, saying in its name that the machine's region wins over Windows' first.
    - `language_to_check_in` is unchanged: a fresh profile already resolves correctly, and the resolver is for what is stored.
  </behavior>
  <action>
    Red first, on a branch: the resolver's tests, each case above one test,
    named in the trailer. Then the function and the two callers. The doc
    comment says why the machine's region beats Windows' first: Windows lists
    the family in its own order and the first is Caribbean on an en-US
    machine, which is how #21 happened.

    Guard record: one for the resolver's middle arm, whose break makes it
    skip the machine's tag and take the first, measured on the whole library
    with `WIXEN_TEST_THREADS` untouched. Adding tests to `mod.rs` names the
    30 records that count it; run the remedy the count check prints,
    detached, and read the result before committing.
  </action>
  <verify>
    <automated>cargo test --lib service::spellcheck::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names every new resolver test in `Fails-until-green:` trailers.
    - `cargo test --lib service::spellcheck::` passes; the summary quotes both counts before and after: the `#[test]` lines in `mod.rs` (56 on 2026-09-16) and the tests the filter runs (65, because `windows_speller.rs` sits under it).
    - `for_language("en")` on this machine returns a Windows speller for `en-US`, shown by a `--nocapture` run the summary quotes, on the pattern of premise 1.
    - One guard record measured with exactly the named test red; every flagged record re-measured through `--remeasure` and the result read.
  </acceptance_criteria>
  <done>A stored bare language resolves to the region this machine is set to, in one function, and the checker uses it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the settings screen shows the language that will be used</name>
  <files>src/presentation/wx_settings.rs, tests/the_language_the_screen_shows_is_the_one_used.rs, scripts/check.sh, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_settings.rs:630-672`, the language row in `build_general_tab`, and `:2920-2926`, where the choice is read back on OK.
    - `tests/checkbox_labels.rs:1-80` and `tests/every_event_has_a_control.rs`, for how a test builds the real settings dialog once per process and reads a control back, and for the one-window-per-process rule they state.
    - `scripts/check.sh:35`, `guards_that_read_the_whole_tree`, and `:365-400`, the documents-only list, to see that a new target reading only source needs neither.
    - Task 1's resolver.
  </read_first>
  <behavior>
    - The language row selects the resolver's answer for `config.language` with `system_language()` as the machine's tag over `available_languages()`, so a profile holding `"en"` on an `en-US` machine shows English (United States).
    - When the resolver answers `None` (a stored tag nothing offers), the row shows the stored tag's own name with "(no dictionary installed)" as the list already does for unavailable languages, and never index 0; the summary says which of the two the executor built and why, because the built-in fallback list already marks non-English entries unavailable and may cover it.
    - `tests/the_language_the_screen_shows_is_the_one_used.rs` builds the real dialog through `build_settings_dialog` with an `AppConfig` whose `language` is `"en"`, reads the language choice's selection back, and requires its tag to be what task 1's resolver answers for `"en"` on this machine; a second case stores `en-AU` and requires `en-AU`. One `#[test]` building the dialog, on the one-window-per-process rule; the second case reads a second control state without a second window if the dialog can be rebuilt, otherwise it is a second target and the summary says so.
    - Pressing OK writes the tag of the selected row, as `:2922-2926` already does, so a person who changes nothing has their stored `"en"` rewritten to the resolved tag only if they press OK; the changelog says so.
  </behavior>
  <action>
    Red first: the new target against the screen as it is, named bare in the
    trailer as integration tests are. Then the row. A new target needs a
    record so `check.sh --suites-for guards/guards.toml src/presentation/wx_settings.rs`
    names it: the break restores `.unwrap_or(0)` in the language row, `suite`
    the new target, measured by hand with `cargo test --test
    the_language_the_screen_shows_is_the_one_used`. Quote `--suites-for`
    afterwards. The target reads no documents, so it goes in neither of
    `check.sh`'s lists; `scripts/check.sh` is in this task's files only
    because the summary must show the `--suites-for` answer, and is edited
    only if the answer is wrong.

    Changelog entry for #21, `[Unreleased]`, in this task: what the tester
    saw, the cause in one sentence (a stored bare "en" from before
    2026-09-03 read as the first English Windows lists), and that the screen
    now shows the language that is used; Known limitations: a stored value is
    rewritten only when OK is pressed.
  </action>
  <verify>
    <automated>cargo test --test the_language_the_screen_shows_is_the_one_used && bash scripts/check.sh --suites-for guards/guards.toml src/presentation/wx_settings.rs</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new target's test bare in its trailer.
    - `cargo test --test the_language_the_screen_shows_is_the_one_used` passes and the summary quotes the tag it read back for `"en"` on this machine.
    - `bash scripts/check.sh --suites-for guards/guards.toml src/presentation/wx_settings.rs` prints the new target beside `every_event_has_a_control` and `checkbox_labels`.
    - `grep -c 'unwrap_or(0)' src/presentation/wx_settings.rs` is lower by one than before the task, quoted before and after.
    - The changelog entry for #21 is under `[Unreleased]` with its Known limitations line.
  </acceptance_criteria>
  <done>Settings shows the spelling language that will really be used, and a test built the real screen to prove it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: a snippet and its index row are words, through the reader the reading path uses, and the stored ones are put right once</name>
  <files>src/data/message_cache/bodies.rs, src/data/message_cache/searching.rs, src/data/message_cache/mod.rs, src/application/long_text.rs, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/data/message_cache/bodies.rs:285-332`, `SNIPPET_LIMIT`, `snippet_from` and `strip_markup`, and `:334-400`, `save_message_body`; the tests at `:894-990`.
    - `src/data/message_cache/searching.rs:335-380`, `index_message_for_search`, the second caller of `strip_markup`, and `:424`, `build_any_missing_search_index`, for how the index is rebuilt today; the tests at `:897` and `:1143` for the shape of an index test.
    - `src/application/long_text.rs:446-540`, `from_markup` and `read_markup`, and `:1074`, `first_line`, before deciding which reader the snippet takes.
    - `src/data/message_cache/bodies.rs:180-200`, the predicate for a body with no plain part, and `:650-700`, `migrate_inline_bodies`, for reading candidates into a `Vec` before writing and for the unpacking reader.
    - `src/data/message_cache/mod.rs:1383-1440`, the open-time backfills and their comments, and the schema for any key-value table (`grep -n 'CREATE TABLE IF NOT EXISTS' src/data/message_cache/mod.rs`).
    - `src/data/message_cache/held_alerts.rs`, 06-09's small additive table, if a marker table is needed.
  </read_first>
  <behavior>
    - `save_message_body` with no plain part and an HTML part whose `<head>` holds `<style>#outlook a { padding: 0; }</style>` and whose body says "Hello from the newsletter" stores the snippet "Hello from the newsletter", and the same for `<script>` and for a `<title>` in the head. The existing test `test_an_html_only_body_still_gives_a_snippet` keeps passing.
    - The snippet of an HTML body with a heading and a list holds the words and not `#` or `- ` markers, one line, bounded by `SNIPPET_LIMIT` as before.
    - `strip_markup` no longer exists (`grep -rn 'strip_markup' src --include='*.rs'` finds nothing), and `long_text` exposes whatever reader the snippet needs, named for it, with its own unit test if it is new; `index_message_for_search` takes an HTML-only body through that same reader, so a search for a word inside a `<style>` block finds nothing and a search for the first words of the body finds the message; a test in `searching.rs` indexes an HTML-only body with a stylesheet and asserts both.
    - A function on `MessageCache` re-derives the snippet of every message whose stored body has no plain part and has an HTML part, through the same code path `save_message_body` uses, calls `index_message_for_search` for each row it changed so the index row is rebuilt from the new snippet and the new body text, returns how many rows it changed, and on a second call returns 0 without reading a body, because a marker says it has run; a test writes an HTML-only body with the old stylesheet snippet planted in the column by SQL, runs it, reads the words back, and searches for a word from the stylesheet and finds nothing; a second test runs it twice and requires 0 the second time.
    - `MessageCache` open runs it beside the other backfills, after `migrate_inline_bodies`, non-fatal with a warning on the pattern at `mod.rs:1390-1392`, and logs the count at info when it is not zero.
  </behavior>
  <action>
    Red first: the `save_message_body` case with a stylesheet, the index case
    in `searching.rs`, the marker case, and the re-derivation case, named in
    the trailer with module paths. Then the reader, the two callers, the
    deletion of `strip_markup`, the backfill with its reindex, and the
    open-time call. If `first_line` already answers the words without markers,
    use it and say so; if a new reader is added to `long_text.rs`, it is one
    function with one test, and the 18 records naming that file are
    re-measured through the printed remedy, as are the 3 naming
    `searching.rs`.

    The marker: read `mod.rs`'s schema first; if a key-value table exists,
    one row in it, else a `CREATE TABLE IF NOT EXISTS` on the `held_alerts`
    shape, additive, nothing dropped or renamed, and say which in the summary.
    The re-derivation reads candidates into a `Vec` before writing, for the
    reason `migrate_inline_bodies` gives (a cached statement held over the
    table while it is written is a lock against itself).

    Guard records: one for the reader (break: the snippet reads
    `<style>` content again), one for the index (break: the index row takes
    the raw HTML), one for the marker (break: the marker is never written, so
    a second call re-reads every body), all measured on the whole library.

    Changelog entry for #32, `[Unreleased]`: what was heard, the cause in a
    sentence, that a snippet now comes through the same reader the message
    does, and that snippets already stored for HTML-only messages are put
    right once on the first start after this build, with what that costs (one
    read of each such body, once, and its index row rebuilt), and that a
    search no longer finds a newsletter by a word in its stylesheet; Known
    limitations: a message whose body was never fetched has no snippet to put
    right, as before.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::bodies:: && cargo test --lib data::message_cache::searching:: && cargo test --lib application::long_text::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the four new tests with their module paths in `Fails-until-green:` trailers.
    - `grep -rn 'strip_markup' src --include='*.rs'` finds nothing.
    - The three `--lib` runs pass; the summary quotes the three files' test counts before and after (43, 25 and 59 on 2026-09-16).
    - The open-time call is at `mod.rs` beside the other backfills with a comment in their manner, and the summary quotes the log line a real profile with one HTML-only body printed on its first open after the change, with the count.
    - Three guard records measured with exactly the named tests red; every flagged record re-measured through `--remeasure` and read, the three naming `searching.rs` among them.
    - The changelog entry for #32 is under `[Unreleased]` with its cost sentence.
  </acceptance_criteria>
  <done>A snippet and its index row are the first words of the message as the reader would say them, the second stripper is gone, and what was stored wrongly is put right once, index included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a message body to the snippet column | HTML from a stranger becomes text read aloud on every row; the reader it passes through is a sanitiser or it is not |
| the stored settings file to the screen | a value written by an older build is read by a newer one and shown as a choice |
| the cache at open to a backfill | a backfill runs on the person's only copy of their mail before they have done anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | HTML from a sender reaching the snippet column or the search index unsanitised | high | mitigate | the snippet and the index row are derived through `long_text`'s reader, which runs `ammonia::clean` first; the crude stripper is deleted rather than kept as a second path, and `grep -rn strip_markup src` finding nothing is an acceptance criterion |
| T-09-06 | Denial of service | the re-derivation reading every HTML body on every open | medium | mitigate | a marker makes it run once; a test holds the second call to 0 without reading a body; non-fatal on error with the next open trying again |
| T-09-07 | Tampering | the re-derivation writing over a snippet derived from a plain part | medium | mitigate | the candidate predicate is "no plain part and an HTML part" and a test plants both kinds and requires the plain one untouched |
| T-09-08 | Information disclosure | a stored language rewritten without the person choosing it | low | accept | OK writes the resolved tag as it always has; Cancel leaves the file alone; the changelog says so |
| T-09-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `Cargo.toml` is not touched |
</threat_model>

<verification>
- The three `<verify>` commands pass; `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge.
- Five guard records measured; every record the count check named re-measured and read; the census in the summary counted by the TOML reader and not by grep.
- Both changelog entries under `[Unreleased]`, no version bump (the tree is at `1.0.0-alpha.1` and stays there until the first alpha is cut, per the README).
- After the merge: `gh issue close 21 --comment "Fixed in <merge commit>. The cause was neither of the two the issue named: a profile created by a build before 2026-09-03 (0.40.0 was the last installer before 0.125.1) holds the bare default 'en', and the current code mapped a bare 'en' to the first English Windows lists, Caribbean, in both the checker and the Settings list. A stored bare language now resolves to this machine's own region, so a profile from an earlier build stops landing on Caribbean, and a language set by hand, as yours now is, is left as set. Settings shows the language that is used."` and `gh issue close 32 --comment "Fixed in <merge commit>: snippets and the search index's body text come through the same reader the message does, and the rows already stored are put right once on the first start, index included."`
</verification>

<success_criteria>
- Roadmap criterion 2 closes structurally; FOUND-02's and FOUND-03's `[D]` lines are each named by a test in the summary; their `[S]` lines about the tester's profile stay open.
- No test was added to `tests/house_style.rs` or `tests/wired.rs`; the new screen test is its own target with its own record.
</success_criteria>

<output>
Create `.planning/phases/09-what-the-first-day-of-testing-found/09-02-SUMMARY.md` when done
</output>
