---
phase: 09-what-the-first-day-of-testing-found
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - src/presentation/scan_target.rs
  - src/presentation/scan_fixtures.rs
  - src/presentation/wx_app.rs
  - src/presentation/wx_managers.rs
  - src/presentation/wx_account_manager.rs
  - .github/workflows/accessibility.yml
  - tests/no_label_is_only_a_space.rs
  - guards/guards.toml
  - docs/wcag-coverage.md
  - docs/changelog.md
autonomous: true
requirements: [FOUND-08]
estimate:
  tokens: 19500
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The contact, condition, filter, signature and account editors are scan targets opened directly with a fixture, in the workflow's list, so both channels reach them (FOUND-08, #42, #40 point 5)."
    - "Every checkbox in those five editors is named through set_accessible_name with the mnemonic stripped, and the MSAA walk on each editor on this machine reports a name for every checkbox, quoted before and after (FOUND-08)."
    - "No StaticText built with an empty literal is added to a sizer and never filled or named afterwards, anywhere under src/presentation/, and tests/no_label_is_only_a_space.rs refuses that shape with a companion that plants one (FOUND-08)."
  artifacts:
    - "src/presentation/scan_target.rs: five variants with their command-line names and doc comments; scan_fixtures.rs: a contact, a rule condition, a filter, a signature and an account to open them on."
    - "src/presentation/wx_app.rs: five arms in the scan-target match, on the WhichDays pattern."
    - ".github/workflows/accessibility.yml: the five names in the target list."
    - "src/presentation/wx_managers.rs and wx_account_manager.rs: set_accessible_name on the five unnamed checkboxes; the eleven empty spacers gone or allowed by name with a reason."
    - "tests/no_label_is_only_a_space.rs: the widened reading, its companion, and an allow list with reasons."
  key_links:
    - "ScanTarget -> wx_app.rs's match -> the workflow's list: test_the_workflow_asks_for_every_target and test_every_window_a_fresh_profile_can_reach_has_a_name hold the three to each other, so a variant without an arm or a name without a variant is a failing test."
    - "scripts/msaa-names.ps1 walks every visible top-level window the process owns: a modal editor opened by a scan target is one, so the walk reaches the checkbox only if the target opens the editor and not the manager behind it."
    - "The five editor builders take a parent Dialog (four) or a &dyn WxWidget (the contact editor): a scan arm opening one directly needs a parent, which is a bare Dialog under the frame or a widened parameter; the executor reads and chooses."
---

<objective>
Reach the five editors with the scan, name every checkbox in them on the
channel NVDA reads, take the empty spacers out of the accessibility tree, and
widen the whole-tree check so the shape cannot come back.

Purpose: two independent reports on the first day said a checkbox had no
name, in the signature editor and on the contact editor's Basic tab, and both
checkboxes are built the same way: an empty `StaticText` before them in a
grid and no `set_accessible_name`. The scan never reached either editor,
because the targets open the managers and the editors are modals behind Add.
Measuring first is the rule here, because a checkbox carries its own text and
the mechanism the issue names is a guess for that control.

Output: five scan targets, the MSAA walk on each before and after, five named
checkboxes, eleven spacers gone or allowed by name, and a widened reading.
Closes #42 and advances #40 (point 5 of six).
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-what-the-first-day-of-testing-found/README.md
@CLAUDE.md
@docs/manual-accessibility-pass.md
</context>

<premise_corrections>
Taken 2026-09-16 against `main` at `524ff24f`. Command first, then what it
said.

**1. The count holds; the class is eleven spacers, and five checkboxes are
unnamed.**

```
grep -rn 'with_label("")' src/presentation/*.rs | wc -l   -> 22
```

Read one by one with the lines around each. Eleven are status or problem
lines something fills or names later: `wx_managers.rs:427`, `:866`, `:1451`,
`:2838`; `wx_account_manager.rs:231`, `:1540`; `wx_app.rs:722` (the live
region); `wx_blocked_senders.rs:182`; `wx_item_form.rs:536`;
`wx_send_later.rs:190`; `wx_settings.rs:2514`. Eleven are spacers nothing
ever fills: `wx_managers.rs:1285` (before Favourite), `:2826` and `:3285`
(before Case Sensitive, in `build_rule_edit_dialog` and
`build_filter_edit_dialog`), `:3309` (before Enabled), `:3837` (before Default
signature); `wx_account_manager.rs:1459` (a section heading's partner),
`:1465` and `:1484` (the `cb` and `cb_with_description` closures), `:1539`
(the sign-in hint's partner), `:1642` (the app-password button's partner),
`:1712` (the allowed note's partner).

```
for v in fav_check cs_check en_check def_check; do grep -c "set_accessible_name(&$v\|set_accessible_name_and_description(&$v" src/presentation/wx_managers.rs; done
  -> 0 0 0 0
sed -n 1466,1478p src/presentation/wx_account_manager.rs
  -> set_accessible_name(&c, &name_from_label(label));   with the comment "every other builder in this dialog says the name outright"
```

So the five checkboxes in `wx_managers.rs` are unnamed and the account
manager's are named. #42's comment that the account manager's checkboxes "go
on the same fix" is right about the spacers and wrong about the names.

**2. Why the checkbox is heard unnamed is not settled by reading.** A native
checkbox is a BUTTON with its own window text, and the MSAA script's header
(`scripts/msaa-names.ps1:26-31`) says a control with a visible label is named
by it on MSAA even when nothing set one. Two testers' reports say otherwise
for two checkboxes built this way. The walk on the real editor is the
instrument, before anything changes; if it reports the checkbox named, the
naming is still done (it is what every other builder does) and the summary
records that the mechanism is elsewhere and what NVDA's own log says on the
tester's machine is the next step, ledgered.

**3. The scan cannot reach the editors today.**

```
sed -n 15513,15535p src/presentation/wx_app.rs   -> ScanTarget::Filters => managers::manage_filters(...); ::Signatures => manage_signatures; ::Contacts => manage_contacts
sed -n '/ScanTarget::WhichDays => {/,/OnReturn/p' src/presentation/wx_app.rs   -> opens wx_which_days::which_days_are_meant(frame, fixture...) directly
grep -n 'targets = @(' -A 4 .github/workflows/accessibility.yml   -> the list at :143-146, 31 names
grep -n 'fn test' src/presentation/scan_target.rs | wc -l   -> 11, among them test_the_workflow_asks_for_every_target and test_every_window_a_fresh_profile_can_reach_has_a_name
```

The five builders: `build_contact_edit_dialog(parent: &dyn WxWidget, ...)`
at `wx_managers.rs:1240`; `build_rule_edit_dialog(parent: &Dialog, ...)` at
`:2775`; `build_filter_edit_dialog(parent: &Dialog, ...)` at `:3225`;
`build_sig_edit_dialog(parent: &Dialog, ...)` at `:3816`;
`build_account_edit_dialog(parent: &Dialog, ...)` at
`wx_account_manager.rs:1384`. Four want a `Dialog` parent, which a scan arm
does not have; a bare `Dialog` built under the frame as the shell, or the
parameter widened to `&dyn WxWidget` as the contact editor's already is, are
the two ways, and the executor reads what each builder does with `parent`
before choosing.

**4. The MSAA walk on this machine.** `.planning/ROADMAP.md`'s 06-06 line:
"the MSAA walk crashes PowerShell on this machine with NVDA running". The
executor runs it with NVDA stopped, or asks Pratik for a window when NVDA can
be stopped, and if neither is possible the walk waits for the next push's CI
run and the summary says so; it does not report a name it did not read.
`tools/axe-windows/` does not exist locally (`ls tools/axe-windows` ->
nothing), so the UI Automation half is CI's; the MSAA half is the one NVDA
reads and the one this plan measures here.

**5. The whole-tree check reads literals and is at one record.**
`tests/no_label_is_only_a_space.rs` reads `with_label(` and `set_label(`
followed by a non-empty whitespace literal, over every `.rs` under `src/`,
with 4 tests and one record naming it. Widening it to "an empty literal whose
binding is never passed to `set_label`, `said_and_shown`, `set_accessible_name`,
`set_accessible_name_and_description` or `set_value` later in the same file"
catches the eleven spacers and none of the eleven lines (each of those is
filled or named by its binding). Ledger 408, a spin control's buddy field, is
a different shape and is not touched here.

**6. Records and the gate.** `wx_managers.rs` 7 records, 44 tests;
`wx_account_manager.rs` 6 records, 14 tests; `scan_target.rs` 2 records, 11
tests; `wx_app.rs` 50 records, no test added. `.github/workflows/` answers
`all`. `scan_fixtures.rs` maps to `--lib presentation::scan_fixtures::`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: five editors as scan targets, and the MSAA walk on each before anything changes</name>
  <files>src/presentation/scan_target.rs, src/presentation/scan_fixtures.rs, src/presentation/wx_app.rs, src/presentation/wx_managers.rs, src/presentation/wx_account_manager.rs, .github/workflows/accessibility.yml, guards/guards.toml</files>
  <read_first>
    - `src/presentation/scan_target.rs:58-240`, the enum, its doc comments and its names, and the tests at `:273-430`, for what holds the enum, the match and the workflow together.
    - `src/presentation/wx_app.rs:15380-15660`, the scan-target match, and the `WhichDays`, `SendLater` and `Destination` arms for opening a nested dialog directly on a fixture.
    - `src/presentation/scan_fixtures.rs`, all of it, for the fixture shape.
    - The five builders' signatures and their first forty lines each, for what `parent` is used for.
    - `scripts/msaa-names.ps1:1-60`, the header and parameters, and `.github/workflows/accessibility.yml:160-200`, how the workflow starts the application per target and runs the walk.
  </read_first>
  <behavior>
    - `ScanTarget` gains `ContactEditor`, `ConditionEditor`, `FilterEditor`, `SignatureEditor` and `AccountEditor`, named `contact-editor`, `condition-editor`, `filter-editor`, `signature-editor` and `account-editor` on the command line, each with a doc comment saying what it opens and why the manager target did not reach it.
    - Each arm opens the editor directly with a fixture (a contact with every field filled, a condition, a filter, a signature, an OAuth account on a domain nothing recognises as the Accounts arm does) and returns `OnReturn::WindowClosed`; the workflow's list names all five; `test_the_workflow_asks_for_every_target` and `test_every_window_a_fresh_profile_can_reach_has_a_name` pass.
    - Each editor is started on a throwaway profile on this machine with `--scan-target <name>` and seen, on the 06-06 pattern; none is unreachable.
  </behavior>
  <action>
    Red first: the `scan_target.rs` tests against the five new names before
    the variants exist, named with module paths in the trailer. Then the
    variants, fixtures, arms and workflow list. Where a builder takes
    `&Dialog`, either build a bare `Dialog` under the frame as the shell or
    widen the parameter to `&dyn WxWidget`; say which and why in the summary,
    and change nothing about how the managers open their editors.

    **Then measure, before task 2 changes anything.** With NVDA not running
    (premise 4), start the application once per new target and run
    `scripts/msaa-names.ps1` against it, the way `accessibility.yml:160-200`
    does, and put the rows for every checkbox in each editor into the summary
    verbatim: the path, the role and the name the walk read. This is the
    before picture and it is what decides whether the mechanism the issue
    names is the mechanism. If NVDA cannot be stopped, say so and record the
    walk as waiting for the next push.

    Two guard records on `scan_target.rs` (one variant removed; one workflow
    name removed), measured by hand; the workflow commit pays the whole gate.
  </action>
  <verify>
    <automated>cargo test --lib presentation::scan_target:: && cargo test --lib presentation::scan_fixtures:: && grep -c 'editor' .github/workflows/accessibility.yml</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the reddened `scan_target` tests with module paths.
    - `cargo test --lib presentation::scan_target::` passes with the five new names; `grep -c "'[a-z]*-editor'" .github/workflows/accessibility.yml` is 5.
    - The summary records each of the five editors seen on this machine through `--scan-target`, and the MSAA walk's rows for every checkbox in each, or the reason the walk could not run here.
    - Two guard records measured.
  </acceptance_criteria>
  <done>The scan reaches the five editors, and what the channel NVDA reads says about their checkboxes today is written down before anything is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: every checkbox named outright, and the empty spacers out of the tree</name>
  <files>src/presentation/wx_managers.rs, src/presentation/wx_account_manager.rs, guards/guards.toml, docs/changelog.md, docs/wcag-coverage.md</files>
  <read_first>
    - The eleven spacer sites in premise 1, each with its ten surrounding lines, and what each spacer's partner does (shown, hidden, named).
    - `src/presentation/accessibility/names.rs:403-480`, `set_accessible_name`, `set_accessible_name_and_description` and `name_from_label`.
    - `src/presentation/wx_account_manager.rs:1459-1495`, the `section`, `cb` and `cb_with_description` closures, which are the shape to copy for naming and the shape to change for spacing.
    - wxdragon 0.9.17 `sizers/base.rs:93-105`, `add_stretch_spacer` and `add_spacer`, and `grid_bag_sizer.rs:193`, `add_spacer_at`, for keeping a two-column grid without a control in it; and whether a sizer item can be hidden, for the spacers whose partners are hidden and shown (`:1539`, `:1642`).
    - `tests/checkbox_labels.rs:1-40`, for the rule that a checkbox carries its own label on both channels, which this task keeps.
  </read_first>
  <behavior>
    - The five checkboxes at `wx_managers.rs:1285`, `:2826`, `:3285`, `:3309` and `:3837` get `set_accessible_name(&check, &name_from_label(label))` in the manner of the account manager's `cb`, keeping their own labels so UI Automation still names them from the window text; unit tests in `wx_managers.rs` that build these dialogs (if any build them without showing) read the name back, else the reading is the MSAA walk in task 3 and the summary says so.
    - The eleven empty spacers are replaced by a sizer spacer where the grid needs the cell, or removed where it does not; where a spacer's partner is shown and hidden (`:1539`, `:1642`) and the sizer cannot hide a spacer item, the empty static stays and is allowed by name in task 3's list with that reason, and the summary lists every site with what was done.
    - `grep -rn 'with_label("")' src/presentation/*.rs | wc -l` falls from 22 to 11 plus the allowed ones, quoted.
  </behavior>
  <action>
    Test-first where a test can reach it: the two `wx_managers.rs` tests
    that build dialogs without showing them (`grep -n 'build_.*_dialog(' src/presentation/wx_managers.rs | grep test`)
    are the place to assert a name; if none builds these editors, the red
    half is the MSAA walk's before picture from task 1 and the green half is
    task 3's after picture, and the summary says the naming had no test to
    redden and why (a name set through `wxAccessible` is not readable from a
    test, `docs/manual-accessibility-pass.md`'s "Which technology proves
    what"). The summary says which of the two it was.

    Then the naming and the spacers, one site at a time, each site's
    decision written in the summary. Do not touch the eleven filled lines.

    Guard records: one per test that was written; none for the walk.
    `wx_managers.rs` is named by 7 records and `wx_account_manager.rs` by 6;
    run the remedy the count check prints and read it.

    Changelog entry for #42 and #40 point 5, `[Unreleased]`: the two
    checkboxes the tester met unnamed, the three more built the same way,
    that every one now says its name outright, that the empty spacers are
    out of the tree, and under Known limitations that what NVDA now says on
    the tester's machine is his to hear. `docs/wcag-coverage.md`'s 4.1.2 row
    gains the five editors as scanned windows, dated.
  </action>
  <verify>
    <automated>cargo test --lib presentation::wx_managers:: && cargo test --lib presentation::wx_account_manager:: && cargo test --test checkbox_labels</automated>
  </verify>
  <acceptance_criteria>
    - Every checkbox in the five editors has a `set_accessible_name` or `set_accessible_name_and_description` call naming it; the summary lists each with its line.
    - `grep -rn 'with_label("")' src/presentation/*.rs | wc -l` is quoted before and after, and every remaining empty literal is either a filled line or on task 3's allow list with a reason.
    - The three `<verify>` commands pass; every flagged record re-measured and read.
    - The changelog entry and the coverage page row are in.
  </acceptance_criteria>
  <done>Every checkbox in the editors says its own name on the channel NVDA reads, and no nameless control sits before one as a spacer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the whole-tree check refuses an empty static nothing fills, and the walk after</name>
  <files>tests/no_label_is_only_a_space.rs, guards/guards.toml</files>
  <read_first>
    - `tests/no_label_is_only_a_space.rs`, all of it, for the reading, its companion and the record that names it.
    - `tests/house_style.rs`, one source-reading guard with an exception list by name and reason (`grep -n 'ALLOWED\|allowed_by_name' tests/house_style.rs | head`), for the list's shape.
    - Premise 5 above.
  </read_first>
  <behavior>
    - The reading also finds every `let NAME = StaticText::builder(...).with_label("")` (across lines, since the builder chain is usually broken over three) and refuses it unless `NAME` appears later in the same file as the receiver of `set_label`, `said_and_shown`, `set_accessible_name`, `set_accessible_name_and_description` or `set_value`, or is on an allow list of `(file, binding, reason)`; the complaint names the file, line and binding.
    - A companion splices a spacer into a real file's own lines in memory and requires the reading to name it; a second companion splices a filled one and requires silence.
    - The allow list holds only what task 2 left, each with its reason, and a test requires every entry on it to still exist in the tree, so an entry cannot outlive its site (the census-emptying failure `CLAUDE.md` describes).
    - After the reading is green: the MSAA walk from task 1 run again on each of the five editors, with the rows for every checkbox quoted beside task 1's, which is the proof #42 asks for.
  </behavior>
  <action>
    Red first: the widened reading against the tree as task 2 left it should
    be green, so the red half is the companion planting a spacer, which is red
    until the reading can see one; name it bare in the trailer. Then the
    reading, the allow list and the existence test.

    Re-run the walk on the five editors (premise 4's conditions) and put the
    after rows in the summary beside the before rows. If the before walk
    reported the checkbox named already, say so plainly: the naming and the
    spacers still stand on their own merits, the tester's report is still
    unexplained, and a ledger entry says the next instrument is NVDA's own
    log on his machine with the tab key on the signature editor.

    One guard record: break plants a spacer in `wx_managers.rs`, `suite` this
    target, measured by hand; the file is named by one record already, which
    the count check will name, so run the remedy and read it.
  </action>
  <verify>
    <automated>cargo test --test no_label_is_only_a_space && bash scripts/check.sh --suites-for guards/guards.toml src/presentation/wx_managers.rs</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new companion bare in its trailer.
    - `cargo test --test no_label_is_only_a_space` passes over the tree; the summary quotes the reading's count of empty literals seen, filled, allowed and refused (refused must be 0).
    - The allow list's every entry names a reason and a site that exists, held by a test.
    - The summary shows the MSAA walk's rows for every checkbox in each of the five editors after the change, beside the rows before, or the reason the walk is waiting for CI.
    - One guard record measured; `--suites-for` names this target for `wx_managers.rs`.
  </acceptance_criteria>
  <done>A nameless spacer before a control cannot be added back without a test saying so, and what the channel NVDA reads says about the five editors' checkboxes is written down after as well as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the code's naming call to the accessibility tree | `set_accessible_name` reaches MSAA only; a call that looks like naming can name nothing, which is guardrail 2 |
| the scan to a nested dialog | a window the scan does not open is not a window with nothing wrong with it |
| a fixture to a scan target | a fixture account or contact is scan-only and must not touch the real state the managers read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-15 | Repudiation | a name claimed from the call and never read on the channel | high | mitigate | the MSAA walk before and after is the proof, quoted; a walk that could not run is said to be waiting, not passed |
| T-09-16 | Tampering | a scan fixture writing an account into a real profile | medium | mitigate | the arms follow the Accounts arm's scan-only fixture and the summary says the real handlers are untouched |
| T-09-17 | Denial of service | the widened reading refusing every filled status line | low | mitigate | the filled-later rule and two companions, one planting a spacer and one a filled line |
| T-09-18 | Tampering | an allow-list entry outliving its site | low | mitigate | a test requires every entry's site to exist |
| T-09-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added |
</threat_model>

<verification>
- The three `<verify>` commands pass; `scripts/check.sh` through the hook on every commit, never piped; `scripts/check.sh all` once before the merge.
- Every guard record measured; flagged records re-measured and read.
- One changelog entry under `[Unreleased]`, no version bump.
- After the merge: `gh issue close 42 --comment "Fixed in <merge commit>: the signature editor and four more editors are scan targets now; every checkbox in them is named outright; the empty spacers are gone; the whole-tree check refuses the shape. The MSAA walk's rows before and after are in 09-05-SUMMARY.md. What NVDA says on your machine is yours to hear."` and `gh issue comment 40 --body "Point 5 (the unnamed Favourite checkbox) is fixed in <merge commit> with #42; points 1 to 4 and 6 are later phases and this issue stays open for them."`
</verification>

<success_criteria>
- Roadmap criterion 5 closes structurally; FOUND-08's three `[D]` lines are named by tests or by the walk's rows in the summary; its listening `[S]` line stays open.
- #40 stays open with a comment naming what remains.
</success_criteria>

<output>
Create `.planning/phases/09-what-the-first-day-of-testing-found/09-05-SUMMARY.md` when done
</output>
