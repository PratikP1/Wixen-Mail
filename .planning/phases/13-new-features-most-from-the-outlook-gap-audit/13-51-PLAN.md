---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 51
type: execute
wave: 53
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06, 13-07, 13-08, 13-09, 13-10, 13-11, 13-12, 13-13, 13-14, 13-15, 13-16, 13-17, 13-17.1, 13-18, 13-19, 13-20, 13-21, 13-22, 13-23, 13-24, 13-24.1, 13-25, 13-26, 13-27, 13-28, 13-29, 13-30, 13-31, 13-32, 13-33, 13-34, 13-35, 13-36, 13-37, 13-38, 13-39, 13-40, 13-41, 13-42, 13-43, 13-44, 13-45, 13-46, 13-47, 13-48, 13-49, 13-50]
files_modified:
  - docs/ALPHA_TESTING.md
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/manual-accessibility-pass.md
  - docs/changelog.md
  - docs/development/measurements.md
  - guards/guards.toml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-01, GAP-02, GAP-03, GAP-04, GAP-05, GAP-06, GAP-07, GAP-08, GAP-09, GAP-10, GAP-11, GAP-12, GAP-13]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The alpha page, the guide, the shortcuts page and the changelog say what phase 13 added (print, undo and redo, the key manager, invitations, encrypted and signed mail, junk and blocks, the directory, free and busy, saved searches, identities, Quick Steps, rules run over a folder, and the rest of import and export), every older sentence kept and dated, and the listening page carries one numbered line per sentence the phase's summaries list as unheard or waiting on an account."
    - "Each of GAP-01 to GAP-13 is read clause by clause against the tree, its box ticked only when every [D] line has a named test, reading or file found in the tree by grep in one pass, its [S] lines left with their ledger numbers; the roadmap, the requirements, the state and the ledger say the same thing, and a plan that stopped or did not run is said everywhere a tick or a number would otherwise read as done."
    - "The four answers phase 13 waited on are each said where they stand: (a) cfb 0.15.0 for .msg, (b) the three windows features for print and rand 0.8 renamed, (c) Microsoft Graph's People.Read and the tasks permission, (d) the upstream wxDragon printing issue, which is ledgered and never posted by a plan; and both conditional plans (13-32, Edit Event scrolling, and 13-50, imported files) are said as run or not."
    - "The whole suite, the release build and the security audit ran for the phase: scripts/check.sh all by hand on this plan's branch after its last commit and before its merge, with main unmoved since the branch was cut, its output written to a file and its exit status read directly, once, or again after a fix when the first run was red; a red one is fixed on this branch as a small red and green pair, and the phase is not called closed on a red run (CLAUDE.md, 'A phase's closing plan runs the full gate once, by hand, before its merge')."
  artifacts:
    - "docs/ALPHA_TESTING.md, docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/manual-accessibility-pass.md, docs/changelog.md: read as one reader would."
    - ".planning/REQUIREMENTS.md: GAP-01 to GAP-13 with dated closing sentences and their traceability rows; .planning/ROADMAP.md: phase 13's criteria closed, its plan list ticked, its progress row, its phase line; .planning/STATE.md; .planning/WINDOWS.md; docs/development/measurements.md: the phase's full-gate row."
  key_links:
    - "each plan's coverage block -> each GAP [D] line: every ref with a # taken as path#name and grepped as fn <name> in that file, in one pass, before any tick is written."
    - "each plan's [S] lines and unrun-verify entries -> the listening page -> the ledger: one number per unheard sentence."
    - "the branch tip with main unmoved -> scripts/check.sh all > file -> exit status read directly -> the measurements row and the roadmap's phase line."
---

<objective>
Close phase 13: say on the pages what it built, write the listening
lines, read GAP-01 to GAP-13 against the tree, and run the full gate
once.

Purpose: the phase's closing read, on 12-12's pattern and 11-12's before
it: a phase that closes says so everywhere a number or a tick would
otherwise read as complete, and a closing read checks every name the
coverage blocks give against the tree in one pass rather than trusting
the summaries. Since 2026-09-23 the closing plan also carries the only
full gate of the phase (12-03.2; Pratik's answer of that day that a red
one is fixed on the closing branch). This plan runs in the phase's
highest wave so it reads the pages and the tree as every plan before it
left them.

Output: the pages, the listening lines, the thirteen requirements read,
the roadmap, the state and the ledger told, the four waits said, the
full gate's row, and the summary's For a person paragraph.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-12-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before any plan of the
phase has run; every count is re-taken on the day with the same
command. Command first, then what it said.

```
ls .planning/phases/13-new-features-most-from-the-outlook-gap-audit/*-PLAN.md   -> the plans on the day; 51 are numbered by RESEARCH-CROSS-CHECK.md, and a planner that split one wrote a decimal number beside it, which the plan list and the progress row count as plans
awk '/^## A\./,/^## B\./' docs/manual-accessibility-pass.md | grep -c '^[0-9]*\. \*\*'   -> 95 (12-12 appended 84 to 95); the items this plan appends start at the next number on the day, and no item number moves
grep -n 'GAP-' .planning/REQUIREMENTS.md   -> the thirteen requirements at :5767 to :5885 and their traceability rows at :6062 to :6074, each "Not planned, 2026-09-20"
sed -n 1463,1513p .planning/ROADMAP.md   -> phase 13's entry, thirteen success criteria, "Plans: not planned yet"; the plan list and the progress row are added when the plans land, and this plan reads them as they stand on the day
grep -n 'current_phase:\|current_plan:\|Current Plan:\|Total Plans in Phase:' .planning/STATE.md   -> current_phase 12 and current_plan 16 on 2026-09-24; the column-zero lines the_planning_files_agree_with_themselves holds to the frontmatter and the disk
head -7 .planning/WINDOWS.md   -> 609 entries, 550 open, 59 fixed; the plans add and close their own, and the frontmatter is corrected here by count
git log origin/main -1 --format='%h %cd' --date=short   -> 630e2a67 2026-09-24: main was pushed at the close of phase 12, so ledger 609's premise ("main has not been pushed since 6cb8f17c") is read again on the day and closed or reworded
grep -n "Phase 12's full gate" docs/development/measurements.md   -> :466, the row this plan's row follows: 516 s, 8,985 passed, the stage line, the conditions
```

**The decimal plans.** When the phase was planned, two planners had
added a plan: `13-17.1-PLAN.md` (keys locked with a passphrase) at wave
18, and `13-24.1-PLAN.md` (the runner, split from 13-24) at wave 26. So
13-18 to 13-24 sit one wave later than RESEARCH-CROSS-CHECK.md's table
and 13-25 to 13-50 two waves later, and this plan is at wave 53, the
phase's highest, with one plan per wave from 1 to 53 (the phase README,
"One plan per wave"). `depends_on` names both decimals; any other
decimal plan the phase's plan list holds on the day is one this plan
also waits for, and the summary names it. This plan's wave must be the
phase's highest on the day; it is read against the plan list before
anything else here runs.

**The completion marks.** A plan leaves four: the roadmap's progress
row, its own `- [ ]` line in the phase's plan list, `STATE.md`'s plan
number in the frontmatter and in the body, and the requirement's `[D]`
lines and traceability row. GAP-13's were written by 13-45 to 13-50 and
its tick by 13-49. `tests/the_planning_files_agree_with_themselves.rs`
compares `STATE.md`'s number with the row and counts no ticks (CLAUDE.md,
the completion-marks paragraph), so this read counts the ticked lines in
the plan list against the row by hand and reports any drift.

**The four waits (Pratik, 2026-09-24).** (a) `cfb` 0.15.0 for `.msg`,
13-47's checkpoint; (b) three `windows` crate features for print (13-03)
and `rand` 0.8 as a renamed direct dependency (13-20), manifest lines
adding no package; (c) Microsoft Graph's People.Read and the tasks
permission, which make the sign-in screen ask for more (13-28 and the
tasks plan that needs it); (d) an issue upstream on wxDragon's printing,
a public post that is ledgered and that no plan posts. Each plan that
needed an answer carried a checkpoint that stopped only when its brief
lacked the answer; the summaries say which stopped.

**The full gate's rule, and the check that does not exist yet.** Ledger
584 is for a check in `tests/the_planning_files_agree_with_themselves.rs`
that the highest-wave plan of every phase from 12 on names
`scripts/check.sh all`; it is not written, so this plan's task 3 is what
holds the rule for phase 13, and the summary says so.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: the pages and the listening lines</name>
  <files>docs/ALPHA_TESTING.md, docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/manual-accessibility-pass.md, docs/changelog.md</files>
  <read_first>
    - Every summary of the phase: each one's `provides`, its `[S]` lines, its ledger table and what it says was not heard; 12-12's summary for the shape of the short version and the listening items.
    - `docs/ALPHA_TESTING.md`, the short version, "What would help most to hear about" and "What is already known to be missing or unproven"; `docs/manual-accessibility-pass.md`, section A's end; `docs/USER_GUIDE.md` and `docs/KEYBOARD_SHORTCUTS.md`, the sections the plans wrote; `docs/changelog.md`, `[Unreleased]`.
  </read_first>
  <action>
    Invoke `writing-craft`. Every sentence is checked against the tree
    before it is written: each command, key and letter a sentence names
    is re-found in the menus with a command, each constant with its
    source line.

    `docs/ALPHA_TESTING.md`: a paragraph in the short version saying
    what phase 13 added, in the order the tester meets it (File, Print;
    Edit, Undo and Redo; the key manager; an invitation in the reader;
    encrypted and signed mail; Report as Junk and a block that moves the
    mail already here; the directory while writing an address; free and
    busy from every source; saved searches ordered and keyed; identities
    in the From list; Quick Steps; a rule run over a folder; the export
    shapes and saved Outlook messages), and that none of it has been
    heard and anything touching a provider has not met one; in the
    known-missing list, one entry per plan's unheard or unproven
    sentence in the register of the list's first item, with the answer
    each of the four waits got; "What would help most" gains the walks
    that fit (a message printed; an undo heard naming the item; an
    invitation answered; a `.msg` from his Outlook imported).

    `docs/manual-accessibility-pass.md`: items from the next number on,
    one per ledger entry the phase opened for the tester's ear, reader,
    machine, account or mailbox, appended after the last so no number
    moves, the count line corrected by dating.

    `docs/KEYBOARD_SHORTCUTS.md`: read against every menu and key the
    phase added (Ctrl+P, Undo and Redo and Undo Send's letters on Edit,
    Report as Junk and Block's keys, the Quick Step and saved-search
    keys, the key manager's and Quick Step manager's letters, the two
    export items), each row present and agreeing with the code, fixed by
    dating where it drifted. `docs/USER_GUIDE.md` and
    `docs/changelog.md`: read as one reader would, each entry the plans
    wrote carrying its own until-sentence, and any sentence a later plan
    falsified corrected by dating in the page's own manner.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test docs_links && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## A\./,/^## B\./' docs/manual-accessibility-pass.md | grep -c '^[0-9]*\. \*\*'` is the day's count before the edit plus the number of ear, reader, machine, account and mailbox ledger entries the phase's summaries opened, both quoted, and the count line says the same number.
    - `grep -c -i 'print' docs/ALPHA_TESTING.md` and `grep -c -i 'quick step' docs/ALPHA_TESTING.md` are each at least 1.
    - Carriage returns 0 on every file touched (`tr -cd '\r' < FILE | wc -c` read as 0); no em or en dash; none of the six words.
  </acceptance_criteria>
  <done>The pages describe the program this phase built rather than the one before it, and every sentence the tester will test by ear is a numbered line on the listening page.</done>
</task>

<task type="auto">
  <name>Task 2: the closing read of GAP-01 to GAP-13, the four waits, and the four planning files told</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - Every summary's coverage block; `.planning/REQUIREMENTS.md`'s phase 13 section and the traceability rows; `.planning/ROADMAP.md`'s phase 13 entry, its plan list and progress row, and the milestone paragraph; `.planning/STATE.md`'s frontmatter and Current Position; `.planning/WINDOWS.md`'s head and the entries the phase opened.
    - The phase README if one exists, and `RESEARCH-CROSS-CHECK.md`'s Numbering table for which plan closes which requirement.
  </read_first>
  <action>
    The one-pass check first: every `ref` in every coverage block of the
    phase holding a `#` taken as `path#name` and grepped as `fn <name>`
    in that file; a name not found is traced to its rename or split and
    said, and no tick is written on a name that is not in the tree.

    Then `.planning/REQUIREMENTS.md`: a paragraph at the head of the
    phase's section saying what the read did and found; each of GAP-01
    to GAP-13 read clause by clause, its box ticked only when every
    `[D]` line has a name found, otherwise left open with the reason on
    its lines; the `[S]` lines untouched; the traceability rows the
    same; the coverage count re-taken with the file's own command.

    The four waits: for each of (a) to (d), the plan that needed it, the
    answer and its date if it came, and if not, which plan stopped at
    its checkpoint and what it left undone, written into the
    requirement lines it bears on and into the summary; (d) is checked
    to be a ledger entry and not a post, with `gh issue list --repo`
    for wxDragon's repository read (not written) to confirm no plan
    posted it. Both conditional plans said as run or not, with their
    requirement lines.

    `.planning/ROADMAP.md`: each of the thirteen criteria with its dated
    closing sentence naming the plans, the merges and the ledger
    numbers; the plan list every line ticked whose summary exists,
    unticked with the reason otherwise, and the ticked lines counted
    against the progress row by hand; the progress row from the disk;
    the phase line ticked only if every plan ran and task 3 is green,
    otherwise open with the reason and the date; the milestone paragraph
    brought forward one sentence. `.planning/STATE.md` by hand: the
    frontmatter, the Current focus line, the Current Position paragraph,
    `Current Plan:` and `Total Plans in Phase:` at column zero,
    `progress.completed_plans` and `total_plans` from
    `ls .planning/phases/*/*-SUMMARY.md | wc -l` and the same over
    `*-PLAN.md`, the session lines. `.planning/WINDOWS.md`: the
    frontmatter counts corrected from the rows and the JSON; ledger 609
    read against `git log origin/main -1` and closed or reworded; one
    `unrun-verify` for whatever the phase leaves to a person that no
    plan's own entry names.

    The summary's For a person paragraph: what changed in this round
    over the build the tester has, which issues closed, what only a
    person can settle, the four waits in plain words, and what is next
    (phase 14, the real-account proofs, the seventh of the seven
    groups).
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - The one-pass check's numbers quoted: refs read, names found, names not found and where each went.
    - `grep -c '^- \[x\] \*\*GAP-' .planning/REQUIREMENTS.md` plus `grep -c '^- \[ \] \*\*GAP-' .planning/REQUIREMENTS.md` equals 13, and the summary says which are which and why.
    - The roadmap's progress row for phase 13 equals the summaries on disk over the plans on disk; the plan list's ticked lines, counted by hand, equal the row's numerator, both quoted; `STATE.md`'s two lines agree with the frontmatter and the disk; `head -7 .planning/WINDOWS.md` agrees with the rows and the JSON, quoted.
    - Each of the four waits said with its answer or the plan that stopped; carriage returns 0 on every file touched; no em or en dash; none of the six words.
  </acceptance_criteria>
  <done>The requirements, the roadmap, the state and the ledger say the same thing about phase 13, every tick stands on a name in the tree, and every wait and every plan that did not run is said where it would otherwise read as done.</done>
</task>

<task type="auto">
  <name>Task 3: the phase's full gate, once, by hand, before the merge</name>
  <files>docs/development/measurements.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/WINDOWS.md, guards/guards.toml</files>
  <read_first>
    - `CLAUDE.md`'s "A phase's closing plan runs the full gate once, by hand, before its merge" and "Since 2026-09-23 the merge is no longer the full gate; the phase's closing plan is".
    - Every summary of the phase for its merge commit's stage line, so a red here can be placed among the merges.
    - `docs/development/measurements.md:466`, phase 12's full-gate row, the shape of this one.
  </read_first>
  <action>
    On this plan's branch, after task 2's commit and before the merge.
    Nothing is fetched. Read `git rev-list HEAD..main --count`, which
    must be 0, so `main` has not moved since the branch was cut and the
    tree this run reads is the tree `main` will hold after the merge. If
    it is not 0, merge `main` into the branch first (a merge commit,
    through the hook) and read it again.

    Then the whole gate, by hand, once, with nothing else building:
    `scripts/check.sh all` with its output redirected to
    `13-51-full-gate.log` in the session scratchpad and its exit status
    echoed on the same line, never piped into anything whose status is
    then read. The log's last lines, its stage line and the test count
    summed over its `test result:` lines are quoted in the summary.

    If it is red, fix it here and run it again, and do not stop to ask:
    Pratik's answer of 2026-09-23. Read what failed and place it: which
    merge brought the file or the test in (`git log --first-parent main
    -- <path>`), and whether it is a defect, the keyring race's
    successor, or a live-window test failing on its own. The red first,
    by `CLAUDE.md`'s rule: where the test the gate named fails for the
    cause itself, that test is the red, run again by hand and quoted,
    and no red commit is needed; where it does not, a test that fails
    for the cause is written and committed red on this branch with its
    `Fails-until-green:` trailer. Then the fix, the green, and this task
    again from `git rev-list HEAD..main --count`, until the run exits 0.
    The fix's files are named in the summary, `guards/guards.toml` among
    them if the fix writes or re-measures a record. The phase line in
    the roadmap stays unticked until the run is green, and the summary's
    first sentence says what was red and the pair that fixed it.

    Green: a row on `docs/development/measurements.md` for phase 13's
    full gate, with the command, the date, the commit and the conditions
    (warm or cold, NVDA running, what else was building) and the stage
    line. The roadmap's phase line ticked if task 2 left it waiting only
    on this run. The pages and the planning files changed, and the Help
    window shows the guide and the changelog, so the branch is then
    pushed with a pull request, the three runs read, any red fixed on
    the branch as a red and green pair with this task's run taken again,
    and then the local `git merge --no-ff`. Pushing `main` stays
    Pratik's.
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list HEAD..main --count` read as 0 immediately before the run, quoted.
    - The run's exit status read directly and quoted, 0, with its stage line and its summed test count. If a run was red first: what failed and the merge it came in with; the red, either the named test run again by hand or the red commit with the hook's line saying `red-commit.sh` accepted it; the green commit; and the exit status of the run after them, 0; each quoted.
    - One row added to the measurements page and passing its reading; the pull request's number and the three runs' conclusions quoted.
  </acceptance_criteria>
  <done>Phase 13's code has been through the whole suite, the release build and the audit once, on the tree main holds when the phase closes, and the runner has read the same tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a summary's claim to a page's sentence | a page describing a program the tree does not hold |
| a coverage block's name to a tick | a tick standing on a test that was renamed or split |
| the phase's merges to main | a break only the whole suite sees, waiting on main since the phase's first merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-51-01 | Repudiation | a page sentence the phase falsified left standing | medium | mitigate | every sentence re-read against the tree with a command; corrections by dating |
| T-13-51-02 | Tampering | a requirement ticked on a name not in the tree | medium | mitigate | the one-pass grep over every ref before any tick; the numbers quoted |
| T-13-51-03 | Repudiation | a wait on Pratik or a stopped plan reading as done | medium | mitigate | each of the four waits and both conditional plans said in the requirements, the roadmap and the summary |
| T-13-51-04 | Denial of service | a red only the whole suite sees, carried into phase 14 | high | mitigate | the full gate once on the closing branch with `main` unmoved; a red fixed here as a red and green pair; the phase line unticked until green |
| T-13-51-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing installed; documents and planning files only, and a gate fix's files named; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the phase's full gate is task 3, `scripts/check.sh all` by hand once on this branch before the merge; the branch is pushed with a pull request and the three runs read; the merge with `git merge --no-ff` then runs what the branch's diff earns. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line, as every plan since 12-03.2 does.
- The one-pass check's numbers in the summary; `the_planning_files_agree_with_themselves` green on the branch and on `main` at the merge.
</verification>

<success_criteria>
- Phase 13's thirteen roadmap criteria each carry a dated closing sentence, and the pages, the listening lines, the thirteen requirements and the four planning files agree.
- Every requirement of the phase carries a dated sentence saying whether it stands and on what; the four waits and the two conditional plans are said.
- The completion marks, all four, for this plan and, re-read, for every plan before it: phase 13's progress row, each plan's `- [ ]` line in the phase's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-01 to GAP-13's lines and traceability rows.
- The phase's full gate green (task 3), after a red and green pair on this branch if its first run was red.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-51-SUMMARY.md` when done
</output>
