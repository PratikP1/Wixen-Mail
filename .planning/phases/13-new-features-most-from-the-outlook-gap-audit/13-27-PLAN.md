---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 27
type: execute
wave: 29
depends_on: [13-26]
files_modified:
  - src/application/directory_sign_in.rs
  - src/application/mod.rs
  - src/presentation/wx_account_manager.rs
  - src/presentation/scan_target.rs
  - .github/workflows/accessibility.yml
  - tests/account_edit_protocol_fields.rs
  - tests/the_directory_sign_in_has_a_window_of_its_own.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/privacy.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-07]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Account Manager has \"&Look People Up at Work...\" (Alt+L), which opens a window for the chosen account holding the directory address, where in it to look, the sign-in name and the password, one job in one window (decision 1, GAP-07's \"its sign-in kept in the credential store\" reachable by a person)."
    - "The password box is empty when a password is saved and a sentence beside it, on both channels, says one is saved and that typing a new one replaces it; saving with a sign-in name and an empty box keeps the saved password; saving with no sign-in name forgets it; the password is written through 13-26's functions and never into the settings."
    - "The directory address and where to look leave the account editor's second page, so that page's letters Y and H are free again, and the settings guard that says the directory is offered by the account screen stays green because the window lives in the same file."
    - "Every label in the new window has its own letter, every field is named at the handle that takes focus on MSAA and on UI Automation, and the window is a scan target the Accessibility workflow reads."
    - "The window says, where the person reads it, that looking people up in a directory has not been tried against a real directory yet."
  artifacts:
    - "src/application/directory_sign_in.rs: what_the_window_keeps(address, look_in, sign_in_as, typed_password, a_password_is_saved) -> Keep { directory, password: Keep | Replace | Forget } and the window's sentences, pure, with rows."
    - "src/presentation/wx_account_manager.rs: the button on the Account Manager, the window, its save through remember_where_to_look_people_up and the directory store functions; the page-two boxes removed."
    - "tests/the_directory_sign_in_has_a_window_of_its_own.rs: the built window read for letters, names on MSAA and the saved-password sentence; the Account Manager read for the button's letter; each with a companion that plants a clash."
    - "src/presentation/scan_target.rs and .github/workflows/accessibility.yml: the directory-sign-in target."
  key_links:
    - "Account Manager -> &Look People Up at Work... -> the window for the chosen account -> directory_sign_in::what_the_window_keeps -> remember_where_to_look_people_up (settings, directories) and directory::keep_the_password or forget_the_password (credential store) -> finding_people::the_organisation reads both on the next lookup."
---

<objective>
A window where a person types the directory sign-in, reached from the
Account Manager, with the password kept where 13-26 keeps it.

Purpose: the rest of GAP-07's LDAP half: "its sign-in kept in the
credential store" reachable by somebody using the program. RESEARCH-3
found the account editor's second page has only J, Q and Z left for
letters and that a third page cannot have a lettered Next, so question 5
offered a window of its own; that recommendation was taken. The
directory address and where to look move there too, so one window does
one job.

Output: the pure save decision, the window, the button, the boxes moved
off page two, the reading target, the scan target, the pages, the
changelog and the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-26-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. A window of its own from the Account Manager (RESEARCH-3 question
5, option a).** Holding the address, where to look, the sign-in name and
the password; the two boxes leave page two.

**2. The button's letter is L, not K (planner's choice, recorded).**
RESEARCH-3 said K is free on the Account Manager; so is L, the label's
first letter (premise 2's grep), and a first letter is the one somebody
guesses.

**3. No Forget button (planner's choice).** Clearing the sign-in name and
saving forgets the password; an empty password box with a name keeps
the saved one. One rule a person can hear in the box's description.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. 13-06 (K5b, the history in every dialog) edits
`wx_account_manager.rs` before this plan; re-take every number and the
letter greps after 13-26's merge.

**1. What 13-26 left for this plan.** 13-26's ledger entry says nothing
writes a directory password outside tests until this plan; this plan's
save writes it and closes that entry in both halves.

**2. The Account Manager's letters and the new button's.**

```
sed -n 149,240p src/presentation/wx_account_manager.rs | grep -v '^\s*//' | grep -oE '"[^"]*&[A-Za-z][^"]*"'
   -> "&Add Account...", "&Edit...", "&Delete", "Set Acti&ve", "Set as Defa&ult", "&Sign In Again", "&Close": A E D V U S C; L and K both free
grep -n 'Alt+L' docs/KEYBOARD_SHORTCUTS.md   -> :163 the Due Now window's Dismiss, :699 the Tools menu's Folders to Keep Up to Date, :705 page two's Use TLS and Leave mail on the server, :1120 a Cancel; none in the Account Manager's window
```

A mnemonic is per window, so L on the Account Manager meets neither.
The new window's letters are its own: "&Directory address:" D, "&Where
in it to look:" W, "Sign-in &name:" N, "&Password:" P; OK and Cancel
carry none. The executor re-reads the built window's letters in the new
target, which fails on a clash.

**3. The page-two boxes and what reads them.**

```
grep -n 'directory_url\|directory_base\|directory_section_heading\|remember_where_to_look_people_up\|the_directory_this_account_names' src/presentation/wx_account_manager.rs
   -> the struct fields :942-946 and :1254-1256, show/hide :974-978, the builders :1930-1944, the fill :2081-2086, the save :1428-1429, the helpers :1315-1370
grep -n 'directory_url_f\|directory_base_f' tests/account_edit_protocol_fields.rs   -> :189-190 and :288-295, the fields the reading expects shown on page two
sed -n 1864p src/presentation/wx_account_manager.rs   -> a comment saying Directory address gave A up for Enable this account, which goes stale when the box leaves
grep -n '("directories", "src/presentation/wx_account_manager.rs")' src/data/config.rs   -> :2737 in OFFERED_BY_ANOTHER_SCREEN, read by test_a_setting_said_to_be_offered_elsewhere_really_is
```

Because the new window lives in `wx_account_manager.rs`, the settings
guard keeps reading `directories` there and `config.rs` is not edited;
moving the window to its own file would redden that guard and need an
edit to `config.rs`, which phase 12's cost rule keeps out of reach.

**4. The scan list.**

```
sed -n 176,183p .github/workflows/accessibility.yml   -> the targets; the two records on this file anchor on the 'which-days' line and the 'contact-editor' line, so the new name goes on the last line, 'account-details', 'phone-number', 'page', 'page-window', which no record anchors
grep -n 'pub const ALL: \[ScanTarget' src/presentation/scan_target.rs   -> :229, 40 at 630e2a67; earlier plans in this phase may have added targets, so the count is re-read
```

**5. Guard records, both readings.** `wx_account_manager.rs` 9 by
`tests_last_seen` and 8 by `file`, 14 tests, no test added; by anchor
the eight records sit at `:477`, `:600` (two), `:768`, `:1083`, `:1678`,
`:1760` and `:1776`; the boxes removed and the button added touch none
of those lines. `tests/account_edit_protocol_fields.rs` 1 and 0, 1
test, rewritten in place (the record at `:1678` names its test, so it is
re-measured). `scan_target.rs` 4 and 1, 11 tests, no test added; its
anchor is the `SignatureEditor` name line, not touched.
`.github/workflows/accessibility.yml` 0 and 3; the edit is on the last
line of the list. The workflow file makes that commit's hook answer
`all`. `directory_sign_in.rs` and the new target new at zero.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: what the window keeps, decided as a pure function</name>
  <files>src/application/directory_sign_in.rs, src/application/mod.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_account_manager.rs:1315-1370` (`the_directory_this_account_names`, `remember_where_to_look_people_up`: both boxes empty removes the entry).
    - `src/service/directory.rs`, 13-26's store functions and `Directory`.
  </read_first>
  <behavior>
    - `what_the_window_keeps(address, look_in, sign_in_as, typed_password, a_password_is_saved) -> Keep { directory: Option<Directory>, password: PasswordChange }`: both address and look-in empty removes the directory and forgets the password; a sign-in name with a typed password replaces it; a name with an empty box keeps the saved one; a name with an empty box and nothing saved is refused with the sentence 13-26's lookup says, before anything is written; no name forgets any saved password; values trimmed.
    - The sentences: the saved-password description, "A password is saved for this directory. Type a new one to replace it, or clear the sign-in name to forget it."; the experimental line, "Looking people up in a directory has not been tried against a real directory yet."
  </behavior>
  <action>
    Red first, on the branch `13-27-directory-sign-in-window`: the rows by
    module path (`application::directory_sign_in::tests::<name>`), a new
    module at zero records. Then the module (per decision 3), with
    `cargo build` between. Invoke `elegant-code`: one function, one
    return shape, no control.

    Guard record: one whose break keeps the saved password when the name
    is cleared, the forget row red; measured.
  </action>
  <verify>
    <automated>cargo test --lib application::directory_sign_in::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the rows by module path.
    - The module passes with at least 7 tests, quoted; one guard record measured.
  </acceptance_criteria>
  <done>What saving the directory window writes, keeps or forgets is decided in one tested place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the window, the button, and the boxes moved off page two</name>
  <files>src/presentation/wx_account_manager.rs, tests/the_directory_sign_in_has_a_window_of_its_own.rs, tests/account_edit_protocol_fields.rs, src/presentation/scan_target.rs, .github/workflows/accessibility.yml, guards/guards.toml, docs/KEYBOARD_SHORTCUTS.md</files>
  <read_first>
    - `src/presentation/wx_account_manager.rs:149-300` (the Account Manager and how Edit acts on the chosen account), `:1250-1260`, `:1315-1440`, `:1925-2090` (premise 3), and `describe_password_box` at `:779`.
    - `tests/account_edit_protocol_fields.rs:1-200` (how a built dialog's children are read over Win32 for letters and visibility).
    - `src/presentation/scan_target.rs:66-140` and `:229-330`, and how the `blocked-senders` target opens its window.
  </read_first>
  <behavior>
    - The new target builds the Account Manager and finds "&Look People Up at Work..." with L claimed once; builds the window for an account and reads each label's letter once (D, W, N, P), each field named on MSAA through `AccessibleObjectFromWindow` at the handle that takes focus, the password box with `ES_PASSWORD`, and, with a password saved in the store double, the saved-password sentence as the box's description and as visible text; a companion plants a second `&D` and requires the reading to name it.
    - `tests/account_edit_protocol_fields.rs`'s page-two readings no longer expect the directory boxes and still hold every letter on the page to one control; the rewrite is in place, with a comment giving the date and the reason.
    - Behaviour: the button with no account chosen says what Edit says; with one chosen it opens the window filled from the settings and the store; OK applies `what_the_window_keeps` through `remember_where_to_look_people_up` and 13-26's store functions and closes; a refusal keeps the window open with the sentence said and shown, focus on the field it names.
    - `ScanTarget::DirectorySignIn` named `directory-sign-in`, opened on a made-up account the way other targets open on fixtures, and the name on the workflow's last list line.
    - The shortcuts page: the Account Manager's `Alt+L`, the window's four letters, and page two's paragraph without `Alt+Y` and `Alt+H`, in the commit that changes them.
  </behavior>
  <action>
    Red first: the new target's readings bare, and the rewritten
    `account_edit_protocol_fields` case bare. Then, with `cargo build`
    between: the button (per decisions 1 and 2), the window built in
    `wx_account_manager.rs` (premise 3's reason), the page-two boxes,
    their struct fields, show and hide, fill and save removed, and the
    stale comment at `:1864` corrected; the scan target and the
    workflow line. Invoke `elegant-code`: the window reads and writes
    through the two helpers that already exist and 13-26's functions;
    nothing new holds a password but the box.

    The Accessibility scan reads the window on the pull request; any
    finding against it is fixed on the branch before the merge.

    Guard records: one on `wx_account_manager.rs` whose break writes the
    password into the settings instead of the store, a reading red,
    `suite` the new target; one whose break drops the saved-password
    description, the reading red; the `:1678` record re-measured for its
    rewritten test; each measured.
  </action>
  <verify>
    <automated>cargo test --test the_directory_sign_in_has_a_window_of_its_own && cargo test --test account_edit_protocol_fields && cargo test --lib presentation::scan_target:: && cargo test --lib data::config:: && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the readings bare.
    - `grep -c '&Look People Up at Work...' src/presentation/wx_account_manager.rs` is 1; comments excluded, `grep -v '^\s*//' src/presentation/wx_account_manager.rs | grep -c 'Director&y address'` is 0; `grep -c 'directory-sign-in' .github/workflows/accessibility.yml` is 1.
    - The new target passes with at least 8 readings, quoted; `data::config::` at 68 with `test_a_setting_said_to_be_offered_elsewhere_really_is` green, quoted; the records measured.
  </acceptance_criteria>
  <done>A person can type a directory sign-in in a window of its own, the password goes to the credential store, and page two is back to letters it can keep.</done>
</task>

<task type="auto">
  <name>Task 3: the guide, the alpha page, the privacy page, the changelog and the ledger</name>
  <files>docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/privacy.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md:97-114` (Managing accounts) and `:704` onward (Composing Email), where "### Looking people up" goes.
    - `docs/privacy.md:266-285` as 13-26 left it.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide: under Composing Email, "### Looking
    people up": the People found list, a directory set in Look People Up
    at Work on the Account Manager, the sign-in and where the password is
    kept, the `ldaps://` rule, and that it is experimental; Managing
    accounts names the new button. The alpha page: a directory sign-in
    has never met a real directory. The privacy page: where the directory
    is set now. The changelog, `[Unreleased]`, Added and Changed: the
    window, the boxes moved off page two, the letters freed.

    Ledger, by hand, both halves: close 13-26's "nothing writes a
    directory password" entry as fixed here; one `unrun-verify` for the
    tester's ear (the button, the window in order, the saved-password
    sentence, a refusal); one `unrun-verify` for phase 14 (a sign-in
    saved here reaching a real directory). One documents commit on the
    branch before the merge carries the pages, the ledger, the summary
    and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Looking people up' docs/USER_GUIDE.md` is 1; `grep -c 'Look People Up at Work' docs/KEYBOARD_SHORTCUTS.md` is at least 1.
    - The ledger entries in both halves, 13-26's closed; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say where the directory sign-in is set and where its password lives, and what has not been proved is ledgered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a typed password to the credential store | a secret moving from a box to one owner |
| the window to the settings file | the address and the name, which are not secrets, stored in settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-27-01 | Information disclosure | a password written into the settings | high | mitigate | written only through `directory::keep_the_password`; a guard record breaks it the other way and a reading goes red |
| T-13-27-02 | Information disclosure | the saved password shown back in the box | high | mitigate | the box opens empty with a sentence saying one is saved; `ES_PASSWORD` read by the target |
| T-13-27-03 | Spoofing | an empty password sent as an anonymous bind | medium | mitigate | refused before anything is written, the sentence 13-26's lookup says |
| T-13-27-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the commit carrying the workflow line answers `all`, which is the gate's rule and not a hand run. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is shown and spoken (a button, a window, page two): push the branch, open a pull request, wait for CI, NVDA and Accessibility (the new scan target among them), then merge locally. Pushing `main` is Pratik's.
- Red commits name lib tests by module path and integration tests bare. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-07's LDAP half ("an LDAP directory with a bind, its sign-in kept in the credential store") has a named test or reading in the summary and is marked with the date; the Graph half waits for 13-28.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-07's `[D]` line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-27-SUMMARY.md` when done
</output>
