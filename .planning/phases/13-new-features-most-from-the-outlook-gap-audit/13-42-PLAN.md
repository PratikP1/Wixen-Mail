---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 42
type: execute
wave: 44
depends_on: [13-41, 13-24.1]
files_modified:
  - src/application/quick_steps.rs
  - src/presentation/wx_app.rs
  - src/presentation/ui_types.rs
  - tests/wired.rs
  - tests/a_quick_step_runs_over_the_selection.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-11]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Action, Quick Steps lists the steps of the account being worked in, in the manager's order, the first three with Ctrl+Shift+7 to Ctrl+Shift+9 beside them, then Manage Quick Steps...; it is rebuilt when the sidebar is read and after the manager closes, only when what it would say differs (GAP-11: named, with a key)."
    - "Choosing a step or pressing its key reads the selection at that moment with the step's reach, refuses above the Select All bound, refuses a message of another account and a folder or label the account no longer has by name, changing nothing, and otherwise carries the step's Outcome out through 13-24.1's runner and its gated paths, saying one sentence that names the step and the count, with one Confirmed signal (GAP-11: over the selection, on the pattern of 11-07's set commands; one sentence saying what it did)."
    - "A key past the account's last step is answered in words from the message list, and a step a newer version wrote is refused in its own words and never half run."
    - "Every surface that runs a step says it is experimental where it is chosen, the alpha page says what could go wrong, and the ledger carries the real-server proof for phase 14; 13-40's stub entry is closed; GAP-11 is ticked and #60 closed from the merge."
  artifacts:
    - "src/application/quick_steps.rs: the sentences for a folder or label gone and for another account's messages, with their cases."
    - "src/presentation/wx_app.rs: QuickStepsLoaded sent with the labels, rebuild_the_quick_steps_menu and put_the_quick_steps_on_the_menu, a block of ids, the range arm that runs a step by its place through 13-24.1's runner, and the key handler past the last step."
    - "src/presentation/ui_types.rs: UIUpdate::QuickStepsLoaded."
    - "tests/wired.rs: the steps' keys among the keys stated by counting."
    - "tests/a_quick_step_runs_over_the_selection.rs: the built menu bar read after steps load and after a move; the key measured on a built list; source readings that a run reads the selection with the step's reach, meets the bound, checks the account and what the step names, calls the runner once and says one sentence; each with a planted-fault companion."
  key_links:
    - "Ctrl+Shift+7 or the item -> ID_QUICK_STEP_FIRST + place -> the active account's StoredStep -> quick_steps::reach -> chosen_messages -> too_many -> owner_of per message -> the folder and label resolved -> 13-24.1's runner with the step's Outcome -> WhatWasDone -> quick_steps::what_a_step_did -> one sentence, one Confirmed."
    - "folder_tree_updates -> UIUpdate::QuickStepsLoaded -> the state's steps and put_the_quick_steps_on_the_menu: one order for the manager, the menu and the key."
---

<objective>
Quick Steps on the Action menu and their keys, run over the selection.

Purpose: #60: "run on demand over the selected messages (which #30 makes a
set) ... a bound key from a small reserved range, said in
`docs/KEYBOARD_SHORTCUTS.md`; the announcement naming the step and how many
messages it touched." GAP-11's `[D]` line: "run by hand over the selection,
named, with a key, on the pattern 11-07's set commands use; one sentence
saying what it did." 13-40 stored steps, 13-41 made them, and 13-24.1 built the
one runner that carries several actions out over a set through the gated
paths (RESEARCH-4 F3; the cross-check moved it ahead so a block could use it
too). This plan joins them, and closes GAP-11 on the tree side.

Output: the sentences for what cannot run, the submenu rebuilt from the
steps, the keys, the run, the reading, the pages, the alpha page's line, the
changelog rewritten from 13-41's, the ledger, GAP-11 ticked, #60 closed.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-24.1-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-40-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-41-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only, and against the
plans of 13-23, 13-24, 13-24.1, 13-40 and 13-41 as designed that day. Command first,
then what it said. Line numbers are places to start; 13-01 to 13-41 land
first.

**1. What this plan closes on the ledger.** 13-40 files a `stub` entry, `file`
`src/application/quick_steps.rs`, saying Quick Steps are stored and nothing
runs them, for this plan to close. Its number is in 13-40's summary; this
plan closes it by hand in both halves in its documents commit, and 13-41's
changelog entry, which says running a step is not built yet, is rewritten.

**2. The runner this plan calls and never writes.** RESEARCH-4 AUT-6 and the
cross-check's numbering put one runner in 13-24; the planner split AUT-6 at
its task boundary on 2026-09-24, so 13-24 builds the quiet do-halves and
13-24.1 builds the runner: carry a settled `Outcome` out over a `Chosen`
set, the gate met once per account before anything changes, flags and
labels first, then the phrase, then the move or the delete, through the
paths the set commands of 11-07 use (`spawn_server_change` at
`wx_app.rs:23529`, `move_or_copy_here_first` at `:21308`, the delete arm at
`:5304`), the folder resolved with `the_folder_a_rule_names`
(`mail_sync.rs:1241`) and the label with 13-23's function. 13-24.1's plan
names it `run_these_actions_over(app, list, cache, chosen, outcome) ->
Result<WhatWasDone, String>` in `wx_app.rs`, beside the pure
`application::acting_on_a_set` (`what_each_message_needs`, `WhatWasDone`,
`said(chosen, done)`), and says the runner speaks no sentence and gives no
signal. The executor reads 13-24.1's summary for the names as built before
the first commit; where the summary's name differs from the plan's, the
summary's is the one every criterion here follows. This plan calls it once
per step and says one sentence, `quick_steps::what_a_step_did(name, done)`
over the `WhatWasDone` it answers, with one `Confirmed` signal (guardrail
5).

**3. How the set commands read the selection and refuse.**

```
grep -n 'fn chosen_messages\|fn toggle_read_state\|fn owner_of' src/presentation/wx_app.rs   -> :10899 (the selection read off the control at the key, with a reach), :11008 (the shape: chosen_messages, the empty refusal with at_least_one_chosen(Thing::MESSAGE) at :11030-11032, too_many at :11033-11035)
Read src/presentation/wx_app.rs:21098-21116   -> owner_of(&s.messages, &s.accounts, row_id, active) gives a message's account, and a row naming no account is refused
grep -n 'pub fn reach_for\|pub fn too_many' src/application/choosing_messages.rs   -> :160, :308; 13-40's quick_steps::reach is the narrowest of a step's commands
```

A step is its account's, and its folder and label are that account's; so a
selection holding a message of another account (All Inboxes spans several)
is refused before anything changes, by name, rather than run over a folder
of the same path somewhere else (RESEARCH-4's threat register).

**4. The menu, the keys and where steps load.** 13-41 built `let
quick_steps_menu = Menu::builder()` with "&Manage Quick Steps..." and
`ID_QUICK_STEP_MGR`, whose arm reads the tree back after the manager closes.
`folder_tree_updates` (`wx_app.rs:12638`) sends `LabelsLoaded` for the active
account (`:12736-12742`), and its arm calls `put_the_labels_on_the_menu`
(`:19389-19392`); `QuickStepsLoaded` is sent beside it and handled the same
way, so a step made, renamed or moved in the manager reaches the menu on the
read that follows. The labels' rebuild and key handler (`:10594-10710`) and
13-38's saved-search versions are the shape. The record "the Label menu is
rebuilt from the labels an account has when they load" anchors
`    if shown != wanted {` (`:10697`); the comparison here is spelled with its
own names, as 13-38's is. `menu_ids!`'s `NAME[count]` (`:95`) reserves the
block; `id >= ` ranges are read as handled by `tests/wired.rs:207-214`.

**5. The keys, searched.** Ctrl+Shift+1 to Ctrl+Shift+6 are the modules
(`wx_app.rs:6971-6996`, the page's `:31-36`); Ctrl+Shift+7 to Ctrl+Shift+9 bind
nothing in `src/presentation` and are written nowhere in
`docs/KEYBOARD_SHORTCUTS.md` (`grep -rnoE '(Ctrl\+Shift|Alt|Ctrl\+Alt|Alt\+Shift)\+[0-9]\b' src/presentation docs/KEYBOARD_SHORTCUTS.md`
at `630e2a67`; 13-38 took Alt+4 to Alt+9). `tests/wired.rs`'s
`test_the_shortcuts_document_names_no_key_the_code_has_never_heard_of`
(`:965`) accepts a key built by counting only through `stated` (`:986-989`),
which 13-38 extended for saved searches and this plan extends for steps. The
labels' list handler ignores a key with Shift down
(`answer_the_label_keys_the_menu_cannot`, `:10649`), so Ctrl+Shift and a
digit reaches nothing but the menu today; whether it reaches a `KEY_DOWN`
handler on a built list is measured first (task 2), as 13-38 measured Alt.

**6. The gate for each file, at `630e2a67` and as 13-40 left them.**
`src/application/quick_steps.rs` 13-40's count and records (at least one),
cases added with the count check named bare; `src/presentation/wx_app.rs`
199 and 119, none added; `src/presentation/ui_types.rs` 79 and 6, a variant
and no test; `tests/wired.rs` 77 and 19, a list grown in a test body and no
test added; the new target at zero. Re-taken before the first commit,
before and after quoted.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation, or settled by Pratik
the same day; Pratik may overrule.**

1. **Keys (RESEARCH-4 question 1): Ctrl+Shift+7 to Ctrl+Shift+9**, the first
   three steps of the account by place.
2. **Action, Quick Steps, with Q (question 7)**, the steps above Manage
   Quick Steps..., settled by Pratik on 2026-09-24.
3. **Per account (question 9).**
4. **The runner is 13-24.1's (question 12)**, built once and called here.
5. **The planner's: a selection holding another account's message is
   refused, not narrowed**, because a step that quietly did half of what was
   chosen is the half-done run the research warns against.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the sentences for a step that cannot run as it is</name>
  <files>src/application/quick_steps.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/quick_steps.rs`, whole, as 13-40 left it.
    - `src/application/status_sentences.rs:100-150` (the refusal shapes the set commands say).
  </read_first>
  <behavior>
    - `what_is_gone(step, missing: Missing) -> String` with `Missing::Folder(path)` and `Missing::Label(name)`: "Archive and read moves mail to Archive, which this account no longer has. Nothing was changed. Edit the step in Manage Quick Steps." and the label's sentence in the same shape.
    - `not_this_accounts(step, account, elsewhere) -> String`: "Archive and read belongs to Work, and 2 of the chosen messages are in another account. Nothing was changed. Choose messages in Work.", the singular right.
    - `written_by_a_newer_version(step) -> String`, for a step this build cannot read, saying it was not run and can be moved or removed in the manager.
    - Cases: each sentence names the step, says nothing was changed, and says what to do; the singular and plural of the count.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path
    (`application::quick_steps::tests::<name>`) and the count check bare,
    since 13-40's records name the file, with the signatures answering
    nothing. Run them and the count check first.

    Then the green commit. Invoke `elegant-code` and `writing-craft` for the
    sentences: what happened, to what, what to do next.

    Guard record: none new here; every record the count check flags is
    re-measured in one `--remeasure` call as the first run.
  </action>
  <verify>
    <automated>cargo test --lib application::quick_steps:: && cargo test --test house_style test_every_guard_record_says_how_many_tests_the_files_it_names_held</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the count check bare.
    - `grep -c 'pub fn what_is_gone' src/application/quick_steps.rs` is 1; `grep -c 'pub fn not_this_accounts' src/application/quick_steps.rs` is 1.
    - `cargo test --lib application::quick_steps::` passes with more tests than before, both counts quoted; every flagged record re-measured and quoted.
  </acceptance_criteria>
  <done>A step that cannot run as it stands is refused in a sentence naming it, saying nothing changed and what to do.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the steps on the submenu, their keys, and the answer past the last</name>
  <files>src/presentation/wx_app.rs, src/presentation/ui_types.rs, tests/wired.rs, tests/a_quick_step_runs_over_the_selection.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:86-120`, `:240-300`, `:10548-10711`, the saved-search menu functions 13-38 wrote, the Quick Steps submenu 13-41 wrote, `:12638-12745`, `:19385-19396`, `:3760-3780` (where the labels' list handler is bound).
    - `src/presentation/ui_types.rs:680-705`.
    - `tests/the_saved_searches_menu_says_the_searches_an_account_has.rs` (13-38), for the key measurement and the menu readings, and `tests/wired.rs:960-1000`.
  </read_first>
  <behavior>
    - Measured first, in the target's process: a `KEY_DOWN` handler bound on a built `ListCtrl` with the real binding function, `WM_KEYDOWN` for `7` posted with Ctrl and Shift held, and whether the handler saw both and 7; the summary and the head comment quote the answer, and the handler is bound where the key arrives.
    - `UIUpdate::QuickStepsLoaded(Vec<StoredStep>)`, sent by `folder_tree_updates` for the active account beside `LabelsLoaded`; its arm stores the steps in the window's state and calls `put_the_quick_steps_on_the_menu(frame, names)`.
    - `menu_ids!` reserves `ID_QUICK_STEP_FIRST[QUICK_STEPS_ON_THE_MENU]`, fifty; `rebuild_the_quick_steps_menu(quick_steps_menu, names)`, its parameter named as 13-41's builder is so `tests/wired.rs`'s letter check still reads its items, deletes every item and appends one per `quick_steps::what_the_menu_says` line under the id at its place, then, when there was a step, a separator, then "&Manage Quick Steps..." as 13-41 wrote it; 13-41's builder calls it with no names; `put_the_quick_steps_on_the_menu` finds the submenu by `ID_QUICK_STEP_MGR` and rebuilds only when the items before the separator differ, the comparison spelled with its own names (premise 4).
    - `a_quick_step_key_the_menu_does_not_answer(menu_bar, position)`, public; `answer_the_quick_step_keys_the_menu_cannot(list, frame, answer)`, public, bound on the message list: Ctrl and Shift with 7, 8 or 9 and no Alt, whose item the menu does not have, consumed and answered with `quick_steps::nothing_there` through the status line and an announcement at Normal; every other key left to the list.
    - `tests/wired.rs`: the `stated` loop gains one over `quick_steps::key_for` up to `quick_steps::REACHABLE_BY_KEY`, in the same test body; no test added.
    - The target: a store in the test with an account holding four steps, one written by a newer version; the real menu bar; the submenu read as the window starts (Manage Quick Steps only), after the steps are put on it (`Archive and read\tCtrl+Shift+7` and on, the fourth with no key), after a move written through 13-40's store and the steps put again, and after the same steps put again (left as it was, a marker on the first item surviving); `a_quick_step_key_the_menu_does_not_answer` for 3 with two steps; the key measurement as a reading; a source reading that `folder_tree_updates` sends `QuickStepsLoaded` and its arm calls `put_the_quick_steps_on_the_menu(`; each with a planted-fault companion.
  </behavior>
  <action>
    Red first, on the same branch: the target's menu readings bare, with the
    public signatures answering nothing. Run the target first and name
    everything that fails.

    Then the green commits, `cargo build` between: the update and its arm;
    the ids, the rebuild and the comparison; the key handler; the
    `tests/wired.rs` list with the first, since task 4's page rows would
    otherwise read as invented keys. Invoke `elegant-code`.

    Guard records: one on `wx_app.rs` whose break drops the call from the
    `QuickStepsLoaded` arm, its reading red, `suite` the target; one on
    `wx_app.rs` whose break gives the key handler Alt instead of Shift, the
    key reading red, `suite` the target; each measured.
  </action>
  <verify>
    <automated>cargo test --test a_quick_step_runs_over_the_selection && cargo test --test wired && cargo test --lib presentation::wx_app:: && cargo test --lib presentation::ui_types::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's menu readings bare.
    - `grep -c 'UIUpdate::QuickStepsLoaded' src/presentation/wx_app.rs` is at least 2; `grep -c 'fn rebuild_the_quick_steps_menu' src/presentation/wx_app.rs` is 1; `grep -c 'ID_QUICK_STEP_FIRST\[QUICK_STEPS_ON_THE_MENU\]' src/presentation/wx_app.rs` is 1; `grep -cx '    if shown != wanted {' src/presentation/wx_app.rs` is 1; `grep -c 'quick_steps::key_for' tests/wired.rs` is 1.
    - `cargo test --test wired`, `presentation::wx_app::` and `presentation::ui_types::` pass with the counts they had before, quoted; the key measurement's answer is in the summary.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The Quick Steps submenu says the account's steps in their order with the keys that run them, rebuilt on each read without emptying an open menu, and a key past the last is answered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: a step runs over the selection, through the one runner, in one sentence</name>
  <files>src/presentation/wx_app.rs, tests/a_quick_step_runs_over_the_selection.rs, guards/guards.toml</files>
  <read_first>
    - 13-24.1's summary, whole, for the runner's name, what it answers (a `WhatWasDone`; it speaks no sentence), and how its gate is met; the runner's own code where the summary points.
    - `src/presentation/wx_app.rs:10880-10975` (`chosen_messages`), `:11008-11040`, `:21040-21160` (a set move: the account per message, the gate), and the saved-search range arm 13-38 wrote.
    - `tests/every_command_acts_on_the_selection.rs`, its readings and companions over `what_ships`.
  </read_first>
  <behavior>
    - The range arm, `_ if id >= ID_QUICK_STEP_FIRST && id < ID_QUICK_STEP_FIRST + QUICK_STEPS_ON_THE_MENU`, calls `run_the_quick_step_at(app, cache, list, a11y, place)`: the active account's `StoredStep` at that place (a `WrittenByANewerVersion` refused with `written_by_a_newer_version`); `quick_steps::reach(does, setting)` with the D-07 setting read where the delete reads it; `chosen_messages` with that reach; empty refused with `at_least_one_chosen(Thing::MESSAGE)`; `too_many`; every chosen message's account through `owner_of`, another account's refused with `not_this_accounts`; the step's folder and label resolved in its account the way 13-24.1's runner resolves them, a missing one refused with `what_is_gone`; then 13-24.1's runner with the step's `Outcome`, once, answering a `WhatWasDone`; then `quick_steps::what_a_step_did(name, done)` over that answer to the status line and announced once at Normal, with one `Confirmed` signal. The runner speaks no sentence and gives no signal (13-24.1's plan), so the step's sentence is the only one said.
    - The key reaches the same function through its menu item's accelerator; the past-the-last answer is task 2's.
    - Undo: the executor reads 13-24.1's and 13-08's summaries for whether the runner's writes join Edit, Undo; the target reads the answer in source (the runner's body calls the undo record 13-08 names, or does not) and the summary and task 4's pages say what is true. This plan builds no undo of its own.
    - The target gains source readings over `what_ships`: `run_the_quick_step_at` calls, in order, `quick_steps::reach(`, `chosen_messages(`, `too_many(`, `owner_of(`, the runner once, and `what_a_step_did(`; it calls no path of its own that writes mail (no `spawn_server_change(`, no `move_or_copy_here_first(`, no `set_says_first(` in its body, since those belong to the runner); the range arm calls it; each with a planted-fault companion (the bound skipped, the account check skipped, the runner called twice, a second sentence), each refused.
  </behavior>
  <action>
    Red first, on the same branch: the target's run readings bare, with
    `run_the_quick_step_at` present and answering nothing. Run them first
    and name everything that fails.

    Then the green commit, `cargo build` between. Invoke `elegant-code`: the
    function is a line of checks each ending in a sentence, then one call
    and one sentence; no second runner, no write of its own; no `unwrap`.

    Guard records: one on `wx_app.rs` whose break skips `too_many`, its
    reading red, `suite` the target; one whose break skips the account
    check, its reading red, `suite` the target; each measured, with every
    flagged record, in one `--remeasure` call per commit.
  </action>
  <verify>
    <automated>cargo test --test a_quick_step_runs_over_the_selection && cargo test --test every_command_acts_on_the_selection && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's run readings bare.
    - `grep -c 'fn run_the_quick_step_at' src/presentation/wx_app.rs` is 1 and `grep -c 'run_the_quick_step_at(' src/presentation/wx_app.rs` is at least 2; `grep -c 'what_a_step_did(' src/presentation/wx_app.rs` is 1.
    - The target passes with at least 14 tests, quoted; `every_command_acts_on_the_selection` and `presentation::wx_app::` with the counts they had before, quoted; the summary says whether a step joins Undo, read from source.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>A Quick Step runs over the selection through the one runner and its gate, refuses what it cannot do whole, and says one sentence naming itself and the count.</done>
</task>

<task type="auto">
  <name>Task 4: the pages, the alpha page, the changelog rewritten, the ledger, GAP-11 ticked, and #60 closed</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md`, the Action Menu section and the Quick Step sections 13-41 wrote; `:23-37` (Module Navigation, Ctrl+Shift+1 to 6).
    - `docs/USER_GUIDE.md`, the Quick Steps section 13-41 wrote; `docs/ALPHA_TESTING.md:102-160` and `:245-300`.
    - `docs/changelog.md`, 13-41's Quick Steps entry.
    - 13-40's summary for its ledger entry's number.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: a Quick Steps table with
    Ctrl+Shift+7 to Ctrl+Shift+9 and what they run, one sentence that the
    keys follow the order in the Quick Step Manager, and the Action Menu row
    naming the steps above Manage Quick Steps. The guide: running a step,
    the one sentence it says, what refuses it and why (another account's
    messages, a folder or label gone, more than the Select All bound), and
    whether Undo takes a step back, as task 3 read it. The alpha page, under
    what is unproven: a step reaches the server through Allow Changes and
    has never met a real one, in the words of `QUICK_STEPS_ARE_EXPERIMENTAL`.
    The changelog: 13-41's entry rewritten, not added to, so it says steps
    are made and run, in #60's words; Known limitations: no step has met a
    real server, nobody has heard one run, and Undo as task 3 read it.

    Ledger, by hand, both halves: 13-40's `stub` entry closed; one
    `unrun-verify` for phase 14, a step that marks, flags, labels and moves
    reaching a real IMAP server in that order, on Gmail (where a move is a
    label change) and on a server with folders; one `unrun-verify` for the
    tester's ear (the submenu with keys, a step heard as one act with one
    sentence, each refusal); and, if task 3 read that a step does not join
    Undo, one `todo` saying so for whoever builds Undo next.

    GAP-11: its box and traceability row ticked with the date, and the dated
    line under its `[D]` line naming the tests and readings for each part
    (13-40's modules, 13-41's target, this plan's target). The four marks:
    phase 13's progress row; this plan's `- [ ]` line in phase 13's plan list
    (ROADMAP.md; the phase README keeps no boxes) ticked; `STATE.md`'s plan number in
    the frontmatter and the body; GAP-11 as above. Run
    `cargo test --test the_planning_files_agree_with_themselves` first.

    This plan changes what is spoken and shown: after this one documents
    commit the branch is pushed with a pull request into `main` under
    Pratik's standing OK, CI, NVDA and Accessibility read before the local
    merge. After the merge, `gh issue close 60 --comment "..."` from the
    merge commit, naming what was built and what waits for a real account.
    Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Ctrl+Shift+7' docs/KEYBOARD_SHORTCUTS.md` is at least 1; `grep -c 'Quick Step' docs/ALPHA_TESTING.md` is at least 1; `grep -c '^- \[x\] \*\*GAP-11\*\*' .planning/REQUIREMENTS.md` is 1.
    - 13-40's entry reads fixed in both halves; the new entries in both halves; carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages name the keys and what a step says, the alpha page says what could go wrong, the ledger carries the real-server proof and the ear, GAP-11 is ticked, and #60 is closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a key to the server | one keystroke that can move, delete or flag many messages at a provider |
| a step's account to the selection's | a step names one account's folders and labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-42-01 | Elevation of privilege | a step moving mail on an account whose changes are off | high | mitigate | 13-24.1's runner meets `outward::permitted(allowed_for(account).mail, ...)` before anything changes; this plan calls nothing else that writes, read in the target |
| T-13-42-02 | Tampering | a step run over another account's messages into a folder of the same path | high | mitigate | every chosen message's account checked with `owner_of`, another account's refused whole; its reading and a guard record |
| T-13-42-03 | Tampering | half a step carried out because its folder or label is gone | medium | mitigate | resolved before the runner, refused by name with nothing changed |
| T-13-42-04 | Denial of service | a flood of announcements over a large set | medium | mitigate | one sentence and one `Confirmed` per step, whatever the count; the bound refused above Select All's |
| T-13-42-05 | Tampering | the key and the menu running different steps | medium | mitigate | one order, read by the menu and the range arm; the target reads both |
| T-13-42-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes what is spoken and shown: after the one documents commit, the branch is pushed with a pull request into `main` (Pratik's standing OK of 2026-09-23), CI, NVDA and Accessibility read on it, a red one fixed on the branch; the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What a real account settles, phase 14: a step's changes reaching a provider in order. What the tester's ear settles: the submenu and a step heard as one act. What the readings prove: the menu after loads and moves, the key reaching the list, the run's checks in order, one runner call, one sentence.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 11 closes structurally: Quick Steps, a named multi-action command on a key over the selected messages.
- GAP-11's `[D]` line has a named test or reading for each part and its box is ticked with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-11's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-42-SUMMARY.md` when done
</output>
