---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 33
type: execute
wave: 35
depends_on: [13-32]
files_modified:
  - src/data/message_cache/mod.rs
  - src/data/message_cache/identities.rs
  - src/data/message_cache/accounts.rs
  - src/application/identities.rs
  - src/application/mod.rs
  - src/presentation/wx_identities.rs
  - src/presentation/mod.rs
  - src/presentation/wx_managers.rs
  - src/presentation/wx_account_manager.rs
  - src/presentation/scan_target.rs
  - .github/workflows/accessibility.yml
  - tests/other_addresses_are_managed_per_account.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-10]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An account can hold other addresses to send from, each with the name people see, in an order the person chooses, stored in a new table added without touching any existing one (GAP-10's [D] line, first half; #59 step 1)."
    - "An address is refused, with a sentence, when it is not an address, when it is the account's own, or when the account already holds it, compared without case; nothing is written until it passes."
    - "The addresses go with their account: removing an account removes its other addresses."
    - "The Account Manager has \"&Other Addresses to Send From...\" (Alt+O), opening a manager for the chosen account on the manager loop every other manager uses, with Move Up and Move Down, and an address window of its own; an account not yet saved is refused with a sentence rather than given addresses nothing holds."
    - "The manager says, where the person reads it, that a provider may refuse or replace an address that is not set up there too, and that this has not been tried against a real provider."
  artifacts:
    - "src/data/message_cache/mod.rs and identities.rs: CREATE TABLE IF NOT EXISTS identities, and add, list in order, replace, remove, put in order and clear for an account, with the older-database case."
    - "src/application/identities.rs: Identity, what_stops_an_address_being_kept(address, account, held), the_from_list(accounts, identities) -> Vec<FromEntry>, and who_it_goes_out_from(account, from_address, from_name), pure, with rows."
    - "src/presentation/wx_identities.rs: the manager on run_manager_loop and the address window."
    - "tests/other_addresses_are_managed_per_account.rs: the built Account Manager and manager read for letters and names, the address window read on MSAA, each with a planted-clash companion."
  key_links:
    - "Account Manager -> &Other Addresses to Send From... -> wx_identities::show for the chosen, saved account -> application::identities::what_stops_an_address_being_kept -> data::message_cache::identities (the signatures cache handle the Account Manager already holds) -> the_from_list, which 13-35 builds the From list from."
    - "accounts::delete_account -> clear_identities(account)."
---

<objective>
Other addresses an account sends from, stored and managed.

Purpose: GAP-10's `[D]` line, "an identity (a From name and address)
per account beyond the first", and step 1 of the order #59 proposes.
RESEARCH-3 (C10) found one address per account in the tree and nothing
anywhere that holds a second. Sending from one is 13-34's data and
13-35's composer; a reply choosing one is 13-36's.

Output: the table and its module, the pure rules and the From list's
entries, the manager and its window, the Account Manager's button, the
reading target, the scan target, the shortcuts page, the changelog and
the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-27-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. An other address signs with its account's signature (RESEARCH-3
question 9).** No schema for a signature per address.

**2. Typed addresses first; Gmail's own "Send mail as" list later
(question 11).** Ledgered.

**3. The button is on the Account Manager, not the account editor's
first page (planner's choice, recorded).** RESEARCH-3 put "&Other
addresses to send from..." on the editor's first page. An account being
added in the editor has no row in the store yet (the Account Manager
saves accounts when it closes), so addresses given to it would have
nothing to belong to. On the Account Manager the button acts on a saved
account, beside 13-27's Look People Up at Work, and an account added in
the same visit is refused with a sentence.

**4. The manager uses the manager loop (planner's choice).** Its
letters are the loop's own (A, E, D, U, W, C), not RESEARCH-3's
"&Remove"; one shape for every manager.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. 13-27 adds a button to the Account Manager; 13-11, 13-12,
13-15, 13-16 and 13-21 add schema to `mod.rs` before this plan
(RESEARCH-CROSS-CHECK, "Schema"). Re-take after 13-32's merge.

**1. Where an account's address lives, and the Account Manager's letters.**

```
grep -n 'CREATE TABLE IF NOT EXISTS accounts\|CREATE TABLE IF NOT EXISTS identities' src/data/message_cache/mod.rs   -> accounts at :2299; no identities table
sed -n 259,310p src/data/message_cache/accounts.rs   -> delete_account: secrets first, then clear_account_cache, clear_drafts, clear_saved_searches, then the row
sed -n 149,240p src/presentation/wx_account_manager.rs | grep -v '^\s*//' | grep -oE '"[^"]*&[A-Za-z][^"]*"'   -> A E D V U S C at 630e2a67; 13-27 adds L; O free
sed -n 99,106p src/presentation/wx_account_manager.rs   -> show_account_manager_dialog takes signatures: Option<&MessageCache>, the handle this manager uses
sed -n 295,345p src/presentation/wx_managers.rs   -> run_manager_loop, private, with &Add..., &Edit..., &Delete, Move &Up, Move Do&wn, &Close
sed -n 534,545p src/presentation/wx_managers.rs   -> make_shell(parent: &Frame, ..): a Frame parent only
sed -n 195,205p src/application/links_in_text.rs   -> is_an_address(word), the one recogniser the snippet and the contact editor use
grep -n 'Several identities per account' docs/comparison.md   -> :45 "Not yet", :123 in the list of what is left (13-36 corrects both)
```

The executor re-runs the letter grep after 13-27's merge and before
writing the label; O must still be free, or the plan stops and says so.
`make_shell` takes a `Frame`, and this manager opens over the Account
Manager's dialog, so its parent parameter is widened to any window the
way the loop's callers need, and the summary says how.

**2. Guard records, both readings.** `mod.rs` 12 by `tests_last_seen`
and 12 by `file`, 23 tests, no test added (the older-database case goes
in the new data module); its anchors lie in existing tables, and the new
table is added after `saved_searches`' block without touching them.
`accounts.rs` 2 and 2, no test added (the identity-goes-with-its-account
case is in the new data module), anchors at `:200` and `:283`, the new
clear call placed beside `clear_saved_searches` below them.
`wx_managers.rs` 14 by `tests_last_seen`, 44 tests, no test added: the
two visibility changes do not touch the anchors, and the readings at
`:5468`, `:5482` and `:5550` search for the text `fn run_manager_loop`,
which `pub(crate) fn run_manager_loop` still contains.
`wx_account_manager.rs` as 13-27 left it, no test added. `scan_target.rs`
and the workflow as in 13-27's premise 4, the name on the list's last
line. The new modules and target at zero.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the identities table, its module, and the rules for an address</name>
  <files>src/data/message_cache/mod.rs, src/data/message_cache/identities.rs, src/data/message_cache/accounts.rs, src/application/identities.rs, src/application/mod.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/mod.rs:1937-1990` (the saved_searches tables, whose shape and placement this follows) and `src/data/message_cache/saved_searches.rs:1390-1420` (the older-database case).
    - `src/data/message_cache/accounts.rs:259-310`.
    - `src/application/links_in_text.rs:190-215`.
  </read_first>
  <behavior>
    - `CREATE TABLE IF NOT EXISTS identities (id TEXT PRIMARY KEY, account_id TEXT NOT NULL, address TEXT NOT NULL COLLATE NOCASE, sender_name TEXT NOT NULL DEFAULT '', position INTEGER, created_at TEXT NOT NULL, UNIQUE(account_id, address))`; a database written before the table existed opens and keeps every account.
    - `add_identity` places a new address last; `identities_for(account)` reads them by position then creation; `replace_identity`, `remove_identity`, `put_identities_in_order(account, ids)` in one transaction; `clear_identities(account)`, called by `delete_account` beside `clear_saved_searches`: a case deletes the account and reads nothing left.
    - `what_stops_an_address_being_kept(address, sender_name, account, held) -> Option<String>`: "That is not an email address." through `is_an_address`; "That is this account's own address." compared without case; "This account already sends from that address."; a name longer than the account name's bound refused; none for a good address.
    - `the_from_list(accounts, identities) -> Vec<FromEntry { account_id, address, sender_name, said }>`: each account's own entry first, said as today (its address), then its other addresses in order, said "help@example.com, another address on Work"; `who_it_goes_out_from(account, from_address, from_name) -> (String, Option<String>)`: the account's own address and name where none is given, the given ones otherwise.
  </behavior>
  <action>
    Red first, on the branch `13-33-other-addresses`: the cases by module
    path (`data::message_cache::identities::tests::<name>`,
    `application::identities::tests::<name>`), both new modules at zero
    records. Then the table (additive, per CLAUDE.md's schema rule), the
    data module, the clear call, and the pure module, with `cargo build`
    between (per decisions 1 and 2). Invoke `elegant-code`: the rules are
    one function answering one sentence or none; `FromEntry` carries
    what every consumer of the From list needs, so 13-35 maps nothing by
    position.

    Guard records: one on the data module whose break keeps an account's
    addresses after the account is deleted, the case red; one on the pure
    module whose break compares with case, the own-address case red;
    each measured with `scripts/guards.sh --remeasure`; the records
    naming `mod.rs` and `accounts.rs` re-measured if the count check
    names them.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::identities:: && cargo test --lib application::identities:: && cargo test --lib data::message_cache::accounts::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the cases by module path.
    - `grep -c 'CREATE TABLE IF NOT EXISTS identities' src/data/message_cache/mod.rs` is 1; `grep -c 'clear_identities' src/data/message_cache/accounts.rs` is 1.
    - The two new modules pass with at least 8 and 10 tests, quoted; `accounts::` unchanged in count, quoted; the records measured.
  </acceptance_criteria>
  <done>Other addresses are kept per account in an additive table, refused with a sentence when they cannot be used, removed with their account, and listed in the order the From list will read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the manager and its window, reached from the Account Manager</name>
  <files>src/presentation/wx_identities.rs, src/presentation/mod.rs, src/presentation/wx_managers.rs, src/presentation/wx_account_manager.rs, src/presentation/scan_target.rs, .github/workflows/accessibility.yml, tests/other_addresses_are_managed_per_account.rs, guards/guards.toml, docs/KEYBOARD_SHORTCUTS.md</files>
  <read_first>
    - `src/presentation/wx_managers.rs:230-345` (`ManagedRow`, `moved`, `run_manager_loop`), `:534-560` (`make_shell`) and `:4175-4300` (the label manager, the pattern with an order and a Key column).
    - `src/presentation/wx_account_manager.rs:149-300` as 13-27 left it (the Look People Up at Work button and its handler).
    - `tests/the_label_menu_says_the_labels_an_account_has.rs:380-440` and 13-27's target (a built manager and a built window read).
  </read_first>
  <behavior>
    - The target builds the Account Manager and finds "&Other Addresses to Send From..." with O claimed once; builds the manager over three addresses and reads the rows in order, the columns Address and Name people see, the loop's letters each once, and a move; builds the address window and reads "&Address:" and "The &name people see:" once each, each field named on MSAA at the handle that takes focus; a companion plants a second `&A` in the window and requires the reading to name it.
    - Behaviour: the button with no account chosen says what Edit says; with an account not yet in the store it says "This account is saved when the Account Manager closes. Close it, open it again, and then add other addresses."; otherwise the manager opens over the Account Manager, and Add and Edit open the address window, which refuses with task 1's sentence and keeps the window open with focus on the field; the order is written when the manager closes; the manager's status line carries the sentence that a provider may refuse or replace an address not set up there too and that this has not met a real provider.
    - `ScanTarget::Identities` named `identities`, opened on a made-up account with two addresses, and the name on the workflow's last list line.
    - The shortcuts page: the Account Manager's `Alt+O`, the manager's and the window's letters, in the commit that binds them.
  </behavior>
  <action>
    Red first: the target's readings bare. Then, with `cargo build`
    between: `run_manager_loop` and `make_shell` made `pub(crate)` and
    `make_shell`'s parent widened (premise 1), `wx_identities.rs` with the
    manager and the window (per decision 4), the button (per decision 3,
    O proven free by premise 1's grep re-run today), the scan target and
    the workflow line. Invoke `elegant-code`: the manager holds no rule
    task 1 holds; it reads, asks, and writes through the data module.

    Guard records: one on `wx_identities.rs` whose break skips the
    address rules on OK, a reading red, `suite` the new target; one on
    `wx_account_manager.rs` whose break opens the manager for an unsaved
    account, a reading red; each measured.
  </action>
  <verify>
    <automated>cargo test --test other_addresses_are_managed_per_account && cargo test --lib presentation::scan_target:: && cargo test --lib presentation::wx_managers:: && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the readings bare.
    - `grep -c '&Other Addresses to Send From...' src/presentation/wx_account_manager.rs` is 1; `grep -c "'identities'" .github/workflows/accessibility.yml` is 1.
    - The target passes with at least 8 readings, quoted; `presentation::wx_managers::` at 44, quoted; two guard records measured.
  </acceptance_criteria>
  <done>A person can add, edit, remove and order an account's other addresses from the Account Manager, with every letter its own and every field named.</done>
</task>

<task type="auto">
  <name>Task 3: the changelog and the ledger</name>
  <files>docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/changelog.md` from `[Unreleased]`, Added.
  </read_first>
  <action>
    Invoke `writing-craft`. The changelog, `[Unreleased]`, Added: other
    addresses per account, managed from the Account Manager, with the
    additive table named; Known limitations: they are offered in compose
    from the next change, and no provider has been asked to send from
    one. The user guide's section is 13-35's, when sending from one works,
    so this plan writes no guide text that promises it.

    Ledger, by hand, both halves: one `todo` that the addresses are kept
    and not yet offered in compose until 13-35 (named in 13-35's premise
    1); one `todo` for Gmail's own "Send mail as" list (decision 2). One
    documents commit on the branch before the merge carries the
    changelog, the ledger, the summary and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The changelog entry names the table; the two ledger entries in both halves; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>What is stored and what is not yet offered are both written down, the second where 13-35 reads it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a typed address to the store | text that later becomes a From header |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-33-01 | Spoofing | a From address the person does not own | medium | accept | the person types it for their own account; the provider decides whether it sends (A2, A3); the manager says so where it is read, and phase 14 reads a real provider |
| T-13-33-02 | Tampering | a header injection through a typed name or address | medium | mitigate | the address must pass `is_an_address`; the name is bounded and later written through the message builder that already quotes names |
| T-13-33-03 | Information disclosure | addresses outliving their account | low | mitigate | cleared in `delete_account`; a case |
| T-13-33-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the commit carrying the workflow line answers `all` by the gate's rule. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is shown and spoken (a button, a manager, a window): push the branch, open a pull request, wait for CI, NVDA and Accessibility (the `identities` target among them), then merge locally. Pushing `main` is Pratik's.
- Schema changes additive only. Red commits name lib tests by module path and integration tests bare. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-10's `[D]` line's first half has named tests in the summary; the line waits for 13-35 and 13-36 before it is marked.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-10's traceability row noting this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-33-SUMMARY.md` when done
</output>
