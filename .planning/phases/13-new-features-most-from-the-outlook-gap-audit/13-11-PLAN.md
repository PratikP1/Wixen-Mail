---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 11
type: execute
wave: 11
depends_on: [13-10]
files_modified:
  - src/application/answering.rs
  - src/application/answered_meetings.rs
  - src/application/invitations.rs
  - src/application/context_menu.rs
  - src/data/message_cache/calendar.rs
  - src/data/message_cache/mod.rs
  - src/presentation/reader_text.rs
  - src/presentation/wx_reader.rs
  - src/presentation/page_jumps.rs
  - src/presentation/wx_context_menu.rs
  - src/presentation/wx_app.rs
  - tests/the_invitation_is_answered_from_the_reader.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-04]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A message whose invitation can be answered shows three native buttons, A&ccept, &Tentative and &Decline, in the text reader's tab after the security bar and before the message, and in the formatted window after the page; each button's accessible description is what_pressing_it_will_do; Alt+C, Alt+T and Alt+D press them in both windows, in the formatted window through the page's own key listener (#84's route) (GAP-04, #50 points 4 and 7)."
    - "When it cannot be answered there are no buttons and the reason is in the bar; no button is ever shown greyed (#50 point 7)."
    - "The answer given is stored beside the version answered, as an enum at the SQL boundary in an additive column, so the reader says 'you accepted this version on ...'; a database written before the column loads with no answer recorded."
    - "Answering takes the message it was pressed on, not the list's selection, so a reader window whose message is not the selected row answers the right meeting; the menu and the context menu pass the selected row."
    - "The reply is queued with In-Reply-To and References naming the invitation's message (#50 point 8)."
    - "The message list's context menu on a message carrying a calendar part offers Accept &invitation, T&entatively accept invitation and Dec&line invitation, reaching the same three commands."
  artifacts:
    - "data/message_cache/calendar.rs and mod.rs: answered_with through ensure_column_exists, remember_the_answer and the_answer_given_here, the enum converted at the boundary."
    - "application/answering.rs and answered_meetings.rs: the answer path from a message row, the reply's threading headers, the answer filed."
    - "presentation/wx_reader.rs: the buttons and on_answer; presentation/wx_app.rs: the formatted window's buttons, the jump arms, the handlers passing the message; presentation/page_jumps.rs: Jump::Answer for Alt+C, Alt+T, Alt+D."
    - "application/context_menu.rs and wx_context_menu.rs: the three entries on a message carrying a calendar part."
    - "tests/the_invitation_is_answered_from_the_reader.rs: the built tab and window read over MSAA at each button's own handle, the keys posted and consumed, no buttons for a cancellation."
  key_links:
    - "a button or Alt+C -> on_answer(message row, Answer) -> answer_the_invitation(message) -> whether_it_can_be_answered -> the_answer_to_send with In-Reply-To and References -> the outbox and the gate -> remember_the_answer: one path from three doors."
    - "the page's keydown listener -> Jump::Answer -> the same button's handler: the key a sighted user reads on the button is the key the page listens for."
---

<objective>
Accept, Tentative and Decline as buttons in both reader windows, the answer
remembered, and the reply threaded.

Purpose: GAP-04's "with Accept, Tentative and Decline", and #50 points 4, 7
and 8 (the audit: `what_pressing_it_will_do` is unwired and the answer given
is not stored; no shortcut, no context menu entry; the reply carries no
`In-Reply-To`). 13-10 made the invitation said; this makes it answerable
where it is said.

Output: the stored answer, the answer path from a message, the threading
headers, the buttons in both windows, the page's keys, the context menu
entries, the target, the pages, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-10-SUMMARY.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said. 13-03 and 13-04 add printing to both reader windows and 13-07 may add
to the message context menu; re-take every grep below at the start of task
1 and quote the new lines and letters in the summary.

**1. The answer path today.**

```
grep -n "fn answer_the_invitation\|selected_message_index\|attachments_with_content" src/presentation/wx_app.rs | sed -n 1,40p
   -> answer_the_invitation at :14717 reads the selection (held.selected_message_index, :14746) and the stored parts (:14764); a reader window whose tab is not the selected row would answer the wrong message
grep -n "ID_ANSWER_ACCEPT\|ID_ANSWER_TENTATIVE\|ID_ANSWER_DECLINE\|Ans&wer Invitation" src/presentation/wx_app.rs
   -> ids at :292-294, the handler at :4593-4602, the submenu at :7012-7031 (&Accept, &Tentative, &Decline), "Ans&wer Invitation" on Action at :7396
grep -n "pub fn what_pressing_it_will_do\|pub fn whether_it_can_be_answered\|pub enum CannotAnswer" src/application/answering.rs
   -> :275, :164, :69: the sentence before pressing is called by tests only (#50 point 4)
grep -n "answered_version" src/data/message_cache/mod.rs
   -> :3182 ensure_column_exists and :4009 the fresh schema: the version is stored, the answer given is not
grep -n "in_reply_to\|references_header" src/data/message_cache/mod.rs
   -> outbox_queue carries both columns (:3111-3112 region); the meeting reply does not fill them (#50 point 8)
```

**2. The two windows and their keys.**

```
sed -n 425,470p src/presentation/wx_reader.rs   -> open(): the warning bar first when there is one, then the text, then a picture, then attachments; buttons go after the bar and before the text
sed -n 290,330p src/presentation/wx_reader.rs   -> the reader's menu bar: File (&Read Attachment, &Save Attachment, &Close Tab, Close &Window) and Go (&Next Message, &Previous Message, &Attachments Alt+A, &Find)
awk '/^### The Reader Window/,/^### The Preview Pane/' docs/KEYBOARD_SHORTCUTS.md | grep -o "Alt+[A-Za-z0-9]*" | sort | uniq -c   -> 6 Alt+A and nothing else: Alt+C, Alt+T, Alt+D are unclaimed in the reader's section; Alt+A is the attachments key, so Accept cannot be &Accept
sed -n 38,52p src/presentation/page_jumps.rs   -> the page listens for F7 and Alt+A only; with page_links.rs (:70-82: Ctrl on a link, Backspace, Alt+Left), no page script claims Alt+C, Alt+T or Alt+D
grep -n "the browser is the first thing in the page window that takes the keyboard" guards/guards.toml   -> a record, suite the_page_window_keeps_the_document_focused_when_it_comes_back: in the formatted window the browser must stay the first control that takes the keyboard
```

Read from the keyboard group's plans on 2026-09-24: 13-04 adds Ctrl+P to
`page_jumps::SCRIPT` as kind `print` with `Jump::Print`, and "&Print...\tCtrl+P"
to the reader's File menu (13-04's premise at its `:89-95`); neither claims
Alt+C, Alt+T or Alt+D, and task 3's `Jump::Answer` arm sits beside
`Jump::Print`'s.

So the research's "a row after the security bar and before the message" in
**both** windows is corrected for the formatted window: there the buttons
go after the page in the tab order, beside the attachment list, and are
reached by their keys from the page. #84 found the browser keeps every key
once it has focus, so a native mnemonic cannot be expected to fire from the
page; task 3 measures that first, and the page's listener is the route
whatever the measurement says, as it is for Alt+A.

**3. The message context menu's letters.**

```
sed -n 311,330p src/application/context_menu.rs   -> &Reply, Reply &all, &Forward, &Mark as (un)read, &Star or unstar, &Delete, Delete &permanently, Mo&ve to folder, Cop&y to folder, Copy to a &task, Copy to the &calendar, Copy to a &note: R A F M S D P V Y T C N; free: B E G H I J K L O Q U W X Z
grep -n "pub struct Entry" -A 4 src/application/context_menu.rs   -> label and action only: no submenus, so three entries, not one
```

Letters handed out here: **I** (Accept &invitation), **E** (T&entatively
accept invitation), **L** (Dec&line invitation), all free today; **J** stays
free for 13-22's Report as Junk. In both reader windows: **C**, **T**, **D**
on the buttons, free by the grep above.

**4. The gate for each file**, by the TOML reader on 2026-09-24:
`answering.rs` 37 tests, 1 and 1; `answered_meetings.rs` 17, 3 and 3;
`invitations.rs` 37, 0 and 0 (plus 13-10's cases); `context_menu.rs` 18, 5
and 3; `data/message_cache/calendar.rs` 39, 2 and 2;
`data/message_cache/mod.rs` 23, 12 and 12; `reader_text.rs` 125, 13 and 11;
`wx_reader.rs` 14, 2 and 1; `page_jumps.rs` 5, 2 and 2;
`wx_context_menu.rs` 2, 0 and 0; `wx_app.rs` 199, 119 and 112; the new
target 0.

**5. The anchors inside what this plan edits.**

- `wx_app.rs:14828-14831`, "answering a meeting with the hold off hands the
  answer to the server, not to the next Send of anything" (suite
  `undo_send_is_where_somebody_looks`): its `before` is the four lines
  `} = &went` ... `flush_outbox(app);` inside `answer_the_invitation`. Task
  1 changes that function's signature and its selection read; the block is
  kept as it is, not re-indented, and the record is named in the summary as
  still naming one place. If the refactor moves the block into a nested
  scope, the record is rewritten and re-measured in the same commit.
- `wx_app.rs:23979-23982`, the browser first in the page window, and
  `:24239-24243`, the keyboard handed to the page when the window comes
  back: task 3 adds the buttons after the page, which keeps both true; both
  named in the summary.
- `wx_app.rs:24141-24145`, Alt+A moving focus to the attachments: the new
  `Jump::Answer` arm is added beside it, not inside it.
- `page_jumps.rs:50` and `:62`: the new arms post `kind: 'answer'` and read
  `"answer"`, which duplicate neither anchor's text.
- `wx_reader.rs:320`, Alt+A on the reader's attachments item: not edited.
- `answered_meetings.rs:169`, `:201-204`, `:220`: `file_the_answer`'s
  lookup, the version write and the calendar write. Task 1 changes the
  version write to the answer write at `:223`; the executor reads whether
  `:220`'s or `:201-204`'s text changes, and rewrites and re-measures any
  record whose `before` the edit touches, in the same commit.
- `context_menu.rs:304-306`, the read message's mark entry, `:472-475`,
  `:534-537`: the new entries are separate statics or a parameter; the
  executor names in the summary which it chose and that the three anchors
  still name one place.
- `data/message_cache/mod.rs`: twelve anchors, none inside the migration
  block at `:3150-3185` the new `ensure_column_exists` joins.
</premise_corrections>

<decisions date="2026-09-24">
**1. Never greyed: no buttons and the reason in the bar.** Taken 2026-09-24
from the research's recommendation (section 6 and pitfall 11); Pratik may
overrule. A disabled button is passed over by Tab, and its reason with it.

**2. Accept is A&ccept, not &Accept.** Alt+A is the attachments key in both
reader windows (research, pitfall 3), so the answer submenu on Action keeps
&Accept (a menu's own letters are its own) and the buttons take C.

**3. An invitation inside decrypted content is shown and offered the
buttons, and never changes the calendar on its own.** Taken 2026-09-24 from
the research's recommendation (question 9); Pratik may overrule. Pressing a
button is the person's own act, so it answers and files as any answer does;
13-13's automatic change is the part that refuses decrypted provenance.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the answer remembered, answered from the message it was pressed on, and the reply threaded</name>
  <files>src/data/message_cache/calendar.rs, src/data/message_cache/mod.rs, src/application/answered_meetings.rs, src/application/answering.rs, src/application/invitations.rs, src/presentation/wx_app.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/calendar.rs:425-475` (`the_version_answered_here`, `remember_the_version_answered`) and the doc comment there on keeping this off `CalendarEventEntry`.
    - `src/data/message_cache/mod.rs:3150-3190` (the calendar migrations) and `:4000-4015` (the fresh schema).
    - `src/application/answered_meetings.rs:150-235` (`file_the_answer`) and `src/application/answering.rs:164-340` (`whether_it_can_be_answered`, `what_pressing_it_will_do`, `the_answer_to_send`).
    - `src/presentation/wx_app.rs:4590-4605` (the menu handler) and `:14717-14900` (`answer_the_invitation`), keeping `:14828-14831` as it is.
  </read_first>
  <behavior>
    - `AnswerGiven { Accepted, Tentative, Declined }` with `as_stored()` and `from_stored(&str) -> Option<Self>` at the SQL boundary; an additive `answered_with TEXT` column on `calendar_events` through `ensure_column_exists` and in the fresh schema; `remember_the_answer(event_id, version, AnswerGiven)` writes both; `the_answer_given_here(event_id) -> Result<Option<(u32, AnswerGiven)>>`; a row with a version and no answer (a database from before the column) reads as the version with no answer, never an error.
    - `file_the_answer` records the answer through `remember_the_answer`; 13-10's `AlreadyAnswered` standing carries the answer and the sentence says "you accepted this version", "you said you might attend this version" or "you declined this version".
    - `the_answer_to_send` takes the invitation message's `Message-ID` and its `References`, and the queued reply carries `In-Reply-To` as that id and `References` as the invitation's references with its id appended.
    - `answer_the_invitation` takes the message it answers as an argument; the Action menu handler passes the selected row; nothing inside it reads `selected_message_index`.
    - Cases: `test_the_answer_given_is_remembered_beside_the_version`; a database opened with only `answered_version` loads with no answer; the queued reply's `in_reply_to` and `references_header`; the sentence for each answer given.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check,
    since `calendar.rs`, `answered_meetings.rs`, `answering.rs` and `mod.rs`
    are named by records. Then the column, the enum, the write, the
    threading and the signature change, with `cargo build` between. Invoke
    `tdd` and `elegant-code`: the enum converts at the boundary and nowhere
    else; the message is a parameter, not a lookup.

    Guard records, measured: one on `calendar.rs` whose break writes the
    version and drops the answer, the remembered case red; one on
    `answering.rs` whose break leaves `In-Reply-To` empty, the threading case
    red. The anchors in premise 5 re-read after the edit; any that moved
    rewritten and re-measured in the same commit.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::calendar:: && cargo test --lib application::answered_meetings:: && cargo test --lib application::answering:: && cargo test --lib application::invitations::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c 'answered_with' src/data/message_cache/mod.rs` is at least 2; `grep -c 'selected_message_index' src/presentation/wx_app.rs` is one fewer than at the start of the task, both counts quoted.
    - The four module runs pass, counts quoted, each larger than its count at the start of the task by the cases added.
  </acceptance_criteria>
  <done>The answer given is remembered beside the version, the reply is threaded under the invitation, and answering takes the message it is given.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the three buttons in the text reader's tab, named and described, or the reason and no buttons</name>
  <files>src/presentation/reader_text.rs, src/presentation/wx_reader.rs, src/presentation/wx_app.rs, tests/the_invitation_is_answered_from_the_reader.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_reader.rs:250-330` (the frame, the menu bar, the accelerator), `:405-420` (`on_save_attachment`, `on_read_attachment`, the handler pattern), `:425-560` (`open`).
    - `src/presentation/reader_text.rs`: `ReaderDocument` and where `with_what_is_said` sets its warning.
    - `tests/the_feedback_dialog_shows_what_it_sends_before_it_goes.rs:140-240` (`AccessibleObjectFromWindow`, reading a built control's MSAA name and description at its own handle).
    - `tests/mark_as_read_says_which_way_it_will_go.rs` (a key posted to a real control and its effect waited for before the next).
  </read_first>
  <behavior>
    - `ReaderDocument` carries `answering: Option<Answering>`: the three `what_pressing_it_will_do` sentences when `whether_it_can_be_answered` says yes; `None` otherwise, and then the reason is in the warning bar through 13-10's sentence, so an unanswerable invitation says why.
    - `open()` builds a horizontal row of three native `Button`s labelled `A&ccept`, `&Tentative`, `&Decline` after the warning bar and before the text when `answering` is `Some`, each with `set_accessible_name_and_description` giving the label as the name and the sentence as the description; nothing at all when `None`.
    - `ReaderWindow::on_answer(handler: impl Fn(i64, Answer))`, called with the tab's message row id; `wx_app` binds it to `answer_the_invitation` with that message.
    - After pressing, focus stays on the button pressed and the outcome is announced once (ledger 155's question), never twice.
    - Measured first on the built tab: whether Alt+C posted to the tab (the text control focused) presses Accept through the panel's mnemonic handling; the summary quotes where the key arrived, and if it does not arrive the letter is bound on the tab's text control the way Alt+A is on the attachment list.
    - The target: a reader window built in its own process, a tab opened for the REQUEST fixture message, the three buttons found as the window's children in Tab order after the bar and before the text, each read over MSAA at its own handle (name "Accept", "Tentative", "Decline"; description the sentence); Alt+C posted and the handler's arrival recorded with the message row it carried; a tab for the cancellation fixture holds no button and its bar holds the reason; a companion that builds the buttons disabled and is refused.
  </behavior>
  <action>
    Red first, on a branch: the target's tests bare, and any `reader_text`
    case by module path with the count check. Then the document field, the
    row, the handler and the wiring, with `cargo build` between. Names on
    both channels at each button's own handle: MSAA in the target; UI
    Automation through the accessibility workflow's scan of the `reader`
    target, whose made-up document is built at `wx_app.rs:17526-17545`
    (`ScanTarget::Reader`): that document is given an answerable invitation
    so the scan reaches the three buttons, with no new target and no
    workflow change, and the summary says the Axe.Windows and
    `msaa-names.ps1` results on the pull request's run.

    Guard records, measured: one on `wx_reader.rs` whose break builds the
    row for a document with `answering: None`, the cancellation reading red,
    `suite` the target; one whose break drops the description, the MSAA
    description reading red, `suite` the target.
  </action>
  <verify>
    <automated>cargo test --test the_invitation_is_answered_from_the_reader && cargo test --lib presentation::wx_reader:: && cargo test --lib presentation::reader_text::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the target's readings bare.
    - `grep -c 'A&ccept' src/presentation/wx_reader.rs` is 1; `grep -c 'Enable(false)\|enable(false)' src/presentation/wx_reader.rs` is the same before and after, quoted.
    - The target passes with at least 5 tests, quoted; the summary quotes where Alt+C arrived.
  </acceptance_criteria>
  <done>In the text reader an answerable invitation has three named, described buttons after the bar, pressed by Alt+C, Alt+T and Alt+D, and an unanswerable one says why with no buttons at all.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the same buttons in the formatted window through the page's keys, and the context menu entries</name>
  <files>src/presentation/page_jumps.rs, src/presentation/wx_app.rs, src/application/context_menu.rs, src/presentation/wx_context_menu.rs, tests/the_invitation_is_answered_from_the_reader.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/page_jumps.rs` whole (the `Jump` enum, `SCRIPT`, `the_jump_the_page_asked_for`, and the test that reads every kind the script posts).
    - `src/presentation/wx_app.rs:14075-14105` (the formatted open of one message) and `:23914-24160` (`show_conversation_as_page`: the page, the attachment list, `wire_the_way_out`, the jump arms at `:24137` onward).
    - `src/application/context_menu.rs:100-140` (`Action`), `:240-330` (`Entry`, `entries_for_messages`, the two message lists); `src/presentation/wx_context_menu.rs:30-90` (`command_for`) and `src/presentation/wx_app.rs:3605-3625` (where the message menu is asked for).
  </read_first>
  <behavior>
    - `page_jumps::Jump::Answer(Answer)`: the listener posts `{ kind: 'answer', answer: 'accept' | 'tentative' | 'decline' }` for Alt with c, t or d in either case, never with Control held (AltGr), taken from the browser before it acts; `the_jump_the_page_asked_for` reads it.
    - Measured first on the built formatted window: whether Alt+C, with the page focused, reaches a native button's mnemonic; the summary quotes the answer. The page's listener is the route either way.
    - `show_conversation_as_page`, when the page shows one message whose invitation can be answered, builds the three buttons after the page and before the attachment list, named and described as in task 2; the jump arm presses the matching button's handler with that message; a conversation of several messages builds none (each message's sentence still says what it is). The browser stays the first control that takes the keyboard.
    - `context_menu`: `Action::AcceptInvitation`, `TentativeInvitation`, `DeclineInvitation`, mapped by `command_for` to `ID_ANSWER_ACCEPT`, `ID_ANSWER_TENTATIVE`, `ID_ANSWER_DECLINE`; the message menu offers `Accept &invitation`, `T&entatively accept invitation`, `Dec&line invitation` when the message under the cursor has a recorded `text/calendar` or `application/ics` attachment (from `get_attachments_for_message`, no parse), and not otherwise; pressing one on a cancellation says the reason.
    - Cases: `page_jumps` reads the three answers and posts them only with Alt; the context menu's letters are distinct in both lists with the entries in; `entries_for_messages` with a calendar part offers the three and without offers none. The target: the formatted window built for the REQUEST fixture, the three buttons found after the browser in Tab order and read over MSAA; a posted `{kind:'answer',answer:'decline'}` reaches the Decline handler with the message row; a conversation of two messages builds no buttons.
  </behavior>
  <action>
    Red first, on a branch: the `page_jumps` and `context_menu` cases by
    module path, the target's new tests bare, and the count check. Then the
    jump, the buttons, the arm and the entries, with `cargo build` between.
    Re-take the context menu's letters before writing them (13-07 may have
    added an entry) and quote the grep in the summary.

    Guard records, measured: one on `page_jumps.rs` whose break stops
    posting `answer`, the posting case red and the target's decline reading
    red; one on `context_menu.rs` whose break offers the entries on every
    message, its case red.
  </action>
  <verify>
    <automated>cargo test --lib presentation::page_jumps:: && cargo test --lib application::context_menu:: && cargo test --test the_invitation_is_answered_from_the_reader && cargo test --test attachments_are_reached_with_alt_a_in_both_views && cargo test --test the_page_window_keeps_the_document_focused_when_it_comes_back && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the new cases by module path, the target's tests bare, and the count check.
    - `grep -c "kind: 'answer'" src/presentation/page_jumps.rs` is 1; `grep -c 'Accept &invitation' src/application/context_menu.rs` is at least 1.
    - The five targets and the two module runs pass, counts quoted; the summary quotes the measurement of Alt+C in the page.
  </acceptance_criteria>
  <done>The formatted window, which is how a message opens by default, answers an invitation by Alt+C, Alt+T and Alt+D from the page and by three named buttons, and the message list's context menu offers the same three.</done>
</task>

<task type="auto">
  <name>Task 4: the shortcuts page, the guide, the changelog and the ledger</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:208-250` (The Reader Window), `:1284-1292` (Context Menu), `:661-692` (Action Menu).
    - `docs/USER_GUIDE.md`, 13-10's `### Meeting invitations`.
    - `.planning/WINDOWS.md` row 155.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: in The Reader Window, a
    table row each for Alt+C, Alt+T and Alt+D in both views, and a sentence
    that the buttons appear only when the invitation can be answered and the
    bar says why when it cannot; the three context menu entries in Context
    Menu. The guide: 13-10's sentence about answering from the Action menu
    rewritten to the buttons, the keys, the context menu, and what is said
    after. The changelog under `[Unreleased]`, Added, #50 points 4, 7 and 8;
    Known limitations: no real organiser has received a reply, and nobody
    has heard the buttons.

    Ledger, by hand, both halves: row 155 updated with what this plan
    settles (one announcement, measured by the target) and what still waits
    for the ear; one `unrun-verify` for the buttons and their descriptions
    under NVDA in both windows.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test wired test_the_shortcuts_document_and_the_menus_agree && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### The Reader Window/,/^### The Preview Pane/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'Alt+C'` is at least 1, and the same for Alt+T and Alt+D.
    - Ledger in both halves; no em dash or en dash; none of the six words; carriage returns 0 on every file touched.
  </acceptance_criteria>
  <done>The keys are documented where the windows that bind them are, and the changelog and ledger say what is proved and what waits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's invitation to a reply the program sends | answering sends mail on the person's behalf |
| the page's script to a native command | a key inside untrusted HTML's page reaching a command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11-01 | Spoofing | a page's own script posting `kind: 'answer'` | high | mitigate | message bodies are sanitised by `ammonia` and carry no script; the listener is injected by this program; the arm acts only on a window whose one message is answerable, and sending still goes through the outbox and its gate and the account's Allow Changes answer |
| T-13-11-02 | Repudiation | an answer sent for the wrong meeting | medium | mitigate | the message row travels with the press; nothing reads the list's selection; the target asserts the row carried |
| T-13-11-03 | Tampering | an answer stored as a free string | low | mitigate | an enum at the SQL boundary; an unknown stored value reads as no answer |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; the summary says so |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass. `scripts/check.sh` through the hook on every commit, never piped; no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the pages, the ledger, the summary and the four completion marks in one documents commit on the branch before the merge; `git merge --no-ff`.
- Spoken or shown: yes. The branch is pushed and a pull request opened after its last commit, and `ci.yml`, `nvda.yml` and `accessibility.yml` are read green before the merge; `main` is not pushed.
- Mnemonics checked as one set per window: the reader tab's C, T, D against the reader's A and menu titles F and G; the context menu's I, E, L against its twelve; the grep quoted in the summary.
</verification>

<success_criteria>
- GAP-04's "with Accept, Tentative and Decline" and "the answer sent through the outbox and the gate" each have a named test in the summary; the box waits for 13-13.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-04's traceability row naming this plan's clauses.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-11-SUMMARY.md` when done
</output>
