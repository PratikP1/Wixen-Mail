---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 25
type: execute
wave: 27
depends_on: [13-24.1]
files_modified:
  - src/application/what_a_rule_catches_here.rs
  - src/application/mod.rs
  - src/application/blocking.rs
  - src/presentation/wx_app.rs
  - tests/a_block_moves_the_mail_already_here.rs
  - tests/several_actions_reach_the_server_in_order.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-06]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After a block is written, the messages already on this computer that the new rule catches are counted on a worker, in every folder of the account but Junk, Trash, Sent and Drafts, through the same matcher that files mail on arrival (GAP-06's second [D] line, #54 point 2)."
    - "When any are here, one question asks whether to move them too, naming the count, the sender and the junk folder, with Yes and No; Yes moves them through the one runner 13-24.1 built, gated per account, and No leaves them; nothing is moved without that answer (decision 1)."
    - "Above the 5,000 every set command is bounded by, nothing is moved and the sentence gives the count and the bound and what to do instead (decision 3)."
    - "What a block says afterwards is true: the sentence that said messages already here stay where they are is said only when they did, and otherwise says how many moved; the sentence before the block no longer promises that they stay."
    - "Block This Sender is on Ctrl+Shift+B; Everyone at This Domain has no key (decision 2)."
  artifacts:
    - "src/application/what_a_rule_catches_here.rs: which_messages_here_a_rule_catches(rule, messages, folders) over FilterEngine::matches, passing over the four folder kinds, with rows; written so 13-43's count over a folder reuses it."
    - "src/application/blocking.rs: what_blocking_will_do without the stay-where-they-are promise, what_blocking_did over MailAlreadyHere { Nothing, Left(n), Moved(n, not_moved), TooMany(n) }, and the question's words."
    - "src/presentation/wx_app.rs: block_the_sender counting on a worker, the question on the window's thread, run_these_actions_over with a move into the junk folder, and \"&This Sender\\tCtrl+Shift+B\"."
    - "tests/a_block_moves_the_mail_already_here.rs: readings that the count runs off the window's thread, the question comes before any move, the move goes through run_these_actions_over, and the key is on the item, each with a planted-fault companion."
  key_links:
    - "block_the_sender -> the rule written (as today) -> a worker: messages_a_saved_search_reads(account, None, ..) -> what_a_rule_catches_here -> UIUpdate with the count and the chosen set -> the question -> run_these_actions_over(.., Outcome { move_to: junk }) -> blocking::what_blocking_did(Moved(n)) -> one sentence."
---

<objective>
A block moves the sender's mail that is already here, after asking once
with the count, and Block This Sender gets a key.

Purpose: #54's second and third points and GAP-06's second `[D]` line:
"a block that moves what is already here, said with the count; through
the gate". Today a block is a rule run on mail as it arrives, so
everything already in the inbox from that sender stays, and the block's
own sentence says so. RESEARCH-3 (C2) and RESEARCH-4 (F3) both found
that this is a rule run over existing mail under another name, which is
why the runner came first.

Output: the pure catch module, the block's sentences made true, the
count, the question and the move, the key, the reading target, the
pages, the changelog and the ledger. Closes GAP-06 with 13-22.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-22-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-24.1-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. Ask once, with the count; every folder but four (RESEARCH-3
question 2).** "Also move the 14 messages already here from them to
Junk?" over every folder of the account except Junk, Trash, Sent and
Drafts.

**2. Keys (question 3).** Block This Sender on `Ctrl+Shift+B`;
Everyone at This Domain without a key.

**3. Above 5,000, refuse with the count and the bound (RESEARCH-4
question 4).** The same bound every set command uses; the sentence
points at a search for the sender and Move to for the rest.

**4. Yes is the question's default (planner's choice, recorded).** The
move goes to Junk, never to Trash, and is what the person asked for a
moment earlier; Escape answers No. RESEARCH-3 left the default open if
Pratik said moving was the riskier answer; he has not.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. 13-01 to 13-24.1 land first; re-take every number in
`wx_app.rs` with the same command before editing.

**1. The runner this plan is the first caller of.** 13-24.1's ledger
entry says `run_these_actions_over` has no caller until this plan; this
plan calls it for the block's move and closes that entry in both halves.
`tests/several_actions_reach_the_server_in_order.rs` gains the reading
that the block is a caller, rewritten in place if 13-24.1 wrote one that
said nothing calls it.

**2. The block, as it is.**

```
grep -n '^fn block_the_sender' src/presentation/wx_app.rs   -> :32141; the rule written at :32270-32291 with create_filter_rule, then what_blocking_did said at Normal
sed -n 32141,32170p src/presentation/wx_app.rs   -> the account is the active account, held.active_account_id, not the message's own
sed -n 7033,7044p src/presentation/wx_app.rs   -> "&This Sender" and "Everyone at This &Domain", no key on either; appended as "Blo&ck" at :7386-7390
sed -n 560,600p src/application/blocking.rs   -> WHAT_IT_DOES_NOT_DO, "This does not tell your mail provider anything, so the mail is still accepted and still arrives here. Messages that already arrived stay where they are.", said by both what_blocking_will_do and what_blocking_did
sed -n 137,160p src/application/blocking.rs   -> a_rule_that_blocks: field from, match_type regex, action move_to_folder
grep -n 'pub fn matches\|pub fn from_persisted_rule' src/application/filters.rs   -> :349 and :450: the matcher arrival rules use
sed -n 382,410p src/data/message_cache/saved_searches.rs   -> messages_a_saved_search_reads(account_id, folder_id: Option<i64>, text): everything cached for an account, deleted mail left out, body text only when asked, not bounded
grep -rn 'Ctrl+Shift+B' src docs/KEYBOARD_SHORTCUTS.md   -> nothing
```

That the block uses the active account rather than the message's own
account matters in All Inboxes, where the two differ. It is not this
plan's subject: the executor reads whether it holds after 13-01 to
13-24.1 and, if it does, ledgers it as a `todo` rather than fixing it
here, because changing which account a block is written to is a
decision about blocking and not about the move.

`grep -rn 'already arrived stay\|provider anything' src tests` on
2026-09-24 found the constant at `blocking.rs:568-569` and one comment
in a test at `:2245-2247` quoting the sentences as said; no assertion
reads the words, so the rewrite reddens nothing by accident, and the
cases this plan adds are what hold the new words.

**3. The Block submenu's letters.** T and D, and the Action menu's own
list is unchanged by this plan (premise 1 of 13-22). A key is not a
letter; `test_no_two_menu_items_claim_the_same_shortcut` (`tests/wired.rs:344`)
and `test_the_shortcuts_document_and_the_menus_agree` (`:932`) hold the
key and the page.

**4. Guard records, both readings.** `blocking.rs` 2 by
`tests_last_seen` and 2 by `file`, 67 tests; tests are added there, so
the count check is named bare in the red trailer and the two records
are re-measured; both anchor at `:843` (`the_rule_that_blocks` in the
unblocking path), which this plan does not touch. `wx_app.rs` 119 and
112, no test added; by anchor, **"the block handler asks the message
rather than a constant"** (`:32227`, `how_to_leave_the_list:
message.list_unsubscribe.as_deref(),`, `suite`
`the_list_warning_reads_the_message`) lies inside `block_the_sender`;
the edit adds after the rule is written and leaves that line where it
is, and if the function's body is restructured the record is rewritten
and re-measured. `what_a_rule_catches_here.rs` and the new target are
new at zero; `tests/several_actions_reach_the_server_in_order.rs` is
13-24.1's, at the records it left.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: which messages here a rule catches, and a block's sentences made true</name>
  <files>src/application/what_a_rule_catches_here.rs, src/application/mod.rs, src/application/blocking.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/filters.rs:301-480` (`FilterEngine`, `matches`, `from_persisted_rule`) and `src/application/blocking.rs:137-160`, `:560-600`, `:650-680`.
    - `src/common/types.rs`, `FolderType` (`grep -n 'pub enum FolderType' -A 20 src/common/types.rs`).
  </read_first>
  <behavior>
    - `which_messages_here_a_rule_catches(rule: &FilterRule, messages: &[CachedMessage], folders: &[CachedFolder]) -> Vec<i64>`: the row ids `FilterEngine::matches` says the rule catches, passing over any message whose folder is Spam, Trash, Sent or Drafts by `FolderType`; rows: the sender's mail in the inbox caught and in Junk and Sent not; a domain block catching two addresses; a message from someone else not caught; an empty list.
    - `blocking::the_question_about_mail_already_here(block, count, junk) -> String`: "Also move the 14 messages already here from ada@example.com to Junk?", the singular for one, "from anybody at example.com" for a domain.
    - `what_blocking_will_do` no longer says messages already here stay; `what_blocking_did(block, junk, allowed, folder, here: MailAlreadyHere)`: `Nothing` says nothing about mail already here; `Left(n)` says "The 14 messages already here from them stay where they are."; `Moved(n, 0)` says "14 messages already here were moved to Junk."; `Moved(n, k)` says how many did not; `TooMany(n)` says "14,212 messages from them are already here, more than the 5,000 one command moves, so none were moved. Search for the sender and use Move to." The sentence that the provider is told nothing stays, and now points at Report as Junk.
  </behavior>
  <action>
    Red first, on the branch `13-25-a-block-moves-what-is-here`: the new
    module's rows by module path, the blocking cases by module path, and
    the count check `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    named bare because tests land in `blocking.rs`. Then the module and
    the sentences, with `cargo build` between; every existing blocking
    test that read the old sentence is rewritten in place to the new
    one. Invoke `elegant-code`: the catch module is one function over
    values; the question and the after-sentence are `blocking`'s, since
    blocking owns every sentence a block says.

    Guard records: one on the new module whose break stops passing over
    Sent, the Sent row red; one on `blocking.rs` whose break says the
    stay-where-they-are sentence after a move, the moved case red; the
    two existing records re-measured; each with `scripts/guards.sh
    --remeasure`, one call per commit.
  </action>
  <verify>
    <automated>cargo test --lib application::what_a_rule_catches_here:: && cargo test --lib application::blocking::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the rows and cases by module path and the count check bare.
    - `grep -c 'pub fn which_messages_here_a_rule_catches' src/application/what_a_rule_catches_here.rs` is 1; `grep -c 'MailAlreadyHere' src/application/blocking.rs` is at least 3.
    - `application::what_a_rule_catches_here::` passes with at least 6 tests and `application::blocking::` with more than 67, both quoted.
  </acceptance_criteria>
  <done>What a block catches among the mail already here is a pure function, and every sentence a block says about that mail is true of what happened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the count on a worker, the question, the move through the runner, and the key</name>
  <files>src/presentation/wx_app.rs, tests/a_block_moves_the_mail_already_here.rs, tests/several_actions_reach_the_server_in_order.rs, guards/guards.toml, docs/KEYBOARD_SHORTCUTS.md</files>
  <read_first>
    - `src/presentation/wx_app.rs` at `block_the_sender` and the `ID_BLOCK_SENDER` arm (premise 2), and 13-24.1's `run_these_actions_over`.
    - A native Yes and No question already asked from a handler (`grep -n 'MessageDialog::builder' src/presentation/wx_app.rs | head -3`), for how the window asks and reads the answer.
    - `docs/KEYBOARD_SHORTCUTS.md`, the Action Menu table's Block row or the submenu table (`grep -n 'Block' docs/KEYBOARD_SHORTCUTS.md`).
  </read_first>
  <behavior>
    - Readings over `what_ships`: `block_the_sender` starts a worker that calls `messages_a_saved_search_reads` and `which_messages_here_a_rule_catches` (the count is never taken on the window's thread); the question is asked before `run_these_actions_over`; the move is `run_these_actions_over` with an outcome that moves into the junk folder, never a second mover; `too_many` is met before the question; the Block submenu's first item reads "&This Sender\tCtrl+Shift+B". Each with a planted-fault companion.
    - Behaviour: after the rule is written, a count of zero says the block's sentence with `Nothing`; a count above the bound says `TooMany` and moves nothing; otherwise the question, Yes the default and Escape No; Yes runs the move and says `Moved`, once, with one Confirmed signal; No says `Left`. The rule is written whatever the answer; the question is about the mail already here only.
    - The shortcuts page gains Block This Sender's key in the commit that binds it.
  </behavior>
  <action>
    Red first: the target's readings bare. Then the worker, a `UIUpdate`
    carrying the count and the chosen set back to the window's thread,
    the question, the call to the runner, and the key on the item (per
    decisions 1 to 4), with `cargo build` between. The chosen set is
    built from the caught row ids in the shape `chosen_messages` builds,
    so the runner meets the same `Chosen` it meets from a selection.
    Invoke `elegant-code`: the handler counts, asks and calls; it words
    nothing `blocking` words.

    Guard records: one on `wx_app.rs` whose break moves before asking,
    the question reading red, `suite` the new target; one whose break
    counts on the window's thread, the worker reading red; each
    measured. `wx_app.rs` stays at 199 tests, quoted; the `:32227`
    record read and re-measured if the function body moved.
  </action>
  <verify>
    <automated>cargo test --test a_block_moves_the_mail_already_here && cargo test --test several_actions_reach_the_server_in_order && cargo test --test the_list_warning_reads_the_message && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the readings bare.
    - `grep -c '&This Sender\\tCtrl+Shift+B' src/presentation/wx_app.rs` is 1; `grep -c 'run_these_actions_over(' src/presentation/wx_app.rs` is at least 2 (the runner and this caller).
    - The target passes with at least 8 readings, quoted; `wired` passes; two guard records measured.
  </acceptance_criteria>
  <done>A block asks once about the mail already here, moves it through the one runner when told to, says one true sentence, and is on a key.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the alpha page's line, the changelog and the ledger</name>
  <files>docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`, the "Reporting junk" section 13-22 wrote (`grep -n '### Reporting junk' docs/USER_GUIDE.md`).
    - `docs/ALPHA_TESTING.md`, the line 13-22 added under what is unproven.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide's section becomes "### Reporting
    junk and blocking a sender": a block files future mail, asks about
    the mail already here with the count, moves it to Junk on Yes, is
    bounded at 5,000, and never tells the provider; Report as Junk does
    that. The alpha page's line gains the block's move: never tried on a
    real server. The changelog, `[Unreleased]`, Added: a block moves the
    mail already here when asked, and Block This Sender is on
    `Ctrl+Shift+B` (#54 points 2 and 3); Changed: the block's sentences.

    Ledger, by hand, both halves: close 13-24.1's "the runner has no
    caller" entry as fixed by this plan; one `unrun-verify` for phase 14
    (a block's move of existing mail at a real server); one
    `unrun-verify` for the tester's ear (the question read on open, the
    count, the answer's sentence); and the active-account `todo` if
    premise 2 still holds. After the merge, the executor comments on #54
    from the merge commit with what closed each point and leaves the
    issue for Pratik to close when a real server has seen it. One
    documents commit on the branch before the merge carries the pages,
    the ledger, the summary and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Reporting junk and blocking a sender' docs/USER_GUIDE.md` is 1; `grep -c 'Ctrl+Shift+B' docs/KEYBOARD_SHORTCUTS.md` is at least 1; the changelog names #54.
    - The ledger entries in both halves, 13-24.1's closed; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say what a block does now, the runner's first caller closes its ledger entry, and what only a real server and the tester can settle is written down for them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stored rule to a move of existing mail | a block's pattern applied to everything already here |
| the cache read to the window | a large read and a regex over every message |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-25-01 | Tampering | a domain block moving a colleague's whole history | high | mitigate | the count asked first with the sender named; moves go to Junk, never Trash; Sent, Drafts, Trash and Junk passed over |
| T-13-25-02 | Elevation of privilege | a move on an account whose changes are off | high | mitigate | the runner meets the gate per account before anything changes |
| T-13-25-03 | Denial of service | the count freezing the window over a large mailbox | medium | mitigate | the read and the match on a worker; a reading holds it; the regex already bounded at 1 MiB in `filters.rs` |
| T-13-25-04 | Denial of service | a run over tens of thousands | low | mitigate | refused above 5,000 with the count, the bound and the next step |
| T-13-25-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is spoken and shown (a question, sentences, a key): push the branch, open a pull request, wait for CI, NVDA and Accessibility, then merge locally. Pushing `main` is Pratik's.
- Red commits name lib tests by module path and integration tests bare; the count check named bare for `blocking.rs`. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-06's second `[D]` line and "through the gate" each have a named test or reading in the summary; with 13-22's line already marked, GAP-06's box is ticked with the date and its traceability row updated; its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-06's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-25-SUMMARY.md` when done
</output>
