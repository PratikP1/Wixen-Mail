---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 45
type: execute
wave: 47
depends_on: [13-03, 13-17, 13-44]
files_modified:
  - src/service/mailbox_archive.rs
  - src/application/exporting_mail.rs
  - src/application/mod.rs
  - src/application/export_tree.rs
  - src/application/importing_messages.rs
  - src/presentation/wx_app.rs
  - tests/wired.rs
  - tests/mail_goes_out_in_every_shape.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-13]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "File, Export Folder as a Mailbox File (Alt+F then F, no shortcut key) writes the mail of the folder the cursor is on, and not the folders inside it, into one mailbox file the person names in a Save dialog that offers the folder's own name; what this program's own import reads back from that file is the same messages (GAP-13's bare mbox line, #53 point 4, decisions 5 and 6)."
    - "A message whose text was never downloaded is left out and counted, a file this computer does not have is counted, and a signed message whose original was not kept is counted, each in the words the zip export already uses for the same fact; the closing sentence names the shape, one mailbox file, and says how many folders inside were not included and that Export Mailbox writes a folder together with the folders inside it."
    - "A folder where no message went in writes no file, leaves whatever was already at the chosen name exactly as it was, and says why; a write that fails partway also leaves the file that was there before untouched and says the export did not finish, because the file is written under another name and put in place only at the end."
    - "A saved search, a label or a branch is refused with the sentence Export Mailbox already says, through one helper that every export handler in the main window calls, and a reading counts the handlers and refuses one that skips it."
    - "writing_out, CHOOSE_THE_MESSAGES_TO_EXPORT and what_the_mail_export_did, production code no non-test path reached, are gone with the tests that held only them (decision 6), and export_tree's header no longer says a message's files were never kept."
  artifacts:
    - "src/service/mailbox_archive.rs: MailboxFileBeingWritten and one_mailbox_file_written_to, writing under the chosen name with .partial on the end, put in place by finish, removed by abandon and by a finish nothing went into."
    - "src/application/exporting_mail.rs (new): one_stored_message_added, shared with the zip export's loop, and one_folder_as_a_mailbox_file, tested against a real store in a temporary home."
    - "src/application/export_tree.rs: what_the_mailbox_file_export_did, sharing its three extra sentences with what_the_folder_export_did through one function; the header's sentence about files corrected."
    - "src/presentation/wx_app.rs: ID_EXPORT_A_FOLDER_AS_A_MAILBOX_FILE, the File menu item, its arm, a_folder_to_write_out, export_a_folder_as_a_mailbox_file."
    - "tests/mail_goes_out_in_every_shape.rs (new): the item, the arm, the handler reaching its writer, every export handler asking the folder question, and a companion that plants a handler skipping it."
  key_links:
    - "File menu item -> ID_EXPORT_A_FOLDER_AS_A_MAILBOX_FILE arm -> export_a_folder_as_a_mailbox_file -> a_folder_to_write_out (WhichRow::Folder) -> worker -> exporting_mail::one_folder_as_a_mailbox_file -> one_stored_message_added -> export_tree::added_to_the_archive -> MailboxFileBeingWritten -> finish (rename) -> export_tree::what_the_mailbox_file_export_did -> the status line."
    - "write_the_mailbox_out (the zip) and one_folder_as_a_mailbox_file call the same one_stored_message_added, so the two shapes cannot come to build a message two ways."
---

<objective>
One folder out as a bare mailbox file, the first half of #53's point 4.

Purpose: #53, point 4: "Export only as a zip of `.mbox`; never a bare
`.mbox` or loose `.eml`, so one folder for Thunderbird means unzipping by
hand." Pratik on 2026-09-17: "Points 4 to 6 (a bare .mbox or loose .eml
export, .msg, .pst export) are later work." GAP-13's `[D]` line: each of
the three built or refused with a sentence on the page saying which and
why. This plan builds the bare mailbox file; 13-46 builds the loose
message files; 13-47 and 13-48 read `.msg`; 13-49 writes the pages that
say which of the three is built and why `.pst` export is not.

Output: the writer, the loop in a module of its own, the sentence, the
menu item and its handler behind one folder question every export asks,
the unused export seams removed, the reading, the pages, the changelog,
the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-5-import-export.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before any plan of the
phase has run. Command first, then what it said. Plans 13-01 to 13-44
change `wx_app.rs` before this one runs, so every line number here is
re-taken with the same command on the day, and a number that moved is
not a finding; a name that is gone is.

**1. The File menu, its letters, and the two plans that change it first.**

```
sed -n '/let file = Menu::builder()/,/\.build();/p' src/presentation/wx_app.rs | grep -o '"[^"]*&[^"]*"'
   -> "&Save\tCtrl+S" "Save &As..." "Check &Mail\tF9" "Open &Draft...\tCtrl+Shift+O" "&Import Mailbox..." "Import a F&older of Messages..." "&Export Mailbox..." "Import PGP Private &Key... (experimental)" "&Quit\tCtrl+Q"
grep -n 'file.prepend_submenu' src/presentation/wx_app.rs   -> :6728, "&New": N is taken by the prepended submenu, which the builder block does not show
grep -n 'Alt+F\b' docs/KEYBOARD_SHORTCUTS.md   -> :703, :705, :748, :1112, :1226 are dialog accelerators in other windows; :1444 "Alt+F for File menu"; the page lists no File menu letter
```

Letters in use on File today: N, S, A, M, D, I, O, E, K, Q. Free: B, C,
F, G, H, J, L, P, R, T, U, V, W, X, Y, Z. The comment at
`wx_app.rs:6697-6701` says "nothing here claims F now". The two plans
that change this menu first say so in their own text, read 2026-09-24:
`13-03-PLAN.md:365` adds a separator and `"&Print...\tCtrl+P"` after
the PGP item and before the separator above Quit (P); `13-17-PLAN.md:37`
and `:101` put `"PGP &Keys... (experimental)"` where Import PGP Private
Key is, keeping K (Pratik's settlement of 2026-09-24), and its `:97`
already names F and X as this group's. So on the day File reads N, S,
A, M, D, I, O, E, K, P, Q. This plan takes **F** for "Export Folder as
a Mailbox &File...", placed directly after Export Mailbox and so before
the key manager's item, and 13-46 takes **X** for "E&xport Folder as
Message Files...". The executor re-runs the first command on the day, before
writing the label, and quotes the list: if 13-03 or 13-17 took F, the
item takes another letter from the free list that reads in its label,
the summary says which and why, and 13-46's plan is told in the same
commit (its `<premise_corrections>` gains one line). `tests/wired.rs`'s
`test_no_two_items_on_one_menu_claim_the_same_letter` (`:1977`) refuses
a collision in any case.

**2. The export as it is.**

```
grep -n 'fn export_a_mailbox\|fn write_the_mailbox_out\|fn an_export_that_broke_off' src/presentation/wx_app.rs   -> :15096, :15196, :15302
sed -n 15106,15133p src/presentation/wx_app.rs   -> three refusals inline: no store, no account or folder, and not a real folder (WhichRow::Folder at :15130); nothing else asks them
sed -n 15228,15256p src/presentation/wx_app.rs   -> the per-message step: text, attachments_with_content, signed_original, added_to_the_archive into a buffer; written only inside write_the_mailbox_out
sed -n 5179,5188p src/presentation/wx_app.rs   -> the ID_EXPORT_MESSAGES arm
grep -n 'fn test_nothing_treats_a_saved_search_as_a_folder_on_a_server' tests/wired.rs   -> :3275; it reads body_of(&ship, "fn export_a_mailbox(") for "WhichRow::Folder", so moving the question into a helper turns it red unless its first reading is re-pointed in the same commit
sed -n 895,995p src/service/mailbox_archive.rs   -> ArchiveBeingWritten opens the zip at once with File::create; there is no writer for a plain file
```

**3. The seams nothing reaches (decision 6).**

```
grep -rn 'writing_out(\|what_the_mail_export_did\|CHOOSE_THE_MESSAGES_TO_EXPORT\|WritingOut::' src tests
   -> definitions at importing_messages.rs:545, :585, :679; the const uses of the_file_ends_with at export_tree.rs:464 and importing_messages.rs:658; test calls at importing_messages.rs:1139-1153, :1217-1219, :1230, :1245, :1259, :1363, :1379, :1383 and export_tree.rs:757, :1372, :1866; nothing else
```

`MessagesExported` stays: it is `FoldersExported`'s field
(`export_tree.rs:654`). `WritingOut` loses `Refused`, which only
`writing_out` built, so `the_file_ends_with` can answer a plain
`&'static str` and the two consts that match on it simplify.

**4. export_tree's own rule puts the loop somewhere else.**

```
sed -n 9,11p src/application/export_tree.rs   -> "Values in, values out. Nothing here opens a file, packs one, talks to a server or touches the database"
sed -n 42,44p src/application/export_tree.rs   -> "The file itself was never kept, so a message that arrived carrying three of them exports as the message and none of them"; false since the attachment store (:245-251 and wx_app.rs:15230-15235 carry the files the computer kept)
grep -n 'mod end_to_end\|fn a_cache' src/application/importing_messages.rs   -> :1445, :1453: application code that takes a real MessageCache in a temporary home is this project's pattern
```

RESEARCH-5 names the starting red test
`application::export_tree::tests::test_one_folder_written_as_a_mailbox_file_reads_back_as_the_same_messages`.
A test that reads a store and writes a file breaks export_tree's first
rule, so the loop and its test live in a new module,
`application::exporting_mail`, and the test keeps its name there:
`application::exporting_mail::tests::test_one_folder_written_as_a_mailbox_file_reads_back_as_the_same_messages`.
export_tree gains only the sentence, which is a value.

**5. The guard records this plan touches, by both readings.** Counted
with the format's own parser, `python -c "import tomllib;
g=tomllib.load(open('guards/guards.toml','rb'))['guard']; ..."`, 1,088
records.

| File | named in `tests_last_seen` | named as `file` | `#[test]` lines by `grep -c` |
|---|---|---|---|
| `src/application/export_tree.rs` | 1 ("a name written back out is quoted when it needs to be", `file` src/common/types.rs) | 0 | 38 |
| `src/application/importing_messages.rs` | 5 | 2 | 41 |
| `src/service/mailbox_archive.rs` | 0 | 0 | 31 |
| `src/presentation/wx_app.rs` | 119 | 112 | 196 |
| `tests/wired.rs` | 19 | 2 | 78 |
| `src/application/exporting_mail.rs`, `tests/mail_goes_out_in_every_shape.rs` | new, 0 | new, 0 | new |

The `grep -c` column is a grep and not what the count check reads (the
records carry 199 for `wx_app.rs` and 77 for `wired.rs` on the same
tree); quote the check's own numbers, not these.

Anchor text inside the regions this plan edits, found by locating every
record's `before` in its `file` with a script and keeping those that
land in the File menu (`:6655-6735`), the id list (`:125-160`), the
dispatch arms (`:5150-5195`), the export handlers (`:14930-15380`),
`importing_messages.rs:300-700` and its tests, `export_tree.rs:1-120`,
`:160-340` and `:440-760`, `mailbox_archive.rs:880-1000` and
`wired.rs:3250-3320`:

- `wx_app.rs:6718-6722`, "a PGP private key somebody can import",
  anchored on the whole Import PGP Private Key `.append_item` call.
  13-17 rewrites or retires that record when it replaces the item;
  this plan inserts its item above whatever stands there on the day
  and duplicates no part of it. Probed again before the edit.
- `importing_messages.rs:632`, "Save As takes the path out of a
  subject before offering it as a name", anchored on
  `'/' | '\\' => '_',`. Untouched here (13-46 moves it); counted
  because the file's test count moves.
- `importing_messages.rs:377-381`, "signed mail brought in from a file
  keeps the bytes its signature is over". Untouched; counted for the
  same reason.

No record's `before` lies inside the export handlers, the per-message
step, the writer, or `wired.rs:3250-3320`. No record's red list names a
test task 2 removes (checked by the same parser over every `red` list
for the five test names and for `what_the_folder_export_did`).
</premise_corrections>

<decisions date="2026-09-24">
Pratik on 2026-09-24 confirmed the phase's order and said "plan and
implement phase 13". Every recommendation in the research's questions
is taken as the decision, recorded here as **taken 2026-09-24 from the
research's recommendation; Pratik may overrule**.
`RESEARCH-CROSS-CHECK.md` holds no merged question list on disk (77
lines on 2026-09-24: Numbering, Collisions, Premises checked,
Packages), so the questions are RESEARCH-5's own eight, and this plan
carries the two that bind it.

**5. The bare mailbox file is this folder alone.** The folders inside
it are not in the file, and the closing sentence says how many and that
Export Mailbox writes a folder with the folders inside it. Taken
2026-09-24 from the research's recommendation; Pratik may overrule.

**6. Two new items next to Export Mailbox, nothing already learned
moves; the unused decision goes.** "Export Folder as a Mailbox File"
(Alt+F then F) here and "Export Folder as Message Files" (Alt+F then X)
in 13-46, no submenu. `writing_out`, which picked `.eml` for one
selected message and `.mbox` for several and was reached by nothing but
its tests, is removed in this plan with `CHOOSE_THE_MESSAGES_TO_EXPORT`
and `what_the_mail_export_did`, the sentence for the export it would
have served, since the two new items cover what it was for. Taken
2026-09-24 from the research's recommendation; Pratik may overrule.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the mailbox file written under another name and put in place at the end, the loop over one folder, and its sentence</name>
  <files>src/service/mailbox_archive.rs, src/application/exporting_mail.rs, src/application/mod.rs, src/application/export_tree.rs, src/presentation/wx_app.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/mailbox_archive.rs:887-995` (`ArchiveBeingWritten`, `written_to`, `finish` with `sync_all`, `went_wrong`) and its test module's first cases for the `tempfile` pattern.
    - `src/presentation/wx_app.rs:15196-15307` (`write_the_mailbox_out` and `an_export_that_broke_off`), the loop whose per-message step moves out.
    - `src/application/export_tree.rs:160-300` (`WhatBecameOfIt`, `counted_in`, `added_to_the_archive`), `:637-751` (`FoldersExported`, `what_the_folder_export_did`, `how_much_came_out`) and `:1496-1550` (the module's every-sentence check and its machinery words, which refuse "mbox" and ".eml").
    - `src/application/importing_messages.rs:1445-1470` (`end_to_end`, `a_cache`, the temporary home) and `src/application/message_files.rs:1135-1145` (`each_message_read_piece_by_piece`) and `:1821` (`what_the_export_did`).
  </read_first>
  <behavior>
    - `service::mailbox_archive::one_mailbox_file_written_to(at: &Path) -> Result<MailboxFileBeingWritten>` opens `<at>.partial` beside the chosen name with `create_new`, so a folder that is not there or a disk that is full is found before a long run, and a file already called that is refused with a sentence naming it rather than written over. `MailboxFileBeingWritten::write_into_it(&mut self, bytes)` writes and remembers that something went in. `finish(self) -> Result<MailboxFileOutcome>`: when nothing went in, the partial file is removed and the answer is `NothingWentIn`, and a file already at the chosen name keeps its bytes; otherwise it flushes, syncs to disk and renames the partial file over the chosen name, answering `Written`. `abandon(self)` removes the partial file and leaves the chosen name as it was. Cases in `service::mailbox_archive::tests`: written bytes land at the chosen name and no partial file is left; nothing written leaves a file already at the chosen name byte for byte and no partial file; abandon after a write leaves the file already there byte for byte; a partial file already in the way is refused and left as it was; a folder that does not exist is refused at the opening.
    - `application::exporting_mail` (new, declared in `src/application/mod.rs`), the one place a folder's mail is read out of the store and handed to a writer, so export_tree keeps its rule. `one_stored_message_added(cache, message: &MessageListRow, into: &mut Vec<u8>) -> WhatBecameOfIt` reads the text, the files this computer has and the kept signed original and calls `export_tree::added_to_the_archive`, exactly the step at `wx_app.rs:15229-15252`; `write_the_mailbox_out` calls it in place of those lines, so the zip and the bare file build a message one way. `one_folder_as_a_mailbox_file(cache, account, folder, to: &Path, progress: &dyn Fn(usize)) -> String` takes the folder alone, counts the folders whose path starts with the folder and a separator (the filter `write_the_mailbox_out` uses at `:15207-15214`) as not included, writes each message that went in, calls `progress` with the count written every 100 messages and at the end, ends with `finish` (or `abandon` and `an_export_that_broke_off`'s sentence when a write fails), and answers the sentence below.
    - `export_tree::what_the_mailbox_file_export_did(written: &FoldersExported, folders_inside_not_included: usize) -> String`: opening from `message_files::what_the_export_did` with "into one mailbox file" (for nought: "No messages were exported, so no file was written"), then the same three sentences `what_the_folder_export_did` says about left-out messages, files not here and signatures not kept, through one function both call so the wording is one, then, when the count is above nought, "1 folder inside it was not included" or "N folders inside it were not included", followed by "Export Mailbox writes a folder together with the folders inside it"; it joins the module's every-sentence check, which refuses "mbox".
    - `application::exporting_mail::tests::test_one_folder_written_as_a_mailbox_file_reads_back_as_the_same_messages`: a real store in a temporary home, folder Work with three messages (one never downloaded, one whose body stops mid-line) and Work/Invoices with one; written to a `tempfile` path; read back with `message_files::each_message_read_piece_by_piece`: two messages with their subjects and bodies, the mid-line body ending where a line ends, Invoices' message absent, and the sentence saying 1 message left out and 1 folder inside not included. Beside it: a folder whose only message was never downloaded writes no file, a file already at the chosen name keeps its bytes, and the sentence says no file was written and why.
  </behavior>
  <action>
    Red first, on a branch: the `mailbox_archive` cases, the
    `exporting_mail` cases and the `export_tree` sentence cases by module
    path, with the count check named bare
    (`test_every_guard_record_says_how_many_tests_the_files_it_names_held`),
    since one record names `export_tree.rs`; run the named targets and the
    count check before the red commit and name everything that fails
    (the executor brief rule of 2026-09-23). Then the writer, the module
    with its two functions, the sentence, and `write_the_mailbox_out`
    calling `one_stored_message_added`, with `cargo build` between.

    Invoke `elegant-code` and `tdd`. The writer holds no policy: it
    writes, finishes or abandons, and every sentence it can produce names
    the file. Errors go through `common::Error`; no `unwrap` or `expect`
    outside tests. The partial name is the chosen name with `.partial`
    appended in the same folder, so the rename stays on one volume. The
    three extra sentences are extracted from `what_the_folder_export_did`
    into one private function taking the counts, called by both, so a
    rewording lands in both at once; the existing wording test that held
    the two exports' left-out sentence together (`export_tree.rs:1858-1870`)
    is rewritten in place in task 2, where the function it compares
    against goes.

    Before the first edit to `wx_app.rs`, probe `guards/guards.toml` with
    the tomllib command for every line of `:15229-15252` the extraction
    removes, and name in the summary any record whose `before` held one
    (premise 5 found none on 2026-09-24). Guard records: one on
    `mailbox_archive.rs` whose break renames when nothing went in (the
    file already there replaced by an empty one), the nothing-written
    case red; one on `exporting_mail.rs` whose break drops the
    separator from the not-included filter so Work/Invoices' mail goes
    into Work's file, the round-trip case red; each measured on the
    library. Every record the count check names for a file touched here
    is re-measured with one `scripts/guards.sh --remeasure` call listing
    all of them, and read.
  </action>
  <verify>
    <automated>cargo test --lib service::mailbox_archive:: && cargo test --lib application::exporting_mail:: && cargo test --lib application::export_tree::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the count check bare, and `red-commit.sh` accepted it (the hook's line quoted).
    - `grep -c 'pub fn one_mailbox_file_written_to' src/service/mailbox_archive.rs` is 1; `grep -c 'pub fn one_folder_as_a_mailbox_file\|pub fn one_stored_message_added' src/application/exporting_mail.rs` is 2; `grep -c 'exporting_mail::one_stored_message_added' src/presentation/wx_app.rs` is 1; `grep -c 'pub fn what_the_mailbox_file_export_did' src/application/export_tree.rs` is 1.
    - `cargo test --lib service::mailbox_archive::` passes with at least 36 tests, `application::exporting_mail::` with at least 3, `application::export_tree::` with at least 40, each quoted.
    - Two guard records measured and quoted; the remeasure call's output quoted.
  </acceptance_criteria>
  <done>One folder's mail goes into one mailbox file written under another name and put in place only when something went in, the zip and the bare file build each message through one step, and the sentence says what was written, what was left out and which folders were not included.</done>
</task>

<task type="auto">
  <name>Task 2: the export seams nothing reached go, and the exporter's header says what is true (decision 6)</name>
  <files>src/application/importing_messages.rs, src/application/export_tree.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/importing_messages.rs:539-695` and every test premise 3 lists, each read whole before anything is removed.
    - `src/application/export_tree.rs:36-50` (the header), `:457-467` (`AN_ARCHIVE_ENDS_WITH`), `:754-760` (the test module's imports) and `:1360-1380` and `:1825-1895` (the tests calling `WritingOut` and `what_the_mail_export_did`).
  </read_first>
  <action>
    No behaviour a non-test path reaches changes here: the three items
    have no caller outside their tests (premise 3), and removing them is
    decision 6, taken 2026-09-24 from the research's recommendation. So
    this task carries no red test, and says so in its commit message
    rather than deciding it quietly; the green gate is the build and
    the tests that remain. Invoke `dead-code-hunter` over the two files
    first and quote what it finds.

    Remove `writing_out`, `CHOOSE_THE_MESSAGES_TO_EXPORT`,
    `what_the_mail_export_did` and `WritingOut::Refused`;
    `the_file_ends_with` answers `&'static str`, and
    `AN_ARCHIVE_ENDS_WITH` and `A_SAVED_MESSAGE_ENDS_WITH` become plain
    calls of it. A test goes only when every assertion in it is about a
    removed item; a test whose property survives is rewritten in place
    against what survives: the endings test holds `.eml` and `.mbox`
    through `the_file_ends_with` alone, the every-sentence test drops
    the removed items from its list, and export_tree's test that held
    the zip export's left-out sentence against the one-folder export's
    now holds `what_the_folder_export_did` against task 1's
    `what_the_mailbox_file_export_did` for the same fact. The summary
    lists every test removed and every test rewritten, with its name.

    Correct the header at `export_tree.rs:42-44` to say what is true
    since the attachment store: the files this computer kept go with
    their message, and one it never kept is counted and said.

    The count check will name the records whose files lost tests (five
    name `importing_messages.rs` and one names `export_tree.rs` on
    2026-09-24); run the count check before committing, then one
    `scripts/guards.sh --remeasure` call listing every record it names,
    and read each result.
  </action>
  <verify>
    <automated>cargo test --lib application::importing_messages:: && cargo test --lib application::export_tree:: && test -z "$(grep -rlE 'fn writing_out\(|CHOOSE_THE_MESSAGES_TO_EXPORT|fn what_the_mail_export_did|WritingOut::Refused' src tests)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE 'fn writing_out\(|CHOOSE_THE_MESSAGES_TO_EXPORT|fn what_the_mail_export_did|WritingOut::Refused' src tests` finds nothing; `cargo build --all-targets` is clean with no dead-code warning.
    - `sed -n 36,50p src/application/export_tree.rs` no longer says the file was never kept, quoted.
    - The summary names every test removed and rewritten; `application::importing_messages::` and `application::export_tree::` pass with their new totals quoted; the remeasure call's output quoted.
  </acceptance_criteria>
  <done>The two export seams nothing reached are gone with the tests that held only them, the tests whose property survives hold it against what survives, and the exporter's header says the files go with their message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the File menu item, one folder question every export asks, the handler and its worker, and the reading</name>
  <files>src/presentation/wx_app.rs, tests/wired.rs, tests/mail_goes_out_in_every_shape.rs, guards/guards.toml</files>
  <read_first>
    - Premise 1's command, re-run and quoted before the label is written.
    - `src/presentation/wx_app.rs`: the `menu_ids!` list around `ID_EXPORT_MESSAGES`, the File builder, the `ID_EXPORT_MESSAGES` arm, `export_a_mailbox` whole (`:15096-15185` on 2026-09-24), and `import_a_mailbox`'s picker (`:14448-14455`) for the dialog builder's shape.
    - `tests/wired.rs:3265-3300` (`test_nothing_treats_a_saved_search_as_a_folder_on_a_server`) and `tests/every_way_a_file_goes_on_a_message.rs:1-60` (a target that reads source through `wixen_mail::common::what_ships::what_ships` and counts a door).
  </read_first>
  <behavior>
    - `ID_EXPORT_A_FOLDER_AS_A_MAILBOX_FILE` in `menu_ids!` beside `ID_EXPORT_MESSAGES`. On File, directly after Export Mailbox: `"Export Folder as a Mailbox &File..."` with the help string "Write this folder's mail, without the folders inside it, into one mailbox file", no shortcut, and a comment giving the reason Import Mailbox's comment gives for having none and naming decision 6.
    - `a_folder_to_write_out(state, cache, ui_tx, runtime, a11y) -> Option<(String, String)>`: the three refusals now inline in `export_a_mailbox` (no store, no account or folder, not a `WhichRow::Folder`), their sentences unchanged; `export_a_mailbox` calls it.
    - `export_a_folder_as_a_mailbox_file(state, cache, frame, ui_tx, runtime, a11y)`: calls the helper; a Save dialog with the message "Write this folder's mail into one mailbox file", the default name the folder's last part made safe through `export_tree::where_each_folder_goes` with the mailbox ending, the filter "Mailbox files (*.mbox)|*.mbox", Save and OverwritePrompt; "No file name was chosen." when the path is missing; the opening sentence "Writing this folder's mail into one mailbox file." on the status line and announced at normal priority; a worker on the pattern at `:15153-15184` calling `exporting_mail::one_folder_as_a_mailbox_file` with a progress closure saying "N messages written out so far." under the status subject the zip export uses, and the closing sentence on the status line.
    - The arm `ID_EXPORT_A_FOLDER_AS_A_MAILBOX_FILE` calls it.
    - `tests/wired.rs`'s saved-search reading keeps its property and reads the helper: its first pair names "writing a mailbox out to a file" and reads `fn a_folder_to_write_out(`. A body change; the file's test count does not move.
    - `tests/mail_goes_out_in_every_shape.rs` (new target, source read through `what_ships`): `test_export_folder_as_a_mailbox_file_is_on_the_file_menu_with_no_shortcut` (the File builder block holds an `append_item` with the id and the label, and the label carries no tab); `test_export_folder_as_a_mailbox_file_reaches_its_writer` (the arm calls the handler, whose body calls `a_folder_to_write_out` and `one_folder_as_a_mailbox_file`); `test_every_export_asks_whether_what_is_chosen_is_a_folder` (every function whose name begins `export_a` calls `a_folder_to_write_out`, the helper's body holds `WhichRow::Folder`, and the census found at least two such functions, the number quoted in the failure text); and the companion `test_the_folder_question_reading_refuses_an_export_that_skips_it`, which hands the same census a planted source holding an `export_a_thing` that does not call the helper and requires a refusal.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare, run before the
    red commit together with the count check, naming everything that
    fails. Then the id, the item, the helper, the handler, the arm and
    the re-pointed `wired.rs` reading, with `cargo build` between.
    Invoke `elegant-code`: the helper answers the account and the folder
    or nothing, and each refusal is said once, where it is decided; the
    handler hands over and says, it does not decide.

    Before the first edit to the File builder, probe the anchor of "a
    PGP private key somebody can import" (or whatever 13-17 left in its
    place) with the tomllib command and confirm the insertion duplicates
    no record's `before`; before committing green, run
    `cargo test --test house_style test_every_guard_record_still_names_one_place_in_the_tree`
    as a cheap check, since the new handler's lines resemble
    `export_a_mailbox`'s.

    Guard record: one on `wx_app.rs` whose break removes the helper call
    from `export_a_folder_as_a_mailbox_file`, with `suite` the new target
    and the census reading red. Its `before` cannot be the call line
    alone, which `export_a_mailbox` holds too: anchor it on the
    handler's signature line together with the lines down to and
    including the call, the longest unique context that still names one
    place (CLAUDE.md, "A record is coupled to the tree a second way").
    Measured, and every record the count check names re-measured with
    one call.
  </action>
  <verify>
    <automated>cargo test --test mail_goes_out_in_every_shape && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - Premise 1's letter list re-taken on the day and quoted, with the letter the item took.
    - A red commit on a branch names the readings bare.
    - `grep -c 'ID_EXPORT_A_FOLDER_AS_A_MAILBOX_FILE' src/presentation/wx_app.rs` is at least 3 (the id, the item, the arm); `grep -c 'a_folder_to_write_out(' src/presentation/wx_app.rs` is at least 3 (the definition and two calls).
    - `cargo test --test mail_goes_out_in_every_shape` passes with 4 tests; `cargo test --test wired` passes with its total unchanged, both quoted.
    - One guard record measured and quoted, its `before` shown to occur once.
  </acceptance_criteria>
  <done>File holds Export Folder as a Mailbox File on a free letter, it reaches the writer through the same folder question Export Mailbox asks, and a reading refuses any export handler that skips the question.</done>
</task>

<task type="auto">
  <name>Task 4: the pages, the changelog, the ledger, and the branch pushed for CI, NVDA and Accessibility</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md:963-1006` (Import and Export), `docs/KEYBOARD_SHORTCUTS.md:599-622` (the File table), `docs/changelog.md:1173-1180` (the gathered issue 53 paragraph), each re-found with `grep -n` on the day.
    - `.planning/WINDOWS.md`, entry 502 in both halves (the table row and the JSON), as the shape of an `unrun-verify` entry.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: a row after Export
    Mailbox, "Export Folder as a Mailbox File", "(none)", saying it
    writes the folder you are looking at without the folders inside it
    into one mailbox file (`.mbox`). The guide's Import and Export
    section: the count of commands in its first sentence corrected to
    what the File menu holds on the day, a row in the table, and one
    sentence that the closing sentence names the folders left out and
    that Export Mailbox is the command for a folder with its folders.
    Claim nothing about another program opening the file: nobody has
    tried it.

    The changelog, `[Unreleased]`, Added: Export Folder as a Mailbox
    File, in #53's own words (point 4, first half), with decision 5's
    rule; Known limitations: no file this writes has been opened in
    another mail program, and nobody has heard the item, its
    description or its sentences. At the end of the gathered issue 53
    paragraph (`:1173-1180` on 2026-09-24), one dated sentence saying
    point 4's first half landed in this entry; 13-49 rewrites that
    paragraph whole.

    Ledger, by hand, both halves: one `unrun-verify` for the tester, the
    file opened in Thunderbird (Thunderbird Daily is installed on this
    machine, `C:\Program Files\Thunderbird Daily\thunderbird.exe`,
    RESEARCH-5) and in his own mail program, and the item, its
    description and the closing sentence heard under NVDA. Nothing here
    touches a server, so nothing goes to phase 14.

    This plan changes what is shown and spoken (a menu item, a dialog,
    three sentences), so after the last branch commit the executor
    pushes the branch and opens a pull request into `main` (Pratik's
    standing OK of 2026-09-23), waits for `ci.yml`, `nvda.yml` and
    `accessibility.yml`, fixes on the branch if any is red, and then
    merges locally with `git merge --no-ff`. Pushing `main` stays his.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Export Folder as a Mailbox File' docs/KEYBOARD_SHORTCUTS.md` is at least 1 and `grep -c 'Export Folder as a Mailbox File' docs/USER_GUIDE.md` is at least 1.
    - The ledger entry in both halves, its number quoted; carriage returns 0 on every file touched (`tr -cd '\r' < FILE | wc -c` read as 0 for each); no em or en dash; none of the six words.
    - The pull request's number and the three runs' conclusions quoted.
  </acceptance_criteria>
  <done>The pages and the changelog describe the new export as it is and say what nobody has tried, the tester has a ledger line for his ear and his mail program, and the runner has read the branch before the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the store to a file somebody chose | a stranger's message written to a path a person named, beside files they own |
| a folder row to a write | a saved search or a label taken for a folder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-45-01 | Tampering | a file already at the chosen name destroyed by an export that wrote nothing or broke off | high | mitigate | written under `.partial` with `create_new` and renamed over the chosen name only at a finish something went into; `abandon` on every early return; cases hold the old bytes; a guard record holds the nothing-written case |
| T-13-45-02 | Repudiation | an export that reads as a complete backup and is not | medium | mitigate | the left-out, files-not-here and signatures-not-kept sentences shared with the zip export through one function; the folders-not-included sentence; a guard record on the not-included filter |
| T-13-45-03 | Tampering | a saved search exported as though it were a folder | medium | mitigate | `a_folder_to_write_out` in every export handler; the census reading and its companion; a guard record |
| T-13-45-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; `Cargo.toml` and `Cargo.lock` are untouched, and the summary says so |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the branch is pushed with a pull request and the three runs read before the merge; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite, the release build and the audit run once, in 13-51's full gate (CLAUDE.md, "A phase's closing plan runs the full gate once, by hand, before its merge"); never `scripts/check.sh all` by hand here. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line to it, as every plan since 12-03.2 does.
- Four guard records measured; every record the count check flagged re-measured with one call and read.
- What the tester settles: the file opened in another mail program, the item and its sentences heard; what the tests prove: the round trip through this program's own reader, the old file kept, the refusals, the one folder question.
</verification>

<success_criteria>
- GAP-13's bare mbox half has a named test and a reading in the summary; its `[D]` line is not ticked here (13-49 ticks it when all three are said on the page); its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (the roadmap's, and the phase README's if one exists); `.planning/STATE.md`'s plan number in the frontmatter and in the body (`Current Plan:`); GAP-13's line and traceability row in `.planning/REQUIREMENTS.md` carrying a dated note that its bare mbox half landed here.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-45-SUMMARY.md` when done
</output>
