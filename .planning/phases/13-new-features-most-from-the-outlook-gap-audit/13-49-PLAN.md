---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 49
type: execute
wave: 51
depends_on: [13-45, 13-46, 13-48]
files_modified:
  - tests/mail_goes_out_in_every_shape.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/comparison.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-13]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The guide says, for each of the three shapes #53's points 4 to 6 asked for, whether it is built and why: a folder goes out as one mailbox file or as message files, a saved Outlook message is read and not written, and an Outlook data file (.pst) is neither written nor offered on any menu, with the reason in one sentence and what to do instead (GAP-13's [D] line, decision 1)."
    - "A reading holds that no menu item offers to write an Outlook data file and another that every export item on the File menu is named in the guide's Import and Export section; each has a companion that plants a violation and requires a refusal, and the companions are the red."
    - "The advice that Outlook takes saved message files dragged into one of its folders is marked on the page as not tried with files Wixen Mail wrote, and the ledger holds it as Pratik's to confirm (RESEARCH-5's A6)."
    - "The changelog's gathered issue 53 paragraph says what was built, what is out and why, every older sentence kept and dated; the comparison page's 'the same way is still generous' is corrected by dating; ledger 502 is closed or reworded for the export shapes; #53 is closed from the merge commit; GAP-13's [D] line and box are ticked with the date and the names they stand on."
  artifacts:
    - "tests/mail_goes_out_in_every_shape.rs: test_no_menu_offers_to_write_an_outlook_data_file, test_every_export_on_the_file_menu_is_named_in_the_guide, and a companion for each."
    - "docs/USER_GUIDE.md's Import and Export section, docs/comparison.md, docs/changelog.md, docs/KEYBOARD_SHORTCUTS.md."
  key_links:
    - "the File builder's export items -> their labels without the & -> the guide's Import and Export section: every one named, and none that writes a .pst."
---

<objective>
The pages say which of the three is built, and why `.pst` export is
not.

Purpose: GAP-13's `[D]` line: "Each of the three built or refused with a
sentence on the page saying which and why; a refused format is not a
menu item." 13-45 and 13-46 built the two export shapes and 13-47 and
13-48 the `.msg` reading; `.pst` export is refused (decision 1). This
plan writes the refusal and the whole of what changed where a person
reads it, holds the "not a menu item" half with a reading, and closes
#53 on the tree side.

Output: the reading and its companion, the guide, the comparison page,
the changelog, the ledger, GAP-13 ticked, #53 closed.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-5-import-export.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-45-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-46-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-48-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before 13-45 to 13-48
ran. Command first, then what it said. Every page line below is
re-found with `grep -n` on the day, since 13-45, 13-46 and 13-48 each
added rows and dated sentences.

```
sed -n 963,1006p docs/USER_GUIDE.md   -> "Import and Export": "Three commands on the File menu", a three-row table, the .pst warning, the Thunderbird profile note, Save As
sed -n 98,114p docs/comparison.md   -> "Goes out 'the same way' is still generous: export writes one zip of mbox files, and nothing else."
grep -n 'What issue 53 asked for' docs/changelog.md   -> :1173, the gathered paragraph naming points 4, 5 and 6 as later work
sed -n 242,253p Cargo.toml   -> the recorded decision: Microsoft's reader chosen over the fork that also writes; "Nothing here writes one."
grep -n '^| 502 ' .planning/WINDOWS.md   -> Save As never opened in another mail program nor run by hand
grep -n 'GAP-13' .planning/REQUIREMENTS.md   -> :5875 (the requirement, one [D] and one [S] line), :6074 (its traceability row, "Not planned, 2026-09-20")
gh issue view 53 --json state   -> read on the day; RESEARCH-5 read it open with Pratik's comment of 2026-09-17 keeping it open for points 4 to 6
```

The research's `.pst` route table (RESEARCH-5, section 3) is what the
page's reason is drawn from: the one Rust writer, `outlook-pst-rw`
1.2.9, is a six-week-old one-person fork publishing under the reader's
own library name, flagged SUS by the legitimacy check, and is in all
likelihood the fork `Cargo.toml` already declined; Extended MAPI needs
Outlook installed (learn.microsoft.com, "Installing the MAPI
Subsystem"), and this machine has none.

The guard records this plan touches, by both readings, taken with the
tomllib parser on the day: the records 13-45 and 13-46 wrote with
`suite` or `tests_last_seen` naming `tests/mail_goes_out_in_every_shape.rs`,
whose test count this plan raises by four, so the count check names
them and one `--remeasure` call covers them. No page is named by any
record (0 and 0 for the four pages on 2026-09-24).
</premise_corrections>

<decisions date="2026-09-24">
Taken 2026-09-24 from the research's recommendation; Pratik may
overrule. RESEARCH-5's question 1.

**1. `.pst` export is said plainly to be out, and is not a menu item.**
The guide's sentence, from the research, with the one change that its
untried advice says it is untried: "Wixen Mail does not write Outlook
data files (`.pst`). Only Outlook can check one, and the one library
that writes them is too new to trust with your mail. To take mail to
Outlook, export a folder as message files and drag them into one of
Outlook's folders, which has not yet been tried with files Wixen Mail
wrote, or let Outlook download the same account." Whether classic
Outlook takes dragged `.eml` files is RESEARCH-5's A6, and the ledger
line in task 2 asks Pratik. Taken 2026-09-24 from the research's
recommendation; Pratik may overrule.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: a reading that no menu offers to write an Outlook data file and that every export item is named in the guide, with a companion that plants one</name>
  <files>tests/mail_goes_out_in_every_shape.rs, guards/guards.toml</files>
  <read_first>
    - `tests/mail_goes_out_in_every_shape.rs` as 13-46 left it, and its census and companion from 13-45 for the shape of a reading with a planted source.
    - The File builder in `src/presentation/wx_app.rs` on the day (`sed -n '/let file = Menu::builder()/,/\.build();/p'`).
  </read_first>
  <behavior>
    - `test_no_menu_offers_to_write_an_outlook_data_file`: over every `Menu::builder()` block in `src/presentation` read through `what_ships`, no item whose label or help string names an Outlook data file or `.pst` together with a word for writing out (export, write, save); the count of builder blocks read is at least one per menu the File menu reading found and is quoted in the failure text.
    - `test_every_export_on_the_file_menu_is_named_in_the_guide`: every File menu item whose id begins `ID_EXPORT` has its label, with the `&` and any trailing ellipsis taken off, in the guide's Import and Export section (from its heading to the next `## `); at least three export items are found (Export Mailbox and the two this phase added).
    - `test_the_outlook_data_file_reading_refuses_a_planted_export_item`: the first reading's function handed a planted builder block holding an item labelled "Export as an Outlook &Data File (.pst)..." answers a refusal naming it.
    - `test_the_guide_reading_refuses_a_section_missing_an_export`: the second reading's function handed the File menu's export labels and a planted section naming only two of them answers a refusal naming the third.
  </behavior>
  <action>
    Red first, on a branch. Both readings pass on the tree from their
    first run, because nothing offers a `.pst` export and 13-45 and 13-46
    each put their row in the guide, so neither can be the red; what
    proves each can see a violation is its companion (CLAUDE.md, "A
    guard that reads documents needs a companion proving the reading can
    see a violation when one exists"). So the reading functions are
    written first answering nothing, the two companions fail, and the
    red commit names the two companions bare, run before the commit with
    the count check and everything that fails named. Then the functions'
    bodies. If on the day the guide lacks an export item, the guide
    reading is red as well and is named with them, and task 2's page
    edit lands before this task's green; the summary says which.

    Invoke `elegant-code`: the section is cut from the page once by one
    function; labels are cleaned by one function; both readings say
    what they read and how many.

    Guard record: one whose break removes 13-46's row from the guide's
    table, the guide reading red, `file` `docs/USER_GUIDE.md`, `suite`
    the target, anchored on the row's whole line; measured. Every record
    the count check names for the target re-measured with one call.
  </action>
  <verify>
    <automated>cargo test --test mail_goes_out_in_every_shape</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the two companions bare (and the guide reading if it was red on the day); `red-commit.sh` accepted it.
    - The target passes with 10 tests, quoted (6 after 13-46 and these four); the number of builder blocks and export items each reading found, quoted.
    - One guard record measured and the remeasure call's output quoted.
  </acceptance_criteria>
  <done>No menu can come to offer a .pst export without a test saying so, and the guide cannot lose an export command the File menu holds.</done>
</task>

<task type="auto">
  <name>Task 2: the guide, the comparison page and the changelog say which of the three is built and why .pst is not; the ledger; GAP-13 ticked; #53 closed</name>
  <files>docs/USER_GUIDE.md, docs/comparison.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - The four pages' sections re-found with the commands in the premise, and 13-45's, 13-46's and 13-48's summaries for what each wrote and what each left.
    - `.planning/WINDOWS.md` entry 502 and the entries 13-45, 13-46 and 13-48 opened, in both halves.
  </read_first>
  <action>
    Invoke `writing-craft`. Every sentence is checked against the tree
    before it is written: each command's name against the File builder,
    each shape against the code that writes or reads it.

    The guide's Import and Export section: the table as the File menu
    holds it on the day, and a short paragraph, "What goes out, and what
    does not", saying a folder goes out as one zip of mailbox files with
    its folders, as one mailbox file without them, or as message files
    with a folder for each folder; that a saved Outlook message (`.msg`)
    is read and not written, since nothing asked for writing one; and
    decision 1's sentence about Outlook data files, with its untried
    advice marked as untried. The comparison page: the "still generous"
    sentence corrected by dating in the page's manner, saying the three
    export shapes and that `.pst` is read and not written. The
    shortcuts page: only what 13-45, 13-46 and 13-48 left inconsistent,
    read against the File menu.

    The changelog: the gathered issue 53 paragraph rewritten whole, in
    its own register, saying what was built for points 4 and 5 (naming
    the entries above it), that point 6 is refused and why, and what
    nobody has tried; every sentence the three plans dated kept and
    dated, never deleted.

    Ledger, by hand, both halves: entry 502 reworded to cover every
    export shape nobody has opened in another mail program, or closed
    where 13-45's and 13-46's entries now hold that; one `unrun-verify`
    for Pratik: whether his Outlook takes message files dragged into one
    of its folders (A6), with what the page says until he answers. And
    one dated sentence on ledger 499 (no real Outlook data file through
    the `.pst` import), both halves, carrying what RESEARCH-5 left for
    that entry's owner: four open pull requests upstream about real
    files (microsoft/outlook-pst-rs #61, #62, #63, #65, read 2026-09-24),
    and that #63 names a public real `.pst`, the EDRM Enron set, which
    could close 499 without Pratik's own file.

    `.planning/REQUIREMENTS.md`: GAP-13's `[D]` line ticked with the
    date and the names it stands on (13-45's round trip, 13-46's round
    trip, 13-47's and 13-48's reader and routing cases, this plan's
    readings, each as `path#name`), its box ticked, its traceability
    row set to the phase and the date; the `[S]` line untouched. The
    one-pass check first: each `path#name` grepped as `fn <name>` in
    its file, and no tick written on a name that is not there.

    After the merge, the executor closes #53 from the merge commit with
    `gh issue close 53 --comment "..."`: points 4 to 6 each named as
    built or refused, with the plan that did it, and what is left to
    Pratik (a real `.msg`, the drag-in question). Closing an issue is
    not a publish (as 12-08 and 12-07 did). The Help window shows the
    guide and the changelog, so this plan changes what is shown: the
    branch is pushed with a pull request after the last branch commit,
    the three runs read, and then the local `git merge --no-ff`.
  </action>
  <verify>
    <automated>cargo test --test mail_goes_out_in_every_shape && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - Read with the lines joined, since prose wraps (CLAUDE.md, "Two more shapes of a check that cannot fail"): `tr '\n' ' ' < docs/USER_GUIDE.md | grep -c 'does not write Outlook data files'` is 1 and `tr '\n' ' ' < docs/USER_GUIDE.md | grep -c 'not yet been tried'` is 1.
    - The comparison page's "still generous" sentence carries a dated correction, quoted; the changelog's gathered paragraph names points 4, 5 and 6 with what became of each, quoted.
    - The one-pass check's numbers quoted (names read, found, not found); `grep -c '^- \[x\] \*\*GAP-13' .planning/REQUIREMENTS.md` is 1.
    - The ledger edits in both halves, quoted by number; carriage returns 0 on every file touched; no em or en dash; none of the six words.
    - The pull request's number, the three runs' conclusions and the issue's closing comment quoted.
  </acceptance_criteria>
  <done>Anyone reading the guide, the comparison page or the changelog learns which of the three shapes #53 asked for exists, which is refused and why, and what nobody has tried; GAP-13 stands on names in the tree and #53 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a page's sentence to a person moving their mail | advice somebody acts on with the only copy of their mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-49-01 | Repudiation | a page promising a shape the program does not write, or advice nobody tried | medium | mitigate | every sentence checked against the tree; the drag-in advice marked untried and ledgered for Pratik; the guide reading holds every export item |
| T-13-49-02 | Tampering | a .pst export item added later with an unvetted writer | medium | mitigate | the no-menu reading and its planted companion; the Cargo.toml decision cited on the page |
| T-13-49-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing installed; the summary says so |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the branch is pushed with a pull request and the three runs read before the merge; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite runs once, in 13-51's full gate; never `scripts/check.sh all` by hand here. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line.
- One guard record measured; every record the count check flagged re-measured with one call and read.
- What only Pratik settles: whether his Outlook takes dragged message files, and a real `.msg`; what the tests prove: no menu offers `.pst`, every export item is on the page.
</verification>

<success_criteria>
- GAP-13's `[D]` line and box ticked on names found in the tree, with the date; its `[S]` line untouched; its traceability row updated.
- Phase 13's roadmap criterion 13 closes structurally.
- The completion marks, all four: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (the roadmap's, and the phase README's if one exists); `.planning/STATE.md`'s plan number in the frontmatter and in the body; GAP-13's line and traceability row, ticked here.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-49-SUMMARY.md` when done
</output>
