---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 21
type: execute
wave: 22
depends_on: [13-20]
files_modified:
  - src/application/protecting.rs
  - src/application/mod.rs
  - src/application/mail_controller.rs
  - src/application/sending_later.rs
  - src/application/allowed.rs
  - src/application/pgp_keys.rs
  - src/data/message_cache/mod.rs
  - src/data/message_cache/outbox.rs
  - src/data/message_cache/drafts.rs
  - src/presentation/editor_document.rs
  - src/presentation/wx_compose.rs
  - src/presentation/wx_app.rs
  - tests/signing_and_encrypting_from_the_composer.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/comparison.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-05]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The composer has two check boxes, Si&gn (experimental) and Encr&ypt (experimental), on Reached so the page watches for Alt+G and Alt+Y while the keyboard is in the message, as it does for the other fifteen letters; both are named on both channels at their own handles and never greyed (GAP-05, #52 points 4 and 5)."
    - "Pressing Send with either box ticked decides the protection before anything is queued: S/MIME when the sender has a certificate for the From address (and, to encrypt, every recipient has a kept certificate), otherwise OpenPGP when the sender has a key and every recipient a public key; when neither can do it, the composer stays open and says which recipient has no key or certificate, or that the sender has none, and nothing is queued."
    - "A locked PGP key asks for its passphrase at Send through 13-17.1's dialog; Cancel keeps the composer open with nothing queued."
    - "The choice is kept in additive columns on the outbox row and the draft, as an enum at the SQL boundary; a row or draft from before the columns loads as plain; the send loop builds the protection from the row, so a queued, scheduled or retried message goes out as it was asked to."
    - "The Sent copy is the protected bytes that went, readable here because the sender is a recipient; the send is announced with how it was protected."
    - "The two stub entries 13-19 and 13-20 opened are fixed; docs/comparison.md says signing and encrypting are offered, experimental; GAP-05's box is ticked."
  artifacts:
    - "src/application/protecting.rs: Choice, what_protection_it_gets, the refusal sentences, pure over what is held."
    - "data/message_cache: protection on outbox_queue and drafts through ensure_column_exists; outbox.rs and drafts.rs carry it."
    - "presentation/editor_document.rs: Reached::Sign and Reached::Encrypt with G and Y; wx_compose.rs: the two boxes, the check at Send, the passphrase ask."
    - "tests/signing_and_encrypting_from_the_composer.rs: the built composer's boxes over MSAA, Alt+G and Alt+Y from the page, Send refused with a keyless recipient and nothing queued, a signed and encrypted send's Sent copy opening."
  key_links:
    - "the boxes -> Choice -> at Send protecting::what_protection_it_gets(choice, from, recipients, what is held) -> refusal said, or the row queued with its protection -> the send loop -> mail_controller::outgoing -> Email.protection -> build_message (13-19, 13-20) -> send_email -> the Sent copy."
---

<objective>
Sign and Encrypt in the composer: signed and encrypted mail can be sent.

Purpose: GAP-05's last clause, "a message can be sent signed and
encrypted", and #52 points 4 and 5. 13-19 and 13-20 built both families
beneath the composer, reached only by tests, and each left a `stub` entry
naming this plan. This plan makes them reachable from the one place a
person writes mail, says before anything is queued when it cannot be done
and why, and keeps the choice through the outbox, a scheduled send and a
draft.

Output: the decision, the columns, the boxes and their keys, the check at
Send, the target, the pages, the changelog, the ledger, GAP-05's box.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-19-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-20-SUMMARY.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said. 13-06 gives every dialog, the composer among them, a longer undo
history, and 13-19 and 13-20 change `smtp.rs`; re-take the greps at the
start of task 1.

**0. The entries this plan closes.** 13-19's `stub` entry ("S/MIME signing
and encrypting is reached only by tests until 13-21 wires the composer")
and 13-20's ("OpenPGP signing and encrypting is reached only by tests until
13-21 wires the composer") are this plan's to fix; their numbers are read
from `.planning/WINDOWS.md` at the start of task 1 and quoted in the
summary. If 13-20 stopped at its checkpoint on Pratik's "hold", there is no
OpenPGP half, and this plan offers S/MIME only and says so in the
composer's box description and the changelog.

**1. The composer's letters, taken as one set.**

```
sed -n 1219,1305p src/presentation/editor_document.rs   -> Reached: From F, To T, Cc C, Bcc B, Subject S, Send N, Schedule H, Undo U, Redo R, Format O, Spelling P, Attach A, SaveDraft D, Discard I, Cancel L; the comment at :1266-1273 names E for the People found list
grep -n "fn test_no_two_things_in_the_composer_answer_to_the_same_letter\|fn test_the_key_that_reaches_the_list_is_not_one_this_window_already_uses" src/presentation/editor_document.rs src/presentation/wx_compose.rs   -> editor_document.rs:2300 and wx_compose.rs:3896: the two cases that hold the set
grep -o '"[^"]*&[A-Za-z][^"]*"' src/presentation/wx_compose.rs   -> the composer's own labels come from Reached; the others ("Insert &Link...", the spelling and table dialogs' labels, "Confirm &Send") belong to menus and dialogs of their own
awk '/^### Composition Window/,/^### Folder Actions/' docs/KEYBOARD_SHORTCUTS.md | grep -o 'Alt+[A-Z]' | sort -u   -> the letters the page documents for the window, read at the start of task 3 and quoted
```

**G** (Si&gn) and **Y** (Encr&ypt) are free today, as the research found
(`editor_document.rs:1258-1305`). The page generates its key table from
`Reached::ALL` (`reach_key_table`, `:1307-1316`), so two new members are
watched for without a second list.

**2. Where the choice has to travel.**

```
sed -n 2083,2092p src/data/message_cache/mod.rs   -> outbox_queue; columns added by ensure_column_exists at :3105-3147 (body_html, in_reply_to, references_header, cc_addr, bcc_addr, attachments, send_after)
grep -n "pub struct QueuedOutboxMessage" src/data/message_cache/mod.rs   -> :925
grep -n "CREATE TABLE IF NOT EXISTS drafts" src/data/message_cache/mod.rs   -> :1817
grep -n "pub struct SendEmailRequest\|pub fn outgoing" src/application/mail_controller.rs   -> :62, :199: the request built from a queued row, and the Email built from it
grep -n "Reached::Send => dialog.end_modal(ID_SEND)" src/presentation/wx_compose.rs   -> :2441; the Send button at :800-806
```

A draft is kept because a draft reopened and sent without its Encrypt box
is a private message sent in the clear, which is worse than the box never
having existed.

**3. The gate for each file**, by the TOML reader on 2026-09-24:
`editor_document.rs` 83 tests, 3 and 3; `wx_compose.rs` 43, 8 and 7;
`mail_controller.rs` 71, 23 and 5; `sending_later.rs` 47, 2 and 0;
`outbox.rs` 25, 0 and 0; `drafts.rs` 6, 1 and 0;
`data/message_cache/mod.rs` 23, 12 and 12; `allowed.rs` 27, 5 and 5; the
new module and target 0. Anchors near the edits, read with the locator: in
`wx_compose.rs`, `:130` (the schedule comment naming Alt+H), `:268`, `:364`,
`:1661`, `:1801`, `:3498`, `:3857`; none lies in the toolbar at `:793-840`
or the dispatch at `:2425-2470` where the boxes and their arms go. In
`editor_document.rs`, `:216-217`, `:812`, `:826-827`, none in `Reached`.
Each named in the summary as untouched.
</premise_corrections>

<decisions date="2026-09-24">
**1. Never greyed; the reason said at Send, nothing queued.** Taken
2026-09-24 from the research's recommendation (section 6 and pitfall 11);
Pratik may overrule.

**2. Which family when both could.** S/MIME first when the sender holds a
certificate for the From address and, to encrypt, every recipient has a
kept certificate; OpenPGP otherwise. The plan's own default, taken
2026-09-24 because Outlook, the program the gap audit measured against,
reads S/MIME without an add-in; Pratik may overrule. The send's
announcement says which family was used, so the choice is never silent.

**3. Encrypted to the sender too, and the Sent copy is what went.** The
research's pitfall 9, carried by 13-19 and 13-20; this plan proves it from
the composer.

**4. The choice is kept on a draft.** The plan's own default, for the
reason in premise 2; Pratik may overrule.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the choice decided purely, and kept on the outbox row and the draft</name>
  <files>src/application/protecting.rs, src/application/mod.rs, src/application/pgp_keys.rs, src/data/message_cache/mod.rs, src/data/message_cache/outbox.rs, src/data/message_cache/drafts.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/protocols/smtp.rs`'s `Protection` as 13-19 and 13-20 left it.
    - `src/data/message_cache/mod.rs:920-960` (`QueuedOutboxMessage`), `:3100-3150`, `:1817-1840`; `src/data/message_cache/outbox.rs:70-130` (`queue_outbox_message_to_go`, `load_outbox_messages`); `src/data/message_cache/drafts.rs`.
    - `src/application/pgp_keys.rs` (13-16) and `data/message_cache/correspondent_certificates.rs` (13-19).
  </read_first>
  <behavior>
    - `protecting::Choice { Plain, Signed, Encrypted, SignedAndEncrypted }` with `as_stored` and `from_stored` at the SQL boundary, unknown or absent reading `Plain`.
    - `protecting::what_protection_it_gets(choice, from, recipients, held: &WhatIsHeld) -> Result<Protection, CannotProtect>`, pure: `WhatIsHeld` carries whether the sender has an S/MIME signer and a PGP key (and whether it is locked), and each recipient's kept certificates and public keys. Decision 2's order; `CannotProtect { NoSigningKey, NoKeyFor { address }, TheKeyIsLocked { whose } }` with a sentence each ("You have no certificate or PGP key for ada@example.com, so this message cannot be signed.", "grace@example.com has sent no certificate and you hold no PGP key for her, so this message cannot be encrypted to her. Nothing was sent.").
    - `protection TEXT` on `outbox_queue` and on `drafts` through `ensure_column_exists`; the queue and the draft save and load it.
    - Cases: each `Choice` round-trips; a row and a draft written before the column load `Plain`; the family order (S/MIME when both are held, OpenPGP when only keys, the refusal naming the first recipient with neither); a locked key answers `TheKeyIsLocked`; signing needs no recipient keys.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check
    (`drafts.rs` and `mod.rs` are named by records). Then the module, the
    columns and the two stores, with `cargo build` between. Invoke `tdd`
    and `elegant-code`: the decision is values in and a value out; the
    stores carry the enum's stored form and nothing else.

    Guard records, measured: one on `protecting.rs` whose break sends plain
    when a recipient has no key, the refusal case red; one on `drafts.rs`
    whose break drops the column on save, the draft round-trip red.
  </action>
  <verify>
    <automated>cargo test --lib application::protecting:: && cargo test --lib data::message_cache::outbox:: && cargo test --lib data::message_cache::drafts::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c '"protection"' src/data/message_cache/mod.rs` is at least 2 (the two `ensure_column_exists` calls).
    - The three runs pass, counts quoted.
  </acceptance_criteria>
  <done>What protection a message gets, or why it cannot get it, is one pure decision, and the choice survives the outbox and a draft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the send loop builds the protection from the row, and the Sent copy is what went</name>
  <files>src/application/mail_controller.rs, src/application/sending_later.rs, src/application/protecting.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/mail_controller.rs:55-120` (`SendEmailRequest`) and `:195-260` (`outgoing`), `:585-605` (`send_email`).
    - `src/application/sending_later.rs`, where a queued row becomes a request.
    - `src/application/sent_copy.rs` around `:900-940`, where the Sent copy is kept from `send_email`'s bytes.
  </read_first>
  <behavior>
    - `SendEmailRequest.protection: Choice`, filled from the queued row; `outgoing` gathers `WhatIsHeld` for the From address and the recipients (the store's signer, the PGP keys, the kept certificates and public keys) and asks `what_protection_it_gets`; a `CannotProtect` at send time (a key removed after queueing) fails the send with its sentence and leaves the row queued, never sending plain.
    - The Sent copy is `send_email`'s formatted bytes, unchanged, so an encrypted message's Sent copy is its envelope and opens here through 13-14 or 13-15 because the sender is a recipient.
    - Cases: a queued `Signed` row with the in-memory keyholder as signer builds an `Email` whose formatted bytes 13-19's verifier reads as holding; an `Encrypted` row to a recipient with a kept certificate builds bytes 13-14's opener opens to the body; a row whose recipient's certificate was forgotten after queueing fails with the sentence and stays queued; a `Plain` row is byte for byte as before; the Sent copy of the encrypted case opens.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check,
    since `mail_controller.rs` is named by 23 records; the remedy the count
    check prints is run once, in the background if it will pass about nine
    minutes. Then the field, the gathering and the failure path, with
    `cargo build` between.

    Guard record, measured: one on `mail_controller.rs` whose break falls
    back to plain on `CannotProtect`, the forgotten-certificate case red.
  </action>
  <verify>
    <automated>cargo test --lib application::mail_controller:: && cargo test --lib application::sending_later:: && cargo test --lib application::protecting::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - The three runs pass, counts quoted; the records the count check flagged re-measured with one call.
  </acceptance_criteria>
  <done>A queued, scheduled or retried message goes out protected as it was asked, or not at all, and its Sent copy is what went.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Si&gn and Encr&ypt in the composer, reached from the page, checked at Send</name>
  <files>src/presentation/editor_document.rs, src/presentation/wx_compose.rs, src/presentation/wx_app.rs, src/application/allowed.rs, tests/signing_and_encrypting_from_the_composer.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/editor_document.rs:1200-1330` (`Reached`, `label`, `letter`, `reach_key_table`) and `:2295-2320` (the letter case).
    - `src/presentation/wx_compose.rs:790-845` (the toolbar), `:2420-2470` (the page's key dispatch), `:3885-3920` (the People found letter case), and where the dialog's result is turned into a queued row in `wx_app.rs` (`grep -n 'ID_SEND' src/presentation/wx_app.rs`).
    - `src/presentation/wx_passphrase.rs` (13-17.1).
    - `tests/the_key_manager_lists_and_names_its_controls.rs` for the MSAA reading, and an existing target that builds the composer if one does (`grep -rln 'wx_compose::' tests`).
  </read_first>
  <behavior>
    - `Reached::Sign` ("Si&gn (experimental)", 'g') and `Reached::Encrypt` ("Encr&ypt (experimental)", 'y') at the end of `Reached::ALL`; the two letter cases pass with them in, and a companion plants G twice and is refused.
    - `wx_compose` builds two native `CheckBox`es from those labels beside Schedule, each named by its label on both channels and described ("Signs this message with your certificate or PGP key. Experimental: no other mail program has checked a signature from Wixen Mail yet." and the Encrypt equivalent); never disabled; the page's Alt+G and Alt+Y toggle them and say the new state.
    - Send (button, Ctrl+Enter, Alt+N): with a box ticked, `what_protection_it_gets` is asked with what is held; `TheKeyIsLocked` asks through `wx_passphrase::ask` and asks again after; `CannotProtect` says its sentence, focus stays in the composer, and the dialog does not end; otherwise the dialog ends and the row is queued with the choice; the draft saved from the composer carries the choice.
    - The announcement after a protected send says "Sent, signed with S/MIME." or "Sent, signed and encrypted with OpenPGP." as it was.
    - `allowed.rs`: a sentence that signing and encrypting is experimental, read by the two descriptions.
    - The target: the composer built in its own process; the two boxes found in Tab order and read over MSAA at their own handles, named "Sign (experimental)" and "Encrypt (experimental)", role check box, not disabled; Alt+G posted through the page's key route toggles Sign and its state reads checked; Send with Encrypt ticked and a recipient with no key leaves the dialog open, says the address, and the temp outbox holds no row; the same with a kept certificate for the recipient and the in-memory signer queues one row whose protection is `SignedAndEncrypted`.
  </behavior>
  <action>
    Red first, on a branch: the `editor_document` cases by module path, the
    target's tests bare, and the count check. Then the members, the boxes,
    the check at Send, the passphrase ask and the sentence, with `cargo
    build` between. Re-take the composer's letters after 13-06 and quote
    them before writing G and Y; if either has been taken, choose a free
    letter by the same grep and say so in the summary.

    Guard records, measured: one on `editor_document.rs` whose break drops
    `Encrypt` from `ALL`, the page's Alt+Y reading red, `suite` the target;
    one on `wx_compose.rs` whose break ends the dialog on `CannotProtect`,
    the nothing-queued reading red, `suite` the target.
  </action>
  <verify>
    <automated>cargo test --lib presentation::editor_document:: && cargo test --lib presentation::wx_compose:: && cargo test --lib application::allowed:: && cargo test --test signing_and_encrypting_from_the_composer && cargo test --test undo_send_is_where_somebody_looks</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the module cases by path, the target's tests bare, and the count check.
    - `grep -c 'Si&gn (experimental)' src/presentation/editor_document.rs` is 1 and `grep -c 'Encr&ypt (experimental)' src/presentation/editor_document.rs` is 1.
    - The five runs pass, counts quoted; the composer's letters before and after quoted.
  </acceptance_criteria>
  <done>A person can tick Sign or Encrypt in the composer by keyboard from anywhere in it, and Send either protects the message or says exactly why it cannot, with nothing sent.</done>
</task>

<task type="auto">
  <name>Task 4: the pages, the changelog, the ledger's two stubs closed, and GAP-05's box</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/comparison.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:887-1134` (Composition Window); `docs/USER_GUIDE.md:704-810` (Composing Email); `docs/comparison.md:46` as 13-14 left it.
    - `.planning/REQUIREMENTS.md:5807-5813` (GAP-05) and the summaries of 13-14, 13-15, 13-18, 13-19 and 13-20.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: Alt+G and Alt+Y in the
    Composition Window, with what Send does when a box cannot be honoured.
    The guide: a `### Signing and encrypting what you send` subsection under
    Composing Email: the two boxes, which family is used and why, what is
    said when a recipient has no key, the passphrase at Send, the Sent copy,
    and that it is experimental. `docs/comparison.md:46`: signing and
    encrypting what you send is offered, S/MIME and OpenPGP, experimental.
    `docs/ALPHA_TESTING.md`: sending signed and encrypted mail is
    experimental and has not been read by another program. The changelog
    under `[Unreleased]`, Added, #52 points 4 and 5, pointing back to the two
    Known limitations lines 13-19 and 13-20 wrote, which are removed.

    GAP-05's box ticked with the date, each `[D]` clause on a named test from
    13-14, 13-15, 13-18, 13-19, 13-20 and this plan; its `[S]` line
    untouched; the traceability row updated.

    Ledger, by hand, both halves: the two stub entries of premise 0 marked
    fixed with this plan's target; one `unrun-verify` for the boxes, the
    refusal sentence and the passphrase at Send under NVDA.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Composition Window/,/^### Folder Actions/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'Alt+G'` is at least 1, and the same for Alt+Y.
    - `grep -c 'nothing goes out signed or encrypted' docs/comparison.md` is 0.
    - GAP-05's line in `.planning/REQUIREMENTS.md` begins `- [x] **GAP-05**`; the two stubs' status is `fixed`.
    - No em dash or en dash; none of the six words; carriage returns 0.
  </acceptance_criteria>
  <done>The pages say how to send protected mail and what happens when it cannot be done, the two stubs are closed by a path a person reaches, and GAP-05 is ticked on named tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the person's choice to the wire | a message asked to be private |
| a stored row to a later send | the choice has to survive a queue, a schedule and a draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-21-01 | Information disclosure | a message asked to be encrypted going out plain | critical | mitigate | refused at Send and again at send time, never a plain fallback; the choice on the row and the draft; two guard records |
| T-13-21-02 | Information disclosure | a Sent copy the sender cannot read | medium | mitigate | the sender is a recipient; a case opens the Sent copy |
| T-13-21-03 | Spoofing | a greyed box read as "not available" with no reason | medium | mitigate | never disabled; the reason said at Send |
| T-13-21-04 | Repudiation | which family protected a message | low | mitigate | the announcement names it; the guide says the order |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass. `scripts/check.sh` through the hook on every commit, never piped; no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the pages, the ledger, the summary and the four completion marks in one documents commit before the merge; `git merge --no-ff`.
- Spoken or shown: yes. The branch is pushed and a pull request opened after its last commit, and `ci.yml`, `nvda.yml` and `accessibility.yml` are read green before the merge; the accessibility run's `compose` scan reaches the two boxes; `main` is not pushed.
- Reachability: `sign_detached`, `encrypt_to` and 13-20's builders now have a non-test caller through the composer's Send and the send loop; the summary names the path.
- Mnemonics: G and Y checked as one set with the composer's other sixteen after 13-06.
- The issue (added 2026-09-24 when the plans were integrated, on 13-25's pattern): after the merge, the executor comments on #52 from the merge commit with what 13-14, 13-15 and 13-18 to this plan built for each point, and leaves it open for Pratik to close, since a message from a real correspondent's key is his account's (GAP-05's last `[S]` line). Commenting is not a publish; closing is his here.
</verification>

<success_criteria>
- GAP-05 ticked with the date, every `[D]` clause on a named test.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-05's `[D]` lines and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-21-SUMMARY.md` when done
</output>
