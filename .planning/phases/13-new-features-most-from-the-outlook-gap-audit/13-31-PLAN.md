---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 31
type: execute
wave: 33
depends_on: [13-30]
files_modified:
  - src/service/windows_zone_names.rs
  - src/service/mod.rs
  - src/service/free_busy.rs
  - src/application/when_people_are_free.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-08]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A guest Microsoft's answer gives working hours for has a zone: the Windows zone name Microsoft writes is turned into the zone it means through Windows' own ICU, and a custom zone or a name Windows does not know gives no zone rather than a wrong one (GAP-08's third [D] line, #57 point 3)."
    - "A guest with a known zone leaves the sentence that says nobody said where they are, and their working day is judged in their own zone."
    - "Each offered time is said with the clock of up to three guests whose zone is known and differs from the organiser's, \"Tuesday at 10:00, which is 15:00 for Ada\", and no clause for anybody else, so the sentence stays bounded."
    - "Off Windows no zone is found and everything behaves as before, the crate still building."
  artifacts:
    - "src/service/windows_zone_names.rs: the_zone_windows_calls(name) -> Option<Tz> behind cfg(target_os = \"windows\") over ucal_getTimeZoneIDForWindowsID, with a fallback of None elsewhere; cases for the names the research probed."
    - "src/service/free_busy.rs: Microsoft's workingHours.timeZone.name read, and the zone carried into Invited where the guest had none."
    - "src/application/when_people_are_free.rs: an offered time said with each known guest's clock beside it, bounded to three."
  key_links:
    - "getSchedule answer -> OneDiary.working_hours.time_zone.name -> windows_zone_names::the_zone_windows_calls -> chrono_tz::Tz -> Invited.zone -> when_we_could_meet judges their day in it -> the sentence drops them from nobody-said-where and adds their clock to each time."
---

<objective>
A guest's own zone, taken from Microsoft's answer, and each offered time
said on their clock as well as the organiser's.

Purpose: GAP-08's third `[D]` line, "a guest's zone shown beside the
time", and #57's point 3: `people_to_ask_about` sets `zone: None`
(`asking_when_free.rs:232`), so every guest's working day is judged in
the organiser's zone and the answer says so. Microsoft's `getSchedule`
returns each guest's working hours with a Windows zone name, and
RESEARCH-3 probed Windows' ICU with the `windows` crate at the pinned
version and the feature already on: "Pacific Standard Time" came back
"America/Los_Angeles".

Output: the zone name module, the working hours read, the time said on
the guest's clock, the guide's section on finding a time, the changelog
and the ledger. A zone on a contact is later work (decision 1).
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-30-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. Microsoft's answer now; a contact's zone later (RESEARCH-3 question
7).** A time zone field on a contact touches the editor and its three
syncs (12-07 measured about seventeen files for one contact field); it
is ledgered as its own later plan.

**2. Windows' ICU, not a table (RESEARCH-3, Don't Hand-Roll).** Windows
keeps the mapping current; no crate and no feature is added.

**3. At most three clocks per time (planner's bound, recorded).** One
clause per guest whose zone is known and differs from the organiser's,
three at most, so a meeting of twenty is not twenty clauses per time.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said.

```
sed -n 399,402p Cargo.toml   -> windows 0.62.2 with Win32_Globalization, under cfg(windows) dependencies
sed -n 2557,2564p ~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/Globalization/mod.rs   -> ucal_getTimeZoneIDForWindowsID(winid, len, region, id, idcapacity, status) linked from icuin.dll
grep -rn 'ucal_\|Win32::Globalization' src --include=*.rs   -> outlook_data_file.rs:1203 and spellcheck/windows_speller.rs use the feature; no ICU call yet
sed -n 476,531p src/service/free_busy.rs   -> OneDiary { schedule_id, schedule_items, error }: workingHours is not read
sed -n 226,236p src/application/asking_when_free.rs   -> people_to_ask_about: zone None for everybody
sed -n 471,487p src/application/when_people_are_free.rs   -> "Nobody said where {names} is, so the times were judged against the working hours set here."
grep -n '^chrono-tz' Cargo.toml   -> "0.10"
```

The research's probe also returned "Asia/Calcutta" for "India Standard
Time", an old name; the cases require `chrono-tz` to parse what ICU
returns for each probed name, so an old name that the zone database
dropped would go red here rather than become no zone in a meeting.

**Guard records, both readings.** `windows_zone_names.rs` new at zero;
`src/service/mod.rs` gains a line, no records; `free_busy.rs` and
`when_people_are_free.rs` carry what 13-29 and 13-30 left (0 and 0,
and 1 by `tests_last_seen`, at `630e2a67`), tests added, so the count
check is named bare where it fires and those records re-measured.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: a Windows zone name turned into the zone it means</name>
  <files>src/service/windows_zone_names.rs, src/service/mod.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/this_machine.rs:140-230` (a Windows answer behind `cfg(target_os = "windows")` with a fallback elsewhere, the shape to follow) and `src/service/spellcheck/windows_speller.rs:30-40` (the `windows` crate's Globalization imports in use).
    - RESEARCH-3's code example for the call (its "Code Examples" section).
  </read_first>
  <behavior>
    - `the_zone_windows_calls(name: &str) -> Option<Tz>`: on Windows, "Pacific Standard Time" gives `America/Los_Angeles`, "GMT Standard Time" `Europe/London`, "W. Europe Standard Time" `Europe/Berlin`, "UTC" a zone at offset zero, "India Standard Time" a zone `chrono-tz` parses (whatever name ICU returns); "Customized Time Zone", "Not A Zone" and an empty name give `None`.
    - The call converts lengths with `i32::try_from`, reads the status and the written length, and never reads past what was written; elsewhere the function is `None` for every name, and a case under `cfg(not(target_os = "windows"))` says so.
  </behavior>
  <action>
    Red first, on the branch `13-31-a-guests-own-clock`: the cases by
    module path (`service::windows_zone_names::tests::<name>`), a new
    module at zero records. Then the module (per decision 2), with `cargo
    build` between. Invoke `elegant-code`: one unsafe call in one small
    function with its reason in a comment; the parse into `Tz` outside
    it.

    Guard record: one whose break returns the name unchanged instead of
    asking ICU, the Pacific case red; measured.
  </action>
  <verify>
    <automated>cargo test --lib service::windows_zone_names::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the cases by module path.
    - The module passes with at least 6 tests on this machine, quoted; one guard record measured.
  </acceptance_criteria>
  <done>A Windows zone name becomes the zone it means through Windows' own data, or no zone, never a wrong one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Microsoft's working hours give the guest a zone, and each time is said on their clock too</name>
  <files>src/service/free_busy.rs, src/application/when_people_are_free.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/free_busy.rs:470-560` and `:745-790` (as 13-30 left the merge).
    - `src/application/when_people_are_free.rs:185-200`, `:340-360`, `:465-520` and `:620-660`.
  </read_first>
  <behavior>
    - `OneDiary` reads `workingHours.timeZone.name`; `what_microsoft_said` carries the zone per person through `the_zone_windows_calls`; the merge of 13-30 keeps a zone from any place that gave one, and a zone the person already had is never overwritten. Case from a recorded answer: "Pacific Standard Time" gives the guest `America/Los_Angeles`; "Customized Time Zone" gives none.
    - `when_people_are_free`: a guest with a zone leaves `where_they_are_not_known`; each offered time is said "Tuesday at 10:00, which is 15:00 for Ada" for up to three guests whose zone is known and whose clock differs from the organiser's, in invitation order, and "and 13:00 for Bo" for the next; a guest in the organiser's own zone adds no clause. Cases: one guest in another zone; one in the same zone; four in other zones, three clauses; a day boundary ("Tuesday at 23:00, which is Wednesday 07:00 for Ada").
  </behavior>
  <action>
    Red first: the cases by module path, and the count check
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    named bare where records name these files. Then the read and the
    sentences (per decision 3), with `cargo build` between. Invoke
    `elegant-code`: the clause is one function over a time, a zone and a
    name; the bound is a named constant.

    Guard records: one on `free_busy.rs` whose break stops reading
    `workingHours`, the Pacific case red; one on `when_people_are_free.rs`
    whose break drops the bound, the four-guests case red; the records
    naming these files re-measured.
  </action>
  <verify>
    <automated>cargo test --lib service::free_busy:: && cargo test --lib application::when_people_are_free:: && cargo test --lib application::asking_when_free:: && cargo test --test item_form_free_busy</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the cases by module path.
    - `grep -c 'working_hours\|workingHours' src/service/free_busy.rs` is at least 2; `grep -c 'the_zone_windows_calls' src/service/free_busy.rs` is at least 1.
    - Both modules pass above their counts after 13-30, quoted; the records measured.
  </acceptance_criteria>
  <done>A colleague on Microsoft is judged by their own working day and hears the offered time on their own clock.</done>
</task>

<task type="auto">
  <name>Task 3: the guide's section on finding a time, the alpha page, the changelog and the ledger</name>
  <files>docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md:1068-1101` (Times in events and reminders), which the new section follows.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide gains "### Finding a time everyone
    is free" after Times in events and reminders: what is asked and
    where (a calendar server, Microsoft and Google, every one the account
    has), what a guest's time being unknown means and the reasons it
    says, a guest's zone from Microsoft and the time said on their clock,
    and that it is experimental. The alpha page: zones have been taken
    only from recorded answers. The changelog, `[Unreleased]`, Added,
    #57 point 3.

    Ledger, by hand, both halves: one `todo` for a time zone field on a
    contact (decision 1) as its own later plan; one `unrun-verify` for
    phase 14 (a real colleague's zone from `getSchedule`); one
    `unrun-verify` for the tester's ear (a time heard with a guest's
    clock). After the merge, the executor comments on #57 from the merge
    commit with what closed each point and leaves it for Pratik. One
    documents commit on the branch before the merge carries the pages,
    the ledger, the summary and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Finding a time everyone is free' docs/USER_GUIDE.md` is 1; the changelog names #57.
    - The ledger entries in both halves; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The guide describes finding a time as it now works, and the contact zone and what only a real colleague settles are ledgered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Microsoft's zone name to a Windows call | untrusted text handed to a C function |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-31-01 | Tampering | a long or strange zone name overrunning the output buffer | medium | mitigate | the capacity passed is the buffer's own length through `i32::try_from`; only the written length is read; the status checked; cases for an unknown and an empty name |
| T-13-31-02 | Tampering | a wrong zone making a guest look free | medium | mitigate | an unknown or custom name gives no zone, which keeps today's organiser-zone judgement and its sentence; a zone the person had is never overwritten |
| T-13-31-03 | Denial of service | a clause per guest per time | low | mitigate | bounded to three per time |
| T-13-31-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate and no feature is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is spoken (the answer's sentences): push the branch, open a pull request, wait for CI, NVDA and Accessibility, then merge locally. Pushing `main` is Pratik's.
- Red commits name lib tests by module path and integration tests bare. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-08's third `[D]` line has a named test in the summary and is marked with the date; with 13-29 and 13-30 marked, GAP-08's box is ticked with the date and its traceability row updated; its `[S]` line is untouched. 13-32 (ledger 417) is not a GAP-08 `[D]` line and does not hold the box.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-08's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-31-SUMMARY.md` when done
</output>
