---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 3
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - Cargo.toml
  - src/presentation/printing.rs
  - src/presentation/mod.rs
  - src/application/printing.rs
  - src/presentation/wx_app.rs
  - tests/printing_draws_what_the_layout_says.rs
  - tests/printing_spools_a_document.rs
  - tests/print_is_on_the_file_menu.rs
  - tests/wired.rs
  - .github/workflows/ci.yml
  - .github/workflows/guards.yml
  - .github/workflows/mutants.yml
  - .github/workflows/release.yml
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/privacy.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: false
requirements: [GAP-01]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "File, Print (Ctrl+P, letter P) in the main window prints the message under the cursor in the message list through Windows' own print dialog, so the printer, the copies and the pages are the ordinary dialog's, with the header lines and the text laid out by 13-02 (GAP-01, #45)."
    - "Printing says one sentence when the job is sent, naming the message, the printer and the page count, and nothing per page; a cancelled dialog says nothing was printed; a failure says what failed; nothing under the cursor is refused with the one refusal sentence."
    - "Every line the layout placed is drawn on its page, read back from an enhanced metafile on any machine including CI, and a document spooled to Microsoft Print to PDF on this machine has the page count the layout gave."
    - "CI says the PDF printer is absent through WIXEN_NO_PDF_PRINTER, and where the flag is set the spool test asserts the printer really cannot be opened, so the flag cannot hide a working printer."
    - "The print job's name in Windows' queue is plain ('Wixen Mail message'), the log carries the printer name and page count and never the text or subject, and the privacy page says printing sends the text to the chosen printer."
  artifacts:
    - "Cargo.toml: three features of windows 0.62.2 (Win32_Graphics_Gdi, Win32_Storage_Xps, Win32_UI_Controls_Dialogs), each with a comment, only after Pratik's confirmation."
    - "src/presentation/printing.rs: the page metrics from a device context, draw_page, print (StartDocW to EndDoc, AbortDoc on failure), NotPrinted telling cancelled from failed, ask_for_a_printer over PrintDlgExW, GlobalLock and GlobalUnlock declared by hand."
    - "src/application/printing.rs: the pages a dialog's page ranges choose, pure and tested."
    - "src/presentation/wx_app.rs: ID_PRINT on File, the arm, the message composition extracted from open_in_the_text_reader so the reader and paper compose once."
    - "tests/printing_draws_what_the_layout_says.rs, tests/printing_spools_a_document.rs, tests/print_is_on_the_file_menu.rs."
  key_links:
    - "Ctrl+P -> ID_PRINT -> the cursor row's message -> the extracted composition with printing::on_paper(out) -> printing::from_document -> ask_for_a_printer(frame) -> lay_out with the chosen printer's metrics -> print -> one sentence."
    - "WIXEN_NO_PDF_PRINTER in every workflow that runs the tests on a Windows runner -> the spool test's absent-printer branch, which asserts CreateDCW fails."
---

<objective>
File, Print and Ctrl+P in the main window, for the message under the
cursor in the message list, through Windows' own print dialog, drawing
13-02's pages with GDI and spooling them as one job.

Purpose: #45, the tester on 2026-09-15: "Add print functionality." The
issue: File, Print (`Ctrl+P`) "through the native Windows print dialog
so the printer, copies and pages are the ordinary dialog's", printing
"the header lines (From, To, Cc, Date, Subject) and the text", and
"Announce what was sent to print and to which printer, once." GAP-01's
`[D]` line asks for "the route (the native printout or the browser's
print) decided by a measurement": the research measured wxdragon's
printing as unable to start a job on Windows (research 1.2) and probed
the Win32 route to a PDF and to a metafile (research 1.3); this plan
builds that route. The message list is the first surface; 13-04 adds
the reader window, a conversation row, the formatted conversation
window and the other five modules, where research K2's split put the
reader window and the conversation row here. They move so this plan is
the device layer and one surface end to end, and nothing it adds is
left without a caller.

Output: the three manifest features (after Pratik's word), the Win32
transport, the page-range rule, three targets, the CI flag, File,
Print on the message list, the pages, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-1-keyboard-basics.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-02-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said.

**1. The manifest, and a fourth call the research did not list.**

```
sed -n 399,439p Cargo.toml   -> windows 0.62.2 with eleven features, each with a comment; no Gdi, Xps or Controls_Dialogs
grep -n 'pub unsafe fn PrintDlgExW' -B3 ~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/UI/Controls/Dialogs/mod.rs   -> :96-98, behind Win32_Graphics_Gdi as well as its own feature
grep -n 'pub unsafe fn StartDocW' -B3 ~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/Storage/Xps/mod.rs   -> :71-73, the same
grep -rn 'pub unsafe fn GlobalLock\|pub unsafe fn GlobalFree' ~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/   -> GlobalFree in Foundation (on), GlobalLock and GlobalUnlock in System/Memory/mod.rs:158 and :174, a feature not on
grep -rn '#\[link(name = "kernel32")\]' src/   -> date_display.rs:148 and four in common/how_the_machine_writes_dates.rs: the tree's pattern for a flat call without a feature
```

Reading the printer's name out of `PRINTDLGEXW.hDevNames` needs
`GlobalLock`, which the research's three features do not give. It is
declared by hand in a `#[link(name = "kernel32")]` block, the pattern
at `date_display.rs:148`, so the manifest question stays at three
features and no fourth is asked for. Features are not recorded in
`Cargo.lock` (research section 4, measured with a probe), so the lock
does not change; `scripts/which-checks.sh` answers `all` for a commit
touching `Cargo.toml`, so that commit pays the whole gate, and the
branch's merge earns everything too.

**2. The File menu, its letters, and the key.**

```
sed -n 6661,6727p src/presentation/wx_app.rs   -> Save, Save As, a separator, Check Mail, a separator, Open Draft, a separator, Import Mailbox, Import a Folder of Messages, Export Mailbox, Import PGP Private Key (:6718-6722), a separator (:6723), Quit (:6724); the New submenu prepended at :6727-6728
awk 'NR>=6640 && NR<=6728' src/presentation/wx_app.rs | grep -o '"[^"]*&[^"]*"'   -> on File itself: &New, &Save, Save &As, Check &Mail, Open &Draft, &Import Mailbox, Import a F&older, &Export Mailbox, Import PGP Private &Key, &Quit: N, S, A, M, D, I, O, E, K, Q; P is free
grep -rnE 'Ctrl\+P([^a-zA-Z]|$)' src/ docs/KEYBOARD_SHORTCUTS.md   -> only `NVDA+Ctrl+P` at KEYBOARD_SHORTCUTS.md:1363; Ctrl+P is bound nowhere
```

Print goes in a group of its own between the PGP key item and Quit:
a separator, "&Print...\tCtrl+P", then the existing separator and
Quit. RESEARCH-CROSS-CHECK.md says later plans may replace the PGP key
item (13-17) and claim F and X (13-45, 13-46); none claims P, and
each of those re-takes the letter reading after this lands.

**3. What composes a message, and what reads that composition.**

```
sed -n 14106,14127p src/presentation/wx_app.rs   -> open_in_the_text_reader: the body from the cache, what_a_message_shows_and_says, attachments_of, then reader_text::single_message(&message, &shown.body, out).with_what_is_said(&shown.said)
grep -n 'open_in_the_text_reader\|what_a_message_shows_and_says' tests/wired.rs   -> :3386 and :4248 read open_in_the_text_reader's body for with_what_is_said(, and :4292 lists it among the surfaces that must call what_a_message_shows_and_says(
sed -n 756,774p src/presentation/wx_app.rs   -> what_the_cursor_stands_for and the_loaded_message_the_row_stands_for, which Save As uses (save_the_message_as at :14940)
```

The composition is extracted into one function both the reader and
Print call, `a_message_as_the_reader_shows_it(cache, message, out) ->
ReaderDocument`, and the three readings in `tests/wired.rs` are
rewritten in place to name it, since they are about whichever function
composes, not about its old name. No test is added to `wired.rs`.

**4. Refusals and sentences.** `status_sentences::nothing_chosen(Thing::MESSAGE)`
(`status_sentences.rs:117`) is the one refusal; 13-02 built
`printing::sent_to_the_printer`, `nothing_was_printed` and
`printing_failed`, each held to the sentence shape.

**5. CI and the PDF printer.**

```
grep -n 'WIXEN_NO_AUDIO' .github/workflows/*.yml   -> ci.yml:20, guards.yml:86, mutants.yml:66, other-platforms.yml:38; release.yml runs `cargo test --quiet` (:67) on windows-latest with no WIXEN_NO_AUDIO at all
grep -n 'runs-on' .github/workflows/ci.yml   -> windows-latest; "Microsoft Print to PDF" was removed from the Windows Server 2025 image (actions/runner-images#12328, research 1.3)
```

Every workflow that runs the tests on a Windows runner gets
`WIXEN_NO_PDF_PRINTER: "1"` beside its other environment: `ci.yml`,
`guards.yml`, `mutants.yml` and `release.yml`'s workflow-level `env`.
`other-platforms.yml` builds on Linux and macOS, where the targets are
`#![cfg(windows)]` and compile to nothing. `release.yml`'s missing
`WIXEN_NO_AUDIO` is a finding this plan ledgers and does not fix,
because the release gate is Pratik's (guardrail 7).

Why the two flags are treated differently, added 2026-09-24 when the
plans were integrated: `WIXEN_NO_PDF_PRINTER` goes into `release.yml`
because this plan's own spool target would otherwise turn the release
gate red on a runner with no PDF printer, so leaving it out would be
this plan breaking the release; the line adds an environment value and
changes no trigger, no level and no step. `WIXEN_NO_AUDIO` was missing
before this plan and no test this plan adds needs it, so whether the
release gate gains it is his. The summary names the `release.yml` line
as the one change this plan makes to the release workflow, and the
ledger `todo` for `WIXEN_NO_AUDIO` names it too, so he sees both in one
place and decides them together.

**6. Records this plan touches, by both readings.** Counted with the
TOML reader on 2026-09-24 (1,088 records).

- By `tests_last_seen` and `file`: `src/presentation/wx_app.rs` 119
  (199 tests); `tests/wired.rs` 19 (77 tests); `src/application/printing.rs`
  as 13-02 leaves it; `Cargo.toml` 0; `.github/workflows/ci.yml` 2,
  `guards.yml` 1, `mutants.yml` 2, `release.yml` 3 (each by `file`);
  the new files 0. No test is added to `wx_app.rs` or `wired.rs`.
- By anchor text inside the regions edited: the File menu
  (:6661-6728) holds one, "a PGP private key somebody can import",
  anchored on the whole `.append_item(ID_IMPORT_PGP_KEY, ...)` call at
  :6718-6722; Print goes after it and outside that text, so it still
  names one place. `open_in_the_text_reader` (:14100-14200) holds one,
  "a PGP message the reader window never offers to the key", anchored
  on the `let shown = what_a_message_shows_and_says(...)` line and the
  comment line after it (:14118-14119); the extraction moves those two
  lines into the new function at the same indentation, so the text
  should still name one place and the record's red list (two tests in
  `wired.rs` reading the composing function) moves with the readings'
  rewrite. The record is re-measured in the extraction's commit. The
  workflows' records are read at execution with the same script before
  the env lines go in, since an anchor on an `env:` block moves when a
  line is added inside it.
</premise_corrections>

<decisions date="2026-09-24">
Taken 2026-09-24 from the research's recommendation (RESEARCH-1,
section 3); Pratik may overrule.

- **Decision 1, the three features:** recommended yes, and **still his
  to confirm**; the checkpoint below stops only if the executor's brief
  does not carry his answer.
- **Decision 2, plain text first:** header lines and words, no pictures
  or formatting; the guide and the changelog say so.
- **Decision 3, Print Preview:** not now. Windows' dialog has none; the
  guide says so plainly.
- **Decision 4, the job's name:** plain and by kind, through 13-02's
  `job_name`.
- **Decision 6, the size:** a fixed 11 point Segoe UI, black on white
  whatever the theme, no new setting.
- **Decision 7, what is said:** one sentence when the job is sent,
  naming the message, the printer and the pages.
- **Decision 11, telling wxDragon about its printing bug:** yes, file
  it, but it is a public post and Pratik's to make. This plan writes
  the draft into its summary and ledgers it; it never posts it.
- **Decision 12, PDFPurr reads Microsoft Print to PDF files as
  nonsense:** a ledger entry now, the fix in PDFPurr on Pratik's
  schedule.
</decisions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: the three windows features for printing (stop here only if the brief does not carry Pratik's answer)</name>
  <decision>Switch on three features of the windows 0.62.2 library the program already builds: Win32_Graphics_Gdi, Win32_Storage_Xps and Win32_UI_Controls_Dialogs.</decision>
  <context>
    Printing needs the drawing, print-job and print-dialog parts of the
    Windows library already in the program. No new library is added and
    nothing new is downloaded: the probe resolved the same 16 packages
    with none new (research section 4), and `Cargo.lock` does not
    change because features are not recorded there. Pratik confirms
    every package before it is installed, and the research recommended
    keeping that rule whole for features too (question 1). If the
    executor's brief carries Pratik's answer, act on it and do not
    stop: "yes" is option a, "no" is option b. Only when the brief
    carries no answer does the executor stop here, report this
    checkpoint in plain words, and wait.
  </context>
  <options>
    <option id="option-a">
      <name>Confirm the three features (recommended)</name>
      <pros>The typed functions and structures come from Microsoft's own bindings; a wrong field size is a compile error rather than a crash; eleven features are already used this way.</pros>
      <cons>More compiled bindings inside the existing crate, unmeasured in size; the Cargo.toml commit pays the whole gate.</cons>
    </option>
    <option id="option-b">
      <name>Decline, and declare every call by hand</name>
      <pros>The manifest does not change.</pros>
      <cons>PRINTDLGEXW, PRINTPAGERANGE, DEVNAMES, DOCINFOW and LOGFONTW written out by hand in `#[link(name = "gdi32")]` and `#[link(name = "comdlg32")]` blocks; a field size wrong by hand is a crash at run time, found only by the dialog opening. The tasks below are otherwise unchanged.</cons>
    </option>
  </options>
  <resume-signal>Pratik's answer, "yes" or "no", carried in the brief or given here.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: the device layer end to end: the features, drawing a laid-out page, spooling a job, and a metafile that reads every line back</name>
  <files>Cargo.toml, src/presentation/printing.rs, src/presentation/mod.rs, tests/printing_draws_what_the_layout_says.rs, guards/guards.toml</files>
  <read_first>
    - Premise 1 above; research section 1.3 (the two probes' outputs and what each call answered) and section 4.
    - `src/application/printing.rs` as 13-02 left it (`Page`, `lay_out`, `job_name`, `Kind`) and `13-02-SUMMARY.md`.
    - `src/presentation/date_display.rs:140-175` (a hand-declared `#[link]` block with its comments) and `src/presentation/accessibility/names.rs:455-480` (SAFETY comments on Win32 calls here).
    - `target/debug/wxWidgets/src/msw/printwin.cpp:210-270` (how wxWidgets tells a cancelled Save dialog of a file printer from a failure, `wxPrinterOperationCancelled`).
  </read_first>
  <behavior>
    - `Cargo.toml` (option a) gains the three features in the windows list, each with a comment on the pattern of the eleven (what it is for, which calls it gives); option b adds nothing to the manifest and declares the calls by hand.
    - `presentation::printing` (Windows code behind `#[cfg(target_os = "windows")]`, a fallback elsewhere that answers `NotPrinted::NotOnThisPlatform` with a sentence): `Metrics::of(hdc, font)` gives the printable width and the lines a page holds at a fixed 11 point Segoe UI (decision 6), and `measure(hdc, text)` through `GetTextExtentPoint32W`; `draw_page(hdc, &Page, &Font)` draws the stamp and each line once with `DrawTextW` and `DT_SINGLELINE | DT_NOPREFIX | DT_EXPANDTABS`, black on white; `print(hdc, job_name, &[Page]) -> Result<Printed, NotPrinted>` runs `StartDocW`, then `StartPage`, `draw_page`, `EndPage` per page, then `EndDoc`, and `AbortDoc` when any step fails; `NotPrinted` is `Cancelled` (a file printer's Save dialog closed, `StartDocW` answering the cancelled error) or `Failed(String)` in words.
    - The target (`#![cfg(windows)]`): lays out a fixed message with 13-02's `lay_out` using `measure` on a metafile DC, draws each page into its own enhanced metafile with `draw_page`, reads every `EMR_EXTTEXTOUTW` string back with `EnumEnhMetaFile`, ignores records that are whitespace alone, and asserts the strings on each page are that page's stamp and lines in order, accents and an ampersand intact; a companion hands the reading a page with a line dropped and is refused. It needs no printer and runs on CI.
  </behavior>
  <action>
    After the checkpoint's answer. This is the tracer for the layers
    this plan adds: a laid-out page goes through the transport and
    comes back from a recording surface, before the dialog or a menu.
    Red first, on a branch cut from `main`: the target with
    `presentation::printing`'s signatures answering failure, committed
    with `Fails-until-green:` naming the readings bare. The manifest
    line lands in its own `build(13-03)` commit before the red, alone,
    never red, so its whole-gate run reads a tree with nothing else in
    it (the 12-07 pattern). Then the transport, with `cargo build`
    between.

    Invoke `elegant-code` and `defensive-boundaries`: every `unsafe`
    block carries a SAFETY comment; the font and the device context are
    owned by small guards that release them on drop (`DeleteObject`,
    `DeleteDC`), so an early return cannot leak a GDI object; the
    wide-string conversion is one helper. Nothing here logs the text or
    the title; the log line on success is the printer's name and the
    page count, and on failure the step that failed (CLAUDE.md, "Never
    log a token, password, or message body"). The measuring and
    drawing use the same font object, so the layout and the page agree
    by construction, and the target proves it.

    Guard records, each measured by taking the break by hand: one on
    `printing.rs` whose break drops the last line of a page in
    `draw_page` (the target red), `suite` the target; one whose break
    drops `DT_NOPREFIX` (the ampersand reading red), `suite` the target.
  </action>
  <verify>
    <automated>cargo test --test printing_draws_what_the_layout_says && cargo test --lib application::printing::</automated>
  </verify>
  <acceptance_criteria>
    - The `build(13-03)` commit touches `Cargo.toml` alone (option a) and `git diff --stat HEAD~1 -- Cargo.lock` after it is empty; under option b no manifest commit exists and the summary says so.
    - A red commit on the branch names the target's readings bare.
    - `grep -c 'SAFETY' src/presentation/printing.rs` is at least `grep -c 'unsafe {' src/presentation/printing.rs`, both read in one command and quoted.
    - The target passes, its count quoted; two guard records measured.
  </acceptance_criteria>
  <done>A page 13-02 laid out is drawn line for line and read back from a metafile on any machine, and a job can be started, drawn and ended or aborted with a cancel told apart from a failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Windows' print dialog, the pages it chooses, a real spool on this machine, and the flag that says CI has no PDF printer</name>
  <files>src/presentation/printing.rs, src/application/printing.rs, tests/printing_spools_a_document.rs, .github/workflows/ci.yml, .github/workflows/guards.yml, .github/workflows/mutants.yml, .github/workflows/release.yml, guards/guards.toml</files>
  <read_first>
    - Premises 1 and 5 above; `target/debug/wxWidgets/src/msw/printdlg.cpp:840-1000` (how wxWidgets fills `PRINTDLGEXW`, reads its answer and frees `hDevMode` and `hDevNames`).
    - `.github/workflows/ci.yml:1-30`, `guards.yml:75-95`, `mutants.yml:55-70`, `release.yml:30-70`.
    - `src/presentation/accessibility/feedback.rs:1140-1150` and `:1455-1500` (`WIXEN_NO_AUDIO` read, and the test that holds the flag honest).
    - The guard records naming each workflow by `file`, read with the TOML reader before the env lines go in (premise 6).
  </read_first>
  <behavior>
    - `application::printing::pages_chosen(ranges: &[(u32, u32)], all: bool, total: usize) -> Vec<usize>`: every page when `all`; otherwise the pages the ranges name, in order, each once, a range past the end cut at the last page, an empty answer when nothing is left; cases for all, one range, two overlapping ranges, a range past the end, and a range wholly past the end.
    - `ask_for_a_printer(owner: &Frame) -> Result<Option<Chosen>, NotPrinted>` over `PrintDlgExW` with `PD_RETURNDC`, `PD_NOSELECTION`, `PD_NOCURRENTPAGE`, `PD_USEDEVMODECOPIESANDCOLLATE` and page ranges allowed, the owner the frame the command came from so focus returns there; `Chosen { hdc, printer_name, ranges, all }`; the printer name read from `DEVNAMES` through the hand-declared `GlobalLock` and `GlobalUnlock`; `hDevMode` and `hDevNames` freed on every path; a cancelled dialog answers `Ok(None)`.
    - The target `tests/printing_spools_a_document.rs` (`#![cfg(windows)]`): with `WIXEN_NO_PDF_PRINTER` unset, opens "Microsoft Print to PDF" with `CreateDCW` and a `DOCINFOW.lpszOutput` naming a file under a temporary directory so no Save dialog opens, lays out a message long enough for three pages at that printer's metrics, runs `print`, and asserts `Printed` says three pages and `pdfpurr` counts three pages in the file (a page counter only: `pdfpurr` 0.4.0 does not apply this PDF's ToUnicode map, research 1.3); with the flag set, asserts `CreateDCW` for that printer really answers no device context, so the flag cannot hide a working printer.
    - `WIXEN_NO_PDF_PRINTER: "1"` in the workflow-level `env` of `ci.yml`, `guards.yml`, `mutants.yml` and `release.yml`, each with a comment on the pattern of `WIXEN_NO_AUDIO`'s naming runner-images#12328.
  </behavior>
  <action>
    Red first, on the branch: `pages_chosen`'s cases by module path and
    the spool target's reading bare, in one red commit under
    `Fails-until-green:`. Then `pages_chosen`, `ask_for_a_printer` and
    the flag, with `cargo build` between. The dialog cannot be opened
    by a test (it needs a person), so `ask_for_a_printer` is kept thin:
    everything it decides is `pages_chosen` or a field copied out, and
    its one listening line is ledgered in task 3.

    Invoke `elegant-code` and `defensive-boundaries`: the `PRINTDLGEXW`
    is filled in one place with every field named; the two global
    handles are owned by a guard that frees them on drop; the printer
    name is read with a bound on its length. The workflow edits are one
    env line and one comment each; read each workflow's guard records
    first and re-anchor any record whose anchor the new line splits, in
    the same commit, re-measured.

    Guard records, measured: one on `application/printing.rs` whose break
    lets `pages_chosen` repeat a page two ranges both name (its case
    red); one on the spool target whose break skips `EndDoc` (the page
    count reading red), carrying the comment the register uses when a
    runner cannot judge a record (`guards.toml:2293`), because every
    runner sets the flag.
  </action>
  <verify>
    <automated>cargo test --lib application::printing:: && cargo test --test printing_spools_a_document && cargo test --test printing_draws_what_the_layout_says</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the page-range cases by module path and the spool reading bare.
    - `grep -c 'WIXEN_NO_PDF_PRINTER' .github/workflows/ci.yml .github/workflows/guards.yml .github/workflows/mutants.yml .github/workflows/release.yml` reports at least 1 for each of the four files.
    - The spool target passes on this machine with the flag unset, its three-page reading quoted; run once more with `WIXEN_NO_PDF_PRINTER=1` set in the shell and its absent-printer reading quoted as what it answered here (it must fail on this machine, where the printer exists, which is the flag's honesty proved by hand; the summary quotes that failure and the command).
    - Two guard records measured; every workflow record read before the edit and any re-anchored one re-measured.
  </acceptance_criteria>
  <done>Windows' own dialog chooses the printer, copies and pages, a real three-page job reaches Print to PDF here, and CI says honestly that it has no PDF printer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: File, Print on the message under the cursor, one composition for reader and paper, the pages, the changelog and the ledger</name>
  <files>src/presentation/wx_app.rs, tests/print_is_on_the_file_menu.rs, tests/wired.rs, guards/guards.toml, docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/privacy.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - Premises 2, 3 and 4 above; `src/presentation/wx_app.rs:6661-6730` (File), `:5600-5620` (the Save As arm, the dispatch shape), `:14100-14130` (`open_in_the_text_reader`), `:14940-14990` (`save_the_message_as` and its cursor-row lookup).
    - `tests/wired.rs:3380-3390`, `:4240-4320` (the readings that name the composing function).
    - `tests/undo_send_is_where_somebody_looks.rs:20-70` (a reading of a menu chain and an arm, with companions) as the shape for the new reading.
    - `docs/KEYBOARD_SHORTCUTS.md:582-623` (File Menu), `docs/USER_GUIDE.md` (Message Actions at :566, Import and Export at :963), `docs/privacy.md` (`grep -n -i print docs/privacy.md` matches five lines, :560 to :568, every one "fingerprint").
  </read_first>
  <behavior>
    - `a_message_as_the_reader_shows_it(cache, message, out) -> ReaderDocument` holds the body read, `what_a_message_shows_and_says`, `attachments_of` and `single_message(...).with_what_is_said(...)`; `open_in_the_text_reader` calls it; `tests/wired.rs`'s three readings name it (no test added there).
    - `ID_PRINT` in the id list; File gains a separator and "&Print...\tCtrl+P" ("Print the message or item you are on, with its header lines") after Import PGP Private Key and before the separator above Quit.
    - The arm, in Mail: the cursor row's message through `what_the_cursor_stands_for` and `the_loaded_message_the_row_stands_for` (as Save As finds it); composed through `a_message_as_the_reader_shows_it` with `printing::on_paper(out)`; `printing::from_document`; `ask_for_a_printer(&frame)`; the chosen printer's `Metrics`; `lay_out`; `pages_chosen`; `print` with `job_name(Kind::Message)`; then one sentence: `sent_to_the_printer` on success, `nothing_was_printed` on a cancel or a cancelled Save dialog, `printing_failed` on a failure, each shown and spoken once. Nothing under the cursor is refused with `nothing_chosen(Thing::MESSAGE)`. A conversation row, in this plan, prints the message the row stands for and the sentence says so; 13-04 prints the whole conversation. In the other five modules Print says, in one sentence, that it prints messages in this build and to switch to Mail, which 13-04 replaces.
    - The reading `tests/print_is_on_the_file_menu.rs`: File appends ID_PRINT with "&Print...\tCtrl+P"; the ID_PRINT arm calls `a_message_as_the_reader_shows_it(`, `on_paper(`, `ask_for_a_printer(` and `print(`; companions plant a Print item without its key, an arm that composes its own document, and an arm that skips the dialog, and each is refused.
  </behavior>
  <action>
    Red first, on the branch: the reading, committed with
    `Fails-until-green:` naming its readings bare. Then the extraction
    (its own commit, green, the three `wired.rs` readings rewritten and
    the record "a PGP message the reader window never offers to the
    key" re-measured in it), then the id, the item and the arm, with
    `cargo build` between. Invoke `elegant-code`: the arm is a
    sequence of calls to tested pieces and one match on the result;
    no printing decision lives in `wx_app.rs`.

    Guard records, measured: one on `wx_app.rs` whose break composes
    the printed message without `on_paper` (the reading red), `suite`
    the reading; one whose break removes the item's key (the reading
    and `wired`'s agreement test red, whatever reddens written down).
    `wx_app.rs` stays at 199 tests and `wired.rs` at 77, quoted.

    Invoke `writing-craft` for the pages. `KEYBOARD_SHORTCUTS.md`: a
    Print row in the File Menu table (`Ctrl+P`, "Print the message you
    are on in the message list, through Windows' print dialog. The
    reader window, the conversation window and the other modules
    follow"), no promise of a date. `USER_GUIDE.md`: a Printing section
    under Reading and Managing Email saying what prints (the header
    lines and the words, full date, 11 point black on white), what does
    not (pictures and formatting, decision 2; no preview, decision 3),
    that the dialog is Windows' own, what is said after, that the job's
    name in the queue is plain, and that a web page opened from a link
    is not printed here. `privacy.md`: a short section saying printing
    sends the message's text to the printer you choose through Windows'
    print spooler, where it waits until printed, that the job is named
    "Wixen Mail message" rather than the subject, and that the log
    keeps the printer's name and the page count only. The changelog,
    `[Unreleased]`, Added: Print in the tester's words (#45), with
    Known limitations (plain text, no preview, the message list first,
    nobody has heard the dialog or seen a page). The version as 13-01
    says: moved once only if a build was cut since `1.0.0-alpha.1`.

    Ledger, by hand, both halves: close 13-02's `stub` entry with this
    plan's reading and spool target named; open an `unrun-verify` for
    the dialog under NVDA and Narrator (the printer list, Preferences,
    the page range and copies heard; focus back on the list) and a
    page on paper looked at; open a `todo` for filing the wxDragon
    printing defect upstream (the four source lines of research 1.2,
    Pratik's call because it is a public post; the draft text is in
    the summary; nothing is posted); open a `todo` for `pdfpurr` 0.4.0
    not applying the ToUnicode map of a Microsoft Print to PDF file, so
    such a PDF attachment reads as nonsense (research 1.3; a fix in
    PDFPurr on Pratik's schedule); open a `todo` for `release.yml`'s
    quality gate running `cargo test` with no `WIXEN_NO_AUDIO`
    (premise 5; the release workflow is Pratik's).
  </action>
  <verify>
    <automated>cargo test --test print_is_on_the_file_menu && cargo test --test wired && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test every_status_sentence_has_one_shape && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the reading's tests bare.
    - The letters on File, read with premise 2's command, are N, S, A, M, D, I, O, E, K, P, Q, each once, quoted.
    - `grep -c 'fn a_message_as_the_reader_shows_it' src/presentation/wx_app.rs` is 1 and `grep -c 'a_message_as_the_reader_shows_it' tests/wired.rs` is at least 3.
    - `awk '/^### File Menu/,/^### Edit Menu/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'Ctrl+P'` is at least 1; `grep -c -i 'print' docs/privacy.md` is greater than the 5 lines it matched at `630e2a67` (:560, :562, :565, :567, :568, every one "fingerprint"), read before and after in the summary.
    - Two guard records measured and the re-anchored PGP composition record re-measured; five ledger changes in both halves; carriage returns 0 on every file touched, measured with `tr -cd '\r' < FILE | wc -c`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>Ctrl+P on a message in the list opens Windows' print dialog and prints its header lines and words, says so once, and the pages, privacy page, changelog and ledger say what it does and does not do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message text to a printer | a stranger's words leaving the machine through the spooler |
| the job name to a shared queue | what other people at a shared printer see |
| a Win32 structure filled by hand | field sizes and handle ownership across the FFI line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Information disclosure | the job name in Windows' queue | medium | mitigate | `job_name(Kind::Message)` only; the reading refuses an arm that passes a subject; the privacy page says so |
| T-13-08 | Information disclosure | the log | medium | mitigate | the printer name and page count only; never the text, the subject or the file path of a PDF |
| T-13-09 | Tampering | an ampersand or tab in a body drawn as a mnemonic or control | low | mitigate | `DT_NOPREFIX | DT_EXPANDTABS | DT_SINGLELINE`; a guard record holds `DT_NOPREFIX` |
| T-13-10 | Denial of service | a GDI object or global handle leaked on an early return | medium | mitigate | owning guards that release on drop for the font, the DC and the two dialog handles |
| T-13-11 | Repudiation | a flag hiding a working printer on a machine that has one | low | mitigate | with `WIXEN_NO_PDF_PRINTER` set the target asserts `CreateDCW` fails; proved failing by hand on this machine |
| T-13-SC | Tampering | cargo features added to an existing crate | high | mitigate | the checkpoint waits for Pratik's word; `Cargo.lock` must not change, read after the build commit; the legitimacy audit (research, Package Legitimacy Audit: windows OK) stands |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the `build(13-03)` commit and the workflow commits answer `all` and pay the whole gate; the merge with `git merge --no-ff` earns everything too (a manifest and workflows changed), and the phase's full gate still runs once, in 13-51; never `scripts/check.sh all` by hand here.
- This plan changes what is spoken and shown, so its branch is pushed and a pull request opened under Pratik's standing OK, and the merge waits for CI, NVDA and Accessibility on it, each verdict read and quoted, CI's Test Suite run reading the spool target's absent-printer branch.
- Seven guard records measured, the composition record re-measured, every record the count check flagged re-measured with the command it printed.
- What only a person settles: the dialog by keyboard under NVDA and Narrator, a page on paper. What the targets prove: every laid-out line drawn, a real job's page count, the menu and the arm's calls.
</verification>

<success_criteria>
- File, Print prints the message under the cursor in the message list through Windows' dialog; GAP-01's `[D]` line waits for 13-04's surfaces.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list in `ROADMAP.md`, ticked; `STATE.md`'s plan number in its frontmatter (`current_plan`) and in its body (`Current Plan:`); GAP-01's evidence line in `REQUIREMENTS.md` naming the three targets and the route the measurement chose, its `[D]` line and box left for 13-04 and its `[S]` lines untouched.
- After the merge, the executor comments on #45 from the merge commit with what prints and where, and leaves it open for 13-04; commenting is not a publish.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-03-SUMMARY.md` when done
</output>
