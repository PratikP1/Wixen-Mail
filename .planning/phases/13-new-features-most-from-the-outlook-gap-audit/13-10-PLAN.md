---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 10
type: execute
wave: 10
depends_on: [13-09]
files_modified:
  - src/application/invitations.rs
  - src/application/answering.rs
  - src/application/reading_a_message.rs
  - src/presentation/reader_text.rs
  - src/presentation/html_renderer.rs
  - tests/an_invitation_is_said_before_the_body.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-04]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A message carrying a meeting invitation says so before its body, on every surface that asks reading_a_message: 'Meeting invitation:' with the title, when, where and the organiser, and whether it is new, a change to the meeting already on the calendar (from what time to what time), or one already answered here (GAP-04, #50 point 1)."
    - "A cancellation says it is one and whether that meeting is on the calendar; somebody's answer to a meeting says who and what they answered; a calendar part that asks nothing reads as a calendar file (#50 point 1)."
    - "The invitation sentence is folded after the envelope and before the signature verdict, so it is above 'More about this signature:' and is spoken (reader_text.rs:1640-1650's rule); a signed invitation proves it."
    - "An invitation sent as application/ics is found as one sent as text/calendar is, and a whole raw message with a calendar part, parsed by service::mime::parse, reaches the sentence end to end (#50 point 5)."
    - "The attachment row for a calendar part says meeting invitation, meeting cancellation, reply to your meeting or calendar file from what the part asks, not from its type alone (reader_text.rs:1460)."
  artifacts:
    - "src/application/invitations.rs: WhatTheInvitationSays and what_the_invitation_says(document, on_the_calendar, answered_here), pure, with a case per variant."
    - "src/application/reading_a_message.rs: the fourth field invitation on WhatIsSaidAboutIt, asked in for_message and set to nothing in nothing() and put_together's callers."
    - "src/presentation/reader_text.rs: with_invitation folded between with_smime_envelope and with_signature; the calendar row's words from the decision."
    - "tests/an_invitation_is_said_before_the_body.rs: a raw REQUEST message and a raw application/ics one stored through the cache, opened through for_message, the bar and the page read."
  key_links:
    - "stored parts -> answering::the_invitation_a_message_carries (text/calendar or application/ics) -> invitations::what_the_invitation_says with the calendar's copy -> WhatIsSaidAboutIt.invitation -> ReaderDocument::with_what_is_said -> said_before_the_message: one decision, every surface."
    - "the fold order pgp -> envelope -> invitation -> signature, held by a rewritten guard record and a new one."
---

<objective>
The reader shows and says a meeting invitation before the body, on every
surface that opens a message.

Purpose: GAP-04's first `[D]` clause, "the invitation's part shown and said
before the body", and #50 points 1 and 5 (the audit of 2026-09-15: the
reader neither shows nor announces the meeting; only `text/calendar` is
found, and no raw message fixture carries a calendar part). The answer
buttons are 13-11; synced events carrying the iCalendar UID are 13-12;
updates and cancellations reaching the calendar are 13-13. This plan is the
sentence and the row, decided once in `application` and folded by the one
function every surface already calls (#51's lesson).

Output: the decision, the field, the fold, the row's words, the finder that
takes `application/ics`, the integration target, the guide, the changelog,
the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said. Line numbers move when 13-01 to 13-09 land: read from their plans on
2026-09-24, 13-02 edits `reader_text.rs` (what a printed page holds), 13-04
edits `page_jumps.rs` and the formatted window (Ctrl+P posted as kind
`print`), and 13-06 edits `wx_reader.rs`; re-take each grep at the start of
task 1 and quote the new lines in the summary.

**1. What the tree decides about an invitation today.**

```
grep -n "pub enum WhatItAsks\|pub fn what_it_asks\|pub struct Invitation\b\|pub fn read_the_invitation\|pub enum WhatChanged\|pub fn what_changed" src/application/invitations.rs
   -> :56, :73, :84, :127, :477, :493: what a document asks by METHOD, the meeting read out of it, and new-or-changed against the calendar
grep -n "AN_INVITATION_ARRIVES_AS\|pub fn the_invitation_a_message_carries" src/application/answering.rs
   -> :712 "text/calendar", :729: the first text/calendar part only; application/ics is never found (#50 point 5)
grep -rn "read_the_invitation\|what_it_asks" src/presentation
   -> nothing: no surface reads an invitation; the only readers are application/ files (#50 point 1)
grep -n "pub fn get_event_by_provider_id\|pub fn the_version_answered_here" src/data/message_cache/calendar.rs
   -> :391 and :430: the calendar's copy and the version answered here, the two facts "new, changed or already answered" needs
```

**2. Where what is said about a message is decided and folded.**

```
grep -n "pub struct WhatIsSaidAboutIt\|pub opened\|pub envelope\|pub signature\|pub fn nothing\|pub fn for_message\|pub fn put_together" src/application/reading_a_message.rs
   -> :88, :91, :93, :95, :106, :127, :144: three fields; the invitation joins as a fourth
sed -n 1778,1786p src/presentation/reader_text.rs
   -> with_what_is_said: with_pgp, then with_smime_envelope, then with_signature
grep -n 'const HOW_IT_WAS_CHECKED\|pub fn said_before_the_message\|"text/calendar" =>' src/presentation/reader_text.rs
   -> :445 "More about this signature:", :668, :1460 "calendar invitation" by type alone
grep -n "fn what_a_message_shows_and_says" src/presentation/wx_app.rs
   -> :14136: the composition every surface calls; this plan does not edit wx_app.rs
```

**3. The gate for each file**, by the TOML reader
(`python -c "import tomllib;..."` over `tests_last_seen` and `file`, 1,088
records on 2026-09-24): `invitations.rs` 37 tests, 0 and 0;
`answering.rs` 37, 1 and 1; `reading_a_message.rs` 7, 5 and 1;
`reader_text.rs` 125, 13 and 11; `html_renderer.rs` 92, 10 and 10; the new
target 0. Adding a test to `reading_a_message.rs`, `answering.rs` or
`reader_text.rs` turns `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
red, so each red commit that adds one names it with the real failures
(CLAUDE.md, "The red gate and the count check collide").

**4. The anchors this plan's edits reach** (records whose `before` lies in a
file it edits, located with the same reader):

- `reader_text.rs:1782-1784`, "the envelope is folded in before the
  signature, so it is spoken": its `before` is the three-line chain
  `with_pgp`, `with_smime_envelope`, `with_signature`. Task 2 inserts
  `.with_invitation(&said.invitation)` inside that chain, so the anchor
  **changes**: the executor rewrites its `before` to the four-line chain and
  its `after` to the same chain without the envelope line, in the same
  commit, and re-measures it; otherwise
  `test_every_guard_record_still_names_one_place_in_the_tree` refuses the
  commit.
- `reader_text.rs:336-338` (two records, suite
  `an_encrypted_message_is_not_left_unexplained`), `:777`, `:1009`,
  `:1025-1028`, `:1331`, `:1357-1359`, `:1803`, `:1826`, `:1854-1858`:
  outside the lines this plan edits; the row table edit at `:1460` sits
  between `:1357-1359` and `:1803` and moves no anchor text. Each is named
  in the summary as untouched.
- `reading_a_message.rs:154`, "a message the key opens is shown as its
  words": `let opened = opening_pgp::for_body(&body);` inside
  `put_together`. The fourth field is added to the struct literal below it;
  the anchor line is not edited, duplicated or moved in text.
- `answering.rs:318-323`: inside `what_pressing_it_will_do`, not edited.
- `html_renderer.rs`: ten records at `:17`, `:106-107`, `:246`, `:438-447`,
  `:561`, `:561-562` (three records), `:727-729`, `:785-789`, `:845-854`.
  Task 2 adds the invitation to the page's security section; the executor
  reads which function builds that section before editing and names in the
  summary whether any of the ten anchors lies inside it (none is expected).
</premise_corrections>

<decisions date="2026-09-24">
**1. The order inside the phase.** Pratik confirmed the order on 2026-09-24
and said "plan and implement phase 13". Invitations come first in this
group.

**2. The fold order: pgp, envelope, invitation, signature.** Taken
2026-09-24 from the research's recommendation (RESEARCH-2, R2-01 and pitfall
10); Pratik may overrule. After the envelope so a security fact about how
the message arrived is heard first; before the signature so the sentence is
above "More about this signature:" and spoken.

**3. An invitation inside decrypted content is shown and never changes the
calendar on its own.** Taken 2026-09-24 from the research's recommendation
(question 9); Pratik may overrule. This plan decides from stored parts only;
13-14 and 13-15 hand the decrypted parts to the same decision with their
provenance, and 13-13's calendar change refuses that provenance.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: what an invitation says, decided once in application, and the finder that takes application/ics</name>
  <files>src/application/invitations.rs, src/application/answering.rs, src/application/reading_a_message.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/invitations.rs:56-157` (`WhatItAsks`, `Invitation`, `read_the_invitation`) and `:468-540` (`AlreadyOnTheCalendar`, `WhatChanged`, `what_changed`); the fixture `an_invitation_that_arrived` near `:615`.
    - `src/application/answering.rs:700-745` (`AN_INVITATION_ARRIVES_AS`, `the_invitation_a_message_carries`).
    - `src/application/reading_a_message.rs:70-205` (the struct, `nothing`, `for_message`, `put_together`, the two `*_check_for`).
    - `src/data/message_cache/calendar.rs:391-460` (`get_event_by_provider_id`, `the_version_answered_here`).
    - `src/presentation/wx_app.rs:14760-14775`: parts are read from `cache.attachments_with_content`, stored when a message is opened; confirm in the reader's open path that the parts are stored before `what_a_message_shows_and_says` runs, and say in the summary which it is.
  </read_first>
  <behavior>
    - `invitations::WhatTheInvitationSays`: `Nothing`; `Invitation { summary, when, place: Option, organiser: Option, standing }` with `standing` one of `New`, `Changed { from, to }` (the calendar copy's time and the invitation's) and `AlreadyAnswered`; `Cancellation { summary, on_the_calendar: bool }`; `AnAnswer { who, answered }` for a `REPLY`; `CalendarFile` for anything else a calendar part holds.
    - `invitations::what_the_invitation_says(document, on_the_calendar: Option<&CalendarEventEntry>, answered_here: Option<u32>) -> WhatTheInvitationSays`, pure, built on `what_it_asks`, `read_the_invitation` and `what_changed`; a document that does not parse is `CalendarFile`, never an error a caller drops.
    - `said()` on each variant: "Meeting invitation: Quarterly review, Thursday 5 March 2026 from 09:00 to 10:00, in Room 4, from Ada Lovelace." with ", and it is new to your calendar", ", a change to the meeting on your calendar, which was at ..." or ", and you have answered this version"; "Meeting cancelled: Quarterly review. It is on your calendar." or "... It is not on your calendar."; "Grace Hopper accepted your meeting: Quarterly review."; "This message carries a calendar file."; times through the date reading the reader already uses, never a raw `DTSTART`.
    - `answering::the_invitation_a_message_carries` finds `text/calendar` and `application/ics`, with parameters, in any case; the constant becomes the two types it names.
    - `reading_a_message::WhatIsSaidAboutIt.invitation`; `nothing()` answers `WhatTheInvitationSays::Nothing`; `for_message` asks `invitation_check_for(cache, message_row_id)`, which reads the stored parts, finds the calendar part, and looks the meeting up on the calendar of the account the message row belongs to, read from the row rather than passed by the surfaces; `put_together` takes the invitation beside the envelope and signature.
    - Cases: one per variant and standing; an `application/ics` part found; a part that is not a calendar found by nothing; a document that fails to parse reads `CalendarFile`; `for_message` over a temp cache holding the REQUEST fixture as a stored part answers `Invitation` with standing `New`, and with the event saved on the calendar at an earlier `SEQUENCE` answers `Changed`.
  </behavior>
  <action>
    Red first, on a branch, per CLAUDE.md's red-commit rule: the cases by
    module path (`application::invitations::tests::<name>`,
    `application::answering::tests::<name>`,
    `application::reading_a_message::tests::<name>`), and
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held`,
    since `answering.rs` and `reading_a_message.rs` are named by records.
    Then the decision, the finder and the field, with `cargo build`
    between. Invoke `tdd` and `elegant-code`: the decision is values in and
    a value out, the sentence is a method on the variant, no `unwrap`
    outside tests, the account is read once where the row is read.

    Every constructor of `WhatIsSaidAboutIt` gains the field; `cargo build
    --all-targets` names each, and the tests' literals take
    `WhatTheInvitationSays::Nothing`.

    Guard records, each measured by hand on the library first and then
    written: one on `invitations.rs` whose break makes a `CANCEL` read as
    `Invitation`, the cancellation case red; one on `answering.rs` whose
    break drops `application/ics` from the finder, its case red. Before
    writing a record, the count check runs, and after it `scripts/guards.sh
    --remeasure` once for every record it flags.
  </action>
  <verify>
    <automated>cargo test --lib application::invitations:: && cargo test --lib application::answering:: && cargo test --lib application::reading_a_message::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the new cases by module path and the count check, and `scripts/red-commit.sh` accepts it.
    - `grep -c 'application/ics' src/application/answering.rs` is at least 1; `grep -c 'pub invitation' src/application/reading_a_message.rs` is 1.
    - The three module runs pass, each count quoted, and each is larger than on 2026-09-24 (37, 37, 7) by the cases added.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>What an invitation, a cancellation or an answer says is one pure decision in application, asked by the one function every surface asks, and an application/ics part is found.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the sentence folded before the signature, the page's section, the row's words, and a raw message end to end</name>
  <files>src/presentation/reader_text.rs, src/presentation/html_renderer.rs, tests/an_invitation_is_said_before_the_body.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/reader_text.rs:440-460` (`HOW_IT_WAS_CHECKED`), `:600-700` (`said_before_the_message`), `:1440-1500` (the attachment kind table), `:1600-1790` (`with_signature`, `with_smime_envelope`, `with_what_is_said`), and the guard record "the envelope is folded in before the signature, so it is spoken" in `guards/guards.toml`.
    - `src/presentation/html_renderer.rs`: the function that writes the security section of a page, found by `grep -n 'Security warning\|security' src/presentation/html_renderer.rs`.
    - `tests/an_encrypted_message_is_not_left_unexplained.rs` for a target that builds a cache and reads the bar end to end, and `src/service/mime.rs:289` (`parse`).
  </read_first>
  <behavior>
    - `ReaderDocument::with_invitation(&WhatTheInvitationSays)`: `Nothing` adds nothing; any other variant puts its sentence at the top of the bar and at the top of the body, as `with_smime_envelope` does for the envelope sentence; `with_what_is_said` folds pgp, envelope, invitation, signature.
    - The page (formatted window, preview) carries the same sentence in its security section, above the signature's, so the formatted window says it as the text reader does.
    - The attachment row for a calendar part, `text/calendar` or `application/ics`: "meeting invitation", "meeting cancellation", "reply to your meeting" or "calendar file" from the message's decision; the table's `"text/calendar" => "calendar invitation"` goes.
    - Cases in `reader_text`: a document with an invitation has "Meeting invitation: Quarterly review" in `said_before_the_message`; with a checked signature as well, the invitation sentence is above "More about this signature:"; a cancellation's row reads "meeting cancellation"; `Nothing` leaves an ordinary message's bar absent.
    - The target: a whole raw message (multipart/alternative text and HTML, then `text/calendar; method=REQUEST`) and a second with `application/ics; method=CANCEL`, each parsed with `service::mime::parse`, stored in a temp cache with its parts through the store the reader's open path uses, opened through `reading_a_message::for_message`, built with `reader_text::single_message(...).with_what_is_said(...)` and through the page renderer; the bar and the page each read and asserted; a companion that plants the fold after the signature and reads the sentence missing from `said_before_the_message`.
  </behavior>
  <action>
    Red first, on a branch: the `reader_text` cases by module path, the
    target's tests bare, and the count check, since `reader_text.rs` is
    named by 13 records. Then the fold, the page section and the row's
    words, with `cargo build` between.

    The anchor rewrite in the same commit as the insertion, per premise 4:
    the record "the envelope is folded in before the signature, so it is
    spoken" gets the four-line chain as `before` and the chain without the
    envelope line as `after`, and is re-measured. A new record whose break
    moves `.with_invitation` after `.with_signature`, measured: the fold
    case and the target's companion red, `suite` the target where its test
    is the one that reddens. A new record whose break puts
    `"calendar invitation"` back for every calendar part, the row case red.
    `scripts/guards.sh --remeasure` once for every record the count check
    flags.

    Invoke `elegant-code`: one method per fold, no second place that
    decides the order.
  </action>
  <verify>
    <automated>cargo test --lib presentation::reader_text:: && cargo test --lib presentation::html_renderer:: && cargo test --test an_invitation_is_said_before_the_body && cargo test --test an_encrypted_message_is_not_left_unexplained && cargo test --test house_style test_every_guard_record_still_names_one_place_in_the_tree</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the `reader_text` cases by module path, the target's tests bare, and the count check.
    - `grep -c 'with_invitation' src/presentation/reader_text.rs` is at least 2; `grep -c '"calendar invitation"' src/presentation/reader_text.rs` is 0.
    - The target passes with at least 4 tests, quoted; `presentation::reader_text::` passes with more than 125, quoted.
    - The rewritten record and the two new ones measured and quoted; the other ten `reader_text.rs` records named as untouched.
  </acceptance_criteria>
  <done>On every surface an invitation, a cancellation or an answer is said at the top of the bar and the body, above the signature's account, and its row says what it is; a raw message proves the whole path.</done>
</task>

<task type="auto">
  <name>Task 3: the guide, the changelog and the ledger</name>
  <files>docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`, `## Reading and Managing Email` (`:194`) and its subsections, for where a "Meeting invitations" subsection belongs.
    - `.planning/WINDOWS.md` rows 152, 154 and 155, and the frontmatter counts.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide: a `### Meeting invitations`
    subsection under Reading and Managing Email saying what is said at the
    top of a message that carries an invitation, a cancellation or an
    answer, with one worked example sentence, and that answering is on
    Action, Answer Invitation until 13-11 adds the buttons (13-11 rewrites
    that sentence). The changelog under `[Unreleased]`, Added, #50 points 1
    and 5; Known limitations: no invitation from a real Outlook, Google or
    CalDAV organiser has been read, and nobody has heard the sentence.

    Ledger, by hand, both halves (the table row and the JSON entry, and the
    frontmatter counts): one `unrun-verify` for the sentence under NVDA in
    the text reader, the formatted window and the preview; one
    `unrun-verify` for phase 14, a real organiser's invitation,
    cancellation and answer read here.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### Meeting invitations' docs/USER_GUIDE.md` is 1.
    - Two ledger entries in both halves; no em dash or en dash; none of the six words; carriage returns 0 on every file touched, measured with `tr`.
  </acceptance_criteria>
  <done>The guide says what an invitation sounds like, the changelog carries it with its limits, and the two listening and real-account lines are on the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's calendar part to a sentence | the title, place and organiser are the sender's words |
| the calendar's copy to "a change" | a stored row compared with an incoming document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10-01 | Spoofing | an invitation naming somebody else as organiser | medium | mitigate | the sentence says "from" the organiser the document names and the bar's own sender line stays; nothing on the calendar changes in this plan |
| T-13-10-02 | Tampering | a malformed calendar part | medium | mitigate | a document that does not parse reads `CalendarFile`; the existing line reader and its size ceiling are reused, never a second parser |
| T-13-10-03 | Information disclosure | the sentence read aloud in a room | low | accept | it says no more than the subject line and the attachment row already say; the mute keys apply |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass. `scripts/check.sh` runs through the hook on every commit, never piped. No hand-run `scripts/check.sh all`: 13-51 runs the phase's full gate once. One `scripts/guards.sh --remeasure` call per commit for every record the count check flags. The pages, the ledger, the summary and the four completion marks go in one documents commit on the branch before the merge; the merge is `git merge --no-ff` through the hook.
- Spoken or shown: yes. The branch is pushed and a pull request opened after its last commit, and `ci.yml`, `nvda.yml` and `accessibility.yml` are read green before the merge (Pratik's standing OK of 2026-09-23); `main` is not pushed.
- What the tests prove: the decision per variant, the fold order, the row's words, a raw message through the cache to the bar and the page. What the ear settles: the sentence under NVDA, on the ledger.
</verification>

<success_criteria>
- GAP-04's first `[D]` clause ("the invitation's part shown and said before the body") has a named test in the summary; the box waits for 13-13.
- The four completion marks, all of them: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list in `.planning/ROADMAP.md` (and in the phase README's list if the README carries one); `STATE.md`'s plan number in its frontmatter and in its body; GAP-04's clause named in `.planning/REQUIREMENTS.md`'s traceability row as this plan's.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-10-SUMMARY.md` when done
</output>
