---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 36
type: execute
wave: 38
depends_on: [13-35]
files_modified:
  - src/application/reply.rs
  - src/presentation/wx_app.rs
  - tests/the_from_list_chooses_who_sends.rs
  - guards/guards.toml
  - docs/comparison.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-10]
estimate:
  tokens: 19200
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reply or a forward opens with the From entry whose address the original was sent to, among its To and Cc, compared without case; where none of an account's other addresses is there, the account's own entry as today (RESEARCH-3 question 10, C12)."
    - "Reply All leaves out an account's other addresses as it leaves out the accounts' own, so a reply to mail sent to help@ does not copy help@ back to itself."
    - "The comparison page and the guide say, with the date, that several addresses per account now exist, and that shared mailboxes, sending on behalf of someone and delegation are later work, each named; #59 stays open for them (GAP-10's [D] line, \"shared mailboxes and delegation as their own later work, said\")."
  artifacts:
    - "src/application/reply.rs: the_entry_a_reply_goes_out_from(original_to, original_cc, entries, account_id) -> Option<&FromEntry>, pure, with rows; reply_recipients given the other addresses among the own ones."
    - "src/presentation/wx_app.rs: start_reply and open_compose choosing that entry and passing every own and other address; production code only."
    - "docs/comparison.md: the row at :45 and the list at :123 corrected by date."
  key_links:
    - "start_reply -> own addresses including every account's other addresses -> reply_recipients; open_compose(Reply, Forward) -> reply::the_entry_a_reply_goes_out_from(original To, Cc, entries) -> the From list's selection -> 13-35's send from the chosen entry."
---

<objective>
A reply goes out from the address the message was sent to, Reply All
never copies one of your own addresses, and the pages say what is built
and what is not.

Purpose: RESEARCH-3's C12 and question 10: once an account sends from
several addresses, a reply to mail that arrived at help@ should leave
from help@, or the person answering support mail answers from their
own address by accident. And GAP-10's `[D]` line ends "shared mailboxes
and delegation as their own later work, said": `docs/comparison.md:45`
still says several identities are "Not yet".

Output: the pure choice, the reply and forward selecting it, Reply All
leaving other addresses out, the comparison page and the guide, the
changelog and the ledger. Closes GAP-10.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-35-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. Yes, a reply goes out from the address it was sent to (RESEARCH-3
question 10).** A forward too, by the same rule.

**2. Shared mailboxes, sending on behalf and delegation are later work,
said and not stubbed (RESEARCH-3, GAP-10 "Cannot be finished here").**
They need IMAP `NAMESPACE`, ACL, a `Sender` header and a real shared
mailbox; #59 stays open for them.

**3. Other addresses count as your own in Reply All (planner's finding,
premise 1).** RESEARCH-3 did not list it; `reply_recipients` drops the
accounts' own addresses, and an other address is one of yours.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. Re-take after 13-35's merge, which changes `open_compose`.

```
sed -n 16510,16545p src/presentation/wx_app.rs   -> start_reply: own_addresses are every account's email, handed to reply_recipients, which drops them from Reply All (reply.rs:135-140)
sed -n 16815,16860p src/presentation/wx_app.rs   -> open_compose: replying covers Reply, ReplyAll and Forward; the selection from sends_from
sed -n 128,140p src/application/reply.rs   -> RepliedTo { from, reply_to, to, cc }; own_addresses "the addresses of the accounts set up here"
sed -n 40,46p docs/comparison.md; sed -n 118,124p docs/comparison.md   -> "Several identities per account ... Not yet" at :45; "2. Several identities per account" at :123 in what is left
grep -rln 'comparison.md' tests   -> tests/house_style.rs reads it (em dash and words), no reading of this row
```

**Guard records, both readings.** `reply.rs` 1 by `tests_last_seen`
and 0 by `file`, 28 tests, tests added, so the count check is named
bare and that record re-measured. `wx_app.rs` 119 and 112, no test
added; no record anchors in `start_reply` (`:16510-16560`) or
`open_compose` (`:16806-16960`) at `630e2a67`, re-read after 13-35.
`tests/the_from_list_chooses_who_sends.rs` is 13-35's, a reading added.
`docs/comparison.md` 0 and 0.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the entry a reply goes out from, and Reply All leaving your other addresses out</name>
  <files>src/application/reply.rs, src/presentation/wx_app.rs, tests/the_from_list_chooses_who_sends.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/reply.rs:1-160` and its tests' helpers (`grep -n 'fn mine\|fn list_message\|fn own_message' src/application/reply.rs`).
    - `src/presentation/wx_app.rs` at `start_reply` and `open_compose` as 13-35 left them, and `src/application/identities.rs` (`FromEntry`).
  </read_first>
  <behavior>
    - `the_entry_a_reply_goes_out_from(original_to, original_cc, entries, account_id) -> Option<&FromEntry>`: among the account's entries, the first other address found in To or Cc, compared without case and read through the same address splitter `reply_recipients` uses; none found gives the account's own entry; an address of another account's never chosen. Rows: sent to help@ gives help@; sent to the own address gives the own entry; sent to both gives the other address; a display name with a comma around the address still found.
    - `reply_recipients` is handed every account's own address and every other address; a Reply All to mail sent to help@ does not put help@ in Cc, as a case.
    - `open_compose` selects that entry for Reply, Reply All and Forward; a reading in the target holds it, with a companion that plants the own entry back.
  </behavior>
  <action>
    Red first, on the branch `13-36-a-reply-from-the-address-it-came-to`:
    the rows by module path (`application::reply::tests::<name>`), the
    reading bare, and the count check `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    named bare for `reply.rs`. Then the function, the own addresses and
    the selection, with `cargo build` between (per decisions 1 and 3).
    `wx_app.rs` gains production code only. Invoke `elegant-code`: one
    function answers which entry; the address splitting is the one that
    already exists.

    Guard records: one on `reply.rs` whose break compares with case, a row
    red; one on `wx_app.rs` whose break leaves the other addresses out of
    the own list, the Reply All case red; the `reply.rs` record
    re-measured.
  </action>
  <verify>
    <automated>cargo test --lib application::reply:: && cargo test --test the_from_list_chooses_who_sends && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the rows by module path, the reading bare and the count check bare.
    - `grep -c 'pub fn the_entry_a_reply_goes_out_from' src/application/reply.rs` is 1; `grep -c 'the_entry_a_reply_goes_out_from' src/presentation/wx_app.rs` is at least 1.
    - `application::reply::` above 28, `presentation::wx_app::` at 199, both quoted; the records measured.
  </acceptance_criteria>
  <done>A reply leaves from the address the mail came to, and Reply All never sends a copy to one of your own addresses.</done>
</task>

<task type="auto">
  <name>Task 2: the comparison page, the guide, the changelog and the ledger</name>
  <files>docs/comparison.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/comparison.md:30-60` and `:110-130`; `docs/USER_GUIDE.md`, "### Sending from another address" (13-35).
  </read_first>
  <action>
    Invoke `writing-craft`. The comparison page: the row at `:45`
    corrected by date, saying several addresses per account exist since
    this plan's merge and naming what is still missing (shared mailboxes,
    sending on behalf, delegation); the list at `:123` the same, dated
    rather than deleted. The guide's section gains the reply rule and a
    sentence naming the three later pieces. The changelog,
    `[Unreleased]`, Added: a reply goes out from the address it came to;
    Reply All leaves your other addresses out.

    Ledger, by hand, both halves: one `todo` for shared mailboxes,
    sending on behalf and delegation (decision 2), naming what each
    needs; one `unrun-verify` for the tester's ear (a reply heard opening
    on the other address). After the merge, the executor comments on #59
    from the merge commit with step 1 built and steps 2 and 3 open, and
    leaves the issue open. One documents commit on the branch before the
    merge carries the pages, the ledger, the summary and the four
    completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Several identities per account' docs/comparison.md` is at least 1 and the row no longer ends "Not yet" alone (`grep -c '| Several identities per account | One mailbox, more than one address to send as | Not yet |' docs/comparison.md` is 0); the changelog names #59.
    - The ledger entries in both halves; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say what sending from several addresses does now and name the three things that remain, and #59 stays open for them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a received message's headers to the From choice | To and Cc from a stranger decide which of your addresses answers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-36-01 | Spoofing | a crafted To choosing an address of another account | medium | mitigate | only the replying account's own entries are candidates; a row |
| T-13-36-02 | Information disclosure | Reply All copying one of your own addresses | low | mitigate | other addresses joined to the own list; a case and a guard record |
| T-13-36-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is shown and spoken (the From entry a reply opens on, Reply All's recipients): push the branch, open a pull request, wait for CI, NVDA and Accessibility, then merge locally. Pushing `main` is Pratik's.
- Red commits name lib tests by module path and integration tests bare; the count check named bare for `reply.rs`. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-10's `[D]` line has named tests or readings in the summary for each half, with 13-33 and 13-35; its box is ticked with the date and its traceability row updated; its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-10's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-36-SUMMARY.md` when done
</output>
