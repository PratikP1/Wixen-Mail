---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 38
type: execute
wave: 40
depends_on: [13-37]
files_modified:
  - src/application/saved_searches.rs
  - src/presentation/wx_app.rs
  - tests/wired.rs
  - tests/the_saved_searches_menu_says_the_searches_an_account_has.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-09]
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Action menu's Saved Searches submenu lists the saved searches of the account being worked in, in the order the tree shows, the first six with Alt+4 to Alt+9 beside them, then Edit Conditions, Rename and Delete; it is rebuilt when the searches load, only when what it would say differs (GAP-09's second [D] clause, on the pattern labels take in EDIT-05)."
    - "Choosing an item or pressing its key puts the tree cursor on that search's row and runs it, exactly as Enter on the row does, a search a newer version wrote refused in the words Enter uses."
    - "Alt and a digit past the account's last search is answered in words from the folder tree and the message list rather than left silent, and the key a person hears beside a search is the key that runs it."
    - "Keys and letters are the ones proven free on 2026-09-24: Alt+4 to Alt+9 bind nothing in the main window and are written nowhere in docs/KEYBOARD_SHORTCUTS.md; the three command items keep C, R and D; search items take no letter."
  artifacts:
    - "src/application/saved_searches.rs: REACHABLE_BY_KEY, key_for, what_the_menu_says, nothing_there, with their cases."
    - "src/presentation/wx_app.rs: a block of menu ids for the searches, rebuild_the_saved_search_menu and put_the_saved_searches_on_the_menu, called from the SavedSearchesLoaded arm, the range arm that runs a search by its place, and the key handler for a key past the last search on the tree and the list."
    - "tests/wired.rs: the saved searches' keys among the keys stated by counting, beside the labels'."
    - "tests/the_saved_searches_menu_says_the_searches_an_account_has.rs: the built menu bar read after searches load, after a rename and a move, past the sixth, and the key handler measured on built controls, each reading with a planted-fault companion."
  key_links:
    - "SavedSearchesLoaded -> the active account's searches in SavedSearchesRead.order -> saved_searches::what_the_menu_says -> the submenu's items and their ids by place -> the range arm -> select_row -> run_a_saved_search: one order for the tree, the menu and the key."
    - "Alt+digit with no item -> the tree's and the list's key handler -> a_saved_search_key_the_menu_does_not_answer -> saved_searches::nothing_there: never silence."
---

<objective>
A key per saved search, and a Saved Searches menu that lists the searches.

Purpose: GAP-09's second `[D]` clause: "a key per search on the pattern labels
take in EDIT-05". 12-10 built that pattern for labels (a menu rebuilt from the
account's own list in its stored order, a key beside each of the first few,
a key past the end answered in words), and 13-37 gave saved searches a stored
order to build it on. #58 point 2 asked for a key for the Save This Search
command itself; the `[D]` line asks for a key per search, and that is what
this builds (decision 3 below).

Output: the menu's words and keys as rules, the submenu rebuilt from the
searches, the range arm, the key handler, the keys stated for `tests/wired.rs`,
the reading, the pages, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-37-SUMMARY.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-10-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only. Command first, then
what it said. Line numbers are places to start; 13-01 to 13-37 land first.

**1. The labels' pattern, which this copies in shape and not in text.**

```
grep -n 'pub const REACHABLE_BY_KEY\|pub struct MenuLine\|pub fn what_the_menu_says\|pub fn key_for\|pub fn nothing_there' src/application/tagging.rs   -> :143, :157, :179, :205, :244: nine keys, one line per label with the key after a tab, a lone ampersand doubled, "There is no label {n}"
grep -n 'LABELS_PAST_NINE_ON_THE_MENU\|ID_LABEL_PAST_NINE\|macro_rules! menu_ids' src/presentation/wx_app.rs   -> :95 (NAME[count] reserves a block), :114, :256
grep -n 'pub fn rebuild_the_label_menu\|fn answer_the_label_keys_the_menu_cannot\|pub fn put_the_labels_on_the_menu\|pub fn a_label_key_the_menu_does_not_answer' src/presentation/wx_app.rs   -> :10594, :10642, :10677, :10707
grep -n 'id >= ID_HELP_TOPIC_FIRST' src/presentation/wx_app.rs   -> :5895, a block answered by a range; tests/wired.rs:207-214 reads `id >= ` as handling the block its first id stands for
grep -n 'pub fn build_menu_bar' src/presentation/wx_app.rs   -> :6585, public since 12-10, which is how a target reads the real menu bar
```

**2. The submenu and where searches load.**

```
Read src/presentation/wx_app.rs:7127-7145   -> saved_search_menu: Edit &Conditions... (ID_EDIT_SEARCH_CONDITIONS), &Rename..., &Delete; appended at :7419-7426 as "Saved Searc&hes"
grep -n 'UIUpdate::SavedSearchesLoaded' src/presentation/wx_app.rs   -> :12743 sent from folder_tree_updates (:12638) beside LabelsLoaded, and the arm at :19393-19395, which only stores the map; LabelsLoaded's arm beside it (:19389-19392) calls put_the_labels_on_the_menu
grep -n 'folder_tree.on_item_activated' src/presentation/wx_app.rs   -> :2915; Enter runs a search through the_search_a_row_names (:12946) and run_a_saved_search (:7949) at :2962-2967
grep -n 'pub fn select_row' src/presentation/wx_app.rs   -> :18507, the cursor put on a row by its identity
```

The state holds every account's searches (`saved_searches` at
`wx_app.rs:514`, a map by account). The menu lists the searches of the
account being worked in, `active_account_id`, in `SavedSearchesRead.order`
from 13-37, readable or not, so the Nth item is the Nth row under that
account's branch.

**3. The record whose anchor this must not copy.** "the Label menu is
rebuilt from the labels an account has when they load" anchors
`    if shown != wanted {` (`wx_app.rs:10697`, `suite`
`the_label_menu_says_the_labels_an_account_has`, five tests red). A second
line spelled the same in `wx_app.rs` makes
`test_every_guard_record_still_names_one_place_in_the_tree` refuse the commit,
so the saved-search comparison is written with names of its own. The same
reading found no other record anchored in `wx_app.rs:86-300`, `:2900-2975`,
`:7120-7147`, `:7415-7427`, `:10540-10711` or `:19385-19396`, nor in
`tests/wired.rs:975-995`.

**4. The keys, searched.**

```
grep -rnoE '(Ctrl\+Shift|Alt|Ctrl\+Alt|Alt\+Shift)\+[0-9]\b' src/presentation docs/KEYBOARD_SHORTCUTS.md | sort | uniq -c   -> Alt+1 to Alt+3 (View panes, wx_app.rs:6878-6888, the page's :766-768), Ctrl+Shift+1 to Ctrl+Shift+6 (modules), Ctrl+Alt+0 to Ctrl+Alt+3 (the composer, a separate window); no Alt+4 to Alt+9 anywhere
Read tests/wired.rs:960-1000   -> test_the_shortcuts_document_names_no_key_the_code_has_never_heard_of reads src/presentation only and accepts a key built by counting only through `stated`, which holds the label keys from tagging::REACHABLE_BY_KEY at :986-989; the saved searches' keys, built in application::saved_searches, join that list or the page's new rows read as invented
grep -n 'fn answer_the_label_keys_the_menu_cannot' -A 12 src/presentation/wx_app.rs   -> the list's handler ignores a key with Shift or Alt down, so Alt and a digit reach nothing but the menu today
```

Whether Alt and a digit reaches a `KEY_DOWN` handler bound on a tree or a
list control, or is taken by the menu bar's own Alt handling first, is not
known and is measured first (task 2), on built controls in the target's
process, by posting `WM_SYSKEYDOWN` with the Alt bit, the way 12-08 measured
where a key landed on a built form.

**5. The letters.** The submenu's commands hold C, R and D
(`wx_app.rs:7133`, `:7138`, `:7143`); search items take no letter, as label
items take none, and a name's ampersand is doubled so it claims none.
`tests/wired.rs:1977` (`test_no_two_items_on_one_menu_claim_the_same_letter`)
reads the literals and holds them. `docs/KEYBOARD_SHORTCUTS.md` names the
submenu's commands at `:691` and no letter for them.

**6. The gate for each file, at `630e2a67`.** `src/application/saved_searches.rs`
76 tests and 3 records, cases added with the count check named bare;
`src/presentation/wx_app.rs` 199 and 119, none added; `tests/wired.rs` 77 and
19, one list in a test body grown and no test added; the new target at zero.
Re-taken before the first commit with the parser command and
`grep -cxE '\s*#\[(tokio::)?test\]' FILE`, before and after quoted.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation; Pratik may overrule.**
Pratik confirmed the order of phase 13 and said "plan and implement phase 13"
on 2026-09-24.

1. **Which keys (RESEARCH-4 question 1).** Saved searches on Alt+4 to Alt+9,
   six keys, the first six searches of the account in the tree's order; the
   rest from the menu. Quick Steps take Ctrl+Shift+7 to Ctrl+Shift+9 (13-42).
2. **What a key does (question 2).** Exactly what Enter on the search's row
   does, with the tree cursor moved onto the row first so the tree and the
   list agree.
3. **No key for Save This Search itself (question 11).** The `[D]` line is a
   key per search; the command stays on the Edit menu by its letter.
4. **Per account (question 9).** The menu and the keys are the account being
   worked in, as the Label menu is.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the menu's words and keys as rules</name>
  <files>src/application/saved_searches.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/tagging.rs:138-250`, whole range (the labels' words, the model to follow).
    - `src/application/saved_searches.rs:360-380` (`a_row_for`) and `:940-1030` (`created`, `LONGEST_NAME`, `name_for`), and the test module's head at `:1187-1230`.
  </read_first>
  <behavior>
    - `REACHABLE_BY_KEY: usize = 6`; `key_for(position) -> Option<String>`: `Alt+4` for the first, `Alt+9` for the sixth, `None` for none and past the sixth; `what_the_menu_says(names: &[String]) -> Vec<tagging::MenuLine>`: one line per search in the order given, the name with a lone ampersand doubled, the key after a tab where there is one, and nothing at all for no searches (a saved search is made, not offered, so there is no starting set as labels have); `nothing_there(position, how_many) -> String`: "Alt+7 runs saved search 4, and this account has 3.", "has 1." for one, and for none "Alt+4 runs saved search 1, and this account has none yet. Save This Search on the Edit menu makes one."
    - Cases: `test_the_first_six_searches_have_keys_and_the_rest_do_not`; `test_a_name_with_an_ampersand_claims_no_letter`; `test_no_searches_make_no_lines`; `test_a_key_past_the_last_search_says_which_key_and_how_many_there_are`, with one and with none.
  </behavior>
  <action>
    Red first, on a branch. The red commit names the four cases by module
    path (`application::saved_searches::tests::<name>`) and the count check
    bare, since three records name this file, and carries only the
    signatures the cases need, answering nothing, so each runs and fails.
    Run the cases and the count check first, and name everything that fails.

    Then the green commit. Invoke `elegant-code`: `tagging::MenuLine` is
    reused rather than a second struct of the same shape; the key is one
    `format!` over the position, so the six keys cannot drift from the
    number; no `unwrap`.

    Guard record: one on `saved_searches.rs` whose break gives a seventh
    search a key, `test_the_first_six_searches_have_keys_and_the_rest_do_not`
    red; measured, with the three records the count check flags, in one
    `scripts/guards.sh --remeasure` call, as the first run.
  </action>
  <verify>
    <automated>cargo test --lib application::saved_searches:: && cargo test --test house_style test_every_guard_record_says_how_many_tests_the_files_it_names_held</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the four cases by module path and the count check bare.
    - `grep -c 'pub const REACHABLE_BY_KEY: usize = 6' src/application/saved_searches.rs` is 1; `grep -c 'pub fn key_for' src/application/saved_searches.rs` is 1; `grep -c 'pub fn nothing_there' src/application/saved_searches.rs` is 1.
    - `cargo test --lib application::saved_searches::` passes with four tests more than before, both counts quoted.
    - One guard record measured and every flagged record re-measured, quoted.
  </acceptance_criteria>
  <done>What the Saved Searches menu says and which key each search carries come from one rule, with the six keys counted from the position.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the submenu rebuilt from the searches, the keys that run them, and the answer past the last</name>
  <files>src/presentation/wx_app.rs, tests/wired.rs, tests/the_saved_searches_menu_says_the_searches_an_account_has.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:86-120` and `:240-300` (`menu_ids!` and the blocks), `:5570-5600` and `:5890-5905` (the saved search arms and the help topics' range arm), `:7120-7147` and `:7415-7427`, `:10548-10711` (the labels' menu functions), `:12806-12844` and `:12946-12990`, `:18507-18560`, `:19385-19396`, `:2900-2970` (the tree's key handling and Enter).
    - `tests/the_label_menu_says_the_labels_an_account_has.rs`, whole: one window session for the file through a `OnceLock`, a store in `WIXEN_MAIL_DATA`, the real menu bar, items read back, companions.
    - `tests/wired.rs:960-1000`.
    - 12-08's summary for how a key posted to a built control was measured.
  </read_first>
  <behavior>
    - Measured first, in the target's process: a `KEY_DOWN` handler bound on a built `TreeCtrl` and on a built `ListCtrl` with the real binding function, `WM_SYSKEYDOWN` for `7` posted with the Alt bit set, and whether the handler saw Alt and 7; the summary and the target's head comment quote the answer. If it does not arrive, the handler goes where the key does arrive, found the same way, and the summary says where.
    - `menu_ids!` reserves `ID_SAVED_SEARCH_FIRST[SAVED_SEARCHES_ON_THE_MENU]`, fifty; `rebuild_the_saved_search_menu(menu, names)` deletes every item and appends one per `saved_searches::what_the_menu_says` line under the id at its place, then, when there was at least one search, a separator, then Edit &Conditions..., &Rename... and &Delete as they read today; the builder at `:7127-7145` calls it with no names, so the commands are written in one place; a search past the fiftieth is logged and left to the tree. The function's menu parameter is named `saved_search_menu`, the builder's own name, because `tests/wired.rs`'s letter check finds a menu's items by `let saved_search_menu = Menu::builder()` and by every line that starts `saved_search_menu.append` (`menu_block`, `tests/wired.rs:1872-1903`); named anything else, the commands' letters would leave the check's sight, which is how `rebuild_the_label_menu` keeps its own (`labels_menu`) in view.
    - `put_the_saved_searches_on_the_menu(frame, names)` finds the submenu by `ID_EDIT_SEARCH_CONDITIONS`, reads the items before the first separator, and rebuilds only when they differ from what the rule says, with a comparison spelled with its own names (premise 3). The `SavedSearchesLoaded` arm calls it with the names of the active account's searches in `SavedSearchesRead.order`, after storing the map.
    - A range arm, `_ if id >= ID_SAVED_SEARCH_FIRST && id < ID_SAVED_SEARCH_FIRST + SAVED_SEARCHES_ON_THE_MENU`, finds the search at that place in the active account's order, puts the tree cursor on its row with `select_row` by its identity, and runs it through `the_search_a_row_names` and `run_a_saved_search`, the path Enter takes; an unreadable search is refused in `SAVED_BY_ANOTHER_VERSION`'s words by that path.
    - `a_saved_search_key_the_menu_does_not_answer(menu_bar, position)`, public, and `answer_the_saved_search_keys_the_menu_cannot(control, frame, answer)`, public, bound on the folder tree and the message list: Alt with a digit from 4 to 9 and neither Ctrl nor Shift, whose item the menu does not have, is consumed and answered with `nothing_there`, through the status line and an announcement at Normal; every other key is left to the control.
    - `tests/wired.rs`: the loop that states the label keys gains one stating `saved_searches::key_for` for every place up to `saved_searches::REACHABLE_BY_KEY`, in the same test body; no test added.
    - The target: a store in the test with two accounts, one with three searches (one renamed, one moved with `put_saved_searches_in_order`, one written with a join word no build knows, so it is unreadable), one with seven; the real menu bar; the submenu read as the window starts (the three commands only), after each account's searches are put on it (`Invoices\tAlt+4` and so on, the unreadable one at its place with its key, the seventh with none), after the same names are put again (left as it was: a marker set on the first item survives); `a_saved_search_key_the_menu_does_not_answer` for 4 with three searches and not for 3; the key measurement above as a reading; source readings over `what_ships` that the `SavedSearchesLoaded` arm calls `put_the_saved_searches_on_the_menu(` and that the range arm calls `select_row(` before `run_a_saved_search(`. Each reading has a planted-fault companion that is refused.
  </behavior>
  <action>
    Red first, on the same branch: the target's readings bare. The red
    commit carries the public signatures the target needs, answering
    nothing. Run the target first and name everything that fails.

    Then the green commits: the ids and the rebuild, then the arm and the
    range arm, then the key handler, `cargo build` between each, and the
    `tests/wired.rs` list with the first of them, since the page's rows in
    task 3 would otherwise be read as invented keys. Invoke `elegant-code`:
    one rebuild function called from the builder and the loader; the range
    arm is a lookup and a call; no `unwrap`. The target's head comment says
    what is read and what is not (whether NVDA reads the key beside an item,
    which is the ledger's).

    Guard records: one on `wx_app.rs` whose break drops the call from the
    `SavedSearchesLoaded` arm, the arm reading red, `suite` the target; one
    on `wx_app.rs` whose break makes the comparison always equal, the rename
    and move readings red, `suite` the target; each measured. `wx_app.rs`'s
    test count and `tests/wired.rs`'s are quoted unchanged.
  </action>
  <verify>
    <automated>cargo test --test the_saved_searches_menu_says_the_searches_an_account_has && cargo test --test wired && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's readings bare.
    - `grep -c 'fn rebuild_the_saved_search_menu' src/presentation/wx_app.rs` is 1; `grep -c 'rebuild_the_saved_search_menu(' src/presentation/wx_app.rs` is at least 3; `grep -c 'put_the_saved_searches_on_the_menu(' src/presentation/wx_app.rs` is at least 2; `grep -c 'ID_SAVED_SEARCH_FIRST\[SAVED_SEARCHES_ON_THE_MENU\]' src/presentation/wx_app.rs` is 1; `grep -cx '    if shown != wanted {' src/presentation/wx_app.rs` is 1.
    - `grep -c 'saved_searches::key_for' tests/wired.rs` is 1.
    - The target passes with at least 10 tests, quoted; `cargo test --test wired` and `cargo test --lib presentation::wx_app::` pass with the counts they had before the plan, quoted; the key measurement's answer is in the summary.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The Saved Searches submenu says the account's searches in the tree's order with the keys that run them, a key runs the search the way Enter does, and a key past the last is answered in words.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the Action menu row dated, the changelog, the ledger and the four marks</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:117-146` (The Sidebars, as 13-37 left it), `:661-692` (Action Menu, the Saved Searches row at `:691`), `:761-770` (View Menu, where Alt+1 to Alt+3 sit), `:1436-1438` (Customization).
    - `docs/USER_GUIDE.md`, the saved searches paragraph 13-37 wrote under Search Functionality.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: in The Sidebars, a table or
    list saying Alt+4 to Alt+9 run the first six saved searches of the
    account you are in, in the order the tree shows them, and put the cursor
    on the row, and that a key with no search says which search it would
    run and how many there are. The Action Menu table's Saved Searches row
    says the account's searches in their order, the first six with their
    keys, then Edit Conditions, Rename and Delete for the one you are on,
    followed by a dated sentence that until 2026-09-24 the row said "Rename
    or delete" while Edit Conditions was there too (#58 point 5). The
    Customization sentence stays as it is: a key by position is not a
    customisation. The guide: the keys, in two sentences. The changelog,
    `[Unreleased]`, Added: a key per saved search and the menu listing them,
    in #58's words; Known limitations: nobody has heard the submenu's items
    with their keys.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's ear
    (the items and keys under NVDA, the answer to a key past the last, the
    cursor landing on the row a key ran).

    The four marks: phase 13's progress row; this plan's `- [ ]` line in
    phase 13's plan list (ROADMAP.md; the phase README keeps no boxes) ticked;
    `STATE.md`'s plan number in the frontmatter and the body; a dated line
    under GAP-09's `[D]` line saying the second clause is closed, naming
    `test_the_first_six_searches_have_keys_and_the_rest_do_not` and the
    target, the box left for 13-39. Run
    `cargo test --test the_planning_files_agree_with_themselves` first.

    This plan changes what is spoken and shown, so after this one documents
    commit the branch is pushed with a pull request into `main` under
    Pratik's standing OK of 2026-09-23, and CI, NVDA and Accessibility are
    read before the local merge.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Alt+4' docs/KEYBOARD_SHORTCUTS.md` is at least 1 and `grep -c 'Alt+9' docs/KEYBOARD_SHORTCUTS.md` is at least 1; the Action Menu section's Saved Searches row names Edit Conditions (`awk '/^### Action Menu/,/^### Account Management/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'Edit Conditions'` is at least 1).
    - `cargo test --test wired` passes, so every key the page names is one the code states.
    - The ledger entry in both halves; carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages name the six keys where the saved searches are described, the drifted Action menu row is corrected by dating, and the four marks are made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the menu's words to the key's action | two readers of one list that must agree |
| a search's name to a menu label | a name somebody typed, read as a label with access letters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-38-01 | Tampering | a key running a search other than the one its item names | high | mitigate | one order, `SavedSearchesRead.order`, read by the menu and the range arm; the target reads both; two guard records |
| T-13-38-02 | Tampering | a name with an ampersand claiming a letter on the submenu | low | mitigate | the ampersand doubled in `what_the_menu_says`; its case |
| T-13-38-03 | Denial of service | an open menu emptied under somebody by a rebuild on every load | medium | mitigate | the rebuild only when the words differ; the target's reading that the same names leave the menu as it was |
| T-13-38-04 | Denial of service | a key past the last search heard as nothing | low | mitigate | the handler on the tree and the list answers in words; measured on built controls |
| T-13-38-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes what is spoken and shown: after the one documents commit, the branch is pushed with a pull request into `main` (Pratik's standing OK of 2026-09-23), CI, NVDA and Accessibility read on it, a red one fixed on the branch; the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What the tester's ear settles: the items and keys heard, a key past the last heard. What the readings prove: the menu's items after a load, a rename and a move; the key's place; the arm's calls; the key reaching the handler on built controls.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 9 advances by its second clause: saved searches can be given a key.
- GAP-09's second `[D]` clause has a named test and reading in the summary and a dated line under the `[D]` line; the box waits for 13-39; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-09's dated line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-38-SUMMARY.md` when done
</output>
