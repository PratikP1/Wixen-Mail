---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 46
type: execute
wave: 48
depends_on: [13-45]
files_modified:
  - src/service/mailbox_archive.rs
  - src/application/exporting_mail.rs
  - src/application/export_tree.rs
  - src/application/importing_messages.rs
  - src/presentation/wx_app.rs
  - tests/mail_goes_out_in_every_shape.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-13]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "File, Export Folder as Message Files (Alt+F then X, no shortcut key) writes the mail of the folder the cursor is on as one saved message per file into a folder the person chooses, with a folder for each folder inside it, and Import a Folder of Messages reads what it wrote back as the same folders and messages (GAP-13's loose eml line, #53 point 4)."
    - "Each file is named with the message's date first and then its subject, 2026-09-24 Hello.eml (decision 4), cleaned by the rule Save As already uses, told apart from every other name in its folder ignoring case, and never written over a file already there: the writer opens with create_new and takes the next number, and the closing sentence counts the messages saved under a numbered name."
    - "A message is written by the step Save As uses, so a kept signed original goes out byte for byte and a message rebuilt from the store carries the files this computer has; a message whose text was never downloaded is left out and counted, with the files not here and the signatures not kept, in the same words as the other two exports."
    - "A saved search, a label or a branch is refused through the folder question 13-45 put in one helper, and the reading 13-45 wrote now counts this handler as well."
  artifacts:
    - "src/service/mailbox_archive.rs: MessageFilesBeingWritten, a_folder and a_message_file trying each offered name with create_new until one is free."
    - "src/application/export_tree.rs: a_message_file_named (date first, then subject) and numbered_names, over one_nothing_else_has_taken made shared; what_the_message_files_export_did."
    - "src/application/importing_messages.rs: the subject made into a file name's stem, extracted from saving_as and shared, still in this file."
    - "src/application/exporting_mail.rs: one_folder_as_message_files, the round trip through the folder import tested against a real store."
    - "src/presentation/wx_app.rs: ID_EXPORT_A_FOLDER_AS_MESSAGE_FILES, the item, its arm, export_a_folder_as_message_files with a folder picker and a worker."
  key_links:
    - "File menu item -> arm -> export_a_folder_as_message_files -> a_folder_to_write_out -> DirDialog -> worker -> exporting_mail::one_folder_as_message_files -> export_tree::one_message_written_out and a_message_file_named -> MessageFilesBeingWritten::a_message_file (create_new) -> what_the_message_files_export_did."
    - "Save As and this export name a file through one stem function in importing_messages.rs, so the cleaning of a stranger's subject happens one way."
---

<objective>
One folder out as loose saved messages, the second half of #53's
point 4.

Purpose: #53, point 4: "never a bare `.mbox` or loose `.eml`, so one
folder for Thunderbird means unzipping by hand." 13-45 built the bare
mailbox file; this builds the loose files, which is also the shape the
page 13-49 writes will point a person at for taking mail to Outlook.

Output: the directory writer, the names, the loop, the sentence, the
menu item and its handler, the readings, the pages, the changelog, the
ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-5-import-export.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-45-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before 13-45 ran.
Command first, then what it said. 13-45 changes the files this plan
edits, so every count and line is re-taken on the day; 13-45's summary
says what it left, and where it and this plan disagree the tree wins.

**1. The File menu's letters.** Premise 1 of 13-45, the same command:

```
sed -n '/let file = Menu::builder()/,/\.build();/p' src/presentation/wx_app.rs | grep -o '"[^"]*&[^"]*"'
   -> on 2026-09-24: N (the prepended New submenu), S, A, M, D, I, O, E, K, Q; X free
```

By the day this runs, 13-03 has added P, 13-17 has put the key manager
in Import PGP Private Key's place, and 13-45 has taken F (or the letter
its summary names). This plan takes **X**, "E&xport Folder as Message
Files...", which is also the letter Windows programs give Export. The
command is re-run before the label is written and quoted; if X is
taken, another free letter that reads in the label, said in the
summary. `docs/KEYBOARD_SHORTCUTS.md` lists no File menu letter
(`grep -n 'Alt+F\b'` finds dialog accelerators and ":1444 Alt+F for File
menu" only).

**2. What the loose export is built from.**

```
grep -n 'pub fn one_message_written_out\|fn one_nothing_else_has_taken\|fn numbered\|pub fn where_each_folder_goes' src/application/export_tree.rs   -> :309, :615 (private), :630 (private), :523
grep -n 'pub fn saving_as\|A_MESSAGE_WITH_NO_SUBJECT_IS_CALLED\|attachment_name::safe_file_name' src/application/importing_messages.rs   -> :622, :650, :639
grep -n 'const MAX_LENGTH' src/service/attachment_name.rs   -> :39, 120 characters
grep -n 'pub date' src/data/message_cache/messages.rs   -> :670, MessageListRow.date: String, the Date header as RFC 3339 (mime.rs:41-42)
sed -n 14480,14501p src/presentation/wx_app.rs   -> Import a Folder of Messages: DirDialog::builder(frame, "...", "").build() at :14492, the picker this plan's handler copies
sed -n 12,19p src/service/mailbox_archive.rs; grep -n 'pub fn opened' src/service/mailbox_archive.rs   -> opened reads a folder or a zip into the same entries (:189)
grep -n 'pub fn where_the_folders_land\|pub fn what_was_chosen' src/application/import_tree.rs   -> :351, :536 (a folder answers AnArchive)
```

So a folder of `.eml` files with folders inside it is exactly what
Import a Folder of Messages reads, and the round trip is a real test of
both halves.

**3. The anchor the extraction moves.** The record "Save As takes the
path out of a subject before offering it as a name" has `file`
`src/application/importing_messages.rs` and `before`
`'/' | '\\' => '_',` (line 632 on 2026-09-24), with red list
`application::importing_messages::tests::test_save_as_takes_the_path_out_of_a_subject_before_offering_it_as_a_name`.
Task 1 moves that line into the extracted stem function, which stays in
`importing_messages.rs`, so the anchor still names one place in its own
file and `saving_as` still reaches it; the record is re-measured in the
same commit. Moving the function to `export_tree.rs` would take the
anchor out of the record's file and break
`test_every_guard_record_still_names_one_place_in_the_tree`, which is
why it stays.

**4. The guard records this plan touches, by both readings.** Counted
with the tomllib parser on 2026-09-24 (1,088 records): `export_tree.rs`
1 in `tests_last_seen` and 0 as `file`; `importing_messages.rs` 5 and 2
(anchors at `:377-381` and `:632`); `mailbox_archive.rs` 0 and 0;
`wx_app.rs` 119 and 112, none anchored in the id list, the File builder
below the key manager's item, the arms or the export handlers;
`exporting_mail.rs` and the target new in 13-45, with the records 13-45
wrote on them. Re-counted on the day, since 13-45 adds records.
</premise_corrections>

<decisions date="2026-09-24">
Taken 2026-09-24 from the research's recommendation; Pratik may
overrule. RESEARCH-5's questions, the two that bind this plan.

**4. A message file's name has the date first.** "2026-09-24 Hello.eml",
so a folder listing reads in the order the mail came. The date is the
calendar day the message's own Date header gives, in the offset it was
written with, so the name does not depend on this computer's time zone;
a message with no usable date is named by its subject alone. Taken
2026-09-24 from the research's recommendation; Pratik may overrule.

**6. A separate item beside Export Mailbox, on X.** As 13-45 records.
Taken 2026-09-24 from the research's recommendation; Pratik may
overrule.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: names with the date first, a writer that never writes over a file, the loop, and the round trip through the folder import</name>
  <files>src/service/mailbox_archive.rs, src/application/export_tree.rs, src/application/importing_messages.rs, src/application/exporting_mail.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/export_tree.rs:299-328` (`one_message_written_out`), `:517-635` (the folder layout and the two private naming functions), and the three shared sentences 13-45 extracted.
    - `src/application/importing_messages.rs:593-661` (`saving_as` and its constants) and the Save As tests beside it.
    - `src/application/exporting_mail.rs` as 13-45 left it, and its test module's store setup.
    - `src/service/mailbox_archive.rs:180-200` (`opened`) and `src/application/import_tree.rs:340-380` (`where_the_folders_land`), the two halves the round trip reads back through.
  </read_first>
  <behavior>
    - `importing_messages::a_subject_as_a_file_stem(subject: &str) -> String`, extracted from `saving_as` with its separator rule, `safe_file_name` and the "message" fallback, in this file (premise 3); `saving_as` calls it and its tests pass unchanged.
    - `export_tree::a_message_file_named(date: &str, subject: &str) -> String`: the stem, with the calendar day of an RFC 3339 date in its own offset in front and a space between ("2026-09-24 Hello"), the subject alone when the date does not read; `export_tree::numbered_names(stem: &str) -> impl Iterator<Item = String>` yielding "stem.eml", "stem (2).eml", "stem (3).eml" and on, with the number before the ending; `one_nothing_else_has_taken` made `pub(crate)` so names in one folder are told apart ignoring case before anything touches the disk. Cases: a date and a subject; an unreadable date; an empty subject ("message"); a subject `CON` with no date is not a device name; a subject with `/` keeps one name; two subjects differing only in case get two names; a 200-character subject stays within the name limit.
    - `service::mailbox_archive::message_files_written_under(root: &Path) -> Result<MessageFilesBeingWritten>`, refusing a root that is not a folder; `a_folder(&self, named: &str) -> Result<PathBuf>` makes the folder and its parents under the root from a name `where_each_folder_goes` gave; `a_message_file(&self, folder: &Path, names: impl Iterator<Item = String>, bytes: &[u8]) -> Result<WhereItWent>` opens each offered name with `create_new` in turn until one is free, writes the bytes, syncs the data and closes, and answers the name used and whether it was the first offered; a write that fails is an error naming the file. Cases: a file already at the first name keeps its bytes and the second name is used; a root that is a file is refused; a folder twelve levels deep with a 120-character name, past 260 characters altogether, is written and read back (RESEARCH-5's A4; if `std::fs` refuses it, the writer uses the verbatim form of the path, and the summary says which happened).
    - `export_tree::what_the_message_files_export_did(written: &FoldersExported, numbered_because_taken: usize) -> String`: "Exported N messages as message files in M folders" (singular forms written out), then the three shared sentences, then "N messages were saved under a numbered name, because a file with that name was already in the folder" when above nought; it joins the module's every-sentence check, which refuses ".eml".
    - `exporting_mail::one_folder_as_message_files(cache, account, folder, root: &Path, progress: &dyn Fn(usize)) -> String`: the folder and every folder inside it, laid out by `where_each_folder_goes`; each message through `export_tree::one_message_written_out` with the text, the files this computer has and the kept original read by the step 13-45 shares; names from `a_message_file_named`, told apart per folder, then offered to the writer through `numbered_names`; a folder no message went into is made empty, as the zip keeps an empty folder; progress every 100 messages and at the end.
    - `application::exporting_mail::tests::test_a_folder_written_as_message_files_comes_back_through_the_folder_import`: a real store in a temporary home; Work with two messages sharing a subject and a third whose subject is `CON` and whose date is empty, and Work/Invoices with one; written into a `tempfile` directory; opened with `mailbox_archive::opened` and placed with `import_tree::where_the_folders_land`: two folders, four messages, two names for the shared subject, no file named as a device. Beside it: `test_a_message_file_already_in_the_folder_is_never_written_over`, the directory already holding the first name with other bytes, those bytes unchanged afterwards and the sentence counting one numbered name.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path in the four
    modules, with the count check named bare, since records name
    `export_tree.rs` and `importing_messages.rs`; the named targets and
    the count check run before the red commit and everything that fails
    named. Then the extraction, the names, the writer, the sentence and
    the loop, with `cargo build` between. Invoke `elegant-code`: the
    writer takes the names it is offered and decides none of them, the
    numbering rule lives in export_tree once, and the date is read by
    the function that already reads an RFC 3339 date in this codebase
    rather than a second parser (find it with
    `grep -rn 'parse_from_rfc3339' src/application` and use that one).

    Guard records: one on `mailbox_archive.rs` whose break opens with
    `File::create` instead of `create_new`, the never-written-over case
    red; one on `export_tree.rs` whose break stops telling names apart
    ignoring case, the case-only case red; one on `exporting_mail.rs`
    whose break names every file by subject alone, the round trip's
    distinct-names reading red. Each measured on the library. The "Save
    As takes the path out of a subject" record re-measured in the same
    commit as the extraction; every record the count check names
    re-measured with one call.
  </action>
  <verify>
    <automated>cargo test --lib application::importing_messages:: && cargo test --lib application::export_tree:: && cargo test --lib service::mailbox_archive:: && cargo test --lib application::exporting_mail::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the count check bare.
    - `grep -cF "=> '_'," src/application/importing_messages.rs` is 1, as it was on 2026-09-24 (the anchor's line still stands once in its file), and `cargo test --test house_style test_every_guard_record_still_names_one_place_in_the_tree` passes.
    - `grep -c 'pub fn a_message_file_named\|pub fn numbered_names\|pub fn what_the_message_files_export_did' src/application/export_tree.rs` is 3; `grep -c 'create_new' src/service/mailbox_archive.rs` is at least 2 (13-45's partial file and this writer).
    - The four module runs pass with their totals quoted, each higher than the total 13-45's summary quotes by the number of cases added here.
    - Three guard records measured and the Save As record re-measured, each quoted; whether the deep-path case needed the verbatim form, said.
  </acceptance_criteria>
  <done>A folder's mail is written as dated, cleaned, distinct message files that never replace a file already there, and this program's own folder import reads the folders and messages back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the File menu item, the folder picker and the worker, and the readings</name>
  <files>src/presentation/wx_app.rs, tests/mail_goes_out_in_every_shape.rs, guards/guards.toml</files>
  <read_first>
    - Premise 1's command, re-run and quoted before the label is written.
    - `src/presentation/wx_app.rs`: the id list, the File builder, 13-45's `export_a_folder_as_a_mailbox_file` and `a_folder_to_write_out`, and `import_a_folder_of_messages` (`:14480-14501` on 2026-09-24) for the folder picker.
    - `tests/mail_goes_out_in_every_shape.rs` as 13-45 left it.
  </read_first>
  <behavior>
    - `ID_EXPORT_A_FOLDER_AS_MESSAGE_FILES` beside 13-45's id; on File, after Export Folder as a Mailbox File: `"E&xport Folder as Message Files..."` with the help string "Write this folder's mail as one saved message per file, with a folder for each folder inside it", no shortcut.
    - `export_a_folder_as_message_files(state, cache, frame, ui_tx, runtime, a11y)`: `a_folder_to_write_out` first; a `DirDialog` titled "Choose the folder the message files go into"; "No folder was chosen." when the path is missing; the opening sentence "Writing this folder's mail as message files." on the status line and announced at normal priority; a worker calling `exporting_mail::one_folder_as_message_files` with the progress closure and the closing sentence on the status line, on 13-45's pattern.
    - The arm calls it.
    - The target gains `test_export_folder_as_message_files_is_on_the_file_menu_with_no_shortcut` and `test_export_folder_as_message_files_reaches_its_writer`; 13-45's census reading now finds three `export_a` handlers, and its failure text quotes the number found.
  </behavior>
  <action>
    Red first, on a branch: the two readings bare, run with the count
    check before the red commit. Then the id, the item, the handler and
    the arm, with `cargo build` between. Invoke `elegant-code`: the
    handler hands over and says; the folder question is 13-45's helper
    and is not written again.

    Before committing green, run
    `cargo test --test house_style test_every_guard_record_still_names_one_place_in_the_tree`,
    since this handler's lines resemble 13-45's, and fix any record
    whose anchor now names two places by lengthening it to the longest
    unique context (CLAUDE.md, "A record is coupled to the tree a second
    way"). Guard record: one on `wx_app.rs` whose break drops the
    `DirDialog`'s answer and writes into the current directory instead,
    the reaching reading red, `suite` the target, anchored on this
    handler's own lines; measured.
  </action>
  <verify>
    <automated>cargo test --test mail_goes_out_in_every_shape && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - The letter list re-taken on the day and quoted, with the letter taken.
    - A red commit on a branch names the readings bare.
    - `grep -c 'ID_EXPORT_A_FOLDER_AS_MESSAGE_FILES' src/presentation/wx_app.rs` is at least 3; `grep -c 'a_folder_to_write_out(' src/presentation/wx_app.rs` is at least 4.
    - The target passes with 6 tests; `wired` passes with its total unchanged; both quoted. One guard record measured and quoted.
  </acceptance_criteria>
  <done>File holds Export Folder as Message Files on a free letter, it asks the one folder question and a folder to write into, and it reaches the writer.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the changelog, the ledger, and the branch pushed for CI, NVDA and Accessibility</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - The rows and sentences 13-45 added to the guide's Import and Export section and the shortcuts page's File table, and its changelog entry and ledger entry.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: a row after 13-45's,
    "Export Folder as Message Files", "(none)", saying it writes one
    saved message per file (`.eml`), dated then named by subject, with a
    folder for each folder inside, and never replaces a file already
    there. The guide: the command count corrected, a table row, and two
    sentences: the naming rule with its example, and that Import a
    Folder of Messages reads the result back. Claim nothing about
    another program: nobody has tried it.

    The changelog, `[Unreleased]`, Added: Export Folder as Message
    Files, #53 point 4's second half, with decision 4's naming; Known
    limitations: no file this writes has been opened in another mail
    program and nobody has heard the item or its sentences. One dated
    sentence at the end of the gathered issue 53 paragraph saying point
    4's second half landed.

    Ledger, by hand, both halves: one `unrun-verify` for the tester, the
    files opened in Thunderbird and in his own mail program, a folder
    with folders inside it written and read back by Import a Folder of
    Messages on his machine, and the item and sentences heard under
    NVDA. Nothing touches a server; nothing for phase 14.

    What is shown and spoken changes, so the branch is pushed with a
    pull request after the last branch commit, the three runs read, any
    red fixed on the branch, and then the local `git merge --no-ff`.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Export Folder as Message Files' docs/KEYBOARD_SHORTCUTS.md` and the same over `docs/USER_GUIDE.md` are each at least 1.
    - The ledger entry in both halves, quoted by number; carriage returns 0 on every file touched; no em or en dash; none of the six words.
    - The pull request's number and the three runs' conclusions quoted.
  </acceptance_criteria>
  <done>The pages describe the loose export and its naming, the changelog says what nobody has tried, and the runner has read the branch before the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's subject to a file name | text a sender chose becomes a path on a person's disk |
| the export to a folder somebody already uses | files written beside ones they own, with no overwrite prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-46-01 | Tampering | a subject naming a path, a device or a reversed name | high | mitigate | the separator rule, then `safe_file_name` (devices, separators, bidirectional overrides, 120 characters), through one stem function Save As shares; cases for `CON`, `/` and a long subject |
| T-13-46-02 | Tampering | a person's file overwritten by an export into a folder they use | high | mitigate | `create_new` for every file and the next number when a name is taken; no check-then-write; a guard record holds it |
| T-13-46-03 | Repudiation | an export that reads as complete and is not | medium | mitigate | the three shared sentences and the numbered-name count |
| T-13-46-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the branch is pushed with a pull request and the three runs read before the merge; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite runs once, in 13-51's full gate; never `scripts/check.sh all` by hand here. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line.
- Four guard records measured and the Save As record re-measured; every record the count check flagged re-measured with one call and read.
- What the tester settles: the files opened in another mail program, the item and sentences heard; what the tests prove: the round trip through this program's own folder import, names that never replace a file, the date-first rule.
</verification>

<success_criteria>
- GAP-13's loose eml half has a named test and a reading in the summary; the `[D]` line is left for 13-49 to tick; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (the roadmap's, and the phase README's if one exists); `.planning/STATE.md`'s plan number in the frontmatter and in the body; GAP-13's line and traceability row carrying a dated note that its loose eml half landed here.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-46-SUMMARY.md` when done
</output>
