---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 19
type: execute
wave: 20
depends_on: [13-18]
files_modified:
  - src/service/signed_mail.rs
  - src/service/protocols/smtp.rs
  - src/application/mail_controller.rs
  - src/data/message_cache/mod.rs
  - src/data/message_cache/correspondent_certificates.rs
  - src/data/message_cache/how_it_arrived.rs
  - guards/guards.toml
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-05]
estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A message can be signed with an S/MIME certificate whose key is in the Windows store: CryptSignMessage with signed attributes, a detached signature over the exact CRLF bytes of the inner part, wrapped as multipart/signed with protocol application/pkcs7-signature and a micalg; this project's own verifier and OpenSSL both say it holds (GAP-05, #52 point 4)."
    - "A message can be encrypted with S/MIME to each recipient's certificate and to the sender's own, with PKCS #1 v1.5 key transport and AES-256-CBC, as application/pkcs7-mime; smime-type=enveloped-data; 13-14's opener and OpenSSL both open it (#52 point 5)."
    - "The certificate a correspondent signs with is kept, when their signature holds and the certificate names their address, in an additive table in the mail database, so a reply to them can be encrypted; certificates are not secret."
    - "build_message takes the protection an Email asks for and wraps the body it already builds; a plain Email is byte for byte what it was."
    - "None of this is reached from the composer until 13-21; the changelog and the ledger say so, and nothing presents it as done."
  artifacts:
    - "src/service/signed_mail.rs: CryptSignMessage and the encrypting call in the file's #[link] block; sign_detached and encrypt_to; the key transport read back by the file's own ASN.1 reading."
    - "src/service/protocols/smtp.rs: Protection (Plain, SmimeSigned, SmimeEncrypted, SmimeSignedAndEncrypted) on Email, and the wrapping in build_message."
    - "src/data/message_cache/correspondent_certificates.rs: the table, keep, certificates_for(address); how_it_arrived.rs: kept at arrival from a signature that holds."
  key_links:
    - "Email.protection -> build_message -> the body part formatted -> signed_mail::sign_detached with the sender's certificate from the store -> multipart/signed; or encrypt_to with the recipients' kept certificates and the sender's -> application/pkcs7-mime -> send_email; the Sent copy is what was sent."
    - "a signed message's arrival -> examine_signed_message holds and names the sender -> correspondent_certificates::keep -> certificates_for when 13-21 encrypts to them."
---

<objective>
Signing and encrypting outgoing mail with S/MIME: the service half.

Purpose: GAP-05's "a message can be sent signed and encrypted", S/MIME
side, #52 points 4 and 5. The research split the sending plan in two
because it is two libraries and two fixtures; this is the Windows CryptoAPI
half, 13-20 is rPGP's, and 13-21 puts the choice in the composer. #52 said
sending needed the send path proven live first; Pratik proved it on
2026-09-18 (phase 12's README), so the condition is met.

Output: the two Windows calls, the wrapping in the one function every send
reaches, the correspondents' certificates kept, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-14-SUMMARY.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said.

**1. Where every send goes.**

```
grep -n "fn build_message\|pub async fn send_email\|let sent = message.formatted()" src/service/protocols/smtp.rs   -> :134, :323, :340: every send builds here and the Sent copy is the formatted message
grep -rn "send_email(" src --include=*.rs | grep -v "fn send_email"   -> mail_controller.rs:601, sent_copy.rs:925, wx_app.rs (five sites), smtp.rs tests; each goes through build_message
grep -rn "sendMail" src   -> nothing: no Graph or Gmail API send path to wrap separately
grep -rn "Email {" src tests --include=*.rs | grep -v "pub struct Email\|SendEmail\|MsEmail\|EmailAddress\|GoogleEmail" | wc -l   -> 17 literal lines, in smtp.rs and mail_controller.rs; each gains protection: Protection::Plain
~/.cargo/registry/src/*/lettre-0.11.22/src/message/mimebody.rs:180-184, :333-342   -> MultiPartKind::Signed { protocol, micalg }, MultiPart::signed (research)
```

**2. What the research's probe measured about Windows' defaults.** Windows
signed with no signed attributes (`signedAttrs: <ABSENT>`) unless some were
passed, and encrypted with RSA-OAEP key transport when nothing asked for
one. Both are acted on: task 1 passes a signing time as an authenticated
attribute, which makes Windows write the signed attributes block, and asks
for `rsaEncryption` key transport. `CryptEncryptMessage`'s parameter block
has no field for the key encryption algorithm; if task 1's first
measurement shows OAEP again, the envelope is built with
`CryptMsgOpenToEncode` and a `CMSG_ENVELOPED_ENCODE_INFO` whose recipient
infos name `1.2.840.113549.1.1.1`, declared in the same `#[link]` block,
and the summary says which route was taken.

**3. How the file calls Windows.** Flat calls in its own `#[link(name =
"crypt32")]` block (`signed_mail.rs:2813-2818`, `:3031-3068`), which 13-14
extended with `CryptDecryptMessage`. The new calls join it; no manifest
line changes.

**4. The gate for each file**, by the TOML reader on 2026-09-24:
`signed_mail.rs` 113 tests, 5 and 5; `smtp.rs` 40, 4 and 2;
`mail_controller.rs` 71, 23 and 5; `data/message_cache/mod.rs` 23, 12 and
12; `how_it_arrived.rs` 9, 3 and 0; the new module 0; plus 13-14 to 13-18's
additions. `mail_controller.rs` at 23 records naming it: its edit is the
`protection` field in literals, no test added, so no count moves.

**5. The anchors inside what this plan edits.** `smtp.rs:137-141`, "a sent
message carries an identifier of its own", and `:169-171`, "a blind copy
reaches the server and never the message", lie in `build_message`'s header
half; task 2 changes the body half after `:190`, and extracting the body
into its own function must move neither block. If the extraction
re-indents or moves either, the record is rewritten and re-measured in the
same commit (CLAUDE.md, the 10-04 case). `signed_mail.rs`'s five anchors are
outside the `#[link]` block and the new functions.
</premise_corrections>

<decisions date="2026-09-24">
**1. Key transport: PKCS #1 v1.5.** Taken 2026-09-24 from the research's
recommendation (question 6); Pratik may overrule: the older padding until a
real Outlook and Apple Mail round trip shows OAEP opens, because a message
nobody can open is worse than the weaker padding. The changelog says which.

**2. Encrypt to the sender too.** The research's pitfall 9: encrypted only
to the recipients, the Sent copy cannot be read by the person who sent it.

**3. Correspondents' certificates kept at arrival.** Taken 2026-09-24 from
the research's recommendation (question 7); Pratik may overrule: a table in
the mail database, from every signed message whose signature holds and whose
certificate names the sender's address, so replying encrypted works.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CryptSignMessage and the encrypting call, checked by this project's own reader and by OpenSSL</name>
  <files>src/service/signed_mail.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/signed_mail.rs` `windows_store` as 13-14 left it (the `#[link]` block, `open_the_envelope`, `holding_only_in_memory`), `:1861` (`examine_signed_message`), `:3731-3830` (`EncryptedMessage::read`).
    - `~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/Security/Cryptography/mod.rs` around `:1256` and `:1842` for `CRYPT_SIGN_MESSAGE_PARA` and `CRYPT_ENCRYPT_MESSAGE_PARA`'s layouts, read and not used.
  </read_first>
  <behavior>
    - `sign_detached(content: &[u8], signer: &Signer) -> Result<Vec<u8>>`: `CryptSignMessage` with detached signature, SHA-256, the signer's certificate included, and a signing time as an authenticated attribute so the signed attributes block is written.
    - `encrypt_to(content: &[u8], recipients: &[Vec<u8>]) -> Result<Vec<u8>>`: AES-256-CBC content, one recipient info per certificate DER, key transport `rsaEncryption`.
    - `CertificateStore::signer_for(address) -> Option<Signer>`: a certificate in the store with a private key and the address in its subject or alternative names; the in-memory store in tests.
    - Cases: the keyholder key held in memory signs a MIME part; `examine_signed_message` over a `multipart/signed` built around it says it holds and names the keyholder; the parsed signature carries signed attributes; one changed byte of the content does not hold. Encrypting to the keyholder's certificate and to Bob's: 13-14's `open_the_envelope` on the in-memory store returns the content byte for byte; `EncryptedMessage::read` lists two recipients and reports each key transport as `rsaEncryption`. `signer_for` an address no certificate names is `None`.
    - Outside the tests, once, recorded in the summary: `openssl cms -verify -noverify` on the signed output and `openssl cms -decrypt` on the envelope with Bob's key from the research's fixtures made again, and `openssl cms -cmsout -print` showing `rsaEncryption` and the signed attributes.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check.
    Then the declarations and the two functions, with `cargo build`
    between. Measure the key transport first; take the `CryptMsgOpenToEncode`
    route of premise 2 only if `CryptEncryptMessage` cannot be told. Invoke
    `elegant-code`: one unsafe function per call with its buffer sizing; no
    Windows message text crosses out.

    Guard records, measured: one whose break drops the authenticated
    attribute, the signed-attributes case red; one whose break leaves the
    sender out of the recipients (in the caller task 2 writes, or here if
    the list is built here), the two-recipients case red.
  </action>
  <verify>
    <automated>cargo test --lib service::signed_mail::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c 'fn CryptSignMessage' src/service/signed_mail.rs` is 1.
    - The run passes, count quoted; the three OpenSSL commands and what they printed are in the summary.
  </acceptance_criteria>
  <done>Windows signs with signed attributes and encrypts with PKCS #1 v1.5 key transport, and two independent readers agree with it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the body build_message makes, wrapped in the protection an Email asks for</name>
  <files>src/service/protocols/smtp.rs, src/application/mail_controller.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/protocols/smtp.rs:50-135` (`Email`, `simple`), `:134-320` (`build_message`), `:323-360` (`send_email`, the Sent copy), and its tests around `:470-480`.
    - `src/application/mail_controller.rs:195-260` (`outgoing`).
  </read_first>
  <behavior>
    - `Protection { Plain, SmimeSigned { signer: Signer }, SmimeEncrypted { recipients: Vec<Vec<u8>> }, SmimeSignedAndEncrypted { signer, recipients } }` on `Email`, `Plain` in `simple` and in every literal.
    - `build_message` builds the body it builds today as one part, then: `Plain` sends it as the message body exactly as now; `SmimeSigned` formats the part with CRLF endings, signs those bytes, and sends `multipart/signed; protocol="application/pkcs7-signature"; micalg=sha-256` holding the part and `smime.p7s`; `SmimeEncrypted` encrypts the formatted part to the recipients plus the sender's own certificate and sends `application/pkcs7-mime; smime-type=enveloped-data; name=smime.p7m`; signed and encrypted signs first and encrypts the signed entity.
    - Cases: a plain `Email` formats to the same bytes as before the change (a fixed message compared with its formatted text taken before the edit and kept as the case's expected value); a signed `Email` formatted and handed to `examine_signed_message` holds; an encrypted one opened by `open_the_envelope` gives back a part `service::mime::parse` reads with the body text; a signed-and-encrypted one opens to a signed entity that holds; the headers above the body (From, To, Message-ID, In-Reply-To) are unchanged by protection.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check.
    Take the plain message's formatted bytes before touching `build_message`
    and keep them as the plain case's expected value. Then the enum, the
    extraction of the body into a function that returns the part, and the
    wrapping, with `cargo build` between. Keep the header half's blocks at
    `:137-141` and `:169-171` where they are (premise 5).

    Guard records, measured: one on `smtp.rs` whose break signs the part's
    LF form instead of its CRLF form, the signed case red; one whose break
    sends the unwrapped body for `SmimeEncrypted`, the encrypted case red.
  </action>
  <verify>
    <automated>cargo test --lib service::protocols::smtp:: && cargo test --lib application::mail_controller:: && cargo test --test house_style test_every_guard_record_still_names_one_place_in_the_tree</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c 'pkcs7-signature' src/service/protocols/smtp.rs` is at least 1; `grep -c 'protection: Protection::Plain' src/service/protocols/smtp.rs src/application/mail_controller.rs` reports every literal, the total equal to the literal count `cargo build --all-targets` required.
    - The three runs pass, counts quoted.
  </acceptance_criteria>
  <done>Every send can be signed, encrypted or both with S/MIME in the one function every send reaches, and a plain send is unchanged to the byte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: a correspondent's certificate kept when their signature holds</name>
  <files>src/data/message_cache/correspondent_certificates.rs, src/data/message_cache/mod.rs, src/data/message_cache/how_it_arrived.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/how_it_arrived.rs` whole (`note_the_form_it_arrived_in`, 13-15's third question).
    - `src/service/signed_mail.rs:1861` onward (`examine_signed_message`'s report: signer, certificate DER, the address match).
    - A small cache module that creates its table, such as 13-16's `pgp_keys.rs`.
  </read_first>
  <behavior>
    - `correspondent_certificates (fingerprint TEXT PRIMARY KEY, address TEXT NOT NULL, der BLOB NOT NULL, not_after TEXT, seen_at TEXT NOT NULL)` through `CREATE TABLE IF NOT EXISTS`; `keep_correspondent_certificate`, `certificates_for(address) -> Vec<Vec<u8>>` (unexpired first, newest first), `forget_correspondent_certificate`.
    - `note_the_form_it_arrived_in` asks a fourth thing: for S/MIME signed bytes, `examine_signed_message` with the sender's address; when the signature holds and the signer's certificate names that address, its DER is kept; otherwise nothing is kept. A failure here never costs the other three facts.
    - Cases: a signed fixture whose signature holds keeps its certificate under the sender's address; a signature that does not hold keeps nothing; a certificate naming another address keeps nothing; `certificates_for` returns it; a database opened before the table existed opens.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check,
    since three records name `how_it_arrived.rs`. Then the table and the
    fourth question, with `cargo build` between. `scripts/guards.sh
    --remeasure` once for every record the count check flags.

    Guard record, measured: one whose break keeps a certificate whose
    signature does not hold, its case red.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::correspondent_certificates:: && cargo test --lib data::message_cache::how_it_arrived::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c 'CREATE TABLE IF NOT EXISTS correspondent_certificates' src/data/message_cache/correspondent_certificates.rs src/data/message_cache/mod.rs` reports 1 in one of them.
    - Both runs pass, counts quoted.
  </acceptance_criteria>
  <done>A correspondent's certificate is on this computer once they have sent a signature that holds, ready to encrypt a reply to them.</done>
</task>

<task type="auto">
  <name>Task 4: the changelog and the ledger, saying the half that is not yet reachable</name>
  <files>docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/changelog.md`'s `[Unreleased]`.
  </read_first>
  <action>
    Invoke `writing-craft`. The changelog under `[Unreleased]`, Known
    limitations: signing and encrypting with S/MIME is built beneath the
    composer and is not offered anywhere yet; it arrives with the Sign and
    Encrypt boxes (13-21), which is where the Added entry goes. Under Added,
    only what a person already gets: a correspondent's certificate is kept
    when their signature holds. PKCS #1 v1.5 key transport and why, in the
    Known limitations line.

    Ledger, by hand, both halves: one `stub` entry, "S/MIME signing and
    encrypting is reached only by tests until 13-21 wires the composer",
    naming 13-21 as the plan that closes it; 13-21's `<premise_corrections>`
    already names this entry as the one its wiring fixes, so the assignment
    is in the plan that carries it (CLAUDE.md, a ledger entry handed to a
    later plan); one `unrun-verify` for phase 14: a signed and an
    encrypted message sent by this program opened in Outlook, Thunderbird
    and Apple Mail, and whether they accept PKCS #1 v1.5.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - Two ledger entries in both halves, the stub naming 13-21; no em dash or en dash; none of the six words; carriage returns 0.
  </acceptance_criteria>
  <done>What is built and what is not yet reachable is said plainly, and the entry that says so names the plan that closes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the person's private key in Windows to a signature | a signature made in their name |
| a stranger's certificate to an encrypted reply | encrypting to a key someone sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-19-01 | Spoofing | a certificate kept from a forged signature and later encrypted to | high | mitigate | kept only when the signature holds and the certificate names the sender's address; a guard record |
| T-13-19-02 | Information disclosure | a Sent copy the sender cannot read, or a message sent unencrypted by mistake | medium | mitigate | the sender is always a recipient; the protection is carried on the Email and a case asserts the wrapped bytes |
| T-13-19-03 | Tampering | a signature over bytes that differ from those sent | high | mitigate | the part's CRLF formatted bytes signed and embedded; a guard record on the LF form |
| T-13-19-04 | Information disclosure | a weaker key transport | low | accept | PKCS #1 v1.5 chosen for reach, Pratik's to overrule; said in the changelog; phase 14 tests the alternative |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate and no manifest line; the calls join the file's `#[link]` block |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass. `scripts/check.sh` through the hook on every commit, never piped; no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the changelog, the ledger, the summary and the four completion marks in one documents commit before the merge; `git merge --no-ff`.
- Spoken or shown: no. Nothing on a window or in a sentence changes until 13-21, so the branch merges without a pull request (Pratik's answer of 2026-09-23).
- Reachability: said, not hidden. `sign_detached`, `encrypt_to` and the non-plain `Protection` arms have no non-test caller until 13-21; the stub entry says so.
</verification>

<success_criteria>
- GAP-05's sending clause has its S/MIME half on named tests; the box waits for 13-21.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-05's traceability row naming this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-19-SUMMARY.md` when done
</output>
