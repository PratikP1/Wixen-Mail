---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 40
type: execute
wave: 42
depends_on: [13-39, 13-23, 13-24.1]
files_modified:
  - src/application/quick_steps.rs
  - src/application/mod.rs
  - src/application/filters.rs
  - src/application/saved_searches.rs
  - src/data/message_cache/quick_steps.rs
  - src/data/message_cache/mod.rs
  - src/data/message_cache/accounts.rs
  - guards/guards.toml
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-11]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Quick Step is a name and what it does, held as a rule's Outcome (read or unread, flag or unflag, a label, a phrase to say first, a folder to move to, or delete), stored per account in its own two tables in the words a rule's action is stored in, so one reader serves rules and steps (GAP-11: a rule's actions, named)."
    - "A step that does nothing, deletes beside anything else, carries a phrase the rule reader would refuse, or has a name that is empty, too long or another step's in that account whatever its case, is refused with a sentence saying what to change, before anything is written."
    - "Steps keep an order, a new one goes last, the first three carry Ctrl+Shift+7 to Ctrl+Shift+9 by place, and a step whose actions this build cannot read is kept at its place and never half run."
    - "An account's steps go when the account does, a database from before opens and keeps everything, and every table is added with CREATE TABLE IF NOT EXISTS."
    - "Nothing runs a step yet, and the ledger says so in an entry 13-42's own text names and closes; nothing a person sees changes, so no changelog entry and no push."
  artifacts:
    - "src/application/quick_steps.rs: QuickStep, StoredStep, actions_of, name_for, what_stops_a_step_being_saved, what_it_does_in_words, reach, WHICH_STEP, moved, REACHABLE_BY_KEY, key_for, what_the_menu_says, nothing_there, what_a_step_did, QUICK_STEPS_ARE_EXPERIMENTAL, with their cases."
    - "src/data/message_cache/quick_steps.rs: create, replace, delete, order, read and clear, with their cases; the tables in mod.rs; clear_quick_steps in delete_account."
    - "src/application/filters.rs: FilterEngine::action_from_stored and FilterAction::stored, the one reading and writing of an action's words, from_persisted_rule calling the first."
  key_links:
    - "the step editor (13-41) -> Outcome -> quick_steps::actions_of -> FilterAction::stored -> quick_step_actions rows -> FilterEngine::action_from_stored -> filters::settle -> QuickStep.does: one vocabulary with rules, read by one function."
    - "delete_account -> clear_saved_searches -> clear_quick_steps: an account's steps leave with it."
---

<objective>
Quick Steps as data: what a step is, what stops one being saved, where it
is kept, and the words every later plan says about it.

Purpose: #60: "A Quick Step is a rule with several actions and a name, run
on demand over the selected messages rather than on arrival ... reusing the
existing rules engine rather than a second one." GAP-11's `[D]` line: "A
Quick Step as a rule's actions run by hand over the selection, named, with a
key, on the pattern 11-07's set commands use; one sentence saying what it
did." This plan is the data and the rules; 13-41 builds the manager, and
13-42 puts the steps on the Action menu and runs them through 13-24.1's
runner. 13-23 made a rule's label action real, which a step inherits.

Output: the pure module, the two tables and their store, the one reading of
an action's words shared with rules, the ledger entry naming what is not yet
reachable. Nothing a person sees changes.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-23-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-24.1-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only. Command first, then
what it said. Line numbers are places to start; 13-01 to 13-39 land first,
13-23 among them, which changes how a rule's label is resolved.

**1. A rule's vocabulary, which a step shares.**

```
grep -n 'pub enum FilterAction\|pub fn from_persisted_rule\|pub struct Outcome\|pub fn settle\|pub fn validated_phrase\|pub const SAY_FIRST_LIMIT' src/application/filters.rs   -> :11, :450, :515, :557, :498, :36
Read src/application/filters.rs:450-467   -> the stored words to a FilterAction, one arm per word, the value validated (move_to_folder and add_tag trimmed and not empty, say_first also at most 40 characters); nothing turns a FilterAction back into words
Read src/application/filters.rs:504-581   -> Outcome holds read, starred, move_to, tags, delete, say_first; settle makes one answer from several actions, Delete winning outright and dropping the rest
grep -rn 'quick.step\|quickstep' src tests   -> nothing
```

So a step is an `Outcome`, the one answer several actions settle into, and
the step editor (13-41) edits one field per question; a step cannot hold two
answers to one question, and the only contradiction left to refuse is a
delete beside anything else, which `settle` would otherwise drop in silence.
The stored words are read by one function, `FilterEngine::action_from_stored`,
extracted from `from_persisted_rule`'s arms under green, and written by its
inverse, `FilterAction::stored`, beside it.

**2. The store's patterns.** `saved_searches` and `saved_search_questions`
(`mod.rs:1939-1983`) are the shape: a row per thing, a row per part with a
`position`, the parts cascading on delete, `UNIQUE(account_id, name)` over a
`COLLATE NOCASE` name. `create_saved_search`, `replace_saved_search`,
`delete_saved_search` and `clear_saved_searches`
(`data/message_cache/saved_searches.rs:95`, `:194`, `:306`, `:320`) write in
one transaction and say whether a row was there. 13-37 added the position
column and `put_saved_searches_in_order`. `delete_account` clears an
account's searches at `accounts.rs:307`; the steps go on the next line. A
search a newer version wrote is kept at its place and never run
(`put_back_together`, `saved_searches.rs:650-673`, and 13-37's `order`); a
step whose action words this build cannot read is kept the same way, as
`StoredStep::WrittenByANewerVersion`, in one ordered list.

**3. The words the later plans need from here.** The manager's Key column
(13-41) and the menu (13-42) read `key_for` and `what_the_menu_says` on the
pattern of `tagging.rs:143-207` and 13-38's saved searches;
`choosing_messages::reach_for` (`choosing_messages.rs:160-168`) decides how
far a conversation row reaches per command, and a step's reach is the
narrowest of its commands', so `reach` lives here; `saved_searches::tidied`
(`application/saved_searches.rs:1034`, private) tidies a typed name, and is
made `pub(crate)` for `name_for` here rather than copied.

**4. The gate for each file, at `630e2a67`.** `src/application/quick_steps.rs`
and `src/data/message_cache/quick_steps.rs` new, zero records;
`src/application/filters.rs` 49 tests and 2 records, none added, the
extraction under green; `src/application/saved_searches.rs` 76 and 3 (more
after 13-38 and 13-39), a visibility changed and no test;
`src/data/message_cache/mod.rs` 23 and 12, the tables only;
`src/data/message_cache/accounts.rs` 22 and 2, one line and no test.
Re-taken before the first commit, before and after quoted.

**5. The anchors, by the second reading.** "a rule's phrase said first
survives settling, not dropped on the way to the message" anchors
`settle`'s `SayFirst` arm (`filters.rs:577`), which this plan does not edit;
no record anchors in `filters.rs:445-502`, `accounts.rs:295-312` or
`mod.rs:1930-1990`. Read again before each commit.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation; Pratik may overrule.**
Pratik confirmed the order of phase 13 and said "plan and implement phase 13"
on 2026-09-24.

1. **Quick Steps are per account (RESEARCH-4 question 9)**, because the
   folders and labels a step names are an account's.
2. **The keys (question 1): Ctrl+Shift+7 to Ctrl+Shift+9**, the first three
   steps of the account by place; the rest from the menu.
3. **A step's folder is chosen, not typed, and stored by its path (question
   5).** The choosing is 13-41's; this plan stores the path in the words a
   rule's move is stored in.
4. **The planner's: a step is one `Outcome`, edited as one field per
   question**, rather than a list of actions with contradictions refused
   after the fact. It holds at most one label, because the accessible
   control for one of several is a choice, and a list of check boxes in
   wxWidgets is drawn by the program and does not report its state to a
   screen reader reliably; the pages say a step puts on one label.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: what a step is and what stops one being saved, as rules</name>
  <files>src/application/quick_steps.rs, src/application/mod.rs, src/application/saved_searches.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/filters.rs:1-40` and `:504-581`; `src/application/choosing_messages.rs:140-262`; `src/application/tagging.rs:138-250`; `src/application/saved_searches.rs:955-1060` (`LONGEST_NAME`, `Naming`, `name_for`, `tidied`), and 13-38's `key_for` and `nothing_there` there.
    - `src/application/allowed.rs:330-400` (how an experimental sentence says what could go wrong).
  </read_first>
  <behavior>
    - `QuickStep { id, name, does: filters::Outcome }`; `StoredStep::{Readable(QuickStep), WrittenByANewerVersion { id, name }}`; `actions_of(does) -> Vec<FilterAction>` in a fixed order (read, flag, label, phrase, move, delete), `settle(&actions_of(o)) == o` for every saveable `o`.
    - `LONGEST_NAME = 60` (read before every sentence a step says); `name_for(asked, already_used) -> StepNaming` with `why_not` in Quick Step words ("A Quick Step needs a name. Type one and try again.", "You already have a Quick Step with that name. Pick a different one.", the length named), tidied by `saved_searches::tidied`.
    - `what_stops_a_step_being_saved(does) -> Option<String>`: nothing to do ("This Quick Step does nothing yet. Choose at least one thing for it to do."); a delete beside anything else ("A Quick Step that deletes does nothing else, because a message is not marked, flagged or moved on its way to the trash. Clear Delete it, or leave the rest as they are."); a phrase empty or past `SAY_FIRST_LIMIT`, with its length and the limit, the reader's own bound.
    - `what_it_does_in_words(does) -> String`, for the manager's column: "Mark read, flag, label Work, move to Archive", "Delete", "Say Urgent first"; `reach(does, setting) -> AConversationReaches`: `reach_for(Delete, setting)` for a delete, `ThisFolderOnly` for a move, the whole account otherwise.
    - `WHICH_STEP` ("Choose a Quick Step first. Move Up and Move Down act on the row the cursor is on.") and `moved(steps, which, direction)` over `reordering::moved`, as `tagging::moved` is, for the manager's Move Up and Move Down in 13-41.
    - `REACHABLE_BY_KEY = 3`; `key_for` gives `Ctrl+Shift+7` to `Ctrl+Shift+9`; `what_the_menu_says(names) -> Vec<tagging::MenuLine>`, ampersands doubled; `nothing_there(position, how_many)`: "Ctrl+Shift+9 runs Quick Step 3, and this account has 2.", and for none "... has none yet. Manage Quick Steps, under Quick Steps on the Action menu, makes one."
    - `what_a_step_did(name, done) -> String`: "Archive and read: 3 messages marked read and moved to Archive", `done` being the `WhatWasDone` 13-24.1's runner answers (the runner speaks no sentence), worded through `acting_on_a_set::said(chosen, done)` or the step's own words, never a sentence taken from the runner; `QUICK_STEPS_ARE_EXPERIMENTAL`, second person and plain: no step has run against a real mail server; a step that moves, deletes, marks or flags changes the message at the provider when Allowed Changes lets this program change mail; what a provider does with many changes at once has not been seen; each step says what it did when it finishes.
    - Cases, one per rule and one per refusal, with the round trip over every field alone and together, the keys at the third and past it, and the sentence's singular and plural taken from `acting_on_a_set::said`'s words (the runner itself answers a `WhatWasDone` and says nothing).
  </behavior>
  <action>
    Red first, on a branch: the module's cases by module path
    (`application::quick_steps::tests::<name>`), a new module at zero
    records, with only the signatures they need, answering nothing. Run
    them first and name everything that fails.

    Then the green commit, `saved_searches::tidied` made `pub(crate)` in the
    same commit. Invoke `elegant-code`: each rule a small function over
    `Outcome`; the refusals are sentences a person reads, each saying what
    to change; no `unwrap`.

    Guard record: one on `quick_steps.rs` whose break lets a delete beside
    a move through `what_stops_a_step_being_saved`, its case red; measured.
  </action>
  <verify>
    <automated>cargo test --lib application::quick_steps:: && cargo test --lib application::saved_searches::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the module's cases by module path.
    - `grep -c 'pub mod quick_steps' src/application/mod.rs` is 1; `grep -c 'pub(crate) fn tidied' src/application/saved_searches.rs` is 1; `grep -c 'pub const QUICK_STEPS_ARE_EXPERIMENTAL' src/application/quick_steps.rs` is 1.
    - `cargo test --lib application::quick_steps::` passes with at least 16 tests, quoted; `application::saved_searches::` with the count it had before, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>What a Quick Step is, what stops one being saved, its keys, its words and its reach are rules with cases, in the vocabulary rules already use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the two tables, the store, and one reading of an action's words</name>
  <files>src/data/message_cache/quick_steps.rs, src/data/message_cache/mod.rs, src/data/message_cache/accounts.rs, src/application/filters.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/saved_searches.rs:85-370` and `:640-740`, as 13-37 left them; `src/data/message_cache/mod.rs:1930-1990` and `:1440-1460`; `src/data/message_cache/accounts.rs:295-312`.
    - `src/data/message_cache/saved_searches.rs:1399-1470` (the older-database case).
    - `src/application/filters.rs:445-502`.
  </read_first>
  <behavior>
    - `CREATE TABLE IF NOT EXISTS quick_steps (id TEXT PRIMARY KEY, account_id TEXT NOT NULL, name TEXT NOT NULL COLLATE NOCASE, position INTEGER, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, UNIQUE(account_id, name))` and `CREATE TABLE IF NOT EXISTS quick_step_actions (step_id TEXT NOT NULL, position INTEGER NOT NULL, action_type TEXT NOT NULL, action_value TEXT, PRIMARY KEY (step_id, position), FOREIGN KEY(step_id) REFERENCES quick_steps(id) ON DELETE CASCADE)`, beside the saved searches' tables, each with a comment saying why.
    - `FilterEngine::action_from_stored(action_type, action_value) -> Option<FilterAction>`, the arms `from_persisted_rule` holds today, which it now calls; `FilterAction::stored(&self) -> (&'static str, Option<String>)`, its inverse; the filters module's tests stay green unchanged.
    - `create_quick_step(account_id, step)` (the step and its actions in one transaction, placed last), `replace_quick_step(step) -> bool` (name and actions, the id and the position kept, one transaction, `false` for no such step), `delete_quick_step(id) -> bool`, `put_quick_steps_in_order(account_id, ids)`, `get_quick_steps_for_account(account_id) -> Vec<StoredStep>` in position, then `created_at`, then `id` order, a step with an action row `action_from_stored` refuses read as `WrittenByANewerVersion` at its place; `clear_quick_steps(account_id)`, called by `delete_account` after `clear_saved_searches`.
    - Cases in `data::message_cache::quick_steps::tests`: every field round-trips alone and together; the order written is read back; a new step goes last; two steps in one account may not share a name whatever its case, and two accounts may; a replace keeps the id and the position and leaves no action it dropped; a delete takes the actions; removing an account takes its steps and leaves another's; an unknown action word reads as a newer version's step at its place; a database from before the tables opens and keeps its rules, saved searches and mail, then holds a step.
  </behavior>
  <action>
    Red first, on the same branch: the data module's cases by module path,
    a new module at zero records, with the store's signatures answering
    nothing and the tables not yet made. Run them first and name everything
    that fails.

    Then the green commits: the extraction of `action_from_stored` in
    `filters.rs` first, alone, with the filters module green before and
    after it (a refactor under green, which changes no behaviour, quoted);
    then `FilterAction::stored`, the tables, the store and the account's
    line together, which the red cases demanded, with `cargo build`
    between. Invoke `elegant-code`: one transaction per write,
    errors through `common::Error` with a sentence naming what failed, the
    action words read and written only through the two functions in
    `filters.rs`.

    Guard record: one on `accounts.rs` whose break drops the
    `clear_quick_steps` line, the removal case red; measured, with every
    record the count check flags, in one `--remeasure` call as the first
    run. `filters.rs` stays at its count, quoted.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::quick_steps:: && cargo test --lib application::filters:: && cargo test --lib data::message_cache::saved_searches:: && cargo test --lib data::message_cache::accounts::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the data module's cases by module path.
    - `grep -c 'CREATE TABLE IF NOT EXISTS quick_steps' src/data/message_cache/mod.rs` is 1 and `grep -c 'CREATE TABLE IF NOT EXISTS quick_step_actions' src/data/message_cache/mod.rs` is 1; `grep -c 'self.clear_quick_steps(account_id)' src/data/message_cache/accounts.rs` is 1; `grep -c 'pub fn action_from_stored' src/application/filters.rs` is 1 and `grep -c 'Self::action_from_stored' src/application/filters.rs` is at least 1.
    - `cargo test --lib data::message_cache::quick_steps::` passes with at least 9 tests, quoted; `application::filters::`, `data::message_cache::saved_searches::` and `data::message_cache::accounts::` with the counts they had before, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>Quick Steps are kept per account in two additive tables, read and written in a rule's own words by one pair of functions, and leave with their account.</done>
</task>

<task type="auto">
  <name>Task 3: the ledger entry for what nothing reaches yet, and the four marks</name>
  <files>.planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/WINDOWS.md`'s front matter and its last entry, both halves.
    - `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-42-PLAN.md`, the premise that names this entry.
  </read_first>
  <action>
    Invoke `writing-craft`. Ledger, by hand, both halves: one `stub` entry,
    `file` `src/application/quick_steps.rs`: Quick Steps are stored and
    nothing in the running program makes, shows or runs one; 13-41 gives
    them a manager and 13-42 puts them on the Action menu and runs them, and
    closes this entry. 13-42's plan already names it (its premise 1), so
    the assignment is in the text of the plan that carries it out, as
    CLAUDE.md requires; the entry's number goes in this plan's summary for
    13-42's executor to close.

    No changelog entry: nothing a person sees or hears changes, and the
    summary says so. No push: nothing spoken or shown changes (Pratik's
    answer of 2026-09-23).

    The four marks: phase 13's progress row; this plan's `- [ ]` line in
    phase 13's plan list (ROADMAP.md; the phase README keeps no boxes) ticked;
    `STATE.md`'s plan number in the frontmatter and the body; a dated line
    under GAP-11's `[D]` line saying the steps are stored and named, with
    `application::quick_steps` and `data::message_cache::quick_steps`, and
    that nothing runs them until 13-42. Run
    `cargo test --test the_planning_files_agree_with_themselves` first.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The ledger entry in both halves, its kind `stub`, naming 13-41 and 13-42; `grep -c 'quick_steps.rs' .planning/WINDOWS.md` grew by at least 1.
    - Carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>What is stored and not yet reachable is on the ledger, named in the plan that reaches it, and the four marks are made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stored step to this build | rows another version wrote, read by a build that may not know every word |
| an account's removal to its data | a step's name and folders left behind in a database that is copied and backed up |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-40-01 | Tampering | a step half read and half run because one action word is unknown | medium | mitigate | the whole step is `WrittenByANewerVersion`, kept at its place and never run; its case |
| T-13-40-02 | Tampering | a delete beside a move, with the move dropped in silence by `settle` | medium | mitigate | refused before saving with a sentence; its case and a guard record |
| T-13-40-03 | Information disclosure | a removed account's steps left in the database | medium | mitigate | `clear_quick_steps` in `delete_account`; its case and a guard record |
| T-13-40-04 | Tampering | two readers of an action's words drifting apart | low | mitigate | one reader and one writer in `filters.rs`, used by rules and steps |
| T-13-40-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes nothing spoken or shown: no push and no pull request (Pratik's answer of 2026-09-23); the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What the readings prove: the rules, the store, the one vocabulary, the account's removal. What is not proved and is ledgered: that anything reaches a step, until 13-42.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 11 advances: a Quick Step exists as a named set of a rule's actions, kept per account.
- GAP-11's `[D]` line has a dated line naming what this plan built and what waits for 13-41 and 13-42; the box waits for 13-42; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-11's dated line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-40-SUMMARY.md` when done
</output>
