---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 20
type: execute
wave: 21
depends_on: [13-19]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - src/service/outward.rs
  - src/service/pgp/mod.rs
  - src/service/pgp/keys.rs
  - src/service/pgp/sending.rs
  - src/service/protocols/smtp.rs
  - guards/guards.toml
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: false
requirements: [GAP-05]
estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "rand 0.8, already locked at 0.8.7 through pgp, is named as a direct dependency under a renamed key only after Pratik confirms the line, and Cargo.lock gains no [[package]] entry; the Cargo.toml comment that says cms has only ever published pre-release versions is corrected in the same commit (research, section 4)."
    - "A message can be encrypted with OpenPGP to each recipient's public key and to the sender's own key, SEIPD v1 for the widest reach, and signed with the sender's private key, unlocked with the passphrase 13-17.1 holds; a locked key with no passphrase held answers that it is locked rather than failing (GAP-05, #52 points 4 and 5)."
    - "build_message wraps a PGP-protected message per RFC 3156: multipart/signed with protocol application/pgp-signature and micalg pgp-sha256 over the inner part's exact CRLF bytes, and multipart/encrypted with protocol application/pgp-encrypted, its Version: 1 control part and its application/octet-stream part."
    - "This project's own opener and verifier read what it writes, and GnuPG reads it too, by hand, with the commands and output recorded."
    - "None of this is reached from the composer until 13-21; the changelog and the ledger say so."
  artifacts:
    - "Cargo.toml: the renamed rand 0.8 line with its comment; the corrected cms comment; Cargo.lock: the root package's dependency list only; src/service/outward.rs: the census naming it."
    - "src/service/pgp/sending.rs: encrypt_for, sign_detached, sign_and_encrypt behind the service::pgp boundary."
    - "src/service/protocols/smtp.rs: Protection's PGP arms and their RFC 3156 wrapping."
  key_links:
    - "Email.protection PgpSigned or PgpEncrypted -> build_message -> service::pgp::sign_detached or encrypt_for with the recipients' public keys and the sender's -> the RFC 3156 wrapper through lettre -> send_email; the Sent copy is what was sent, readable because the sender is a recipient."
---

<objective>
Signing and encrypting outgoing mail with OpenPGP: the service half.

Purpose: GAP-05's sending clause, OpenPGP side, #52 points 4 and 5. 13-19
built S/MIME's half; this is rPGP's. rPGP 0.20's message builder needs a
random source whose traits come from `rand` 0.8, and the tree's direct
`rand` is 0.10, whose traits it does not accept, so one renamed manifest
line naming the 0.8 already in the lock is the one package change in this
group, and Pratik confirms it before it lands.

Output: the manifest line and its census entry, the corrected `cms`
comment, the three building functions, the RFC 3156 wrapping, the
changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-19-SUMMARY.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said.

**1. The random source rPGP accepts, and what is locked.**

```
grep -n '^rand = ' Cargo.toml   -> :93 rand = "0.10"
grep -n 'name = "rand"' -A 1 Cargo.lock   -> :4628 0.8.7 and :4639 0.10.2: both locked; 0.8.7 through num-bigint-dig from pgp (research: cargo tree -i rand@0.8.7)
~/.cargo/registry/src/*/pgp-0.20.0/src/composed/message/builder.rs:11, :321   -> the builder's bounds are rand 0.8's CryptoRng + Rng (research)
```

A dependency's features can pull entries a transitive use did not, so task
2 measures `git diff --stat Cargo.lock` after the line: only the root
package's dependency list may change. If a `[[package]]` entry appears,
the line takes `default-features = false` with the smallest feature set
rPGP's builder compiles with, the measurement is quoted, and the new entry
is named for Pratik before anything is committed.

**2. What the manifest line touches in the tree.**

```
grep -n "A_CRATE_THAT_CANNOT: \[&str; \|fn test_every_dependency_has_been_told_apart_from_a_way_out" src/service/outward.rs   -> :1595 (54 names, "rand" among them at :1616) and :1734: a manifest dependency on neither list is refused
sed -n 280,283p Cargo.toml   -> "`cms` has only ever published pre-release versions": crates.io lists stable 0.2.0 to 0.2.3 (2023-03 to 2024-01), measured by the research; the refusal stands on other grounds (no decryption in 0.2.3, no licence text in the published crate, no stable release in 32 months)
grep -rln 'Cargo.toml' tests scripts   -> the readers of the manifest the task greps before the line lands (skill question 7), quoted with what each reads
```

The `cms` correction lives here rather than in 13-14's documents task,
because `scripts/which-checks.sh` answers `all` for a `Cargo.toml` change
beyond the version line, and this plan's manifest commit pays that once for
both.

**3. The builder and the keys.** 13-16 holds private keys in the store's
slots and public keys in the mail database; 13-17.1 holds a typed
passphrase as rPGP's `Password` inside `service::pgp`; 13-18 verifies a
detached signature. The research read the builder's `seipd_v1` at
`builder.rs:315`, `encrypt_to_key` at `:442` and `sign` at `:791`.

**4. The gate for each file**, by the TOML reader on 2026-09-24:
`outward.rs` 38 tests, 11 and 4 (anchors outside the census list; the list
gains a name and its length); `smtp.rs` 40, 4 and 2 plus 13-19's;
`pgp/mod.rs` and `pgp/keys.rs` as 13-16 to 13-18 left them; `Cargo.toml`,
`Cargo.lock` no records and no scoped target. The census's array length in
its type (`[&str; 54]`) changes with the name, which is the one edit the
count check does not see; the census test is what reddens if it is wrong.
</premise_corrections>

<decisions date="2026-09-24">
**1. rand 0.8 as a renamed direct dependency.** The research recommends
it (section 4's verdict: ADD, after Pratik confirms). It is a manifest line
adding no package and it is still his to confirm (the phase brief of
2026-09-24, item b). Task 1 is the checkpoint; the line lands only with his
answer.

**2. SEIPD v1.** The research's choice for the widest reach among
correspondents' programs; SEIPD v2 is ledgered for phase 14 against real
recipients.

**3. Encrypt to the sender too.** The research's pitfall 9, as 13-19.
</decisions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Pratik confirms the rand 0.8 line (stop here only if the brief does not already carry his answer)</name>
  <decision>Whether `rand08 = { package = "rand", version = "0.8" }` may be added to `Cargo.toml` as a direct dependency.</decision>
  <context>If the brief this executor was given carries Pratik's answer to this question (the phase brief's item b, "rand 0.8 as a renamed direct dependency"), record it in the summary with its date and continue to task 2 without stopping. Otherwise stop here and ask him in plain words: "Signing and encrypting PGP mail needs a random number source of an older version than the one Wixen Mail names. That older version is already built into the program, because the PGP library uses it. May I name it directly in the manifest? It adds no new package." The research's verdict: 0 new lock entries, 0 second versions, MIT OR Apache-2.0 as locked (the licence text and maintenance are read again in task 2, research assumption A7).</context>
  <options>
    <option id="add">
      <name>Add the renamed line</name>
      <pros>rPGP's builder gets the random source its bounds name; no new package; 13-21 can send PGP mail.</pros>
      <cons>A second name for `rand` in the manifest, which a reader has to understand; it is a line that exists for one library's sake.</cons>
    </option>
    <option id="hold">
      <name>Hold the line</name>
      <pros>No manifest change.</pros>
      <cons>OpenPGP sending cannot be built; 13-20 makes task 2's commit with the corrected `cms` comment alone (the false sentence goes on either answer), then stops, and 13-21 offers S/MIME only, said in the composer and the changelog.</cons>
    </option>
  </options>
  <resume-signal>Pratik's answer in words, "add" or "hold", with the date; or the brief's own record of it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: the manifest line, its census name, and the corrected cms comment, in one build commit</name>
  <files>Cargo.toml, Cargo.lock, src/service/outward.rs</files>
  <read_first>
    - `Cargo.toml:85-95` and `:270-330` (the `rand` line, the `ring` and `cms` comment, the `pgp` block).
    - `src/service/outward.rs:1590-1740` (the two lists and the census test).
    - The readers of `Cargo.toml` under `tests/` and `scripts/` found by `grep -rln 'Cargo.toml' tests scripts`.
  </read_first>
  <action>
    The manifest line and its census name only after task 1's answer is
    "add". On "hold", this task's one `build(13-20)` commit carries the
    corrected `cms` comment alone, with the same acceptance lines except
    the two about the `rand` line and `Cargo.lock`, and the plan stops
    after it; the `cms` fix moved here from 13-14 (premise 2), so no other
    plan would make it. Added 2026-09-24 when the plans were integrated.
    On "add": invoke `dependency-audit`: read the
    locked `rand` 0.8.7's licence file from the vendored crate and its
    repository's last release, and quote both, closing the research's
    assumption A7.

    One `build(13-20)` commit, green, never red (`which-checks.sh` asks about
    a red before a manifest, so a red here would be judged by a scoped
    run): the line `rand08 = { package = "rand", version = "0.8" }` beside
    `pgp` with a comment saying it exists for rPGP's builder bounds and
    names the version already locked; `rand08` or `rand` added to
    `A_CRATE_THAT_CANNOT` as the census test reads the manifest, with the
    array's length; the `cms` comment at `:280-283` rewritten to say `cms`
    0.2.0 to 0.2.3 are stable and the refusal stands on the three grounds
    in premise 2. Measure `git diff --stat Cargo.lock`: the root package's
    list only, quoted. The commit answers `all` and pays the whole gate,
    `cargo audit` among it.
  </action>
  <verify>
    <automated>cargo build && cargo test --lib service::outward:: && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'package = "rand", version = "0.8"' Cargo.toml` is 1; `git diff --stat HEAD~1 -- Cargo.lock` shows one file changed with no `[[package]]` added (`git diff HEAD~1 -- Cargo.lock | grep -c '^+\[\[package\]\]'` is 0).
    - `tr -s '\n#' '  ' < Cargo.toml | tr -s ' ' | grep -c 'only ever published pre-release versions'` is 0 (it answered 1 on 2026-09-24).
    - The commit's hook answered `all` and passed; its mode line quoted.
  </acceptance_criteria>
  <done>The random source rPGP needs is named, nothing new is compiled in, and the manifest no longer says something false about `cms`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sign, encrypt, and both, behind the service::pgp boundary, read back by this project and by GnuPG</name>
  <files>src/service/pgp/sending.rs, src/service/pgp/mod.rs, src/service/pgp/keys.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/pgp/mod.rs`, `keys.rs` and `signatures.rs` as 13-16 to 13-18 left them.
    - `~/.cargo/registry/src/*/pgp-0.20.0/src/composed/message/builder.rs` around `:315`, `:442`, `:791`, quoted.
  </read_first>
  <behavior>
    - `encrypt_for(content: &[u8], recipients: &[String], sender: &Fingerprint) -> Sending`: SEIPD v1, one encryption to each recipient's public armour and one to the sender's own key, armoured; `sign_detached(content, sender) -> Sending`: a detached signature by the sender's private key over the exact bytes, armoured, hash SHA-256; `sign_and_encrypt(content, recipients, sender) -> Sending`: signs inside and encrypts the result.
    - `Sending { Built(String), TheKeyIsLocked { whose }, NoPrivateKey, ARecipientHasNoKey { address } }`: a locked sender key with no held passphrase answers `TheKeyIsLocked`, never a crate error; the held `Password` from 13-17.1 unlocks it otherwise.
    - The random source is `rand08`'s `OsRng` or thread generator as the builder's bounds take it, in one place.
    - Cases: a message encrypted for Alice opens with `open_a_message` and Alice's key to the same bytes; Bob's key alone does not open it; the sender's key opens it too; a detached signature by Alice over a CRLF part holds under 13-18's `verify_detached` with her public key; a signed-and-encrypted message opens and its inner signature holds; the locked fixture with nothing held answers `TheKeyIsLocked`. `test_no_caller_outside_this_module_names_the_crate` still passes.
    - Outside the tests, once, recorded in the summary: the encrypted output decrypted with `gpg --decrypt` and the signature checked with `gpg --verify`, with a short `GNUPGHOME` holding Alice's key, commands and output quoted.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path. Then the module, with
    `cargo build` between. Invoke `elegant-code`: no crate type crosses
    `service::pgp`'s boundary; armour in, armour out.

    Guard records, measured: one whose break leaves the sender off the
    recipients, the sender-opens case red; one whose break signs the LF form
    of the content, the detached case red.
  </action>
  <verify>
    <automated>cargo test --lib service::pgp::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path.
    - `grep -rln 'use pgp::\|pgp::composed' src --include=*.rs` lists only files under `src/service/pgp/`.
    - The run passes, count quoted; the GnuPG commands and output are in the summary.
  </acceptance_criteria>
  <done>OpenPGP mail can be signed, encrypted to its recipients and its sender, or both, and GnuPG reads what this writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: RFC 3156 wrapping in build_message, the changelog and the ledger</name>
  <files>src/service/protocols/smtp.rs, guards/guards.toml, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `src/service/protocols/smtp.rs` as 13-19 left it (`Protection`, the body part, the S/MIME arms).
    - `~/.cargo/registry/src/*/lettre-0.11.22/src/message/mimebody.rs:180-184, :333-342`.
  </read_first>
  <behavior>
    - `Protection` gains `PgpSigned { sender }`, `PgpEncrypted { recipients, sender }`, `PgpSignedAndEncrypted { recipients, sender }`.
    - `PgpSigned`: `multipart/signed; micalg=pgp-sha256; protocol="application/pgp-signature"` holding the part and `signature.asc`, the signature over the part's CRLF formatted bytes; `PgpEncrypted`: `multipart/encrypted; protocol="application/pgp-encrypted"` holding the `application/pgp-encrypted` part with `Version: 1` and the `application/octet-stream` part named `encrypted.asc`; signed and encrypted: the signed entity encrypted.
    - A `Sending` other than `Built` stops `build_message` with an error whose sentence names the reason (the key is locked, no private key, which recipient has no key), so nothing is sent unprotected.
    - Cases: a PGP-signed `Email` formatted, split at its boundary, and checked with `verify_detached` holds; a PGP-encrypted one is marked by 13-15's `claims_pgp_encryption` and opens through 13-15's path to the body text; a missing recipient key stops the build with the address named; a plain `Email` is still byte for byte what 13-19's case holds.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path and the count check.
    Then the arms, with `cargo build` between.

    Guard record, measured: one whose break sends the body unwrapped when
    `Sending` is not `Built`, the missing-key case red.

    Then the documents, in the one documents commit: the changelog under
    `[Unreleased]`, Known limitations: OpenPGP signing and encrypting is
    built beneath the composer and not offered yet; it arrives with the
    Sign and Encrypt boxes (13-21); SEIPD v1 for reach. Ledger, by hand,
    both halves: one `stub` entry, "OpenPGP signing and encrypting is
    reached only by tests until 13-21 wires the composer", naming 13-21,
    whose `<premise_corrections>` already names it; one `unrun-verify` for
    phase 14: this program's PGP mail opened in Thunderbird and Proton, and
    whether SEIPD v2 should be offered.
  </action>
  <verify>
    <automated>cargo test --lib service::protocols::smtp:: && cargo test --lib service::pgp:: && cargo test --test house_style && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c 'application/pgp-encrypted' src/service/protocols/smtp.rs` is at least 1.
    - The runs pass, counts quoted; two ledger entries in both halves; no em dash or en dash; none of the six words; carriage returns 0.
  </acceptance_criteria>
  <done>Every send can carry OpenPGP protection per RFC 3156, nothing goes out unprotected when a key is missing, and the ledger names the plan that makes it reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a new manifest line to the build | a dependency named directly |
| the person's private key to a signature | a signature made in their name |
| a correspondent's public key to an encrypted message | encrypting to a key someone supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-20-SC | Tampering | the rand 0.8 manifest line | high | mitigate | Pratik's confirmation at task 1; legitimacy answered OK by the research's gsd-tools check; the lock diff measured to add no package; the licence read from the vendored crate |
| T-13-20-01 | Information disclosure | a message sent unprotected when protection was asked | high | mitigate | any `Sending` other than `Built` stops the build with a named reason; a guard record |
| T-13-20-02 | Tampering | a signature over bytes other than those sent | high | mitigate | the CRLF formatted part signed; a guard record on the LF form |
| T-13-20-03 | Information disclosure | the passphrase or key in an error | high | mitigate | outcomes are four words chosen here; nothing from the crate crosses out |
| T-13-20-04 | Spoofing | encrypting to a public key that is not the correspondent's | medium | accept | a public key is its own claim; the manager says so; phase 14 tests real keys |
</threat_model>

<verification>
- The commands in the three `<automated>` blocks pass. The `build(13-20)` commit answers `all` through the hook and pays the whole gate once; every other commit through the hook, never piped; no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the changelog, the ledger, the summary and the four completion marks in one documents commit before the merge; `git merge --no-ff`, which answers `all` because the branch's diff holds the manifest.
- Spoken or shown: no. The branch merges without a pull request (Pratik's answer of 2026-09-23).
- Waits on Pratik: task 1, unless the brief carries his answer.
</verification>

<success_criteria>
- GAP-05's sending clause has its OpenPGP half on named tests; the box waits for 13-21.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-05's traceability row naming this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-20-SUMMARY.md` when done
</output>
