---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 17
type: execute
wave: 17
depends_on: [13-16]
files_modified:
  - src/presentation/wx_pgp_keys.rs
  - src/presentation/mod.rs
  - src/presentation/wx_app.rs
  - src/presentation/wx_reader.rs
  - src/presentation/scan_target.rs
  - src/application/pgp_keys.rs
  - src/application/allowed.rs
  - .github/workflows/accessibility.yml
  - tests/the_key_manager_lists_and_names_its_controls.rs
  - tests/wired.rs
  - guards/guards.toml
  - .cargo/audit.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/privacy.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-03]
estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "File, PGP &Keys... (experimental) opens the PGP key manager; it replaces File, Import PGP Private Key at the same place and keeps its letter, K, per Pratik's menu decision of 2026-09-24 (GAP-03, #49)."
    - "The manager says first, where a person reads it, what keys can and cannot do in this build, then lists every key here, private and public, one row per key read as a person before a fingerprint: name and address, kind, key id, fingerprint, created, expires, what it can do."
    - "It imports from a file or from pasted text, one key or many, private or public; exports a key's public half to a file or copies it to the clipboard; and removes a key only after a confirmation that names the key and says what will stop working; every answer is shown on a line and said."
    - "Every control is named on both channels at its own handle and every letter is distinct in the window: What these keys can do &here, &Keys, Import from &File, &Paste a Key, E&xport Public Key, &Copy Public Key, &Remove, Cl&ose."
    - "Enter on an attachment that is a PGP key, in either reader window, says whose key it is and asks whether to import it."
    - "READING_PGP_MAIL_IS_EXPERIMENTAL no longer says one key at a time; .cargo/audit.toml's narrowing fact names the manager; GAP-03's [D] line says File rather than Tools, with Pratik's decision and its date."
  artifacts:
    - "src/presentation/wx_pgp_keys.rs: the dialog on Blocked Senders' pattern, its label list with a distinct-letters case."
    - "src/application/pgp_keys.rs: WHAT_KEYS_CAN_DO_HERE and the removal confirmation sentences, true for this plan's build."
    - "src/presentation/wx_app.rs: the File menu item, the handler, the key attachment's import question, ScanTarget::PgpKeys; the old import_a_pgp_private_key gone."
    - "tests/the_key_manager_lists_and_names_its_controls.rs: the built dialog with two keys, read over MSAA at each control's handle; tests/wired.rs's two PGP menu tests rewritten for the manager."
  key_links:
    - "File, PGP Keys -> wx_pgp_keys::show -> application::pgp_keys (every_key_here, import, remove, export_public) -> service::pgp and data::message_cache::pgp_keys: the manager holds no key logic of its own."
    - "a key attachment's Enter -> the reader's read handler -> application::pgp_keys::import after a yes: the same import as the manager's."
---

<objective>
The PGP key manager, where the File menu's key import was.

Purpose: GAP-03 and #49, the tester on 2026-09-15: "While there is an
import PGP key option, there is no way to manage these keys." The issue asks
for a list with user ids, key id, fingerprint, creation and expiry, private
or public; import from a file, an attachment or pasted text; removal "with
a confirmation that says what will stop opening"; export of a public key to
a file or the clipboard; and a plain statement "where a person will read it,
what this build can and cannot do with a key". 13-16 built everything the
manager calls. Pratik settled its place on 2026-09-24: it replaces File,
Import PGP Private Key, taking its place and its letter.

Output: the dialog, the menu item, the attachment's import question, the
scan target, the rewritten sentences and records, the pages, the
changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-16-SUMMARY.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said. 13-03 adds File, Print on P before this plan runs; re-take the File
menu's letters at the start of task 2 and quote them.

**1. Where the manager goes, which corrects the research and the
requirement.** The research placed it on Tools as "PGP Ke&ys..." and
GAP-03's `[D]` line says "The manager under Tools"
(`.planning/REQUIREMENTS.md:5794`). Pratik decided on 2026-09-24 that it
replaces File, Import PGP Private Key and takes its place and its letter if
that letter reads well in the new label. It does:

```
awk 'NR>=6661 && NR<=6724' src/presentation/wx_app.rs | grep -o '"[^"]*&[A-Za-z][^"]*"'   -> &Save, Save &As, Check &Mail, Open &Draft, &Import Mailbox, Import a F&older, &Export Mailbox, Import PGP Private &Key, &Quit, with the New submenu prepended as &New: N S A M D I O E K Q; free: B C F G H J L P R T U V W X Y Z (13-03 takes P; 13-45 and 13-46 are to claim F and X later)
sed -n 6718,6722p src/presentation/wx_app.rs   -> .append_item(ID_IMPORT_PGP_KEY, "Import PGP Private &Key... (experimental)", crate::application::allowed::READING_PGP_MAIL_IS_EXPERIMENTAL)
```

"PGP &Keys... (experimental)" keeps K, which reads well; no letter changes
on the File menu. The requirement's line is corrected in task 3 with the
decision and its date.

**2. What the old import does and what depends on it.**

```
grep -n "ID_IMPORT_PGP_KEY\|fn import_a_pgp_private_key" src/presentation/wx_app.rs   -> :147 the id, :5176 the handler, :6719 the item, :14366 the function (announces only, nothing on a line: ledger 496)
grep -n "fn test_importing_a_pgp_private_key_is_on_a_menu_and_reaches_the_importer\|fn test_the_menu_says_reading_pgp_mail_is_experimental_before_it_is_chosen" tests/wired.rs   -> :4140 and :4174
python over guards.toml   -> "a PGP private key somebody can import", file wx_app.rs, suite wired, before = the four-line append_item at :6718-6722, red = test_every_handled_command_has_something_that_raises_it and the two tests above
sed -n 399,404p src/application/allowed.rs   -> READING_PGP_MAIL_IS_EXPERIMENTAL: "... Wixen Mail holds one key at a time, and it has to be exported without a passphrase ..."
sed -n 131,136p .cargo/audit.toml   -> "PGP private key import is labelled experimental in the File menu, so very few machines hold a key" (after 13-14's rewrite of the sentence before it)
```

The record's anchor is the menu item this plan replaces, so it is
**removed** by the edit: task 2 rewrites the record's `before` to the new
item, renames its red list to the two rewritten tests, and re-measures it
in the same commit. The two tests are renamed, so
`test_every_test_a_guard_record_names_is_a_test_that_exists` names the
record at once, which is the prompt to re-measure rather than edit.

**3. The pattern and the scan.**

```
sed -n 170,195p src/presentation/wx_blocked_senders.rs   -> a heading StaticText named with what the list holds, a report ListCtrl named with the title, a status StaticText named "What happened", then the buttons
grep -n "ScanTarget::BlockedSenders" src/presentation/scan_target.rs src/presentation/wx_app.rs   -> :95, :239, :284 ("blocked-senders"), wx_app.rs:17677
grep -n "fn test_the_workflow_asks_for_every_target" src/presentation/scan_target.rs   -> :406: a new target not in .github/workflows/accessibility.yml's list (:177) is refused, so the workflow gains 'pgp-keys' in the same commit
sed -n 1178,1188p docs/KEYBOARD_SHORTCUTS.md   -> "Blocked Senders Dialog Accelerators", the section the new one sits beside
sed -n 199,205p src/presentation/wx_reader.rs   -> can_be_read_here: a PDF, text, a picture; a key is not
```

A change under `.github/workflows/` answers `all` in `scripts/which-checks.sh`
for the commit that makes it, so that commit pays the whole gate; it is
the task 2 commit that adds the target, and nothing else rides on it.

**4. Letters inside the dialog.** H, K, F, P, X, C, R, O: eight distinct,
from the research's allocation (section 6), checked here against each
other. The Paste a Key dialog: "&Key text:" (K), OK and Cancel. The
confirmation is a native message box with Yes and No.

**5. The gate for each file**, by the TOML reader on 2026-09-24:
`wx_app.rs` 199 tests, 119 and 112; `wx_reader.rs` 14, 2 and 1;
`scan_target.rs` 11, 4 and 1 (the anchor at `:309` is the editor-name
arm, not the list the target joins); `allowed.rs` 27, 5 and 5 (anchors at
`:87-88`, `:137-138`, `:258-260`, `:304-306`, `:375-376`, none in the
constant at `:399-404`); `tests/wired.rs` 77 tests, 19 and 2; the dialog
and the target new. `wx_app.rs:14118-14119`, "a PGP message the reader
window never offers to the key", lies near the reader's open path and is
not edited; named in the summary.
</premise_corrections>

<decisions date="2026-09-24">
**1. The menu place and letter: File, PGP &Keys... (experimental), where
Import PGP Private Key was, on K.** Pratik, 2026-09-24. The research's Tools
placement and Y are not used, which also leaves Tools' one free letter to
nobody in this group.

**2. The File menu's Import PGP Private Key goes.** Taken 2026-09-24 from
the research's recommendation (question 8); Pratik may overrule: two doors
to one thing is one more item to arrow past. The manager holds import.

**3. Keys locked with a passphrase.** Taken 2026-09-24 from the research's
recommendation (question 2, option b); Pratik may overrule. 13-17.1 builds
it; this plan's limits sentence says a locked key cannot be imported yet,
and 13-17.1 rewrites that clause.

**4. The limits sentence is true for the build it ships in.** Each later
plan that changes what a key can do here (13-17.1, 13-18, 13-21) rewrites
`WHAT_KEYS_CAN_DO_HERE`, and a case pins each clause to the code that makes
it true. The plan's own rule, from CLAUDE.md's "No stubs presented as
complete".
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the manager dialog, its sentences, and its controls named on both channels</name>
  <files>src/presentation/wx_pgp_keys.rs, src/presentation/mod.rs, src/application/pgp_keys.rs, tests/the_key_manager_lists_and_names_its_controls.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_blocked_senders.rs` whole (the dialog, `said_and_shown`, focus on open, its tests).
    - `src/application/pgp_keys.rs` and `src/service/pgp/mod.rs`'s `KeyListing` (13-16).
    - `tests/the_feedback_dialog_shows_what_it_sends_before_it_goes.rs:1-40` and `:140-240` (the MSAA reading at a control's own handle, in Tab order).
  </read_first>
  <behavior>
    - `application::pgp_keys::WHAT_KEYS_CAN_DO_HERE`: "A private key here opens PGP messages sent to it. A key locked with a passphrase cannot be imported yet. Public keys are kept here; nothing uses them yet. Keys are kept in the Windows credential store, and removing one here removes it from this computer." With a case per clause pinned to the code (a locked key is refused by `import`; nothing outside `pgp_keys` reads the public table yet).
    - `removal_question(listing) -> String`: for a private key, "Remove the private key for Ada Lovelace <ada@example.com>, 1A2B 3C4D ...? Messages encrypted to it will no longer open here, and it cannot be brought back unless you import it again."; for a public key, "Remove the public key for Grace Hopper <grace@example.com>, ...? It will no longer be kept here."
    - `wx_pgp_keys::show(parent, cache, a11y)`: "What these keys can do &here:" and a read-only multiline text holding the sentence, named by its label; "&Keys:" and a report `ListCtrl` with columns Name and address, Kind, Key id, Fingerprint, Created, Expires, Can, one row per key from `every_key_here`, first column the first user id; "What happened" status line; buttons "Import from &File...", "&Paste a Key...", "E&xport Public Key...", "&Copy Public Key", "&Remove...", "Cl&ose". Focus on open: the list's first row when there is one, else Import from File. Every answer through the one call that shows it on the line and announces it. Remove asks `removal_question` with Yes and No, No the default. Paste a Key opens a dialog with "&Key text:" (multiline, paste allowed) and OK and Cancel. No control is ever disabled: a button pressed with nothing chosen says "Choose a key in the list first."
    - `wx_pgp_keys::LABELS`: every label in the window, and a case that no two claim the same letter, with a companion that plants a duplicate and is refused.
    - The target: the dialog built with Alice's private and Bob's public key imported in a temp home, read in Tab order: the sentence's text named "What these keys can do here", the list named "Keys" with two rows whose first column is the user id, six buttons each named by its label, all read over MSAA at their own handles; Remove pressed on Alice with the question answered No leaves two rows, answered Yes leaves one and the status line says so; Copy Public Key puts armour on the clipboard that `describe` reads as Alice's public key.
  </behavior>
  <action>
    Red first, on a branch: the application cases by module path, the
    dialog's letter case by module path, the target's tests bare, and the
    count check if a named file gains a test. Then the sentences and the
    dialog, with `cargo build` between. Invoke `elegant-code`: the dialog
    calls `application::pgp_keys` and nothing else; the question and the
    sentence are functions, not strings in the window.

    Guard records, measured: one on `wx_pgp_keys.rs` whose break makes
    Remove act without asking, the target's No reading red; one on
    `application/pgp_keys.rs` whose break says a locked key can be
    imported, its pinning case red.
  </action>
  <verify>
    <automated>cargo test --lib application::pgp_keys:: && cargo test --lib presentation::wx_pgp_keys:: && cargo test --test the_key_manager_lists_and_names_its_controls</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the target's tests bare.
    - `grep -c 'set_enabled(false)\|enable(false)\|Enable(false)' src/presentation/wx_pgp_keys.rs` is 0.
    - The target passes with at least 6 tests, quoted; the two module runs pass, counts quoted.
  </acceptance_criteria>
  <done>The manager lists every key, imports, exports, copies and removes with a question that says what stops, says its limits first, and every control answers on both channels at its own handle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the File menu item, the old import retired, the key attachment's question, and the scan</name>
  <files>src/presentation/wx_app.rs, src/presentation/wx_reader.rs, src/presentation/scan_target.rs, src/application/allowed.rs, .github/workflows/accessibility.yml, tests/wired.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:140-150` (the id), `:5170-5185` (the handler), `:6700-6725` (the File menu), `:14350-14420` (`import_a_pgp_private_key`), `:17670-17690` (the Blocked Senders scan arm), and the reader's read-attachment handler (`grep -n 'on_read_attachment\|read_attachment_now' src/presentation/wx_app.rs`).
    - `tests/wired.rs:4130-4200` (the two PGP menu tests) and `:1960-2000` (the letter check).
    - `src/presentation/wx_reader.rs:195-240` (`can_be_read_here`, `describe_for_refusal`).
    - The guard record "a PGP private key somebody can import" in `guards/guards.toml`.
  </read_first>
  <behavior>
    - The File menu item becomes `ID_PGP_KEYS`, "PGP &Keys... (experimental)", with `READING_PGP_MAIL_IS_EXPERIMENTAL` as its description, at the same place; the handler opens `wx_pgp_keys::show` with the message cache; `import_a_pgp_private_key` and `ID_IMPORT_PGP_KEY` are gone.
    - `READING_PGP_MAIL_IS_EXPERIMENTAL` rewritten: reading PGP mail is experimental, no message from a real correspondent has been through it and no real key imported; keys are managed from File, PGP Keys; a key locked with a passphrase cannot be imported yet. Its existing cases kept, one added that it no longer says "one key at a time".
    - `tests/wired.rs`: `test_the_pgp_key_manager_is_on_the_file_menu_and_reaches_the_manager` and `test_the_menu_says_the_pgp_key_manager_is_experimental_before_it_is_chosen` replace the two old tests; the letter check passes with K on the new label.
    - A key attachment (`application/pgp-keys`, or a `.asc` or `.key` file whose text holds a PGP key block) can be read in both reader windows: Enter on it says whose key it is and its kind, and asks "Import this key?" with Yes and No; Yes imports through `application::pgp_keys::import` and says the outcome; nothing about the key's bytes is said.
    - `ScanTarget::PgpKeys`, named "pgp-keys", opening the manager on a fresh profile, and `'pgp-keys'` in `.github/workflows/accessibility.yml`'s target list.
  </behavior>
  <action>
    Red first, on a branch: the two rewritten `wired` tests bare, the
    `allowed` case and the reader's key-attachment case by module path,
    `scan_target`'s workflow test bare if it lives in the library
    (`presentation::scan_target::tests::test_the_workflow_asks_for_every_target`),
    and the count check. Then the menu, the handler, the removal of the old
    function, the attachment question and the scan target, with `cargo
    build` between.

    In the same commit as the menu edit: the record "a PGP private key
    somebody can import" gets the new four-line item as `before`, its `after`
    unchanged in kind (the item removed), its red list the two new test
    names and `test_every_handled_command_has_something_that_raises_it`,
    and is re-measured. `scripts/guards.sh --remeasure` once for every record
    the count check flags. Ledger 496 is closed by the manager's status line
    and named so in task 3.

    Guard record, measured: one on `wx_reader.rs` whose break stops a key
    attachment being readable, the reader's case red.
  </action>
  <verify>
    <automated>cargo test --test wired && cargo test --lib application::allowed:: && cargo test --lib presentation::wx_reader:: && cargo test --lib presentation::scan_target:: && cargo test --test house_style test_every_test_a_guard_record_names_is_a_test_that_exists</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the rewritten tests bare and the module cases by path.
    - `grep -c 'Import PGP Private' src/presentation/wx_app.rs` is 0; `grep -c 'PGP &Keys... (experimental)' src/presentation/wx_app.rs` is 1; `grep -c "'pgp-keys'" .github/workflows/accessibility.yml` is 1.
    - `sed -n '/pub const READING_PGP_MAIL_IS_EXPERIMENTAL/,/;$/p' src/application/allowed.rs | grep -c 'one key at a time'` is 0 (it answered 1 on 2026-09-24; the whole file answers 3, because the doc comment at `:397` and the case at `:732` name the phrase too, so the check is scoped to the constant); the case at `:732` asserts the phrase's absence and the comment at `:397` is rewritten.
    - The five runs pass, counts quoted; the rewritten record measured and quoted.
  </acceptance_criteria>
  <done>File, PGP Keys opens the manager where the import was, on the same letter, a key sent as an attachment can be imported from either reader, and the accessibility workflow scans the new window.</done>
</task>

<task type="auto">
  <name>Task 3: the advisory entry, the pages, the requirement's line, the changelog and the ledger</name>
  <files>.cargo/audit.toml, docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/privacy.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.cargo/audit.toml:131-136` after 13-14 and 13-15.
    - `docs/KEYBOARD_SHORTCUTS.md:582-625` (File Menu, the row at `:618`) and `:1178-1188`.
    - `.planning/REQUIREMENTS.md:5789-5797` (GAP-03) and rows 146 and 496 in `.planning/WINDOWS.md`.
  </read_first>
  <action>
    Invoke `writing-craft`. `.cargo/audit.toml`: the narrowing fact says
    the PGP key manager on the File menu is labelled experimental, so few
    machines hold a key; `bash scripts/audit.test.sh` after. The shortcuts
    page: the File Menu row becomes "PGP Keys", no shortcut, opens the key
    manager, experimental; a "PGP Keys Dialog Accelerators" section beside
    Blocked Senders' with the eight letters and what each reaches; the
    reader's Enter on a key attachment. The guide: a `### PGP keys`
    subsection under Signed and encrypted mail: the manager, what the
    limits sentence means, import, export, remove, and a key sent as an
    attachment. The privacy page: where the manager's keys live, from
    13-16. `docs/ALPHA_TESTING.md`: the manager is experimental. The
    changelog under `[Unreleased]`, Added, #49, and Changed: File, Import PGP
    Private Key is now File, PGP Keys.

    `.planning/REQUIREMENTS.md`: GAP-03's `[D]` line reads "The manager on
    the File menu, where Import PGP Private Key was (Pratik, 2026-09-24;
    until then this line said under Tools)", the rest of the line as it is.

    Ledger, by hand, both halves: 496 fixed (the manager's line shows and
    says each answer); 146 updated (the import's answers are now the
    manager's); one `unrun-verify` for the manager under NVDA: the list's
    rows read as people, the question read in full, the letters.
  </action>
  <verify>
    <automated>bash scripts/audit.test.sh && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test wired test_the_shortcuts_document_and_the_menus_agree && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### PGP Keys Dialog Accelerators' docs/KEYBOARD_SHORTCUTS.md` is 1; `grep -c 'Import PGP Private Key |' docs/KEYBOARD_SHORTCUTS.md` is 0.
    - `sed -n '/\*\*GAP-03\*\*/,/^- \[/p' .planning/REQUIREMENTS.md | grep -c 'under Tools; the limits'` is 0.
    - Ledger in both halves; no em dash or en dash; none of the six words; carriage returns 0.
  </acceptance_criteria>
  <done>The pages, the advisory entry and the requirement say where the manager is and what it does, and the ledger closes the import's missing line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a key file, pasted text or a stranger's attachment to the store | private keys enter the credential store, public keys the database |
| the clipboard and an exported file | a public key leaves the program |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-17-01 | Information disclosure | a private key exported or copied | high | mitigate | Export and Copy take the public half only, through `public_half`; a case asserts the clipboard armour is a public key |
| T-13-17-02 | Tampering | a stranger's attachment imported without asking | medium | mitigate | Enter says whose key it claims to be and asks; No imports nothing; nothing imports on open |
| T-13-17-03 | Spoofing | a public key claiming a correspondent's address | medium | accept | the limits sentence and the guide say a key's name is its own claim; nothing here verifies ownership; 13-18's sentences never call a key trusted |
| T-13-17-04 | Repudiation | a key removed by a stray press | medium | mitigate | a question naming the key with No as the default |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass. `scripts/check.sh` through the hook on every commit, never piped (the workflow commit answers `all` and pays the whole gate); no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the pages, the advisory entry, the requirement's line, the ledger, the summary and the four completion marks in one documents commit before the merge; `git merge --no-ff`.
- Spoken or shown: yes. The branch is pushed and a pull request opened after its last commit, and `ci.yml`, `nvda.yml` and `accessibility.yml` are read green before the merge; the accessibility run scans `pgp-keys` for the first time, and its Axe.Windows and `msaa-names.ps1` results are quoted in the summary; `main` is not pushed.
- Mnemonics: the File menu's letters re-taken after 13-03 and quoted; the dialog's eight checked by its own case.
</verification>

<success_criteria>
- GAP-03's lists, imports a public key, removes and exports one, and says its limits where a person reads it, each on a named test; the box waits for 13-17.1.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-03's `[D]` line corrected and its traceability row naming this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-17-SUMMARY.md` when done
</output>
