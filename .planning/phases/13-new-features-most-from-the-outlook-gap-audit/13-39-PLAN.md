---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 39
type: execute
wave: 41
depends_on: [13-38]
files_modified:
  - src/application/saved_searches.rs
  - src/presentation/wx_managers.rs
  - src/presentation/wx_new_saved_search.rs
  - src/presentation/mod.rs
  - src/presentation/wx_app.rs
  - src/presentation/scan_target.rs
  - src/presentation/scan_fixtures.rs
  - .github/workflows/accessibility.yml
  - tests/a_saved_search_can_be_made_from_nothing.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-09]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A saved search can be made from nothing: Save This Search with nothing searched, and New Saved Search on the Saved Searches submenu, open a window asking a name and where to look, then the conditions window, and nothing is written until that window closes with at least one condition (GAP-09's third [D] clause, #58 point 3)."
    - "The conditions window asks whether a message has to meet every condition or any of them, for a search made from nothing and for one opened with Edit Conditions alike, and Edit Conditions writes the answer back with the questions, so an \"any\" search can become \"every\" (RESEARCH-4 question 3)."
    - "One wording refuses a search that asks nothing, held in application::saved_searches and read by the window, the write-back and the store's caller."
    - "Both windows are named on the channel NVDA reads at the handle focus reaches, read over MSAA on the built windows, and both are scan targets the Accessibility workflow walks."
    - "GAP-09 is closed on the tree side: its four [D] clauses each have a named test or reading across 13-37, 13-38 and this plan, and its box and traceability row are ticked with the date; #58 is closed from the merge."
  artifacts:
    - "src/application/saved_searches.rs: ASKS_NOTHING, EditedConditions, a_search_from_nothing, where_a_search_can_look, with their cases."
    - "src/presentation/wx_managers.rs: build_conditions_window and show_rule_manager_dialog asking the join and returning EditedConditions; what_a_condition_list_still_needs answering ASKS_NOTHING."
    - "src/presentation/wx_new_saved_search.rs: build_new_saved_search_dialog and ask, the name and the place."
    - "src/presentation/wx_app.rs: make_a_saved_search_from_nothing, reached from Save This Search with nothing searched and from New Saved Search; the_search_to_write_back taking the join; two scan target arms."
    - "tests/a_saved_search_can_be_made_from_nothing.rs: both windows built and read over MSAA, the places offered, and source readings of both doors, each with a planted-fault companion."
  key_links:
    - "Save This Search with nothing run, or New Saved Search -> make_a_saved_search_from_nothing -> wx_new_saved_search::ask -> show_rule_manager_dialog with no questions -> saved_searches::a_search_from_nothing -> create_saved_search -> read_the_tree_back -> saved_searches::created said."
    - "Edit Conditions -> show_rule_manager_dialog with the search's join -> EditedConditions -> the_search_to_write_back -> replace_saved_search: the join written with the questions, one transaction."
---

<objective>
A saved search made from nothing, and its every-or-any answer changeable
afterwards.

Purpose: #58 point 3: "A search cannot be saved from scratch: Save This Search
refuses unless a search has just run; Edit Conditions edits only one that
exists." GAP-09's third `[D]` clause: "Save as Search from an empty box."
RESEARCH-4 found the join of a search cannot be changed anywhere once made
(`the_search_to_write_back` carries it over, `wx_app.rs:8395-8414`), and the
decision of 2026-09-24 gives the choice to both doors, so a search made as
"any" can become "every". This plan closes GAP-09, whose first two clauses
13-37 and 13-38 closed and whose drifted pages they dated.

Output: the rules as pure functions, the join in the conditions window and
its write-back, the New Saved Search window and its two doors, two scan
targets, the reading, the pages, the changelog, the ledger, GAP-09 ticked,
#58 closed from the merge.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-38-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only. Command first, then
what it said. Line numbers are places to start; 13-01 to 13-38 land first.

**1. The refusal, the name dialog and the write-back.**

```
Read src/presentation/wx_app.rs:8244-8345 (save_this_search)   -> with nothing typed it refuses "Search your mail first, and then this will keep that search." (:8276-8284); otherwise the name through ask_for_a_search_name (:8206, Asking at :27114) with names_already_used (:8184), create_saved_search, read_the_tree_back, saved_searches::created said at High
Read src/presentation/wx_app.rs:8395-8507   -> the_search_to_write_back(search, edited: Option<Vec<Question>>) carries the name, the join, the folder and the id over (:8411-8414); edit_the_chosen_searchs_conditions opens show_rule_manager_dialog with the name and the questions only (:8449-8454)
Read src/presentation/wx_app.rs:34342-34500 (mod editing_the_conditions_of_a_saved_search)   -> six tests; test_a_changed_condition_list_is_written_back_under_the_same_search asserts writing.join == stored.join, which this plan's decision makes false, so it is rewritten in place to assert the join comes from the window; no test added
grep -n 'pub fn what_a_condition_list_still_needs\|pub fn show_rule_manager_dialog' src/presentation/wx_managers.rs   -> :3690, :3740; the sentence "A saved search has to ask at least one thing about a message. Add a condition before closing this window." lives in presentation, where application code cannot read it
grep -n 'fn test_' src/presentation/wx_managers.rs | grep -i condition   -> the case at :6240-6256 reads the sentence through the function, and stays green when the function returns the moved constant
```

**2. The letters in the two windows.** The conditions window is
`run_manager_loop` over `make_shell` (`wx_managers.rs:3749-3778`): &Add, &Edit,
&Delete and &Close (`:313-343`), no Move buttons, since `Question`'s
`ManagedRow::moved` is `None` (`:256`), and the list carries a name and no
visible label (`make_shell`, `:550`). So M is free for "Find messages that
&match:". The New Saved Search window is new: "&Name for this search:" (N,
the label Save This Search already uses at `wx_app.rs:8309`) and "&Look in:"
(L), with OK and Cancel carrying no letter, as the filter editor's do
(`wx_managers.rs:3943-3948`). The Saved Searches submenu after 13-38 holds C,
R and D; "&New Saved Search..." takes N. `grep -n 'New Saved Search\|Find messages that' docs/KEYBOARD_SHORTCUTS.md`
finds nothing, so the page claims no letter in either window. The executor
greps each window's and the submenu's `&` letters again before writing,
since 13-38 rewrote the submenu.

**3. The scan targets, and the two lines of the workflow a record anchors.**

```
grep -n 'pub const ALL: \[ScanTarget; ' src/presentation/scan_target.rs   -> :229, 40 targets at 630e2a67; test_the_workflow_asks_for_every_target (:406) is red for a target the workflow does not name
python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];[print(r['name'],repr(r.get('before'))) for r in g if r.get('file')=='.github/workflows/accessibility.yml']"   -> three records; two anchor whole lines of the target list: "          'which-days', 'send-later', 'add-address-book', 'about', 'feedback'," and "          'contact-editor', 'condition-editor', 'filter-editor', 'signature-editor', 'account-editor'"
grep -n "'page-window'" .github/workflows/accessibility.yml   -> :182, the list's last line
```

So the two new names go on a new line after the last line of the list, with
a comma put after `'page-window'`, and neither anchored line is touched.
Earlier phase 13 plans may add targets of their own; the executor reads the
list and the two records again with the parser before editing, and the
`.github` commit answers `all` through the hook, which is expected and paid
once, in the commit that carries the workflow line.

**4. Where the places come from.** The tree shows the folders
`folders_in_the_tree` returns (`wx_app.rs:12776-12795`), and a saved search
stores a folder by its path (`SavedSearch.folder`, read at run time within
its own account, `saved_searches.rs:421-435` in data). The Look in choice
offers "Everywhere in <account>" first and then that account's folders by
path in the tree's order, so a search can only name a folder of the account
it is saved under.

**5. The gate for each file, at `630e2a67`.** `src/application/saved_searches.rs`
76 tests and 3 records (80 after 13-38), cases added with the count check
named bare; `src/presentation/wx_managers.rs` 44 and 14, none added;
`src/presentation/wx_app.rs` 199 and 119, none added, one rewritten in place;
`src/presentation/scan_target.rs` 11 and 4, none added;
`src/presentation/scan_fixtures.rs` 11 and 0, none added;
`src/presentation/wx_new_saved_search.rs` and the new target new, zero.
Re-taken before the first commit with the parser command and
`grep -cxE '\s*#\[(tokio::)?test\]' FILE`, before and after quoted.

**6. The anchors, by the second reading.** None of the records whose `file`
is `wx_managers.rs` anchors inside `:3680-3790` or `:5040-5140`; two anchor
helpers this plan calls and must not copy: "no status line is built with a
label that is only a space" (`make_shell`, `:550-555`) and "a checkbox in an
editor grid is named outright, not left to the fallback" (`add_checkbox`,
`:80-82`). None anchors in `wx_app.rs:8238-8508`, `:34340-34500` or
`:17560-17930`. Read again before each commit.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation; Pratik may overrule.**
Pratik confirmed the order of phase 13 and said "plan and implement phase 13"
on 2026-09-24.

1. **Every or any can be changed after a search is made (RESEARCH-4 question
   3).** The window that makes a search from nothing offers it, and Edit
   Conditions gains the same choice in the same plan. The planner's way of
   doing both with one control: the choice lives in the conditions window,
   which both doors open, rather than in the new window alone.
2. **The 500 cap on a saved search's list stays (question 10).** #24 is
   closed and `MOST_RESULTS_SHOWN` still bounds the list
   (`saved_searches.rs:889`, read at `wx_app.rs:8090`); a separate issue, not
   this group. This plan files a ledger entry saying so and does not open an
   issue, which is a public post and Pratik's to make.
3. **No key for Save This Search (question 11)**, as 13-38 recorded.
4. **A new search is saved under the account being worked in**, as Save This
   Search saves one today (`wx_app.rs:8263-8269`), per account (question 9).
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: one refusal, the answer a conditions window gives, and a search from nothing as rules</name>
  <files>src/application/saved_searches.rs, src/presentation/wx_managers.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/saved_searches.rs:130-170` (`Join`, `read`, `written_down`), `:495-560` (`WhatASavedSearchWillAsk`), `:630-660` (`a_search_in_words`), `:940-1070` (`created`, `name_for`, `SavedSearch`).
    - `src/presentation/wx_managers.rs:3670-3695` and `:6230-6260`.
  </read_first>
  <behavior>
    - `ASKS_NOTHING: &str`, the sentence `what_a_condition_list_still_needs` says today, moved here word for word; `wx_managers::what_a_condition_list_still_needs` answers it, so its existing case stays green unchanged.
    - `Join::in_the_window(self) -> &'static str`: "every condition" for `All`, "any condition" for `Any`, and `Join::CHOICES` in that order, the words the window's choice shows.
    - `EditedConditions { join: Join, questions: Vec<Question> }`, what the conditions window gives back when anything changed.
    - `a_search_from_nothing(id, name, folder: Option<String>, edited: EditedConditions) -> Result<SavedSearch, &'static str>`: the search with those parts, or `Err(ASKS_NOTHING)` when it asks nothing.
    - `where_a_search_can_look(account_name, folder_paths) -> Vec<(String, Option<String>)>`: "Everywhere in Work" with no folder first, then each path as itself with `Some(path)`, in the order given.
    - Cases: `test_a_search_from_nothing_keeps_its_name_join_folder_and_questions`; `test_a_search_from_nothing_that_asks_nothing_is_refused_in_the_one_wording`; `test_the_window_offers_every_condition_before_any_condition`; `test_a_search_can_look_everywhere_in_the_account_or_in_one_of_its_folders`.
  </behavior>
  <action>
    Red first, on a branch. The red commit names the four cases by module
    path (`application::saved_searches::tests::<name>`) and the count check
    bare, and carries only the signatures they need, answering wrongly, so
    each runs and fails. Run them and the count check first.

    Then the green commit. Invoke `elegant-code`: the constant moves, never
    copied, and the presentation function returns it; no `unwrap`.

    Guard record: one on `saved_searches.rs` whose break lets an empty list
    through `a_search_from_nothing`, its refusal case red; measured, with
    every record the count check flags, in one `--remeasure` call as the
    first run.
  </action>
  <verify>
    <automated>cargo test --lib application::saved_searches:: && cargo test --lib presentation::wx_managers:: && cargo test --test house_style test_every_guard_record_says_how_many_tests_the_files_it_names_held</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the four cases by module path and the count check bare.
    - `grep -c 'pub const ASKS_NOTHING' src/application/saved_searches.rs` is 1; `grep -c 'saved_searches::ASKS_NOTHING' src/presentation/wx_managers.rs` is at least 1; `grep -c 'pub fn a_search_from_nothing' src/application/saved_searches.rs` is 1.
    - `cargo test --lib application::saved_searches::` passes with four more tests than before, and `presentation::wx_managers::` with the same count as before, both quoted.
    - One guard record measured and every flagged record re-measured, quoted.
  </acceptance_criteria>
  <done>The refusal has one home a rule can read, and making a search from nothing is a pure function that refuses one that asks nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the conditions window asks every or any, and Edit Conditions writes it back</name>
  <files>src/presentation/wx_managers.rs, src/presentation/wx_app.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_managers.rs:230-470` (`run_manager_loop`), `:530-570` (`make_shell`), `:3719-3781` (`show_rule_manager_dialog`), `:3840-3960` (how the filter editor builds and names a `Choice`).
    - `src/presentation/wx_app.rs:8375-8507` and `:34342-34500`.
    - The global rule on verifying a name at the handle focus reaches, both channels.
  </read_first>
  <behavior>
    - `build_conditions_window(parent, search_named, questions, join, palette)`, public, builds what `show_rule_manager_dialog` builds today plus, above the list, a `StaticText` "Find messages that &match:" and a `Choice` over `Join::CHOICES`' words with `join` selected, named with `set_accessible_name(&choice, &name_from_label(label))`, and gives back the dialog, the sizer, the list, the status line and the choice, so a target can build it without the modal loop.
    - `show_rule_manager_dialog(parent, search_named, questions, join, a11y) -> Option<EditedConditions>` runs the loop over that window and answers `Some` when the questions or the join changed, `None` when neither did; the close refusal is unchanged.
    - `the_search_to_write_back(search, edited: Option<EditedConditions>)`: nothing changed writes nothing; an empty list is refused with `ASKS_NOTHING`; otherwise the join and the questions from the window, the name, the folder and the id carried over. `edit_the_chosen_searchs_conditions` opens the window with the search's join. The six tests in `editing_the_conditions_of_a_saved_search` are rewritten in place to hand `EditedConditions`, and `test_a_changed_condition_list_is_written_back_under_the_same_search` asserts the join is the window's (`Join::Any` over a stored `Join::All`) while the id, the name and the folder are the stored ones; no test added.
  </behavior>
  <action>
    Red first, on the same branch. The red commit rewrites the six tests in
    place to hand `EditedConditions`, with the signatures they need and the
    join not yet carried, so
    `presentation::wx_app::editing_the_conditions_of_a_saved_search::test_a_changed_condition_list_is_written_back_under_the_same_search`
    runs and fails and the other five pass; it names that one by module
    path (the module read again before naming it). Run the module first and
    name everything that fails.

    Then the green commit, with `cargo build` between. Invoke
    `elegant-code`: the window is built once, by the builder, and the loop
    runs over what it gave back; the choice is read once, when the loop
    ends. The target in task 3 reads the choice's name over MSAA on the
    built window; this task adds no test to `wx_managers.rs` or `wx_app.rs`.

    Guard record: one on `wx_managers.rs` whose break answers `None` when
    only the join changed, the target's join reading red once task 3 has
    written it, `suite` the target; written and measured in task 3's green
    commit, when its suite exists.
  </action>
  <verify>
    <automated>cargo test --lib presentation::wx_app:: && cargo test --lib presentation::wx_managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the rewritten join case by module path.
    - `grep -c 'pub fn build_conditions_window' src/presentation/wx_managers.rs` is 1; `grep -c 'Find messages that &match:' src/presentation/wx_managers.rs` is 1; `grep -c 'EditedConditions' src/presentation/wx_app.rs` is at least 2.
    - `cargo test --lib presentation::wx_app::` and `presentation::wx_managers::` pass with the counts they had before the plan, quoted.
  </acceptance_criteria>
  <done>The conditions window asks every or any, both doors open it with the search's answer, and a changed answer is written with the questions in one replace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the New Saved Search window, its two doors, and the two scan targets</name>
  <files>src/presentation/wx_new_saved_search.rs, src/presentation/mod.rs, src/presentation/wx_app.rs, src/presentation/scan_target.rs, src/presentation/scan_fixtures.rs, .github/workflows/accessibility.yml, tests/a_saved_search_can_be_made_from_nothing.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:8184-8236` (`names_already_used`, `ask_for_a_search_name` and its refusal), `:8244-8345`, `:12776-12795`, `:17560-17930` (the scan target arms, `ConditionEditor` and `PhoneNumber` as the shape), and the Saved Searches submenu as 13-38 left it (`rebuild_the_saved_search_menu`).
    - `src/presentation/scan_target.rs:1-120`, `:225-330`, `:400-430`; `.github/workflows/accessibility.yml:125-185`.
    - `tests/the_about_dialog_names_its_owners_and_its_links.rs:130-300` (reading a built window's children over MSAA through `AccessibleObjectFromWindow`).
  </read_first>
  <behavior>
    - `wx_new_saved_search.rs`, new: `build_new_saved_search_dialog(parent, places, palette)` builds a dialog titled "New Saved Search" with "&Name for this search:" and a text box, "&Look in:" and a `Choice` over the places' words with the first chosen, a sentence "Next, add the conditions a message has to meet." with no letter, OK and Cancel; each control named from its label; `ask(parent, a11y, places, already_used) -> Option<(String, Option<String>)>` shows it, refuses a name `saved_searches::name_for` refuses by saying `why_not` aloud at High and in a message box and keeping the window open, and answers the tidied name and the chosen place's folder.
    - `make_a_saved_search_from_nothing(app, cache, frame, a11y)` in `wx_app.rs`: the account being worked in, or "Choose an account first."; the places from `where_a_search_can_look` over that account's name and the paths `folders_in_the_tree` gives; `wx_new_saved_search::ask`; then `show_rule_manager_dialog` titled for the new name with no questions and `Join::All`; nothing written when it answers `None`; otherwise `a_search_from_nothing` with a new id, `create_saved_search`, `read_the_tree_back`, and `saved_searches::created` sent to the status line and announced at High, as Save This Search does.
    - The doors: `save_this_search` with nothing typed calls it in place of its refusal; "&New Saved Search..." (`ID_NEW_SAVED_SEARCH`) is the first command after the separator in `rebuild_the_saved_search_menu`, its arm calling it.
    - Two targets: `ScanTarget::NewSavedSearch` ("new-saved-search") builds the new window over the scan-only account's places and shows it; `ScanTarget::SearchConditions` ("search-conditions") builds the conditions window over a fixture search from `scan_fixtures` with two questions and shows it; both in `ALL`, `as_name`, their `wx_app.rs` arms, and the workflow's list on a new line after its last (premise 3).
    - The target `tests/a_saved_search_can_be_made_from_nothing.rs`: the new window built and each control's MSAA name read at its own handle (the text box "Name for this search", the choice "Look in"), the places read back ("Everywhere in Work", then the paths); the conditions window built with `Join::Any` and its choice's MSAA name "Find messages that match" and its selection read; source readings over `what_ships` that `save_this_search`'s nothing-typed branch calls `make_a_saved_search_from_nothing(` and no longer holds the old refusal's call, that the `ID_NEW_SAVED_SEARCH` arm calls it, that it calls `a_search_from_nothing(` before `create_saved_search(`, and that `edit_the_chosen_searchs_conditions` passes the search's `.join`; each with a planted-fault companion that is refused.
  </behavior>
  <action>
    Red first, on the same branch: the target's readings bare, with the
    public signatures they need answering nothing. Run the target first and
    name everything that fails.

    Then the green commits, `cargo build` between: the window and `ask`;
    the flow and its two doors; the two targets, whose `ALL` entries and
    workflow line land in one commit so
    `test_the_workflow_asks_for_every_target` is never red on a commit, that
    commit answering `all` through the hook. Invoke `elegant-code`: the flow
    is a line of calls, each refusal a sentence; `ask` loops on its own
    refusal rather than the caller re-opening it. Invoke `naming-review` on
    the new module and functions.

    Guard records: one on `wx_app.rs` whose break restores the refusal in
    `save_this_search`'s nothing-typed branch, its reading red, `suite` the
    target; one on `wx_new_saved_search.rs` whose break drops the look-in
    choice's name, its MSAA reading red, `suite` the target; task 2's record
    on `wx_managers.rs`; each measured, with every record the count check
    flags, in one `--remeasure` call per commit.
  </action>
  <verify>
    <automated>cargo test --test a_saved_search_can_be_made_from_nothing && cargo test --lib presentation::scan_target:: && cargo test --lib presentation::wx_app:: && cargo test --test wired</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's readings bare.
    - `grep -c 'fn make_a_saved_search_from_nothing' src/presentation/wx_app.rs` is 1 and `grep -c 'make_a_saved_search_from_nothing(' src/presentation/wx_app.rs` is at least 3; `grep -c '"&New Saved Search..."' src/presentation/wx_app.rs` is 1; `grep -c "'new-saved-search', 'search-conditions'" .github/workflows/accessibility.yml` is 1, and the two anchored lines premise 3 quotes each still match once (`grep -cxF` on each, 1).
    - The target passes with at least 10 tests, quoted; `presentation::scan_target::` and `presentation::wx_app::` with the counts they had before, quoted; `cargo test --test wired` passes, so the submenu's letters are still one each.
    - Three guard records measured and quoted.
  </acceptance_criteria>
  <done>A saved search can be made from nothing from two doors, through a window named on the channel NVDA reads, and both windows are walked by the scan.</done>
</task>

<task type="auto">
  <name>Task 4: the pages, the changelog, the ledger, GAP-09 ticked, and #58 closed</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md`, The Sidebars and the Action Menu's Saved Searches row as 13-37 and 13-38 left them, and the Edit Menu section (`:625-660`), where Save This Search is.
    - `docs/USER_GUIDE.md`, the saved searches paragraphs under Search Functionality.
    - `.planning/REQUIREMENTS.md`, GAP-09 and its traceability row, and the dated lines 13-37 and 13-38 wrote.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: the Edit Menu row for Save
    This Search says that with nothing searched it opens New Saved Search;
    the Action Menu's Saved Searches row names New Saved Search; a short
    list of the New Saved Search window's and the conditions window's
    letters (N, L; M) under the dialog accelerator sections, on the pattern
    of the others. The guide: a Saved searches section under Search
    Functionality in steps: making one from nothing, choosing every or any,
    changing it later with Edit Conditions; and one sentence that a saved
    search reads only mail on this computer and shows the newest 500, which
    stays as it is (decision 2). The changelog, `[Unreleased]`, Added, in
    #58's words; Known limitations: nobody has heard the two windows.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's ear
    (both windows in order under NVDA, the refusal of an empty list, the
    created sentence); one `todo` for the 500 cap, which #24's closing did
    not lift (`saved_searches.rs:889`, `wx_app.rs:8090`), for Pratik to file
    as an issue if he wants it changed, and saying this plan opened no issue.

    GAP-09: its box and its traceability row ticked with the date, and the
    dated line under its `[D]` line naming, for each of the four clauses,
    the test or reading that closes it (13-37's order test and target,
    13-38's key test and target, this plan's target, and the two dated
    pages). The four marks: phase 13's progress row; this plan's `- [ ]`
    line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes) ticked;
    `STATE.md`'s plan number in the frontmatter and the body; GAP-09 as
    above. Run `cargo test --test the_planning_files_agree_with_themselves`
    first.

    This plan changes what is spoken and shown: after this one documents
    commit the branch is pushed with a pull request into `main` under
    Pratik's standing OK, CI, NVDA and Accessibility read before the local
    merge, the Accessibility run's two new targets read by name in its log.
    After the merge, the executor closes the issue from the merge commit:
    `gh issue close 58 --comment "..."` naming what was built for each of
    its points, point 2 answered by decision 3 and point 4 by decision 2.
    Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'New Saved Search' docs/KEYBOARD_SHORTCUTS.md` is at least 2; `grep -c 'New Saved Search' docs/USER_GUIDE.md` is at least 1.
    - GAP-09's line reads `- [x] **GAP-09**` (`grep -c '^- \[x\] \*\*GAP-09\*\*' .planning/REQUIREMENTS.md` is 1).
    - Both ledger entries in both halves; carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say how a saved search is made from nothing and how every or any is changed, GAP-09 is ticked with a named test for each clause, and #58 is closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a typed name to the store | a name somebody typed, unique per account whatever its case |
| the conditions window's answer to the store | the join and the questions must land together or not at all |
| a folder path to an account | a path is unique inside one account and not across them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-39-01 | Tampering | a search that asks nothing written, which takes every message or none | medium | mitigate | refused by `a_search_from_nothing`, the window and the store, one wording; its case and a guard record |
| T-13-39-02 | Tampering | the join written apart from the questions | medium | mitigate | `replace_saved_search`'s one transaction carries both; the rewritten write-back test |
| T-13-39-03 | Tampering | a new search naming another account's folder | low | mitigate | the places come from the account the search is saved under; the target reads them |
| T-13-39-04 | Denial of service | a window whose controls NVDA cannot name | medium | mitigate | named from their labels, read over MSAA at their own handles in the target, walked by two scan targets |
| T-13-39-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the workflow commit answers `all`, once; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes what is spoken and shown: after the one documents commit, the branch is pushed with a pull request into `main` (Pratik's standing OK of 2026-09-23), CI, NVDA and Accessibility read on it, a red one fixed on the branch; the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns, which here includes `all` for the workflow; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What the tester's ear settles: both windows in order and the created sentence. What the readings prove: the rules, the join written back, the names over MSAA, both doors reaching the flow.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 9 closes structurally: saved searches can be reordered, given a key and saved from scratch, and the two pages that drifted are corrected.
- GAP-09's `[D]` clauses each have a named test or reading and its box is ticked with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-09's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-39-SUMMARY.md` when done
</output>
