---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 30
type: execute
wave: 32
depends_on: [13-29]
files_modified:
  - src/application/asking_when_free.rs
  - src/service/free_busy.rs
  - src/presentation/managers.rs
  - tests/item_form_free_busy.rs
  - guards/guards.toml
  - docs/privacy.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-08]
estimate:
  tokens: 19200
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every place an account can be asked is asked: every signed-in calendar server, Microsoft and Google, all at once, so one slow place costs only its own answers (GAP-08's second [D] line, #57 point 2)."
    - "Each person's answers are merged: a person any place answered for is answered, their busy time from every place united, never replaced; a person is unknown only where no place answered, and the reasons are kept for the sentence."
    - "Nowhere is the answer only when the account has no place at all; a list of one place behaves exactly as today."
    - "People keep the order they were invited in, as every sentence reads them."
  artifacts:
    - "src/application/asking_when_free.rs: every_place_to_ask(calendars, sign_in, microsoft, google) -> Vec<WhereToAsk> and questions_for_every_place(places, people) -> Vec<AskHere>."
    - "src/service/free_busy.rs: the answers merged per address inside everybody_asked, where the address is still known."
    - "src/presentation/managers.rs: the worker asks every place; production code only."
    - "tests/item_form_free_busy.rs: a reading that the worker builds every place, with a planted-fault companion."
  key_links:
    - "managers::asking_when_people_are_free -> every_place_to_ask -> questions_for_every_place -> when_they_are_free (join_all) -> merged per address -> Invited in invitation order -> when_we_could_meet."
---

<objective>
Free/busy asks every source an account has and merges what they say
about each person.

Purpose: GAP-08's second `[D]` line, "every calendar source an account
has asked", and #57's point 2. `where_to_ask` returns one place and
`one_question` builds one question (`asking_when_free.rs:172-243`),
while the service already asks several places at once
(`free_busy.rs:698-747`) and answers each person from the one place they
were put in, with no merge. RESEARCH-3 (C7) sized this at two tasks.

Output: every place, the merge, the call site, the reading, the privacy
page, the changelog and the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-29-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. Answered anywhere wins, busy united (RESEARCH-3, C7).** A person is
unknown only when no place answered; two places' busy stretches are
both kept.

**2. A personal Microsoft account's refusal is ledgered, not guessed at
(planner's choice, recorded).** RESEARCH-3 pitfall 6 says Microsoft's
`getSchedule` does not serve personal accounts and the sentence should
say so. What Microsoft answers such an account is not known here, and a
sentence keyed on a guessed error would be a guess said as a fact; the
error shape is a phase 14 reading, ledgered by this plan.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before 13-29 added
Google. Command first, then what it said.

```
sed -n 172,243p src/application/asking_when_free.rs   -> where_to_ask returns one WhereToAsk; the_first_calendar_server_signed_in_to takes the first; one_question builds one AskHere
sed -n 698,747p src/service/free_busy.rs   -> when_they_are_free(outward, places: &[AskHere], about) and everybody_asked: batches over every place, join_all, then everybody_in per batch, flat: one Invited per person per place
sed -n 750,760p src/service/free_busy.rs   -> everybody_in builds Invited { called, zone, calendar } and drops the address, so a merge after it has only the name to go on
grep -n 'pub struct Invited' -A 8 src/application/when_people_are_free.rs   -> Invited carries called, zone and calendar, no address
```

So the merge happens inside `everybody_asked`, where each batch still
holds its `AskAbout` with the address, before the addresses are
dropped; merging on names after would join two different people who
share a name.

**Guard records, both readings.** `asking_when_free.rs` 0 and 0;
`free_busy.rs` 0 and 0; `managers.rs` 54 and 39, no test added, no
record anchored in `:1005-1145`; `tests/item_form_free_busy.rs` 0 and 0.
Re-take after 13-29, whose records on `free_busy.rs` and
`asking_when_free.rs` are this plan's to re-measure when its tests land
there.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: every place to ask, and one answer per person merged from all of them</name>
  <files>src/application/asking_when_free.rs, src/service/free_busy.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/asking_when_free.rs:160-250` (as 13-29 left it) and `src/service/free_busy.rs:690-830`.
    - `src/application/when_people_are_free.rs:85-135` (`TheirCalendar`, `Stretch`, `WhyNot`).
  </read_first>
  <behavior>
    - `every_place_to_ask(calendars, sign_in, microsoft, google) -> Vec<WhereToAsk>`: every signed-in calendar server (each server once), Microsoft where a token is held, Google where a Google calendar and a token are held; empty when none; `questions_for_every_place(places, people) -> Vec<AskHere>` asks every person at every place, empty for no people.
    - In `everybody_asked`: one `Invited` per person in invitation order; `Answered` where any place answered, covering the window, with every place's busy stretches kept (two overlapping stretches both present, not one replacing the other); `NotKnown` only where every place failed, carrying the first place's reason so the sentence names a reason.
    - Cases: a person one place answers and another does not is answered; busy time from two places united; nobody answered anywhere unknown; one place behaves as before; two people with one name and two addresses stay two people.
  </behavior>
  <action>
    Red first, on the branch `13-30-every-free-busy-source`: the cases by
    module path (`application::asking_when_free::tests::<name>`,
    `service::free_busy::tests::<name>`); with 13-29's records on these
    files, the count check `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    is named bare if it fires. Then the functions and the merge, with
    `cargo build` between (per decision 1). `where_to_ask` and
    `one_question` are retired if nothing else calls them, with every
    test that named them rewritten in place to the new names. Invoke
    `elegant-code`: the merge is one function over one person's answers.

    Guard records: one whose break replaces rather than unites busy
    time, the united case red; one whose break asks only the first
    place, the every-place case red; each measured.
  </action>
  <verify>
    <automated>cargo test --lib application::asking_when_free:: && cargo test --lib service::free_busy::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the cases by module path.
    - `grep -c 'pub fn every_place_to_ask' src/application/asking_when_free.rs` is 1.
    - Both modules pass above their counts after 13-29, quoted; two guard records measured.
  </acceptance_criteria>
  <done>Every place an account has is asked, and each person gets one answer built from all of them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the call site asks every place, the privacy page, the changelog and the ledger</name>
  <files>src/presentation/managers.rs, tests/item_form_free_busy.rs, guards/guards.toml, docs/privacy.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `src/presentation/managers.rs:1005-1140` and `tests/item_form_free_busy.rs` (13-29's reading).
    - `docs/privacy.md:287-305`.
  </read_first>
  <behavior>
    - The worker builds `every_place_to_ask` and `questions_for_every_place` and passes them to `when_they_are_free`; the reading in the target holds it, rewritten in place from 13-29's reading of the single place, with a companion that plants the single place back.
  </behavior>
  <action>
    Red first: the rewritten reading bare. Then the call site, production
    code only in `managers.rs`, with `cargo build` between. Invoke
    `writing-craft` for the pages: the privacy page says every place the
    account keeps a calendar is asked, each with the same content. The
    changelog, `[Unreleased]`, Changed, #57 point 2.

    Ledger, by hand, both halves: one `todo` for what Microsoft answers a
    personal account's `getSchedule` (decision 2), for phase 14 to read
    and a later plan to word; one `unrun-verify` for phase 14 (an account
    with a calendar server and Google both answering). Guard record: one
    on `managers.rs` whose break asks one place, the reading red, `suite`
    `item_form_free_busy`, measured; `managers.rs` stays at 137 tests,
    quoted. One documents commit on the branch before the merge carries
    the pages, the ledger, the summary and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test item_form_free_busy && cargo test --lib presentation::managers:: && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the reading bare.
    - `grep -c 'every_place_to_ask' src/presentation/managers.rs` is 1; the changelog names #57.
    - The ledger entries in both halves; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>Find when everyone is free asks every place the account has and the privacy page says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| several providers' answers to one person's time | answers from places that disagree, merged into one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-30-01 | Tampering | one place's empty answer replacing another's busy time | high | mitigate | busy time united; a case and a guard record |
| T-13-30-02 | Tampering | two people with one name merged | medium | mitigate | the merge is by address inside `everybody_asked`; a case |
| T-13-30-03 | Information disclosure | a guest list sent to every provider an account has | low | accept | each place is one the account itself keeps a calendar at, and the privacy page says every one is asked |
| T-13-30-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is spoken (the answers and who is counted): push the branch, open a pull request, wait for CI, NVDA and Accessibility, then merge locally. Pushing `main` is Pratik's.
- Red commits name lib tests by module path and integration tests bare. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-08's second `[D]` line has a named test or reading in the summary and is marked with the date.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-08's `[D]` line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-30-SUMMARY.md` when done
</output>
