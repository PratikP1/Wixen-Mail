---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 50
type: execute
wave: 52
depends_on: [13-48, 13-49]
files_modified:
  - src/application/message_files.rs
  - src/application/importing_messages.rs
  - src/application/import_tree.rs
  - src/data/message_cache/attachment_content.rs
  - src/presentation/wx_app.rs
  - src/application/exporting_mail.rs
  - tests/every_number_carries_its_command_and_its_date.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-13]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A message imported from a saved message, a mailbox file, a zip, a folder or a saved Outlook message arrives with the files it carried, each stored with its name, type and bytes through the store the reader already fills, where until this plan it arrived with its words and a mark saying it had files, and nothing said the files were missing (RESEARCH-5's finding beside GAP-13, question 7, this plan IN)."
    - "The reader and the import build a message's attachment records from the parsed list and the files by one function, by position, so the two cannot come to disagree about which file is which."
    - "Imported mail is filed as here-only, so the store's budget never drops its files (ONLY_COPY_IS_HERE); a file larger than the store keeps is listed on its message and not kept, and the closing sentence of every import counts those files and says they stay only in the file imported from."
    - "An imported message with a file goes out again with it: the folder export as message files writes the file back, byte for byte."
    - "A signed message imported from a file still has the bytes its signature is over recorded, and its files as well; the .pst import is unchanged, its files counted as staying in the data file as before."
  artifacts:
    - "src/application/message_files.rs: MessageFromAFile carrying the files of the one message being filed, taken in read_out_of only when the parse lists any."
    - "src/data/message_cache/attachment_content.rs: AttachmentWithContent::all_from_a_parse, and the size rule shareable."
    - "src/application/importing_messages.rs: file_one_imported_message storing the files; the count and its sentence, said by both closing sentences."
    - "src/presentation/wx_app.rs: the reader building its records through all_from_a_parse; the folder import carrying the count."
  key_links:
    - "a file of mail -> message_files::read_out_of -> mime::attachments_with_bytes (only when the parse lists files) -> MessageFromAFile.files -> file_one_imported_message -> AttachmentWithContent::all_from_a_parse -> MessageCache::replace_attachments_with_content -> attachments_with_content -> the reader and every export."
---

<objective>
Imported messages keep their files.

Purpose: RESEARCH-5 found, beside GAP-13, that
`file_one_imported_message` never stores a message's files: an `.eml`,
`.mbox`, zip or folder import brings the words and a flag saying there
were files, and not the files, and no page, changelog line or ledger
entry says so. Guardrail 9's shape. Question 7's answer, taken
2026-09-24 from the research's recommendation, is to fix it in this
group as its own plan, and it decides that a saved Outlook message's
files come in too (13-47 put them inside the bytes it hands on).

Output: the files carried through the read and filed, one records
function for the reader and the import, the count of files too large to
keep and its sentence, the export round trip, the pages, the changelog,
the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-5-import-export.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-47-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-48-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before 13-45 to 13-49
ran. Command first, then what it said.

**1. The finding is derived, not measured; task 1's first test measures
it.**

```
grep -rn 'replace_attachments_with_content' src --include=*.rs | grep -v 'attachment_content.rs\|how_it_arrived.rs'   -> wx_app.rs:24782 (the reader), :32357, :32584, and a comment in mail_sync.rs:5783; nothing under importing_messages
sed -n 331,383p src/application/importing_messages.rs   -> file_one_imported_message writes the row, the text and, for a signed message, the form it arrived in; no attachment is recorded
grep -n 'has_attachments' src/application/filing.rs   -> :79, set from the parse's list, so the row says it had files
sed -n 87,122p src/application/message_files.rs   -> MessageFromAFile { message, the_form_it_arrived_in }; read_out_of (:116) is the one place one is built, and keeps the raw bytes only for a message that claims a signature
grep -n 'pub fn attachments_with_bytes' src/service/mime.rs   -> :255, one parse and one walk, in the order parse lists them
sed -n 24755,24783p src/presentation/wx_app.rs   -> the reader builds its records by position with AttachmentWithContent::from_a_parsed_part and the walk's bytes
```

If task 1's first test passes before any change, the finding is wrong:
the summary says so first, the test stays as the guard it would have
been, and only the sentence work of tasks 2 and 3 that still applies is
done.

**2. The budget cannot drop imported files, which is why keeping them
is safe.**

```
sed -n 422,460p src/data/message_cache/attachment_content.rs   -> the sweep never offers a file any message with ONLY_COPY_IS_HERE carries
grep -n 'ONLY_COPY_IS_HERE' src/data/message_cache/messages.rs   -> :44, "(filed_here = 1 OR pop_uidl IS NOT NULL)"
grep -n 'SET filed_here = 1' src/data/message_cache/messages.rs   -> :1153, in file_message_here, which file_one_imported_message calls
grep -n 'pub const LARGEST_ATTACHMENT_KEPT_BYTES\|fn is_small_enough_to_keep' src/data/message_cache/attachment_content.rs   -> :51 (25 MiB), :137 (private)
```

So an imported file is never swept, and the one way an imported file is
not kept is its size, which is what the new count says. No schema
change: the `attachments` table and the content store already exist,
and nothing here adds a table or a column. No setting is added, and
nothing here touches a server, so nothing goes to phase 14.

**3. The page figure and its reading.** A guide sentence restating 25 MB
is a figure the constants reading must hold:
`tests/every_number_carries_its_command_and_its_date.rs:1570` holds
`THE_FIGURES_THAT_RESTATE_A_CONSTANT: [Restated; 12]` on 2026-09-24, each
a page, the words before the figure and the constant; task 3 adds the
guide's figure to it and raises the length by one from whatever it
holds on the day. A body change: the file's test count does not move.

**4. The guard records this plan touches, by both readings.** tomllib,
1,088 records on 2026-09-24, re-counted on the day:

| File | `tests_last_seen` | `file` | anchors in the regions edited |
|---|---|---|---|
| `src/application/message_files.rs` | 1 | 0 | none |
| `src/application/importing_messages.rs` | 5 | 2 | `:377-381`, "signed mail brought in from a file keeps the bytes its signature is over", `before` the whole `if let Some(raw) = &read.the_form_it_arrived_in` block: the files are stored in a statement of their own after that block, leaving its text byte for byte, and the record is re-read after the edit |
| `src/application/import_tree.rs` | 1 | 1 | `:540`, not edited here |
| `src/data/message_cache/attachment_content.rs` | 1 | 1 | `:51`, the `LARGEST_ATTACHMENT_KEPT_BYTES` line, not edited; the size rule is made shareable where it stands at `:137` |
| `src/presentation/wx_app.rs` | 119 | 112 | none in `:24700-24800`, probed with the tomllib command on 2026-09-24 |
| `tests/every_number_carries_its_command_and_its_date.rs` | 7 | 1 | `:811`, not edited |
| `src/application/exporting_mail.rs` | 13-45's and 13-46's records | | none inside the test module this plan adds to |
</premise_corrections>

<decisions date="2026-09-24">
Taken 2026-09-24 from the research's recommendation; Pratik may
overrule.

**7. Imported mail brings its files, as its own plan, for every kind of
import that reads a message from a file:** a saved message, a mailbox
file, a zip, a folder and a saved Outlook message. The `.pst` import
keeps its present rule, that a message's files stay in the data file and
are counted, because its reader reads no attachment at all and changing
that is its own work. Taken 2026-09-24 from the research's
recommendation; Pratik may overrule.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the files carried through the read and filed with the message, through one records function the reader shares</name>
  <files>src/application/message_files.rs, src/application/importing_messages.rs, src/data/message_cache/attachment_content.rs, src/presentation/wx_app.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/message_files.rs:60-160` (`MessageFromAFile`, its doc comment on why a mailbox is read one message at a time, `read_out_of`, `read_one_message_as_it_arrived`) and `:900-925` (the archive reader's call of `read_out_of`).
    - `src/application/importing_messages.rs:303-383` and `:1445-1720` (`end_to_end`, the real-store cases, and 13-48's saved Outlook message case).
    - `src/data/message_cache/attachment_content.rs:78-130` (`AttachmentWithContent`, `described_only`, `from_a_parsed_part`) and `:180-260` (`replace_attachments_with_content`).
    - `src/presentation/wx_app.rs:24740-24785`.
  </read_first>
  <behavior>
    - `MessageFromAFile` gains `files: Vec<mime::AttachmentWithBytes>`, the files of this one message, in the order the parse lists them; `read_out_of` fills it through `mime::attachments_with_bytes(raw)` only when the parse lists at least one attachment, so ordinary mail pays nothing, and its doc comment says the files are held for the one message being filed and let go with it, which keeps the rule that no part of a mailbox is held twice.
    - `AttachmentWithContent::all_from_a_parse(message_id, parsed: &[AttachmentInfo], files: &[AttachmentWithBytes]) -> Vec<AttachmentWithContent>`, by position, a file missing from the walk giving a record described only; the reader at `wx_app.rs:24757-24780` calls it in place of its own mapping, with no change to what it stores.
    - `file_one_imported_message` records the message's files through `replace_attachments_with_content(stored, &all_from_a_parse(stored, &read.message.attachments, &read.files))` in a statement after the signed-form block (premise 4), logging a failure and not counting the message as unfiled, the rule the text and the signed form already follow.
    - `application::importing_messages::end_to_end::test_a_message_imported_from_a_file_keeps_the_files_it_carried`: a saved message with one file (a small PDF-shaped byte run and a name) imported through the single-file path into a real store in a temporary home; `attachments_with_content` for its row answers the file with its name, type and bytes. Beside it: a message inside a mailbox file read through `each_message_in` with the archive reading; a saved Outlook message with two files through 13-48's `a_saved_outlook_message_brought_in`; a signed message whose form it arrived in is still recorded and whose file is recorded too; `test_the_reader_and_the_import_build_the_same_records` in `attachment_content`, over one parse and its walk.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path, with the count
    check named bare, since records name `message_files.rs`,
    `importing_messages.rs` and `attachment_content.rs`; run before the
    red commit and everything that fails named. The first case failing
    for want of the file is the measurement premise 1 asks for; quote
    its failure. Then the field, the function, the reader's switch and
    the filing, with `cargo build` between; every construction of
    `MessageFromAFile` is `read_out_of`'s, so the compiler names no
    other site, and the summary says whether it named any.

    Invoke `elegant-code`: one function turns a parse and its walk into
    records, and both callers use it; the filing adds one statement.
    Invoke `dead-code-hunter` on `wx_app.rs:24740-24785` after the
    switch.

    Guard records: one on `importing_messages.rs` whose break drops the
    files statement, the kept-files case red, anchored on that statement
    with its comment line; one on `attachment_content.rs` whose break
    pairs each record with the next file, the shared-records case red;
    each measured on the library. The signed-mail record re-read and its
    anchor shown to occur once. Every record the count check names
    re-measured with one call.
  </action>
  <verify>
    <automated>cargo test --lib application::importing_messages:: && cargo test --lib application::message_files:: && cargo test --lib data::message_cache::attachment_content::</automated>
  </verify>
  <acceptance_criteria>
    - The first case's red failure quoted as the measurement of the finding, or, if it passed before any change, the summary's first sentence says the finding was wrong.
    - A red commit on a branch names the cases by module path and the count check bare.
    - `grep -c 'all_from_a_parse(' src/application/importing_messages.rs src/presentation/wx_app.rs` shows one call in each; `grep -c 'from_a_parsed_part(' src/presentation/wx_app.rs` is 0.
    - The three module runs pass with their totals quoted; two guard records measured and the signed-mail anchor shown to occur once, quoted.
  </acceptance_criteria>
  <done>Every import that reads a message from a file keeps the files it carried, through the records function the reader uses, and the signed form is still kept beside them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: files too large to keep counted and said by every import, and an imported file going out again</name>
  <files>src/application/importing_messages.rs, src/application/import_tree.rs, src/data/message_cache/attachment_content.rs, src/presentation/wx_app.rs, src/application/exporting_mail.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/importing_messages.rs:420-537` (`MessagesImported`, `what_the_mail_import_did`) and 13-48's `what_saved_outlook_messages_left` beside it; `src/application/import_tree.rs:318-492` (`FoldersImported`, `what_the_folder_import_did`); `src/presentation/wx_app.rs`'s `fill_folders_from` archive loop as 13-48 left it.
    - `src/application/exporting_mail.rs` and its tests as 13-46 left them.
  </read_first>
  <behavior>
    - `is_small_enough_to_keep` made `pub(crate)`; `MessagesImported` and `FoldersImported` gain `files_too_large_to_keep: usize`, counted from the files a message was filed with by that same rule, so the count and what was kept cannot disagree; the folder import carries it onto `FoldersImported` as it carries the saved-message counts.
    - One sentence, composed by one function both closing sentences call: "1 file was larger than Wixen Mail keeps, so it is listed on its message and stays only in the file you imported from" and the plural written out, the limit named in megabytes from the constant rather than typed.
    - `application::importing_messages::end_to_end::test_a_file_larger_than_the_store_keeps_is_listed_counted_and_said`: a message carrying a file one byte over the limit, imported; its record listed with no bytes, the count 1, the sentence said.
    - `application::exporting_mail::tests::test_an_imported_message_goes_out_again_with_its_files`: a saved message with a file imported into a real store and the folder exported as message files (13-46); the written file parsed back with `mime::attachments_with_bytes` gives the same name and bytes.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path, with the count
    check named bare; run before the red commit and everything that
    fails named. Then the counts, the sentence and the loop's carrying,
    with `cargo build` between. Invoke `cognitive-accessibility` on the
    sentence: what happened, why, and where the file still is. Both
    modules' every-sentence checks gain it.

    Guard records: one on `import_tree.rs` or `importing_messages.rs`,
    whichever holds the shared sentence function, whose break drops the
    too-large sentence, the case red; one on `exporting_mail.rs` whose
    break exports an imported message with no files (the round trip's
    file reading red); each measured. Every record the count check names
    re-measured with one call.
  </action>
  <verify>
    <automated>cargo test --lib application::importing_messages:: && cargo test --lib application::import_tree:: && cargo test --lib application::exporting_mail::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the count check bare.
    - `grep -c 'files_too_large_to_keep' src/application/importing_messages.rs src/application/import_tree.rs` shows it in both; `grep -c 'LARGEST_ATTACHMENT_KEPT_BYTES' src/application/importing_messages.rs src/application/import_tree.rs` shows the figure taken from the constant in whichever file composes the sentence.
    - The three module runs pass with their totals quoted; two guard records measured and quoted.
  </acceptance_criteria>
  <done>A file too large to keep is still listed, counted and said with where it still is, and an imported message's files go out again when its folder is exported.</done>
</task>

<task type="auto">
  <name>Task 3: the guide, the figure registered, the changelog, the ledger, GAP-13's note, and the branch pushed</name>
  <files>docs/USER_GUIDE.md, tests/every_number_carries_its_command_and_its_date.rs, docs/changelog.md, .planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - The guide's Import and Export section as 13-49 left it; `tests/every_number_carries_its_command_and_its_date.rs:1515-1600` (the restated figures and the page constants).
    - `docs/changelog.md`'s `[Unreleased]` and 13-45 to 13-49's entries.
  </read_first>
  <action>
    Invoke `writing-craft`. The guide: one short paragraph in Import and
    Export saying imported messages bring their files, that a file
    larger than 25 MB is listed on its message and not kept, so it stays
    only in the file you imported from, and that an Outlook data file's
    messages still arrive without their files, as its warning already
    says. The figure registered in `THE_FIGURES_THAT_RESTATE_A_CONSTANT`
    as a `Restated` entry naming the guide, the words before the figure,
    the unit and `LARGEST_ATTACHMENT_KEPT_BYTES`, the array's length
    raised by one, and the guide's page constant added beside
    `THE_PRIVACY_PAGE` if none exists; its reading passes against the
    guide, and planting 24 in the guide's sentence by hand turns it red
    before it is put back, quoted.

    The changelog, `[Unreleased]`, Fixed, plainly: until this build a
    message imported from a saved message, a mailbox file, a zip or a
    folder arrived with its words and a mark saying it had files, and
    without the files, and nothing said so; found on 2026-09-24 while
    researching phase 13; imported messages now bring their files,
    saved Outlook messages included, and a file over the limit is
    listed and counted. Known limitations: the `.pst` import still
    leaves files in the data file, as it says.

    Ledger, by hand, both halves: one entry for the finding, opened and
    marked fixed in this plan with the commit, so the history of the gap
    stays visible (guardrail 9); one `unrun-verify` for the tester, a
    real mailbox file with attachments imported and a file opened from
    the imported message, heard under NVDA. `.planning/REQUIREMENTS.md`:
    GAP-13's line gains a dated note that imported mail keeps its files
    since this plan, with the test names as `path#name`, each grepped as
    `fn <name>` in its file first; its tick, written by 13-49, is left as
    it is.

    The closing sentences change, so the branch is pushed with a pull
    request after the last branch commit, the three runs read, any red
    fixed on the branch, and then the local `git merge --no-ff`.
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The planted 24 turning the constants reading red, and back to green, quoted; the array's length before and after quoted.
    - `tr '\n' ' ' < docs/USER_GUIDE.md | grep -c 'stays only in the file you imported from'` is 1.
    - Both ledger entries in both halves, quoted by number; carriage returns 0 on every file touched; no em or en dash; none of the six words.
    - The pull request's number and the three runs' conclusions quoted.
  </acceptance_criteria>
  <done>The guide says imported messages bring their files and where a file too large to keep still is, the figure is held to its constant, and the gap's history is on the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's attachment in a file to the store | bytes somebody else chose, written into this computer's database |
| the store's budget to imported files | files that have no server to be fetched from again |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-50-01 | Denial of service | a file of mail carrying huge attachments filling the store | medium | mitigate | the store's own `LARGEST_ATTACHMENT_KEPT_BYTES` refuses each file over it, listed and counted; files are held for one message at a time and let go when it is filed |
| T-13-50-02 | Information disclosure | a file's name or contents written to the log | medium | mitigate | the filing logs a failure without the name or bytes, as the attachment store's module rule requires; nothing new is logged |
| T-13-50-03 | Tampering | the reader and the import pairing a record with the wrong file | medium | mitigate | one `all_from_a_parse` by position for both; a case over one parse; a guard record |
| T-13-50-04 | Repudiation | imported files silently missing, the gap this plan closes | high | mitigate | the kept-files cases over every kind of import; the too-large count and sentence; a ledger entry recording the gap and its fix |
| T-13-50-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing installed; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the branch is pushed with a pull request and the three runs read before the merge; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite runs once, in 13-51's full gate; never `scripts/check.sh all` by hand here. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line.
- Four guard records measured and the signed-mail anchor re-read; every record the count check flagged re-measured with one call and read.
- What the tester settles: a real mailbox with attachments imported and a file opened from it; what the tests prove: every file-reading import keeps its files, the too-large rule is counted and said, the files go out again.
</verification>

<success_criteria>
- The finding is measured by a red test before it is fixed, or said to be wrong.
- GAP-13's line carries a dated note naming this plan's tests; its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (the roadmap's, and the phase README's if one exists); `.planning/STATE.md`'s plan number in the frontmatter and in the body; GAP-13's line and traceability row carrying this plan's dated note.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-50-SUMMARY.md` when done
</output>
