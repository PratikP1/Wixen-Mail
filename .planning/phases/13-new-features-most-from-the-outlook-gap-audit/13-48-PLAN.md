---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 48
type: execute
wave: 50
depends_on: [13-47]
files_modified:
  - src/application/import_tree.rs
  - src/application/importing_messages.rs
  - src/service/outlook_data_file/one_saved_message.rs
  - src/presentation/wx_app.rs
  - tests/wired.rs
  - guards/guards.toml
  - docs/USER_GUIDE.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/comparison.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-13]
estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "File, Import Mailbox takes a saved Outlook message: the picker offers .msg, the file is recognised by its first eight bytes and never by its name, and the message is filed under Imported through the one place imported mail is filed (GAP-13's msg read, the reaching half; #53 point 5)."
    - "Import a Folder of Messages, and a zip, take saved Outlook messages inside them: an entry that begins like the container goes to the same reader, read whole under the archive's own bound on one entry, so a folder somebody dragged a hundred messages out of Outlook into arrives as a hundred messages in its folder."
    - "The closing sentence of either import separates what arrived from what stayed in the files, one sentence per cause (files not brought, blind copies left off, formatting kept only in Outlook's own format, signatures not kept, saved items that are not messages), and says once, at the end, that reading saved Outlook messages is new and has read only files made for its tests."
    - "A single chosen file the reader refuses is refused with the reader's own sentence; inside a folder, a container that is not a saved message is counted with the files that held no mail, and one damaged or too large is counted as could not be read."
  artifacts:
    - "src/application/import_tree.rs: WhatWasChosen::AnOutlookMessage and where_one_entry_lands routing a container entry; FoldersImported carrying the saved-message counts; what_the_folder_import_did saying them."
    - "src/application/importing_messages.rs: ReadAs::OneSavedOutlookMessage, one_saved_outlook_message_in, a_saved_outlook_message_brought_in (a real-store test), the counts on MessagesImported and one sentence function both closing sentences call."
    - "src/presentation/wx_app.rs: the fourth arm in fill_folders_from, the archive loop reading container entries whole, the picker's filter."
    - "tests/wired.rs: test_importing_mail_sends_each_kind_of_file_to_its_own_reader reading the fourth answer and the archive loop's route."
  key_links:
    - "Import Mailbox -> picker (*.msg) -> fill_folders_from -> import_tree::what_was_chosen (HOW_A_SAVED_MESSAGE_BEGINS before the mail question) -> AnOutlookMessage arm -> importing_messages::a_saved_outlook_message_brought_in -> one_saved_message::read over the open file -> each_message_in over its bytes -> file_one_imported_message -> the closing sentence."
    - "a folder or zip -> where_one_entry_lands -> ReadAs::OneSavedOutlookMessage -> one_entry_read_through (most_one_thing_unpacks_to) -> one_saved_outlook_message_in -> file_one_imported_message -> FoldersImported's counts -> what_the_folder_import_did."
---

<objective>
Saved Outlook messages through both import commands, the reaching half
of #53's point 5.

Purpose: 13-47 built a reader nothing calls. A reader nobody reaches is
the shape guardrail 1 names, so this plan is where `.msg` becomes a
thing a person can do: pick one, pick a folder of them, or import a zip
that holds some.

Output: the routing, the fourth arm, the entries read whole, the counts
and the sentences, a real-store test, the wiring reading, the pages,
the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-5-import-export.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-47-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before 13-47 ran; the
reader's names are 13-47's plan's and are re-read from its summary.
Command first, then what it said.

**1. The routing and the worker, as they are.**

```
sed -n 494,548p src/application/import_tree.rs   -> WhatWasChosen { AnArchive, MailInOneFile, AnOutlookDataFile }; what_was_chosen tests the .pst signature at :540 before asking message_files whether the bytes are mail; a .msg today answers NotMail there and so AnArchive, and the zip reader refuses it as not an archive (RESEARCH-5, derived from :543-546 and mailbox_archive.rs:262)
sed -n 150,171p src/application/import_tree.rs   -> where_one_entry_lands decides an archive entry from its first bytes: many messages, one message, or NotMail (held_no_mail)
grep -rn 'ReadAs::' src --include=*.rs   -> matched at importing_messages.rs:201-202 and :295-299 (each_message_in), import_tree.rs:162-163, :215-226 (the_folder_named_by) and :718-721, plus tests; a third variant is found by the compiler at every one
sed -n 14448,14455p src/presentation/wx_app.rs   -> the picker's filter "Mailboxes and Outlook data files (*.zip;*.eml;*.mbox;*.pst)"
sed -n 14634,14703p src/presentation/wx_app.rs   -> fill_folders_from: the three arms, then the archive loop reading every entry in pieces through one_entry_read_in_pieces
sed -n 15335,15373p src/presentation/wx_app.rs   -> one_file_of_mail_brought_in: a chosen file read whole with std::fs::read and filed under Imported through file_one_imported_message; the .msg path follows it but opens the file for reading and seeking rather than reading it whole
grep -n 'most_one_thing_unpacks_to\|pub fn one_entry_read_through' src/service/mailbox_archive.rs   -> :155 (a gigabyte by default, :178), :721: an entry read whole is bounded
grep -n 'pub fn what_the_data_file_import_did' src/application/importing_an_outlook_data_file.rs; sed -n 440,444p src/application/importing_an_outlook_data_file.rs   -> :323; "No real Outlook data file has been through this program before, so check what arrived ...": the pattern for saying the new reader is new, once, at the end
grep -n 'fn test_importing_mail_sends_each_kind_of_file_to_its_own_reader' tests/wired.rs   -> :3515
```

**2. The guard records this plan touches, by both readings.** tomllib,
1,088 records on 2026-09-24, re-counted on the day:

| File | `tests_last_seen` | `file` | anchors inside the regions edited |
|---|---|---|---|
| `src/application/import_tree.rs` | 1 | 1 | `:540`, "an Outlook data file is sent to its own reader rather than the archive's", `before` the `.pst` signature line; the new check is its own line naming `HOW_A_SAVED_MESSAGE_BEGINS`, so the anchor stays one place, re-read after the edit |
| `src/application/importing_messages.rs` | 5 | 2 | `:377-381` (the signed-form block, not edited here) and `:632` (moved within the file by 13-46) |
| `src/presentation/wx_app.rs` | 119 | 112 | `:14643-14650`, "an Outlook data file the worker recognises is handed to its reader", `before` the whole `AnOutlookDataFile` arm; the new arm sits beside it and duplicates none of it, re-read after the edit; `:14496-14500`, "a chosen folder of messages is handed to the import worker", in the folder picker, which this plan does not edit |
| `tests/wired.rs` | 19 | 2 | none; `:3515`'s body gains the fourth answer and its count does not move |
| `src/service/outlook_data_file/one_saved_message.rs` | 13-47's records | 13-47's | the test-only fixture helper this plan makes shared is added beside the test module, not inside an anchor |

Adding tests to `import_tree.rs` and `importing_messages.rs` changes
counts the six records above name; the count check prints the
`--remeasure` command and running it once, for all of them, is not
optional.

**3. What 13-47 left for this plan to say.** Its `LeftInTheFile` counts
(files not brought, blind copies, markup only in Outlook's own format,
signature not kept) and `WhyItWasNotRead` with its sentences. Until
13-50, the filing keeps no file for any kind of import; that is 13-50's
finding and fix, and this plan neither says the files arrive nor adds a
sentence about them.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the routing from the first bytes, a third way of reading an entry, the counts, and one sentence function for both closing sentences</name>
  <files>src/application/import_tree.rs, src/application/importing_messages.rs, src/service/outlook_data_file/one_saved_message.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/import_tree.rs:90-240` (`EntryInTheArchive`, `where_one_entry_lands`, `the_folder_named_by`), `:300-492` (`FolderToFill`, `FoldersImported`, `where_the_folders_land`, `what_the_folder_import_did`), `:494-548`.
    - `src/application/importing_messages.rs:140-300` (`ReadAs`, `importing`, `WhatToDoWithIt`, `each_message_in`), `:420-537` (`MessagesImported`, `what_the_mail_import_did`), `:1445-1480` (`end_to_end`, `a_cache`).
    - 13-47's summary for `read`, `SavedOutlookMessage`, `LeftInTheFile`, `WhyItWasNotRead` and the test fixture helper.
  </read_first>
  <behavior>
    - `import_tree::WhatWasChosen::AnOutlookMessage`; `what_was_chosen` answers it when the bytes begin with `one_saved_message::HOW_A_SAVED_MESSAGE_BEGINS`, asked before the mail question as the `.pst` signature is. Cases: a container's first bytes answer `AnOutlookMessage`; a `.pst`'s still answer `AnOutlookDataFile`; a saved message and a folder unchanged.
    - `importing_messages::ReadAs::OneSavedOutlookMessage`; `import_tree::where_one_entry_lands` answers `Into { read: OneSavedOutlookMessage, .. }` for an entry beginning with the container bytes, filed in the folder it sits in as a single saved message is (its last name part dropped). Case: `Work/Invoice.msg` beginning with the container bytes lands in Work.
    - `importing_messages::one_saved_outlook_message_in(bytes: &[u8]) -> SavedOutlookMessageRead { read: Result<MessageFromAFile>, left_in_the_file: LeftInTheFile, refused: Option<WhyItWasNotRead> }`: `one_saved_message::read` over a cursor with the default limits, and its bytes through `each_message_in(.., ReadAs::OneMessage)`, so a saved Outlook message becomes a message exactly as a `.pst` message does; `each_message_in`'s arm for the new reading takes this function's `read`, so there is one path.
    - `MessagesImported` gains `from_saved_outlook_messages: WhatSavedOutlookMessagesLeft` (read at all, files not brought, blind copies, formatting only in Outlook's own format, signatures not kept, items that were not messages) with `count_one_saved_outlook_message(&SavedOutlookMessageRead)`: a refusal as another kind or a kind this program does not keep counts as not a message; damaged or too large counts in `could_not_be_read`; not an Outlook message at all counts nowhere here and is the caller's to count with the files that held no mail. `FoldersImported` gains the same field.
    - `importing_messages::what_saved_outlook_messages_left(said: &mut SummingUp, left: &WhatSavedOutlookMessagesLeft)`, called by `what_the_mail_import_did` and `what_the_folder_import_did`: one sentence per non-zero count with its singular written out (files that stayed in the saved messages and why, blind copy recipients left off because a message here has no line for them, messages whose formatting Outlook kept only in its own format arriving as their words, signed messages arriving without what proves the signature, saved Outlook items that were not messages and were left out), and when any saved message was read at all, last: "Reading saved Outlook messages is new and has read only files made for its tests, so check what arrived against Outlook." Both modules' every-sentence checks gain the new sentences.
    - `importing_messages::a_saved_outlook_message_brought_in(cache, account, at: &Path) -> String`: opens the file for reading and seeking, reads it through `one_saved_message::read`, files the message under Imported through `a_folder_for_imported_mail` and `file_one_imported_message` (already-here and not-written counted as the single-file import counts them), and answers `what_the_mail_import_did`, or the reader's own sentence when it refused the file. The real-store case, `application::importing_messages::end_to_end::test_a_saved_outlook_message_is_filed_under_imported_with_its_subject_and_text`, files a fixture built with 13-47's helper into a temporary home and reads back one row with its subject, sender and text, and the sentence ending with the new-reader sentence; beside it, an appointment saved as a file answers the reader's refusal and files nothing.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path, and the count
    check named bare, since records name both application files; the
    named targets and the count check run before the red commit and
    everything that fails named. 13-47's fixture helper becomes
    `#[cfg(test)] pub(crate)` so these tests build containers the way
    13-47's do, in the same red commit. Then the variants, the routing,
    the counts and the sentence function, with `cargo build` between;
    the compiler names every `ReadAs` match, and each one gets the arm
    that reads as a single saved message does.

    Invoke `elegant-code`: the counts are one struct added to in one
    method; the sentences are composed in one function both closing
    sentences call, so the single-file import and the folder import
    cannot come to word one fact two ways. Invoke
    `cognitive-accessibility` on the new sentences: plain words, the
    cause in each, nothing that names a mechanism (the modules' checks
    refuse "header", ".eml" and "parse").

    Guard records: one on `import_tree.rs` whose break asks the mail
    question before the container bytes, so a saved Outlook message is
    sent to the archive reader, the routing case red; one on
    `importing_messages.rs` whose break drops the blind-copy count from
    `count_one_saved_outlook_message`, its sentence case red; each
    measured. The record at `import_tree.rs:540` re-read and its anchor
    shown to occur once; every record the count check names re-measured
    with one call.
  </action>
  <verify>
    <automated>cargo test --lib application::import_tree:: && cargo test --lib application::importing_messages:: && cargo test --lib service::outlook_data_file::one_saved_message::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path and the count check bare.
    - `grep -c 'AnOutlookMessage' src/application/import_tree.rs` is at least 2 and `grep -c 'OneSavedOutlookMessage' src/application/importing_messages.rs src/application/import_tree.rs` shows it in both files; `grep -c 'what_saved_outlook_messages_left(' src/application/importing_messages.rs src/application/import_tree.rs` shows a call in each.
    - The three module runs pass with their totals quoted; the real-store case among them.
    - Two guard records measured, the `:540` record re-read, and the remeasure call's output quoted.
  </acceptance_criteria>
  <done>A saved Outlook message is recognised by its first bytes, chosen alone or found in a folder or zip, read through 13-47's reader into the one filing path, and what stayed in the file is counted and said one cause at a time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the worker's fourth arm, container entries read whole under the archive's bound, the picker, and the wiring reading</name>
  <files>src/presentation/wx_app.rs, tests/wired.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:14436-14465` (`import_a_mailbox` and its picker), `:14619-14703` (`fill_folders_from`), `:15335-15373`, and the File builder's Import Mailbox item and its help string.
    - `tests/wired.rs:3500-3560` (`test_importing_mail_sends_each_kind_of_file_to_its_own_reader`).
    - `src/service/mailbox_archive.rs:700-800` (`one_entry_read_through`, `one_entry_read_in_pieces`).
  </read_first>
  <behavior>
    - `fill_folders_from`'s match gains `WhatWasChosen::AnOutlookMessage => return importing_messages::a_saved_outlook_message_brought_in(cache, account, at)`.
    - In the archive loop, an entry whose `read` is `OneSavedOutlookMessage` is read whole through `one_entry_read_through`, which the archive bounds by `most_one_thing_unpacks_to`, handed to `one_saved_outlook_message_in`, filed through `file_one_imported_message` when it is to be brought in, and counted with `count_one_saved_outlook_message`; a refusal as not an Outlook message at all adds one to the folder import's files that held no mail. After the loop the saved-message counts go onto `FoldersImported` beside the message count.
    - The picker's filter: "Mailboxes, saved messages and Outlook files (*.zip;*.eml;*.mbox;*.msg;*.pst)|*.zip;*.eml;*.mbox;*.msg;*.pst|All files (*.*)|*.*". The Import Mailbox item's help string names saved Outlook messages beside Outlook data files; its label and letter do not change.
    - `tests/wired.rs`'s `test_importing_mail_sends_each_kind_of_file_to_its_own_reader` reads the fourth arm reaching `a_saved_outlook_message_brought_in`, and reads the archive loop sending `OneSavedOutlookMessage` entries to `one_entry_read_through` and `one_saved_outlook_message_in`; its message names the kind whose route went missing. A body change: the file's count does not move.
  </behavior>
  <action>
    Red first, on a branch: the extended wiring reading, named bare,
    run before the red commit with the count check. Then the arm, the
    loop's branch, the filter and the help string, with `cargo build`
    between. Invoke `elegant-code`: the loop branches once on the entry's
    reading; the filing and counting are the application's, and the
    window only reads the entry and hands it over.

    Before the edit, probe the `wx_app.rs:14643-14650` anchor with the
    tomllib command, and after it, show it still occurs once. Guard
    record: one on `wx_app.rs` whose break reads a container entry in
    pieces like a mailbox file, the wiring reading red, anchored on the
    loop's new branch with its comment line above; measured. Every
    record the count check names re-measured with one call.
  </action>
  <verify>
    <automated>cargo test --test wired && cargo test --lib application::importing_messages::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the reading bare.
    - `grep -c 'WhatWasChosen::AnOutlookMessage' src/presentation/wx_app.rs` is 1; `grep -c '\*\.msg' src/presentation/wx_app.rs` is at least 1; `grep -c 'one_saved_outlook_message_in(' src/presentation/wx_app.rs` is 1.
    - `cargo test --test wired` passes with its total unchanged, quoted; one guard record measured and the `:14643` anchor shown to occur once, quoted.
  </acceptance_criteria>
  <done>Both import commands reach the saved-message reader: a chosen .msg through its own arm and every container inside a folder or zip through the archive loop, bounded, counted and said.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the changelog, the ledger, and the branch pushed for CI, NVDA and Accessibility</name>
  <files>docs/USER_GUIDE.md, docs/KEYBOARD_SHORTCUTS.md, docs/comparison.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/USER_GUIDE.md`'s Import and Export section as 13-45 and 13-46 left it; `docs/KEYBOARD_SHORTCUTS.md`'s Import Mailbox and Import a Folder of Messages rows (`:615-616` on 2026-09-24); `docs/comparison.md:98-114`; the gathered issue 53 paragraph in `docs/changelog.md`.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page's two import rows name a
    saved Outlook message (`.msg`) among what each takes. The guide's
    table row for Import Mailbox names `.msg`, and one short paragraph
    says a saved Outlook message is read into Imported like a saved
    message, that one found inside a folder or zip is read too, that
    appointments, contacts, tasks and notes saved as `.msg` are refused
    by name, and, beside the existing `.pst` warning in the same
    register, that no message saved by Outlook itself has been read yet
    and the closing sentence says so. The comparison page's sentence on
    what mail comes in from gains saved Outlook messages, corrected by
    dating in the page's manner.

    The changelog, `[Unreleased]`, Added: saved Outlook messages through
    both imports, #53 point 5, with decision 2's refusal by name and
    Pratik's answer on `cfb` from 13-47; Known limitations: no `.msg`
    written by Outlook has been read by the property mapping (the
    container reader opened four real ones in the research's probe),
    and formatting kept only in Outlook's own format arrives as plain
    words. One dated sentence at the end of the gathered issue 53
    paragraph saying point 5's reading landed.

    Ledger, by hand, both halves: one `unrun-verify` for Pratik's own
    files (RESEARCH-5's question 8): two or three messages saved from
    his Outlook, one with an attachment and one from a work address,
    imported singly and as a folder, and what arrived compared with
    Outlook; one `unrun-verify` for the tester's ear, the picker's
    filter and the closing sentence with its new-reader line heard.
    Nothing touches a server; nothing for phase 14.

    What is shown and spoken changes, so the branch is pushed with a
    pull request after the last branch commit, the three runs read, any
    red fixed on the branch, and then the local `git merge --no-ff`.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test docs_links && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.msg' docs/KEYBOARD_SHORTCUTS.md` is at least 2 and `grep -c '\.msg' docs/USER_GUIDE.md` at least 1.
    - Both ledger entries in both halves, quoted by number; carriage returns 0 on every file touched; no em or en dash; none of the six words.
    - The pull request's number and the three runs' conclusions quoted.
  </acceptance_criteria>
  <done>The pages say a saved Outlook message can be imported, what is refused and what nobody has tried, and Pratik has a ledger line for his own files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a chosen file or an archive entry to a reader | a stranger's bytes routed by how they begin |
| the reader's answer to a person's mailbox | what is filed and what is said about what was not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-48-01 | Denial of service | a huge container inside a zip or folder read whole | high | mitigate | `one_entry_read_through` under `most_one_thing_unpacks_to`, then 13-47's per-item limit on what the reader holds; a chosen file is read through seeking, never whole |
| T-13-48-02 | Tampering | a .msg routed to the archive reader or a .pst to the new one | medium | mitigate | routing from the first bytes with each signature asked before the mail question; cases for each; a guard record |
| T-13-48-03 | Repudiation | files, blind copies or signatures left in the file with nothing said | medium | mitigate | one sentence function both closing sentences call; a guard record on the blind-copy count; the new-reader sentence |
| T-13-48-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added here; cfb arrived in 13-47 on Pratik's answer; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the branch is pushed with a pull request and the three runs read before the merge; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite runs once, in 13-51's full gate; never `scripts/check.sh all` by hand here. After the merge, one commit on `main` touching only the summary adds the merge commit's mode line and stage line.
- Three guard records measured, the two anchors beside the new code re-read; every record the count check flagged re-measured with one call and read.
- What only Pratik settles: a `.msg` saved by his Outlook; what the tester settles: the filter and the sentences heard; what the tests prove: the routing, the counts, the one filing path, the wiring.
</verification>

<success_criteria>
- GAP-13's msg read has a named test and a reading in the summary for both halves (13-47's and this); the `[D]` line is left for 13-49; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (the roadmap's, and the phase README's if one exists); `.planning/STATE.md`'s plan number in the frontmatter and in the body; GAP-13's line and traceability row carrying a dated note that `.msg` reading is reachable from both imports.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-48-SUMMARY.md` when done
</output>
