---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 4
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - src/presentation/wx_reader.rs
  - src/presentation/wx_app.rs
  - src/application/printing.rs
  - src/presentation/page_jumps.rs
  - tests/print_is_on_the_file_menu.rs
  - tests/wired.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-01]
estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Print (Ctrl+P, letter P on its File menu) in the reader window prints the tab you are on: a message or a conversation with its header lines and full dates, an attachment read in a tab as it is shown (GAP-01, #45: 'the list, the open message window, the preview and the conversation window')."
    - "Ctrl+P on a conversation row in the message list prints the whole conversation, every message in order, each with its heading."
    - "Ctrl+P in the formatted conversation window prints the conversation it shows, through the same dialog, and says so once."
    - "In Contacts, Calendar, Tasks, Notes and Reminders, File, Print prints the item under the cursor with the fields its Shift+Space reading says, one to a line; nothing chosen is refused with that module's one refusal sentence; the print job is named by kind."
    - "The reader window's menu letters and keys are held by the same two readings that hold the main window's, each window read on its own."
  artifacts:
    - "src/presentation/wx_reader.rs: ID_PRINT on its File menu, what each tab prints, the handler."
    - "src/presentation/wx_app.rs: the paper composition handed to each reader tab, the conversation row printed whole, the five modules' items printed, the formatted conversation window's print answer."
    - "src/application/printing.rs: Kind for a module, the conversation on paper with each message's full date."
    - "src/presentation/page_jumps.rs: Ctrl+P posted as a print kind, read back by the window."
    - "tests/print_is_on_the_file_menu.rs and tests/wired.rs: the readings widened to the reader window and the five modules."
  key_links:
    - "open_in_the_text_reader -> reader.open with the document and printing::from_document(a_message_as_the_reader_shows_it(..., on_paper(out))) -> the reader's Ctrl+P -> ask_for_a_printer(reader frame) -> print: the tab prints what the main window composed for paper."
    - "Ctrl+P in the conversation page -> page_jumps::SCRIPT posts kind 'print' -> the_jump_the_page_asked_for -> Jump::Print -> the page's own conversation on paper -> print."
    - "File, Print in a module -> showing.item_kind() -> selected_row(list) -> the item's fields(out) -> printing::from_item -> print, or nothing_chosen(Thing::<kind>)."
---

<objective>
Print from every other surface that shows a message or an item: the
reader window, a conversation row, the formatted conversation window,
and the other five modules.

Purpose: #45 asks for File, Print "on a message in the list, the open
message window, the preview and the conversation window", and "In the
other modules, Print prints the open event, contact, task, note or
reminder the way it is shown." 13-03 printed the message under the
cursor in the list, which also covers the preview (the preview is kept
unfocusable, `wx_app.rs:1538`, so Print from the list is how it is
reached). This plan closes GAP-01's `[D]` line: "File, Print on the
mail surfaces and the other modules' items". It carries the reader
window and the conversation row that research K2 had placed in 13-03,
moved here so 13-03 could be one surface end to end.

Output: the reader window's Print, the conversation row and window, the
five modules, the readings widened, the pages, the changelog, the
ledger, GAP-01 ticked, #45 closed from the merge.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-1-keyboard-basics.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-03-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, before 13-01 to 13-03
land; every line number here is re-read at execution, since 13-03
changes `wx_app.rs` above most of them.

**1. The reader window's menu, letters and keys.**

```
sed -n 284,327p src/presentation/wx_reader.rs   -> File: "&Read Attachment	Ctrl+O" (the tab written as a literal character at :291, not "\t"), "&Save Attachment...\tCtrl+S", a separator, "&Close Tab\tCtrl+W", "Close &Window\tEsc"; Go: "&Next Message\tCtrl+Down", "&Previous Message\tCtrl+Up", a separator, "&Attachments\tAlt+A", "&Find\tCtrl+F"; the bar "&File", "&Go"
awk 'NR>=287 && NR<=301' src/presentation/wx_reader.rs | grep -o '"[^"]*&[^"]*"'   -> R, S, C, W on File; P is free there (Previous Message's P is on Go, a different menu)
grep -n 'ID_SAVE_ATTACHMENT\|ID_CLOSE_TAB' src/presentation/wx_reader.rs   -> ids at :31-37, the handler at :804 and :844
sed -n 60,75p src/presentation/wx_reader.rs   -> documents: Rc<RefCell<Vec<ReaderDocument>>>, one per tab; open() at :425 pushes at :556
```

Print goes after Save Attachment in a group of its own: "&Print...\tCtrl+P".
File's letters become R, S, P, C, W. The literal tab in Read
Attachment's label becomes "\t", the same character written the way
every other label writes it, so the key reading sees Ctrl+O.

**2. The readings that hold letters and keys read the main window only.**

```
sed -n 344,354p tests/wired.rs   -> test_no_two_menu_items_claim_the_same_shortcut reads src/presentation/wx_app.rs alone and splits a label on the escaped "\t"
sed -n 1977,1983p tests/wired.rs   -> test_no_two_items_on_one_menu_claim_the_same_letter reads wx_app.rs alone
```

Both are widened in place to read `wx_reader.rs` as well, each window
on its own (Ctrl+S is Save in one and Save Attachment in the other,
which is two windows and no collision). No test is added to `wired.rs`,
so its count stays and none of its 19 records is flagged by count. The
widening is proved by the guard records in task 1, whose breaks plant
a collision in the reader's menu that only a widened reading can see.

**3. Conversations, and the date each message is headed with.**

```
grep -n 'pub fn conversation(' src/presentation/reader_text.rs   -> :1139, conversation(subject, parts) with no Reading
sed -n 1164,1170p src/presentation/reader_text.rs   -> each part is headed "N. Message from <from>, <part.message.date>" with the date as the part carries it
grep -n 'date: node.date.clone()' src/presentation/wx_app.rs   -> in conversation_parts (:14184 onwards); ThreadNode.date is a String (wx_thread_view.rs:32)
sed -n 23945,23955p src/presentation/wx_app.rs   -> show_conversation_as_page already composes `let above = reader_text::conversation(subject, parts);` at :23953
```

The executor reads where `ThreadNode.date` is filled before writing
the conversation's paper form. If it is the stored form, the paper
composition writes each part's date through `date_display` with
`DateStyle::Absolute` inside `application::printing`, and the reader's
own heading is left as it is and ledgered as a finding, since changing
what the reader shows is not this plan's.

**4. The formatted conversation window's keys.**

```
grep -n 'wire_the_way_out(&page, "conversation window", PageKeys::TheWayOutAndTheJumps)' src/presentation/wx_app.rs   -> :24068; TheWayOutAndTheJumps is given only there (the separate page window takes TheWayOut, page_window.rs:228-232)
sed -n 32,64p src/presentation/page_jumps.rs   -> Jump { Attachments, Warning }; SCRIPT posts kind 'attachments' for Alt+A and 'warning' for F7; the_jump_the_page_asked_for reads them back; a test walks every kind the script posts through the reader
grep -n 'page_jumps::the_jump_the_page_asked_for' src/presentation/wx_app.rs   -> the window's handler at about :24140
sed -n 23985,23992p src/presentation/wx_app.rs   -> enable_browser_accelerator_keys(false): the browser's own Ctrl+P does not fire in that window
```

Ctrl+P joins `page_jumps::SCRIPT` as kind 'print', never with Alt held
(AltGr), and `Jump::Print` joins the enum; the window answers it by
printing its conversation. The existing walk over every kind the script
posts goes red when the script posts 'print' before the reader reads
it, which is the red half for free. The separate page window is not
given the jumps, so a web page opened from a link is not printed here,
and the guide says so.

**5. The five modules.**

```
sed -n 4323,4376p src/presentation/wx_app.rs   -> the module commands: `showing`, `module.item_kind()`, and `selected_row(&contact_list)` and its siblings per module
sed -n 1875,1975p src/presentation/wx_app.rs   -> wire_read_aloud's lookups: s.contacts.get(index) and its siblings, with a Reading built from s.dates and now
grep -n 'pub const CONTACT\|pub const EVENT\|pub const REMINDER\|pub const TASK\|pub const NOTE' src/application/status_sentences.rs   -> :64-68
```

**6. Records this plan touches, by both readings.** Counted with the
TOML reader on 2026-09-24 (1,088 records); re-take at execution, since
13-01 to 13-03 add records.

- By `tests_last_seen` and `file`: `src/presentation/wx_reader.rs` 2 (14
  tests); `src/presentation/wx_app.rs` 119 (199 tests);
  `src/presentation/page_jumps.rs` 2 (5 tests); `tests/wired.rs` 19 (77
  tests); `src/application/printing.rs` and
  `tests/print_is_on_the_file_menu.rs` as 13-02 and 13-03 leave them.
  One test is added to `page_jumps.rs` (Ctrl+P posted and read), so its
  2 records are flagged and re-measured; cases added to
  `tests/print_is_on_the_file_menu.rs` flag the records 13-03 wrote
  naming it, re-measured with the command the check prints. No test is
  added to `wx_app.rs`, `wx_reader.rs` or `wired.rs`.
- By anchor text: in `wx_reader.rs` one record, "the reader's
  attachments item is on alt+a, not back on f8" (:320, on Go, not
  touched); in `page_jumps.rs` two, "the page's script posts the
  attachments jump for alt+a, not nothing" (:50) and "the window reads
  the warning kind the page's script posts, not only the attachments"
  (:62): the new lines are added after the existing `if` blocks and
  match arms, not inside their anchored lines, and the anchor reading is
  re-run after the edit; in `wx_app.rs` around the conversation window
  (:23914-24200) two, "alt+a in the page window moves focus to the
  attachments list, not only says so" (:24141-24145) and "the browser is
  the first thing in the page window that takes the keyboard"
  (:23979-23982), neither inside the arm added.
</premise_corrections>

<decisions date="2026-09-24">
Taken 2026-09-24 from the research's recommendation (RESEARCH-1,
section 3); Pratik may overrule. Decisions 2, 3, 4, 6 and 7 of 13-03
hold here unchanged: plain text, no preview, the job named by kind, a
fixed 11 point, one sentence.

- **Discretion taken here:** a tab holding an attachment prints what the
  tab shows, since that is the document the person is reading; a
  message or conversation tab prints the paper composition the main
  window hands it, so its dates are full. A web page opened from a link
  is not printed and the guide says so (research's out-of-scope line).
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the reader window's Print, a conversation row printed whole, and the letter and key readings widened to the reader window</name>
  <files>src/presentation/wx_reader.rs, src/presentation/wx_app.rs, src/application/printing.rs, tests/print_is_on_the_file_menu.rs, tests/wired.rs, guards/guards.toml</files>
  <read_first>
    - Premises 1, 2 and 3 above; `13-03-SUMMARY.md` (the transport's names as built, the extracted composition's name).
    - `src/presentation/wx_reader.rs:40-80`, `:255-330`, `:420-560`, `:790-860`.
    - `src/presentation/wx_app.rs`: `open_in_the_text_reader` and `open_conversation` (at :14109 and :14169 before 13-03), `conversation_parts`, and the ID_PRINT arm 13-03 wrote.
    - `tests/wired.rs:340-420` and `:1970-2060` (the two readings and their helpers).
  </read_first>
  <behavior>
    - `ReaderWindow` keeps, per tab, an `Option<Printable>` beside its document; `open` takes it (or a sibling method does, whichever keeps the four call sites plain); a tab with none prints `printing::from_document` of its own document.
    - The reader's File menu: "&Print...\tCtrl+P" ("Print the message or attachment in this tab, with its header lines") after Save Attachment; its handler asks `ask_for_a_printer` with the reader's frame as owner, lays out, prints and says one sentence through the reader's own accessibility handle, the same three sentences 13-03 uses; Read Attachment's literal tab written "\t".
    - `open_in_the_text_reader` and `open_conversation` hand each tab its paper composition, through `on_paper(out)` for a message and through `printing`'s conversation form for a conversation (premise 3).
    - The main window's ID_PRINT arm prints a conversation row as the whole conversation (the nodes the row's Enter reaches, `conversation_parts`), replacing 13-03's one-message answer and its sentence.
    - The two `wired.rs` readings read `wx_reader.rs` as well, each window on its own, with no test added.
    - `tests/print_is_on_the_file_menu.rs` gains cases: the reader's File appends its print id with "&Print...\tCtrl+P"; its handler calls `ask_for_a_printer(` and `print(`; the reader tabs opened from the main window are handed a printable; companions plant a reader print item without its key and a tab opened with no printable, each refused.
  </behavior>
  <action>
    Red first, on a branch cut from `main`: the new cases in the
    reading, committed with `Fails-until-green:` naming them bare, and
    the count check's test named with them, since adding cases to a
    file 13-03's records name turns it red until those records are
    re-measured (the red gate and the count check collide, and the way
    through is to name the count check, `CLAUDE.md`). Then the tab's
    printable, the item and handler, the hand-over at the two open
    sites, the conversation row, and the widened readings, with
    `cargo build` between.

    Invoke `elegant-code`: the reader's handler and the main window's
    arm share the sentence functions and the transport; neither builds
    a sentence of its own. The mnemonic grep of premise 1 is re-run
    after the edit and quoted.

    Guard records, each measured by taking the break by hand: one on
    `wx_reader.rs` whose break writes the reader's Print as "P&rint...\tCtrl+P",
    claiming R, which Read Attachment holds (the widened letter reading
    red, and nothing red without the widening, which is what proves the
    widening reads the reader); one whose break gives the reader's Print
    `Ctrl+W`, Close Tab's key (the widened shortcut reading red); one on
    `wx_app.rs` whose break hands a message tab no printable (the
    reading's hand-over case red).
  </action>
  <verify>
    <automated>cargo test --test print_is_on_the_file_menu && cargo test --test wired && cargo test --lib application::printing::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the new cases bare and the count check's test, and the message says why.
    - The reader's File letters, read with premise 1's command, are R, S, P, C, W, each once, quoted.
    - `wired` passes with the count it held at the start of this plan, quoted before and after (77 at `630e2a67`; this plan adds no test to it); the reading passes with its count quoted.
    - Three guard records measured; the flagged records re-measured with the command the count check printed.
  </acceptance_criteria>
  <done>The reader window prints its tab with full dates, a conversation row prints the whole conversation, and the reader's letters and keys are held like the main window's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Print in Contacts, Calendar, Tasks, Notes and Reminders</name>
  <files>src/presentation/wx_app.rs, src/application/printing.rs, tests/print_is_on_the_file_menu.rs, guards/guards.toml</files>
  <read_first>
    - Premise 5 above; 13-02's `from_item` and the five `fields` methods (`src/presentation/read_aloud.rs`).
    - The ID_PRINT arm as task 1 left it, and 13-03's sentence for the other modules that this task replaces.
  </read_first>
  <behavior>
    - `printing::Kind::for_module(PimModule) -> Kind` is pure and tested for all six modules.
    - In a module other than Mail, File, Print takes `selected_row` of that module's list, the item from state the way `wire_read_aloud`'s lookup reads it, its `fields(on_paper(out))`, `from_item`, the dialog, the layout and `print` with `job_name(kind)`, then one sentence; no row is refused with `nothing_chosen(Thing::CONTACT)`, `Thing::EVENT`, `Thing::TASK`, `Thing::NOTE` or `Thing::REMINDER`. 13-03's messages-only sentence is gone.
    - The reading gains cases: the arm names `for_module(` and `from_item(`, and every module's list reaches it; a companion plants an arm that answers one module with the old sentence and is refused.
  </behavior>
  <action>
    Red first, on the branch: `for_module`'s case by module path and the
    reading's new cases bare, in one red commit under
    `Fails-until-green:`, with the count check's test named again if the
    added cases flag records. Then the mapping and the arm, with
    `cargo build` between. Invoke `elegant-code`: one match on the
    module picks the list and the item; the rest is the path Mail uses.
    No test is added to `wx_app.rs`.

    Guard record, measured: one on `wx_app.rs` whose break answers
    Calendar with the refusal of another kind (the reading's per-module
    case red), `suite` the reading.
  </action>
  <verify>
    <automated>cargo test --lib application::printing:: && cargo test --test print_is_on_the_file_menu</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the new cases.
    - `grep -c 'for_module(' src/presentation/wx_app.rs` is at least 1 and 13-03's messages-only sentence is found nowhere in `src/`, read with the sentence's own words in the summary.
    - Both commands pass, counts quoted; one guard record measured.
  </acceptance_criteria>
  <done>The item under the cursor prints in every module, with its fields one to a line, and nothing chosen is refused in that module's words.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ctrl+P in the formatted conversation window, the pages, the changelog, the ledger, GAP-01 ticked</name>
  <files>src/presentation/page_jumps.rs, src/presentation/wx_app.rs, guards/guards.toml, docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - Premise 4 above; `src/presentation/page_jumps.rs` whole; the conversation window's handler in `wx_app.rs` (`page.on_script_message_received`, about :24112-24170 before 13-03).
    - `tests/a_key_is_documented_where_the_surface_that_binds_it_is.rs:204-260` (why the jumps' keys belong in the reader window's section) and `tests/attachments_are_reached_with_alt_a_in_both_views.rs:278-400`.
    - `docs/KEYBOARD_SHORTCUTS.md:208-360` (The Reader Window and its subsections) and the Printing section 13-03 wrote in `docs/USER_GUIDE.md`.
  </read_first>
  <behavior>
    - `page_jumps::SCRIPT` posts `{ kind: 'print' }` for Ctrl+P (`e.ctrlKey && !e.altKey`, 'p' or 'P'), taken from the browser first; `Jump::Print`; `the_jump_the_page_asked_for` reads it; a case asserts the script posts it and the reader reads it.
    - The conversation window answers `Jump::Print` by printing its conversation's paper form (task 1's conversation form, from the parts the window was built with), owner the window's frame, one sentence.
  </behavior>
  <action>
    Red first, on the branch: the script's new listener lands with the
    new case, and the existing walk over every kind the script posts
    goes red because the reader does not read 'print' yet; the red
    commit names both by module path
    (`presentation::page_jumps::tests::<name>`) and the count check's
    test, since the added case flags `page_jumps.rs`'s 2 records. Then
    `Jump::Print`, the reader's arm and the window's answer, with
    `cargo build` between; the two flagged records re-measured.

    Guard record, measured: one on `page_jumps.rs` whose break stops the
    reader reading 'print' (both cases red).

    Invoke `writing-craft` for the pages. `KEYBOARD_SHORTCUTS.md`: The
    Reader Window gains Print (`Ctrl+P`) for the reader and for the
    formatted conversation window, and the File Menu row 13-03 wrote
    loses its "follow" clause and says Print works on the item you are
    on in every module. The guide's Printing section says what each
    surface prints, that a conversation prints every message, that an
    attachment prints as shown, and that a web page opened from a link
    is not printed here. The changelog, `[Unreleased]`: 13-03's entry
    widened to every surface, Known limitations kept. Ledger, by hand,
    both halves: an `unrun-verify` for the reader window's Print and the
    conversation window's Ctrl+P heard under NVDA (the sentence, focus
    back on the tab or the page), and a `todo` for the conversation
    heading's date if premise 3 found the stored form reaching the
    reader.

    `REQUIREMENTS.md`: GAP-01's `[D]` line and box ticked with the date
    and each named test (13-02's cases, 13-03's three targets, this
    plan's reading and page-jump cases); its `[S]` lines untouched; the
    traceability row updated. After the merge, the executor closes #45
    from the merge commit with the surfaces named and the limitations
    listed; closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --lib presentation::page_jumps:: && cargo test --test attachments_are_reached_with_alt_a_in_both_views && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test wired && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the new case, the walk and the count check's test.
    - `grep -c "kind: 'print'" src/presentation/page_jumps.rs` is 1; `presentation::page_jumps::` passes with the count it held at the start of this plan plus the one this plan adds, quoted before and after (5 at `630e2a67`, so 6).
    - `awk '/^### The Reader Window/,/^### The Preview Pane/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'Ctrl+P'` is at least 1.
    - GAP-01's `[D]` line and box ticked, each named test found by `grep -rn` in `src` and `tests`; the ledger entries in both halves; carriage returns 0 on every file touched, measured with `tr -cd '\r' < FILE | wc -c`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>Every surface that shows a message or an item prints it, the pages say what each prints, and GAP-01's structural half is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a page's script to the host | a posted kind from a document holding a stranger's markup |
| an item's fields to paper | a contact's or event's text leaving the machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Spoofing | a message's own script posting kind 'print' to make the window print | low | accept | a message's markup is sanitised with `ammonia` before the page shows it (CLAUDE.md, the WebView paragraph), which removes its scripts, and the executor confirms that on the conversation window's composition; the worst a forged post could do is open the print dialog, which asks the person before anything leaves |
| T-13-13 | Information disclosure | the job name in a shared queue | medium | mitigate | `job_name(kind)` only, never a subject or a contact's name |
| T-13-14 | Denial of service | Ctrl+P taken from a language layout where Ctrl+Alt is AltGr | low | mitigate | the listener requires Ctrl without Alt, as the Alt+A listener requires Alt without Ctrl |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | nothing is added to the manifest; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the merge with `git merge --no-ff` runs what the branch's whole diff earns, and the whole suite runs once, in 13-51's full gate; never `scripts/check.sh all` by hand here.
- This plan changes what is spoken and shown, so its branch is pushed and a pull request opened under Pratik's standing OK, and the merge waits for CI, NVDA and Accessibility on it, each verdict read and quoted.
- Five guard records measured; every record the count check flagged re-measured with the command it printed.
- What only a person settles: the dialog and the sentence heard from each surface, a page looked at. What the readings and cases prove: every surface reaches the transport, the letters and keys, the page's print key.
</verification>

<success_criteria>
- Phase 13's success criterion 1 holds structurally: File, Print (Ctrl+P) prints the message or the item under the cursor through the native dialog, the header lines and the text, on every surface that shows one.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list in `ROADMAP.md`, ticked; `STATE.md`'s plan number in its frontmatter (`current_plan`) and in its body (`Current Plan:`); GAP-01's `[D]` line, box and traceability row in `REQUIREMENTS.md`, ticked with the date.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-04-SUMMARY.md` when done
</output>
