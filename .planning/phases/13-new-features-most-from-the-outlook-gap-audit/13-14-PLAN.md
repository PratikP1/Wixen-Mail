---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 14
type: execute
wave: 14
depends_on: [13-13]
files_modified:
  - src/service/signed_mail.rs
  - src/application/encrypted_mail.rs
  - src/application/reading_a_message.rs
  - src/application/meeting_changes.rs
  - src/presentation/reader_text.rs
  - src/presentation/html_renderer.rs
  - src/presentation/wx_app.rs
  - tests/an_encrypted_message_is_not_left_unexplained.rs
  - guards/guards.toml
  - .cargo/audit.toml
  - docs/privacy.md
  - docs/comparison.md
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-05]
estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An S/MIME encrypted message addressed to a certificate whose private key is in the Windows store opens: CryptDecryptMessage undoes the envelope where the key lives, the inner MIME is parsed in memory, and the reader shows its words and its parts (GAP-05, #52 point 1)."
    - "Four outcomes, each its own sentence: opened; not addressed to a certificate here; the key here refused; damaged. None carries text from Windows or from the message."
    - "An envelope that wraps a signature hands the inner signed message to examine_signed_message, and its verdict is said as any signature's is."
    - "Nothing decrypted is written to the message cache: the stored body, parts and marks are the same bytes after opening as before; it is decrypted each time it is opened, and the pages say search does not look inside encrypted mail."
    - "A page holding decrypted content never fetches a remote picture, whatever the picture setting says, and decrypted content is never joined with the message's clear parts into one page section (EFAIL, and the RSA advisory's expiry condition)."
    - "An invitation inside decrypted content is said and offered the answer buttons, and opening it never changes the calendar on its own."
    - "Opening encrypted mail says it is experimental where the person reads the outcome, and on the alpha testing page."
  artifacts:
    - "src/service/signed_mail.rs: CertificateStore::open_the_envelope, the Windows implementation through CryptDecryptMessage in the file's own #[link] block, WhatTheEnvelopeHeld, an OpenSSL-made envelope to the keyholder certificate with the command in its comment."
    - "src/application/encrypted_mail.rs and reading_a_message.rs: the envelope opened on reading, the body and parts in memory, the four sentences, the inner signature checked."
    - "src/presentation/html_renderer.rs and reader_text.rs: fetching held back for decrypted content; wx_app.rs: the open path passing decrypted provenance so 13-13's change refuses it."
    - ".cargo/audit.toml: the narrowing fact that said S/MIME decryption is refused rewritten."
  key_links:
    - "stored envelope -> encrypted_mail::for_message -> this_computers_certificates().open_the_envelope(der) -> CryptDecryptMessage with the MY store -> inner bytes -> service::mime::parse in memory -> WhatAMessageShowsAndSays.body and parts -> the reader; nothing back to the cache."
    - "decrypted provenance -> Fetching held back in the page -> no request whose presence says 'this decrypted'; -> meeting_changes refuses -> no calendar change that says it either."
---

<objective>
S/MIME encrypted mail opens with a key in the Windows certificate store,
decrypted each time it is opened and never stored.

Purpose: GAP-05's reading half for S/MIME and #52 point 1. The comment at
`signed_mail.rs:3225-3236` refuses decryption because it "cannot be tested
without a real certificate installed"; the research's probe showed
`CryptDecryptMessage` opening OpenSSL envelopes with a key imported into
memory only, which is the technique the tree's own tests already use
(`holding_only_in_memory`), so it can be tested now. Decrypted mail reopens
the argument `.cargo/audit.toml` makes about the RSA advisory, so the rules
that keep decryption from becoming an observable signal land in the same
plan.

Output: the Windows call and its four outcomes, the fixture, the reading
path, the picture rule, the provenance refusal, the audit entry, the pages,
the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-13-SUMMARY.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said.

**1. How this file talks to Windows, which corrects the research.**

```
sed -n 2813,2819p src/service/signed_mail.rs   -> "Flat calls behind a small #[link] block, which is how the rest of this project talks to Windows outside the spell checker."
sed -n 3031,3068p src/service/signed_mail.rs   -> #[link(name = "crypt32")] declares CertCreateCertificateContext, CertGetCertificateChain, CertOpenSystemStoreW, CertEnumCertificatesInStore, CertGetCertificateContextProperty, and PFXImportCertStore under #[cfg(test)]
grep -n '"Win32_Security_Cryptography"' Cargo.toml   -> :424: the windows crate has the feature too
```

The research said `CryptDecryptMessage` goes "through the `windows` crate
(feature already on)". The file's own rule is flat calls in its `#[link]`
block, so `CryptDecryptMessage` and its `CRYPT_DECRYPT_MESSAGE_PARA` are
declared there beside the calls it already makes. Both reach the same
function in `crypt32.dll`; this keeps one way of calling Windows in one
file, and changes no manifest line.

**2. What refuses today, and the fixtures.**

```
sed -n 2735,2740p src/service/signed_mail.rs   -> the trait's fn unwrap_content_key(&self, recipient: &Recipient) -> Result<Vec<u8>>
sed -n 3225,3237p src/service/signed_mail.rs   -> the Windows impl refuses: "Wixen Mail cannot open an S/MIME encrypted message yet ... that part is not built."
grep -n "fn unwrap_content_key(&self, _recipient" src/service/signed_mail.rs   -> :2790 as well, the other implementation, which refuses too
grep -n "fn holding_only_in_memory\|const A_KEY_AND_ITS_CERTIFICATE\|const THE_TEST_KEYS_PASSWORD\|pub(super) const ENCRYPTED_TO_ALICE" src/service/signed_mail.rs   -> :3129, :4491, :4545 ("wixen-test"), :4120
```

`ENCRYPTED_TO_ALICE` cannot be the fixture: it is addressed to serial
`0x24738CFA8319EFF0BC4F0DFFBD0831BE5865833C` and the keyholder certificate
is `52C0434CEA86BF6FA0E8EBA9DEA1A2BDE46E6D8F`; OpenSSL refused to open one
with the other (research, measured). Task 1 makes a new envelope with
OpenSSL to the keyholder certificate, and writes the command in the
fixture's comment. The research's probe also showed Windows opening
AES-256-CBC, RSA-OAEP and `authEnvelopedData` envelopes; the fixture is
the first, and the summary says which others were tried.

**3. What must stay true for the RSA advisory.**

```
sed -n 131,136p .cargo/audit.toml   -> "Two things narrow it further and both are already true. S/MIME decryption is refused ... And PGP private key import is labelled experimental in the File menu"
sed -n 150,157p .cargo/audit.toml   -> the expiry: "a background fetch whose presence or timing depends on whether a body opened. Any of them."
sed -n 168,179p .cargo/audit.toml   -> pictures are fetched by default since #28; a PGP body that opens is text, so it points at nothing; "make an opened PGP body a MessageBody::Html, and the fetch of its pictures says 'this decrypted'"
grep -n "fn what_the_settings_say" src/presentation/html_renderer.rs   -> :308: Fetching from hold_back_remote_pictures, read on use
sed -n 457,461p docs/privacy.md   -> "a PGP message that opens is shown as its words, as text, so it points at no picture a browser could fetch"
```

S/MIME decryption goes through Windows CNG, not the `rsa` crate the
advisory is about, so the first narrowing fact is rewritten rather than
deleted: S/MIME decryption now happens, outside the advisory's crate, and
under the same rules (no picture fetched, no calendar change, no receipt
depending on it). A decrypted S/MIME body is usually HTML, which is exactly
the line the advisory names, so the picture rule is built here and not
left to 13-15.

**4. The gate for each file**, by the TOML reader on 2026-09-24:
`signed_mail.rs` 113 tests, 5 and 5; `encrypted_mail.rs` 7, 0 and 0;
`reading_a_message.rs` 7, 5 and 1 (plus 13-10's); `reader_text.rs` 125, 13
and 11; `html_renderer.rs` 92, 10 and 10; `meeting_changes.rs` 13-13's;
`tests/an_encrypted_message_is_not_left_unexplained.rs` 10 tests, 2 records
naming it. `docs/privacy.md` is the `file` of one record, at `:89`, "a page
cannot restate a constant with a figure the code does not hold" (suite
`every_number_carries_its_command_and_its_date`); the edit is the paragraph
at `:450-464`, not `:89`.

**5. The anchors inside what this plan edits.** `signed_mail.rs:122-128`,
`:1391-1397`, `:1905-1908`, `:3383`, `:3441`: none lies in the trait
(`:2717-2740`), the other implementation (`:2785-2795`), the `#[link]`
block (`:3031-3068`) or the refusing method (`:3225-3237`) this plan edits;
each named in the summary as untouched. `html_renderer.rs`'s ten anchors:
the executor reads whether `what_the_settings_say` (`:308-331`) holds any;
the locator found none there on 2026-09-24.
</premise_corrections>

<decisions date="2026-09-24">
**1. Decrypted mail is never stored.** Taken 2026-09-24 from the research's
recommendation (question 4); Pratik may overrule: decrypt each time it is
opened, never store the decrypted text, so an encrypted message stays
encrypted at rest, and say plainly that search does not look inside
encrypted mail.

**2. Reading before the key manager.** Taken 2026-09-24 from the research's
recommendation (question 5), confirmed by Pratik's order of the same day:
reading encrypted mail first (it needs one key), then the manager, then
signatures and sending.

**3. Invitations inside encrypted mail.** Taken 2026-09-24 from the
research's recommendation (question 9); Pratik may overrule: shown and
offered the answer buttons, never changing the calendar on their own.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CryptDecryptMessage behind the certificate store, four outcomes, and an envelope made for the keyholder</name>
  <files>src/service/signed_mail.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/signed_mail.rs:2700-2800` (the trait and the other implementation), `:2813-2900` (`windows_store`'s constants), `:3020-3080` (the `#[link]` blocks), `:3110-3160` (`holding_only_in_memory` and `PFXImportCertStore`'s flags), `:3200-3240` (`which_recipient_is_us`, the refusal), `:3731-3830` (`EncryptedMessage::read`, `spoken`).
    - `src/service/signed_mail.rs:4480-4560` (the keyholder fixture and password) and `:1861` (`examine_signed_message`).
    - `~/.cargo/registry/src/*/windows-0.62.2/src/Windows/Win32/Security/Cryptography/mod.rs:8579-8584` for `CRYPT_DECRYPT_MESSAGE_PARA`'s layout, read and not used.
  </read_first>
  <behavior>
    - The trait's `unwrap_content_key` becomes `open_the_envelope(&self, envelope_der: &[u8]) -> WhatTheEnvelopeHeld`, with `WhatTheEnvelopeHeld { Opened(Vec<u8>), NotAddressedToACertificateHere, TheKeyHereRefused, Damaged }`; the other implementation answers `TheKeyHereRefused` with no platform text.
    - Windows: `CryptDecryptMessage` declared in the `crypt32` block with `CryptDecryptMessagePara { size, encoding, store_count, stores }`, called twice (size, then bytes) against the store the implementation was built on (the person's `MY` store in the program, the in-memory store in tests); its failure read with `GetLastError` and mapped: `CRYPT_E_NO_DECRYPT_CERT` (`0x8009200C`) to not addressed here, `NTE_*` and key-access codes to the key refusing, ASN.1 and message-type codes to damaged; the codes each fixture really produced quoted in the summary.
    - The fixture: an envelope made on this machine by `openssl smime -encrypt -aes256 -binary -outform DER` with the keyholder certificate taken out of `A_KEY_AND_ITS_CERTIFICATE`, over a short `text/plain` MIME entity, kept in the test module as base64 with the commands in its comment.
    - `test_an_envelope_made_for_the_keyholder_opens_with_the_key_held_in_memory`: the bytes that come back are the MIME entity OpenSSL was given, byte for byte. Its companion: the same envelope offered to a store holding no key for it answers `NotAddressedToACertificateHere`. `ENCRYPTED_TO_ALICE` offered to the keyholder's store answers the same. One flipped byte in the fixture's ciphertext answers `Damaged` or `TheKeyHereRefused`, never `Opened`, and the case says which it measured. A second fixture, an envelope wrapping a signed message, opens to bytes `examine_signed_message` reads as signed.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path
    (`service::signed_mail::tests::<name>`, or the Windows module's test
    path the file uses) and the count check, since five records name
    `signed_mail.rs`. Then the declarations, the method and the mapping,
    with `cargo build` between. Invoke `tdd` and `elegant-code`: the unsafe
    call in one function with its buffer sizing, the mapping a match on
    codes, no `unwrap` outside tests, never a Windows message string in an
    outcome. The comment at `:3225-3236` is replaced by one saying how it
    is tested now.

    Guard records, measured: one whose break maps `CRYPT_E_NO_DECRYPT_CERT`
    to `TheKeyHereRefused`, the companion red; one whose break returns the
    first call's empty buffer, the opened case red.
  </action>
  <verify>
    <automated>cargo test --lib service::signed_mail::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path and the count check.
    - `grep -c 'fn CryptDecryptMessage' src/service/signed_mail.rs` is 1; `grep -c 'cannot open an S/MIME encrypted message yet' src/service/signed_mail.rs` is 0: the refusal at `:3233` and the test at `:5603` that asserts it are both replaced, the test by the opened case.
    - `cargo test --lib service::signed_mail::` passes with more than 113 tests, quoted.
    - The fixture's OpenSSL commands and the error codes measured are in the summary.
  </acceptance_criteria>
  <done>The Windows certificate store opens an envelope addressed to a key it holds, says which of three other things happened when it cannot, and is tested with a key that never touches the person's store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: opened on reading, parsed in memory, never stored, no picture fetched, no calendar changed</name>
  <files>src/application/encrypted_mail.rs, src/application/reading_a_message.rs, src/application/meeting_changes.rs, src/presentation/reader_text.rs, src/presentation/html_renderer.rs, src/presentation/wx_app.rs, tests/an_encrypted_message_is_not_left_unexplained.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/encrypted_mail.rs` whole (the module comment "Nothing here decrypts anything" at `:11`, `WhatTheEnvelopeSays`, `for_message`).
    - `src/application/reading_a_message.rs:120-205` and 13-10's `invitation_check_for`.
    - `src/presentation/html_renderer.rs:295-335` (`what_the_settings_say`) and where a page's parts reach it.
    - `src/application/meeting_changes.rs` (13-13) and where `wx_app.rs`'s open path calls it.
    - `tests/an_encrypted_message_is_not_left_unexplained.rs` whole.
  </read_first>
  <behavior>
    - `WhatTheEnvelopeSays` gains `Opened { said, body: MessageBody, parts }`, `NotAddressedHere { said }`, `TheKeyRefused { said }`, `Damaged { said }` beside the existing variants; `for_message` opens the envelope `the_envelope_it_carried` returns, parses the bytes with `service::mime::parse`, and, when they are a signed message, checks them with `examine_signed_message` so the signature is said.
    - Sentences: "This message was encrypted to your certificate and was opened here. Opening encrypted mail is experimental."; "This message is encrypted to a certificate this computer does not hold a key for, so it cannot be opened here."; "This message is encrypted to a certificate on this computer, and Windows would not let its key be used, so it was not opened."; "This message is encrypted, and what arrived is damaged, so it cannot be opened."
    - `reading_a_message::put_together` hands on the opened body as the body to show, as PGP's opened words are; the parts go to the document in memory; 13-10's invitation decision is asked of the decrypted parts too, marked as found inside encrypted mail.
    - `meeting_changes` gains `Why::InsideEncryptedMail`, answered before anything else for an invitation found there; the open path passes that provenance.
    - The page: a page holding any part whose body came out of decryption is built with remote pictures held back whatever `hold_back_remote_pictures` says; decrypted content is its own section and never joined with the clear parts' sections.
    - Nothing is written: after opening, the cache's stored body, attachments and marks for the message read back byte for byte as before.
    - Cases: `for_message` over a temp cache holding the task 1 fixture as the arrived envelope, with the in-memory store, answers `Opened` with the entity's words; the other three outcomes; the body stored before and after is equal; the page for an opened HTML fixture carries no remote `src` and says the pictures were held back; an opened envelope carrying a `REQUEST` gives `Why::InsideEncryptedMail`. The target gains a reading that the opened sentence is above the body and above "More about this signature:".
  </behavior>
  <action>
    Red first, on a branch: the cases by module path, the target's new
    readings bare, and the count check. Then the variants, the reading, the
    picture rule and the provenance, with `cargo build` between. The
    in-memory store reaches `encrypted_mail` through the seam the module
    comment describes (a function that takes a `&dyn CertificateStore`),
    not by a test-only branch in production code.

    Guard records, measured: one on `html_renderer.rs` whose break lets the
    setting decide for decrypted content, the no-remote-`src` case red; one
    on `encrypted_mail.rs` whose break writes the opened body to the cache,
    the before-and-after case red; one on `meeting_changes.rs` whose break
    drops the provenance check, its case red.
  </action>
  <verify>
    <automated>cargo test --lib application::encrypted_mail:: && cargo test --lib application::reading_a_message:: && cargo test --lib application::meeting_changes:: && cargo test --lib presentation::html_renderer:: && cargo test --lib presentation::reader_text:: && cargo test --test an_encrypted_message_is_not_left_unexplained</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path, the target's readings bare, and the count check.
    - `grep -c 'Nothing here decrypts anything' src/application/encrypted_mail.rs` is 0; `grep -c 'InsideEncryptedMail' src/application/meeting_changes.rs` is at least 2.
    - The six runs pass, counts quoted.
  </acceptance_criteria>
  <done>An S/MIME message addressed to a key here reads as its words and parts, the three other outcomes each say what happened, nothing decrypted is stored, and nothing about its opening leaves the machine or changes the calendar.</done>
</task>

<task type="auto">
  <name>Task 3: the advisory entry, the pages, the changelog and the ledger</name>
  <files>.cargo/audit.toml, docs/privacy.md, docs/comparison.md, docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.cargo/audit.toml:77-203`.
    - `docs/privacy.md:440-465`; `docs/comparison.md:46`; `docs/ALPHA_TESTING.md` for where experimental features are listed.
    - `.planning/WINDOWS.md` rows 141 and 143.
  </read_first>
  <action>
    Invoke `writing-craft`. `.cargo/audit.toml`: the narrowing paragraph at
    `:131-136` rewritten to say S/MIME decryption now happens through
    Windows' own CNG, which is not the `rsa` crate this advisory is about,
    and is held to the same rules: no picture fetched for decrypted
    content, no calendar change from it, and the read receipt still decided
    from the header alone; the line the condition turns on names
    `html_renderer`'s rule for decrypted content beside
    `receipt_for_the_open_message`. `scripts/audit.test.sh`'s cases read
    this file; the executor runs them.

    `docs/privacy.md`: the paragraph at `:450-464` says decrypted mail is
    decrypted each time it is opened and never stored, search does not look
    inside it, and a page holding it fetches no picture. `docs/comparison.md:46`:
    S/MIME-encrypted mail is read, inline PGP opens, nothing goes out
    signed or encrypted yet (13-21 changes it again). `docs/USER_GUIDE.md`: a
    `### Signed and encrypted mail` subsection under Reading and Managing
    Email with the four sentences and what each asks you to do.
    `docs/ALPHA_TESTING.md`: opening S/MIME mail is experimental and has met
    no message from Outlook or Thunderbird. The changelog under
    `[Unreleased]`, Added, #52 point 1; Known limitations.

    Ledger, by hand, both halves: rows 141 and 143 advanced (an envelope
    from OpenSSL now opens with an in-memory key; a real envelope from
    Outlook or Thunderbird and a real certificate in the person's store are
    still unmet); one `unrun-verify` for phase 14: a key marked for strong
    protection or on a smart card, where Windows shows its own prompt.
  </action>
  <verify>
    <automated>bash scripts/audit.test.sh && cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The old claim is gone across line breaks: `tr -s '\n#' '  ' < .cargo/audit.toml | tr -s ' ' | grep -c 'S/MIME decryption is refused'` is 0, and the same pipeline with `'S/MIME decryption now happens'` is 1 (the joined reading, because the comment wraps; CLAUDE.md on absence checks).
    - `grep -c 'there is no OpenPGP at all' docs/comparison.md` is 0; `grep -c '^### Signed and encrypted mail' docs/USER_GUIDE.md` is 1.
    - Ledger in both halves; no em dash or en dash; none of the six words; carriage returns 0.
  </acceptance_criteria>
  <done>The advisory's argument says what is true now, the pages say where decrypted mail lives and what it never does, and the ledger says what only a real correspondent can prove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stranger's envelope to Windows' key | decryption with the person's private key |
| decrypted content to the page | untrusted HTML that has just become readable |
| decryption to anything outward | a request or change whose presence depends on success |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-14-01 | Information disclosure | EFAIL: decrypted HTML fetching a remote picture | high | mitigate | pictures held back for any page holding decrypted content whatever the setting; decrypted content never joined with clear parts; a guard record |
| T-13-14-02 | Information disclosure | a decryption oracle through a calendar change | high | mitigate | `Why::InsideEncryptedMail` answered first; a guard record |
| T-13-14-03 | Information disclosure | decrypted text at rest | high | mitigate | nothing written to the cache; a before-and-after case and a guard record |
| T-13-14-04 | Information disclosure | Windows' or the message's text in an outcome | medium | mitigate | four fixed sentences; the mapping carries codes, never strings |
| T-13-14-05 | Denial of service | a damaged or huge envelope | medium | mitigate | the arrival path's 25 MB ceiling on kept bytes stands; `CryptDecryptMessage`'s failure is an outcome, never a panic |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate and no manifest line; the call is declared in the file's own `#[link]` block |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass. `scripts/check.sh` through the hook on every commit, never piped; no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the pages, the audit entry, the ledger, the summary and the four completion marks in one documents commit before the merge; `git merge --no-ff`.
- Spoken or shown: yes. The branch is pushed and a pull request opened after its last commit, and `ci.yml`, `nvda.yml` and `accessibility.yml` are read green before the merge; `main` is not pushed. `cargo audit` runs in CI's Security Audit on that pull request and reads the rewritten entry.
- What the tests prove: the Windows call with an in-memory key, four outcomes, nothing stored, no picture, no calendar change. What they cannot: a real envelope from another program and a real certificate, on the ledger for phase 14.
</verification>

<success_criteria>
- GAP-05's "S/MIME-encrypted ... mail is read" has a named test in the summary; the box waits for 13-21.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-05's traceability row naming this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-14-SUMMARY.md` when done
</output>
