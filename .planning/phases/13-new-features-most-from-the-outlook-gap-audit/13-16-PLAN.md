---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 16
type: execute
wave: 16
depends_on: [13-15]
files_modified:
  - src/service/secret_store.rs
  - src/service/pgp/mod.rs
  - src/service/pgp/keys.rs
  - src/data/message_cache/mod.rs
  - src/data/message_cache/pgp_keys.rs
  - src/application/pgp_keys.rs
  - src/application/mod.rs
  - src/application/forget.rs
  - guards/guards.toml
  - docs/privacy.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-03]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The test credential store refuses what the Windows store refuses, a secret over 2,560 bytes as UTF-16, so the defect that an ordinary RSA key cannot be stored on a real Windows machine is red in a test before it is fixed (research finding 1)."
    - "A private key is stored across fixed-name entries under wixen-mail-pgp, a fixed number of keys each in a fixed number of parts of at most 1,280 characters, so an RSA key with a subkey is stored and opens mail; keyring_entries names every one of those entries and the old private-key entry without reading anything, so uninstalling still erases exactly what was written (GAP-03)."
    - "An older machine's single private-key entry is moved into the new entries on first use and still named for erasing forever."
    - "Several private keys are held and a message is opened by whichever one it was encrypted to."
    - "Correspondents' public keys are kept in an additive table in the mail database, because they are not secret; the database gains nothing secret."
    - "One listing, in the project's words, of every key here: user ids, key id, fingerprint, created, expires, private or public, can encrypt, can sign; import from armoured text holding one key or many; remove by fingerprint; export a key's public half as armour. Nothing outside src/service/pgp/ names the crate."
  artifacts:
    - "service/secret_store.rs: the test backing's size refusal, worded as the real store's."
    - "service/pgp/keys.rs and mod.rs: KEY_SLOTS and PARTS_PER_KEY, the split write and joined read, the migration, keyring_entries, open trying each key, KeyListing, import_keys, remove_key, public_half."
    - "data/message_cache/pgp_keys.rs: pgp_public_keys, CREATE TABLE IF NOT EXISTS, keep, list, forget."
    - "application/pgp_keys.rs: every_key_here and the import, remove and export the manager will call."
  key_links:
    - "an armoured file -> application::pgp_keys::import -> service::pgp::import_keys -> private: split into key-N-part-M entries in the credential store; public: data::message_cache::pgp_keys -> every_key_here lists both."
    - "service::pgp::keyring_entries -> application::forget::entries_for -> uninstall: every fixed name, read nothing."
---

<objective>
Keys that fit Windows' credential store, several of them, and other
people's public keys.

Purpose: GAP-03's storage premise, and the research's first finding.
`keyring` 4.1.5 on Windows refuses a secret over 2,560 bytes as UTF-16
(1,280 characters), and the tree's own GnuPG RSA-2048 fixture is 1,836
characters armoured, so importing it on a real Windows machine answers "Could
not be stored" and no test sees it, because the test store takes anything.
The key manager (13-17) cannot list several keys until keys can be kept.
This plan is the service and data half; nothing new is shown until 13-17.

Output: the honest test store, the split storage and its uninstall list,
several private keys, the public key table, the listing, import, remove and
export, the privacy page, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-2-reading-mail.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what it
said.

**1. The limit, and the store that hides it.**

```
windows-native-keyring-store-1.1.0/src/cred.rs:82-83 and src/utils.rs:79-94   -> set_password encodes as UTF-16 and refuses when blob.len() > CRED_MAX_CREDENTIAL_BLOB_SIZE (research, read from the vendored source)
windows-sys Security/Credentials/mod.rs:378   -> CRED_MAX_CREDENTIAL_BLOB_SIZE is 2560
sed -n 133,143p src/service/secret_store.rs   -> the test backing's write inserts whatever it is given: no limit
python over keys.rs:231 and :269 (research)   -> Alice 1,836 characters armoured, 3,672 bytes as UTF-16; Bob 1,832 and 3,664
```

**2. One key, one entry, today.**

```
grep -n "pub const KEYRING_SERVICE\|pub const KEYRING_PRIVATE_KEY\|pub fn keyring_entries" src/service/pgp/mod.rs   -> :74 "wixen-mail-pgp", :90 "private-key", :102 one pair
sed -n 57,89p src/service/pgp/keys.rs   -> import refuses public keys (:69) and locked keys (:72), then secret_store::write(KEYRING_SERVICE, KEYRING_PRIVATE_KEY, armoured) at :80
sed -n 45,54p src/application/forget.rs   -> entries_for asks pgp::keyring_entries(), so the uninstaller names whatever that returns
grep -n "fn test_no_caller_outside_this_module_names_the_crate" src/service/pgp/mod.rs   -> the guard that keeps the crate inside src/service/pgp/
```

**3. The sizes to design for.** The fixtures are RSA-2048 primary keys with
no subkey. A usual GnuPG key has an encryption subkey and is roughly twice
that; RSA-4096 larger again; Ed25519 with Curve25519 is a few hundred bytes
(research, `[DERIVED]`, and measuring failed there because `gpg-agent`
refused the scratchpad's long path). Task 1 measures first with a short
`GNUPGHOME` (`C:/g13`): an RSA-4096 key with an RSA-4096 subkey exported
armoured, and an Ed25519 key, and sets `PARTS_PER_KEY` from the larger with
room over it; the sizes and commands go in the constant's doc comment with
the date.

**4. The gate for each file**, by the TOML reader on 2026-09-24:
`secret_store.rs` 6 tests, 2 and 1; `pgp/keys.rs` 11, 0 and 0; `pgp/mod.rs`
6, 0 and 0; `forget.rs` 24, 1 and 1; `data/message_cache/mod.rs` 23, 12 and
12; the two new modules 0. Anchors: `secret_store.rs:41-43` ("the first
credential entry of a process sets the store up", suite
`the_credential_store_is_ready_for_every_thread`) is `open_entry`, not the
test backing's `write` this plan edits; `forget.rs:52-54` ("an owner of
credential store entries that uninstalling never names") is the loop over
`pgp::keyring_entries()`, not edited; `docs/privacy.md:89` is not the
paragraph edited. Each named in the summary as untouched.
</premise_corrections>

<decisions date="2026-09-24">
**1. Where private keys live.** Taken 2026-09-24 from the research's
recommendation (question 1, option a); Pratik may overrule: split each key
across several credential store entries with fixed names, so the
uninstaller still names them without reading anything. It keeps CLAUDE.md's
rule that secrets live in the credential store and nowhere else; the other
two options (a file protected with DPAPI, or a file encrypted with one key
from the store) would change that rule.

**2. Other people's public keys and certificates are not secret.** Taken
2026-09-24 from the research's recommendation (question 7); Pratik may
overrule: a table in the mail database. Certificates from signed mail are
13-19's.

**3. Keys locked with a passphrase.** Taken 2026-09-24 from the research's
recommendation (question 2, option b); Pratik may overrule: asked when a
message first needs the key and remembered until Wixen Mail closes, never
stored. That is 13-17.1's, a plan added after the manager for it; this plan
keeps refusing a locked private key at import, with the sentence it has.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the test store refuses what Windows refuses, and a key is stored in parts that uninstalling names</name>
  <files>src/service/secret_store.rs, src/service/pgp/mod.rs, src/service/pgp/keys.rs, src/application/forget.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/secret_store.rs:46-175` (both backings and the public `write`, `read`, `remove`).
    - `src/service/pgp/mod.rs:54-110` (the constants and `keyring_entries`, with the permanence note) and `src/service/pgp/keys.rs:50-180` (`import`, `open`, `open_with`, `a_key_is_here`) and its tests `:350-549`.
    - `src/application/forget.rs:30-60` and its tests on the PGP entries.
  </read_first>
  <behavior>
    - The test backing's `write` refuses a secret whose UTF-16 encoding is over 2,560 bytes with the reason the real store's error carries through `backing::write` ("Could not save it: ..."), measured by reading the real `keyring` error text for that case in the vendored source and quoted in the summary.
    - `service::secret_store::tests::test_a_secret_windows_would_refuse_is_refused_here_too`: 1,280 characters stored, 1,281 refused.
    - With that in, `test_a_message_encrypted_to_the_imported_key_opens_and_reads` and the other Alice imports are red: the defect, shown.
    - `KEY_SLOTS` and `PARTS_PER_KEY` in `pgp/mod.rs` with the measured sizes in their doc comment; entry names `key-{slot}-part-{part}`, each part at most 1,280 characters; `keyring_entries()` returns every slot's every part and `private-key`, built from the constants, reading nothing.
    - Storing a key writes its parts into the first free slot and removes any stale part past its last; reading joins a slot's parts in order until the first absent part; a key larger than the slots allow is refused with a sentence, never truncated.
    - On first use, an entry under `private-key` is moved into a slot and the old entry removed; `private-key` stays on `keyring_entries` forever.
    - `service::pgp::keys::tests::test_an_ordinary_rsa_key_is_stored_and_opens_mail`: Alice's key through the refusing store is stored and opens her message; `test_the_key_is_stored_under_the_one_name_uninstalling_erases` rewritten to assert every entry written is on `keyring_entries`; `application::forget`'s case that the uninstall list holds every PGP entry.
  </behavior>
  <action>
    Red first, on a branch, in two red commits. The first adds the refusal
    case and the RSA storage case, both red, named by module path with the
    count check (two records name `secret_store.rs`). The second is still
    red: it makes the test store refuse (test-only code in the `#[cfg(test)]`
    backing), which turns the refusal case green and the existing Alice
    import cases red in every module that imports her key (found with
    `grep -rn 'alices_private_key' src`, 13-15's cases among them), and it
    names every one of them and the RSA case by module path, since the gate
    refuses a red commit with an unnamed failure. If a test outside PGP goes
    red under the honest store (an OAuth token, a password, a sign-in), that
    is the research's out-of-group finding proven rather than a fixture to
    shorten. Settled when the plans were integrated on 2026-09-24, so this
    plan does not halt mid-sitting: the split then belongs in this plan,
    because it cannot land green without it and the parts write it builds
    is the mechanism. That value's entry is written and read through the
    same parts functions under its own service's fixed names, those names
    join the uninstall sweep's list the way the key's do, the red test is
    named in the second red commit, one guard record breaks the split for
    it, and the summary names every file added to this plan's list with the
    test's name and the value's length. Only if the value is larger than
    the parts can hold does the executor stop and report it, with the
    name and the length. That commit is the defect shown in the gate's own record:
    `scripts/red-commit.sh` proves each named case really failed. Then the
    constants, the split write, the joined read, the migration and the list,
    green, with `cargo build` between. Measure the two key sizes before
    choosing `PARTS_PER_KEY`.

    Guard records, measured: one on `secret_store.rs` whose break lifts the
    test store's limit, the refusal case red and the RSA storage case green
    for the wrong reason (the record names what reddens); one on
    `pgp/keys.rs` whose break writes a key in one entry, the RSA case red;
    one on `pgp/mod.rs` whose break drops the last part from
    `keyring_entries`, the forget case red.
  </action>
  <verify>
    <automated>cargo test --lib service::secret_store:: && cargo test --lib service::pgp:: && cargo test --lib application::forget::</automated>
  </verify>
  <acceptance_criteria>
    - Two red commits: the first names the refusal and RSA cases; the second names the Alice import cases the refusing store turns red, with the RSA case; `scripts/red-commit.sh` accepts both.
    - `grep -c 'PARTS_PER_KEY' src/service/pgp/mod.rs` is at least 2; the measured key sizes and the `GNUPGHOME` commands are in the summary and in the constant's doc comment.
    - The three runs pass, counts quoted.
  </acceptance_criteria>
  <done>An ordinary RSA key is stored on Windows' terms and opens mail, and the uninstaller names every entry a key can occupy without reading one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: several private keys, a public key table, one listing, and import, remove and export</name>
  <files>src/service/pgp/mod.rs, src/service/pgp/keys.rs, src/data/message_cache/mod.rs, src/data/message_cache/pgp_keys.rs, src/application/pgp_keys.rs, src/application/mod.rs, guards/guards.toml</files>
  <read_first>
    - `~/.cargo/registry/src/*/pgp-0.20.0/src/composed/signed_key/parse.rs:43` (`from_armor_many`), `composed/signed_key/secret.rs:162` (`to_public_key`), and the key id, fingerprint, created and expiry accessors, read and quoted.
    - `src/data/message_cache/mod.rs`: how a table is created with `CREATE TABLE IF NOT EXISTS` and registered with `schema_template.rs`, from an existing small module such as `signatures.rs`.
  </read_first>
  <behavior>
    - `service::pgp::KeyListing { user_ids, key_id, fingerprint, created, expires: Option, private: bool, can_encrypt: bool, can_sign: bool }` in the project's words; `describe(armour) -> Vec<KeyListing>`; `private_keys_here() -> Vec<KeyListing>` from the slots.
    - `service::pgp::import_keys(armoured) -> Vec<WhatImportingAKeyFound>`: one answer per key the text holds (`from_armor_many`); a private key goes to a slot; a public key is answered `PublicKeyToKeep { armour, listing }` for the caller to keep; a locked private key is refused as today; a key already here is `AlreadyHere`.
    - `remove_private_key(fingerprint)`; `public_half(fingerprint or armour) -> String`, armoured.
    - `open` tries the private key whose id the message names, then the others, so several keys each open their own mail.
    - `data::message_cache::pgp_keys`: `pgp_public_keys (fingerprint TEXT PRIMARY KEY, armour TEXT NOT NULL, added_at TEXT NOT NULL)`, `keep_public_key`, `public_keys`, `forget_public_key`.
    - `application::pgp_keys::every_key_here(cache) -> Vec<KeyListing>` (private first, then public, each sorted by first user id); `import(cache, armoured) -> Vec<Imported>` with a sentence each; `remove(cache, fingerprint)`; `export_public(cache, fingerprint) -> String`.
    - Cases: a text with Alice's private and Bob's public key imports both and lists both; Alice's and Bob's private keys each open their own message; removing Alice leaves Bob; exporting Alice's public half gives armour `describe` lists as public with her fingerprint; the public table survives a reopen; a second import of the same key is `AlreadyHere`; `test_no_caller_outside_this_module_names_the_crate` still passes.
  </behavior>
  <action>
    Red first, on a branch: the cases by module path, and the count check
    where a named file gains a test. Then the service calls, the table, and
    the application module, with `cargo build` between. Invoke `tdd` and
    `elegant-code`: the crate stays behind `service::pgp`; the data module
    stores armour text and knows nothing of keys; the application module
    joins the two.

    Guard records, measured: one on `pgp/keys.rs` whose break tries only
    the first slot, Bob's message case red; one on `application/pgp_keys.rs`
    whose break drops public keys from the listing, the listing case red.
  </action>
  <verify>
    <automated>cargo test --lib service::pgp:: && cargo test --lib data::message_cache::pgp_keys:: && cargo test --lib application::pgp_keys::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the cases by module path.
    - `grep -rln 'use pgp::\|pgp::composed' src --include=*.rs` lists only files under `src/service/pgp/`.
    - `grep -c 'CREATE TABLE IF NOT EXISTS pgp_public_keys' src/data/message_cache/pgp_keys.rs src/data/message_cache/mod.rs` reports 1 in one of them.
    - The three runs pass, counts quoted.
  </acceptance_criteria>
  <done>Every key here is listed in one place, private keys open their own mail, public keys are kept where they belong, and each can be imported, removed and exported by code the manager can call.</done>
</task>

<task type="auto">
  <name>Task 3: the privacy page, the changelog and the ledger</name>
  <files>docs/privacy.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/privacy.md`'s section on what the credential store holds (`grep -n 'credential store\|wixen-mail-pgp' docs/privacy.md`).
    - `.planning/WINDOWS.md` row 144.
  </read_first>
  <action>
    Invoke `writing-craft`. `docs/privacy.md`: private keys are in the
    Windows credential store, split across several entries under
    `wixen-mail-pgp` because one entry holds 1,280 characters, all erased
    when the program is uninstalled; other people's public keys are in the
    mail database and are not secret. The changelog under `[Unreleased]`,
    Fixed: an ordinary RSA private key could not be stored on Windows and
    now can; Added: several keys and public keys, reachable from 13-17's
    manager (the entry says the manager arrives in the next plan until it
    does). Known limitations: no real key from a real correspondent has
    been imported.

    Ledger, by hand, both halves: one `deviation` recording the size defect
    found and fixed in this plan, with the test that shows it; row 144
    updated; one `todo` for the same limit outside this group, which the
    research found and did not plan: `service::oauth` writes a token's JSON
    through `secret_store::write` (`oauth.rs:910` on 2026-09-24), a Microsoft
    access token is commonly over 1,280 characters (research assumption A2,
    not measured), and the honest test store this plan builds now refuses
    what Windows refuses, so any OAuth test storing a long token turns red
    here first; the entry names the sign-in owner's next plan as the place
    to measure a real token and split it the same way, and says whether any
    OAuth test went red in this plan.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wixen-mail-pgp' docs/privacy.md` is at least 1.
    - Ledger in both halves; no em dash or en dash; none of the six words; carriage returns 0.
  </acceptance_criteria>
  <done>The privacy page says where keys live and why in several entries, and the ledger records the defect this plan found and closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a key file to the credential store | the most valuable secret this program handles |
| the credential store to uninstall | every entry written must be named |
| a correspondent's public key to the database | not secret, but trusted for encrypting to them later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-16-01 | Information disclosure | a key part left behind after uninstall | high | mitigate | `keyring_entries` built from the constants names every slot and part and the old name; a guard record on the last part |
| T-13-16-02 | Information disclosure | a key's bytes in an error or log | high | mitigate | outcomes carry the store's reason only, as today; the refusal test that no line of the file appears is kept |
| T-13-16-03 | Tampering | a truncated key stored as whole | medium | mitigate | a key larger than the slots is refused, never cut; the joined read stops at the first absent part and a key that does not parse after joining is `TheKeyHereCouldNotBeRead` |
| T-13-16-04 | Spoofing | a public key claiming a correspondent's address | medium | accept | a public key says whose it claims to be and nothing here verifies that; 13-17's manager says so where keys are listed, and 13-18's sentences never call a key trusted |
| T-13-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; `pgp` 0.20 is already in the tree |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass. `scripts/check.sh` through the hook on every commit, never piped; no hand-run `scripts/check.sh all` (13-51 runs it once); one `scripts/guards.sh --remeasure` call per commit for every flagged record; the pages, the ledger, the summary and the four completion marks in one documents commit before the merge; `git merge --no-ff`.
- Spoken or shown: no. The File menu's import keeps its sentences; nothing new is on a window until 13-17, so the branch merges without a pull request (Pratik's answer of 2026-09-23).
- Secrets: only in the credential store; the database gains public keys, which are not secret. Schema additive: one `CREATE TABLE IF NOT EXISTS`.
</verification>

<success_criteria>
- GAP-03's storage premise has named tests in the summary; the box waits for 13-17.1.
- The four completion marks: phase 13's progress row in `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list (and the README's if it has one); `STATE.md`'s plan number in its frontmatter and body; GAP-03's traceability row naming this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-16-SUMMARY.md` when done
</output>
