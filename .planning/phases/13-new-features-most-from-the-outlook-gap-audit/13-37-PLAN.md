---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 37
type: execute
wave: 39
depends_on: [13-36]
files_modified:
  - src/data/message_cache/mod.rs
  - src/data/message_cache/saved_searches.rs
  - src/presentation/folder_tree.rs
  - src/presentation/wx_app.rs
  - src/application/favourites.rs
  - src/application/context_menu.rs
  - tests/a_saved_search_moves_with_the_gesture.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-09]
estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Alt+Shift+Up and Alt+Shift+Down on a saved search's row in the folder tree move it one place within its own account's searches and say where it now is in the words accounts and pinned folders use (\"Invoices, 2 of 4.\", \"Invoices is already first of 4.\"), the tree read back with the cursor on it (GAP-09's first [D] clause, #58 point 1)."
    - "The order is stored in a position column added with ensure_column_exists, a new search goes last, and every search written before the column keeps the order it had, numbered on open in created_at then id order."
    - "The folder tree shows an account's searches in the stored order, a search written by a newer version included at its own place, so what a move says and what the tree shows are one order."
    - "Move Up and Move Down on Action, This Folder, and two new entries on a saved search's context menu (Move this search &up, Move this search do&wn) reach the same move; the gesture's refusal names saved searches among what it moves; nothing here reaches a server, and the check in favourites.rs reads the new function to say so."
    - "The shortcuts page's line saying one row per saved search is corrected by dating, and the older changelog limit on Subject Only is dated as fixed (part of GAP-09's last clause, #58 point 5)."
  artifacts:
    - "src/data/message_cache/mod.rs: ensure_column_exists(\"saved_searches\", \"position\", \"INTEGER\") and number_the_unnumbered_saved_searches called on open beside number_the_unnumbered_labels."
    - "src/data/message_cache/saved_searches.rs: create_saved_search placing a search last, get_saved_searches_for_account ordered by position then created_at then id, SavedSearchesRead.order, put_saved_searches_in_order, number_the_unnumbered_saved_searches, their tests."
    - "src/presentation/folder_tree.rs: WhatMoves::SavedSearch { account, id } from what_the_gesture_moves."
    - "src/presentation/wx_app.rs: move_the_chosen_row's SavedSearch arm, move_the_chosen_search, every_saved_search walking the stored order, the Move Up and Move Down help text."
    - "tests/a_saved_search_moves_with_the_gesture.rs: source readings that the gesture reaches the store and the tree, each with a planted-fault companion."
  key_links:
    - "Alt+Shift+Up -> ID_MOVE_UP -> move_the_chosen_row -> folder_tree::what_the_gesture_moves -> WhatMoves::SavedSearch -> move_the_chosen_search -> reordering::moved over every_saved_search's order -> put_saved_searches_in_order -> read_the_tree_back: one list, one order, written whole."
    - "saved_searches.position -> get_saved_searches_for_account -> SavedSearchesRead.order -> every_saved_search -> folder_tree::saved_search_rows: the tree reads the order the store keeps."
---

<objective>
Saved searches keep an order somebody chooses, moved with the gesture the
tree already has.

Purpose: #58 point 1, from the audit of 2026-09-15: "Saved searches keep
creation order and cannot be moved: Alt+Shift+Up and Down rearrange accounts
and pinned folders only, a saved-search row answers nothing
(`folder_tree.rs:774`)." GAP-09's first `[D]` clause: "The reordering gesture
the tree has." 12-10 gave labels a stored order on the same pattern
(`tags.rs:109`, `:133`), and this plan follows it for searches. It also takes
the part of GAP-09's last clause ("the pages corrected by dating") that falls
on what it changes: the shortcuts page's "one row per saved search" and the
older changelog limit.

Output: the column and the pass, the stored order read by the tree, the
gesture's arm and its function, the refusal's words, two context menu
entries, the reading, the pages, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-10-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only. Command first, then
what it said. Plans 13-01 to 13-36 land before this one and several edit
`wx_app.rs`, so every line number below is a place to start looking, found
again by the function's name before it is edited.

**1. The order today is creation, and nothing stores another.**

```
grep -n 'ORDER BY created_at, id' src/data/message_cache/saved_searches.rs   -> :345, inside get_saved_searches_for_account (:336-363); its doc (:330-335) says oldest first
grep -n 'CREATE TABLE IF NOT EXISTS saved_searches' -A 10 src/data/message_cache/mod.rs   -> :1939-1948: id, account_id, name COLLATE NOCASE, all_or_any, folder, created_at, updated_at, UNIQUE(account_id, name); no position
grep -n 'ensure_column_exists("saved_searches"' src/data/message_cache/mod.rs   -> nothing
grep -n 'ensure_column_exists("tags", "position"\|number_the_unnumbered_labels' src/data/message_cache/mod.rs   -> :2802 and :1455, 12-10's column and its pass, run on every open and not fatal
grep -n 'pub fn put_labels_in_order\|pub fn number_the_unnumbered_labels\|COALESCE(MAX(position)' src/data/message_cache/tags.rs   -> :109, :133, :16 and :147: the whole order in one transaction, the pass in name order, a new label last
```

Three comments say the old order and change with it: `SavedSearchesRead`'s
field doc (`saved_searches.rs:63`, "in the order they were made"), the read's
doc (`:330-335`), and `replace_saved_search`'s note that `created_at` is left
alone because "the folder tree lists these oldest first" (`:251-254`); the
last stays true in what it does and is reworded to say the tree reads the
position.

**2. The tree shows readable searches first, and that is a second order.**
`put_back_together` (`saved_searches.rs:650-673`) splits a read into
`searches` and `saved_by_another_version`, and `every_saved_search`
(`wx_app.rs:12822-12844`) chains the two, so the tree shows every readable
search before any a newer version wrote (`folder_tree.rs:705-709` says so).
With a stored order that split is a second order: a move across it would be
written and not shown. So the read keeps the stored order beside the two
lists, as `SavedSearchesRead.order`, and `every_saved_search` walks it. The
three `SavedSearchesRead` literals in `wx_app.rs`'s tests (`:28468`, `:28614`,
`:28629`) gain the field in place; `test_every_saved_search_gets_a_row_including_the_ones_this_build_cannot_read`
(`:28490`) is rewritten in place so its `order` puts the unreadable search
first and the rows follow it. No test is added to `wx_app.rs`.

**3. The gesture, and where it is refused.**

```
grep -n 'pub enum WhatMoves\|pub fn what_the_gesture_moves' src/presentation/folder_tree.rs   -> :763, :773; a saved search row falls to `_ => WhatMoves::Nothing`
grep -n 'fn test_the_gesture_moves' src/presentation/folder_tree.rs   -> :1542 (the positive cases) and :1561 (the rows that move nothing, WhichRow::SavedSearch among them at :1575-1578)
grep -n 'fn move_the_chosen_row\|fn move_the_chosen_pin' src/presentation/wx_app.rs   -> :10225 and :10249; the Nothing arm refuses with favourites::WHICH_ROW (:10240); the pin move is the template: its own account's list, reordering, every position written, read_the_tree_back, send_status, announce at High
grep -n 'pub const WHICH_ROW' -A 2 src/application/favourites.rs   -> :174-176, "Move Up and Move Down rearrange accounts and pinned folders. Choose an account branch, or a folder under Favourites."
grep -n 'const THE_COMMANDS' -A 4 src/application/favourites.rs   -> :518-522, three function names read by nothing_here_reaches_a_server, whose test at :631 is red for a name the file does not hold
grep -n 'pub fn moved' src/application/reordering.rs   -> :55; "{name}, {n} of {howmany}." and "{name} is already first of {howmany}."
grep -n 'ID_MOVE_UP' src/presentation/wx_app.rs src/presentation/wx_context_menu.rs   -> wx_app.rs:4399 the arm calling move_the_chosen_row, :7250-7258 the This Folder items with help "Move the chosen account or pinned folder one place up"; wx_context_menu.rs:65 Action::MoveUp to ID_MOVE_UP
grep -rn 'rearrange accounts and pinned\|WHICH_ROW' nvda-tests tests src --include=*   -> favourites.rs and wx_app.rs only; no NVDA case waits for the sentence
```

**4. The letters on the context menu.** `SAVED_SEARCHES` at
`context_menu.rs:374-379` holds Edit &conditions, &Run this search again,
Re&name, &Delete this search: c, r, n and d. An account's row says "Move this
account &up" and "Move this account &down" (`:401-402`), but d is Delete here,
so the two new entries are "Move this search &up" (u) and "Move this search
do&wn" (w), both free, and `test_no_two_entries_in_one_menu_share_a_mnemonic`
(`:766`) holds them. `test_a_saved_search_row_offers_only_what_works_on_a_saved_search`
(`:694`) gains the two in place. `docs/KEYBOARD_SHORTCUTS.md`'s Context
Menu section (`:1284-1292`) names no entry's letter, and
`grep -n -i 'saved search' docs/KEYBOARD_SHORTCUTS.md` finds `:124-128`,
`:691` and `:772`, none naming a letter on this menu. The executor greps
the entries' `&` again before writing them.

**5. The pages that drifted, as #58 point 5 names them.**
`docs/KEYBOARD_SHORTCUTS.md:124-125` says "a **Saved Searches** heading, with
one row per saved search"; there has been a branch per account since D-2-05.
`:1142-1143` (Folder Actions) say Move Up and Move Down move "the account, or
the pinned folder". `docs/changelog.md:5441-5445`, under `[Unreleased]`
(`:7` to `:12990`), lists "A saved search remembers the folder it was made
in, but not whether you asked for Subject Only or From Only", which the entry
at `:4748-4760` fixed. Both are dated, not deleted. The Action menu table's
Saved Searches row (`:691`) is 13-38's, because 13-38 changes that submenu.

**6. The gate for each file, taken with the parser and the count check's
own rule at `630e2a67`.** `src/data/message_cache/saved_searches.rs` 27 tests
and 2 records, three cases added with the count check named bare;
`src/data/message_cache/mod.rs` 23 and 12, the column and the call only;
`src/presentation/folder_tree.rs` 97 and 5, two tests rewritten in place and
none added; `src/application/context_menu.rs` 18 and 5, one rewritten in
place; `src/application/favourites.rs` 19 and 1, a list of names grown and no
test added; `src/presentation/wx_app.rs` 199 and 119, none added;
`src/application/reordering.rs` 3 and 0, not edited; the new target at zero.
Plans 13-01 to 13-36 move these, so the executor re-takes each before the
first commit with
`python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(sum(1 for r in g if any(e['file']=='FILE' for e in r.get('tests_last_seen',[]))))"`
and `grep -cxE '\s*#\[(tokio::)?test\]' FILE`, and quotes before and after.

**7. The anchors, by the second reading.** No record whose `file` is one this
plan edits has its `before` text inside a region it edits (the register read
with `tomllib` against `saved_searches.rs:55-139,245-363,1054-1080`,
`mod.rs:1450-1460,1937-1983,2795-2805`, `folder_tree.rs:700-791,1540-1600`,
`favourites.rs:172-177,505-525`, `context_menu.rs:358-380,694-712` and
`wx_app.rs:7236-7258,10215-10300,12700-12845`). Two sit beside them and must
not be copied: "one heading holds every saved search, however it was made"
anchors `saved_search_rows`' loop (`folder_tree.rs:740-753`), which this plan
does not edit; "pinning a folder never reaches a server" anchors the
`let done = match pinning {` block in `pin_or_unpin_the_chosen_folder`, which
`move_the_chosen_search` must not copy. The executor runs the same reading
again before each commit, since earlier plans move the regions.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation; Pratik may overrule.**
Pratik confirmed the order of phase 13 and said "plan and implement phase 13"
on 2026-09-24; every recommendation in the research's question list was taken
as the decision.

- **Per account (RESEARCH-4 question 9).** A search moves within its own
  account's searches, as a pin moves within its own account's part of
  Favourites, because a search's rows sit under its account's branch.
- **The planner's, for the same reason as premise 2:** the tree shows the
  stored order, a newer version's search at its own place, so there is one
  order and the sentence after a move describes what the tree then shows.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the position, the pass, and the order the read keeps</name>
  <files>src/data/message_cache/mod.rs, src/data/message_cache/saved_searches.rs, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/saved_searches.rs:55-139`, `:245-363`, `:640-735` (the read, `put_back_together`, the test helpers `a_cache`, `a_search`, `names_in`), `:1054-1080` and `:1399-1470` (the order test and the older-database test).
    - `src/data/message_cache/tags.rs:1-20` and `:100-155` (a new label last, `put_labels_in_order`, `number_the_unnumbered_labels`), and `src/data/message_cache/mod.rs:1440-1460` and `:2795-2805`.
    - 12-10's summary, key decisions: the pass runs on every open over rows with no place, because a marker would decide nothing.
  </read_first>
  <behavior>
    - `saved_searches` gains `position INTEGER` through `ensure_column_exists`, beside the labels' column; `create_saved_search` writes `(SELECT COALESCE(MAX(position), 0) + 1 FROM saved_searches WHERE account_id = ?2)`, so a new search goes last in its account.
    - `get_saved_searches_for_account` orders by `position`, then `created_at`, then `id`; `SavedSearchesRead` gains `order: Vec<String>`, every search's id, readable or not, in that order; `put_back_together` fills it from the rows as read.
    - `put_saved_searches_in_order(account_id, ids)` writes positions 1.. in one transaction, only for rows of that account; `number_the_unnumbered_saved_searches()` gives every row with no position one, after the rows that have one, in `created_at` then `id` order, per account, in one transaction, and is called on every open beside `number_the_unnumbered_labels`, not fatal, logged at warn when it fails.
    - Cases, new in the module's tests: `test_the_order_written_is_the_order_read_back` (three searches, an order written, read back in it, a second account untouched); `test_a_new_search_goes_last_in_its_accounts_order` (after an order is written, a fourth search is read last); `test_searches_from_before_the_order_keep_the_order_they_were_made` (rows written with no position, set to NULL through the connection the test module reaches, numbered on reopen in the order they were made, readable and unreadable interleaved as made, and `order` naming them in that order). `test_the_searches_stay_in_the_order_they_were_made_when_one_is_renamed` stays green unchanged, since a rename moves no position.
  </behavior>
  <action>
    Red first, on a branch. The red commit carries the three cases by module
    path (`data::message_cache::saved_searches::tests::<name>`) and the count
    check bare (`test_every_guard_record_says_how_many_tests_the_files_it_names_held`),
    since two records name this file, and only what the cases need to
    compile: `order` on the struct left empty, `put_saved_searches_in_order`
    and `number_the_unnumbered_saved_searches` that write nothing, so each
    named case runs and fails. Run the named cases and the count check before
    committing, and name everything that fails.

    Then the green commit: the column, the create, the read, the pass and its
    call, with `cargo build` between. Rewrite the three comments premise 1
    names so they say the stored order. Invoke `elegant-code`: the order
    lives in one `ORDER BY`; the pass is one statement per row in one
    transaction, as `number_the_unnumbered_labels` is; errors map through
    `common::Error` with a sentence naming what failed.

    Guard record: one on `saved_searches.rs` whose break drops `position`
    from the read's `ORDER BY`, `test_the_order_written_is_the_order_read_back`
    red; measured with `scripts/guards.sh --remeasure`, together with the two
    records the count check flags, in one call, as the first run.
  </action>
  <verify>
    <automated>cargo test --lib data::message_cache::saved_searches:: && cargo test --lib data::message_cache::tags:: && cargo test --test house_style test_every_guard_record_says_how_many_tests_the_files_it_names_held</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the three cases by module path and the count check bare, and `scripts/red-commit.sh` accepts it through the hook.
    - `grep -c 'ensure_column_exists("saved_searches", "position"' src/data/message_cache/mod.rs` is 1; `grep -c 'number_the_unnumbered_saved_searches()' src/data/message_cache/mod.rs` is 1; `grep -c 'ORDER BY position, created_at, id' src/data/message_cache/saved_searches.rs` is 1; `grep -c 'pub fn put_saved_searches_in_order' src/data/message_cache/saved_searches.rs` is 1.
    - `cargo test --lib data::message_cache::saved_searches::` passes with three tests more than before the plan, both counts quoted.
    - One guard record written and measured, and every flagged record re-measured, each quoted with what it reddened.
  </acceptance_criteria>
  <done>Saved searches have an order the store keeps, a new one goes last, and every search from before keeps the order it had, with the read carrying that order whole.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the gesture moves a saved search, from the keys, the This Folder menu and the row's own menu</name>
  <files>src/presentation/folder_tree.rs, src/presentation/wx_app.rs, src/application/favourites.rs, src/application/context_menu.rs, tests/a_saved_search_moves_with_the_gesture.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/folder_tree.rs:755-791` and `:1540-1590`.
    - `src/presentation/wx_app.rs:10215-10300` (`move_the_chosen_row`, `move_the_chosen_pin`), `:12806-12844` (`every_saved_search`), `:12700-12716` (where the tree's searches are gathered), `:7236-7258` (Move Up and Move Down), `:8147-8180` (`read_the_tree_back`), `:28455-28510` (the test state and the test premise 2 rewrites).
    - `src/application/favourites.rs:170-226` and `:492-660`; `src/application/reordering.rs`, whole; `src/application/context_menu.rs:355-405` and `:690-790`.
    - `tests/every_command_acts_on_the_selection.rs`, the head comment and one reading with its planted-fault companion, for the shape of a source reading over `what_ships`.
  </read_first>
  <behavior>
    - `WhatMoves::SavedSearch { account, id }` from `what_the_gesture_moves` for `WhichRow::SavedSearch`; `test_the_gesture_moves_an_account_on_an_account_row_and_a_pin_on_a_pinned_row` gains the saved search case in place, and `test_the_gesture_moves_nothing_on_a_row_that_is_neither` loses `WhichRow::SavedSearch` from its list in place; no test added.
    - `move_the_chosen_row`'s new arm calls `move_the_chosen_search(app, cache, a11y, &account, &id, direction)`: this account's searches as `(id, name)` from `every_saved_search` over the held `saved_searches` (the tree's own order, premise 2), `reordering::moved` over them with the saved-search refusal the file already has (`WHICH_SAVED_SEARCH`, `wx_app.rs:10195`) as the words for a row no longer in the list; nothing moved says so through `CommandAnswered`; a move writes the whole order with `put_saved_searches_in_order`, then `read_the_tree_back`, `send_status` and `announce` at High, as a pin's move does. No call that leaves this machine.
    - `favourites::WHICH_ROW` names saved searches: "Move Up and Move Down rearrange accounts, pinned folders and saved searches. Choose an account branch, a folder under Favourites, or a saved search." `THE_COMMANDS` gains `"fn move_the_chosen_search"`, so `nothing_here_reaches_a_server` reads it.
    - The This Folder items' help says "Move the chosen account, pinned folder or saved search one place up" and "... down"; `context_menu::SAVED_SEARCHES` gains "Move this search &up" (`Action::MoveUp`) and "Move this search do&wn" (`Action::MoveDown`) after Re&name, and `test_a_saved_search_row_offers_only_what_works_on_a_saved_search` asserts both in place.
    - The new target, `tests/a_saved_search_moves_with_the_gesture.rs`, reads `what_ships` of `wx_app.rs`: `move_the_chosen_row`'s body names `WhatMoves::SavedSearch` and calls `move_the_chosen_search(`; `move_the_chosen_search`'s body calls `put_saved_searches_in_order(` and then `read_the_tree_back(`, in that order, and `every_saved_search(`; `every_saved_search`'s body reads `.order`. Each reading has a companion that plants the fault (the arm sent to the refusal, the tree read before the write, the order not read) and is refused, so a reading that passes is one that could fail. The head comment says what is read and what is not (that a person hears the sentence, which is the ledger's).
  </behavior>
  <action>
    Red first, on the same branch. The red commit names, by module path,
    `presentation::folder_tree::tests::test_the_gesture_moves_an_account_on_an_account_row_and_a_pin_on_a_pinned_row`,
    `application::context_menu::tests::test_a_saved_search_row_offers_only_what_works_on_a_saved_search`,
    `application::favourites::nothing_here_reaches_a_server::test_the_commands_this_reads_are_still_there_under_those_names`
    (red because `THE_COMMANDS` names a function the file does not yet
    hold), `presentation::wx_app::tests::test_every_saved_search_gets_a_row_including_the_ones_this_build_cannot_read`
    (in `mod tests`, which opens at `wx_app.rs:27329`; read again before
    naming it, since earlier plans move it), and the new target's readings
    bare. It carries only what they need to compile: the
    variant, answered by nothing; `every_saved_search` still chaining the two
    lists. Run them and name everything that fails.

    Then the green commit, with `cargo build` between. Invoke `elegant-code`:
    `move_the_chosen_search` is the pin move's shape and none of its text,
    since the record premise 7 names anchors inside
    `pin_or_unpin_the_chosen_folder`; the list is one expression over
    `every_saved_search`; no `unwrap`. Invoke `naming-review` on the new
    variant and function.

    Guard records: one on `wx_app.rs` whose break sends
    `WhatMoves::SavedSearch` to `WHICH_ROW`'s refusal, the target's dispatch
    reading red, `suite` the target; one on `folder_tree.rs` whose break
    answers `Nothing` for a saved search row again, the positive case red;
    each measured, with every record the count check flags, in one
    `--remeasure` call per commit.
  </action>
  <verify>
    <automated>cargo test --test a_saved_search_moves_with_the_gesture && cargo test --lib presentation::folder_tree:: && cargo test --lib application::context_menu:: && cargo test --lib application::favourites:: && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the four cases by module path and the target's readings bare.
    - `grep -c 'fn move_the_chosen_search' src/presentation/wx_app.rs` is 1; `grep -c 'WhatMoves::SavedSearch' src/presentation/wx_app.rs` is at least 1; `grep -c '"fn move_the_chosen_search"' src/application/favourites.rs` is 1; `grep -c 'Move this search &up' src/application/context_menu.rs` is 1 and `grep -c 'Move this search do&wn' src/application/context_menu.rs` is 1.
    - `cargo test --lib presentation::folder_tree::`, `application::context_menu::`, `application::favourites::` and `presentation::wx_app::` each pass with the same count before and after the plan, quoted; the target passes with at least 8 tests (four readings, four companions), quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The gesture moves a saved search within its account from the keys, from This Folder and from the row's own menu, says where it went, writes the order whole, and the tree shows it there.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the dated corrections, the changelog, the ledger and the four marks</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:117-146` (The Sidebars) and `:1135-1166` (Folder Actions and the Alt+Shift warning).
    - `docs/USER_GUIDE.md:810-832` (Search Functionality) and `:888`.
    - `docs/changelog.md:7-40` and `:5430-5450`, `:4740-4762`.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: in The Sidebars, the
    sentence at `:124-125` says a Saved Searches heading with a branch per
    account and one row per search under it, followed by a dated sentence
    saying that until 2026-09-24 it said one row per saved search; and a
    sentence that Alt+Shift+Up and Alt+Shift+Down move a saved search within
    its account's searches, pointing at the Alt+Shift warning. Folder
    Actions: Move Up and Move Down name saved searches. The guide: under
    Search Functionality, two sentences on moving a saved search and what an
    installation from before keeps (the order its searches were made in).
    The changelog, `[Unreleased]`, Added: saved searches can be moved, in
    #58's words, and that nothing reaches a server; beneath the older limit
    at `:5441-5445`, one dated sentence saying the Subject Only and From Only
    limit was fixed by the entry "Saving a search now keeps what you chose in
    the In list", and that this line was left in place and dated on
    2026-09-24 (#58 point 5). Known limitations: nobody has heard a search
    moved.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's ear
    (the sentence after a move, the cursor staying on the moved row, the two
    context menu entries), `file` `src/presentation/wx_app.rs`.

    The four marks, each named: phase 13's progress row in
    `.planning/ROADMAP.md`; this plan's `- [ ]` line in phase 13's plan list,
    in `ROADMAP.md` (the phase README keeps no boxes), ticked; `STATE.md`'s plan number
    in its frontmatter (`current_plan`) and in its body ("Current plan N of
    M"); and GAP-09 in `REQUIREMENTS.md`, where a dated line under its `[D]`
    line says the first clause is closed and names
    `test_the_order_written_is_the_order_read_back` and the target, the box
    and the traceability row left for 13-39, which closes the last clause.
    Run `cargo test --test the_planning_files_agree_with_themselves` before
    the documents commit.

    This plan changes what is spoken and shown, so after this one documents
    commit the branch is pushed and a pull request opened into `main` under
    Pratik's standing OK of 2026-09-23, and CI, NVDA and Accessibility are
    read on it before the local merge.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'one row per saved search' docs/KEYBOARD_SHORTCUTS.md` is 1, inside the dated sentence, and `grep -c 'branch per account' docs/KEYBOARD_SHORTCUTS.md` is at least 1; `awk '/^### Folder Actions/,/^#### A warning/' docs/KEYBOARD_SHORTCUTS.md | grep -c 'saved search'` is at least 2.
    - `grep -c '#58' docs/changelog.md` grew by at least 1; the dated sentence under the older limit names 2026-09-24.
    - The ledger entry in both halves; carriage returns 0 on every file touched, measured with `tr` rather than grep; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say how a saved search moves and correct the two drifted lines by dating, the changelog carries the change, and the four marks are made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the stored order to the tree | two readers of one order that could disagree |
| a database written by an older build to this one | rows with no position must keep the order they were shown in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-37-01 | Tampering | a move written and not shown, across the readable and unreadable split | medium | mitigate | `SavedSearchesRead.order` is the one order the tree and the move read; the rewritten wx_app test and the target read it |
| T-13-37-02 | Tampering | the pass numbering older rows in an order other than the one shown | medium | mitigate | numbered in `created_at` then `id` order, the old read's order; `test_searches_from_before_the_order_keep_the_order_they_were_made` |
| T-13-37-03 | Tampering | a move reaching another account's searches | low | mitigate | the list is the row's own account's; `put_saved_searches_in_order` updates only rows of that account |
| T-13-37-04 | Information disclosure | a call leaving the machine from a preference change | low | mitigate | `THE_COMMANDS` reads `move_the_chosen_search` for any call that leaves this machine |
| T-13-37-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. One `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes what is spoken and shown: after the branch's last commit, the one documents commit, the branch is pushed with a pull request into `main` (Pratik's standing OK of 2026-09-23), and CI, NVDA and Accessibility are read on it; a red one is fixed on the branch. The merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What the tester's ear settles: the sentence after a move, the cursor on the moved row, the context menu entries. What the readings prove: the order stored and read, the pass, the gesture's arm reaching the store and the tree.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 9 advances by its first clause: saved searches can be reordered.
- GAP-09's first `[D]` clause has a named test and reading in the summary and a dated line under the `[D]` line; the box waits for 13-39; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-09's dated line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-37-SUMMARY.md` when done
</output>
