---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 29
type: execute
wave: 31
depends_on: [13-28]
files_modified:
  - src/service/free_busy.rs
  - src/application/asking_when_free.rs
  - src/application/when_people_are_free.rs
  - src/service/oauth.rs
  - src/presentation/managers.rs
  - tests/item_form_free_busy.rs
  - guards/guards.toml
  - docs/privacy.md
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-08]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An account whose calendar is a Google calendar is asked at Google's freeBusy endpoint when Find when everyone is free is chosen, through the same door the other two sources use, with the calendar permission the Google sign-in already holds (GAP-08's first [D] line, #57)."
    - "Google's busy stretches become the same stretches a calendar server's answer gives, and a person Google cannot find is never counted free: their time is unknown and the sentence says their calendar is not shared with you."
    - "Nothing new is asked of anybody: no new Google permission, no request for an account with no Google calendar, and the question carries only the addresses and the window of dates."
  artifacts:
    - "src/service/free_busy.rs: WhereToAsk::Google { base, token }, ask_google through outward.asking_when_people_are_free, and what_google_said(json, about) pure, with cases against a test server and recorded answers."
    - "src/application/when_people_are_free.rs: WhyNot::NotSharedWithYou, said \"their calendar is not shared with you\", in the reasons census."
    - "src/application/asking_when_free.rs: where_to_ask learns an account with a Google calendar."
    - "src/service/oauth.rs: a_google_token_for(account)."
    - "src/presentation/managers.rs: the Google token fetched off the window's thread and handed to where_to_ask; production code only."
  key_links:
    - "Find when everyone is free -> managers::asking_when_people_are_free -> a worker: oauth::a_google_token_for when the account holds a gmail calendar -> asking_when_free::where_to_ask(.., google) -> WhereToAsk::Google -> free_busy::ask_google -> POST {base}/freeBusy -> what_google_said -> stretches or NotKnown(NotSharedWithYou)."
---

<objective>
Google as a free/busy source.

Purpose: GAP-08's first `[D]` line, "the Google free/busy endpoint", and
#57's first point: free/busy asks a calendar server or Microsoft today
(`asking_when_free.rs:172-188`), never Google, although a Google account
is the commonest calendar a tester has. RESEARCH-3 (C6) read Google's
freeBusy reference: the calendar permission the Google sign-in already
asks for covers it, so nobody signs in again.

Output: the third source, its parse, the reason a Google guest's time is
unknown, the token, the call site, the pages, the changelog and the
ledger. Asking every source is 13-30's; zones are 13-31's.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. No new Google permission (RESEARCH-3, GAP-08).** The `calendar`
scope already asked (`oauth.rs:73-84`) is one of the four freeBusy
accepts.

**2. Google's `notFound` is its own reason (RESEARCH-3, anti-patterns).**
Not free and not "the server would not say": "their calendar is not
shared with you", a fourth reason with words of its own.

**3. One place per account still, in this plan (planner's order).** A
calendar server first, then Microsoft, then Google, as today's order
extended; 13-30 asks every place.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. 13-28 edits `oauth.rs`; re-take its numbers after that merge.

**1. The sources and the door.**

```
sed -n 98,124p src/service/free_busy.rs   -> WhereToAsk { CalendarServer, Microsoft { base, token }, Nowhere }
sed -n 800,822p src/service/free_busy.rs   -> what_this_server_said dispatches the three
sed -n 937,967p src/service/free_busy.rs   -> ask_microsoft: POST {base}/me/calendar/getSchedule through outward.asking_when_people_are_free(&url)
sed -n 209,214p src/service/outward.rs   -> asking_when_people_are_free: a POST door kept apart from the gate on changes; test at :2553
sed -n 405,409p src/service/google_api.rs   -> CALENDAR_API_BASE = "https://www.googleapis.com/calendar/v3"
sed -n 70,84p src/service/oauth.rs   -> the Google consent list holds https://www.googleapis.com/auth/calendar
grep -n 'const GOOGLE' src/application/calendar.rs   -> :74, "gmail": a Google calendar's source_provider
```

**2. The reasons and the call site.**

```
sed -n 105,135p src/application/when_people_are_free.rs   -> WhyNot { ThereIsNowhereToAsk, TheServerWouldNotSay, TheReplyCouldNotBeRead }; EVERY_REASON_A_CALENDAR_IS_NOT_KNOWN: [WhyNot; 3] at :123, iterated by a test at :449
sed -n 165,190p src/application/asking_when_free.rs   -> where_to_ask(calendars, sign_in, microsoft): a calendar server first, then Microsoft, else Nowhere
sed -n 1060,1082p src/presentation/managers.rs   -> the worker: a_microsoft_token, where_to_ask, one_question, when_they_are_free
sed -n 1127,1140p src/presentation/managers.rs   -> a_microsoft_token, one line over oauth::a_graph_token_for
```

**3. Guard records, both readings.** `free_busy.rs` 0 and 0, 37 tests;
`asking_when_free.rs` 0 and 0, 11 tests; `when_people_are_free.rs` 1 by
`tests_last_seen` and 0 by `file`, 47 tests, a test added, so the count
check is named bare; `oauth.rs` 3 and 3 at `630e2a67`, no test added
here, its anchors at `:416`, `:782` and `:1028` not touched;
`managers.rs` 54 and 39, 137 tests, no test added (phase 12's cost
rule, carried), and no record anchors in `:1005-1145`;
`tests/item_form_free_busy.rs` 0 and 0, 1 test, a reading added.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Google's answer read, and a guest Google cannot find kept unknown</name>
  <files>src/service/free_busy.rs, src/application/when_people_are_free.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/free_busy.rs:470-560` (Microsoft's answer read, the shape to follow), `:690-830`, `:930-970`, and a Microsoft case against a test server (`grep -n 'WhereToAsk::Microsoft {' src/service/free_busy.rs`).
    - `src/application/when_people_are_free.rs:85-135` and `:440-460`.
  </read_first>
  <behavior>
    - `what_google_said(reply, about) -> Result<WhatTheySaid>` over a recorded answer: `calendars.(address).busy[]` with `start` and `end` becomes `TheirCalendar::Answered` covering the window asked, each stretch Busy; `calendars.(address).errors[]` with reason `notFound` becomes `NotKnown(NotSharedWithYou)`; any other reason `NotKnown(TheServerWouldNotSay)`; an address absent from the answer stays as today, unknown and never free; malformed JSON an error with a sentence.
    - `WhereToAsk::Google { base, token }`; `ask_google` posts `{base}/freeBusy` with `timeMin`, `timeMax` and `items[].id` for the batch, through `outward.asking_when_people_are_free`, with the token as a bearer header; a case against a test server reads the request line and body and hands back a recorded answer.
    - `WhyNot::NotSharedWithYou` in words "their calendar is not shared with you", in `EVERY_REASON_A_CALENDAR_IS_NOT_KNOWN`, which becomes four long, so the census test reads it.
  </behavior>
  <action>
    Red first, on the branch `13-29-google-free-busy`: the cases by module
    path (`service::free_busy::tests::<name>`,
    `application::when_people_are_free::tests::<name>`), and the count
    check `test_every_guard_record_says_how_many_tests_the_files_it_names_held`
    named bare for `when_people_are_free.rs`. Then the arm, the parse, the
    reason and its census entry, with `cargo build` between (per
    decisions 1 and 2). Invoke `elegant-code`: the parse is pure and the
    sender thin, as `what_microsoft_said` and `ask_microsoft` are.

    Guard records: one on `free_busy.rs` whose break reads `notFound` as
    free, the not-shared case red; one whose break drops the bearer
    header, the request case red; the record naming
    `when_people_are_free.rs` re-measured; each with
    `scripts/guards.sh --remeasure`.
  </action>
  <verify>
    <automated>cargo test --lib service::free_busy:: && cargo test --lib application::when_people_are_free::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the cases by module path and the count check bare.
    - `grep -c 'fn what_google_said' src/service/free_busy.rs` is 1; `grep -c 'NotSharedWithYou' src/application/when_people_are_free.rs` is at least 3.
    - `service::free_busy::` above 37 and `application::when_people_are_free::` above 47, both quoted; the records measured.
  </acceptance_criteria>
  <done>Google's freeBusy answer is read into the same stretches every source gives, and a guest Google cannot find is heard as not shared, never as free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: an account with a Google calendar asked at Google, with its token fetched off the window's thread</name>
  <files>src/application/asking_when_free.rs, src/service/oauth.rs, src/presentation/managers.rs, tests/item_form_free_busy.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/asking_when_free.rs:160-245` and its tests.
    - `src/service/oauth.rs` at `a_graph_token_for` and `get_valid_token` (`grep -n 'pub async fn a_graph_token_for\|pub async fn get_valid_token' src/service/oauth.rs`).
    - `src/presentation/managers.rs:1005-1140` and `tests/item_form_free_busy.rs` whole.
  </read_first>
  <behavior>
    - `where_to_ask(calendars, sign_in, microsoft, google: Option<(&str, &str)>)`: a signed-in calendar server first, then Microsoft, then Google where the account holds a calendar whose `source_provider` is `gmail` and a token was found; `Nowhere` otherwise. Cases: a Google-only account asked at Google; a Google account with no token answers Nowhere, not an error; an account with a calendar server keeps it.
    - `oauth::a_google_token_for(account) -> Option<String>`, one expression over `AuthManager::new(account, "gmail", ..).get_valid_token()`, beside `a_graph_token_for`.
    - The worker in `managers::asking_when_people_are_free` asks for the Google token only when the account's calendars include a Google one, and hands it to `where_to_ask` with `free_busy::GOOGLE_CALENDAR_BASE`, a public constant holding `https://www.googleapis.com/calendar/v3`, the value `google_api.rs:407` keeps private (so `google_api.rs` is not edited).
    - A reading in `tests/item_form_free_busy.rs`: the worker passes a Google token into `where_to_ask`, fetched inside `rt.spawn` and not on the window's thread; a companion plants the call outside the worker.
  </behavior>
  <action>
    Red first: the `asking_when_free` cases by module path and the reading
    bare. Then the argument, the token function and the call site, with
    `cargo build` between (per decision 3). `managers.rs` gains production
    code only; its reading lives in the target. Invoke `elegant-code`:
    one more argument, one more line in the worker, no second copy of how
    a token is got.

    Guard records: one on `asking_when_free.rs` whose break passes Google
    over, the Google-only case red; one on `managers.rs` whose break stops
    handing the Google token, the reading red, `suite`
    `item_form_free_busy`; each measured. `managers.rs` stays at 137
    tests, quoted.
  </action>
  <verify>
    <automated>cargo test --lib application::asking_when_free:: && cargo test --test item_form_free_busy && cargo test --lib service::oauth:: && cargo test --lib presentation::managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the cases by module path and the reading bare.
    - `grep -c 'pub async fn a_google_token_for' src/service/oauth.rs` is 1; `grep -c 'a_google_token_for' src/presentation/managers.rs` is at least 1.
    - `application::asking_when_free::` above 11, `presentation::managers::` at 137, both quoted; the records measured.
  </acceptance_criteria>
  <done>Find when everyone is free on a Google account asks Google, with the sign-in it already has, from the worker.</done>
</task>

<task type="auto">
  <name>Task 3: the privacy page, the shortcuts row, the alpha page, the changelog and the ledger</name>
  <files>docs/privacy.md, docs/KEYBOARD_SHORTCUTS.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/privacy.md:287-305` and `:480-487`; `docs/KEYBOARD_SHORTCUTS.md:183-185`.
  </read_first>
  <action>
    Invoke `writing-craft`. The privacy page: the free/busy section says
    the question goes to your calendar server, to Microsoft or to Google,
    whichever holds the account's calendar, with the same content; the
    summary line at `:483` follows. The shortcuts page's Find when
    everyone is free row says "your calendar" rather than "your calendar
    server". The alpha page: Google's answer has never been read from a
    real account, and a guest outside your organisation is likely to come
    back not shared. The changelog, `[Unreleased]`, Added, #57's first
    point.

    Ledger, by hand, both halves: one `unrun-verify` for phase 14 (a
    Google account's free/busy for a colleague and for a guest outside
    the domain); one `unrun-verify` for the tester's ear (the not-shared
    sentence). One documents commit on the branch before the merge
    carries the pages, the ledger, the summary and the four completion
    marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test every_number_carries_its_command_and_its_date && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Google' docs/privacy.md` grew, quoted before and after; the changelog names #57.
    - The ledger entries in both halves; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The privacy page names Google as a place a free/busy question can go, and what a real account settles is ledgered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a guest list to Google | addresses and a window of dates sent to a third party |
| Google's JSON to the answer | untrusted text deciding whether somebody is free |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-29-01 | Tampering | a guest Google cannot find counted free | high | mitigate | `notFound` is `NotKnown(NotSharedWithYou)`; a case and a guard record; absent addresses stay unknown |
| T-13-29-02 | Information disclosure | a question sent to Google for an account with no Google calendar | medium | mitigate | the token is asked for and Google is chosen only where a `gmail` calendar is held; a case |
| T-13-29-03 | Denial of service | the token refresh on the window's thread | low | mitigate | fetched inside the worker; a reading with a companion |
| T-13-29-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is spoken and shown (answers from Google, a new reason): push the branch, open a pull request, wait for CI, NVDA and Accessibility, then merge locally. Pushing `main` is Pratik's.
- Network code is tested against recorded answers and a test server's received request, never Google. Red commits name lib tests by module path and integration tests bare; the count check named bare for `when_people_are_free.rs`. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- GAP-08's first `[D]` line has a named test or reading in the summary and is marked with the date; the box waits for 13-31.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-08's `[D]` line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-29-SUMMARY.md` when done
</output>
