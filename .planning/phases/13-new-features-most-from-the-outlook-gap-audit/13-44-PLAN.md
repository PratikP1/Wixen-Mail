---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 44
type: execute
wave: 46
depends_on: [13-43, 13-24.1]
files_modified:
  - src/presentation/wx_app.rs
  - src/presentation/ui_types.rs
  - src/presentation/wx_managers.rs
  - src/presentation/managers.rs
  - tests/a_rule_runs_over_a_folder_when_asked.rs
  - tests/the_list_at_two_hundred_thousand_rows.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - docs/development/measurements.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-12]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Action, This Folder, Run a Rule on This Folder... (L) offers the account's rules, switched off ones marked, counts on a worker what the chosen rule would change in the folder on screen, and asks before anything changes in a native question NVDA reads on open, with Enter answering no for a rule that deletes; nothing to change is said and nothing asked (GAP-12: Run Rule Now on the Action menu; the count said before the run with a way to stop)."
    - "The Filter Manager has Run on a Folder... (R), which saves the manager's changes, asks which folder of the account, and then does the same (GAP-12: Run Rule Now where rules are made and edited)."
    - "Yes hands the counted set to 13-24.1's runner, which carries the rule's change out through the gated paths the set commands use, and one sentence names the rule, the count and where; the account's Allowed Changes answer is met before the question is asked, so a refusal is said instead of a question that cannot be carried out (GAP-12: the run through the same arms a check uses)."
    - "The question says a rule run has never met a real mail server when its change reaches one, the alpha page says what could go wrong, the ledger carries the real-server proof for phase 14, the count over 200,000 rows is timed and recorded, 13-43's stub entry is closed, GAP-12 is ticked and #61 closed from the merge."
  artifacts:
    - "src/presentation/wx_app.rs: Run a Rule on This Folder... and its arm, count_what_a_rule_would_change on a worker, the arm for UIUpdate::ARuleWasCounted that asks and runs, and the Filter Manager arm's handing on of a rule to run."
    - "src/presentation/ui_types.rs: UIUpdate::ARuleWasCounted."
    - "src/presentation/wx_managers.rs: build_filter_manager split out, the Run on a Folder... button for rows that run on a folder, FilterManagerAction::RunOnAFolder."
    - "src/presentation/managers.rs: manage_filters saving first and answering the rule to run."
    - "tests/a_rule_runs_over_a_folder_when_asked.rs: source readings that both doors count before they ask, ask before they write, meet the gate first and call the one runner; the built Filter Manager's button read over MSAA; the real menu bar's item; each with a planted-fault companion."
    - "tests/the_list_at_two_hundred_thousand_rows.rs: one timed step, the folder read and the count; docs/development/measurements.md: its row."
  key_links:
    - "This Folder, Run a Rule on This Folder -> choose_from_list over the account's rules -> count_what_a_rule_would_change (worker: messages_a_saved_search_reads, tags_by_message_in_folder, running_a_rule_now::what_a_rule_would_change, the_question) -> UIUpdate::ARuleWasCounted -> the gate -> MessageDialog with asking's style -> run_these_actions_over, 13-24.1's runner, with the_set_to_run -> WhatWasDone -> running_a_rule_now::what_the_rule_did."
    - "Filter Manager, Run on a Folder -> FilterManagerAction::RunOnAFolder -> manage_filters saves -> choose_from_list over the account's folders -> count_what_a_rule_would_change: one flow from the count on."
---

<objective>
Run a rule over a folder when asked, from the Action menu and from the Filter
Manager, saying first what it would change.

Purpose: #61: "A 'Run on this folder' command in the Filter Manager and on
the This Folder menu; a dry run that counts matches and asks before acting
('This rule would move 214 messages in Inbox. Run it?'); the same runner
Quick Steps would use; every action through the existing gated write paths
so the Allowed Changes answer still governs it." GAP-12's `[D]` line: "Run
Rule Now on the rule editor and the Action menu; the count said before the
run with a way to stop; the run through the same arms a check uses." 13-43
built the count and its words; 13-24.1 built the runner; this plan builds the
two doors, times the count at scale, and closes GAP-12.

Output: the This Folder item and its flow, the worker and the question, the
Filter Manager's button, the reading, the timed row, the pages, the alpha
page's line, the changelog, the ledger, GAP-12 ticked, #61 closed.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-24.1-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-43-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only, and against the
plans of 13-24, 13-24.1 and 13-43 as designed that day. Command first, then what it
said. Line numbers are places to start; 13-01 to 13-43 land first.

**1. What this plan closes on the ledger.** 13-43 files a `stub` entry, `file`
`src/application/running_a_rule_now.rs`, saying the count and the question
are built and nothing asks them, for this plan to close; its number is in
13-43's summary. RESEARCH-4 F2, that an arrival rule's Mark as read and Flag
are written to this computer only and nothing queues them for the server, is
"a ledger entry for whoever owns rules next"; the executor greps the ledger
for it (`grep -n -i 'flag sync\|arrival rule' .planning/WINDOWS.md`) and files
it if no entry names it, since an on-demand run goes through the set paths
and does not share the gap.

**2. "The rule editor" in the `[D]` line.** Rules are made and edited in the
Filter Manager (`wx_managers.rs:3048-3089`, Tools, Message &Filters... at
`wx_app.rs:7480`); the per-rule editor it opens (`build_filter_edit_dialog`,
`:3840`) has OK and Cancel and holds a rule not yet saved. So Run Rule Now
goes on the Filter Manager, over a saved rule, and on Action, This Folder,
which is how RESEARCH-4 and #61 read it.

**3. The letters.**

```
awk 'NR>=7147 && NR<=7276' src/presentation/wx_app.rs | grep -o '"[^"]*&[^"]*"'   -> This Folder: &Refresh, Get &Older, Re&name, &Move Folder, Delete Fol&der, Empt&y, Mar&k Folder Read, Move &Up, Move Do&wn, &Pin, Unp&in: R, O, N, M, D, Y, K, U, W, P, I; L free
Read src/presentation/wx_managers.rs:312-356   -> the Filter Manager's buttons are the loop's &Add, &Edit, &Delete, &Close; FilterRule's ManagedRow::moved is None (:255), so no Move buttons; R free
Read src/presentation/wx_managers.rs:5045-5100 (build_choose_from_list_dialog)   -> the list takes the caller's label and its OK the caller's word; Cancel carries no letter
```

"Run a Ru&le on This Folder..." takes L; "&Run on a Folder..." takes R.
`grep -n 'Run a Rule\|Run on a Folder' docs/KEYBOARD_SHORTCUTS.md` finds
nothing, and the page's Rules Management rows (`:736-741`) name no letter
in the Filter Manager. The rule chooser is "&Rules in Work:" with "&Count" (R, C) and the folder chooser
"&Folders in Work:" with "&Count" (F, C). The executor greps each menu's and
dialog's `&` letters again before writing, since 13-37 edited This Folder's
help text and earlier plans may add items.

**4. The question's buttons, read in the toolkit.**

```
grep -n 'pub fn ' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/dialogs/message_dialog.rs   -> builder, show_modal, as_ptr, new, with_style, build: no way to relabel Yes and No
grep -n 'pub fn yes_no_where_enter_answers_no\|pub fn yes_no_where_enter_answers_yes\|pub fn which_of_the_two' src/presentation/asking.rs   -> :40, :65, :106; a house style check holds that nothing outside this file names the flag that puts two answers on a box
```

RESEARCH-4 asked for buttons reading Run and Don't Run. wxDragon 0.9.17's
`MessageDialog` cannot relabel its buttons, so the question ends "Run it?"
and is answered Yes or No, with Enter's answer from 13-43's
`enter_answers_yes` through `asking`'s two styles.

**5. The folder's messages, its labels, and the worker.**

```
grep -n 'pub fn messages_a_saved_search_reads\|pub fn tags_by_message_in_folder' src/data/message_cache/saved_searches.rs src/data/message_cache/tags.rs   -> saved_searches.rs:382 (account, Some(folder_id), text: the message text only when asked), tags.rs:278
grep -n 'pub fn a_rule_reads_the_message_text' src/application/filters.rs   -> :295, whether the text is worth reading for this rule
grep -n 'fn folder_on_screen\|const WHICH_SAVED_SEARCH\|fn run_a_saved_search' src/presentation/wx_app.rs   -> :19035, :10195, :7949 (a worker read sending its answer back through a UIUpdate, the shape the count takes)
```

A saved search open on screen is not a folder; the This Folder door refuses
it in the sentence the file already has for a mailbox command asked of a
saved search (`wx_app.rs:10197` onward).

**6. The gate, met before the question.** A set move meets
`outward::permitted(allowed_for(account).mail, doing)` per account before
anything changes (`wx_app.rs:21363-21371`). 13-24.1's runner meets it
again, per account, before any do-half (13-24.1's plan), which covers the
second gate. This plan asks the same predicate first, through whatever
13-24.1's summary names, so a refusal is said in place of a question whose
Yes would be refused.

**7. The gate for each file, at `630e2a67`.** `src/presentation/wx_app.rs`
199 tests and 119 records, none added; `src/presentation/ui_types.rs` 79 and
6, a variant and no test; `src/presentation/wx_managers.rs` 44 and 14, none
added; `src/presentation/managers.rs` 137 and 54, none added;
`tests/the_list_at_two_hundred_thousand_rows.rs` 10 and 1, a timed step
inside `measure` and no test added; the new target at zero. Re-taken before
the first commit, before and after quoted. No record anchors in
`wx_app.rs:7147-7280`, `wx_managers.rs:230-470` or `:3020-3180`, or
`the_list_at_two_hundred_thousand_rows.rs:256-460` (the register read with
`tomllib`); read again before each commit.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation; Pratik may overrule.**
Pratik confirmed the order of phase 13 and said "plan and implement phase 13"
on 2026-09-24.

1. **A switched-off rule can be run by hand (RESEARCH-4 question 6)**; the
   chooser marks it "(switched off)" so nobody runs one thinking it is on.
2. **Which answer Enter gives (question 8)**: yes, except for a rule that
   deletes, where no.
3. **Above 5,000 (question 4)**, as 13-43 reads it: a run takes the first
   5,000 and the question says so.
4. **The runner is 13-24.1's (question 12).**
5. **No entry on a folder row's context menu.** RESEARCH-4 listed one "if
   Pratik wants it there too"; the `[D]` line names two doors, and a third
   is his to ask for.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run a Rule on This Folder, counted on a worker, asked, and run</name>
  <files>src/presentation/wx_app.rs, src/presentation/ui_types.rs, tests/a_rule_runs_over_a_folder_when_asked.rs, guards/guards.toml</files>
  <read_first>
    - 13-24.1's summary, whole: the runner's name, what it answers (a `WhatWasDone`; it speaks no sentence), the gate's predicate. 13-43's summary and `src/application/running_a_rule_now.rs`.
    - `src/presentation/wx_app.rs:7147-7280`, `:7940-8130` (a saved search run on a worker), `:10190-10215`, `:19035-19050`, `:21040-21160`, and the arm that runs a set move.
    - `src/presentation/asking.rs`, whole; `src/presentation/ui_types.rs:680-720`.
    - `tests/every_command_acts_on_the_selection.rs`, a source reading and its companion.
  </read_first>
  <behavior>
    - "Run a Ru&le on This Folder..." (`ID_RUN_A_RULE_HERE`) on This Folder after Mar&k Folder Read, its help ending with `RUNNING_A_RULE_NOW_IS_EXPERIMENTAL`. Its arm: the folder on screen and the account being worked in, or the refusals the file has (no folder chosen; a saved search open); the account's rules, none refused with "Work has no rules yet. Message Filters on the Tools menu makes one."; `choose_from_list(frame, "Run a Rule on This Folder", "&Rules in Work:", "&Count", names)`, a switched-off rule shown as "Newsletters (switched off)"; Cancel changes nothing and says nothing.
    - `count_what_a_rule_would_change(app, cache, account, folder, rule)`: the step sentence "Counting what Newsletters would change in Inbox..." once on the status line; on a worker, `messages_a_saved_search_reads(account, Some(folder_id), text)` with the text only when `a_rule_reads_the_message_text(rule.field)`, `tags_by_message_in_folder(folder_id)`, `what_a_rule_would_change`, and either `the_question` and `the_set_to_run` or `nothing_to_change`, sent back as `UIUpdate::ARuleWasCounted(Box<...>)` holding the account, the rule's name and `Outcome`, the words, Enter's answer and the set; a read that fails is said in a sentence naming the folder.
    - The arm for `ARuleWasCounted`: nothing to change is said at Normal and nothing asked; otherwise the gate's predicate first (premise 6), a refusal said instead; otherwise a `MessageDialog` with the question and `asking::yes_no_where_enter_answers_no()` or `..._yes()`, `which_of_the_two` read; Yes hands the set and the `Outcome` to 13-24.1's runner once, which answers a `WhatWasDone` and says nothing, and `what_the_rule_did` is said once over that answer with one `Confirmed`; No says nothing and changes nothing.
    - The target's readings over `what_ships`: the item's arm calls `choose_from_list(` before `count_what_a_rule_would_change(`; the worker calls `what_a_rule_would_change(` and sends `ARuleWasCounted`; the counted arm meets the gate before it builds the `MessageDialog`, builds it before it calls the runner, and calls the runner once; no write of its own in either function; the real menu bar's This Folder holds the item with the experimental sentence in its help. Each with a planted-fault companion (the runner before the question, the gate after it, the count skipped), each refused.
  </behavior>
  <action>
    Red first, on a branch: the target's readings bare, with the item's id,
    the update variant and the functions present and answering nothing. Run
    the target first and name everything that fails.

    Then the green commits, `cargo build` between: the item and its arm;
    the worker and the update; the question and the run. Invoke
    `elegant-code`: the worker computes and sends, the arm decides and asks,
    the runner writes; one sentence at each end; no `unwrap`. The executor
    greps This Folder's letters before writing L (premise 3).

    Guard records: one on `wx_app.rs` whose break calls the runner before
    the question, the order reading red, `suite` the target; one whose break
    drops the gate before the question, its reading red, `suite` the target;
    each measured, with every flagged record, in one `--remeasure` call per
    commit.
  </action>
  <verify>
    <automated>cargo test --test a_rule_runs_over_a_folder_when_asked && cargo test --test wired && cargo test --lib presentation::wx_app:: && cargo test --lib presentation::ui_types::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's readings bare.
    - `grep -c '"Run a Ru&le on This Folder..."' src/presentation/wx_app.rs` is 1; `grep -c 'fn count_what_a_rule_would_change' src/presentation/wx_app.rs` is 1; `grep -c 'UIUpdate::ARuleWasCounted' src/presentation/wx_app.rs` is at least 2.
    - `cargo test --test wired` passes, so L is This Folder's once; `presentation::wx_app::` and `presentation::ui_types::` with the counts they had before, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>From This Folder a rule is chosen, counted on a worker, asked about in a native question, and run through the one runner and its gate in one sentence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run on a Folder in the Filter Manager, saving first, then the same flow</name>
  <files>src/presentation/wx_managers.rs, src/presentation/managers.rs, src/presentation/wx_app.rs, tests/a_rule_runs_over_a_folder_when_asked.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_managers.rs:230-470`, `:3024-3120`, `:5040-5140`.
    - `src/presentation/managers.rs:360-470` (`manage_filters` and its save); `src/presentation/wx_app.rs:5640-5660` (the Filter Manager's arm).
    - `tests/the_about_dialog_names_its_owners_and_its_links.rs:130-300` (a built window's children over MSAA).
  </read_first>
  <behavior>
    - `ManagedRow` gains `fn runs_on_a_folder() -> bool { false }`, answered `true` by `FilterRule` only; the loop adds "&Run on a Folder..." (`ID_MGR_RUN_ON_A_FOLDER`) for such rows, which ends the modal with that id when a row is chosen and says "Choose a rule first." when none is; every other manager's buttons are unchanged, which `tests/manager_dialog_labels.rs` and `tests/wired.rs` go on holding.
    - `build_filter_manager(parent, rules, palette)`, public, split out of `show_filter_manager_dialog` as `build_tag_manager` was; `FilterManagerAction::RunOnAFolder { rules, which }` when the button ended the loop.
    - `managers::manage_filters` answers `Option<RunARuleNow { account, rule_id }>`: on `RunOnAFolder`, the rules saved first through `save_what_the_filter_manager_returned`, a failure said and nothing run. The Filter Manager's arm in `wx_app.rs`, given a rule, asks `choose_from_list(frame, "Run on a Folder", "&Folders in Work:", "&Count", paths)` over the account's folders from `folders_in_the_tree` by path, then `count_what_a_rule_would_change`, the flow task 1 built from the count on.
    - The target gains: the built Filter Manager's children read and the button's MSAA name "Run on a Folder..." at its own handle; a built Label Manager holding no such button; source readings that `manage_filters` saves before it answers a rule to run and that the arm asks for the folder before it counts; companions refused.
  </behavior>
  <action>
    Red first, on the same branch: the target's Filter Manager readings
    bare, with the public signatures answering nothing. Run them first and
    name everything that fails.

    Then the green commits, `cargo build` between. Invoke `elegant-code`:
    the button is the loop's, asked of the row type as Move is; the flow
    from the count on is task 1's, called, never copied.

    Guard records: one on `managers.rs` whose break runs before it saves,
    its reading red, `suite` the target; one on `wx_managers.rs` whose break
    gives every manager the button, the Label Manager reading red, `suite`
    the target; each measured.
  </action>
  <verify>
    <automated>cargo test --test a_rule_runs_over_a_folder_when_asked && cargo test --test manager_dialog_labels && cargo test --lib presentation::wx_managers:: && cargo test --lib presentation::managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's Filter Manager readings bare.
    - `grep -c '"&Run on a Folder..."' src/presentation/wx_managers.rs` is 1; `grep -c 'fn runs_on_a_folder' src/presentation/wx_managers.rs` is at least 2; `grep -c 'pub fn build_filter_manager' src/presentation/wx_managers.rs` is 1.
    - The target passes with at least 14 tests, quoted; `manager_dialog_labels`, `presentation::wx_managers::` and `presentation::managers::` with the counts they had before, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>From the Filter Manager a saved rule runs over a folder somebody chooses, through the same count, question and runner as This Folder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the count timed over 200,000 rows, and the row that records it</name>
  <files>tests/the_list_at_two_hundred_thousand_rows.rs, docs/development/measurements.md, guards/guards.toml</files>
  <read_first>
    - `tests/the_list_at_two_hundred_thousand_rows.rs:1-120` (what is timed and the format-holding half) and `:160-460` (`a_cache_of`, `taken`, `measure`).
    - `docs/development/measurements.md:19-46` (how to add a row) and `:100-180` (what the scale rows time).
  </read_first>
  <behavior>
    - `measure` gains one timed step, named "a rule counted over the folder": `messages_a_saved_search_reads(account, Some(folder), without text)` and `what_a_rule_would_change` for a mark-read rule on the subject, over the cache the harness already builds; the format-holding half at 2,000 rows runs on every commit and holds the new row's shape as it holds the others; no test added.
    - The run at 200,000 rows taken once with the harness's own command, in release, and its figure written on `measurements.md` as one row: what, value, command, date, commit, conditions (the file warm, no window, the count only and not the run), and one sentence in "What the scale rows time" saying what this row times and what it does not.
  </behavior>
  <action>
    Red first, on the same branch: "a rule counted over the folder" joins
    the file's `WHAT_A_ROW_TIMES` list, so
    `test_every_row_the_measurement_prints_has_the_pages_shape_and_names_what_it_timed`
    (`tests/the_list_at_two_hundred_thousand_rows.rs:877`, which asserts a
    printed row for every kind in that list) runs and fails, named bare. No
    test is added, so the count check is not flagged. Run it first and name
    everything that fails.

    Then the green commit with the step, then the measurement taken with
    `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1`,
    its output written to a file and read, and the row written from it.
    Invoke `writing-craft` for the row's conditions.

    The record naming this file is re-measured if the count check flags it.
  </action>
  <verify>
    <automated>cargo test --test the_list_at_two_hundred_thousand_rows</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'a rule counted over the folder' tests/the_list_at_two_hundred_thousand_rows.rs` is at least 1; `grep -c 'a rule counted over the folder' docs/development/measurements.md` is 1, in a row naming the command, the date and the commit.
    - `cargo test --test the_list_at_two_hundred_thousand_rows` passes with the count it had before, quoted; the 200,000-row figure is quoted in the summary with its command.
  </acceptance_criteria>
  <done>What somebody waits for between choosing a rule and hearing the question is measured at 200,000 rows and written down with its conditions.</done>
</task>

<task type="auto">
  <name>Task 4: the pages, the alpha page, the changelog, the ledger, GAP-12 ticked, and #61 closed</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:661-760` (Action Menu, This Folder's row, Rules Management, the filter editor's letters).
    - `docs/USER_GUIDE.md:521-566` ("The rules run once, when mail arrives" at `:541`).
    - `docs/ALPHA_TESTING.md:245-300`; 13-43's summary for its ledger entry's number.
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: This Folder's row names Run
    a Rule on This Folder (`L`); Rules Management gains Run on a Folder
    (`R` in the Filter Manager); the chooser's letters. The guide: the
    sentence at `:541` that rules run once when mail arrives gains, dated,
    that a rule can now be run over a folder by hand, then the steps from
    either door, what the question says, that Enter answers no for a rule
    that deletes, the 5,000 bound and running it again, and that a switched
    off rule can be run by hand. The alpha page, under what is unproven: a
    rule run changes mail at the provider through Allow Changes and has
    never met a real server. The changelog, `[Unreleased]`, Added, in #61's
    words, with the Yes and No buttons explained in a sentence (premise 4);
    Known limitations: no run has met a real server, nobody has heard the
    question and the result as one act.

    Ledger, by hand, both halves: 13-43's `stub` entry closed; one
    `unrun-verify` for phase 14, a rule run over a folder of thousands at a
    real provider's pace, a move and a delete on Gmail and on a server with
    folders; one `unrun-verify` for the tester's ear (the chooser, the
    question read on open, the result); F2's entry if premise 1's grep finds
    none.

    GAP-12: its box and traceability row ticked with the date, and the
    dated line under its `[D]` line naming 13-43's module and this plan's
    target for each part. The four marks: phase 13's progress row; this
    plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase
    README keeps no boxes) ticked; `STATE.md`'s plan number in the frontmatter and the
    body; GAP-12 as above. Run
    `cargo test --test the_planning_files_agree_with_themselves` first.

    This plan changes what is spoken and shown: after this one documents
    commit the branch is pushed with a pull request into `main` under
    Pratik's standing OK, CI, NVDA and Accessibility read before the local
    merge, the `filters` scan target's walk of the new button read in the
    Accessibility log. After the merge, `gh issue close 61 --comment "..."`
    from the merge commit, naming what was built and what waits for a real
    account. Closing an issue is not a publish.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Run a Rule on This Folder' docs/KEYBOARD_SHORTCUTS.md` is at least 1 and `grep -c 'Run on a Folder' docs/KEYBOARD_SHORTCUTS.md` is at least 1; `grep -c 'rule run' docs/ALPHA_TESTING.md` is at least 1; `grep -c '^- \[x\] \*\*GAP-12\*\*' .planning/REQUIREMENTS.md` is 1.
    - 13-43's entry reads fixed in both halves; the new entries in both halves; carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say how to run a rule over a folder from either door, the alpha page says what could go wrong, the ledger carries the real-server proof and the ear, GAP-12 is ticked, and #61 is closed from the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a question answered by Enter to the server | one key that can move or delete a folder's mail at a provider |
| the Filter Manager's unsaved edits to a run | a rule run must be the rule as saved |
| a rule imported rather than typed to a folder's worth of mail | a pattern run over thousands of messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-44-01 | Elevation of privilege | a run on an account whose changes are off | high | mitigate | the gate asked before the question and again by 13-24.1's runner; the order read in the target; a guard record |
| T-13-44-02 | Tampering | a run before anybody answered | high | mitigate | the question built before the runner is called, read in order; a guard record |
| T-13-44-03 | Tampering | Enter deleting a folder's mail partway through the question | high | mitigate | `asking::yes_no_where_enter_answers_no` for a rule that deletes, from 13-43's data |
| T-13-44-04 | Tampering | a run of a rule other than the one saved | medium | mitigate | the manager saves before it answers a rule to run; read in the target; a guard record |
| T-13-44-05 | Denial of service | a large folder's count freezing the window and the screen reader | medium | mitigate | the count on a worker, a step sentence while it runs, its cost measured at 200,000 rows |
| T-13-44-06 | Tampering | a run touching another account's folder of the same path | medium | mitigate | the folder is on screen or chosen from the rule's own account's folders |
| T-13-44-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes what is spoken and shown: after the one documents commit, the branch is pushed with a pull request into `main` (Pratik's standing OK of 2026-09-23), CI, NVDA and Accessibility read on it, a red one fixed on the branch; the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What a real account settles, phase 14: a run at a provider's pace and its changes landing. What the tester's ear settles: the chooser, the question on open, the result as one act. What the readings prove: both doors count before they ask, ask before they write, meet the gate first and call the one runner; the button's name over MSAA; the count's cost at scale.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 12 closes structurally: a rule can be run over a folder on demand, saying first how many messages it would touch.
- GAP-12's `[D]` line has a named test or reading for each part and its box is ticked with the date; its `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-12's line and traceability row.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-44-SUMMARY.md` when done
</output>
