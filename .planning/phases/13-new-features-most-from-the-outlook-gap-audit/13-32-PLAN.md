---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 32
type: execute
wave: 34
depends_on: [13-31]
files_modified:
  - src/presentation/wx_item_form.rs
  - tests/the_event_form_scrolls_to_the_field_in_focus.rs
  - guards/guards.toml
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-08]
estimate:
  tokens: 19200
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a screen 768 pixels tall, or with text at twice its size, every field of Edit Event can be reached and seen: the form's fields sit in a scrolled area, the window is never taller than the screen's working area, and the field that takes focus is scrolled into view (ledger 417, #57 point 4; WCAG 1.4.10, 1.4.4, 2.4.11)."
    - "Save, Cancel and the line that says why Save refused stay outside the scrolled area, always visible, so the person can always keep or leave the form."
    - "The task, reminder and note forms, which open through the same builder, keep their layout where they fit and scroll where they do not."
    - "Focus order and names are unchanged: the scrolled area adds no stop to Tab and no unnamed control to either accessibility channel."
  artifacts:
    - "src/presentation/wx_item_form.rs: the fields of each page inside a ScrolledWindow with a scroll rate, the dialog's height capped to the display's working area, the buttons and the problem line outside it."
    - "tests/the_event_form_scrolls_to_the_field_in_focus.rs: the built event form at a small height and with a doubled font, focus moved to the last field, its rectangle read inside the visible area; a Tab walk that meets no extra stop."
  key_links:
    - "ask_for -> the page Panel -> a ScrolledWindow holding the fields -> wxWidgets' child-focus handling scrolls the focused field into view -> the dialog capped to the working area; Save and the problem line in the dialog's own sizer."
---

<objective>
Edit Event scrolls, so every field is reachable and visible on a small
screen and at large text.

Purpose: ledger 417 and #57's point 4: on the Accessibility runner's
768-pixel screen Edit Event is 720 pixels tall, Show as, Status,
Category and Times offered are six pixels tall at the bottom edge, and
the form does not scroll, so at 200% text the last four fields are cut
off and a keyboard user reaches a field nobody can see. RESEARCH-3's
question 8 asked whether this belonged in phase 13; the answer taken
puts it here, after 13-29 to 13-31 made the form's answer longer.

Output: the scrolled fields, the capped height, the reading target, the
changelog and ledger 417 closed.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. In this phase, after the free/busy plans (RESEARCH-3 question 8).**
The plan RESEARCH-CROSS-CHECK marked "only if Pratik says yes" is in, on
the brief's instruction that both conditional plans are in.

**2. wxWidgets' own scrolling (planner's choice, recorded).**
`wxdragon` 0.9.17 exposes `ScrolledWindow` with `set_scroll_rate`
(premise 1); wxWidgets scrolls a focused child into view in a scrolled
window by itself, so no focus handler is written unless the reading
shows it does not.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. 13-06 (K5b) edits the item form before this plan; re-take the
numbers after 13-31's merge.

**1. The form and the widget.**

```
awk -F'|' '$2+0==417' .planning/WINDOWS.md   -> open, todo: Edit Event 720 tall on a 768 screen, the last four fields six pixels tall, no scrolling; WCAG 1.4.10 and 1.4.4
sed -n 536,620p src/presentation/wx_item_form.rs   -> the dialog built with ResizeBorder and no height; fields built onto the dialog, or onto the One-Time and Recurrence pages of a Notebook; the free/busy controls onto the same page
sed -n 700,720p src/presentation/wx_item_form.rs   -> dialog.set_sizer_and_fit(sizer, true): sized by its content, so a long form is as tall as its fields
grep -rn 'ScrolledWindow' src --include=*.rs   -> nothing: no scrolled window anywhere in the tree yet
grep -n 'pub fn set_scroll_rate\|pub fn scroll_coords' ~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/scrolled_window.rs   -> :101, :139
```

**2. Guard records, both readings.** `wx_item_form.rs` 5 by
`tests_last_seen` and 5 by `file` at `630e2a67`, 16 tests, no test
added; by anchor the five records sit at `:384`, `:532`, `:1164`,
`:1191` and `:1839`; the scrolled area is built at `:546-620`, which
none of them lies in, so none is moved unless the executor restructures
the builder more widely, in which case each moved anchor is rewritten
and re-measured. Re-take after 13-06, which edits this file. The new
target at zero.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the fields in a scrolled area, the window capped to the screen, the focused field in view</name>
  <files>src/presentation/wx_item_form.rs, tests/the_event_form_scrolls_to_the_field_in_focus.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_item_form.rs:279-720` (`ask_for`, the pages, the problem line, the buttons, the fit).
    - `tests/event_times_move_in_blocks.rs` (12-08: the built form in a test process, how it is opened and read) and `tests/item_form_recurrence_tab.rs`.
    - `~/.cargo/registry/src/*/wxdragon-0.9.17/src/widgets/scrolled_window.rs:1-200`.
  </read_first>
  <behavior>
    - The target builds the event form with free/busy on, sets its client height to 480 pixels, moves focus to the last field on the One-Time page and to Times offered, and reads each field's rectangle inside the scrolled area's visible rectangle; the same with the form's font doubled before layout; Save and the problem line read inside the dialog's client rectangle in both.
    - The form's height never exceeds the display's working area; a form that fits keeps its content height.
    - A Tab walk from the first field to Save meets the same controls in the same order as before, with no stop on the scrolled area itself (the walk's list compared with the list read before the change, written into the target as data with the date).
    - The task and reminder forms still open and save through the existing targets.
  </behavior>
  <action>
    Red first, on the branch `13-32-edit-event-scrolls`: the target's
    readings bare. Then, with `cargo build` between, each page's fields
    built onto a `ScrolledWindow` with a scroll rate (per decision 2),
    the problem line and the buttons left in the dialog's own sizer, and
    the dialog's height capped to the display's working area after the
    fit. If the reading shows focus does not bring a field into view,
    the scrolled area scrolls to the focused child on the child-focus
    event, and the summary says which was needed. Invoke `elegant-code`:
    one helper builds a scrolled page for any field list, used by the
    single page and by both notebook pages.

    Guard record: one whose break takes the scroll rate away (the area
    cannot scroll), the small-height reading red, `suite` the new target;
    measured. `wx_item_form.rs` stays at 16 tests, quoted.
  </action>
  <verify>
    <automated>cargo test --test the_event_form_scrolls_to_the_field_in_focus && cargo test --test item_form_free_busy && cargo test --test item_form_recurrence_tab && cargo test --test event_times_move_in_blocks && cargo test --test every_spin_control_names_the_field_a_person_types_in && cargo test --lib presentation::wx_item_form::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the readings bare.
    - `grep -c 'ScrolledWindow' src/presentation/wx_item_form.rs` is at least 1.
    - The target passes with at least 5 readings, quoted; the existing item form targets pass unchanged in count, quoted; one guard record measured.
  </acceptance_criteria>
  <done>Every field of Edit Event can be reached and seen at 480 pixels and at twice the text size, and Save is always on screen.</done>
</task>

<task type="auto">
  <name>Task 2: the changelog, ledger 417 closed, and the scan's reading</name>
  <files>docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md`, entry 417 in both halves.
  </read_first>
  <action>
    Invoke `writing-craft`. The changelog, `[Unreleased]`, Fixed: the
    event form scrolls, and its last four fields are reachable at small
    sizes and large text (#57 point 4, ledger 417). Ledger 417 closed as
    fixed in both halves, naming this plan and, after the pull request's
    Accessibility run, the run's number and what it read for the
    `new-event` target; if the scan still finds the fields cut off, the
    entry stays open with the run quoted and the plan's summary says so.
    One `unrun-verify` for the tester's ear and eye (the form at 200%
    text, a field reached by Tab and seen). One documents commit on the
    branch before the merge carries the changelog, the ledger, the
    summary and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - Ledger 417 closed in both halves with the scan run quoted, or left open with it quoted; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The defect the scan found is closed on the scan's own reading, or stays open with the reading that says why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | layout only; no input crosses a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-32-01 | Denial of service | Save scrolled out of reach, so nothing can be kept | high | mitigate | Save and the problem line stay in the dialog's own sizer; the reading holds both inside the client area at small sizes |
| T-13-32-02 | Denial of service | a new Tab stop or an unnamed pane for a screen reader | medium | mitigate | the Tab walk compared with the list before; the scan reads both channels on the pull request |
| T-13-32-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in both `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- This plan changes what is shown (the form's layout): push the branch, open a pull request, wait for CI, NVDA and Accessibility (the `new-event` target reads the form), then merge locally. Pushing `main` is Pratik's.
- Red commits name integration tests bare. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- Ledger 417 is closed on the scan's reading or left open with it; #57 point 4 is answered in the summary. GAP-08's `[D]` lines are 13-29 to 13-31's; this plan ticks none.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-08's traceability row noting this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-32-SUMMARY.md` when done
</output>
