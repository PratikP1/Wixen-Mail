---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 41
type: execute
wave: 43
depends_on: [13-40]
files_modified:
  - src/presentation/wx_managers.rs
  - src/presentation/managers.rs
  - src/presentation/manager_words.rs
  - src/presentation/wx_app.rs
  - src/presentation/scan_target.rs
  - src/presentation/scan_fixtures.rs
  - .github/workflows/accessibility.yml
  - tests/the_quick_step_manager_says_what_each_step_does.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/changelog.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-11]
estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The Action menu has a Quick Steps submenu (Q) holding Manage Quick Steps... (M), whose help is the experimental sentence, and it opens the Quick Step Manager for the account being worked in (the decision of 2026-09-24: the manager lives inside Action, Quick Steps)."
    - "The manager lists the account's steps in their order with the columns Name, Key and What it does, and adds, edits, deletes and moves a step with Move Up and Move Down or Alt+Shift+Up and Alt+Shift+Down; the order and every change are written when it closes; a step a newer version wrote is listed, can be moved and removed, and is not opened for editing."
    - "The step editor asks one question per control: Name, Mark as read or unread, Flag, Label, Move to (the account's folders, chosen and stored by path), Delete it, and Phrase to say first; its letters are N, R, F, L, M, D and H, one each; each control is named at the handle focus reaches and read over MSAA on the built window; what stops a step being saved is said before OK closes and leaves the window open."
    - "Both windows are scan targets the Accessibility workflow walks, and nothing a step does can run yet: the changelog says so plainly, and 13-42 runs them."
  artifacts:
    - "src/presentation/wx_managers.rs: QuickStepEntry with ManagedRow::moved, build_quick_step_manager and populate_quick_steps, show_quick_step_manager_dialog, build_quick_step_edit_dialog, what_the_editor_holds and show_quick_step_edit."
    - "src/presentation/managers.rs: manage_quick_steps and save_what_the_quick_step_manager_returned."
    - "src/presentation/wx_app.rs: the Quick Steps submenu with Manage Quick Steps..., its arm, and two scan target arms."
    - "tests/the_quick_step_manager_says_what_each_step_does.rs: the manager's rows, Key column and a move stored; the editor's names over MSAA and its controls read into an Outcome; the submenu on the real menu bar; each with a planted-fault companion."
  key_links:
    - "Action, Quick Steps, Manage Quick Steps... -> ID_QUICK_STEP_MGR -> managers::manage_quick_steps -> show_quick_step_manager_dialog -> run_manager_loop -> show_quick_step_edit -> what_the_editor_holds -> quick_steps::name_for and what_stops_a_step_being_saved -> save_what_the_quick_step_manager_returned -> create, replace, delete, put_quick_steps_in_order."
---

<objective>
The Quick Step Manager, and the editor that makes a step.

Purpose: #60 asked for "a manager beside the Filter Manager"; GAP-11's `[D]`
line needs a step to be named, which is a window somebody works by keyboard.
Pratik's settlement of 2026-09-24 puts the manager inside the Action menu,
under Quick Steps, as "Manage Quick Steps...", rather than on Tools, where
only Q was free and the PGP key manager also wanted a letter. 13-40 built
the rules and the store; 13-42 fills the same submenu with the steps and runs
them.

Output: the manager and its rows on the loop every manager uses, the step
editor as a form, the submenu and its item, two scan targets, the reading,
the pages, the changelog, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-40-SUMMARY.md
@.planning/phases/12-the-editors-and-what-the-alpha-still-owes/12-10-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only. Command first, then
what it said. Line numbers are places to start; 13-01 to 13-40 land first.

**1. The loop, the Label Manager as the model, and the helpers to call.**

```
grep -n 'fn run_manager_loop\|pub trait ManagedRow\|impl ManagedRow for TagEntry\|pub fn build_tag_manager\|pub fn move_the_chosen_row\|pub fn populate_tags\|fn add_field\|fn add_checkbox\|fn a_sub_dialog_needs\|pub fn make_shell' src/presentation/wx_managers.rs   -> :295, :236, :257, :4227, :4257, :4300, :59, :79, :2278, :534
Read src/presentation/wx_managers.rs:312-356   -> the loop's buttons &Add, &Edit, &Delete, Move &Up and Move Do&wn where the row's ManagedRow::moved answers, &Close: A, E, D, U, W, C
grep -n 'pub fn manage_tags\|pub fn save_what_the_tag_manager_returned\|fn manager_account' src/presentation/managers.rs   -> :106, the save beside it, :35; the order written on Close, the tree read again after, which is what puts a change on the menu
grep -n 'pub(crate) const' src/presentation/manager_words.rs   -> :12-22, one word per kind of row; a Quick Step is a seventh
```

Two records anchor inside helpers this plan calls and must not copy: "no
status line is built with a label that is only a space" (`make_shell`,
`wx_managers.rs:550-555`) and "a checkbox in an editor grid is named
outright, not left to the fallback" (`add_checkbox`, `:80-82`). So the
editor's check box comes from `add_checkbox` and its text boxes from
`add_field`.

**2. The Action menu's letters, and where the submenu goes.**

```
awk 'NR>=7278 && NR<=7440' src/presentation/wx_app.rs | grep -n '"[^"]*&[^"]*"\|append_submenu'   -> R, A, O, F, U, N, X, E, S, I, K, P, D, M, V, C, W, Y, L, G, B, T, H taken; J, Q, Z free at 630e2a67
```

13-22 (Report as Junk) takes J before this plan runs, and the cross-check
leaves Q for Quick Steps. The executor greps Action's letters again before
writing "&Quick Steps", since other phase 13 plans edit the menu first. The
submenu is new, so "&Manage Quick Steps..." has M to itself; 13-42 puts the
steps above it with no letter. `tests/wired.rs:1977`'s letter check reads a
menu by `let NAME = Menu::builder()` and by every line starting
`NAME.append` (`tests/wired.rs:1872-1903`), so the submenu is bound as
`quick_steps_menu` and 13-42's rebuild takes a parameter of that name.

**3. The editor's letters, allocated once for the new window.** "&Name:" N,
"Mark as &read or unread:" R, "&Flag:" F, "&Label:" L, "&Move to:" M, "&Delete
it" D, "P&hrase to say first:" H (the rule editor's own letter for the same
words, `wx_managers.rs:3148`); OK and Cancel carry none, as the filter
editor's do (`:3943-3948`). Seven letters, seven controls, none shared.
`grep -n -i 'quick step' docs/KEYBOARD_SHORTCUTS.md` finds nothing at
`630e2a67`, so the page claims no letter in either window or on the
submenu.

**4. Where the folders and labels come from.** `folders_in_the_tree`
(`wx_app.rs:12776`, private) gives the folders the tree shows, and a step
stores a folder by its path (13-40, decision 3); it is made `pub(crate)` and
called by `manage_quick_steps`, so the editor offers what the tree shows and
nothing else. Labels come from `get_tags_for_account` in their order
(`tags.rs:78`).

**5. The scan targets, and the workflow's anchored lines.** `ScanTarget::ALL`
(`scan_target.rs:229`) and the workflow's list (`accessibility.yml:176-182`
at `630e2a67`, a line longer after 13-39). Two records anchor whole lines of
that list (13-39's premise 3 quotes them); the two new names go on the last
line of the list or a new one after it, and the executor reads the list's
records with the parser first. The `.github` commit answers `all`.

**6. The gate for each file, at `630e2a67`.** `src/presentation/wx_managers.rs`
44 tests and 14 records, none added; `src/presentation/managers.rs` 137 and
54, none added; `src/presentation/manager_words.rs` 8 and 1, a constant and
no test; `src/presentation/wx_app.rs` 199 and 119, none added;
`src/presentation/scan_target.rs` 11 and 4 and `scan_fixtures.rs` 11 and 0,
none added; the new target at zero. Re-taken before the first commit, before
and after quoted.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation, or settled by Pratik
the same day; Pratik may overrule.**

1. **The manager lives inside Action, Quick Steps, as "Manage Quick
   Steps..."**, settled by Pratik on 2026-09-24, with a letter proven free in
   that submenu (M, premise 2).
2. **Action takes Q for Quick Steps (RESEARCH-4 question 7)**; the next new
   Action item goes on a submenu.
3. **A step's folder is chosen from the account's folders and stored by
   path (question 5).**
4. **Per account (question 9).**
5. **The planner's, carried from 13-40: one field per question**, one label
   per step, said on the pages.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the Quick Step Manager, its rows, its Key column, its moves, and what it writes</name>
  <files>src/presentation/wx_managers.rs, src/presentation/managers.rs, src/presentation/manager_words.rs, src/presentation/wx_app.rs, tests/the_quick_step_manager_says_what_each_step_does.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_managers.rs:120-470`, `:4140-4320` (the Label Manager, whole).
    - `src/presentation/managers.rs:30-60` and `:100-200` (`manager_account`, `manage_tags`, `save_what_the_tag_manager_returned`).
    - `tests/the_label_menu_says_the_labels_an_account_has.rs:373-440` (the manager built and read in a target).
    - `src/application/quick_steps.rs` and `src/data/message_cache/quick_steps.rs` as 13-40 left them.
  </read_first>
  <behavior>
    - `QuickStepEntry { id, name, does: Option<Outcome> }`, `None` for a step a newer version wrote; `impl ManagedRow` with `moved` over `quick_steps::moved`, so the loop gives it Move Up, Move Down and Alt+Shift+Up and Down.
    - `build_quick_step_manager(parent, steps, palette)`, public: `make_shell` titled "Quick Step Manager" over a list named "Quick Steps", columns Name, Key (`quick_steps::key_for` by place, blank past the third) and What it does (`what_it_does_in_words`, or "Written by a newer version of Wixen Mail; it can be moved or removed here" for `None`); `populate_quick_steps` fills it.
    - `show_quick_step_manager_dialog(parent, steps, labels, folders, a11y) -> QuickStepManagerAction` runs the loop with `manager_words::QUICK_STEP` ("Quick Step"); Edit on a `None` row says why it cannot open and changes nothing.
    - `managers::manage_quick_steps(state, cache, frame, tx, rt, a11y)`: the account from `manager_account`, its steps, labels and the tree's folders; on `Updated`, `save_what_the_quick_step_manager_returned(cache, account, stored, updated)`, public: new rows created, changed rows replaced, removed rows deleted, the whole order written with `put_quick_steps_in_order`, and any failure named through `report`, as the Label Manager's save does.
    - The target: a store in the test with one account holding four steps, one written with an action word no build knows; the manager built and its rows read off the live list (names, Keys `Ctrl+Shift+7` to `Ctrl+Shift+9` and a blank, the words for each, the newer version's row); a move made with `move_the_chosen_row` and the returned rows saved, the store's order read back; companions that plant a Key column past the third and a save that skips the order, each refused.
  </behavior>
  <action>
    Red first, on a branch: the target's manager readings bare, with the
    public signatures they need answering nothing. Run the target first and
    name everything that fails.

    Then the green commit, `cargo build` between. Invoke `elegant-code`:
    the manager is the Label Manager's shape with its own rows, one save
    function, no `unwrap`. `folders_in_the_tree` is made `pub(crate)` in
    `wx_app.rs` and called, not copied (premise 4).

    Guard records: one on `wx_managers.rs` whose break gives a fourth step
    a key, the Key reading red, `suite` the target; one on `managers.rs`
    whose break drops `put_quick_steps_in_order` from the save, the order
    reading red, `suite` the target; each measured.
  </action>
  <verify>
    <automated>cargo test --test the_quick_step_manager_says_what_each_step_does && cargo test --lib presentation::wx_managers:: && cargo test --lib presentation::managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's manager readings bare.
    - `grep -c 'pub fn build_quick_step_manager' src/presentation/wx_managers.rs` is 1; `grep -c 'impl ManagedRow for QuickStepEntry' src/presentation/wx_managers.rs` is 1; `grep -c 'pub fn manage_quick_steps' src/presentation/managers.rs` is 1; `grep -c 'put_quick_steps_in_order(' src/presentation/managers.rs` is at least 1.
    - `cargo test --lib presentation::wx_managers::` and `presentation::managers::` pass with the counts they had before the plan, quoted.
    - Two guard records measured and quoted.
  </acceptance_criteria>
  <done>The Quick Step Manager lists an account's steps in their order with their keys and what each does, moves them with the gesture the tree has, and writes what changed when it closes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the step editor, one question per control, refusing before OK closes</name>
  <files>src/presentation/wx_managers.rs, tests/the_quick_step_manager_says_what_each_step_does.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_managers.rs:55-100` (`add_field`, `add_checkbox`), `:2270-2290` (`a_sub_dialog_needs`), `:3100-3180` and `:3840-3990` (the filter editor: its `Choice`s, their names, its OK refusal).
    - `tests/the_contact_editor_fills_the_name_and_its_parts_from_each_other.rs`, how a built editor's controls are set and read in a target, and its MSAA reading.
    - The global rule on verifying a name at the handle focus reaches.
  </read_first>
  <behavior>
    - `build_quick_step_edit_dialog(parent, existing, labels, folders, palette) -> QuickStepEditor`, public, a dialog titled "New Quick Step" or "Edit Quick Step": "&Name:" (`add_field`); "Mark as &read or unread:" a `Choice` of "Leave as it is", "Mark read", "Mark unread"; "&Flag:" a `Choice` of "Leave as it is", "Flag it", "Take the flag off"; "&Label:" a `Choice` of "No label" then the account's labels in their order; "&Move to:" a `Choice` of "Leave it where it is" then the account's folders by path in the tree's order; "&Delete it" (`add_checkbox`); "P&hrase to say first:" (`add_field`); OK and Cancel. Each `Choice` named with `set_accessible_name(&choice, &name_from_label(label))`. An existing step fills every control from its `Outcome`; a label or folder the account no longer has is shown selected as "Work (not in this account any more)" so saving it again is a choice somebody sees.
    - `what_the_editor_holds(editor) -> (String, Outcome)`, public: the controls read into a name and an `Outcome`, "Leave as it is" as `None`.
    - `show_quick_step_edit(parent, existing, rows, labels, folders, palette) -> Option<QuickStepEntry>`: on OK, `quick_steps::name_for` against the other rows' names and `what_stops_a_step_being_saved`; a refusal through `a_sub_dialog_needs` and the window stays open; otherwise the entry, a new one with a new id.
    - The target gains: the editor built over an existing step and each control's MSAA name read at its own handle (Name, Mark as read or unread, Flag, Label, Move to, Delete it, Phrase to say first); the choices' entries read back; controls set programmatically and `what_the_editor_holds` read (read and flag and label and move together; delete alone; a phrase); the dialog's `&` letters read off its children, seven and none twice; companions that plant a choice mapped the wrong way and an unnamed choice, each refused.
  </behavior>
  <action>
    Red first, on the same branch: the target's editor readings bare, with
    the public signatures answering nothing. Run them first and name
    everything that fails.

    Then the green commit, `cargo build` between. Invoke `elegant-code`:
    one function reads the controls, one decides, the dialog only asks; the
    check box and text boxes from the helpers premise 1 names. Invoke
    `naming-review` on the editor's types and functions.

    Guard record: one on `wx_managers.rs` whose break reads "Mark unread"
    as read in `what_the_editor_holds`, the mapping reading red, `suite` the
    target; measured.
  </action>
  <verify>
    <automated>cargo test --test the_quick_step_manager_says_what_each_step_does && cargo test --lib presentation::wx_managers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's editor readings bare.
    - `grep -c 'pub fn build_quick_step_edit_dialog' src/presentation/wx_managers.rs` is 1; `grep -c 'pub fn what_the_editor_holds' src/presentation/wx_managers.rs` is 1; `grep -c '"Mark as &read or unread:"' src/presentation/wx_managers.rs` is 1.
    - The target passes with at least 10 tests, quoted; `presentation::wx_managers::` with the count it had before, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>A step is made and edited in a form with one question per control, each named on the channel NVDA reads, and a step that cannot be saved is refused before the window closes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Action, Quick Steps, Manage Quick Steps, and the two scan targets</name>
  <files>src/presentation/wx_app.rs, src/presentation/scan_target.rs, src/presentation/scan_fixtures.rs, .github/workflows/accessibility.yml, tests/the_quick_step_manager_says_what_each_step_does.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:7278-7430` (the Action menu, as the earlier phase 13 plans left it), `:5640-5680` (the manager arms), `:7536-7552` (Pause Downloading, whose help is its experimental sentence), `:17640-17660` and `:17790-17830` (the Tags and editor scan target arms).
    - `src/presentation/scan_target.rs:225-330`; `.github/workflows/accessibility.yml:170-190`; `src/presentation/scan_fixtures.rs`, how a fixture filter is made.
  </read_first>
  <behavior>
    - `let quick_steps_menu = Menu::builder()` holding "&Manage Quick Steps..." (`ID_QUICK_STEP_MGR`, help `quick_steps::QUICK_STEPS_ARE_EXPERIMENTAL`), appended to Action as "&Quick Steps" with help "Commands you make that do several things to the selected messages at once", after Saved Searc&hes. Its arm: `managers::manage_quick_steps(...)` then `read_the_tree_back`, as the Label Manager's arm does.
    - `ScanTarget::QuickSteps` ("quick-steps") opens the manager over the scan-only account; `ScanTarget::QuickStepEditor` ("quick-step-editor") builds the editor over `scan_fixtures::quick_step()`, a step that marks read and moves, with two labels and two folders offered, and shows it; both in `ALL`, `as_name`, their arms and the workflow's list (premise 5), in one commit.
    - The target gains: the real menu bar's Action menu read (`WxMailApp::build_menu_bar()`): a "&Quick Steps" submenu holding "&Manage Quick Steps..." with the experimental sentence as its help; a source reading that the `ID_QUICK_STEP_MGR` arm calls `manage_quick_steps(`; companions refused.
  </behavior>
  <action>
    Red first, on the same branch: the target's menu readings bare. Run
    them first and name everything that fails.

    Then the green commits: the submenu and its arm; the two targets with
    their workflow line in one commit, which answers `all` through the
    hook. Invoke `elegant-code`.

    Guard record: one on `wx_app.rs` whose break leaves the Quick Steps
    submenu off the Action menu, the menu reading red, `suite` the target;
    measured, with every flagged record, in one `--remeasure` call per
    commit.
  </action>
  <verify>
    <automated>cargo test --test the_quick_step_manager_says_what_each_step_does && cargo test --lib presentation::scan_target:: && cargo test --test wired && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the target's menu readings bare.
    - `grep -c '"&Manage Quick Steps..."' src/presentation/wx_app.rs` is 1; `grep -c '"&Quick Steps"' src/presentation/wx_app.rs` is 1; `grep -c "'quick-steps', 'quick-step-editor'" .github/workflows/accessibility.yml` is 1, and the two anchored lines of 13-39's premise 3 still match once each.
    - `cargo test --test wired` passes, so Q is Action's once and M the submenu's once; `presentation::scan_target::` and `presentation::wx_app::` with the counts they had before, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>The manager is reached from Action, Quick Steps, Manage Quick Steps, with the experimental sentence where the item is chosen, and both windows are walked by the scan.</done>
</task>

<task type="auto">
  <name>Task 4: the pages, the changelog, the ledger and the four marks</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/changelog.md, .planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:661-700` (Action Menu), `:736-760` (Rules Management and the filter editor's letters), `:1168-1283` (the dialog accelerator sections).
    - `docs/USER_GUIDE.md:521-566` (rules) and `:129-160` (Other tools).
  </read_first>
  <action>
    Invoke `writing-craft`. The shortcuts page: the Action Menu table gains
    a Quick Steps row (Manage Quick Steps, `M` on the submenu); a "Quick Step
    Manager and editor accelerators" section listing the manager's letters
    and the editor's seven. The guide: a Quick Steps section in steps,
    making a step, what each question does, that a step puts on at most one
    label (13-40, decision 4), that a step moves to a folder chosen from the
    account's folders, and one sentence that running a step from the menu
    or by key arrives with the next change. The changelog, `[Unreleased]`,
    Added, in #60's words: Quick Steps can be made, edited and put in order
    in the Quick Step Manager, and running them is not built yet, said
    plainly; 13-42 rewrites this entry when they run. Known limitations:
    nobody has heard the manager or the editor.

    Ledger, by hand, both halves: one `unrun-verify` for the tester's ear
    (the submenu, the manager's rows and Key column, the editor's seven
    questions in order, a refusal).

    The four marks: phase 13's progress row; this plan's `- [ ]` line in
    phase 13's plan list (ROADMAP.md; the phase README keeps no boxes) ticked;
    `STATE.md`'s plan number in the frontmatter and the body; a dated line
    under GAP-11's `[D]` line saying steps are named and made in a manager,
    naming the target, and that running them is 13-42's. Run
    `cargo test --test the_planning_files_agree_with_themselves` first.

    This plan changes what is spoken and shown: after this one documents
    commit the branch is pushed with a pull request into `main` under
    Pratik's standing OK, CI, NVDA and Accessibility read before the local
    merge, the two new scan targets read by name in the Accessibility log.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test wired && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Manage Quick Steps' docs/KEYBOARD_SHORTCUTS.md` is at least 1; `grep -c 'Quick Step' docs/USER_GUIDE.md` is at least 3; `grep -c '#60' docs/changelog.md` grew by at least 1.
    - The ledger entry in both halves; carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages say how a Quick Step is made and that running one comes next, the changelog says the same, and the four marks are made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the editor's controls to a stored step | what somebody chose, read into one answer per question |
| a step a newer version wrote to this build's manager | a row this build cannot understand, editable by nothing here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-41-01 | Tampering | a control read the wrong way into the step | medium | mitigate | `what_the_editor_holds` read in the target with the controls set; a guard record |
| T-13-41-02 | Tampering | a newer version's step rewritten without the actions this build cannot read | medium | mitigate | Edit refuses a `None` row with a sentence; move and delete only |
| T-13-41-03 | Tampering | a step moving mail to another account's folder | low | mitigate | the folders offered are the step's own account's, from the tree's own read |
| T-13-41-04 | Denial of service | a control NVDA cannot name, or two sharing a letter | medium | mitigate | named from labels, read over MSAA at their own handles; letters read off the built dialog; two scan targets |
| T-13-41-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all four `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; the workflow commit answers `all`, once; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes what is spoken and shown: after the one documents commit, the branch is pushed with a pull request into `main` (Pratik's standing OK of 2026-09-23), CI, NVDA and Accessibility read on it, a red one fixed on the branch; the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What the tester's ear settles: the submenu, the manager, the editor in order. What the readings prove: the rows and keys, the move stored, the controls read into a step, the names over MSAA, the letters one each.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 11 advances: a Quick Step is named and made in a manager.
- GAP-11's `[D]` line has a dated line naming this plan's target; the box waits for 13-42; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-11's dated line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-41-SUMMARY.md` when done
</output>
