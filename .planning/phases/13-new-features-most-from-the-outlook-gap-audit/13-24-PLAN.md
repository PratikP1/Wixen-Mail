---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 24
type: execute
wave: 25
depends_on: [13-23]
files_modified:
  - src/presentation/wx_app.rs
  - tests/each_set_command_has_a_quiet_do_half.rs
  - tests/every_command_acts_on_the_selection.rs
  - tests/report_as_junk_is_on_the_action_menu.rs
  - guards/guards.toml
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-11, GAP-12, GAP-06]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Mark as Read, Star, a label, Move and Delete over a selection each have a do-half of their own, a function that takes the chosen messages and an explicit direction or destination, carries the change out here and at the server through the paths it used before, says nothing, and answers what it did (RESEARCH-4, AUT-6 task 1)."
    - "Each of the seven set commands and Report as Junk calls its do-half and then says exactly the sentence and gives exactly the signal it gave before this plan: no sentence, signal, gate or bound moves or changes."
    - "The readings in tests/every_command_acts_on_the_selection.rs stay green without their meaning changed, or are rewritten in place where a function they name moved, and the summary says which."
    - "Every guard record whose anchor text lay inside a block this plan moved or re-indented is rewritten on the new text and re-measured in the commit that moved it."
  artifacts:
    - "src/presentation/wx_app.rs: mark_these_read, star_these, label_these, move_these and delete_these, each quiet, each called by its command."
    - "tests/each_set_command_has_a_quiet_do_half.rs: source readings over what_ships that each do-half exists, takes the chosen set, says nothing, and is what its command calls, each with a planted-fault companion."
  key_links:
    - "ID_TOGGLE_STAR arm -> read half (chosen_messages, refusals, too_many, what_star_does) -> star_these(app, cache, list, &chosen, starred) -> say half (what_was_done, announce, Confirmed): the do-half is the piece 13-24.1's runner calls."
    - "move_or_copy_message and report_the_chosen_as_junk -> move_these -> the here-first move with its gate -> an answer the caller words."
---

<objective>
Split the five set actions into a quiet do-half each, with every command
saying what it said before.

Purpose: #60 and #61 both name one runner that carries several rule
actions out over a set of messages through the gated paths the set
commands of 11-07 already use, and 13-25's block reuses it. RESEARCH-4
sized that runner "M bordering L" and said to split it at the task
boundary if the extraction is larger than one sitting. It is: five
do-halves in the largest file in the tree, eleven guard records anchored
in or beside the blocks that move, and a reading target that reads those
functions by name. So this plan is the extraction alone, and 13-24.1 is
the runner and its order proof.

Output: five quiet do-halves, the commands calling them, the new reading
target, the rewritten records, a changelog line, the ledger.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-22-SUMMARY.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Taken 2026-09-24 from the research's recommendation; Pratik
may overrule.

**1. The runner is built ahead of the block, across this plan and
13-24.1 (RESEARCH-4 question 12, RESEARCH-CROSS-CHECK "Shared runner").**
A generic runner, used by 13-25's block, 13-42's Quick Steps and 13-44's
rule over a folder. This plan builds only the quiet do-halves it stands
on. `run_these_actions_over`, `application::acting_on_a_set`,
`WhatWasDone` and `said` are 13-24.1's, and a later plan that wants them
reads 13-24.1's summary, not this one.

**2. Split at the task boundary (RESEARCH-4, AUT-6 size).** This plan is
AUT-6's task 1; 13-24.1 is its tasks 2 and 3. The planner's split,
reported to the orchestrator.

**3. `said_for_the_set` retires here if the quiet move makes it
redundant (planner's choice).** 13-22 gave `AMoveAsked` that field so
Report as Junk's sentence was the one said. Once `move_these` answers
what it did and says nothing, Report as Junk and Move each word their
own sentence from the answer, and the field and 13-22's record on it are
rewritten in this plan; the summary says which way it went.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`. Command first, then what
it said. 13-01 to 13-23 land first; re-take every number with the same
command before editing, and re-take the anchor list below with the
scratch command at the end of this block.

**1. The five commands, as they are.**

```
grep -n '_ if id == ID_TOGGLE_STAR =>\|_ if id == ID_DELETE || id == ID_DELETE_OUTRIGHT =>\|_ if id == ID_MARK_READ =>\|^fn toggle_read_state\|^fn label_the_message\|^fn move_or_copy_message\|^fn move_or_copy_here_first\|^fn spawn_folder_move\|^fn spawn_server_change\|^fn complete_here_then_tell_the_server' src/presentation/wx_app.rs
   -> the star arm inline at :4181-4271; the delete arm inline at :5304-5503; toggle_read_state :11008; label_the_message :11459; move_or_copy_message :21058; move_or_copy_here_first :21308; spawn_folder_move :21712; spawn_server_change :23529
sed -n 4181,4271p src/presentation/wx_app.rs   -> read half (chosen_messages at the whole account's reach, the empty refusal, too_many, what_star_does), do half (per message: the row, write_flags_or_put_the_row_back, spawn_server_change Flagged), say half (what_was_done, announce, send_shown, Confirmed)
sed -n 5405,5503p src/presentation/wx_app.rs   -> after say_the_one_word("Delete"): the do half, cancel_if_queued, delete_if_local, where_a_delete_goes_here, complete_here_then_tell_the_server with spawn_server_change Deleted
```

**2. The readings that name these functions.**

```
sed -n 108,131p tests/every_command_acts_on_the_selection.rs   -> THE_TOGGLE "fn toggle_read_state(", THE_LABELS "fn label_the_message(", THE_MOVE "fn move_or_copy_message(", THE_BATCH "fn spawn_folder_move(", and the cursor commands
grep -n 'fn test_' tests/every_command_acts_on_the_selection.rs   -> 17 tests; the set readings at :339-370 look for chosen_messages( and the one sentence inside those named functions
```

The read half stays in the named functions and arms, so these readings
keep reading the selection where they read it today. The do-half moves
out; a reading that looked for the server call inside a named function
is rewritten in place to follow it into the do-half, with the reason in
its comment.

**3. Guard records, both readings.** `wx_app.rs` 119 by
`tests_last_seen` and 112 by `file`, 199 tests, no test added. By anchor
text inside the blocks this plan touches, with where each anchor ends up:

| Record | Anchor at | This plan |
|---|---|---|
| no window words a refusal for nothing chosen of its own | `:5354`, delete's read half | stays; re-indented only if the arm body moves, then rewritten |
| delete over a selection refuses above the bound, not runs whatever the size | `:5360`, delete's read half | stays |
| a delete says the one word delete at the key, not the subject again | `:5413`, delete's say-at-the-key | stays |
| mark as read acts on every selected message, not the cursor row alone | `:11021`, toggle_read_state's read half | stays |
| the reason a command did nothing does not go out as a status line | `:11471`, label_the_message's read half | stays |
| a crossing is made here before any session is asked for, not after | `:21479`, move_or_copy_here_first's do half | moves with `move_these` if the body moves; rewritten and re-measured then |
| nothing this window words for itself is a delete sentence | `:23497`, FlagChange::done | not touched |
| the delete path says what the one owner of that sentence gave it | `:23663`, spawn_server_change's worker | not touched |
| the count of sign-ins that go round the helper is read from the tree | `:23747`, spawn_server_change's worker | not touched |

Plus 13-22's two records on `wx_app.rs` (the mark before the move, and
`said_for_the_set`), rewritten if decision 3 retires the field. The
anchor list is re-taken before editing with the format's own parser,
every record whose `before` text lies in `wx_app.rs` with the line it
starts on, and read against the line ranges the moves touch:

```
python -c "import tomllib;t=open('src/presentation/wx_app.rs',encoding='utf-8').read();[print(t.count('\n',0,t.find(r['before']))+1,r['name']) for r in tomllib.load(open('guards/guards.toml','rb'))['guard'] if r.get('file')=='src/presentation/wx_app.rs' and r['before'] in t]"
```

The summary quotes the list it acted on.
`tests/every_command_acts_on_the_selection.rs` 3 by `tests_last_seen`,
0 by `file`, 17 tests; a reading rewritten in place keeps the count.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the reading that each set action has a quiet do-half, and Mark as Read, Star and a label split</name>
  <files>tests/each_set_command_has_a_quiet_do_half.rs, src/presentation/wx_app.rs, tests/every_command_acts_on_the_selection.rs, guards/guards.toml</files>
  <read_first>
    - `tests/every_command_acts_on_the_selection.rs:60-130` and `:430-520` (`body_of`, `the_id_arm`, the planted-fault companions).
    - `src/presentation/wx_app.rs` at the star arm, `toggle_read_state`, `label_the_message` and `labels_for` (premise 1), and `write_flags_or_put_the_row_back` (`grep -n '^fn write_flags_or_put_the_row_back'`).
  </read_first>
  <behavior>
    - The new target reads `wx_app.rs` through `what_ships` and finds `fn mark_these_read(`, `fn star_these(` and `fn label_these(` in this task, and `fn move_these(` and `fn delete_these(` from task 2, each taking the chosen set (`&Chosen` or `Chosen`) and an explicit direction, label or destination; the body of each holds none of `announce(`, `signal(`, `send_shown(`, `send_status(`, `say_the_one_word(`; and the star arm, `toggle_read_state`, `label_the_message`, `move_or_copy_message`, the delete arm and `report_the_chosen_as_junk` each call theirs. Each reading has a companion that plants the fault (an `announce(` in a do-half; a command that carries the change out inline) and requires the reading to name it.
    - This task writes the readings for the first three (Mark as Read, Star, a label) and their companions, red first and green by its end; task 2 adds the move and delete readings in its own red commit, so no green commit in between carries a reading still red.
    - Behaviour unchanged: Mark as Read, Star and each label say the same sentences and give the same signals, read by the existing targets.
  </behavior>
  <action>
    Red first, on the branch `13-24-quiet-do-halves`: the new target's
    readings bare in the trailer. Then, with `cargo build` between,
    `mark_these_read`, `star_these` and `label_these` taken out of their
    commands: each receives what the read half decided, carries out the
    per-message local write, the put-back on a failed write and the
    `spawn_server_change` exactly as before, refreshes the list, and
    answers a value the command words (for Star, `Outcome::Starred` or
    `Unstarred`; a failed local write answers that it stopped, so the
    command returns as it does today). The commands keep their read
    halves and say halves where the readings look for them. Invoke
    `elegant-code`: a do-half is named for what it does to the set,
    takes no window it does not need, and has one return shape per
    command.

    Records: each anchor premise 3 marks as moving is rewritten on its
    new text in the commit that moved it and re-measured with one
    `scripts/guards.sh --remeasure` call; one new record on the star
    do-half whose break adds an `announce(` inside it, the reading red,
    `suite` the new target, measured.
  </action>
  <verify>
    <automated>cargo test --test each_set_command_has_a_quiet_do_half && cargo test --test every_command_acts_on_the_selection && cargo test --test deleting_a_message_lands_on_the_next_one && cargo test --test every_status_sentence_has_one_shape && cargo test --lib presentation::wx_app::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the new target's readings bare.
    - `grep -c '^fn star_these\|^fn mark_these_read\|^fn label_these' src/presentation/wx_app.rs` is 3.
    - `every_command_acts_on_the_selection` passes at 17, quoted; `presentation::wx_app::` at 199, quoted.
    - Every record premise 3 lists is accounted for in the summary: stayed, or rewritten and re-measured.
  </acceptance_criteria>
  <done>Three of the five set actions carry their change out in a quiet function of their own, and every command using them says what it said before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move and Delete split, and Report as Junk wording its own sentence from the move's answer</name>
  <files>src/presentation/wx_app.rs, tests/each_set_command_has_a_quiet_do_half.rs, tests/report_as_junk_is_on_the_action_menu.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs` at `move_or_copy_message`, `move_or_copy_here_first` with `AMoveAsked`, `spawn_folder_move`, `complete_here_then_tell_the_server`, the delete arm (premise 1), and `report_the_chosen_as_junk` (13-22).
    - `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-22-SUMMARY.md`, the sections on `said_for_the_set` and whether Undo reaches a report.
  </read_first>
  <behavior>
    - `move_these` is the here-first move for a set within an account or across two, its gate met before anything changes, answering the outcome (`MovedTo { into, not_moved }` or `CopiedTo`) and saying nothing; one message keeps its own shown outcome as #83 set, which is part of the move and not a sentence the caller words.
    - `delete_these` is the delete arm's do half after `say_the_one_word("Delete")`: cancel a queued row, delete a local one, group the rest by account, complete here and tell the server; the arm keeps its question for a conversation row, its bound and its one word.
    - Move to, Copy to Somewhere else and Report as Junk call `move_these` and word the answer: Move and Copy as `what_was_done` does today, Report as Junk as `reporting_junk::what_reporting_did` does; `said_for_the_set` is retired if nothing is left reading it (decision 3), and 13-22's readings are rewritten in place to follow the sentence to where it is now worded.
    - The new target's five readings green.
  </behavior>
  <action>
    Red first: the move and delete readings and their companions added to
    the new target, and 13-22's readings rewritten to the new shape,
    named bare in one red commit. Then the
    two do-halves, with `cargo build` between, and the callers moved to
    them. Invoke `elegant-code`: the move keeps one route for within an
    account and across two, as it has since #86; the delete keeps the
    one owner of its sentence (`server_delete`), which this plan does
    not touch.

    Records: the `:21479` record and 13-22's two, rewritten and
    re-measured wherever their text moved; one new record on
    `delete_these` whose break adds `say_the_one_word(` inside it, the
    reading red, `suite` the new target, measured.
  </action>
  <verify>
    <automated>cargo test --test each_set_command_has_a_quiet_do_half && cargo test --test report_as_junk_is_on_the_action_menu && cargo test --test every_command_acts_on_the_selection && cargo test --test a_move_across_accounts_completes_here_first && cargo test --test deleting_a_message_lands_on_the_next_one</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^fn move_these\|^fn delete_these' src/presentation/wx_app.rs` is 2.
    - The new target passes with at least 10 readings (five and their companions), quoted.
    - The summary says whether `said_for_the_set` was retired, and every record it names is re-measured and quoted.
  </acceptance_criteria>
  <done>All five set actions have a quiet do-half that answers what it did, and Move, Copy, Delete and Report as Junk each word their own one sentence from that answer.</done>
</task>

<task type="auto">
  <name>Task 3: the changelog line, the ledger and the summary</name>
  <files>docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/changelog.md` from `[Unreleased]`, the Changed heading.
  </read_first>
  <action>
    Invoke `writing-craft`. The changelog, `[Unreleased]`, Changed, one
    line: nothing a person hears changes; the commands that act on a
    selection now share the pieces Quick Steps and a rule run over a
    folder will use, so the pull request's NVDA run is the check that
    nothing moved. Ledger, by hand, both halves: one `todo` that the
    do-halves have no caller but their own commands until 13-24.1's
    runner, filed where 13-24.1's own text names it (it does, in its
    premise 1). One documents commit on the branch before the merge
    carries the changelog, the ledger, the summary and the four
    completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The changelog line is present; the ledger entry is in both halves; carriage returns 0; no em dash; none of the six words.
  </acceptance_criteria>
  <done>The refactor is recorded as one that changes nothing heard, and the next plan's dependency on it is written where that plan reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a key press to a server write | five commands that change mail at a provider, moved without changing what reaches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-24-01 | Elevation of privilege | a gate left behind when a do-half moved | high | mitigate | the gate stays in the move and delete paths it is in today; the existing targets that read the gate run in the verify; a do-half is carried out, not rewritten |
| T-13-24-02 | Denial of service | a sentence said twice, once by a do-half and once by its command | medium | mitigate | the reading refuses any saying call in a do-half, with planted-fault companions |
| T-13-24-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`.
- Nothing heard is meant to change, but seven spoken commands' code paths move: push the branch, open a pull request, wait for CI, NVDA and Accessibility, then merge locally. Pushing `main` is Pratik's.
- Red commits name integration tests bare. One `scripts/guards.sh --remeasure` call per commit for every flagged record; a record whose anchor moved is rewritten on its new text, never edited until it applies without being measured.
- No scripted rewrite of a tracked file; `git commit -F`; no AI attribution.
</verification>

<success_criteria>
- The five quiet do-halves exist and are called by their commands; no requirement's `[D]` line closes here, and the summary names 13-24.1 as the plan that gives them a second caller.
- The summary says in one sentence near its top that the runner is not in this plan: `run_these_actions_over`, `acting_on_a_set`, `WhatWasDone` and `said` are built in 13-24.1, so an executor who loads this summary looking for the runner is sent to `13-24.1-SUMMARY.md`.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and the traceability rows of GAP-06, GAP-11 and GAP-12 noting this plan.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-24-SUMMARY.md` when done
</output>
