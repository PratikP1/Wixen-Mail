---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 22
type: execute
wave: 23
depends_on: [13-21]
files_modified:
  - src/application/reporting_junk.rs
  - src/application/mod.rs
  - src/service/protocols/imap/flag.rs
  - src/service/protocols/imap.rs
  - src/presentation/wx_app.rs
  - tests/report_as_junk_is_on_the_action_menu.rs
  - guards/guards.toml
  - docs/KEYBOARD_SHORTCUTS.md
  - docs/USER_GUIDE.md
  - docs/ALPHA_TESTING.md
  - docs/changelog.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [GAP-06]
estimate:
  tokens: 28800
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Action, Report as Junk (Alt+A then J, or Ctrl+Shift+J) acts on every selected message, and a conversation row contributes the messages in the folder being read, the way Move to does (GAP-06's first [D] line, #54)."
    - "On an IMAP server whose folder says in PERMANENTFLAGS that it keeps \\* or $Junk, $NotJunk is taken off and then $Junk is put on at the server, in that order, before the message moves into the account's junk folder; on a server that keeps no such keyword nothing is stored and the sentence says only the folder marks it (RFC 9051, section 2.3.2)."
    - "On Gmail no keyword is set: the move into Spam is the report, by Google's own page, and the sentence says the move tells Google; on a Microsoft account the sentence says Microsoft has not been told, because Microsoft's only reporting call is a beta endpoint needing a permission this program does not ask for (decision 1)."
    - "A POP account, an account with no junk folder, an account that has not learned its folders, and an account whose changes are off each get one sentence and nothing is sent; a message already in the junk folder is passed over and counted in the sentence."
    - "One sentence per account at the end of a report, never one per message, and it names the verb reported, so it is told apart from Move's sentence."
    - "The menu item's description says the command has never met a real mail server, and docs/ALPHA_TESTING.md says the same, so the person using it is told where they meet it."
  artifacts:
    - "src/application/reporting_junk.rs: AccountKind::of(account), what_a_report_does(kind, junk, allowed) -> Report, what_reporting_did(...) sentences, and mark_as_junk_at_the_server over a session, with cases against a scripted server from common::answering::conversing."
    - "src/service/protocols/imap/flag.rs: JUNK and NOT_JUNK beside the five flags, and keeps_keyword(permanent, keyword) as a pure function with the RFC's rows."
    - "src/service/protocols/imap.rs: select_folder reads PERMANENTFLAGS into MailboxStatus as keeps_the_junk_mark: bool, so MailboxStatus stays Copy."
    - "src/presentation/wx_app.rs: ID_REPORT_JUNK, the Action item \"Report as &Junk\\tCtrl+Shift+J\", its arm, report_the_chosen_as_junk, and AMoveAsked's said_for_the_set."
    - "tests/report_as_junk_is_on_the_action_menu.rs: the built menu bar read for the item, its letter and its key; source readings that the handler reads the selection, meets the gate and marks before it moves, each with a planted-fault companion."
  key_links:
    - "Ctrl+Shift+J -> ID_REPORT_JUNK -> report_the_chosen_as_junk -> chosen_messages -> reporting_junk::what_a_report_does per account -> a worker: mark_as_junk_at_the_server through mail_session::the_session_at -> UIUpdate -> move_or_copy_here_first with said_for_the_set -> one sentence."
    - "select_folder -> ResponseCode::PermanentFlags -> flag::keeps_keyword(.., JUNK) -> MailboxStatus.keeps_the_junk_mark -> whether any STORE is sent."
---

<objective>
Report as Junk on the Action menu, for every selected message, told to
the provider where the provider listens and said plainly where it does
not.

Purpose: #54's first remaining point and GAP-06's first `[D]` line:
"Report Junk on the Action menu for a provider with an endpoint, said
when there is none". The research (RESEARCH-3, GAP-06) found three
routes and one honest gap: an IMAP server takes the `$Junk` keyword
where its folder keeps keywords, Gmail documents a move into Spam as a
report, and Microsoft offers no supported call a mail program may use,
so for Microsoft the sentence says it was not told.

Output: the pure decision and sentence module, the keyword read and
write, the Action item with its letter and key, the reading target, the
pages, the changelog and the ledger. The block that moves existing mail
is 13-25's.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-3-provider-features.md
@CLAUDE.md
</context>

<decisions date="2026-09-24">
Pratik confirmed the order on 2026-09-24 and said "plan and implement
phase 13". Each decision below was taken 2026-09-24 from the research's
recommendation; Pratik may overrule.

**1. Microsoft junk reports (RESEARCH-3 question 1).** A Microsoft
account's message is moved to Junk Email and the sentence says Microsoft
has not been told. No Graph call and no new permission: `reportMessage`
is documented under the beta only and needs `Mail.ReadWrite`.

**2. Report as Junk on `Ctrl+Shift+J` (question 3).** Block This Sender's
`Ctrl+Shift+B` is 13-25's.

**3. The Action menu letter J (RESEARCH-4 question 7).** Q goes to Quick
Steps (13-42) and Z stays free; the next Action item goes on a submenu.

**4. Gmail gets no keyword (planner's choice, recorded).** Google's page
says a move into Spam is a report. A second signal adds nothing Google
reads, so the keyword step is skipped for an account whose provider is
`gmail`, and the move alone is sent.

**5. The report's one sentence rides on the move (planner's choice).**
`move_or_copy_here_first` speaks one sentence for a set; this plan gives
`AMoveAsked` an optional `said_for_the_set`, so the report's sentence is
the one spoken and Move's is not said beside it. 13-24 later extracts
the quiet do-half every set command needs; this field is the smallest
change that keeps one sentence per command now.
</decisions>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, `guards/guards.toml`
holding 1,088 records by the TOML reader. Command first, then what it
said. Plans 13-01 to 13-21 land before this one and move line numbers in
`wx_app.rs`; re-take every number with the same command before editing.

**1. The Action menu's letters and the key.**

```
sed -n 7277,7426p src/presentation/wx_app.rs | grep -v '^\s*//' | grep -oE '"[^"]*&[A-Za-z][^"]*"' | grep -oE '&[A-Za-z]' | tr a-z A-Z | sort | uniq -c
   -> A B C D E F G H I K L M N O P R S T U V W X Y, once each: J, Q and Z are free
grep -n 'let message = Menu::builder()\|"Saved Searc&hes"' src/presentation/wx_app.rs   -> :7277 and :7424, the Action menu's first and last lines
grep -rn 'Ctrl+Shift+J' src docs/KEYBOARD_SHORTCUTS.md   -> nothing: the key is free in the code and on the page
sed -n 661,692p docs/KEYBOARD_SHORTCUTS.md   -> the Action Menu table the row joins
grep -n 'fn test_no_two_items_on_one_menu_claim_the_same_letter\|fn test_no_two_menu_items_claim_the_same_shortcut\|fn test_the_shortcuts_document_and_the_menus_agree' tests/wired.rs   -> :1977, :344, :932: the letter, the key and the page are each held by a reading, so a key bound and not documented is red on arrival
```

The executor re-runs the first and third commands after 13-21's merge
and before writing the label; if 13-01 to 13-21 took J or the key, the
plan stops and says so rather than choosing another.

**2. The IMAP half.**

```
sed -n 384,397p src/service/protocols/imap.rs   -> MailboxStatus derives Copy and holds uid_validity and highest_modseq only
sed -n 1084,1119p src/service/protocols/imap.rs   -> select_folder matches UidValidity and HighestModSeq; nothing reads PERMANENTFLAGS
grep -n 'PERMANENTFLAGS\|PermanentFlags\|permanent_flags' src/service/protocols/imap.rs src/service/protocols/imap/flag.rs   -> nothing
grep -rn '\$Junk\|\$NotJunk' src   -> one test literal, src/application/mail_across_accounts.rs:1435; no constant
sed -n 1380,1396p src/service/protocols/imap.rs   -> set_flag: may_i("change a message"), UID STORE +FLAGS or -FLAGS, gated already
sed -n 620,628p src/application/mail_controller.rs   -> MailController::set_flag selects the folder first when another is selected
grep -n 'pub async fn select_folder' src/application/mail_controller.rs   -> :490, answering the MailboxStatus the junk mark is read from
grep -n 'fn conversing' src/common/answering.rs; sed -n 410,420p src/application/flag_changes_waiting.rs   -> a scripted server a test can drive a real session against, answering SELECT with lines the test writes
```

`imap-proto` 0.16.7 carries `ResponseCode::PermanentFlags(Vec<Cow<str>>)`
(RESEARCH-3, stated at `imap-proto-0.16.7/src/types.rs:131`). Keeping
the answer as a `bool` computed in `select_folder` keeps `MailboxStatus`
`Copy`, so no caller that copies it changes.

**3. The move and the gate this reuses.**

```
grep -n '^fn move_or_copy_here_first\|^struct AMoveAsked\|AMoveAsked {' src/presentation/wx_app.rs   -> :21308, :21529, one construction at :21281 and one destructure at :21322
sed -n 21360,21372p src/presentation/wx_app.rs   -> every account a message is at meets outward::permitted(allowed_for(id).mail, ...) before anything changes
sed -n 21379,21393p src/presentation/wx_app.rs   -> one_sentence = what_was_done(&chosen, MovedTo) for a set; one message keeps its own outcome (#83)
grep -n 'fn the_session_at' src/application/mail_session.rs   -> the held session spawn_folder_move asks for
sed -n 285,296p src/application/blocking.rs   -> where_blocked_mail_goes over FolderType::Spam: TheJunkFolder, NoJunkFolderFound, NoFoldersKnownYet
sed -n 610,618p src/data/account.rs; sed -n 125,130p src/service/oauth.rs   -> Account::protocol() tells POP apart; provider "gmail" and "outlook"
```

The sentences `blocking::NO_JUNK_FOLDER_FOUND` and `NO_FOLDERS_KNOWN_YET`
begin "Nothing has been blocked", so this plan writes its own two in
`reporting_junk` rather than reusing them. A refused change uses the
gate's own words, the way the move does, so no wording is private to
`blocking.rs` and that file is not edited.

**4. Guard records, both readings.** Counted with
`python -c "import tomllib;..."` over `tests_last_seen` and `file`:
`wx_app.rs` 119 and 112, 199 tests, no test added; `imap.rs` 47 and 47,
102 tests, no test added; `flag.rs` 0 and 0, 2 tests, tests added there;
`mail_controller.rs` not edited; `reporting_junk.rs` and the new target
new at zero. By anchor text inside what this plan edits: in `imap.rs`,
**"a second SELECT of an already-open folder still asks the server"**
(its `after` text builds a `MailboxStatus` literal with two fields, so
once the struct gains a third field the break stops compiling: the
record is rewritten in this plan so its `after` names the new field,
and re-measured) and **"the highest modseq a server names on opening is
heard"** (its `before` is the `HighestModSeq` arm followed by
`_ => None,`, so the new `PermanentFlags` arm goes before the
`UidValidity` arm and that anchor still names one place; if the
executor puts it elsewhere, the record is rewritten and re-measured). In
`wx_app.rs`, no record anchors inside the menu id list, the Action menu
(`:7277-7426`), `AMoveAsked` or its two sites; the one anchor near
(`:32227`, "the block handler asks the message rather than a
constant") is inside `block_the_sender`, which this plan does not edit.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: what a report does per account, and the junk mark read and written at the server</name>
  <files>src/application/reporting_junk.rs, src/application/mod.rs, src/service/protocols/imap/flag.rs, src/service/protocols/imap.rs, guards/guards.toml</files>
  <read_first>
    - `src/service/protocols/imap/flag.rs` whole (69 lines), `src/service/protocols/imap.rs:375-400` and `:1084-1119`, and the `against_a_server_that_answers` tests that read `select_folder` (`grep -n 'mod against_a_server_that_answers' src/service/protocols/imap.rs`).
    - `src/application/flag_changes_waiting.rs:380-440` (a real session against `common::answering::conversing`, the shape this module's cases follow) and `src/common/answering.rs` around `fn conversing`.
    - `src/application/blocking.rs:225-300` (the junk folder decision) and `src/application/choosing_messages.rs:195-262` (`how_many`, `what_was_done`, the one-sentence words).
  </read_first>
  <behavior>
    - `flag::JUNK = "$Junk"`, `flag::NOT_JUNK = "$NotJunk"`; `flag::keeps_keyword(permanent: Option<&[String]>, keyword: &str) -> bool`: true where the list holds `\*` or the keyword itself (case-insensitive, RFC 9051 keywords are), false where it holds neither, false where the server sent no PERMANENTFLAGS at all; rows for each.
    - `select_folder` reads `ResponseCode::PermanentFlags` into `MailboxStatus.keeps_the_junk_mark` through `keeps_keyword(.., JUNK)`; the existing `against_a_server_that_answers` tests stay green and the rewritten guard record's `after` compiles.
    - `reporting_junk::AccountKind::of(&Account)`: `Pop` for `Protocol::Pop3`, `Gmail` for provider `gmail`, `Microsoft` for `outlook`, `OtherImap` otherwise.
    - `what_a_report_does(kind, junk: BlockedMailGoesTo, may_change_mail: Result<(), String>) -> Report`: `NothingSent(sentence)` for POP ("This account collects its mail with POP, which has no junk folder at the server, so nothing was reported."), no junk folder and no folders known yet (each its own sentence beginning "Nothing was reported."), and the gate's refusal in its own words; otherwise `MarkThenMove { junk, mark: bool }` where `mark` is false for Gmail.
    - `what_reporting_did(kind, how_many_went, junk_name, passed_over, marked: Marked) -> String` over `choosing_messages::how_many`: "3 messages reported as junk and moved to Junk." where the mark was kept; "3 messages moved to Spam, which tells Google they are junk." on Gmail; "3 messages moved to Junk. This server does not keep a junk mark, so only the folder says they are junk." where it keeps none; "3 messages moved to Junk Email. Microsoft offers no supported way for a mail program to report junk, so Microsoft has not been told." on Microsoft; ", and 1 already in Junk was passed over" where any were; "The junk mark could not be set: {reason}." appended where the STORE failed, with the move still made; singular right for one message.
    - `mark_as_junk_at_the_server(controller, folder, uids) -> Result<Marked>` (async): selects the folder, and where `keeps_the_junk_mark` sends `-FLAGS ($NotJunk)` before `+FLAGS ($Junk)` for each uid through the gated `set_flag`, answering `Marked::Kept`; answering `Marked::NotKept` and sending no STORE where the folder keeps no such keyword. Cases against `conversing`: the order of the two STOREs read from the server's own record of what it was sent; no STORE to a folder whose PERMANENTFLAGS lacks both; a refused STORE answered as an error carrying the server's words.
  </behavior>
  <action>
    Red first, on the branch `13-22-report-as-junk`: the `flag` rows and
    the `reporting_junk` cases by module path
    (`service::protocols::imap::flag::tests::<name>`,
    `application::reporting_junk::tests::<name>`), and, because a test
    lands in `flag.rs`, which no record names, no count check is owed
    there; `reporting_junk.rs` is new. Then the constants, the pure
    function, the `select_folder` arm placed before the `UidValidity` arm
    (premise 4), the field, and the module, with `cargo build` between.
    Invoke `elegant-code`: every decision is a function over plain values;
    the async function is the only part that talks to a server and it
    calls the gated `MailController::set_flag`, never a new gated method
    (RESEARCH-3 pitfall 10: the census of gated writes in `outward.rs`
    counts `may_i(` markers per method).

    Guard records: rewrite **"a second SELECT of an already-open folder
    still asks the server"** so its `after` literal carries
    `keeps_the_junk_mark: false,` and re-measure it; add one on
    `flag.rs` whose break makes `keeps_keyword` answer true with no
    PERMANENTFLAGS, the no-list row red; one on `reporting_junk.rs` whose
    break swaps the two STOREs, the order case red; each measured on the
    library with `scripts/guards.sh --remeasure`, one call per commit.
  </action>
  <verify>
    <automated>cargo test --lib service::protocols::imap::flag:: && cargo test --lib application::reporting_junk:: && cargo test --lib service::protocols::imap::against_a_server_that_answers::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the new cases by module path.
    - `grep -c 'pub const JUNK' src/service/protocols/imap/flag.rs` is 1; `grep -c 'keeps_the_junk_mark' src/service/protocols/imap.rs` is at least 2; `grep -c 'PermanentFlags' src/service/protocols/imap.rs` is 1.
    - `cargo test --lib application::reporting_junk::` passes with at least 12 tests, quoted; the flag module with at least 6, quoted; `against_a_server_that_answers` unchanged in count, quoted.
    - The rewritten record and two new records measured and quoted.
  </acceptance_criteria>
  <done>A report's decision and every sentence it can say are pure and held by cases, and the junk mark is read from the server's own PERMANENTFLAGS and written in the order RFC 9051 requires, proved against a scripted server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Report as Junk on the Action menu, over the selection, marking before it moves</name>
  <files>src/presentation/wx_app.rs, tests/report_as_junk_is_on_the_action_menu.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs` at the Action menu (`grep -n 'let message = Menu::builder()' src/presentation/wx_app.rs`), the Block submenu beside it (`grep -n '"Blo&ck"'`), the `ID_BLOCK_SENDER` arm (`grep -n 'id == ID_BLOCK_SENDER'`), `chosen_messages` (`grep -n '^fn chosen_messages'`), `move_or_copy_message` and `move_or_copy_here_first` with `AMoveAsked` (premise 3).
    - `tests/every_command_acts_on_the_selection.rs:60-130` (`body_of`, `the_id_arm`, and how a reading carries a planted-fault companion) and `tests/the_label_menu_says_the_labels_an_account_has.rs:400-420` (a built menu bar read).
  </read_first>
  <behavior>
    - The target builds the menu bar with `build_menu_bar()` and finds on Action the item "Report as &Junk" with `Ctrl+Shift+J`; J is claimed once on that menu.
    - Source readings over `what_ships`: the arm for `ID_REPORT_JUNK` calls `report_the_chosen_as_junk`; that function reads the selection with `chosen_messages` at the reach Move uses, refuses above `choosing_messages::too_many`, calls `reporting_junk::what_a_report_does` per account before any worker starts, calls `mark_as_junk_at_the_server` on the worker before it asks for the move, and hands the move to `move_or_copy_here_first` with `said_for_the_set`; each reading has a companion that plants the fault (the move asked before the mark, the selection read off the cursor row) and requires the reading to say so.
    - Behaviour: the selection is grouped by the account each message is at; per account, a `NothingSent` answer is said once and nothing leaves; messages already in the junk folder are passed over and counted; the rest are marked on a worker through `mail_session::the_session_at` and then moved into the junk folder through the existing move, whose gate is met before anything changes; one sentence per account, `what_reporting_did`, spoken once at the end; a mark that failed is in that sentence and the move still happens.
    - The item's description says the command has not been tried against a real mail server, the pattern Pause Downloading's description uses.
  </behavior>
  <action>
    Red first: the target's readings bare in the trailer. Then, with
    `cargo build` between: `ID_REPORT_JUNK` in the `menu_ids!` list, the
    item appended to Action beside the Block submenu as
    "Report as &Junk\tCtrl+Shift+J" (per decision 2 and 3, J proven free
    by premise 1's command re-run today), its arm, and
    `report_the_chosen_as_junk` as a function of its own rather than
    inline in the event match, so a reading can find it. `AMoveAsked`
    gains `said_for_the_set: Option<String>` (decision 5), `None` at its
    existing construction, read at the one place the set sentence is
    built. Invoke `elegant-code`: the handler decides nothing a pure
    function could decide; it groups, asks `reporting_junk`, starts one
    worker, and sends one update. The keys table in
    `docs/KEYBOARD_SHORTCUTS.md` gains the row in this same commit, or
    `test_the_shortcuts_document_and_the_menus_agree` stays red.

    Whether Edit, Undo (13-07 to 13-09) takes a reported message back out
    of Junk depends on where 13-08 recorded a move; read 13-08's summary
    and say in this plan's summary which it is. If Undo does not reach
    it, a ledger `todo` names it; nothing is built for it here.

    Guard records: one on `wx_app.rs` whose break asks for the move
    before the mark, the reading red, `suite` the new target; one whose
    break drops `said_for_the_set` from the sentence, the reading red;
    each measured. `wx_app.rs` stays at 199 tests, quoted.
  </action>
  <verify>
    <automated>cargo test --test report_as_junk_is_on_the_action_menu && cargo test --test wired && cargo test --test every_command_acts_on_the_selection && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the target's readings bare.
    - `grep -c 'Report as &Junk\\tCtrl+Shift+J' src/presentation/wx_app.rs` is 1; `grep -c '^fn report_the_chosen_as_junk' src/presentation/wx_app.rs` is 1; `grep -c 'said_for_the_set' src/presentation/wx_app.rs` is at least 3.
    - The target passes with at least 6 readings, quoted; `wired` passes, `test_no_two_items_on_one_menu_claim_the_same_letter` and `test_no_two_menu_items_claim_the_same_shortcut` among them.
    - Two guard records measured and quoted; the summary says whether Undo reaches a report.
  </acceptance_criteria>
  <done>Report as Junk is on the Action menu with its letter and key, reads the selection, marks at the server where the server keeps the mark, moves through the existing gated move, and says one sentence per account.</done>
</task>

<task type="auto">
  <name>Task 3: the pages, the alpha page's line, the changelog and the ledger</name>
  <files>docs/KEYBOARD_SHORTCUTS.md, docs/USER_GUIDE.md, docs/ALPHA_TESTING.md, docs/changelog.md, .planning/WINDOWS.md</files>
  <read_first>
    - `docs/KEYBOARD_SHORTCUTS.md:661-692` (the Action Menu table, whose row task 2 added).
    - `docs/USER_GUIDE.md:647-703` (Moving, deleting and copying happen here first), where a new section follows.
    - `docs/ALPHA_TESTING.md:89-101` and `:245` onward (everything that writes is experimental; what is known to be missing or unproven).
  </read_first>
  <action>
    Invoke `writing-craft`. The guide gains "### Reporting junk" after
    "Moving, deleting and copying happen here first": what Report as
    Junk does on each kind of account in the sentences task 1 wrote, that
    it acts on the selection, that it is gated by Allow Changes, and that
    a block (13-25) is a different thing. The alpha page gains a line
    under what is unproven: no report has reached a real server, Google's
    page is the only evidence a move into Spam is a report, and nobody
    knows whether any server trains on `$Junk`. The changelog,
    `[Unreleased]`, Added, #54's first point, with Known limitations:
    Microsoft is not told, and nothing has met a real server.

    Ledger, by hand, both halves: one `unrun-verify` for phase 14 (a
    report against Pratik's Gmail and an IMAP server that keeps
    keywords, the keyword and the folder read back from another client);
    one `unrun-verify` for the tester's ear (the item, the key, the
    sentence per kind, heard once for a set). One documents commit on
    the branch before the merge carries the pages, the ledger, the
    summary and the four completion marks.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_words_that_say_nothing && cargo test --test a_key_is_documented_where_the_surface_that_binds_it_is && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### Reporting junk' docs/USER_GUIDE.md` is 1; `grep -c 'Ctrl+Shift+J' docs/KEYBOARD_SHORTCUTS.md` is at least 1; the changelog names #54.
    - Two ledger entries in both halves; carriage returns 0 on every file touched (`tr -cd '\r' < FILE | wc -c` read by eye, not as a gate); no em dash; none of the six words.
  </acceptance_criteria>
  <done>The pages describe Report as Junk as it is, including what it cannot do for Microsoft, and the two unproven halves are ledgered for phase 14 and the tester.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a key press to a server write | a report changes flags and moves mail at the provider |
| a server's SELECT answer to a decision | PERMANENTFLAGS is untrusted text deciding whether a keyword is sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-22-01 | Elevation of privilege | a report on an account whose changes are off | high | mitigate | the gate met per account before any worker starts, in its own words; the STORE goes through `set_flag`'s `may_i`; a reading holds the order |
| T-13-22-02 | Tampering | `$Junk` and `$NotJunk` left together, which RFC 9051 reads as neither | medium | mitigate | `-FLAGS ($NotJunk)` before `+FLAGS ($Junk)`, proved against a scripted server; a guard record on the order |
| T-13-22-03 | Repudiation | a sentence claiming the provider was told when it was not | medium | mitigate | one sentence per account kind from `what_reporting_did`; Microsoft's says it was not told; cases for each |
| T-13-22-04 | Denial of service | a sentence per message over a large selection | low | mitigate | one sentence per account at the end; `too_many` bounds the set |
| T-13-22-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped. Never `scripts/check.sh all` by hand here: the whole suite runs once, in 13-51's full gate. Merge with `git merge --no-ff`, which runs what the branch's whole diff earns.
- This plan changes what is spoken and shown (a menu item and new sentences): after the last branch commit, push the branch and open a pull request, wait for CI, NVDA and Accessibility, fix on the branch if any is red, then merge locally. Pushing `main` is Pratik's.
- Red commits name lib tests by module path and integration tests bare; the count check `test_every_guard_record_says_how_many_tests_the_files_it_names_held` is named bare wherever a test lands in a file a record names. One `scripts/guards.sh --remeasure` call per commit for every flagged record.
- No scripted rewrite of a tracked file: Read, then Edit or Write. `git commit -F`; no AI attribution.
- What a real server settles: whether any server keeps and trains on `$Junk`, and whether Gmail counts the move; what the readings prove: the decision per kind, the order of the STOREs, the menu, the key, the gate.
</verification>

<success_criteria>
- GAP-06's first `[D]` line has a named test or reading in the summary and is marked with the date; the requirement's box and traceability row wait for 13-25.
- The completion marks, all four: phase 13's progress row in `ROADMAP.md`, this plan's `- [ ]` line in phase 13's plan list, `STATE.md`'s plan number in the frontmatter and in the body, and GAP-06's `[D]` line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-22-SUMMARY.md` when done
</output>
