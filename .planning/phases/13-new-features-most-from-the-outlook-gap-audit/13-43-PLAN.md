---
phase: 13-new-features-most-from-the-outlook-gap-audit
plan: 43
type: execute
wave: 45
depends_on: [13-42, 13-23, 13-24.1, 13-25]
files_modified:
  - src/application/running_a_rule_now.rs
  - src/application/mod.rs
  - src/application/choosing_messages.rs
  - guards/guards.toml
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GAP-12]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Given a rule and a folder's messages, what the rule would change is counted before anything runs: the messages it matches through the arm the arrival check uses, whether the rule is switched on or not, less those already the way it would leave them (a read message a rule marks read, a message already in the folder a rule files into, a label already on), with the same per-message answer 13-24.1's runner acts on, so the count said is the count done (GAP-12: saying first how many messages it would touch)."
    - "The question names the rule, the folder, the change and the count with the singular right, ends by asking whether to run it, and adds that no rule run has met a real mail server when the change reaches one; when a rule would change nothing it says so and asks nothing."
    - "Above the Select All bound of 5,000 the question says the count and the bound, and a run takes the first 5,000, so running it again does the next; Enter answers no to a question about a rule that deletes and yes otherwise."
    - "The messages a run would change become the set 13-24.1's runner takes, and nothing reaches this yet: the ledger says so in an entry 13-44's own text closes; nothing a person sees changes, so no changelog entry and no push."
  artifacts:
    - "src/application/running_a_rule_now.rs: WouldChange, what_a_rule_would_change, the_set_to_run, the_question, nothing_to_change, what_the_rule_did, RUNNING_A_RULE_NOW_IS_EXPERIMENTAL, with their cases, the no-op rows among them."
  key_links:
    - "a rule and a folder's messages -> the arrival check's match (FilterEngine::matches, through 13-25's catcher) -> filters::settle -> acting_on_a_set::what_each_message_needs (13-24.1), no-ops dropped -> WouldChange -> the_question -> the_set_to_run -> run_these_actions_over, 13-24.1's runner (13-44)."
---

<objective>
What a rule would change in a folder, counted and worded before anything
runs.

Purpose: #61: "a dry run that counts matches and asks before acting ('This
rule would move 214 messages in Inbox. Run it?'); the same runner Quick Steps
would use." GAP-12's `[D]` line: "the count said before the run with a way to
stop; the run through the same arms a check uses." This plan is the pure half:
the count, the question and what is said after, with the no-op cases as rows.
13-44 builds the two doors and runs the answer through 13-24.1's runner.

Output: one pure module, its cases, and the ledger entry naming what is not yet
reachable. Nothing a person sees changes.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-4-automation.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/RESEARCH-CROSS-CHECK.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-23-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-24.1-SUMMARY.md
@.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-25-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-24 against `main` at `630e2a67`, read-only, and against the
research for 13-23, 13-24, 13-24.1 and 13-25 as it stood that day. Command first,
then what it said.

**1. The arm a check uses, and whether a switched-off rule reaches it.**

```
grep -n 'pub fn rules_matching\|pub fn matches\|rule.enabled' src/application/filters.rs   -> :335, :349, :341: rules_matching filters on `enabled`; FilterEngine::matches does not, and is the arm itself
grep -n 'pub fn from_persisted_rule' src/application/filters.rs   -> :450, a stored rule to one FilterRule with one action
grep -n 'pub fn settle\|pub fn touches_the_server' src/application/filters.rs   -> :557, :542
```

So a rule chosen by hand is matched with `FilterEngine::matches`, whatever
its switch, which is decision 3. RESEARCH-3's C2 (13-25) writes
`which_messages_here_a_rule_catches(rule, messages)` over that arm in
`application::what_a_rule_catches_here` "so GAP-12 reuses it rather than
writing a second one"; the executor reads 13-25's summary for its name and
calls it when it is a pure match over the messages it is handed. If it also
decides which folders count (the block leaves Junk, Trash, Sent and Drafts
out), the folder here is the one somebody chose, so this plan calls the
match beneath that decision and the summary says so; a one-line filter over
`FilterEngine::matches` is not written a second time either way.

**2. What each message would get, which the runner acts on.** RESEARCH-4 AUT-6
puts "which writes each message gets from an `Outcome`, with no-ops dropped"
in one pure module, `application::acting_on_a_set`, and the planner's split
of 2026-09-24 put that module in 13-24.1, not 13-24: 13-24.1's plan names
`what_each_message_needs(outcome, message, folders, labels) -> Result<Needs,
WhyNot>`, pure, with the no-op writes already dropped. The count uses that
answer, so the count and the run cannot disagree, and this plan writes no
no-op dropping of its own. The executor reads 13-24.1's summary for the name
as built; where it differs from the plan's, the summary's is the one every
criterion here follows.

What a message carries for the no-op test: `CachedMessage` holds `read`,
`starred` and `folder_id` (`data/message_cache/mod.rs:193-226`), not its
labels; `tags_by_message_in_folder(folder_id)` (`tags.rs:278`) gives a
folder's labels by message in one read, which 13-44 hands in. A phrase said
first is not on `CachedMessage` and no read gives it by folder
(`grep -rn 'fn .*say_first\|fn .*phrase' src/data/message_cache/*.rs` finds
only `set_says_first` at `messages.rs:2824`), so a Say this first rule
counts every match as a change and the question says "would say ... first
on", which is true either way.

**3. The bound and its words.**

```
grep -n 'pub fn too_many\|fn with_commas\|MOST_ROWS_WORTH_SELECTING' src/application/choosing_messages.rs   -> :308, :186 (private), :33; the bound is Select All's, 5,000, and "5,001" is written with its comma
grep -n 'pub fn how_many' src/service/caldav.rs   -> the singular and plural every count here uses
```

`with_commas` is made `pub(crate)` and called, not copied. The comparison in
`too_many` (`choosing_messages.rs:309-311`) is a record's anchor ("a command
over more than the bound is refused with a sentence, not run"); this plan's
comparison lives in its own file, where it names no second place.

**4. The gate for each file, at `630e2a67`.** `src/application/running_a_rule_now.rs`
new, zero records; `src/application/choosing_messages.rs` 18 tests and 3
records, a visibility changed and no test; `src/application/mod.rs` 0 and 0.
Re-taken before the first commit, before and after quoted.
</premise_corrections>

<decisions date="2026-09-24">
**Taken 2026-09-24 from the research's recommendation; Pratik may overrule.**
Pratik confirmed the order of phase 13 and said "plan and implement phase 13"
on 2026-09-24.

1. **Above 5,000 (RESEARCH-4 question 4).** "Refuse above 5,000 with the count
   and the bound, the same number every set command uses; running it again
   after the first 5,000 have moved does the next." Read as a bound on one
   run rather than a refusal of the whole, because the second half has a
   second run do the next: the question says the count and the bound, and a
   run takes the first 5,000 in the order the folder was read.
2. **Which answer Enter gives (question 8).** Yes, except for a rule that
   deletes, where No; through `presentation::asking`'s two styles in 13-44,
   decided here as data.
3. **A switched-off rule can be run by hand (question 6)**, because choosing
   it is the reason to run it.
4. **The runner and the matcher are 13-24.1's and 13-25's (question 12)**,
   called, never copied.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: what a rule would change in a folder, with the no-ops dropped</name>
  <files>src/application/running_a_rule_now.rs, src/application/mod.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/filters.rs:335-480` and `:504-581`.
    - 13-24.1's summary and its pure module, `application::acting_on_a_set`; 13-25's summary and `application::what_a_rule_catches_here`; 13-23's summary for how a rule's label name meets a label.
    - `src/application/choosing_messages.rs:36-100` (`MessageRef`, `Chosen`).
  </read_first>
  <behavior>
    - `WouldChange { matched: usize, changing: Vec<CachedMessage>, outcome: Outcome }`; `what_a_rule_would_change(rule, messages, labels_on, folder_path) -> WouldChange`: the rule's `Outcome` from `settle` over its one action; the matches through premise 1's catcher, switched off or not; each match kept only when 13-24.1's per-message answer, `acting_on_a_set::what_each_message_needs` (premise 2), holds a write.
    - `the_set_to_run(would) -> Chosen`: the changing messages as `MessageRef`s in the order read, no conversations, at most `MOST_ROWS_WORTH_SELECTING` of them.
    - Cases, each a row: a read message under a mark-read rule is not counted and an unread one is; a flagged message under a flag rule; a message in Archive under a move-to-Archive rule run over Archive; a label already on; a delete counts every match; a say-first rule counts every match; a switched-off rule counts as a switched-on one; a rule naming a field this build does not know matches nothing; 5,001 changing gives a set of 5,000 and `matched` and `changing` still say 5,001.
  </behavior>
  <action>
    Red first, on a branch: the module's cases by module path
    (`application::running_a_rule_now::tests::<name>`), a new module at zero
    records, with the signatures answering nothing. Run them first and name
    everything that fails.

    Then the green commit. Invoke `elegant-code`: the module calls the
    catcher and the per-message answer and adds only the count and the set;
    no `unwrap`; no read of the cache, which is 13-44's worker.

    Guard record: one on `running_a_rule_now.rs` whose break counts a read
    message under a mark-read rule, its row red; measured.
  </action>
  <verify>
    <automated>cargo test --lib application::running_a_rule_now::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the module's cases by module path.
    - `grep -c 'pub mod running_a_rule_now' src/application/mod.rs` is 1; `grep -c 'pub fn what_a_rule_would_change' src/application/running_a_rule_now.rs` is 1; `grep -c 'FilterEngine::matches\|which_messages_here_a_rule_catches' src/application/running_a_rule_now.rs` is at least 1.
    - `cargo test --lib application::running_a_rule_now::` passes with at least 9 tests, quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>What a rule would change in a folder is counted from the check's own arm and the runner's own per-message answer, with no-ops dropped and the set bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the question, the answer when there is nothing to do, and the sentence after</name>
  <files>src/application/running_a_rule_now.rs, src/application/choosing_messages.rs, guards/guards.toml</files>
  <read_first>
    - `src/application/choosing_messages.rs:170-317` (`how_many_chosen`, `with_commas`, `what_was_done`, `too_many`).
    - `src/presentation/asking.rs:1-110` (the two answers Enter can give, and why).
    - `src/application/allowed.rs:330-400` (an experimental sentence that says what could go wrong).
  </read_first>
  <behavior>
    - `the_question(rule_name, folder_name, would) -> Question { text, enter_answers_yes: bool }`, one clause for the rule's change: "The rule Newsletters would move 214 messages in Inbox to Archive. Run it?"; "would mark 12 messages in Inbox as read"; "would flag", "would take the flag off", "would label ... Work", "would delete", "would say "Urgent" first on"; when some matches are already that way, "The rule Newsletters matches 230 messages in Inbox; 12 would be marked read and 218 are read already. Run it?"; above the bound, the count with its comma, "One run changes 5,000 at most, so this run would move the first 5,000 and running it again would move the rest."; `RUNNING_A_RULE_NOW_IS_EXPERIMENTAL` ("No rule run has met a real mail server yet.") before "Run it?" whenever 13-24.1's runner sends the change to a server, as its summary says, and never for Say this first; `enter_answers_yes` false exactly when the rule deletes.
    - `nothing_to_change(rule_name, folder_name, matched) -> String`: "The rule Newsletters would change nothing in Inbox." with no match, and "The rule Newsletters matches 16 messages in Inbox, and every one is already that way." otherwise; no question is asked for either.
    - `what_the_rule_did(rule_name, done) -> String`: "Newsletters: 214 messages moved to Archive", `done` being the runner's sentence, one sentence.
    - Cases: each clause, the singular ("1 message"), the comma at 5,001, the bound's sentence, the experimental sentence present for a move and absent for a phrase, Enter's answer for a delete and for a move, both nothing-to-change sentences.
  </behavior>
  <action>
    Red first, on the same branch: the cases by module path, with the
    signatures answering nothing. Run them first and name everything that
    fails.

    Then the green commit, `with_commas` made `pub(crate)` in the same
    commit. Invoke `elegant-code` and `writing-craft` for the sentences:
    what would happen, to how many, where, then the question, in that
    order; the count first after the rule's name, because it is what
    somebody deciding is listening for.

    Guard record: one on `running_a_rule_now.rs` whose break lets Enter
    answer yes to a rule that deletes, its case red; measured.
  </action>
  <verify>
    <automated>cargo test --lib application::running_a_rule_now:: && cargo test --lib application::choosing_messages::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on a branch names the cases by module path.
    - `grep -c 'pub fn the_question' src/application/running_a_rule_now.rs` is 1; `grep -c 'pub const RUNNING_A_RULE_NOW_IS_EXPERIMENTAL' src/application/running_a_rule_now.rs` is 1; `grep -c 'pub(crate) fn with_commas' src/application/choosing_messages.rs` is 1.
    - `cargo test --lib application::running_a_rule_now::` passes with at least 20 tests, and `application::choosing_messages::` with the count it had before, both quoted.
    - One guard record measured and quoted.
  </acceptance_criteria>
  <done>The question says what a rule would do, to how many and where, asks before anything runs, and says plainly when there is nothing to do.</done>
</task>

<task type="auto">
  <name>Task 3: the ledger entry for what nothing reaches yet, and the four marks</name>
  <files>.planning/WINDOWS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/WINDOWS.md`'s front matter and its last entry, both halves.
    - `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-44-PLAN.md`, the premise that names this entry.
  </read_first>
  <action>
    Ledger, by hand, both halves: one `stub` entry, `file`
    `src/application/running_a_rule_now.rs`: the count, the question and
    the sentence after are built and nothing in the running program asks
    them; 13-44 builds the two doors and closes this entry, and 13-44's plan
    names it (its premise 1). The entry's number goes in this plan's summary.

    No changelog entry and no push: nothing spoken or shown changes, and the
    summary says so.

    The four marks: phase 13's progress row; this plan's `- [ ]` line in
    phase 13's plan list (ROADMAP.md; the phase README keeps no boxes) ticked;
    `STATE.md`'s plan number in the frontmatter and the body; a dated line
    under GAP-12's `[D]` line saying the count is built and worded, naming
    `application::running_a_rule_now`, and that the doors are 13-44's. Run
    `cargo test --test the_planning_files_agree_with_themselves` first.
  </action>
  <verify>
    <automated>cargo test --test house_style && cargo test --test the_planning_files_agree_with_themselves</automated>
  </verify>
  <acceptance_criteria>
    - The ledger entry in both halves, its kind `stub`, naming 13-44; `grep -c 'running_a_rule_now.rs' .planning/WINDOWS.md` grew by at least 1.
    - Carriage returns 0 on every file touched, measured with `tr`; no em dash; none of the six words.
  </acceptance_criteria>
  <done>What is counted and not yet asked is on the ledger, named in the plan that asks it, and the four marks are made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the count said to the run done | a person decides on a number that must be the number acted on |
| a rule imported rather than typed | a pattern built elsewhere, run over a whole folder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-43-01 | Tampering | a count that differs from what the run changes | high | mitigate | the count uses 13-24.1's own per-message answer; the no-op rows are cases; a guard record |
| T-13-43-02 | Denial of service | a regex from an imported rule matching slowly over a folder | medium | mitigate | the arm's own 1 MiB bound (`filters.rs:430-433`); the count runs on 13-44's worker, never the window's thread |
| T-13-43-03 | Tampering | Enter deleting a folder's mail partway through hearing the question | high | mitigate | Enter answers no for a rule that deletes; its case and a guard record |
| T-13-43-04 | Denial of service | a run over more than the window can bear | medium | mitigate | the set bounded to Select All's 5,000, said in the question |
| T-13-43-SC | Tampering | npm, pip or cargo installs | high | mitigate | no crate is added to `Cargo.toml`; the summary says so |
</threat_model>

<verification>
- The commands in all three `<automated>` blocks pass; `scripts/check.sh` passes on every commit through the hook, never piped; one `scripts/guards.sh --remeasure` call per commit for every flagged record, as the first run.
- This plan changes nothing spoken or shown: no push and no pull request (Pratik's answer of 2026-09-23); the merge is local with `git merge --no-ff`, whose hook runs what the branch's whole diff earns; the whole suite runs once, in 13-51's full gate. Never `scripts/check.sh all` by hand here.
- What the readings prove: the count, the no-ops, the bound, the words, Enter's answer. What is not proved and is ledgered: that anything asks the question, until 13-44.
</verification>

<success_criteria>
- Phase 13's roadmap criterion 12 advances: what a rule would touch is counted and worded before it runs.
- GAP-12's `[D]` line has a dated line naming this plan's module; the box waits for 13-44; the `[S]` line is untouched.
- The completion marks, all four: phase 13's progress row, this plan's `- [ ]` line in phase 13's plan list (ROADMAP.md; the phase README keeps no boxes), `STATE.md`'s plan number in the frontmatter and in the body, GAP-12's dated line.
</success_criteria>

<output>
Create `.planning/phases/13-new-features-most-from-the-outlook-gap-audit/13-43-SUMMARY.md` when done
</output>
