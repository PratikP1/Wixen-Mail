---
phase: 08-every-number-the-project-quotes
plan: 08
type: execute
wave: 8
depends_on: ["08-01", "08-07"]
files_modified:
  - scripts/mutants.sh
  - scripts/mutants_report.py
  - .cargo/mutants.toml
  - docs/measurements.md
  - docs/plans/20260914-whole-tree-mutation-run.md
  - docs/IMPLEMENTATION_STATUS.md
  - docs/changelog.md
  - guards/guards.toml
autonomous: false
requirements: [PERF-07]
estimate:
  tokens: 22500
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The per-mutant rate on this machine is measured on one shard before anything is scheduled, for both suite shapes the run could use, and the product over the mutant count is on docs/measurements.md with both terms dated."
    - "A whole-tree run is a set of shards on one commit, each shard a unit that can be re-run alone, started by a person, waiting for a quiet machine before each, and skipping shards already complete, so a kill loses one shard and not the run."
    - "The report is read only after every shard's process has exited, merged from every shard's own record, and refused if any shard is missing, partial, or from a different commit."
    - "Mutants whose compiler never started are re-run rather than counted; every survivor is killed with a test or recorded with a reason on a page that is the next round's input; and how much of the tree the run judged is stated, not implied."
    - "Which shape of run to make, and whether the criterion is revised, is Pratik's decision, put to him with the products beside the options."
  artifacts:
    - "scripts/mutants.sh: --shard k/n and --shards n, each shard to its own output directory with the commit written beside it, everything after -- passed through to cargo mutants."
    - "scripts/mutants_report.py: --shards DIR N merging every shard's mutants.json and outcomes.json into one Run, with worked examples for each refusal."
    - "docs/measurements.md: two rate rows, the products, and after the run the wall time, the counts by outcome and how much asked a question."
    - "docs/plans/20260914-whole-tree-mutation-run.md: the survivors, each killed or reasoned, grouped by file, dated, with the run's commit."
  key_links:
    - "cargo mutants --shard k/n -> one list: shards divide the mutant list of the tree they run in, so every shard must run at one commit, which is why the run lives in a worktree and the merger compares the commit files."
    - "scripts/mutants_report.py:283-303 read_run -> the merger: one directory today; the merger reads N of them the same way and sums, and the four refusals in why_this_is_not_an_answer apply to the sum."
    - "08-01's mutant-count row and this plan's rate rows -> the checkpoint: the products the person chooses between are those rows multiplied, with the date."
    - "PERF-07's second [D] line -> the never-started re-run: cargo mutants -F takes a regex over the names --list prints, so the reader's never-started names are the filter for the re-run."
---

<objective>
Make one whole-tree mutation run possible on this machine, put its real
cost to Pratik with the options, run it in shards a person starts, and read
the report after the last process exits.

Purpose: PERF-07 and roadmap criterion 4. What has never happened is a
whole-tree run with a result anybody can trust: the 2026-08-05 attempt filed
473 mutants that never reached a compiler as tested. The count is now
known, 12,335 mutants over 247 files, and the only rate in the tree is from
2026-08-01 over a suite that has since halved and grown back. "About two
days" has no basis; at the rate a guard record costs, the whole tree is
weeks, and the plan says so before anybody starts it.

Output: sharding and merging in the scripts; two measured rates and their
products; the decision; the run; the survivors page.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-every-number-the-project-quotes/README.md
@.planning/phases/08-every-number-the-project-quotes/08-RESEARCH.md
@.planning/phases/08-every-number-the-project-quotes/08-01-SUMMARY.md
@.planning/phases/08-every-number-the-project-quotes/08-07-SUMMARY.md
@docs/plans/20260801-mutation-sweep.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-14 against `main` at `b14d6379`, version `0.124.0`.

**1. The count is 12,335 and the tool shards.**

```
cargo mutants --version                  -> cargo-mutants 27.1.0
cargo mutants --list | wc -l             -> 12335
cargo mutants --list-files | wc -l       -> 247
cargo mutants --help | grep -n 'shard\|baseline\|in-place'
-> --shard k/n; --sharding round-robin|slice; --baseline; --in-place
```

`--shard` divides the list the tool builds from the tree it runs in. Two
shards from two commits are two lists, and a mutant can fall between them
or appear in both. So every shard runs at one commit, in a worktree, and
the merger refuses shards whose recorded commit differs.

**2. `scripts/mutants.sh` writes one output directory and cannot pass
arguments through.** `:69-70`: `OUT="target/mutants"`, fixed; `:75-100`:
three modes, none takes `--shard`, and nothing after the mode reaches
`cargo mutants`. `scripts/mutants_report.py:283-303`: `read_run` reads one
directory. Both change.

**3. The rate has to be measured, and there are two candidates.** `cargo
mutants` runs `cargo test` for the package per mutant: every target at
the harness default thread count. Arguments after `--` go to `cargo test`
and after a second `--` to the test binary, so `-- --lib -- --test-threads=8`
runs only the library at eight threads. From 08-01's suite rows, read as
an inference: about 29 s to rebuild plus the suite figure, per mutant.
With every target that is minutes; with the library at eight threads it
is nearer a minute and a half. Over 12,335 mutants the two differ by
weeks, so both are measured on one shard and both products go to the
person.

**4. One job, still.** `scripts/mutants.sh:60-68`: workers collide on
Windows at `-j 2` and above, cargo-mutants 27.1.0. `MUTANTS_JOBS` stays
at 1 unless a new version is measured to fix it. Sharding is the unit of
resumption, not parallelism; two shards at once on this machine contend the
way `CLAUDE.md` measured for worktrees.

**5. The four refusals still apply and extend to a merge.**
`why_this_is_not_an_answer` refuses a baseline that failed, mutants filed
unviable whose compiler never started, a run in which the suite never ran
against a mutant, and a partial run. A merge of shards is partial if any
shard is, and the merger says which.

**6. Where the tests are.** `scripts/mutants_report.py`'s worked examples
are run and counted by `tests/house_style.rs::test_the_mutation_report_
still_obeys_its_own_examples`, in the tree-reading list; a commit touching
the script on a branch runs it. `scripts/mutants.sh` is reached by
`test_no_mutation_result_is_read_from_the_lists_written_for_people` and
`test_no_mutation_run_has_its_failure_swallowed`, which read it as text.
The summary names all three.

**7. The run may outlast the phase.** If the person chooses the whole
tree at either rate, the shards run for days to weeks of machine time
across idle periods. 08-09 closes the phase on whatever has completed and
says plainly what has not; nothing here waits for the run to finish before
the rest of the phase can close.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shards that can be run one at a time and merged, and the rate measured on one</name>
  <files>scripts/mutants.sh, scripts/mutants_report.py, docs/measurements.md, guards/guards.toml</files>
  <read_first>
    - `scripts/mutants.sh` in full and `scripts/mutants_report.py` in full: `Run`, `run_from_outcomes`, `read_run`, `why_this_is_not_an_answer`, `say_what_it_found`, `main`.
    - `tests/house_style.rs:4366-4400` and `:4805-4818`, the two readings over `mutants.sh`, so the changes keep both true.
    - `cargo mutants --help`, `--shard`, `--sharding`, `--baseline`, `-F` and the `--` rules.
    - `docs/plans/20260801-mutation-sweep.md:1-40`, for the shape of a run recorded with its conditions.
  </read_first>
  <behavior>
    - `scripts/mutants.sh --shard k/n [-- cargo test args [-- binary args]]` runs `cargo mutants --shard k/n` with everything after `--` passed through, writes to `target/mutants/shard-k-of-n/`, and writes the commit (`git rev-parse HEAD`) and the pass-through arguments into `target/mutants/shard-k-of-n/conditions.txt` before the run starts; on exit it reads that one shard through the report as today.
    - `scripts/mutants.sh --shards n [-- ...]` runs shards 0 to n-1 in turn, skipping any whose directory already holds a complete `outcomes.json` (as many outcomes as `mutants.json` declares), waiting before each until no `cargo.exe` or `rustc.exe` is running that it did not start, and printing which shard it is on; killed and restarted, it picks up at the first incomplete shard. `--baseline skip` is passed for every shard after the first complete one, with the reason in a comment: the baseline is the tree's own tests, which the first shard proved.
    - `scripts/mutants_report.py --shards DIR N` reads `DIR/shard-k-of-N` for every k, refuses naming the shard if any directory or record is missing, if any `conditions.txt` records a different commit or different arguments from the others, or if any shard is partial, and otherwise merges into one `Run` and reports it as today. Worked examples for each refusal and for a clean merge of two shards.
    - `--shard` and `--shards` each report the shard's own timing: the wall time, the number of mutants the suite came back about, and the seconds per mutant, on a line whose shape a worked example holds, printed and also written to `target/mutants/shard-k-of-n/timing.txt`, so a restarted launcher's overwritten stdout loses nothing and the merger can sum the shards' wall times from their own directories.
  </behavior>
  <action>
    Red first, on a branch: the worked examples for the merger and the
    timing line before the code, so `house_style::test_the_mutation_report_
    still_obeys_its_own_examples` is the red; name it in the trailer.

    Keep the report's rules. A merged run passes through the same
    `why_this_is_not_an_answer` as a single one, so a set of shards with
    one that never built is refused the same way. Write the commit check
    in the merger and not in the shell, because the shell has no test.

    Then measure the rate. Pick `n` so one shard is about twenty-five
    mutants (`n` near 500 for today's count; compute it from
    `cargo mutants --list | wc -l` on the day), run `--shard 0/n` twice:
    once with no pass-through, once with `-- --lib -- --test-threads=8`.
    Read each shard's timing line. Add four rows to `docs/measurements.md`:
    the two rates, each with the shard, the commit, the arguments and the
    thread setting in `conditions`, and the two products over the day's
    mutant count, each `value` showing the multiplication. If a shard
    reports mutants that never started, note it on the row; that failure
    comes and goes here.

    **Guard record, measured on the target.** The break makes the merger
    accept shards from two commits; `suite` is `house_style`, `red` names
    the examples test. Apply, run, quote.
  </action>
  <verify>
    <automated>cargo test --test house_style test_the_mutation_report_still_obeys_its_own_examples && cargo test --test house_style test_no_mutation_result_is_read_from_the_lists_written_for_people && cargo test --test house_style test_no_mutation_run_has_its_failure_swallowed && cargo test --test every_number_carries_its_command_and_its_date</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names `house_style::test_the_mutation_report_still_obeys_its_own_examples`.
    - The four commands in `<verify>` pass at green; `python -m doctest -v scripts/mutants_report.py` reports at least five more worked examples, both counts quoted.
    - `scripts/mutants.sh --shard 0/<n>` ran twice with the two argument shapes and each wrote `conditions.txt` and a complete `outcomes.json`; the summary quotes both timing lines.
    - `docs/measurements.md` has two rate rows and two product rows, the products shown as multiplications over the day's `--list` count.
    - `scripts/mutants_report.py --shards target/mutants <n>` on the one completed shard refuses naming the missing shards, quoted.
    - One guard record, measured, counts quoted.
  </acceptance_criteria>
  <done>A whole-tree run is a set of shards on one commit that a person can start, stop and merge, and what one mutant costs on this machine is measured under both suite shapes.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: which run to make, then start it on an idle machine</name>
  <decision>
    A whole-tree mutation run over 12,335 mutants is not about two days on
    this machine at any rate it can give. The products are on
    `docs/measurements.md` from task 1 and the executor quotes them here
    with their dates. What runs, and whether criterion 4 is revised, is
    yours.
  </decision>
  <context>
    | Option | What runs | What it costs and gives up |
    |---|---|---|
    | 1. The whole tree, every target | every mutant, `cargo test` as the gate runs it | the larger product; nothing given up; weeks of idle machine time in shards |
    | **2. The whole tree, the library at eight threads. Recommended** | every mutant, `-- --lib -- --test-threads=8` | the smaller product, still days; the integration targets under `tests/` do not run against mutants, and most of those read source, documents or windows and catch few, but a mutant only they would catch survives and is filed as untested |
    | 3. A scoped run and a revised criterion | the modules no mutation run has touched, listed from `docs/plans/20260801-mutation-sweep.md` and the 2026-08-12 record, with `--lib` | hours; criterion 4 is revised under criterion 6 with this table as the reason, and PERF-07 stays open with the whole tree as the next round |
    | 4. A combination | option 3 now, option 2 across the weeks after | the scoped answer arrives inside the phase and the whole one arrives when it does |

    Recommended: 2, started now, read by task 3 when it completes, and
    08-09 closing the phase on whatever has completed with the rest stated
    as running. Both shards and the merge refuse a run that mixes commits,
    so the worktree stays at one commit for the whole run however long it
    takes.

    **What none of these settle.** Whether a survivor is dead code or
    untested behaviour is a judgement per survivor, and at this count the
    reading is the larger job; task 3 groups and reasons, and the page it
    writes is the next round's input, not a headline.
  </context>
  <options>
    - Option 1, the whole tree with every target.
    - Option 2, the whole tree with the library at eight threads, recommended.
    - Option 3, a scoped run and criterion 4 revised with the reason.
    - Option 4, a combination, which is the usual answer here.
  </options>
  <resume-signal>
    Say which. Then start it, in a worktree at the commit 08-07 merged
    (`git worktree add ../wixen-mail-mutants <commit>`), with nothing else
    building, from PowerShell inside the worktree:
    `Start-Process -FilePath bash -ArgumentList 'scripts/mutants.sh','--shards','<n>','--','--lib','--','--test-threads=8' -WindowStyle Hidden -RedirectStandardOutput mutants.log`
    with the arguments the option chose and `n` from task 1's sizing. Close
    the window. Kill it when you need the machine; restart with the same
    command and it picks up at the first incomplete shard. Tell the
    executor when a start prints that every shard is complete, and do not
    read the report before then.
  </resume-signal>
  <acceptance_criteria>
    - The answer is recorded in the summary with the date and Pratik's words.
    - The worktree's commit, the shard count, the arguments and the start date are in the summary.
    - If option 3 or 4 was chosen, criterion 4's revision is written into the summary for 08-09 to carry into the roadmap with this table as the reason.
  </acceptance_criteria>
  <done>The run that is made is the one the person whose machine it is chose, with the cost in front of him.</done>
</task>

<task type="auto">
  <name>Task 3: the report read after the last process exits, never-started mutants re-run, every survivor killed or reasoned</name>
  <files>docs/plans/20260914-whole-tree-mutation-run.md, docs/measurements.md, docs/IMPLEMENTATION_STATUS.md, docs/changelog.md, .cargo/mutants.toml</files>
  <read_first>
    - `scripts/mutants_report.py`, `say_what_it_found` and `how_much_of_it_asked_a_question`, for the report's own vocabulary.
    - `docs/plans/20260801-mutation-sweep.md` in full, the shape of a run's page and its progress table, which is a record and is not edited.
    - `.planning/REQUIREMENTS.md`, PERF-07, the three `[D]` lines, each read as a clause to answer.
    - `CLAUDE.md`, "Read a partial run as partial" and the paragraph beginning "A compiler that never starts".
  </read_first>
  <action>
    **Reading a report and writing it up is a document change; killing a
    survivor with a test is code and is red then green per survivor, with
    the survivor's name in the red commit's trailer as the reason the test
    exists. The summary says which commits were which.**

    Only after every shard is complete: `scripts/mutants_report.py
    --shards target/mutants <n>` from the worktree, and keep its whole
    output. If it refuses, the refusal is the finding; fix what it names
    (re-run the shard it calls partial) and read again.

    Mutants the report lists as never started: build the `-F` regex from
    their names per file and re-run them with `cargo mutants -F` in the
    worktree, at the same commit and arguments, until each has been
    handed to the suite or the compiler rejected it. Merge those outcomes
    into the count by hand on the page, saying they were re-run and why.

    Write `docs/plans/20260914-whole-tree-mutation-run.md` on the 2026-08-01
    page's shape: the commit, the dates, the arguments, the shard count,
    the rate rows by reference, the counts by outcome, how much of the tree
    asked a question, and then every survivor grouped by file, each line
    saying one of: killed, with the commit of the test; dead code, with the
    commit that removed it; equivalent, with why the change cannot be
    observed; a string only a person reads, on the config's own
    `exclude_re` reasoning; untested behaviour, queued as the next round's
    input with the module named. Timeouts are listed with the config's
    warning that a busy machine produces them and are neither killed nor
    counted as caught. If a group shares one reason, the reason is written
    once above the group and each line still says it; a reason that
    covers whatever survived is not a reason.

    Kill what can be killed inside the plan's budget, red then green,
    smallest files first because a test added to a file many records name
    costs re-measurements (premise: `src/application/contacts_sync.rs` is
    named by 77 records); run each remedy the count check prints, detached,
    and read it. What is not killed is queued, and the ledger gains one
    entry naming the page as the next round's input.

    Add rows to `docs/measurements.md`: the run's wall time from the
    shards' timing lines summed, the counts by outcome, the never-started
    re-run's counts, and how much asked a question. Point the status page's
    mutation paragraph at the run page. In `.cargo/mutants.toml`, if the
    run produced timeouts, add the dated figure to the calibration comment
    beside 08-01's, without changing either setting unless the run showed
    a real hang, and say which.

    Changelog under `[Unreleased]`: the run, its counts, the survivors
    page, what was killed and what is queued. Under Known limitations: the
    run judged one commit; the integration targets did not run against
    mutants if option 2 was chosen; the wx layer is excluded by the config
    and its reasoning; the queued survivors are untested behaviour until
    the next round.
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && cargo test --test house_style test_no_dashes_that_should_be_punctuation && python scripts/mutants_report.py --shards target/mutants $(ls target/mutants | grep -c shard-)</automated>
  </verify>
  <acceptance_criteria>
    - The merged report's whole output is quoted in the summary and its counts match the page.
    - Every mutant the report listed as never started was re-run, and the summary says how many there were and what each became.
    - The run page lists every survivor with one of the five dispositions, grouped by file, and the count of lines equals the report's missed count; the summary states both numbers.
    - Every killed survivor has a red commit naming it and a green commit, and the summary lists them.
    - `docs/measurements.md` has the run's rows; the status page points at the run page; the changelog entry carries the four Known limitations.
    - `.cargo/mutants.toml`'s settings are unchanged, or the summary says what hang the run showed.
  </acceptance_criteria>
  <done>One whole-tree run has a result somebody can trust: read after it exited, its untested part re-run, and its survivors each killed or written down with a reason as the next round's input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shards to one list | shards from different commits are different lists that read as one |
| the writer to the reader | the tool records every phase of every mutant; a reader that sums lists written for people reads a partial run as a result |
| a survivor to a reason | a reason that fits whatever survived is a headline, not a judgement |
| the run to the machine | a run of days on a machine that is also used has timeouts that are the machine, not the code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-33 | Tampering | shards merged from two commits | critical | mitigate | each shard writes its commit before it starts; the merger refuses a mismatch and a worked example shows it; the run lives in a worktree. |
| T-08-34 | Repudiation | a partial or never-started shard read as a result | high | mitigate | the merger applies the same four refusals as a single run, per shard and to the sum; the checkpoint forbids reading before every shard is complete. |
| T-08-35 | Denial of service | a run scheduled from the 2026-08-01 rate or from "about two days" | high | mitigate | the rate is measured on one shard under both shapes before the decision; the products are on the page with dates; the decision is Pratik's with the table. |
| T-08-36 | Tampering | timeouts from a busy machine filed as hangs or hidden by lowering the multiplier | medium | mitigate | `--shards` waits for a quiet machine before each shard; timeouts are listed and not counted; the settings change only for a shown hang. |
| T-08-37 | Repudiation | survivors dismissed under one reason | medium | mitigate | five dispositions, one per line, grouped but each line answering; the page is the next round's input and the ledger names it. |
| T-08-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `cargo-mutants` is already installed and its version is on the rows. |
</threat_model>

<verification>
- The three `house_style` readings over the mutation scripts pass, with the new worked examples counted.
- `scripts/check.sh` passes on every commit through the hook, never piped; a commit touching the scripts answers `affected` on a branch and runs the tree-reading guards; `scripts/check.sh all` once before each merge.
- The report was read only after every shard's process exited, and the summary quotes the final shard line that says so.
- Every remedy the count check printed after a killing test was run and read.
- Every document this plan writes carries no em dash and no carriage return.
</verification>

<success_criteria>
- The rate is measured, the products are dated, and the decision was Pratik's with the costs in front of him.
- A whole-tree run, or the run he chose, completed in shards on one commit and its report was read after it exited.
- Never-started mutants were re-run rather than counted.
- Every survivor is killed or has a reason on a page that is the next round's input, and the page says how much of the tree the run judged.
</success_criteria>

<output>
Create `.planning/phases/08-every-number-the-project-quotes/08-08-SUMMARY.md` when done
</output>
