---
phase: 08-every-number-the-project-quotes
plan: 05
type: execute
wave: 5
depends_on: ["08-01", "08-02"]
files_modified:
  - docs/measurements.md
  - docs/IMPLEMENTATION_STATUS.md
  - docs/changelog.md
autonomous: true
requirements: [PERF-05]
estimate:
  tokens: 15000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Line coverage has a current number with its date, its command and its commit, on docs/measurements.md, and the status page quotes that row rather than the 2026-07-26 one."
    - "The least covered areas are named with their own figures, and each is attributed to the network transport that has never met a live account where that is the reason, rather than treated as a number to raise."
    - "Nothing in this plan writes a test to move the number; the requirement says attribute, and the changelog says why."
  artifacts:
    - "docs/measurements.md: one row for whole-library line coverage and one row per attributed area."
    - "docs/IMPLEMENTATION_STATUS.md: the coverage paragraph rewritten around the new row, the old figure kept as the figure of its date."
  key_links:
    - "cargo llvm-cov --lib --summary-only -> the row: the same command the 2026-07-26 figure came from, so the two are the same quantity and the difference is real."
    - "the per-file table -> the attribution: the areas are summed from the per-file lines of the same run, with the sum shown, not read off a separate run."
    - "08-02's provenance reading -> the status page: the rewritten paragraph is read on every commit and must carry its date and command."
---

<objective>
Re-measure line coverage, write the number down with its conditions, and
attribute the low areas rather than raise them.

Purpose: PERF-05. The last reading is 60.4% on 2026-07-26 and 1,824 of the
repository's 2,023 commits postdate it. The requirement's own `[S]` line
already says the low coverage in the transport is the same fact as never
having met a live account, and roadmap criterion 3 repeats it; this plan
makes that attribution a sentence with figures beside it.

Output: the rows; the paragraph; the changelog entry.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-every-number-the-project-quotes/README.md
@.planning/phases/08-every-number-the-project-quotes/08-RESEARCH.md
@.planning/phases/08-every-number-the-project-quotes/08-01-SUMMARY.md
@.planning/phases/08-every-number-the-project-quotes/08-02-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-14 against `main` at `b14d6379`, version `0.124.0`.

**1. The tool is installed and the last reading is where the research
said.**

```
cargo llvm-cov --version                 -> cargo-llvm-cov 0.8.7
grep -n "60.4" docs/IMPLEMENTATION_STATUS.md   -> :265
git rev-list --count --since=2026-07-26 HEAD   -> 1824
git rev-list --count HEAD                       -> 2023
```

**2. A coverage run is a full instrumented rebuild.** It shares no
fingerprints with the ordinary build, so it costs a full compile of the
library and its tests, on a `target/` the research measured at 246 GB in
debug artefacts on 2026-09-12. Run it with nothing else building and
expect it to take longer than the gate; write the wall time on the row's
`conditions` so the next person knows.

**3. The transport is where the research says it is.**

```
ls src/service/protocols   -> imap, imap.rs, pop3, pop3.rs, smtp.rs, xoauth2.rs, mod.rs
ls src/service | grep -E 'oauth|google_api|microsoft_graph|caldav|carddav|onenote'
```

The areas to attribute are `src/service/protocols/`, `src/service/oauth.rs`
and `oauth_credentials.rs`, and the provider clients `google_api.rs`,
`microsoft_graph.rs`, `caldav.rs`, `carddav.rs`, `onenote_notes.rs` and
`onenote_page.rs`. Each is summed from the per-file lines of the one run.

**4. Two coverage targets are in the tree and they differ.**
`docs/architecture.md:400` says "target: 80%+"; `docs/integration-guide.md:6`
says a 95% target "that was never met" and keeps the page as historical.
Neither is this plan's to judge; 08-09 does that under criterion 6. This
plan records the measurement they will be judged against.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: coverage re-measured, whole library and per area, from one run</name>
  <files>docs/measurements.md</files>
  <read_first>
    - `docs/IMPLEMENTATION_STATUS.md:265-270`, the paragraph and the command it names.
    - `cargo llvm-cov --help`, for `--summary-only`, `--text`, `--json` and `--ignore-filename-regex`.
    - `docs/measurements.md`, the row shape and 08-01's rows.
  </read_first>
  <action>
    **This is a measurement written to a page, which is a document change;
    no code is written and the summary says so.**

    With nothing else building, run `cargo llvm-cov --lib --summary-only`
    exactly as the 2026-07-26 reading did, and time it. Keep the whole
    output. Then, from the same run, get the per-file table: `cargo
    llvm-cov --lib --text` is per line and large, `--json` is per file and
    is what to sum; read the `--help` and pick the one that gives a
    per-file line count and covered count, and say which. Sum the lines
    and the covered lines for each area in premise 3, and compute each
    area's percentage from its sums, showing the sums.

    Add rows to `docs/measurements.md`: whole-library line coverage, with
    the command, the date, the commit, the version, and in `conditions`
    the wall time, the thread count if any, and that it is `--lib` and not
    `--all-targets`; one row per area with its sums in `value` and the
    same command in `command`; one row for the per-file reading's command.
    Do not add a test. The requirement says attribute.
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && grep -c "llvm-cov" docs/measurements.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/measurements.md` holds a whole-library coverage row and at least three area rows, each with date, commit, version and command, and the reading passes over them.
    - The whole-library row's command is `cargo llvm-cov --lib --summary-only`, so the quantity matches the 2026-07-26 reading's.
    - Each area row's `value` shows covered lines over total lines and the percentage from them.
    - The summary quotes the summary-only output's last line and the wall time.
  </acceptance_criteria>
  <done>Coverage has a current figure and the low areas have theirs, all from one run, all on one page.</done>
</task>

<task type="auto">
  <name>Task 2: the status page quotes the row and attributes the low areas</name>
  <files>docs/IMPLEMENTATION_STATUS.md, docs/changelog.md</files>
  <read_first>
    - `docs/IMPLEMENTATION_STATUS.md:255-275`, the paragraph and the ones around it, so the rewrite keeps the page's voice.
    - `.planning/REQUIREMENTS.md`, PERF-05, the `[S]` line about the transport and the `[D]` line, so the paragraph answers both.
    - `docs/wcag-coverage.md:1-30`, for how 06-07 wrote a corrected sentence keeping the old wording with its date.
  </read_first>
  <action>
    **A document change; `writing-craft` applies, and the summary says
    this task wrote no code.**

    Rewrite the coverage paragraph on the status page. It states the new
    figure with its date and command by reference to the page's row; it
    keeps "was 60.4% on 2026-07-26" as the figure of that date; and it
    attributes, by name and with each area's figure, the transport and the
    provider clients to never having met a live account, saying that no
    amount of test writing substitutes for that and that the number is not
    a number to raise. If an area outside the transport is low, say so and
    say it is not attributed, because an attribution that covers whatever
    is low is not one. 08-02's provenance reading reads this paragraph on
    every commit; it must carry the date and the command.

    Under `[Unreleased]` in the changelog: coverage re-measured, the
    figure, and that the low areas are attributed and not raised, with the
    reason.
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && cargo test --test house_style test_no_dashes_that_should_be_punctuation</automated>
  </verify>
  <acceptance_criteria>
    - The status page's paragraph carries the new figure, its date and its command, and the 2026-07-26 figure as the figure of that date.
    - Every area in premise 3 is named with its own figure and attributed, and any low area outside them is named as not attributed.
    - Both commands in `<verify>` pass.
    - The changelog entry exists under `[Unreleased]`.
  </acceptance_criteria>
  <done>The page says what coverage is, when it was taken, and why the low parts are low, and it does not promise to make them higher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a coverage figure to a claim | a percentage read as a quality figure rather than as what never runs |
| a per-file table to a sum | an area summed from a different run than the headline is a different quantity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-21 | Tampering | an attribution that names whatever is low | high | mitigate | the areas are named in advance from the requirement's `[S]` line; a low area outside them is reported as not attributed. |
| T-08-22 | Repudiation | area figures from a different run than the headline | medium | mitigate | one run, the per-file table from it, the sums shown on the rows. |
| T-08-23 | Denial of service | a coverage rebuild contending with another build and corrupting `target/` | medium | mitigate | run with nothing else building; the wall time on the row tells the next person what to expect. |
| T-08-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `cargo-llvm-cov` is already installed and its version is on the row. |
</threat_model>

<verification>
- `cargo test --test every_number_carries_its_command_and_its_date` passes over the new rows and the rewritten paragraph.
- `scripts/check.sh` passes on every commit through the hook, never piped; this plan's commits are documents only and answer `docs_only`; `scripts/check.sh all` once before the merge.
- No test was added anywhere; no guard record was written or re-measured; the summary says so.
- Every document this plan writes carries no em dash and no carriage return.
</verification>

<success_criteria>
- A current coverage number replaces the stale one, with its date and command, and the stale one is kept as the figure of its date.
- The low areas are attributed to the untested transport with their figures beside them, and nothing treats the number as one to raise.
</success_criteria>

<output>
Create `.planning/phases/08-every-number-the-project-quotes/08-05-SUMMARY.md` when done
</output>
