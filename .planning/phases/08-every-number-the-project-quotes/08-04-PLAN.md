---
phase: 08-every-number-the-project-quotes
plan: 04
type: execute
wave: 4
depends_on: ["08-01"]
files_modified:
  - src/presentation/virtual_rows.rs
  - src/presentation/sample_mailbox.rs
  - src/presentation/mail_sort.rs
  - src/presentation/mod.rs
  - src/presentation/wx_app.rs
  - tests/the_list_reads_only_memory.rs
  - tests/the_list_at_two_hundred_thousand_rows.rs
  - guards/guards.toml
  - docs/development/measurements.md
  - docs/changelog.md
autonomous: true
requirements: [PERF-03]
estimate:
  tokens: 22500
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The text the message list paints for a row comes from a function whose inputs are slices and copies, so it cannot reach the connection the program holds; the closure the list calls does nothing but lock, borrow and call it; and a reading holds both the function and the closure to naming no database, with a companion that plants a call and sees it named."
    - "The sort, the filter and the scroll paths each have a number over 200,000 rows on docs/development/measurements.md, taken by a harness that builds the rows and runs the real functions, with no window, and each row says what was timed and what was not."
    - "The sample mailbox generator and the sort live in modules a test outside the wx layer can reach and the mutation tool can mutate, and the Help menu path that loads the sample is unchanged."
    - "No criterion here claims a real provider mailbox was used, and the page says so in the rows' conditions."
  artifacts:
    - "src/presentation/virtual_rows.rs: text_for over slices, its tests."
    - "src/presentation/sample_mailbox.rs and src/presentation/mail_sort.rs: the generator and the sort, moved, with the two guard records that break the sort re-pointed and re-measured."
    - "tests/the_list_reads_only_memory.rs: the source reading over virtual_rows.rs and the closure, its companion."
    - "tests/the_list_at_two_hundred_thousand_rows.rs: the harness, the format-holding tests, the measurement behind #[ignore]."
    - "docs/development/measurements.md: rows for the listing read, the filter, each sort order, the page paint and the full pass."
  key_links:
    - "wx_app.rs:1222-1253 -> virtual_rows::text_for: the message list's closure, registered on msg_list, captures state and column_layout, locks, reads visible columns, and dispatches on showing; all of that after the lock is the function. The second registration at :1572 is the five PIM lists' one closure and is outside PERF-03's claim."
    - "wx_app.rs:22225 -> mail_sort::sort_messages: two guard records break lines inside it, named in premise 4, and their file key moves with it."
    - "wx_app.rs:9809-9870 -> sample_mailbox: the constant and the generator; the handler at :5271-5281 keeps calling them by the new path."
    - "the filter -> MessageCache::search_messages at searching.rs:477: the real path the search box takes, which the sample mailbox never reaches because it is pushed into memory through MessagesLoaded and never enters SQLite; the harness writes rows into a tempfile cache so it can."
---

<objective>
Exercise the message list against 200,000 synthetic rows so that sort,
filter and scroll each produce a number, and prove by type and by reading
that the virtual text callback reads memory and never the database.

Purpose: PERF-03's two halves. The generator ships and is reachable, and no
number from it exists anywhere in the tree. The callback's comment says it
never touches the database, and no test says so. Both halves were verified
this way on 2026-09-04 and again on 2026-09-12 and are still so today.

Output: three modules pulled out of the wx layer where tests and the
mutation tool can reach them; a reading and a record for the callback; a
harness and its numbers.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-every-number-the-project-quotes/README.md
@.planning/phases/08-every-number-the-project-quotes/08-RESEARCH.md
@.planning/phases/08-every-number-the-project-quotes/08-01-SUMMARY.md
@.planning/phases/03-mail-at-scale-on-the-wire/03-04-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-14 against `main` at `b14d6379`, version `0.124.0`. Every
`wx_app.rs` line number moves; re-take with the greps shown.

**1. There are two virtual text callbacks in the file, and PERF-03 is
about one of them.**

```
grep -n 'set_virtual_text_callback' src/presentation/wx_app.rs
-> 1222:            let callback_registered = msg_list.set_virtual_text_callback({
-> 1572:                let registered = list.set_virtual_text_callback(move |row, column| {
sed -n 265,452p src/presentation/wx_app.rs | grep -E '^\s+pub [a-z_]+:'   -> 43 fields; none is a MessageCache, a Connection or a rusqlite type
grep -n 'message_cache: Option<MessageCache>' src/presentation/wx_app.rs   -> :549, a field of WxMailApp, not of WxUIState
```

The first draft of this plan said one registration at `:1205`; the grep
answers two. The one at `:1222` is the message list's, registered on
`msg_list`, and it is the callback PERF-03 means: the requirement's
evidence cites it by line, its comment is the "never touches the database"
claim, and 200,000 rows is the message list's scale. The one at `:1572` is
registered in a loop over the five PIM lists, dispatches by name to
`pim_rows::*` cell functions, and reads `s.contacts`, `s.events`,
`s.reminders`, `s.tasks` and `s.notes` out of the same locked state with
`date_settings` and `now`. It is outside PERF-03's claim, which names the
message list and the 200,000-row sample, and no PIM list is exercised at
scale by this phase. The reading in task 1 anchors on
`msg_list.set_virtual_text_callback(`, asserts that exactly two
registrations exist so a third arriving is noticed, holds the message
list's closure to calling `text_for` and naming no database, and holds the
PIM closure to naming no database only, because that assertion costs one
line and extracting the PIM closure is not this phase's work.

The type-level claim is available today for the message list and costs an
extraction: the closure locks `Arc<StdMutex<WxUIState>>`, borrows
`Rc<RefCell<ColumnLayout>>`, and dispatches on `state.showing` to
`message_rows::cell_text` or `conversation_cell_text`. Everything after the
lock takes slices and copies.

**2. The type alone does not close the question, so the companion adds
something.** A function whose inputs hold no connection can still open one
from a path literal. The reading in task 1 holds `virtual_rows.rs` and the
closure to naming no `MessageCache`, no `rusqlite`, no `Connection` and no
`open`, and that half is a recordable guard because its break compiles. The
phase README's Assumption 4 said a companion only if the planner found it
added something; this is the finding, and it is the same shape the research
named at `messages.rs:7973`.

**3. The sample mailbox never enters SQLite, so it cannot give a filter
number.** `wx_app.rs:5271-5281`: the handler generates the rows and sends
`UIUpdate::MessagesLoaded(generated)`. The search box's path is
`MessageCache::search_messages` at `searching.rs:477`, a SQL query over the
cache. Assumption 3 in the README: write 200,000 rows into a `tempfile`
cache and time `search_messages`. The listing read the list takes when a
folder opens, `get_messages_for_folder` at `messages.rs:2056`, is timed on
the same cache, because it is the read that precedes any scroll.

**4. Two guard records break lines inside `sort_messages`.**

```
python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print([r['name'] for r in g if 'sort_by_key(|a| a.read)' in r['before'] or 'b.date.cmp(&a.date)' in r['before']])"
-> ['each way of sorting the message list really puts it in that order', 'sorting by unread brings the unread ones to the top']
```

Both name `src/presentation/wx_app.rs` as `file` and one test each in
`wx_app.rs`'s test module. Moving the function moves the break; the
records' `file` key follows it and each is re-measured. No record breaks
`sample_mailbox` or the callback (`grep -c` for `senders[i % senders.len()]`,
`Sample message`, `set_virtual_text_callback` in `guards/guards.toml` all
answer 0).

**5. The tests that call `sort_messages` stay where they are.**
`wx_app.rs:23293` and `:23304` call it from that file's own test module.
After the move they call it through a `use`, unchanged, so `wx_app.rs`'s
test count stays at 199 and its 48 records are not flagged. New tests go in
the new modules, which are at zero records.

**6. Phase 3 measured at 200,000 rows and removed the instrument.**
`03-03-SUMMARY.md:301-305`: "The measurement was taken with a temporary test
that was then removed ... It is not in the tree: it takes minutes and asserts
nothing." `03-04-SUMMARY.md:284-295` records the listing at 0.75 to 1.2 s on
200,000 rows in 10,000 conversations, release, warm. This plan keeps its
instrument, behind `#[ignore]`, with the format-holding half running on
every commit, so the number can be taken again.

**7. The sort allocates a lowercase copy per comparison.** `wx_app.rs:22229-
22236`: `sort_by_key(|a| a.from.to_lowercase())`. Over 200,000 rows that is
millions of allocations and the sender and subject orders may come out
seconds apart from the date orders. The sort runs off the UI thread
(`apply_sort` at `:22176` spawns it and sends `MessagesLoaded`), so a slow
sort is not a freeze. This plan measures and records; it does not change
the sort. If an order exceeds a second the row says so and the ledger
carries the one-line fix, `sort_by_cached_key`, for a plan that can take it
red then green against the measurement.

**8. `cargo mutants` excludes `src/presentation/wx_*.rs`.** `.cargo/
mutants.toml:18`. A pure function moved out of `wx_app.rs` into a module
of its own becomes something 08-08's run can mutate, which is a reason for
the move beyond reachability from `tests/`.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the row text comes from a function that cannot reach the database, and a reading says so</name>
  <files>src/presentation/virtual_rows.rs, src/presentation/mod.rs, src/presentation/wx_app.rs, tests/the_list_reads_only_memory.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:1196-1258`, the comment above the message list's callback and the closure in full, `:1560-1600`, the PIM lists' one closure registered in a loop, and `:1096`, where `date_settings` comes from.
    - `src/presentation/message_rows.rs:59-70` and `:125-135`, the two cell functions the closure dispatches to, and `:41`, `PLACEHOLDER`.
    - `src/presentation/view_state.rs`, `Showing`, and `src/presentation/message_columns.rs` for `ColumnLayout::visible` and `MessageColumn`.
    - `src/data/message_cache/messages.rs:7973-8060`, the listing guard and its companion, the shape this reading follows.
    - `tests/wired.rs:1-80`, the header on source-reading tests over `wx_app.rs` and how they cut the file, and one reading that finds a closure by its registration call.
  </read_first>
  <behavior>
    - `virtual_rows::text_for(showing: Showing, messages: &[MessageItem], conversations: &[ConversationItem], columns: &[MessageColumn], row: u32, column: u32, dates: DateSettings, now: DateTime<Local>) -> String` answers what the closure answers today: the cell text for the row and column in the view being shown, `PLACEHOLDER` for a row past the end, an empty string for a column past the visible ones.
    - Tests in the module: a message row and a conversation row each give their cell text; a row past the end gives `PLACEHOLDER` in both views; a column past the end gives an empty string; the function's signature is the type-level claim and the doc comment says so.
    - The closure in `wx_app.rs` becomes: take the lock or answer `PLACEHOLDER`; borrow the visible columns; call `text_for`. Nothing else.
    - The reading in `tests/the_list_reads_only_memory.rs` reads `src/presentation/virtual_rows.rs` up to its first `#[cfg(test)]`, counts the registrations of `set_virtual_text_callback(` in `wx_app.rs` and requires exactly two, naming the count if it moves; reads the message list's closure body between `msg_list.set_virtual_text_callback(` and its closing `})` and requires it to contain `text_for(` and none of `MessageCache`, `rusqlite`, `Connection`, `open(` or `conn`, so a closure rewritten to do its own work is caught; and reads the PIM lists' closure body after `list.set_virtual_text_callback(` and requires it to name none of the same five, without asking for `text_for(`, because that closure is outside PERF-03's claim and is read only because the assertion is one line. Companions: one splices `MessageCache::new(` into the function's real text and requires exactly that complaint; one removes `text_for(` from the message list's closure and requires that one; one plants a third registration and requires the count complaint.
  </behavior>
  <action>
    Red first, on a branch. The new module and the new target are at zero
    records and redden against nothing existing; say so. Adding a module
    line to `presentation/mod.rs` changes no test count.

    Write `virtual_rows.rs` with the function and a doc comment that makes
    the argument in words: the callback runs while wxWidgets paints; its
    inputs are slices and copies; there is no connection to reach because
    none is passed; and the reading in `tests/` is what stops the body
    opening one, which the type cannot. Say that this is the check
    PERF-03's second `[D]` line asks for, in two halves, and why neither
    half alone is it. Name in the comment that this cannot be a guard
    record in the type half, because its break is a compile error and the
    runner measures breaks by which tests go red; the reading half is the
    recordable one.

    Rewrite the closure to the three lines. Keep the comment above it about
    why the callback reads the view out of state rather than being
    registered again; move the sentence about never touching the database
    onto `text_for`'s doc comment, where the claim is now held.

    Write the target on the shape of `tests/wired.rs`: read the file, cut
    at the first test module, find the message list's closure by its
    registration call on `msg_list` and the PIM closure by its call on
    `list`, count the registrations, and fail with the line quoted. Do not cut `wx_app.rs` at its first
    `#[cfg(test)]` and keep what is above as "what ships": `wired.rs`'s
    header records why that is wrong for that file and what it does
    instead; follow it.

    **Guard record, measured on the target.** The break plants one line
    inside `text_for` that calls the cache type's constructor with an empty
    path and discards the result, which compiles; `suite` is the new
    target; `red` names the reading. Apply by hand, run, quote. Then a second record for
    the closure half: the break replaces the `text_for(` call with the old
    dispatch inlined, if that can be done as one edit that compiles;
    if it cannot, say so and record only the first.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::virtual_rows:: && cargo test --test the_list_reads_only_memory && bash scripts/check.sh --suites-for guards/guards.toml src/presentation/virtual_rows.rs</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the module's tests and the target's tests in `Fails-until-green:` trailers.
    - The three commands in `<verify>` pass at green and the last prints the target.
    - `grep -c 'text_for(' src/presentation/wx_app.rs` is 1 and it is inside the closure registered by `msg_list.set_virtual_text_callback`; `grep -c 'set_virtual_text_callback' src/presentation/wx_app.rs` is 2; the summary quotes the message list's closure and says the PIM closure was held to naming no database and nothing more.
    - `grep -n 'MessageCache\|rusqlite\|Connection' src/presentation/virtual_rows.rs` finds nothing outside the doc comment that names them as forbidden, and the reading excuses that comment by cutting doc comments before it reads, with a test that shows it.
    - `wx_app.rs`'s `#[test]` count is 199 before and after; the summary says so.
    - Guard records: at least one, measured, counts quoted.
  </acceptance_criteria>
  <done>What the list paints is computed from slices by a function that holds nothing else, the closure only calls it, and a reading holds both to that on every commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the generator and the sort live where a test outside the wx layer can reach them</name>
  <files>src/presentation/sample_mailbox.rs, src/presentation/mail_sort.rs, src/presentation/mod.rs, src/presentation/wx_app.rs, guards/guards.toml</files>
  <read_first>
    - `src/presentation/wx_app.rs:9805-9870`, the constant, the generator and its doc comment on why it is on the Help menu; `:5271-5281`, the handler; `:22176-22240`, `apply_sort` and `sort_messages`; `:23280-23310`, the two tests that call the sort.
    - `guards/guards.toml`, the two records named in premise 4, in full.
    - `src/presentation/ui_types.rs:216`, `MailSortOption`.
  </read_first>
  <behavior>
    - `sample_mailbox::SAMPLE_MAILBOX_SIZE` and `sample_mailbox::sample_mailbox(count)` are the same constant and function, `pub`, in their own module with the doc comment moved with them; the handler calls them by the new path and the menu item, its id and its label are untouched.
    - Tests in `sample_mailbox.rs`: the count asked for is the count returned; uids are one-based and dense; dates descend; every fifth row is a thread parent and the rest name a thread; the fifth subject is empty, because the generator's rows are what the scale numbers are taken over and a generator that drifted would move the numbers.
    - `mail_sort::sort_messages` is the same function, `pub`, in its own module; `wx_app.rs`'s two existing tests call it through a `use` and are not edited; `apply_sort` calls it by the new path.
    - The two guard records' `file` keys name `src/presentation/mail_sort.rs`, their `before` and `after` are unchanged, and each is re-measured.
  </behavior>
  <action>
    Red first, on a branch: the `sample_mailbox.rs` tests before the module
    exists. `mail_sort.rs` gains no new test in this task; moving a function
    is not a behaviour change and the two existing tests hold it. Say so.

    Move, do not copy. `test_every_guard_record_still_names_one_place_in_
    the_tree` reddens the moment the function leaves `wx_app.rs` with the
    two records still naming it; correct the two `file` keys in the same
    commit, and name that test in the trailer of the red commit if the move
    lands before the records do. Then `scripts/guards.sh --remeasure` both,
    detached, and read the result: each should still redden exactly the one
    test it names.

    Keep the Help menu path intact and prove it by running the release
    binary and loading the sample from Help once; the summary quotes the
    log line at `wx_app.rs:5272-5279`.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::sample_mailbox:: presentation::mail_sort:: && cargo test --test house_style test_every_guard_record_still_names_one_place_in_the_tree</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the `sample_mailbox` tests.
    - The two commands in `<verify>` pass at green.
    - `grep -n 'fn sort_messages\|fn sample_mailbox\|const SAMPLE_MAILBOX_SIZE' src/presentation/wx_app.rs` finds nothing; the same grep over the two new modules finds each once.
    - The two records name `src/presentation/mail_sort.rs`, and `scripts/guards.sh --remeasure` on both reports each reddening exactly its one named test; the summary quotes the run.
    - `wx_app.rs`'s `#[test]` count is 199 before and after.
    - The sample loaded from the Help menu in a release build and the summary quotes the log line.
  </acceptance_criteria>
  <done>The generator and the sort are modules of their own, reachable from tests and from the mutation tool, and the two records that guard the sort followed it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the list over 200,000 rows, sort, filter and scroll each timed, with no window</name>
  <files>tests/the_list_at_two_hundred_thousand_rows.rs, docs/development/measurements.md, docs/changelog.md, guards/guards.toml</files>
  <read_first>
    - `src/data/message_cache/messages.rs:1668`, `upsert_messages`, and `:2056`, `get_messages_for_folder`; `src/data/message_cache/searching.rs:440-520`, `search_messages`, `as_a_search` and `WhereToSearch`.
    - `03-04-SUMMARY.md:284-295`, the table of listing costs, for the shape of a measured table and the conditions it carries.
    - `tests/the_numbers_the_targets_ask_for.rs` as 08-03 left it, for the release-only refusal, the row function and how a measurement test prints.
    - `docs/development/measurements.md`, the row shape.
  </read_first>
  <behavior>
    - `a_cache_of(count, into: &Path)` writes `count` rows into one folder of one account in a `MessageCache` at `into`, in batches through `upsert_messages`, with subjects and senders from the same lists `sample_mailbox` uses so a filter word hits about one row in five, and no bodies; a test at 2,000 rows reads back 2,000.
    - The measurement, behind `#[ignore]` and refusing a debug build: over the 200,000-row cache, time `get_messages_for_folder` once cold and once warm; time `search_messages` for a word in one subject in five, for a word in no subject, and for a sender, each under `WhereToSearch`'s default and under its narrowest, with the row count each returned; over `sample_mailbox(200_000)` in memory, time `sort_messages` for each of the seven orders on a fresh clone each; time `text_for` over one page of 40 rows and every visible column, and over all 200,000 rows for one column, in the messages view. Each timing is taken three times and the median printed as a row.
    - A format-holding test at 2,000 rows runs the same code and asserts every row it prints has the page's column count and names what it timed.
    - Each row's `conditions` says: release build, warm or cold as timed, the row and column counts, the machine, and that these are synthetic rows in a cache and no provider mailbox.
  </behavior>
  <action>
    Red first, on a branch. The target is at zero records. The
    `#[ignore]` measurement is not run by the gate; the format test at
    2,000 rows is, and it is the red.

    Build the release binary and run the measurement with `cargo test
    --release --test the_list_at_two_hundred_thousand_rows -- --ignored
    --nocapture` on a machine doing nothing else. Write the rows onto
    `docs/development/measurements.md`. In the changelog, under `[Unreleased]`, say the
    numbers exist and where; under Known limitations, in as many words:
    that the sort runs off the UI thread and the cost of applying its
    result to the list control was not timed because there is no window in
    the harness; that a scroll in the running program is the page paint
    timed here plus wxWidgets' own work, which was not; that the rows are
    synthetic and no provider mailbox was used; and, if any sort order
    exceeded a second, which and why per premise 7, with the fix named.

    `.planning/WINDOWS.md` gains one entry each for the untimed apply, the
    untimed paint, and the sort fix if it is owed, through `gsd-tools
    windows append` with no backslash in the text.

    **Guard record, measured on the target.** The break halves the batch
    size in `a_cache_of` so the 2,000-row test's row count disagrees, or
    changes the row's column count; whichever reddens the format test.
    Apply, run, quote.
  </action>
  <verify>
    <automated>cargo test --test the_list_at_two_hundred_thousand_rows && cargo test --test every_number_carries_its_command_and_its_date</automated>
  </verify>
  <acceptance_criteria>
    - A red commit names the format-holding test.
    - Both commands in `<verify>` pass at green; the first lists the measurement as ignored.
    - `docs/development/measurements.md` holds at least eleven new rows: the listing cold and warm, five filter timings with their row counts, seven sort orders, the page paint and the full pass, each with date, commit, version, machine, command and the synthetic-rows condition.
    - The summary quotes every printed row and the three takes behind each median.
    - The changelog entry carries the four Known limitations in the action.
    - Guard record: one, measured, counts quoted.
  </acceptance_criteria>
  <done>Sort, filter and scroll each have a number over 200,000 rows, taken by a harness that stays in the tree, and the rows say what was and was not timed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the paint thread to the database | a callback that runs while wxWidgets paints and reaches SQLite blocks every paint on a lock |
| a synthetic cache to a real one | 200,000 rows written by a test must land in a `tempfile` directory and nowhere else |
| a measurement to a claim | a number over synthetic rows read as a number over a mailbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-16 | Denial of service | `text_for` or the closure reaching a connection | critical | mitigate | the function takes slices and copies; the reading forbids the database names in both and requires the closure to call the function; a record plants a call and the reading names it. |
| T-08-17 | Tampering | the moved sort or generator changing behaviour on the way | high | mitigate | the two existing sort tests are unedited and pass; the generator's shape is pinned by new tests; the two records are re-measured. |
| T-08-18 | Tampering | the scale harness writing into a real data folder | critical | mitigate | it takes a path and the tests give it a `tempfile` directory; `test_no_test_builds_its_own_temp_folder` holds the tree. |
| T-08-19 | Repudiation | a sort number quoted as a freeze or as not a freeze | medium | mitigate | the row says the sort runs off the UI thread and the apply was not timed; the changelog says so in as many words. |
| T-08-20 | Information disclosure | rows read as a provider mailbox's numbers | medium | mitigate | every row's conditions says synthetic, and PERF-03's third `[D]` line is quoted in the changelog. |
| T-08-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `Cargo.toml` is not touched. |
</threat_model>

<verification>
- `cargo test --lib -- presentation::virtual_rows:: presentation::sample_mailbox:: presentation::mail_sort::` passes. One `--lib`, several filters after `--`.
- `cargo test --test the_list_reads_only_memory`, `cargo test --test the_list_at_two_hundred_thousand_rows` and `cargo test --test every_number_carries_its_command_and_its_date` pass.
- `scripts/check.sh` passes on every commit through the hook, never piped; `scripts/check.sh all` once on the branch before the merge.
- `wx_app.rs`'s `#[test]` count is 199 before and after every task; no record naming it was re-measured, and the summary says so.
- The two sort records were re-measured after the move and each reddens exactly its one named test.
- Every document this plan writes carries no em dash and no carriage return.
</verification>

<success_criteria>
- A test asserts, by the function's type and by a reading, that the virtual text callback issues no SQLite query.
- The list is exercised against 200,000 synthetic rows and sort, filter and scroll each have a recorded number with its conditions.
- No criterion claims a real provider mailbox was used.
- The generator and the sort are reachable from tests and from the mutation tool, and the Help menu path is unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-every-number-the-project-quotes/08-04-SUMMARY.md` when done
</output>
