---
phase: 08-every-number-the-project-quotes
plan: 09
type: execute
wave: 9
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06", "08-07", "08-08"]
files_modified:
  - docs/roadmap.md
  - docs/development/requirements-backlog.md
  - docs/architecture.md
  - docs/integration-guide.md
  - docs/IMPLEMENTATION_STATUS.md
  - docs/changelog.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07]
estimate:
  tokens: 15000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every target in the tree that this phase measured against is either met, with the row that shows it, or revised, with the reason written beside it and the old target kept as what it used to say."
    - "PERF-07's justification no longer rests on the ratio; it says what the August sweeps found and why that is the reason to run mutation testing, or it says the requirement's motivation did not survive, whichever the runs showed."
    - "Each of the seven requirements is ticked or left open clause by clause against its [D] lines, with the plan that closed each clause named, and the roadmap's six criteria are read the same way."
    - "What this phase could not do on this machine is in the ledger, one entry each, and what remains for a person after the last phase of the milestone is written where a person will find it."
  artifacts:
    - "docs/roadmap.md, docs/development/requirements-backlog.md, docs/architecture.md, docs/integration-guide.md: each target judged, dated, old wording kept."
    - "docs/IMPLEMENTATION_STATUS.md: the mutation paragraph's reason rewritten."
    - ".planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md: closed clause by clause."
    - ".planning/WINDOWS.md: the entries for what no plan could measure."
  key_links:
    - "docs/development/measurements.md -> every judgement: a target is met or revised against a row, by reference, never against a number restated here."
    - "08-08's summary -> criterion 4: if the run is still going, the criterion is closed on what completed and the rest is stated as running, with the shard count and the date."
    - "the roadmap's criterion 5 -> 08-07's log: closed by the count of verdicts in the log, not by the file's record count."
---

<objective>
Read every number the phase produced against the requirement that set it,
decide met or revised for each target with the reason written down, and
close the phase and the milestone's last phase honestly.

Purpose: roadmap criterion 6, and the clause-by-clause reading this
project's own phase records keep finding open clauses with. A target
quietly left aspirational is what the phase exists to end; so is a
requirement ticked because its plan finished.

Output: the judged targets; the requirements and roadmap closed clause by
clause; the ledger entries; the handoff to the person.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-every-number-the-project-quotes/README.md
@.planning/phases/08-every-number-the-project-quotes/08-RESEARCH.md
@.planning/phases/08-every-number-the-project-quotes/08-03-SUMMARY.md
@.planning/phases/08-every-number-the-project-quotes/08-04-SUMMARY.md
@.planning/phases/08-every-number-the-project-quotes/08-05-SUMMARY.md
@.planning/phases/08-every-number-the-project-quotes/08-07-SUMMARY.md
@.planning/phases/08-every-number-the-project-quotes/08-08-SUMMARY.md
@docs/manual-accessibility-pass.md
@CLAUDE.md
</context>

<premise_corrections>
Taken 2026-09-14 against `main` at `b14d6379`, version `0.124.0`. Written
before any plan of the phase has run; every line number below is re-taken
on the day with the grep beside it.

**1. The targets and where each lives.**

```
grep -n "2 seconds\|100MB\|150MB" docs/roadmap.md docs/development/requirements-backlog.md
-> roadmap.md:221 "Startup time optimization (<2 seconds)", :253 "Fast startup time (< 2 seconds)", :254 "Low memory footprint (< 100MB idle)"; requirements-backlog.md:81 "<150MB with 1000 cached messages", :82 "<2 seconds cold start"
grep -n "80%" docs/architecture.md            -> :400 "High code coverage (target: 80%+)"
grep -n "95%" docs/integration-guide.md      -> :6, already described as never met and kept as history
grep -n "100K" docs/roadmap.md docs/development/requirements-backlog.md   -> roadmap.md:219 unticked; backlog.md:83 "Validate with real-world 100K+ mailboxes"
```

The last is not this phase's to tick: PERF-03's third `[D]` line says
synthetic rows answer the list question and the provider question waits
for a live account. It is revised to say so, dated.

**2. Two of the four sweep figures were the roadmap's and `CLAUDE.md`'s
and are gone by now.** Criterion 5's "roughly 15 hours" was replaced by
the planner on 2026-09-14 with the dated product; 08-06 retired the rest.
What this plan writes against criterion 5 is the log's verdict count from
08-07, and the measured wall time beside the predicted one.

**3. PERF-07's stated reason is the ratio 08-02 retired.** `.planning/
REQUIREMENTS.md`, PERF-07's evidence and `[S]` lines, and `docs/
IMPLEMENTATION_STATUS.md:214`, rest on "most tests describe rather than
specify". The research's reading is that the motivation survives for a
different reason: the August sweeps found dead code and whole families of
untested behaviour regardless of when the tests were written
(`docs/plans/20260801-mutation-sweep.md`, and `CLAUDE.md`'s "157 mutants,
141 caught" paragraph). 08-08's run either confirms that or does not, and
this plan writes whichever it showed.

**4. The ledger already holds entries for some of what cannot be measured
here.** The ledger's JSON entries carry a `"description"` key and no
`"title"` key (`grep -c '"title"' .planning/WINDOWS.md` answers 0 and
`grep -c '"description"'` answers 441 on 2026-09-14); this plan's first
draft grepped the key that is not there, which would have answered 0 for
ever and had the executor adding duplicates. Taken with the key that is:

```
grep '"description"' .planning/WINDOWS.md | grep -ic 'account'         -> 69
grep '"description"' .planning/WINDOWS.md | grep -ic 'sign'            -> 26
grep '"description"' .planning/WINDOWS.md | grep -ic 'linux\|macos'    -> 3
```

So entries for no real account, no signed artefact and no off-Windows
build exist from earlier phases, many times over. This plan adds only what
is missing, after reading the matching descriptions rather than counting
them, and says which it found already there and by which numbers.

**5. The manual pass waits for after this phase.**
`docs/manual-accessibility-pass.md` (06-08) lists seventy-six items and
its first sentence says none has happened. Phase 8 is the last phase of
the milestone, so what is left for a person after it is that page, the
ledger's open entries, and whatever 08-08's run has not finished.
</premise_corrections>

<tasks>

<task type="auto">
  <name>Task 1: each target met or revised, with the reason written beside it</name>
  <files>docs/roadmap.md, docs/development/requirements-backlog.md, docs/architecture.md, docs/integration-guide.md, docs/IMPLEMENTATION_STATUS.md, docs/changelog.md</files>
  <read_first>
    - `docs/development/measurements.md` in full.
    - Every site in premise 1, and the sentences around each, so the judgement is written in the page's voice.
    - `docs/wcag-coverage.md:1-30` and `docs/roadmap.md:130-140` as 08-02 left it, for the shape of a corrected line keeping its old wording with the date.
    - `08-03-SUMMARY.md`, `08-05-SUMMARY.md` and `08-08-SUMMARY.md`, for the numbers and what each plan said its numbers did not show.
  </read_first>
  <action>
    **Documents only; `writing-craft` applies; the summary says this task
    wrote no code.**

    For each target: read the row, decide met or not, and write the
    judgement on the line that carries the target. Met: tick it, name the
    row and its date. Not met: do not tick; revise the target or keep it,
    and in either case write the reason beside it, dated, with the old
    target kept as what it used to say. A target that the page's row meets
    on one machine on one day is met on that machine on that day, and the
    line says so rather than claiming more.

    Cold start against 2 seconds: the median of five, and the after-build
    figure beside it if it differs in which side of 2 s it falls. Memory
    with 1,000 messages against 150 MB, and idle against 100 MB: the
    application process alone and with its WebView2 tree, and if the two
    fall on different sides of the target, say which the target is about
    and why, because the target was written before the preview was a
    browser. Coverage against 80%: revise or keep with the attribution
    from 08-05 as the reason; the 95% page already calls itself history
    and stays. The 100K+ mailbox lines: revised to say the list question
    is answered by 200,000 synthetic rows, by reference to the rows, and
    the provider question waits for a live account.

    Rewrite PERF-07's reason on the status page as premise 3 says,
    according to what 08-08's run showed; if the run is still going, write
    the August evidence as the reason and say the whole-tree result is
    pending with the date it started.

    Changelog under `[Unreleased]`: every target judged, in one list, met
    or revised, each by reference to the page.
  </action>
  <verify>
    <automated>cargo test --test every_number_carries_its_command_and_its_date && cargo test --test house_style test_no_dashes_that_should_be_punctuation && cargo test --test docs_links</automated>
  </verify>
  <acceptance_criteria>
    - Every site in premise 1 carries a judgement with a date and a reference to a row, and the summary lists each with met or revised and the reason.
    - No target's line claims more than the row shows; a target met on one machine says so.
    - The status page's mutation paragraph gives a reason that does not rest on the ratio.
    - The three commands in `<verify>` pass.
    - The changelog entry lists every target.
  </acceptance_criteria>
  <done>No figure in the documents is both aspirational and undated: each target is met on a named day or revised with the reason beside it.</done>
</task>

<task type="auto">
  <name>Task 2: the requirements and the roadmap closed clause by clause, the ledger told what could not be measured, and the person told what is left</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:1930-2100`, every `[D]` line of the seven requirements, and the traceability rows at the foot.
    - `.planning/ROADMAP.md`, the phase 8 entry, the six criteria and the progress row, and the phase 7 entry above it for the shape of a plan list with per-plan verdicts.
    - `.planning/STATE.md`, the frontmatter and Current Position, and `tests/the_planning_files_agree_with_themselves.rs:1-60` for what is held to the disk.
    - `.planning/WINDOWS.md`, the entries from phases 5 to 7 about accounts, releases and platforms, so nothing is added twice.
    - Every summary of this phase, for what each said it closed and what it said it could not.
  </read_first>
  <action>
    **Documents only. `gsd-tools roadmap update-plan-progress` counts a
    README as a plan; edit the roadmap by hand and read the diff.
    `gsd-tools windows append` corrupts an entry holding a backslash; write
    none.**

    For each of PERF-01 to PERF-07, read every `[D]` line as a clause and
    answer it from the summaries: closed by which plan, with what
    evidence, or open with why. Tick the requirement only when every
    clause is closed; otherwise leave the box and write under the evidence
    which clauses are open. Update the traceability rows to match. PERF-07
    is ticked only if 08-08's run completed and was read; if it is
    running, the row says `In progress` with the date.

    In `.planning/ROADMAP.md`: tick each plan's line with the merge commit
    and a sentence saying which criterion it closed and what it could not,
    in the shape phase 7's entry uses; read each of the six criteria
    clause by clause against the summaries and write the verdict under the
    plan list; correct the progress row by counting `*-SUMMARY.md` files on
    disk. Criterion 5 closes by the log's verdict count. Criterion 4 closes
    only on a completed and read run; otherwise the entry says what
    completed and what is running, and if Pratik chose option 3 or 4 at
    08-08's checkpoint the criterion is revised under criterion 6 with that
    table as the reason. Criterion 2's "scroll" clause is not ticked without
    the sentence 08-04's changelog entry carries: the number is the page
    paint over the visible rows and columns, and wxWidgets' own paint and
    the apply of a sort's result to the list control were not timed,
    because the harness has no window. A tick without that sentence claims
    a measurement nobody took.

    In `.planning/STATE.md`: the frontmatter and Current Position to this
    phase's last plan, `progress.completed_plans` by counting summaries on
    disk, and the Current Position saying that this is the last phase of
    the milestone and what is left for a person: the manual pass page, the
    ledger's open count read from its frontmatter on the day, and 08-08's
    run if it is still going.

    In `.planning/WINDOWS.md`, one entry each, after checking the ledger
    for an existing one: anything against a real account; the provider
    question for a 100,000-message mailbox; a Linux or macOS build; a
    signed artefact and the update download's size; screen reader
    confirmation of anything this phase changed (the usable line is a log
    line and nothing speaks it); and, if it applies, the mutation run's
    unfinished shards. Say in the summary which entries were already
    there and their numbers.
  </action>
  <verify>
    <automated>cargo test --test the_planning_files_agree_with_themselves && cargo test --test house_style test_no_dashes_that_should_be_punctuation</automated>
  </verify>
  <acceptance_criteria>
    - Each of the seven requirements has every `[D]` clause answered under its evidence with the closing plan named, and is ticked only when all are closed; the traceability rows agree.
    - The roadmap's phase 8 entry has nine ticked or explained plan lines with merge commits, six criterion verdicts read clause by clause, and a progress row that `test_the_roadmap_counts_the_files_that_are_on_disk` accepts.
    - Criterion 2's verdict names the page paint as the scroll number and says wxWidgets' own paint and the apply of a sort were not timed; criterion 4's verdict says completed, running, or revised with 08-08's table as the reason.
    - `.planning/STATE.md` names this phase and plan in its frontmatter and heading, counts summaries on disk, and says what is left for a person.
    - `.planning/WINDOWS.md` gains one entry per item in the action not already present, and the summary lists the existing numbers for the rest.
    - Both commands in `<verify>` pass.
  </acceptance_criteria>
  <done>The phase and the milestone's last phase are closed on what was measured and what was not, clause by clause, and the person knows what waits for them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a number to a judgement | a target met on one machine on one day read as met |
| a plan finishing to a requirement closing | a box ticked because its plan finished, not because its clauses did |
| this phase to the milestone | the last phase closing with an unfinished run and an unrun manual pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-38 | Repudiation | a target ticked against a number that does not meet it, or met only without the WebView2 tree | high | mitigate | each judgement names the row; where the process and the tree fall on different sides the line says which the target is about and why. |
| T-08-39 | Tampering | a requirement ticked with an open clause | high | mitigate | every `[D]` line is answered under the evidence; the traceability row follows the answers, not the plan count. |
| T-08-40 | Repudiation | criterion 4 closed on a run still going | medium | mitigate | the roadmap entry says what completed and what is running with the date; the requirement row says `In progress`. |
| T-08-41 | Denial of service | the progress row or STATE counts disagreeing with the disk and failing every later commit | medium | mitigate | counted from the disk, and `the_planning_files_agree_with_themselves` is in `<verify>`. |
| T-08-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added; `Cargo.toml` is not touched. |
</threat_model>

<verification>
- `cargo test --test every_number_carries_its_command_and_its_date`, `cargo test --test the_planning_files_agree_with_themselves`, `cargo test --test docs_links` and the named `house_style` test pass.
- `scripts/check.sh` passes on every commit through the hook, never piped; this plan's commits are documents only and answer `docs_only`; `scripts/check.sh all` once before the merge.
- No test was added or removed; no guard record changed; the summary says so.
- Every document this plan writes carries no em dash and no carriage return, and no ledger entry holds a backslash.
</verification>

<success_criteria>
- Each target is met with the row named or revised with the reason written down.
- The seven requirements and the six criteria are closed clause by clause, and what is open says why.
- What could not be measured on this machine is in the ledger, once each.
- The person whose milestone this is knows what waits for them.
</success_criteria>

<output>
Create `.planning/phases/08-every-number-the-project-quotes/08-09-SUMMARY.md` when done
</output>
