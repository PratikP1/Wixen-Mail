---
phase: 05.1-notes-and-contacts-reach-a-server
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - src/application/notes_backend.rs
  - src/application/mod.rs
  - src/application/context_menu.rs
  - src/presentation/wx_settings.rs
  - docs/development/the-notes-seam.md
  - guards/guards.toml
  - docs/changelog.md
  - Cargo.toml
autonomous: true
requirements: [PIM-07, PIM-08, PIM-04]
estimate:
  tokens: 108000
  raw_tokens: 105000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "One place decides where a note goes, given an account, and every screen and menu that asks the question asks that place rather than answering it themselves."
    - "An account with no notes backend keeps its notes on this computer and the settings screen says so, in a section somebody moving by keyboard would look in, rather than offering a switch that does nothing."
    - "The note folder menu offers Sync now exactly where the seam says a backend exists, in both directions, so it cannot offer a sync that cannot happen and cannot hide one that can."
    - "What the seam assumes about a backend is written down: how a note is identified, what happens when both copies moved, and what happens to a note whose backend is taken off an account."
    - "Nothing ships a hosted client, a network call to one, or a setting offering one. The seam takes a second implementation and nothing half exists."
  artifacts:
    - "src/application/notes_backend.rs: the answer to which backend an account's notes use, with a variant for none, and the trait a backend implements, with no implementation behind it."
    - "docs/development/the-notes-seam.md: identity, conflict, and removal, written as what a backend must answer rather than as what one backend does."
    - "src/presentation/wx_settings.rs: a Notes section on the Calendar and PIM tab saying where an account's notes go and saying plainly where they do not go anywhere."
    - "src/application/context_menu.rs: the note folder menu asking the seam, and its test turned from asserting an absence into asserting agreement in both directions."
  key_links:
    - "new_item::supports(account, kind) is the existing per-account answer to 'does this account sync this kind of thing', it returns false for Note with a comment giving a reason per provider, and it has exactly one production caller: destination() in the same file. grep -rn 'supports' src/ tests/ --include=*.rs confirms it. It is a bool, and the seam needs a which, so it is the shape to follow rather than the function to widen."
    - "There is no `syncs` predicate anywhere. 05-RESEARCH.md names one twice. grep -rn 'syncs' src/application/new_item.rs finds only prose in comments. An executor told to widen `syncs` would look for a function that does not exist."
    - "context_menu.rs's NOTE_FOLDERS static has no Action::SyncNow and a comment above it saying why, and test_notes_are_not_offered_a_sync_they_cannot_do asserts the absence. Both are correct today and both are about a constant; the seam turns them into a question."
    - "test_move_is_offered_exactly_where_it_means_something is the both-directions shape to copy for the sync test: loop over the set, ask the menu, ask the rule, assert they agree."
    - "AddressBook (src/data/message_cache/mod.rs) is three variants where the third is Other(String), with a doc comment saying it exists so a word this code does not recognise survives being read and written back. That is the shipped, working example PIM-08 asks the seam to be like."
---

<objective>
Build the one place that decides where a note goes, write down what a backend
has to answer, and make the settings screen say plainly that notes stay on this
computer.

Purpose: PIM-07's first `[D]` line, all of PIM-08, and PIM-04's last two. The
decision was made on 2026-08-29 and is recorded in `.planning/REQUIREMENTS.md`:
a note has a backend chosen by the account it belongs to, the local note is a
first-class Markdown document, and the seam is shaped so a hosted service can be
added later without a migration. This plan builds the seam and ships no backend
behind it, which is what PIM-08's last `[D]` line asks for in as many words:
preparing for it means the seam does not forbid it, not that anything half
exists.

Output: one answer to "where do this account's notes go", a written contract for
what a backend has to do, a settings screen that says the honest answer, and a
menu that offers a sync exactly where one is possible.

Which backends exist is settled. Decision 2 of 2026-09-06: all three. CalDAV
VJOURNAL in `05.1-03`, a second implementation that exists only in tests in
`05.1-04`, and OneNote in phase 5.2. This plan builds the seam they all go
behind and ships none of them, which is what PIM-08's last `[D]` line asks for
in as many words.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-the-other-five-modules-keep-up/05-RESEARCH.md
@.planning/phases/05.1-notes-and-contacts-reach-a-server/05.1-01-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Everything below was checked against `main` at `9611b70`: a clean checkout,
version `0.75.0`, `guards/guards.toml` holding 632 records, `rust-version`
`1.88`. The findings were first taken on 2026-09-06 against an earlier tree and
every figure was re-measured against this one when the set was assembled;
phases 4 and 4.2 landed in between, which is what moved the record counts. Each
claim below names the command that produced it so it can be re-run rather than
trusted, and a figure is only a figure with the tree it came from: if the tree
has moved again, re-run them.

**The tree has moved again, and these premises were re-measured on 2026-09-10
against `main` at `1aba3a5`.** Phase 5 landed in between. Version is `0.102.0`,
not `0.75.0`; `guards/guards.toml` holds 683 records, not 632. Two of the
premises below turned out to be false rather than stale, and one of those two
is the stated reason task 1's only promised RED would have been red. Every
correction says what the sentence used to say, because the reason a premise was
plausible is usually the reason the next one will be.

**The Rust floor is 1.88 and it has already re-linted code no phase-5 plan
opens.** `04-09` moved it from 1.87 because rPGP declares 1.88, and clippy only
suggests a construct once the declared floor allows it, so the bump turned on
`collapsible_if`'s let-chain form and cost seven fixes across six files that
plan never opened. `main` is clean at that floor now, so nothing is owed. What
it means here is that new code in this plan is linted against 1.88 with
`-D warnings`, where a let-chain suggestion is a build failure rather than a
hint.

**1. `05-RESEARCH.md` names a function that does not exist, twice.** It says
"`new_item.rs`'s `syncs` predicate is the natural seam and it currently returns
`false` for `Note`" and repeats the claim in its PIM-07 section.
`grep -rn "syncs" src/application/new_item.rs` returns four hits, all prose
inside comments, and no function. `grep -n "^pub fn \|^    pub fn " src/application/new_item.rs`
lists `kept_in`, `label`, `plural`, `shortcut`, `holding`, `holds`,
`account_id`, `spoken`, `supports`, `destination`, `renaming_works`,
`container_destination`, `sends_from`, `default_after_change`,
`deletion_question`, `deletion_reaches_provider`, `the_provider_has_a_copy` and
`deletion_warning`. **The function meant is `supports`**, whose doc comment at
line 238 reads "Whether this account's provider syncs this kind of item". An
executor told to widen `syncs` would spend real time looking for it.

**2. `supports` has exactly one production caller and it is not a sync.**
`grep -rn "supports" src/ tests/ --include=*.rs` returns eighteen hits, of which
one is the definition, one is a comment in `mail_controller.rs` about something
else, four are inside its own tests, several are unrelated words in
`folder_tree.rs`, `message_columns.rs`, `wx_reader.rs` and `imap.rs`, and
**exactly one is a call: `destination()` in the same file**, which decides
whether a newly made item goes to the account or to this computer. Nothing in
any `*_sync.rs` file calls it.

So `supports` is the right *shape* and the wrong *function to widen*. It answers
a bool and the seam needs a which. And widening it would change where a new note
is filed, which is a behaviour change nobody asked for and would land the moment
the first backend shipped. **Write the seam beside it, in its own module, and
have `supports` answer from the seam if that reads better than two answers to
one question.** Say in the summary which was chosen and why.

**3. The green test that has to change is about a constant, and turning it into
a question is the point.** `test_notes_are_not_offered_a_sync_they_cannot_do` in
`src/application/context_menu.rs` asserts `entries_for(Focus::Containers(ContainerKind::NoteFolder))`
does not contain `Action::SyncNow`. Above the `NOTE_FOLDERS` static is a comment
saying "No sync: notes are kept on this computer and go nowhere, so offering to
sync them would be offering something that cannot happen." Both are correct
today. **After this plan they are still correct, because no backend ships**, so
the test does not simply invert. What has to change is that the menu asks the
seam instead of holding a constant, and the test asserts agreement in both
directions rather than an absence, the way
`test_move_is_offered_exactly_where_it_means_something` beside it already does.

**Corrected 2026-09-10. This paragraph used to end: "That is a real red: an
absence assertion is satisfied by a menu that can never offer it, and an
agreement assertion is not." The first half is right and the second half is
wrong, and the difference is where the expected answer comes from.**

`test_move_is_offered_exactly_where_it_means_something` is a real check because
it compares the menu against `PimCommand::Move.applies_to(kind)`, which lives in
another module. A notes agreement assertion written the same way would compare
the menu against the seam, and both sides would be the seam. The break this plan
prescribes for it, making the seam always answer that a backend exists, then
moves both sides together and nothing goes red. Premise 15 has the measurement.

What makes an agreement assertion real here is a table of literals: one row per
variant of the seam's answer, with "does the menu offer Sync now" written down
rather than computed. Task 1 carries the corrected instruction.

**Keep `test_notes_are_not_offered_a_sync_they_cannot_do` rather than replacing
it.** It asserts against a literal, which is exactly what makes it immune to the
break above, and it stays true after this plan for the reason two paragraphs up.
Replacing it was also unworkable at the commit gate: see task 1's correction on
naming a replaced test in a `Fails-until-green:` trailer.

**4. The menus are static arrays, and a menu that has to ask a question is a
change to how `entries_for` works.** `CALENDARS`, `TASK_LISTS`, `NOTE_FOLDERS`
and `CONTACT_GROUPS` are `static &[Entry]` in `context_menu.rs`, and
`entries_for(Focus::Containers(kind))` returns one of them. A note folder menu
that depends on the account is no longer a constant. **Read `entries_for` and
`Focus` in full before deciding how the account reaches it**, and if the shape
of `Focus` has to change, say so and say what else it costs, because several
tests loop over it. Do not thread an account through every menu because one of
them needs it.

**5. The settings section has a place and a precedent.** `fn build_calendar_pim_tab`
in `src/presentation/wx_settings.rs` builds `section(panel, "Calendar")`, which
is empty today, then Reminders, then Working Day. The Permissions tab's Contacts
section is the worked example of the shape this wants: a `section`, a control,
and a `StaticText` under it carrying a named constant, with
`set_accessible_name` set to the same string so both Windows channels say it.
Read `build_permissions_tab`'s Contacts block before writing the Notes one.

For a static text the label is what Narrator reads and `set_accessible_name` is
what NVDA reads, so both are set to the same sentence. `CLAUDE.md` explains the
split.

**6. This plan adds no setting, and that is deliberate.** `CLAUDE.md`'s rule is
that every setting a user has is reachable from the settings screen. The
converse is also true here: **a sentence is not a setting.** Where an account
has no notes backend there is nothing to choose, so a switch would be the
"control that does nothing" this project has removed repeatedly, and PIM-04's
own `[D]` line asks for a sentence rather than a switch. If a later backend gives
somebody a real choice, that is a setting and it is introduced in the plan that
introduces the choice.

**7. There is a document guard that a per-account feature can trip.**
`A_CONTROL_NO_SCREEN_WRITES` in `tests/house_style.rs` is a list of phrases a
document may not use, currently `"set it per account"` and
`"for each account separately"`, and the check reads each line together with the
one after it so a phrase split across a line break is still caught. A notes
backend chosen by account is exactly the subject that invites one of those
phrases. **Do not write either into any document in this plan**, and if the
honest sentence needs one, that is a finding and the guard is what to talk about,
not what to work around.

**8. `AddressBook::Other(String)` is the shipped example PIM-08 names, and it is
worth copying rather than admiring.** `sed -n '/pub enum AddressBook/,/^}/p' src/data/message_cache/mod.rs`
gives three variants, and the doc comment above says `Other` exists so a word
this code does not recognise survives being read and written back, because an
address book named by a build that came later is still an address book and
forgetting its name would silently join two of its contacts together. The notes
seam wants the same property for the same reason.

**9. `application::deletions` is the other thing the contract has to answer to,
and it already exists.** Read its module header in full. It states one rule for
everything this computer deletes: a note of the deletion outlives the row,
because a deleted row cannot carry a "not yet sent" flag, and every read asks
the notes before writing anything down. Contacts, calendar events and tasks each
have a `deleted_*` table; `grep -n "CREATE TABLE IF NOT EXISTS" src/data/message_cache/mod.rs`
shows `deleted_contacts`, `deleted_calendar_events` and `deleted_tasks`, and
**no `deleted_notes`**. That is `05.1-03`'s work, and the contract this plan
writes has to say that a backend needs it, or the first backend will rediscover
the bug `deletions.rs` was written about.

**10. Guard-record cost, measured by parsing `tests_last_seen`, not by grepping
for a file name.** Re-measured 2026-09-10 on `main` at `1aba3a5`, which holds
683 records. Brackets are what this table used to say at `9611b70`:

| file | records fingerprinting it | test functions today |
|---|---|---|
| `src/application/context_menu.rs` | 1 (was 0) | 17 (was 15) |
| `src/application/new_item.rs` | 1 | 42 |
| `src/presentation/wx_settings.rs` | 2 (was 1) | 0 |
| `src/data/config.rs` | 3 (was 2) | 59 (was 53) |
| a new module under `src/application/` | 0 | 0 |

The counting command is in `05-01-PLAN.md` premise correction 8, and test counts
are counted the way `how_many_tests_are_in` in `tests/house_style.rs` counts.

**Corrected 2026-09-10: `context_menu.rs` is no longer free, and task 1's
action repeats the claim that it is.** One record names it,
"the copy line is on exactly the menus whose command accepts one", with
`tests_last_seen = [{ file = "src/application/context_menu.rs", tests = 17 }]`.
Adding any `#[test]` to that file moves the count and reddens
`test_every_guard_record_says_how_many_tests_the_files_it_names_held`.
`house_style` is in `guards_that_read_the_whole_tree` in `scripts/check.sh`, and
those targets run in every mode including `red`, so it is an unnamed failure and
`red-commit.sh` refuses the commit for it.

Task 1 adds tests to `context_menu.rs`, so this is not hypothetical. Two ways
through, decided before starting: name the count check among task 1's RED
`Fails-until-green:` failures, which is the pattern `CLAUDE.md` describes for
this exact collision, or leave the new menu tests in the seam's own module and
reach `entries_for` from there. The first is honest and costs one
`scripts/guards.sh --remeasure` for that one record, about 95 seconds. The
second costs nothing and reads worse, because a test about a menu belongs beside
the menu. Pick the first unless there is a reason not to, and say which.

**Put the seam in a new module, which costs nothing.** That is not only about
cost: `03-09` adopted the same rule and `05-RESEARCH.md` repeats it, which is to
put new types and new decisions in new modules rather than growing the files
everything already fingerprints.

**11. Adding a `guards/guards.toml` record bumps a census in the same commit.**
Re-measured 2026-09-10: 192 + 491 = 683, on lines 79 and 80. This used to say
192 + 440 = 632. Read both lines rather than trusting either pair, because
`test_the_sweep_written_at_the_top_of_the_guard_records_covers_every_record_in_it`
compares them against the file on every commit.

**12. Corrected 2026-09-10. This used to say a commit touching `Cargo.toml`
makes `scripts/which-checks.sh` answer `all`, around 311 to 353 seconds warm,
and that those commits must be run detached. That stopped being true on
2026-09-09.** The script now carries `only_the_packages_own_version_moved`, and
escalates only when that function is false:
`if [ "${#manifests[@]}" -gt 0 ] && ! only_the_packages_own_version_moved ...;
then echo all`. It reads the staged manifest diff and accepts it when every
changed line is this package's own `version = "..."` line.

Every commit in this plan bumps the version beside source and `guards/guards.toml`,
so every one of them answers `affected`: formatting, clippy, the tests reaching
the changed modules, and the whole-tree guards. Not the full suite and not the
release build.

**The dangerous direction is the other one.** The old sentence would have had
the executor report that the full gate ran on each of those commits when it ran
neither the whole suite nor the release build. An executor who believes that can
skip the `scripts/check.sh all` the branch owes before the merge, and both then
run for the first time on `main`. Run it once, before the merge.

```bash
grep -n -B2 -A4 'only_the_packages_own_version_moved' scripts/which-checks.sh
```

**13. Corrected 2026-09-10, and this one was false rather than stale. It used
to say: "A new module needs a `pub mod` line, and under `-D warnings` a type
nothing uses will not build. So the seam and its first consumer land together,
and its first consumer in this plan is the settings sentence and the menu."**
The `pub mod` half is true. The rest is not.

This is a library crate with public modules. `src/lib.rs` carries
`#![recursion_limit = "512"]` and nothing else: no `deny(dead_code)`, no
`unreachable_pub`. Every module line in it is `pub mod`, and
`src/application/mod.rs` declares its files the same way. `Cargo.toml`'s
`[lints.clippy]` sets two lints, `significant_drop_in_scrutinee` and
`await_holding_lock`, and neither is about unused items. rustc's `dead_code`
lint does not fire on a `pub` item reachable from a library crate root. **So a
`pub enum` or a `pub trait` in `src/application/notes_backend.rs` with no caller
anywhere compiles clean under `-D warnings`, and the same is true of the trait
task 3 writes.**

```bash
grep -nE '^#!\[|^pub mod' src/lib.rs
grep -n -A4 '\[lints.clippy\]' Cargo.toml
```

**This premise is load-bearing and its falseness is what breaks task 1.** It is
the stated reason the RED commit carries the wiring, and carrying the wiring is
what would have made task 1's one promised RED green. The next correction is
about that. In task 3 the same false premise sends the executor hunting for a
consumer for a trait that needs none, in the same paragraph that forbids writing
the obvious one.

**14. Task 1's promised RED, as written, could not have been red, and the fix
changes what task 1 builds.** The correction is at the top of task 1 rather than
here, because it is a change to the work rather than to what the executor knows.
The short version: all four fixtures the plan enumerates answer "stays on this
computer", `NOTE_FOLDERS` offers no `Action::SyncNow`, and so
`assert_eq!(offered, a_backend_exists)` is `false == false` on every input the
plan names. Nothing in this plan makes the seam answer that a backend exists,
by design, because it ships no backend.

**15. The guard record task 1 prescribes has no break, for the same reason, and
that is a separate defect from the one above.** Once `entries_for` reads the
seam, both sides of an agreement assertion are computed from the seam, so
"make the seam always answer that a backend exists" moves them together and the
assertion still holds. Nothing else in `context_menu.rs` reddens under it
either: `test_no_two_entries_in_one_menu_share_a_mnemonic` has no clash for a
`"&Sync notes now"` line against `&New folder` and `&Delete this folder`, whose
mnemonics are `n` and `d`; `test_no_menu_offers_the_same_thing_twice` dedupes
actions and `SyncNow` is not among the two already there; and
`test_every_container_menu_can_make_and_remove_one` asserts only that
`NewContainer` and `DeleteContainer` are present, so an added entry cannot
redden it at all.

The four tests in that file that already compare a menu against a computed
predicate are the safe version of the pattern and are worth reading before
writing this one:
`test_move_is_offered_exactly_where_it_means_something`,
`test_a_copy_is_offered_exactly_where_it_means_something`,
`test_rename_is_offered_exactly_where_it_works` and
`test_done_and_pin_are_offered_only_where_they_mean_something`. Each compares
against a predicate in a **different module**, `pim_command` or `new_item`, so a
break inside `context_menu.rs` alone reddens them. A seam function living in
`context_menu.rs` and asserted against by a test in the same file is the
degenerate case, and premise 3 pointed at the first shape while task 1
prescribed the second.

Task 1's correction resolves both: the expected column is written as literals
per variant, so the break moves one side only.
</premise_corrections>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One place answers where an account's notes go, and the note folder menu asks it</name>

  **Corrected 2026-09-10, and this changes what task 1 builds rather than only
  what it knows. Read this before the action block.**

  **The test this task promised could not have been red.** The behaviour list
  below enumerates four fixtures, a Gmail account, an Outlook account, an
  unrecognised provider and no provider at all, and every one of them answers
  "stays on this computer" because this plan ships no backend, on purpose.
  `NOTE_FOLDERS` in `src/application/context_menu.rs` offers no
  `Action::SyncNow`. So `assert_eq!(offered, a_backend_exists)` is
  `false == false` on all four, and the acceptance criterion "the RED commit is
  on a branch and `scripts/red-commit.sh` accepted it" could not be met:
  `red-commit.sh` refuses a commit that names a test which passed. The executor
  would have found that out after writing the module, the wiring and the test.

  **What to build instead: a table of literals over the seam's answer, not an
  agreement assertion over an account.** One row per variant the seam can
  return, with the expected menu answer written down rather than computed:

  | the seam's answer | does the menu offer Sync now |
  |---|---|
  | notes stay on this computer | no |
  | each named backend variant | yes |
  | a backend word this build does not recognise | decide it, argue it, write it down |

  That makes three things true at once that the old shape could not. The
  backend rows are red today, because the menu holds a constant and offers
  nothing, so task 1 has a genuine RED half. The break premise 15 prescribes,
  making the seam always answer that a backend exists, reddens the first row,
  because the expected column is literals and only one side moves. And the
  unrecognised-word row forces a decision that the seam's own design raises and
  the plan never asked: a build that meets a backend name it does not know
  cannot sync to it, so the honest answer is probably "no", and it is a
  decision either way.

  **This means the menu's decision takes the seam's answer, not the account.**
  Whatever function `entries_for` grows to reach, the piece the test drives has
  to accept a `NotesBackend` value directly. Deriving the expected column from
  the seam instead of writing it down puts back the tautology this correction
  removes.

  **Do not replace `test_notes_are_not_offered_a_sync_they_cannot_do`.** Two
  reasons. It asserts against a literal, which is what makes it survive the
  break above, so it is the good version of the pattern rather than the bad one.
  And a replaced test is a deleted test: `red-commit.sh` refuses a named test
  that never ran, so it cannot go in a `Fails-until-green:` trailer while it is
  being deleted, and it passes if it is left, so it cannot go in one either way.
  The old action block asked for exactly that and there was no reading of it
  that the gate accepts. Keep the test, and let the new table sit beside it.

  <files>src/application/notes_backend.rs, src/application/mod.rs, src/application/context_menu.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `src/application/new_item.rs` in full: the module header's three paragraphs about why notes and reminders stay local, `ItemKind`, `kept_in`, `supports` with its inline comment giving a reason per provider, `provider_key`, and `destination` which is `supports`'s only production caller.
    - `src/application/context_menu.rs` in full. It is 669 lines and holds `Focus`, `Action`, every static menu, `entries_for`, and the tests that hold the menus to the commands.
    - `src/data/message_cache/mod.rs`: `AddressBook` and its doc comment, and `ProviderIdentity` with the comment on `provider_version`.
    - `src/application/tasks_sync.rs`: `TaskService`, its doc comment about being named for what it is rather than for either provider's HTTP, and the `Scripted` fake in its tests. That is this project's worked example of a service trait with a fake.
    - `src/application/mail_auth.rs`, `provider_of`, which is how an account's provider is named.
    - `src/application/deletions.rs`'s module header in full.
  </read_first>
  <behavior>
    - Asked about a Gmail account, the seam answers that its notes stay on this computer, and says why in words that can be shown to somebody.
    - Asked about an Outlook account, the same.
    - Asked about an account whose provider this build does not recognise, the seam answers that its notes stay on this computer rather than refusing or guessing.
    - Asked about an account with no provider at all, which is every plain IMAP or POP account, the same.
    - The note folder menu offers Sync now exactly where the seam says a backend exists, checked in both directions, which today means nowhere.
    - The calendar, task list and contact group menus are unchanged, and their sync entries are still offered.
  </behavior>
  <action>
    RED first, on a branch.

    Tests go in the new module, which is free, and in
    `src/application/context_menu.rs`. **Corrected 2026-09-10: this used to say
    `context_menu.rs` is fingerprinted by no guard record. One record names it
    now, so a test added there reddens the count check inside the commit gate.
    Premise 10 says which record and gives the two ways through.**

    Write the seam as its own module under `src/application/`, named for what it
    answers rather than for notes syncing, and add its `pub mod` line to
    `src/application/mod.rs`. Do not put it in `new_item.rs`: that module is
    about where a new thing goes when somebody presses a key, and this is about
    where a thing already made is sent. Two subjects in one file is how the next
    person comes to believe there is one question.

    **The answer is a which, not a bool, and that is the whole design.** Model it
    the way `AddressBook` is modelled: named variants for what this build knows
    about, and one for a word it does not recognise, so a database or a
    configuration written by a later build survives being read. Its doc comment
    says that, and says what it is copying and why, the way `AddressBook`'s does.
    A variant for "nothing" is a real answer and not an absence, because the
    settings sentence and the menu both need to say it.

    Decide what to do about `new_item::supports` and say why in the summary.
    Two answers are defensible: leave it alone, because it answers a different
    question about where a new item is filed, and have the seam beside it; or
    have `supports` answer from the seam for `Note`, so there is one place. The
    second is tidier and it changes where a new note is filed the moment a
    backend ships, which is a behaviour change with its own test. **Whichever is
    chosen, do not leave two functions that could disagree about one account
    without a test holding them together.**

    Walk every fixture by hand. Three traps.

    A test asserting the seam answers "stays here" for a Gmail account is green
    the moment the enum exists with a single variant, and says nothing.

    **Corrected 2026-09-10.** The paragraph that used to follow said the
    assertion that goes red is the menu agreeing with the seam in both
    directions, and the one after it said to turn the absence test into that
    agreement assertion. Neither survives measurement: the agreement assertion
    is `false == false` on every fixture this plan names, and turning the
    absence test into it destroys the one check in that file which is immune to
    the guard break. The correction at the top of this task has the shape to
    build instead, and premises 14 and 15 have the measurements.

    The trap that remains, and it is the real one: **an assertion whose expected
    side is computed from the thing under test cannot fail.** Write the expected
    column as literals, one row per variant.

    **Corrected 2026-09-10: the sentence that used to sit here said "Under
    `-D warnings` a type nothing uses will not build, so the red commit carries
    the module and its first consumer". That is false, and premise 13 says why.**
    A `pub enum` in `src/application/notes_backend.rs` with no caller compiles
    clean. So the RED commit does not have to carry the wiring, and carrying it
    is what made the promised red green. Commit the module and its tests red,
    and wire `entries_for` in the GREEN half where it belongs.

    Commit RED with `Fails-until-green:` trailers at column 0 naming every new
    test function. Name functions, never assertions inside them. **Do not name
    `test_notes_are_not_offered_a_sync_they_cannot_do`**: it is not being
    replaced, and it passes, and `red-commit.sh` refuses a named test that
    passed. If premise 10's first way through was chosen, this is also the
    commit that names
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held` among
    its failures, with the reason in the message.

    GREEN. Make `entries_for` ask the seam for the note folder menu. Read
    `Focus` and `entries_for` first: the menus are static arrays and one of them
    is now conditional, and how the account reaches it is a design decision.
    **Do not thread an account through every menu because one needs it.** If the
    shape of `Focus` has to change, say what else that costs; several tests loop
    over it.

    **Corrected 2026-09-10: as scoped, this task can make the library function
    ask the seam while the running program never does, and the file list is why.**
    The only production route into `entries_for` is
    `wx_context_menu::show(&owner, focus)`, whose only caller is
    `wire_context_menu` in `src/presentation/wx_app.rs`, reached for notes
    through `wire_context_menu(&notes_sb.tree, || Some(Focus::Containers(ContainerKind::NoteFolder)))`.
    That closure hands over a constant `Focus` and knows no account, and its
    bound is `Fn() -> Option<Focus> + 'static`, which carries none either.
    Neither `wx_app.rs` nor `wx_context_menu.rs` is in this task's file list.

    ```bash
    grep -rnw 'entries_for' src/presentation src/application/context_menu.rs
    grep -n 'wire_context_menu(&notes_sb.tree' src/presentation/wx_app.rs
    ```

    (The other `entries_for` calls a plain grep finds, in
    `src/presentation/folder_tree.rs`, are inside a `#[cfg(test)] mod which_menu`
    and are not a second production route. A bare grep also matches
    `forget::entries_for`, `oauth::entries_for_account` and
    `default_apps::capability_entries_for`, which are different functions;
    `grep -rnw` is what separates them.)

    Two ways to finish, and the executor picks one and says which. **Add
    `src/presentation/wx_app.rs` and `src/presentation/wx_context_menu.rs` to the
    file list and wire it properly**, remembering that 47 guard records name
    `wx_app.rs`, so no `#[test]` goes in it. Or **stop at the library function
    and say plainly what is not wired**, which is defensible only because no
    backend exists yet, so every account's answer is the same and there is
    nothing per-account to carry through: the wiring then belongs to `05.1-03`,
    in the plan that first makes two accounts answer differently, and it goes in
    `.planning/WINDOWS.md` rather than in a summary sentence.

    What is not available is doing the second and describing it as the first.
    A library function that asks a question the running program never asks it is
    guardrail 1 exactly, and this project has shipped it before.

    Update the comment above `NOTE_FOLDERS` in the same commit that makes it
    stale. What it says now is true and is about to be true for a different
    reason.

    Write the `docs/changelog.md` entry under `[Unreleased]` and bump the
    version. This one is nearly all about what has *not* changed, so say it that
    way: notes still stay on this computer, there is now one place that decides
    that rather than the answer being written into a menu, and the menu no longer
    offers a sync because of a comment.

    Add a `guards/guards.toml` record for the menu asking the seam. Break it by
    making the seam always answer that a backend exists, which is the constant
    rather than the removal and is what a half-finished first backend would look
    like. Run `cargo test --all-targets --no-fail-fast` on a clean tree with
    nothing else building, quote the whole red list, and bump the census on line
    80.

    **Corrected 2026-09-10: that break reddens nothing unless the corrected test
    shape at the top of this task was built.** Premise 15 has the measurement of
    what the file does and does not notice. With the table of literals in place
    the break reddens the "stays on this computer" row and the record is real.
    With an agreement assertion in place it reddens nothing, and the plan's
    instruction to measure it with `cargo test --all-targets --no-fail-fast` on
    a clean tree would have spent a full-target run to produce an empty red list
    and then required the record to be redesigned from scratch. Measure the
    break after the test is written, not before, and if the red list comes back
    empty do not write the record down: that is the finding, and it means the
    test shape is still wrong.
  </action>
  <verify>
    <automated>cargo test --lib -- application::context_menu:: application::new_item::</automated>
  </verify>
  <acceptance_criteria>
    - The named test filter passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
    - The summary says plainly that `05-RESEARCH.md`'s `syncs` predicate does not exist and that `supports` is the function meant, with the command that shows it.
    - The seam is a new module with its own `pub mod` line, and the summary says why it is not in `new_item.rs`.
    - The seam answers a which and not a bool, has a variant for a word this build does not recognise, and its doc comment names `AddressBook` as the model.
    - The decision about `new_item::supports` is made in writing, and if two functions can answer for one account a test holds them together.
    - **Corrected 2026-09-10.** This used to read: "The note folder menu test asserts agreement in both directions rather than an absence, and the summary says why the absence assertion was weak." Instead: the note folder menu test is a table with one row per variant of the seam's answer and the expected column written as literals, `test_notes_are_not_offered_a_sync_they_cannot_do` is kept unchanged, and the summary says why an agreement assertion computed from the seam would have been the tautology rather than the fix.
    - The unrecognised-backend row's answer is decided in writing and argued for in one sentence.
    - The calendar, task list and contact group menus are unchanged, with a test.
    - The comment above `NOTE_FOLDERS` was corrected in the same commit.
    - If `Focus` changed shape, the summary says what else that cost and names the tests that loop over it.
    - **Whether the running program reaches the seam is answered plainly.** Either `wx_app.rs` and `wx_context_menu.rs` were opened and wired, and the summary says so, or the summary says the note folder menu in the running program still does not ask per account, says why that is harmless while no backend exists, and the gap is recorded in `.planning/WINDOWS.md` rather than only in prose.
    - The guard record's break is the seam answering that a backend exists, was measured with `--no-fail-fast`, and its full red list is quoted. **If that red list came back empty, no record was written and the summary says so**, because an empty red list means the test still computes both sides from the same place. Line 80's census was corrected.
    - If a `#[test]` was added to `context_menu.rs`, the count check was either named in the RED commit's failures or avoided by premise 10's second way through, and the scoped `scripts/guards.sh --remeasure` was run.
  </acceptance_criteria>
  <done>There is one place that answers where an account's notes go, the menu asks it instead of holding a constant, and the answer today is still that they stay here.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The settings screen says where an account's notes go</name>

  **Corrected 2026-09-10, and this changes what task 2 builds. Read it before
  the behavior list.**

  **The settings screen has no account, and the behavior line "the section says
  where notes go for the account being looked at" cannot be met as scoped.**
  `build_settings_dialog` takes `(&Frame, &AppConfig, &Arc<Accessibility>)` and
  `build_calendar_pim_tab` takes `(&Panel, &AppConfig)`. There is no account
  being looked at, because the dialog never had one. Reaching a chosen account
  means changing `show_settings_dialog`, `build_settings_dialog` and their
  caller in `src/presentation/wx_app.rs`, which 47 guard records name and which
  is in neither this task's file list nor the plan's `files_modified`.

  Without an account the honest implementation is one fixed sentence, and one
  fixed sentence is exactly the break this task's own guard record is supposed
  to catch. So the record would have had no break either, for the same reason
  task 1's did.

  **`AppConfig` is not as empty as it looks, and this is what makes the task
  buildable inside its file list.** It carries `pub default_account_id: String`
  and `pub allowed_per_account: HashMap<String, Allowed>`, both top-level
  fields. There is no roster of accounts, so the screen still cannot enumerate
  them, but a default account id is reachable from the `&AppConfig` the tab
  already receives. So:

  **Build the section for the default account, and say in the sentence which
  account it is about.** The screen asks the seam for the default account's
  answer, which is a real question with a real answer that a break can change,
  and the sentence names the account so nobody reads it as a statement about
  all of them. Say in the summary that this is the default account only, and
  why a per-account answer needs a signature change through `wx_app.rs` that
  this plan does not budget.

  ```bash
  grep -n 'fn build_settings_dialog\|fn build_calendar_pim_tab' src/presentation/wx_settings.rs
  grep -n 'pub default_account_id\|pub allowed_per_account' src/data/config.rs
  ```

  One note on the evidence, because the finding that produced this correction
  got it slightly wrong: a grep for `Account` or `accounts` in `wx_settings.rs`
  really does return nothing, but lowercase `account` appears nine times, all
  of it prose inside labels and comments. The conclusion holds and the grep that
  was quoted for it does not, which is the shape this project keeps writing
  down.

  <files>src/presentation/wx_settings.rs, src/application/notes_backend.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `src/presentation/wx_settings.rs`: `fn build_calendar_pim_tab` in full including the comment about the removed view picker, `fn section`, `fn labelled_choice`, and `fn build_permissions_tab`'s Contacts block with `CONTACT_CHANGES_WHEN_THIS_IS_OFF` and the comment above the section about where a warning sits relative to what it warns about.
    - `src/application/allowed.rs`: `SETTINGS_SECTION`, `READING_SECTION`, and the comment explaining why a second heading exists rather than a third box under the first.
    - `src/data/config.rs`'s `mod every_setting_is_acted_on`, both tests and the three exception constants, so it is clear why adding no field means adding no obligation there.
    - `CLAUDE.md`'s rule that every setting is reachable from the settings screen and sorted into a section somebody would look in, and its paragraph about the two settings that break it.
    - `tests/house_style.rs`: `A_CONTROL_NO_SCREEN_WRITES` and `a_line_and_the_one_after_it`, so the sentence written here is checked against the phrases a document may not use.
  </read_first>
  <behavior>
    - The Calendar and PIM tab has a Notes section, and it comes before or after the existing sections in an order somebody moving through them by keyboard would expect, said out loud in the summary with the reason.
    - The section says where notes go for the default account, named in the sentence, taking the answer from the seam rather than repeating it. **Corrected 2026-09-10: this used to say "for the account being looked at". The settings screen has no account being looked at; see the correction at the top of this task.**
    - Where the answer is that they stay on this computer, the sentence says so and says nothing about a switch, because there is nothing to switch.
    - The sentence is carried on both Windows accessibility channels, so NVDA and Narrator say the same thing.
    - No new field is added to `AppConfig`, so the two settings-screen tests are unaffected, and the summary says so rather than leaving it to be noticed.
  </behavior>
  <action>
    RED first, on a branch. Tests go in the seam's module for the wording and in
    `src/data/config.rs` for the screen-reading assertion if one is needed.
    **Re-measured 2026-09-10: `config.rs` costs three records, not two, and
    `wx_settings.rs` costs two, not one.** Measure again before committing and
    run the scoped `scripts/guards.sh --remeasure` the gate prints, detached.

    **Corrected 2026-09-10: this used to say to run it with
    `WIXEN_TEST_THREADS=4`. Do not set it.** `scripts/guards.py` now defaults to
    8, and its comment records the re-measurement that moved it: on 2026-09-09
    at `10effea`, over 6,720 library tests on 24 cores, 4 threads took 161s and
    8 took 140s. Setting 4 makes the run about 15% slower than doing nothing,
    and the old sentence presented it as the speed-up. Budget about 95 seconds
    per record, and re-take that rate rather than quoting it, because both the
    record count and the suite size go on moving.

    `wx_settings.rs` holds no `#[test]` at all today, and the existing checks
    about the settings screen read its source from `config.rs`. Follow that
    rather than starting a test module in the screen.

    Write the sentence in the seam's module as a named constant, not inline in
    the screen. `CONTACT_CHANGES_WHEN_THIS_IS_OFF` and
    `calendar_source::NOT_TRIED_FOR_REAL` are the two worked examples: the
    screen names the constant and the constant holds the words, so the sentence
    exists once and a test can assert its content without building a window.

    Check the sentence against `A_CONTROL_NO_SCREEN_WRITES` before writing it.
    A notes backend chosen by account invites "set it per account", which is one
    of the two phrases that guard forbids, and the check reads each line with
    the one after it so wrapping does not hide it.

    The sentence is not a placeholder that a backend removes. Write it so it can
    say "this account's notes stay on this computer" for an account that has no
    backend and go on saying it after three backends exist, because a consumer
    Gmail account will still have none: Google Keep's API is Workspace only and
    `src/application/new_item.rs:22` already says so. A sentence written as "not
    yet" has to be rewritten later; a sentence written as "this account has no
    notes backend" does not.

    Walk the fixtures by hand. The trap: a test asserting the settings screen
    source contains the constant's name is satisfied by a screen that names it
    and shows it nowhere, which is exactly what
    `test_whether_message_text_may_be_fetched_is_offered_by_a_screen` in
    `config.rs` was written about. Read that test before writing this one and
    assert the same two things it does: that the screen names the constant and
    that the control is built from it.

    Commit RED with `Fails-until-green:` trailers naming every new test.

    GREEN. Put the section on the Calendar and PIM tab. Decide its position
    deliberately: somebody moving by keyboard meets the sections in order and
    cannot skim, so a section's position is part of whether the setting is
    findable, which is the reason `CLAUDE.md` gives for the rule.

    Follow the Contacts block's shape: a `section`, then a `StaticText` with the
    constant as its label and `set_accessible_name` set to the same string.

    Add no field to `AppConfig`. Say so in the summary and say why: there is
    nothing to choose while every account's answer is the same, and a control
    that does nothing is the failure this project has fixed repeatedly.

    Extend the `docs/changelog.md` entry and bump the version.

    Add a `guards/guards.toml` record for the screen showing the seam's answer
    rather than a fixed sentence. Break it by making the screen show the
    "stays here" sentence whatever the seam answered, which is the shape that
    survives the first backend shipping and says nothing. Run `--no-fail-fast`,
    quote the red list, bump line 80.

    **Corrected 2026-09-10: that break only exists if the screen really asks a
    question, which is what the correction at the top of this task is for.** A
    screen with no account can only hold a fixed sentence, so the prescribed
    break would have been the shipped code and the record would have had no
    break. Asking the seam for the default account is what gives it one. Measure
    it after the section is built; if the red list comes back empty, do not
    write the record, because an empty red list means the screen is not really
    asking.
  </action>
  <verify>
    <automated>cargo test --lib -- data::config:: application::notes_backend::</automated>
  </verify>
  <acceptance_criteria>
    - The named test filter passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch and `scripts/red-commit.sh` accepted it.
    - The sentence is a named constant in the seam's module, and the screen names the constant rather than holding the words.
    - The sentence was checked against `A_CONTROL_NO_SCREEN_WRITES` and the summary says so.
    - The sentence says what is true of this account rather than what is true yet, so it survives three backends shipping and still fits a consumer Gmail account, which will never have one.
    - The section's position on the tab is argued for in one sentence naming the keyboard reason.
    - Both Windows channels carry the sentence, and the summary quotes the two lines that set them.
    - No field was added to `AppConfig`, and the summary says why rather than leaving it to be inferred.
    - The test asserts the screen both names the constant and builds a control from it, the way `test_whether_message_text_may_be_fetched_is_offered_by_a_screen` does.
    - The scoped `scripts/guards.sh --remeasure` the gate printed was run, detached, with `WIXEN_TEST_THREADS` left alone, and the summary names the records.
    - **The sentence names which account it is about, and the summary says the screen answers for the default account only and what a per-account answer would have cost.**
    - The guard record's break is the fixed sentence, was measured with `--no-fail-fast`, and its red list is quoted. **If that red list came back empty, no record was written and the summary says so.** Line 80's census was corrected.
  </acceptance_criteria>
  <done>Somebody who wonders whether their notes go anywhere finds the answer in the settings screen, in the section they would look in, and the answer comes from the same place the menu asks.</done>
</task>

<task type="auto">
  <name>Task 3: What a notes backend has to answer, written down</name>
  <files>docs/development/the-notes-seam.md, src/application/notes_backend.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `src/application/deletions.rs`'s module header in full, and `TheDeletionSoFar` in `src/data/message_cache/mod.rs` with its comment about two questions that used to be one question.
    - `src/application/conflict_choice.rs` in full: `BothCopies`, `WhichCopy`, `TheOtherCopy`, `fields_that_differ`, `what_is_being_asked`, `what_was_chosen`, `WhatChoosingCallsFor`, `how_many_are_waiting_to_be_chosen`, `what_that_means` and `choosing`. This is the conflict model plan 03-09 built and it is the one PIM-07 must consume rather than duplicate.
    - `src/data/message_cache/held_conflicts.rs`: `AHeldConflict` and how a conflict is held across restarts.
    - `src/application/contacts_sync.rs`: `WhoseCopyWins`, `whose_copy_wins` with its doc comment about comparing markers rather than clocks, and `hold_both_copies_of`. Read those three and nothing else in that file; it is 281 tests long and 77 guard records name it.
    - `src/application/tasks_sync.rs`: `TaskService` and its `Scripted` fake, and `SyncResult` including `waiting_on_the_setting`, `local_only` and `needs_sign_in`.
    - `src/data/message_cache/mod.rs`: `ProviderIdentity`, especially `provider_version` and `change_is_waiting` and their comments about why each belongs where it does.
    - `Cargo.toml`'s comments over `ring`, `x509-parser` and `image`, for what a written justification looks like in this project.
  </read_first>
  <behavior>
    - The document answers three questions a backend must answer, and each answer is stated as a requirement on any backend rather than as a description of one.
    - The trait in the seam's module names the operations a backend performs, with outcomes as separate variants rather than one error string.
    - Nothing implements the trait, and nothing calls a network.
    - A second, different implementation could be written against the document without changing what is stored, and the document says which parts of the stored form a backend may not change.
  </behavior>
  <action>
    This task writes prose and a trait. Nothing here changes behaviour, so
    `tdd="true"` does not apply; what keeps it honest is that the document is
    checked against the code it describes and that the trait has to compile.

    Write `docs/development/the-notes-seam.md`. Use the `writing-craft` skill:
    plain language, no em-dashes, no AI-slop vocabulary, worked examples.

    Three questions, and each is answered from something already in this tree
    rather than invented:

    **Identity.** How a backend names a note, and what happens when the name it
    gives is not one this build recognises. `ProviderIdentity` is the shape:
    per-backend rather than per-item, with a version marker that is
    `Option<String>` because not every backend gives one. Say why the identity
    belongs to the pairing of a note and a backend rather than to the note, and
    quote `ProviderIdentity`'s own comment, which records what went wrong when a
    flag was kept on the contact instead.

    **Conflict.** What happens when both copies moved. **The answer is that the
    backend does not decide.** `src/application/conflict_choice.rs` exists, plan
    03-09 built it, and it is on `main`: `git log --oneline main -1 -- src/application/conflict_choice.rs`
    returns the commit. `TheOtherCopy` has two variants today, `AnAddressBook`
    and `ACalendar`, each carrying the words used in a sentence, and its doc
    comment explains that it is a parameter rather than a second set of
    sentences so that one set of words with a hole in it cannot drift from
    itself. A notes backend adds a third variant and nothing else. **Say in the
    document, in as many words, that a backend must not resolve a conflict
    itself**, and say why: two conflict models disagree the first time either is
    changed, which is the mistake `03-RESEARCH.md` warned about and SCALE-06 was
    corrected to avoid.

    **Removal.** What happens to a note whose backend is taken off an account.
    The note stays, because it is a first-class local document and always was,
    and the identity it carried for that backend stops meaning anything. Say what
    must not happen: the note must not be deleted, and it must not be sent to a
    different backend under the old identity. `AddressBook::Other`'s comment
    about forgetting a name silently joining two contacts is the failure to point
    at.

    Say a fourth thing the document has to carry even though the requirement
    does not name it: **a backend needs a record of deletions that outlives the
    row**. `application::deletions` states the rule for contacts, events and
    tasks and there is no `deleted_notes` table.
    `grep -n "CREATE TABLE IF NOT EXISTS deleted" src/data/message_cache/mod.rs`
    finds three and none of them is notes. Without it, the first backend
    rediscovers the exact bug that module was written about: a note deleted here
    comes back on the next read, under the backend's own identifier, with nothing
    left saying it was ever deleted.

    Write the contract against what is known about all three backends, not
    against CalDAV. Three facts about OneNote, measured on 2026-09-06 from
    Microsoft's reference and repeated in this phase's README with their
    sources, are what the contract has to survive. A page has no ETag, only a
    read-only `lastModifiedDateTime`, so a backend's version marker cannot be
    assumed to be an opaque token that changes only when the content does. A
    page's body cannot be replaced, only appended to, so "write the whole
    document" is not an operation every backend has. And a page lives four
    levels deep, notebook then section group then section then page, so "which
    container" is not one identifier everywhere.

    The contract does not have to solve any of those. It has to be written so
    that a backend can answer them differently without the seam changing shape,
    and where it cannot, it has to say so in writing rather than silently assume
    CalDAV. `05.2-03` is required to report every place this document turned out
    to have assumed CalDAV, so write it as something that can be found wrong.

    Write the trait in the seam's module. Name it for what a notes sync asks of
    a service rather than for any provider, the way `TaskService`'s doc comment
    argues for. Name the outcomes as separate variants rather than one error
    string, for the reason `04-09` gives about a crate's error text not being
    written for the person who hears it.

    **Nothing implements it in this plan. Corrected 2026-09-10: this used to
    add "Under `-D warnings` a trait nothing uses will not build, so either give
    it the one test that exercises it, or say in the commit which and why."
    That is false, and premise 13 has the measurement.** A `pub trait` in a
    library crate's public module with no implementor and no caller compiles
    clean, so nothing is owed and there is nothing to say in the commit. The old
    sentence sent the executor hunting for a consumer in the same paragraph that
    forbids writing the obvious one, which is the shape worth noticing: a false
    constraint and a rule against the only way to satisfy it. Do not write a
    fake implementation here just to make it build: the fake belongs with the
    backend in `05.1-03`, where it can
    be driven against real behaviour, and `05.1-04` is the plan whose whole
    subject is a second implementation written to disagree with the first. A
    fake with nothing to compare against is the "test double that cannot fail"
    shape.

    Extend the `docs/changelog.md` entry and bump the version, or say why this
    one is not user-visible and belongs only in the developer document. Either is
    defensible; say which and why.

    Add a `guards/guards.toml` record only if there is behaviour to guard. A
    document is guardable when something reads it, and nothing reads this one.
    If no record is added, say so in the summary rather than leaving it looking
    forgotten.
  </action>
  <verify>
    <automated>cargo test --lib -- application::notes_backend:: && cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - Both commands pass, and `scripts/check.sh` passes for this commit's file list.
    - `docs/development/the-notes-seam.md` answers identity, conflict, removal and the deletion record, each as a requirement on any backend rather than a description of one.
    - The conflict answer says in as many words that a backend does not resolve a conflict itself, and names `conflict_choice.rs` and the third `TheOtherCopy` variant a notes backend would add.
    - The document says a `deleted_notes` record is needed and quotes the failure `application::deletions` was written about.
    - The version marker, the write operation and the container identifier are each written so a backend can answer them differently, or the document says in writing that it has assumed CalDAV for that one and why. The summary names which of the three fell which way.
    - The trait names outcomes as separate variants, and the summary quotes the type.
    - Nothing implements the trait and nothing calls a network, and the summary says so plainly.
    - The document contains no em-dash and none of the phrases in `A_CONTROL_NO_SCREEN_WRITES`, which `tests/house_style.rs` checks.
    - The summary says whether a guard record was added and why or why not.
  </acceptance_criteria>
  <done>Somebody writing the second notes backend has a document saying what it must answer, and a first one to compare it against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a stored backend name from a later build -> this build's seam | a word this build does not recognise can be in a database or a configuration it opens |
| the seam's answer -> what a menu offers | a wrong answer offers a sync that cannot happen, or hides one that can |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Denial of service | an unrecognised backend name refusing to be read, so notes made by a later build cannot be opened | medium | mitigate | a variant for a word this build does not recognise, modelled on `AddressBook::Other`, with a test driving it. |
| T-05-22 | Repudiation | a menu offering a sync that cannot happen, or a settings sentence promising one | high | mitigate | the menu asks the seam, held in both directions by a test, and the settings sentence takes its answer from the same place. The guard record's break is the seam always answering that a backend exists. This is the failure the note editor stub taught this project to avoid. |
| T-05-23 | Tampering | a backend resolving a conflict itself and overwriting somebody's note | high | mitigate | the contract says in as many words that a backend does not resolve a conflict, and names `conflict_choice.rs`. Nothing implements the trait in this plan, so the mitigation is a written requirement `05.1-03`, `05.1-04` and `05.2-03` are each held to rather than code. Say so plainly. |
| T-05-24 | Information disclosure | a document that names a per-account control nothing offers | low | mitigate | `A_CONTROL_NO_SCREEN_WRITES` in `tests/house_style.rs` already catches two such phrases and reads across line breaks. The sentence is checked against it before it is written. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package is added. `Cargo.toml` is touched only by version bumps and the diff is read rather than assumed. `05.1-03` and `05.2-02` may add one, and each of those plans carries the legitimacy gate. |
</threat_model>

<verification>
- `cargo test --lib -- application::context_menu:: application::new_item:: application::notes_backend:: data::config::` passes.
- `cargo test --test house_style` passes, which is what checks the new document for em-dashes and for the forbidden per-account phrases.
- `scripts/check.sh` passes on every commit through the commit hook, with nothing using `--no-verify`. **Corrected 2026-09-10: this used to say commits touching `Cargo.toml` answer `all` and must be run detached. They answer `affected`; premise 12 says why, and says that the danger is now an executor reporting a full gate that did not run.** One `scripts/check.sh all` on the branch before the merge is what is actually owed.
- Guard records added were measured by hand with `--no-fail-fast` on a clean tree, with full red lists quoted, and line 80's census corrected in each commit that added one. **Any record whose measured red list came back empty was not written down, and the summary says which and why.**
- The scoped `scripts/guards.sh --remeasure` printed by the gate for `src/data/config.rs`, `src/presentation/wx_settings.rs` or `src/application/context_menu.rs` was run, detached, with `WIXEN_TEST_THREADS` left at its default.
- No `#[test]` was added to `contacts_sync.rs`, `tasks_sync.rs`, `managers.rs` or `wx_app.rs`; counts before and after are reported.
- `grep -rn "reqwest\|http" src/application/notes_backend.rs` returns nothing, because nothing in this plan reaches a network.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed to the phase-8 sweep and must not block the merge.
</verification>

<success_criteria>
**PIM-07's first `[D]` line closes.** One place decides where a note goes, the
account's provider picks it, and an account with no backend keeps its notes local
and says so.

**PIM-04's last two `[D]` lines close.** The settings screen says notes do not
sync yet rather than offering a switch that does nothing, and the gate through
`Allowed::personal_information` is a requirement written into the contract that
`05.1-03` is held to, since nothing here reaches a network. Say that distinction
plainly: a written requirement is not a closed criterion.

The settings sentence is not a placeholder a backend removes. After all three
backends ship, a consumer Gmail account still keeps its notes on this computer,
because Google Keep's API is Workspace only and `new_item.rs:22` already says
so. The criterion has real work in it after all three.

**PIM-08 closes on its second and third `[D]` lines and not on its first.** What
the seam assumes is written down, and nothing ships a hosted client, a network
call or a setting offering one. Whether the seam really takes a second
implementation with no change to the stored form is not proved by a seam with no
implementations at all. `05.1-04` puts a second implementation behind it inside
this phase, shaped from what OneNote really does, and `05.2-03` answers it
properly with the real client and is required to report where `05.1-04` was
wrong.

**PIM-07's other three `[D]` lines are `05.1-03`'s.** Backends added one at a time,
a note that cannot be sent staying local and marked as waiting, and no backend
claiming to work against a real server until it has run against one.

**It is not closed by ear**, and that goes in `.planning/WINDOWS.md`, which ends
at entry 230 before this phase, not the 119 this used to say. Read its last row
rather than either number: `grep -nE '^\| *[0-9]+ ' .planning/WINDOWS.md |
tail -1`. Entries go in through `gsd-tools windows`, because the ledger has two
halves and `test_both_halves_of_the_ledger_say_the_same_thing` holds them
together, so a row typed into the table by hand reverts on the next tool write.

What goes in it: whether the Notes section is found where it was put, and
whether a sentence saying notes stay on this computer is heard as an answer or
as an apology. **And, if task 1 stopped at the library function, that the note
folder menu in the running program does not yet ask the seam per account.**
</success_criteria>

<output>
Create `.planning/phases/05.1-notes-and-contacts-reach-a-server/05.1-02-SUMMARY.md` when done

**Corrected 2026-09-10.** This, the `phase:` field and the context include above
all used to name `05.1-notes-and-contacts-reach-a-server-for-the-first-time`,
which is not a directory that exists. The phase lives at
`.planning/phases/05.1-notes-and-contacts-reach-a-server`, which is where this
plan file itself sits. The `@` include therefore loaded nothing, so the
executor would have started without `05.1-01`'s summary, this plan's only
declared dependency, and nothing would have said so. Writing the summary at the
old path would have created a second phase directory:
`phases_on_disk()` in `tests/the_planning_files_agree_with_themselves.rs`
normalises both spellings to the same key and inserts by it, so one directory
replaces the other in the map, and `test_the_state_file_counts_the_plans_that_are_on_disk`
and `test_the_roadmap_counts_the_files_that_are_on_disk` then compare against
whichever won. That target runs on every commit, so it would have gone red on
the docs commit at the very end of the plan and stayed red.
</output>
