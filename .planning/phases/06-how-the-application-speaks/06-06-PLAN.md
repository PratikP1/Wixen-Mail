---
phase: 06-how-the-application-speaks
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - scripts/which-checks.sh
  - scripts/which-checks.test.sh
  - .github/workflows/accessibility.yml
  - src/presentation/scan_target.rs
  - guards/guards.toml
autonomous: false
requirements: [FEEDBACK-03]
estimate:
  tokens: 9000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A commit that changes an accessibility workflow runs the two tests that could catch it disagreeing with the code, which today it does not."
    - "The rule set the scan runs is a version somebody can name, so a coverage list written against it is a claim about something reproducible."
    - "Which windows the scan covers is a decision somebody made, written down, rather than whichever list happened to be there."
    - "Nothing in CI downloads and runs a binary whose contents nobody pinned."
  artifacts:
    - "scripts/which-checks.sh: a change under .github/workflows/ answering all, on the pattern the installer script already sets."
    - "scripts/which-checks.test.sh: cases proving it, and proving the answer for a file the rule must not catch."
    - ".github/workflows/accessibility.yml: a pinned Axe.Windows release, recorded with the date it was chosen."
    - "src/presentation/scan_target.rs: whatever the target list decision requires, and the two tests that keep it in step with the workflow."
  key_links:
    - "scripts/which-checks.sh -> check.sh's run_the_tests_that_reach_what_changed: which-checks answers affected for a .yml, and check.sh maps only src/*.rs and tests/*.rs to targets, so affected selects no test at all for a workflow change. Both halves were read; neither alone explains the hole."
    - "scan_target.rs's two workflow-reading tests -> the commits they run on: they are unit tests in src/, so the scoped run reaches them only when scan_target.rs changes. src/presentation/scan_target.rs carries 0 guard records, so the guards/guards.toml coupling path does not rescue them either."
    - "the unpinned CLI -> the coverage list 06-07 writes: accessibility.yml fetches the latest Axe.Windows release at run time, so the rule set can change without a commit here. A document saying the scan judges three criteria is a claim about a version nothing pins."
    - "ScanTarget::ALL -> the workflow's $targets array: two tests read the workflow file to keep them in step, and adding a target means adding it in both places."
---

<objective>
Make the accessibility scan something a document can honestly describe: a known
rule set, a decided list of windows, and a commit gate that runs the tests which
could catch the workflow and the code disagreeing.

Purpose: success criterion 3, the half that is not prose. 06-07 writes the
coverage list and this is what makes that list a claim about something rather
than about whatever was downloaded that morning.

Output: a closed gate hole, a pinned binary, and a target list somebody chose.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-how-the-application-speaks/06-RESEARCH.md
@.planning/phases/06-how-the-application-speaks/README.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-12 at `b8857bc8`, command first.

**1. The gate hole is real and both halves were read rather than one.**
`scripts/which-checks.sh:329-338` is the last rule on the branch path:

```sh
for path in "$@"; do
    case "$path" in
        *.md | *.txt) ;;
        *)
            echo affected
            exit 0
            ;;
    esac
done
```

So a `.yml` answers `affected`. And `scripts/check.sh:387-432`,
`run_the_tests_that_reach_what_changed`, maps exactly two path shapes: `src/*.rs`
becomes `cargo test --lib module::` and `tests/*.rs` becomes
`cargo test --test target`. A `.yml` matches neither, so `affected` selects no
scoped test for it. The whole-tree guards still run, and none of them reads this
workflow.

**Note the branch, because it matters to how this is tested.** On `main`,
`which-checks.sh:138-147` already answers `all` for anything that is not `*.md`
or `*.txt`, so the hole exists on a branch only. That is still the hole: a branch
is where a workflow change is written.

**2. The two tests that go unrun are in `src/`, and their file carries no guard
record.**

```
grep -n 'accessibility.yml' src/presentation/scan_target.rs
-> 27 (a doc comment), 172, 194
```

`test_the_command_line_and_the_workflow_use_the_same_flag` at `:164` and
`test_the_workflow_asks_for_every_target` at `:189` both read the workflow file.
`src/presentation/scan_target.rs` carries **0** guard records and holds **6**
`#[test]` functions, taken 2026-09-12, so the `guards/guards.toml` coupling that
rescues an integration guard does not apply.

**3. The precedent for the fix is in the same file and is recent.**
`which-checks.sh:311-320` makes any `*.iss` answer `all`, and its comment gives
exactly this reasoning one layer down: three tests read the installer script,
all three are unit tests in `src/`, and `affected` chose no scoped target for an
`.iss`, so all three ran on every commit except the ones that could break them.
The comment also records why the pattern is `*.iss` rather than
`installer/*.iss`: the extension decides, so a second one added elsewhere cannot
be missed. **Follow that, and put the new rule where that one is: below the
manifest block**, for the reason its comment gives about the version-bump
exception handing the softer answer to a commit that also bumps.

**4. The target list is eleven and `ScanTarget::ALL` is ten, and that is
correct rather than a disagreement.**

```
grep -n 'targets = @' .github/workflows/accessibility.yml
-> 100: $targets = @('main', 'settings', 'accounts', 'compose', 'reader', 'search', 'filters', 'calendar', 'first-run', 'add-calendar', 'blocked-senders')
grep -n 'ScanTarget; ' src/presentation/scan_target.rs
-> 72:    pub const ALL: [ScanTarget; 10] = [
```

Eleven targets is the main window plus ten dialogs, and `:125` of the workflow
shows why: `$arguments = if ($target -eq 'main') { @() } else { @("--scan-target", $target) }`.
`main` is not a `ScanTarget`. `blocked-senders` arrived after the 2026-09-06
research, which said ten and nine.

**5. The CLI is fetched unpinned on every run.** `accessibility.yml:78-81`:

```powershell
$release = Invoke-RestMethod 'https://api.github.com/repos/microsoft/axe-windows/releases/latest'
$asset = $release.assets | Where-Object { $_.name -like 'AxeWindowsCLI*.zip' } | Select-Object -First 1
if (-not $asset) { throw 'No AxeWindowsCLI zip in the latest axe-windows release' }
Invoke-WebRequest $asset.browser_download_url -OutFile axe-cli.zip
```

So the rule set is whatever Microsoft shipped most recently. The research read
the rule table twice, five days apart, and got different answers, at least part
of which was its own measurement error, which is exactly the ambiguity an
unpinned dependency creates: neither reading can be checked against the other.

**6. `scripts/which-checks.test.sh` has no case about a workflow file.**

```
grep -n 'yml\|workflow' scripts/which-checks.test.sh
-> no match
```

And no guard record names any shell suite:

```
grep -c 'test\.sh' guards/guards.toml
-> 0
```

**7. Shell suite case descriptions are constrained, and the constraint was found
by breaking it.** `scripts/shell-suite.sh` refuses an empty description, one
holding ` ... ` and one holding a comma, and refuses two cases sharing a
description. `CLAUDE.md` records what the comma cost: a commit naming
`which-checks::main, code changed` was read as two tests, neither of which had
ever run, and about ten descriptions were reworded.

**8. `scripts/*.test.sh` costs 108 seconds on every invocation of `check.sh`, in
every mode.** Measured 2026-09-10 at `eda2719`. That is the floor under the
cheapest possible run and adding cases to `which-checks.test.sh` adds to it. One
sitting on 2026-09-09 took that suite alone from 28.7s to 34.4s with seven
cases. **Measure this suite before and after and report both numbers**, because
`CLAUDE.md`'s own account of how that figure went stale is that nothing
re-measured the total when each addition was cheap.
</premise_corrections>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: a workflow change earns the checks that could catch it</name>
  <files>scripts/which-checks.sh, scripts/which-checks.test.sh</files>
  <read_first>
    - `scripts/which-checks.sh` in full. It is long and almost all of it is the reasoning; read the whole thing rather than the branch you are changing.
    - In particular `:255-280`, the manifest block and `only_the_packages_own_version_moved`, and `:282-338`, the installer rule and the final markdown rule, so the new rule goes in the right place for the reason the installer rule's comment gives.
    - `scripts/check.sh:387-432`, `run_the_tests_that_reach_what_changed`, so the claim that `affected` selects nothing for a `.yml` is read rather than repeated.
    - `scripts/which-checks.test.sh:1-70` and its `expect` and `expect_from` helpers, and `scripts/shell-suite.sh`, including what it refuses in a case description.
    - `src/presentation/scan_target.rs:160-210`, the two tests that read the workflow.
  </read_first>
  <behavior>
    - A commit touching any file under `.github/workflows/` answers `all`, on a branch as well as on `main`.
    - A commit touching a markdown file still answers `docs_only`, and one touching a Rust file still answers `affected`, so the new rule catches what it is for and nothing else.
    - A commit touching a workflow file and a document together answers `all`, because the stronger answer wins.
    - The suite says which case failed, on the passing path as well as the failing one, and ends at `suite_verdict`.
  </behavior>
  <action>
    Red first, on a branch. The red is a case in `scripts/which-checks.test.sh`
    asserting `all` for `.github/workflows/accessibility.yml`, which answers
    `affected` today. **`scripts/check.sh` collects the shell suites' output into
    the same run log the cargo runs use and refuses on it in every mode but
    `red`**, so a red commit naming a shell case is the supported path. The name
    is `<suite>::<the case description>`, so the trailer reads:

    ```
    Fails-until-green: which-checks::a workflow file earns every check
    ```

    Write the case descriptions so `shell-suite.sh` accepts them: no empty
    description, no ` ... `, no comma, and none the same as another in the file.
    The comma is the one that has already cost this project a rewrite of about
    ten descriptions, because a commit naming a case with a comma in it was read
    as two tests neither of which had ever run.

    Write four cases, not one. One for a workflow file on a branch. One for a
    workflow file together with a document. One proving a plain markdown change
    still answers `docs_only`, and one proving a plain Rust change still answers
    `affected`, because **a rule that refuses the wrong thing is worse than
    none** and the suite's own header says the allow cases matter as much as the
    refusals.

    Then add the rule to `scripts/which-checks.sh`, **below the manifest block
    and beside the installer rule**, matching on the path prefix
    `.github/workflows/` rather than on the `.yml` extension. State the
    difference from the installer rule in the comment and why it goes the other
    way here: an `.iss` anywhere in this tree is a setup script, and a `.yml`
    anywhere is not necessarily a workflow, so here the directory decides. Say
    what the rule is for in the same shape the installer comment uses: name the
    two tests in `src/presentation/scan_target.rs` that read
    `.github/workflows/accessibility.yml`, say they are unit tests in `src/` so
    `affected` chose no scoped target for them, and say that they therefore ran
    on every commit except the ones that could break them.

    **Measure what the suite costs, before and after.** Time
    `bash scripts/which-checks.test.sh` on its own, both ways, and put both
    numbers with the date in the summary. `CLAUDE.md` records that this suite
    family went from milliseconds to 108 seconds a case at a time with nothing
    re-measuring the total, and this plan is adding cases to it.

    **Prove the hole was real before closing it, and report the output.** On a
    branch, stage a change to `.github/workflows/accessibility.yml`, run
    `scripts/which-checks.sh` with that path, and record that it answered
    `affected`. Then break `ScanTarget::ALL` so it disagrees with the workflow,
    run `scripts/check.sh`, and record that it passed. That is the defect
    measured. Revert both, apply the rule, and show the same sequence failing.

    Do not bump the version and do not write a changelog entry. Nothing
    user-visible changed.
  </action>
  <verify>
    <automated>bash scripts/which-checks.test.sh</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names the new shell cases in `Fails-until-green:` trailers as `which-checks::<description>`, and every named case ran and failed with nothing else failing.
    - `bash scripts/which-checks.test.sh` passes at green and reaches `suite_verdict`.
    - `scripts/check.sh` passes on every commit through the hook.
    - Four cases were added: a workflow file, a workflow file with a document, a markdown-only change and a Rust-only change. Quote each description.
    - No case description holds a comma or ` ... `, and none duplicates another. Say that `shell-suite.sh` was allowed to judge that rather than the descriptions being eyeballed.
    - The before-and-after measurement of the defect is in the summary, with the commands and their output: `which-checks.sh` answering `affected`, `check.sh` passing over a real disagreement, and both changing after the rule.
    - The suite's own runtime is reported before and after, with the date and the machine.
    - The new rule sits below the manifest block, and its comment says why the directory decides here where the extension decides for `.iss`.
  </acceptance_criteria>
  <done>A commit that changes an accessibility workflow runs the tests that could catch it being wrong.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: pin the scanner, and how many windows should it look at</name>
  <decision>
    Two questions about the scan, both cheap to ask together because the answer
    to each changes what 06-07's coverage list can honestly say.

    First: the workflow downloads the latest Axe.Windows release on every run.
    Pin it to a version, or leave it and date the list?

    Second: the scan looks at eleven windows and there are at least nine more
    dialogs. Widen it in this phase, or say which windows are outside it?
  </decision>
  <context>
    **On pinning, the recommendation is to pin.** Two lines either way.

    | Option | What it gives | What it costs |
    |---|---|---|
    | **Pin the release tag** | The coverage list becomes a claim about a named rule set that anybody can re-run. One fewer unpinned third-party binary downloaded and executed in CI, which is a supply-chain surface as well as a reproducibility one | Somebody has to bump it, and a newer rule set arrives only when they do. A pin nobody moves is a scanner going stale quietly |
    | Leave it unpinned | New rules arrive free | The list needs a version and a date beside it and will drift. The rule table read twice five days apart did not agree, and nobody can now tell how much of that was upstream drift and how much was a bad parse, which is the argument |

    **On the target list, the recommendation is to leave it at eleven and say so
    in the coverage document.** Widening is good work and it is not this phase's.

    Nine dialogs are not scanned, read from `ls src/presentation/wx_*.rs` and the
    workflow's own array: `wx_columns`, `wx_conflict_choice`, `wx_destination`,
    `wx_folder_choice`, `wx_item_form`, `wx_managers`, `wx_reminder_alert`,
    `wx_thread_view`, `wx_which_days`. Three are worth naming.

    - `wx_item_form` is where somebody enters an event, contact, task or note. It
      is the one dialog `tests/checkbox_labels.rs` covers, so its check boxes are
      tested and its scan coverage is absent.
    - `wx_reminder_alert` is the modal 06-05 is about.
    - `wx_which_days` is the dialog the one skipped NVDA test names, and that
      test's own header says the reason it is skipped is that there is no
      `ScanTarget` for it. A target would unblock a skipped test as well as a
      scan.

    | Option | What it gives | What it costs |
    |---|---|---|
    | **Leave it at eleven** | The coverage document says which windows are outside it, which is the honest minimum either way | Nine dialogs go unscanned for longer |
    | Widen to some of them | A stronger list, and `wx_which_days` unblocks a skipped NVDA test | Each target is a `ScanTarget` variant, a command-line name, a workflow entry and a window that has to be reachable from a fresh profile. More scan time and probably more findings, which 06-08 then has to judge |
    | Widen to all nine | Most complete | Most of the above, and some of them may not be reachable from a fresh profile at all, which is what the skipped NVDA test found for `wx_which_days` |

    **The combination worth considering.** Pin the scanner, leave the list at
    eleven for this phase, and add one target, `wx_which_days`, because it is the
    only one that unblocks something already written and skipped. That is one
    variant, one name, one workflow entry, and it turns a skipped test into a
    running one.

    **What neither answer settles.** Whether any finding the scan produces is
    real. That is 06-08.
  </context>
  <options>
    - On the scanner: pin the release tag (recommended), or leave it unpinned and date the list.
    - On the windows: leave it at eleven (recommended), add `wx_which_days` only, add some, or add all nine.
    - a combination, which is the usual answer here.
  </options>
  <resume-signal>
    Say whether to pin, and how many windows. If any target is added, name which.
  </resume-signal>
  <acceptance_criteria>
    - Both answers are recorded in the summary with the date.
    - Where the scanner is left unpinned, the summary says that 06-07's list must carry the version and the date it was read, and a ledger entry records that the list describes something that can change with no commit here.
  </acceptance_criteria>
  <done>What the scan runs, and what it looks at, are decided by the person whose program it is.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a scanner somebody can name, looking at windows somebody chose</name>
  <files>.github/workflows/accessibility.yml, src/presentation/scan_target.rs, guards/guards.toml</files>
  <read_first>
    - `.github/workflows/accessibility.yml` in full, all of it: the triggers, the install step, the scan loop, the target array, the not-scanned versus scanned-clean distinction, the summary step and the artifact upload.
    - `src/presentation/scan_target.rs` in full. It is short, and its two workflow-reading tests are what task 1 was about.
    - `nvda-tests/tests/which-days-focus-and-tick.test.js:1-21`, the header explaining why that test is skipped and naming `scan_target.rs` as the reason.
    - `scripts/msaa-names.ps1:1-25`, its header, which is the strongest in-repo statement of what the two channels can and cannot judge and which 06-07 will quote.
    - The checkpoint's answers.
  </read_first>
  <behavior>
    - The scan runs a version of Axe.Windows that a document can name, or the workflow says in a comment why it does not and what that costs.
    - The target list is whatever the checkpoint decided, and `ScanTarget::ALL` and the workflow's array still agree.
    - The two tests that hold them in step still pass, and now run on the commits that could break them.
    - Where a target is added, it is reachable from a fresh profile the way `--scan-target accounts` reaches the Account Manager, proved by the scan running rather than by reading the code.
  </behavior>
  <action>
    Red first where there is something to redden. Adding a `ScanTarget` variant
    reddens `test_the_workflow_asks_for_every_target` until the workflow array
    gains it, and that is a genuine red half. Adding nothing to `ScanTarget`
    leaves only the pin, which is a configuration change and has no test to write
    first; say which of those happened rather than leaving it unremarked.

    **Pin the CLI as the checkpoint decided.** Replace the `releases/latest` call
    with a named release tag, and record in a comment beside it: the tag, the
    date it was chosen, and the reason, which is that a coverage document
    describing what the scan can judge is a claim about a rule set. Keep the
    existing `throw` when the asset is missing rather than letting a silent
    failure through; `CLAUDE.md`'s fourth guardrail is about exactly that shape.

    **Take the version from the release itself rather than from memory.** Ask
    GitHub which releases exist, pick one, and say in the summary which endpoint
    was called and what it returned. Do not write a tag that was not read from
    the API.

    Where the checkpoint asked for a target, add it in both places, as a variant
    with its command-line name and as an entry in the workflow's array, and let
    the two tests hold them together. Where the added target is
    `wx_which_days`, say in the summary what it means for
    `nvda-tests/tests/which-days-focus-and-tick.test.js`: its own header says the
    skip exists because there is no target for the dialog, so the skip can now be
    re-examined. **Do not un-skip it in this plan.** That test needs a real NVDA
    run to say anything, which is not something this phase can produce, and an
    un-skipped test that has never passed is a check nobody reads.

    Where the checkpoint asked for no widening, say in the summary which nine
    dialogs are outside the scan, by module name, so 06-07 can put the list in
    the document without re-deriving it.

    **Write one guard record if a target was added**, its break being removing the
    new target from the workflow array while leaving the variant. Apply it by
    hand, run the tests, write down what really went red. If nothing was added,
    write no record and say why: a break that reddens nothing is a finding, not a
    failure.

    Do not bump the version and do not write a changelog entry. Nothing
    user-visible changed, and the scan is not something a user meets.

    **This commit touches a workflow, so after task 1 it answers `all`.** That is
    the rule working. Expect the full gate, including the release build, and run
    it rather than working around it.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::scan_target::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib -- presentation::scan_target::` passes, and both workflow-reading tests are named in the output.
    - `scripts/check.sh` passes on every commit through the hook, and the summary confirms this commit answered `all` because it touched a workflow, quoting `scripts/which-checks.sh`'s answer.
    - The pinned tag was read from the GitHub releases API in this session. Quote the endpoint and the tag.
    - The workflow comment beside the pin names the tag, the date and the reason.
    - `ScanTarget::ALL`'s length and the workflow's `$targets` array agree, and the summary quotes both.
    - Where no target was added, the summary lists the nine unscanned dialogs by module name for 06-07 to use.
    - Where `wx_which_days` was added, the summary says what it means for the skipped NVDA test and confirms the test was not un-skipped.
    - A guard record exists only if a target was added, and its red list was measured by applying the break.
  </acceptance_criteria>
  <done>The scan runs a rule set somebody can name, over a list of windows somebody chose, and a change to either earns the tests that check it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a third-party binary fetched at CI run time to a runner with the repository checked out | an executable downloaded from a URL resolved at run time, run against a build of this application |
| a workflow file to what the commit gate runs | a file that decides what CI does, currently changeable with no test running |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-21 | Tampering | the Axe.Windows CLI fetched unpinned from `releases/latest` on every run | high | mitigate | pin the release tag, read from the API in the session that writes it, with the tag and the date in a comment beside it. This is both the supply-chain fix and the reproducibility fix and it is two lines. Where the checkpoint declines to pin, the disposition becomes accept and the reason is recorded in the ledger. |
| T-06-22 | Repudiation | a workflow change that makes the scan cover less, with no test running on the commit | high | mitigate | task 1, with the hole measured before it is closed and the four cases proving the rule catches what it is for and nothing else. |
| T-06-23 | Repudiation | a coverage document describing a rule set that can change with no commit here | high | mitigate | the pin. Where it is declined, 06-07's list carries the version and the date and a ledger entry says it will drift. |
| T-06-24 | Denial of service | a scan target added that is not reachable from a fresh profile, so the scan reports "not scanned" and reads as covered | medium | mitigate | the workflow already distinguishes not-scanned from scanned-clean. A new target is proved by the scan running, not by reading the code, and 06-08's checkpoint is where that is seen. |
| T-06-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added to `Cargo.toml`. The one third-party binary this plan touches is the Axe.Windows CLI and pinning it is the mitigation above. |
</threat_model>

<verification>
- `bash scripts/which-checks.test.sh` passes and reaches `suite_verdict`.
- `cargo test --lib -- presentation::scan_target::` passes.
- `scripts/check.sh` passes on every commit through the hook. The task 2 commit answers `all` and is expected to; the task 1 commit touches `scripts/` and answers `affected`.
- `Cargo.toml` is not touched, and no version bump is owed, because nothing user-visible changed.
- `scripts/check.sh` and `scripts/which-checks.sh` are also named by phase 7's plans. Read the shared regions before editing and say in the summary whether anything conflicted.
- `.planning/WINDOWS.md` gains at least one entry: the scan has not been re-run since the pin, so what the pinned rule set finds is unknown until 06-08. Where the scanner is left unpinned, a second entry records that the coverage list describes something that can change with no commit here.
</verification>

<success_criteria>
- A commit under `.github/workflows/` answers `all`, proved by four cases in the shell suite and by the hole having been measured before it was closed.
- The Axe.Windows version the scan runs is a tag somebody chose, read from the API, with the date beside it, or the reason it is not is recorded rather than left to be discovered.
- `ScanTarget::ALL` and the workflow's target array agree, and the tests that hold them together now run on the commits that could break them.
- The nine unscanned dialogs are written down by name, so 06-07's coverage document does not have to re-derive them.
</success_criteria>

<output>
Create `.planning/phases/06-how-the-application-speaks/06-06-SUMMARY.md` when done
</output>
