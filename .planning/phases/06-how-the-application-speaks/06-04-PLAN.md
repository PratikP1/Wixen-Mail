---
phase: 06-how-the-application-speaks
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/data/config.rs
  - src/presentation/wx_account_manager.rs
  - tests/house_style.rs
  - docs/ALPHA_TESTING.md
  - guards/guards.toml
  - docs/changelog.md
  - Cargo.toml
autonomous: false
requirements: [FEEDBACK-01]
estimate:
  tokens: 14000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A permission that is stored per account, honoured all the way out to the provider clients, and reachable from no screen becomes one somebody can see and change."
    - "The control says plainly that a per-account answer can only ever narrow the application-wide one, because that is all the model can do."
    - "The list that recorded this as a defect is emptied, and the guard watching that list is retired or given a companion in the same commit, so nothing is left green and blind."
    - "The settings-screen guard still passes, because the exception moved rather than being deleted."
    - "A product page may now describe the control, and the check that forbade describing it is gone rather than worked around."
  artifacts:
    - "src/presentation/wx_account_manager.rs: a per-account Allow Changes control on the account edit dialog, built with real labels."
    - "src/data/config.rs: allowed_per_account moved from STORED_AND_OFFERED_BY_NOTHING into OFFERED_BY_ANOTHER_SCREEN, pointing at the account manager."
    - "src/data/config.rs: a hand-named companion asserting the control is built from the real fields, shows the stored answer and is read back."
    - "tests/house_style.rs: A_CONTROL_NO_SCREEN_WRITES and the test reading it, retired."
  key_links:
    - "STORED_AND_OFFERED_BY_NOTHING -> test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing: with the list empty the filter iterates nothing, now_offered is empty and the assertion passes unconditionally. Emptying the list without retiring the test leaves a guard that is green and blind, which is the census-emptying failure CLAUDE.md describes and which 07-05's summary says is sitting one commit away."
    - "the two guards read different file sets: test_every_setting_somebody_can_change_is_offered_by_a_screen reads THE_SETTINGS_SCREEN, which is wx_settings.rs alone, and test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing reads EVERY_SCREEN, which is all of src/presentation. So a control on the account manager reddens the second and leaves the first failing if the exception is merely deleted. The remedy is a move, not a deletion."
    - "OFFERED_BY_ANOTHER_SCREEN -> test_a_setting_said_to_be_offered_elsewhere_really_is: moving the entry there arms a check that the named screen really names the setting, which would catch the control later being taken away."
    - "allowed_for -> the control's wording: .and(self.allowed_changes) means a per-account answer can only ever narrow. A control that looks like it can widen is a control that lies."
    - "test_nothing_offers_a_setting_per_account_that_no_screen_writes -> the documentation: it forbids a product page from saying 'set it per account' or 'for each account separately'. It must go in the same change that makes those sentences true, or the feature cannot be documented."
---

<objective>
Give the person a screen for a permission the program already honours behind
their back, and empty the list that recorded it without disarming the guard
that watches the list.

Purpose: the first item inherited from phase 1. The roadmap calls it
"FEEDBACK-01's exact shape already live in the tree", and it is: a setting that
is stored, read and obeyed out to the provider clients, and offered by nothing.

Output: a control on the account edit dialog, an exception moved rather than
deleted, and two guards left in a state somebody can trust.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-how-the-application-speaks/06-RESEARCH.md
@.planning/phases/06-how-the-application-speaks/README.md
@.planning/phases/01-folders-and-conversations/deferred-items.md
@.planning/phases/07-installing-updating-and-what-is-stored/07-05-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-12 at `b8857bc8`, command first.
**Every line number `CLAUDE.md` and `06-RESEARCH.md` give for `src/data/config.rs`
has moved**, several by more than a hundred lines, because 07-05 landed between
the research and this plan.

**1. The line numbers, re-taken.**

```
grep -n 'pub allowed_per_account\|const STORED_AND_OFFERED_BY_NOTHING\|const OFFERED_BY_ANOTHER_SCREEN\|const NOT_ANYTHING_ANYBODY_CHOOSES\|const THE_SETTINGS_SCREEN\|const EVERY_SCREEN\|pub fn allowed_for\|mod every_setting_is_acted_on' src/data/config.rs
```

| What | `CLAUDE.md` says | `06-RESEARCH.md` says | Where it is today |
|---|---|---|---|
| `allowed_per_account` | 276 | 307 | **307** |
| `allowed_for` | not given | 709 | **742** |
| `mod every_setting_is_acted_on` | not given | not given | **2097** |
| `THE_SETTINGS_SCREEN` | not given | 2140 | **2269** |
| `EVERY_SCREEN` | not given | 2144 | **2273** |
| `OFFERED_BY_ANOTHER_SCREEN` | not given | 2151 | **2280** |
| `NOT_ANYTHING_ANYBODY_CHOOSES` | not given | not given | **2299** |
| `STORED_AND_OFFERED_BY_NOTHING` | 1799 | 2190 | **2319** |
| the hand-named companion precedent | 1829 | 2220 | **2349** and **2403** |
| `test_every_setting_somebody_can_change_is_offered_by_a_screen` | not given | 2371 | **2500** |
| `test_a_setting_said_to_be_offered_elsewhere_really_is` | not given | 2415 | **2544** |
| `test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing` | 1947 | 2435 | **2564** |

The substance checks out at every new location. Re-take them again before
starting: phase 7 has four plans left and they were executing while this was
written.

**2. Phase 7 did not touch the module, and that was the largest risk to this
plan.** `07-05-SUMMARY.md` says nothing inside `mod every_setting_is_acted_on`
changed: not one of the four tests, not `stored_setting_names`, not
`files_that_act`, and not one of the three exception lists. Verified here:
`OFFERED_BY_ANOTHER_SCREEN` holds three entries, `NOT_ANYTHING_ANYBODY_CHOOSES`
two, `STORED_AND_OFFERED_BY_NOTHING` one. **And phase 7's four remaining plans
name neither `src/data/config.rs` nor `src/presentation/wx_settings.rs` in their
`files_modified`.** Read out of their own frontmatter; the only mention of
either in those four documents is one line of 07-09 quoting a cost figure.

**3. `config.rs` is fingerprinted by five guard records, not four, and holds 63
tests.** 07-05 added the fifth. Taken 2026-09-12 with the `awk` from
`CLAUDE.md`. **Land every `config.rs` change this plan makes in one commit.**
The count check fires per file, so four tests cost the same remeasure as one,
which is five builds and five runs.

**4. That makes 07-05's own advice not apply here, and it is worth saying why.**
That plan homed its hand-named companion in `src/service/update_check.rs`
because `update_check.rs` was fingerprinted by nothing while `config.rs` was
fingerprinted by four, so a companion in `config.rs` would have cost the count
check. **This plan is already paying that cost**, because it has to edit the
exception lists and they are in `config.rs`. So the marginal cost of the
companion there is zero, and `config.rs` is where its two siblings live and
where a reader will look. Put it there.

**5. `Allowed` has three fields, so a per-account control is three answers, not
one.** From `src/application/allowed.rs:57-80`: `mail`,
`personal_information`, `reading`. The third is on by default and the other two
are off, and the doc comment explains why `Default` is written out rather than
derived. That is the substance of the checkpoint below.

**6. A per-account answer can only ever narrow.** Verbatim from
`src/data/config.rs:742-749`:

```rust
    pub fn allowed_for(&self, account_id: &str) -> crate::application::allowed::Allowed {
        self.allowed_per_account
            .get(account_id)
            .copied()
            .unwrap_or(self.allowed_changes)
            .and(self.allowed_changes)
    }
```

The `.and(self.allowed_changes)` is the whole constraint. A control that looks
like it can widen would be a control that lies.

**7. The two guards read different file sets, and this is what makes the remedy
a move rather than a deletion.**

| Test | Reads | Constant |
|---|---|---|
| `test_every_setting_somebody_can_change_is_offered_by_a_screen` | the settings screen only | `THE_SETTINGS_SCREEN = "src/presentation/wx_settings.rs"` |
| `test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing` | every file under `src/presentation` | `EVERY_SCREEN = "src/presentation"` |

So a control on the account manager reddens the second, which is the red half
for free, and deleting the exception entry then makes the **first** fail,
because it reads only `wx_settings.rs` and the name will not be there.

**8. `.planning/decisions-2026-09-06.md` decision 5 is not about this and could
be misread as settling it.** It begins "Allow Changes stays as it is, and the
move says so sooner", and it is about when a task move tells somebody what
happened, not about whether a per-account control exists. Verified by reading
`.planning/decisions-2026-09-06.md:29-34`. **Do not read it as an instruction to
leave the control unbuilt.**

**9. The guard in `tests/house_style.rs` that has to go, and what it costs.**
`test_nothing_offers_a_setting_per_account_that_no_screen_writes` at
`house_style.rs:210` reads `A_CONTROL_NO_SCREEN_WRITES` at `:180-183` and
refuses any product page saying "set it per account" or "for each account
separately". Once the control exists those sentences are true and the guard
forbids documenting the feature.

`tests/house_style.rs` is fingerprinted by **19** guard records and holds **69**
`#[test]` functions. Removing one moves the count and fires the check for all
nineteen, which is nineteen builds and nineteen runs. **That cost is real, and
it must not be dodged by inventing a replacement test to keep the number
level.** Choosing what to test by what keeps a count stable is the opposite of
what the count is for. Decide what is worth guarding, do that, and report the
cost.

**10. That guard has no companion, which is its own small finding.** Its
siblings in the same file mostly do: `test_the_forgotten_control_check_can_tell_the_two_apart`,
`test_the_fixed_answer_check_can_tell_the_two_apart`,
`test_the_encryption_check_can_tell_the_two_apart` and others. This one has
none, so nothing proves its reading can see a planted phrase. Say so in the
summary; it is going away, but the pattern is the one `CLAUDE.md` warns about
and the next document-reading guard somebody writes should carry one.

**11. `NOT_ANYTHING_ANYBODY_CHOOSES` has no check of its own, and that is
already recorded as `WINDOWS.md` 320 by 07-05.** Do not fix it here and do not
re-record it. It is named so that nobody reads this plan's emptying of a
different list as having covered it.
</premise_corrections>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: how much of Allow Changes does one account get, and how do phases 6 and 7 share the tree</name>
  <decision>
    Two questions, both yours, both cheap to ask together because they are met
    at the same moment.

    First: `Allowed` holds three answers, mail, personal information and
    reading. A per-account control can offer all three or one. Which?

    Second: phase 7 is executing beside this. The file collision the research
    called the largest risk to this phase is measured gone. What is left is
    smaller and still real.
  </decision>
  <context>
    **On the first question, the recommendation is all three.**

    | Option | What an account gets | What it costs |
    |---|---|---|
    | **All three** | The same three answers the Permissions tab offers, for one account | Three more controls on the account edit dialog, met in order by somebody moving through it with a screen reader. Honest: it is exactly what the model holds and exactly what `allowed_for` obeys |
    | One toggle | "This account may change less than the default" | One control. Cannot express what the model can hold, so a settings file with two of the three narrowed becomes unreachable again the moment somebody touches the toggle. That is the defect this plan exists to close, arriving by a different door |
    | Two | Mail separately, the other two together | Splits where the cost of being wrong differs, which is the argument the Permissions tab's own comment already makes: a sent message cannot be recalled, a task in the wrong place can be moved back. Reading is the odd one, because it is on by default and nothing it does is irreversible |

    **The deferred item was written when `Allowed` had fewer fields**, so "a
    control per account" in that entry does not mean three. It is worth knowing
    that the entry is not promising one shape.

    **Whatever is chosen, the control can only narrow.** `allowed_for` ends in
    `.and(self.allowed_changes)`, so an account can be told to do less than the
    application default and never more. The wording has to say that, and a box
    for something switched off application-wide has to read as unavailable
    rather than as an offer.

    **On the second question, the recommendation is to carry on and not wait.**

    Measured 2026-09-12 at `b8857bc8`, from phase 7's own plan frontmatter:

    | File | This phase | Phase 7 |
    |---|---|---|
    | `src/data/config.rs` | this plan | **none of 07-06 to 07-09** |
    | `src/presentation/wx_settings.rs` | 06-02 | **none of 07-06 to 07-09** |
    | `src/presentation/wx_app.rs` | 06-05 | 07-09 |
    | `tests/house_style.rs` | this plan, possibly 06-07 | 07-06 |
    | `guards/guards.toml` | all eight | 07-07, 07-08, 07-09 |
    | `docs/changelog.md` | seven of eight | all four |
    | `.planning/WINDOWS.md` | all | all |

    `07-05` merged at `d83bed3` and changed nothing inside the module this plan
    edits. So the collision that mattered is gone and what is left is two source
    files and three shared documents.

    | Option | What it means |
    |---|---|
    | **Carry on** | Phase 6 runs beside phase 7. Two source files want care: 06-05 and 07-09 both touch `wx_app.rs`, and this plan and 07-06 both touch `tests/house_style.rs`. Neither is the same region of either file |
    | Wait for phase 7 to finish | No conflicts at all, and phase 6 starts later by four plans |
    | Agree a split | Say now which of the two files each phase owns first |

    **What carrying on really costs**, so it is not a guess: a merge conflict in
    `tests/house_style.rs` if 07-06 and this plan both change it, which 07-06's
    own file list says it will. Its change is adding a workflow path to a list;
    this one removes a test and a constant. Different regions, so a conflict is
    unlikely and not impossible.
  </context>
  <options>
    - On the control: all three answers per account (recommended), one toggle, or two.
    - On the sharing: carry on (recommended), wait for phase 7, or agree a split now.
    - a combination, which is the usual answer here.
  </options>
  <resume-signal>
    Say how many answers per account, and say whether to carry on beside phase 7.
  </resume-signal>
  <acceptance_criteria>
    - Both answers are recorded in the summary with the date.
    - Where the answer is fewer than three, the summary says what the model can still hold that no screen can now reach, and a ledger entry records it, because that is the same defect in a smaller shape.
  </acceptance_criteria>
  <done>How much of a permission one account gets, and whether this runs beside phase 7, are decided by the person whose program it is.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: the control, on the screen that lists accounts</name>
  <files>src/presentation/wx_account_manager.rs, src/data/config.rs, guards/guards.toml, Cargo.toml</files>
  <read_first>
    - `src/data/config.rs:280-310`, `allowed_changes` and `allowed_per_account` with their doc comments, and `default_allowed` at `:540`.
    - `src/data/config.rs:742-752`, `allowed_for` in full.
    - `src/application/allowed.rs:57-130`, the `Allowed` struct, all three field doc comments, its hand-written `Default` and the `and` method.
    - `src/presentation/wx_settings.rs:1682-1760`, `build_permissions_tab`, which is how the application-wide answers are offered today and whose comments carry the reasoning to reuse.
    - `src/presentation/wx_account_manager.rs:986-1060`, `AccountEditWidgets` and the comment about which fields are `pub` and why.
    - `src/presentation/wx_account_manager.rs:1230-1320`, `build_account_edit_dialog`, far enough to see how a control is built and named there.
    - `src/data/config.rs:2340-2420`, both hand-named companions, so the new one matches their shape.
    - The checkpoint's answers.
  </read_first>
  <behavior>
    - The account edit dialog offers whatever the checkpoint decided, per account, with a real label on each control.
    - Each control shows the answer stored for that account, and where there is no stored answer it shows the application-wide one, which is what `allowed_for` would give.
    - An answer for something switched off application-wide reads as unavailable rather than as an offer, because `allowed_for` can only narrow.
    - Saving writes into `allowed_per_account` under the account's id, and an answer identical to the application-wide one writes nothing rather than a redundant entry.
    - Cancelling writes nothing.
    - `allowed_for` is unchanged and still narrows.
    - A settings file written before this change is read unchanged.
  </behavior>
  <action>
    Red first, on a branch. **The first red is free and it is the point of the
    whole mechanism**: naming `allowed_per_account` anywhere in the shipping half
    of a file under `src/presentation` reddens
    `test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing`,
    because that test reads `EVERY_SCREEN`, which is the whole directory. Let it
    go red, quote the failure, and name it in the `Fails-until-green:` trailers
    alongside the tests you wrote.

    Adding tests to `config.rs` also fires the count check for its five records.
    Name that in the trailers too, with the reason, and **land every `config.rs`
    change this plan makes in one commit**, this task's and task 2's both, so the
    remeasure is paid once.

    Build the control on the account edit dialog, not on the settings screen.
    That is where the precedent points: `OFFERED_BY_ANOTHER_SCREEN` already names
    `src/presentation/wx_account_manager.rs` for `directories` and
    `default_account_id`, with a comment saying that both are per account so they
    belong on the screen that lists accounts. Reuse that reasoning in the new
    entry's comment.

    Build each control with a real label through `with_label`, never an empty
    label plus `set_accessible_name`. A control named only through
    `set_accessible_name` has a name on MSAA, which NVDA reads, and none on UI
    Automation, which Narrator reads.

    **Word it so it cannot lie.** The wording says that an account may be allowed
    less than the application default and never more. Where the application-wide
    answer is off, the per-account control is disabled and says why, rather than
    being shown as an offer somebody can take. Take the words for the section
    from `crate::application::allowed::SETTINGS_SECTION`, which is what
    `build_permissions_tab` already does and for the reason its comment gives:
    a person told to turn this on should read the words they were told.

    Write the hand-named companion in `config.rs`, beside its two siblings, on
    the four-assertion shape
    `test_whether_a_decorative_picture_is_announced_is_offered_by_a_screen` uses.
    It asserts the control is built from the real fields of `Allowed`, shows the
    stored answer, and is read back into `allowed_per_account`. **That companion
    is the reason the settings-screen guard being satisfied by a mention in a
    comment does not matter here**, which is the weakness 07-05's summary names.

    Do not change `allowed_for`. Report its diff as empty.

    Do not touch `NOT_ANYTHING_ANYBODY_CHOOSES`. It has no check of its own and
    that is `WINDOWS.md` 320 already, recorded by 07-05.

    Bump the version in `Cargo.toml`, minor, in this commit.
  </action>
  <verify>
    <automated>cargo test --lib -- data::config:: presentation::wx_account_manager:: application::allowed::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names `test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing` among the failures, with its output quoted, because that is the mechanism working.
    - `cargo test --lib -- data::config:: presentation::wx_account_manager:: application::allowed::` passes at green.
    - `scripts/check.sh` passes on every commit through the hook.
    - Every new control was built with `with_label`. Name each control and the label it carries.
    - A test proves that an answer equal to the application-wide one writes no entry, so the map does not fill with redundant rows.
    - A test proves a per-account answer cannot widen: set the application-wide answer off, set the per-account answer on, and assert `allowed_for` still says off.
    - The hand-named companion exists in `config.rs` and asserts all four things: the control is built, from the real fields, showing the stored answer, and read back.
    - `allowed_for` is unchanged. Report its diff as empty.
    - Every `config.rs` change, this task's and task 2's, is in one commit, and the summary says what the record count moved from and to for all five records.
  </acceptance_criteria>
  <done>A permission the program already obeys per account is one somebody can see and change.</done>
</task>

<task type="auto">
  <name>Task 2: empty the list, and do not leave a guard green and blind</name>
  <files>src/data/config.rs, tests/house_style.rs, docs/ALPHA_TESTING.md, guards/guards.toml, docs/changelog.md</files>
  <read_first>
    - `src/data/config.rs:2269-2330`, all three exception lists with their comments.
    - `src/data/config.rs:2500-2590`, all three of `test_every_setting_somebody_can_change_is_offered_by_a_screen`, `test_a_setting_said_to_be_offered_elsewhere_really_is` and `test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing`, in full, including every comment.
    - `tests/house_style.rs:175-245`, `A_CONTROL_NO_SCREEN_WRITES`, `a_line_and_the_one_after_it`, `the_pages_that_speak_for_the_product` and the test itself.
    - `07-05-SUMMARY.md`, the section headed "One thing phase 6 must not do by accident".
    - `CLAUDE.md`, the paragraph beginning "A census that asserts a floor is itself a guard", and the paragraph about emptying an exception list disarming the guard watching it.
    - `docs/ALPHA_TESTING.md`, to see which pages describe what may be changed at a server.
  </read_first>
  <behavior>
    - `STORED_AND_OFFERED_BY_NOTHING` is empty, or the entry has moved out of it.
    - `test_every_setting_somebody_can_change_is_offered_by_a_screen` still passes, because the exception moved into `OFFERED_BY_ANOTHER_SCREEN` rather than being deleted.
    - `test_a_setting_said_to_be_offered_elsewhere_really_is` now covers the new entry, so the control being taken away later would be caught.
    - No guard is left iterating an empty list and passing unconditionally.
    - A product page may describe the new control, and the check that forbade it is gone rather than worked around.
  </behavior>
  <action>
    **Nothing here is written test first, and that is said rather than assumed.**
    Every edit in this task is inside a test module or a document: an exception
    list moving between two constants, a test being retired, and a page being
    written. No production behaviour changes, so there is no behaviour to specify
    in advance, and `CLAUDE.md`'s documentation exception covers the rest. The
    behaviour this task's move exists to support was written test first in task 1.
    Say this in the summary rather than letting it pass unremarked.

    **Move the entry, do not delete it.** Take `"allowed_per_account"` out of
    `STORED_AND_OFFERED_BY_NOTHING` and put it into `OFFERED_BY_ANOTHER_SCREEN`
    as `("allowed_per_account", "src/presentation/wx_account_manager.rs")`, with
    a comment in the voice of the three already there saying why it lives on the
    screen that lists accounts. Deleting it instead makes
    `test_every_setting_somebody_can_change_is_offered_by_a_screen` fail, because
    that test reads only `wx_settings.rs`.

    **Then decide what happens to the guard watching the emptied list, and do it
    in the same commit.** With `STORED_AND_OFFERED_BY_NOTHING` empty,
    `test_a_setting_recorded_as_offered_by_nothing_is_still_offered_by_nothing`
    iterates nothing, `now_offered` is empty and the assertion passes
    unconditionally. Two honest answers, and the summary says which was taken and
    why:

    - Retire the test with the list. Correct while there is nothing to record,
      and it has to come back when a setting is next recorded as offered by
      nothing.
    - Keep the list and the test, and give the test a companion that plants a
      name in a copy of the list and asserts the reading reports it. That keeps
      the mechanism armed for the next entry at the cost of a companion.

    Take whichever, and **prove it**. If the test is retired, say so in the
    summary and in a comment where the list was, so the next person adding an
    entry knows the test has to come back with it. If it is kept, the companion
    ships in the same commit, and a walk over an empty list that passes is not a
    companion.

    **Retire `test_nothing_offers_a_setting_per_account_that_no_screen_writes`
    and `A_CONTROL_NO_SCREEN_WRITES` in `tests/house_style.rs`.** The sentences
    it forbids are now true, and a check that refuses a true sentence is a check
    that stops the feature being documented.

    **Report what that costs and do not dodge it.** `tests/house_style.rs` is
    fingerprinted by nineteen guard records and holds sixty-nine `#[test]`
    functions today. Removing one moves the count and fires
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held` for all
    nineteen. Run the remedy the commit prints, detached:

    ```bash
    scripts/guards.sh --remeasure "<every name it prints>"
    ```

    **Do not invent a replacement test to keep the count level.** If a
    replacement guard is genuinely worth having, because the pages describing the
    new control should stay true, write it on its merits and say so. If it is
    not, let the count move and pay the remeasure. Choosing what to test by what
    keeps a number stable is the opposite of what the number is for, and the
    summary must say which of those two happened.

    Say in the summary that the retired guard had no companion, unlike most of
    its siblings in that file, so nothing ever proved its reading could see a
    planted phrase. It is going away either way; the pattern is worth recording.

    Document the control on whichever product page describes what may be changed
    at a server, in plain language, saying it can only narrow the
    application-wide answer. Keep it short.

    Write the `[Unreleased]` changelog entry. Say what the control does, say it
    can only narrow, and say under Known limitations that nobody has heard it and
    that none of the sync paths it governs has ever run against a real account.
  </action>
  <verify>
    <automated>cargo test --lib -- data::config::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib -- data::config::` passes, and `cargo test --test house_style` passes.
    - `scripts/check.sh` passes on every commit through the hook.
    - `grep -n 'STORED_AND_OFFERED_BY_NOTHING' src/data/config.rs` shows the list empty or gone, and the summary quotes the new text.
    - `grep -n 'allowed_per_account' src/data/config.rs` shows it inside `OFFERED_BY_ANOTHER_SCREEN` with the account manager beside it.
    - No guard in this repository now iterates an empty list and passes. Name which of the two answers was taken and quote the code that proves it.
    - `grep -rn 'A_CONTROL_NO_SCREEN_WRITES' tests/` finds nothing.
    - The `#[test]` count in `tests/house_style.rs` is reported before and after, and the summary says whether nineteen records were re-measured and what each moved from and to, or why the count did not move.
    - The summary says explicitly whether a replacement test was written on its merits or not written at all, and confirms no test was written to keep a count level.
    - A `[Unreleased]` entry exists naming the control and its limitation.
  </acceptance_criteria>
  <done>The list that recorded a live defect is empty, the guard watching it is either retired or armed, and the feature can be written about.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a per-account answer to what a provider client is allowed to do | this permission is honoured all the way out to code that sends, deletes and overwrites data at somebody's provider |
| a stored settings file to that answer | a file written by an older build, or by hand, decides what an account may change |
| what the control shows to what the program will do | the whole defect being closed is that the program behaves in a way the person cannot see |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Elevation of privilege | a control that appears to let one account do more than the application default | critical | mitigate | `allowed_for` ends in `.and(self.allowed_changes)` and is unchanged, so the model cannot widen. A test sets the application-wide answer off and the per-account answer on and asserts `allowed_for` still says off. The control is disabled and says why where the application-wide answer is off. |
| T-06-14 | Repudiation | a screen showing an answer the program does not obey | high | mitigate | the hand-named companion asserts the control shows the stored answer and is read back, which is the four-assertion shape the two siblings already use. |
| T-06-15 | Tampering | a settings file with a per-account entry nobody can see | high | mitigate | this is the defect being closed. After this plan every entry in `allowed_per_account` has a control, and `test_a_setting_said_to_be_offered_elsewhere_really_is` catches the control later being taken away. |
| T-06-16 | Repudiation | a guard left green and blind after its list empties | high | mitigate | task 2, in the same commit as the emptying, with the answer named and proved. This is the failure `CLAUDE.md` describes at length and `07-05-SUMMARY.md` says is one commit away. |
| T-06-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added. `Cargo.toml` is touched only by a version bump and the diff is read rather than assumed. |
</threat_model>

<verification>
- `cargo test --lib -- data::config:: presentation::wx_account_manager:: application::allowed::` passes. One `--lib` with several filters after `--`.
- `cargo test --test house_style` passes.
- `scripts/check.sh` passes on every commit through the hook. Never pipe it.
- Every `src/data/config.rs` change is in one commit, so its five records are re-measured once.
- `tests/house_style.rs` is touched once, and 07-06 also touches it. Rebase rather than merge if both land close together, and say in the summary whether a conflict happened and where.
- `Cargo.toml` is touched once, by a minor version bump beside the change, so that commit answers `affected`.
- `.planning/WINDOWS.md` gains at least two entries: the new control has not been heard, and none of the sync paths this permission governs has ever run against a real account. Add them through `gsd-tools windows`.
</verification>

<success_criteria>
- `STORED_AND_OFFERED_BY_NOTHING` is empty and nothing is left green and blind behind it.
- A permission stored per account and honoured out to the provider clients has a labelled control, and a hand-named companion proves the control is built from the real fields and read back.
- The control cannot widen, proved by a test rather than by a comment.
- The check that forbade documenting this is gone, and the documentation exists.
</success_criteria>

<output>
Create `.planning/phases/06-how-the-application-speaks/06-04-SUMMARY.md` when done
</output>
