---
phase: 06-how-the-application-speaks
plan: 07
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - docs/wcag-coverage.md
  - docs/accessibility.md
  - docs/IMPLEMENTATION_STATUS.md
  - docs/principles.md
  - .github/workflows/accessibility.yml
  - src/presentation/what_the_scans_can_judge.rs
  - src/presentation/mod.rs
  - .planning/REQUIREMENTS.md
  - guards/guards.toml
  - docs/changelog.md
autonomous: false
requirements: [FEEDBACK-03]
estimate:
  tokens: 14000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Somebody can read which WCAG 2.2 AA success criteria the automated scans can produce a finding against, and which they cannot, as a list rather than as a fraction."
    - "The four product-facing sentences claiming the scans cover roughly half of WCAG are corrected, and the two that say half of accessibility defects are left alone because they are defensible."
    - "The list says which windows are outside the scan and which criteria only a person can judge, so a clean scan is never read as a clean bill of health."
    - "The reading that checks the list has been shown to catch a planted wrong criterion, so it is not a walk over obedient text that passes whatever it finds."
    - "Where a regulation rather than WCAG decided a criterion does not apply to desktop software, the regulation is named."
  artifacts:
    - "docs/wcag-coverage.md: 55 success criteria, what each scan channel can say about each, and what is left for a person."
    - "src/presentation/what_the_scans_can_judge.rs: the three criteria the scan can judge held as code, and a reading that holds the document to them, with a companion."
    - "docs/accessibility.md, docs/IMPLEMENTATION_STATUS.md, docs/principles.md and .github/workflows/accessibility.yml: four corrected sentences."
  key_links:
    - "the three WCAG criteria in the code -> the document: a document alone goes stale the way the five WebView2 findings did. A check alone is not what somebody reads. The list is in the document and the three criteria it rests on are in the code, and the check holds them together."
    - "the scan's target list -> the coverage document: 06-06 decided how many windows are scanned. A coverage document that does not say which windows are outside it is dishonest whatever the criteria table says."
    - "scripts/msaa-names.ps1's header -> the document: it is the strongest in-repo statement of what a UI Automation scan can and cannot say about a native control, and quoting it is better than paraphrasing it."
    - "WCAG2ICT -> the applicability column: WCAG2ICT does not decide what applies. It records what Section 508 and EN 301 549 decided, and those two lists differ from each other, so the column names the regulation."
---

<objective>
Turn "automated scanning covers roughly half of WCAG" into a list of
fifty-five criteria saying, for each, what the scans can produce a finding
against and what only a person can judge.

Purpose: success criterion 3 and FEEDBACK-03. The figure in the tree is not
merely imprecise. It is about the proportion of accessibility defects an
automated tool finds, and four product-facing copies state it as the proportion
of success criteria a scan can judge, which is wrong by about an order of
magnitude.

Output: one document, four corrections, and a check that has been shown to
catch a wrong entry.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-how-the-application-speaks/06-RESEARCH.md
@.planning/phases/06-how-the-application-speaks/README.md
@.planning/phases/06-how-the-application-speaks/06-06-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Checked against the working tree on 2026-09-12 at `b8857bc8`, command first. The
two counts below must be **re-derived by the executor, not copied from here**,
and the reason is that this exact figure has now been got wrong four times by
four readers, including twice in this project's own research.

**1. Re-derive the rule count, and check that the parts sum to the whole.** The
2026-09-06 research reported 144 rules with a breakdown of 81 / 28 / 19 / 14 /
13, which sums to 155, and nobody added it up for five days. A language model
asked the same question answered 173. The 2026-09-12 research's own first parse
answered 141, because a filter written to skip the header row (`$1 !~ /^Name/`)
also skipped fourteen data rows whose first column was the name of a rule
beginning with "Name".

```bash
curl -sL https://raw.githubusercontent.com/microsoft/axe-windows/main/docs/RulesDescription.md \
  -o axe-rules.md
awk -F'|' '$0 ~ /\|/ && $0 !~ /^Name \|/ && $0 !~ /^-+ \|/ && $0 !~ /^#/ \
  {s=$NF; gsub(/^[ \t]+|[ \t]+$/,"",s); c[s]++; n++} \
  END {for(k in c) printf "%4d  %s\n", c[k], k; printf "TOTAL %d\n", n}' axe-rules.md
```

The answer on 2026-09-12 was **155**, split 61 WCAG 1.3.1, 53 Section 508
502.3.1, 23 Section 508 502.3.10, 9 WCAG 4.1.2, 9 WCAG 2.1.1. Sixty-one plus
fifty-three plus twenty-three plus nine plus nine is 155. **If your parse does
not sum to its own total, it is wrong. Do not report it.**

**If 06-06 pinned the CLI, read the rule table from the pinned tag rather than
from `main`**, because the document is a claim about what the scan runs.

**2. Re-derive the denominator too, and expect 55 rather than 56.** Parse
`https://www.w3.org/TR/WCAG22/`, taking each section that carries a
`Success Criterion N.N.N` heading and reading the `(Level X)` marker from its
text. On 2026-09-12 that gave 87 sections, A=31, AA=24, AAA=31, summing to 86,
with no level found for **4.1.1 Parsing**, which the page itself marks
"(Obsolete and removed)" and says "no longer has utility and is removed". So
Level A and AA conformance is **31 + 24 = 55**, not the 56 commonly published,
which counts 4.1.1 because it was Level A in WCAG 2.0 and 2.1.

**3. The MSAA walk judges one thing, and the document should say which third of
which criterion.** From `scripts/msaa-names.ps1`, it builds a record with
`name`, `role` and `operated` and exits 1 if any element whose MSAA role is in
its `$OPERATED` map has an empty `accName`. It never looks at role correctness or
state. So it contributes to 4.1.2 Name, Role, Value, and to the **Name** third of
it only.

**4. Quote the PowerShell header rather than paraphrasing it.** It is the
strongest statement in this repository of what criterion 3 is about, and it says
something a coverage document has to carry: that the UI Automation scan "is wrong
in both directions on native controls: it reports a missing name where the name
is in fact present and spoken, and it would report nothing amiss if every
`set_accessible_name` call in the tree were deleted."

**5. Where the four wrong copies are, re-taken 2026-09-12.**

```
grep -rn "roughly half\|about half" docs/ .github/ src/ CLAUDE.md
```

| File and line | Form | What to do |
|---|---|---|
| `docs/accessibility.md:12` | "catches roughly half of what WCAG asks for" | **correct** |
| `docs/IMPLEMENTATION_STATUS.md:177` | "covers roughly half of WCAG" | **correct** |
| `docs/principles.md:67` | "covers roughly half of WCAG" | **correct** |
| `.github/workflows/accessibility.yml:247` | the step summary: "covers about half of WCAG" | **correct** |
| `.github/workflows/accessibility.yml:14` | "catch roughly half of accessibility defects" | leave. Defensible |
| `CLAUDE.md:705` | "catch roughly half of accessibility defects" | leave. Defensible, and out of this plan's scope |
| `docs/changelog.md:10621` | "It covers roughly half of WCAG" | history. See below |

Two other matches are unrelated to accessibility and must not be touched:
`src/service/spellcheck/mod.rs:303` ("disagree about half the words") and
`src/service/update_check.rs:1279` ("about half an hour of re-measurement").
**The research's line numbers for five of these had already moved. Take them
again.**

**6. The changelog copy is history and editing it is the wrong move.**
`docs/changelog.md:10621` sits in a released-version note describing what was
true when it was written. Add a dated correction under `[Unreleased]` rather than
rewriting a shipped entry. Note that
`test_no_changelog_list_is_introduced_by_a_count_that_disagrees_with_it` and
`test_the_changelog_does_not_claim_in_prose_that_a_list_is_everything` both read
this file, so a new entry introducing a count has to agree with its own list.

**7. WCAG2ICT records what regulations decided; it does not decide.** Verbatim
from `https://www.w3.org/TR/wcag2ict-22/`, a W3C Group Note of 11 December 2025:
"This document does not seek to determine which WCAG 2 provisions [...] should
or should not apply to non-web documents and software, but rather, if applied,
how they would apply."

What it records splits two ways, and the split matters to a document claiming to
be honest:

| Criteria | Excluded for non-web by |
|---|---|
| 2.4.1 Bypass Blocks, 2.4.5 Multiple Ways, 3.2.3 Consistent Navigation, 3.2.4 Consistent Identification | Section 508 **and** EN 301 549 |
| 2.4.2 Page Titled, 3.1.2 Language of Parts | EN 301 549 only |

The 2026-09-06 research described WCAG2ICT as determining what does not apply.
It says it does not. **Attribute these to the regulations by name**, and say that
this project makes no Section 508 conformance claim, which
`docs/accessibility.md:18-19` already says.

**8. Costs, taken 2026-09-12.** `tests/house_style.rs` carries **19** guard
records and **69** tests; `src/presentation/scan_target.rs` carries **0** and
holds 6, unless 06-06 added one. A new module carries 0. **Nothing in this plan
goes in `tests/house_style.rs`.**

**9. This plan touches `.github/workflows/accessibility.yml`, so after 06-06 its
commits answer `all`.** That is the rule 06-06 built working. Keep the workflow
edit in its own commit so the document commits stay `docs_only`, which is the
one-kind-of-file-per-commit rule and is worth several minutes a commit here.
</premise_corrections>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: is the coverage list a document, a check, or both, and does REQUIREMENTS.md get corrected</name>
  <decision>
    Two questions, asked together because they are both about where a written
    claim lives and what keeps it true.

    First: should the coverage list be a document, a check in the code, or both?

    Second: three of `.planning/REQUIREMENTS.md`'s evidence lines are wrong about
    the code as it stands. Correct them in place, or record the corrections in
    the plans the way phases 3 and 4 did?
  </decision>
  <context>
    **On the list, the recommendation is both**, with the document holding the
    fifty-five rows and the code holding the three criteria the whole thing rests
    on.

    | Option | What it gives | What it costs |
    |---|---|---|
    | A document only | What somebody reads. Fifty-five rows, honest about every one | Goes stale exactly the way the five WebView2 findings did: that count was written when the scan covered one window on one channel and it now covers eleven on two, and nothing said so for seven weeks |
    | A check only | Cannot go stale | Nobody reads it, and criterion 3 asks for the scan output to name the criteria, which is prose |
    | **Both** | The rows are in the document and the three criteria are in the code, and a check holds the document to them | A guard record, a companion, and one more thing to keep in step. The companion is not optional: a list-reading guard with an empty or narrowed list passes unconditionally, which this repository has fallen into twice |

    **What "both" means concretely.** The three criteria the scan can produce a
    finding against, 1.3.1, 2.1.1 and 4.1.2, live in code. The document's table
    has to agree with them, and a reading checks that. The other fifty-two rows
    are prose and a person's judgement, and no check can hold those.

    **On `REQUIREMENTS.md`, the recommendation is to correct it in place**, which
    is a change from what phases 3 and 4 did, and the reason is that these three
    are wrong about the code rather than merely out of date.

    | What is wrong | Where |
    |---|---|
    | FEEDBACK-01's evidence calls `per_event` a `fn`. It is a struct field, so "make it public" is not a coherent instruction | FEEDBACK-01 |
    | FEEDBACK-02's evidence names one file where four shipping sites write an English month or day name | FEEDBACK-02 |
    | FEEDBACK-03 rests on "roughly half", which is the figure this plan disproves | FEEDBACK-03 |

    | Option | What it gives | What it costs |
    |---|---|---|
    | Record in the plans, as phases 3 and 4 did | The requirement stays as written, which is a record of what was asked for | The wrong sentences stay in the document somebody reads next, and the correction lives in a plan summary nobody opens |
    | **Correct in place, with the date and what it used to say** | The document somebody reads is true | `.planning/REQUIREMENTS.md` is also named by phase 7's plan 07-07, so two phases may edit it close together |

    **What neither settles.** Whether the coverage list is right. Nobody has
    walked fifty-five criteria against this application, and writing the list is
    a judgement about applicability as well as a derivation.
  </context>
  <options>
    - On the list: a document only, a check only, or both. Recommended: both.
    - On the requirements: record in the plans, or correct in place. Recommended: correct in place.
    - a combination, which is the usual answer here.
  </options>
  <resume-signal>
    Say whether the list is a document, a check or both, and whether
    `REQUIREMENTS.md` is corrected in place.
  </resume-signal>
  <acceptance_criteria>
    - Both answers are recorded in the summary with the date.
    - Where the list is a document only, a ledger entry records that nothing holds it to the code and names what will make it stale first.
  </acceptance_criteria>
  <done>Where the coverage claim lives, and whether the requirements are corrected, are decided by the person whose program it is.</done>
</task>

<task type="auto">
  <name>Task 1: count both numbers again, and write the list</name>
  <files>docs/wcag-coverage.md, docs/changelog.md</files>
  <read_first>
    - `06-06-SUMMARY.md`, for the pinned Axe.Windows tag if there is one and for the nine unscanned dialogs by name.
    - `scripts/msaa-names.ps1` in full, especially its header and the `$OPERATED` map, so the document says exactly what the MSAA walk asks.
    - `.github/workflows/accessibility.yml`, the scan loop and the summary step, so the document describes what really runs.
    - `docs/accessibility.md` in full, which is the page this one sits beside and must not contradict.
    - `nvda-tests/tests/` and `nvda-tests/README.md`, for what the NVDA suite covers: three tests that run and one `test.skip` whose header says why.
    - `CLAUDE.md`, the paragraph about a guard whose trigger is "a document mentions X" being disarmed by the workaround it recommends.
  </read_first>
  <behavior>
    - The document lists every WCAG 2.2 Level A and AA success criterion, counted rather than quoted.
    - For each, it says whether Axe.Windows can produce a finding against it, whether the MSAA walk can, whether the NVDA suite can, and what is left for a person.
    - Where a criterion does not apply to desktop software, the regulation that decided that is named, not WCAG2ICT.
    - It says which windows the scan does not reach, by name.
    - It says which version of the scanner it was written against, and the date.
    - It does not claim the manual pass has happened.
  </behavior>
  <action>
    **Count both numbers yourself before writing a word**, with the two commands
    in the premise corrections, and put the raw output of each in the summary.
    This figure has been got wrong four times by four readers and the thing that
    catches it every time is checking that the parts sum to the whole. If your
    parse does not sum to its own total, it is wrong; find out why before
    reporting it.

    If 06-06 pinned the scanner, read the rule table from that tag rather than
    from `main`. The document is a claim about what the scan runs, and reading
    `main` makes it a claim about something else.

    Write `docs/wcag-coverage.md`. Invoke the `writing-craft` skill: plain
    language, semantic structure, no em dashes, no AI-slop vocabulary.

    Six columns, the shape the research proposes:

    | Column | Where it comes from |
    |---|---|
    | Success criterion, A and AA only, fifty-five of them | WCAG 2.2, counted this session |
    | Does it apply to a Windows desktop application? | the regulation, named, per WCAG2ICT's record of what Section 508 and EN 301 549 decided |
    | Can Axe.Windows produce a finding against it? | yes for 1.3.1, 2.1.1 and 4.1.2; no for the other fifty-two |
    | Can the MSAA walk? | the Name third of 4.1.2 and nothing else |
    | Can the NVDA suite? | the three interactions its three running tests touch, named |
    | Otherwise | a person, or nothing |

    **Say what a rule count is not.** Sixty-one rules derived from 1.3.1 do not
    add up to judging 1.3.1: each tests one narrow structural property. Say that
    in the document, because the whole failure this page exists to correct is
    reading a number of rules as a proportion of coverage.

    **Quote `scripts/msaa-names.ps1`'s header** rather than paraphrasing it, in
    particular the sentence about the UI Automation scan being wrong in both
    directions on native controls. It is the clearest thing anybody here has
    written about what these scans mean.

    **Name the windows the scan does not reach**, from 06-06's summary, and say
    that a criterion answered "yes" is answered yes only for the windows that are
    scanned.

    **Say the version and the date.** If 06-06 pinned the scanner, name the tag.
    If it did not, say the date the rule table was read and say that the rule set
    can change with no commit in this repository.

    **Say what has not happened.** No manual pass, no listening pass over most of
    the application, and no Section 508 conformance claim.

    Write the `[Unreleased]` changelog entry. Point at the new page and add the
    dated correction for the shipped entry at `docs/changelog.md:10621`: say that
    it said the scan covers roughly half of WCAG, that the figure is about
    defects rather than criteria, and what the real answer is. **Do not rewrite
    the shipped entry.** If the new entry introduces a count, make the count agree
    with its own list, because two tests in `tests/house_style.rs` read this file
    for exactly that.

    This is a documents-only commit. Keep it that way and it answers `docs_only`.
  </action>
  <verify>
    <automated>cargo test --test house_style --test docs_links</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test house_style --test docs_links` passes.
    - `scripts/check.sh` passes for this commit through the hook, and the summary confirms it answered `docs_only`.
    - The raw output of both counting commands is in the summary, with the date, and the rule breakdown sums to its own stated total. Say so explicitly.
    - The denominator is 55 and the summary says why it is not 56, naming 4.1.1.
    - Every one of the fifty-five criteria has a row. Count the rows and say the number.
    - The six criteria a regulation excludes are attributed to Section 508 or EN 301 549 by name, split the two ways they really split, and not to WCAG2ICT.
    - The document names the scanner version or the date it was read, and says which it is.
    - The nine unscanned dialogs are named.
    - `scripts/msaa-names.ps1`'s header is quoted rather than paraphrased.
    - No em dashes anywhere in the new file. Check with a byte search, not a visual one.
    - The changelog entry corrects the shipped line by addition and does not rewrite it.
  </acceptance_criteria>
  <done>Fifty-five criteria, each with what the scans can and cannot say about it, written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: four corrected sentences, and a reading that can see a wrong one</name>
  <files>docs/accessibility.md, docs/IMPLEMENTATION_STATUS.md, docs/principles.md, .github/workflows/accessibility.yml, src/presentation/what_the_scans_can_judge.rs, src/presentation/mod.rs, guards/guards.toml</files>
  <read_first>
    - The four sites, each in its surrounding paragraph, so the replacement fits what is around it.
    - `.github/workflows/accessibility.yml:206-250`, the whole summary step, so the corrected sentence sits in the flow of the others.
    - `tests/house_style.rs:2509-2570`, `test_no_document_says_the_cache_is_encrypted` and its companion, as the pattern for a document-reading check with a companion that proves the reading works.
    - `src/data/config.rs:2340-2420`, both hand-named companions, for the shape of a check that reads the shipping half of a file.
    - `CLAUDE.md`, the paragraph beginning "A guard whose trigger is 'a document mentions X' is disarmed by the workaround it recommends", in full.
    - Task 1's document.
  </read_first>
  <behavior>
    - None of the four product-facing sentences says the scans cover a fraction of WCAG.
    - The two sentences about a fraction of accessibility defects are unchanged, because that claim is about defects and is defensible.
    - The check, where the checkpoint asked for one, holds the coverage document's "yes" rows to the three criteria the code names.
    - The companion proves the reading reports a planted wrong criterion and a planted missing one, in both directions.
    - The check does not pass when its list is empty.
  </behavior>
  <action>
    Red first where there is a check to write. If the checkpoint chose a document
    only, this task is prose and the exception is stated rather than assumed.

    **Correct the four sentences.** Each replacement says the same true thing in
    the voice of the page it is on, and each points at
    `docs/wcag-coverage.md` rather than repeating its table. The shape: the scans
    can produce a finding against three of the fifty-five Level A and AA success
    criteria, the widely quoted "about half" is about the proportion of
    accessibility defects an automated tool finds rather than the proportion of
    criteria it can judge, and the difference is about an order of magnitude.

    **Leave the two "of defects" sentences alone**, at
    `.github/workflows/accessibility.yml:14` and in `CLAUDE.md`. They are saying
    the defensible thing. Report that you read both and left them, so the next
    person does not think they were missed.

    **Keep the workflow edit in its own commit.** After 06-06 that commit answers
    `all` and the three document commits answer `docs_only`, and mixing them
    makes every one of them pay for the workflow.

    Write `src/presentation/what_the_scans_can_judge.rs`, or whatever the
    checkpoint's answer requires. Hold the three criteria the Axe.Windows rule
    set draws from, and the one third of one criterion the MSAA walk covers, as
    code. Read `docs/wcag-coverage.md` and check that the rows saying the scan
    can produce a finding are exactly those, no more and no fewer.

    **Both directions, because a reading that reports everything is as useless as
    one that reports nothing.** A row claiming a fourth criterion is caught. A
    row that should say yes and says no is caught.

    **Write the companion in the same commit, and make it plant a violation.**
    Feed the reading a fixture document with a fourth criterion marked as
    scannable and assert it reports it. Feed it one with 2.1.1 marked as not
    scannable and assert it reports that too. `CLAUDE.md` gives the reason at
    length: a guard that reads documents needs something proving the reading can
    see a violation when one exists, and the version of this guard that reads a
    document and finds nothing to complain about is indistinguishable from one
    that has been narrowed until it cannot see.

    **Make the empty case fail rather than pass.** If the document has no rows at
    all, or the code's list is empty, the check fails and says which. That is the
    census-emptying failure this project has already met twice, and the
    protection is one assertion.

    Put the module under `src/presentation/`, not in `tests/house_style.rs`. That
    file is fingerprinted by nineteen guard records and holds sixty-nine tests; a
    new module is fingerprinted by nothing. That is the technique 07-05 recorded
    and this is the case it was written for.

    **Write one guard record.** Its break: change one of the three criteria in
    the code without changing the document, or the reverse. Apply it by hand, run
    the whole library, write down what really went red, and quote the count.

    Where the checkpoint said to correct `.planning/REQUIREMENTS.md`, do it in
    this plan's last commit, with the date and what each line used to say beside
    the correction. Phase 7's plan 07-07 also names that file; say in the summary
    whether anything conflicted.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::what_the_scans_can_judge::</automated>
  </verify>
  <acceptance_criteria>
    - A red commit on the branch names every failing test in `Fails-until-green:` trailers, where a check was written.
    - `cargo test --lib -- presentation::what_the_scans_can_judge::` passes at green, and `cargo test --test house_style` passes.
    - `scripts/check.sh` passes on every commit through the hook. The workflow commit answered `all` and the document commits answered `docs_only`; say which commit answered what.
    - `grep -rn "roughly half of WCAG\|about half of WCAG\|half of what WCAG" docs/ .github/ src/` finds nothing outside `docs/changelog.md`, and the summary quotes the command and its output.
    - The two "half of accessibility defects" sentences are unchanged. Report that both were read and left, and say why.
    - The companion plants a violation in both directions and both assertions are in the same commit as the check.
    - A test asserts the check fails rather than passes when the document has no rows and when the code's list is empty.
    - One new record in `guards/guards.toml`, its red list measured by applying the break and running the whole library, with the count quoted.
    - Where `REQUIREMENTS.md` was corrected, each corrected line carries the date and what it used to say.
  </acceptance_criteria>
  <done>Nothing in this project says an automated scan covers half of WCAG, and the reading that holds the list to the code has been shown to catch a wrong entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an upstream rule table fetched over the network to a claim in a product document | a figure in a page somebody reads, taken from a file on somebody else's main branch |
| a document claiming coverage to somebody deciding whether to test by hand | the whole purpose of this page is that a clean scan is not read as a clean bill of health |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-25 | Repudiation | a product page saying automated scanning covers half of WCAG | high | mitigate | four corrections, with a grep in the acceptance criteria proving none is left, and the changelog copy corrected by addition rather than rewritten. |
| T-06-26 | Repudiation | a coverage document that goes stale the way the five findings did | high | mitigate | the three criteria live in code and a check holds the document to them, with a companion proving the reading sees a planted violation and a test proving the empty case fails. |
| T-06-27 | Tampering | a rule count read from an upstream file that can change with no commit here | medium | mitigate | read from the pinned tag where 06-06 pinned one, and the document names the version or the date. The count is re-derived by the executor and its parts must sum to its whole. |
| T-06-28 | Repudiation | attributing to WCAG2ICT a decision it says it does not make | medium | mitigate | the applicability column names Section 508 or EN 301 549, split the two ways they really split, with the W3C note quoted saying it does not decide. |
| T-06-SC | Tampering | npm, pip or cargo installs | high | mitigate | no package is added. The one network fetch is a `curl` of a markdown file, read and parsed, never executed. |
</threat_model>

<verification>
- `cargo test --lib -- presentation::what_the_scans_can_judge::` passes.
- `cargo test --test house_style --test docs_links` passes, and `tests/house_style.rs` is not edited. Report its diff as empty.
- `scripts/check.sh` passes on every commit through the hook. Never pipe it.
- The workflow commit answers `all` after 06-06's rule; the three document commits answer `docs_only`. Say which is which.
- `Cargo.toml` is not touched and no version bump is owed. A corrected sentence in a document is not a feature.
- `.planning/REQUIREMENTS.md` is also named by phase 7's plan 07-07. Rebase rather than merge if they land close together.
- `.planning/WINDOWS.md` gains at least three entries: fifty-two criteria only a person can judge and nobody has; the coverage list is a judgement about applicability that nobody has checked against this application; and the nine unscanned dialogs. Add them through `gsd-tools windows`.
</verification>

<success_criteria>
- `docs/wcag-coverage.md` holds fifty-five rows, each saying what each scan channel can and cannot judge, with the counts re-derived in this session and their parts summing to their whole.
- No product-facing sentence says an automated scan covers a fraction of WCAG.
- The reading that holds the document to the code has been shown a planted wrong entry in both directions and caught both, and fails rather than passes on an empty list.
- The document says which windows are outside the scan, which version it was written against, and that no manual pass has happened.
</success_criteria>

<output>
Create `.planning/phases/06-how-the-application-speaks/06-07-SUMMARY.md` when done
</output>
