---
phase: 05-the-other-five-modules-keep-up
plan: 06
type: execute
wave: 6
depends_on: ["05-03"]
files_modified:
  - tests/a_moved_task_is_in_one_list.rs
  - src/application/pim_command.rs
  - src/presentation/managers.rs
  - guards/guards.toml
  - docs/changelog.md
  - docs/IMPLEMENTATION_STATUS.md
  - docs/ALPHA_TESTING.md
  - .planning/REQUIREMENTS.md
  - Cargo.toml
autonomous: true
requirements: [PIM-01]
estimate:
  tokens: 108000
  raw_tokens: 105000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A task moved to another list is in that list and in no other, and a test built on the storage that really holds it says so, so criterion 1's local half is measured rather than assumed."
    - "A task a provider holds is refused a move before any window opens and before anything is written, and the sentence explaining why is the one somebody hears, not one only the log sees."
    - "When a move is written and cannot leave this computer yet, the person is told at the moment they moved it, rather than finding out one sync later or not at all."
    - "Where Allow Changes is off for that account, the sentence names the setting to turn on, using the same words the syncs already use for the same fact."
  artifacts:
    - "tests/a_moved_task_is_in_one_list.rs: a task moved through the write the running program uses, read back from storage, asserting one list and not two, and asserting the refusal for a provider-held task writes nothing at all."
    - "src/application/pim_command.rs: what a move says when the change is waiting to be sent, and what it says when a setting is holding it."
    - ".planning/REQUIREMENTS.md: PIM-01's `Allowed` criterion reworded to describe the gate where it really is, per decision 5, and its 'exactly one list' criterion left standing with `05-07` and `05-08` named as its owners."
  key_links:
    - "TaskEntry.task_list_id is one nullable column, so a task cannot be in two lists in this storage. 'Exactly one list' is therefore structurally true locally and the whole risk lives in the delete-then-create nobody has written. Say that plainly rather than letting a passing test read as coverage of the risk."
    - "file_under -> moving_can_be_told: file_under asks it again after the chooser, so no route can write a provider-held move. That is the second half of the refusal and it is what makes 'never in both and never in neither' true today by refusing rather than by handling."
    - "Allowed::personal_information -> the four places the HTTP client is built: tasks_api.rs, google_api.rs, microsoft_graph.rs, caldav.rs. grep -c allowed src/presentation/managers.rs returns 0, so no move is gated at move time and the gate bites at the push."
    - "allowed::changes_waiting_here -> the sync summaries: the sentence naming the setting already exists and is already said by the task, calendar and contacts syncs. Anything said at move time uses that sentence rather than a second one, because two copies of it drifted once already and only one was corrected."
---

<objective>
Make criterion 1's local half a measured fact, make the move say when nothing
has left the machine yet, and make PIM-01's own text say what is true.

Purpose: criterion 1 of phase 5. A task already moves between lists in one
action, from a menu and from a key, and `docs/IMPLEMENTATION_STATUS.md` and
`docs/ALPHA_TESTING.md` were corrected on 2026-09-04 to say so. What is left is
not the move. It is that "ends in exactly one list, including when the move
fails at the provider" is about a sequence nobody has written, that the criterion
about `Allowed::personal_information` describes a gate that lives somewhere else,
and that neither of those is a plan detail.

Output: the local half proved, the move honest about what has and has not
happened, and PIM-01's two criteria saying what is true.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-the-other-five-modules-keep-up/05-RESEARCH.md
@.planning/phases/05-the-other-five-modules-keep-up/05-03-SUMMARY.md
@CLAUDE.md
</context>

<premise_corrections>
Everything below was checked against `main` at `9611b70`: a clean checkout,
version `0.75.0`, `guards/guards.toml` holding 632 records, `rust-version`
`1.88`. The findings were first taken on 2026-09-06 against an earlier tree and
every figure was re-measured against this one when the set was assembled;
phases 4 and 4.2 landed in between, which is what moved the record counts. Each
claim below names the command that produced it so it can be re-run rather than
trusted, and a figure is only a figure with the tree it came from: if the tree
has moved again, re-run them.

**The Rust floor is 1.88 and it has already re-linted code no phase-5 plan
opens.** `04-09` moved it from 1.87 because rPGP declares 1.88, and clippy only
suggests a construct once the declared floor allows it, so the bump turned on
`collapsible_if`'s let-chain form and cost seven fixes across six files that
plan never opened. `main` is clean at that floor now, so nothing is owed. What
it means here is that new code in this plan is linted against 1.88 with
`-D warnings`, where a let-chain suggestion is a build failure rather than a
hint.

**1. "In exactly one list" is structurally true locally, and a test that passes
is not evidence about the risk the criterion is written for.**
`grep -n "pub struct TaskEntry" -A 20 src/data/message_cache/mod.rs` shows
`task_list_id: Option<String>`, one nullable column. `file_under` in
`managers.rs` reads the task, sets `task.task_list_id = Some(into.to_string())`,
sets `task.pending = true` and calls `cache.save_task(&task)`. One row, one
column, so two lists is not a state this storage can hold. **The whole risk the
criterion names is in the delete-there-create-here sequence that does not
exist.** Write the local test anyway, because it is the thing that would notice
if somebody later wrote the provider move by inserting a second row, and say in
the summary that it is a floor rather than the criterion.

**2. The provider-held refusal is asked twice, and the second time is what makes
it airtight.** `moving_can_be_told` is called from `where_it_could_go` before the
chooser opens, and again from `file_under` before any write. `file_under`'s doc
comment says why: asking before the chooser means nobody answers a question
whose answer is thrown away, and asking again is what makes it impossible to
write one of those moves by any route. So today, a provider-held task ends in
exactly one list by being refused, and the criterion is vacuously satisfied. It
stops being vacuous the moment somebody writes the provider move.

**3. `grep allowed src/presentation/managers.rs` returns nothing, and I ran it.**
`grep -c allowed src/presentation/managers.rs` returned `0`, and
`grep -in allowed src/presentation/managers.rs` returned nothing either, so it
is not a case difference. The gate is applied where the HTTP client is built:
`grep -rn "personal_information" src/ --include=*.rs` shows
`src/service/tasks_api.rs`, `src/service/google_api.rs`,
`src/service/microsoft_graph.rs` and `src/service/caldav.rs`, each of the form
`http: if allowed_for(account_id).personal_information { may_change_things } else
{ default }`. **A move is written locally and marked waiting whatever the gate
says.**

**4. But nobody is left uninformed, and `05-RESEARCH.md` does not say this.**
The push does not silently drop a change the setting refused. Read
`tasks_sync.rs` around `result.waiting_on_the_setting += 1`: a push refused by
the gate is counted rather than pushed into `errors`, with a comment saying why,
and the summary says
`crate::application::allowed::changes_waiting_here(count)`, which is
`"1 change is waiting here: turn on Allow Changes in Settings to send it"`. The
calendar and contacts syncs say the same sentence from the same function, and
its comment records that two copies of it drifted and only one was corrected.

**So the real state of PIM-01's second `[D]` line is: nobody is refused at move
time, and everybody is told at the next sync, in a sentence that names the
setting.** That is a materially different fact from "the criterion is not true",
and it is why decision 5 rewords the criterion rather than changing the
behaviour. Task 3 makes that edit.

**5. What is genuinely missing is the moment.** `pim_command::moved(name, into)`
says `"{title} moved to {into}"`, full stop. For a task in a synced list that is
only half of what happened: the row moved on this computer and nothing has
reached the provider. Somebody who cannot see the list has that sentence and
nothing else. Saying so at the moment of the move is the "and the move says so
sooner" half of decision 5, which is why task 2 does it and does not wait for
the provider move `05-08` builds.

**6. Both stale documents were already corrected, so do not re-plan that work.**
`grep -n -i "moving and copying" docs/IMPLEMENTATION_STATUS.md docs/ALPHA_TESTING.md`
finds `IMPLEMENTATION_STATUS.md` saying "This entry used to say that moving and
copying work for mail only. That stopped being true when the command was added
and nobody corrected it here", and `ALPHA_TESTING.md` carrying a known-limitation
entry that says the same. `05-RESEARCH.md` assumption A6 asks for a sweep; it has
been done for this claim. **What both documents now say about a task move will
become wrong again if this plan changes the sentence**, so they are on this
plan's file list for that reason, not for the old one.

**7. `05-RESEARCH.md` assumption A2 is not settled and cannot be settled here.**
A2 is that `Ctrl+Shift+V` really reaches the handler in the non-mail modules
rather than only appearing in a menu label. `tests/wired.rs`'s own header says a
bound key proves Windows will dispatch it and says nothing about what the
handler then does with the right thing on screen. Nothing in this repository can
answer it. Record it in `.planning/WINDOWS.md`, which ends at entry 192 on
2026-09-08 and further by now, so read its last row before choosing a number, rather than letting a green wiring test read as an answer.

**8. Guard-record cost, measured by parsing `tests_last_seen`, not by grepping
for a file name.** `05-RESEARCH.md` says `managers.rs` 68 and `tasks_sync.rs`
32; the real figures are 40 and 19. Measured on `main` at `9611b70`, which
holds 632 records:

| file | records fingerprinting it | test functions today |
|---|---|---|
| `src/application/pim_command.rs` | 3 | 18 |
| `src/application/tasks_sync.rs` | 19 | 106 |
| `src/presentation/managers.rs` | 40 | 137 |
| `src/application/allowed.rs` | measure it in task 2 before writing tests there | measure it |
| a new file under `tests/` | 0 | 0 |

The counting command is in `05-01-PLAN.md` premise correction 8, and the test
counts are counted the way `how_many_tests_are_in` in `tests/house_style.rs`
counts. **This plan has not measured `src/application/allowed.rs`; measure it
before putting a test there**, because a grep for the file name is what six
phase-3 plans got wrong.

**Put task 1's test in a new file under `tests/`, which is free. Put task 2's
words in `pim_command.rs`, which costs three. Add no `#[test]` to
`managers.rs` or `tasks_sync.rs`.**

**9. A guard living in `tests/` needs a `guards/guards.toml` record with
`suite = "<the file name without .rs>"`**, or it runs on every commit except
the ones that could break it. `CLAUDE.md` says so and two guards were invisible
to the gate for exactly this reason until 2026-09-02.

**10. Adding a record bumps the census on line 80 of `guards/guards.toml` in the
same commit.** 192 + 440 = 632 today. Read both lines rather than trusting these
two numbers.

**11. A commit touching `Cargo.toml` makes `scripts/which-checks.sh` answer
`all`.** Run those commits detached; the whole gate is around 311 to 353 seconds
warm.
</premise_corrections>

<tasks>

<task type="tracer">
  <name>Task 1: A moved task, read back from storage, is in one list and not two</name>
  <files>tests/a_moved_task_is_in_one_list.rs, guards/guards.toml, Cargo.toml, docs/changelog.md</files>
  <read_first>
    - `src/presentation/managers.rs`: `file_under`, `moving_can_be_told`, `where_it_could_go`, `held_in` and `move_item`, read as one path.
    - `src/data/message_cache/tasks.rs`: `save_task`, `pending_tasks`, `get_all_tasks_for_account`, `get_tasks_for_list`.
    - `src/data/message_cache/mod.rs`: `TaskEntry` in full, so it is clear that `task_list_id` is one column.
    - `src/application/tasks_sync.rs`: `a_provider_holds`, `Provider::made_here`, `Provider::prefix`, and `Provider::is_local`.
    - `src/application/pim_command.rs`: `cannot_be_moved` and its whole doc comment, which is the argument for the refusal.
    - One existing integration test for shape: `tests/the_conflict_choice_can_be_heard.rs`.
  </read_first>
  <action>
    This task measures what is already true and leaves behind something that
    would notice if it stopped being true. It adds no behaviour, so there is
    nothing for a failing test to demand; what replaces the red half is a
    measured break.

    Write `tests/a_moved_task_is_in_one_list.rs`. A new file under `tests/` is
    named by no guard record, so it is free, and this question spans the write
    in `presentation::managers` and the storage in
    `data::message_cache::tasks`.

    `file_under` is private. Do not make it public to test it: reach the same
    write through whatever public path the running program uses, or, if there is
    none reachable from an integration test, say so plainly and put the test
    where it can reach the behaviour without adding a `#[test]` to
    `managers.rs`. Report in the summary which route was taken and what the
    alternative would have cost. Do not widen a function's visibility for a
    test without saying so; that is a change to the shape of the code and wants
    a sentence.

    Assert three things, all through storage read back rather than through a
    value held in memory:

    - a task moved from one list to another is returned by `get_tasks_for_list`
      for the new list and by no other list, and `get_all_tasks_for_account`
      returns it exactly once
    - the moved task is in `pending_tasks`, because a move that does not mark
      the row is a move the sync never sends, which is the bug
      `docs/changelog.md` records at the entry about a move marking the item as
      waiting
    - a task a provider holds is refused, and after the refusal the task is
      still in the list it started in and `pending_tasks` has not grown

    Build the provider-held case from the id prefix, since that is what
    `Provider::made_here` reads, and assert `a_provider_holds` is true of the
    fixture before using it. A fixture that is not actually provider-held would
    make the refusal test pass for the wrong reason, which is the shape
    observation 0217 in this project's own log describes.

    Take the break by hand. Make `file_under` skip the second
    `moving_can_be_told` check, which is the half-fix somebody would write while
    tidying up a duplicated question, rather than deleting the refusal. Run
    `cargo test --all-targets --no-fail-fast` on a clean tree with nothing else
    building, and write down everything that really went red.

    Write the `guards/guards.toml` record with
    `file = "src/presentation/managers.rs"`,
    `suite = "a_moved_task_is_in_one_list"`, and the measured red list. Add
    `tests_last_seen` for every file its red list names and for the file it
    breaks. Bump the census on line 80.

    Write the `docs/changelog.md` entry under `[Unreleased]` and bump the
    version. Say what is now proved rather than what was built, and say the
    limit in the same sentence: a task moved on this computer is in one list and
    a test says so; a move that has to reach Google or Microsoft is still
    refused here, and that refusal is what makes the promise true until `05-08`
    builds the provider half.

    Say one more thing in the summary, because a green test reported without it
    reads as coverage of the risk. `TaskEntry.task_list_id` is one nullable
    column, so two lists is not a state this storage can hold. The local half of
    the criterion is therefore structurally true rather than carefully
    maintained, and this test is a floor under it rather than the criterion
    itself. The whole of the risk lives in the delete-there-create-here that
    `05-07` and `05-08` build.
  </action>
  <verify>
    <automated>cargo test --test a_moved_task_is_in_one_list</automated>
  </verify>
  <acceptance_criteria>
    - The named test target passes, and `scripts/check.sh` passes for this commit's file list.
    - The summary states plainly that "exactly one list" is structurally true locally because `task_list_id` is one column, and that this test is a floor rather than the criterion.
    - The test asserts `a_provider_holds` is true of the provider-held fixture before relying on the refusal.
    - The route taken to reach the write is named, and if any visibility was widened the summary says which and why.
    - The guard record's break is skipping the second check rather than deleting the refusal, was measured with `--no-fail-fast` on a clean tree, and its full red list is quoted.
    - The record carries `suite` and `tests_last_seen`, and line 80's census was corrected in the same commit.
    - No `#[test]` was added to `managers.rs` or `tasks_sync.rs`; counts before and after are reported.
  </acceptance_criteria>
  <done>A task moved on this computer is in one list, that is measured, and something would notice if the refusal that makes it true went away.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A move says when nothing has left this computer yet</name>
  <files>src/application/pim_command.rs, src/presentation/managers.rs, guards/guards.toml, docs/changelog.md, docs/IMPLEMENTATION_STATUS.md, docs/ALPHA_TESTING.md, Cargo.toml</files>
  <read_first>
    - `src/application/pim_command.rs`: `moved`, `cannot_be_moved`, `cannot_be_moved_into`, `did_not_happen` and `confirmed_detail`, with every doc comment. The wording rules for this whole module are in its header and they are strict.
    - `src/application/allowed.rs`: `changes_waiting_here`, `removals_waiting_here` and the comment explaining why they are two functions and not one, `SETTINGS_SECTION`, `allowed_for` and `Allowed::personal_information`.
    - `src/application/tasks_sync.rs`: the two `result.waiting_on_the_setting += 1` arms with their comments, and `SyncResult::summary` where `changes_waiting_here` is said.
    - `src/presentation/managers.rs`: the `PimCommand::Move` arm of `pim_command`, `send_status`, and the comment about why a refusal goes down `CommandRefused` and not the status line.
    - `docs/IMPLEMENTATION_STATUS.md` around "Moving a task between lists has never been done against a real account", and the matching entry in `docs/ALPHA_TESTING.md`, both of which describe what the move says today.
  </read_first>
  <behavior>
    - Moving a task that lives in a list a provider syncs says where it went and says the change has not reached the account yet.
    - Moving a note, which never leaves this computer, says where it went and says nothing about waiting, because there is nothing to wait for.
    - Where Allow Changes is off for that account, the sentence names the setting to turn on, in the same words the syncs already use.
    - Where Allow Changes is on, the sentence does not name the setting, because naming a setting that is already right teaches nothing and is heard on every move.
    - The sentence stays one sentence. Somebody hearing it after every move must not be given a paragraph.
    - Nothing about the refusal for a provider-held task changes.
  </behavior>
  <action>
    RED first, on a branch.

    Tests go in `src/application/pim_command.rs`, which 3 records fingerprint.
    That is affordable; pay it. **Add no `#[test]` to `managers.rs`**, which 40
    records fingerprint, and none to `tasks_sync.rs`, which 19 do.

    Measure `src/application/allowed.rs` with the `tests_last_seen` parse before
    putting any test there, because this plan did not measure it and a grep for
    the file name is what six phase-3 plans got wrong. Report the number.

    When the commit gate prints
    `scripts/guards.sh --remeasure "<name>" "<name>" "<name>"` for the three
    records naming `pim_command.rs`, run it, detached, with
    `WIXEN_TEST_THREADS=4`. That remedy is on the critical path and cannot be
    deferred; `CLAUDE.md`'s rule that guard re-measurement is off the critical
    path is about the full sweep and not about the count check.

    Walk every fixture by hand. The trap: a test asserting the sentence names
    the setting when Allow Changes is off will read the real settings file on
    whoever's machine runs it if it goes through `allowed_for`. Read
    `allowed_for`'s doc comment and `narrowed_by` beside it, which record
    exactly this problem and the parallel-test failure it caused. Take the
    answer as an argument rather than reading it inside the function under test.

    Commit RED with `Fails-until-green:` trailers at column 0 naming every new
    test function, plus
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held` if a
    `#[test]` landed in `pim_command.rs` in the same commit.

    GREEN. Say the extra clause only where it is true. A note is filed on this
    computer and nothing waits for it; a task or an event in a container a
    provider syncs is waiting. `new_item::supports` is the existing answer to
    "does this account sync this kind of thing", and it takes an `Account`; work
    out how the move reaches one, and if it cannot, say so and say what you used
    instead rather than inventing a second answer to the same question.

    Use `allowed::changes_waiting_here` for the setting clause rather than
    writing a second sentence. Its own comment records that two copies of that
    fact drifted and only one was corrected. If the wording does not fit a
    single move, that is a finding worth a sentence in the summary, and the
    answer is a third function beside the other two with a comment saying why,
    the way `removals_waiting_here` already is, not a string written inline.

    Keep it to one sentence. Guardrail 5 is the rule: feedback has to be distinct
    and bounded, and bounded is the half that is easy to lose. This is heard
    after every single move.

    Correct `docs/IMPLEMENTATION_STATUS.md` and `docs/ALPHA_TESTING.md` in the
    same commit, since both describe what a move says today and both become
    wrong. Do not rewrite the honest limitation they carry; add to it.

    Extend the `docs/changelog.md` entry and bump the version.

    Add a `guards/guards.toml` record for the clause appearing only where
    something is really waiting. Break it by saying the clause on every move,
    which is what somebody would write while making the sentence uniform, and
    which no test that only asks whether the clause exists would catch. Run
    `--no-fail-fast`, quote the whole red list, bump line 80.
  </action>
  <verify>
    <automated>cargo test --lib -- application::pim_command:: application::allowed::</automated>
  </verify>
  <acceptance_criteria>
    - The named test filter passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch, names the count check among its trailers if it fired, and `scripts/red-commit.sh` accepted it.
    - The summary reports `src/application/allowed.rs`'s record count, measured with the `tests_last_seen` parse, since this plan did not measure it.
    - The setting's answer is taken as an argument rather than read inside the function under test, and the summary says why, quoting `allowed_for`'s own comment about the parallel-test failure.
    - A note says nothing about waiting, with a test, and the summary quotes both sentences side by side.
    - The clause naming the setting comes from `allowed::changes_waiting_here` or from a sibling of it with a comment saying why, and the summary says which and quotes the sentence somebody hears in full.
    - The whole announcement is one sentence, and the summary quotes the longest one it can produce.
    - `docs/IMPLEMENTATION_STATUS.md` and `docs/ALPHA_TESTING.md` were corrected in the same commit, and the summary quotes what each says now.
    - The scoped `scripts/guards.sh --remeasure` the gate printed was run, detached, and the summary names the records.
    - The guard record's break is the clause on every move, was measured with `--no-fail-fast`, and its full red list is quoted. Line 80's census was corrected.
  </acceptance_criteria>
  <done>Somebody who moves a task hears that it moved and hears whether it has gone anywhere, at the moment they did it.</done>
</task>

<task type="auto">
  <name>Task 3: PIM-01's two criteria say what is true</name>
  <files>.planning/REQUIREMENTS.md, docs/changelog.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`, PIM-01 in full, including the evidence
      paragraph rewritten on 2026-09-04 and both `[D]` lines this task changes.
    - `.planning/decisions-2026-09-06.md`, decisions 1 and 5, which are what
      this task is carrying out.
    - `src/application/allowed.rs`, `changes_waiting_here`, so the reworded
      criterion uses the words the program already says rather than new ones.
  </read_first>
  <action>
    Reword PIM-01's `Allowed::personal_information` criterion so it describes
    what the program does, per decision 5. Nobody is refused at move time. The
    gate is applied where each HTTP client is built, a refused push is counted,
    and the sync summary names the setting to turn on. Task 2 of this plan makes
    the move say the same thing at the moment it happens. Write the criterion as
    that, and say in one clause why it is not a refusal at move time: every
    other edit in the program behaves this way, and making a task move the
    exception is worse for somebody moving by keyboard.

    Leave PIM-01's third criterion, about exactly one list, exactly as it is.
    Decision 1 says the provider move gets built, so that criterion becomes true
    by being satisfied rather than by being reworded, and `05-07` and `05-08`
    are the plans that do it. Add one sentence under the requirement saying
    which two plans own it and that it is not closed by this one.

    Date and attribute both edits the way the 2026-08-29 decision under PIM-04
    is dated and attributed, so the next reader can tell a decision from an
    opinion.

    Add nothing to `docs/changelog.md` about the rewording itself, which is not
    user-visible. Task 2's entry is the user-visible one.
  </action>
  <verify>
    <automated>cargo test --test house_style</automated>
  </verify>
  <acceptance_criteria>
    - PIM-01's `Allowed` criterion describes the gate where it really is, and names the sentence the syncs already say.
    - PIM-01's "exactly one list" criterion is unchanged, and carries a line naming `05-07` and `05-08` as its owners.
    - Both edits are dated 2026-09-06 and attributed to Pratik's decisions 1 and 5.
    - `tests/house_style.rs` passes, since it reads documents and has caught two real breaks in markdown.
    - No em-dash was introduced, which is what one of those breaks was.
  </acceptance_criteria>
  <done>PIM-01 says what is true today and names who makes the rest true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a locally written move -> the next push to a provider | a row changed here becomes a request sent to somebody else's server |
| the Allow Changes setting -> what the person is told | a setting decides whether anything leaves the machine, and the person has to be able to find that out |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Repudiation | a move announced as done when nothing has reached the account | high | mitigate | task 2 says so at the moment of the move, in one sentence, only where something really is waiting, with a guard record whose break is saying it everywhere. This is the whole of task 2. |
| T-05-14 | Tampering | a future provider move leaving a task in two lists or none | critical | transfer | not built here and refused, which is what makes the criterion true after this plan. Decision 1 of 2026-09-06 settles that it does get built: `05-07` makes the half-finished state a stored, visible, recoverable object and `05-08` makes the provider calls, in that order. The ordering argument in `one_day_kept_out_of_the_series` is the precedent both follow: write the thing that can be seen and put right before the thing that cannot be got back. |
| T-05-15 | Information disclosure | a task title in a log line explaining a failure | medium | accept | already decided against in `tasks_sync.rs`, whose comment says the id and not the title goes to the log because a title is the person's own words in the same way a message body is. Nothing here changes it. |
| T-05-16 | Elevation of privilege | a local write treated as permission to reach a provider | medium | accept | the gate is at the HTTP client, so a locally written row cannot cause a request. Decision 5 of 2026-09-06 upholds that design, and task 3 rewords PIM-01's criterion to describe it rather than changing the behaviour to match the old wording. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package is added. `Cargo.toml` is touched only by version bumps and the diff is read rather than assumed. |
</threat_model>

<verification>
- `cargo test --test a_moved_task_is_in_one_list` passes.
- `cargo test --lib -- application::pim_command:: application::allowed::` passes.
- `scripts/check.sh` passes on every commit through the commit hook, with nothing using `--no-verify`. Commits touching `Cargo.toml` answer `all`; run them detached.
- Two `guards/guards.toml` records were measured by hand with `--no-fail-fast` on a clean tree, with full red lists quoted, and line 80's census corrected in each commit that added one.
- The scoped `scripts/guards.sh --remeasure` printed by the gate for `src/application/pim_command.rs` was run, detached.
- No `#[test]` was added to `managers.rs` or `tasks_sync.rs`; counts before and after are reported.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed to the phase-8 sweep and must not block the merge.
</verification>

<success_criteria>
**PIM-01's first `[D]` line was already true before this plan.** A user moves a
task to another list by keyboard, in one action. Both user-facing documents were
corrected to say so on 2026-09-04.

**Criterion 1's local half closes and is measured.** A task moved on this
computer is in exactly one list, read back from storage, and something would
notice if the refusal that guarantees it went away.

**Criterion 1's provider half does not close here, and it is not deferred
either.** Decision 1 of 2026-09-06 says it gets built, in `05-07` and `05-08`.
Say that plainly rather than marking the criterion met on the strength of a
refusal, and rather than reading a refusal as the answer.

**PIM-01's `Allowed` `[D]` line is reworded by task 3 rather than left open.**
Decision 5 keeps the behaviour and corrects the sentence: the gate is where each
HTTP client is built, and what this plan adds is making that audible at the
moment somebody moves something rather than one sync later.

**Nothing here has run against an account**, and `05-RESEARCH.md`'s assumption
A2, that `Ctrl+Shift+V` really reaches the handler in the non-mail modules rather
than only appearing in a menu label, is not settled by any test in this
repository. Record it in `.planning/WINDOWS.md`, which ends at entry 192 on
2026-09-08 and further by now, so read its last row before choosing a number, along with whether the new waiting clause is heard as useful or as
noise after the twentieth move.
</success_criteria>

<output>
Create `.planning/phases/05-the-other-five-modules-keep-up/05-06-SUMMARY.md` when done
</output>
