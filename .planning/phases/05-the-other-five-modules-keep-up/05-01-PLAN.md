---
phase: 05-the-other-five-modules-keep-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/a_moved_day_is_shown_once.rs
  - src/presentation/ui_types.rs
  - src/presentation/read_aloud.rs
  - src/presentation/pim_rows.rs
  - tests/calendar_immediate_actions.rs
  - guards/guards.toml
  - docs/changelog.md
  - Cargo.toml
autonomous: true
requirements: [PIM-03]
estimate:
  tokens: 72000
  raw_tokens: 70000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A day of a repeating event that was moved to another date is on the calendar list once, on the date it really is, and not also on the date the rule would have put it. That is measured against a stored series and a stored override row read back through the query and the expansion the running program uses, not reasoned about."
    - "A row that is one day of a series changed on its own says so in the row text itself, which is what a screen reader speaks while arrowing, so somebody moving down the list can tell the changed Tuesday from the fifty-one ordinary ones without opening it. It says so in the Space reading too, in the same words from one function."
    - "A day the series calls off is on no date at all, which is already true and now has a test that would notice if it stopped being true."
    - "Nothing about an ordinary event changes: no extra word, no extra row, no extra clause read out."
  artifacts:
    - "tests/a_moved_day_is_shown_once.rs: a stored series plus a stored override, read back through events_that_could_fall_between and CalendarEventItem::every_day_shown, counting the rows on the original date and on the new one."
    - "src/presentation/ui_types.rs: CalendarEventItem carries whether this row is a day cut out of a series, filled in CalendarEventItem::from_entry from the stored row rather than worked out again."
    - "src/presentation/pim_rows.rs: the clause a changed day carries, appended to the time cell in event_cell, which is what the virtual list asks for and therefore what is heard while arrowing."
    - "src/presentation/read_aloud.rs: the same clause in read_short, from the same function pim_rows calls, so Space and arrowing say one thing rather than two."
    - "guards/guards.toml: a record whose break is the de-duplication going away, and a record whose break is the clause going away."
  key_links:
    - "one_day_kept_out_of_the_series (application::calendar) -> occurrences::falls_on: the first writes the EXDATE that makes the second skip the day. Everything in this plan's first truth rests on that pair, and no test in the tree joins them today. Task 1 is that test."
    - "CalendarEventEntry.cut_from_event_id and .provider_recurrence_id -> CalendarEventItem: the stored row knows it is an exception in two different ways depending on who made it, and the display row carries neither. from_entry is the one place that has the entry and builds the row."
    - "CalendarEventItem::from_entry -> shown_days: shown_days builds each day with `..Self::from_entry(entry)`, so a field set in from_entry reaches every expanded row without shown_days changing. Confirm that by reading it rather than assuming it."
    - "CalendarEventItem -> 13 construction sites, not 21. Corrected 2026-09-08. That grep returns 21 lines, but 3 are declarations and 6 are `-> CalendarEventItem {` return-type signatures. The literals the compiler will name are 12, plus the one `Self {` at ui_types.rs:1084 inside from_entry, which is the only production construction. ui_types.rs:1126 uses `..Self::from_entry(entry)` and needs nothing. Nine of the twelve are in read_aloud.rs below `mod tests`. Count them with the compiler, and do not size the RED commit off the grep."
    - "src/presentation/pim_rows.rs::event_cell -> the virtual list: wx_app.rs:1509-1526 registers set_virtual_text_callback and answers the calendar with event_cell. That is the text a screen reader reads while arrowing. read_short is NOT: wire_read_aloud binds KEY_DOWN and returns unless the key code is 32, so read_short is the Space reading only. This is premise correction 7 and it is what task 2 is shaped around."
---

<objective>
Settle by running it whether a moved occurrence is already shown once, and then
make a day that was changed on its own say so when it is read.

Purpose: criterion 3 of phase 5, second half. `05-RESEARCH.md` assumption A1
says a moved occurrence appears twice, once expanded from the series and once
as its own row, and says plainly that this came from reading rather than
running and should be the first failing test of any PIM-03 plan. Reading the
same code again on 2026-09-06 says the opposite, and gives a reason: every path
that stores a moved day also takes that day off the series. Neither reading is
evidence. A test is.

What is missing either way is the second clause of PIM-03's second `[D]` line:
an exception "announces that it differs from the series". Nothing on the display
row carries the fact and nothing said out loud mentions it.

Output: one integration test that settles the de-duplication and would notice if
it broke, and a row that says when it is a day changed on its own.
</objective>

<execution_context>
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/prati/Documents/projects/Wixen-Mail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-the-other-five-modules-keep-up/05-RESEARCH.md
@CLAUDE.md
</context>

<premise_corrections>
Everything below was checked against `main` at `9611b70`: a clean checkout,
version `0.75.0`, `guards/guards.toml` holding 632 records, `rust-version`
`1.88`. The findings were first taken on 2026-09-06 against an earlier tree and
every figure was re-measured against this one when the set was assembled;
phases 4 and 4.2 landed in between, which is what moved the record counts. Each
claim below names the command that produced it so it can be re-run rather than
trusted, and a figure is only a figure with the tree it came from: if the tree
has moved again, re-run them.

**The Rust floor is 1.88 and it has already re-linted code no phase-5 plan
opens.** `04-09` moved it from 1.87 because rPGP declares 1.88, and clippy only
suggests a construct once the declared floor allows it, so the bump turned on
`collapsible_if`'s let-chain form and cost seven fixes across six files that
plan never opened. `main` is clean at that floor now, so nothing is owed. What
it means here is that new code in this plan is linted against 1.88 with
`-D warnings`, where a let-chain suggestion is a build failure rather than a
hint.

**1. `05-RESEARCH.md`'s assumption A1 is probably wrong, and this plan is
arranged so that it does not matter which way it goes.** A1 says nothing
subtracts an override row's original date from its series' expansion. What the
tree says:

- `grep -rn "one_day_kept_out_of_the_series" src/ --include=*.rs` returns four
  call sites: `src/application/caldav_sync.rs`, two in
  `src/application/calendar.rs`, and `src/presentation/managers.rs`.
- The function itself is `pub fn one_day_kept_out_of_the_series` in
  `src/application/calendar.rs`. Read it: it saves the changed day, then saves
  the series with `with_one_more_day_called_off` applied. That is the EXDATE
  `occurrences::days_called_off` reads.
- The two sites in `calendar.rs` are the Google read and the Outlook read. Each
  has a `let Some(the_day_it_was) = ... else` arm above it which logs
  `"...that day cannot be taken off the series and the meeting may be shown
  twice"` and does `result.days_that_may_be_shown_twice += 1`. So the tree
  already names the double-show as the exception rather than the rule, and
  already counts it.
- `grep -rn "days_that_may_be_shown_twice" src/ --include=*.rs` shows it
  reaching `calendar.rs:189` (`said.sentence(a_day_moved_in_outlook(...))`),
  `ui_types.rs`, and four sites in `wx_app.rs`. It is announced, not swallowed.

So the likely answer is: a moved day is shown once wherever the provider says
which day it replaces, and twice where it does not, and the second case is
counted and said out loud. **Do not act on that. Task 1 runs it.**

**2. Which means task 1's test is very likely green on arrival, and the honest
answer to that is a measured break rather than a pretend red.** A test that
passes the moment it is written proves nothing about whether it would notice.
`scripts/guards.sh` is this project's answer to exactly that, and
`guards/guards.toml`'s own header says so. Task 1 therefore ends by taking the
break by hand and writing down everything that really went red. If the test is
red on arrival, A1 was right, and the decision rule for that case is in task 1's
action.

**3. `falls_on` filters by EXDATE and by nothing else, and that part of A1 is
right.** Read `pub fn falls_on` in `src/application/occurrences.rs`: the only
filter on the expansion is `.filter(|day| !called_off.contains(day))`, where
`called_off` comes from `days_called_off`, which reads
`event.exception_dates`. `grep -rn "provider_recurrence_id" src/application/occurrences.rs`
returns nothing. So the de-duplication, if it works, works because the writer
put an EXDATE on the series, not because the reader knows about overrides.

**4. INFERRED, not checked: an override row carries no recurrence rule of its
own.** If it did, it would expand itself over the whole window and appear on
every date, which is a worse bug than the one A1 describes.
`the_day_kept_on_its_own` in `src/presentation/managers.rs` sets
`recurrence_rule: None` explicitly, with a comment saying why, so the local path
is safe. The Google and Outlook paths build the row with
`google_event_to_local` / the Outlook equivalent, and I read the call rather
than the body. **Check it in task 1** by asserting the stored override row's
`recurrence_rule` is `None` after the write, and say in the summary what it
really was.

**5. `CalendarEventItem` carries nothing about being an exception, and this is
the 04-05 shape.** `sed -n '/pub struct CalendarEventItem/,/^}/p' src/presentation/ui_types.rs`
lists 18 fields: id, summary, description, start, end, location, is_all_day,
status, provider, calendar_id, calendar_name, calendar_color, reminder_minutes,
repeats, categories, show_as, recurrence_rule, attendees_json. None of them is
`cut_from_event_id` or `provider_recurrence_id`. The stored row has both:
`grep -n "cut_from_event_id\|provider_recurrence_id" src/data/message_cache/mod.rs`
finds them on `CalendarEventEntry`. **The claim is about a type, so ask the
type, not a line number.** 04-05 lost half a day to a line number that was real
and was inside a different struct.

**6. A new field on `CalendarEventItem` is 13 construction sites, not 21.**
Corrected 2026-09-08 by reading every hit. That grep returns 21 lines, and 9 of
them are not constructions: 3 declarations (`ui_types.rs:679` the struct,
`ui_types.rs:1064` the impl, `read_aloud.rs:416` the `impl ReadAloud`) and 6
`-> CalendarEventItem {` return-type signatures (`managers.rs:5517`,
`managers.rs:8400`, `pim_rows.rs:350`, `read_aloud.rs:629`, `ui_types.rs:1346`,
`tests/calendar_immediate_actions.rs:77`). The literals are 12: nine in
`read_aloud.rs` below `mod tests`, one in `pim_rows.rs`, one in `ui_types.rs`,
one in `tests/calendar_immediate_actions.rs`. Add the `Self {` at
`ui_types.rs:1084` inside `from_entry`, which is the only production
construction, for 13 the compiler names. `ui_types.rs:1126` uses
`..Self::from_entry(entry)` and needs nothing.

**Neither of `managers.rs`'s two hits is a construction site**, which is why
that file left this plan's file list. Both are return-type lines below
`mod tests`, and both bodies build the value through
`CalendarEventItem::shown_days(..)`.

Let the compiler name them and report the compiler's number in the summary. The
point of the correction is the RED commit: under `-D warnings` it has to carry
every site, and sizing that off the grep inflates it by roughly two thirds.

**7. `shown_days` needs no change, and that is worth confirming rather than
assuming.** `CalendarEventItem::shown_days` builds each day with
`..Self::from_entry(entry)`, so a field set in `from_entry` reaches every
expanded row. Read those two functions together before writing anything; if it
had been the other way round, every day of a series would have carried the
default.

**8. Guard-record cost, measured by parsing `tests_last_seen` rather than by
grepping for a file name.** `05-RESEARCH.md`'s table is roughly double the truth
in every row, because a grep for a file name counts mentions and each record
mentions its file about twice. Six phase-3 plans made the same mistake and all
six were corrected. Measured on `main` at `9611b70`, which holds 632 records:

| file | records fingerprinting it | test functions today |
|---|---|---|
| `src/presentation/ui_types.rs` | 0 | 54 |
| `src/presentation/read_aloud.rs` | 2 | 46 |
| `src/presentation/pim_rows.rs` | 1 | 23 |
| `src/presentation/managers.rs` | 40 | 137 |
| `src/application/occurrences.rs` | 3 | 62 |
| `src/application/calendar.rs` | 69 | 194 |
| `tests/calendar_immediate_actions.rs` | 0 | 1 |
| a new file under `tests/` | 0 | 0 |

The command that produced the left column, so it can be re-run:

```
python -c "
import re,collections
src=open('guards/guards.toml',encoding='utf-8').read()
cnt=collections.Counter()
for r in re.split(r'(?m)^\[\[guard\]\]\s*$', src)[1:]:
    m=re.search(r'(?ms)^tests_last_seen\s*=\s*\[(.*?)^\]', r)
    if m:
        for f in set(re.findall(r'file\s*=\s*\"([^\"]+)\"', m.group(1))): cnt[f]+=1
print(cnt.most_common())
"
```

The right column is counted the way the gate counts:
`test_every_guard_record_says_how_many_tests_the_files_it_names_held` uses
`how_many_tests_are_in` in `tests/house_style.rs`, which counts lines whose
whole trimmed content is `#[test]` or `#[tokio::test]`. Count that way or the
numbers will not agree with the check.

**So: put task 1's test in a new file under `tests/`, which costs nothing, and
add no `#[test]` to `src/application/calendar.rs` or
`src/presentation/managers.rs`.** A test added to `calendar.rs` flags 69 records
for the count check, and that check runs inside the commit gate, so the remedy
`scripts/guards.sh --remeasure "..."` is on the critical path and cannot be put
off. 69 records is a build and a full library run each.

**9. A guard living in `tests/` needs a `guards/guards.toml` record or it runs
on every commit except the ones that could break it.** `CLAUDE.md` says this in
as many words, and it is the reason two guards over `src/presentation` were
invisible to the gate until 2026-09-02. A record for an integration guard
carries `suite = "<the file name without .rs>"` and names its tests bare, with
no module path. `grep -n 'suite = "wired"' -A 20 guards/guards.toml` shows the
shape.

**10. Adding a record to `guards/guards.toml` also changes a census at the top
of that file, in the same commit.** Line 79 says `192 records were swept that
day.` and line 80 says `440 records have arrived since and have not been through
it.` `test_the_sweep_written_at_the_top_of_the_guard_records_covers_every_record_in_it`
in `tests/house_style.rs` adds those two and compares them with the number of
`[[guard]]` lines. 192 + 440 = 632, which is what the file holds today. Two new
records means line 80 becomes 442. Read both lines before trusting either
number: they are a measurement with a date and this plan is not the only thing
adding records. 04-05 met this as a refused commit and
recorded it as a missing-critical deviation; it is written here so this plan
does not.

**11. A commit that touches `Cargo.toml` makes `scripts/which-checks.sh` answer
`all`.** Read the `Cargo.toml | Cargo.lock | */Cargo.toml | */Cargo.lock)` arm
in that script and the comment above it. The version bump goes in the same
commit as the user-visible change, so every GREEN commit in this plan runs the
whole gate through the commit hook. `CLAUDE.md` measures the whole gate at
around 311 to 353 seconds warm. **Run those commits detached, or with a timeout
well past ten minutes**, and never pipe `check.sh` into anything whose exit
status is then read.

**12. A red commit is allowed on a branch and refused on `main`**, and must name
every failing test in `Fails-until-green:` trailers at column 0, with every
named test having run and failed and nothing else having failed. An assertion on
a `.len()` inside a test body cannot be named in a trailer, which 04-05 found
the hard way: name the test function, not the assertion.
</premise_corrections>

<tasks>

<task type="tracer">
  <name>Task 1: A stored series and a stored moved day, read back the way the panel reads them</name>
  <files>tests/a_moved_day_is_shown_once.rs, guards/guards.toml, Cargo.toml, docs/changelog.md</files>
  <read_first>
    - `src/application/calendar.rs`: `one_day_kept_out_of_the_series` and its whole doc comment, `WhoTookTheDayOut`, `with_one_more_day_called_off`, and the two `days_that_may_be_shown_twice` arms with the `tracing::warn!` above each.
    - `src/application/occurrences.rs`: `falls_on` in full, `days_called_off`, `HOW_FAR_BACK` and `HOW_FAR_FORWARD`, and `CANNOT_BE_READ`.
    - `src/presentation/ui_types.rs`: `CalendarEventItem::from_entry`, `shown_days`, `every_day_shown`, `the_window_now` and `the_window_around`, read together so it is clear which of them knows about a window and which does not.
    - `src/data/message_cache/calendar.rs`: `events_that_could_fall_between` in full, including the doc comment about why it is two queries, and `save_calendar_event`.
    - `src/service/caldav.rs`: `the_called_off_value_for`, so the EXDATE the writer produces and the EXDATE the reader parses are known to be the same shape.
    - `src/presentation/managers.rs`: `the_day_kept_on_its_own`, for the local half and for its explicit `recurrence_rule: None`.
    - One existing integration test that really opens a cache, for the fixture shape: `tests/integration_tests.rs`, lines 59 to 60, where `tempfile::tempdir()` is followed by `MessageCache::new(dir.path().join("groups.db"), None)`. `tests/one_sign_in_per_piece_of_work.rs` and `tests/finding_people_answers.rs` are the other two that do. Corrected 2026-09-08: this bullet used to name `tests/the_conflict_choice_can_be_heard.rs`, which opens no cache at all. It is a source-reading test that calls `fs::read_to_string` and counts builder calls, so it has none of the helpers this task needs.
  </read_first>
  <action>
    This task settles a question and leaves behind something that would notice
    if the answer changed. It adds no behaviour, so there is nothing for a
    failing test to demand and `tdd="true"` does not apply; what replaces the
    red half is a measured break, which is stronger evidence and is this
    project's own idiom.

    Write `tests/a_moved_day_is_shown_once.rs`. A new file under `tests/` is
    named by no guard record, so it is free, and an integration test is the
    right home: the question spans the writer in `application::calendar`, the
    query in `data::message_cache::calendar` and the expansion in
    `presentation::ui_types`, and no unit test can hold all three.

    Build the fixture through the public API only, since an integration test
    links the library without `cfg(test)`. A `MessageCache` in a `tempfile`
    directory, one weekly series, one changed day built the way
    `the_day_kept_on_its_own` builds one, and
    `application::calendar::one_day_kept_out_of_the_series` to store the pair.
    Use `WhoTookTheDayOut::TheProviderItself` for one case and
    `WhoTookTheDayOut::SomebodyHere` for the other, because the only thing that
    argument changes is whether the series is left waiting to be sent and both
    have to leave one row on each date.

    Then read it back the way the panel does: `events_that_could_fall_between`
    over a window that contains both dates, then
    `CalendarEventItem::every_day_shown`. Assert on rows, by date:

    - the original date has no row from the series and no row from the override
    - the date it moved to has exactly one row
    - every other date the rule falls on still has exactly one row, so the
      EXDATE took one day out and not the series

    **Do not assert that the stored override row's `recurrence_rule` is `None`.**
    An earlier draft of this task did, as a way of settling premise correction 4,
    and that assertion cannot fail. Corrected 2026-09-08. The fixture builds
    `that_day` the way `the_day_kept_on_its_own` builds one, and that sets
    `recurrence_rule: None` explicitly; `one_day_kept_out_of_the_series` then does
    nothing but `cache.save_calendar_event(that_day)?`. So the assertion asks
    whether `save_calendar_event` round-trips a constant the test itself wrote,
    and it is green against every implementation, including the one premise 4
    calls a worse bug than A1: an override arriving from a provider that carries
    a rule and expands across the whole window.

    Premise 4 is about what a *provider* sends, and both paths that could
    populate it derive it from the payload: `google_event_to_local` at
    `src/application/calendar.rs:2635` sets it from
    `only_the_line_naming(&event.recurrence, "RRULE")`, and `ms_event_to_local`
    at `:3105` from `event.recurrence.as_ref().and_then(..)`. No local round trip
    can reach either. So premise 4 stays open, and the summary says it stayed
    open and why, rather than reporting a green that answered a different
    question.

    Add the day-called-off case in the same file, since it is the other half of
    the same `[D]` line and costs one more fixture:
    `application::calendar::one_day_called_off` on the series, then the same
    read, asserting the day is on no date at all.

    **Run it before writing anything else, and let the result decide what
    follows.**

    - **Green, which is what the code says it will be.** A1 was wrong. Record
      that plainly in the summary, quote the four call sites that make it true,
      and go on to the break below. Task 2 is unchanged.
    - **Red.** A1 was right, the de-duplication is not there, and this plan is
      bigger than it says. Stop and report before writing a fix: say which
      assertion failed, whether the surplus row came from the series or from the
      override, and which of the four call sites did not run. A fix that
      subtracts an override's date inside `falls_on` would be a second
      de-duplication beside a broken first one, which is the mistake this task
      exists to avoid.

    Take the break by hand. Make `one_day_kept_out_of_the_series` save the
    changed day and not the series, which is the half-fix somebody would
    actually write and is the shape plan 03-08 found matters, rather than
    deleting the function.

    **Measure the red list with `cargo test --test a_moved_day_is_shown_once`,
    not with `--all-targets`.** Corrected 2026-09-08; an earlier draft said to
    run everything and quote all of it, and that instruction reads as prudence
    while producing a record that cannot be measured. A record carrying `suite`
    is scoped to that one target: `scripts/guards.py:119` builds
    `("--test", entry["suite"])`, and `measure()` runs only that target, then
    raises `Wrong("the test harness never ran ...")` for every named test the run
    did not report. The commit gate refuses it before that:
    `tests/house_style.rs:5252` resolves every red-list name through
    `the_file_a_test_lives_in(test, suite)`, which returns `tests/<suite>.rs` for
    every name once a suite is set, so a library test named in a suite record is
    looked for inside the integration file and reported gone. The record at
    `guards/guards.toml:4197-4212` is the existing example of this shape, and its
    red list holds one name from its own target only.

    Run it on a clean tree with nothing else building, and write down everything
    in that target that really went red rather than what you expected.

    If the break also reddens tests in `src/application/calendar.rs`, and it very
    likely does, that is a **second record** with no `suite` and a red list of
    library test paths, not extra names on the first one. Two records is the
    correct answer here, not a workaround.

    Write the `guards/guards.toml` record with `file = "src/application/calendar.rs"`,
    `suite = "a_moved_day_is_shown_once"`, and the red list exactly as measured.
    Add `tests_last_seen` entries for every file its red list names and for the
    file it breaks, counted the way `how_many_tests_are_in` counts. Bump the
    census on line 80 by the number of records you actually added, in the same
    commit.

    Write the `docs/changelog.md` entry under `[Unreleased]` and bump the
    version. Say what is now proved rather than what was built: a meeting moved
    to another day is on the calendar once, on the day it really is, and there
    is a test that would notice if it stopped being. Say the limit in the same
    breath: where a provider does not say which day of a series a moved
    appointment stands in for, it is still shown twice, that is counted, and the
    sync says so.
  </action>
  <verify>
    <automated>cargo test --test a_moved_day_is_shown_once</automated>
  </verify>
  <acceptance_criteria>
    - The named test target passes, and `scripts/check.sh` passes for this commit's file list.
    - The summary states plainly whether the test was green or red on arrival, and says what that means for `05-RESEARCH.md` assumption A1.
    - The summary names the four call sites of `one_day_kept_out_of_the_series` found by grep, with the command.
    - The summary says that premise correction 4 stayed open, and why a local round trip cannot settle it.
    - The guard record's break is the half-fix, not a deletion. The suite record's red list was measured with `cargo test --test a_moved_day_is_shown_once` and holds names from that target only. Where the break also reddens library tests, a second record with no `suite` carries those, measured separately.
    - Every record carries `tests_last_seen`, and line 80 of `guards/guards.toml` was raised by the number of records added, in the same commit.
    - No `#[test]` was added to `src/application/calendar.rs` or `src/application/occurrences.rs`. Their counts are reported before and after.
    - `docs/changelog.md` names the limit as well as the fact, and the version is bumped in the same commit.
  </acceptance_criteria>
  <done>Whether a moved day is shown once is a measured fact with a test that would notice if it changed, rather than two people reading the same code and disagreeing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A day changed on its own says so when it is read</name>
  <files>src/presentation/ui_types.rs, src/presentation/read_aloud.rs, src/presentation/pim_rows.rs, tests/calendar_immediate_actions.rs, guards/guards.toml, docs/changelog.md, Cargo.toml</files>
  <read_first>
    - `src/presentation/ui_types.rs`: the whole `CalendarEventItem` struct with every field's comment, `from_entry`, and `shown_days`. The comments on `repeats` and on `recurrence_rule` are the two worked examples of what a field on this struct is for and how it is worded.
    - `src/presentation/read_aloud.rs`: `impl ReadAloud for CalendarEventItem` in full, all three methods, and the comment inside `read_id` about why a day of a series carries the date in its identity. Then `impl ReadAloud for NoteItem` and `impl ReadAloud for TaskItem` beside it, for how a short reading is kept short.
    - `src/presentation/pim_rows.rs`: `event_cell` in full. This is the function the virtual list asks for each painted cell, so its output is what a screen reader speaks while arrowing. Read column 0 in particular: it already appends a qualifying clause to the time with `format!("{when}, {note}")` where the note says the hour falls outside the working day. That is the precedent this task follows, and it is the only place in the file that appends one.
    - `src/presentation/wx_app.rs` lines 1509 to 1526, where `set_virtual_text_callback` is registered and the calendar arm answers with `event_cell`, and lines 9569 to 9600, where `wire_read_aloud` binds `KEY_DOWN` and returns unless the key code is 32. Read the two together. They are why the clause cannot live in `read_short` alone.
    - `src/data/message_cache/mod.rs`: `CalendarEventEntry`, specifically `cut_from_event_id` and `provider_recurrence_id` and their comments, because the two mean the same thing to a reader and are set by different writers.
    - `src/application/calendar.rs`: `one_day_taken_off` and `what_was_done`, which are the existing sentences about a single day of a series, so this one sounds like them rather than like a new voice.
  </read_first>
  <behavior>
    - A row built from an event with `cut_from_event_id` set is a day changed on its own, and says so.
    - A row built from an event with `provider_recurrence_id` set is a day changed on its own too, and says the same thing, because the difference between the two is who made the change and that is not something a listener needs.
    - A row built from an ordinary event says nothing extra, and neither does an ordinary day of a series.
    - The clause is in the row text `event_cell` builds, appended to the time cell, so somebody arrowing down the list hears it from the screen reader without pressing anything.
    - The clause is in the short reading too, so Space says it as well, and the two come from one function so they cannot drift. It is not repeated a second time in the full reading.
    - The clause is not the words `repeats` already uses, so a listener does not hear two overlapping statements about the same event.
    - An event that is both a day of a series and unreadable as a rule still says the unreadable-rule sentence, which is the one case the short reading already spends a line on.
  </behavior>
  <action>
    RED first, on a branch.

    Tests go in `src/presentation/read_aloud.rs`, which 2 records fingerprint,
    in `src/presentation/pim_rows.rs`, which 1 does, and in
    `src/presentation/ui_types.rs`, which none do. **Add no `#[test]` to
    `src/presentation/managers.rs`**, which 40 records fingerprint. Corrected
    2026-09-08: `managers.rs` is off this task's file list entirely. It has no
    `CalendarEventItem` construction site. Its two grep hits are
    `-> CalendarEventItem {` return-type lines at `:5517` and `:8400`, both below
    `mod tests`, and both bodies build the value through
    `CalendarEventItem::shown_days(..)`, which needs nothing.

    **The clause must reach `event_cell`, and putting it only in `read_short`
    would be the failure guardrail 2 is about.** `read_short` is the Space
    reading: `wire_read_aloud` returns unless the key code is 32, before it ever
    calls `lookup`. What a screen reader says while arrowing is the virtual list
    row text, which the calendar answers with `pim_rows::event_cell`. So a clause
    that lives only in `read_short` is structure present and experience absent:
    the test passes, and the person moving down the list hears nothing.

    Append it to column 0, the time cell, following the working-day note already
    in that arm. Column 0 rather than a new column, because a new column changes
    the column set, the headers and every calendar list; and column 0 rather than
    the status column, because status already means the meeting's own status and
    two unrelated facts in one cell is worse to listen to than a longer first
    cell. Write that reasoning into the code as a comment, the way the
    working-day note above it carries its own.

    One function builds the clause and both callers use it, so Space and arrowing
    cannot drift apart. That is the same rule the phase applies to the sync
    sentences, and two copies of one sentence have already drifted once in this
    codebase.

    The two records that fingerprint `read_aloud.rs` will be flagged by
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held` the
    moment a `#[test]` is added there. That check runs inside the commit gate,
    so the commit is refused until it is dealt with. Run the exact command it
    prints, `scripts/guards.sh --remeasure "<name>" "<name>"`, with
    `WIXEN_TEST_THREADS=4` set, and do it detached: two records is two builds
    and two full library runs, on the order of a few minutes each. This is not
    the full sweep `CLAUDE.md` puts off until phase 8; it is the scoped remedy
    that is on the critical path and cannot be deferred.

    Walk every fixture by hand before trusting it. Two traps.

    The first: a test that asserts an ordinary event says nothing extra is green
    before the field exists and stays green after, so it says nothing about this
    task. The assertion that goes red is the one about the changed day.

    The second: `read_id` already puts the day into a series row's identity, so
    two rows of one series are already distinguishable to the Space cycle. That
    is not the same as being distinguishable to a person listening, and a test
    that asserts the identities differ would pass today and prove nothing about
    what is heard.

    Under `-D warnings` a field nothing reads will not build, so the red commit
    carries the field, the twenty-odd construction sites and the reading
    together. That is expected and is the case phase 3 met: say in the commit
    message why the red commit carries the wiring, and keep it
    behaviour-neutral so every named failure is still failing on the thing it is
    about.

    Commit RED with `Fails-until-green:` trailers at column 0 naming every new
    test, plus
    `test_every_guard_record_says_how_many_tests_the_files_it_names_held` if a
    `#[test]` landed in `read_aloud.rs` in the same commit. Name test functions,
    never assertions inside them.

    GREEN. Put one field on `CalendarEventItem` and fill it in `from_entry` from
    the stored row, from whichever of `cut_from_event_id` and
    `provider_recurrence_id` is set. One field and not two: a listener does not
    need to know whether it was this computer or a calendar server that moved
    the day, and two fields would need two sentences that would drift apart.
    Name it for what it means to a reader rather than for either column.

    Let the compiler name the construction sites. Report its number in the
    summary; premise 6 expects 13, and the grep's 21 is wrong for the reason
    given there.

    Choose the words with some care and argue for them in one paragraph. It is
    read out while arrowing, so it is paid on every changed day, and the list
    already says the date and the summary. It has to distinguish this row from
    the ordinary days of the same series without implying the event was
    cancelled or that anything is wrong. Look at `one_day_taken_off` and
    `what_was_done` in `application::calendar` first, because those are the
    sentences already spoken about a single day of a series and the new one
    should sound like them.

    Do not touch `repeats`. It says how often the event comes round and this
    says that this particular day was changed; folding them together would make
    the unreadable-rule sentence and this one fight over one field.

    Extend the `docs/changelog.md` entry from task 1 and bump the version.

    Add a `guards/guards.toml` record whose break is the clause going away.
    Break it by making the field always answer that the row is an ordinary day,
    which is the constant rather than the removal, and is what a half-finished
    version would look like. Run `--no-fail-fast` on a clean tree and write down
    the whole red list. Bump the census on line 80 again.
  </action>
  <verify>
    <automated>cargo test --lib -- presentation::ui_types:: presentation::read_aloud::</automated>
  </verify>
  <acceptance_criteria>
    - The named test filter passes, and `scripts/check.sh` passes for this commit's file list.
    - The RED commit is on a branch, names every new test and the count check if that fired, and `scripts/red-commit.sh` accepted it.
    - The summary gives the number of construction sites the compiler named, and says whether it agreed with the grep's 21.
    - One field, not two, and the summary says why the two stored columns collapse to one thing a listener hears.
    - The chosen words are argued for in one paragraph that names the sentences in `application::calendar` they were written to sound like.
    - A test drives the `provider_recurrence_id` case and the `cut_from_event_id` case and asserts they say the same thing.
    - A test asserts an ordinary event and an ordinary day of a series say nothing extra.
    - The clause is in the short reading and is not repeated in the full reading, with a test.
    - The scoped `scripts/guards.sh --remeasure` the commit gate printed was run, detached, and the summary names the records and says what changed.
    - The guard record's break is a constant rather than a removal, was measured with `--no-fail-fast`, and its full red list is quoted.
    - `docs/changelog.md` carries the entry and the version is bumped in the same commit.
  </acceptance_criteria>
  <done>Arrowing down a calendar, the Tuesday that was moved sounds different from the fifty-one that were not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a calendar server's or a provider's occurrence payload -> the stored override row | a remote party decides which day an instance replaces, and getting it wrong writes an EXDATE for a day nobody cancelled |
| a stored row's exception columns -> a sentence read aloud | the clause is spoken about somebody's appointments, in whatever room they are in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | a provider naming a replaced day this program reads onto the wrong side of midnight, calling off a day nobody cancelled | high | accept | pre-existing and unchanged by this plan. `AGoogleMoment`'s own doc comment already names this as the one disagreement that costs something, and the reading is already drawn once. Task 1's test makes a regression visible rather than adding a new risk. |
| T-05-02 | Denial of service | a series whose rule falls on no day walking forward for ever while the window is held | low | accept | already bounded by `MOST_STEPS` at 40,000 and `MOST_DAYS_ONE_SERIES_SHOWS` at 800 in `occurrences.rs`, both with comments saying why. This plan adds no expansion. |
| T-05-03 | Information disclosure | a longer row reading revealing more about an appointment in a room where somebody is listening | low | accept | the clause says only that this day differs from its series and names nothing. The mute this project's fifth guardrail requires already exists. |
| T-05-04 | Repudiation | a row saying a day was changed when it was not, or staying silent when it was | medium | mitigate | one field with one source, filled in `from_entry` from the stored columns and nowhere else, plus a guard record whose break is the field answering a constant. Both stored columns are driven by a test. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package is added. `Cargo.toml` is touched only by the version bumps, and the diff is read rather than assumed. |
</threat_model>

<verification>
- `cargo test --test a_moved_day_is_shown_once` passes.
- `cargo test --lib -- presentation::ui_types:: presentation::read_aloud::` passes.
- `cargo test --test calendar_immediate_actions` passes, since the compiler names two construction sites in it.
- `scripts/check.sh` passes on every commit through the commit hook, with nothing using `--no-verify`. Commits that touch `Cargo.toml` answer `all` and take several minutes; run them detached.
- Two `guards/guards.toml` records were measured by hand with `--no-fail-fast` on a clean tree, with full red lists quoted, and line 80's census was corrected in the same commits.
- The scoped `scripts/guards.sh --remeasure` printed by the commit gate for `src/presentation/read_aloud.rs` was run.
- No `#[test]` was added to `src/application/calendar.rs`, `src/presentation/managers.rs` or `src/application/occurrences.rs`; counts before and after are reported. `managers.rs` is not on either task's file list and should not have been opened for a change at all.
- `scripts/guards.sh --touched-by <the commit this branch left main at>` is owed to the phase-8 sweep and must not block the merge.
</verification>

<success_criteria>
**PIM-03's first `[D]` line was already true and now has a test.** A recurring
event appears on every date it occurs, expanded from the rule. That was true of
the only view that exists before this plan; `05-02` extends it to the week and
month windows and asserts it there.

**PIM-03's second `[D]` line closes structurally, both clauses**, if task 1 came
back green: an exception shows on the date it really is, which task 1 measured,
and announces that it differs from the series, which task 2 built. If task 1
came back red, only the second clause closes and the first becomes work with a
size somebody has to be told about before it is planned.

**PIM-03's third `[D]` line is not touched by this plan and is not closed.** It
asks that the two stated limitations, a one-day change that cannot be sent and a
weekly rule naming two or more days, are restated in the product rather than
left in the changelog. `can_be_honoured` already refuses in the product with a
sentence; the recurrence editor's limitation was not checked by this plan and is
carried in this phase's `README.md` as owed work. Say so in the summary rather than
letting the requirement read as closed.

**It is not closed by ear**, and that goes in `.planning/WINDOWS.md`. **Read the
end of that file before choosing a number.** It held 192 entries on 2026-09-08,
not the 119 an earlier draft of this plan and the phase README both said, and
`tests/the_planning_files_agree_with_themselves.rs` refuses a duplicated ledger
id inside the commit gate, with a message about the two halves disagreeing
rather than about the number. The entry: whether the clause about a changed day
is heard
as useful or as clutter when fifty-two rows of one series go past, and whether
it is confused with the unreadable-rule sentence when both could apply.
</success_criteria>

<output>
Create `.planning/phases/05-the-other-five-modules-keep-up/05-01-SUMMARY.md` when done
</output>
