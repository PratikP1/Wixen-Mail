# Measurements

This is the one page a figure about this tree is written on. How many guard
records there are, how many mutants the configuration allows, how long the
suite takes: each of those is a number that was true on the day somebody took
it, and this page says which day, which command, and which commit. Every other
page points here instead of restating a number, so that a figure which has
moved reads as a dated measurement rather than as a current fact.

**Nothing on this page is promised to be current.** A row says that its value
is what its command reported on its date at its commit, under its conditions,
and nothing more. The check that reads this page,
`tests/every_number_carries_its_command_and_its_date.rs`, holds the shape of
every row and never its value: it refuses a row without a command, a date or a
commit, and it never compares a value with what the command reports today.
That is on purpose. PERF-06 records that a check asserting a written count
equals today's count is false the next time somebody adds a test.

## What belongs here, and what does not

Four kinds of number live in this project's documents, and only the first
belongs on this page.

| Kind | Example | Where it lives |
|---|---|---|
| A measurement of the tree | how many guard records there are; how long the suite took; the rate one guard record costs | here, one row each |
| A target | memory under 150 MB with 1,000 cached messages | `.planning/REQUIREMENTS.md`, marked as a target |
| A constant the code holds | a single attachment is kept up to 25 MB, which is `LARGEST_ATTACHMENT_KEPT_BYTES` | the code, and a check that the prose agrees with it |
| A historical record | what a sweep found on 2026-08-12 | wherever it was written, and never corrected |

A duration belongs here with the settings that change it: the thread count,
whether the build was warm, what else was running. A duration without those is
a number without its conditions, and this project has quoted several of them
to people planning work.

## How to add a row

Run the command. Write the value it printed, the command as you ran it, the
date, the short hash of the commit you ran it at, and what would move the
figure. If nothing would, write `none`. Do not copy a figure from a plan, a
research document or `CLAUDE.md`; run the command. If the same figure is taken
again on a later day, add a row rather than editing the old one, so the page
keeps the series. Two rows with the same `what` and the same date are refused.

Pipes inside a command are written `\|` so the table stays a table.

## The definitions the start and memory rows were taken under

Three of the project's targets use words they do not define: memory under
150 MB with 1,000 cached messages, cold start under 2 seconds to a usable
list, and idle memory under 100 MB. The rows below were taken under these
meanings, which are the same words the harness's header states in
`tests/the_numbers_the_targets_ask_for.rs`, so the next person re-taking a
number takes the same one.

**1,000 cached messages** means 1,000 rows in `messages` for one IMAP
account's `INBOX`, written through the same call the sync uses, with a
plain-text body of about 2 KB each through the body store, no attachment
content and no signed originals. Because that is what the list reads at
startup and what the target was written about; the attachment and
signed-original budgets are constants with their own rows and their own
limits, and mixing them in would measure the budgets rather than the
messages.

**Cold start** means a fresh process of the release binary against the
1,000-message profile, from the first instruction of `main` to the usable
line. The first start after the binary is built is reported on its own as
the file-cache-cold figure; the next five starts are the series and their
median is the number. The requirement says "cold" and does not say cold
for what; both readings are given so nobody has to guess which was meant.

**Usable** means the first `MessagesLoaded` after startup whose rows
reached the list control's count and were at least one, which is the line
`wixen_mail::common::started` writes once per process.

**Idle** means no input after the usable line, the window left where the
start put it, and memory read at 60 s and at 120 s; the 120 s reading is
the number, the 60 s reading sits beside it, and the difference is
reported as idle growth.

**Memory with 1,000 cached messages** is the peak working set of the
application process between start and the 60 s reading, plus the WebView2
tree's working set at 60 s, on the 1,000-message profile, because "with
1,000 cached messages" is about what loading them costs and the peak is
when it cost most. The empty profile's 120 s reading is the floor and is
reported beside both.

Two things the first taking found that the definitions did not say. The
list the usable line reports holds 500 rows and not 1,000, because the
window opens on All Inboxes and that view lists its first page,
`ALL_INBOXES_LIMIT` in `src/presentation/wx_app.rs`; the other 500 are in
the cache and are read when somebody asks for older mail. And the WebView2
tree is not the application's own memory in any sense the targets were
written about: it is six `msedgewebview2.exe` processes Windows starts for
the preview pane, and it weighs the same on an empty profile as on the
1,000-message one, so every memory row gives the application process and
the tree separately as well as summed. Whether a target is met against
the sum or against the process is 08-09's judgement, not this page's.

## What the scale rows time, and what they do not

The rows over 200,000 rows were taken on 2026-09-14 by
`tests/the_list_at_two_hundred_thousand_rows.rs`, a harness that builds the
rows through `sample_mailbox`, the generator on the Help menu, and runs the
real functions with no window. Its header says what each kind of row means;
the short form is here so the rows can be read without it.

**Listing** is `MessageCache::get_messages_for_folder`, the read the list
takes when a folder opens and the one that precedes any scroll. The rows
were written into a cache in a `tempfile` directory through
`upsert_messages`, the call the sync uses, because the Help menu's sample
never enters SQLite. Cold is the first read on a connection opened after the
rows were written; the file was warm in the operating system's cache either
way, because the same process had just written it.

**Filter** is `MessageCache::search_messages`, the query the search box
runs, at the limit the box passes, under All Folders and under the
narrowest answer the In list offers for that kind of word. One row raises
the limit to every match, which the box never does, so the page says what
reading every match costs.

**Sort** is `mail_sort::sort_messages` over the generated rows in memory,
each order on a fresh clone with the clone outside the timing. In the
running program `apply_sort` runs it off the interface thread and sends the
result back through `MessagesLoaded`; the cost of the list control taking
that result was not timed, because there is no window.

**Page paint** and **full pass** are `virtual_rows::text_for`, the whole of
what the list's paint callback does after taking the lock, over one page of
every visible inbox column and over every row of one column. A scroll in the
running program is the page paint plus wxWidgets' own painting, which was
not timed, for the same reason. The full pass is what painting the whole
list once would cost the callback, which no scroll does.

**The list's own read path**, rows added 2026-09-17 by the same harness for
10-02, is a different quantity from the listing above, and the difference
is the reason the rows exist. The window never calls
`get_messages_for_folder`. When a folder opens, `load_folder_messages` in
`src/presentation/wx_app.rs` runs three steps on the interface thread, where
no key is answered until they end: `get_message_list_sorted`, with the sort
inside the query and, until 10-02, a `LIMIT` of 500 that the listing row
above never had; then the threading, `threading::thread_messages` over every
row read, which walks every row to find its relatives; then one labels query
over the rows. The rows time each step on its own, cold and warm for the
read, over a folder with a label on one row in ten, at two counts: the
tester's folder of 12,872 messages, the count the phase 10 README quotes
from his profile, and 200,000. The 200,000 labels row before the change
says `refused` rather than a time: the query sent one bound parameter per
row, and the bundled SQLite refuses more than 32,766 of them, so the window
dropped the error and showed the folder with no labels. The generated rows
carry no `References`, so every message is a conversation of one, which is
the cheapest shape the threading has. The rows at `dbcddb93` were taken
before the page came off, on the tree that still passed `Some(500)`; the
harness passes `None` itself, so those rows are the cost of the whole folder
under the old read, which is the cost the page was about to be dropped
against. The rows named "after 10-02", at `760a4d87`, were taken after it,
on the same machine, with the same command, and their labels step is the
read by folder. The command carries `--test-threads=1` since that day
because the target holds two ignored tests and, left to itself, the harness
runs them at once.

**Read the two sets against each other with care, because the machine
slowed between them and the page holds a control for it.** The before rows
were taken at 14:47Z and the after rows at 15:57Z on 2026-09-17, and every
step that did not change reads slower in the second set: the `Listing`
rows above, whose code no commit of 10-02 touched, printed by the same runs
and not written here, read 256.71 ms cold at 14:47Z and 360.23 ms cold at
15:57Z. So the before commit was checked out into a worktree and run again
at 16:00Z, three minutes after the after rows, with the same command,
`cargo test --release --test the_list_at_two_hundred_thousand_rows --
--ignored --nocapture --test-threads=1` at `dbcddb93`: at 200,000 rows the
sorted read 482.08 ms cold and 499.59 ms warm, the threading 244.84 ms and
the labels refused; at 12,872 rows 24.19 ms cold, 22.30 ms warm, the
threading 7.42 ms and the labels 6.64 ms. Those are the figures to hold the
after rows against, and they say the sorted read and the threading cost
what they cost before within the spread of three takes, and the labels went
from a refusal to 103.45 ms at 200,000 and from 6.64 ms to 5.05 ms at
12,872. What slowed the machine between 14:47Z and 15:57Z is not known; the
processor load read 2 to 5 percent, the clock its nominal 2000 MHz, and
nothing was building, checked with `tasklist` before each run. The 14:47Z
rows stay on the page as taken, because they are the rows the page was
dropped against and the day's fastest reading of the same code. What moving
the three steps off the interface thread would buy is the sum of the three
"after 10-02" rows at a count, and nothing else: 817 ms of answered keys on
the first open of a folder of 200,000 (464.94 + 248.73 + 103.45, taken
2026-09-17 at `760a4d87` by the command above), and 39 ms at the tester's
12,872 (23.89 + 10.31 + 5.05), which is under the time a key takes to be
pressed and released.

**All Inboxes in a chosen sort**, rows added 2026-09-17 by the same harness
for 10-02.1, times the combined view's read, `unified_inbox` with no
`LIMIT`, in the fixed order the index `idx_messages_date` serves and in four
of the orders the Sort Messages menu offers, three takes warm after one
untimed read, the median the value. The fixed order is the control: it is
what the view read before 10-02.1 whatever was chosen, and what a person
who never chose a sort still reads. A chosen order is a sort of every inbox
row, Date newest from the menu included, because `Received` sorts on
`COALESCE(m.internaldate, m.date)` and no index serves that; a folder
already pays the same, by the read path's sorted read above, which is the
default sort of an inbox over the same expression. The rows say what that
sort costs on top of reading every row, and at 200,000 it is little: the
fixed order 500.31 ms, Date newest 511.84 ms, Date oldest 499.98 ms, Unread
first 518.62 ms, Sender A to Z 570.93 ms, taken 2026-09-17 at `b39a7409`.
At 12,872 the five orders sit between 21.02 ms and 23.96 ms and the spread
of three takes is wider than the gap between them. Read the two sizes
against each other as a whole read that grows with the count and a sort
that adds a few percent at either size, rather than as an index that
serves one order and not the others: the index's own comment quotes 0.15
ms and 3.07 ms for the fixed order at 200,000, and those were reads with a
`LIMIT`, one screenful walked off the index, where these read the whole
of it, so the two are not the same quantity and are not reconciled here.
The machine was in the state of the "after 10-02" rows when these were
taken: the read-path rows the same run printed, not written here because
the page holds a row once per name and date, read 483.97 ms cold, 507.44
ms warm, 240.04 ms and 100.53 ms at 200,000 and 23.29, 22.49, 8.82 and
4.60 ms at 12,872, within a few percent of the rows at `760a4d87`, so no
same-state re-take was owed.

Every one of these rows is over synthetic rows and no provider mailbox, and
each says so in its conditions.

The row for marking the bound's worth of messages read is the cost of a
command over the largest selection the list allows (#30, 11-07): the
message list selects more than one message since 2026-09-19, and Mark as
Read, Star, the Labels, Delete, Move to and Copy to act on every selected
message up to `MOST_ROWS_WORTH_SELECTING`, which is 5,000 and is the bound
Select All already had. The row times the cache alone: 5,000 rows written
unread through `upsert_messages`, then one `update_message_flags` per row,
the write the arm makes for each message, on the interface thread. The
server changes queue behind it, one task per message through
`spawn_server_change`, and what those cost against a provider has not been
measured, because nothing here has met one with a thousand flag changes.
So the row bounds the part of the command that holds the window; the part
that holds the network is a queue and is not on this page.

## What the coverage rows count

The coverage rows were taken on 2026-09-14 by `cargo llvm-cov --lib
--summary-only`, the command the 2026-07-26 reading of 60.4% came from, so
the whole-library figure is the same quantity as that one and the gap between
them is real. `--lib` means the library's own tests under `#[cfg(test)]` and
nothing under `tests/`: none of the integration targets ran, including the
ones that open a window. Line coverage is what the rows give; regions,
functions and branches are in the same output and are not written here.

The area rows are not separate runs. The one run leaves its profile data
under `target/llvm-cov-target`, and `cargo llvm-cov report --json
--summary-only` reads that data back per file in about a second without
building or running anything. Each area row is the sum of the covered lines
and the sum of the lines over the files it names, from that per-file table,
with both sums in the value so the percentage can be checked by division.
The per-file table's own total agrees with the summary's `TOTAL` line to the
line, and the row for the table says so.

The areas are the ones `PERF-05` names in `.planning/REQUIREMENTS.md`,
chosen before the run and not from it: the wire protocols under
`src/service/protocols/`, the two OAuth files, and the six provider clients
for Google, Microsoft Graph, CalDAV, CardDAV and OneNote. One more row is
the low area the run found outside those three, the wxWidgets windows, which
is reported on its own so that a reader can see it is not covered by the
attribution the requirement gives.

## What the mutation rate rows measure, and what they do not

A mutation run costs two things that scale with different counts. Each
shard pays once for the copy of the tree, the build and the baseline run
before its first mutant, plus the tool's bookkeeping between mutants; each
mutant then pays a rebuild and a suite run. The `timed:` line
`scripts/mutants.sh --shard` prints after a shard keeps the two apart, and
the rows below quote them apart, so a product over the whole tree is the
rate times the mutant count plus the fixed cost times the shard count, and
not one rate under conditions nobody wrote down.

The rate rows were taken on 2026-09-15 on shard 0 of 496, which the tool's
slice sharding fills with the first 25 mutants of its list, all in
`src/presentation/accessibility.rs`. Most of the presentation layer depends
on that file, so each mutant rebuilt 58 to 75 s where the guard-record rate
row above reads 44 to 46 s for a rebuild after a one-file change in
`src/presentation/managers.rs`. Read the rate as a rate for a file at the
heavy end, and the products as what the tree would cost if every file were
that file. A shard of a leaf module rebuilds faster; nothing here measured
one.

Two of the four runs the rows come from could not be the runs the plan
asked for, and the rows say which. The every-target shape cannot run in a
scratch copy at all: the tool copies the tree without `.git`, and
`test_the_share_of_history_before_red_green_is_computed_and_printed` runs
`git merge-base`, so that baseline fails in the copy and the tool refuses
to start. It ran in place instead, which is the only way it can. And the
every-target shape is `-- --all-targets`, the way the gate runs the suite;
the tool's own default, nothing after `--`, also builds and runs every
doctest, which the gate does not.

The runner rows were taken on 2026-09-15 from two dispatches of
`.github/workflows/mutants.yml`, one runner per shard, and read from each
shard's own `timing.txt` after `gh run download`. A runner's rate is given as
a median and a mean over the shards rather than one figure, because the
shards differ by a factor of nearly three on the same machine class: a shard
whose mutants the compiler mostly rejected pays a build and no run for each,
and a shard whose mutants all reached the suite pays both. The fixed term on
a runner is a build from nothing unless the workflow's cache had been filled
by an earlier job, and the rows say which shards had it.

## What the Settings rows time, and what they do not

The Settings rows were taken by `tests/the_settings_dialog_opens_in.rs`,
the harness written for #34 on 2026-09-16, whose header defines each term;
the short form is here so the rows can be read without it.

**Built** is the span of the line the application writes, `settings built
in N ms`: from the top of the window's `ID_SETTINGS` arm, where `Ctrl+,`
lands, to the moment before `show_modal`. It holds the configuration read,
the calendar-server query and every page's controls. It is not the tester's
"until the dialog is spoken and takes keys": the show, the focus landing and
the screen reader's first announcement all come after that moment, and no
row here can see them.

**A candidate on its own** is one of the three things the build asks the
machine for, timed in a test process before any window exists:
`spellcheck::available_languages()`, `fonts::installed_families()` and
`SoundScheme::discover`. Each row gives the first run, which is what a
process pays the first time it asks, and the median of five.

**The dialog build in a test process** is `build_settings_dialog` with a
default `AppConfig`, no accounts and no calendar server, five times under
one `wxdragon::main`, each dialog destroyed before the next is built. It
includes the three candidates, so the difference between it and their sum
is what building the pages of controls costs.

**The release binary's own line** is the built line read off the log of the
release binary started against a throwaway profile with no account, with
Settings opened five times in one process through the same menu command the
accelerator sends and closed each time. The first open is on its own, the
median of five beside it. The harness sends nothing to any window it did not
start.

**The first visit of each later tab** is what showing each tab after General
costs the first time, in the test process, on one more dialog built after
the five: since 09-09 a page is built when its tab is first shown, so this
is where the cost that left the open went. The test process's frame is never
shown, and a scratch run on 2026-09-16 that showed it found every control
about twice as dear under the screen reader running in this session; the
figure understates what a person with a screen reader pays by about that
much, and the release binary's line is the figure that counts for the open.

## The figures

| What | Value | Command | Date | Commit | Conditions |
|---|---|---|---|---|---|
| Guard records in `guards/guards.toml` | 1,035 | `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(len(g))"` | 2026-09-22 | 3641084e | One more with every record added. The two census numbers at the top of the file must sum to this, and a check holds them to it on every commit. A TOML reader rather than a grep, because a grep for `[[guard]]` would also count one inside a `before` or `after` string. It read 783 on 2026-09-14 at `7b2482b1`, and the sweep rows below quote a count, so this is re-taken with them rather than left to be read against them. |
| Guard records naming exactly one file in `tests_last_seen` | 568 | `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(sum(1 for r in g if len(r.get('tests_last_seen',[]))==1))"` | 2026-09-14 | 7b2482b1 | Moves with every record added and with every record whose break starts reddening a test in a second file. |
| Guard records spelled as inline tables | 4 | `grep -c 'tests_last_seen = \[{' guards/guards.toml` | 2026-09-14 | 7b2482b1 | The awk snippet in `CLAUDE.md` skips a record in this shape, which is why the TOML reader is the method above. Moves when a record is written or rewritten in that shape. |
| Distinct files named by guard records in `tests_last_seen` | 200 | `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(len({e['file'] for r in g for e in r.get('tests_last_seen',[])}))"` | 2026-09-14 | 7b2482b1 | Moves when a record first names a file no record named, or the last record naming a file goes. |
| Guard records naming `tests/house_style.rs` | 21 | `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(sum(1 for r in g if any(e['file']=='tests/house_style.rs' for e in r.get('tests_last_seen',[]))))"` | 2026-09-14 | 7b2482b1 | A test added to that file flags this many records for re-measurement, at the per-record rate below each. Moves with every record that names the file. |
| Guard records naming `src/presentation/wx_app.rs` | 48 | `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(sum(1 for r in g if any(e['file']=='src/presentation/wx_app.rs' for e in r.get('tests_last_seen',[]))))"` | 2026-09-14 | 7b2482b1 | The same cost as the row above, for the other hot file. Moves with every record that names it. |
| Mutants the configuration allows | 12,335 | `cargo mutants --list \| wc -l` | 2026-09-14 | 7b2482b1 | `cargo-mutants 27.1.0`, reading `.cargo/mutants.toml`, which excludes the wxWidgets layer, `main.rs`, the vendored code, and every `Display` and `fmt`. The listing took 3 seconds and built nothing. Moves with every function added or removed in the files the configuration allows, and with any change to the exclusions. |
| Files those mutants are in | 247 | `cargo mutants --list-files \| wc -l` | 2026-09-14 | 7b2482b1 | The same configuration as the row above. Moves with every file added under `src/` that the exclusions do not cover. |
| Commits on `main` | 2,025 | `git rev-list --count HEAD` | 2026-09-14 | 7b2482b1 | Taken on `main` at its tip. One more per commit, and a merge counts every commit it brings. |
| Test attribute lines under `src/` and `tests/` | 7,679 | `grep -rhE '^\s*#\[(test\|tokio::test)\]\s*$' src/ tests/ \| wc -l` | 2026-09-14 | 7b2482b1 | Counts lines that are only the attribute, so a `#[test]` in a doc comment is not counted and an ignored test is. It is not the number of tests that run: the library run below is smaller because it leaves out `tests/`, and a run under `--all-targets` differs again. |
| Tests the library builds | 7,245 | `cargo test --lib -- --list \| tail -1` | 2026-09-14 | 7b2482b1 | The library alone. Nothing under `tests/` is in it, and the one ignored test is. This is the denominator of the per-record rate below. |
| Tests every target builds | 7,697 | `cargo test --all-targets -- --list 2>&1 \| grep -E '^[0-9]+ tests?,' \| awk '{s+=$1} END{print s}'` | 2026-09-14 | a42331bb | The library, the binary and every target under `tests/`, summed over the one summary line each target prints; 55 targets that day. The library row above is the unit half of this and the row below is the integration half, and the three add up. This is the figure `docs/IMPLEMENTATION_STATUS.md` states. Moves with every test added anywhere. |
| Tests the targets under `tests/` build | 452 | `cargo test --all-targets -- --list 2>&1 \| awk '/Running tests/{t=1;next} /Running/{t=0;next} t && /^[0-9]+ tests?,/{s+=$1} END{print s}'` | 2026-09-14 | a42331bb | The integration targets alone, 53 of them that day, read off the same listing by summing only the summary lines that follow a `Running tests/...` line. The binary's unit tests are neither here nor in the library row and numbered none. Moves with every test added under `tests/`. |
| Test functions in `tests/house_style.rs` | 70 | `grep -cE '^\s*#\[(test\|tokio::test)\]\s*$' tests/house_style.rs` | 2026-09-14 | 7b2482b1 | The file 21 guard records name. A test added here changes this and flags those records. |
| Test functions in `src/presentation/wx_app.rs` | 199 | `grep -cE '^\s*#\[(test\|tokio::test)\]\s*$' src/presentation/wx_app.rs` | 2026-09-14 | 7b2482b1 | The file 48 guard records name. A test added here changes this and flags those records. |
| Lines of Rust under `src/` | 360,792 over 286 files | `find src -name '*.rs' \| xargs wc -l \| tail -1` for the lines, `find src -name '*.rs' \| wc -l` for the files | 2026-09-14 | 7b2482b1 | Counts blank lines and comments as lines. Moves with every edit. |
| Entries in `.planning/WINDOWS.md` | 441 | `grep '^total_count:' .planning/WINDOWS.md` | 2026-09-14 | 7b2482b1 | Read from the ledger's own frontmatter, which the tool that appends entries keeps. One more per entry appended; closing an entry does not move it. |
| Full gate duration, `scripts/check.sh all`, from commit bodies | a band, 275 s to 654 s | `git log --format='@@@%ad %h%n%B' --date=short -400 \| awk '/^@@@/ {hdr=substr($0,4); next} /[0-9][0-9]+ seconds/ { if (match($0, /[0-9]+ seconds/)) print hdr "  " substr($0, RSTART, RLENGTH) }'` | 2026-09-14 | 7b2482b1 | A band and not a figure, and it is read with its sentences. The full-gate figures the harvest returns are 275 s at `06aa8765` on 2026-09-14 and 419 s to 654 s across `5da0702a`, `b8857bc8`, `44615216`, `ba2ff758`, `a0b909e8`, `5111ed3a` and `c6546e66` on 2026-09-12. The harvest also returns 64 s at `05ec26a4`, which that commit's body describes as a scoped run before a rule was added, and 95 s at `cccb2d4d`, which is a per-record cost, so a number pulled out of a sentence is that sentence's number only with the sentence. The band moves with whether the previous commit forced a clippy rebuild, with what else the machine was doing, and with the suite's size. From 2026-09-23 the full gate is taken once a phase, by hand in the phase's closing plan, and no longer at every merge, so a new row of this kind arrives once a phase. |
| Cost of one guard record through `scripts/guards.sh` | 92 s a record: rebuild 44 to 46 s, run 47 s | `scripts/guards.sh --remeasure "the editor stores off when the last alert is taken away"`, twice, reading the `timed:` line the runner prints after the record | 2026-09-14 | bb61e88e | `WIXEN_TEST_THREADS` at its default of 8. The library at 7,245 tests. The build warm and nothing else building, checked with `tasklist` for `cargo.exe` and `rustc.exe` before each take. The record names one file, `src/presentation/managers.rs`, and its suite is the library, which is the common case. The two takes read rebuild 46 s and run 47 s, then rebuild 44 s and run 47 s, 93 s and 91 s in all. The `--named-only` shape of the same record read rebuild 44 s and run 3 s, 47 s. The pre-read the runner does once per suite before any break is not in the rate. Taken with the timing line in the working tree before it was committed; the Rust under `src/` was the tree at the commit named. Both terms move: the rebuild with what a one-file change invalidates, the run with the test count and the thread setting. |
| The whole guard sweep, every record once | 784 x 92 s = 72,128 s, about 20 hours | the record count, `python -c "import tomllib;g=tomllib.load(open('guards/guards.toml','rb'))['guard'];print(len(g))"`, times the rate from `scripts/guards.sh --remeasure` in the row above | 2026-09-14 | bb61e88e | A product of two dated terms and not a promise. Since 2026-09-10 at `eda27192`, when the rate was 29 s to rebuild plus 66 s to run over 683 records, the rebuild has risen to 44 s, the run has fallen to 47 s, and the count has risen to 784. Both terms go on moving and the sweep's own log carries a timing line per record, so the real figure is read from the log afterwards. Triage of what the sweep finds is on top. |
| The whole guard sweep on GitHub's runners, wall clock | 4 h 7 min 32 s, 11:56:37Z to 16:04:09Z | `gh run view 34965790937 --json jobs -q '.jobs[] \| [.name,.startedAt,.completedAt] \| @tsv'`, the first shard's start to the last shard's end | 2026-09-15 | df3437a1 | Run 34965790937 of `.github/workflows/guards.yml`, dispatched with `shards=41 first=0 last=40` at `main`'s head. 41 shards of 19 or 20 records on `windows-latest`, 4 cores, 20 jobs at a time, so two waves; `WIXEN_TEST_THREADS` at the script's default of 8 on 4 cores, unmeasured. The first shard to finish took 72 min, of which 17 was its first build. The product row above predicted about 20 hours on one machine; the runners took a fifth of that in wall clock and 64.7 hours of runner time summed over the 41 jobs' own timestamps, about three times as much machine time. |
| Guard records the sweep measured, and how many agreed | 803 measured, 772 agreed, 31 not what they said, 0 contended, 0 unmeasurable | `python -c "import sys;sys.path.insert(0,'scripts');import guards;from collections import Counter;print(Counter(guards.verdicts_in(open('.planning/phases/08-every-number-the-project-quotes/sweep.log',encoding='utf-8').read()).values()))"` | 2026-09-15 | df3437a1 | The 41 shards' logs concatenated in shard order into `sweep.log`; every shard ended with its closing line, so none was killed at the six-hour cap. The 31 were measured again on this machine before any record was edited: 29 short here too, 2 right here and blind on a runner (a UTC clock, no certificate chain). Moves with every record added and every test added near a rule. |
| Guard records the sweep found short, by direction | 24 named too few; 5 named a test that stayed green; 0 whose break no longer applied; 0 that could not be measured | the command in the row above, then the `stayed green` and `does not name` lines of the 31 blocks read by hand | 2026-09-15 | df3437a1 | Of the 24, 21 have the unnamed test in a file the record had never named, which the count check cannot see, and 3 in a named file written before the 2026-09-02 recount stamped every record without measuring. Of the 5, 2 are the runner's environment and not the record, 2 named a test that stopped reaching the break, and 1 reddened nothing. Every one is dated on its record in `guards/guards.toml`. |
| Cost of one guard record on a GitHub Windows runner | 260 s a record, the median: rebuild 144 s, run 111 s; the mean 234 s | `python`, over the 803 `timed:` lines that follow a `-- ` line in `sweep.log`, the median of rebuild, of run and of their sum | 2026-09-15 | df3437a1 | `windows-latest`, 4 cores, `WIXEN_TEST_THREADS` 8, the library at 7,264 tests by `--list`; the mean is lower than the median because records naming an integration target rebuild and run in seconds. Beside this machine's 92 s in the rate row above: 2.8 times slower a record, on a machine that costs nothing and runs twenty at once. Moves with the runner image, the test count and the thread setting. |
| The second guard sweep on GitHub's runners, wall clock and runner time | 7 h 9 min 45 s of wall clock, 15:38:26Z to 22:48:11Z; 278,675 s of logged work, 77.4 h, of which rebuild 175,855 s and run 102,820 s | `gh run view 35520204784 --json jobs -q '.jobs[] \| [.name,.startedAt,.completedAt] \| @tsv'` for the wall clock, the first shard's start to the last shard's end; the `timed:` lines of the 52 shard logs summed for the work | 2026-09-22 | 0ad66e48 | Run 35520204784 of `.github/workflows/guards.yml`, dispatched with `shards=52 first=0 last=51` at `main`'s head on 2026-09-20 over the 1,029 records the file held that day. `windows-latest`, 4 cores, `WIXEN_TEST_THREADS` at the script's default of 8, unmeasured there. The two terms move for different reasons and neither is the bill: summed over the 52 jobs' own start and end timestamps the runners were busy 302,060 s, 83.9 h, which is 23,385 s more than the logs account for. One shard was killed at the 360-minute cap with nine records unmeasured, so the logged figure is the work that was recorded and not the time that was spent. |
| Guard records the second sweep measured, and how many agreed | 1,020 with a verdict, 994 agreed, 21 short, 5 could not be measured, over 1,021 `-- ` headers, so one record has no verdict at all | `python -c "import sys;sys.path.insert(0,'scripts');import guards;from collections import Counter;print(Counter(guards.verdicts_in(open('sweep.log',encoding='utf-8').read()).values()))"` over run 35520204784's 52 shard logs concatenated in shard order, downloaded with `gh run download 35520204784` | 2026-09-22 | 0ad66e48 | `short` is both directions at once: 15 naming too few and 6 naming a test that stayed green. The 5 are 3 breaks that no longer applied to their file and 2 that no longer compiled. The record with no verdict is the one that ended the shard. Read back on 2026-09-22 and every one of the 26 measured again here before any record was edited, which is where 12-02.1's summary says what each turned out to be. |
| Guard records the second sweep found wrong, by direction | 15 named too few; 6 named a test that stayed green, 3 of them reddening nothing at all; 3 whose break no longer applied; 2 whose break no longer compiled; 1 with no verdict | the command in the row above, then the `stayed green` and `does not name` lines of run 35520204784's 26 such blocks read by hand | 2026-09-22 | 0ad66e48 | Measured again here on 2026-09-22 and five of the 15 were not short at all: each was reported on a different shard with the same single unnamed test, `service::spellcheck::tests::test_the_source_named_without_a_checker_is_the_one_a_checker_built_would_report`, under five unrelated breaks, so one runner condition the pre-read did not subtract read as five short records. Of the 10 that were short, every one was short in a file the record had never named, which the count check cannot see. Of the 6 that reddened little, 2 are the runner's environment and now say so on themselves, 1 named a test that cannot see its break, and 3 reddened nothing: 1 retired, 1 re-anchored, 1 whose test was strengthened. |
| Cost of one guard record on a GitHub Windows runner, second sweep | 223 s a record, the mean; median 267 s, p95 314 s, longest 456 s, over 1,015 records with a timing line | `python`, over run 35520204784's 52 shard logs one at a time, taking the first `timed:` line after each `-- ` line and resetting at each shard | 2026-09-22 | 0ad66e48 | `windows-latest`, 4 cores, `WIXEN_TEST_THREADS` 8. The mean is below the median because records naming an integration target rebuild and run in seconds. Beside the 92 s a record on this machine in the rate row above, and beside the 260 s median of the 2026-09-15 sweep. Per shard and not over the logs concatenated: shard 40 ends with a header and no timing line, so a reading that runs on across the join gives that record the next shard's pre-read, 828 s, and a longest of 828 instead of 456. The 160 pre-read lines, 52,102 s, are not in this rate; the longest of those was 1,266 s. |
| Every target, `cargo test --all-targets`, wall time | 104 s and 103 s | `s=$(date +%s); cargo test --all-targets --no-fail-fast; e=$(date +%s); echo $((e-s))` | 2026-09-14 | 9399a1e2 | The harness's default thread count, which is what `cargo mutants` runs per mutant unless told otherwise. Every target built beforehand with `--no-run`, so this is the run and not the build. Nothing else building, checked with `tasklist` before each take. 7,687 tests passed, summed over 55 result lines. This is the figure the timeout settings in `.cargo/mutants.toml` are read against, and the figure `CLAUDE.md`'s gate paragraphs call the test suite's term. Moves with the test count, the thread count, and what else the machine is doing. |
| The library alone at eight threads, `cargo test --lib`, wall time | 52 s and 52 s | `s=$(date +%s); cargo test --lib -- --test-threads=8; e=$(date +%s); echo $((e-s))` | 2026-09-14 | 9399a1e2 | `--test-threads=8`, the same setting `scripts/guards.py` uses. The library already built. Nothing else building, checked with `tasklist` before each take. 7,244 passed and 1 ignored; the harness's own line said 51.06 s and 51.60 s. This is the shape a mutation run may be told to use by passing `--lib` after `--`, and the run term the guard runner reported as 47 s in the rate row was the same suite at the same setting. Moves with the test count and the thread count. |
| Cold start to a usable list, 1,000 cached messages | 476 ms, the median of five | `cargo test --release --test the_numbers_the_targets_ask_for -- --ignored --nocapture test_cold_start_and_memory_with_a_thousand_cached_messages`, reading the `usable:` line it prints | 2026-09-14 | 9d5f15c5 | Under the definitions above. The release binary at version 0.124.0, built from this commit after `cargo clean --release -p wixen-mail`, on an AMD Ryzen AI 9 HX PRO 370 with 24 logical processors and 92 GB, as `Get-CimInstance Win32_Processor` and `Win32_ComputerSystem` report it. The five: 721, 476, 474, 483 and 468 ms. The first start after the build, on its own: 520 ms. A sixth start after the series, taken to read the log: 472 ms. Every start was checked with `tasklist` for `cargo.exe` and `rustc.exe` first and found none but the one running the test; the test binary runs one ignored test, so no thread setting is in play. The usable line said 500 rows each time, the first page of All Inboxes. The profile's account points at `127.0.0.1` on a closed port and was never dialled: nothing checks mail on a schedule, so a start attempts no connection and the log holds no WARN or ERROR line. Moves with the machine, with what else it is doing, with the page size and with anything added to the startup path. **The condition this row was taken under ended on 2026-09-18:** a start attempts every enabled account since 10-06 (#37), and the row below is the same number under that condition. |
| Cold start to a usable list, 1,000 cached messages, a start that attempts one refusable account | 536 ms, the median of five | `cargo test --release --test the_numbers_the_targets_ask_for -- --ignored --nocapture test_cold_start_and_memory_with_a_thousand_cached_messages`, reading the `usable:` line it prints | 2026-09-18 | 7ad596ca | Under the definitions above, with the condition the harness's header describes since 2026-09-18: a start checks every enabled account and asks for a watch on each (#37), so the measurement account is attempted at once, twice, and refused at once by the credential store because the profile stores no password; the port is never dialled, ledger 526. The release binary at version `1.0.0-alpha.1`, built from this commit by `cargo build --release` without a clean, on the same machine as the row above. The five, each a fresh process: 536, 617, 550, 479 and 534 ms. The first start after the build, on its own: 501 ms. The usable line said 1000 rows each time, the whole of All Inboxes, because the page of 500 went on 2026-09-17 (10-02). The log of every run holds three WARN lines, "Could not watch the inbox of The measurement account: Authentication error: No password is saved", at the start, about forty seconds in and about a hundred seconds in, each followed by the watch's own line, "Trying the watch again in 30 seconds", "in 1 minute", and then "left to the schedule alone until TheServerAnswersAgain"; the check's refusal is spoken as "Error: Authentication error: No password is saved" once at the start, and no download line, because a check that went through nowhere asks for none. Every start was checked with `tasklist` for `cargo.exe` and `rustc.exe` first and found none but the one running the test. Sixty milliseconds above the row above, and the two were not taken on the same day or the same binary, so how much of that is the check and the watch and how much is the day is not known; the tree between the two commits also took the page off the list, which is 1000 rows loaded where 500 were. Moves with everything the row above moves with, and with what the start now does before the list is usable. |
| Memory with 1,000 cached messages: the application's peak working set to 60 s plus its WebView2 tree at 60 s | 390 MB, the median of five: the application 57 MB peak plus the tree 333 MB | `cargo test --release --test the_numbers_the_targets_ask_for -- --ignored --nocapture test_cold_start_and_memory_with_a_thousand_cached_messages`, reading the `at 60 s:` line | 2026-09-14 | 9d5f15c5 | Under the definitions above, the same runs, binary and machine as the cold-start row. The five: 397, 390, 390, 393 and 390 MB. The application process alone peaked at 57 MB in every run, with a working set of 56 to 57 MB and 18 to 19 MB private. The tree was six `msedgewebview2.exe` processes every time, 333 to 340 MB at 60 s; in the sixth run they were 122, 73, 56, 41, 22 and 17 MB. The after-build run read 396 MB. Whole megabytes of 1,048,576 bytes, read through `Get-Process` as `PeakWorkingSet64` and `WorkingSet64`. Moves with WebView2's version, with what the preview shows, and with the machine. |
| Idle memory at 120 s, 1,000 cached messages: the application's working set plus its WebView2 tree | 391 MB, the median of five: the application 56 MB plus the tree 334 MB | `cargo test --release --test the_numbers_the_targets_ask_for -- --ignored --nocapture test_cold_start_and_memory_with_a_thousand_cached_messages`, reading the `at 120 s:` line | 2026-09-14 | 9d5f15c5 | Under the definitions above, the same runs, binary and machine as the cold-start row. The five at 120 s: 398, 391, 390, 393 and 391 MB; at 60 s: 397, 389, 389, 392 and 390 MB, median 390 MB, so idle growth between the two readings is about 1 MB and all of it in the tree. The application process alone sat at 56 to 57 MB working set and 18 to 19 MB private at both readings, unchanged. No input after the usable line and the window left where the start put it. Moves with whatever runs on a timer and with WebView2. **The condition this row was taken under ended on 2026-09-18:** idle includes a start that attempts every enabled account and a watch tried again after a growing wait since 10-06 (#37), and the row below is the same number under that condition. |
| Idle memory at 120 s, 1,000 cached messages, a start that attempts one refusable account and a watch tried again twice | 373 MB, the median of five: the application 58 MB plus the tree 315 MB | `cargo test --release --test the_numbers_the_targets_ask_for -- --ignored --nocapture test_cold_start_and_memory_with_a_thousand_cached_messages`, reading the `at 120 s:` line | 2026-09-18 | 7ad596ca | Under the definitions above, the same runs, binary and machine as the cold-start row for this condition, and the same condition: by 120 s the watch has been refused three times by the credential store, waited thirty seconds and then a minute between them, and been left to the schedule alone, and the check that refused at the start has not run again, because the account's interval is five minutes. The five at 120 s: 373, 373, 373, 375 and 373 MB; at 60 s: 372, 372, 372, 374 and 372 MB, median 372 MB, so idle growth between the two readings is about 1 MB and in the tree, as before. The application process alone sat at 58 MB working set, 58 MB peak and 19 MB private at both readings in every run, 1 to 2 MB above the row above; the tree was six `msedgewebview2.exe` processes at 314 to 318 MB, 16 to 20 MB below the row above, which is WebView2's day and not this program's. Still no live connection: the watch never reaches a server here, ledger 447 and 526. Moves with everything the row above moves with, and with the wait rule's cadence. |
| The empty-profile floor: idle memory at 120 s with no account and no mail | 390 MB, the median of three: the application 54 MB plus the tree 335 MB | `cargo test --release --test the_numbers_the_targets_ask_for -- --ignored --nocapture test_the_empty_profile_floor`, reading the `at 120 s:` line | 2026-09-14 | 9d5f15c5 | The same binary and machine as the rows above, a profile holding the settings and nothing else, no usable line to wait for, the three readings taken at the same moments from the start. The three at 120 s: 389, 390 and 391 MB; the application alone 54 MB working set, 55 MB peak, 17 MB private in every run; the tree six `msedgewebview2.exe` processes at 334 to 336 MB. So 1,000 cached messages cost the application process about 3 MB over an empty profile and cost the tree nothing measurable. Moves with WebView2 and with the machine. |
| Listing 200000 rows from the cache, cold | 351.44 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The one take: 351.44 ms. One take: the first `get_messages_for_folder` on a connection opened after the rows were written, 200000 rows returned. The file was warm in the operating system's cache because this process had just written it. |
| Listing 200000 rows from the cache, warm | 370.53 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 370.18 ms, 370.53 ms, 379.10 ms. The `get_messages_for_folder` reads after the cold one on the same connection, 200000 rows returned each time. |
| Filter 200000 rows for `quarterly`, a word in one subject in five, All Folders | 78.24 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 78.24 ms, 77.85 ms, 80.13 ms. `search_messages` at the search box's limit of 500, 500 rows returned each time; the rows carry no message text, so the index holds subjects and senders only. |
| Filter 200000 rows for `quarterly`, a word in one subject in five, Subject Only | 78.91 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 80.90 ms, 78.91 ms, 77.75 ms. `search_messages` at the search box's limit of 500, 500 rows returned each time; the rows carry no message text, so the index holds subjects and senders only. |
| Filter 200000 rows for `zebra`, a word in no subject and no sender, All Folders | 0.16 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 0.53 ms, 0.16 ms, 0.11 ms. `search_messages` at the search box's limit of 500, 0 rows returned each time; the rows carry no message text, so the index holds subjects and senders only. |
| Filter 200000 rows for `zebra`, a word in no subject and no sender, Subject Only | 0.13 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 0.16 ms, 0.13 ms, 0.09 ms. `search_messages` at the search box's limit of 500, 0 rows returned each time; the rows carry no message text, so the index holds subjects and senders only. |
| Filter 200000 rows for `grace`, a sender, one in four, All Folders | 108.10 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 109.82 ms, 108.10 ms, 108.03 ms. `search_messages` at the search box's limit of 500, 500 rows returned each time; the rows carry no message text, so the index holds subjects and senders only. |
| Filter 200000 rows for `grace`, a sender, one in four, Sender Only | 109.67 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 109.35 ms, 111.34 ms, 109.67 ms. `search_messages` at the search box's limit of 500, 500 rows returned each time; the rows carry no message text, so the index holds subjects and senders only. |
| Filter 200000 rows for `quarterly`, every match, All Folders | 192.55 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 192.55 ms, 192.55 ms, 195.29 ms. `search_messages` with the limit raised to 200000, which the search box never does, 40000 rows returned each time: the cost of every match rather than the first page of them. |
| Sort 200000 rows in memory, Date (Newest First) | 126.58 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 126.58 ms, 130.29 ms, 125.24 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Sort 200000 rows in memory, Date (Oldest First) | 112.50 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 112.50 ms, 113.62 ms, 107.69 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Sort 200000 rows in memory, Sender (A-Z) | 259.50 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 257.55 ms, 273.12 ms, 259.50 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Sort 200000 rows in memory, Sender (Z-A) | 235.80 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 235.80 ms, 237.86 ms, 235.36 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Sort 200000 rows in memory, Subject (A-Z) | 224.42 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 223.46 ms, 224.42 ms, 225.88 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Sort 200000 rows in memory, Subject (Z-A) | 223.50 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 223.50 ms, 218.02 ms, 224.69 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Sort 200000 rows in memory, Unread First | 61.10 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 60.22 ms, 61.10 ms, 67.60 ms. `sort_messages` over the generated rows, a fresh clone each take, the clone outside the timing. In the running program this runs off the interface thread and the list control taking the result was not timed, because there is no window. |
| Page paint: `text_for` over 40 rows and 6 columns | 0.09 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 0.38 ms, 0.09 ms, 0.09 ms. One page of the messages view, every visible column of an inbox, 4421 characters of cell text each take. A scroll in the running program is this plus wxWidgets' own painting, which was not timed, because there is no window. |
| Full pass: `text_for` over 200000 rows, one column | 28.94 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture` | 2026-09-14 | 5cf04528 | 0.125.0 at 5cf04528, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 30.80 ms, 28.94 ms, 24.13 ms. Every row of the messages view, the subject column, 3960000 characters of cell text each take: what painting the whole list once would cost the callback, which no scroll does. |
| The list's own read path, 200000 rows: the sorted read, no limit, cold | 324.37 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The one take: 324.37 ms. One take: the first `get_message_list_sorted` on a connection opened after the rows were written, `ORDER BY COALESCE(m.internaldate, m.date) DESC`, the default sort of an inbox, and no `LIMIT`, 200000 rows returned. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 200000 rows: the sorted read, no limit, warm | 368.90 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 349.26 ms, 368.90 ms, 371.50 ms. The `get_message_list_sorted` reads after the cold one on the same connection, the same order and no `LIMIT`, 200000 rows returned each time. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 200000 rows: the threading | 208.79 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 211.06 ms, 208.79 ms, 207.01 ms. `thread_messages` over every row read, with the inputs built from the rows inside the timing as the window's `apply_threading` builds them, 200000 conversations found each take. The generated rows carry no `References`, so every message is a conversation of one, which is the cheapest shape the threading has; a folder of replies costs more. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 200000 rows: the labels | refused | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. No take: the step refused with: Error: Failed to prepare statement: variable number must be between ?1 and ?32766, followed by the statement's text, cut here. The refusal is the finding. `get_tags_for_messages` over the 200000 ids read, one bound parameter per id, as the window's `attach_labels` asks it. The window's `attach_labels` drops the error and sends the list with no labels, so a folder this size shows none. 20000 rows had been given a label. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 12872 rows: the sorted read, no limit, cold | 16.50 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The one take: 16.50 ms. One take: the first `get_message_list_sorted` on a connection opened after the rows were written, `ORDER BY COALESCE(m.internaldate, m.date) DESC`, the default sort of an inbox, and no `LIMIT`, 12872 rows returned. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 12872 rows: the sorted read, no limit, warm | 15.26 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 15.58 ms, 14.93 ms, 15.26 ms. The `get_message_list_sorted` reads after the cold one on the same connection, the same order and no `LIMIT`, 12872 rows returned each time. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 12872 rows: the threading | 5.63 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 5.63 ms, 5.65 ms, 5.17 ms. `thread_messages` over every row read, with the inputs built from the rows inside the timing as the window's `apply_threading` builds them, 12872 conversations found each take. The generated rows carry no `References`, so every message is a conversation of one, which is the cheapest shape the threading has; a folder of replies costs more. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path, 12872 rows: the labels | 4.47 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | dbcddb93 | 1.0.0-alpha.1 at dbcddb93, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 7.58 ms, 4.47 ms, 4.38 ms. `get_tags_for_messages` over the 12872 ids read, one bound parameter per id, as the window's `attach_labels` asks it; 1288 rows carry a label, one row in 10 having been given one (1288 in all). The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 200000 rows: the sorted read, no limit, cold | 464.94 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The one take: 464.94 ms. One take: the first `get_message_list_sorted` on a connection opened after the rows were written, `ORDER BY COALESCE(m.internaldate, m.date) DESC`, the default sort of an inbox, and no `LIMIT`, 200000 rows returned. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 200000 rows: the sorted read, no limit, warm | 499.87 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 499.87 ms, 500.57 ms, 491.92 ms. The `get_message_list_sorted` reads after the cold one on the same connection, the same order and no `LIMIT`, 200000 rows returned each time. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 200000 rows: the threading | 248.73 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 251.50 ms, 248.73 ms, 240.47 ms. `thread_messages` over every row read, with the inputs built from the rows inside the timing as the window's `apply_threading` builds them, 200000 conversations found each take. The generated rows carry no `References`, so every message is a conversation of one, which is the cheapest shape the threading has; a folder of replies costs more. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 200000 rows: the labels | 103.45 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 104.76 ms, 101.59 ms, 103.45 ms. `tags_by_message_in_folder` over the folder, one bound parameter however many rows it holds, as the window's `attach_labels` asks it; 20000 rows carry a label, one row in 10 having been given one (20000 in all). The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 12872 rows: the sorted read, no limit, cold | 23.89 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The one take: 23.89 ms. One take: the first `get_message_list_sorted` on a connection opened after the rows were written, `ORDER BY COALESCE(m.internaldate, m.date) DESC`, the default sort of an inbox, and no `LIMIT`, 12872 rows returned. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 12872 rows: the sorted read, no limit, warm | 24.14 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 24.43 ms, 24.14 ms, 23.70 ms. The `get_message_list_sorted` reads after the cold one on the same connection, the same order and no `LIMIT`, 12872 rows returned each time. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 12872 rows: the threading | 10.31 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 7.55 ms, 10.97 ms, 10.31 ms. `thread_messages` over every row read, with the inputs built from the rows inside the timing as the window's `apply_threading` builds them, 12872 conversations found each take. The generated rows carry no `References`, so every message is a conversation of one, which is the cheapest shape the threading has; a folder of replies costs more. The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| The list's own read path after 10-02, 12872 rows: the labels | 5.05 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | 760a4d87 | 1.0.0-alpha.1 at 760a4d87, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 5.05 ms, 4.65 ms, 5.19 ms. `tags_by_message_in_folder` over the folder, one bound parameter however many rows it holds, as the window's `attach_labels` asks it; 1288 rows carry a label, one row in 10 having been given one (1288 in all). The window runs this step on the interface thread, where it answers no keys until the step ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 200000 rows: the fixed order, newest first by the sent date | 500.31 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 497.36 ms, 506.98 ms, 500.31 ms. `unified_inbox` handed no order, so the query's own `ORDER BY m.date DESC, m.uid DESC`, the order `idx_messages_date` serves and what a person who never chose a sort reads; 200000 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 200000 rows: Date (Newest First) | 511.84 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 511.84 ms, 522.17 ms, 508.23 ms. `unified_inbox` handed the clause the window builds for Date (Newest First), `ORDER BY COALESCE(m.internaldate, m.date) DESC, m.uid DESC`, which no index serves, so every inbox row is sorted; 200000 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 200000 rows: Date (Oldest First) | 499.98 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 499.37 ms, 499.98 ms, 559.71 ms. `unified_inbox` handed the clause the window builds for Date (Oldest First), `ORDER BY COALESCE(m.internaldate, m.date) ASC, m.uid DESC`, which no index serves, so every inbox row is sorted; 200000 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 200000 rows: Sender (A-Z) | 570.93 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 579.58 ms, 562.14 ms, 570.93 ms. `unified_inbox` handed the clause the window builds for Sender (A-Z), `ORDER BY m.from_addr COLLATE NOCASE ASC, m.uid DESC`, which no index serves, so every inbox row is sorted; 200000 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 200000 rows: Unread First | 518.62 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 200000 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 534.69 ms, 518.62 ms, 511.93 ms. `unified_inbox` handed the clause the window builds for Unread First, `ORDER BY m.read ASC, COALESCE(m.internaldate, m.date) DESC, m.uid DESC`, which no index serves, so every inbox row is sorted; 200000 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 12872 rows: the fixed order, newest first by the sent date | 22.82 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 27.11 ms, 22.82 ms, 21.74 ms. `unified_inbox` handed no order, so the query's own `ORDER BY m.date DESC, m.uid DESC`, the order `idx_messages_date` serves and what a person who never chose a sort reads; 12872 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 12872 rows: Date (Newest First) | 21.02 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 21.02 ms, 21.01 ms, 21.21 ms. `unified_inbox` handed the clause the window builds for Date (Newest First), `ORDER BY COALESCE(m.internaldate, m.date) DESC, m.uid DESC`, which no index serves, so every inbox row is sorted; 12872 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 12872 rows: Date (Oldest First) | 21.34 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 21.34 ms, 20.67 ms, 22.07 ms. `unified_inbox` handed the clause the window builds for Date (Oldest First), `ORDER BY COALESCE(m.internaldate, m.date) ASC, m.uid DESC`, which no index serves, so every inbox row is sorted; 12872 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 12872 rows: Sender (A-Z) | 23.96 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 23.96 ms, 23.86 ms, 24.64 ms. `unified_inbox` handed the clause the window builds for Sender (A-Z), `ORDER BY m.from_addr COLLATE NOCASE ASC, m.uid DESC`, which no index serves, so every inbox row is sorted; 12872 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| All Inboxes in a chosen sort after 10-02.1, 12872 rows: Unread First | 22.20 ms | `cargo test --release --test the_list_at_two_hundred_thousand_rows -- --ignored --nocapture --test-threads=1` | 2026-09-17 | b39a7409 | 1.0.0-alpha.1 at b39a7409, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset and one test running. 12872 synthetic rows from `sample_mailbox` and no provider mailbox; the cache rows in a `tempfile` directory. The takes, the median being the value: 22.20 ms, 22.93 ms, 22.12 ms. `unified_inbox` handed the clause the window builds for Unread First, `ORDER BY m.read ASC, COALESCE(m.internaldate, m.date) DESC, m.uid DESC`, which no index serves, so every inbox row is sorted; 12872 rows returned each take, no `LIMIT`, three takes warm after one untimed read on the same connection. The window runs this read on the interface thread, where it answers no keys until the read ends; the harness has no window. |
| Marking 5,000 messages read in the cache, one write each, the bound of a command over a selection | 75 ms | `cargo test --release --test every_command_acts_on_the_selection -- --ignored --nocapture --test-threads=1` | 2026-09-19 | 9155c6be | 1.0.0-alpha.1 on the branch at 9155c6be with the arms written and not yet committed, release build, AMD Ryzen AI 9 HX PRO 370 w/ Radeon 890M, 24 logical processors, 92 GB, `WIXEN_TEST_THREADS` unset, nothing else building. 5,000 synthetic rows from `sample_mailbox`, written unread through `upsert_messages` into a cache in a `tempfile` directory, then one `update_message_flags(id, true, starred)` per row, the write `write_flags_or_put_the_row_back` makes for each chosen message, on one connection, and the folder read back with every row read. Two takes, 75 ms and 77 ms, the first with the release build made in the same command and the second with it already made; the value is the first. The cache alone: the server changes queue behind it, one task per message, and no provider has been asked for 5,000 flag changes. The window makes these writes on the interface thread; the harness has no window. |
| Line coverage of the library | 83.34%: 160,966 of 193,153 lines | `cargo llvm-cov --lib --summary-only` | 2026-09-14 | 55464a5e | `cargo-llvm-cov 0.8.7` on toolchain 1.98.1, version 0.125.0 at the tip of `main`, the working tree clean. Wall time 398 s: the instrumented build 5 m 29 s from nothing under `target/llvm-cov-target`, because an instrumented build shares no fingerprints with the ordinary one, then the library's tests in 63.10 s by the harness's own line, 7,263 passed, 1 ignored, none failed. `WIXEN_TEST_THREADS` unset and nothing passed after `--`, so the harness's default thread count. Nothing else building: `tasklist` found no `cargo.exe` or `rustc.exe` before the run. Before compiling, the tool ran `rustup component add llvm-tools-preview --toolchain 1.98.1-x86_64-pc-windows-msvc` itself, because the pinned toolchain did not have it; a first run on a fresh toolchain pays that download. The `TOTAL` line read 288,414 regions at 82.31%, 20,159 functions at 85.03% and 193,153 lines at 83.34% over 280 files, `src/vendor/` included and nothing excluded. The same command as the 2026-07-26 reading of 60.4%, so the two are one quantity. Moves with every line and every test added, and a second run with `target/llvm-cov-target` warm is shorter than this one. |
| The per-file coverage table the area rows below are summed from | 280 files; its total 160,966 of 193,153 lines, the same as the summary's `TOTAL` line | `cargo llvm-cov report --json --summary-only --output-path coverage.json` | 2026-09-14 | 55464a5e | Read from the profile data the run in the row above left under `target/llvm-cov-target`; built nothing, ran no test, 1 s. The JSON gives `count`, `covered` and `percent` for lines per file under `data[0].files[].summary.lines`, and the run's total under `data[0].totals.lines`. Each area row sums `covered` and `count` over its files from this table and gives the percentage as that division. Moves only when the run above is taken again. |
| Line coverage of `src/service/protocols/` | 92.06%: 4,648 of 5,049 lines over 12 files | `cargo llvm-cov report --json --summary-only --output-path coverage.json` | 2026-09-14 | 55464a5e | Summed from the per-file table in the row above. IMAP, POP3, SMTP and XOAUTH2, every file under the directory. The lowest file is `pop3.rs` at 70.44%, 317 of 450; `imap.rs` is 92.98%, 2,556 of 2,749; `smtp.rs` 92.27%, 812 of 880; the eight small files under `imap/` and `pop3/` are between 96.48% and 100%. Every request in these tests is answered by a loopback server the test starts; no live server has ever been reached. Moves with the run. |
| Line coverage of `src/service/oauth.rs` and `src/service/oauth_credentials.rs` | 84.55%: 1,062 of 1,256 lines over 2 files | `cargo llvm-cov report --json --summary-only --output-path coverage.json` | 2026-09-14 | 55464a5e | Summed from the per-file table. `oauth.rs` 82.57%, 900 of 1,090; `oauth_credentials.rs` 97.59%, 162 of 166. No token has ever been exchanged with a real provider. Moves with the run. |
| Line coverage of the provider clients: `google_api.rs`, `microsoft_graph.rs`, `caldav.rs`, `carddav.rs`, `onenote_notes.rs` and `onenote_page.rs` under `src/service/` | 96.75%: 8,847 of 9,144 lines over 6 files | `cargo llvm-cov report --json --summary-only --output-path coverage.json` | 2026-09-14 | 55464a5e | Summed from the per-file table. `caldav.rs` 98.51%, 4,357 of 4,423; `carddav.rs` 88.44%, 719 of 813; `google_api.rs` 95.42%, 979 of 1,026; `microsoft_graph.rs` 96.50%, 1,763 of 1,827; `onenote_notes.rs` 96.45%, 543 of 563; `onenote_page.rs` 98.78%, 486 of 492. `caldav_journal.rs` sits beside them, is not in the requirement's list, and read 79.67%, 243 of 305. Every one of these has only ever spoken to a loopback server a test started. Moves with the run. |
| Line coverage of everything outside `src/presentation/` | 95.53%: 128,443 of 134,454 lines over 206 files | `cargo llvm-cov report --json --summary-only --output-path coverage.json` | 2026-09-14 | 55464a5e | Summed from the per-file table over every file whose path does not start with `src/presentation/`. By directory: `src/application/` 97.20%, 64,302 of 66,156 over 102 files; `src/data/` 95.82%, 26,510 of 27,666 over 31; `src/service/` 92.94%, 33,946 of 36,523 over 57; `src/common/` 93.95%, 2,952 of 3,142 over 12; `src/vendor/` 75.80%, 733 of 967 over 4. Here so that the row below can be read against the rest of the tree. Moves with the run. |
| Line coverage of the wxWidgets windows, `src/presentation/wx_*.rs` | 26.88%: 8,642 of 32,149 lines over 27 files | `cargo llvm-cov report --json --summary-only --output-path coverage.json` | 2026-09-14 | 55464a5e | Summed from the per-file table over the 27 files whose name starts `wx_` under `src/presentation/`. This is the low area the run found, and it is outside the three areas `PERF-05` attributes to the transport, so it is reported here and not attributed. It holds 23,507 of the run's 32,187 missed lines, 73%. `wx_app.rs` alone is 29.83%, 5,369 of 18,000, and 12,631 missed lines; `wx_settings.rs` is 0 of 1,742; ten more of the 27 are at 0%, the largest `wx_add_calendar.rs` at 130 lines; `wx_managers.rs` 22.62%, 705 of 3,117; `wx_compose.rs` 17.38%, 389 of 2,238; `wx_account_manager.rs` 20.81%, 353 of 1,696. The whole of `src/presentation/` is 55.41%, 32,523 of 58,699 over 74 files. These files build windows, and under `--lib` no test opens one; the targets that do are under `tests/` and outside this run. Whether that is a reason or a number to raise is 08-09's question, not this page's. Moves with the run. |
| Mutants the configuration allows, re-taken for the shard sizing | 12,391 | `cargo mutants --list \| wc -l` | 2026-09-15 | 2847391c | Taken in the worktree `../wixen-mail-mutants` at the commit named, the tree the rate rows below ran in; `cargo-mutants 27.1.0`, the same configuration as the row of 2026-09-14, which read 12,335 at `7b2482b1`. 12,391 over 25 a shard is 495.6, so the shard count the rows below use is 496. Moves with every function added or removed in the files the configuration allows. |
| Cost of one mutant, the library at eight threads, in a scratch copy | 116 s a mutant: 2,889 s over 25 mutants | `scripts/mutants.sh --shard 0/496 --out target/mutants-rate-lib -- --lib -- --test-threads=8`, reading the second term of the `timed:` line | 2026-09-15 | 2847391c | Under the section above: shard 0 of 496, 25 mutants all in `src/presentation/accessibility.rs`, each rebuilt 58 to 75 s and tested 51 to 58 s by the record. `copy = scratch`, `baseline = run` in `conditions.txt`; the tool copied the tree to a temp directory and built it there. 24 answered by the suite, 1 the compiler rejected, 0 timed out, 0 never started. The tool auto-set the test timeout to 261 s from its 52 s baseline. `WIXEN_TEST_THREADS` at its default; `--test-threads=8` is the run's own pass-through. Run in the worktree with nothing else building, `tasklist` finding no `cargo.exe` or `rustc.exe` before the start; `cargo-mutants 27.1.0`. Moves with the file the shard mutates, the test count and the thread count. |
| Cost of one mutant, the library at eight threads, in place | 118 s a mutant: 2,958 s over 25 mutants | `scripts/mutants.sh --shard 0/496 --out target/mutants-rate-lib-in-place --in-place -- --lib -- --test-threads=8`, reading the second term of the `timed:` line | 2026-09-15 | 2847391c | The same shard, arguments and worktree as the row above, with `copy = in-place`: the tool mutated the worktree's own tree and built in its `target/`. The per-mutant term is within 3 percent of the scratch copy's, because the mutated file forces the same rebuild either way; what `--in-place` saves is in the fixed-cost row below. 24 answered, 1 rejected, 0 timed out, 0 never started; the same mutant missed. The tool auto-set the test timeout to 272 s from its 54 s baseline. `git status --porcelain` in the worktree named nothing but the logs afterwards. Moves as the row above. |
| The per-shard fixed cost: the copy, the build and the baseline before the first mutant, plus the tool's bookkeeping between mutants | 402 s in a scratch copy and 97 s in place, the library shape: 337 s build + 52 s test in the copy, 39 s build + 54 s test in place, the rest the copy and the bookkeeping | the same two runs as the rows above, reading the first term of the `timed:` line | 2026-09-15 | 2847391c | The scratch copy builds from nothing every shard, because the tool copies the tree without `target/`; in place the build is incremental over the worktree's warm `target/`. The every-target shape in a scratch copy read 439 s (353 s build + 76 s test) from `scripts/mutants.sh --shard 0/496 --out target/mutants-rate-all` before its baseline was refused, for the reason the section above gives, so that figure is the copy's cost and not a shard's. Paid once per shard, so 496 times over a whole-tree run. Moves with the shard count, the copy setting and what the build has to do from nothing. |
| Cost of one mutant, every target, in place | 130 s a mutant: 3,241 s over 25 mutants | `scripts/mutants.sh --shard 0/496 --out target/mutants-rate-all-in-place --in-place -- --all-targets`, reading the second term of the `timed:` line | 2026-09-15 | 2847391c | The same shard and worktree as the library rows, `copy = in-place`, `arguments = --all-targets`, the harness's default thread count, which is the suite as the gate runs it. In place because a scratch copy has no `.git` and the every-target baseline fails in it, as the section above says. The fixed term of this run's `timed:` line read 194 s: 71 s build + 122 s test for the baseline. Per mutant the record shows 60 to 72 s build and 61 to 65 s test for the 23 caught, against 119 s test for the one missed, because `cargo test` stops at the first target that fails and the library catches these before any target under `tests/` runs; a whole-tree product at this rate is therefore lower than one in which more mutants survive. 24 answered, 1 rejected, 0 timed out, 0 never started; the same mutant missed as under the library. The tool auto-set the test timeout to 611 s from its 122 s baseline. Moves with the file the shard mutates, the test count, and how many mutants survive the library. |
| A whole-tree mutation run, the library at eight threads, in place | 12,391 x 118 s + 496 x 97 s = 1,462,138 s + 48,112 s = 1,510,250 s, about 420 hours, about 17.5 days | the count from `cargo mutants --list \| wc -l` times the rate read from the `timed:` line of `scripts/mutants.sh --shard 0/496 --in-place -- --lib -- --test-threads=8`, plus 496 shards times that line's first term | 2026-09-15 | 2847391c | A product of dated terms and not a promise: the rate is from one file at the heavy end, the fixed cost from one shard, and the mutant count from the day. The same product with the scratch copy's terms, 12,391 x 116 s + 496 x 402 s = 1,437,356 s + 199,392 s = 1,636,748 s, about 18.9 days, is what `--in-place` saves: about a day and a half, all of it in the fixed term. Idle machine time, in shards a person starts and stops; triage of what survives is on top. Moves with both terms and the count. |
| A whole-tree mutation run, every target, in place | 12,391 x 130 s + 496 x 194 s = 1,610,830 s + 96,224 s = 1,707,054 s, about 474 hours, about 19.8 days | the count from `cargo mutants --list \| wc -l` times the rate read from the `timed:` line of `scripts/mutants.sh --shard 0/496 --in-place -- --all-targets`, plus 496 shards times that line's first term | 2026-09-15 | 2847391c | The same caveats as the row above. About 13 percent over the library shape rather than the weeks apart the phase's plan inferred from the two suite rows of 2026-09-14, because a caught mutant stops at the first failing target and most are caught by the library; the every-target shape costs its full 122 s test only on the mutants the library misses. No scratch-copy figure, because that shape cannot run in a copy. Moves with both terms, the count, and the share of mutants that survive the library. |
| Cost of one mutant on a GitHub runner, every target, in place: `src/service/protocols/**` | median 372 s a mutant, mean 353 s, across 18 shards; the shards read 152 to 432 s | `gh run download 34979540954 --dir target/runner-mutants-protocols`, then the second term of each shard's `timing.txt`, `python -c "import statistics; ..."` over the 18 figures | 2026-09-15 | 3e633252 | Dispatched from `main` at the commit named through `.github/workflows/mutants.yml` with `mode=shards`, `file=src/service/protocols/**`, `shards=18`, `first=0`, `last=17`; each shard on its own `windows-latest` runner, `cargo-mutants 27.1.0`, `--in-place -- --all-targets`, the harness's default thread count. Against 130 s a mutant on this machine under the same arguments, the row above: about 2.9 times, where the checkpoint guessed three. A shard's rate is low where the compiler rejected most of its mutants (shard 9: 17 of 25 rejected, 152 s) and high where the suite answered every one (shard 17: 432 s), because a rejected mutant pays a build and no run. Moves with the runner GitHub hands out, the file the shard mutates, and the share the compiler rejects. |
| Cost of one mutant on a GitHub runner, every target, in place: `src/service/caldav.rs` | median 405 s a mutant, mean 415 s, across 17 shards; the shards read 329 to 489 s | `gh run download 34979543954 --dir target/runner-mutants-caldav`, then the second term of each shard's `timing.txt` | 2026-09-15 | 3e633252 | The same workflow and arguments with `file=src/service/caldav.rs`, `shards=17`, `first=0`, `last=16`, dispatched two seconds after the protocols run; 420 mutants, so the last shard held 20. Higher than the protocols area because fewer of its mutants were rejected by the compiler, 25 against 91, so more of them ran the suite. Moves as the row above. |
| The per-shard fixed cost on a GitHub runner: the checkout's build from nothing and the baseline | protocols: median 1,250 s, 1,003 to 1,352 s, of which 750 to 1,070 s build and 187 to 277 s test; CalDAV: median 626 s, 450 to 1,310 s | the first term of each shard's `timing.txt` in the two downloads above | 2026-09-15 | 3e633252 | The runner starts with no `target/`, so the first term is a build from nothing plus the every-target baseline, against 194 s on this machine with a warm `target/`. Nine of the CalDAV shards read 450 to 626 s because the workflow's cache step, keyed on `Cargo.lock` and shared by every job, had been filled by an earlier job of the same day by the time they started; the other eight and every protocols shard built from nothing. Paid once per shard, so 18 and 17 times here. Moves with the cache and the runner. |
| The `src/service/protocols/**` mutation run on GitHub's runners: wall clock, runner time, and counts by outcome | 4.7 hours wall from dispatch to the last shard's end, 4.0 hours from the first shard's start; 180,690 runner-seconds, 50.2 runner-hours; 450 mutants: 316 caught, 41 nothing noticed, 91 the compiler rejected, 2 timed out, 0 never started | `gh run view 34979540954 --json jobs`, `python scripts/mutants_report.py --shards target/runner-mutants-protocols 18` | 2026-09-15 | 3e633252 | The merger accepted the run: 18 of 18 shards present, complete and at one commit. 357 of the 450 mutants got an answer from the suite; 91 the compiler rejected and 2 timed out, so 21 percent of the run asked nothing. Both timeouts were run again on this machine on 2026-09-16 at a 594 s budget and timed out again; `docs/plans/20260915-whole-tree-mutation-run.md` says what each is. The 41 survivors are on that page, each killed or given its reason. Wall clock is the longer of two runs that shared the account's runners: the CalDAV run's shards queued behind these. Moves with the runners available and the code. |
| The `src/service/caldav.rs` mutation run on GitHub's runners: wall clock, runner time, and counts by outcome | 8.1 hours wall from dispatch to the last shard's end, 6.4 hours from the first shard's start; 188,801 runner-seconds, 52.4 runner-hours; 420 mutants: 378 caught, 15 nothing noticed, 25 the compiler rejected, 2 timed out, 0 never started | `gh run view 34979543954 --json jobs`, `python scripts/mutants_report.py --shards target/runner-mutants-caldav 17` | 2026-09-15 | 3e633252 | The merger accepted the run: 17 of 17 shards present, complete and at one commit. 393 of the 420 got an answer; 25 rejected and 2 timed out, 6 percent asked nothing. The wall clock is long because its shards waited for runners the protocols run held, the first starting an hour and forty minutes after dispatch. Both timeouts re-run here and timed out again, on the same page. Moves as the row above. |
| Settings: `available_languages()` on its own | first 1 ms, median 0 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `available_languages:` line it prints | 2026-09-16 | d169df71 | Under the Settings definitions above, before 09-09's change. Version 1.0.0-alpha.1, the release build of that tree, on an AMD Ryzen AI 9 HX PRO 370 with 24 logical processors and 92 GB, as `Get-CimInstance Win32_Processor` and `Win32_ComputerSystem` report it. The five: 1, 0, 0, 0, 0 ms, in the test process before any window, the first run creating the Windows spell-checker factory; the machine offered 19 spelling languages through it. `tasklist` checked for `cargo.exe`, `rustc.exe` and `wixen-mail.exe` before the run and found none; the harness runs one ignored test, so no thread setting is in play; NVDA was running in this session and nothing stopped it. Moves with the language packs installed and with the machine. |
| Settings: `fonts::installed_families()` on its own | first 1 ms, median 1 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `installed_families:` line it prints | 2026-09-16 | d169df71 | The same run, build and machine as the row above. The five: 1, 1, 1, 1, 1 ms, walking `EnumFontFamiliesExW` in the test process before any window; the machine offered 272 typeface families after the rotated and repeated faces were dropped. Moves with the fonts installed and with the machine. |
| Settings: `SoundScheme::discover` on an empty schemes folder | first 0 ms, median 0 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `SoundScheme::discover:` line it prints | 2026-09-16 | d169df71 | The same run, build and machine as the rows above. The five: 0, 0, 0, 0, 0 ms over the throwaway profile's schemes folder, which held nothing, as a profile nobody imported a scheme into holds nothing; a profile with imported schemes reads one manifest per scheme. Moves with the schemes installed. |
| Settings: `build_settings_dialog` in a test process | first 679 ms, median 663 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `build_settings_dialog:` line it prints | 2026-09-16 | d169df71 | The same run, build and machine as the rows above, before 09-09's change. The five: 679, 643, 663, 684, 658 ms, a default `AppConfig` with no accounts and no calendar server, five dialogs built and destroyed in turn under one `wxdragon::main` whose frame is never shown. The three candidates above are inside this figure and cost about 2 ms together, so the rest is the seven pages of controls, built in full before the dialog is shown. Moves with the controls a page holds, with the machine and with what else it is doing. |
| Settings built, the release binary's own line | first 2206 ms, median 2206 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `settings built in, the release binary:` line it prints | 2026-09-16 | d169df71 | The same run, build and machine as the rows above, before 09-09's change. The five: 2206, 2384, 2182, 2149, 2332 ms, read off the log of the release binary started against a throwaway profile with no account, Settings opened through its own menu command posted to the main window and the dialog closed each time, the first open three seconds after the main window appeared. The boundary is the line's: from the top of the `ID_SETTINGS` arm to just before `show_modal`, so it holds the configuration read, the calendar-server query and every page's controls, and leaves out the show, the focus landing and the screen reader's first announcement, which the tester's "until the dialog is spoken and takes keys" includes; the whole wait he feels is longer than this. Three times the test process's figure for the same build: the one difference this run can name is that the release binary's main window is shown and NVDA, running in this session as on the tester's machine, watches it, and that is not diagnosed. Moves with everything the row above moves with, and with the screen reader running. |
| Settings: `available_languages()` on its own, after 09-09 | first 1 ms, median 0 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `available_languages:` line it prints | 2026-09-17 | 2b697408 | After 09-09, which changed nothing here: the pair of the row of 2026-09-16, same machine, same build profile, version 1.0.0-alpha.1. The five: 1, 0, 3, 0, 0 ms; 19 languages. `tasklist` checked for `cargo.exe`, `rustc.exe` and `wixen-mail.exe` before the run and found none; NVDA running in this session and not stopped. Moves as its pair. |
| Settings: `fonts::installed_families()` on its own, after 09-09 | first 1 ms, median 1 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `installed_families:` line it prints | 2026-09-17 | 2b697408 | After 09-09, which changed nothing here: the pair of the row of 2026-09-16, the same run as the row above. The five: 1, 1, 1, 1, 1 ms; 272 typeface families. Moves as its pair. |
| Settings: `SoundScheme::discover` on an empty schemes folder, after 09-09 | first 0 ms, median 0 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `SoundScheme::discover:` line it prints | 2026-09-17 | 2b697408 | After 09-09, which changed nothing here: the pair of the row of 2026-09-16, the same run as the rows above. The five: 0, 0, 0, 0, 0 ms. Moves as its pair. |
| Settings: `build_settings_dialog` in a test process, after 09-09 | first 169 ms, median 160 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `build_settings_dialog:` line it prints | 2026-09-17 | 2b697408 | After 09-09: the dialog frozen while it is built, the spelling sentence worded without building a checker, and only the General page built before the dialog is shown. The pair of the row of 2026-09-16 (663 ms), same machine and build profile, version 1.0.0-alpha.1, the same run as the rows above, with nothing compiled since the run before it. The five: 169, 160, 161, 149, 143 ms. Two other runs the same day: at this commit right after the test binary compiled, 201, 181, 179, 154 and 161 ms, median 179; and at `8162ef57`, the commit that made the change, 140, 138, 133, 135 and 134 ms, median 135. The spread between runs of the same build is about a fifth, wider than the before rows showed. Moves with the controls the General page holds, with the machine and with what else it is doing. |
| Settings: the first visit of each later tab, in a test process | Compose 32 ms, Reading 156 ms, Permissions 8 ms, Calendar & PIM 82 ms, Feedback 49 ms, Advanced 19 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `first visit of each later tab:` line it prints | 2026-09-17 | 2b697408 | Under the Settings definitions above, the same run as the rows above: one dialog built after the five and each tab after General shown once, in order, `set_selection` timed, which is the page-changed handler building the page on a frozen panel and laying it out. New with 09-09: before it every page was built in the open and a tab's first visit cost nothing. The other run the same day: 23, 132, 8, 80, 43 and 28 ms. In a test process whose frame is never shown; a person with a screen reader pays about twice per control, by the scratch run of 2026-09-16, so Reading's first visit is the one they may notice. Moves with the controls a page holds and with the screen reader running. |
| Settings built, the release binary's own line, after 09-09 | first 397 ms, median 397 ms | `cargo test --release --test the_settings_dialog_opens_in -- --ignored --nocapture`, reading the `settings built in, the release binary:` line it prints | 2026-09-17 | 2b697408 | After 09-09: the pair of the row of 2026-09-16 (2,206 ms), same boundary, same machine, same build profile, version 1.0.0-alpha.1, the same run as the rows above, NVDA running in this session as on the tester's machine. The five: 397, 375, 411, 439, 387 ms. Two other runs the same day: at this commit right after the test binary compiled, 426, 431, 405, 388 and 350 ms, median 405; at `8162ef57`, 310, 341, 384, 341 and 366 ms, median 341. What is left in the figure is the General page's controls under the screen reader's hooks, about twice the test process's build, and the configuration read; the six other pages are paid at their first visit, the row above. The boundary still leaves out the show, the focus landing and the first announcement. Moves as its pair. |
| What `X-GM-THRID` costs on the wire, per message, after 11-08.1 | 31 bytes a message in the header fetch's reply; 54 bytes a message in the once-only pass's reply; 958,662 bytes for the pass over 17,753 messages | `cargo test --lib application::server_thread_ids::tests::test_what_the_field_costs_per_message_on_the_wire -- --nocapture`, reading the `X-GM-THRID costs` line it prints | 2026-09-19 | 3e4a406c | Bytes, from the shape of the reply the scripted server writes for one message and not from a real server: the attribute `X-GM-THRID` and a nineteen-digit id and the space after it in a header fetch's reply, and one whole `* n FETCH (UID n X-GM-THRID id)` line in the pass's, at a five-digit uid. Gmail's ids are nineteen digits today and can be twenty; a request line per batch is on top of both and is shared by up to `sequence_set::MAX_SET_LENGTH` messages. The tester's count is the profile read of 2026-09-18. No real server has answered the pass; ledger 549. Moves with the id's width, the uid's width and the mailbox. |
| Hook run on a red commit, by the stage line, after 12-03.2 | median 95.5 s, range 60 s to 110 s, over 8 runs; median stages: clippy 26.5 s, the shell suites 37.5 s, the scoped tests 25 s | `git commit` through `.githooks/commit-msg`, reading the `check.sh: red passed after N s: ...` line the run ends with | 2026-09-23 | c4798dba | 12-03.2's own red commits `f3e4b8ee`, `6b880981`, `37b98bb3`, `4dd77e13`, `0e985381`, `940da640`, `a8596b88` and `de9d5130`, each read from its own hook output, warm, NVDA running for the tester on the same machine. The shell-suite stage ran between none and all four suites by what each commit staged, 3 s and 4 s when none ran and 53 s for two. These commits stage gate scripts far more than a feature plan's do, so the suite stage is high and the scoped stage low; a feature plan's reds will read the other way. The measurement of nine executors on 2026-09-23 put a red commit at 4.25 minutes. Moves with what each commit stages and with what the commit before it left to rebuild. |
| Hook run on a code commit, affected mode, by the stage line, after 12-03.2 | median 86 s, range 60 s to 120 s, over 12 runs; median stages: clippy 27 s, the shell suites 31 s, the scoped tests 25 s | `git commit` through `.githooks/commit-msg`, reading the `check.sh: affected passed after N s: ...` line the run ends with | 2026-09-23 | c4798dba | 12-03.2's own green commits and record commits from `00fb8e4e` to `c4798dba`, each read from its own hook output, warm, NVDA running. The one that ran no suite took 60 s and the one staging the hook, which runs every suite, 120 s. The same caution as the row above: gate scripts, not features. The measurement of nine executors on 2026-09-23 put a code commit at 4.2 minutes and a merge at 6.9. Moves as the row above. |
| The four shell suites by hand, one after another, after 12-03.2 | audit 5.4 s, check 31.8 s, red-commit 7.2 s, which-checks 18.5 s | `bash scripts/<suite>.test.sh` for each, timed from the shell | 2026-09-23 | c4798dba | The same run at `8eee380a`, before 12-03.2, read 4, 9, 6 and 11 s. The check suite went from 49 cases to 89 and which-checks from 52 to 68, and every new case that asks `check.sh` a question starts a `bash` and parses the script. Since that plan a commit runs a suite only when it stages one of the suite's inputs, so a commit staging only `guards/guards.toml` pays the check suite alone. NVDA running. Moves with the cases in each suite. |
| The guard runner's pre-read, read and then kept, after 12-03.2 | read: 14 s for the invocation; kept: 7 s; read again after the kept file was deleted: 11 s | three `scripts/guards.sh --remeasure "a kept pre-read is used only for the tree it was kept for"` one after another, the kept file deleted before the third | 2026-09-23 | ca2db29d | One record whose suite is `house_style`, a target whose pre-read is 5 s warm, so this shows the kept reading used and the way to discard it rather than the saving; a library pre-read is 50 to 100 s by the measurement of nine executors, and that is what a kept reading skips. Moves with the suite read. |
